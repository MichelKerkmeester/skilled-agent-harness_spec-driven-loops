# v2.3.0.0

> Part of [OpenCode Dev Environment](https://github.com/MichelKerkmeester/opencode--spec-kit-skilled-agent-orchestration)

---

## [**2.3.0.0**] - 2026-03-27

Realigned agent documentation around the multi-runtime run history model, updated all three the AGENTS guide governance templates with the full 6-command memory suite, and remediated 7 P1 findings from content alignment review. The main changes here are multi-runtime run history model and cross-runtime drift fixes. The goal is to make the workflow easier to follow and easier to trust.

---

### Multi-runtime Lineage Model

**Problem:** This part of the agent instructions needed clearer guidance and less room for confusion.

**Fix:** Clarified base, ChatGPT, Codex, Claude, and Gemini inheritance and storage paths across the agent ecosystem. This makes the agent instructions easier to follow.

---

### Cross-runtime Drift Fixes

**Problem:** This area needed a clearer and safer way to work.

**Fix:** Fixed missing /memory:shared, stale @explore references, incomplete memory command coverage, and outdated deep-review schemas across all runtime agents. This makes the behavior more predictable and lowers the chance of mistakes.

---

### 7 P1 Findings Remediated

**Problem:** This part of the documentation needed to match the related files more closely.

**Fix:** Applied fixes from content alignment review including delegation accuracy and runtime consistency improvements. This helps keep different copies of the same guidance from drifting apart.

---

### Quick-reference Table Updates

**Problem:** This part of the agent instructions needed clearer guidance and less room for confusion.

**Fix:** Updated all three the AGENTS guide templates to reflect the full 6-command memory suite: /memory:analyze, /memory:shared, richer constitutional memory subcommands, and ingest operations. This makes the agent instructions easier to follow.

---

### Deep Review Remediation

**Problem:** This part of the documentation needed to match the related files more closely.

**Fix:** Applied fixes from 3-pass deep review on agents-alignment plus memory cleanup. This helps keep different copies of the same guidance from drifting apart.

---

### Chatgpt Runtime Removal

**Problem:** The older guidance still pointed to something that was outdated or no longer used.

**Fix:** Removed ChatGPT agent runtime and model convention prose as part of agent ecosystem modernization. This reduces confusion by keeping the notes focused on supported behavior.

---

### Runtime Path Tables

**Problem:** People needed clearer support for this new part of the workflow.

**Fix:** Added Codex command-line tool and Gemini command-line tool to runtime path resolution tables across the AGENTS guide and related docs. This makes the workflow easier to discover and use.

---

## Files Changed

| File | Action | Notes |
| ---- | ------ | ----- |
| `AGENTS.md` | Modified | Quick-reference tables, memory suite, runtime paths |
| `.opencode/agent/*.md` | Modified | Content alignment across all agent definitions |
| `.claude/agents/*.md` | Modified | Cross-runtime sync |
| `.codex/agents/*.toml` | Modified | Cross-runtime sync |

---

## Upgrade

- No migration required. Agent behavior is unchanged; documentation now accurately reflects the shipped surface.
