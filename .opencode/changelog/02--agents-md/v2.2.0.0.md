Aligned Quick Reference tables across all 3 the AGENTS guide variants + the Claude guide, fixed Gate ordering to match priority rules, and replaced Barter-specific skill sections with correct per-template skills. The main changes here are g-01 and g-02. The goal is to make the workflow easier to follow and easier to trust.

---

### G-01

**Problem:** People needed clearer support for this new part of the workflow.

**Fix:** (P1). FS-Enterprises "Resume prior work" row: added /memory:continue and memory_search() alternative (was missing, present in Universal + Barter). This makes the workflow easier to discover and use.

---

### G-02

**Problem:** People needed clearer support for this new part of the workflow.

**Fix:** (P1). FS-Enterprises "Save context" row: added /memory:save command alternative (was missing, present in Universal + Barter). This makes the workflow easier to discover and use.

---

### G-03

**Problem:** The older guidance still pointed to something that was outdated or no longer used.

**Fix:** (P2). Universal "Claim completion" row: removed trailing extra pipe character. This reduces confusion by keeping the notes focused on supported behavior.

---

### G-04

**Problem:** The older guidance still pointed to something that was outdated or no longer used.

**Fix:** (P2). Barter "Claim completion" row: removed trailing extra pipe character. This reduces confusion by keeping the notes focused on supported behavior.

---

### G-05

**Problem:** People needed clearer support for this new part of the workflow.

**Fix:** (P2). FS-Enterprises "Documentation" row: added the related file reference (was missing, present in all other files). This makes the workflow easier to discover and use.

---

### G-06

**Problem:** This part of the agent instructions needed clearer guidance and less room for confusion.

**Fix:** (P2). All several files "File modification" row: reordered Gate 1 to Gate 2 to Gate 3 to Gate 3 to Gate 1 to Gate 2 to match Gate 3's "Overrides Gates 1-2" priority rule. This makes the agent instructions easier to follow.

---

### Added Web Code Row Referencing Sk-code-

**Problem:** People needed clearer support for this new part of the workflow.

**Fix:** web skill. This makes the workflow easier to discover and use.

---

### Added Opencode System Code Row Referencing Sk-code-

**Problem:** People needed clearer support for this new part of the workflow.

**Fix:** opencode skill. This makes the workflow easier to discover and use.

---

### Added Git Workflow Row Referencing Sk

**Problem:** People needed clearer support for this new part of the workflow.

**Fix:** git skill. This makes the workflow easier to discover and use.

---

### Universal the Agents Guide

**Problem:** This part of the documentation needed to match the related files more closely.

**Fix:** Replaced Barter-specific sk-code with sk-code-opencode (multi-language standards for OpenCode system code) including language table, system-spec-kit and Spec Kit Memory references. This helps keep different copies of the same guidance from drifting apart.

---

### Universal the Agents Guide

**Problem:** This part of the agent instructions needed clearer guidance and less room for confusion.

**Fix:** Replaced read-only sk-git with full git workflow orchestrator (Worktree / Commit / Finish sub-workflows). This makes the agent instructions easier to follow.

---

### Fs-enterprises the Agents Guide

**Problem:** This part of the agent instructions needed clearer guidance and less room for confusion.

**Fix:** Replaced Barter-specific sk-code with sk-code-full-stack (stack-agnostic orchestrator) including 3-phase lifecycle and stack verification table. This makes the agent instructions easier to follow.

---

### Fs-enterprises the Agents Guide

**Problem:** This part of the agent instructions needed clearer guidance and less room for confusion.

**Fix:** Replaced read-only sk-git with full git workflow orchestrator (same as Universal). This makes the agent instructions easier to follow.

---

### GPT-5

**Problem:** This part of the agent instructions needed clearer guidance and less room for confusion.

**Fix:** 4 ultra-think review (via Copilot command-line tool) validated G-01 through G-04 and surfaced G-05 and G-06. This makes the agent instructions easier to follow.

---

## Files Changed

| File | Action | Notes |
| ---- | ------ | ----- |
| `AGENTS.md` | Modified | G-03, G-06, new skill rows, sk-code-opencode + sk-git overhaul |
| `AGENTS_example_fs_enterprises.md` | Modified | G-01, G-02, G-05, G-06, sk-code-full-stack + sk-git overhaul |
| `../Barter/coder/AGENTS.md` | Modified | G-04, G-06 |
| `CLAUDE.md` | Modified | G-06 (synced via the AGENTS guide) |
| `.opencode/specs/.../018-agents-md-alignment/implementation-summary.md` | Modified | Refinement pass documentation |

---

## Upgrade

- No migration required. Documentation-only changes to governance framework tables and skill reference sections. No behavioral or code changes.
