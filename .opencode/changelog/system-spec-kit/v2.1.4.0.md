This release memory save auto-detect adding auto-detection of spec folder from Gate 3 conversation context with a 5-priority detection cascade. It explains the main changes in simple language so the release is easier to understand.

---

## What Changed

#### Auto-detect folder

This release updates Auto-detect folder. From Gate 3 conversation context. This matters because the release is clearer and easier to work with.

&nbsp;

#### 5-priority detection cascade

This release updates 5-priority detection cascade. Explicit path, Gate 3, recent spec, cwd, ask user. This matters because the release is clearer and easier to work with.

&nbsp;

#### Minimal change

This release updates Minimal change. Only 2 files modified. This matters because the release is clearer and easier to work with.

&nbsp;

#### Streamlined workflow

This release updates Streamlined workflow. Memory save workflow simplified. This matters because the release is clearer and easier to work with.

---

## Files Changed

| File | What Changed |
| ---- | ------------ |
| `Not listed in source changelog` | The original note did not name specific files. |
