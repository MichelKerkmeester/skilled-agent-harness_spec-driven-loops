This release memory command consolidation reducing from 9 commands to 5 for a simpler mental model, with intent detection improvements and cross-platform temp file handling. It explains the main changes in simple language so the release is easier to understand.

---

## What Changed

#### Memory command consolidation

This release updates Memory command consolidation. Reduced from 9 to 5 commands. This matters because the release is clearer and easier to work with.

&nbsp;

#### /memory:learn

This release updates /memory:learn. Now includes correct subcommand for corrections. This matters because the release is clearer and easier to work with.

&nbsp;

#### /memory:manage

This release updates /memory:manage. New unified command for database, checkpoint, and maintenance operations. This matters because the release is clearer and easier to work with.

&nbsp;

#### Intent detection

This release updates Intent detection. Updated to phrase-based matching (avoids false positives). This matters because the release is clearer and easier to work with.

&nbsp;

#### Rollback mechanism

There was a reliability problem around Rollback mechanism. Added for checkpoint restore operations. This matters because it reduces confusing failures and makes the system more dependable.

&nbsp;

#### Checkpoint validation

The team needed stronger proof that Checkpoint validation worked as expected. Added before cleanup operations. This matters because the team has clearer proof that the change works.

&nbsp;

#### Cross-platform temp file handling

This release updates Cross-platform temp file handling. Added. This matters because the release is clearer and easier to work with.

&nbsp;

#### /memory:search

/memory:search was no longer needed in its old form. Replaced by /memory:context [deprecated]. This matters because the project becomes smaller and easier to maintain.

&nbsp;

#### /memory:database

/memory:database was no longer needed in its old form. Consolidated into /memory:manage [deprecated]. This matters because the project becomes smaller and easier to maintain.

&nbsp;

#### /memory:checkpoint

/memory:checkpoint was no longer needed in its old form. Consolidated into /memory:manage [deprecated]. This matters because the project becomes smaller and easier to maintain.

&nbsp;

#### /memory:why

/memory:why was no longer needed in its old form. Removed (causal tracing available via MCP tools) [deprecated]. This matters because the project becomes smaller and easier to maintain.

&nbsp;

#### /memory:correct

/memory:correct was no longer needed in its old form. Consolidated into /memory:learn correct [deprecated]. This matters because the project becomes smaller and easier to maintain.

&nbsp;

#### MCP tool references

There was a reliability problem around MCP tool references. Fixed (memory_drift_context to memory_context). This matters because it reduces confusing failures and makes the system more dependable.

&nbsp;

#### Non-existent tool reference

There was a reliability problem around Non-existent tool reference. Removed references to non-existent memory_drift_learn tool. This matters because it reduces confusing failures and makes the system more dependable.

&nbsp;

#### AutoImportance type inconsistency

There was a reliability problem around AutoImportance type inconsistency. Fixed in learn. This matters because it reduces confusing failures and makes the system more dependable.

&nbsp;

#### Example numbering gap

There was a reliability problem around Example numbering gap. Fixed in learn. This matters because it reduces confusing failures and makes the system more dependable.

&nbsp;

#### Date typo

There was a reliability problem around Date typo. Fixed (2026 to 2025) in context. This matters because it reduces confusing failures and makes the system more dependable.

---

## Files Changed

| File | What Changed |
| ---- | ------------ |
| `/memory:context` | Referenced in the original changelog. |
| `/memory:manage` | Referenced in the original changelog. |
| `/memory:learn correct` | Referenced in the original changelog. |
| `learn.md` | Referenced in the original changelog. |
| `context.md` | Referenced in the original changelog. |
