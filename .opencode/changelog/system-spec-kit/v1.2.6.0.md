This release ecosystem-wide deep analysis and remediation with 3 critical bugs fixed, schema unification via Migration v12, gate numbering standardization, and AGENTS naming migration across 59 files. It focuses on making the instructions and release notes easier for non-developers to follow. The goal is to make the workflow easier to follow and easier to trust.

---

## What Changed

#### CREATE_LINKED database constraint

There was a reliability problem around CREATE_LINKED database constraint. Added to memory_conflicts CHECK constraint in migration v4 schema and create_schema(). This matters because it reduces confusing failures and makes the system more dependable.

&nbsp;

#### Ghost tools in speckit

There was a reliability problem around Ghost tools in speckit. Rewrote tool layers from wrong 5-layer to correct 7-layer, 22-tool architecture; removed memory_drift_context and memory_drift_learn references. This matters because it reduces confusing failures and makes the system more dependable.

&nbsp;

#### Stale SKILL cross-references

People needed clearer guidance about Stale SKILL cross-references. Fixed 5 section references + standardized template file counts. This matters because the release notes and instructions are easier to read and trust.

&nbsp;

#### Three conflicting database schema schemas unified

There was a reliability problem around Three conflicting database schema schemas unified. Memory_conflicts had incompatible column names across vector index, create_schema(), and prediction error gate; unified to 14-column schema. This matters because it reduces confusing failures and makes the system more dependable.

&nbsp;

#### Migration v12

The project needed a cleaner setup for Migration v12. DROP+CREATE with unified schema; schema version 11 to 12. This matters because setup and maintenance are easier to manage.

&nbsp;

#### Column renames

The project needed a cleaner setup for Column renames. Similarity_score to similarity, notes to reason in both INSERT paths. This matters because setup and maintenance are easier to manage.

&nbsp;

#### Error swallowing removed

There was a reliability problem around Error swallowing removed. Silent try/catch replaced with console.error() logging. This matters because it reduces confusing failures and makes the system more dependable.

&nbsp;

#### Ensure_conflicts_table() deprecated

There was a reliability problem around Ensure_conflicts_table() deprecated. Converted to no-op in prediction error gate. This matters because it reduces confusing failures and makes the system more dependable.

&nbsp;

#### Gate enforcement

This release updates Gate enforcement. Full renumbering to match AGENTS (Gate 1=Understanding, 2=Skill Routing, 3=Spec Folder). This matters because the release is clearer and easier to work with.

&nbsp;

#### Old gate references removed

Older gate numbering was still showing up in active files after the workflow had changed. Removed stale Gate 4/5/6 references. This matters because the project becomes smaller and easier to maintain.

&nbsp;

#### Legacy install guide

People needed clearer guidance about Legacy install guide. Rewritten from 7-gate to 3-gate + 3 behavioral rules. This matters because the release notes and instructions are easier to read and trust.

&nbsp;

#### Context server

This release updates Context server. Added toolCache.shutdown() to shutdown signal/termination signal/uncaughtException. This matters because the release is clearer and easier to work with.

&nbsp;

#### Access tracker

Access tracker was no longer needed in its old form. Removed duplicate shutdown signal/termination signal handlers. This matters because the project becomes smaller and easier to maintain.

&nbsp;

#### Schema migration.test

People needed clearer guidance about Schema migration.test. Column names updated for v12 schema. This matters because the release notes and instructions are easier to read and trust.

&nbsp;

#### Decision format, epistemic vectors

People needed clearer guidance about Decision format, epistemic vectors. Gate number corrections. This matters because the release notes and instructions are easier to read and trust.

&nbsp;

#### README schema references

People needed clearer guidance about the README files that still mentioned the older schema version. Schema version references updated. This matters because the release notes and instructions are easier to read and trust.

&nbsp;

#### AGENTS naming

This release updates AGENTS naming. 59 files updated (~218 replacements) to use AGENTS instead of CLAUDE. This matters because the release is clearer and easier to work with.

&nbsp;

#### Speckit

This release updates Speckit. Added 4 missing commands + 3 scripts to Capability Scan. This matters because the release is clearer and easier to work with.

&nbsp;

#### SKILL

This release updates SKILL. Added 3 scripts to Key Scripts table. This matters because the release is clearer and easier to work with.

&nbsp;

#### Skill advisor

There was a reliability problem around Skill advisor. Memory/context intent boost (passes 0.8 threshold); debug disambiguation. This matters because it reduces confusing failures and makes the system more dependable.

&nbsp;

#### Template counts

People needed clearer guidance about Template counts. Standardized across speckit, SKILL, README. This matters because the release notes and instructions are easier to read and trust.

---

## Files Changed

| File | What Changed |
| ---- | ------------ |
| `AGENTS.md` | Referenced in the original changelog. |
| `vector-index.js` | Referenced in the original changelog. |
| `speckit.md` | Referenced in the original changelog. |
| `SKILL.md` | Referenced in the original changelog. |
| `prediction-error-gate.js` | Referenced in the original changelog. |
| `gate-enforcement.md` | Referenced in the original changelog. |
| `context-server.js` | Referenced in the original changelog. |
| `access-tracker.js` | Referenced in the original changelog. |
| `schema-migration.test.js` | Referenced in the original changelog. |
| `decision-format.md` | Referenced in the original changelog. |
| `epistemic-vectors.md` | Referenced in the original changelog. |
| `search/README.md` | Referenced in the original changelog. |
| `core/README.md` | Referenced in the original changelog. |
| `CLAUDE.md` | Referenced in the original changelog. |
| `skill_advisor.py` | Referenced in the original changelog. |
| `README.md` | Referenced in the original changelog. |
