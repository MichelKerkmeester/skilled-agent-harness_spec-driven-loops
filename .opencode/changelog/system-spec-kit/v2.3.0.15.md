This release comprehensive MCP Server Remediation (Sprint 8) - Fixed 15 critical bugs, removed ~360 lines of dead code, applied 13 performance improvements and 4 test quality fixes across 35+ production files and 18+ test files. 3-wave parallel agent delegation with full verification: 226 test files, 7003/7003 tests passing. It focuses on fixing important problems and explaining the results in clearer terms. The goal is to make the workflow easier to follow and easier to trust.

---

## What Changed

#### B1

There was a reliability problem around B1. B1: Fixed reconsolidation referencing non-existent frequency_counter column - replaced with importance_weight merge logic. This matters because it reduces confusing failures and makes the system more dependable.

&nbsp;

#### B2

There was a reliability problem around B2. B2: Moved database schema statements before BEGIN in checkpoints - SQLite silently auto-commits on database schema, corrupting transaction boundaries during restore. This matters because it reduces confusing failures and makes the system more dependable.

&nbsp;

#### B3

There was a reliability problem around B3. B3: Fixed database operator precedence in causal edges - WHERE a AND b OR c to WHERE a AND (b OR c). This matters because it reduces confusing failures and makes the system more dependable.

&nbsp;

#### B4

The project needed a cleaner setup for B4. B4: Added .changes > 0 guard on UPDATE statements in memory save - previously reported success on zero-row updates. This matters because setup and maintenance are easier to manage.

&nbsp;

#### C1

This release updates C1. C1: Clamped composite score to [0,1] with Math.max(0, Math.min(1, composite)) in composite scoring. This matters because the release is clearer and easier to work with.

&nbsp;

#### C2

There was a reliability problem around C2. C2: Removed citation fallback chain (was conflating recency with citation authority); returns 0 when no citation data. This matters because it reduces confusing failures and makes the system more dependable.

&nbsp;

#### C3

This release updates C3. C3: Changed UNION ALL to UNION in recursive CTE in causal boost - prevents cycle amplification. This matters because the release is clearer and easier to work with.

&nbsp;

#### C4

This release updates C4. C4: Replaced naive binomial coefficient with log-space computation in ablation framework - prevents overflow for n>50. This matters because the release is clearer and easier to work with.

&nbsp;

#### D1

This release updates D1. D1: Summary candidates in Stage 1 now pass through minQualityScore filter in stage1 candidate gen. This matters because the release is clearer and easier to work with.

&nbsp;

#### D2

This release updates D2. D2: Unified FTS5 (built-in text search) tokenization via shared sanitizeQueryTokens() array across bm25 index and sqlite fts. This matters because the release is clearer and easier to work with.

&nbsp;

#### D3

This release updates D3. D3: Lowered QUALITY_FLOOR from 0.2 to 0.005 in channel representation - RRF scores (~0.01-0.03) were being filtered out. This matters because the release is clearer and easier to work with.

&nbsp;

#### E1

There was a reliability problem around E1. E1: Fixed inner loop j = i + 1 in temporal contiguity - eliminated double-counting of pair boosts. This matters because it reduces confusing failures and makes the system more dependable.

&nbsp;

#### E2

There was a reliability problem around E2. E2: Removed wrong-memory fallback in extraction adapter - returns null on entity resolution failure. This matters because it reduces confusing failures and makes the system more dependable.

&nbsp;

#### A1

This release updates A1. A1: Consolidated divergent normalizeEntityName functions - single Unicode-aware version in entity linker, imported by entity extractor. This matters because the release is clearer and easier to work with.

&nbsp;

#### A2

This release updates A2. A2: Consolidated duplicate computeEdgeDensity - single version in entity linker, imported by entity extractor. This matters because the release is clearer and easier to work with.

&nbsp;

#### Removed dead RSF branch and shadow-scoring

Removed dead RSF branch and shadow-scoring was no longer needed in its old form. Removed dead RSF branch and shadow-scoring branch from hybrid search (~80 lines). This matters because the project becomes smaller and easier to maintain.

&nbsp;

#### Removed dead feature flag functions

Removed dead feature flag functions was no longer needed in its old form. Removed dead feature flag functions: isShadowScoringEnabled(), isRsfEnabled(), isInShadowPeriod(). This matters because the project becomes smaller and easier to maintain.

&nbsp;

#### Removed dead module-level state

Removed dead module-level state was no longer needed in its old form. Removed dead module-level state: stmtCache, lastComputedAt, activeProvider, flushCount, 3 dead config fields. This matters because the project becomes smaller and easier to maintain.

&nbsp;

#### Removed dead functions/exports

Removed dead functions/exports was no longer needed in its old form. Removed dead functions/exports: computeCausalDepth single-node, getSubgraphWeights, RECOVERY_HALF_LIFE_DAYS, logCoActivationEvent. This matters because the project becomes smaller and easier to maintain.

&nbsp;

#### Preserved computeStructuralFreshness/computeGraphCentrality in fsrs (planned features

The team needed stronger proof that Preserved computeStructuralFreshness/computeGraphCentrality in fsrs (planned features worked as expected. Preserved computeStructuralFreshness/computeGraphCentrality in fsrs (planned features, not dead). This matters because the team has clearer proof that the change works.

&nbsp;

#### P1a

There was a reliability problem around P1a. P1a: Math.max(...spread) to reduce-based max in tfidf summarizer (RangeError prevention). This matters because it reduces confusing failures and makes the system more dependable.

&nbsp;

#### P1b

This release updates P1b. P1b: Added LIMIT clause to unbounded query in memory summaries. This matters because the release is clearer and easier to work with.

&nbsp;

#### P1c

This release updates P1c. P1c: database COUNT(*) with json_extract in mutation ledger (replaced O(n) JS scan). This matters because the release is clearer and easier to work with.

&nbsp;

#### P3a

This release updates P3a. P3a: Set for O(1) lookups in entity linker (was O(n) .includes()). This matters because the release is clearer and easier to work with.

&nbsp;

#### P3b

This release updates P3b. P3b: Batch edge count query in entity linker (was N+1). This matters because the release is clearer and easier to work with.

&nbsp;

#### P4a

This release updates P4a. P4a: Eliminated post-upsert SELECT via lastInsertRowid in causal edges (3to2 round-trips). This matters because the release is clearer and easier to work with.

&nbsp;

#### P4b

The project needed a cleaner setup for P4b. P4b: WeakMap TTL cache (60s) for buildHierarchyTree() in spec folder hierarchy. This matters because setup and maintenance are easier to manage.

&nbsp;

#### Increased timeout in memory save extended.vitest (5000→15000ms)

The team needed stronger proof that Increased timeout in memory save extended.vitest (5000to15000ms) worked as expected. The release updates increased timeout in memory save extended.vitest (5000to15000ms). This matters because the team has clearer proof that the change works.

&nbsp;

#### Added db.close() in entity linker.vitest afterEach

The team needed stronger proof that Added db.close() in entity linker.vitest afterEach worked as expected. The release updates added db.close() in entity linker.vitest aftereach. This matters because the team has clearer proof that the change works.

&nbsp;

#### Rewrote 4 tautological tests in integration

The team needed stronger proof that Rewrote 4 tautological tests in integration worked as expected. Rewrote 4 tautological tests in integration search pipeline.vitest. This matters because the team has clearer proof that the change works.

&nbsp;

#### Added /z_archive/ exclusion to isMemoryFile() in

The team needed stronger proof that Added /z_archive/ exclusion to isMemoryFile() in worked as expected. Added /z_archive/ exclusion to isMemoryFile() in memory parser. This matters because the team has clearer proof that the change works.

&nbsp;

#### TypeScript compilation

There was a reliability problem around TypeScript compilation. TypeScript compilation: npx tsc --noEmit - 0 errors. This matters because it reduces confusing failures and makes the system more dependable.

&nbsp;

#### Full test suite

The team needed stronger proof that Full test suite worked as expected. Full test suite: 226 files, 7003/7003 tests, 0 failures. This matters because the team has clearer proof that the change works.

&nbsp;

#### Dead code grep

Dead code grep was no longer needed in its old form. Dead code grep: 0 hits for all removed functions. This matters because the project becomes smaller and easier to maintain.

&nbsp;

#### Spot-check

There was a reliability problem around Spot-check. Spot-check: all 8 critical fix verifications pass. This matters because it reduces confusing failures and makes the system more dependable.

---

## Files Changed

| File | What Changed |
| ---- | ------------ |
| `.opencode/specs/system-spec-kit/022-hybrid-rag-fusion-refinement/010-comprehensive-remediation/` | Spec folder referenced by this release. |
| `.opencode/skill/system-spec-kit/mcp_server/handlers/memory-save.ts` | Updated in this release. |
| `.opencode/skill/system-spec-kit/mcp_server/handlers/memory-index-discovery.ts` | Updated in this release. |
| `.opencode/skill/system-spec-kit/mcp_server/lib/cognitive/archival-manager.ts` | Updated in this release. |
| `.opencode/skill/system-spec-kit/mcp_server/lib/cognitive/co-activation.ts` | Updated in this release. |
| `.opencode/skill/system-spec-kit/mcp_server/lib/cognitive/temporal-contiguity.ts` | Updated in this release. |
| `.opencode/skill/system-spec-kit/mcp_server/lib/cognitive/working-memory.ts` | Updated in this release. |
| `.opencode/skill/system-spec-kit/mcp_server/lib/eval/ablation-framework.ts` | Updated in this release. |
| `.opencode/skill/system-spec-kit/mcp_server/lib/eval/shadow-scoring.ts` | Updated in this release. |
| `.opencode/skill/system-spec-kit/mcp_server/lib/extraction/entity-extractor.ts` | Updated in this release. |
| `.opencode/skill/system-spec-kit/mcp_server/lib/extraction/extraction-adapter.ts` | Updated in this release. |
| `.opencode/skill/system-spec-kit/mcp_server/lib/graph/community-detection.ts` | Updated in this release. |
| `.opencode/skill/system-spec-kit/mcp_server/lib/graph/graph-signals.ts` | Updated in this release. |
| `.opencode/skill/system-spec-kit/mcp_server/lib/parsing/memory-parser.ts` | Updated in this release. |
| `.opencode/skill/system-spec-kit/mcp_server/lib/scoring/composite-scoring.ts` | Updated in this release. |
| `.opencode/skill/system-spec-kit/mcp_server/lib/scoring/negative-feedback.ts` | Updated in this release. |
| `.opencode/skill/system-spec-kit/mcp_server/lib/search/bm25-index.ts` | Updated in this release. |
| `.opencode/skill/system-spec-kit/mcp_server/lib/search/causal-boost.ts` | Updated in this release. |
| `.opencode/skill/system-spec-kit/mcp_server/lib/search/channel-enforcement.ts` | Updated in this release. |
| `.opencode/skill/system-spec-kit/mcp_server/lib/search/channel-representation.ts` | Updated in this release. |
| `.opencode/skill/system-spec-kit/mcp_server/lib/search/cross-encoder.ts` | Updated in this release. |
| `.opencode/skill/system-spec-kit/mcp_server/lib/search/entity-linker.ts` | Updated in this release. |
| `.opencode/skill/system-spec-kit/mcp_server/lib/search/graph-search-fn.ts` | Updated in this release. |
| `.opencode/skill/system-spec-kit/mcp_server/lib/search/hybrid-search.ts` | Updated in this release. |
| `.opencode/skill/system-spec-kit/mcp_server/lib/search/learned-feedback.ts` | Updated in this release. |
| `.opencode/skill/system-spec-kit/mcp_server/lib/search/memory-summaries.ts` | Updated in this release. |
| `.opencode/skill/system-spec-kit/mcp_server/lib/search/pipeline/stage1-candidate-gen.ts` | Updated in this release. |
| `.opencode/skill/system-spec-kit/mcp_server/lib/search/rsf-fusion.ts` | Updated in this release. |
| `.opencode/skill/system-spec-kit/mcp_server/lib/search/search-flags.ts` | Updated in this release. |
| `.opencode/skill/system-spec-kit/mcp_server/lib/search/spec-folder-hierarchy.ts` | Updated in this release. |
| `.opencode/skill/system-spec-kit/mcp_server/lib/search/sqlite-fts.ts` | Updated in this release. |
| `.opencode/skill/system-spec-kit/mcp_server/lib/search/tfidf-summarizer.ts` | Updated in this release. |
| `.opencode/skill/system-spec-kit/mcp_server/lib/storage/access-tracker.ts` | Updated in this release. |
| `.opencode/skill/system-spec-kit/mcp_server/lib/storage/causal-edges.ts` | Updated in this release. |
| `.opencode/skill/system-spec-kit/mcp_server/lib/storage/checkpoints.ts` | Updated in this release. |
| `.opencode/skill/system-spec-kit/mcp_server/lib/storage/mutation-ledger.ts` | Updated in this release. |
| `.opencode/skill/system-spec-kit/mcp_server/lib/storage/reconsolidation.ts` | Updated in this release. |
| `.opencode/skill/system-spec-kit/mcp_server/tests/channel-enforcement.vitest.ts` | Updated in this release. |
| `.opencode/skill/system-spec-kit/mcp_server/tests/channel-representation.vitest.ts` | Updated in this release. |
| `.opencode/skill/system-spec-kit/mcp_server/tests/co-activation.vitest.ts` | Updated in this release. |
| `.opencode/skill/system-spec-kit/mcp_server/tests/decay-delete-race.vitest.ts` | Updated in this release. |
| `.opencode/skill/system-spec-kit/mcp_server/tests/entity-extractor.vitest.ts` | Updated in this release. |
| `.opencode/skill/system-spec-kit/mcp_server/tests/entity-linker.vitest.ts` | Updated in this release. |
| `.opencode/skill/system-spec-kit/mcp_server/tests/extraction-adapter.vitest.ts` | Updated in this release. |
| `.opencode/skill/system-spec-kit/mcp_server/tests/five-factor-scoring.vitest.ts` | Updated in this release. |
| `.opencode/skill/system-spec-kit/mcp_server/tests/full-spec-doc-indexing.vitest.ts` | Updated in this release. |
| `.opencode/skill/system-spec-kit/mcp_server/tests/graph-search-fn.vitest.ts` | Updated in this release. |
| `.opencode/skill/system-spec-kit/mcp_server/tests/graph-signals.vitest.ts` | Updated in this release. |
| `.opencode/skill/system-spec-kit/mcp_server/tests/integration-search-pipeline.vitest.ts` | Updated in this release. |
| `.opencode/skill/system-spec-kit/mcp_server/tests/intent-routing.vitest.ts` | Updated in this release. |
| `.opencode/skill/system-spec-kit/mcp_server/tests/learned-feedback.vitest.ts` | Updated in this release. |
| `.opencode/skill/system-spec-kit/mcp_server/tests/memory-save-extended.vitest.ts` | Updated in this release. |
| `.opencode/skill/system-spec-kit/mcp_server/tests/reconsolidation.vitest.ts` | Updated in this release. |
| `.opencode/skill/system-spec-kit/mcp_server/tests/session-cleanup.vitest.ts` | Updated in this release. |
| `.opencode/skill/system-spec-kit/mcp_server/tests/shadow-scoring.vitest.ts` | Updated in this release. |
| `.opencode/skill/system-spec-kit/mcp_server/tests/working-memory.vitest.ts` | Updated in this release. |
| `.opencode/skill/system-spec-kit/mcp_server/tests/README.md` | Updated in this release. |
| `specs/system-spec-kit/022-hybrid-rag-fusion-refinement/summary_of_new_features.md` | Updated in this release. |

---

## Upgrade

- No schema migration required. All changes are backward-compatible source-level fixes. Dead code removal reduces codebase surface area. Performance improvements are additive (caching, Set conversion, database optimization). Test count decreased from 7,027 to 7,003 (24 tests for removed dead-code features were deleted).
