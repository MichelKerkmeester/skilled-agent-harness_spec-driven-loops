This release memory index deep audit and fix correcting the critical FSRS formula , adding token budget observability, and removing 214 lines of dead code from vector index. It focuses on making the instructions and release notes easier for non-developers to follow. The goal is to make the workflow easier to follow and easier to trust.

---

## What Changed

#### Token budget observability

This release updates Token budget observability. TokenBudget field in response metadata for all handler responses. This matters because the release is clearer and easier to work with.

&nbsp;

#### Console.error logging

There was a reliability problem around Console.error logging. When token budgets exceeded. This matters because it reduces confusing failures and makes the system more dependable.

&nbsp;

#### Hint injection

This release updates Hint injection. In responses when budget thresholds are hit. This matters because the release is clearer and easier to work with.

&nbsp;

#### Test suite

The team needed stronger proof that Test suite worked as expected. 46/60 to 58/58 (100% pass rate); 2 orphan test files removed. This matters because the team has clearer proof that the change works.

&nbsp;

#### Vector index

Vector index was no longer needed in its old form. 705 to 491 lines (214 lines dead code removed). This matters because the project becomes smaller and easier to maintain.

&nbsp;

#### 12+ READMEs

People needed clearer guidance about 12+ READMEs. Rewritten across MCP server subdirectories. This matters because the release notes and instructions are easier to read and trust.

&nbsp;

#### Search weights

This release updates Search weights. Cleaned dead parameters and regex patterns. This matters because the release is clearer and easier to work with.

&nbsp;

#### CRITICAL: FSRS formula corrected

There was a reliability problem around CRITICAL: FSRS formula corrected. Factor changed from 19.0 to 19/81 (~0.2346), formula from R = )^(-0.5) to R = ^(-0.5); memories were decaying ~4.5x slower than FSRS v4 standard; now R(S,S) = 0.9 as specified. This matters because it reduces confusing failures and makes the system more dependable.

&nbsp;

#### ClearConstitutionalCache bug

There was a reliability problem around ClearConstitutionalCache bug. In vector index; was calling wrong internal method. This matters because it reduces confusing failures and makes the system more dependable.

&nbsp;

#### 3 broken delegations

There was a reliability problem around 3 broken delegations. In vector-index facade. This matters because it reduces confusing failures and makes the system more dependable.

&nbsp;

#### Integration test paths

There was a reliability problem around Integration test paths. Bug-fix tests, preflight tests. This matters because it reduces confusing failures and makes the system more dependable.

&nbsp;

#### SafeParseJson security alignment

There was a reliability problem around SafeParseJson security alignment. Across modules. This matters because it reduces confusing failures and makes the system more dependable.

&nbsp;

#### 5 test failures

There was a reliability problem around 5 test failures. Confidence-tracker, handler-memory-search, memory-context, tier-classifier, tool-cache. This matters because it reduces confusing failures and makes the system more dependable.

&nbsp;

#### Orphan core

Orphan core was no longer needed in its old form. Was shadowing dist/core/ directory. This matters because the project becomes smaller and easier to maintain.

&nbsp;

#### 9 orphaned files

The team needed stronger proof that 9 orphaned files worked as expected. 7 dist .js + 2 test files. This matters because the team has clearer proof that the change works.

&nbsp;

#### 3 fabricated sections

3 fabricated sections was no longer needed in its old form. In memory system. This matters because the project becomes smaller and easier to maintain.

---

## Files Changed

| File | What Changed |
| ---- | ------------ |
| `19/81` | Referenced in the original changelog. |
| `vector-index.ts` | Referenced in the original changelog. |
| `search-weights.json` | Referenced in the original changelog. |
| `R = (1 + t/(FACTOR*S))^(-0.5)` | Referenced in the original changelog. |
| `R = (1 + FACTOR*t/S)^(-0.5)` | Referenced in the original changelog. |
| `dist/core.js` | Referenced in the original changelog. |
| `dist/core/` | Referenced in the original changelog. |
| `.js` | Referenced in the original changelog. |
| `references/memory/memory_system.md` | Referenced in the original changelog. |
