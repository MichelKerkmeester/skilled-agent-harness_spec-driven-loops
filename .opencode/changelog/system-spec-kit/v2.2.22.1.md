This release memory save path-resolution hotfix for .opencode/specs inputs -- Fixed generate context folder detection so JSON SPEC_FOLDER values that include .opencode/specs/... resolve directly instead of falling through to nested auto-detection. It explains the main changes in simple language so the release is easier to understand.

---

## What Changed

#### JSON -folder normalization added

This release updates JSON -folder normalization added. JSON spec-folder normalization added: detectSpecFolder() now correctly handles absolute paths, specs/..., and .opencode/specs/... in collected data. This matters because the release is clearer and easier to work with.

&nbsp;

#### Alignment base-path corrected

This release updates Alignment base-path corrected. Alignment base-path corrected: Folder-alignment checks now run against the resolved folder basename and parent directory, preventing malformed join results. This matters because the release is clearer and easier to work with.

&nbsp;

#### Symlink duplicate guard added

This release updates Symlink duplicate guard added. Symlink duplicate guard added: Existing specs directories are de-duplicated by realpath, so specs to .opencode/specs no longer appears as two competing roots. This matters because the release is clearer and easier to work with.

&nbsp;

#### Misrouted memory recovered

This release updates Misrouted memory recovered. Misrouted memory recovered: The session memory file was moved to the intended spec folder and re-indexed under 04--agent-orchestration/021-codex-orchestrate. This matters because the release is clearer and easier to work with.

&nbsp;

#### Stale index entries removed

There was a reliability problem around Stale index entries removed. Stale index entries removed: Wrong-folder memory rows created by fallback routing were deleted from the memory index. This matters because it reduces confusing failures and makes the system more dependable.

---

## Files Changed

| File | What Changed |
| ---- | ------------ |
| `.opencode/skill/system-spec-kit/scripts/core/config.ts` | De-duplicate specs roots by realpath. |
| `.opencode/skill/system-spec-kit/scripts/spec-folder/folder-detector.ts` | Normalize JSON SPEC_FOLDER path forms and fix alignment base path. |
| `.opencode/specs/04--agent-orchestration/021-codex-orchestrate/memory/19-02-26_17-51__codex-orchestrate.md` | Recovered and re-indexed memory in correct spec folder. |

---

## Upgrade

- No migration is required. Pull latest and use /memory:save with either specs/... or .opencode/specs/... folder forms.
