This release sQLite WAL contention fix and database maintenance - Resolves intermittent "no such table: memory_index" errors under concurrent access, and cleans up test artifacts, stale conflicts, and missing trigger phrases. It focuses on fixing important problems and explaining the results in clearer terms. The goal is to make the workflow easier to follow and easier to trust.

---

## What Changed

#### SQLite busy_timeout PRAGMA added

There was a reliability problem around SQLite busy_timeout PRAGMA added. SQLite busy_timeout PRAGMA added: Added db.pragma('busy_timeout = 10000') to database initialization in vector-index-impl. When multiple MCP server processes access the WAL-mode database concurrently, sqlite3_prepare() could fail to acquire the WAL lock during schema loading, falling back to a stale main database file with an empty schema - producing the "no such table: memory_index" error. The 10-second busy timeout allows concurrent readers to wait for the lock instead of failing immediately. This matters because it reduces confusing failures and makes the system more dependable.

&nbsp;

#### Test artifacts removed

The team needed stronger proof that Test artifacts removed worked as expected. Test artifacts removed: Deleted 67 test artifact entries (spec_folder='T') and 2 temporary-tier entries from the memory index. This matters because the team has clearer proof that the change works.

&nbsp;

#### Stale conflicts purged

Stale conflicts purged was no longer needed in its old form. Stale conflicts purged: Removed 9,626 orphaned records from the memory_conflicts table. This matters because the project becomes smaller and easier to maintain.

&nbsp;

#### Missing trigger phrases generated

This release updates Missing trigger phrases generated. Missing trigger phrases generated: Auto-generated trigger phrases for 1,463 memories that had none, using title and path-based extraction. This matters because the release is clearer and easier to work with.

&nbsp;

#### Post-maintenance state

The team needed stronger proof that Post-maintenance state worked as expected. Post-maintenance state: 2,909 memories, 0 test artifacts, 0 temporary entries, 0 stale conflicts, 0 missing trigger phrases. This matters because the team has clearer proof that the change works.

&nbsp;

#### Incremental scan completed

This release updates Incremental scan completed. Incremental scan completed: 23 new files indexed, 30 updated, 1,485 unchanged (mtime-based skip). This matters because the release is clearer and easier to work with.

---

## Files Changed

| File | What Changed |
| ---- | ------------ |
| `.opencode/skill/system-spec-kit/mcp_server/lib/search/vector-index-impl.ts` | Added busy_timeout = 10000 PRAGMA after WAL journal mode. |
| `.opencode/skill/system-spec-kit/mcp_server/dist/lib/search/vector-index-impl.js` | Same fix applied to compiled JS output. |
| `dist/database/context-index.sqlite` | Test artifacts, temp entries, conflicts purged; trigger phrases generated. |

---

## Upgrade

- No migration required. The busy_timeout PRAGMA is applied automatically on database open. Restart any running MCP server processes to pick up the fix.
