## [**1.3.0.1**] - 2026-03-18

**Reference-validation enforcement recorded for dependent CocoIndex docs** -- Applied `sk-doc` validation to the Phase 3 CocoIndex documentation set, fixed the missing `OVERVIEW` sections in the new reference docs, and documented the difference between true reference-doc regressions and manual-testing snippet validator scope limits.

> Spec folder: `.opencode/specs/03--commands-and-skills/022-mcp-coco-integration/` (Level 2)

---

## Highlights

### Validation Coverage

- **Reference-mode validation exercised**: `validate_document.py` caught missing required `overview` sections in the new CocoIndex reference docs before completion claims were finalized.
- **Skill-level validation re-confirmed**: `quick_validate.py` continued to pass for the full `mcp-coco-index` skill after the standards fixes.
- **Link/reference sweep stayed clean**: Local markdown references in the touched CocoIndex skill docs resolved successfully after the updates.

### Standards Enforcement

- **`cross_cli_playbook.md` aligned**: Added a proper `OVERVIEW` section so the document now satisfies `sk-doc` reference validation rules.
- **`downstream_adoption_checklist.md` aligned**: Added the same top-level overview framing so the rollout guide matches the reference-doc contract instead of starting directly with checklist content.
- **Snippet-scope caveat captured**: Manual-testing snippet files remain outside the current recursive validator contract, so generic standalone-doc failures there were treated as validator-scope limits rather than as active regression signals.

## Files Changed

- `.opencode/skill/mcp-coco-index/references/cross_cli_playbook.md`
- `.opencode/skill/mcp-coco-index/references/downstream_adoption_checklist.md`

## Upgrade

No migration required. Use `validate_document.py --type reference` on new reference docs before delivery, and treat manual-testing snippet validation separately from the root playbook validation workflow until recursive snippet validation is added.
