# v1.0.6.0

> Part of [OpenCode Dev Environment](https://github.com/MichelKerkmeester/opencode-spec-kit-framework)

---

## [**1.0.6.0**] - 2026-02-14

**Form upload documentation alignment with production bug fixes** -- Updated FilePond reference docs after fixing DOCX rejection, empty `cv_url`, and error classification (spec 035). **+136 lines** across 2 reference files.

---

## Highlights

### Changed

- **`form_upload_workflows.md`** -- Documented `processfile` error-path handling (ERROR state instead of forced COMPLETE), upload URL validation guards (connector-level and form-level submit prevention), extension alias fallback mechanism with native `accept` sync, and `resolve_validation_error_message` mapping for wording variants
- **`third_party_integrations.md`** -- Expanded `DEFAULTS.acceptedTypes` and `MIME_TYPE_MAP` (added TXT, MD, ODT, RTF), added `errorUpload` label, updated extension alias appending logic, corrected R2 bucket domains (separate buckets for user files vs. project assets), updated HTML template (`cv_url`, file type descriptions)
- **`README.md`** -- Version bump to v1.0.6.0 with changelog section documenting all changes
