Removed the ChatGPT agent runtime and model convention prose, and added Codex command-line tool and Gemini command-line tool to runtime path resolution tables. The main changes here are chatgpt runtime removal and runtime path expansion. The goal is to make the workflow easier to follow and easier to trust.

---

#### Chatgpt Runtime Removal

The older guidance still pointed to something that was outdated or no longer used. Removed ChatGPT-specific agent runtime definitions and model convention prose that were no longer in use. This reduces confusion by keeping the notes focused on supported behavior.

---

#### Runtime Path Expansion

People needed clearer support for this new part of the workflow. Added Codex command-line tool (.codex/agents/) and Gemini command-line tool (.gemini/agents/) to the runtime path resolution tables, completing the 5-runtime coverage. This makes the workflow easier to discover and use.

---

## Files Changed

| File | Action | Notes |
| ---- | ------ | ----- |
| `AGENTS.md` | Modified | Runtime path tables, ChatGPT removal |
| `CLAUDE.md` | Modified | Runtime path tables |
| `.opencode/agent/chatgpt/` | Deleted | ChatGPT runtime agents removed |

---

## Upgrade

- No migration required. ChatGPT agent runtime references should be removed from any custom configurations.
