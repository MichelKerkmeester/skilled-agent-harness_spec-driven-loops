## [**2.2.0.0**] - 2026-03-01

all 9 runtime config definition files created from ChatGPT markdown sources.* Automated conversion pipeline extracted system prompts from the related file, applied text substitutions (model refs, path conventions, profile names), and wrapped content in Codex command-line tool config overlay runtime config format. The missing [agents.ultra-think] entry was added to the related file, bringing the agent registry to. The main changes here are 9 agent toml files created in.codex/agents/ and read-only agents.

### 1. 9 Agent TOML Files Created In.codex/agents/

- Need: This part of the agent coordination guidance needed clearer guidance and less room for confusion.
- What changed: completing the Codex command-line tool agent system. Each file contains sandbox_mode, model_reasoning_effort, and developer_instructions (full system prompt).
- Why it matters: This makes the agent coordination guidance easier to follow.

### 2. Read-only Agents

- Need: This part of the agent coordination guidance needed clearer guidance and less room for confusion.
- What changed: (context, orchestrate, review, ultra-think) configured with sandbox_mode = "read-only" to prevent file modifications.
- Why it matters: This makes the agent coordination guidance easier to follow.

### 3. Write-capable Agents

- Need: This area needed a clearer and safer way to work.
- What changed: (debug, handover, research, speckit, write) configured with sandbox_mode = "workspace-write" for implementation tasks.
- Why it matters: This makes the behavior more predictable and lowers the chance of mistakes.

### 4. Reasoning Effort Tiered Per Role

- Need: This area needed a clearer and safer way to work.
- What changed: medium for documentation-focused agents (handover, speckit, write), high for analysis agents (context, orchestrate, research), xhigh for deep reasoning agents (debug, review, ultra-think).
- Why it matters: This makes the behavior more predictable and lowers the chance of mistakes.

### 5. Python Batch Conversion Script

- Need: This part of the agent coordination guidance needed clearer guidance and less room for confusion.
- What changed: reads 9 ChatGPT markdown files, strips workflow config top-of-file metadata, applies text substitutions, and generates runtime config output.
- Why it matters: This makes the agent coordination guidance easier to follow.

### 6. Text Substitutions Applied

- Need: This part of the agent coordination guidance needed clearer guidance and less room for confusion.
- What changed: openai/gpt-5.3-codex replaced with generic model references, the related file paths updated to the related file, ChatGPT profile references replaced with Codex command-line tool.
- Why it matters: This makes the agent coordination guidance easier to follow.

### 7. TOML Literal Strings

- Need: This part of the agent coordination guidance needed clearer guidance and less room for confusion.
- What changed: used instead of basic strings (""") to avoid escape sequence issues with markdown content containing backslashes.
- Why it matters: This makes the agent coordination guidance easier to follow.

### 8. [agents.ultra-think] Added to the Related File

- Need: People needed clearer support for this new part of the workflow.
- What changed: , completing the agent registry with all 9 entries.
- Why it matters: This makes the workflow easier to discover and use.

### 9. All Config_file References Now Resolve

- Need: This part of the agent coordination guidance needed clearer guidance and less room for confusion.
- What changed: to existing runtime config files in.codex/agents/.
- Why it matters: This makes the agent coordination guidance easier to follow.

### 10. All 9 Files Parse Correctly

- Need: This part of the agent coordination guidance needed clearer guidance and less room for confusion.
- What changed: with tomli (runtime config 1.0 spec-compliant parser).
- Why it matters: This makes the agent coordination guidance easier to follow.

### 11. Content Spot-check Passed

- Need: This part of the agent coordination guidance needed clearer guidance and less room for confusion.
- What changed: for key workflow sections across all agents.
- Why it matters: This makes the agent coordination guidance easier to follow.

### 12. No Stale References Remain

- Need: This part of the agent coordination guidance needed clearer guidance and less room for confusion.
- What changed: no openai/gpt-5.3-codex or ChatGPT profile strings in output.
- Why it matters: This makes the agent coordination guidance easier to follow.

## Files Changed

| File | Action | Notes |
| ---- | ------ | ----- |
| `.codex/agents/context.toml` | Modified | 21K chars |
| `.codex/agents/debug.toml` | Modified | 14K chars |
| `.codex/agents/handover.toml` | Modified | 12K chars |
| `.codex/agents/orchestrate.toml` | Modified | 53K chars |
| `.codex/agents/research.toml` | Modified | 23K chars |
| `.codex/agents/review.toml` | Modified | 19K chars |
| `.codex/agents/speckit.toml` | Modified | 22K chars |
| `.codex/agents/ultra-think.toml` | Modified | 23K chars |
| `.codex/agents/write.toml` | Modified | 21K chars |
| `.codex/config.toml` | Modified | +3 lines |
| `.opencode/changelog/03--agent-orchestration/v2.2.0.0.md` | Added | — |

## Upgrade

- No migration required. The runtime config files are additive config overlays. If model_reasoning_effort = "xhigh" is unsupported by a model provider, change to "high". tool connection servers are inherited from parent config and not redeclared in agent files.
