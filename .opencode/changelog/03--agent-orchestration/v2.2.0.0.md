all 9 runtime config definition files created from ChatGPT markdown sources.* Automated conversion pipeline extracted system prompts from the related file, applied text substitutions (model refs, path conventions, profile names), and wrapped content in Codex command-line tool config overlay runtime config format. The missing [agents.ultra-think] entry was added to the related file, bringing the agent registry to. The main changes here are 9 agent toml files created in.codex/agents/ and read-only agents. The goal is to make the workflow easier to follow and easier to trust.

---

### 9 Agent TOML Files Created In.codex/agents/

**Problem:** This part of the agent coordination guidance needed clearer guidance and less room for confusion.

**Fix:** completing the Codex command-line tool agent system. Each file contains sandbox_mode, model_reasoning_effort, and developer_instructions (full system prompt). This makes the agent coordination guidance easier to follow.

---

### Read-only Agents

**Problem:** This part of the agent coordination guidance needed clearer guidance and less room for confusion.

**Fix:** (context, orchestrate, review, ultra-think) configured with sandbox_mode = "read-only" to prevent file modifications. This makes the agent coordination guidance easier to follow.

---

### Write-capable Agents

**Problem:** This area needed a clearer and safer way to work.

**Fix:** (debug, handover, research, speckit, write) configured with sandbox_mode = "workspace-write" for implementation tasks. This makes the behavior more predictable and lowers the chance of mistakes.

---

### Reasoning Effort Tiered Per Role

**Problem:** This area needed a clearer and safer way to work.

**Fix:** medium for documentation-focused agents (handover, speckit, write), high for analysis agents (context, orchestrate, research), xhigh for deep reasoning agents (debug, review, ultra-think). This makes the behavior more predictable and lowers the chance of mistakes.

---

### Python Batch Conversion Script

**Problem:** This part of the agent coordination guidance needed clearer guidance and less room for confusion.

**Fix:** reads 9 ChatGPT markdown files, strips workflow config top-of-file metadata, applies text substitutions, and generates runtime config output. This makes the agent coordination guidance easier to follow.

---

### Text Substitutions Applied

**Problem:** This part of the agent coordination guidance needed clearer guidance and less room for confusion.

**Fix:** openai/gpt-5.3-codex replaced with generic model references, the related file paths updated to the related file, ChatGPT profile references replaced with Codex command-line tool. This makes the agent coordination guidance easier to follow.

---

### TOML Literal Strings

**Problem:** This part of the agent coordination guidance needed clearer guidance and less room for confusion.

**Fix:** used instead of basic strings (""") to avoid escape sequence issues with markdown content containing backslashes. This makes the agent coordination guidance easier to follow.

---

### [agents.ultra-think] Added to the Related File

**Problem:** People needed clearer support for this new part of the workflow.

**Fix:** , completing the agent registry with all 9 entries. This makes the workflow easier to discover and use.

---

### All Config_file References Now Resolve

**Problem:** This part of the agent coordination guidance needed clearer guidance and less room for confusion.

**Fix:** to existing runtime config files in.codex/agents/. This makes the agent coordination guidance easier to follow.

---

### All 9 Files Parse Correctly

**Problem:** This part of the agent coordination guidance needed clearer guidance and less room for confusion.

**Fix:** with tomli (runtime config 1.0 spec-compliant parser). This makes the agent coordination guidance easier to follow.

---

### Content Spot-check Passed

**Problem:** This part of the agent coordination guidance needed clearer guidance and less room for confusion.

**Fix:** for key workflow sections across all agents. This makes the agent coordination guidance easier to follow.

---

### No Stale References Remain

**Problem:** This part of the agent coordination guidance needed clearer guidance and less room for confusion.

**Fix:** no openai/gpt-5.3-codex or ChatGPT profile strings in output. This makes the agent coordination guidance easier to follow.

---

## Files Changed

| File | Action | Notes |
| ---- | ------ | ----- |
| `.codex/agents/context.toml` | Modified | 21K chars |
| `.codex/agents/debug.toml` | Modified | 14K chars |
| `.codex/agents/handover.toml` | Modified | 12K chars |
| `.codex/agents/orchestrate.toml` | Modified | 53K chars |
| `.codex/agents/research.toml` | Modified | 23K chars |
| `.codex/agents/review.toml` | Modified | 19K chars |
| `.codex/agents/speckit.toml` | Modified | 22K chars |
| `.codex/agents/ultra-think.toml` | Modified | 23K chars |
| `.codex/agents/write.toml` | Modified | 21K chars |
| `.codex/config.toml` | Modified | +3 lines |
| `.opencode/changelog/03--agent-orchestration/v2.2.0.0.md` | Added | - |

---

## Upgrade

- No migration required. The runtime config files are additive config overlays. If model_reasoning_effort = "xhigh" is unsupported by a model provider, change to "high". tool connection servers are inherited from parent config and not redeclared in agent files.
