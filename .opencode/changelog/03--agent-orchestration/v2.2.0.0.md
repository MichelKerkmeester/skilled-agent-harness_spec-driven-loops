## [**2.2.0.0**] - 2026-03-01

**Codex CLI agent system completed: all 9 TOML definition files created from ChatGPT markdown sources.** Automated conversion pipeline extracted system prompts from `.opencode/agent/chatgpt/*.md`, applied text substitutions (model refs, path conventions, profile names), and wrapped content in Codex CLI config overlay TOML format. The missing `[agents.ultra-think]` entry was added to `config.toml`, bringing the agent registry to 9/9.

---

## Highlights

### Category: Codex CLI Agent Definitions

- **9 agent TOML files created in `.codex/agents/`** completing the Codex CLI agent system. Each file contains `sandbox_mode`, `model_reasoning_effort`, and `developer_instructions` (full system prompt).
- **Read-only agents** (context, orchestrate, review, ultra-think) configured with `sandbox_mode = "read-only"` to prevent file modifications.
- **Write-capable agents** (debug, handover, research, speckit, write) configured with `sandbox_mode = "workspace-write"` for implementation tasks.
- **Reasoning effort tiered per role**: `medium` for documentation-focused agents (handover, speckit, write), `high` for analysis agents (context, orchestrate, research), `xhigh` for deep reasoning agents (debug, review, ultra-think).

### Category: Automated Conversion Pipeline

- **Python batch conversion script** reads 9 ChatGPT markdown files, strips YAML frontmatter, applies text substitutions, and generates TOML output.
- **Text substitutions applied**: `openai/gpt-5.3-codex` replaced with generic model references, `.opencode/agent/*.md` paths updated to `.codex/agents/*.toml`, `ChatGPT profile` references replaced with `Codex CLI`.
- **TOML literal strings (`'''`)** used instead of basic strings (`"""`) to avoid escape sequence issues with markdown content containing backslashes.

### Category: Config Registry Completion

- **`[agents.ultra-think]` added to `config.toml`**, completing the agent registry with all 9 entries.
- **All `config_file` references now resolve** to existing TOML files in `.codex/agents/`.

### Category: Verification

- **All 9 files parse correctly** with `tomli` (TOML 1.0 spec-compliant parser).
- **Content spot-check passed** for key workflow sections across all agents.
- **No stale references remain** — no `openai/gpt-5.3-codex` or `ChatGPT profile` strings in output.

## Files Changed

| File | Action | Size |
|------|--------|------|
| `.codex/agents/context.toml` | Modified | 21K chars |
| `.codex/agents/debug.toml` | Modified | 14K chars |
| `.codex/agents/handover.toml` | Modified | 12K chars |
| `.codex/agents/orchestrate.toml` | Modified | 53K chars |
| `.codex/agents/research.toml` | Modified | 23K chars |
| `.codex/agents/review.toml` | Modified | 19K chars |
| `.codex/agents/speckit.toml` | Modified | 22K chars |
| `.codex/agents/ultra-think.toml` | Modified | 23K chars |
| `.codex/agents/write.toml` | Modified | 21K chars |
| `.codex/config.toml` | Modified | +3 lines |
| `.opencode/changelog/03--agent-orchestration/v2.2.0.0.md` | Added | — |

**Total: 10 files modified, 1 file added**

## Upgrade

No migration required. The TOML files are additive config overlays. If `model_reasoning_effort = "xhigh"` is unsupported by a model provider, change to `"high"`. MCP servers are inherited from parent config and not redeclared in agent files.
