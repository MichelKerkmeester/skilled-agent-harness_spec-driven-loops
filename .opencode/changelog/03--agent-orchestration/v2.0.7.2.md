Combines the single-hop delegation safety rollout, Claude parity sync, and core OpenCode model-unpin changes into one release note for specs/04--agent-orchestration/021-codex-orchestrate. The main changes here are single-hop delegation enforced across agent suites and context agents are direct-only. The goal is to make the workflow easier to follow and easier to trust.

---

### Single-hop Delegation Enforced Across Agent Suites

**Problem:** This part of the agent coordination guidance needed clearer guidance and less room for confusion.

**Fix:** nested sub-agent dispatch is explicitly blocked for LEAF agents, with depth bounded to orchestrator -> leaf execution. This makes the agent coordination guidance easier to follow.

---

### Context Agents Are Direct-only

**Problem:** This part of the agent coordination guidance needed clearer guidance and less room for confusion.

**Fix:** retrieval stays memory-first and local, with no nested Task fan-out in Copilot/Claude context profiles. This makes the agent coordination guidance easier to follow.

---

### Leaf Guardrails Standardized

**Problem:** This part of the documentation needed to match the related files more closely.

**Fix:** debug, research, handover, review, speckit, and write include hard-block illegal-nesting language. This helps keep different copies of the same guidance from drifting apart.

---

### Orchestrate Read Access Enabled Where Needed

**Problem:** This part of the agent coordination guidance needed clearer guidance and less room for confusion.

**Fix:** orchestrators can load definitions/specs while still delegating implementation/exploration work. This makes the agent coordination guidance easier to follow.

---

### Model Pin Removed

**Problem:** The older guidance still pointed to something that was outdated or no longer used.

**Fix:** deleted top-of-file metadata model from the related file, the related file, and the related file so runtime/default model selection is centrally controlled. This reduces confusion by keeping the notes focused on supported behavior.

---

### Chatgpt + Copilot Alignment Captured

**Problem:** This part of the documentation needed to match the related files more closely.

**Fix:** policy and wording fixes synchronized without expanding Copilot context budgets. This helps keep different copies of the same guidance from drifting apart.

---

### Claude Parity Sync Completed

**Problem:** This part of the documentation needed to match the related files more closely.

**Fix:** .claude/agents/* now mirrors Copilot safety behavior while preserving Claude top-of-file metadata conventions (tools, model, mcpServers, permissionMode). This helps keep different copies of the same guidance from drifting apart.

---

### Runtime Directory Routing Documented

**Problem:** The writing in this area needed to be easier to read and follow.

**Fix:** the AGENTS guide now explicitly maps provider/runtime to main agent-definition roots (.opencode/agent,.opencode/agent/chatgpt,.opencode/agent/claude), with.claude/agents preserved as a compatibility alias. This makes the documentation easier for non-developers to read.

---

### Workflows-documentation Validation Passed

**Problem:** The writing in this area needed to be easier to read and follow.

**Fix:** the related file + the related file are PASS. This makes the documentation easier for non-developers to read.

---

### Claude Suite Validated Correctly

**Problem:** This part of the agent coordination guidance needed clearer guidance and less room for confusion.

**Fix:** .claude/agents/*.md are PASS using explicit --type agent. This makes the agent coordination guidance easier to follow.

---

### Model-unpin Edits Validated

**Problem:** This part of the agent coordination guidance needed clearer guidance and less room for confusion.

**Fix:** the three core OpenCode files pass the related file --type agent. This makes the agent coordination guidance easier to follow.

---

### Chatgpt Adaptive Budgets Retained

**Problem:** This part of the documentation needed to match the related files more closely.

**Fix:** Quick ~1.8K/55, Standard ~3.5K/105, Deep ~5.5K/165. This helps keep different copies of the same guidance from drifting apart.

---

### Copilot Remains Conservative

**Problem:** This part of the agent coordination guidance needed clearer guidance and less room for confusion.

**Fix:** tighter-window defaults preserved. This makes the agent coordination guidance easier to follow.

---

## Files Changed

| File | Action | Notes |
| ---- | ------ | ----- |
| `.opencode/agent/chatgpt/context.md` | Direct-only context retrieval, illegal-nesting protocol, verification alignment | Direct-only context retrieval, illegal-nesting protocol, verification alignment |
| `.opencode/agent/chatgpt/debug.md` | LEAF illegal-nesting hard-block + wording cleanup | LEAF illegal-nesting hard-block + wording cleanup |
| `.opencode/agent/chatgpt/handover.md` | LEAF illegal-nesting hard-block + model/profile alignment | LEAF illegal-nesting hard-block + model/profile alignment |
| `.opencode/agent/chatgpt/orchestrate.md` | Single-hop NDP, depth/dispatch policy refinements, read-enabled orchestration behavior | Single-hop NDP, depth/dispatch policy refinements, read-enabled orchestration behavior |
| `.opencode/agent/chatgpt/research.md` | LEAF hard-block + memory-save exception clarity | LEAF hard-block + memory-save exception clarity |
| `.opencode/agent/chatgpt/review.md` | LEAF hard-block + Read({ filePath }) terminology + P0 semantics | LEAF hard-block + Read({ filePath }) terminology + P0 semantics |
| `.opencode/agent/chatgpt/speckit.md` | LEAF hard-block + Level/validation consistency updates | LEAF hard-block + Level/validation consistency updates |
| `.opencode/agent/chatgpt/write.md` | LEAF hard-block + template-first/DQI alignment | LEAF hard-block + template-first/DQI alignment |
| `.opencode/agent/copilot/context.md` | Direct-only retrieval (task: deny) + illegal-nesting protocol | Direct-only retrieval (task: deny) + illegal-nesting protocol |
| `.opencode/agent/copilot/debug.md` | LEAF hard-block + fast-path wording cleanup | LEAF hard-block + fast-path wording cleanup |
| `.opencode/agent/copilot/handover.md` | LEAF hard-block + 5-section wording correction | LEAF hard-block + 5-section wording correction |
| `.opencode/agent/copilot/orchestrate.md` | Single-hop NDP + read-capable orchestrator policy | Single-hop NDP + read-capable orchestrator policy |
| `.opencode/agent/copilot/research.md` | LEAF hard-block + trivial-research memory-save exception rules | LEAF hard-block + trivial-research memory-save exception rules |
| `.opencode/agent/copilot/review.md` | LEAF hard-block + filePath + P0 semantics alignment | LEAF hard-block + filePath + P0 semantics alignment |
| `.opencode/agent/copilot/speckit.md` | LEAF hard-block + Level 3/validation semantics alignment | LEAF hard-block + Level 3/validation semantics alignment |
| `.opencode/agent/copilot/write.md` | LEAF hard-block + template-first fast path + mode-aware DQI rule | LEAF hard-block + template-first fast path + mode-aware DQI rule |
| `.claude/agents/context.md` | Synced to direct-only retrieval body; removed Task from tools | Synced to direct-only retrieval body; removed Task from tools |
| `.claude/agents/debug.md` | Synced LEAF policy and debug workflow parity | Synced LEAF policy and debug workflow parity |
| `.claude/agents/handover.md` | Synced handover policy/body parity | Synced handover policy/body parity |
| `.claude/agents/orchestrate.md` | Synced single-hop orchestration body; includes Read + Task | Synced single-hop orchestration body; includes Read + Task |
| `.claude/agents/research.md` | Synced research workflow and LEAF constraints | Synced research workflow and LEAF constraints |
| `.claude/agents/review.md` | Synced review rubric/policy body; Claude wording normalization | Synced review rubric/policy body; Claude wording normalization |
| `.claude/agents/speckit.md` | Synced template/validation and LEAF policy body | Synced template/validation and LEAF policy body |
| `.claude/agents/write.md` | Synced documentation workflow and LEAF policy body | Synced documentation workflow and LEAF policy body |
| `.opencode/agent/orchestrate.md` | Removed frontmatter model (runtime default model selection) | Removed top-of-file metadata model (runtime default model selection) |
| `.opencode/agent/research.md` | Removed frontmatter model (runtime default model selection) | Removed top-of-file metadata model (runtime default model selection) |
| `.opencode/agent/debug.md` | Removed frontmatter model (runtime default model selection) | Removed top-of-file metadata model (runtime default model selection) |
| `AGENTS.md` | Added runtime/provider-specific agent directory resolution rules | Added runtime/provider-specific agent directory resolution rules |

---

## Upgrade

- No migration required. Pull latest to apply consolidated orchestration safety changes, Claude parity sync, and runtime-model unpinning.
