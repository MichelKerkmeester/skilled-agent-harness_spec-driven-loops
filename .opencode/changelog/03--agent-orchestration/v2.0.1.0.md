Synced the related file and the related file with.opencode/agent/ changes from v2.0.0.4 (Agent Loading Protocol, routing overhaul). The main changes here are the related file and the related file. The goal is to make the workflow easier to follow and easier to trust.

---

#### The Related File

People needed clearer support for this new part of the workflow. Promoted to "exclusive entry point for ALL exploration tasks", added Routing Rule block. This makes the workflow easier to discover and use.

---

#### The Related File

People needed clearer support for this new part of the workflow. Agent Loading Protocol, routing table update, Rule 4 rewrite, new anti-patterns, task template fields. This makes the workflow easier to discover and use.

---

## Files Changed

| File | Action | Notes |
| ---- | ------ | ----- |
| `.claude/agents/context.md · .claude/agents/orchestrate.md` | Updated | Updated in this release |

---

## Upgrade

- No action required. Pull latest to get updates.
