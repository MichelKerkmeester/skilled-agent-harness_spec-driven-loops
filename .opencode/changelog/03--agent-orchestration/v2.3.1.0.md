Agent alignment verification, epic reconciliation, and ChatGPT agent config cleanup. Verified 017-agents-alignment spec completeness across all 5 runtime directories (45 agent files), closed T031, updated the parent epic dashboard with alignment specs 015-018, fixed a duplicate 015 row, and removed pre-specified model and reasoningEffort fields from all 7 ChatGPT agent definitions so they inherit from global config. The main changes here are 1:1 correspondence confirmed across all runtimes and body content parity verified.

---

### 1:1 Correspondence Confirmed Across All Runtimes

**Problem:** This area needed a clearer and safer way to work.

**Fix:** all 9 main agents (context, debug, handover, orchestrate, research, review, speckit, ultra-think, write) verified present in.opencode/agent/,.claude/agents/,.gemini/agents/,.opencode/agent/chatgpt/, and.codex/agents/. This makes the behavior more predictable and lowers the chance of mistakes.

---

### Body Content Parity Verified

**Problem:** This part of the agent coordination guidance needed clearer guidance and less room for confusion.

**Fix:** runtime-specific top-of-file metadata (tools, model, tool connection servers) correctly preserved while markdown body content matches main definitions. This makes the agent coordination guidance easier to follow.

---

### No Orphaned or Missing Files

**Problem:** This part of the agent coordination guidance needed clearer guidance and less room for confusion.

**Fix:** zero discrepancies across 45 agent files (9 agents x 5 runtimes). This makes the agent coordination guidance easier to follow.

---

### Alignment Specs 015-018 Added to Dashboard

**Problem:** People needed clearer support for this new part of the workflow.

**Fix:** the related file updated with 4 new rows showing 015-skill-alignment, 016-command-alignment, 017-agents-alignment, and 018-agents-md-alignment as Complete. This helps keep different copies of the same guidance from drifting apart.

---

### Stale 015-finalization Row Removed

**Problem:** The older guidance still pointed to something that was outdated or no longer used.

**Fix:** orphaned placeholder with no corresponding spec folder was creating a duplicate 015 entry. Identified by GPT-5.4 ultra-think cross-review and fixed. This reduces confusion by keeping the notes focused on supported behavior.

---

### T031 Closed

**Problem:** This part of the documentation needed to match the related files more closely.

**Fix:** last pending task in 017-agents-alignment marked complete with evidence and date. This helps keep different copies of the same guidance from drifting apart.

---

### 5-agent Parallel Audit

**Problem:** This part of the documentation needed to match the related files more closely.

**Fix:** documentation quality (8.), T031 closure, feature catalog coverage, implementation existence ( source + test files), and sync completeness all verified by specialized agents. This helps keep different copies of the same guidance from drifting apart.

---

### Gpt-5.4 Ultra-think Cross-review

**Problem:** This part of the agent coordination guidance needed clearer guidance and less room for confusion.

**Fix:** independent review via Copilot command-line tool with Analytical + Critical + Holistic lenses scored the verification at 7. and caught the duplicate 015 row defect. This makes the agent coordination guidance easier to follow.

---

### Removed Model: from 7 Agents

**Problem:** The older guidance still pointed to something that was outdated or no longer used.

**Fix:** context, debug, handover, research, review, speckit, write no longer pin a specific model. They inherit from the global ChatGPT/Copilot config instead. This reduces confusion by keeping the notes focused on supported behavior.

---

### Removed Reasoningeffort: from All 9 Agents

**Problem:** The older guidance still pointed to something that was outdated or no longer used.

**Fix:** previously set to xhigh (later changed to high). Now fully removed so agents inherit the global reasoning effort setting. xhigh should never be used for GPT-5.4 or GPT-5.3-Codex. high is the correct ceiling. This reduces confusion by keeping the notes focused on supported behavior.

---

### ~the Related File Set to High

**Problem:** This part of the agent coordination guidance needed clearer guidance and less room for confusion.

**Fix:** global reasoning effort changed from xhigh to high. This makes the agent coordination guidance easier to follow.

---

## Files Changed

| File | Action | Notes |
| ---- | ------ | ----- |
| `.opencode/specs/.../001-hybrid-rag-fusion-epic/spec.md` | Modified | Added 015-018 alignment rows to dashboard; removed stale 015-finalization placeholder |
| `.opencode/specs/.../017-agents-alignment/tasks.md` | Modified | T031 marked complete with evidence (2026-03-16) |
| `.opencode/agent/chatgpt/context.md` | Modified | Removed model: and reasoningEffort: from top-of-file metadata |
| `.opencode/agent/chatgpt/debug.md` | Modified | Removed model: and reasoningEffort: from top-of-file metadata |
| `.opencode/agent/chatgpt/handover.md` | Modified | Removed model: and reasoningEffort: from top-of-file metadata |
| `.opencode/agent/chatgpt/orchestrate.md` | Unchanged | Already had no model: or reasoningEffort: |
| `.opencode/agent/chatgpt/research.md` | Modified | Removed model: and reasoningEffort: from top-of-file metadata |
| `.opencode/agent/chatgpt/review.md` | Modified | Removed model: and reasoningEffort: from top-of-file metadata |
| `.opencode/agent/chatgpt/speckit.md` | Modified | Removed model: and reasoningEffort: from top-of-file metadata |
| `.opencode/agent/chatgpt/ultra-think.md` | Unchanged | Already had no model: or reasoningEffort: |
| `.opencode/agent/chatgpt/write.md` | Modified | Removed model: and reasoningEffort: from top-of-file metadata |

---

## Upgrade

- No migration required. ChatGPT agents now inherit model and reasoning effort from global config (~the related file). Set reasoning_effort to high (never xhigh) in global config.
