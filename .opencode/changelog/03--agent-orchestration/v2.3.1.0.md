# v2.3.1.0

> Part of [OpenCode Dev Environment](https://github.com/MichelKerkmeester/opencode-spec-kit-framework)

---

## [**2.3.1.0**] - 2026-03-16

**Agent alignment verification, epic reconciliation, and ChatGPT agent config cleanup.** Verified 017-agents-alignment spec completeness across all 5 runtime directories (45 agent files), closed T031, updated the parent epic dashboard with alignment specs 015-018, fixed a duplicate `015` row, and removed pre-specified `model` and `reasoningEffort` fields from all 7 ChatGPT agent definitions so they inherit from global config.

---

## Highlights

### Category: Verification — Agent Sync Completeness

- **1:1 correspondence confirmed across all runtimes** — all 9 canonical agents (context, debug, handover, orchestrate, research, review, speckit, ultra-think, write) verified present in `.opencode/agent/`, `.claude/agents/`, `.gemini/agents/`, `.opencode/agent/chatgpt/`, and `.codex/agents/`.
- **Body content parity verified** — runtime-specific frontmatter (tools, model, MCP servers) correctly preserved while markdown body content matches canonical definitions.
- **No orphaned or missing files** — zero discrepancies across 45 agent files (9 agents x 5 runtimes).

### Category: Parent Epic Dashboard Update

- **Alignment specs 015-018 added to dashboard** — `001-hybrid-rag-fusion-epic/spec.md` updated with 4 new rows showing 015-skill-alignment, 016-command-alignment, 017-agents-alignment, and 018-agents-md-alignment as Complete.
- **Stale `015-finalization` row removed** — orphaned placeholder with no corresponding spec folder was creating a duplicate `015` entry. Identified by GPT-5.4 ultra-think cross-review and fixed.
- **T031 closed** — last pending task in 017-agents-alignment marked complete with evidence and date.

### Category: Cross-AI Verification

- **5-agent parallel audit** — documentation quality (8.5/10), T031 closure, feature catalog coverage, implementation existence (23/23 source + 24/24 test files), and sync completeness all verified by specialized agents.
- **GPT-5.4 ultra-think cross-review** — independent review via Copilot CLI with Analytical + Critical + Holistic lenses scored the verification at 7.8/10 and caught the duplicate `015` row defect.

### Category: ChatGPT Agent Config Cleanup

- **Removed `model:` from 7 agents** — context, debug, handover, research, review, speckit, write no longer pin a specific model. They inherit from the global ChatGPT/Copilot config instead.
- **Removed `reasoningEffort:` from all 9 agents** — previously set to `xhigh` (later changed to `high`). Now fully removed so agents inherit the global reasoning effort setting. `xhigh` should never be used for GPT-5.4 or GPT-5.3-Codex — `high` is the correct ceiling.
- **`~/.copilot/config.json` set to `high`** — global reasoning effort changed from `xhigh` to `high`.

## Files Changed

| File | Action | Description |
|------|--------|-------------|
| `.opencode/specs/.../001-hybrid-rag-fusion-epic/spec.md` | Modified | Added 015-018 alignment rows to dashboard; removed stale `015-finalization` placeholder |
| `.opencode/specs/.../017-agents-alignment/tasks.md` | Modified | T031 marked complete with evidence (2026-03-16) |
| `.opencode/agent/chatgpt/context.md` | Modified | Removed `model:` and `reasoningEffort:` from frontmatter |
| `.opencode/agent/chatgpt/debug.md` | Modified | Removed `model:` and `reasoningEffort:` from frontmatter |
| `.opencode/agent/chatgpt/handover.md` | Modified | Removed `model:` and `reasoningEffort:` from frontmatter |
| `.opencode/agent/chatgpt/orchestrate.md` | Unchanged | Already had no `model:` or `reasoningEffort:` |
| `.opencode/agent/chatgpt/research.md` | Modified | Removed `model:` and `reasoningEffort:` from frontmatter |
| `.opencode/agent/chatgpt/review.md` | Modified | Removed `model:` and `reasoningEffort:` from frontmatter |
| `.opencode/agent/chatgpt/speckit.md` | Modified | Removed `model:` and `reasoningEffort:` from frontmatter |
| `.opencode/agent/chatgpt/ultra-think.md` | Unchanged | Already had no `model:` or `reasoningEffort:` |
| `.opencode/agent/chatgpt/write.md` | Modified | Removed `model:` and `reasoningEffort:` from frontmatter |

**Total: 9 files modified (7 ChatGPT agents + 2 spec docs)**

## Upgrade

No migration required. ChatGPT agents now inherit model and reasoning effort from global config (`~/.copilot/config.json`). Set `reasoning_effort` to `high` (never `xhigh`) in global config.
