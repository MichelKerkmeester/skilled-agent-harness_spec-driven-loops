Removed all Narsil tool references from agent files. Replaced narsil_neural_search(), narsil_find_symbols(), narsil_get_callers() with native exact text search, file-pattern search, Read tools. Cleaned permission blocks. The main changes here are the related file and the related file. The goal is to make the workflow easier to follow and easier to trust.

---

### The Related File

**Problem:** The older guidance still pointed to something that was outdated or no longer used.

**Fix:** Removed Narsil references, removed narsil: allow permission. This reduces confusion by keeping the notes focused on supported behavior.

---

### The Related File

**Problem:** The older guidance still pointed to something that was outdated or no longer used.

**Fix:** Removed Narsil tool integration, updated tool references. This reduces confusion by keeping the notes focused on supported behavior.

---

### The Related File

**Problem:** People needed a better way to find the right information without guessing exact words.

**Fix:** Replaced Narsil tools with native exact text search/file-pattern search/Read. This helps people find the right place faster, even when they do not know the exact term to search for.

---

### The Related File

**Problem:** The older guidance still pointed to something that was outdated or no longer used.

**Fix:** Removed Narsil tool references. This reduces confusion by keeping the notes focused on supported behavior.

---

### The Related File

**Problem:** The older guidance still pointed to something that was outdated or no longer used.

**Fix:** Removed narsil: allow and narsil: deny permissions. This reduces confusion by keeping the notes focused on supported behavior.

---

## Files Changed

| File | Action | Notes |
| ---- | ------ | ----- |
| `.opencode/agent/debug.md · .opencode/agent/orchestrate.md · .opencode/agent/research.md · .opencode/agent/review.md · .opencode/agent/handover.md` | Updated | Updated in this release |

---

## Upgrade

- No action required. Pull latest to get updates.
