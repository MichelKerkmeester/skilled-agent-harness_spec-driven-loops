Removed mcp-narsil skill references across all agents, replacing semantic/structural search with native exact text search/file-pattern search/Read tools. Simplified tool stack while maintaining full debugging and research capabilities. The main changes here are the related file and the related file. The goal is to make the workflow easier to follow and easier to trust.

---

#### The Related File

People needed a better way to find the right information without guessing exact words. Replaced narsil.* tool calls with exact text search, file-pattern search, and Read. Updated Phase 2 ANALYZE workflow to use native tools for code context understanding. Revised tool routing table and decision trees (21 insertions, 22 deletions). This helps people find the right place faster, even when they do not know the exact term to search for.

---

#### The Related File

The older guidance still pointed to something that was outdated or no longer used. Removed narsil: deny permission entry (1 deletion). This reduces confusion by keeping the notes focused on supported behavior.

---

#### The Related File

The older guidance still pointed to something that was outdated or no longer used. Removed mcp-narsil from skills table. Updated capability scan to reflect simplified tool stack (21 insertions, 22 deletions). This reduces confusion by keeping the notes focused on supported behavior.

---

#### The Related File

People needed a better way to find the right information without guessing exact words. Replaced Narsil references with exact text search/file-pattern search/Read workflows in code discovery patterns (26 insertions, 27 deletions). This helps people find the right place faster, even when they do not know the exact term to search for.

---

#### The Related File

People needed a better way to find the right information without guessing exact words. Updated code analysis workflows to use native search tools instead of Narsil (46 insertions, 46 deletions). This helps people find the right place faster, even when they do not know the exact term to search for.

---

#### The Related File

This part of the documentation needed to match the related files more closely. Minor formatting alignment (2 insertions, 2 deletions). This helps keep different copies of the same guidance from drifting apart.

---

#### The Related File

The older guidance still pointed to something that was outdated or no longer used. Removed Narsil reference from template mapping table (1 deletion). This reduces confusion by keeping the notes focused on supported behavior.

---

## Files Changed

| File | Action | Notes |
| ---- | ------ | ----- |
| `.opencode/agent/debug.md · .opencode/agent/handover.md · .opencode/agent/orchestrate.md · .opencode/agent/research.md · .opencode/agent/review.md · .opencode/agent/speckit.md · .opencode/agent/write.md` | Updated | Updated in this release |

---

## Upgrade

- No action required. Pull latest to get updates.
