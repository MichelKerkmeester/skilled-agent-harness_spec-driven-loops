Removed the duplicate.opencode/agent/claude tree and standardized orchestration/runtime documentation to resolve top-of-file metadata instability tied to the old mirrored path. The main changes here are main claude root enforced and legacy mirrored tree removed. The goal is to make the workflow easier to follow and easier to trust.

---

### Canonical Claude Root Enforced

**Problem:** This part of the agent coordination guidance needed clearer guidance and less room for confusion.

**Fix:** Claude agent definitions are now treated as /.claude/agents in runtime mapping guidance. This makes the agent coordination guidance easier to follow.

---

### Legacy Mirrored Tree Removed

**Problem:** The older guidance still pointed to something that was outdated or no longer used.

**Fix:** Deprecated.opencode/agent/claude/* files were removed to avoid split-source drift and invalid top-of-file metadata handling across duplicate trees. This helps keep different copies of the same guidance from drifting apart.

---

### Agent Directory Resolution Updated

**Problem:** This part of the documentation needed to match the related files more closely.

**Fix:** Policy/runtime references now align around.opencode/agent,.opencode/agent/chatgpt, and /.claude/agents. This helps keep different copies of the same guidance from drifting apart.

---

### Spec Evidence Updated

**Problem:** This part of the documentation needed to match the related files more closely.

**Fix:** 021-codex-orchestrate implementation artifacts were synchronized to reflect the finalized Claude path model. This helps keep different copies of the same guidance from drifting apart.

---

## Files Changed

| File | Action | Notes |
| ---- | ------ | ----- |
| `.opencode/agent/claude/{context,debug,handover,orchestrate,research,review,speckit,write}.md` | Removed legacy mirrored Claude definitions | Removed legacy mirrored Claude definitions |
| `.claude/agents/{context,debug,handover,orchestrate,research,review,speckit,write}.md` | Added/retained canonical Claude agent definitions | Added/retained canonical Claude agent definitions |
| `AGENTS.md` | Claude runtime directory mapping normalized to /.claude/agents | Claude runtime directory mapping normalized to /.claude/agents |
| `.opencode/specs/04--agent-orchestration/021-codex-orchestrate/implementation-summary.md` | Updated runtime mapping evidence | Updated runtime mapping evidence |

---

## Upgrade

- No migration is required. Pull latest and use /.claude/agents as the Claude runtime source of truth.
