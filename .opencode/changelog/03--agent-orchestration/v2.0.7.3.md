# v2.0.7.3

> Part of [OpenCode Dev Environment](https://github.com/MichelKerkmeester/opencode-spec-kit-framework)

---

## [**2.0.7.3**] - 2026-02-19

**Claude agent source-root relocation to `/.claude/agents`** -- Removed the duplicate `.opencode/agent/claude` tree and standardized orchestration/runtime documentation to resolve frontmatter instability tied to the old mirrored path.

> Spec folder: `.opencode/specs/004-agents/021-codex-orchestrate/` (Level 3)

---

## Highlights

### Category: Runtime Directory Alignment

- **Canonical Claude root enforced**: Claude agent definitions are now treated as `/.claude/agents` in runtime mapping guidance.
- **Legacy mirrored tree removed**: Deprecated `.opencode/agent/claude/*` files were removed to avoid split-source drift and invalid frontmatter handling across duplicate trees.

### Category: Policy and Documentation Sync

- **Agent directory resolution updated**: Policy/runtime references now align around `.opencode/agent`, `.opencode/agent/chatgpt`, and `/.claude/agents`.
- **Spec evidence updated**: `021-codex-orchestrate` implementation artifacts were synchronized to reflect the finalized Claude path model.

## Files Changed

| File | Change |
|------|--------|
| `.opencode/agent/claude/{context,debug,handover,orchestrate,research,review,speckit,write}.md` | Removed legacy mirrored Claude definitions |
| `.claude/agents/{context,debug,handover,orchestrate,research,review,speckit,write}.md` | Added/retained canonical Claude agent definitions |
| `AGENTS.md` | Claude runtime directory mapping normalized to `/.claude/agents` |
| `.opencode/specs/004-agents/021-codex-orchestrate/implementation-summary.md` | Updated runtime mapping evidence |

## Upgrade

No migration is required. Pull latest and use `/.claude/agents` as the Claude runtime source of truth.
