## [**1.0.8.3**] - 2026-01-24

Multi-Model Agent Configuration Overhaul - Optimizing 7 agents for GitHub Copilot model selection with GPT-5.2-Codex as default for debug/review agents and standardized model options. This release focuses on making the environment easier to use and easier to trust.

### 1. Model Architecture Updates
- Need: This part of the release makes agent behavior more consistent across the supported runtimes.
- What changed: GPT-5.2-Codex Support: Debug and review agents now default to GPT-5.2-Codex for superior bug finding precision (22 errors/MLOC). Copilot Model Identifiers: Added gpt-5.2-codex model picker reference in agent MODEL PREFERENCE sections. Model Frontmatter: All 7 agents now have explicit model: parameter in structured config files frontmatter.
- Why it matters: That matters because the same instructions now behave more predictably in different runtimes.

### 2. Agent Configuration Changes
- Need: This part of the release improves how the environment saves, finds, and uses context.
- What changed: Debug.md: opus → gpt (GPT-5.2-Codex for root cause analysis). Review.md: opus → gpt (GPT-5.2-Codex for security vulnerability detection). Orchestrate.md: added opus (complex task decomposition).
- Why it matters: That matters because saved context is easier to find, easier to trust, and less likely to get in the way.

### 3. Standardization
- Need: This part of the release makes agent behavior more consistent across the supported runtimes.
- What changed: Model Options Standardized: Replaced haiku/sonnet/opus with opus/gemini/gpt across all commands and YAMLs. Gemini Description Updated: Changed from "cost-sensitive, simple tasks" to "Pro for quality, Flash for speed" (reflecting Gemini Pro/Flash capabilities). Debug.md Q2 Model Selection: Codex now recommended (option A), moved Claude to option B.
- Why it matters: That matters because the same instructions now behave more predictably in different runtimes.

### 4. Bug Fixes
- Need: This part of the release makes agent behavior more consistent across the supported runtimes.
- What changed: Model Alignment: All spec_kit commands now match their corresponding agent model defaults.
- Why it matters: That matters because the same instructions now behave more predictably in different runtimes.
