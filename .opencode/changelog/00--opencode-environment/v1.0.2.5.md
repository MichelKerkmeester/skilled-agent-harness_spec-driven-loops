## [**1.0.2.5**] - 2026-01-02

Security and Documentation Release - Fixes hardcoded API key exposure in.utcp_config.json and broken install script configuration. This release focuses on making the environment easier to use and easier to trust.

### CWE-798 (Hardcoded Credentials)
- Need: This part of the release fixes rough edges and makes the environment more dependable.
- What changed: CWE-798 (Hardcoded Credentials): Fixed hardcoded VOYAGE_API_KEY in.utcp_config.json - now uses ${VOYAGE_API_KEY} variable reference loaded from.env.
- Why it matters: That matters because the environment is easier to maintain and less likely to break in everyday use.

### Invalid Config
- Need: This part of the release fixes rough edges and makes the environment more dependable.
- What changed: Invalid Config: install-narsil.sh generating invalid config with _note, _neural_backends fields that break Code Mode parsing.
- Why it matters: That matters because the environment is easier to maintain and less likely to break in everyday use.

### Missing Flag
- Need: This part of the release fixes rough edges and makes the environment more dependable.
- What changed: Missing Flag: Missing --watch flag in all recommended Narsil configurations.
- Why it matters: That matters because the environment is easier to maintain and less likely to break in everyday use.

### New
- Need: This part of the release makes the written guidance easier to follow and easier to trust.
- What changed: Neural Backend Comparison: Table showing all 3 options: Voyage AI (recommended) · OpenAI · Local ONNX. Configuration Examples: Separate examples for each neural backend in install guide. HTTP Server Documentation: Stdin pipe trick (tail -f /dev/null | to prevent EOF shutdown).
- Why it matters: That matters because people can understand the system faster and use it more confidently.

### Narsil Config Examples
- Need: This part of the release improves how the environment saves, finds, and uses context.
- What changed: Narsil Config Examples: Now include --watch flag for auto-reindexing.
- Why it matters: That matters because saved context is easier to find, easier to trust, and less likely to get in the way.

### API Key References
- Need: This part of the release improves api key references.
- What changed: API Key References: Changed from hardcoded values to ${VOYAGE_API_KEY} variable syntax.
- Why it matters: That matters because it makes the environment easier to use and maintain.

### Install Script Help
- Need: This part of the release improves how the environment saves, finds, and uses context.
- What changed: Install Script Help: Expanded with Neural Search Backends section.
- Why it matters: That matters because saved context is easier to find, easier to trust, and less likely to get in the way.
