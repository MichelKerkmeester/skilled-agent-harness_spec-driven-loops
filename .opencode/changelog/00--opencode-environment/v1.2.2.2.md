## [**1.2.2.2**] - 2026-02-04

tool connection layer Server Reliability - Fixes 5 critical bugs in Spec Kit Memory tool connection layer server improving embedding reliability and error handling. This release focuses on making the environment easier to use and easier to trust.

### 1. Critical Bug Fixes
- Need: This part of the release improves how the environment saves, finds, and uses context.
- What changed: Background Retry Job: Now starts automatically on tool connection layer startup (was never called). Dimension Validation: Detects provider/database dimension mismatch on startup. Fallback Warmup: Handles warmup errors gracefully in factory.js.
- Why it matters: That matters because saved context is easier to find, easier to trust, and less likely to get in the way.

### 2. ClickUp Tool Connection Layer Config
- Need: This part of the release improves how the environment saves, finds, and uses context.
- What changed: ClickUp Tool Connection Layer Config: Simplified.utcp_config.json.
- Why it matters: That matters because saved context is easier to find, easier to trust, and less likely to get in the way.
