## [**1.0.0.6**] - 2025-12-29

Enhanced /create Command Security - Strengthened @write agent enforcement with HARD BLOCK validation and prerequisite checks for all skill creation operations. This release focuses on making the environment easier to use and easier to trust.

### Breaking
- Need: This part of the release makes agent behavior more consistent across the supported runtimes.
- What changed: HARD BLOCK enforcement for @write agent on /create commands. Prompt prefix requirement now strictly validated. Prerequisite check validation gates all skill operations.
- Why it matters: That matters because the same instructions now behave more predictably in different runtimes.

### New
- Need: This part of the release makes the day-to-day workflow easier to follow and less error-prone.
- What changed: Validation command for skill creation workflow.
- Why it matters: That matters because the workflow is easier to use correctly without extra guesswork.


---

## Breaking Changes

- HARD BLOCK enforcement for @write agent on /create commands.
- Prompt prefix requirement now strictly validated.
- Prerequisite check validation gates all skill operations.


---

## Upgrade

- Ensure all /create commands use proper @write agent prefix and pass prerequisite validation checks.
