## [**2.0.0.1**] - 2026-02-11

This release updates the OpenCode environment with work on fixes and corrections, cleanup and updates, documentation. The goal is to make the platform clearer, more reliable, and easier to use day to day.

### 1. Fixes And Corrections
- Need: This part of the release improves how the environment saves, finds, and uses context.
- What changed: Skill_advisor.py Dual-Threshold Bug: command-line interface --threshold flag was applying dual-threshold validation (confidence + uncertainty) instead of confidence-only filtering. Fixed to use simple confidence threshold when --threshold is explicitly set. Constitutional Memory References: 2 broken file paths in constitutional/README.md fixed (save_workflow.md, trigger_config.md). Stale file listing updated to include speckit-exclusivity.md. Dead Install Guide Links: 3 dead links in mcp-figma and 1 in mcp-code-mode redirected to local INSTALL_GUIDE.md. mcp-code-mode INSTALL_GUIDE: 21 dot-vs-underscore naming patterns fixed.
- Why it matters: That matters because saved context is easier to find, easier to trust, and less likely to get in the way.

### 2. Cleanup And Updates
- Need: This part of the release improves how the environment saves, finds, and uses context.
- What changed: Sensitive Content Scrubbed: 200+ anobel.com domain references replaced with example.com across 23 files in workflows-code--web-dev. Personal paths, names, and domain references removed from PUBLIC_RELEASE.md, AGENTS.md, CHANGELOG.md, and 2 command files. Version Alignment: mcp-code-mode SKILL.md version corrected 1.2.0 → 1.1.0.3. mcp-figma SKILL.md version corrected 1.0.0 → 1.1.0.1. Root README.md Accuracy: Skill count 8 → 9, script count 244 → 90, removed phantom /search:* commands, tool connection layer servers 4 → 3, database path corrected.
- Why it matters: That matters because saved context is easier to find, easier to trust, and less likely to get in the way.

### 3. Documentation
- Need: This part of the release makes the written guidance easier to follow and easier to trust.
- What changed: Build Documentation Added: "Building from Source" section added to root README.md explaining npm install andand npm run build for first-time users (compiled dist/ directories are gitignored).
- Why it matters: That matters because people can understand the system faster and use it more confidently.
