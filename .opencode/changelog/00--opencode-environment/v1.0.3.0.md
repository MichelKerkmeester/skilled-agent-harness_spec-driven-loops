## [**1.0.3.0**] - 2026-01-04

Dual Specs Location Support - Memory files and spec folders can now be stored in either specs/ or.opencode/specs/ for flexible project organization. This release focuses on making the environment easier to use and easier to trust.

### 1. Dual Specs Location Support
- Need: This part of the release introduces the main capabilities added in this version.
- What changed: Dual Specs Location Support: Both specs/ (project root) and.opencode/specs/ now supported for spec folders and memory files.
- Why it matters: That matters because the environment can do more useful work without extra setup.

### 2. Precedence Rule
- Need: This part of the release introduces the main capabilities added in this version.
- What changed: Precedence Rule: Project root location takes precedence if both exist.
- Why it matters: That matters because the environment can do more useful work without extra setup.

### 3. Changes
- Need: This part of the release improves how the environment saves, finds, and uses context.
- What changed: Path Validation Updated: context-server.js handles dual locations. Directory Scanning Enhanced: memory-parser.js scans both specs/ and.opencode/specs/ directories. Context Generation Flexible: generate-context.js supports spec folder in either location.
- Why it matters: That matters because saved context is easier to find, easier to trust, and less likely to get in the way.

### 4. Fixes
- Need: This part of the release makes the written guidance easier to follow and easier to trust.
- What changed: Cross-repo Shortcut Link Corrected: INSTALL_GUIDE.md in Public repo now points locally instead of to external project.
- Why it matters: That matters because people can understand the system faster and use it more confidently.

## Upgrade

- No action required. Existing specs/ folders continue to work unchanged.
