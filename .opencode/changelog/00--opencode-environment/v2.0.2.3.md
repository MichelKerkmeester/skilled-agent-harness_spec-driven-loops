## [**2.0.2.3**] - 2026-02-15

Relocated node_modules from workspace root into mcp_server/ where dependencies belong -- 12+ files updated, npm install-strategy=nested adopted (spec 120). This release focuses on making the environment easier to use and easier to trust.

### Node Modules Relocation
- Need: This part of the release fixes rough edges and makes the environment more dependable.
- What changed: .npmrc: NEW -- install-strategy=nested disables workspace hoisting. Package.json (root): Removed redundant better-sqlite3 from root deps. Mcp_server/package.json: Added sqlite-vec-darwin-arm64 as optionalDependency for nested installs.
- Why it matters: That matters because the environment is easier to maintain and less likely to break in everyday use.

### Documentation Path Updates
- Need: This part of the release improves how the environment saves, finds, and uses context.
- What changed: Mcp_server/INSTALL_GUIDE.md: All system-spec-kit/node_modules/ -> mcp_server/node_modules/. Install_guides/README.md: HuggingFace cache path corrected. Install-spec-kit-memory.sh: Install/rebuild/verify commands target mcp_server/.
- Why it matters: That matters because saved context is easier to find, easier to trust, and less likely to get in the way.

### Source Code Updates
- Need: This part of the release fixes rough edges and makes the environment more dependable.
- What changed: Scripts/spec-folder/folder-detector.ts: better-sqlite3 require path -> mcp_server/node_modules/. Scripts/tests/test-folder-detector-functional.js: Test paths updated to match.
- Why it matters: That matters because the environment is easier to maintain and less likely to break in everyday use.

### Verification
- Need: This part of the release improves how the environment saves, finds, and uses context.
- What changed: Tool Connection Layer Server Starts: 1142/1142 entries valid, embedding dimension 1024. sqlite-vec loads correctly (platform package at sibling level). TypeScript build succeeds.
- Why it matters: That matters because saved context is easier to find, easier to trust, and less likely to get in the way.


---

## Upgrade

- No action required. Pull latest to get relocated dependencies. If running a linked project, restart the tool connection layer server.
