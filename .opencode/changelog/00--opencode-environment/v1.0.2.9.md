## [**1.0.2.9**] - 2026-01-02

Critical tool connection layer Server Fixes - Resolves import naming mismatches preventing Spec Kit Memory operations. This release focuses on making the environment easier to use and easier to trust.

### Critical Import Naming Fixes
- Need: This part of the release improves how the environment saves, finds, and uses context.
- What changed: GetDatabasePath Mismatch - GetDatabasePath → Get_database_path In Mcp_server/context-server.js: 1454 (health check response) and mcp_server/lib/vector-index.js:100 (database path resolution). ValidateFilePath Mismatch - ValidateFilePath → Validate_file_path In Mcp_server/context-server.js: 55 and mcp_server/lib/vector-index.js:13. Error Handler Mismatches - IsTransientError / UserFriendlyError → Is_transient_error / User_friendly_error In Mcp_server/context-server.js: 214.
- Why it matters: That matters because saved context is easier to find, easier to trust, and less likely to get in the way.
