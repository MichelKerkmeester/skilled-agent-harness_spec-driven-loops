## [**2.1.3.3**] - 2026-02-18

This release updates the OpenCode environment with work on explicit scan scope in retry example, reduced operator ambiguity, memory command alignment. The goal is to make the platform clearer, more reliable, and easier to use day to day.

### 1. Explicit Scan Scope In Retry Example
- Need: This part of the release improves how the environment saves, finds, and uses context.
- What changed: Explicit Scan Scope In Retry Example: Updated the /memory:save full-folder retry snippet to pass includeSkillRefs: true, matching new workflows-code indexing behavior.
- Why it matters: That matters because saved context is easier to find, easier to trust, and less likely to get in the way.

### 2. Reduced Operator Ambiguity
- Need: This part of the release makes the written guidance easier to follow and easier to trust.
- What changed: Reduced Operator Ambiguity: Prevents false-negative expectations when validating skillRefFiles after restart and re-scan.
- Why it matters: That matters because people can understand the system faster and use it more confidently.

### 3. Memory Command Alignment
- Need: This part of the release improves how the environment saves, finds, and uses context.
- What changed: Memory Command Alignment: /memory:save docs now match /memory:manage scan source-scope semantics for skill references/ and assets/ indexing.
- Why it matters: That matters because saved context is easier to find, easier to trust, and less likely to get in the way.

## Files Changed

| File or Area | Note |
| --- | --- |
| `.opencode/command/memory/save.md` | Updated in this release. |
| `.opencode/changelog/04--commands/v2.0.4.1.md` | Updated in this release. |

## Upgrade

- No action required. Pull latest to get the clarified memory re-index command example and release metadata updates.
