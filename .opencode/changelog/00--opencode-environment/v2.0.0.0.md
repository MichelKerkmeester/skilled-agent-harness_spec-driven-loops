## [**2.0.0.0**] - 2026-02-11

This release updates the OpenCode environment with work on new, changed, fixed. The goal is to make the platform clearer, more reliable, and easier to use day to day.

### 1. New
- Need: This part of the release improves how the environment saves, finds, and uses context.
- What changed: TypeScript Infrastructure: tsconfig.json with project references for 3 workspaces (shared/, mcp_server/, scripts/), shared/types.ts central type definitions, sqlite-vec.d.ts native extension declarations. CommonJS output preserves backward compatibility. 1,589 New Tests (spec 100): Achieved 100% module export coverage across 26 test files, including 48 full-text search SQL injection security tests. Test suite grew from ~700 to 3,872 tests across 114 test files. Memory Save Auto-Detect (spec 097) - 5-priority Detection Cascade For Spec Folder Resolution: explicit path → Gate 3 context → recent spec → cwd → ask user. Streamlines the memory save workflow.
- Why it matters: That matters because saved context is easier to find, easier to trust, and less likely to get in the way.

### 2. Changed
- Need: This part of the release improves how the environment saves, finds, and uses context.
- What changed: 186+ Source Files Converted (spec 092): All.js source files in shared/, mcp_server/, and scripts/ converted to TypeScript with full type annotations. 21 migration phases. CommonJS output unchanged - zero runtime behavior changes, all require() paths preserved. Type Safety Hardened - @ts-nocheck Removed From 96 Test Files With ~1,100 Type Errors Resolved (spec 103). Unsafe Casts Reduced From 48 → 3 (spec 099). 199: any remaining (tracked, non-critical). Documentation Accuracy (specs 106-107) - 20-agent Parallel Audit Identified ~120 Critical Issues. 303 Fixes Applied Across ~45 Files: phantom file references removed, stale counts updated, 39 mcp-narsil references purged, all import extensions corrected.
- Why it matters: That matters because saved context is easier to find, easier to trust, and less likely to get in the way.

### 3. Fixed
- Need: This part of the release improves how the environment saves, finds, and uses context.
- What changed: 85+ Bugs Fixed (spec 096) - 8 CRITICAL: memory decay scoring decay formula 18.45x error, macOS SQLite crash, database no-op writes. Plus 27 regression tests added. 12 Non-Functional Features Restored (spec 098): 10 highest-priority + 18 high-priority issues resolved across the tool connection layer server. Features that had silently broken during development restored to working state. Memory Index Reliability (spec 093): Indexing success rate improved from 129/244 → 240/245. 2 root-cause bugs fixed. memory decay scoring v4 decay formula corrected.
- Why it matters: That matters because saved context is easier to find, easier to trust, and less likely to get in the way.

### 4. Current State
- Need: This part of the release improves how the environment saves, finds, and uses context.
- What changed: Build: tsc --noEmit PASS (0 errors). Tests: 3,872 passing, 71 skipped, 114/118 suites (4 skipped). Source: 204.ts files, 0.js source files in tool connection layer server.
- Why it matters: That matters because saved context is easier to find, easier to trust, and less likely to get in the way.
