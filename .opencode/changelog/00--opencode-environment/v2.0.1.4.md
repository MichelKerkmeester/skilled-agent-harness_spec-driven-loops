# v2.0.1.4

> Part of [OpenCode Dev Environment](https://github.com/MichelKerkmeester/opencode-spec-kit-framework)

---

## [**2.0.1.4**] - 2026-02-14

**Create YAML Workflow Alignment + CHANGELOG Style Standardization** — Replaced `REFERENCE ONLY` headers in all **6 create YAML workflow files** with proper workflow title headers and standardized **10 CHANGELOG files** for consistent formatting.

---

## Highlights

### Create YAML Workflow Alignment

Replaced `REFERENCE ONLY` headers in all 6 create YAML workflow files with proper workflow title headers matching the spec_kit YAML convention. The create YAMLs now identify as executable workflows (`OPENCODE CREATE: [WORKFLOW] (CONFIRM MODE)`) rather than subordinate reference documents — consistent with how spec_kit YAMLs use `SMART SPECKIT: [WORKFLOW] ([MODE])`.

### CHANGELOG Style Standardization

Standardized **10 CHANGELOG files** across the project: removed emoji prefixes from section headers, unified repository link format, and applied bold version number formatting for consistency.

## Files Changed

**6 YAML workflows:** `create_skill.yaml` · `create_agent.yaml` · `create_skill_reference.yaml` · `create_skill_asset.yaml` · `create_install_guide.yaml` · `create_folder_readme.yaml` in `.opencode/command/create/assets/`

**10 CHANGELOGs:** `CHANGELOG.md` (root) · `.opencode/scripts/CHANGELOG.md` · 8 skill CHANGELOGs in `.opencode/skill/*/CHANGELOG.md`

## Upgrade

Drop-in replacement. Copy `.opencode/command/create/assets/` to your project.
