## [**2.0.1.4**] - 2026-02-14

This release updates the OpenCode environment with work on create structured config files workflow alignment, changelog style standardization. The goal is to make the platform clearer, more reliable, and easier to use day to day.

### 1. Create Structured Config Files Workflow Alignment
- Need: This part of the release makes the written guidance easier to follow and easier to trust.
- What changed: Replaced REFERENCE ONLY headers in all 6 create structured config files workflow files with proper workflow title headers matching the spec_kit structured config files convention. The create YAMLs now identify as executable workflows (OPENCODE CREATE: [WORKFLOW] (CONFIRM MODE)) rather than subordinate reference documents - consistent with how spec_kit YAMLs use SMART SPECKIT: [WORKFLOW] ([MODE]).
- Why it matters: That matters because people can understand the system faster and use it more confidently.

### 2. CHANGELOG Style Standardization
- Need: This part of the release makes the written guidance easier to follow and easier to trust.
- What changed: Standardized 10 CHANGELOG files across the project: removed emoji prefixes from section headers, unified repository link format, and applied bold version number formatting for consistency.
- Why it matters: That matters because people can understand the system faster and use it more confidently.

## Upgrade

- Drop-in replacement. Copy.opencode/command/create/assets/ to your project.
