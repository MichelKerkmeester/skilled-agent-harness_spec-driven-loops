## [**1.0.2.3**] - 2025-12-31

Comprehensive Spec Kit and Memory System Audit - Test suite fixes, documentation improvements, and new Script Registry for dynamic script discovery. This release focuses on making the environment easier to use and easier to trust.

### Tests
- Need: This part of the release fixes rough edges and makes the environment more dependable.
- What changed: Test Fixtures Renamed: 51 fixtures updated to follow ###-short-name naming convention. Template Marker: Added SPECKIT_TEMPLATE_SOURCE marker to all test fixture frontmatter. Validation Rule: FRONTMATTER_VALID rule now skips template marker check for test-fixtures/ directory.
- Why it matters: That matters because the environment is easier to maintain and less likely to break in everyday use.

### New
- Need: This part of the release improves how the environment saves, finds, and uses context.
- What changed: Script Registry: scripts-registry.json - Centralized JSON config for all 14 scripts and 9 rules. Registry Loader: registry-loader.sh - command-line interface tool to query script registry. Test Fixtures: 41 new fixtures for comprehensive edge case coverage.
- Why it matters: That matters because saved context is easier to find, easier to trust, and less likely to get in the way.

### Changed
- Need: This part of the release improves how the environment saves, finds, and uses context.
- What changed: Memory_search Documentation: Clarified that query OR concepts parameter is REQUIRED. Check-frontmatter.sh: Now supports SKIP_TEMPLATE_CHECK=1 environment variable. Documentation Updates: AGENTS.md, SKILL.md, and memory_system.md updated with parameter requirements.
- Why it matters: That matters because saved context is easier to find, easier to trust, and less likely to get in the way.

### Removed
- Need: This part of the release improves how the environment saves, finds, and uses context.
- What changed: Deprecated: mcp_server/INSTALL_GUIDE.md (duplicate of install_guides/tool connection layer/tool connection layer - Spec Kit Memory.md).
- Why it matters: That matters because saved context is easier to find, easier to trust, and less likely to get in the way.
