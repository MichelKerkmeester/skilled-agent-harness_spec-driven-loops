## [**1.0.0.2**] - 2025-12-29

Comprehensive stabilization - 80+ bug fixes addressing checkpoints, file orphaning, path resolution, and transaction safety. This release focuses on making the environment easier to use and easier to trust.

### Core Fixes
- Need: This part of the release improves how the environment saves, finds, and uses context.
- What changed: Duplicate Checkpoint Entries: Resolution and cleanup. Orphaned File Detection: Now cascades delete for unreferenced files. Broken Skill References: Path validation added.
- Why it matters: That matters because saved context is easier to find, easier to trust, and less likely to get in the way.

### Architecture
- Need: This part of the release makes agent behavior more consistent across the supported runtimes.
- What changed: AGENTS.md Universalization: Fully removed project-specific patterns. Shortcut Link Migration: Converted to relative paths for portability.
- Why it matters: That matters because the same instructions now behave more predictably in different runtimes.


---

## Upgrade

- No action required. Pull latest.
