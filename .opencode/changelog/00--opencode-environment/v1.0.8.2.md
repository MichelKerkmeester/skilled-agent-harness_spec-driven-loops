## [**1.0.8.2**] - 2026-01-24

Comprehensive Test Suite - Adding 1,087 tests across 8 new test files covering cognitive memory, tool connection layer handlers, session learning, validation rules, and Five Checks Framework. This release focuses on making the environment easier to use and easier to trust.

### 1. Test Coverage Expansion
- Need: This part of the release improves how the environment saves, finds, and uses context.
- What changed: Test-session-learning.js: 161 tests for preflight/postflight handlers, Learning Index formula validation. Test-memory-handlers.js: 162 tests for memory_search, triggers, CRUD, save, and index operations. Test-cognitive-integration.js: 96 integration tests for cognitive memory subsystem pipeline.
- Why it matters: That matters because saved context is easier to find, easier to trust, and less likely to get in the way.

### 2. Bug Fixes
- Need: This part of the release improves how the environment saves, finds, and uses context.
- What changed: Memory-crud.js Import Mismatch: Changed isValidTier to is_valid_tier (snake_case) to match module exports.
- Why it matters: That matters because saved context is easier to find, easier to trust, and less likely to get in the way.

### 3. Documentation Updates
- Need: This part of the release improves how the environment saves, finds, and uses context.
- What changed: Mcp_server/tests/README.md: Updated to include 3 new test files (session-learning, memory-handlers, cognitive-integration). Scripts/tests/README.md: Updated to include 5 new test files, corrected count from 13 to 12.
- Why it matters: That matters because saved context is easier to find, easier to trust, and less likely to get in the way.
