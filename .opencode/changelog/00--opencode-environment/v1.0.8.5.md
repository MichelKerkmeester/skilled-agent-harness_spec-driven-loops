## [**1.0.8.5**] - 2026-01-26

Performance Patterns Series - Adding Phase 0: Research to workflows-code, 6 new performance references, AGENTS.md Section 9 for multi-stack code implementation, with all 27 documents verified and quality fixes applied. This release focuses on making the environment easier to use and easier to trust.

### 1. Phase 0: Research Stage
- Need: This part of the release improves how the environment saves, finds, and uses context.
- What changed: Pre-implementation Analysis: New optional phase in workflows-code for complex performance work. 10-agent Methodology: Reference for parallel analysis covering HTML, JS, CSS, third-party, LCP, animations, network. Research-first Approach: Prevents "fix symptoms, miss root cause" anti-pattern.
- Why it matters: That matters because saved context is easier to find, easier to trust, and less likely to get in the way.

### 2. Performance References (6 New Files)
- Need: This part of the release makes agent behavior more consistent across the supported runtimes.
- What changed: Cwv_remediation.md: LCP safety timeout (3s), FCP preconnects, TBT requestIdleCallback, CLS prevention. Resource_loading.md: Preconnect with crossorigin, async CSS (onload="this.rel='stylesheet'"), script defer/async. Webflow_constraints.md: TypeKit sync loading, jQuery auto-injection, CSS generation limits, workarounds table.
- Why it matters: That matters because the same instructions now behave more predictably in different runtimes.

### 3. AGENTS.md Enhancement
- Need: This part of the release makes agent behavior more consistent across the supported runtimes.
- What changed: Section 9: CODE IMPLEMENTATION: New dedicated section for workflows-code guidance. Multi-stack Examples: Detection markers table (Go, Node.js, Python, Angular, React Native, DevOps). Stack-specific Verification: Commands table with go test, npm test, pytest, ng test.
- Why it matters: That matters because the same instructions now behave more predictably in different runtimes.

### 4. Quality Fixes (27 Files)
- Need: This part of the release fixes rough edges and makes the environment more dependable.
- What changed: Highest-priority: validation_patterns.js: 44 methods + 45 variables converted camelCase → snake_case. High-priority: BEM convention: Fixed.block--element →.block__element in code_quality_checklist.md. High-priority: Broken links: Fixed./performance_patterns.md →../implementation/performance_patterns.md.
- Why it matters: That matters because the environment is easier to maintain and less likely to break in everyday use.
