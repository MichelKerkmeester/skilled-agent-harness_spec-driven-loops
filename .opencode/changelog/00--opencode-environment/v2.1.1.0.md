# v2.1.1.0

> Part of [OpenCode Dev Environment](https://github.com/MichelKerkmeester/opencode-spec-kit-framework)

---

## [**2.1.1.0**] - 2026-02-16

**Single-database routing across OpenCode/Claude/VSCode/Codex configurations** -- Environment-level MCP configuration now pins `MEMORY_DB_PATH` to one canonical Spec Kit database path to prevent multi-file memory drift across clients and symlinked repositories.

> Spec folder: `N/A (operational config hardening)`

---

## Highlights

### Unified MCP Database Target

- **Pinned canonical DB path** -- Added `MEMORY_DB_PATH` in MCP configs so sessions consistently target `.../mcp_server/dist/database/context-index.sqlite`.
- **Cross-client alignment** -- Updated OpenCode, Claude, VSCode, and Codex MCP configuration notes to reference the same canonical runtime path.

### Install/Setup Documentation Alignment

- **Install guidance updated** -- Updated install guide scripts/docs so generated MCP config uses the same canonical database path by default.
- **Troubleshooting path updates** -- Updated install-guide memory troubleshooting examples to check canonical DB path and current table naming (`memory_index`).

## Files Changed

| File | Action |
|------|--------|
| `opencode.json` | Modified |
| `.claude/mcp.json` | Modified |
| `.vscode/mcp.json` | Modified |
| `.codex/config.toml` | Modified |
| `.opencode/install_guides/install_scripts/install-spec-kit-memory.sh` | Modified |
| `.opencode/install_guides/README.md` | Modified |

**Total: 6 files modified**

## Upgrade

Restart active OpenCode/Claude/VSCode/Codex sessions to reload MCP environment variables and pick up the unified DB path.
