## [**1.3.2.0**] - 2026-02-11

Governance and routing - Established @speckit exclusivity rule for spec folder documentation, added orchestrator parallel ceiling, renamed @context_loader → @context, and reduced @context dispatch limits. This release focuses on making the environment easier to use and easier to trust.

### 1. @speckit Exclusivity Rule - New Enforcement System Ensuring ONLY @speckit Can Create/write Documentation (*.md) Inside Spec Folders. Constitutional Memory #280 Created With 29 Trigger Phrases. Enforcement Across 5 Layers
- Need: This part of the release improves how the environment saves, finds, and uses context.
- What changed: @speckit Exclusivity Rule - New Enforcement System Ensuring ONLY @speckit Can Create/write Documentation (*.md) Inside Spec Folders. Constitutional Memory #280 Created With 29 Trigger Phrases. Enforcement Across 5 Layers: constitutional memory, orchestrate.md, AGENTS.md, individual agent files, and skill documentation. Four exceptions: memory/ (uses generate-context.js), scratch/ (any agent), handover.md (@handover only), research.md (@research only). 10 files modified across agents, skills, and framework docs.
- Why it matters: That matters because saved context is easier to find, easier to trust, and less likely to get in the way.

### 2. Orchestrator Default Parallel Ceiling - New Default Maximum Of 3 Agents Dispatched Simultaneously In Orchestrate.md. Previously Unlimited (up To 4). Ranges 4 - 9 And 10 - 20 Require Explicit User Override. Updated §13 (parallel Table + Ceiling Paragraph), §23 (summary), §25 (scaling Heuristics), §27 (scale Thresholds), §28 (collection Patterns). Escape Clause
- Need: This part of the release makes agent behavior more consistent across the supported runtimes.
- What changed: Orchestrator Default Parallel Ceiling - New Default Maximum Of 3 Agents Dispatched Simultaneously In Orchestrate.md. Previously Unlimited (up To 4). Ranges 4 - 9 And 10 - 20 Require Explicit User Override. Updated §13 (parallel Table + Ceiling Paragraph), §23 (summary), §25 (scaling Heuristics), §27 (scale Thresholds), §28 (collection Patterns). Escape Clause: "unless user explicitly requests more".
- Why it matters: That matters because the same instructions now behave more predictably in different runtimes.

### 3. @context_loader → @context Rename
- Need: This part of the release makes agent behavior more consistent across the supported runtimes.
- What changed: @context_loader → @context Rename: Agent file renamed from context_loader.md to context.md. Display name changed from "The Context Loader" to "The Context Agent". 78 references updated across 14 files - zero remaining references verified.
- Why it matters: That matters because the same instructions now behave more predictably in different runtimes.

### 4. @context Dispatch Limits Reduced - Reduced Maximum Dispatch Limits
- Need: This part of the release makes agent behavior more consistent across the supported runtimes.
- What changed: @context Dispatch Limits Reduced - Reduced Maximum Dispatch Limits: medium from 2→1, thorough from 3→2 (quick unchanged at 0). Escape clause added: "User can explicitly request higher limits".
- Why it matters: That matters because the same instructions now behave more predictably in different runtimes.
