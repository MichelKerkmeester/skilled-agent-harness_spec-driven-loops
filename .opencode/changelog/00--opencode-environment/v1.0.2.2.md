## [**1.0.2.2**] - 2025-12-31

Security Patch and Documentation - Critical DoS vulnerability fix in query string parser with comprehensive tool connection layer tool reference documentation. This release focuses on making the environment easier to use and easier to trust.

### 1. HIGH (CVE-2025-15284)
- Need: This part of the release improves how the environment saves, finds, and uses context.
- What changed: HIGH (CVE-2025-15284): Fixed DoS vulnerability in qs query string parser (6.14.0 → 6.14.1) - arrayLimit bypass via bracket notation allowed memory exhaustion. Transitive dependency through express@5.2.1.
- Why it matters: That matters because saved context is easier to find, easier to trust, and less likely to get in the way.

### 2. Resume.md Command
- Need: This part of the release improves how the environment saves, finds, and uses context.
- What changed: Resume.md Command: Added 8 missing tool connection layer tools to Section 6 (tool connection layer Tool Usage) - Memory tools: memory_delete, memory_update, memory_validate, memory_index_scan, memory_health - Checkpoint tools: checkpoint_create, checkpoint_list, checkpoint_delete.
- Why it matters: That matters because saved context is easier to find, easier to trust, and less likely to get in the way.

### 3. Tool Connection Layer Tool Examples
- Need: This part of the release improves how the environment saves, finds, and uses context.
- What changed: Tool Connection Layer Tool Examples: Added example invocations for all new tool connection layer tools.
- Why it matters: That matters because saved context is easier to find, easier to trust, and less likely to get in the way.

## Files Changed

| File or Area | Note |
| --- | --- |
| `package.json (qs dependency bumped)` | Updated in this release. |
| `package-lock.json (dependencies updated)` | Updated in this release. |
| `commands/spec_kit/resume.md (documentation)` | Updated in this release. |

## Upgrade

- Apply immediately. This addresses a HIGH severity DoS vulnerability in a transitive dependency.
