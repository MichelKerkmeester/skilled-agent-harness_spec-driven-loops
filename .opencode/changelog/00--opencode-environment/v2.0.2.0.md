## [**2.0.2.0**] - 2026-02-14

This release updates the OpenCode environment with work on agent routing compliance, test suite, 2.0.2.0. The goal is to make the platform clearer, more reliable, and easier to use day to day.

### 1. Agent Routing Compliance
- Need: This part of the release improves how the environment saves, finds, and uses context.
- What changed: Create Commands (18 Files): @speckit for spec folder creation (Rule 5), @context for discovery (Rule 4), @review for quality gates (Section 3). 204/204 verification checks passed. Spec_kit Commands (15 Files): @debug at failure >= 3, @research on confidence < 60%, dual-phase @review (Mode 2 + Mode 4), @handover at session end. Key Decisions: @review non-blocking on create commands; scope narrowed from 12 to 4 files for spec folder routing after grep audit.
- Why it matters: That matters because saved context is easier to find, easier to trust, and less likely to get in the way.

### 2. Test Suite
- Need: This part of the release improves how the environment saves, finds, and uses context.
- What changed: 18 Tests: 7 happy paths + 11 error/edge cases across plan, implement, complete, research, debug, resume, handover. Architecture: Orchestrator dispatches 7 test agents in 3 batches, each simulating structured config files workflow execution with scripted inputs. All 18/18 Passed: Artifact generation, quality gates, error handling, agent routing all verified.
- Why it matters: That matters because saved context is easier to find, easier to trust, and less likely to get in the way.

### 3. 2.0.2.0
- Need: This part of the release makes agent behavior more consistent across the supported runtimes.
- What changed: 2.0.2.0: Command agent utilization audit (create + spec_kit + test suite).
- Why it matters: That matters because the same instructions now behave more predictably in different runtimes.

## Upgrade

- No action required. Pull latest to get agent routing compliance and test suite.
