## [**1.0.1.5**] - 2025-12-29

JavaScript Limitations - Documents JavaScript-specific Narsil limitations discovered during testing. This release focuses on making the environment easier to use and easier to trust.

### 1. Known Issues
- Need: This part of the release improves known issues.
- What changed: Call graph empty for JavaScript (tree-sitter limitation).
- Why it matters: That matters because it makes the environment easier to use and maintain.

### 2. Known Issues
- Need: This part of the release fixes rough edges and makes the environment more dependable.
- What changed: Security scan limited (backend-focused rules).
- Why it matters: That matters because the environment is easier to maintain and less likely to break in everyday use.

### 3. Known Issues
- Need: This part of the release improves how the environment saves, finds, and uses context.
- What changed: Neural search stale after index clear.
- Why it matters: That matters because saved context is easier to find, easier to trust, and less likely to get in the way.

### 4. Working
- Need: This part of the release improves working.
- What changed: find_symbols for symbol discovery. get_symbol_definition for definitions. get_file for file content.
- Why it matters: That matters because it makes the environment easier to use and maintain.
