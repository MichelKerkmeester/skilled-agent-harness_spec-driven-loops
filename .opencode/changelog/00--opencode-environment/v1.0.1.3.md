## [**1.0.1.3**] - 2025-12-29

Narsil HTTP and React Frontend - Interactive code graph visualization with HTTP backend server and React frontend. This release focuses on making the environment easier to use and easier to trust.

### 1. HTTP Backend
- Need: This part of the release improves http backend.
- What changed: HTTP Backend: Server (port 3000) for graph data.
- Why it matters: That matters because it makes the environment easier to use and maintain.

### 2. React Frontend
- Need: This part of the release improves react frontend.
- What changed: React Frontend: Interactive visualization (port 5173).
- Why it matters: That matters because it makes the environment easier to use and maintain.

### 3. Graph Views - Five Types
- Need: This part of the release improves graph views - five types.
- What changed: Graph Views - Five Types: import · call · symbol · hybrid · flow.
- Why it matters: That matters because it makes the environment easier to use and maintain.

### 4. Fixed
- Need: This part of the release makes the written guidance easier to follow and easier to trust.
- What changed: Tool names corrected in documentation.
- Why it matters: That matters because people can understand the system faster and use it more confidently.

### 5. Language Count
- Need: This part of the release fixes rough edges and makes the environment more dependable.
- What changed: Language Count: 16 → 15.
- Why it matters: That matters because the environment is easier to maintain and less likely to break in everyday use.
