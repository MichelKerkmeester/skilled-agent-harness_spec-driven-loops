## [**2.0.0.7**] - 2026-02-12

This release updates the OpenCode environment with work on architecture and modularization, new modules and utilities, bug fixes and hardening. The goal is to make the platform clearer, more reliable, and easier to use day to day.

### Architecture And Modularization
- Need: This part of the release improves how the environment saves, finds, and uses context.
- What changed: Compose.sh Dynamic Templates: Replaced all 12 hardcoded L3/L3+ heredoc composition paths with dynamic fragment assembly from templates/addenda/. 1030→750 lines of content (-27%). Zero heredocs remain. compose.sh --verify validates all fragment paths at runtime. Create.sh Modularization: Extracted 3 shell libraries (shell-common.sh, git-branch.sh, template-utils.sh) + 5 externalized sharded templates. 928→566 lines of content (-39%). validate.sh also wired to shared libs. Workflow.ts Module Extraction - Extracted 4 Focused Modules: quality-scorer.ts (122 lines of content), topic-extractor.ts (88 lines of content), file-writer.ts (33 lines of content), memory-indexer.ts (159 lines of content). 865→495 lines of content (-43%).
- Why it matters: That matters because saved context is easier to find, easier to trust, and less likely to get in the way.

### New Modules And Utilities
- Need: This part of the release introduces the main capabilities added in this version.
- What changed: Shared/utils/jsonc-strip.ts: Shared JSONC comment-stripping utility (95 lines of content). config.ts and content-filter.ts now use shared utility instead of inline regex. Fixed latent bug in content-filter.ts naive regex that could corrupt strings containing //. Shell Libraries: scripts/lib/shell-common.sh (54 lines of content), scripts/lib/git-branch.sh (134 lines of content), scripts/lib/template-utils.sh (80 lines of content). Template Fragments: 5 addenda fragments (level3-arch/ prefix/suffix/guidance, level3plus-govern/ suffix/guidance) and 5 sharded templates (spec-index.md, 01-overview.md, 02-requirements.md, 03-architecture.md, 04-testing.md).
- Why it matters: That matters because the environment can do more useful work without extra setup.

### Bug Fixes And Hardening
- Need: This part of the release improves how the environment saves, finds, and uses context.
- What changed: Sed/grep POSIX Portability - 5 GNU-specific Fixes: 3 sed 's/-\+/-/g' → sed 's/--*/-/g' (BSD treats \+ as literal), 1 fragile sed i\ rewrite to printf+sed, 1 grep -P '\b' → grep -w (POSIX compliant). Code Quality Hardening - 11 Untyped Catch (e) → Catch (e: unknown) with proper instanceof Error guards, 8 non-null assertions (!) replaced with explicit null checks across 10 files. IsMemoryFile README Bug: isSkillReadme check in memory-parser.ts incorrectly matched README.md files inside constitutional/ directories. Added!normalizedPath.includes('/constitutional/') guard ---.
- Why it matters: That matters because saved context is easier to find, easier to trust, and less likely to get in the way.

### Verification
- Need: This part of the release fixes rough edges and makes the environment more dependable.
- What changed: Build: tsc --build --force PASS (0 errors) Tests: 3,872 passing, 0 failures Shell: All scripts pass bash -n syntax check.
- Why it matters: That matters because the environment is easier to maintain and less likely to break in everyday use.

### Verification
- Need: This part of the release fixes rough edges and makes the environment more dependable.
- What changed: -.
- Why it matters: That matters because the environment is easier to maintain and less likely to break in everyday use.
