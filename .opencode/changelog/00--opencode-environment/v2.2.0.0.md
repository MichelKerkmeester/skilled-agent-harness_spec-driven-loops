## [**2.2.0.0**] - 2026-02-20

This release updates the OpenCode environment with work on testing, retrieval and ranking, auditability and observability. The goal is to make the platform clearer, more reliable, and easier to use day to day.

### Testing
- Need: This part of the release fixes rough edges and makes the environment more dependable.
- What changed: Deferred Suites Activated: api-key-validation, api-validation, and lazy-loading moved from deferred placeholders to active tests. Targeted Verification: 3 passed files, 15 passed tests, 0 skipped. All-features Verification: 142 passed files (142 total), 4415 passed tests, 19 skipped (4434 total).
- Why it matters: That matters because the environment is easier to maintain and less likely to break in everyday use.

### Retrieval And Ranking
- Need: This part of the release improves how the environment saves, finds, and uses context.
- What changed: Typed Retrieval Trace Shipped: all retrieval responses now include a typed ContextEnvelope with per-stage trace and degraded-mode contract metadata. Artifact-aware Routing Shipped: the engine now classifies and routes across 9 artifact classes with deterministic strategy tables. Adaptive Hybrid Fusion Shipped: intent-aware fusion profiles for 7 task intents (fix_bug, add_feature, understand, refactor, security_audit, find_spec, find_decision) with dark-run comparison support.
- Why it matters: That matters because saved context is easier to find, easier to trust, and less likely to get in the way.

### Auditability And Observability
- Need: This part of the release improves how the environment saves, finds, and uses context.
- What changed: Append-only Mutation Ledger Shipped: memory mutations are recorded through immutable SQLite-trigger enforcement and hash-chain linkage. Extended Telemetry Shipped: retrieval now tracks latency, mode, fallback, and quality-proxy dimensions for search/context flows.
- Why it matters: That matters because saved context is easier to find, easier to trust, and less likely to get in the way.

### Hardening
- Need: This part of the release improves how the environment saves, finds, and uses context.
- What changed: Extraction/provenance Hardening Shipped: deterministic extraction memory_id fallback, bounded causal-seed input, and canonical boost import-path normalization. Configuration Safety Hardening Shipped: cognitive config parsing moved to Zod validation and LRU eviction ordering is deterministic under load. Embedding-provider Drift Closure: legacy lib/interfaces/embedding-provider path usage is now historical only, with active tests aligned to shared contracts.
- Why it matters: That matters because saved context is easier to find, easier to trust, and less likely to get in the way.

### Documentation
- Need: This part of the release improves how the environment saves, finds, and uses context.
- What changed: Root Spec Evidence Synchronized: Updated test-results.md, implementation-summary.md, and checklist.md in spec 136 with current totals and closure state. Addendum Closure Finalized: Updated package 007-documentation-alignment status and evidence records. CHK-336 Closed: Added "Last Verified" rows to module-count/stat tables in system-spec-kit skill and tool connection layer documentation.
- Why it matters: That matters because saved context is easier to find, easier to trust, and less likely to get in the way.

### Versioning
- Need: This part of the release makes the written guidance easier to follow and easier to trust.
- What changed: Component Changelog Added:.opencode/changelog/01--system-spec-kit/v2.2.24.0.md. Skill Version Bump:.opencode/skill/system-spec-kit/SKILL.md from 2.2.23.0 to 2.2.24.0.
- Why it matters: That matters because people can understand the system faster and use it more confidently.

### Delivery And Governance
- Need: This part of the release fixes rough edges and makes the environment more dependable.
- What changed: Dark-launch Verification Completed: baseline path validated with feature flags disabled (99.65% pass rate, 15 expected cognitive-feature failures). Staged Rollout Completed: 10% -> 50% -> 100% gates completed with monotonic failure reduction (20 -> 7 -> 1). Governance Sign-off Completed: technical, data, and product approval packets closed, with 13 success criteria passing and 23 requirements mapped to evidence.
- Why it matters: That matters because the environment is easier to maintain and less likely to break in everyday use.


---

## Files Changed

| File or Area | Note |
| --- | --- |
| `.opencode/skill/system-spec-kit/mcp_server/tests/api-key-validation.vitest.ts` | Updated in this release. |
| `.opencode/skill/system-spec-kit/mcp_server/tests/api-validation.vitest.ts` | Updated in this release. |
| `.opencode/skill/system-spec-kit/mcp_server/tests/lazy-loading.vitest.ts` | Updated in this release. |
| `.opencode/skill/system-spec-kit/mcp_server/lib/{contracts,search,storage,telemetry,extraction,cognitive,config}/**` | Updated in this release. |
| `.opencode/skill/system-spec-kit/mcp_server/handlers/{memory-search,memory-context,memory-save}.ts` | Updated in this release. |
| `.opencode/specs/02--system-spec-kit/020-mcp-working-memory-hybrid-rag/*.md` | Updated in this release. |
| `.opencode/specs/02--system-spec-kit/020-mcp-working-memory-hybrid-rag/007-documentation-alignment/*.md` | Updated in this release. |
| `.opencode/skill/system-spec-kit/{SKILL.md,README.md}` | Updated in this release. |
| `.opencode/skill/system-spec-kit/mcp_server/{README.md,lib/README.md}` | Updated in this release. |
| `.opencode/changelog/01--system-spec-kit/v2.2.24.0.md` | Updated in this release. |
| `.opencode/changelog/00--opencode-environment/v2.2.0.0.md` | Updated in this release. |


---

## Upgrade

- No migration is required. Pull latest to get the activated suite coverage and updated verification/release documentation.
