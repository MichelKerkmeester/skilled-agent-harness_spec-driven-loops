## [**1.0.1.0**] - 2025-12-29

Complete Narsil Migration - Replaces LEANN with Narsil for unified code intelligence. This release focuses on making the environment easier to use and easier to trust.

### 1. Breaking Changes
- Need: This part of the release improves how the environment saves, finds, and uses context.
- What changed: LEANN Completely Removed: Migration path required. API Changes: leann_leann_search() → narsil.narsil_neural_search(). Tool Connection Layer Transition: LEANN tool connection layer → Narsil (via Code Mode).
- Why it matters: That matters because saved context is easier to find, easier to trust, and less likely to get in the way.

### 2. New Capabilities
- Need: This part of the release improves how the environment saves, finds, and uses context.
- What changed: 76 Specialized Tools: Neural search, security scanning, call graph analysis. Unified Code Intelligence: Single interface for all search patterns. Multiple Embedding Backends: Flexible embedding strategy.
- Why it matters: That matters because saved context is easier to find, easier to trust, and less likely to get in the way.

## Files Changed

| File or Area | Note |
| --- | --- |
| `tool connection layer configuration (Narsil integration)` | Updated in this release. |
| `Skill definitions and routing` | Updated in this release. |
| `Search API implementations` | Updated in this release. |
| `Backend embedding configuration` | Updated in this release. |

## Breaking Changes

- LEANN Completely Removed: Migration path required.
- API Changes: leann_leann_search() → narsil.narsil_neural_search().
- Tool Connection Layer Transition: LEANN tool connection layer → Narsil (via Code Mode).
- Skill Updates: mcp-leann skill → mcp-narsil skill.
- Embedding Backends: MLX embeddings → Voyage/OpenAI/ONNX backends.
- Skill Count: Reduced from 8 to 7 (consolidated Narsil tooling).

## Upgrade

- Update opencode.json to remove LEANN.
- Update skills from mcp-leann → mcp-narsil.
- Update search calls: leann_leann_search() → narsil.narsil_neural_search().
- Test search functionality in your project.
