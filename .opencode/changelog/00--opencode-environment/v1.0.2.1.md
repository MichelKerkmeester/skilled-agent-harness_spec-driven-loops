## [**1.0.2.1**] - 2025-12-31

Comprehensive System Hardening - Critical embedding dimension fixes, security hardening, and performance optimizations for Spec Kit Memory system. This release focuses on making the environment easier to use and easier to trust.

### Fixed
- Need: This part of the release improves how the environment saves, finds, and uses context.
- What changed: Embedding Dimension Mismatch (Critical): Checkpoints now use dynamic dimension detection instead of hardcoded 768, fixing failures with Voyage (1024) and OpenAI (1536) providers. Dimension Detection (Critical): getEmbeddingDimension() now correctly detects dimension from provider, environment variables, or API keys before falling back to default. Memory Indexing Failures: Fixed dimension validation mismatches.
- Why it matters: That matters because saved context is easier to find, easier to trust, and less likely to get in the way.

### New
- Need: This part of the release improves how the environment saves, finds, and uses context.
- What changed: Embedding Cache: LRU cache (1000 entries) reduces redundant API calls. Shared Utilities: shared/utils.js consolidated validateFilePath and escapeRegex utilities. Parallel Embeddings: 3-5x faster with configurable concurrency (default: 5).
- Why it matters: That matters because saved context is easier to find, easier to trust, and less likely to get in the way.

### Changed
- Need: This part of the release fixes rough edges and makes the environment more dependable.
- What changed: Lib Consolidation: Shared modules moved to shared/ folder with re-export wrappers for backward compatibility. Path Portability: All hardcoded paths now use environment variables. Validator Cleanup: Deprecated JS validators removed (bash validators preferred).
- Why it matters: That matters because the environment is easier to maintain and less likely to break in everyday use.

### Security
- Need: This part of the release improves how the environment saves, finds, and uses context.
- What changed: CWE-22: Added path validation to command-line interface CONFIG.DATA_FILE. CWE-22: Added validateFilePath() checks for DB-stored paths. CWE-400: Added input length limits to tool connection layer handler parameters.
- Why it matters: That matters because saved context is easier to find, easier to trust, and less likely to get in the way.


---

## Files Changed

| File or Area | Note |
| --- | --- |
| `lib/memory/checkpoint-manager.js (critical fix)` | Updated in this release. |
| `lib/shared/utils.js (new)` | Updated in this release. |
| `lib/memory/parallel-embedder.js (performance)` | Updated in this release. |
| `scripts/validate/check-folder-naming.sh (new)` | Updated in this release. |
| `scripts/validate/check-frontmatter.sh (new)` | Updated in this release. |
| `Multiple SKILL.md files (documentation)` | Updated in this release. |


---

## Upgrade

- Recommended immediately for production systems. Fixes critical embedding dimension issues and security vulnerabilities.
