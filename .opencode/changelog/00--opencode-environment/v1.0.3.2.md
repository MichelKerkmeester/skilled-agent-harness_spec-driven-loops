## [**1.0.3.2**] - 2026-01-05

This release updates the OpenCode environment with work on embedded tool connection layer servers, new, changed. The goal is to make the platform clearer, more reliable, and easier to use day to day.

### 1. Embedded Tool Connection Layer Servers
- Need: This part of the release improves how the environment saves, finds, and uses context.
- What changed: Embedded Tool Connection Layer Servers: Narsil source in mcp-narsil/mcp_server/, Code Mode source in mcp-code-mode/mcp_server/.
- Why it matters: That matters because saved context is easier to find, easier to trust, and less likely to get in the way.

### 2. New
- Need: This part of the release makes the written guidance easier to follow and easier to trust.
- What changed: Environment Template -.env.example template with Code Mode prefixed variables documented.
- Why it matters: That matters because people can understand the system faster and use it more confidently.

### 3. Changed
- Need: This part of the release improves how the environment saves, finds, and uses context.
- What changed: Install Guides Updated: tool connection layer - Narsil.md · tool connection layer - Code Mode.md with prefixed variable documentation. Code Mode Install Guide - Added "CRITICAL: Prefixed Environment Variables" section. Narsil Install Guide: Added prefixed variable note in Neural Search Configuration.
- Why it matters: That matters because saved context is easier to find, easier to trust, and less likely to get in the way.

### 4. Documentation Gap
- Need: This part of the release makes the written guidance easier to follow and easier to trust.
- What changed: Documentation Gap: Code Mode requires {manual}_{VAR} format (e.g., narsil_VOYAGE_API_KEY).
- Why it matters: That matters because people can understand the system faster and use it more confidently.

### 5. Public Repo Configs
- Need: This part of the release fixes rough edges and makes the environment more dependable.
- What changed: Public Repo Configs: Removed hardcoded API keys and absolute paths.
- Why it matters: That matters because the environment is easier to maintain and less likely to break in everyday use.

## Upgrade

- Add prefixed variables to.env for Code Mode: ```bash # Standard variable VOYAGE_API_KEY=pa-your-key.
- # Code Mode prefixed version (REQUIRED) narsil_VOYAGE_API_KEY=pa-your-key ```.
- See.env.example for complete template.
