## [**2.1.3.1**] - 2026-02-18

tool connection layer post-update recovery hardening -- Hardened Spec Kit Memory installation and recovery workflows with a full guide rewrite, installer-script alignment, and validated end-to-end recovery checks for update-related failures. This release focuses on making the environment easier to use and easier to trust.

### 1. Installer Hardening
- Need: This part of the release improves how the environment saves, finds, and uses context.
- What changed: Installer Hardening:.opencode/install_guides/install_scripts/install-spec-kit-memory.sh now runs workspace-root install/build, supports automatic build fallback, performs native module health checks, and runs startup smoke testing.
- Why it matters: That matters because saved context is easier to find, easier to trust, and less likely to get in the way.

### 2. Native Recovery Path
- Need: This part of the release makes the written guidance easier to follow and easier to trust.
- What changed: Native Recovery Path: Automatic fallback path now guides users through native module rebuild and re-verification when health checks report failures.
- Why it matters: That matters because people can understand the system faster and use it more confidently.

### 3. Runtime Path Verification
- Need: This part of the release makes agent behavior more consistent across the supported runtimes.
- What changed: Runtime Path Verification: Installer verification now checks canonical DB path and reports compatibility shortcut link status.
- Why it matters: That matters because the same instructions now behave more predictably in different runtimes.

### 4. Install Guide Rewrite
- Need: This part of the release improves how the environment saves, finds, and uses context.
- What changed: Install Guide Rewrite:.opencode/install_guides/tool connection layer - Spec Kit Memory.md (backed by mcp_server/INSTALL_GUIDE.md) was rewritten to workflows-documentation standards.
- Why it matters: That matters because saved context is easier to find, easier to trust, and less likely to get in the way.

### 5. Root-cause Guidance
- Need: This part of the release makes the written guidance easier to follow and easier to trust.
- What changed: Root-cause Guidance: The guide now documents relocation as one contributing factor alongside workspace mismatch, native ABI mismatch, and DB-path confusion.
- Why it matters: That matters because people can understand the system faster and use it more confidently.

### 6. Operational Recovery Guidance
- Need: This part of the release makes agent behavior more consistent across the supported runtimes.
- What changed: Operational Recovery Guidance: Added clear post-update recovery and verification commands for OpenCode/Claude users.
- Why it matters: That matters because the same instructions now behave more predictably in different runtimes.

### 7. Spec Finalization
- Need: This part of the release makes the written guidance easier to follow and easier to trust.
- What changed: Spec Finalization: Completed Level 2 documentation in.opencode/specs/02--system-spec-kit/z_archive/018-mcp-issues-after-update/ with fully populated tasks/checklist/implementation summary and validator pass.
- Why it matters: That matters because people can understand the system faster and use it more confidently.

## Files Changed

| File or Area | Note |
| --- | --- |
| `.opencode/install_guides/install_scripts/install-spec-kit-memory.sh` | Updated in this release. |
| `.opencode/skill/system-spec-kit/mcp_server/INSTALL_GUIDE.md` | Updated in this release. |
| `.opencode/specs/02--system-spec-kit/z_archive/018-mcp-issues-after-update/spec.md` | Updated in this release. |
| `.opencode/specs/02--system-spec-kit/z_archive/018-mcp-issues-after-update/plan.md` | Updated in this release. |
| `.opencode/specs/02--system-spec-kit/z_archive/018-mcp-issues-after-update/tasks.md` | Updated in this release. |
| `.opencode/specs/02--system-spec-kit/z_archive/018-mcp-issues-after-update/checklist.md` | Updated in this release. |
| `.opencode/specs/02--system-spec-kit/z_archive/018-mcp-issues-after-update/implementation-summary.md` | Updated in this release. |
| `.opencode/changelog/01--system-spec-kit/v2.2.21.0.md` | Updated in this release. |

## Upgrade

- Pull latest from main.
- Re-run.opencode/install_guides/install_scripts/install-spec-kit-memory.sh if your tool connection layer runtime was impacted by a recent update.
- Restart your tool connection layer client (OpenCode/Claude Code) after installer completion.
