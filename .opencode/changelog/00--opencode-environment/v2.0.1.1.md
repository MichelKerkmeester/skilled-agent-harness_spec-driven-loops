## [**2.0.1.1**] - 2026-02-14

This release updates the OpenCode environment with work on security - command dispatch vulnerability fix, style - header alignment and standardization, fixed - agents.md audit and repository cleanup. The goal is to make the platform clearer, more reliable, and easier to use day to day.

### Security - Command Dispatch Vulnerability Fix
- Need: This part of the release improves how the environment saves, finds, and uses context.
- What changed: Imperative Guardrail Blocks: Added lines 7-18 to all 7 command files with explicit DO NOT dispatch agents directly instruction, creating a hard stop before any phantom dispatch text could be processed. Structured Config Files Loading Instructions At Top: Moved the actual "load structured config files and execute" instruction from lines 140-183 to the top of each command file, ensuring it's the first substantive instruction the LLM processes. Fenced Unfenced Dispatch Templates: 4 command files (complete, debug, research, plan) had raw dispatch template text that could be interpreted as live instructions. Wrapped in explicit code fences with REFERENCE ONLY labels.
- Why it matters: That matters because saved context is easier to find, easier to trust, and less likely to get in the way.

### Style - Header Alignment And Standardization
- Need: This part of the release makes the written guidance easier to follow and easier to trust.
- What changed: H2 Emoji Headers: Added emojis to all 112 H2 headers across 7 command files, aligning with workflows-documentation style standards. Decimal Step Renumbering: Renumbered ## 7.5 to ## 8. in plan.md (with subsequent sections renumbered), eliminating non-standard decimal step numbering.
- Why it matters: That matters because people can understand the system faster and use it more confidently.

### Fixed - AGENTS.md Audit And Repository Cleanup
- Need: This part of the release makes agent behavior more consistent across the supported runtimes.
- What changed: Skill_advisor.py Phantom References (B2): Fixed references to 2 removed skills (mcp-narsil, mcp-browser) that caused skill routing failures. Template Paths And Skill Table (B3): Corrected stale template paths in §9 and added 4 missing skills to the Available Skills table. Specs Removed From Tracking: All spec folders removed from git tracking (kept on disk, added to.gitignore), reducing repo size for public consumers.
- Why it matters: That matters because the same instructions now behave more predictably in different runtimes.


---

## Files Changed

| File or Area | Note |
| --- | --- |
| `7 command.md files in.opencode/command/spec_kit/` | Updated in this release. |
| `11 structured config files assets in.opencode/command/spec_kit/assets/` | Updated in this release. |
| `AGENTS.md` | Updated in this release. |
| `.gitignore` | Updated in this release. |
| `.agents/ (symlinks)` | Updated in this release. |


---

## Upgrade

- Drop-in replacement. Copy.opencode/command/spec_kit/ and updated files to your project.
- --.
