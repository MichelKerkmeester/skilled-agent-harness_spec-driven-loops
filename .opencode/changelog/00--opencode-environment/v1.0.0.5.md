## [**1.0.0.5**] - 2025-12-29

@write Agent Enforcement - Multi-layer enforcement for skill creation with HARD BLOCK validation and prompt prefix requirements. This release focuses on making the environment easier to use and easier to trust.

### 1. Breaking
- Need: This part of the release makes agent behavior more consistent across the supported runtimes.
- What changed: Skill creation now requires @write agent prefix (HARD BLOCK enforcement). Prerequisite checks enforce validation before skill operations.
- Why it matters: That matters because the same instructions now behave more predictably in different runtimes.

### 2. Changed
- Need: This part of the release makes the day-to-day workflow easier to follow and less error-prone.
- What changed: Quick Reference updated with CDN deployment workflow.
- Why it matters: That matters because the workflow is easier to use correctly without extra guesswork.

### 3. Changed
- Need: This part of the release makes the day-to-day workflow easier to follow and less error-prone.
- What changed: Quick Reference updated with JS minification workflow.
- Why it matters: That matters because the workflow is easier to use correctly without extra guesswork.

### 4. Changed
- Need: This part of the release improves changed.
- What changed: Code Mode examples now include Narsil.
- Why it matters: That matters because it makes the environment easier to use and maintain.

## Breaking Changes

- Skill creation now requires @write agent prefix (HARD BLOCK enforcement).
- Prerequisite checks enforce validation before skill operations.

## Upgrade

- Update your skill creation workflows to use @write agent prefix. Review updated Quick Reference for new CDN and minification workflows.
