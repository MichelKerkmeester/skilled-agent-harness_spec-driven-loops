## [**2.0.5.0**] - 2026-02-16

This release updates the OpenCode environment with work on 5-source indexing pipeline, 7 intent types, document-type scoring. The goal is to make the platform clearer, more reliable, and easier to use day to day.

### 5-Source Indexing Pipeline
- Need: This part of the release improves how the environment saves, finds, and uses context.
- What changed: 1: Spec Memories. 2: Constitutional Rules. 3: Skill READMEs.
- Why it matters: That matters because saved context is easier to find, easier to trust, and less likely to get in the way.

### 7 Intent Types
- Need: This part of the release makes the written guidance easier to follow and easier to trust.
- What changed: Find_spec: Spec documents, plans, decision records. Find_decision: Decision records, architectural context.
- Why it matters: That matters because people can understand the system faster and use it more confidently.

### Document-Type Scoring
- Need: This part of the release improves how the environment saves, finds, and uses context.
- What changed: Spec: 1.4x. Plan: 1.3x. Constitutional: 2.0x.
- Why it matters: That matters because saved context is easier to find, easier to trust, and less likely to get in the way.

### Documentation Alignment
- Need: This part of the release makes the written guidance easier to follow and easier to trust.
- What changed: All READMEs, SKILL.md and reference files updated to reflect post-126 state. Zero stale "4-source" or "5 intent" references remain.
- Why it matters: That matters because people can understand the system faster and use it more confidently.


---

## Files Changed

| File or Area | Note |
| --- | --- |
| `README.md` | Modified. |
| `.opencode/skill/system-spec-kit/README.md` | Modified. |
| `.opencode/skill/system-spec-kit/mcp_server/README.md` | Modified. |
| `.opencode/skill/system-spec-kit/SKILL.md` | Modified. |
| `3 Reference Files` | Modified. |
| `3 Sub-directory READMEs` | Modified. |


---

## Upgrade

- No action required. Pull latest. Spec 126 code changes require npm run build in.opencode/skill/system-spec-kit/.
