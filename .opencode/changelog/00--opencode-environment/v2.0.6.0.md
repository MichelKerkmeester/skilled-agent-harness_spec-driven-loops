## [**2.0.6.0**] - 2026-02-24

This release updates the OpenCode environment with work on agents.md / claude.md condensation, agents_full_stack.md (new). The goal is to make the platform clearer, more reliable, and easier to use day to day.

### 1. AGENTS.md / CLAUDE.md Condensation
- Need: This part of the release makes agent behavior more consistent across the supported runtimes.
- What changed: ASCII box gates → structured markdown headers with bullets and blockquotes. §3 (Consolidated Question Protocol) merged into §2 (Gates) as sub-section. §4-§6 Condensed: duplicated content replaced with references to system-spec-kit SKILL.md.
- Why it matters: That matters because the same instructions now behave more predictably in different runtimes.

### 2. AGENTS_FULL_STACK.md (New)
- Need: This part of the release makes agent behavior more consistent across the supported runtimes.
- What changed: Sk-code--full-stack: Stack detection (Go, Swift, React Native, React/Next.js, Node.js), 3-phase lifecycle, verification commands. Sk-git: Worktree, commit, finish sub-workflows. Same condensed formatting as base AGENTS.md.
- Why it matters: That matters because the same instructions now behave more predictably in different runtimes.

## Files Changed

| File or Area | Note |
| --- | --- |
| `AGENTS.md` | Modified (structural overhaul). |
| `CLAUDE.md` | Modified (synced with AGENTS.md). |
| `AGENTS_FULL_STACK.md` | Created. |

## Upgrade

- No migration required. Pull latest. Behavioral rules unchanged - formatting and deduplication only.
