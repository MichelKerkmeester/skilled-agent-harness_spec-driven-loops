## [**2.0.1.0**] - 2026-02-13

This release updates the OpenCode environment with work on memory system integration (spec 111), documentation alignment (specs 112-115), command and prompt optimization (specs 116-117). The goal is to make the platform clearer, more reliable, and easier to use day to day.

### 1. Memory System Integration (Spec 111)
- Need: This part of the release improves how the environment saves, finds, and uses context.
- What changed: 4-source Indexing Pipeline: Memory system now indexes from specFiles, constitutionalFiles, skillReadmes, and projectReadmes (was 2-source). README Content Discovery: findProjectReadmes() and findSkillReadmes() auto-discover READMEs with README_EXCLUDE_PATTERNS exclusion. Anchor Prefix Matching: Search anchor summary matches summary-049, summary-111 etc. with exact-match priority and shortest-prefix fallback.
- Why it matters: That matters because saved context is easier to find, easier to trust, and less likely to get in the way.

### 2. Documentation Alignment (Specs 112-115)
- Need: This part of the release improves how the environment saves, finds, and uses context.
- What changed: ~473 Anchor Tags: Embedded across 74 READMEs in.opencode/ for precise memory retrieval via Spec Kit Memory tool connection layer. README Template: readme_template.md reduced 1589→1058 lines (-33%), 16→14 sections, added 5 evolved patterns (badge shields, ASCII diagrams, innovation tables, before/after comparisons, anchor placement rules). Root README: Restructured 7→9 sections (756→1118 lines), enriched with tool connection layer tools, cognitive features, search architecture, usage examples. Embedded substantive content from tool connection layer server and system-spec-kit READMEs (+148 lines).
- Why it matters: That matters because saved context is easier to find, easier to trust, and less likely to get in the way.

### 3. Command And Prompt Optimization (Specs 116-117)
- Need: This part of the release makes agent behavior more consistent across the supported runtimes.
- What changed: Structured Config Files Reduction: 13 structured config files prompt assets 13,333→5,378 lines (-59.7%) using compact flow notation, removed Mermaid diagrams and decorative separators. Prompt Reduction: 7.md command files 3,746→2,239 lines (-40.2%) preserving all routing logic and workflow steps. /create: folder_readme: structured config files 765→611 lines (-20%), aligned 4 README types with canonical 9-section structure, fixed 10 alignment gaps, standardized emoji vocabulary.
- Why it matters: That matters because the same instructions now behave more predictably in different runtimes.

### 4. Testing And Validation
- Need: This part of the release improves how the environment saves, finds, and uses context.
- What changed: Test Suite: 4,037 tests across 120 test files (71 skipped = external API/embedding tests). Memory Integration: 49 new tests for anchor schema (prefix matching + ID simplification). Validation: 13/13 structured config files files parse valid, all step counts preserved, all asset path references intact ---.
- Why it matters: That matters because saved context is easier to find, easier to trust, and less likely to get in the way.
