## [**1.0.1.7**] - 2025-12-30

Enhanced tool connection layer Guides and Agent Creation - Comprehensive install guide improvements with H1 descriptions for all tool connection layer servers and new agent creation command. This release focuses on making the environment easier to use and easier to trust.

### Create Agent Command
- Need: This part of the release makes agent behavior more consistent across the supported runtimes.
- What changed: Create Agent Command: /create:agent with 5-phase workflow for agent creation.
- Why it matters: That matters because the same instructions now behave more predictably in different runtimes.

### Agent Template
- Need: This part of the release makes agent behavior more consistent across the supported runtimes.
- What changed: Agent Template: agent_template.md for consistent agent structure.
- Why it matters: That matters because the same instructions now behave more predictably in different runtimes.

### Tool Connection Layer Install Guides
- Need: This part of the release improves how the environment saves, finds, and uses context.
- What changed: Tool Connection Layer Install Guides: All guides now include detailed H1 descriptions.
- Why it matters: That matters because saved context is easier to find, easier to trust, and less likely to get in the way.

### Command Template
- Need: This part of the release makes the day-to-day workflow easier to follow and less error-prone.
- What changed: Command Template: Reduced 27% by removing duplication.
- Why it matters: That matters because the workflow is easier to use correctly without extra guesswork.


---

## Files Changed

| File or Area | Note |
| --- | --- |
| `commands/create/agent/create-agent.md (new)` | Updated in this release. |
| `commands/create/agent/agent_template.md (new)` | Updated in this release. |
| `All tool connection layer server install guides updated` | Updated in this release. |


---

## Upgrade

- No breaking changes. Safe to upgrade.
