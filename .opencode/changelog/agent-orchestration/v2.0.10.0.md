8 runtime-native agent definitions with optimized top-of-file metadata, role-tuned max_turns and timeout_mins, routing-optimized descriptions, and template-compliant body content. The main changes here are 8 runtime-native agents and role-tuned max_turns. The goal is to make the workflow easier to follow and easier to trust.

---

#### 8 Runtime-native Agents

This area needed a clearer and safer way to work. orchestrate, context, debug, research, review, speckit, write, handover. all with runtime command-line tool workflow config top-of-file metadata (kind, tools array, max_turns, timeout_mins). This makes the behavior more predictable and lowers the chance of mistakes.

---

#### Role-tuned Max_turns

This area needed a clearer and safer way to work. orchestrate=25, debug/research/speckit=20, review/write=15, context/handover=10. This makes the behavior more predictable and lowers the chance of mistakes.

---

#### Role-tuned Timeout_mins

This area needed a clearer and safer way to work. orchestrate=15, debug/research=15, speckit/write=10, context/review/handover=5. This makes the behavior more predictable and lowers the chance of mistakes.

---

#### Routing-optimized Descriptions

This part of the agent coordination guidance needed clearer guidance and less room for confusion. Each agent description rewritten to help the runtime's routing logic decide when to invoke. includes purpose, trigger conditions, output type, and boundary constraints. This makes the agent coordination guidance easier to follow.

---

#### Token Budget Analysis

This part of the agent coordination guidance needed clearer guidance and less room for confusion. All agents validated against the effective context limit. Per-agent budgets calculated: system prompt (3-13K) + overhead (15K) + per-turn cost (5-15K) = safe margins for all configured max_turns. This makes the agent coordination guidance easier to follow.

---

#### Tool Scoping

This part of the agent coordination guidance needed clearer guidance and less room for confusion. Tools matched to role. context/review are read-only (read_file, grep_search, list_directory); research/write include web_search; handover excludes run_shell_command; orchestrate has read_file only. This makes the agent coordination guidance easier to follow.

---

#### Path Convention Aligned

This part of the documentation needed to match the related files more closely. All agent bodies referenced the former runtime mirror as main path. This helps keep different copies of the same guidance from drifting apart.

---

#### Template Compliance

The writing in this area needed to be easier to read and follow. All 8 agents validated against workflows-documentation agent template. required sections (CORE WORKFLOW, CAPABILITY SCAN, OUTPUT VERIFICATION, ANTI-PATTERNS, RELATED RESOURCES, SUMMARY) confirmed present. This makes the documentation easier for non-developers to read.

---

## Files Changed

| File | Action | Notes |
| ---- | ------ | ----- |
| Former runtime mirrors for orchestrate, context, debug, and research | Updated | Updated in this release |
| Former runtime mirrors for review, speckit, write, and handover | Updated | Updated in this release |
| `Total: 8 files created/modified` | Updated | Updated in this release |

---

## Upgrade

- No migration required. The former runtime mirror directory is additive. Requires the runtime command-line tool with experimental.enableAgents: true in the related file.
