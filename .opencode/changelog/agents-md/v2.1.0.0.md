This release rewrites the AGENTS guide into a simpler, more readable format without changing the underlying rules. It replaces heavy visual scaffolding with plain markdown, moves related gate behavior together, and adds a matching `AGENTS_FULL_STACK.md` variant built from the same shared guidance.

---

#### ASCII Box Gates → Markdown

This part of the agent instructions needed clearer guidance and less room for confusion. Replaced ~148 lines of code-block box art (Pre-Execution + Post-Execution gates) with structured #### headers, bullet lists, blockquotes, and checkboxes (~55 lines). This makes the agent instructions easier to follow.

---

#### §3 Consolidated Question Protocol Merged Into §2 Gates

This part of the agent instructions needed clearer guidance and less room for confusion. Question protocol is gate behavior, now lives as #### CONSOLIDATED QUESTION PROTOCOL under Gate 3. This makes the agent instructions easier to follow.

---

#### Sections Renumbered 1-8

This part of the agent instructions needed clearer guidance and less room for confusion. All internal cross-references were updated to match the new numbering (for example, old `§5` references now point at `§4`). This makes the agent instructions easier to follow.

---

#### §4 Documentation

The older guidance still pointed to something that was outdated or no longer used. Removed duplicated Spec Folder Structure table, Sub-Folder Versioning, Dynamic State, Scratch vs Memory. replaced with single reference to system-spec-kit the skill guide §3. Kept Documentation Levels table as quick-reference. This reduces confusion by keeping the notes focused on supported behavior.

---

#### §4 Confidence

The older guidance still pointed to something that was outdated or no longer used. Thresholds from inline text to scannable table. Removed clarification question format example. This reduces confusion by keeping the notes focused on supported behavior.

---

#### §5 Analysis

This part of the agent instructions needed clearer guidance and less room for confusion. Solution Flow ASCII art to inline arrow chain. Pre-Change Checklist code block to checkbox markdown. Five Checks table to inline. This makes the agent instructions easier to follow.

---

#### AGENTS_FULL_STACK note

This part of the agent instructions needed clearer guidance and less room for confusion. Made explicit with bold emphasis and cross-reference to the skill guide §4 Rule 13. This makes the agent instructions easier to follow.

---

#### Agent Definitions

This part of the agent instructions needed clearer guidance and less room for confusion. Table to bullet list format for better readability of long @speckit exclusivity description. This makes the agent instructions easier to follow.

---

#### @speckit Exclusivity Enforcement

The older guidance still pointed to something that was outdated or no longer used. Removed from §2. already documented in system-spec-kit the skill guide §1 and @speckit agent definition in §6. This reduces confusion by keeping the notes focused on supported behavior.

---

#### AGENTS_FULL_STACK created

This part of the agent instructions needed clearer guidance and less room for confusion. Added an anonymized `AGENTS_FULL_STACK.md` variant with no project-specific branding. It pulls its stack-detection and verification flow from `sk-code-full-stack` and its git workflow guidance from `sk-git`, so teams get the same structure in a more reusable form.

---

## Files Changed

| File | Action | Notes |
| ---- | ------ | ----- |
| `AGENTS.md - Major restructure` | Updated | Updated in this release |
| `CLAUDE.md - Synced with AGENTS.md changes (auto-sync)` | Updated | Updated in this release |
| `AGENTS_FULL_STACK.md - Created (anonymized from Barter template)` | Updated | Updated in this release |

---

## Upgrade

- No migration required. Behavioral rules are unchanged; only formatting and deduplication changed. All condensed content now points back to `system-spec-kit` as the source of truth.
