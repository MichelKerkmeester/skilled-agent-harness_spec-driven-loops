Removed @explore row from the AGENTS guide agent table. Expanded @context description to "ALL codebase exploration, file search, pattern discovery, context loading." Updated Quick Reference to reflect @context as sole exploration entry point. The main changes here are @explore removed and @context expanded. The goal is to make the workflow easier to follow and easier to trust.

---

#### @explore Removed

The older guidance still pointed to something that was outdated or no longer used. Entire row deleted from agent routing table. This reduces confusion by keeping the notes focused on supported behavior.

---

#### @context Expanded

People needed a better way to find the right information without guessing exact words. Now covers "ALL codebase exploration, file search, pattern discovery, context loading. Internally dispatches sub-agents.". This helps people find the right place faster, even when they do not know the exact term to search for.

---

#### Quick Reference

People needed a better way to find the right information without guessing exact words. "Context loading / exploration" to "ALL codebase exploration / file search / context loading". This helps people find the right place faster, even when they do not know the exact term to search for.

---

## Files Changed

| File | Action | Notes |
| ---- | ------ | ----- |
| `AGENTS.md` | Updated | Updated in this release |

---

## Upgrade

- No action required. Pull latest to get updates.
