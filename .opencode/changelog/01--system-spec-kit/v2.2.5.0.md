# v2.2.5.0

> Part of [OpenCode Dev Environment](https://github.com/MichelKerkmeester/opencode--spec-kit-skilled-agent-orchestration)

---

## [**2.2.5.0**] - 2026-02-13

This release rEADME anchor schema and documentation alignment deploying ~473 anchor tags across 74 READMEs, aligning memory commands, standardizing styles, and optimizing SKILL with agent routing fixes. It focuses on making the instructions and release notes easier for non-developers to follow. The goal is to make the workflow easier to follow and easier to trust.

---

## What Changed

---

### ~473 anchor tags

**Problem:** People needed clearer guidance about ~473 anchor tags.

**Fix:** Added across 74 README files using <!-- anchor:tag-name -to format, enabling precise memory retrieval via anchor-based content filtering; covers all .opencode/ skill, command, agent, and infrastructure READMEs (Spec 111). This matters because the release notes and instructions are easier to read and trust.

---

### 5 memory command READMEs aligned

**Problem:** People needed clearer guidance about 5 memory command READMEs aligned.

**Fix:** Save, learn, manage, continue, context aligned with documentation standards (Spec 112). This matters because the release notes and instructions are easier to read and trust.

---

### 7 style rules applied

**Problem:** People needed clearer guidance about 7 style rules applied.

**Fix:** Applied from readme template to 75 active README files, standardizing H1 format, badge placement, section ordering, table formatting, link style, frontmatter, dividers (Spec 113). This matters because the release notes and instructions are easier to read and trust.

---

### SKILL

**Problem:** This release updates SKILL.

**Fix:** Reduced from 1,055 to 701 lines (34% reduction) with all features preserved (Spec 114). This matters because the release is clearer and easier to work with.

---

### 12 command files

**Problem:** There was a reliability problem around 12 command files.

**Fix:** Restored to <=600 lines from over-reduced state (64-114 lines) (Spec 114). This matters because it reduces confusing failures and makes the system more dependable.

---

### Style alignment

**Problem:** People needed clearer guidance about Style alignment.

**Fix:** Applied command template style to all 12 command files (~160 H2 headings) (Spec 114). This matters because the release notes and instructions are easier to read and trust.

---

### 4 YAML assets

**Problem:** There was a reliability problem around 4 YAML assets.

**Fix:** Subagent_type: explore changed to subagent_type: context (Spec 114). This matters because it reduces confusing failures and makes the system more dependable.

---

### 5 .md command files

**Problem:** There was a reliability problem around 5 .md command files.

**Fix:** Added @context and @speckit routing references (Spec 114). This matters because it reduces confusing failures and makes the system more dependable.

---

### 2 YAML assets

**Problem:** There was a reliability problem around 2 YAML assets.

**Fix:** Added @speckit routing for debug delegation (Spec 114). This matters because it reduces confusing failures and makes the system more dependable.

---

### Full compliance

**Problem:** There was a reliability problem around Full compliance.

**Fix:** 19/19 commands properly route per AGENTS agent table (Spec 114). This matters because it reduces confusing failures and makes the system more dependable.

---

## Files Changed

| File | What Changed |
| ---- | ------------ |
| `SKILL.md` | Referenced in the original changelog. |
| `.opencode/` | Referenced in the original changelog. |
| `README.md` | Referenced in the original changelog. |
| `readme_template.md` | Referenced in the original changelog. |
| `command_template.md` | Referenced in the original changelog. |
| `AGENTS.md` | Referenced in the original changelog. |
| `debug-delegation.md` | Referenced in the original changelog. |
