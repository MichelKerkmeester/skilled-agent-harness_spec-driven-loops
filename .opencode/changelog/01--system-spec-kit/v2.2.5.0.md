# v2.2.5.0

> Part of [OpenCode Dev Environment](https://github.com/MichelKerkmeester/opencode-spec-kit-framework)

---

## [**2.2.5.0**] - 2026-02-13

**README anchor schema & documentation alignment** (Specs 111-114) deploying ~473 anchor tags across 74 READMEs, aligning memory commands, standardizing styles, and optimizing SKILL.md with agent routing fixes.

---

## Highlights

### New
- **~473 anchor tags** — Added across 74 README.md files using `<!-- anchor:tag-name -->` format, enabling precise memory retrieval via anchor-based content filtering; covers all `.opencode/` skill, command, agent, and infrastructure READMEs (Spec 111)
- **5 memory command READMEs aligned** — `save`, `learn`, `manage`, `continue`, `context` aligned with documentation standards (Spec 112)
- **7 style rules applied** — Applied from `readme_template.md` to 75 active README.md files, standardizing H1 format, badge placement, section ordering, table formatting, link style, frontmatter, dividers (Spec 113)

### Changed
- **SKILL.md** — Reduced from 1,055 to 701 lines (34% reduction) with all features preserved (Spec 114)
- **12 command files** — Restored to <=600 lines from over-reduced state (64-114 lines) (Spec 114)
- **Style alignment** — Applied `command_template.md` style to all 12 command files (~160 H2 headings) (Spec 114)

### Fixed
- **4 YAML assets** — `subagent_type: explore` changed to `subagent_type: context` (per AGENTS.md §7) (Spec 114)
- **5 .md command files** — Added @context and @speckit routing references (Spec 114)
- **2 YAML assets** — Added @speckit routing for `debug-delegation.md` (Spec 114)
- **Full compliance** — 19/19 commands properly route per AGENTS.md agent table (Spec 114)
