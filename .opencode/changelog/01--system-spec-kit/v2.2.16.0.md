## [**2.2.16.0**] - 2026-02-15

This release system-wide audit of upgrade level - 2 P0 fail-fast fixes, 7 P1 hardening patches, automatic rollback on failure, and a 14-test regression suite. It focuses on fixing important problems and explaining the results in clearer terms.

---

## What Changed

### Fail-fast guard for shell common sourcing

- Need: There was a reliability problem around Fail-fast guard for shell common sourcing.
- Change: Upgrade level now exits immediately if the shared library cannot be sourced (lines 29-33).
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

### Recursive backup for memory/ subdirectories

- Need: There was a reliability problem around Recursive backup for memory/ subdirectories.
- Change: Backup logic now preserves nested directory structures instead of silently skipping them (lines 303-338).
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

### Comment header leak

- Need: There was a reliability problem around Comment header leak.
- Change: Upgrade_plan() and upgrade_checklist() no longer inject stray comment lines into generated output.
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

### OPEN QUESTIONS warning

- Need: There was a reliability problem around OPEN QUESTIONS warning.
- Change: Now emitted to stderr so it surfaces visibly during upgrades.
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

### Dead code removed

- Need: Dead code removed was no longer needed in its old form.
- Change: SED_INPLACE_FLAG variable eliminated.
- Why it matters: This matters because the project becomes smaller and easier to maintain.

### Multi-Agent detection regex

- Need: This release updates Multi-Agent detection regex.
- Change: Anchored to table row pattern to prevent false positives.
- Why it matters: This matters because the release is clearer and easier to work with.

### Complexity flag exit pattern

- Need: This release updates Complexity flag exit pattern.
- Change: Tightened to avoid partial matches.
- Why it matters: This matters because the release is clearer and easier to work with.

### Iteration guard in find_insert_point()

- Need: This release updates Iteration guard in find_insert_point().
- Change: Capped at 200 iterations to prevent infinite loops.
- Why it matters: This matters because the release is clearer and easier to work with.

### Level detection pattern

- Need: This release updates Level detection pattern.
- Change: Anchored to line start for reliable matching.
- Why it matters: This matters because the release is clearer and easier to work with.

### Automatic rollback on upgrade failure

- Need: There was a reliability problem around Automatic rollback on upgrade failure.
- Change: New restore_from_backup() function (lines 345-373) triggers at line 1488 when upgrade fails, restoring the spec folder to its pre-upgrade state.
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

### 14-test regression suite

- Need: The team needed stronger proof that 14-test regression suite worked as expected.
- Change: New test upgrade level covering shell-common guard, input validation, level detection, dry-run mode, already-at-target, and missing spec scenarios.
- Why it matters: This matters because the team has clearer proof that the change works.

### 121 orphan memory cleanup

- Need: 121 orphan memory cleanup was no longer needed in its old form.
- Change: Deleted junk constitutional memory #1346, regenerated as #1361.
- Why it matters: This matters because the project becomes smaller and easier to maintain.

### Cross-lineage audit

- Need: There was a reliability problem around Cross-lineage audit.
- Change: Resolved contradictions across the 121→123→124 chain.
- Why it matters: This matters because the release is clearer and easier to work with.

### Full Level 3+ documentation

- Need: People needed clearer guidance about Full Level 3+ documentation.
- Change: Evidence-cited checklist with complete traceability.
- Why it matters: This matters because the release notes and instructions are easier to read and trust.


---

## Files Changed

| File | What Changed |
| ---- | ------------ |
| `specs/02--system-spec-kit/z_archive/008-codex-system-wide-audit/` | Spec folder referenced by this release. |
| `.opencode/skill/system-spec-kit/scripts/spec/upgrade-level.sh` | Updated in this release. |
| `.opencode/skill/system-spec-kit/scripts/tests/test-upgrade-level.sh (new)` | Updated in this release. |
| `specs/02--system-spec-kit/z_archive/008-codex-system-wide-audit/ (all documentation files)` | Updated in this release. |


---

## Upgrade

- No action required.
