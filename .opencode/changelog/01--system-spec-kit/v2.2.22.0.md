## [**2.2.22.0**] - 2026-02-19

This release hybrid memory retrieval upgrade with traceability, adaptive fusion, and mutation auditing -- Expanded the Spec Kit Memory MCP runtime with typed retrieval traces, artifact-aware routing, intent-adaptive fusion, append-only mutation logging, and extended telemetry, delivered through the Level 3+ rollout in 136-mcp-working-memory-hybrid-rag. It focuses on fixing important problems and explaining the results in clearer terms.

---

## What Changed

### Typed retrieval contracts added

- Need: This release updates Typed retrieval contracts added.
- Change: Typed retrieval contracts added: Retrieval responses now carry a typed ContextEnvelope with explicit pipeline trace/degraded-mode metadata for explainable ranking behavior.
- Why it matters: This matters because the release is clearer and easier to work with.

### Artifact-aware routing shipped

- Need: This release updates Artifact-aware routing shipped.
- Change: Artifact-aware routing shipped: Search behavior now adapts by artifact class (spec, plan, decision-record, memory, research, and others) to improve relevance per document type.
- Why it matters: This matters because the release is clearer and easier to work with.

### Intent-adaptive hybrid fusion added

- Need: There was a reliability problem around Intent-adaptive hybrid fusion added.
- Change: Intent-adaptive hybrid fusion added: Weighted fusion now supports intent-aware profiles (fix_bug, add_feature, understand, refactor, security_audit, find_spec, find_decision) behind feature-flag control.
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

### Append-only mutation ledger introduced

- Need: There was a reliability problem around Append-only mutation ledger introduced.
- Change: Append-only mutation ledger introduced: Memory CRUD and re-index operations are now captured in a tamper-evident audit trail.
- Why it matters: This matters because the release is clearer and easier to work with.

### Extended retrieval telemetry enabled

- Need: There was a reliability problem around Extended retrieval telemetry enabled.
- Change: Extended retrieval telemetry enabled: Search/context handlers now emit richer latency, mode, fallback, and quality-proxy telemetry for evaluation and operations.
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

### Cognitive config hardening

- Need: The project needed a cleaner setup for Cognitive config hardening.
- Change: Cognitive config hardening: Config parsing now uses schema-validated paths and deterministic working-memory pressure/eviction behavior.
- Why it matters: This matters because setup and maintenance are easier to manage.

### New runtime modules added

- Need: The team needed stronger proof that New runtime modules added worked as expected.
- Change: New runtime modules added: Contracts, extraction adapters, adaptive fusion, artifact routing, causal/session boost, mutation ledger, and telemetry modules landed in MCP server runtime.
- Why it matters: This matters because the team has clearer proof that the change works.

### Expanded test suite

- Need: The team needed stronger proof that Expanded test suite worked as expected.
- Change: Expanded test suite: Added focused test files for new retrieval/routing/telemetry/ledger features and updated integration coverage.
- Why it matters: This matters because the team has clearer proof that the change works.

### Evaluation lane scripts added

- Need: The team needed stronger proof that Evaluation lane scripts added worked as expected.
- Change: Evaluation lane scripts added: New eval/KPI scripts support staged rollout checks, quality backfill, performance benchmarks, and calibration workflows.
- Why it matters: This matters because the team has clearer proof that the change works.


---

## Files Changed

| File | What Changed |
| ---- | ------------ |
| `.opencode/specs/system-spec-kit/020-mcp-working-memory-hybrid-rag/` | Spec folder referenced by this release. |
| `.opencode/skill/system-spec-kit/mcp_server/context-server.ts` | Updated orchestration and retrieval wiring. |
| `.opencode/skill/system-spec-kit/mcp_server/handlers/*.ts` | Extended context/search/index/save/checkpoint behavior. |
| `.opencode/skill/system-spec-kit/mcp_server/lib/{contracts,extraction,search,storage,telemetry,cognitive}/**` | Added new runtime capabilities and support logic. |
| `.opencode/skill/system-spec-kit/mcp_server/tests/*.vitest.ts` | Added and updated feature/integration tests. |
| `.opencode/skill/system-spec-kit/scripts/{evals,kpi,extractors,memory}/**` | Added rollout, quality, and validation tooling. |
| `.opencode/specs/system-spec-kit/020-mcp-working-memory-hybrid-rag/**` | Added rollout waves, documentation alignment, and completion artifacts. |


---

## Upgrade

- No migration is required. Pull latest to get the new retrieval/runtime capabilities. Keep SPECKIT_ADAPTIVE_FUSION disabled for baseline parity, then enable it deliberately when ready to trial adaptive ranking behavior.
