# v2.2.27.0

> Part of [OpenCode Dev Environment](https://github.com/MichelKerkmeester/opencode-spec-kit-framework)

---

## [**2.2.27.0**] - 2026-02-20

**Unified Memory Dashboard Visual Design System** across all 5 memory commands with 10 shared output components: `MEMORY:<COMMAND>` headers, `→ Title Case` section labels, `█░` bar charts, `PASS`/`WARN`/`FAIL` indicators, and consistent key-value formatting. Approximately 29 output templates standardized.

---

## Highlights

### Category: Features

- **Unified command headers**: All memory commands now use `MEMORY:<COMMAND>` ALL CAPS format with thick `━━━` separator line, replacing 5 different header styles (box-drawing, emoji-prefixed, mixed case)
- **Section labels**: Introduced `→ Title Case ──────────── count` arrow-prefixed section labels across all commands, replacing ad-hoc dividers and mixed formatting
- **Bar charts**: Added `█░` block character visualizations for scores, progress, tier distribution, confidence, and stability across all numeric displays
- **Action menus**: Standardized interactive prompts to `[key] verb` format after thin `───` separator, replacing inconsistent `[a]ction | [b]ction` pipe-separated styles
- **ASCII indicators**: Replaced all emoji status indicators (`✓`, `⚠`, `❌`, `🔄`, `✅`) with plain text `PASS` / `WARN` / `FAIL` words

### Category: Documentation

- **10-component design system**: Defined shared output vocabulary (command header, section label, key-value pair, bar chart, result item, status line, action menu, indicators, inline lists, empty state)
- **Spec folder 036**: Full Level 2 spec folder with spec.md, plan.md, implementation-summary.md, and checklist.md

## Files Changed

| File | Change |
|------|--------|
| `.opencode/command/memory/context.md` | Section 4 Step 4: replaced box-drawing output template with design system format |
| `.opencode/command/memory/save.md` | Section 10: replaced save display and trigger edit templates |
| `.opencode/command/memory/manage.md` | Sections 7-15: replaced ~15 output templates across stats, scan, cleanup, tier, triggers, validate, delete, health, and checkpoint operations |
| `.opencode/command/memory/learn.md` | Sections 6, 10-12: replaced 6 output templates for learn, correct, undo, and history subcommands |
| `.opencode/command/memory/continue.md` | Sections 6, 12: replaced recovery summary, action menu, and 3 use case example templates |

## Upgrade

No migration required. All changes are documentation-only (output template formatting in command instruction files). No runtime code, MCP tool signatures, or database schema changes.
