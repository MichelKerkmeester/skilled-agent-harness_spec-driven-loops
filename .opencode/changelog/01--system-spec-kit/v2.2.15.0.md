This release spec folder level upgrade script - New 1,490+ LOC bash script that upgrades existing spec folders between documentation levels (L1toL2, L2toL3, L3toL3+) with full user content preservation, dry-run preview, template-based generation, and idempotent operation. It focuses on making the instructions and release notes easier for non-developers to follow. The goal is to make the workflow easier to follow and easier to trust.

---

## What Changed

---

### Multi-level upgrade support

**Problem:** This release updates Multi-level upgrade support.

**Fix:** L1toL2, L2toL3, L3toL3+, and skip-level chains (L1toL3 via sequential L1toL2 then L2toL3). This matters because the release is clearer and easier to work with.

---

### Dry-run mode

**Problem:** This release updates Dry-run mode.

**Fix:** Safe preview of all planned changes without modifying files (--dry-run). This matters because the release is clearer and easier to work with.

---

### Template-based file generation

**Problem:** People needed clearer guidance about Template-based file generation.

**Fix:** New files created from SpecKit addendum templates. This matters because the release notes and instructions are easier to read and trust.

---

### Metadata and marker updates

**Problem:** This release updates Metadata and marker updates.

**Fix:** SPECKIT_LEVEL markers and metadata table level fields updated across all modified files. This matters because the release is clearer and easier to work with.

---

### Idempotent operation

**Problem:** This release updates Idempotent operation.

**Fix:** Dual-pattern detection prevents duplicate sections on re-run. This matters because the release is clearer and easier to work with.

---

### JSON output mode

**Problem:** This release updates JSON output mode.

**Fix:** Machine-parseable output for toolchain integration (--json). This matters because the release is clearer and easier to work with.

---

### 1,490+ LOC, 22 functions

**Problem:** The team needed stronger proof that 1,490+ LOC, 22 functions worked as expected.

**Fix:** 6-phase architecture: input validation, level detection, backup creation, file upgrade, marker updates, output generation. This matters because the team has clearer proof that the change works.

---

### Bash 3.2+ compatible

**Problem:** The project needed a cleaner setup for Bash 3.2+ compatible.

**Fix:** Works on stock macOS without requiring brew-installed bash 5; no associative arrays or bashisms. This matters because setup and maintenance are easier to manage.

---

### Portable sed handling

**Problem:** This release updates Portable sed handling.

**Fix:** _sed_inplace() wrapper abstracts macOS vs GNU sed -i differences. This matters because the release is clearer and easier to work with.

---

### Fuzzy heading matching

**Problem:** There was a reliability problem around Fuzzy heading matching.

**Fix:** Finds approximate insertion points when users have modified standard headings, with end-of-file fallback. This matters because it reduces confusing failures and makes the system more dependable.

---

### Auto-cleanup

**Problem:** Auto-cleanup was no longer needed in its old form.

**Fix:** Keeps 3 most recent backups, removes older ones automatically. This matters because the project becomes smaller and easier to maintain.

---

## Files Changed

| File | What Changed |
| ---- | ------------ |
| `specs/system-spec-kit/z_archive/007-upgrade-level-script/` | Spec folder referenced by this release. |
| `.opencode/skill/system-spec-kit/scripts/spec/upgrade-level.sh (created, 1,490+ lines)` | Updated in this release. |

---

## Upgrade

- No action required.
