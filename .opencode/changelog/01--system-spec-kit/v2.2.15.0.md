## [**2.2.15.0**] - 2026-02-15

This release spec folder level upgrade script - New 1,490+ LOC bash script that upgrades existing spec folders between documentation levels (L1→L2, L2→L3, L3→L3+) with full user content preservation, dry-run preview, template-based generation, and idempotent operation. It focuses on making the instructions and release notes easier for non-developers to follow.

---

## What Changed

### Multi-level upgrade support

- Need: This release updates Multi-level upgrade support.
- Change: L1→L2, L2→L3, L3→L3+, and skip-level chains (L1→L3 via sequential L1→L2 then L2→L3).
- Why it matters: This matters because the release is clearer and easier to work with.

### Dry-run mode

- Need: This release updates Dry-run mode.
- Change: Safe preview of all planned changes without modifying files (--dry-run).
- Why it matters: This matters because the release is clearer and easier to work with.

### Template-based file generation

- Need: People needed clearer guidance about Template-based file generation.
- Change: New files created from SpecKit addendum templates.
- Why it matters: This matters because the release notes and instructions are easier to read and trust.

### Metadata and marker updates

- Need: This release updates Metadata and marker updates.
- Change: SPECKIT_LEVEL markers and metadata table level fields updated across all modified files.
- Why it matters: This matters because the release is clearer and easier to work with.

### Idempotent operation

- Need: This release updates Idempotent operation.
- Change: Dual-pattern detection prevents duplicate sections on re-run.
- Why it matters: This matters because the release is clearer and easier to work with.

### JSON output mode

- Need: This release updates JSON output mode.
- Change: Machine-parseable output for toolchain integration (--json).
- Why it matters: This matters because the release is clearer and easier to work with.

### 1,490+ LOC, 22 functions

- Need: The team needed stronger proof that 1,490+ LOC, 22 functions worked as expected.
- Change: 6-phase architecture: input validation, level detection, backup creation, file upgrade, marker updates, output generation.
- Why it matters: This matters because the team has clearer proof that the change works.

### Bash 3.2+ compatible

- Need: The project needed a cleaner setup for Bash 3.2+ compatible.
- Change: Works on stock macOS without requiring brew-installed bash 5; no associative arrays or bashisms.
- Why it matters: This matters because setup and maintenance are easier to manage.

### Portable sed handling

- Need: This release updates Portable sed handling.
- Change: _sed_inplace() wrapper abstracts macOS vs GNU sed -i differences.
- Why it matters: This matters because the release is clearer and easier to work with.

### Fuzzy heading matching

- Need: There was a reliability problem around Fuzzy heading matching.
- Change: Finds approximate insertion points when users have modified standard headings, with end-of-file fallback.
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

### Auto-cleanup

- Need: Auto-cleanup was no longer needed in its old form.
- Change: Keeps 3 most recent backups, removes older ones automatically.
- Why it matters: This matters because the project becomes smaller and easier to maintain.


---

## Files Changed

| File | What Changed |
| ---- | ------------ |
| `specs/system-spec-kit/z_archive/007-upgrade-level-script/` | Spec folder referenced by this release. |
| `.opencode/skill/system-spec-kit/scripts/spec/upgrade-level.sh (created, 1,490+ lines)` | Updated in this release. |


---

## Upgrade

- No action required.
