## [**2.2.25.0**] - 2026-02-20

This release sQLite WAL contention fix and database maintenance - Resolves intermittent "no such table: memory_index" errors under concurrent access, and cleans up test artifacts, stale conflicts, and missing trigger phrases. It focuses on fixing important problems and explaining the results in clearer terms.

---

## What Changed

---

### SQLite busy_timeout PRAGMA added

- Need: There was a reliability problem around SQLite busy_timeout PRAGMA added.
- Change: SQLite busy_timeout PRAGMA added: Added db.pragma('busy_timeout = 10000') to database initialization in vector-index-impl. When multiple MCP server processes access the WAL-mode database concurrently, sqlite3_prepare() could fail to acquire the WAL lock during schema loading, falling back to a stale main database file with an empty schema - producing the "no such table: memory_index" error. The 10-second busy timeout allows concurrent readers to wait for the lock instead of failing immediately.
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

---

### Test artifacts removed

- Need: The team needed stronger proof that Test artifacts removed worked as expected.
- Change: Test artifacts removed: Deleted 67 test artifact entries (spec_folder='T') and 2 temporary-tier entries from the memory index.
- Why it matters: This matters because the team has clearer proof that the change works.

---

### Stale conflicts purged

- Need: Stale conflicts purged was no longer needed in its old form.
- Change: Stale conflicts purged: Removed 9,626 orphaned records from the memory_conflicts table.
- Why it matters: This matters because the project becomes smaller and easier to maintain.

---

### Missing trigger phrases generated

- Need: This release updates Missing trigger phrases generated.
- Change: Missing trigger phrases generated: Auto-generated trigger phrases for 1,463 memories that had none, using title and path-based extraction.
- Why it matters: This matters because the release is clearer and easier to work with.

---

### Post-maintenance state

- Need: The team needed stronger proof that Post-maintenance state worked as expected.
- Change: Post-maintenance state: 2,909 memories, 0 test artifacts, 0 temporary entries, 0 stale conflicts, 0 missing trigger phrases.
- Why it matters: This matters because the team has clearer proof that the change works.

---

### Incremental scan completed

- Need: This release updates Incremental scan completed.
- Change: Incremental scan completed: 23 new files indexed, 30 updated, 1,485 unchanged (mtime-based skip).
- Why it matters: This matters because the release is clearer and easier to work with.


---

## Files Changed

| File | What Changed |
| ---- | ------------ |
| `.opencode/skill/system-spec-kit/mcp_server/lib/search/vector-index-impl.ts` | Added busy_timeout = 10000 PRAGMA after WAL journal mode. |
| `.opencode/skill/system-spec-kit/mcp_server/dist/lib/search/vector-index-impl.js` | Same fix applied to compiled JS output. |
| `dist/database/context-index.sqlite` | Test artifacts, temp entries, conflicts purged; trigger phrases generated. |


---

## Upgrade

- No migration required. The busy_timeout PRAGMA is applied automatically on database open. Restart any running MCP server processes to pick up the fix.
