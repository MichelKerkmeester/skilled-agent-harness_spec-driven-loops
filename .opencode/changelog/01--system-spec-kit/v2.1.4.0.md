## [**2.1.4.0**] - 2026-02-09

This release memory save auto-detect  adding auto-detection of spec folder from Gate 3 conversation context with a 5-priority detection cascade. It explains the main changes in simple language so the release is easier to understand.

---

## What Changed

### 1. Auto-detect folder

- Need: This release updates Auto-detect folder.
- Change: From Gate 3 conversation context.
- Why it matters: This matters because the release is clearer and easier to work with.

### 2. 5-priority detection cascade

- Need: This release updates 5-priority detection cascade.
- Change: Explicit path, Gate 3, recent spec, cwd, ask user.
- Why it matters: This matters because the release is clearer and easier to work with.

### 3. Minimal change

- Need: This release updates Minimal change.
- Change: Only 2 files modified.
- Why it matters: This matters because the release is clearer and easier to work with.

### 4. Streamlined workflow

- Need: This release updates Streamlined workflow.
- Change: Memory save workflow simplified.
- Why it matters: This matters because the release is clearer and easier to work with.

## Files Changed

| File | What Changed |
| ---- | ------------ |
| `Not listed in source changelog` | The original note did not name specific files. |
