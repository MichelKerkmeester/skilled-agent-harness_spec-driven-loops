This release clarify Legacy Node Index as Supplemental -- Updated SKILL Graph Status header and index to explicitly frame the legacy node index as a supplemental navigation layer, not a replacement for SKILL. Fixed replacement-implying language across all 8 spec documents in 002-skill-graph-integration. It focuses on making the instructions and release notes easier for non-developers to follow. The goal is to make the workflow easier to follow and easier to trust.

---

## What Changed

---

### Replaced "running in graph mode" /

**Problem:** This release updates Replaced "running in graph mode" /.

**Fix:** Replaced "running in graph mode" / "compatibility entrypoint" block with corrected supplemental framing. This matters because the release is clearer and easier to work with.

---

### SKILL explicitly labeled as primary entrypoint

**Problem:** This release updates SKILL explicitly labeled as primary entrypoint.

**Fix:** SKILL explicitly labeled as primary entrypoint (activation rules, routing, core behavior). This matters because the release is clearer and easier to work with.

---

### Index labeled as supplemental navigation (optional

**Problem:** This release updates Index labeled as supplemental navigation (optional.

**Fix:** Index labeled as supplemental navigation (optional deep-dive into specific topics). This matters because the release is clearer and easier to work with.

---

### Added navigation note

**Problem:** This release updates Added navigation note.

**Fix:** Added navigation note: "This is a supplemental deep-dive index. SKILL remains the primary entrypoint". This matters because the release is clearer and easier to work with.

---

### Fixed ~35 replacement-implying phrases across

**Problem:** People needed clearer guidance about Fixed ~35 replacement-implying phrases across.

**Fix:** Fixed ~35 replacement-implying phrases across spec, decision record, implementation summary, plan, tasks, checklist, handover, research. This matters because the release notes and instructions are easier to read and trust.

---

### Removed

**Problem:** People needed clearer guidance about Removed.

**Fix:** Removed: "compatibility entrypoint", "during migration", "were monolithic", "decomposed into". This matters because the release notes and instructions are easier to read and trust.

---

### Added

**Problem:** People needed clearer guidance about Added.

**Fix:** Added: "primary entrypoint", "supplemental node navigation", "Graph Status header". This matters because the release notes and instructions are easier to read and trust.

---

## Files Changed

| File | What Changed |
| ---- | ------------ |
| `.opencode/specs/system-spec-kit/022-hybrid-rag-fusion/002-skill-graph-integration/` | Spec folder referenced by this release. |
| `.opencode/skill/system-spec-kit/SKILL.md -- Graph Status header` | Updated in this release. |
| `.opencode/skill/system-spec-kit/index.md -- Navigation note` | Updated in this release. |
| `8 files in .opencode/specs/system-spec-kit/022-hybrid-rag-fusion/002-skill-graph-integration/` | Updated in this release. |

---

## Upgrade

- No action required. Language-only changes with no behavioral impact.
