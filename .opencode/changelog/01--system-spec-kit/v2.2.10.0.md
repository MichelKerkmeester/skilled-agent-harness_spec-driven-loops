# v2.2.10.0

> Part of [OpenCode Dev Environment](https://github.com/MichelKerkmeester/opencode--spec-kit-skilled-agent-orchestration)

---

## [**2.2.10.0**] - 2026-02-15

This release reorganizes where project dependencies are installed. It makes setup paths clearer and reduces confusion for people working on the server. The goal is to make the workflow easier to follow and easier to trust.

---

## What Changed

---

### Npmrc

**Problem:** The project needed a cleaner setup for Npmrc.

**Fix:** Npmrc: New file with install-strategy=nested - npm installs deps per-workspace instead of hoisting to root. This matters because setup and maintenance are easier to manage.

---

### Root package

**Problem:** The project needed a cleaner setup for Root package.

**Fix:** Root package: Removed redundant better-sqlite3 from root dependencies. This matters because setup and maintenance are easier to manage.

---

### Package

**Problem:** The project needed a cleaner setup for Package.

**Fix:** Package: Added sqlite-vec-darwin-arm64 as optionalDependency for nested install compatibility. This matters because setup and maintenance are easier to manage.

---

### Symlink

**Problem:** There was a reliability problem around Symlink.

**Fix:** Symlink: lib/cache/cognitive to ../cognitive fixes TypeScript build paths. This matters because it reduces confusing failures and makes the system more dependable.

---

### INSTALL GUIDE

**Problem:** People needed clearer guidance about INSTALL GUIDE.

**Fix:** INSTALL GUIDE: All system-spec-kit/node_modules/ references corrected to mcp_server/node_modules/. This matters because the release notes and instructions are easier to read and trust.

---

### Install kit memory

**Problem:** People needed clearer guidance about Install kit memory.

**Fix:** Install spec kit memory: Install, rebuild, and verify commands now target mcp_server/. This matters because the release notes and instructions are easier to read and trust.

---

### Check native modules

**Problem:** People needed clearer guidance about Check native modules.

**Fix:** Check native modules: better-sqlite3 probe path updated. This matters because the release notes and instructions are easier to read and trust.

---

### Rebuild native modules

**Problem:** People needed clearer guidance about Rebuild native modules.

**Fix:** Rebuild native modules: Rebuild location updated to mcp_server/. This matters because the release notes and instructions are easier to read and trust.

---

### Folder detector

**Problem:** This release updates Folder detector.

**Fix:** Folder detector: better-sqlite3 require path updated to mcp_server/node_modules/. This matters because the release is clearer and easier to work with.

---

### Test folder detector functional

**Problem:** The team needed stronger proof that Test folder detector functional worked as expected.

**Fix:** Test folder detector functional: Test paths updated to match. This matters because the team has clearer proof that the change works.

---

## Files Changed

| File | What Changed |
| ---- | ------------ |
| `.opencode/specs/system-spec-kit/z_archive/003-node-modules-relocation/` | Spec folder referenced by this release. |
| `.opencode/skill/system-spec-kit/.npmrc` | Updated in this release. |
| `package.json` | Updated in this release. |
| `package-lock.json` | Updated in this release. |
| `mcp_server/package.json` | Updated in this release. |
| `mcp_server/INSTALL_GUIDE.md` | Updated in this release. |
| `mcp_server/lib/cache/cognitive (symlink)` | Updated in this release. |
| `scripts/spec-folder/folder-detector.ts` | Updated in this release. |
| `scripts/tests/test-folder-detector-functional.js` | Updated in this release. |
| `scripts/setup/check-native-modules.sh` | Updated in this release. |
| `scripts/setup/rebuild-native-modules.sh` | Updated in this release. |

---

## Upgrade

- No action required. Pull latest. If running a linked project, restart the MCP server to pick up the new dependency paths.
