## [**2.2.10.0**] - 2026-02-15

This release reorganizes where project dependencies are installed. It makes setup paths clearer and reduces confusion for people working on the server.

---

## What Changed

### 1. Npmrc

- Need: The project needed a cleaner setup for Npmrc.
- Change: Npmrc: New file with install-strategy=nested - npm installs deps per-workspace instead of hoisting to root.
- Why it matters: This matters because setup and maintenance are easier to manage.

### 2. Root package

- Need: The project needed a cleaner setup for Root package.
- Change: Root package: Removed redundant better-sqlite3 from root dependencies.
- Why it matters: This matters because setup and maintenance are easier to manage.

### 3. Package

- Need: The project needed a cleaner setup for Package.
- Change: Package: Added sqlite-vec-darwin-arm64 as optionalDependency for nested install compatibility.
- Why it matters: This matters because setup and maintenance are easier to manage.

### 4. Symlink

- Need: There was a reliability problem around Symlink.
- Change: Symlink: lib/cache/cognitive to ../cognitive fixes TypeScript build paths.
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

### 5. INSTALL GUIDE

- Need: People needed clearer guidance about INSTALL GUIDE.
- Change: INSTALL GUIDE: All system-spec-kit/node_modules/ references corrected to mcp_server/node_modules/.
- Why it matters: This matters because the release notes and instructions are easier to read and trust.

### 6. Install kit memory

- Need: People needed clearer guidance about Install kit memory.
- Change: Install spec kit memory: Install, rebuild, and verify commands now target mcp_server/.
- Why it matters: This matters because the release notes and instructions are easier to read and trust.

### 7. Check native modules

- Need: People needed clearer guidance about Check native modules.
- Change: Check native modules: better-sqlite3 probe path updated.
- Why it matters: This matters because the release notes and instructions are easier to read and trust.

### 8. Rebuild native modules

- Need: People needed clearer guidance about Rebuild native modules.
- Change: Rebuild native modules: Rebuild location updated to mcp_server/.
- Why it matters: This matters because the release notes and instructions are easier to read and trust.

### 9. Folder detector

- Need: This release updates Folder detector.
- Change: Folder detector: better-sqlite3 require path updated to mcp_server/node_modules/.
- Why it matters: This matters because the release is clearer and easier to work with.

### 10. Test folder detector functional

- Need: The team needed stronger proof that Test folder detector functional worked as expected.
- Change: Test folder detector functional: Test paths updated to match.
- Why it matters: This matters because the team has clearer proof that the change works.

## Files Changed

| File | What Changed |
| ---- | ------------ |
| `.opencode/specs/02--system-spec-kit/z_archive/003-node-modules-relocation/` | Spec folder referenced by this release. |
| `.opencode/skill/system-spec-kit/.npmrc` | Updated in this release. |
| `package.json` | Updated in this release. |
| `package-lock.json` | Updated in this release. |
| `mcp_server/package.json` | Updated in this release. |
| `mcp_server/INSTALL_GUIDE.md` | Updated in this release. |
| `mcp_server/lib/cache/cognitive (symlink)` | Updated in this release. |
| `scripts/spec-folder/folder-detector.ts` | Updated in this release. |
| `scripts/tests/test-folder-detector-functional.js` | Updated in this release. |
| `scripts/setup/check-native-modules.sh` | Updated in this release. |
| `scripts/setup/rebuild-native-modules.sh` | Updated in this release. |

## Upgrade

- No action required. Pull latest. If running a linked project, restart the MCP server to pick up the new dependency paths.
