This release deterministic memory indexing for alias roots and tier precedence normalization - Fixed duplicate indexing caused by specs/ vs .opencode/specs/ path aliasing and standardized importance-tier resolution across parser and scoring paths. It focuses on fixing important problems and explaining the results in clearer terms. The goal is to make the workflow easier to follow and easier to trust.

---

## What Changed

---

### Canonical-path dedup in discovery

**Problem:** There was a reliability problem around Canonical-path dedup in discovery.

**Fix:** Canonical-path dedup in discovery: parser file discovery now mainizes and deduplicates logical paths before indexing. This matters because it reduces confusing failures and makes the system more dependable.

---

### Unique merge enforcement in scan handler

**Problem:** There was a reliability problem around Unique merge enforcement in scan handler.

**Fix:** Unique merge enforcement in scan handler: memory index now enforces unique merged file sets before batch processing. This matters because it reduces confusing failures and makes the system more dependable.

---

### Tier precedence normalized

**Problem:** There was a reliability problem around Tier precedence normalized.

**Fix:** Tier precedence normalized: tier logic is now deterministic with precedence metadata to inline marker to default. This matters because it reduces confusing failures and makes the system more dependable.

---

### Diagnostics improved

**Problem:** There was a reliability problem around Diagnostics improved.

**Fix:** Diagnostics improved: scan outputs now include dedup-oriented diagnostics to explain unique-vs-skipped behavior. This matters because it reduces confusing failures and makes the system more dependable.

---

### Targeted regressions passed

**Problem:** The team needed stronger proof that Targeted regressions passed worked as expected.

**Fix:** Targeted regressions passed: parser/index/tier suites passed (52 tests). This matters because the team has clearer proof that the change works.

---

### Extended regressions passed

**Problem:** The team needed stronger proof that Extended regressions passed worked as expected.

**Fix:** Extended regressions passed: extended parser/spec indexing suites passed (186 tests). This matters because the team has clearer proof that the change works.

---

### Scoped lint and validation passed

**Problem:** The team needed stronger proof that Scoped lint and validation passed worked as expected.

**Fix:** Scoped lint and spec validation passed: modified runtime and test files passed scoped ESLint and spec validation checks. This matters because the team has clearer proof that the change works.

---

### No schema migration included

**Problem:** The project needed a cleaner setup for No schema migration included.

**Fix:** No schema migration included: historical row cleanup was explicitly out of scope for this spec. This matters because setup and maintenance are easier to manage.

---

### Behavioral fix only

**Problem:** There was a reliability problem around Behavioral fix only.

**Fix:** Behavioral fix only: change focuses on deterministic indexing and tier assignment consistency. This matters because it reduces confusing failures and makes the system more dependable.

---

## Files Changed

| File | What Changed |
| ---- | ------------ |
| `.opencode/specs/system-spec-kit/022-hybrid-rag-fusion/003-index-tier-anomalies/` | Spec folder referenced by this release. |
| `.opencode/skill/system-spec-kit/mcp_server/lib/parsing/memory-parser.ts` | Canonical-path dedup and normalization flow. |
| `.opencode/skill/system-spec-kit/mcp_server/handlers/memory-index.ts` | Unique merged file-set enforcement and dedup diagnostics. |
| `.opencode/skill/system-spec-kit/mcp_server/lib/scoring/importance-tiers.ts` | Deterministic tier precedence and default mapping behavior. |
| `.opencode/skill/system-spec-kit/mcp_server/tests/memory-parser.vitest.ts` | Tier precedence and parser edge-case coverage. |
| `.opencode/skill/system-spec-kit/mcp_server/tests/handler-memory-index.vitest.ts` | Alias-root dedup regression coverage. |
| `.opencode/skill/system-spec-kit/mcp_server/tests/importance-tiers.vitest.ts` | Tier consistency assertions. |

---

## Upgrade

- No migration is required. Restart MCP services and run a fresh index scan to confirm deduplicated scan counters and stable tier mapping in diagnostics.
