## [**2.3.0.3**] - 2026-02-22

This release deterministic memory indexing for alias roots and tier precedence normalization - Fixed duplicate indexing caused by specs/ vs .opencode/specs/ path aliasing and standardized importance-tier resolution across parser and scoring paths. It focuses on fixing important problems and explaining the results in clearer terms.

---

## What Changed

---

### Canonical-path dedup in discovery

- Need: There was a reliability problem around Canonical-path dedup in discovery.
- Change: Canonical-path dedup in discovery: parser file discovery now canonicalizes and deduplicates logical paths before indexing.
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

---

### Unique merge enforcement in scan handler

- Need: There was a reliability problem around Unique merge enforcement in scan handler.
- Change: Unique merge enforcement in scan handler: memory index now enforces unique merged file sets before batch processing.
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

---

### Tier precedence normalized

- Need: There was a reliability problem around Tier precedence normalized.
- Change: Tier precedence normalized: tier logic is now deterministic with precedence metadata to inline marker to default.
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

---

### Diagnostics improved

- Need: There was a reliability problem around Diagnostics improved.
- Change: Diagnostics improved: scan outputs now include dedup-oriented diagnostics to explain unique-vs-skipped behavior.
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

---

### Targeted regressions passed

- Need: The team needed stronger proof that Targeted regressions passed worked as expected.
- Change: Targeted regressions passed: parser/index/tier suites passed (52 tests).
- Why it matters: This matters because the team has clearer proof that the change works.

---

### Extended regressions passed

- Need: The team needed stronger proof that Extended regressions passed worked as expected.
- Change: Extended regressions passed: extended parser/spec indexing suites passed (186 tests).
- Why it matters: This matters because the team has clearer proof that the change works.

---

### Scoped lint and validation passed

- Need: The team needed stronger proof that Scoped lint and validation passed worked as expected.
- Change: Scoped lint and spec validation passed: modified runtime and test files passed scoped ESLint and spec validation checks.
- Why it matters: This matters because the team has clearer proof that the change works.

---

### No schema migration included

- Need: The project needed a cleaner setup for No schema migration included.
- Change: No schema migration included: historical row cleanup was explicitly out of scope for this spec.
- Why it matters: This matters because setup and maintenance are easier to manage.

---

### Behavioral fix only

- Need: There was a reliability problem around Behavioral fix only.
- Change: Behavioral fix only: change focuses on deterministic indexing and tier assignment consistency.
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.


---

## Files Changed

| File | What Changed |
| ---- | ------------ |
| `.opencode/specs/system-spec-kit/022-hybrid-rag-fusion/003-index-tier-anomalies/` | Spec folder referenced by this release. |
| `.opencode/skill/system-spec-kit/mcp_server/lib/parsing/memory-parser.ts` | Canonical-path dedup and normalization flow. |
| `.opencode/skill/system-spec-kit/mcp_server/handlers/memory-index.ts` | Unique merged file-set enforcement and dedup diagnostics. |
| `.opencode/skill/system-spec-kit/mcp_server/lib/scoring/importance-tiers.ts` | Deterministic tier precedence and default mapping behavior. |
| `.opencode/skill/system-spec-kit/mcp_server/tests/memory-parser.vitest.ts` | Tier precedence and parser edge-case coverage. |
| `.opencode/skill/system-spec-kit/mcp_server/tests/handler-memory-index.vitest.ts` | Alias-root dedup regression coverage. |
| `.opencode/skill/system-spec-kit/mcp_server/tests/importance-tiers.vitest.ts` | Tier consistency assertions. |


---

## Upgrade

- No migration is required. Restart MCP services and run a fresh index scan to confirm deduplicated scan counters and stable tier mapping in diagnostics.
