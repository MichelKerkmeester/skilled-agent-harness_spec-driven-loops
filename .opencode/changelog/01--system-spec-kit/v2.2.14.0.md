## [**2.2.14.0**] - 2026-02-15

This release fix subfolder path resolution in generate context - path.isAbsolute() was not handling relative subfolder paths correctly, causing failures with nested spec folder paths like 003-parent/121-child. Consolidated path resolution logic to handle both absolute and relative inputs. It focuses on fixing important problems and explaining the results in clearer terms.

---

## What Changed

---

### Subfolder path resolution

- Need: There was a reliability problem around Subfolder path resolution.
- Change: Fixed path.isAbsolute() not handling relative subfolder paths, resolving P0 regression for nested spec folder inputs.
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

---

### Subfolder utils

- Need: There was a reliability problem around Subfolder utils.
- Change: Added reusable path resolution utility for consistent subfolder handling.
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

---

### Test coverage

- Need: There was a reliability problem around Test coverage.
- Change: Added test subfolder resolution to verify nested path resolution.
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.


---

## Files Changed

| File | What Changed |
| ---- | ------------ |
| `specs/system-spec-kit/z_archive/006-generate-context-subfolder/` | Spec folder referenced by this release. |
| `.opencode/skill/system-spec-kit/scripts/dist/memory/generate-context.js` | Updated in this release. |
| `.opencode/skill/system-spec-kit/scripts/core/subfolder-utils.ts` | Updated in this release. |
| `.opencode/skill/system-spec-kit/scripts/tests/test-subfolder-resolution.js` | Updated in this release. |


---

## Upgrade

- No action required.
