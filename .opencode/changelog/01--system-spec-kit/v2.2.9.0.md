# v2.2.9.0

> Part of [OpenCode Dev Environment](https://github.com/MichelKerkmeester/opencode--spec-kit-skilled-agent-orchestration)

---

## [**2.2.9.0**] - 2026-02-14

This release agent Routing Documentation Update - Updated SKILL and README to document agent routing changes from spec 014 (Command Agent Utilization Audit). Added Agent Dispatch table, debug delegation exception, and :with-research / :auto-debug mode suffixes. It focuses on fixing important problems and explaining the results in clearer terms. The goal is to make the workflow easier to follow and easier to trust.

---

## What Changed

---

### Agent Exclusivity exceptions

**Problem:** There was a reliability problem around Agent Exclusivity exceptions.

**Fix:** Agent Exclusivity exceptions: Added debug delegation to @debug agent only (alongside existing @handover and @research exceptions). This matters because it reduces confusing failures and makes the system more dependable.

---

### Agent Dispatch in Commands

**Problem:** This release updates Agent Dispatch in Commands.

**Fix:** Agent Dispatch in Commands: New subsection documenting all agent routing in spec_kit commands:. This matters because the release is clearer and easier to work with.

---

### @debug

**Problem:** There was a reliability problem around @debug.

**Fix:** @debug: implement/complete at failure >= 3. This matters because it reduces confusing failures and makes the system more dependable.

---

### @review

**Problem:** There was a reliability problem around @review.

**Fix:** @review: implement/complete dual-phase (Mode 2 Pre-Commit + Mode 4 Gate Validation), debug post-fix (advisory). This matters because it reduces confusing failures and makes the system more dependable.

---

### @research

**Problem:** This release updates @research.

**Fix:** @research: plan at confidence < 60%. This matters because the release is clearer and easier to work with.

---

### @handover

**Problem:** This release updates @handover.

**Fix:** @handover: plan/implement/complete/research at session end. This matters because the release is clearer and easier to work with.

---

### Mode Suffixes table expanded

**Problem:** People needed clearer guidance about Mode Suffixes table expanded.

**Fix:** Mode Suffixes table expanded: Added :with-research and :auto-debug flags with "Applies To" column showing /spec_kit:complete exclusivity. This matters because the release notes and instructions are easier to read and trust.

---

### Recent Changes section

**Problem:** People needed clearer guidance about Recent Changes section.

**Fix:** Recent Changes section: Added spec 014 agent routing compliance summary. This matters because the release notes and instructions are easier to read and trust.

---

## Files Changed

| File | What Changed |
| ---- | ------------ |
| `.claude/specs/system-spec-kit/z_archive/002-agent-routing-doc-update/` | Spec folder referenced by this release. |
| `.opencode/skill/system-spec-kit/SKILL.md - Agent Exclusivity + Agent Dispatch subsection` | Updated in this release. |
| `.opencode/skill/system-spec-kit/README.md - Mode Suffixes + Recent Changes` | Updated in this release. |

---

## Upgrade

- No action required. Pull latest to get updated documentation.
