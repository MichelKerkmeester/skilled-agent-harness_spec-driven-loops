This release auto-detected spec session selection hardened for resume/handover workflows - Session detection now deterministically avoids archived targets, mainizes alias paths, and applies safe low-confidence behavior for ambiguous selections. It focuses on fixing important problems and explaining the results in clearer terms. The goal is to make the workflow easier to follow and easier to trust.

---

## What Changed

---

### Archive avoidance enforced

**Problem:** There was a reliability problem around Archive avoidance enforced.

**Fix:** Archive avoidance enforced: detection flow prioritizes active non-archived spec folders over stale archive/fixture candidates. This matters because it reduces confusing failures and makes the system more dependable.

---

### Alias normalization stabilized

**Problem:** There was a reliability problem around Alias normalization stabilized.

**Fix:** Alias normalization stabilized: specs/ and .opencode/specs/ equivalents resolve consistently. This matters because it reduces confusing failures and makes the system more dependable.

---

### Ambiguity guard added

**Problem:** There was a reliability problem around Ambiguity guard added.

**Fix:** Ambiguity guard added: low-confidence detection paths now require confirmation or safe fallback behavior. This matters because it reduces confusing failures and makes the system more dependable.

---

### Functional regression suite passed

**Problem:** The team needed stronger proof that Functional regression suite passed worked as expected.

**Fix:** Functional regression suite passed: test folder detector functional reports 32 passed, 0 failed, 0 skipped. This matters because the team has clearer proof that the change works.

---

### Review gate passed

**Problem:** The team needed stronger proof that Review gate passed worked as expected.

**Fix:** Review gate passed: completion evidence records review score 88/100 with no P0/P1 findings. This matters because the team has clearer proof that the change works.

---

### Validation passed

**Problem:** The team needed stronger proof that Validation passed worked as expected.

**Fix:** Spec validation passed: folder-level validator succeeded for this scope. This matters because the team has clearer proof that the change works.

---

### Command guidance aligned

**Problem:** People needed clearer guidance about Command guidance aligned.

**Fix:** Command guidance aligned: /spec_kit:resume and /spec_kit:handover guidance was verified against detector behavior. This matters because the release notes and instructions are easier to read and trust.

---

### No forced no-op code churn

**Problem:** People needed clearer guidance about No forced no-op code churn.

**Fix:** No forced no-op code churn: detector implementation was confirmed active without unnecessary rewrite. This matters because the release notes and instructions are easier to read and trust.

---

## Files Changed

| File | What Changed |
| ---- | ------------ |
| `.opencode/specs/system-spec-kit/022-hybrid-rag-fusion/005-auto-detected-session-bug/` | Spec folder referenced by this release. |
| `.opencode/skill/system-spec-kit/scripts/tests/test-folder-detector-functional.js` | Regression coverage for archive preference, alias normalization, and mtime distortion cases. |
| `.opencode/skill/system-spec-kit/scripts/spec-folder/folder-detector.ts` | Verified active implementation for deterministic selection behavior. |
| `.opencode/skill/system-spec-kit/scripts/dist/spec-folder/folder-detector.js` | Verified runtime artifact alignment. |
| `.opencode/command/spec_kit/resume.md` | Verified command guidance alignment with deterministic session selection. |
| `.opencode/command/spec_kit/handover.md` | Verified command guidance alignment with selection/fallback behavior. |

---

## Upgrade

- No migration required. Existing command flows continue working with higher selection reliability; explicit path input still overrides auto-detection when provided.
