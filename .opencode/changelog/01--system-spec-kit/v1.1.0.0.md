## [**1.1.0.0**] - 2025-01-29

This release major cognitive memory upgrade implementing FSRS power-law decay validated on 100M+ users, Prediction Error Gating to prevent duplicates, and 30 bug fixes from comprehensive 10-agent audit. Memory commands aligned with template standards. It focuses on fixing important problems and explaining the results in clearer terms.

---

## What Changed

### 1. Cognitive Memory System

- Need: This release updates Cognitive Memory System.
- Change: The release updates cognitive memory system.
- Why it matters: This matters because the release is clearer and easier to work with.

### 2. FSRS power-law algorithm

- Need: This release updates FSRS power-law algorithm.
- Change: R(t,S) = * t/S)^(-0.5) replaces arbitrary exponential decay.
- Why it matters: This matters because the release is clearer and easier to work with.

### 3. Prediction Error Gating

- Need: There was a reliability problem around Prediction Error Gating.
- Change: 4-tier thresholds (0.95 DUPLICATE, 0.90 HIGH_MATCH, 0.70 MEDIUM_MATCH, 0.50 LOW_MATCH).
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

### 4. 5-state memory model

- Need: This release updates 5-state memory model.
- Change: HOT (>=0.8), WARM (0.25-0.8), COLD (0.05-0.25), DORMANT (0.02-0.05), ARCHIVED (<0.02).
- Why it matters: This matters because the release is clearer and easier to work with.

### 5. Testing Effect

- Need: The team needed stronger proof that Testing Effect worked as expected.
- Change: Accessing memories strengthens stability (desirable difficulty bonus).
- Why it matters: This matters because the team has clearer proof that the change works.

### 6. Schema v4

- Need: The project needed a cleaner setup for Schema v4.
- Change: New columns: stability, difficulty, last_review, review_count + memory_conflicts table.
- Why it matters: This matters because setup and maintenance are easier to manage.

### 7. Files

- Need: This release updates Files.
- Change: The release updates files.
- Why it matters: This matters because the release is clearer and easier to work with.

### 8. Fsrs scheduler

- Need: This release updates Fsrs scheduler.
- Change: FSRS algorithm implementation.
- Why it matters: This matters because the release is clearer and easier to work with.

### 9. Prediction error gate

- Need: There was a reliability problem around Prediction error gate.
- Change: Duplicate detection and conflict resolution.
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

### 10. Fsrs scheduler.test

- Need: The team needed stronger proof that Fsrs scheduler.test worked as expected.
- Change: 30 unit tests for FSRS.
- Why it matters: This matters because the team has clearer proof that the change works.

### 11. Verify cognitive upgrade

- Need: The team needed stronger proof that Verify cognitive upgrade worked as expected.
- Change: Comprehensive verification script.
- Why it matters: This matters because the team has clearer proof that the change works.

### 12. Critical Bugs (P0)

- Need: There was a reliability problem around Critical Bugs (P0).
- Change: The release fixes the issue in critical bugs (p0).
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

### 13. BUG-001

- Need: There was a reliability problem around BUG-001.
- Change: FSRS function never executed; fixed signature mismatch in tier classifier.
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

### 14. BUG-002

- Need: There was a reliability problem around BUG-002.
- Change: MEDIUM_MATCH = LOW_MATCH; changed LOW_MATCH to 0.50.
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

### 15. BUG-003

- Need: There was a reliability problem around BUG-003.
- Change: COLD = DORMANT thresholds; changed DORMANT to 0.02.
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

### 16. High Bugs (P1)

- Need: There was a reliability problem around High Bugs (P1).
- Change: The release fixes the issue in high bugs (p1).
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

### 17. BUG-004

- Need: There was a reliability problem around BUG-004.
- Change: LRUCache missing keys() and delete() methods.
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

### 18. BUG-005

- Need: There was a reliability problem around BUG-005.
- Change: Missing await on database check in delete/update handlers.
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

### 19. BUG-006

- Need: There was a reliability problem around BUG-006.
- Change: Unhandled promise rejection; wrapped reinforce_existing_memory in try/catch.
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

### 20. BUG-007

- Need: There was a reliability problem around BUG-007.
- Change: NaN propagation; early return with 0.5 default when no timestamps.
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

### 21. BUG-008

- Need: There was a reliability problem around BUG-008.
- Change: Data loss on restore failure; SAVEPOINT/ROLLBACK pattern in checkpoints.
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

### 22. BUG-009

- Need: There was a reliability problem around BUG-009.
- Change: ReDoS in YAML regex; line-by-line parsing in memory parser.
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

### 23. BUG-010

- Need: There was a reliability problem around BUG-010.
- Change: ReDoS in trigger patterns; bounded greedy quantifiers in trigger extractor.
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

### 24. BUG-011

- Need: There was a reliability problem around BUG-011.
- Change: Silent delete errors; added console.warn logging.
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

### 25. Medium Bugs (P2)

- Need: There was a reliability problem around Medium Bugs (P2).
- Change: The release fixes the issue in medium bugs (p2).
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

### 26. BUG-012

- Need: There was a reliability problem around BUG-012.
- Change: Cache thundering herd; loading flag with try/finally.
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

### 27. BUG-013

- Need: There was a reliability problem around BUG-013.
- Change: Inconsistent tier weights; centralized to importance tiers.
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

### 28. BUG-014

- Need: There was a reliability problem around BUG-014.
- Change: NaN from env vars; isNaN() validation in working memory.
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

### 29. BUG-015

- Need: There was a reliability problem around BUG-015.
- Change: Negative scores not clamped; Math.max(0, score) in co activation.
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

### 30. BUG-016

- Need: There was a reliability problem around BUG-016.
- Change: Partial transaction commits; track failures, rollback all.
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

### 31. BUG-017

- Need: There was a reliability problem around BUG-017.
- Change: Token metrics wrong order; capture before reassignment.
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

### 32. BUG-018

- Need: There was a reliability problem around BUG-018.
- Change: Undo doesn't check result; verify changes > 0 in history.
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

### 33. BUG-019

- Need: There was a reliability problem around BUG-019.
- Change: Migration not transactional; database.transaction() wrapper.
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

### 34. BUG-020

- Need: There was a reliability problem around BUG-020.
- Change: UTF-16 BE unsupported; manual byte-swap conversion.
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

### 35. Low Bugs (P3)

- Need: There was a reliability problem around Low Bugs (P3).
- Change: The release fixes the issue in low bugs (p3).
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

### 36. BUG-021-030

- Need: There was a reliability problem around BUG-021-030.
- Change: Input validation, error sanitization, Unicode boundaries, symlink detection, division guards.
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

### 37. MCP Server

- Need: This release updates MCP Server.
- Change: The release updates mcp server.
- Why it matters: This matters because the release is clearer and easier to work with.

### 38. Composite scoring

- Need: This release updates Composite scoring.
- Change: Added retrievability weight (0.15), adjusted similarity (0.30 to 0.25).
- Why it matters: This matters because the release is clearer and easier to work with.

### 39. Tier classifier

- Need: This release updates Tier classifier.
- Change: Extended from 3-state to 5-state model.
- Why it matters: This matters because the release is clearer and easier to work with.

### 40. Attention decay

- Need: This release updates Attention decay.
- Change: Integrated FSRS decay functions.
- Why it matters: This matters because the release is clearer and easier to work with.

### 41. Memory Commands

- Need: This release updates Memory Commands.
- Change: The release updates memory commands.
- Why it matters: This matters because the release is clearer and easier to work with.

### 42. Checkpoint

- Need: People needed clearer guidance about Checkpoint.
- Change: Template alignment: emojis, section naming.
- Why it matters: This matters because the release notes and instructions are easier to read and trust.

### 43. Database

- Need: There was a reliability problem around Database.
- Change: Added section number to COGNITIVE MEMORY MODEL, fixed emoji.
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

### 44. Save

- Need: There was a reliability problem around Save.
- Change: PHASE 1B to 2 (integer numbering), emoji fixes.
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

### 45. Search

- Need: This release updates Search.
- Change: Numbered CONSTITUTIONAL/COGNITIVE sections.
- Why it matters: This matters because the release is clearer and easier to work with.

## Files Changed

| File | What Changed |
| ---- | ------------ |
| `R(t,S) = (1 + (19/81) * t/S)^(-0.5)` | Referenced in the original changelog. |
| `fsrs-scheduler.js` | Referenced in the original changelog. |
| `prediction-error-gate.js` | Referenced in the original changelog. |
| `fsrs-scheduler.test.js` | Referenced in the original changelog. |
| `verify-cognitive-upgrade.js` | Referenced in the original changelog. |
| `tier-classifier.js` | Referenced in the original changelog. |
| `checkpoints.js` | Referenced in the original changelog. |
| `memory-parser.js` | Referenced in the original changelog. |
| `trigger-extractor.js` | Referenced in the original changelog. |
| `importance-tiers.js` | Referenced in the original changelog. |
| `working-memory.js` | Referenced in the original changelog. |
| `co-activation.js` | Referenced in the original changelog. |
| `history.js` | Referenced in the original changelog. |
| `checkpoint.md` | Referenced in the original changelog. |
| `database.md` | Referenced in the original changelog. |
| `save.md` | Referenced in the original changelog. |
| `search.md` | Referenced in the original changelog. |
