## [**2.3.0.4**] - 2026-02-22

This release frontmatter normalization and idempotent reindex workflow finalized - Canonical metadata formatting and migration execution were completed for templates/spec/memory markdown, followed by successful rebuild and regression verification. It focuses on making the instructions and release notes easier for non-developers to follow.

---

## What Changed

---

### Canonical frontmatter policy enforced

- Need: People needed clearer guidance about Canonical frontmatter policy enforced.
- Change: Canonical frontmatter policy enforced: normalization and composition workflow now uses a deterministic schema for targeted markdown classes.
- Why it matters: This matters because the release notes and instructions are easier to read and trust.

---

### Idempotent migration gate validated

- Need: The project needed a cleaner setup for Idempotent migration gate validated.
- Change: Idempotent migration gate validated: migration apply and dry-run modes were exercised with stable second-run behavior.
- Why it matters: This matters because setup and maintenance are easier to manage.

---

### Reindex workflow verified

- Need: The project needed a cleaner setup for Reindex workflow verified.
- Change: Reindex workflow verified: index rebuild completed with STATUS=OK after migration.
- Why it matters: This matters because setup and maintenance are easier to manage.

---

### Template/build checks passed

- Need: People needed clearer guidance about Template/build checks passed.
- Change: Template/build checks passed: build and template compose verification commands passed.
- Why it matters: This matters because the release notes and instructions are easier to read and trust.

---

### Migration and parser suites passed

- Need: The team needed stronger proof that Migration and parser suites passed worked as expected.
- Change: Migration and parser suites passed: backfill, parser, and indexing regressions were executed successfully.
- Why it matters: This matters because the team has clearer proof that the change works.

---

### Idempotency proof captured

- Need: This release updates Idempotency proof captured.
- Change: Idempotency proof captured: final dry-run evidence reports changed 0 / unchanged 1789 / failed 0.
- Why it matters: This matters because the release is clearer and easier to work with.

---

### Legacy archive anchor warnings remain non-blocking

- Need: There was a reliability problem around Legacy archive anchor warnings remain non-blocking.
- Change: Legacy archive anchor warnings remain non-blocking: archived invalid-anchor warnings were documented and intentionally not remediated in this scope.
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

---

### Non-functional control backlog remains

- Need: There was a reliability problem around Non-functional control backlog remains.
- Change: Non-functional control backlog remains: performance and formal governance controls remain tracked in the checklist.
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.


---

## Files Changed

| File | What Changed |
| ---- | ------------ |
| `.opencode/specs/system-spec-kit/022-hybrid-rag-fusion/004-frontmatter-indexing/` | Spec folder referenced by this release. |
| `.opencode/skill/system-spec-kit/scripts/lib/frontmatter-migration.ts` | Canonical normalization/migration helpers. |
| `.opencode/skill/system-spec-kit/scripts/dist/memory/backfill-frontmatter.js` | Migration CLI flow used in execution evidence. |
| `.opencode/skill/system-spec-kit/mcp_server/lib/parsing/memory-parser.ts` | Parser/compose alignment for normalized frontmatter. |
| `.opencode/skill/system-spec-kit/mcp_server/tests/memory-parser.vitest.ts` | Frontmatter parsing regression coverage. |
| `.opencode/skill/system-spec-kit/mcp_server/tests/spec126-full-spec-doc-indexing.vitest.ts` | Indexing regression coverage for normalized frontmatter. |
| `.opencode/skill/system-spec-kit/scripts/tests/test-frontmatter-backfill.js` | Migration behavior, malformed handling, and edge-case coverage. |


---

## Upgrade

- Run migration in dry-run mode before apply for any new corpus updates, then rerun reindex. No database schema migration is required, but the migration/reindex order must remain normalize to verify to rebuild to preserve deterministic outcomes.
