This release frontmatter normalization and idempotent reindex workflow finalized - Canonical metadata formatting and migration execution were completed for templates/spec/memory markdown, followed by successful rebuild and regression verification. It focuses on making the instructions and release notes easier for non-developers to follow. The goal is to make the workflow easier to follow and easier to trust.

---

## What Changed

---

### Canonical frontmatter policy enforced

**Problem:** People needed clearer guidance about Canonical frontmatter policy enforced.

**Fix:** Canonical frontmatter policy enforced: normalization and composition workflow now uses a deterministic schema for targeted markdown classes. This matters because the release notes and instructions are easier to read and trust.

---

### Idempotent migration gate validated

**Problem:** The project needed a cleaner setup for Idempotent migration gate validated.

**Fix:** Idempotent migration gate validated: migration apply and dry-run modes were exercised with stable second-run behavior. This matters because setup and maintenance are easier to manage.

---

### Reindex workflow verified

**Problem:** The project needed a cleaner setup for Reindex workflow verified.

**Fix:** Reindex workflow verified: index rebuild completed with STATUS=OK after migration. This matters because setup and maintenance are easier to manage.

---

### Template/build checks passed

**Problem:** People needed clearer guidance about Template/build checks passed.

**Fix:** Template/build checks passed: build and template compose verification commands passed. This matters because the release notes and instructions are easier to read and trust.

---

### Migration and parser suites passed

**Problem:** The team needed stronger proof that Migration and parser suites passed worked as expected.

**Fix:** Migration and parser suites passed: backfill, parser, and indexing regressions were executed successfully. This matters because the team has clearer proof that the change works.

---

### Idempotency proof captured

**Problem:** This release updates Idempotency proof captured.

**Fix:** Idempotency proof captured: final dry-run evidence reports changed 0 / unchanged 1789 / failed 0. This matters because the release is clearer and easier to work with.

---

### Legacy archive anchor warnings remain non-blocking

**Problem:** There was a reliability problem around Legacy archive anchor warnings remain non-blocking.

**Fix:** Legacy archive anchor warnings remain non-blocking: archived invalid-anchor warnings were documented and intentionally not remediated in this scope. This matters because it reduces confusing failures and makes the system more dependable.

---

### Non-functional control backlog remains

**Problem:** There was a reliability problem around Non-functional control backlog remains.

**Fix:** Non-functional control backlog remains: performance and formal governance controls remain tracked in the checklist. This matters because it reduces confusing failures and makes the system more dependable.

---

## Files Changed

| File | What Changed |
| ---- | ------------ |
| `.opencode/specs/system-spec-kit/022-hybrid-rag-fusion/004-frontmatter-indexing/` | Spec folder referenced by this release. |
| `.opencode/skill/system-spec-kit/scripts/lib/frontmatter-migration.ts` | Canonical normalization/migration helpers. |
| `.opencode/skill/system-spec-kit/scripts/dist/memory/backfill-frontmatter.js` | Migration CLI flow used in execution evidence. |
| `.opencode/skill/system-spec-kit/mcp_server/lib/parsing/memory-parser.ts` | Parser/compose alignment for normalized frontmatter. |
| `.opencode/skill/system-spec-kit/mcp_server/tests/memory-parser.vitest.ts` | Frontmatter parsing regression coverage. |
| `.opencode/skill/system-spec-kit/mcp_server/tests/spec126-full-spec-doc-indexing.vitest.ts` | Indexing regression coverage for normalized frontmatter. |
| `.opencode/skill/system-spec-kit/scripts/tests/test-frontmatter-backfill.js` | Migration behavior, malformed handling, and edge-case coverage. |

---

## Upgrade

- Run migration in dry-run mode before apply for any new corpus updates, then rerun reindex. No database schema migration is required, but the migration/reindex order must remain normalize to verify to rebuild to preserve deterministic outcomes.
