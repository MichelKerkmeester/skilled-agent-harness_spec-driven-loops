## [**1.2.6.0**] - 2026-02-05

This release ecosystem-wide deep analysis and remediation  with 3 critical bugs fixed, schema unification via Migration v12, gate numbering standardization, and AGENTS naming migration across 59 files. It focuses on making the instructions and release notes easier for non-developers to follow.

---

## What Changed

### 1. CREATE_LINKED database constraint

- Need: There was a reliability problem around CREATE_LINKED database constraint.
- Change: Added to memory_conflicts CHECK constraint in migration v4 schema and create_schema().
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

### 2. Ghost tools in speckit

- Need: There was a reliability problem around Ghost tools in speckit.
- Change: Rewrote tool layers from wrong 5-layer to correct 7-layer, 22-tool architecture; removed memory_drift_context and memory_drift_learn references.
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

### 3. Stale SKILL cross-references

- Need: People needed clearer guidance about Stale SKILL cross-references.
- Change: Fixed 5 section references + standardized template file counts.
- Why it matters: This matters because the release notes and instructions are easier to read and trust.

### 4. Three conflicting database schema schemas unified

- Need: There was a reliability problem around Three conflicting database schema schemas unified.
- Change: Memory_conflicts had incompatible column names across vector index, create_schema(), and prediction error gate; unified to 14-column schema.
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

### 5. Migration v12

- Need: The project needed a cleaner setup for Migration v12.
- Change: DROP+CREATE with unified schema; schema version 11 → 12.
- Why it matters: This matters because setup and maintenance are easier to manage.

### 6. Column renames

- Need: The project needed a cleaner setup for Column renames.
- Change: Similarity_score → similarity, notes → reason in both INSERT paths.
- Why it matters: This matters because setup and maintenance are easier to manage.

### 7. Error swallowing removed

- Need: There was a reliability problem around Error swallowing removed.
- Change: Silent try/catch replaced with console.error() logging.
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

### 8. Ensure_conflicts_table() deprecated

- Need: There was a reliability problem around Ensure_conflicts_table() deprecated.
- Change: Converted to no-op in prediction error gate.
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

### 9. Gate enforcement

- Need: This release updates Gate enforcement.
- Change: Full renumbering to match AGENTS (Gate 1=Understanding, 2=Skill Routing, 3=Spec Folder).
- Why it matters: This matters because the release is clearer and easier to work with.

### 10. Old gate references removed

- Need: Older gate numbering was still showing up in active files after the workflow had changed.
- Change: Removed stale Gate 4/5/6 references.
- Why it matters: This matters because the project becomes smaller and easier to maintain.

### 11. Legacy install guide

- Need: People needed clearer guidance about Legacy install guide.
- Change: Rewritten from 7-gate to 3-gate + 3 behavioral rules.
- Why it matters: This matters because the release notes and instructions are easier to read and trust.

### 12. Context server

- Need: This release updates Context server.
- Change: Added toolCache.shutdown() to shutdown signal/termination signal/uncaughtException.
- Why it matters: This matters because the release is clearer and easier to work with.

### 13. Access tracker

- Need: Access tracker was no longer needed in its old form.
- Change: Removed duplicate shutdown signal/termination signal handlers.
- Why it matters: This matters because the project becomes smaller and easier to maintain.

### 14. Schema migration.test

- Need: People needed clearer guidance about Schema migration.test.
- Change: Column names updated for v12 schema.
- Why it matters: This matters because the release notes and instructions are easier to read and trust.

### 15. Decision format, epistemic vectors

- Need: People needed clearer guidance about Decision format, epistemic vectors.
- Change: Gate number corrections.
- Why it matters: This matters because the release notes and instructions are easier to read and trust.

### 16. README schema references

- Need: People needed clearer guidance about the README files that still mentioned the older schema version.
- Change: Schema version references updated.
- Why it matters: This matters because the release notes and instructions are easier to read and trust.

### 17. AGENTS naming

- Need: This release updates AGENTS naming.
- Change: 59 files updated (~218 replacements) to use AGENTS instead of CLAUDE.
- Why it matters: This matters because the release is clearer and easier to work with.

### 18. Speckit

- Need: This release updates Speckit.
- Change: Added 4 missing commands + 3 scripts to Capability Scan.
- Why it matters: This matters because the release is clearer and easier to work with.

### 19. SKILL

- Need: This release updates SKILL.
- Change: Added 3 scripts to Key Scripts table.
- Why it matters: This matters because the release is clearer and easier to work with.

### 20. Skill advisor

- Need: There was a reliability problem around Skill advisor.
- Change: Memory/context intent boost (passes 0.8 threshold); debug disambiguation.
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

### 21. Template counts

- Need: People needed clearer guidance about Template counts.
- Change: Standardized across speckit, SKILL, README.
- Why it matters: This matters because the release notes and instructions are easier to read and trust.

## Files Changed

| File | What Changed |
| ---- | ------------ |
| `AGENTS.md` | Referenced in the original changelog. |
| `vector-index.js` | Referenced in the original changelog. |
| `speckit.md` | Referenced in the original changelog. |
| `SKILL.md` | Referenced in the original changelog. |
| `prediction-error-gate.js` | Referenced in the original changelog. |
| `gate-enforcement.md` | Referenced in the original changelog. |
| `context-server.js` | Referenced in the original changelog. |
| `access-tracker.js` | Referenced in the original changelog. |
| `schema-migration.test.js` | Referenced in the original changelog. |
| `decision-format.md` | Referenced in the original changelog. |
| `epistemic-vectors.md` | Referenced in the original changelog. |
| `search/README.md` | Referenced in the original changelog. |
| `core/README.md` | Referenced in the original changelog. |
| `CLAUDE.md` | Referenced in the original changelog. |
| `skill_advisor.py` | Referenced in the original changelog. |
| `README.md` | Referenced in the original changelog. |
