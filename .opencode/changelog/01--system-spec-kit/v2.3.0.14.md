This release bulk index hard-clean + z_archive indexing parity - Added z_archive spec-doc discovery support, hardened forced indexing behavior to avoid PE/reconsolidation collapse during rebuilds, and completed a clean full re-index with alias cleanup and database sync back to main paths. It focuses on fixing important problems and explaining the results in clearer terms. The goal is to make the workflow easier to follow and easier to trust.

---

## What Changed

#### Enabled z_archive -doc discovery by removing

This release updates Enabled z_archive -doc discovery by removing. Enabled z_archive spec-doc discovery by removing z_archive from spec-doc exclusion in memory index discovery. This matters because the release is clearer and easier to work with.

&nbsp;

#### Enabled z_archive -doc validation path by

The team needed stronger proof that Enabled z_archive -doc validation path by worked as expected. Enabled z_archive spec-doc validation path by allowing spec documents under z_archive in memory file classification. This matters because the team has clearer proof that the change works.

&nbsp;

#### Coverage result

This release updates Coverage result. Coverage result: all z_archive memory files are indexed; most z_archive spec docs are indexed, with residual exclusions limited to scratch/ and hidden backup folders. This matters because the release is clearer and easier to work with.

&nbsp;

#### Force-path hardening

There was a reliability problem around Force-path hardening. Force-path hardening: during force: true indexing, prediction-error candidate selection and reconsolidation-on-save are skipped to prevent duplicate suppression/merge behavior in bulk rebuild scenarios. This matters because it reduces confusing failures and makes the system more dependable.

&nbsp;

#### Prevents repeated upsert/collapse loops for identical

This release updates Prevents repeated upsert/collapse loops for identical. Prevents repeated upsert/collapse loops for identical file paths during long-running forced scans. This matters because the release is clearer and easier to work with.

&nbsp;

#### Performed full memory database clean and

This release updates Performed full memory database clean and. Performed full memory database clean and bulk re-index. This matters because the release is clearer and easier to work with.

&nbsp;

#### Synced rebuilt profile database to canonical

This release updates Synced rebuilt profile database to main. Synced rebuilt profile database to main database paths used by MCP runtime. This matters because the release is clearer and easier to work with.

&nbsp;

#### Reconciled residual identical alias artifacts and

This release updates Reconciled residual identical alias artifacts and. Reconciled residual identical alias artifacts and refreshed database update marker for live runtime visibility. This matters because the release is clearer and easier to work with.

&nbsp;

#### Memory_health

This release updates Memory_health. Memory_health reports healthy system with alias divergence resolved. This matters because the release is clearer and easier to work with.

&nbsp;

#### Targeted test suites passed

The team needed stronger proof that Targeted test suites passed worked as expected. Targeted test suites passed:. This matters because the team has clearer proof that the change works.

&nbsp;

#### Memory save integration.vitest

The team needed stronger proof that Memory save integration.vitest worked as expected. The release updates memory save integration.vitest. This matters because the team has clearer proof that the change works.

&nbsp;

#### Folder discovery integration.vitest

The team needed stronger proof that Folder discovery integration.vitest worked as expected. The release updates folder discovery integration.vitest. This matters because the team has clearer proof that the change works.

---

## Files Changed

| File | What Changed |
| ---- | ------------ |
| `.opencode/specs/system-spec-kit/022-hybrid-rag-fusion-refinement/010-comprehensive-remediation/` | Spec folder referenced by this release. |
| `.opencode/skill/system-spec-kit/mcp_server/handlers/memory-index-discovery.ts` | Updated in this release. |
| `.opencode/skill/system-spec-kit/mcp_server/lib/parsing/memory-parser.ts` | Updated in this release. |
| `.opencode/skill/system-spec-kit/mcp_server/handlers/memory-save.ts` | Updated in this release. |
| `.opencode/skill/system-spec-kit/mcp_server/dist/handlers/memory-index-discovery.js` | Updated in this release. |
| `.opencode/skill/system-spec-kit/mcp_server/dist/lib/parsing/memory-parser.js` | Updated in this release. |
| `.opencode/skill/system-spec-kit/mcp_server/dist/handlers/memory-save.js` | Updated in this release. |

---

## Upgrade

- No schema migration required. Existing indexing flows continue to work; forced reindexes are now safer for bulk rebuild operations, and z_archive spec docs participate in discovery except for intentional scratch/ and hidden-directory exclusions.
