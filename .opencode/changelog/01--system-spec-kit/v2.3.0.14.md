## [**2.3.0.14**] - 2026-02-28

**Bulk index hard-clean + z_archive indexing parity** - Added `z_archive` spec-doc discovery support, hardened forced indexing behavior to avoid PE/reconsolidation collapse during rebuilds, and completed a clean full re-index with alias cleanup and DB sync back to canonical paths.

> Spec folder: `.opencode/specs/02--system-spec-kit/022-hybrid-rag-fusion-refinement/010-comprehensive-remediation/`

---

## Highlights

### Category: Indexing and Discovery

- **Enabled z_archive spec-doc discovery** by removing `z_archive` from spec-doc exclusion in memory index discovery.
- **Enabled z_archive spec-doc validation path** by allowing spec documents under `z_archive` in memory file classification.
- **Coverage result:** all `z_archive` memory files are indexed; most `z_archive` spec docs are indexed, with residual exclusions limited to `scratch/` and hidden backup folders.

### Category: Forced Reindex Safety

- **Force-path hardening:** during `force: true` indexing, prediction-error candidate selection and reconsolidation-on-save are skipped to prevent duplicate suppression/merge behavior in bulk rebuild scenarios.
- Prevents repeated upsert/collapse loops for identical file paths during long-running forced scans.

### Category: Operational Remediation

- Performed **full memory DB clean** and **bulk re-index**.
- Synced rebuilt profile DB to canonical DB paths used by MCP runtime.
- Reconciled residual identical alias artifacts and refreshed DB update marker for live runtime visibility.

### Category: Verification

- `memory_health` reports healthy system with alias divergence resolved.
- Targeted test suites passed:
  - `tests/memory-save-integration.vitest.ts`
  - `tests/folder-discovery-integration.vitest.ts`

## Files Changed

- `.opencode/skill/system-spec-kit/mcp_server/handlers/memory-index-discovery.ts`
- `.opencode/skill/system-spec-kit/mcp_server/lib/parsing/memory-parser.ts`
- `.opencode/skill/system-spec-kit/mcp_server/handlers/memory-save.ts`
- `.opencode/skill/system-spec-kit/mcp_server/dist/handlers/memory-index-discovery.js`
- `.opencode/skill/system-spec-kit/mcp_server/dist/lib/parsing/memory-parser.js`
- `.opencode/skill/system-spec-kit/mcp_server/dist/handlers/memory-save.js`

## Upgrade

No schema migration required. Existing indexing flows continue to work; forced reindexes are now safer for bulk rebuild operations, and `z_archive` spec docs participate in discovery except for intentional `scratch/` and hidden-directory exclusions.
