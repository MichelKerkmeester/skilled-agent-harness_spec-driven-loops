## [**2.3.0.14**] - 2026-02-28

This release bulk index hard-clean + z_archive indexing parity - Added z_archive spec-doc discovery support, hardened forced indexing behavior to avoid PE/reconsolidation collapse during rebuilds, and completed a clean full re-index with alias cleanup and database sync back to canonical paths. It focuses on fixing important problems and explaining the results in clearer terms.

---

## What Changed

### Enabled z_archive -doc discovery by removing

- Need: This release updates Enabled z_archive -doc discovery by removing.
- Change: Enabled z_archive spec-doc discovery by removing z_archive from spec-doc exclusion in memory index discovery.
- Why it matters: This matters because the release is clearer and easier to work with.

### Enabled z_archive -doc validation path by

- Need: The team needed stronger proof that Enabled z_archive -doc validation path by worked as expected.
- Change: Enabled z_archive spec-doc validation path by allowing spec documents under z_archive in memory file classification.
- Why it matters: This matters because the team has clearer proof that the change works.

### Coverage result

- Need: This release updates Coverage result.
- Change: Coverage result: all z_archive memory files are indexed; most z_archive spec docs are indexed, with residual exclusions limited to scratch/ and hidden backup folders.
- Why it matters: This matters because the release is clearer and easier to work with.

### Force-path hardening

- Need: There was a reliability problem around Force-path hardening.
- Change: Force-path hardening: during force: true indexing, prediction-error candidate selection and reconsolidation-on-save are skipped to prevent duplicate suppression/merge behavior in bulk rebuild scenarios.
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

### Prevents repeated upsert/collapse loops for identical

- Need: This release updates Prevents repeated upsert/collapse loops for identical.
- Change: Prevents repeated upsert/collapse loops for identical file paths during long-running forced scans.
- Why it matters: This matters because the release is clearer and easier to work with.

### Performed full memory database clean and

- Need: This release updates Performed full memory database clean and.
- Change: Performed full memory database clean and bulk re-index.
- Why it matters: This matters because the release is clearer and easier to work with.

### Synced rebuilt profile database to canonical

- Need: This release updates Synced rebuilt profile database to canonical.
- Change: Synced rebuilt profile database to canonical database paths used by MCP runtime.
- Why it matters: This matters because the release is clearer and easier to work with.

### Reconciled residual identical alias artifacts and

- Need: This release updates Reconciled residual identical alias artifacts and.
- Change: Reconciled residual identical alias artifacts and refreshed database update marker for live runtime visibility.
- Why it matters: This matters because the release is clearer and easier to work with.

### Memory_health

- Need: This release updates Memory_health.
- Change: Memory_health reports healthy system with alias divergence resolved.
- Why it matters: This matters because the release is clearer and easier to work with.

### Targeted test suites passed

- Need: The team needed stronger proof that Targeted test suites passed worked as expected.
- Change: Targeted test suites passed:.
- Why it matters: This matters because the team has clearer proof that the change works.

### Memory save integration.vitest

- Need: The team needed stronger proof that Memory save integration.vitest worked as expected.
- Change: The release updates memory save integration.vitest.
- Why it matters: This matters because the team has clearer proof that the change works.

### Folder discovery integration.vitest

- Need: The team needed stronger proof that Folder discovery integration.vitest worked as expected.
- Change: The release updates folder discovery integration.vitest.
- Why it matters: This matters because the team has clearer proof that the change works.


---

## Files Changed

| File | What Changed |
| ---- | ------------ |
| `.opencode/specs/system-spec-kit/022-hybrid-rag-fusion-refinement/010-comprehensive-remediation/` | Spec folder referenced by this release. |
| `.opencode/skill/system-spec-kit/mcp_server/handlers/memory-index-discovery.ts` | Updated in this release. |
| `.opencode/skill/system-spec-kit/mcp_server/lib/parsing/memory-parser.ts` | Updated in this release. |
| `.opencode/skill/system-spec-kit/mcp_server/handlers/memory-save.ts` | Updated in this release. |
| `.opencode/skill/system-spec-kit/mcp_server/dist/handlers/memory-index-discovery.js` | Updated in this release. |
| `.opencode/skill/system-spec-kit/mcp_server/dist/lib/parsing/memory-parser.js` | Updated in this release. |
| `.opencode/skill/system-spec-kit/mcp_server/dist/handlers/memory-save.js` | Updated in this release. |


---

## Upgrade

- No schema migration required. Existing indexing flows continue to work; forced reindexes are now safer for bulk rebuild operations, and z_archive spec docs participate in discovery except for intentional scratch/ and hidden-directory exclusions.
