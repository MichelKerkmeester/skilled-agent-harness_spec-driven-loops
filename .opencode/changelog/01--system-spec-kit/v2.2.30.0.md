## [**2.2.30.0**] - 2026-02-21

This release fix MCP Server "no such table" Errors in /memory:manage -- Eliminated SQLite "no such table" errors (memory_index, checkpoints) that occurred when the LLM fell back to raw sqlite3 Bash commands against uninitialized databases. Four-layer fix: removed Bash from manage allowed-tools, added missing checkDatabaseUpdated() guard to health handler, extracted idempotent ensureCompanionTables() for schema resilience, and improved error messages with actionable recovery instructions. Code review pass: normalized all snake_case locals to camelCase, consolidated toErrorMessage usage in checkpoints, fixed stale section references in manage, and updated health table list. It focuses on fixing important problems and explaining the results in clearer terms.

---

## What Changed

### 1. Removed Bash fallback path

- Need: There was a reliability problem around Removed Bash fallback path.
- Change: Removed Bash fallback path: Removed Bash from manage allowed-tools to prevent the LLM from running raw sqlite3 commands against uninitialized database files. Added explicit tool enforcement block prohibiting direct database access.
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

### 2. Health handler guard

- Need: There was a reliability problem around Health handler guard.
- Change: Health handler guard: Added await checkDatabaseUpdated() to handleMemoryHealth() - the only handler missing this standard guard call. Added no such table detection that returns E_SCHEMA_MISSING with recovery instructions instead of silently reporting "degraded" status.
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

### 3. Companion table resilience

- Need: There was a reliability problem around Companion table resilience.
- Change: Companion table resilience: Extracted ensureCompanionTables() function containing idempotent CREATE TABLE IF NOT EXISTS statements for memory_history, checkpoints, and memory_conflicts plus their indexes. Called from both the existing-database early-return path and the new-database path of create_schema(), ensuring companion tables are always created even if memory_index already exists.
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

### 4. Actionable error messages

- Need: There was a reliability problem around Actionable error messages.
- Change: Actionable error messages: Improved and aligned error messages in requireDb(), checkpoints.getDatabase(), handleMemoryList(), and handleMemoryStats() null-db responses to suggest specific recovery actions.
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

### 5. CamelCase normalization

- Need: This release updates CamelCase normalization.
- Change: CamelCase normalization: Renamed all snake_case local variables in handleMemoryDelete, handleMemoryUpdate, handleMemoryList, and handleMemoryStats to camelCase (spec_folder to specFolder, checkpoint_name to checkpointName, allow_partial_update to allowPartialUpdate, etc.).
- Why it matters: This matters because the release is clearer and easier to work with.

### 6. ToErrorMessage consolidation

- Need: There was a reliability problem around ToErrorMessage consolidation.
- Change: ToErrorMessage consolidation: Imported and applied toErrorMessage() from db helpers across all 8 inline error instanceof Error ? error.message : String(error) patterns in checkpoints.
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

### 7. NonNullable<> type

- Need: This release updates NonNullable<> type.
- Change: NonNullable<> type: Replaced and {} intersection trick in requireDb() return type with standard NonNullable<> utility type.
- Why it matters: This matters because the release is clearer and easier to work with.

### 8. Property shorthand

- Need: This release updates Property shorthand.
- Change: Property shorthand: Applied ES2015 shorthand for embeddingModelReady and memoryCount in health handler response.
- Why it matters: This matters because the release is clearer and easier to work with.

### 9. Migration note

- Need: The project needed a cleaner setup for Migration note.
- Change: Migration note: Added removal guidance comment to backward-compatible snake_case alias block.
- Why it matters: This matters because setup and maintenance are easier to manage.

### 10. Error handling table

- Need: People needed clearer guidance about Error handling table.
- Change: Error handling table: Added two new rows to manage error handling table: "MCP tool unavailable" and "Database not initialized" with actionable guidance.
- Why it matters: This matters because the release notes and instructions are easier to read and trust.

### 11. Section references

- Need: People needed clearer guidance about Section references.
- Change: Section references: Fixed 7 stale section number references in manage routing tree (Section 5 to 7, etc.).
- Why it matters: This matters because the release notes and instructions are easier to read and trust.

### 12. Health table list

- Need: People needed clearer guidance about Health table list.
- Change: Health table list: Updated health check output template to include memory_history, checkpoints, and memory_conflicts tables; removed session_state (managed by session-manager, not companion tables).
- Why it matters: This matters because the release notes and instructions are easier to read and trust.

## Files Changed

| File | What Changed |
| ---- | ------------ |
| `.opencode/command/memory/manage.md` | Removed Bash from allowed-tools, added tool enforcement block, added error recovery rows, fixed section references, updated health table list. |
| `.opencode/skill/system-spec-kit/mcp_server/handlers/memory-crud.ts` | Added checkDatabaseUpdated() to health handler, no such table detection, camelCase normalization across all handlers, property shorthand, alias migration note. |
| `.opencode/skill/system-spec-kit/mcp_server/lib/search/vector-index-impl.ts` | Extracted ensureCompanionTables() with TSDoc, called from both paths of create_schema(). |
| `.opencode/skill/system-spec-kit/mcp_server/utils/db-helpers.ts` | Improved requireDb() error message, NonNullable<> return type. |
| `.opencode/skill/system-spec-kit/mcp_server/lib/storage/checkpoints.ts` | Improved getDatabase() error message, removed module prefix from throw, imported and applied toErrorMessage across 8 inline patterns. |

## Upgrade

- Restart the MCP server after updating to apply schema changes. On next startup, ensure_companion_tables() will auto-create any missing companion tables. No manual migration needed.
