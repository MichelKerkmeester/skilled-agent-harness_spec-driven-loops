## [**1.2.1.0**] - 2026-02-02

This release memory command consolidation reducing from 9 commands to 5 for a simpler mental model, with intent detection improvements and cross-platform temp file handling. It explains the main changes in simple language so the release is easier to understand.

---

## What Changed

### 1. Memory command consolidation

- Need: This release updates Memory command consolidation.
- Change: Reduced from 9 to 5 commands.
- Why it matters: This matters because the release is clearer and easier to work with.

### 2. /memory:learn

- Need: This release updates /memory:learn.
- Change: Now includes correct subcommand for corrections.
- Why it matters: This matters because the release is clearer and easier to work with.

### 3. /memory:manage

- Need: This release updates /memory:manage.
- Change: New unified command for database, checkpoint, and maintenance operations.
- Why it matters: This matters because the release is clearer and easier to work with.

### 4. Intent detection

- Need: This release updates Intent detection.
- Change: Updated to phrase-based matching (avoids false positives).
- Why it matters: This matters because the release is clearer and easier to work with.

### 5. Rollback mechanism

- Need: There was a reliability problem around Rollback mechanism.
- Change: Added for checkpoint restore operations.
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

### 6. Checkpoint validation

- Need: The team needed stronger proof that Checkpoint validation worked as expected.
- Change: Added before cleanup operations.
- Why it matters: This matters because the team has clearer proof that the change works.

### 7. Cross-platform temp file handling

- Need: This release updates Cross-platform temp file handling.
- Change: Added.
- Why it matters: This matters because the release is clearer and easier to work with.

### 8. /memory:search

- Need: /memory:search was no longer needed in its old form.
- Change: Replaced by /memory:context [deprecated].
- Why it matters: This matters because the project becomes smaller and easier to maintain.

### 9. /memory:database

- Need: /memory:database was no longer needed in its old form.
- Change: Consolidated into /memory:manage [deprecated].
- Why it matters: This matters because the project becomes smaller and easier to maintain.

### 10. /memory:checkpoint

- Need: /memory:checkpoint was no longer needed in its old form.
- Change: Consolidated into /memory:manage [deprecated].
- Why it matters: This matters because the project becomes smaller and easier to maintain.

### 11. /memory:why

- Need: /memory:why was no longer needed in its old form.
- Change: Removed (causal tracing available via MCP tools) [deprecated].
- Why it matters: This matters because the project becomes smaller and easier to maintain.

### 12. /memory:correct

- Need: /memory:correct was no longer needed in its old form.
- Change: Consolidated into /memory:learn correct [deprecated].
- Why it matters: This matters because the project becomes smaller and easier to maintain.

### 13. MCP tool references

- Need: There was a reliability problem around MCP tool references.
- Change: Fixed (memory_drift_context to memory_context).
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

### 14. Non-existent tool reference

- Need: There was a reliability problem around Non-existent tool reference.
- Change: Removed references to non-existent memory_drift_learn tool.
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

### 15. AutoImportance type inconsistency

- Need: There was a reliability problem around AutoImportance type inconsistency.
- Change: Fixed in learn.
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

### 16. Example numbering gap

- Need: There was a reliability problem around Example numbering gap.
- Change: Fixed in learn.
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

### 17. Date typo

- Need: There was a reliability problem around Date typo.
- Change: Fixed (2026 to 2025) in context.
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

## Files Changed

| File | What Changed |
| ---- | ------------ |
| `/memory:context` | Referenced in the original changelog. |
| `/memory:manage` | Referenced in the original changelog. |
| `/memory:learn correct` | Referenced in the original changelog. |
| `learn.md` | Referenced in the original changelog. |
| `context.md` | Referenced in the original changelog. |
