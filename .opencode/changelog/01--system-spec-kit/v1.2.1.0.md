# v1.2.1.0

> Part of [OpenCode Dev Environment](https://github.com/MichelKerkmeester/opencode-spec-kit-framework)

---

## [**1.2.1.0**] - 2026-02-02

**Memory command consolidation** reducing from 9 commands to 5 for a simpler mental model, with intent detection improvements and cross-platform temp file handling.

---

## Highlights

### Changed
- **Memory command consolidation** — Reduced from 9 to 5 commands
- **/memory:learn** — Now includes `correct` subcommand for corrections
- **/memory:manage** — New unified command for database, checkpoint, and maintenance operations
- **Intent detection** — Updated to phrase-based matching (avoids false positives)
- **Rollback mechanism** — Added for checkpoint restore operations
- **Checkpoint validation** — Added before cleanup operations
- **Cross-platform temp file handling** — Added
- **/memory:search** — Replaced by `/memory:context` [deprecated]
- **/memory:database** — Consolidated into `/memory:manage` [deprecated]
- **/memory:checkpoint** — Consolidated into `/memory:manage` [deprecated]
- **/memory:why** — Removed (causal tracing available via MCP tools) [deprecated]
- **/memory:correct** — Consolidated into `/memory:learn correct` [deprecated]

### Fixed
- **MCP tool references** — Fixed (`memory_drift_context` to `memory_context`)
- **Non-existent tool reference** — Removed references to non-existent `memory_drift_learn` tool
- **autoImportance type inconsistency** — Fixed in learn.md
- **Example numbering gap** — Fixed in learn.md
- **Date typo** — Fixed (2026 to 2025) in context.md
