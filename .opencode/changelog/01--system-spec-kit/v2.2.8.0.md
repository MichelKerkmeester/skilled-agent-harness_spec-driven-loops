## [**2.2.8.0**] - 2026-02-13

This release yAML and .md prompt reduction  compressing 20 files by 55.4% total - 9,462 lines removed while preserving all routing logic, workflow steps, and asset paths. It explains the main changes in simple language so the release is easier to understand.

---

## What Changed

### 13 YAML prompt assets

- Need: 13 YAML prompt assets was no longer needed in its old form.
- Change: Compressed from 13,333 to 5,378 lines (59.7% reduction) in command/spec_kit/assets/ using compact YAML flow notation ({key: value}, [a, b]), removed Mermaid diagrams (never rendered in CLI), decorative separators, verbose description fields.
- Why it matters: This matters because the project becomes smaller and easier to maintain.

### 7 .md command files

- Need: This release updates 7 .md command files.
- Change: Compressed from 3,746 to 2,239 lines (40.2% reduction) in command/spec_kit/; all routing logic, workflow steps, and config keys preserved.
- Why it matters: This matters because the release is clearer and easier to work with.

### Combined

- Need: Combined was no longer needed in its old form.
- Change: 9,462 lines removed (55.4% total) across 20 files.
- Why it matters: This matters because the project becomes smaller and easier to maintain.

### Validation

- Need: The team needed stronger proof that Validation worked as expected.
- Change: 13/13 YAML files parse valid, all step counts preserved, all asset paths intact.
- Why it matters: This matters because the team has clearer proof that the change works.


---

## Files Changed

| File | What Changed |
| ---- | ------------ |
| `command/spec_kit/assets/` | Referenced in the original changelog. |
| `command/spec_kit/` | Referenced in the original changelog. |
