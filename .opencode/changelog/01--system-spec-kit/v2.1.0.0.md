## [**2.1.0.0**] - 2026-02-09

This release memory index deep audit and fix  correcting the critical FSRS formula , adding token budget observability, and removing 214 lines of dead code from vector index. It focuses on making the instructions and release notes easier for non-developers to follow.

---

## What Changed

### Token budget observability

- Need: This release updates Token budget observability.
- Change: TokenBudget field in response metadata for all handler responses.
- Why it matters: This matters because the release is clearer and easier to work with.

### Console.error logging

- Need: There was a reliability problem around Console.error logging.
- Change: When token budgets exceeded.
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

### Hint injection

- Need: This release updates Hint injection.
- Change: In responses when budget thresholds are hit.
- Why it matters: This matters because the release is clearer and easier to work with.

### Test suite

- Need: The team needed stronger proof that Test suite worked as expected.
- Change: 46/60 → 58/58 (100% pass rate); 2 orphan test files removed.
- Why it matters: This matters because the team has clearer proof that the change works.

### Vector index

- Need: Vector index was no longer needed in its old form.
- Change: 705 → 491 lines (214 lines dead code removed).
- Why it matters: This matters because the project becomes smaller and easier to maintain.

### 12+ READMEs

- Need: People needed clearer guidance about 12+ READMEs.
- Change: Rewritten across MCP server subdirectories.
- Why it matters: This matters because the release notes and instructions are easier to read and trust.

### Search weights

- Need: This release updates Search weights.
- Change: Cleaned dead parameters and regex patterns.
- Why it matters: This matters because the release is clearer and easier to work with.

### CRITICAL: FSRS formula corrected

- Need: There was a reliability problem around CRITICAL: FSRS formula corrected.
- Change: Factor changed from 19.0 to 19/81 (~0.2346), formula from R = )^(-0.5) to R = ^(-0.5); memories were decaying ~4.5x slower than FSRS v4 standard; now R(S,S) = 0.9 as specified.
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

### ClearConstitutionalCache bug

- Need: There was a reliability problem around ClearConstitutionalCache bug.
- Change: In vector index; was calling wrong internal method.
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

### 3 broken delegations

- Need: There was a reliability problem around 3 broken delegations.
- Change: In vector-index facade.
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

### Integration test paths

- Need: There was a reliability problem around Integration test paths.
- Change: Bug-fix tests, preflight tests.
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

### SafeParseJson security alignment

- Need: There was a reliability problem around SafeParseJson security alignment.
- Change: Across modules.
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

### 5 test failures

- Need: There was a reliability problem around 5 test failures.
- Change: Confidence-tracker, handler-memory-search, memory-context, tier-classifier, tool-cache.
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

### Orphan core

- Need: Orphan core was no longer needed in its old form.
- Change: Was shadowing dist/core/ directory.
- Why it matters: This matters because the project becomes smaller and easier to maintain.

### 9 orphaned files

- Need: The team needed stronger proof that 9 orphaned files worked as expected.
- Change: 7 dist .js + 2 test files.
- Why it matters: This matters because the team has clearer proof that the change works.

### 3 fabricated sections

- Need: 3 fabricated sections was no longer needed in its old form.
- Change: In memory system.
- Why it matters: This matters because the project becomes smaller and easier to maintain.


---

## Files Changed

| File | What Changed |
| ---- | ------------ |
| `19/81` | Referenced in the original changelog. |
| `vector-index.ts` | Referenced in the original changelog. |
| `search-weights.json` | Referenced in the original changelog. |
| `R = (1 + t/(FACTOR*S))^(-0.5)` | Referenced in the original changelog. |
| `R = (1 + FACTOR*t/S)^(-0.5)` | Referenced in the original changelog. |
| `dist/core.js` | Referenced in the original changelog. |
| `dist/core/` | Referenced in the original changelog. |
| `.js` | Referenced in the original changelog. |
| `references/memory/memory_system.md` | Referenced in the original changelog. |
