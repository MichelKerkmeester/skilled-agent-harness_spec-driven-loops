This release bundles two deprecation cutovers that complete the v3.4.0.0 modernization arc.

**Phase 013 — `[spec]/memory/*.md` write path retirement.** The v3.4.0.0 release notes stated the surface was retired, but the runtime still created, wrote, indexed, and read from `[spec]/memory/` on every save — a deep-review audit (packet `026/013-memory-folder-deprecation-audit`, 8 iterations) surfaced 25 findings (9 P0 runtime contradictions) documenting the gap. Phase 013 cuts the write path, deletes the manual-snippet template, migrates tests and docs, and leaves the runtime consistent with the v3.4.0.0 intent.

**Phase 012 — Middleware cleanup (M9).** The v3.4.0.0 canonical-doc routing made the `/spec_kit:handover` and `/spec_kit:debug` wrapper commands plus the `@handover` and `@speckit` agents redundant: continuity now lives in canonical spec docs via `/memory:save`, and `@debug` dispatches via the Task tool. Phase 012 M9 deletes the 15 enumerated redundant paths, replaces the `@speckit` exclusivity narrative with a distributed-governance rule across the four canonical governance surfaces (`CLAUDE.md`, `AGENTS.md`, `AGENTS_example_fs_enterprises.md`, `system-spec-kit/SKILL.md`), and removes the `:auto-debug` flag from `/spec_kit:complete` in favor of explicit user escalation.

> Spec folders:
> - `.opencode/specs/system-spec-kit/026-graph-and-context-optimization/013-memory-folder-deprecation-audit/` (Level 2, audit + Path A execution)
> - `.opencode/specs/system-spec-kit/026-graph-and-context-optimization/012-spec-kit-commands/` (Level 2, M1–M9; Phase 012 M9 covered below)

---

## overview

This release closes the remaining documentation and workflow drift from the v3.4.0.0 modernization work. Phase 013 removes the last live `[spec]/memory/*.md` write path, while Phase 012 M9 removes deprecated wrapper commands and agent surfaces without regressing canonical continuity, recovery, or debug escalation.

## TABLE OF CONTENTS

- [MEMORY FOLDER WRITE PATH REMOVED](#memory-folder-write-path-removed)
- [TEMPLATE AND DOC CLEANUP](#template-and-doc-cleanup)
- [MIDDLEWARE DEPRECATION (PHASE 012 M9)](#middleware-deprecation-phase-012-m9)
- [PHASE 013 (MEMORY FOLDER RETIREMENT)](#phase-013-memory-folder-retirement)
- [PHASE 012 M9 (MIDDLEWARE CLEANUP)](#phase-012-m9-middleware-cleanup)

---

## Memory Folder Write Path Removed

The save workflow no longer produces `[spec]/memory/*.md` artifacts. Every category that used to land there now routes through the content-router into canonical spec docs, so the continuity surface is the same one the packet already tracks.

#### `[spec]/memory/` directories no longer created on save

`setupContextDirectory` used to unconditionally `mkdir [spec]/memory/` for every save, even when no memory file was produced. It has been replaced with `ensureSpecFolderExists`, which only validates that the target spec folder exists. New spec folders created via `create.sh` no longer get a `memory/` subdirectory, and the `--help` output no longer mentions one. Existing `memory/` directories in older packets are preserved as a read-only historical corpus.

&nbsp;

#### `writeFilesAtomically` and the memory/*.md write leg deleted

`scripts/core/file-writer.ts` was the primary producer of `[spec]/memory/YYYY-MM-DD__topic.md` files via atomic-commit semantics. The whole module is deleted. Step 9 of the save workflow no longer renders a memory file, no longer assigns a `ctxFilename`, and no longer calls the atomic-commit path. `writeFilesAtomically`, `checkForDuplicateContent`, and the sibling-glob dedup logic are gone.

&nbsp;

#### `indexMemory` wrapper removed; vector DB indexing goes through canonical docs

The script-side `scripts/core/memory-indexer.ts::indexMemory` wrapper previously called `vectorIndex.indexMemory({ filePath: path.join(contextDir, contextFilename) })` after every save, registering the new memory file as memory #N in the vector DB. That wrapper is deleted along with its `updateMetadataEmbeddingStatus` helper. Indexing now flows exclusively through Step 11.5 auto-index (introduced in this session's Step 11.5 patch), which re-indexes canonical spec docs + `graph-metadata.json` after save. The MCP handler call sites (`pe-gating.ts`, `chunking-orchestrator.ts`, `save/reconsolidation-bridge.ts`, `save/create-record.ts`) still call `vectorIndex.indexMemory()` directly with externally-supplied paths — those are unchanged.

&nbsp;

#### Dedup contract (phantom) and causal-link reads deleted

`save.md` §Session Deduplication previously specified a SHA-256 + 1h + interactive-prompt dedup contract with zero runtime implementation. The documentation section has been removed; the contract was a phantom spec. The pre-save SHA-1 overlap check that globbed `memory/*.md` siblings is also deleted. `autoPopulateCausalLinks` in `memory-metadata.ts` (the function that walked `[spec]/memory/*.md` to pre-populate causal graph edges) is deleted; packet-level relationships route through `graph-metadata.json.manual.depends_on/related_to/supersedes` and MCP causal-edge handlers.

---

## Template and Doc Cleanup

The template and documentation surface that taught operators the retired memory-file workflow has been purged or rewritten to match the new canonical-doc routing.

#### `context_template.md` deleted

The 589-line template used to render standalone memory snippets (`context_template.md`) has been deleted along with its couplings: the `LEGACY_TEMPLATE_BANNER_RE` regex in `shared/parsing/memory-template-contract.ts`, the banner-strip regex in `scripts/renderers/template-renderer.ts`, the filename-classification branch in `scripts/lib/frontmatter-migration.ts:1263`, and three test files that specifically exercised the retired template (`memory-quality-phase1.vitest.ts`, `memory-quality-phase6-template.test.ts`, `anchor-id-simplification.vitest.ts`). The `templates/memory/` directory (template + README) is gone.

&nbsp;

#### CHK-052 removed from four checklist templates

The Level 2, Level 3, Level 3+, and `addendum/level2-verify` checklist templates each carried a `CHK-052 "Findings saved to memory/"` verification row. That row has been removed. New spec folders no longer inherit a retirement-violating QA gate, and existing checklists that still carry CHK-052 can be safely unchecked (the underlying surface is gone).

&nbsp;

#### Reference and command docs aligned with retired state

`.opencode/command/memory/save.md` dropped the Session Deduplication section, the Recovery-table phantom-dedup row, and any step description that referenced `[spec]/memory/*.md` as an output artifact. `manage.md` already carried accurate retirement wording. `mcp_server/INSTALL_GUIDE.md` lost the stale `memory context in spec folders: specs/*/memory/` pointer. `references/memory/save_workflow.md`, `memory_system.md`, `trigger_config.md`, and `references/workflows/worked_examples.md` now describe the content-router + canonical-doc routing instead of the retired write path. `SKILL.md`, `README.md`, and the four `templates/level_{1,2,3,3+}/README.md` files have their retirement language harmonized so every audit surface agrees with every other audit surface.

&nbsp;

#### Test fixtures migrated or removed

Eight vitest files that asserted `[spec]/memory/*.md` behavior (`slug-uniqueness`, `tree-thinning`, `validate-memory-quality`, `artifact-routing`, `handler-memory-index`, `full-spec-doc-indexing`, `content-hash-dedup`, plus the three phase-1/6 template tests) have been deleted or migrated to canonical-doc fixtures. The self-contradictory `full-spec-doc-indexing.vitest.ts` ("returns memory" vs "rejects legacy memory") has been reduced to the "rejects legacy memory" half, which matches the new runtime.

---

## Middleware Deprecation (Phase 012 M9)

Phase 012 M9 retires the wrapper commands and exclusive-writer agents that the v3.4.0.0 canonical-doc routing made redundant. 15 enumerated paths are deleted, ~50 files are updated to swap the `@speckit` exclusivity narrative for a distributed-governance rule, and `/spec_kit:complete` loses its `:auto-debug` flag in favor of explicit user escalation.

#### Deleted commands and agents (15 verified paths)

The following files have been removed from disk; `ls` returns "No such file or directory" for each path:

- **Commands (5):** `.opencode/command/spec_kit/handover.md`, `.opencode/command/spec_kit/debug.md`, `.opencode/command/spec_kit/assets/spec_kit_handover_full.yaml`, `.opencode/command/spec_kit/assets/spec_kit_debug_auto.yaml`, `.opencode/command/spec_kit/assets/spec_kit_debug_confirm.yaml`
- **`@handover` agent (4 runtime mirrors):** `.opencode/agent/handover.md`, `.claude/agents/handover.md`, `.codex/agents/handover.toml`, `.gemini/agents/handover.md`
- **`@speckit` agent (4 runtime mirrors):** `.opencode/agent/speckit.md`, `.claude/agents/speckit.md`, `.codex/agents/speckit.toml`, `.gemini/agents/speckit.md`
- **Gemini command mirrors (2):** `.gemini/commands/spec_kit/handover.toml`, `.gemini/commands/spec_kit/debug.toml`

A zero-reference grep sweep across active docs (`*.md`, `*.txt`, `*.toml`, `*.yaml`, `*.json` with the standard archive/iterations/changelog/specs exclusions) returns 0 matches for `/spec_kit:(handover|debug)`, `@handover`, and `@speckit`.

&nbsp;

#### Distributed-Governance Rule replaces `@speckit` exclusivity

The `@speckit` exclusive-writer narrative for spec folder `*.md` files has been replaced with a distributed-governance rule, identical wording across all four canonical governance surfaces (`CLAUDE.md`, `AGENTS.md`, `AGENTS_example_fs_enterprises.md`, `.opencode/skill/system-spec-kit/SKILL.md`):

> Any agent writing spec folder docs (`*.md`) MUST use templates from `.opencode/skill/system-spec-kit/templates/level_N/`, run `bash .opencode/skill/system-spec-kit/scripts/spec/validate.sh [spec_folder] --strict` after each file write, and route continuity updates through `/memory:save`. `@deep-research` retains exclusive write access for `research/research.md`; `@debug` retains exclusive write access for `debug-delegation.md`.

Quality enforcement now lives in template scaffolds plus `validate.sh --strict` rather than agent gatekeeping. `@deep-research` and `@debug` keep their file-specific exclusivity, and the `@debug` agent itself is preserved across all four runtimes with description-only updates that clarify Task-tool dispatch.

&nbsp;

#### `/memory:save` becomes the canonical `handover.md` maintainer

`.opencode/command/memory/save.md` gained a "Handover Document Maintenance" subsection in §1 positioning `/memory:save` as the canonical maintainer of `handover.md::session-log` via the existing `handover_state` content-router category. The `handover_state` contract row now references the template path `.opencode/skill/system-spec-kit/templates/handover.md` for initial creation. §8 Next-Steps and §9 Related-Commands no longer mention the deleted `/spec_kit:handover` command. Stop-hook autosave and `/spec_kit:resume` recovery ladder are unchanged: the resume ladder still reads `handover.md` first (8 references in `resume.md`), then `_memory.continuity`, then canonical spec docs.

&nbsp;

#### `:auto-debug` flag removed from `/spec_kit:complete`

The `:auto-debug` argument-hint, EXECUTION PROTOCOL branch, and workflow logic have been removed entirely from `.opencode/command/spec_kit/complete.md` and both `spec_kit_complete_{auto,confirm}.yaml` assets. The replacement is an explicit user-escalation path: when `failure_count >= 3` during implementation, the workflow surfaces the diagnostic summary and the user may manually dispatch `@debug` via the Task tool. `grep :auto-debug` over both YAML files returns 0 matches.

&nbsp;

#### Orchestrate, command, and skill docs aligned

- **4 `@orchestrate` runtime mirrors** (`.opencode/agent/`, `.claude/agents/`, `.codex/agents/`, `.gemini/agents/`): `@speckit` (row 4) and `@handover` (row 9) removed from the routing matrix, LEAF agent list, and agent-files table. `@debug` routing language updated to Task-tool dispatch. Decision-tree rows for `/spec_kit:debug` and `/spec_kit:handover` removed; replaced with `/memory:save` + Task-tool dispatch patterns.
- **7 `/spec_kit:*` command files** (`resume.md`, `plan.md`, `complete.md`, `implement.md`, `start.md`, `deep-research.md`, `deep-review.md`): deprecated agents removed from Do-Not-Dispatch lists; deprecated-command Next-Steps rows updated to `/memory:save`.
- **10 YAML workflow assets** (`spec_kit_{complete,plan,implement,start,deep-research,deep-review}_{auto,confirm}.yaml`): handover-check steps and deprecated-command Next-Steps removed; `@speckit`/`@handover` dispatch calls cleaned out while preserving shared `handover.md` template references.
- **15+ skill, install-guide, and reference docs** (`.opencode/README.md`, 3 install guides, 2 command READMEs, system-spec-kit README, sk-code-web README + debugging workflow, 8 system-spec-kit reference documents, 5 CLI skill references, miscellaneous): deprecated-command and deprecated-agent references removed; `@debug` references updated to Task-tool dispatch; `@speckit (spec folder docs)` and `@handover (handover.md)` attributions replaced with distributed-governance language while preserving template, recovery-surface, and `@deep-research` / `@debug` exclusivity refs.

---

### Phase 013 (Memory Folder Retirement)

- Historical `[spec]/memory/*.md` files in existing spec folders are preserved on disk as a read-only content archive.
- Existing `memory_index` DB rows pointing at those historical files remain indexed; no migration script deletes them. `/memory:search` still returns historical matches when relevant.
- `/spec_kit:resume` recovery is unchanged: the ladder reads `handover.md` first, then `_memory.continuity`, then canonical spec docs. It never read `[spec]/memory/*.md` in any earlier release, so no regression is possible.
- Stop-hook `runContextAutosave()` is unchanged: it routes through `generate-context.js`, which now produces canonical-doc updates instead of memory files. Continuity at session end is preserved.
- `_memory.continuity` YAML frontmatter key, `.opencode/command/memory/` command namespace, and MCP tool names (`memory_save`, `memory_index_scan`, `memory_context`, `memory_stats`, `memory_update`) are all unchanged.
- The `system-spec-kit` skill package, all Level 1/2/3/3+ templates except the retired `context_template.md`, all MCP server source code (handlers, routing, schemas, types, content-router) are unchanged.

### Phase 012 M9 (Middleware Cleanup)

- **`@debug` agent preserved across all 4 runtimes** (`.opencode/agent/debug.md`, `.claude/agents/debug.md`, `.codex/agents/debug.toml`, `.gemini/agents/debug.md`). Description-only updates clarify Task-tool dispatch; workflow, permissions, phases, and template references are unchanged.
- **`@deep-research` exclusivity for `research/research.md` preserved** as a standalone rule (no longer a carve-out from `@speckit`).
- **Templates preserved:** `templates/handover.md` (initial-creation source for `/memory:save`), `templates/debug-delegation.md` (used by `@debug`), and all `templates/level_{1,2,3,3+}/` files remain unchanged.
- **MCP server code unchanged:** `handlers/memory-save.ts`, `routing-prototypes.json` (including the `handover_state` cluster + 14 negative-hint references), and the `'handover_state'` type in `types.ts` are all preserved. The `handover_state` content-router category is the runtime backbone that lets `/memory:save` continue to maintain `handover.md::session-log`.
- **`/spec_kit:resume` recovery ladder unchanged:** still reads `handover.md` first (8 references in `resume.md`), then `_memory.continuity`, then canonical spec docs. Functional resume on a spec folder with `handover.md` works exactly as before.
- **Trade-off accepted:** Full 7-section `handover.md` regeneration on demand is no longer available as a standalone wrapper — `/memory:save` still maintains the `session-log` anchor through the content router, and stop-hook autosave continues to refresh continuity. A future packet may enhance `/memory:save` to auto-initialize from the full template.
- **Trade-off accepted:** Spec folder write quality is now discipline-based plus programmatic (`validate.sh --strict`) rather than agent-based exclusivity. The distributed-governance rule plus `validate.sh --strict` enforcement at the YAML workflow level catches template drift before completion.

---

### Phase 013

| Check | Result |
|-------|--------|
| `cd .opencode/skill/system-spec-kit/scripts && npm run build` | PASS (after follow-up — see Note below) |
| `cd .opencode/skill/system-spec-kit/mcp_server && npm run build` | PASS |
| `create.sh --help` no longer mentions `memory/` | PASS |
| Test spec folder scaffolded via `create.sh` contains no `memory/` subdir | PASS |
| Audit packet 013 grep sweep for `context_template` | 0 matches |
| Audit packet 013 grep sweep for active `writeFilesAtomically`, `autoPopulateCausalLinks`, `checkForDuplicateContent`, script-side `indexMemory` wrapper calls | 0 matches |
| `vectorIndex.indexMemory` call sites in MCP handlers (4 locations — `pe-gating`, `chunking-orchestrator`, `reconsolidation-bridge`, `create-record`) | Preserved, unchanged |
| `/spec_kit:resume` recovery ladder | Unchanged |
| `handover.md`, `_memory.continuity`, canonical spec docs | Preserved and invested in |

> **Note (post-publish addendum).** A second-pass deep-review (r2, 14-iter) caught two test files (`scripts/tests/workflow-warning.vitest.ts`, `scripts/tests/memory-indexer-weighting.vitest.ts`) that still imported the deleted `indexMemory` / `updateMetadataEmbeddingStatus` symbols. They were testing the retired surface and have been removed in a follow-up commit, restoring the build-pass claim above. The same r2 pass also: (a) cleaned vestigial public-contract fields `contextFilename` + `indexingStatus` from `WorkflowResult`; (b) removed dead-compute Steps 8.5/8.6/CG-07/CG-07b that scored a never-persisted artifact; (c) removed the phantom Session Deduplication block from `save.md` Pre-Flight Check 2; (d) reframed `memory_system.md` Indexable Content Sources table to mark the legacy `memory/*.md` row as retired-compatibility-only. The compat alias `setupContextDirectory` (re-export of `ensureSpecFolderExists`) is intentionally preserved because 5+ existing test files import it.

### Phase 012 M9

| Check | Result |
|-------|--------|
| Phase 012 packet — tasks `[x]` | 54 / 54 |
| Phase 012 packet — checklist items `[x]` | 46 / 46 (10 P0, 7 P1, 29 P2) |
| Phase 012 packet — tasks blocked | 0 |
| 15 enumerated deletion paths verified absent via `ls` | PASS |
| Distributed-governance rule present in CLAUDE.md, AGENTS.md, AGENTS_example_fs_enterprises.md, system-spec-kit/SKILL.md | PASS (4 / 4 surfaces, identical wording) |
| `:auto-debug` removed from `spec_kit_complete_{auto,confirm}.yaml` | PASS (0 grep matches) |
| Zero-reference sweep for `/spec_kit:(handover\|debug)` across active docs (excluding archive/iterations/changelog/specs) | 0 matches |
| Zero-reference sweep for `@handover\|@speckit` across active docs | 0 matches |
| `@debug` runtime preservation (4 / 4) | PASS |
| `@deep-research` runtime preservation + `research/research.md` exclusivity | PASS |
| `templates/handover.md`, `templates/debug-delegation.md`, `templates/level_{1,2,3,3+}/` preservation | PASS |
| `routing-prototypes.json` `handover_state` cluster preservation | PASS (1 cluster + 14 negative-hint refs intact) |
| `/spec_kit:resume` `handover.md` recovery references | PASS (8 refs intact) |
| `/memory:save` "Handover Document Maintenance" subsection in §1 + template path in `handover_state` contract | PASS |
| Deep-review iteration count to convergence | 2 iterations (1 P1 caught + fixed: stale `@speckit` nodes in `system-spec-kit/graph-metadata.json:147–157`) |
| sk-doc validator on Phase 012 modified markdown | 26 PASS / 21 WARN / 0 FAIL |
| Packet-local nested changelog generated | `.opencode/specs/system-spec-kit/026-graph-and-context-optimization/changelog/changelog-026-012-spec-kit-commands.md` |
| `decision-record.md` created with corrected shared-line similarity overlap metric | PASS |

---

### Phase 013

1. **Residual test hits (205 matches).** Broad grep for `memory/.*\.md` across `scripts/tests/` and `mcp_server/tests/` still returns 205 hits outside the migrated-surface slice. These are memory-infrastructure tests (trigger phrases, frontmatter parsing, embedding quality) that reference `memory/*.md` as a file-shape token in fixtures, not as a retirement-violating surface. A full grep-zero state requires a broader follow-up pass through tests unrelated to the runtime surface this release closes.
2. **Vitest runner startup.** Both packages have pre-existing vitest startup issues (`scripts` can't resolve `vitest/config`; `mcp_server` hangs on summary emit in some environments). Those are unrelated to this release and can be diagnosed separately.
3. **Path C deferred.** The deep-review audit identified 4 findings (F017, F019, F020, F021) in the dedup-contract-phantom family. Path A supersedes them in context (`save.md` §Session Deduplication is deleted, so the contract is no longer a phantom — there's simply no dedup contract). If future work wants deduplication semantics, a new packet should design them against the canonical-doc content-router, not against the retired memory-file surface.
4. **DB row preservation.** Historical `memory_index` rows pointing at `memory/*.md` files remain indexed (option 8a from the plan). This was a deliberate choice: the historical corpus retains retrieval signal for older packet context. A future cleanup can delete stale rows if the corpus becomes a retrieval noise source.

### Phase 012 M9

1. **Packet 012 strict-validation debt deferred.** `validate.sh --strict` against the 012 packet still exits 2 with three categories of failures, all scoped outside M9 implementation: (a) `SPEC_DOC_INTEGRITY` — 126 stale markdown-path references in older packet docs (`spec.md`, `plan.md`, `tasks.md`, `checklist.md`, `decision-record.md`); (b) `SPEC_DOC_SUFFICIENCY` — `research/research.md` is missing a citation; (c) `FRONTMATTER_MEMORY_BLOCK` — `implementation-summary.md`'s `recent_action` text is flagged as too narrative. None of these block M9; they are pre-existing packet-doc hygiene debt that should be cleared in a focused follow-on packet.
2. **No runtime fixture execution.** Phase 012 verification is source-contract grep evidence plus deep-review code audit. Fixture-based dry runs of `/spec_kit:start` on empty/repair/placeholder-upgrade folders, `/spec_kit:plan` and `/spec_kit:complete` on `no-spec`/`partial-folder`/`repair-mode` folders, and deep-research seed-create + post-synthesis write-back are NOT exercised in this release. The contracts are implemented and grep-verified; runtime fixture execution is recommended before the first user-facing run.
3. **Standalone `handover.md` regeneration unavailable.** The wrapper that produced a full 7-section `handover.md` from a single command is gone. `/memory:save` continues to maintain the `session-log` anchor through the content router, but a packet that wants the full template structure must initialize from `templates/handover.md` directly. A future enhancement to `/memory:save` could auto-initialize from the full template when no `handover.md` exists.

---

No migration required for either phase. The release is backward compatible:

### Phase 013

- Existing spec folders keep their `memory/` directories and historical `.md` files as read-only archive.
- New spec folders never get a `memory/` directory.
- Runtime save path routes to canonical docs via the existing content-router; no new configuration needed.
- `SPECKIT_INDEX_SPEC_DOCS` env var behavior unchanged.
- `SPECKIT_AUTO_INDEX_TOUCHED` (added earlier this session for Step 11.5 auto-index) kill switch still works.

### Phase 012 M9

- **Replace `/spec_kit:handover` with `/memory:save`** for end-of-session continuity. `/memory:save` updates `handover.md::session-log` via the existing `handover_state` content-router category. If your spec folder lacks a `handover.md`, copy from `.opencode/skill/system-spec-kit/templates/handover.md`.
- **Replace `/spec_kit:debug` with direct Task-tool dispatch** of `@debug`. The agent itself is unchanged across all 4 runtimes; only the wrapper command is gone.
- **Replace `/spec_kit:complete :auto-debug`** with the explicit user-escalation path: when `failure_count >= 3` during implementation, the workflow surfaces a diagnostic summary and the user dispatches `@debug` via the Task tool.
- **`@speckit` exclusivity is gone**, replaced by the distributed-governance rule. Any agent writing spec folder docs MUST use templates from `.opencode/skill/system-spec-kit/templates/level_N/`, run `validate.sh --strict` after each file write, and route continuity updates through `/memory:save`. `@deep-research` and `@debug` keep file-specific exclusivity for `research/research.md` and `debug-delegation.md`.
- **No environment variables, MCP tool names, or content-router categories changed.** `handover_state`, `narrative_progress`, `narrative_delivery`, `decision`, `research_finding`, `task_update`, `metadata_only`, `drop` all behave exactly as before.

Per-packet details:
- Phase 013: `.opencode/specs/system-spec-kit/026-graph-and-context-optimization/013-memory-folder-deprecation-audit/review/review-report.md` (473-line audit report with 25 findings tabulated + 3 remediation paths analyzed + Path A chosen).
- Phase 012 M9: `.opencode/specs/system-spec-kit/026-graph-and-context-optimization/012-spec-kit-commands/implementation-summary.md` and `decision-record.md`; nested changelog at `.opencode/specs/system-spec-kit/026-graph-and-context-optimization/changelog/changelog-026-012-spec-kit-commands.md`.
