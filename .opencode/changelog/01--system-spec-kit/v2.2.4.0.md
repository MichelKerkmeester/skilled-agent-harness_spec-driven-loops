## [**2.2.4.0**] - 2026-02-12

This release 4-source indexing pipeline, anchor prefix matching, and 165 new tests expanding memory system to index project READMEs alongside spec files, constitutional files, and skill READMEs. It focuses on making the instructions and release notes easier for non-developers to follow.

---

## What Changed

---

### 4-source indexing pipeline

- Need: People needed clearer guidance about 4-source indexing pipeline.
- Change: Memory system now indexes from spec files, constitutional files, skill READMEs, AND project READMEs (previously only 3 sources).
- Why it matters: This matters because the release notes and instructions are easier to read and trust.

---

### FindProjectReadmes()

- Need: People needed clearer guidance about FindProjectReadmes().
- Change: New discovery function for project-level README files with importance weight 0.4.
- Why it matters: This matters because the release notes and instructions are easier to read and trust.

---

### IncludeReadmes parameter

- Need: People needed clearer guidance about IncludeReadmes parameter.
- Change: Controls README scanning in memory_index_scan (default: true).
- Why it matters: This matters because the release notes and instructions are easier to read and trust.

---

### Tiered importance weights

- Need: People needed clearer guidance about Tiered importance weights.
- Change: User work , Project READMEs , Skill READMEs with scoring formula score *= (0.5 + importance_weight).
- Why it matters: This matters because the release notes and instructions are easier to read and trust.

---

### Anchor prefix matching

- Need: There was a reliability problem around Anchor prefix matching.
- Change: Anchors: ['summary'] now matches composite IDs like summary-session-... with exact-match priority and shortest-prefix fallback.
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

---

### README anchor schema

- Need: People needed clearer guidance about README anchor schema.
- Change: 74 READMEs anchored with ~473 anchor tags across the project.
- Why it matters: This matters because the release notes and instructions are easier to read and trust.

---

### YAML frontmatter support

- Need: People needed clearer guidance about YAML frontmatter support.
- Change: READMEs can include frontmatter with title, description, trigger_phrases, importance_tier.
- Why it matters: This matters because the release notes and instructions are easier to read and trust.

---

### README

- Need: People needed clearer guidance about README.
- Change: New directory structure documentation (245 lines, 9 sections).
- Why it matters: This matters because the release notes and instructions are easier to read and trust.

---

### Readme indexing

- Need: People needed clearer guidance about Readme indexing.
- Change: Comprehensive reference doc for README indexing pipeline (291 lines).
- Why it matters: This matters because the release notes and instructions are easier to read and trust.

---

### 165 new tests

- Need: People needed clearer guidance about 165 new tests.
- Change: Across 8 test files covering README parsing, discovery, integration, regression, prefix matching, and anchor ID validation.
- Why it matters: This matters because the release notes and instructions are easier to read and trust.

---

### Context template

- Need: People needed clearer guidance about Context template.
- Change: Simplified all 24 anchor IDs, removed {{SESSION_ID}}-{{SPEC_FOLDER}} suffixes for clean semantic names.
- Why it matters: This matters because the release notes and instructions are easier to read and trust.

---

### FindSkillReadmes()

- Need: People needed clearer guidance about FindSkillReadmes().
- Change: Now handles symlinked READMEs.
- Why it matters: This matters because the release notes and instructions are easier to read and trust.

---

### Search results

- Need: There was a reliability problem around Search results.
- Change: Anchor filtering supports prefix matching with hyphen-boundary enforcement.
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

---

### Memory system

- Need: This release updates Memory system.
- Change: Updated from "three sources" to "four sources".
- Why it matters: This matters because the release is clearer and easier to work with.

---

### Save workflow

- Need: People needed clearer guidance about Save workflow.
- Change: Added project READMEs to indexed content documentation.
- Why it matters: This matters because the release notes and instructions are easier to read and trust.

---

### SKILL

- Need: People needed clearer guidance about SKILL.
- Change: Expanded README Content Discovery section with findProjectReadmes() and weight tiers.
- Why it matters: This matters because the release notes and instructions are easier to read and trust.

---

### README

- Need: People needed clearer guidance about README.
- Change: Fixed 17 parameter documentation mismatches (3 phantom removed, 14 undocumented added).
- Why it matters: This matters because the release notes and instructions are easier to read and trust.

---

### Troubleshooting

- Need: There was a reliability problem around Troubleshooting.
- Change: Fixed version v1.7.1 to v1.7.2, documented dual decay model (FSRS day-based + turn-based).
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

---

### Test statistics

- Need: The team needed stronger proof that Test statistics worked as expected.
- Change: Updated from 3,872 to 4,037 tests, 114 to 120 test files.
- Why it matters: This matters because the team has clearer proof that the change works.

---

### 7 ADRs documented

- Need: This release updates 7 ADRs documented.
- Change: ADR-001 through ADR-007.
- Why it matters: This matters because the release is clearer and easier to work with.

---

### 85 tasks tracked

- Need: This release updates 85 tasks tracked.
- Change: T001-T085.
- Why it matters: This matters because the release is clearer and easier to work with.

---

### 192 checklist items

- Need: The team needed stronger proof that 192 checklist items worked as expected.
- Change: CHK-001-192.
- Why it matters: This matters because the team has clearer proof that the change works.

---

### README indexing audit

- Need: People needed clearer guidance about README indexing audit.
- Change: 96 READMEs on disk, 93 → 94 indexed (100% coverage after symlink fix).
- Why it matters: This matters because the release notes and instructions are easier to read and trust.

---

### Anchor lookup failure

- Need: There was a reliability problem around Anchor lookup failure.
- Change: Anchors: ['summary'] returned empty when keys were composite IDs; now uses prefix fallback.
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

---

### Anchor ID validation

- Need: People needed clearer guidance about Anchor ID validation.
- Change: Template-generated IDs (80-120 chars with underscores) violated VALID_ANCHOR_PATTERN; simplified to short semantic names.
- Why it matters: This matters because the release notes and instructions are easier to read and trust.

---

### Symlink README discovery

- Need: People needed clearer guidance about Symlink README discovery.
- Change: FindSkillReadmes() missed symlinked READMEs; added isSymbolicLink() check.
- Why it matters: This matters because the release notes and instructions are easier to read and trust.

---

### Resume detection bugs

- Need: There was a reliability problem around Resume detection bugs.
- Change: 5 root causes: wrong base path in glob patterns, insufficient glob depth, wrong semantic query in tier 2, too-generic trigger in tier 3, tier skipping.
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

---

### README

- Need: People needed clearer guidance about README.
- Change: Anchor name mismatch (structure → architecture).
- Why it matters: This matters because the release notes and instructions are easier to read and trust.

---

### False bug report corrected

- Need: There was a reliability problem around False bug report corrected.
- Change: Check anchors awk was incorrectly reported as having a / parsing bug; B5 audit confirmed no bug exists.
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.


---

## Files Changed

| File | What Changed |
| ---- | ------------ |
| `README.md` | Referenced in the original changelog. |
| `.opencode/README.md` | Referenced in the original changelog. |
| `readme_indexing.md` | Referenced in the original changelog. |
| `context_template.md` | Referenced in the original changelog. |
| `search-results.ts` | Referenced in the original changelog. |
| `memory_system.md` | Referenced in the original changelog. |
| `save_workflow.md` | Referenced in the original changelog. |
| `SKILL.md` | Referenced in the original changelog. |
| `mcp_server/README.md` | Referenced in the original changelog. |
| `troubleshooting.md` | Referenced in the original changelog. |
| `mcp-code-mode/README.md` | Referenced in the original changelog. |
| `/` | Referenced in the original changelog. |
| `check-anchors.sh` | Referenced in the original changelog. |
