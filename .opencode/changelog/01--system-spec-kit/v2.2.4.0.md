This release 4-source indexing pipeline, anchor prefix matching, and 165 new tests expanding memory system to index project READMEs alongside spec files, constitutional files, and skill READMEs. It focuses on making the instructions and release notes easier for non-developers to follow. The goal is to make the workflow easier to follow and easier to trust.

---

## What Changed

---

### 4-source indexing pipeline

**Problem:** People needed clearer guidance about 4-source indexing pipeline.

**Fix:** Memory system now indexes from spec files, constitutional files, skill READMEs, AND project READMEs (previously only 3 sources). This matters because the release notes and instructions are easier to read and trust.

---

### FindProjectReadmes()

**Problem:** People needed clearer guidance about FindProjectReadmes().

**Fix:** New discovery function for project-level README files with importance weight 0.4. This matters because the release notes and instructions are easier to read and trust.

---

### IncludeReadmes parameter

**Problem:** People needed clearer guidance about IncludeReadmes parameter.

**Fix:** Controls README scanning in memory_index_scan (default: true). This matters because the release notes and instructions are easier to read and trust.

---

### Tiered importance weights

**Problem:** People needed clearer guidance about Tiered importance weights.

**Fix:** User work , Project READMEs , Skill READMEs with scoring formula score *= (0.5 + importance_weight). This matters because the release notes and instructions are easier to read and trust.

---

### Anchor prefix matching

**Problem:** There was a reliability problem around Anchor prefix matching.

**Fix:** Anchors: ['summary'] now matches composite IDs like summary-session-... with exact-match priority and shortest-prefix fallback. This matters because it reduces confusing failures and makes the system more dependable.

---

### README anchor schema

**Problem:** People needed clearer guidance about README anchor schema.

**Fix:** 74 READMEs anchored with ~473 anchor tags across the project. This matters because the release notes and instructions are easier to read and trust.

---

### YAML frontmatter support

**Problem:** People needed clearer guidance about YAML frontmatter support.

**Fix:** READMEs can include frontmatter with title, description, trigger_phrases, importance_tier. This matters because the release notes and instructions are easier to read and trust.

---

### README

**Problem:** People needed clearer guidance about README.

**Fix:** New directory structure documentation (245 lines, 9 sections). This matters because the release notes and instructions are easier to read and trust.

---

### Readme indexing

**Problem:** People needed clearer guidance about Readme indexing.

**Fix:** Comprehensive reference doc for README indexing pipeline (291 lines). This matters because the release notes and instructions are easier to read and trust.

---

### 165 new tests

**Problem:** People needed clearer guidance about 165 new tests.

**Fix:** Across 8 test files covering README parsing, discovery, integration, regression, prefix matching, and anchor ID validation. This matters because the release notes and instructions are easier to read and trust.

---

### Context template

**Problem:** People needed clearer guidance about Context template.

**Fix:** Simplified all 24 anchor IDs, removed {{SESSION_ID}}-{{SPEC_FOLDER}} suffixes for clean semantic names. This matters because the release notes and instructions are easier to read and trust.

---

### FindSkillReadmes()

**Problem:** People needed clearer guidance about FindSkillReadmes().

**Fix:** Now handles symlinked READMEs. This matters because the release notes and instructions are easier to read and trust.

---

### Search results

**Problem:** There was a reliability problem around Search results.

**Fix:** Anchor filtering supports prefix matching with hyphen-boundary enforcement. This matters because it reduces confusing failures and makes the system more dependable.

---

### Memory system

**Problem:** This release updates Memory system.

**Fix:** Updated from "three sources" to "four sources". This matters because the release is clearer and easier to work with.

---

### Save workflow

**Problem:** People needed clearer guidance about Save workflow.

**Fix:** Added project READMEs to indexed content documentation. This matters because the release notes and instructions are easier to read and trust.

---

### SKILL

**Problem:** People needed clearer guidance about SKILL.

**Fix:** Expanded README Content Discovery section with findProjectReadmes() and weight tiers. This matters because the release notes and instructions are easier to read and trust.

---

### README

**Problem:** People needed clearer guidance about README.

**Fix:** Fixed 17 parameter documentation mismatches (3 phantom removed, 14 undocumented added). This matters because the release notes and instructions are easier to read and trust.

---

### Troubleshooting

**Problem:** There was a reliability problem around Troubleshooting.

**Fix:** Fixed version v1.7.1 to v1.7.2, documented dual decay model (FSRS day-based + turn-based). This matters because it reduces confusing failures and makes the system more dependable.

---

### Test statistics

**Problem:** The team needed stronger proof that Test statistics worked as expected.

**Fix:** Updated from 3,872 to 4,037 tests, 114 to 120 test files. This matters because the team has clearer proof that the change works.

---

### 7 ADRs documented

**Problem:** This release updates 7 ADRs documented.

**Fix:** ADR-001 through ADR-007. This matters because the release is clearer and easier to work with.

---

### 85 tasks tracked

**Problem:** This release updates 85 tasks tracked.

**Fix:** T001-T085. This matters because the release is clearer and easier to work with.

---

### 192 checklist items

**Problem:** The team needed stronger proof that 192 checklist items worked as expected.

**Fix:** CHK-001-192. This matters because the team has clearer proof that the change works.

---

### README indexing audit

**Problem:** People needed clearer guidance about README indexing audit.

**Fix:** 96 READMEs on disk, 93 to 94 indexed (100% coverage after symlink fix). This matters because the release notes and instructions are easier to read and trust.

---

### Anchor lookup failure

**Problem:** There was a reliability problem around Anchor lookup failure.

**Fix:** Anchors: ['summary'] returned empty when keys were composite IDs; now uses prefix fallback. This matters because it reduces confusing failures and makes the system more dependable.

---

### Anchor ID validation

**Problem:** People needed clearer guidance about Anchor ID validation.

**Fix:** Template-generated IDs (80-120 chars with underscores) violated VALID_ANCHOR_PATTERN; simplified to short semantic names. This matters because the release notes and instructions are easier to read and trust.

---

### Symlink README discovery

**Problem:** People needed clearer guidance about Symlink README discovery.

**Fix:** FindSkillReadmes() missed symlinked READMEs; added isSymbolicLink() check. This matters because the release notes and instructions are easier to read and trust.

---

### Resume detection bugs

**Problem:** There was a reliability problem around Resume detection bugs.

**Fix:** 5 root causes: wrong base path in glob patterns, insufficient glob depth, wrong semantic query in tier 2, too-generic trigger in tier 3, tier skipping. This matters because it reduces confusing failures and makes the system more dependable.

---

### README

**Problem:** People needed clearer guidance about README.

**Fix:** Anchor name mismatch (structure to architecture). This matters because the release notes and instructions are easier to read and trust.

---

### False bug report corrected

**Problem:** There was a reliability problem around False bug report corrected.

**Fix:** Check anchors awk was incorrectly reported as having a / parsing bug; B5 audit confirmed no bug exists. This matters because it reduces confusing failures and makes the system more dependable.

---

## Files Changed

| File | What Changed |
| ---- | ------------ |
| `README.md` | Referenced in the original changelog. |
| `.opencode/README.md` | Referenced in the original changelog. |
| `readme_indexing.md` | Referenced in the original changelog. |
| `context_template.md` | Referenced in the original changelog. |
| `search-results.ts` | Referenced in the original changelog. |
| `memory_system.md` | Referenced in the original changelog. |
| `save_workflow.md` | Referenced in the original changelog. |
| `SKILL.md` | Referenced in the original changelog. |
| `mcp_server/README.md` | Referenced in the original changelog. |
| `troubleshooting.md` | Referenced in the original changelog. |
| `mcp-code-mode/README.md` | Referenced in the original changelog. |
| `/` | Referenced in the original changelog. |
| `check-anchors.sh` | Referenced in the original changelog. |
