## [**1.2.2.0**] - 2026-02-03

This release 30-agent audit bug fixes  - comprehensive remediation of 34 bugs discovered by 30 parallel agents auditing Spec Kit v1.2.x. All P0/P1/P2 issues resolved. It focuses on fixing important problems and explaining the results in clearer terms.

---

## What Changed

---

### Core Infrastructure (Agent 1)

- Need: There was a reliability problem around Core Infrastructure (Agent 1).
- Change: The release fixes the issue in core infrastructure (agent 1).
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

---

### Schema version mismatch

- Need: There was a reliability problem around Schema version mismatch.
- Change: Updated schema version from 9 to 11 in vector index:108.
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

---

### Duplicate error codes

- Need: There was a reliability problem around Duplicate error codes.
- Change: QUERY_TOO_LONG changed from E040 to E042, QUERY_EMPTY from E041 to E043 (now unique).
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

---

### Scoring Modules (Agent 2)

- Need: There was a reliability problem around Scoring Modules (Agent 2).
- Change: The release fixes the issue in scoring modules (agent 2).
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

---

### Tier weight inconsistency

- Need: There was a reliability problem around Tier weight inconsistency.
- Change: Aligned folder scoring TIER_WEIGHTS with authoritative importance tiers.
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

---

### Invalid tier names

- Need: There was a reliability problem around Invalid tier names.
- Change: Removed non-existent "high" and "low" tiers from composite scoring multipliers.
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

---

### Test alignment

- Need: There was a reliability problem around Test alignment.
- Change: Updated five factor scoring.test to use valid tier names.
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

---

### Extractors (Agent 3)

- Need: There was a reliability problem around Extractors (Agent 3).
- Change: The release fixes the issue in extractors (agent 3).
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

---

### Null pointer crashes

- Need: There was a reliability problem around Null pointer crashes.
- Change: Added defensive null checks for collected_data and observations in file extractor:61-63.
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

---

### Process termination

- Need: There was a reliability problem around Process termination.
- Change: Replaced process.exit(1) with throw new Error() in file extractor:31 and diagram extractor:22,28.
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

---

### Templates (Agents 4 and 5)

- Need: People needed clearer guidance about Templates (Agents 4 and 5).
- Change: The release fixes the issue in templates (agents 4 and 5).
- Why it matters: This matters because the release notes and instructions are easier to read and trust.

---

### Section numbering

- Need: There was a reliability problem around Section numbering.
- Change: Fixed spec section 10 to 7 (OPEN QUESTIONS).
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

---

### Progressive inheritance

- Need: People needed clearer guidance about Progressive inheritance.
- Change: Level 3 templates now correctly inherit L2 addendum content.
- Why it matters: This matters because the release notes and instructions are easier to read and trust.

---

### L3+ content removal

- Need: There was a reliability problem around L3+ content removal.
- Change: Removed L3+ sections from checklist (belongs only in level_3+).
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

---

### Double separators

- Need: People needed clearer guidance about Double separators.
- Change: Fixed ------ to --- in L2 and L3 templates.
- Why it matters: This matters because the release notes and instructions are easier to read and trust.

---

### Template comments

- Need: People needed clearer guidance about Template comments.
- Change: Updated template source comments to reflect inheritance chain.
- Why it matters: This matters because the release notes and instructions are easier to read and trust.

---

### Memory Commands (Agent 6)

- Need: There was a reliability problem around Memory Commands (Agent 6).
- Change: The release fixes the issue in memory commands (agent 6).
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

---

### Tool prefix standardization

- Need: There was a reliability problem around Tool prefix standardization.
- Change: All memory commands now use full spec_kit_memory_ prefix.
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

---

### Files updated

- Need: There was a reliability problem around Files updated.
- Change: Context, manage, save, learn (MCP Matrix tables and allowed-tools).
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

---

### SKILL Documentation (Agent 7)

- Need: People needed clearer guidance about SKILL Documentation (Agent 7).
- Change: The release fixes the issue in skill documentation (agent 7).
- Why it matters: This matters because the release notes and instructions are easier to read and trust.

---

### Tool table expansion

- Need: There was a reliability problem around Tool table expansion.
- Change: Added 8 missing tools (14 to 22): memory_context, task_preflight, task_postflight, memory_drift_why, memory_causal_link, memory_causal_stats, memory_causal_unlink, memory_get_learning_history.
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

---

### LOC counts

- Need: People needed clearer guidance about LOC counts.
- Change: Updated template line counts to match actual sizes (L1: 450, L2: 890, L3: 890, L3+: 1080).
- Why it matters: This matters because the release notes and instructions are easier to read and trust.

---

### Path fix

- Need: There was a reliability problem around Path fix.
- Change: Corrected decision format path to validation/ subfolder.
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

---

### Error Handling (Agent 8)

- Need: There was a reliability problem around Error Handling (Agent 8).
- Change: The release fixes the issue in error handling (agent 8).
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

---

### False positive reduction

- Need: There was a reliability problem around False positive reduction.
- Change: Added hasWholeWord() and areTermsInSameContext() helpers to prediction error gate for better contradiction detection.
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

---

### Error message clarity

- Need: There was a reliability problem around Error message clarity.
- Change: Improved workflow:279-281 error message with actionable guidance.
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

---

### Shared Utilities (Agent 9)

- Need: There was a reliability problem around Shared Utilities (Agent 9).
- Change: The release fixes the issue in shared utilities (agent 9).
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

---

### Deduplication logic

- Need: There was a reliability problem around Deduplication logic.
- Change: Trigger extractor:448-499 now prefers longer, more specific phrases over shorter ones.
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

---

### Embedding Providers (Agent 10)

- Need: There was a reliability problem around Embedding Providers (Agent 10).
- Change: The release fixes the issue in embedding providers (agent 10).
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

---

### Provider identification

- Need: There was a reliability problem around Provider identification.
- Change: Added getProviderName() method to all 3 providers.
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

---

### Documentation accuracy

- Need: People needed clearer guidance about Documentation accuracy.
- Change: Fixed README API documentation to match actual process_with_retry() parameters.
- Why it matters: This matters because the release notes and instructions are easier to read and trust.

---

### Fixed

- Need: There was a reliability problem around Fixed.
- Change: All fixes independently verified: schema version 11 confirmed at vector index:108, error codes E042/E043 confirmed unique at core:40-41, zero process.exit() calls in extractors, all 3 providers have getProviderName() method, memory commands use full tool prefixes.
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.


---

## Files Changed

| File | What Changed |
| ---- | ------------ |
| `Audit Spec: specs/003-memory-and-spec-kit/084-speckit-30-agent-audit/` | Updated in this release. |
| `Fix Spec: specs/003-memory-and-spec-kit/085-audit-fixes/` | Updated in this release. |
| `Completion: 34/34 bugs fixed (100%)` | Updated in this release. |
