## [**2.2.22.1**] - 2026-02-19

This release memory save path-resolution hotfix for .opencode/specs inputs -- Fixed generate context folder detection so JSON SPEC_FOLDER values that include .opencode/specs/... resolve directly instead of falling through to nested auto-detection. It explains the main changes in simple language so the release is easier to understand.

---

## What Changed

### JSON -folder normalization added

- Need: This release updates JSON -folder normalization added.
- Change: JSON spec-folder normalization added: detectSpecFolder() now correctly handles absolute paths, specs/..., and .opencode/specs/... in collected data.
- Why it matters: This matters because the release is clearer and easier to work with.

### Alignment base-path corrected

- Need: This release updates Alignment base-path corrected.
- Change: Alignment base-path corrected: Folder-alignment checks now run against the resolved folder basename and parent directory, preventing malformed join results.
- Why it matters: This matters because the release is clearer and easier to work with.

### Symlink duplicate guard added

- Need: This release updates Symlink duplicate guard added.
- Change: Symlink duplicate guard added: Existing specs directories are de-duplicated by realpath, so specs to .opencode/specs no longer appears as two competing roots.
- Why it matters: This matters because the release is clearer and easier to work with.

### Misrouted memory recovered

- Need: This release updates Misrouted memory recovered.
- Change: Misrouted memory recovered: The session memory file was moved to the intended spec folder and re-indexed under 04--agent-orchestration/021-codex-orchestrate.
- Why it matters: This matters because the release is clearer and easier to work with.

### Stale index entries removed

- Need: There was a reliability problem around Stale index entries removed.
- Change: Stale index entries removed: Wrong-folder memory rows created by fallback routing were deleted from the memory index.
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.


---

## Files Changed

| File | What Changed |
| ---- | ------------ |
| `.opencode/skill/system-spec-kit/scripts/core/config.ts` | De-duplicate specs roots by realpath. |
| `.opencode/skill/system-spec-kit/scripts/spec-folder/folder-detector.ts` | Normalize JSON SPEC_FOLDER path forms and fix alignment base path. |
| `.opencode/specs/04--agent-orchestration/021-codex-orchestrate/memory/19-02-26_17-51__codex-orchestrate.md` | Recovered and re-indexed memory in correct spec folder. |


---

## Upgrade

- No migration is required. Pull latest and use /memory:save with either specs/... or .opencode/specs/... folder forms.
