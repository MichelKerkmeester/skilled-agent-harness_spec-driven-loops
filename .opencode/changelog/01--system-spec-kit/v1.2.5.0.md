## [**1.2.5.0**] - 2026-02-03

This release infrastructure work domain detection  fixing root cause of memory files being saved to incorrect spec folders when working on shared infrastructure. It focuses on making the project setup more consistent and easier to work with.

---

## What Changed

### Detect_work_domain() function

- Need: The project needed a cleaner setup for Detect_work_domain() function.
- Change: New function that analyzes file paths from observations to detect infrastructure vs project work: calculates ratio of .opencode/ files, identifies subpath , returns domain info with confidence score and matching patterns.
- Why it matters: This matters because setup and maintenance are easier to manage.

### Calculate_alignment_score_with_domain() function

- Need: The project needed a cleaner setup for Calculate_alignment_score_with_domain() function.
- Change: Wraps base scoring with infrastructure awareness: applies +40 bonus to folders matching infrastructure patterns, patterns mapped per subpath.
- Why it matters: This matters because setup and maintenance are easier to manage.

### Validate_content_alignment() and validate_folder_alignment()

- Need: The project needed a cleaner setup for Validate_content_alignment() and validate_folder_alignment().
- Change: Now use domain-aware scoring: show infrastructure mismatch warning when detected, list better-matching alternatives with boosted scores.
- Why it matters: This matters because setup and maintenance are easier to manage.

### Step 3: Validate Content vs Folder Alignment

- Need: The team needed stronger proof that Step 3: Validate Content vs Folder Alignment worked as expected.
- Change: Added validation of key_files against spec_folder before recovery.
- Why it matters: This matters because the team has clearer proof that the change works.

### Step numbering

- Need: Step numbering was no longer needed in its old form.
- Change: Removed half-steps (2.5 → 3, renumbered subsequent steps).
- Why it matters: This matters because the project becomes smaller and easier to maintain.

### KEY FILES in recovery summary

- Need: The team needed stronger proof that KEY FILES in recovery summary worked as expected.
- Change: Added so users can verify spec folder makes sense for the work.
- Why it matters: This matters because the team has clearer proof that the change works.


---

## Files Changed

| File | What Changed |
| ---- | ------------ |
| `.opencode/` | Referenced in the original changelog. |
| `alignment-validator.js` | Referenced in the original changelog. |
| `skill/system-spec-kit` | Referenced in the original changelog. |
| `command/memory` | Referenced in the original changelog. |
| `agent/` | Referenced in the original changelog. |
| `continue.md` | Referenced in the original changelog. |
