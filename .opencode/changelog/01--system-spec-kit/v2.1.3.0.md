## [**2.1.3.0**] - 2026-02-09

This release spec Kit Memory bug audit  - most comprehensive bug-fix release in project history with 85+ bugs fixed and 27 new regression tests. It focuses on fixing important problems and explaining the results in clearer terms.

---

## What Changed

---

### FSRS formula: 18.45x decay rate error

- Need: There was a reliability problem around FSRS formula: 18.45x decay rate error.
- Change: Corrected; memories were decaying at the wrong rate due to incorrect factor calculation.
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

---

### MacOS crash bug

- Need: There was a reliability problem around MacOS crash bug.
- Change: In SQLite operations; platform-specific crash during concurrent database access.
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

---

### Database no-op operations

- Need: There was a reliability problem around Database no-op operations.
- Change: Writes that silently did nothing, causing data loss without errors.
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

---

### 85+ bugs fixed

- Need: There was a reliability problem around 85+ bugs fixed.
- Change: Total across all severity levels.
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

---

### 27 new tests

- Need: There was a reliability problem around 27 new tests.
- Change: Added for regression coverage.
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

---

### All critical paths

- Need: There was a reliability problem around All critical paths.
- Change: Verified post-fix.
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.


---

## Files Changed

| File | What Changed |
| ---- | ------------ |
| `Not listed in source changelog` | The original note did not name specific files. |
