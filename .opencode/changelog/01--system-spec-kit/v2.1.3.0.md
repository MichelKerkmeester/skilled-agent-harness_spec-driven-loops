# v2.1.3.0

> Part of [OpenCode Dev Environment](https://github.com/MichelKerkmeester/opencode--spec-kit-skilled-agent-orchestration)

---

## [**2.1.3.0**] - 2026-02-09

This release spec Kit Memory bug audit - most comprehensive bug-fix release in project history with 85+ bugs fixed and 27 new regression tests. It focuses on fixing important problems and explaining the results in clearer terms. The goal is to make the workflow easier to follow and easier to trust.

---

## What Changed

---

### FSRS formula: 18.45x decay rate error

**Problem:** There was a reliability problem around FSRS formula: 18.45x decay rate error.

**Fix:** Corrected; memories were decaying at the wrong rate due to incorrect factor calculation. This matters because it reduces confusing failures and makes the system more dependable.

---

### MacOS crash bug

**Problem:** There was a reliability problem around MacOS crash bug.

**Fix:** In SQLite operations; platform-specific crash during concurrent database access. This matters because it reduces confusing failures and makes the system more dependable.

---

### Database no-op operations

**Problem:** There was a reliability problem around Database no-op operations.

**Fix:** Writes that silently did nothing, causing data loss without errors. This matters because it reduces confusing failures and makes the system more dependable.

---

### 85+ bugs fixed

**Problem:** There was a reliability problem around 85+ bugs fixed.

**Fix:** Total across all severity levels. This matters because it reduces confusing failures and makes the system more dependable.

---

### 27 new tests

**Problem:** There was a reliability problem around 27 new tests.

**Fix:** Added for regression coverage. This matters because it reduces confusing failures and makes the system more dependable.

---

### All critical paths

**Problem:** There was a reliability problem around All critical paths.

**Fix:** Verified post-fix. This matters because it reduces confusing failures and makes the system more dependable.

---

## Files Changed

| File | What Changed |
| ---- | ------------ |
| `Not listed in source changelog` | The original note did not name specific files. |
