This release spec Kit Memory bug audit - most comprehensive bug-fix release in project history with 85+ bugs fixed and 27 new regression tests. It focuses on fixing important problems and explaining the results in clearer terms. The goal is to make the workflow easier to follow and easier to trust.

---

## What Changed

#### FSRS formula: 18.45x decay rate error

There was a reliability problem around FSRS formula: 18.45x decay rate error. Corrected; memories were decaying at the wrong rate due to incorrect factor calculation. This matters because it reduces confusing failures and makes the system more dependable.

&nbsp;

#### MacOS crash bug

There was a reliability problem around MacOS crash bug. In SQLite operations; platform-specific crash during concurrent database access. This matters because it reduces confusing failures and makes the system more dependable.

&nbsp;

#### Database no-op operations

There was a reliability problem around Database no-op operations. Writes that silently did nothing, causing data loss without errors. This matters because it reduces confusing failures and makes the system more dependable.

&nbsp;

#### 85+ bugs fixed

There was a reliability problem around 85+ bugs fixed. Total across all severity levels. This matters because it reduces confusing failures and makes the system more dependable.

&nbsp;

#### 27 new tests

There was a reliability problem around 27 new tests. Added for regression coverage. This matters because it reduces confusing failures and makes the system more dependable.

&nbsp;

#### All critical paths

There was a reliability problem around All critical paths. Verified post-fix. This matters because it reduces confusing failures and makes the system more dependable.

---

## Files Changed

| File | What Changed |
| ---- | ------------ |
| `Not listed in source changelog` | The original note did not name specific files. |
