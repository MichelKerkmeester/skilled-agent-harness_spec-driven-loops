This release full spec folder document indexing + documentation alignment - Spec 126 adds schema v13 with 11 document types, scoring multipliers, 2 new intents, a spec document crawler and causal chains. Spec 127 aligns all documentation with post-126 state. It focuses on making the instructions and release notes easier for non-developers to follow. The goal is to make the workflow easier to follow and easier to trust.

---

## What Changed

---

### Schema v13

**Problem:** The project needed a cleaner setup for Schema v13.

**Fix:** Document_type TEXT DEFAULT 'memory' + spec_level INTEGER columns on memory_index. This matters because setup and maintenance are easier to manage.

---

### 11 Document Types

**Problem:** People needed clearer guidance about 11 Document Types.

**Fix:** Spec, plan, tasks, checklist, decision_record, implementation_summary, research, handover, memory, readme, constitutional. This matters because the release notes and instructions are easier to read and trust.

---

### Scoring Multipliers

**Problem:** People needed clearer guidance about Scoring Multipliers.

**Fix:** Document-type scoring: spec (1.4x), plan (1.3x), constitutional (2.0x), memory (1.0x), readme (0.8x), scratch (0.6x). This matters because the release notes and instructions are easier to read and trust.

---

### 2 New Intents

**Problem:** This release updates 2 New Intents.

**Fix:** Find_spec + find_decision (total: 7, was 5). This matters because the release is clearer and easier to work with.

---

### Doc Crawler

**Problem:** This release updates Doc Crawler.

**Fix:** FindSpecDocuments() walks .opencode/specs/ for spec folder documents. This matters because the release is clearer and easier to work with.

---

### Causal Chains

**Problem:** This release updates Causal Chains.

**Fix:** CreateSpecDocumentChain() creates spectoplantotaskstoimpl_summary edges. This matters because the release is clearer and easier to work with.

---

### Feature Flag

**Problem:** This release updates Feature Flag.

**Fix:** SPECKIT_INDEX_SPEC_DOCS env var + includeSpecDocs parameter on memory_index_scan. This matters because the release is clearer and easier to work with.

---

### 5th Indexing Source

**Problem:** People needed clearer guidance about 5th Indexing Source.

**Fix:** Spec folder documents join memory, constitutional, skill READMEs, project READMEs. This matters because the release notes and instructions are easier to read and trust.

---

### Root README

**Problem:** People needed clearer guidance about Root README.

**Fix:** Updated Memory Engine sources (5th source), intent table (5to7 types). This matters because the release notes and instructions are easier to read and trust.

---

### System Kit README

**Problem:** People needed clearer guidance about System Kit README.

**Fix:** Updated Key Innovations (7 intents, document-type scoring), 5-Source Pipeline, includeSpecDocs param, Spec 126 in Recent Changes. This matters because the release notes and instructions are easier to read and trust.

---

### MCP Server README

**Problem:** People needed clearer guidance about MCP Server README.

**Fix:** Updated Key Innovations, params, intents (7), feature flags, schema v13, structure comments. This matters because the release notes and instructions are easier to read and trust.

---

### SKILL

**Problem:** People needed clearer guidance about SKILL.

**Fix:** Version bump to 2.2.9.0, 5-source discovery, document-type scoring concepts. This matters because the release notes and instructions are easier to read and trust.

---

### Reference files

**Problem:** People needed clearer guidance about Reference files.

**Fix:** Memory system (5th source, params, intents), readme indexing (5-Source Pipeline), save workflow (spec docs row). This matters because the release notes and instructions are easier to read and trust.

---

### Sub-directory READMEs

**Problem:** People needed clearer guidance about Sub-directory READMEs.

**Fix:** README, README, README updated (5to7 intents). This matters because the release notes and instructions are easier to read and trust.

---

## Files Changed

| File | What Changed |
| ---- | ------------ |
| `README.md` | Modified. |
| `.opencode/skill/system-spec-kit/README.md` | Modified. |
| `.opencode/skill/system-spec-kit/mcp_server/README.md` | Modified. |
| `.opencode/skill/system-spec-kit/SKILL.md` | Modified. |
| `.opencode/skill/system-spec-kit/references/memory/memory_system.md` | Modified. |
| `.opencode/skill/system-spec-kit/references/memory/readme_indexing.md` | Modified. |
| `.opencode/skill/system-spec-kit/references/memory/save_workflow.md` | Modified. |
| `.opencode/skill/system-spec-kit/mcp_server/lib/README.md` | Modified. |
| `.opencode/skill/system-spec-kit/mcp_server/lib/search/README.md` | Modified. |
| `.opencode/skill/system-spec-kit/mcp_server/tests/README.md` | Modified. |
| `specs/system-spec-kit/127-spec126-documentation-alignment/` | Created. |

---

## Upgrade

- No action required. Documentation-only changes (spec 127). Spec 126 code changes require npm run build in .opencode/skill/system-spec-kit/.
