## [**2.2.24.0**] - 2026-02-20

This release hybrid memory retrieval rollout completion and verification closure - This release documents every shipped feature from spec 136, including typed retrieval trace, artifact-aware routing, adaptive fusion, mutation ledger, telemetry, extraction/config hardening, and final deferred-test closure. It focuses on fixing important problems and explaining the results in clearer terms.

---

## What Changed

### 1. Typed retrieval trace shipped

- Need: There was a reliability problem around Typed retrieval trace shipped.
- Change: Typed retrieval trace shipped: every retrieval now returns a typed ContextEnvelope with stage trace (candidate, filter, fusion, rerank, fallback), degraded-mode contract, and retrieval metadata.
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

### 2. Artifact-aware routing shipped

- Need: This release updates Artifact-aware routing shipped.
- Change: Artifact-aware routing shipped: retrieval strategy now routes across 9 artifact classes with deterministic weight tables.
- Why it matters: This matters because the release is clearer and easier to work with.

### 3. Adaptive hybrid fusion shipped

- Need: There was a reliability problem around Adaptive hybrid fusion shipped.
- Change: Adaptive hybrid fusion shipped: intent-aware fusion profiles now support fix_bug, add_feature, understand, refactor, security_audit, find_spec, and find_decision.
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

### 4. Dark-run comparison mode available

- Need: This release updates Dark-run comparison mode available.
- Change: Dark-run comparison mode available: adaptive behavior can be compared against deterministic fusion without changing live retrieval output.
- Why it matters: This matters because the release is clearer and easier to work with.

### 5. Feature-flag safety model retained

- Need: This release updates Feature-flag safety model retained.
- Change: Feature-flag safety model retained: SPECKIT_ADAPTIVE_FUSION remains default-off and SPECKIT_EXTENDED_TELEMETRY remains default-on for controlled rollout behavior.
- Why it matters: This matters because the release is clearer and easier to work with.

### 6. Append-only mutation ledger shipped

- Need: There was a reliability problem around Append-only mutation ledger shipped.
- Change: Append-only mutation ledger shipped: create/update/delete/reindex operations are recorded through SQLite trigger-enforced immutable entries with hash-chain linkage.
- Why it matters: This matters because the release is clearer and easier to work with.

### 7. Extended telemetry shipped

- Need: There was a reliability problem around Extended telemetry shipped.
- Change: Extended telemetry shipped: retrieval now captures latency, mode, fallback, and quality-proxy dimensions across memory_search and memory_context.
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

### 8. Extraction and provenance hardening shipped

- Need: There was a reliability problem around Extraction and provenance hardening shipped.
- Change: Extraction and provenance hardening shipped: deterministic memory_id fallback for extraction, bounded causal-seed selection, and canonical boost import-path normalization.
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

### 9. Configuration safety hardening shipped

- Need: The team needed stronger proof that Configuration safety hardening shipped worked as expected.
- Change: Configuration safety hardening shipped: cognitive config parsing now uses Zod validation and working-memory eviction now enforces deterministic LRU ordering.
- Why it matters: This matters because the team has clearer proof that the change works.

### 10. Embedding-provider architecture drift closed

- Need: The team needed stronger proof that Embedding-provider architecture drift closed worked as expected.
- Change: Embedding-provider architecture drift closed: legacy lib/interfaces/embedding-provider references were removed from active test paths and aligned to shared provider contracts/factory.
- Why it matters: This matters because the team has clearer proof that the change works.

### 11. Deferred suites activated

- Need: People needed clearer guidance about Deferred suites activated.
- Change: Deferred suites activated: api key validation.vitest, api validation.vitest, and lazy loading.vitest now run as active suites.
- Why it matters: This matters because the release notes and instructions are easier to read and trust.

### 12. Targeted closure verification

- Need: People needed clearer guidance about Targeted closure verification.
- Change: Targeted closure verification: 3 passed files, 15 passed tests, 0 skipped.
- Why it matters: This matters because the release notes and instructions are easier to read and trust.

### 13. All-features regression verification

- Need: People needed clearer guidance about All-features regression verification.
- Change: All-features regression verification: 142 passed files (142 total), 4415 passed tests, 19 skipped (4434 total).
- Why it matters: This matters because the release notes and instructions are easier to read and trust.

### 14. Root and addendum docs synchronized

- Need: People needed clearer guidance about Root and addendum docs synchronized.
- Change: Root and addendum docs synchronized: spec 136 root docs and package 007-documentation-alignment now reflect current totals and closure evidence.
- Why it matters: This matters because the release notes and instructions are easier to read and trust.

### 15. CHK-336 closed

- Need: People needed clearer guidance about CHK-336 closed.
- Change: CHK-336 closed: added Last Verified rows in module-count/stat tables for skill root README, MCP server README, and MCP lib README.
- Why it matters: This matters because the release notes and instructions are easier to read and trust.

### 16. Dark-launch verification completed

- Need: This release updates Dark-launch verification completed.
- Change: Dark-launch verification completed: baseline run with all new feature flags disabled preserved expected deterministic behavior (99.65% pass rate, 15 expected cognitive-feature failures).
- Why it matters: This matters because the release is clearer and easier to work with.

### 17. Staged rollout completed

- Need: This release updates Staged rollout completed.
- Change: Staged rollout completed: feature enablement progressed through 10%, 50%, and 100% gates with monotonic failure reduction (20 to 7 to 1) and GO decisions.
- Why it matters: This matters because the release is clearer and easier to work with.

### 18. Governance packets completed

- Need: This release updates Governance packets completed.
- Change: Governance packets completed: technical, data, and product approval artifacts were produced, with 13 success criteria passing and 23 requirements mapped to evidence.
- Why it matters: This matters because the release is clearer and easier to work with.

### 19. Outcome confirmation completed

- Need: This release updates Outcome confirmation completed.
- Change: Outcome confirmation completed: 14-day KPI monitoring closure recorded 12/12 measurable KPIs passing with margin, and capability truth-matrix review confirmed zero drift.
- Why it matters: This matters because the release is clearer and easier to work with.

### 20. Wave 3 survey framework included

- Need: People needed clearer guidance about Wave 3 survey framework included.
- Change: Wave 3 survey framework included: administrative user-survey framework artifacts were included in outcome confirmation documentation.
- Why it matters: This matters because the release notes and instructions are easier to read and trust.

### 21. Skill version bump

- Need: This release updates Skill version bump.
- Change: Skill version bump: SKILL updated from 2.2.23.0 to 2.2.24.0.
- Why it matters: This matters because the release is clearer and easier to work with.

## Files Changed

| File | What Changed |
| ---- | ------------ |
| `.opencode/specs/02--system-spec-kit/020-mcp-working-memory-hybrid-rag/` | Spec folder referenced by this release. |
| `.opencode/skill/system-spec-kit/mcp_server/lib/{contracts,search,storage,telemetry,extraction,cognitive,config}/**` | Retrieval, ledger, telemetry, extraction, and config hardening runtime features. |
| `.opencode/skill/system-spec-kit/mcp_server/handlers/{memory-search,memory-context,memory-save}.ts` | Handler wiring for adaptive retrieval, telemetry, and mutation logging. |
| `.opencode/skill/system-spec-kit/mcp_server/tests/{api-key-validation,api-validation,lazy-loading,embeddings}.vitest.ts` | Deferred drift closure and active coverage updates. |
| `.opencode/specs/02--system-spec-kit/020-mcp-working-memory-hybrid-rag/{implementation-summary,test-results,checklist}.md` | Root verification sync for full feature set and closure evidence. |
| `.opencode/specs/02--system-spec-kit/020-mcp-working-memory-hybrid-rag/007-documentation-alignment/*.md` | Addendum closure sync, CHK-336 closure, and status normalization. |
| `.opencode/skill/system-spec-kit/{README.md,SKILL.md}` | Last verified table updates and skill version bump. |
| `.opencode/skill/system-spec-kit/mcp_server/{README.md,lib/README.md}` | Module/stat table verification-date updates. |

## Upgrade

- No migration is required. Pull latest to get active coverage for the three formerly deferred suites and synchronized verification documentation.
