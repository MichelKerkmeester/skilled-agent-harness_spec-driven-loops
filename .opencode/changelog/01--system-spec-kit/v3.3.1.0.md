This patch release tightens several parts of the review-improvement bundle so they behave more safely and more predictably. It hardens session isolation, fixes wave-state transitions, closes optimizer boundary gaps, and strengthens the supporting tests.

> Spec folder: `.opencode/specs/skilled-agent-orchestration/042-sk-deep-research-review-improvement-2/` (Level 3)
> Review session: `rvw-2026-04-12T11-30-00Z`

---

## Security Fixes

These changes close safety gaps where the system could cross boundaries it should respect. They make day-to-day use safer without changing the release scope.

### Coverage-graph namespace isolation hardened

**Problem:** Coverage-gap, unresolved-finding, and contradiction queries used bare IDs in subqueries, allowing foreign session scopes to satisfy them. Read tools permitted cross-session aggregation without explicit admin scope.

**Fix:** Full identity predicates (`spec_folder`, `loop_type`, `session_id`) were added to all `NOT EXISTS` subqueries and contradiction joins in `coverage-graph-query.ts`. A shared query helper now enforces that same session scope everywhere, `sessionId` is required for non-admin reads, and `coverage-graph-signals.ts` carries the full scope through nested edge lookups.

---

### Optimizer path traversal and boundary enforcement

**Problem:** The promotion-report save path could write to arbitrary repo locations. Config search also accepted out-of-bounds and locked-field changes, while corpus helpers hid integrity failures.

**Fix:** Promotion output is now restricted to a dedicated audit directory with symlink rejection. The evaluator requires a manifest, search space comes from that manifest with unknown and locked fields rejected, and corpus helpers now fail loudly when integrity checks break.

---

## Bug Fixes

These fixes correct behavior that could confuse operators or produce the wrong result. The goal is to make the workflow more predictable in normal use.

### Wave hotspot detection uses composite scoring

**Problem:** The hotspot spread calculation treated every median-complexity directory as a hotspot, which triggered false waves on otherwise uniform inventories.

**Fix:** Hotspots now require a strict-above-baseline composite score including churn and issues, not just `complexity >= median`.

---

### Domain clustering uses registrable-domain heuristic

**Problem:** The cluster-assignment helper used the TLD, collapsing unrelated research domains into the same cluster.

**Fix:** Fallback clusters derived from registered-domain or topic-based rule.

---

### Wave lifecycle enforces adjacent phase transitions

**Problem:** The phase-advance helper enforced forward progress but not the required phase order, allowing illegal jumps straight to merge.

**Fix:** Added an allowed-transition matrix. Non-adjacent jumps require explicit audited API.

---

### Board merge uses full composite key

**Problem:** Merge deduplicated findings by `findingId` alone, silently collapsing distinct segment findings.

**Fix:** Storage and replay now use the full 5-key composite: `(sessionId, generation, segment, wave, findingId)`.

---

### Unified merge contract across wave modules

**Problem:** Merge and provenance rules were implemented three different ways across lifecycle, board, and segment-state.

**Fix:** The board module now owns one shared merge path. Lifecycle and segment-state only call into that shared logic instead of each carrying their own version.

---

### Board transition API wired to lifecycle

**Problem:** The board transition API existed but the main board never advanced its own status.

**Fix:** Lifecycle updates now flow through `transitionBoardStatus()` inside `updateBoard()`, so the board actually advances its own status.

---

### Replay schema normalized

**Problem:** Graph-aware replay scoring was out of sync between corpus extraction and runner.

**Fix:** The corpus builder now emits the exact metric shape the replay runner expects.

---

## Maintainability

Some changes were mainly about keeping the system easier to understand and maintain over time. They reduce hidden complexity without changing the core purpose of the release.

### Session normalization centralized

`coverage-graph-core.cjs` now uses a shared session normalization helper. Auto-created nodes propagate `metadata.sessionId`.

### computeMomentum renamed and alias removed

`computeMomentum` renamed to `computeRecentEdgeActivity` in `coverage-graph-signals.cjs`. The compatibility alias was removed. The TS export in `coverage-graph-signals.ts` adds `sessionId` parameter.

### Cycle handling matches documented contract

`coverage-graph-signals.cjs` cycle-handling now implements the documented fallback for cyclic components.

### Mixed-session edge precedence documented

`coverage-graph-session.cjs` now rejects mixed-session edges without explicit `edge.sessionId` or documents and tests the precedence rule.

---

## Test Improvements

This section summarizes the main change in this area and why it matters in practice.

- `coverage-graph-integration.vitest.ts`: Shadow-copied contract replaced with real TS source import. Tautological checks replaced with executable assertions.
- `coverage-graph-stress.vitest.ts`: Wall-clock budgets moved to opt-in benchmark guards. Contradiction-free graph test now builds an explicit clean graph.
- `session-isolation.vitest.ts`: Colliding-ID fixtures added for higher-level read paths.
- `optimizer-replay-runner.vitest.ts`: Threshold-straddling fixtures with concrete delta assertions.
- `benchmark-stability.vitest.ts`: Asserts heuristic outcomes directly.
- `improvement-journal.vitest.ts`: `session_end` alias tests added.
- New `coverage-graph-contradictions.vitest.ts` with session-scoped tests.
- Stale `coverage-graph-db.vitest.ts` and `coverage-graph-tools.vitest.ts` archived.

---

## Verification

| Check | Result |
|-------|--------|
| 30-iteration GPT-5.4 deep review | PASS - 0 P0 remaining |
| Post-fix validation (10 iterations) | PASS - all findings resolved |
