## [**1.2.0.0**] - 2026-02-02

This release major release implementing SpecKit Reimagined  with 33 features, 107 tasks, and 5 new memory commands. Adds session deduplication, causal memory graph, intent-aware retrieval, BM25 (exact word matching) hybrid search, and comprehensive crash recovery. It explains the main changes in simple language so the release is easier to understand.

---

## What Changed

### 1. Session Management (W-S)

- Need: This release updates Session Management (W-S).
- Change: The release updates session management (w-s).
- Why it matters: This matters because the release is clearer and easier to work with.

### 2. Session deduplication

- Need: This release updates Session deduplication.
- Change: Hash-based tracking prevents re-surfacing same memories (50% token savings).
- Why it matters: This matters because the release is clearer and easier to work with.

### 3. Crash recovery

- Need: There was a reliability problem around Crash recovery.
- Change: Session_state table with automatic CONTINUE SESSION generation.
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

### 4. SessionStateManager API

- Need: This release updates SessionStateManager API.
- Change: ResetInterruptedSessions(), recoverState() with _recovered flag.
- Why it matters: This matters because the release is clearer and easier to work with.

### 5. Search and Retrieval (W-R)

- Need: This release updates Search and Retrieval (W-R).
- Change: The release updates search and retrieval (w-r).
- Why it matters: This matters because the release is clearer and easier to work with.

### 6. BM25 (exact word matching) hybrid search

- Need: There was a reliability problem around BM25 (exact word matching) (exact word matching) hybrid search.
- Change: Pure JavaScript BM25 (exact word matching) implementation for lexical search fallback.
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

### 7. RRF fusion enhancement

- Need: This release updates RRF fusion enhancement.
- Change: K=60 parameter with 10% convergence bonus for multi-source results.
- Why it matters: This matters because the release is clearer and easier to work with.

### 8. Intent-aware retrieval

- Need: There was a reliability problem around Intent-aware retrieval.
- Change: 5 intent types (add_feature, fix_bug, refactor, security_audit, understand).
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

### 9. Cross-encoder reranking

- Need: This release updates Cross-encoder reranking.
- Change: Voyage/Cohere/local providers with length penalty (100 char threshold).
- Why it matters: This matters because the release is clearer and easier to work with.

### 10. Fuzzy matching

- Need: This release updates Fuzzy matching.
- Change: Levenshtein distance (<=2) + ACRONYM_MAP (30+ entries).
- Why it matters: This matters because the release is clearer and easier to work with.

### 11. Decay and Scoring (W-D)

- Need: This release updates Decay and Scoring (W-D).
- Change: The release updates decay and scoring (w-d).
- Why it matters: This matters because the release is clearer and easier to work with.

### 12. Type-specific half-lives

- Need: This release updates Type-specific half-lives.
- Change: 9 memory types with distinct decay rates.
- Why it matters: This matters because the release is clearer and easier to work with.

### 13. Multi-factor decay

- Need: This release updates Multi-factor decay.
- Change: Pattern alignment and citation recency factors.
- Why it matters: This matters because the release is clearer and easier to work with.

### 14. Usage boost scoring

- Need: This release updates Usage boost scoring.
- Change: Enhanced 5-factor composite scoring.
- Why it matters: This matters because the release is clearer and easier to work with.

### 15. Consolidation pipeline

- Need: This release updates Consolidation pipeline.
- Change: 5-phase duplicate detection and semantic merge.
- Why it matters: This matters because the release is clearer and easier to work with.

### 16. Causal Memory Graph (W-G)

- Need: This release updates Causal Memory Graph (W-G).
- Change: The release updates causal memory graph (w-g).
- Why it matters: This matters because the release is clearer and easier to work with.

### 17. Causal edges

- Need: This release updates Causal edges.
- Change: 6 relationship types (caused, enabled, supersedes, contradicts, derived_from, supports).
- Why it matters: This matters because the release is clearer and easier to work with.

### 18. Decision lineage

- Need: This release updates Decision lineage.
- Change: Depth-limited traversal (max 10 hops) with cycle detection.
- Why it matters: This matters because the release is clearer and easier to work with.

### 19. Learning from corrections

- Need: This release updates Learning from corrections.
- Change: Stability penalties (0.5x) and boosts (1.2x) for replacements.
- Why it matters: This matters because the release is clearer and easier to work with.

### 20. Infrastructure (W-I)

- Need: The project needed a cleaner setup for Infrastructure (W-I).
- Change: The release updates infrastructure (w-i).
- Why it matters: This matters because setup and maintenance are easier to manage.

### 21. Recovery hints catalog

- Need: There was a reliability problem around Recovery hints catalog.
- Change: 49 error codes with actionable recovery guidance.
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

### 22. Tool output caching

- Need: This release updates Tool output caching.
- Change: 60s TTL with LRU eviction.
- Why it matters: This matters because the release is clearer and easier to work with.

### 23. Lazy model loading

- Need: This release updates Lazy model loading.
- Change: Deferred embedding initialization.
- Why it matters: This matters because the release is clearer and easier to work with.

### 24. Standardized response envelope

- Need: This release updates Standardized response envelope.
- Change: {summary, data, hints, meta} structure.
- Why it matters: This matters because the release is clearer and easier to work with.

### 25. Layered tool organization

- Need: This release updates Layered tool organization.
- Change: 7-layer MCP architecture (L1-L7).
- Why it matters: This matters because the release is clearer and easier to work with.

### 26. Incremental indexing

- Need: This release updates Incremental indexing.
- Change: Content hash + mtime-based diff updates.
- Why it matters: This matters because the release is clearer and easier to work with.

### 27. Pre-flight quality gates

- Need: The team needed stronger proof that Pre-flight quality gates worked as expected.
- Change: ANCHOR validation, duplicate detection, token budget checks.
- Why it matters: This matters because the team has clearer proof that the change works.

### 28. Protocol abstractions

- Need: This release updates Protocol abstractions.
- Change: Provider-agnostic embedding interfaces.
- Why it matters: This matters because the release is clearer and easier to work with.

### 29. API key validation

- Need: The team needed stronger proof that API key validation worked as expected.
- Change: Secure key handling with actionable guidance.
- Why it matters: This matters because the team has clearer proof that the change works.

### 30. Fallback chain

- Need: There was a reliability problem around Fallback chain.
- Change: Primary API to Local to BM25 (exact word matching)-only with logging.
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

### 31. Deferred indexing

- Need: This release updates Deferred indexing.
- Change: Graceful degradation when embeddings fail.
- Why it matters: This matters because the release is clearer and easier to work with.

### 32. Retry logic

- Need: This release updates Retry logic.
- Change: Exponential backoff (1s, 2s, 4s) with jitter.
- Why it matters: This matters because the release is clearer and easier to work with.

### 33. Transaction atomicity

- Need: This release updates Transaction atomicity.
- Change: SAVEPOINT/ROLLBACK patterns.
- Why it matters: This matters because the release is clearer and easier to work with.

### 34. Files

- Need: This release updates Files.
- Change: The release updates files.
- Why it matters: This matters because the release is clearer and easier to work with.

### 35. Session manager - Session deduplication and

- Need: There was a reliability problem around Session manager - Session deduplication and.
- Change: Session manager - Session deduplication and crash recovery (~1050 lines).
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

### 36. Recovery hints - 49 error codes

- Need: There was a reliability problem around Recovery hints - 49 error codes.
- Change: Recovery hints - 49 error codes with hints (~520 lines).
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

### 37. Bm25 index - Pure JS BM25 (exact word matching)

- Need: This release updates Bm25 index - Pure JS BM25 (exact word matching) (exact word matching).
- Change: Bm25 index - Pure JS BM25 (exact word matching) implementation (~380 lines).
- Why it matters: This matters because the release is clearer and easier to work with.

### 38. Cross encoder - Reranking with Voyage/Cohere/local

- Need: This release updates Cross encoder - Reranking with Voyage/Cohere/local.
- Change: Cross encoder - Reranking with Voyage/Cohere/local (~490 lines).
- Why it matters: This matters because the release is clearer and easier to work with.

### 39. Intent classifier - Intent detection for

- Need: This release updates Intent classifier - Intent detection for.
- Change: Intent classifier - Intent detection for 5 types.
- Why it matters: This matters because the release is clearer and easier to work with.

### 40. Fuzzy match - Levenshtein + acronym

- Need: This release updates Fuzzy match - Levenshtein + acronym.
- Change: Fuzzy match - Levenshtein + acronym expansion [NEVER IMPLEMENTED].
- Why it matters: This matters because the release is clearer and easier to work with.

### 41. Corrections - Memory corrections tracking (~560

- Need: This release updates Corrections - Memory corrections tracking (~560.
- Change: Corrections - Memory corrections tracking (~560 lines) [NEVER IMPLEMENTED].
- Why it matters: This matters because the release is clearer and easier to work with.

### 42. Archival manager - Automatic archival background

- Need: This release updates Archival manager - Automatic archival background.
- Change: Archival manager - Automatic archival background job (~450 lines).
- Why it matters: This matters because the release is clearer and easier to work with.

### 43. Consolidation - 5-consolidation pipeline [NEVER IMPLEMENTED]

- Need: This release updates Consolidation - 5-consolidation pipeline [NEVER IMPLEMENTED].
- Change: Consolidation - 5-phase consolidation pipeline [NEVER IMPLEMENTED].
- Why it matters: This matters because the release is clearer and easier to work with.

### 44. Layer definitions - 7-layer MCP architecture

- Need: This release updates Layer definitions - 7-layer MCP architecture.
- Change: Layer definitions - 7-layer MCP architecture (~320 lines).
- Why it matters: This matters because the release is clearer and easier to work with.

### 45. Provider chain - Embedding fallback chain

- Need: There was a reliability problem around Provider chain - Embedding fallback chain.
- Change: Provider chain - Embedding fallback chain (~340 lines) [NEVER IMPLEMENTED].
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

### 46. Envelope - Standardized response structure (~280 lines)

- Need: This release updates Envelope - Standardized response structure (~280 lines).
- Change: The release updates envelope - standardized response structure (~280 lines).
- Why it matters: This matters because the release is clearer and easier to work with.

### 47. Causal edges - Causal edge management

- Need: This release updates Causal edges - Causal edge management.
- Change: Causal edges - Causal edge management (~529 lines).
- Why it matters: This matters because the release is clearer and easier to work with.

### 48. Memory context - L1 Orchestration unified

- Need: This release updates Memory context - L1 Orchestration unified.
- Change: Memory context - L1 Orchestration unified entry (~420 lines).
- Why it matters: This matters because the release is clearer and easier to work with.

### 49. Causal graph - MCP handlers for

- Need: This release updates Causal graph - MCP handlers for.
- Change: Causal graph - MCP handlers for causal graph (~319 lines).
- Why it matters: This matters because the release is clearer and easier to work with.

### 50. Commands

- Need: This release updates Commands.
- Change: The release updates commands.
- Why it matters: This matters because the release is clearer and easier to work with.

### 51. /memory

- Need: There was a reliability problem around /memory.
- Change: /memory:continue - Session recovery from crash/compaction.
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

### 52. /memory

- Need: This release updates /memory.
- Change: /memory:context - Unified entry with intent awareness.
- Why it matters: This matters because the release is clearer and easier to work with.

### 53. /memory

- Need: This release updates /memory.
- Change: /memory:why - Decision lineage tracing via causal graph.
- Why it matters: This matters because the release is clearer and easier to work with.

### 54. /memory

- Need: This release updates /memory.
- Change: /memory:correct - Learning from mistakes with stability penalties.
- Why it matters: This matters because the release is clearer and easier to work with.

### 55. /memory

- Need: This release updates /memory.
- Change: /memory:learn - Explicit learning capture (patterns, pitfalls, insights).
- Why it matters: This matters because the release is clearer and easier to work with.

### 56. MCP Tools

- Need: This release updates MCP Tools.
- Change: The release updates mcp tools.
- Why it matters: This matters because the release is clearer and easier to work with.

### 57. Memory_drift_why - Trace causal chain for

- Need: This release updates Memory_drift_why - Trace causal chain for.
- Change: Memory_drift_why - Trace causal chain for "why" queries.
- Why it matters: This matters because the release is clearer and easier to work with.

### 58. Memory_causal_link - Create causal relationships between memories

- Need: This release updates Memory_causal_link - Create causal relationships between memories.
- Change: The release updates memory_causal_link - create causal relationships between memories.
- Why it matters: This matters because the release is clearer and easier to work with.

### 59. Memory_causal_stats - Graph statistics and coverage metrics

- Need: This release updates Memory_causal_stats - Graph statistics and coverage metrics.
- Change: The release updates memory_causal_stats - graph statistics and coverage metrics.
- Why it matters: This matters because the release is clearer and easier to work with.

### 60. Memory_causal_unlink - Remove causal relationships

- Need: Memory_causal_unlink - Remove causal relationships was no longer needed in its old form.
- Change: The release removes or retires memory_causal_unlink - remove causal relationships.
- Why it matters: This matters because the project becomes smaller and easier to maintain.

### 61. Memory_drift_context - Unified context with intent

- Need: Memory_drift_context - Unified context with intent was no longer needed in its old form.
- Change: Memory_drift_context - Unified context with intent awareness [NEVER IMPLEMENTED -- removed from roadmap].
- Why it matters: This matters because the project becomes smaller and easier to maintain.

### 62. Memory_drift_learn - Capture explicit learning [NEVER IMPLEMENTED

- Need: Memory_drift_learn - Capture explicit learning [NEVER IMPLEMENTED was no longer needed in its old form.
- Change: Memory_drift_learn - Capture explicit learning [NEVER IMPLEMENTED -- removed from roadmap].
- Why it matters: This matters because the project becomes smaller and easier to maintain.

### 63. Database Schema

- Need: The project needed a cleaner setup for Database Schema.
- Change: The release updates database schema.
- Why it matters: This matters because setup and maintenance are easier to manage.

### 64. V8

- Need: This release updates V8.
- Change: Causal_edges table (6 relation types, strength, evidence).
- Why it matters: This matters because the release is clearer and easier to work with.

### 65. V9

- Need: This release updates V9.
- Change: Memory_corrections table (stability tracking, pattern extraction).
- Why it matters: This matters because the release is clearer and easier to work with.

### 66. V10

- Need: There was a reliability problem around V10.
- Change: Session_state table (crash recovery, dirty flag).
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

### 67. V11

- Need: This release updates V11.
- Change: Added is_archived, archived_at columns.
- Why it matters: This matters because the release is clearer and easier to work with.

### 68. Templates

- Need: People needed clearer guidance about Templates.
- Change: The release updates templates.
- Why it matters: This matters because the release notes and instructions are easier to read and trust.

### 69. Context template v2.2

- Need: People needed clearer guidance about Context template v2.2.
- Change: CONTINUE_SESSION section, session_dedup metadata, causal_links.
- Why it matters: This matters because the release notes and instructions are easier to read and trust.

### 70. Save

- Need: The team needed stronger proof that Save worked as expected.
- Change: Phase 0 pre-flight validation, section 16 session deduplication, deferred indexing.
- Why it matters: This matters because the team has clearer proof that the change works.

### 71. Search

- Need: This release updates Search.
- Change: Hybrid search architecture, intent-aware retrieval, compression tiers.
- Why it matters: This matters because the release is clearer and easier to work with.

### 72. Memory Commands

- Need: This release updates Memory Commands.
- Change: The release updates memory commands.
- Why it matters: This matters because the release is clearer and easier to work with.

### 73. All 9 memory commands

- Need: This release updates All 9 memory commands.
- Change: All 9 memory commands updated with spec 082 cross-references.
- Why it matters: This matters because the release is clearer and easier to work with.

### 74. All 7 spec_kit commands

- Need: This release updates All 7 spec_kit commands.
- Change: All 7 spec_kit commands updated with Five Checks Framework.
- Why it matters: This matters because the release is clearer and easier to work with.

### 75. 12 YAML workflow assets

- Need: This release updates 12 YAML workflow assets.
- Change: 12 YAML workflow assets updated with context loading integration.
- Why it matters: This matters because the release is clearer and easier to work with.

### 76. Folder detector

- Need: There was a reliability problem around Folder detector.
- Change: CLI arguments now take priority over alignment validation (never override explicit user intent).
- Why it matters: This matters because it reduces confusing failures and makes the system more dependable.

## Files Changed

| File | What Changed |
| ---- | ------------ |
| `Spec Folder: specs/003-memory-and-spec-kit/082-speckit-reimagined/` | Updated in this release. |
| `Implementation Tasks: 107 tasks across 5 workstreams (W-S, W-R, W-D, W-G, W-I)` | Updated in this release. |
| `Test Coverage: cross-encoder (22), intent-classifier (25), provider-chain (28), causal-edges (29), archival-manager (32), fuzzy-match (40), five-factor-scoring (65), tier-classifier (78); overall 83.2% (89/107 tasks)` | Updated in this release. |
