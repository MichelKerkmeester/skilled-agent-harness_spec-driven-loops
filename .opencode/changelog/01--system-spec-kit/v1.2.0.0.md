# v1.2.0.0

> Part of [OpenCode Dev Environment](https://github.com/MichelKerkmeester/opencode--spec-kit-skilled-agent-orchestration)

---

## [**1.2.0.0**] - 2026-02-02

This release major release implementing SpecKit Reimagined with 33 features, 107 tasks, and 5 new memory commands. Adds session deduplication, causal memory graph, intent-aware retrieval, BM25 (exact word matching) hybrid search, and comprehensive crash recovery. It explains the main changes in simple language so the release is easier to understand.

---

## What Changed

---

### Session Management (W-S)

**Problem:** This release updates Session Management (W-S).

**Fix:** The release updates session management (w-s). This matters because the release is clearer and easier to work with.

---

### Session deduplication

**Problem:** This release updates Session deduplication.

**Fix:** Hash-based tracking prevents re-surfacing same memories (50% token savings). This matters because the release is clearer and easier to work with.

---

### Crash recovery

**Problem:** There was a reliability problem around Crash recovery.

**Fix:** Session_state table with automatic CONTINUE SESSION generation. This matters because it reduces confusing failures and makes the system more dependable.

---

### SessionStateManager API

**Problem:** This release updates SessionStateManager API.

**Fix:** ResetInterruptedSessions(), recoverState() with _recovered flag. This matters because the release is clearer and easier to work with.

---

### Search and Retrieval (W-R)

**Problem:** This release updates Search and Retrieval (W-R).

**Fix:** The release updates search and retrieval (w-r). This matters because the release is clearer and easier to work with.

---

### BM25 (exact word matching) hybrid search

**Problem:** There was a reliability problem around BM25 (exact word matching) (exact word matching) hybrid search.

**Fix:** Pure JavaScript BM25 (exact word matching) implementation for lexical search fallback. This matters because it reduces confusing failures and makes the system more dependable.

---

### RRF fusion enhancement

**Problem:** This release updates RRF fusion enhancement.

**Fix:** K=60 parameter with 10% convergence bonus for multi-source results. This matters because the release is clearer and easier to work with.

---

### Intent-aware retrieval

**Problem:** There was a reliability problem around Intent-aware retrieval.

**Fix:** 5 intent types (add_feature, fix_bug, refactor, security_audit, understand). This matters because it reduces confusing failures and makes the system more dependable.

---

### Cross-encoder reranking

**Problem:** This release updates Cross-encoder reranking.

**Fix:** Voyage/Cohere/local providers with length penalty (100 char threshold). This matters because the release is clearer and easier to work with.

---

### Fuzzy matching

**Problem:** This release updates Fuzzy matching.

**Fix:** Levenshtein distance (<=2) + ACRONYM_MAP (30+ entries). This matters because the release is clearer and easier to work with.

---

### Decay and Scoring (W-D)

**Problem:** This release updates Decay and Scoring (W-D).

**Fix:** The release updates decay and scoring (w-d). This matters because the release is clearer and easier to work with.

---

### Type-specific half-lives

**Problem:** This release updates Type-specific half-lives.

**Fix:** 9 memory types with distinct decay rates. This matters because the release is clearer and easier to work with.

---

### Multi-factor decay

**Problem:** This release updates Multi-factor decay.

**Fix:** Pattern alignment and citation recency factors. This matters because the release is clearer and easier to work with.

---

### Usage boost scoring

**Problem:** This release updates Usage boost scoring.

**Fix:** Enhanced 5-factor composite scoring. This matters because the release is clearer and easier to work with.

---

### Consolidation pipeline

**Problem:** This release updates Consolidation pipeline.

**Fix:** 5-phase duplicate detection and semantic merge. This matters because the release is clearer and easier to work with.

---

### Causal Memory Graph (W-G)

**Problem:** This release updates Causal Memory Graph (W-G).

**Fix:** The release updates causal memory graph (w-g). This matters because the release is clearer and easier to work with.

---

### Causal edges

**Problem:** This release updates Causal edges.

**Fix:** 6 relationship types (caused, enabled, supersedes, contradicts, derived_from, supports). This matters because the release is clearer and easier to work with.

---

### Decision lineage

**Problem:** This release updates Decision run history.

**Fix:** Depth-limited traversal (max 10 hops) with cycle detection. This matters because the release is clearer and easier to work with.

---

### Learning from corrections

**Problem:** This release updates Learning from corrections.

**Fix:** Stability penalties (0.5x) and boosts (1.2x) for replacements. This matters because the release is clearer and easier to work with.

---

### Infrastructure (W-I)

**Problem:** The project needed a cleaner setup for Infrastructure (W-I).

**Fix:** The release updates infrastructure (w-i). This matters because setup and maintenance are easier to manage.

---

### Recovery hints catalog

**Problem:** There was a reliability problem around Recovery hints catalog.

**Fix:** 49 error codes with actionable recovery guidance. This matters because it reduces confusing failures and makes the system more dependable.

---

### Tool output caching

**Problem:** This release updates Tool output caching.

**Fix:** 60s TTL with LRU eviction. This matters because the release is clearer and easier to work with.

---

### Lazy model loading

**Problem:** This release updates Lazy model loading.

**Fix:** Deferred embedding initialization. This matters because the release is clearer and easier to work with.

---

### Standardized response envelope

**Problem:** This release updates Standardized response envelope.

**Fix:** {summary, data, hints, meta} structure. This matters because the release is clearer and easier to work with.

---

### Layered tool organization

**Problem:** This release updates Layered tool organization.

**Fix:** 7-layer MCP architecture (L1-L7). This matters because the release is clearer and easier to work with.

---

### Incremental indexing

**Problem:** This release updates Incremental indexing.

**Fix:** Content hash + mtime-based diff updates. This matters because the release is clearer and easier to work with.

---

### Pre-flight quality gates

**Problem:** The team needed stronger proof that Pre-flight quality gates worked as expected.

**Fix:** ANCHOR validation, duplicate detection, token budget checks. This matters because the team has clearer proof that the change works.

---

### Protocol abstractions

**Problem:** This release updates Protocol abstractions.

**Fix:** Provider-agnostic embedding interfaces. This matters because the release is clearer and easier to work with.

---

### API key validation

**Problem:** The team needed stronger proof that API key validation worked as expected.

**Fix:** Secure key handling with actionable guidance. This matters because the team has clearer proof that the change works.

---

### Fallback chain

**Problem:** There was a reliability problem around Fallback chain.

**Fix:** Primary API to Local to BM25 (exact word matching)-only with logging. This matters because it reduces confusing failures and makes the system more dependable.

---

### Deferred indexing

**Problem:** This release updates Deferred indexing.

**Fix:** Graceful degradation when embeddings fail. This matters because the release is clearer and easier to work with.

---

### Retry logic

**Problem:** This release updates Retry logic.

**Fix:** Exponential backoff (1s, 2s, 4s) with jitter. This matters because the release is clearer and easier to work with.

---

### Transaction atomicity

**Problem:** This release updates Transaction atomicity.

**Fix:** SAVEPOINT/ROLLBACK patterns. This matters because the release is clearer and easier to work with.

---

### Files

**Problem:** This release updates Files.

**Fix:** The release updates files. This matters because the release is clearer and easier to work with.

---

### Session manager - Session deduplication and

**Problem:** There was a reliability problem around Session manager - Session deduplication and.

**Fix:** Session manager - Session deduplication and crash recovery (~1050 lines). This matters because it reduces confusing failures and makes the system more dependable.

---

### Recovery hints - 49 error codes

**Problem:** There was a reliability problem around Recovery hints - 49 error codes.

**Fix:** Recovery hints - 49 error codes with hints (~520 lines). This matters because it reduces confusing failures and makes the system more dependable.

---

### Bm25 index - Pure JS BM25 (exact word matching)

**Problem:** This release updates Bm25 index - Pure JS BM25 (exact word matching) (exact word matching).

**Fix:** Bm25 index - Pure JS BM25 (exact word matching) implementation (~380 lines). This matters because the release is clearer and easier to work with.

---

### Cross encoder - Reranking with Voyage/Cohere/local

**Problem:** This release updates Cross encoder - Reranking with Voyage/Cohere/local.

**Fix:** Cross encoder - Reranking with Voyage/Cohere/local (~490 lines). This matters because the release is clearer and easier to work with.

---

### Intent classifier - Intent detection for

**Problem:** This release updates Intent classifier - Intent detection for.

**Fix:** Intent classifier - Intent detection for 5 types. This matters because the release is clearer and easier to work with.

---

### Fuzzy match - Levenshtein + acronym

**Problem:** This release updates Fuzzy match - Levenshtein + acronym.

**Fix:** Fuzzy match - Levenshtein + acronym expansion [NEVER IMPLEMENTED]. This matters because the release is clearer and easier to work with.

---

### Corrections - Memory corrections tracking (~560

**Problem:** This release updates Corrections - Memory corrections tracking (~560.

**Fix:** Corrections - Memory corrections tracking (~560 lines) [NEVER IMPLEMENTED]. This matters because the release is clearer and easier to work with.

---

### Archival manager - Automatic archival background

**Problem:** This release updates Archival manager - Automatic archival background.

**Fix:** Archival manager - Automatic archival background job (~450 lines). This matters because the release is clearer and easier to work with.

---

### Consolidation - 5-consolidation pipeline [NEVER IMPLEMENTED]

**Problem:** This release updates Consolidation - 5-consolidation pipeline [NEVER IMPLEMENTED].

**Fix:** Consolidation - 5-phase consolidation pipeline [NEVER IMPLEMENTED]. This matters because the release is clearer and easier to work with.

---

### Layer definitions - 7-layer MCP architecture

**Problem:** This release updates Layer definitions - 7-layer MCP architecture.

**Fix:** Layer definitions - 7-layer MCP architecture (~320 lines). This matters because the release is clearer and easier to work with.

---

### Provider chain - Embedding fallback chain

**Problem:** There was a reliability problem around Provider chain - Embedding fallback chain.

**Fix:** Provider chain - Embedding fallback chain (~340 lines) [NEVER IMPLEMENTED]. This matters because it reduces confusing failures and makes the system more dependable.

---

### Envelope - Standardized response structure (~280 lines)

**Problem:** This release updates Envelope - Standardized response structure (~280 lines).

**Fix:** The release updates envelope - standardized response structure (~280 lines). This matters because the release is clearer and easier to work with.

---

### Causal edges - Causal edge management

**Problem:** This release updates Causal edges - Causal edge management.

**Fix:** Causal edges - Causal edge management (~529 lines). This matters because the release is clearer and easier to work with.

---

### Memory context - L1 Orchestration unified

**Problem:** This release updates Memory context - L1 Orchestration unified.

**Fix:** Memory context - L1 Orchestration unified entry (~420 lines). This matters because the release is clearer and easier to work with.

---

### Causal graph - MCP handlers for

**Problem:** This release updates Causal graph - MCP handlers for.

**Fix:** Causal graph - MCP handlers for causal graph (~319 lines). This matters because the release is clearer and easier to work with.

---

### Commands

**Problem:** This release updates Commands.

**Fix:** The release updates commands. This matters because the release is clearer and easier to work with.

---

### /memory

**Problem:** There was a reliability problem around /memory.

**Fix:** /memory:continue - Session recovery from crash/compaction. This matters because it reduces confusing failures and makes the system more dependable.

---

### /memory

**Problem:** This release updates /memory.

**Fix:** /memory:context - Unified entry with intent awareness. This matters because the release is clearer and easier to work with.

---

### /memory

**Problem:** This release updates /memory.

**Fix:** /memory:why - Decision run history tracing via causal graph. This matters because the release is clearer and easier to work with.

---

### /memory

**Problem:** This release updates /memory.

**Fix:** /memory:correct - Learning from mistakes with stability penalties. This matters because the release is clearer and easier to work with.

---

### /memory

**Problem:** This release updates /memory.

**Fix:** /memory:learn - Explicit learning capture (patterns, pitfalls, insights). This matters because the release is clearer and easier to work with.

---

### MCP Tools

**Problem:** This release updates MCP Tools.

**Fix:** The release updates mcp tools. This matters because the release is clearer and easier to work with.

---

### Memory_drift_why - Trace causal chain for

**Problem:** This release updates Memory_drift_why - Trace causal chain for.

**Fix:** Memory_drift_why - Trace causal chain for "why" queries. This matters because the release is clearer and easier to work with.

---

### Memory_causal_link - Create causal relationships between memories

**Problem:** This release updates Memory_causal_link - Create causal relationships between memories.

**Fix:** The release updates memory_causal_link - create causal relationships between memories. This matters because the release is clearer and easier to work with.

---

### Memory_causal_stats - Graph statistics and coverage metrics

**Problem:** This release updates Memory_causal_stats - Graph statistics and coverage metrics.

**Fix:** The release updates memory_causal_stats - graph statistics and coverage metrics. This matters because the release is clearer and easier to work with.

---

### Memory_causal_unlink - Remove causal relationships

**Problem:** Memory_causal_unlink - Remove causal relationships was no longer needed in its old form.

**Fix:** The release removes or retires memory_causal_unlink - remove causal relationships. This matters because the project becomes smaller and easier to maintain.

---

### Memory_drift_context - Unified context with intent

**Problem:** Memory_drift_context - Unified context with intent was no longer needed in its old form.

**Fix:** Memory_drift_context - Unified context with intent awareness [NEVER IMPLEMENTED -- removed from roadmap]. This matters because the project becomes smaller and easier to maintain.

---

### Memory_drift_learn - Capture explicit learning [NEVER IMPLEMENTED

**Problem:** Memory_drift_learn - Capture explicit learning [NEVER IMPLEMENTED was no longer needed in its old form.

**Fix:** Memory_drift_learn - Capture explicit learning [NEVER IMPLEMENTED -- removed from roadmap]. This matters because the project becomes smaller and easier to maintain.

---

### Database Schema

**Problem:** The project needed a cleaner setup for Database Schema.

**Fix:** The release updates database schema. This matters because setup and maintenance are easier to manage.

---

### V8

**Problem:** This release updates V8.

**Fix:** Causal_edges table (6 relation types, strength, evidence). This matters because the release is clearer and easier to work with.

---

### V9

**Problem:** This release updates V9.

**Fix:** Memory_corrections table (stability tracking, pattern extraction). This matters because the release is clearer and easier to work with.

---

### V10

**Problem:** There was a reliability problem around V10.

**Fix:** Session_state table (crash recovery, dirty flag). This matters because it reduces confusing failures and makes the system more dependable.

---

### V11

**Problem:** This release updates V11.

**Fix:** Added is_archived, archived_at columns. This matters because the release is clearer and easier to work with.

---

### Templates

**Problem:** People needed clearer guidance about Templates.

**Fix:** The release updates templates. This matters because the release notes and instructions are easier to read and trust.

---

### Context template v2.2

**Problem:** People needed clearer guidance about Context template v2.2.

**Fix:** CONTINUE_SESSION section, session_dedup metadata, causal_links. This matters because the release notes and instructions are easier to read and trust.

---

### Save

**Problem:** The team needed stronger proof that Save worked as expected.

**Fix:** Phase 0 pre-flight validation, section 16 session deduplication, deferred indexing. This matters because the team has clearer proof that the change works.

---

### Search

**Problem:** This release updates Search.

**Fix:** Hybrid search architecture, intent-aware retrieval, compression tiers. This matters because the release is clearer and easier to work with.

---

### Memory Commands

**Problem:** This release updates Memory Commands.

**Fix:** The release updates memory commands. This matters because the release is clearer and easier to work with.

---

### All 9 memory commands

**Problem:** This release updates All 9 memory commands.

**Fix:** All 9 memory commands updated with spec 082 cross-references. This matters because the release is clearer and easier to work with.

---

### All 7 spec_kit commands

**Problem:** This release updates All 7 spec_kit commands.

**Fix:** All 7 spec_kit commands updated with Five Checks Framework. This matters because the release is clearer and easier to work with.

---

### 12 YAML workflow assets

**Problem:** This release updates 12 YAML workflow assets.

**Fix:** 12 YAML workflow assets updated with context loading integration. This matters because the release is clearer and easier to work with.

---

### Folder detector

**Problem:** There was a reliability problem around Folder detector.

**Fix:** CLI arguments now take priority over alignment validation (never override explicit user intent). This matters because it reduces confusing failures and makes the system more dependable.

---

## Files Changed

| File | What Changed |
| ---- | ------------ |
| `Spec Folder: specs/003-memory-and-spec-kit/082-speckit-reimagined/` | Updated in this release. |
| `Implementation Tasks: 107 tasks across 5 workstreams (W-S, W-R, W-D, W-G, W-I)` | Updated in this release. |
| `Test Coverage: cross-encoder (22), intent-classifier (25), provider-chain (28), causal-edges (29), archival-manager (32), fuzzy-match (40), five-factor-scoring (65), tier-classifier (78); overall 83.2% (89/107 tasks)` | Updated in this release. |
