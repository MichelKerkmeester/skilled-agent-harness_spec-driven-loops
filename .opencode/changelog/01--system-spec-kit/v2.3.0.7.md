This release two critical MCP server bugs fixed: tool schema rejection and vec_memories UNIQUE constraint - Claude API tool calls were blocked by a top-level oneOf in the memory_delete schema, and memory_save with force: true failed on orphaned vector entries from prior incomplete deletions. It focuses on fixing important problems and explaining the results in clearer terms. The goal is to make the workflow easier to follow and easier to trust.

---

## What Changed

---

### Tool schema API rejection fixed

**Problem:** There was a reliability problem around Tool schema API rejection fixed.

**Fix:** Tool schema API rejection fixed: Removed oneOf from memory_delete input schema. The Claude API rejects oneOf, allOf, and anyOf at the top level of tool schemas, which blocked ALL MCP tool calls including subagent dispatch. This matters because it reduces confusing failures and makes the system more dependable.

---

### Vec_memories UNIQUE constraint fixed

**Problem:** There was a reliability problem around Vec_memories UNIQUE constraint fixed.

**Fix:** Vec_memories UNIQUE constraint fixed: Added defensive DELETE before INSERT in the index_memory creation path. Without AUTOINCREMENT on memory_index, SQLite can reuse rowids of deleted rows whose vec_memories counterpart was not cleaned up, causing UNIQUE constraint failures on memory_save. This matters because it reduces confusing failures and makes the system more dependable.

---

### Schema structural meta-test added

**Problem:** The team needed stronger proof that Schema structural meta-test added worked as expected.

**Fix:** Schema structural meta-test added: New test verifies zero oneOf/allOf/anyOf across all 22 tool schemas to prevent future regressions. This matters because the team has clearer proof that the change works.

---

### Test suite expanded

**Problem:** The team needed stronger proof that Test suite expanded worked as expected.

**Fix:** Test suite expanded: tool input schema.vitest expanded from 5 to 15 tests covering schema integrity, memory_delete argument permutations, and type validation. This matters because the team has clearer proof that the change works.

---

### Full regression suite passed

**Problem:** The team needed stronger proof that Full regression suite passed worked as expected.

**Fix:** Full regression suite passed: 4,605/4,606 tests pass (1 pre-existing modularization failure, unrelated). This matters because the team has clearer proof that the change works.

---

### Alignment verifier passed

**Problem:** The team needed stronger proof that Alignment verifier passed worked as expected.

**Fix:** Alignment verifier passed: 288 files scanned, 0 findings. This matters because the team has clearer proof that the change works.

---

## Files Changed

| File | What Changed |
| ---- | ------------ |
| `.opencode/skill/system-spec-kit/mcp_server/tool-schemas.ts` | Removed oneOf from memoryDelete schema; constraint documented in description. |
| `.opencode/skill/system-spec-kit/mcp_server/lib/search/vector-index-impl.ts` | Defensive DELETE before vec_memories INSERT to prevent orphaned rowid conflicts. |
| `.opencode/skill/system-spec-kit/mcp_server/tests/tool-input-schema.vitest.ts` | Expanded from 5 to 15 tests: structural integrity + exhaustive memory_delete coverage. |

---

## Upgrade

- No migration required. Both fixes are backward-compatible. The memory_delete handler already validated the either/or constraint server-side; the schema change only affects Claude API compatibility. The defensive DELETE is a no-op when no orphaned vector exists.
