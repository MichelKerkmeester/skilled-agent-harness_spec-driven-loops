This release alignment remediation completion for spec 023/016: fixed save-quality-gate persistence edge cases, hardened hybrid-search dedup and fallback routing behavior, and reconciled feature summary documentation with runtime truth. It focuses on fixing important problems and explaining the results in clearer terms. The goal is to make the workflow easier to follow and easier to trust.

---

## What Changed

#### Save quality gate

There was a reliability problem around Save quality gate. Save quality gate now tracks config-table ensure state per active database handle and reruns database schema safely after handle changes. This matters because the release is clearer and easier to work with.

&nbsp;

#### Activation timestamp initialization

There was a reliability problem around Activation timestamp initialization. Activation timestamp initialization now preserves persisted warn-only window state across restart (ensureActivationTimestampInitialized). This matters because the release is clearer and easier to work with.

&nbsp;

#### Added focused regressions WO6 and WO7

There was a reliability problem around Added focused regressions WO6 and WO7. Added focused regressions WO6 and WO7 in save quality gate.vitest. This matters because the team has clearer proof that the change works.

&nbsp;

#### Canonical ID dedup

This release updates Canonical ID dedup. Canonical ID dedup is now applied in both combinedLexicalSearch() and legacy hybridSearch() (42, "42", mem:42). This matters because the release is clearer and easier to work with.

&nbsp;

#### Tier-2 fallback

There was a reliability problem around Tier-2 fallback. Tier-2 fallback now forces all retrieval channels via forceAllChannels and bypasses simple-route channel reduction. This matters because it reduces confusing failures and makes the system more dependable.

&nbsp;

#### Added regressions T031-LEX-05, T031-BASIC-04, and C138-P0-FB-T2

The team needed stronger proof that Added regressions T031-LEX-05, T031-BASIC-04, and C138-P0-FB-T2 worked as expected. Added regressions T031-LEX-05, T031-BASIC-04, and C138-P0-FB-T2 in hybrid search.vitest. This matters because the team has clearer proof that the change works.

&nbsp;

#### Corrected parent summary drift for RSF/shadow/fallback

People needed clearer guidance about Corrected parent summary drift for RSF/shadow/fallback. Corrected parent summary drift for RSF/shadow/fallback semantics, R11 default and shadow-period wording, TM-05 hook scope, and dead-code references. This matters because the release notes and instructions are easier to read and trust.

&nbsp;

#### Corrected S5/entity-linking wording for global density

People needed clearer guidance about Corrected S5/entity-linking wording for global density. Corrected S5/entity-linking wording for global density checks, database batching description, MPAB naming, PI-B3 labeling, and active search-flag count. This matters because the release notes and instructions are easier to read and trust.

&nbsp;

#### Finalized child Level 2 docs

People needed clearer guidance about Finalized child Level 2 docs. Finalized child Level 2 spec docs in 016-refinement-phase-5 to completed tranche-1 through tranche-4 status. This matters because the release notes and instructions are easier to read and trust.

&nbsp;

#### Targeted suite passed

The team needed stronger proof that Targeted suite passed worked as expected. Targeted suite passed: hybrid search.vitest, save quality gate.vitest, mpab quality gate integration.vitest (176 tests). This matters because the team has clearer proof that the change works.

&nbsp;

#### Child validation

There was a reliability problem around Child validation. Child spec validation passed with zero errors and zero warnings. This matters because it reduces confusing failures and makes the system more dependable.

&nbsp;

#### Checklist completion check

The team needed stronger proof that Checklist completion check worked as expected. Checklist completion check reports 32/32 complete. This matters because the team has clearer proof that the change works.

---

## Files Changed

| File | What Changed |
| ---- | ------------ |
| `.opencode/specs/system-spec-kit/022-hybrid-rag-fusion/016-refinement-phase-5` | Spec folder referenced by this release. |
| `.opencode/skill/system-spec-kit/mcp_server/lib/validation/save-quality-gate.ts` | Updated in this release. |
| `.opencode/skill/system-spec-kit/mcp_server/lib/search/hybrid-search.ts` | Updated in this release. |
| `.opencode/skill/system-spec-kit/mcp_server/lib/search/entity-linker.ts` | Updated in this release. |
| `.opencode/skill/system-spec-kit/mcp_server/tests/save-quality-gate.vitest.ts` | Updated in this release. |
| `.opencode/skill/system-spec-kit/mcp_server/tests/hybrid-search.vitest.ts` | Updated in this release. |
| `.opencode/specs/system-spec-kit/022-hybrid-rag-fusion/000-feature-overview/summary_of_existing_features.md` | Updated in this release. |
| `.opencode/specs/system-spec-kit/022-hybrid-rag-fusion/000-feature-overview/summary_of_new_features.md` | Updated in this release. |
| `.opencode/specs/system-spec-kit/022-hybrid-rag-fusion/016-refinement-phase-5/spec.md` | Updated in this release. |
| `.opencode/specs/system-spec-kit/022-hybrid-rag-fusion/016-refinement-phase-5/plan.md` | Updated in this release. |
| `.opencode/specs/system-spec-kit/022-hybrid-rag-fusion/016-refinement-phase-5/tasks.md` | Updated in this release. |
| `.opencode/specs/system-spec-kit/022-hybrid-rag-fusion/016-refinement-phase-5/checklist.md` | Updated in this release. |
| `.opencode/specs/system-spec-kit/022-hybrid-rag-fusion/016-refinement-phase-5/implementation-summary.md` | Updated in this release. |

---

## Upgrade

- No migration required.
