# v3.0.1.4

> Part of [OpenCode Dev Environment](https://github.com/MichelKerkmeester/opencode--spec-kit-skilled-agent-orchestration)

---

## [**3.0.1.4**] - 2026-03-29

The create:changelog command can now publish GitHub releases in addition to creating changelog files. Previously, creating a changelog and publishing a release were separate manual steps -- you had to create the changelog file, then separately create a git tag, push it, and run `gh release create` with properly formatted notes. Now the command handles the entire flow with a single `--release` flag.

> Spec folder: `.opencode/specs/03--commands-and-skills/038-cmd-create-changelog-and-releases/` (Level 2)

---

## What Changed

### Commands

- **create:changelog now supports GitHub release creation** -- Added a `--release` flag and Q3 prompt that optionally creates an annotated git tag, pushes it, composes plain-English release notes (stripping local wrapper lines), and runs `gh release create`. The release notes follow the PUBLIC_RELEASE.md Section 7 format: explain what was broken, what was fixed, and why it matters, written for a smart person who is not a developer.

- **Release notes are auto-composed from the changelog file** -- The command reads the changelog it just created, strips the local-only headers (`# vX.X.X`, `> Part of...`, `## [**X.X.X**] - date`), and uses the remaining content as the GitHub release body. A link back to the full changelog file is appended.

- **Error handling for release failures** -- New error recovery for tag conflicts (ask user to force-update or increment BUILD), auth failures (suggest `gh auth login`), and release conflicts (offer to update existing or skip).

## Files Changed

| File | Action | Description |
| ---- | ------ | ----------- |
| `.opencode/command/create/changelog.md` | Modified | Added --release flag, Q3 prompt, Release Creation Phase (Section 8), updated examples/errors/resources |
| `.agents/commands/create/changelog.toml` | Modified | Mirrored changelog.md changes |
| `.opencode/skill/sk-git/references/finish_workflows.md` | Modified | Added cross-reference to create:changelog --release in Step 6 and decision matrix |

## Upgrade

No migration required. Existing `/create:changelog` usage is unchanged -- the `--release` flag is optional and defaults to No.
