## [**2.0.5.1**] - 2026-02-19

Normalized command-side Claude runtime mapping to /.claude/agents and aligned memory command documentation with adaptive-fusion, telemetry, and mutation-ledger behavior. The main changes here are claude runtime path corrected and recovery-path detection updated.

### Claude Runtime Path Corrected

- Need: The older guidance still pointed to something that was outdated or no longer used.
- What changed: Command markdown runtime mapping now points to /.claude/agents instead of the removed.opencode/agent/claude path.
- Why it matters: This reduces confusion by keeping the notes focused on supported behavior.

### Recovery-path Detection Updated

- Need: This part of the command documentation needed clearer guidance and less room for confusion.
- What changed: /memory:continue mismatch detection matrix now treats /.claude/agents as the canonical Claude agent root.
- Why it matters: This makes the command documentation easier to follow.

### Adaptive Fusion Guidance Added

- Need: People needed clearer support for this new part of the workflow.
- What changed: /memory:context and /memory:continue now document adaptive weighting behavior under SPECKIT_ADAPTIVE_FUSION.
- Why it matters: This makes the workflow easier to discover and use.

### Telemetry and Auditability Clarified

- Need: This part of the command documentation needed clearer guidance and less room for confusion.
- What changed: /memory:manage, /memory:save, and /memory:learn now include extended telemetry and mutation-ledger behavior notes.
- Why it matters: This makes the command documentation easier to follow.


---

## Files Changed

| File | Action | Notes |
| ---- | ------ | ----- |
| `.opencode/command/create/{agent,folder_readme,install_guide,skill,skill_asset,skill_reference}.md` | Claude runtime mapping switched to /.claude/agents | Claude runtime mapping switched to /.claude/agents |
| `.opencode/command/spec_kit/{debug,handover}.md` | Runtime mapping switched to /.claude/agents | Runtime mapping switched to /.claude/agents |
| `.opencode/command/memory/continue.md` | Claude root detection and adaptive recovery guidance updated | Claude root detection and adaptive recovery guidance updated |
| `.opencode/command/memory/{context,learn,manage,save}.md` | Added adaptive-fusion, telemetry, and mutation-ledger guidance | Added adaptive-fusion, telemetry, and mutation-ledger guidance |


---

## Upgrade

- No migration is required. Pull latest to get corrected Claude runtime-path guidance in command workflows.
