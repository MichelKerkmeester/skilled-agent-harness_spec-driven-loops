# v2.0.5.1

> Part of [OpenCode Dev Environment](https://github.com/MichelKerkmeester/opencode-spec-kit-framework)

---

## [**2.0.5.1**] - 2026-02-19

**Command runtime-path correction for Claude profile plus memory-command guidance updates** -- Normalized command-side Claude runtime mapping to `/.claude/agents` and aligned memory command documentation with adaptive-fusion, telemetry, and mutation-ledger behavior.

> Spec folders: `.opencode/specs/004-agents/021-codex-orchestrate/` and `.opencode/specs/003-system-spec-kit/136-mcp-working-memory-hybrid-rag/`

---

## Highlights

### Category: Runtime Path Resolution

- **Claude runtime path corrected**: Command markdown runtime mapping now points to `/.claude/agents` instead of the removed `.opencode/agent/claude` path.
- **Recovery-path detection updated**: `/memory:continue` mismatch detection matrix now treats `/.claude/agents` as the canonical Claude agent root.

### Category: Memory Command Documentation

- **Adaptive fusion guidance added**: `/memory:context` and `/memory:continue` now document adaptive weighting behavior under `SPECKIT_ADAPTIVE_FUSION`.
- **Telemetry and auditability clarified**: `/memory:manage`, `/memory:save`, and `/memory:learn` now include extended telemetry and mutation-ledger behavior notes.

## Files Changed

| File | Change |
|------|--------|
| `.opencode/command/create/{agent,folder_readme,install_guide,skill,skill_asset,skill_reference}.md` | Claude runtime mapping switched to `/.claude/agents` |
| `.opencode/command/spec_kit/{debug,handover}.md` | Runtime mapping switched to `/.claude/agents` |
| `.opencode/command/memory/continue.md` | Claude root detection and adaptive recovery guidance updated |
| `.opencode/command/memory/{context,learn,manage,save}.md` | Added adaptive-fusion, telemetry, and mutation-ledger guidance |

## Upgrade

No migration is required. Pull latest to get corrected Claude runtime-path guidance in command workflows.
