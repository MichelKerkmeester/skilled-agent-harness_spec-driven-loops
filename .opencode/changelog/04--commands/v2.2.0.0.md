## [**2.2.0.0**] - 2026-03-01

Mode-based workflow command wrapping the sk-prompt-improver skill. Provides a streamlined entry point for prompt engineering with 7 proven frameworks, DEPTH thinking methodology, and CLEAR quality scoring. Follows full create namespace conventions (Phase 0, Unified Setup Phase, Phase Status Verification). The main changes here are /create:prompt command and phase 0 + unified setup phase.

### 1. /create:prompt Command

- Need: People needed clearer support for this new part of the workflow.
- What changed: New 530-line command at the related file that wraps the sk-prompt-improver skill for prompt creation and improvement.
- Why it matters: This makes the workflow easier to discover and use.

### 2. Phase 0 + Unified Setup Phase

- Need: This part of the command documentation needed clearer guidance and less room for confusion.
- What changed: Full create namespace gate pattern: @general agent verification, consolidated Q0-Q3 prompt (prompt input, enhancement mode, save location, execution mode) in a single user interaction.
- Why it matters: This makes the command documentation easier to follow.

### 3. 7 Mode Prefixes

- Need: This part of the documentation needed to match the related files more closely.
- What changed: $text, $improve, $refine, $short, $json, $yaml, $raw with aliases, mapping to sk-prompt-improver operating modes and energy levels (Raw/Quick/Standard/Deep).
- Why it matters: This helps keep different copies of the same guidance from drifting apart.

### 4. DEPTH Processing Pipeline

- Need: This part of the command documentation needed clearer guidance and less room for confusion.
- What changed: 5-phase enhancement (Discover-Engineer-Prototype-Test-Harmonize) with configurable round counts per mode.
- Why it matters: This makes the command documentation easier to follow.

### 5. CLEAR Scoring

- Need: This part of the command documentation needed clearer guidance and less room for confusion.
- What changed: 50-point quality scoring (Correctness, Logic, Expression, Arrangement, Reusability) with dimension floors and 40+/50 threshold enforcement.
- Why it matters: This makes the command documentation easier to follow.

### 6. Prompt Saving to Spec Folders

- Need: This part of the command documentation needed clearer guidance and less room for confusion.
- What changed: Optional save to specs/[folder]/prompts/[descriptive-name].md with top-of-file metadata metadata (title, framework, mode, CLEAR score, created date).
- Why it matters: This makes the command documentation easier to follow.

### 7. Dual Execution Modes

- Need: This part of the command documentation needed clearer guidance and less room for confusion.
- What changed: :auto (autonomous, default) and:confirm (interactive with approval gates at each DEPTH phase).
- Why it matters: This makes the command documentation easier to follow.

## Files Changed

| File | Action | Notes |
| ---- | ------ | ----- |
| `.opencode/command/create/prompt.md` | Created | 530 |
