## [**2.0.5.0**] - 2026-02-19

Removed hardcoded agent-definition paths from command markdown/workflow config and standardized command-side runtime routing through canonical profile roots. The main changes here are canonical runtime mapping documented in command docs and command yaml agent dispatch pathing normalized.

---

### Canonical Runtime Mapping Documented in Command Docs

- Need: The writing in this area needed to be easier to read and follow.
- What changed: Default/Copilot ->.opencode/agent, ChatGPT ->.opencode/agent/chatgpt, Claude ->.opencode/agent/claude.
- Why it matters: This makes the documentation easier for non-developers to read.

---

### Command YAML Agent Dispatch Pathing Normalized

- Need: This part of the command documentation needed clearer guidance and less room for confusion.
- What changed: Replaced hardcoded agent_file: ".opencode/agent/<agent>.md" with agent_file: "[runtime_agent_path]/<agent>.md" in create/spec_kit assets.
- Why it matters: This makes the command documentation easier to follow.

---

### Cross-runtime Command Compatibility Improved

- Need: This part of the documentation needed to match the related files more closely.
- What changed: Command instructions now align with current provider routing while remaining compatible with symlinked command trees.
- Why it matters: This helps keep different copies of the same guidance from drifting apart.

---

### /create Docs Updated

- Need: People needed clearer support for this new part of the workflow.
- What changed: Added explicit runtime path resolution sections and runtime-aware path defaults.
- Why it matters: This makes the workflow easier to discover and use.

---

### /spec_kit Docs Updated

- Need: People needed clearer support for this new part of the workflow.
- What changed: Added runtime path resolution guidance for debug/handover command flows.
- Why it matters: This makes the workflow easier to discover and use.

---

### /memory:continue Updated

- Need: This part of the command documentation needed clearer guidance and less room for confusion.
- What changed: Expanded agent-path mismatch detection to include all runtime roots.
- Why it matters: This makes the command documentation easier to follow.

---

### Create Command Troubleshooting Updated

- Need: People needed clearer support for this new part of the workflow.
- What changed: Added runtime-path verification guidance in the README guide.
- Why it matters: This makes the workflow easier to discover and use.

---

### YAML Parse Validation Passed

- Need: This part of the command documentation needed clearer guidance and less room for confusion.
- What changed: 23 modified command workflow config files parsed successfully (YAML_OK 23).
- Why it matters: This makes the command documentation easier to follow.

---

### Hardcoded Path Scan Passed

- Need: This part of the command documentation needed clearer guidance and less room for confusion.
- What changed: No remaining agent_file: ".opencode/agent/..." matches in.opencode/command workflow config files.
- Why it matters: This makes the command documentation easier to follow.

---

### Change Scope Verified

- Need: This part of the command documentation needed clearer guidance and less room for confusion.
- What changed: .opencode/command diff reported several files changed, 147 insertions, 71 deletions.
- Why it matters: This makes the command documentation easier to follow.


---

## Files Changed

| File | Action | Notes |
| ---- | ------ | ----- |
| `.opencode/command/create/*.md` | Added runtime-path resolution sections and runtime-aware command guidance | Added runtime-path resolution sections and runtime-aware command guidance |
| `.opencode/command/create/assets/*.yaml` | Replaced hardcoded agent_file values with [runtime_agent_path] placeholders; normalized create-agent defaults | Replaced hardcoded agent_file values with [runtime_agent_path] placeholders; normalized create-agent defaults |
| `.opencode/command/spec_kit/*.md` | Added runtime-path resolution sections for spec-kit command flows | Added runtime-path resolution sections for spec-kit command flows |
| `.opencode/command/spec_kit/assets/*.yaml` | Replaced hardcoded agent_file paths with [runtime_agent_path] placeholders | Replaced hardcoded agent_file paths with [runtime_agent_path] placeholders |
| `.opencode/command/memory/continue.md` | Expanded runtime-path detection matrix for agent-related memory continuation | Expanded runtime-path detection matrix for agent-related memory continuation |
| `.opencode/command/create/README.txt` | Added troubleshooting note for runtime-path verification | Added troubleshooting note for runtime-path verification |


---

## Upgrade

- No migration required. Pull latest to apply runtime-aware command path routing and remove hardcoded command agent path assumptions.
