# v2.0.2.0

> Part of [OpenCode Dev Environment](https://github.com/MichelKerkmeester/opencode-spec-kit-framework)

---

## [**2.0.2.0**] - 2026-02-14

**Agent Routing Compliance** — All **33 command files** (12 create YAML + 6 create MD + 10 spec_kit YAML + 5 spec_kit MD) now route through proper agents per AGENTS.md rules. **204 verification checks** passed for create commands; **18/18 test suite** passed for spec_kit commands.

> Spec folder: `anobel.com/.opencode/specs/004-agents/014-command-agent-utilization/` (Level 3)

---

## Highlights

### Create Commands (18 files)

- **@speckit routing**: `create_skill` and `create_agent` spec folder setup now dispatches @speckit instead of inline Write (4 YAML)
- **@context routing**: `create_skill` and `create_agent` discovery now dispatches @context instead of inline Glob/Grep (4 YAML)
- **@review quality gate**: All 12 YAML files have non-blocking @review step (100-point rubric, threshold 70)
- **Agent Routing sections**: All 6 MD reference files document step-agent-rule-purpose mappings

### Spec Kit Commands (15 files)

- **`implement.md` / `complete.md`**: @debug dispatch at failure >= 3, dual-phase @review (Mode 2 Pre-Commit + Mode 4 Gate), @handover at session end
- **`plan.md`**: @research on confidence < 60%, @handover at session end
- **`debug.md`**: @review post-fix validation (advisory)
- **`research.md`**: @handover at session end
- **`resume.md` / `handover.md`**: No changes (meta-workflow tools, no artifact creation)

### Key Decisions

- **Scope narrowed**: Only 4 of 12 create YAML files had spec_folder_setup steps — avoided modifying 8 files that don't create spec folders
- **@review non-blocking**: `blocking: false` on create commands; dual-phase (advisory + blocking) on spec_kit code-writing workflows
- **Standardized `agent_routing` block**: 5-field structure (`agent`, `agent_file`, `rule_reference`, `dispatch`, `fallback`/`blocking`) across all files

## Files Changed

**Create YAML (12):** `.opencode/command/create/assets/create_{skill,agent,folder_readme,install_guide,skill_asset,skill_reference}_{auto,confirm}.yaml`

**Create MD (6):** `.opencode/command/create/{skill,agent,folder_readme,install_guide,skill_asset,skill_reference}.md`

**Spec Kit YAML (10):** `.opencode/command/spec_kit/assets/spec_kit_{plan,implement,complete,debug,research}_{auto,confirm}.yaml`

**Spec Kit MD (5):** `.opencode/command/spec_kit/{plan,implement,complete,debug,research}.md`

## Upgrade

No action required. Pull latest to get agent routing compliance across all commands.
