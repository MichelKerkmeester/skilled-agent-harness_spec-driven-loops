## [**2.6.1.0**] - 2026-03-27

Integrated CocoIndex search by meaning instead of exact words across spec_kit and create commands, remediated all P1+P2 findings from the 012 command alignment deep review, and folded in the command README rewrite wave from spec 033 plus its same-day punctuation cleanup. The main changes here are semantic search in commands and code search routing table.

### Semantic Search in Commands

- Need: People needed a better way to find the right information without guessing exact words.
- What changed: Integrated mcp__cocoindex_code__search into spec_kit planning (step 5 explorer agents) and create commands for concept-based code discovery.
- Why it matters: This helps people find the right place faster, even when they do not know the exact term to search for.

### Code Search Routing Table

- Need: People needed clearer support for this new part of the workflow.
- What changed: Added CocoIndex as first-choice tool for concept/intent queries in the planning phase parallel dispatch configuration.
- Why it matters: This makes the workflow easier to discover and use.

### P1+P2 Remediation

- Need: People needed clearer support for this new part of the workflow.
- What changed: Fixed all findings from command alignment deep review including stale command boundaries, ownership clarity, and documentation accuracy.
- Why it matters: This helps keep different copies of the same guidance from drifting apart.

### Memory Command Surface

- Need: People needed clearer support for this new part of the workflow.
- What changed: Updated memory command documentation to 33 memory tools, 6 commands with explicit ownership.
- Why it matters: This makes the workflow easier to discover and use.

### Readme Rewrite Wave

- Need: The writing in this area needed to be easier to read and follow.
- What changed: Rewrote the root, create, memory, and spec_kit command READMEs from text files to Markdown files using the README creation workflow, the README template, and HVR v0.210 rules.
- Why it matters: This makes the documentation easier for non-developers to read.

### Follow-up Separator Cleanup

- Need: The writing in this area needed to be easier to read and follow.
- What changed: Replaced lingering separators in command-related READMEs so related-documents sections and file-tree comments match the updated documentation voice.
- Why it matters: This makes the documentation easier for non-developers to read.


---

## Files Changed

| File | Action | Notes |
| ---- | ------ | ----- |
| `.opencode/command/spec_kit/plan.md` | Modified | CocoIndex search routing |
| `.opencode/command/spec_kit/assets/spec_kit_plan_auto.yaml` | Modified | Explorer agent prompts |
| `.opencode/command/spec_kit/assets/spec_kit_plan_confirm.yaml` | Modified | Explorer agent prompts |
| `.opencode/command/create/changelog.md` | Modified | CocoIndex integration |
| `.opencode/command/memory/*.md` | Modified | Alignment fixes |
| `.opencode/command/README.md` | Created | Root command README rewrite from text files to Markdown files |
| `.opencode/command/create/README.md` | Created | create command README rewrite from text files to Markdown files |
| `.opencode/command/memory/README.md` | Created | memory command README rewrite and separator cleanup |
| `.opencode/command/spec_kit/README.md` | Created | spec_kit command README rewrite and separator cleanup |


---

## Upgrade

- No migration required. Pull latest if you want the CocoIndex-aware command surface and the rewritten Markdown READMEs together.
