# v2.0.4.1

> Part of [OpenCode Dev Environment](https://github.com/MichelKerkmeester/opencode-spec-kit-framework)

---

## [**2.0.4.1**] - 2026-02-18

**Memory save retry example alignment** -- Updated `/memory:save` documentation so the full-folder `memory_index_scan` retry example explicitly sets `includeSkillRefs: true`, aligning command docs with the skill-reference indexing capability introduced in Source #6.

---

## Highlights

### Category: Documentation

- **Explicit skill-ref inclusion**: Full-folder retry now shows `spec_kit_memory_memory_index_scan({ specFolder: "011-memory", force: true, includeSkillRefs: true })`.
- **Cross-command consistency**: `/memory:save` example now mirrors `/memory:manage scan` source-scope behavior for workflow skill `references/` and `assets/` content.

## Files Changed

- `.opencode/command/memory/save.md`

## Upgrade

No action required. Pull latest to get the corrected command example.
