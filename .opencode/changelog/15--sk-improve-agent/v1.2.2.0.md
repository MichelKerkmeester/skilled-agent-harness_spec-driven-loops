# v1.2.2.0

> Part of [OpenCode Dev Environment](https://github.com/MichelKerkmeester/opencode--spec-kit-skilled-agent-orchestration)

---

## [**1.2.2.0**] - 2026-04-12

This patch release focuses on reducer schema mismatches, dead helper code, journal taxonomy gaps, and workflow enforcement. The goal is to make the workflow easier to trust in everyday use.

> Spec folder: `.opencode/specs/skilled-agent-orchestration/042-sk-deep-research-review-improvement-2/` (Level 3)
> Review session: `rvw-2026-04-12T11-30-00Z`

---

## Bug Fixes

These fixes correct behavior that could confuse operators or produce the wrong result. The goal is to make the workflow more predictable in normal use.

### Lineage schema aligned between producer and reducer

**Problem:** `candidate-run history.cjs` wrote `candidateId`/`parentCandidateId` but the reducer expected `id`/`parentId`. The reducer silently failed to read run history files.

**Fix:** Schema aligned on one main shape so the reducer can consume run history artifacts.

---

### Mutation-coverage metrics now derivable by reducer

**Problem:** The reducer expected a `metrics` block that `mutation-coverage.cjs` never wrote.

**Fix:** The reducer now derives coverage ratio and uncovered counts from the raw arrays when the computed metrics block is missing.

---

### Dashboard now surfaces journal, lineage, and coverage

**Problem:** The reducer read replay artifacts into the registry but dropped them from the dashboard. Operators had no visibility.

**Fix:** Added dedicated dashboard sections for journal summary, candidate run history, and mutation coverage.

---

### Legal-stop events now emitted from improve-agent workflows

**Problem:** The improve-agent loop never emitted `legal_stop_evaluated` or `blocked_stop` events that the other reducers depend on.

**Fix:** Both auto and confirm workflow YAMLs now emit structured legal-stop events with gate bundles.

---

### Trade-off detector trajectory extraction fixed

**Problem:** Journal-based trajectory extraction was incompatible with emitted `candidate_scored` payloads - the scored dimension array was missing from journal rows.

**Fix:** `trade-off-detector.cjs` now emits the scored dimension array into the journal row.

---

### Plateau stop reason added to taxonomy

**Problem:** The stop-reason taxonomy could not represent plateau exits. Plateau stops were falsified as `converged`.

**Fix:** Added `plateau` to the `STOP_REASONS` enum in `improvement-journal.cjs`.

---

### Phase 005 helpers now called in live workflows

**Problem:** `mutation-coverage.cjs`, `candidate-run history.cjs`, `trade-off-detector.cjs`, and `benchmark-stability.cjs` were never called from the auto/confirm workflow steps - they were dead code.

**Fix:** Added explicit loop steps calling all 4 helpers at appropriate workflow points.

---

### Integration scan now targets actual agent, not profile label

**Problem:** `scan-integration.cjs` received the profile label instead of the real target agent identifier, so it scanned the wrong file.

**Fix:** Both workflow YAMLs now pass the real target agent identifier.

---

### Session-boundary gate enforced in confirm mode

**Problem:** The confirm-mode session gate was advisory only - no `verify`/`on_fail` enforcement.

**Fix:** Confirm mode now mirrors the auto workflow's structured halt pattern.

---

## Documentation

The supporting guidance was updated so the written instructions match the shipped behavior. That makes the workflow easier to follow without reading the code first.

### improvement_config.json stripped of phantom resume

Dormant resume fields removed. Config aligned with the `new`-only current-release expected behavior.

### Command doc resume flow removed

`command/improve/agent.md` Step 6D (unsupported resume flow) removed.

### Replay-consumer playbook corrected

`034-replay-consumer.md` now describes journal, run history, and coverage as registry-only outputs (not dashboard sections that don't exist yet - they now do after this release).

---

## Verification

| Check | Result |
|-------|--------|
| 30-iteration GPT-5.4 deep review | PASS - 0 P0 remaining |
| Post-fix validation (10 iterations) | PASS - all findings resolved |
