# /create:benchmark Presentation Contract

This file is the single source of truth for user-facing presentation in `/create:benchmark`: startup questions, setup/status dashboards, and result display. The command router owns asset routing only. The workflow YAML owns execution behavior.

## 1. Phase 0 Verification Display

Run this automatic self-check before setup. This is not a user question.

```text
SELF-CHECK: Are you operating as @markdown?

Indicators:
- Invoked with @markdown prefix
- Template-first benchmark report and SOURCE.md generation behavior is available
- sk-doc quality validation behavior is available

If all indicators are present:
- create_benchmark_verified = true
- phase_0_status = PASSED

If any indicator is missing or uncertain:
- Stop before loading workflow YAML
- Display the hard-block message below
```

Hard-block message:

```text
This command requires @markdown for template-first benchmark authoring and sk-doc validation.
Restart with: @markdown /create:benchmark <skill-or-mode> <spec-packet> [create|update] --family=<FAMILIES-key>

STATUS=FAIL ERROR="Markdown agent required"
```

## 2. Auto Setup Resolution

For `:auto`, resolve setup using the system auto-mode contract, then load `create_benchmark_auto.yaml` only when every required field is available.

Pre-bound setup schema:

```yaml
PRE-BOUND SETUP ANSWERS:
  benchmark_family: mcp_promotion
  skill_name: system-spec-kit
  source_packet_path: .opencode/specs/example-benchmark-packet
  operation: create
  source_strategy: promote-benchmark
  benchmark_date: 2026-05-17
  skill_path: .opencode/skills/
  benchmarks_root: .opencode/skills/system-spec-kit/mcp_server/benchmarks
  benchmark_folder: .opencode/skills/system-spec-kit/mcp_server/benchmarks/benchmark-2026-05-17
  execution_mode: AUTONOMOUS
  memory_choice: skip
```

Default resolution table:

| Field | Required | Resolves Via | Default | Tier-2 Candidate |
| --- | --- | --- | --- | --- |
| `benchmark_family` | Yes | `--family=<FAMILIES-key>`, marker, or family choice | none | Yes |
| `skill_name` | Yes | First positional token or marker | none | No |
| `source_packet_path` | Yes | Second positional token or marker | none | No |
| `operation` | Yes | positional token, marker, or target existence | none | Yes |
| `source_strategy` | MCP only | marker or targeted promotion type choice | `promote-benchmark` | Yes |
| `benchmark_date` | MCP only | `--date`, marker, or benchmark evidence date | none | Yes |
| `benchmark_id` | Conformance only | `--benchmark-id`, marker, or branch question | none | Yes |
| `skill_path` | Yes | `--path`, marker, or default | `.opencode/skills/` | No |
| `benchmarks_root` | MCP only | marker or derived from skill path/name | derived | No |
| `benchmark_folder` | MCP only | marker or derived from root/date | derived | No |
| `target_mode_path` | Conformance only | derived from skill path/name | derived | No |
| `conformance_package_root` | Conformance only | derived as `<target_mode_path>/assets/conformance_benchmark` | derived | No |
| `benchmark_package` | Conformance only | derived from package root and benchmark ID | derived | No |
| `execution_mode` | Yes | `:auto`, `:confirm`, or marker | `AUTONOMOUS` under `:auto` | No |
| `memory_choice` | No | marker, prior-work detection, or default | `skip` when no prior continuity exists | No |

Command-bound families are `mcp_promotion` and `conformance_benchmark`. Other
keys remain valid create-benchmark skill routes but stop at this command boundary
until their authoring branches are registered.

## 3. Consolidated Startup Prompt

Ask one prompt containing only applicable questions. Do not split these questions across turns.

```text
Before proceeding, please answer:

Q0. Benchmark Family (if missing)
A) mcp_promotion
B) conformance_benchmark
C) behavior_benchmark
D) skill_benchmark
E) model_benchmark
F) agent_improvement

Only A and B are command-bound. Selecting C-F stops with a branch-not-registered
message and routes the user to the live create-benchmark skill contract.

Q1. Skill or Owning Mode Name (if missing)
Required format: hyphen-case folder name. MCP promotion requires `mcp_server/`;
conformance authoring requires a mode that owns `assets/`.

Q2. Source Spec Packet (if missing)
Provide the .opencode/specs/... packet path that owns the benchmark decision and evidence.

Q3. Operation (if missing)
A) create
B) update

Q4. Promotion Type (MCP promotion only)
A) promote-benchmark: create a skill-local folder from accepted benchmark evidence
B) rerun-update: update an existing folder after a confirming re-run
C) retirement-update: mark an existing benchmark retired while preserving the folder

Q5. Benchmark Execution Date (MCP promotion only, if missing)
Required format: YYYY-MM-DD. Use the date the benchmark ran, not today's date.

Q6. Benchmark ID (conformance only, if missing)
Required format: stable lowercase-hyphen ID describing the corpus, not a date or run.

Q7. Prior Work Context (when the existing spec has prior continuity)
A) Load handover plus implementation-summary continuity
B) Load canonical spec docs up to a recent cap
C) Skip

Q8. Execution Mode (if no :auto/:confirm suffix)
A) Interactive (:confirm)
B) Autonomous (:auto)
```

Hard stops:

- Do not infer missing operation from context.
- Do not infer a missing benchmark family.
- Do not split setup across multiple prompts.
- Do not continue with missing required fields.
- Do not enter a workflow for a FAMILIES key without a registered command branch.

## 4. Setup Dashboard

| Field | Required | Rule | Value |
| --- | --- | --- | --- |
| `create_benchmark_verified` | Yes | `true` | `[value]` |
| `benchmark_family` | Yes | exact create-benchmark `FAMILIES` key | `[value]` |
| `skill_name` | Yes | explicit, hyphen-case | `[value]` |
| `source_packet_path` | Yes | existing spec packet path | `[value]` |
| `operation` | Yes | `create` or `update` | `[value]` |
| `source_strategy` | MCP only | one normalized choice | `[value or N/A]` |
| `benchmark_date` | MCP only | `YYYY-MM-DD` execution date | `[value or N/A]` |
| `benchmark_id` | Conformance only | lowercase-hyphen | `[value or N/A]` |
| `skill_path` | Yes | default if omitted | `[value]` |
| `benchmarks_root` | MCP only | derived from skill name and path | `[value or N/A]` |
| `benchmark_folder` | MCP only | derived from root and benchmark date | `[value or N/A]` |
| `target_mode_path` | Conformance only | derived from skill name and path | `[value or N/A]` |
| `benchmark_package` | Conformance only | `<target_mode_path>/assets/conformance_benchmark/<benchmark_id>` | `[value or N/A]` |
| `execution_mode` | Yes | `:auto` or `:confirm` | `[value]` |

Proceed only when every required field validates.

## 5. Completion Result Template

```text
Benchmark Command Complete

Skill: [skill_name]
Family: [benchmark_family]
Operation: [operation]
Source strategy: [source_strategy or N/A]
Mode: [:auto|:confirm]
Target: [benchmark_folder or benchmark_package]

Validation:
- Markdown validation: [PASSED|FAILED]
- JSON/artifact validation: [PASSED|FAILED|N/A]
- Index and path consistency checks: [PASSED|FAILED]
- Authoring-only stop enforced: [YES|N/A]

Context:
- Source packet: [source_packet_path]
- Memory loaded: [yes/no]

STATUS=[OK|CANCELLED|FAIL] PATH=[benchmark_folder or benchmark_package]
```
