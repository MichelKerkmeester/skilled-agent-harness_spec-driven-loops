# Memory Learn Presentation Contract — DEPRECATED

DEPRECATED — the constitutional memory layer has been retired.

This asset previously defined the overview, create/qualify, approval, list, edit, remove, and
budget displays for `/memory:learn`. That command no longer manages a constitutional rule tier
(the always-surface, search-boosted tier was removed from the code), so there is no authoring UI
left to render here.

No active displays. See `.opencode/commands/memory/learn.md` for the deprecation notice and the
replacement commands (`/memory:save`, `/memory:search`, `/memory:manage`, `/speckit:resume`).
