# Compiled Command Contract: /deep:alignment

**NOT machine-generated.** Unlike `deep_review.contract.md` / `deep_research.contract.md` / `deep_ai-council.contract.md`, this file was not produced by `.opencode/skills/system-deep-loop/runtime/scripts/compile-command-contracts.cjs` — `deep/alignment` is not yet registered in that script's `COMMANDS` map (it has no `presentationPath`, `sourcePaths`, `renderMarkers`, `setup`, `outputTemplate`, `writeBoundary`, or `tools` definition there).

`.opencode/skills/system-deep-loop/shared/rollout/command-injection-rollout.json` has no `deep/alignment` entry, so `resolve-injection-mode.cjs`'s `DEFAULT_MODE` of `"fallback"` applies (the same state `deep/ai-council` currently runs in). In `fallback` mode, `render-command-contract.cjs`'s `renderPayload()` returns the legacy body (`.opencode/commands/deep/assets/legacy/deep_alignment.body.md`) directly and never reads or asserts freshness on this file's content — `assertCompiledContractFresh()` only runs in `"fix"` mode. This file exists solely so `buildManifestRow()`'s unconditional `compiledContractSha256` hash (computed regardless of mode) has a real file to read; it carries no instructional content and the command orchestrator must never treat it as authoritative.

A future phase MAY register `deep/alignment` in `compile-command-contracts.cjs`, author a `deep_alignment_presentation.txt`, and run `node compile-command-contracts.cjs --command deep/alignment --write` to replace this placeholder with a real generated contract and move the rollout mode to `"fix"`, mirroring `/deep:review`'s and `/deep:research`'s current state. That upgrade is out of scope for this phase.
