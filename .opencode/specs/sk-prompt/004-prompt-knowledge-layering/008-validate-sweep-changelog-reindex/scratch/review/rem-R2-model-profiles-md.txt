I’ll verify the documented schema against the actual registry, then make a surgical markdown-only edit in the approved file and run a focused verification.
The memory trigger call rejected the ad hoc session id, so I’m retrying without session scoping as the tool instructed.
SKILL ROUTING: system-spec-kit is active for the pre-approved spec-folder workflow; customize-opencode also applies because the scoped file is under `.opencode/skills/`.
I’m loading the documentation skill because this is a markdown reference update, then I’ll read only the target markdown and the JSON registry used for verification.
I found the exact drift: the markdown still documents `provider` and `quota_pool` as model-level fields, while the JSON uses `executors[].provider`, `executors[].quota_pool`, and `primary_quota_pool`. I’m patching only the requested markdown file to align those sections and normalize framework ids.
The markdown patch is applied. I’m checking the edited sections and validating the registry facts from JSON before reporting completion.
Verification caught one remaining uppercase `RCAF` in the schema prose and a few quota wording remnants. I’m correcting those in the same markdown file, then I’ll rerun the checks with a corrected JSON query.
The JSON verification passed: 10 entries, 8 active, 2 optional, no model-level `provider` or `quota_pool`, and executor-level provider/quota fields present. I’m rerunning the markdown greps with a directory-scoped search because the first grep invocation used a file path with an include filter.
The readback shows the edited prose is aligned. I’m doing final validation now: exact text checks, spec-folder validation, and a scoped diff to confirm only the approved markdown changed.
The scoped diff includes pre-existing uncommitted edits in the same file from before my patch, so I’m not treating that whole diff as mine. I’m checking the worktree and locating the approved spec folder path for validation.
The approved spec folder exists as an untracked packet directory in `git status`, even though the glob search did not surface it. I’m reading that directory directly and then running its strict validator if the expected docs are present.
Strict spec validation passed for the phase parent and all child phases. Final checks are complete; I’m updating the task list and then I’ll report the exact file-level edits.
Updated only `.opencode/skills/sk-prompt/references/model-profiles.md`.

**Edits Made**
- Rewrote stale schema sections:
  - `provider` → `executors[].provider`
  - `quota_pool` → `executors[].quota_pool` and `primary_quota_pool`
  - Clarified there is no model-level `provider` or `quota_pool`.
- Updated quota/protocol wording for optional stubs and new-model updates to use executor `quota_pool` plus model `primary_quota_pool`.
- Normalized framework ids in prose/tables to lowercase: `rcaf`, `costar`, `race`, `cidi`, `tidd-ec`.
- Adjusted active-model wording to state the registry has 8 active models plus 2 optional unverified stubs, and that stubs are not active routing targets.
- Aligned provider examples with actual JSON executor providers.

**Verification**
- Checked JSON registry: 10 total models, 8 active, 2 optional; no model-level `provider` or `quota_pool`; all models have `primary_quota_pool`; executor provider/quota fields present.
- Checked markdown for stale uppercase framework ids and old top-level schema headings: no matches.
- Ran strict spec validation for `.opencode/specs/skilled-agent-orchestration/z_archive/004-prompt-knowledge-layering`: passed with 0 errors and 0 warnings.
