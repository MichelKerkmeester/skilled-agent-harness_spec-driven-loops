DEEP-RESEARCH
Resolved route: mode=research; target_agent=@deep-research; execution=single_iteration; state_source=externalized_files; do_not_switch_mode=true

# Deep-Research Iteration 7

Read lineage state, strategy, registry, and relevant prior iterations first. Execute one LEAF iteration.

## Focus
Compare Hallmark's twenty-theme catalog, four genre guides, optional per-theme specs, theme-selection logic, custom-theme fallback, and theme-aware behavior against sk-design's extracted styles corpus (reported around 1,290 bundles), styles DB/engine retrieval and rights gates, relational-exemplar authority, register contract, design-interface corpus adapter, and foundations token model. Determine whether a curated-theme model is a useful complement to the extracted corpus. Be precise about corpus cardinality from actual files/metadata, selection semantics, explainability, rights/provenance, anti-preset boundaries, and how curated archetypes could serve as labels or evaluation lenses without becoming copyable presets. Produce candidate rows and a concrete coexistence recommendation with exact target files, value, effort, and licensing treatment.

## Writes
Only `iterations/iteration-007.md`, one append-safe iteration-7 state record, and `deltas/iter-007.jsonl` under the detached lineage. No other writes or sub-agents. Use separate file creation plus byte-preserving state append. Max 12 calls.

Include corpus/theme comparison matrix, Candidate Matrix, Ruled Out, Dead Ends, exact citations, and next focus. Route proof exactly: `target_agent:"deep-research"`, `resolved_route:"Resolved route: mode=research target_agent=deep-research"`. Delta first line equals state. Verify unique run 7.

Recovery note for the sole re-dispatch: a valid report was assembled on the first attempt, but a combined multi-file `apply_patch` failed atomically when its state-tail anchor differed. Do not call `apply_patch` for any output. Write the new narrative and delta files directly as separate write-once files, then append one state JSON line with a byte-preserving append command that does not inspect or match the prior line. No iteration-007 artifact or run-7 state record exists from the failed attempt.
