DEEP-RESEARCH
Resolved route: mode=research; target_agent=@deep-research; execution=single_iteration; state_source=externalized_files; do_not_switch_mode=true

# Deep-Research Iteration 6

Read state and prior iterations first; run one LEAF iteration, no sub-agents.

## Focus
Compare Hallmark `study` behavior and `design-md.md`, `study.md`, `export-formats.md`, and relevant README examples against shipped sk-design MD-GENERATOR, `/interface:design-reference`, its v3 `DESIGN.md` schema, extract-write-validate pipeline, report/provenance fields, and styles DB ingestion. Produce a field-level schema comparison: which Hallmark design-DNA fields already exist, which useful fields are absent, which are weaker than measured extraction, and which should be skipped. Evaluate screenshot/URL diagnosis versus measured browser/CSS extraction, portability of `design.md` versus `DESIGN.md`, output naming/case, confidence/provenance, content/motion/shape/texture/anti-pattern fields, and export formats. Name exact files and HOW they would change. Preserve the shipped pipeline as canonical; prefer schema or report extensions over replacement.

## Writes
Only `iterations/iteration-006.md`, one canonical append-safe iteration-6 state record, and `deltas/iter-006.jsonl` under `.opencode/specs/sk-design/014-hallmark-design-skill-research/001-research/research/lineages/sol-opencode/`. Create files separately and append state; no context-dependent multi-file patch. No other writes. Max 12 calls.

Include a field-level matrix, candidate rows, Ruled Out, Dead Ends, citations, licensing distinctions, and next focus. Canonical route proof must use exactly `target_agent:"deep-research"` and `resolved_route:"Resolved route: mode=research target_agent=deep-research"` (no `@` and no semicolon form), with iteration/run 6 and all standard fields. Delta first line exactly matches state. Verify mechanically before returning.
