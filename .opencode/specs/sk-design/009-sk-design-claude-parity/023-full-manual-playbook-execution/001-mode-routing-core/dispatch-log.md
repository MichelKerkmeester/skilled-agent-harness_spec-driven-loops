# Dispatch Log: Wave 001 - Mode Routing Core

One row per dispatch executed by this wave. Each dispatch = advisor probe (`skill_advisor.py --threshold 0.8`) + real `opencode run --model openai/gpt-5.5-fast --variant medium --format json` dispatch, graded against the constituent scenario file's own `### Pass/Fail Criteria` section.

| dispatch_id | scenario_id | exact prompt used | advisor top-1 / confidence | resolved mode / packet / resources | verdict | rationale (cites criterion) |
|---|---|---|---|---|---|---|
| MR-001 | MR-001: Interface Mode Routing | "Make this SaaS pricing page look less generic and give it a distinctive visual direction." | `sk-design` / 0.95 | mode `interface`; packet `design-interface/SKILL.md` (loaded via `skill` tool, after `sk-design` hub call); resources: `design_principles.md`, `brief_to_dials.md`, `interface_preflight_card.md`, `register.md`, `context_loading_contract.md` all loaded/cited | **PASS** | Satisfies "PASS iff advisor top-1 is `sk-design`, resolved mode is `interface`, packet is `design-interface/SKILL.md`, required shared resources are cited, and no mutating tool is used" — all 4 conjuncts confirmed; zero `Write`/`Edit`/`Bash` calls. |
| MR-002 | MR-002: Foundations Mode Routing | "Create an OKLCH color token system, typography scale, spacing rhythm, and responsive grid for this dashboard." | `sk-design` / 0.9441 (native scorer; `sk-code` second at 0.8444) | mode `foundations` (explicit "Selected mode: `foundations`"); packet `design-foundations/SKILL.md` loaded via `skill` tool; resource paths `corpus_map.md`, `oklch_workflow.md`, `palette_theming.md`, `typography_system.md`, `layout_responsive.md`, `token_starter.md`, `design_token_vocabulary.md`, `context_loading_contract.md` all confirmed present in the returned `<skill_content>` blob | **PASS** | Satisfies "PASS iff advisor top-1 is `sk-design`, resolved mode is `foundations`, packet is `design-foundations/SKILL.md`, and the loaded resources cover color, type, layout, and token paths" — response body itself delivers OKLCH color tokens (light + dark), a type scale, an 8px spacing rhythm, and a 12-col responsive grid with breakpoints. |
| MR-003 | MR-003: Motion Mode Routing | "Design the hover micro-interactions and reduced-motion fallback for this command menu." | `sk-design` / 0.9404 (native scorer; `sk-code` second at 0.8593) | mode `motion` (explicit "Selected mode: `motion`"); packet `design-motion/SKILL.md` loaded via direct `read`; resources `animation_decision_framework.md`, `micro_interactions.md`, `performance_reduced_motion.md`, `motion_pattern_cards.md`, `shared/cognitive_laws.md`, `shared/anti_slop_principles.md`, `shared/register.md` all loaded | **PASS** | Satisfies "PASS iff advisor top-1 is `sk-design`, resolved mode is `motion`, packet is `design-motion/SKILL.md`, and motion decision plus reduced-motion resources are loaded" — response gives an explicit hover-treatment CSS block plus a `@media (prefers-reduced-motion: reduce)` fallback block and a handoff card; no `interface`/`audit` mode confusion, no mutating tool. |
| MR-004 | MR-004: Audit Mode Routing | "Audit this checkout UI for WCAG contrast, keyboard focus, responsive issues, and design slop." | `sk-code` / 0.872 (top-1); `sk-design` / 0.8486 (second, still above 0.80 threshold); `sk-git` / 0.82 (third) — reproduced identically on a second run | mode `audit` (procedure `procedures/accessibility_audit.md` applied); packet `design-audit/SKILL.md` loaded directly via `skill` tool (model skipped its own internal advisor call and the `sk-design` parent-hub `skill` call entirely); resources `audit_contract.md`, `accessibility_performance.md`, `anti_patterns_production.md`, `audit_report_template.md`, `corpus_map.md`, `register.md`, `context_loading_contract.md`, `sk_code_handoff.md` all confirmed present in the returned `<skill_content>` blob | **PARTIAL** | The scenario's PASS is a strict AND: "PASS iff advisor top-1 is `sk-design`, resolved mode is `audit`, packet is `design-audit/SKILL.md`, and the response is findings-first with accessibility/performance evidence boundaries." 3 of 4 conjuncts hold (mode, packet, findings-first response with explicit P1/P2/P3 severities and an "illustrative... not confirmed defects" evidence-boundary statement) but the advisor-top-1 conjunct fails (`sk-code` won, not `sk-design`). The scenario's own FAIL triggers ("`interface` starts redesigning, `foundations` starts token authoring, or mutating tools are used") also did not occur, so this is neither a clean PASS nor a defined FAIL — graded PARTIAL and the advisor-tier discrepancy flagged as a genuine finding (see `implementation-summary.md` Known Limitations #1). |
| MR-006 | MR-006: Mode Hint Override to Motion | "motion: make the menu transition feel bolder and more deliberate." | `sk-design` / 0.82 (native scorer, lexical-lane match; only skill above threshold) | mode `motion` (explicit "The motion route is active"); packet `design-motion/SKILL.md` loaded via `skill` tool (model skipped the `sk-design` parent-hub call, went straight to `design-motion`); resources `register.md`, `animation_decision_framework.md`, `motion_strategy.md`, `micro_interactions.md`, `performance_reduced_motion.md`, `procedures/interaction_states_pass.md` all loaded via direct `read` | **PASS** | Satisfies "PASS iff advisor top-1 is `sk-design`, resolved mode is `motion`, packet is `design-motion/SKILL.md`, and the response handles temporal design rather than static visual direction" — response is choreography/timing/easing/state-matrix, not a static palette or layout; the `motion:` hint correctly overrode `bolder`'s interface-alias pull, matching the scenario's FAIL-avoidance note ("FAIL iff `interface` wins solely because of `bolder`, or the AI ignores the `motion:` hint") — neither occurred. |

## Summary

- **PASS**: MR-001, MR-002, MR-003, MR-006 (4/5)
- **PARTIAL**: MR-004 (1/5) — advisor-tier discrepancy (`sk-code` top-1 over `sk-design`), all other conjuncts correct
- **FAIL**: none
- **SKIP**: none
