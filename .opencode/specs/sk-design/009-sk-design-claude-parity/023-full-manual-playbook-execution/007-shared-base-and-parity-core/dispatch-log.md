# Dispatch Log: Wave 007 - Shared Base & Parity Core

One row per real `opencode run` dispatch executed in this wave. All dispatches used `--model openai/gpt-5.5-fast --variant medium --format json --dir /Users/michelkerkmeester/MEGA/Development/Code_Environment/Public`, ran strictly one at a time, and captured full JSON-lines stdout under `/tmp/skd-<dispatch-id>-response.jsonl`.

| dispatch_id | scenario_id | exact prompt used | advisor top-1 / confidence | resolved mode / packet / resources | verdict | rationale (citing scenario criterion) |
|---|---|---|---|---|---|---|
| SR004 | SR-004 | "For a design audit, show which packet owns the scoring logic and which hub file only routes." | sk-design / 0.904 (native, clean top-1) | `sk-design` → `audit`; loaded `mode-registry.json`, `design-audit/SKILL.md`, `design-audit/references/audit_contract.md`, `sk-design/SKILL.md` | **PASS** | Meets "the AI says the hub routes through `mode-registry.json`, the scoring logic lives in `design-audit/SKILL.md` and its references, and no per-mode audit logic is attributed to the hub" — response explicitly cited `design-audit/SKILL.md:44` and `audit_contract.md:47-58` as scoring owner, and `sk-design/SKILL.md:15/41` + `mode-registry.json:103-121` as routing-only. No mutating tool used. |
| PB001 | PB-001 | "Make this fintech dashboard feel premium and less generic. Before giving direction, state the public sk-design mode, the internal procedure card you selected, and why that card fits the available context." | sk-design / 0.878 (native, clean top-1) | `sk-design` → `interface`; loaded `design_principles.md`, `shared/register.md`, `brief_to_dials.md`, `shared/context_loading_contract.md`, `interface_preflight_card.md`, `procedures/aesthetic_direction.md` | **PASS** | Meets "advisor top-1 is `sk-design`, resolved mode is `interface`, the response names `design-interface/procedures/aesthetic_direction.md`... rationale ties the card to missing stronger brand/reference context and non-generic direction... no mutating tool" — response explicitly named the card and reasoned "no target file, screenshot, brand system, or existing UI to preserve... greenfield visual-direction request." Only `skill`/`read` tools used. |
| PB002 | PB-002 | "Review the supplied dashboard screenshot description for hierarchy and spacing rhythm. Before recommendations, list the context you used, what is confirmed, what is inferred, and what proof would be required before calling the design ready." | sk-design / 0.89 — **NOT top-1** (native advisor unavailable at probe time; local fallback scorer tied `sk-code`/`system-spec-kit` at 0.95, sk-design 3rd) | `sk-design` → bundled `audit` (primary) + `foundations` (supporting); loaded `design-audit`, `design-foundations`, applied `design-foundations/procedures/hierarchy_rhythm_review.md` | **PARTIAL** | Criterion is "advisor top-1 is `sk-design`, resolved mode is `foundations`, the response names `design-foundations/procedures/hierarchy_rhythm_review.md`, it separates confirmed evidence from inferred claims, it names proof still required before readiness, and no mutating tool is used." Card name, confirmed/inferred separation, and proof-required section were all present and correct; but the standalone advisor probe did not return sk-design top-1, and the live dispatch resolved a bundled `audit`+`foundations` mode rather than pure `foundations`. No FAIL condition (no ready-verdict-without-proof, no omitted fields, no fabrication, no mutating tool) was triggered. |
| PB004 | PB-004 | "motion: define hover, focus, active, loading, disabled, and reduced-motion behavior for this command menu. Before giving timing guidance, state the public sk-design mode, the internal procedure card you selected, and why that card fits." | sk-design / 0.867 (native, clean top-1) | `sk-design` → `motion`; loaded `procedures/interaction_states_pass.md`, `animation_decision_framework.md`, `micro_interactions.md`, `performance_reduced_motion.md`, `shared/register.md` | **PASS** | Meets "advisor top-1 is `sk-design`, resolved mode is `motion`, the response names `design-motion/procedures/interaction_states_pass.md`, the rationale ties it to interaction states and reduced-motion behavior, the context basis appears before timing guidance, and no mutating tool is used" — response's Route/Restraint Gate/State Behavior sections precede its "Timing Guidance" section. Only `skill`/`read`/memory-trigger tools used. |
| PB005 | PB-005 (primary) | "audit: review this checkout screen for WCAG contrast, keyboard focus, and form accessibility. State the public sk-design mode, the internal procedure card you selected, and why it is not the AI-slop card." | sk-design / 0.95 — tied #1 with sk-code (native advisor unavailable at probe time; local fallback scorer). Live orchestrator's internal advisor call surfaced sk-design at 0.87 and disambiguated correctly. | `sk-design` → `audit`; selected `procedures/accessibility_audit.md` | contributes to **PASS** (combined w/ negative control below) | Selected `accessibility_audit.md` and explicitly stated "Why not the AI-slop card: this request targets WCAG contrast, keyboard focus, and form accessibility... `procedures/ai_slop_check.md` would apply if the review were about generic visual language, model tells, over-decoration, or AI-template risk." No mutating tool used. |
| PB005neg | PB-005 (negative control) | "audit: review this hero section for generic AI-template visual language and model-tell decoration." | sk-design / 0.914 (native, clean top-1) | `sk-design` → `audit`; loaded `anti_patterns_production.md`, `ai_fingerprint_tells.md`, `anti_patterns_score_rubric.md`, `procedures/ai_slop_check.md` | contributes to **PASS** (combined w/ primary above) | Selected `procedures/ai_slop_check.md` with explicit AI-template-tell findings (decoration-over-premise, generic hero grammar, model-tell decoration, posture-heavy copy, over-smoothed texture) and an anti-patterns score estimate. No mutating tool used. Combined with the primary row, satisfies PB-005's full criterion: "accessibility/WCAG prompt selects `accessibility_audit.md`, the negative-control prompt selects `ai_slop_check.md`, both responses cite why the selected card fits, and no mutating tool is used." |

## Summary

| Scenario | Verdict |
|---|---|
| SR-004 | PASS |
| PB-001 | PASS |
| PB-002 | PARTIAL |
| PB-004 | PASS |
| PB-005 | PASS |

4 PASS, 1 PARTIAL, 0 FAIL, 0 SKIP.
