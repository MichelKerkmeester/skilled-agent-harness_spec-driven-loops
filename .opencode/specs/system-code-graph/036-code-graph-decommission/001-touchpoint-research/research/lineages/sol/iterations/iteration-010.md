# Iteration 010 — Adversarial Residual Sweep

## Focus

Search alternative identities, indirect contract language, stale counts, environment variables, and look-alike graph subsystems after the primary inventory appeared complete.

## Method

- Used `rg --hidden --no-ignore` over package names, namespace prefixes, socket names, boundary modules, plugin names, environment prefixes, “graph-first,” fallback fields, server totals, and structural-search prose.
- Excluded Git internals, worktrees, dependencies, the active lineage, `.opencode/specs/**`, changelogs, and benchmark reports from the actionable set.
- Read ambiguous results to reject substring collisions such as “photograph-first.”

## Findings

1. Additional command grants exist beyond the first allowlist set: `.opencode/commands/speckit/complete.md`, `speckit/implement.md`, `create/changelog.md`, `create/agent.md`, and `create/skill.md`. Their `mcp__mk_code_index__code_graph_query` grants must be removed.
2. Skill Advisor owns cross-daemon operational assumptions: `README.md:164-171`, `graph-metadata.json`, `verify-zombie-soak.sh`, `tri-daemon-drill.vitest.ts`, `launcher-lease.vitest.ts`, `skill-advisor-cli-test-utils.ts`, and scorer fixtures. Convert tri-daemon expectations to the surviving daemon set and preserve advisor-only lease coverage.
3. `sk-git` current docs and worktree playbooks promise `SPECKIT_CODE_GRAPH_DB_DIR` isolation (`SKILL.md:227` plus feature catalog/playbook). Update them with `worktree-session.sh`; these are live current docs, not archived specs.
4. A current Spec Kit constitutional file, `goal-prompting-runtime-specific.md:40`, lists `mk-code-graph` in committed-history guidance. Remove the retired example without changing the surrounding history rule.
5. The alternative-name sweep confirms current logs and Vitest cache contain runtime strings. `.opencode/logs/**` and `.opencode/node_modules/.vite/**` are ephemeral residue: clear through normal cache/log policy, do not treat them as source edits or archival records.
6. Database PID/owner/readiness markers are present inside the retiring skill’s database directory. They prove live-state cleanup must precede directory deletion and reinforce that ignored runtime state is not recoverable from Git.
7. `.gitattributes` contains Code Graph maintainer-filter guidance in addition to `.gitignore` database patterns. Reconcile both late, after operator state and rollback decisions.
8. The generic adversarial regex produced `sk-design` “photograph-first” false positives. These are unrelated and must not be edited. This validates exact-token plus semantic classification rather than bulk substitution.
9. No new independent registration authority appeared. The three physical registrations and their symlink aliases remain the complete startup set found by the sweeps.
10. No evidence overturned the subsystem boundary: Skill Advisor’s skill graph, Spec Memory causal/knowledge graphs, and deep-loop coverage/council graphs survive. Residual validation must whitelist those by ownership and exact identity, never by a blanket `graph` exclusion.

## Exhaustiveness Conclusion

Every requested class now has at least one direct sweep and one adversarial cross-check: registrations, imports, shell-outs, hooks, plugins, CI, documents, agent grants, command grants, generated mirrors, operator state, caches, worktrees, tests, build outputs, ordering, and rollback. Remaining new information is path-level refinement within already classified families.

## Telemetry

- Findings: 10
- New-information ratio: 0.36
- Convergence: above threshold; telemetry only
- Stop reason: configured maximum of 10 iterations reached
