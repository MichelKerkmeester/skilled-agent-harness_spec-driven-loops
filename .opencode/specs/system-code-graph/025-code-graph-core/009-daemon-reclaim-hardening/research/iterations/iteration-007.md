[0m
> build · glm-5.2
[0m
[0m→ [0mRead .opencode/bin/mk-code-index-launcher.cjs
[0m→ [0mRead .opencode/bin/lib/launcher-ipc-bridge.cjs
{"angle":"self-heal-on-acquire","insights":["The orphan escaped because `acquireOwnerLeaseFile` (L474-515) only sets `reclaimed` when a STALE lease EXISTS to unlink+reclaim; in this incident the lease file was already GONE, so `existing` is null, `reclaimed` is undefined, and the L1061-1067 guard never records `reclaimedOrphanPid` → the L1174-1180 reap block is skipped → the wedged daemon survives and `launchServer` opens a SECOND writer against the same DB+WAL.","`reapOwnerBeforeRespawn` (L640-666) already implements the correct kill ladder (SIGTERM → RESPAWN_REAP_GRACE_MS 7s → SIGKILL), ESRCH-safe, and refuses on `unknown-eperm`; it is reusable as-is — the gap is that no pid reaches it when the lease is missing.","The orphan pid can also diverge from the lease's `ownerPid` when `refreshOwnerLeaseFile` to the child pid FAILS (L974-981: 'launcher pid remains the recorded owner'); reaping `ownerPid` then SIGKILLs the launcher while the reparented daemon child (ppid→1) survives holding the WAL. So the owner lease is NOT a reliable orphan handle.","`acquireBootstrapLock` (L921) with `requireLock:true` is ALREADY the correct critical section — mkdir-CAS plus `removeStaleBootstrapLock` (L890) dead-owner reclaim via pid stamp. The self-heal must run INSIDE this lock exactly as `respawnAfterDeadSocket` does (L683); no new lock is needed and the CAS stays single-writer.","`wal_checkpoint(TRUNCATE)` CANNOT succeed while the orphan holds the WAL file lock, so it MUST run AFTER `waitForPidExit` confirms dead (L631-638) and BEFORE `launchServer` opens the child DB — otherwise the successor inherits the 17MB WAL or collides on the file lock.","The stale `daemon-ipc.sock` (SOCKET_FILE_NAME, bridge L13) must be unlinked before spawn or the new child hits EADDRINUSE; `maybeBridgeLeaseHolder` only checks existence (bridge L405) and never unlinks on the respawn path."],"recommendations":[{"change":"Add a pre-spawn self-heal gate `reapOrphanHolderIfAny({dbDir,socketPath})` invoked in `launcherMain` right after `acquireBootstrapLock({requireLock:true})` succeeds, REPLACING the narrow L1174-1180 block (which only fires on a reclaimed stale lease). Discover the orphan pid via three descending sources: (a) reclaimedOrphanPid when present, (b) a new crash-surviving daemon-pid registry (see next rec), (c) `lsof`/`fuser -t` on `code-graph.sqlite-wal`. Reap ONLY when `processLiveness(pid)==='alive'` AND the recorded socket is provably dead (reuse `probeLeaseHolderWithRetries` with the N-consecutive-failure gate from bridge L417, NOT a single miss). Hand the pid to the existing `reapOwnerBeforeRespawn` for the kill ladder.","where":"mk-code-index-launcher.cjs:launcherMain ~L1102-1189","why":"Closes the 'lease gone, orphan alive' hole: today the winner spawns a second writer because it has no pid to reap. The gate runs inside the held bootstrap lock so only the single CAS winner reaps; a losing racer's mkdir fails and it bridges/reports as today."},{"change":"Write a crash-surviving `.code-graph-daemon-pid.json` in `launchServer` immediately after `childProcess.pid` is known (after L973), recording {daemonPid, launcherPid, socketPath, startedAt, canonicalDbDir}. Do NOT clear it in `clearAllLeaseFiles`; unlink it ONLY in the child 'exit' handler (L985) on a clean exit. This file is the durable orphan handle that survives SIGKILL (no handlers run) and owner-lease loss.","where":"mk-code-index-launcher.cjs:launchServer L973-983 and child exit handler L985-1001","why":"The owner lease is unreliable after a failed child-pid refresh (L979) or once a secondary reclaims+clears it; a separate registry that survives unclean death is the only way the self-heal can recover a pid when both leases are absent — exactly this incident."},{"change":"After `reapOwnerBeforeRespawn` reports the orphan dead, run `wal_checkpoint(TRUNCATE)`: best-effort `spawnSync('sqlite3', [dbPath, 'PRAGMA wal_checkpoint(TRUNCATE);'])` from the launcher; if the `sqlite3` binary is absent, defer to a one-shot `PRAGMA wal_checkpoint(TRUNCATE)` executed in the daemon's boot path (it already owns better-sqlite3). Place AFTER `waitForPidExit` confirms dead and BEFORE `writeLeaseFile`/`launchServer`.","where":"mk-code-index-launcher.cjs:new gate between L1180 and L1182; plus daemon boot in mcp_server","why":"A 17MB orphaned WAL left in place slows the successor's open and risks WAL-recovery races; checkpointing after the lock is released (dead holder) and before the successor opens collapses it to the main db. Idempotent: TRUNCATE is a no-op once already checkpointed."},{"change":"Clean the stale socket artifact before spawn: `fs.rmSync(path.join(socketDir, SOCKET_FILE_NAME), {force:true})` (and any `.sock.lock`), then `fs.mkdirSync(socketDir, {recursive:true, mode:0o700})`. Use the lease-recorded socketPath when available, else `getIpcSocketPath('mk-code-index', {dbDir})` (bridge L85). Place AFTER checkpoint, BEFORE launchServer.","where":"mk-code-index-launcher.cjs:new gate, after checkpoint step","why":"A leftover .sock file makes the new child's bind fail EADDRINUSE; force:true unlink + recursive mkdir is idempotent and safe to repeat across retries."},{"change":"Enforce the strict ordering, all under the held bootstrap lock (requireLock:true): (1) discover orphan pid [registry→lsof], (2) verify pid alive AND socket dead via N-consecutive deep-probe failures, (3) SIGTERM→7s→SIGKILL via `reapOwnerBeforeRespawn`, (4) `waitForPidExit` re-confirms dead, (5) `wal_checkpoint(TRUNCATE)`, (6) unlink stale socket + guarded lease clears (`clearOwnerLeaseFileIfOwner` L552 / clearLeaseFile ownership re-check L766), (7) `writeLeaseFile`, (8) `launchServer`. Mirror this same sequence in `respawnAfterDeadSocket` (L668-717) so the secondary-driven respawn path also checkpoints + cleans the socket instead of only reaping.","where":"mk-code-index-launcher.cjs:launcherMain winner path + respawnAfterDeadSocket L668-717","why":"Reap-before-checkpoint is mandatory (WAL lock held by live process); checkpoint-before-spawn prevents the successor inheriting the WAL; cleaning the socket before spawn prevents EADDRINUSE. Keeping both respawn paths symmetric stops the secondary path from leaving the same WAL/socket residue."}],"risks":["`lsof`/`fuser` may be absent or path-different across macOS/Linux; never block on it — bound with a timeout and fall back to the daemon-pid registry. Treat lsof as a third-line signal, not the primary handle.","PID reuse: the OS may recycle the recorded pid for an unrelated process. Mitigate by recording `startedAt`/process start-time and cross-checking `ps -o lstart` (or /proc/$pid/stat boot-time field) before killing; refuse the kill on mismatch and let the new daemon's SQLite recover the WAL instead.","A busy daemon mid large scan/FTS-merge can transiently miss a deep probe; the socket-dead decision MUST reuse `resolveLeaseProbeAttempts` (bridge L52, default 2 consecutive failures) — a single miss must NOT trigger a reap.","Launcher-side `wal_checkpoint` opens a second sqlite connection; if the reap race left the orphan not fully dead, checkpoint collides. Guard: only checkpoint after `processLiveness(pid)==='dead'` is re-confirmed post-SIGKILL (waitForPidExit returned true), and prefer the daemon-boot checkpoint as the reliable fallback.","The new `.code-graph-daemon-pid.json` is another state file to migrate/clean; add it to the legacy-path probe set and the migration `dbFiles` COPYFILE_EXCL list (L1125) only if it should follow the DB, else keep it strictly skill-local and let each DB dir own its own."]}
