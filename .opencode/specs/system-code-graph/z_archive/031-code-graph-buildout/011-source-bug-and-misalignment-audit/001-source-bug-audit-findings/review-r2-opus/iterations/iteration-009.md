<!-- deep-review iteration (Opus 4.8, Workflow round 9/10) -->
# Iteration 9 — Deferred-findings re-verification + maintainability sweep [maintainability]

> Opus-4.8 finder + 2 independent adversarial skeptics per finding. A finding is CONFIRMED-REAL only if BOTH skeptics agree it is outstanding in current code.

### OR-9-01 [P2] [maintainability] (new-issue) Owner-lease state machine is duplicated across launcher.cjs and owner-lease.ts with DIVERGENT concurrency primitives

file: `.opencode/bin/mk-code-index-launcher.cjs + .opencode/skills/system-code-graph/mcp_server/lib/owner-lease.ts:launcher 261-404 (buildOwnerLease/classifyOwnerLease/processLiveness/readParentPid/writeOwnerLeaseFileExclusive/acquireOwnerLeaseFile/refreshOwnerLeaseFile) vs owner-lease.ts 232-455 (buildOwnerLease-equivalent/classifyOwner/getProcessLiveness/readProcessParentPid/writeOwnerLeaseExclusive/acquireOwnerLease/refreshOwnerLease)`

why: The launcher (.cjs, bootstrap) and owner-lease.ts (TS, in-process) each implement the full owner-lease lifecycle (read/classify/liveness/parent-pid/atomic-write/acquire/refresh/release). The DR-002-01 single-winner fix had to be applied separately to BOTH copies, and the two achieve single-winner differently: owner-lease.ts uses a mutation-lock-protected CAS (acquireOwnerLease 378-428, tryAcquireOwnerLeaseMutationLock), while the launcher uses last-writer-wins-then-reread (acquireOwnerLeaseFile 373-386). Every future lease invariant change must be mirrored by hand across two languages with non-identical algorithms; this is a standing drift hazard, not a per-fix bug. The DR fixes themselves are each individually correct in both copies (verified re-read CAS in launcher 377-384/400-401 and lock+reread in owner-lease 396-408/466-468).

fix: Document the intentional divergence in a single ADR or shared comment block referenced from both files (the launcher genuinely cannot import compiled TS ESM at startup), and add a paired-change checklist note at the top of each acquire fn so future lease edits are mirrored. Do NOT attempt to merge them — the bootstrap constraint is real.

confidence: 0.78

- skeptic verdict: **PRE_EXISTING** (refuted=true, sev→P2, conf=0.9)
  Verified both implementations in current code. The finding's FACTS are accurate: launcher (.cjs) and owner-lease.ts both implement the full owner-lease lifecycle, and their single-winner concurrency primitives diverge — launcher acquireOwnerLeaseFile uses last-writer-wins-then-reread (mk-code-index-launcher.cjs:377-386, reread check `reread.ownerPid !== process.pid`), while owner-lease.ts acquireOwnerLease uses a mutation-lock CAS (owner-lease.ts:378-428 via tryAcquireOwnerLeaseMutationLock + check-under-lock + writeOwnerLeaseAtomic). DR-002-01 was indeed applied separately to both copies.  REFUTED as an outstanding session-introduced defect for three grounded reasons:  1) PRE-EXISTING duplication. `git show 0ef38d58:.opencode/bin/mk-code-index-launcher.cjs` shows the launcher ALREADY had writeOwnerLeaseFileExclusive (L265), buildOwnerLease (L281), classifyOwnerLease (L325), acquireOwnerLeaseFile (L344) at baseline. `git show 0ef38d58:.../owner-lease.ts` shows acquireOwnerLease (L235), writeOwnerLeaseExclusive (L115) already present. The two-implementation split and their differing exclusive-write styles pre-date baseline 0ef38d58 — the session did not introduce the duplication.  2) Not a reportable category. The four reportable categories are (a) genuinely-new issue, (b) landed fix that is itself wrong/incomplete, (c) deferred finding whose status changed, (d) false-confidence regression test. This fits none. The finder explicitly concedes "the DR fixes themselves are each individually correct in both copies" and "this is a standing drift hazard, not a per-fix bug." I independently verified both fixes are correct (launcher reread at L382-385; owner-lease lock+recheck at L396-428). The proposed remedy is an optional ADR/doc comment with explicit instruction "do NOT attempt to merge them" — a hygiene opinion, not a defect.  3) The divergence is architecturally justified and partly already documented. The launcher header (L1-11) documents it is a bootstrap child-process launcher that loads env, builds dist, serializes starts BEFORE the compiled TS ESM exists — so it genuinely cannot import owner-lease.ts at startup; the finder agrees ("the bootstrap constraint is real"). A partial cross-reference already exists at mk-code-index-launcher.cjs:411 ("closing it would require the launcher to share owner-lease.ts's mutation lock"). The only gap vs the proposed fix is a top-of-function paired-change checklist note and a back-reference in owner-lease.ts — pure P2 doc polish on pre-existing intentional structure.  Net: real observation but PRE-EXISTING architecture; not a bug, not a broken fix, not a false-confidence test. Severity at most P2 (doc nicety). Refuted as an outstanding session-introduced finding.
- skeptic verdict: **PRE_EXISTING** (refuted=true, sev→P2, conf=0.9)
  The finding's factual claims are all verified against current code: the owner-lease state machine is genuinely duplicated across launcher.cjs and owner-lease.ts, the two achieve single-winner via different primitives (last-writer-wins-then-reread in the launcher acquireOwnerLeaseFile L376-384 vs mutation-lock CAS in owner-lease.ts acquireOwnerLease L378/395-408), and no shared ADR or paired-change checklist documents the intentional divergence. HOWEVER, the review charter reports a finding only if it is (a) genuinely NEW, (b) a landed fix that is itself wrong/incomplete, (c) a deferred finding whose status changed, or (d) a false-confidence regression test. This is none of those. The duplication and the divergent algorithms existed verbatim at baseline 0ef38d58 (all six launcher functions and all four owner-lease.ts functions present; launcher already used a plain last-writer-wins write, owner-lease.ts already used writeOwnerLeaseExclusive/Atomic). The session's DR-002-01 work merely hardened each pre-existing copy (added re-read in launcher, added mutation-lock CAS in owner-lease.ts), and the finding itself concedes "the DR fixes themselves are each individually correct in both copies." The bootstrap constraint (a .cjs launcher cannot import compiled TS ESM at startup) is real, so the duplication is architecturally necessary, not a defect. This is a standing maintainability/drift hazard that predates the baseline — PRE_EXISTING per the charter. Severity stays P2/maintainability; the proposed ADR + paired-change checklist is reasonable hygiene but is documentation work for a pre-existing condition, not remediation of a session defect or a flawed landed fix. Refuted as a session-attributable finding.

**RESULT: REFUTED / ALREADY-FIXED / FALSE-POSITIVE**

### OR-9-02 [P2] [maintainability] (new-issue) WorkingSetTracker has zero production consumers; the session's serialize/deserialize symbol-persistence + version-envelope change adds complexity to dead-in-production code

file: `.opencode/skills/system-code-graph/mcp_server/lib/working-set-tracker.ts:15-26, 84-109`

why: Repo-wide search (excluding dist/tests/README/.d.ts) finds NO production caller of WorkingSetTracker, serialize(), deserialize(), trackFile(), trackSymbol(), getTopRoots(), or getTopSymbols() — it is exported only via the lib/index.ts barrel and exercised solely by tests. The session added a SerializedWorkingSetTracker version envelope, a LegacySerializedWorkingSetTracker union, and an isSerializedWorkingSetTracker type guard to round-trip symbols, but no runtime path persists or restores hook state through this class. The added forward-compat machinery is currently untestable against real behavior.

fix: Either wire WorkingSetTracker into the hook-state persistence path it documents (lines 4-5 claim 'Stored in hook state'), or mark it experimental/unused and defer the serialization-format expansion until a consumer exists. At minimum add a comment noting it has no production caller so reviewers do not assume the envelope is load-bearing.

confidence: 0.7

- skeptic verdict: **PRE_EXISTING** (refuted=true, sev→P2, conf=0.85)
  The finding's descriptive facts check out, but it is REFUTED as a reportable session finding because the substance (zero production consumers / dead-in-production) PRE-DATES baseline 0ef38d58 — the session did not create the orphan, it only reshaped its serialization.  Verified against current code: - Only import of the class anywhere outside dist is a TEST: `.opencode/skills/system-spec-kit/mcp_server/tests/compact-merger.vitest.ts:4` imports WorkingSetTracker and at lines 22/35/47 does `new WorkingSetTracker(...)`, `WorkingSetTracker.deserialize(tracker.serialize())`. No production `import ... WorkingSetTracker` exists repo-wide (rg for import statements returned only that test file). - The class is re-exported only via the barrel `lib/index.ts:13` (`export * from './working-set-tracker.js'`). - The `getTrackedFiles()` calls in handlers/scan.ts (571/574/674) and lib/ensure-ready.ts (439/713/745) are on `graphDb`, which is `import * as graphDb from '../lib/code-graph-db.js'` — a NAME COLLISION with the standalone `code-graph-db.ts:865 export function getTrackedFiles()`, NOT WorkingSetTracker method calls. So those are not production consumers of WorkingSetTracker. - The only `new WorkingSetTracker()` in the source file itself is line 99, internal to `deserialize()`.  Session delta (git diff 0ef38d58 on working-set-tracker.ts): added `SerializedWorkingSetTracker` version-1 envelope (15-19), `LegacySerializedWorkingSetTracker` union (21), `isSerializedWorkingSetTracker` guard (23-26), and symbol round-trip in serialize/deserialize (85-109). That is real added complexity on an already-orphaned class.  Why refuted (not CONFIRMED_REAL): 1. Lens is "reproduces" — this is maintainability/hygiene, not a defect that reproduces under any runtime trigger. No crash, no data loss, no incorrect behavior. 2. The orphan status is PRE-EXISTING, not a NEW issue introduced this session, so it fails the charter's "genuinely NEW issue" test. 3. The landed change is itself CORRECT and forward-compatible (the legacy union + guard preserve back-compat), so it is not a broken/incomplete fix. 4. The finding's claim that the envelope is "currently untestable against real behavior" is partly inaccurate: compact-merger.vitest.ts round-trip tests `deserialize(serialize())`, so the format IS test-exercised — just not wired to a production persistence path.  This is a legitimate but minor pre-existing hygiene note (a clarifying "no production caller" comment would be reasonable), not a reportable session regression or broken fix.
- skeptic verdict: **PRE_EXISTING** (refuted=true, sev→P2, conf=0.9)
  Verified against current code and `git diff 0ef38d58`. The finding's factual core is accurate but its framing as a session "new-issue" is wrong, and one sub-claim is false.  WHAT'S TRUE: WorkingSetTracker has no production consumer. Repo-wide rg (excluding dist/.d.ts/the def file) shows the only non-test references are the barrel re-export `lib/index.ts:13` (`export * from './working-set-tracker.js'`) and the internal self-call `this.trackFile(filePath, [fqName])` at working-set-tracker.ts:154. `startup-brief.ts` does NOT use it (it only matched generic terms `getTopRoots`/`hookState`). The session's diff did add `SerializedWorkingSetTracker` (lines 15-19), `LegacySerializedWorkingSetTracker` (line 21), `isSerializedWorkingSetTracker` guard (lines 23-26), and changed serialize()/deserialize() (lines 85-109) to round-trip symbols with a version envelope + legacy fallback.  WHY PRE_EXISTING (not a session regression): The dead-in-production condition predates baseline 0ef38d58. It is documented in archived research as already-known unused: `.../024-compact-code-graph/030.../research/iterations/iteration-008.md:10` ("the unused internal WorkingSetTracker as an immediate upgrade path") and `.../024-compact-code-graph/research/iterations/iteration-102.md:28` ("WorkingSetTracker exists and is useful... but appears not yet wired into dispatch"). The session did not create the no-consumer state; it only extended the serialization format of a long-parked utility.  WHY ONE CLAIM IS FALSE: The finding asserts the added forward-compat machinery is "currently untestable against real behavior." Not true — `system-spec-kit/mcp_server/tests/compact-merger.vitest.ts:35` exercises the exact changed round-trip: `const restored = WorkingSetTracker.deserialize(tracker.serialize())`, plus trackFile/trackSymbol/getTopRoots/eviction (lines 22-58). The serialization change IS test-covered and round-trips correctly; it is backward-compatible (legacy `Record<string,FileAccess>` still deserializes via the guard's false branch). So the landed change is correct and complete on its own terms.  This is not (a) a new session-introduced issue, (b) an incorrect/incomplete landed fix, (c) a status-changed deferred finding, nor (d) a false-confidence test. It is a pre-existing maintainability observation about parked code, with one factually wrong sub-claim. The only actionable residue (add an "no production caller" comment) is pure hygiene against pre-existing dead code. Severity stays P2 at most; refuted as a session regression.</reasoning> <parameter name="currentCodeEvidence">working-set-tracker.ts:154 (`this.trackFile(filePath, [fqName])` — only non-test caller is internal); lib/index.ts:13 (`export * from './working-set-tracker.js'` — barrel re-export, no consumer); compact-merger.vitest.ts:35 (`const restored = WorkingSetTracker.deserialize(tracker.serialize())` — real round-trip test exercising the session's change, refuting "untestable"); archived iteration-008.md:10 and iteration-102.md:28 document the unused state as pre-baseline.

**RESULT: REFUTED / ALREADY-FIXED / FALSE-POSITIVE**

### OR-9-03 [P2] [correctness] (fix-incomplete) deserialize() type guard accepts symbols:null (typeof null === 'object'), then Object.entries(null) throws

file: `.opencode/skills/system-code-graph/mcp_server/lib/working-set-tracker.ts:23-26, 100-107`

why: isSerializedWorkingSetTracker (line 25) checks `typeof candidate.symbols === 'object'`, which is true for null. A serialized blob shaped {version:1, files:{}, symbols:null} passes the guard, so line 101 sets `symbols = data.symbols` (null), and line 105 `Object.entries(symbols)` throws TypeError. Same latent risk for files:null. This is only reachable from a corrupted/hand-crafted state blob and the class has no production caller (see OR-9-02), so it cannot fire in normal use — hence P2/low. The session introduced this code so the gap is attributable here, but severity is minimal.

fix: Tighten the guard to reject null: `candidate.files !== null && candidate.symbols !== null && typeof candidate.files === 'object' && typeof candidate.symbols === 'object'`, or fall back to `?? {}` at lines 100-101.

confidence: 0.6

- skeptic verdict: **DOWNGRADE** (refuted=true, sev→P2, conf=0.9)
  The logic flaw is mechanically real in the current code: isSerializedWorkingSetTracker (working-set-tracker.ts:25) returns `candidate.version === 1 && typeof candidate.files === 'object' && typeof candidate.symbols === 'object'`. Because `typeof null === 'object'`, a blob `{version:1, files:{}, symbols:null}` passes the guard; line 101 then binds `symbols = data.symbols` (null), and line 105 `Object.entries(symbols)` throws TypeError. The mechanics check out, and the type guard + symbols branch were genuinely session-introduced (confirmed via `git diff 0ef38d58`: the `+` lines add isSerializedWorkingSetTracker and the deserialize rewrite). So this is not a misread and not strictly pre-existing for the symbols-field path.  However it does not reproduce under any realistic trigger, so it cannot be CONFIRMED_REAL at a severity above the hygiene floor: (1) I grepped all of `.opencode` for callers and found ZERO production callers of `WorkingSetTracker.deserialize()` — only the class definition and a barrel re-export in lib/index.ts; (2) there are ZERO test callers either; (3) the only producer, `serialize()` (line 85-95), always emits `{version:1, files, symbols}` with non-null object values, so no real persisted blob ever carries `symbols:null`. The only way to reach the throw is a hand-crafted/corrupted blob fed to a function that nothing calls. The finding itself self-rates this P2/low for exactly these reasons, and that self-rating is correct: dead code + hand-crafted-only trigger = no plausible runtime condition (P0/P1 require crash in normal use or under a plausible condition). Note the top-level-null fragility (`Object.entries(data)` on a null argument) is actually pre-existing — the baseline deserialize had the same throw — so only the sub-field guard looseness is new, and it is unreachable. Verdict: DOWNGRADE, refuted=true. The observation is accurate but is already at the P2 hygiene floor with no reachable path; it is dead-code tidiness, not an outstanding defect warranting fix-incomplete escalation. The proposed `!== null` guard tightening or `?? {}` fallback is a reasonable defensive cleanup if/when a caller is ever wired up.
- skeptic verdict: **DOWNGRADE** (refuted=true, sev→P2, conf=0.9)
  Verified against current code at .opencode/skills/system-code-graph/mcp_server/lib/working-set-tracker.ts. The mechanical claim is correct: line 25's guard `candidate.version === 1 && typeof candidate.files === 'object' && typeof candidate.symbols === 'object'` does NOT reject null (typeof null === 'object'). A hand-forged blob {version:1, files:{}, symbols:null} passes isSerializedWorkingSetTracker(), so line 101 sets symbols=null and line 105 Object.entries(symbols) throws TypeError. The git diff vs 0ef38d58 confirms the session introduced isSerializedWorkingSetTracker and the deserialize() rewrite, so the gap is attributable to this session and no LANDED remediation touches this file (only 4f1dc0edef touched it for unrelated CG-fixes; working-set-tracker is not in that list). So it is NOT ALREADY_FIXED and NOT a FALSE_POSITIVE — the null-passthrough genuinely exists at line 25.\n\nHowever it does not warrant being carried as an outstanding bug: (1) I scanned the entire repo — there is ZERO production caller of WorkingSetTracker.deserialize (only the class def, the compiled dist mirror, and index.ts/index.d.ts barrel re-exports; all other matches are spec/doc/output text files). (2) There is no test exercising it. (3) serialize() always emits symbols as an object, so a legitimately-persisted blob never yields symbols:null — the throw is reachable ONLY via an externally corrupted/hand-crafted state blob fed to a method nothing invokes. The finder itself concedes 'cannot fire in normal use — hence P2/low'. Per the rubric P2 = minor/hygiene, and the finding is already at that floor. This is unreachable dead-code defensive-hygiene, not a defect affecting behavior under any plausible condition. Refuting it as a CONFIRMED_REAL outstanding bug; the honest classification is DOWNGRADE with severity held at the P2 floor (no escalation, no behavioral impact, no required action).

**RESULT: REFUTED / ALREADY-FIXED / FALSE-POSITIVE**

### OR-9-04 [P2] [maintainability] (new-issue) pruneDanglingEdges() carefully returns the removed-edge count but the only caller discards it

file: `.opencode/skills/system-code-graph/mcp_server/lib/code-graph-db.ts + .opencode/skills/system-code-graph/mcp_server/handlers/scan.ts:code-graph-db.ts 784-792 (returns result.changes); scan.ts 714-716 (return value discarded)`

why: pruneDanglingEdges() returns result.changes (the number of orphaned edges swept), and its doc comment advertises 'Returns the number of edges removed.' The sole production caller (scan.ts:715, `graphDb.pruneDanglingEdges();`) ignores the value. The BUG-03 post-scan sweep is exactly the place where a non-zero dangling-edge count would be a useful diagnostic signal (it would indicate genuinely-orphaned edges after cross-file resolution), but it is silently dropped — no log line, no inclusion in the scan response. Observability gap, not a correctness bug.

fix: Capture the count and either log it (`const pruned = graphDb.pruneDanglingEdges(); if (pruned > 0) log(...)`) or surface it in the scan response/structuralErrors so operators can spot abnormal orphan rates. Otherwise drop the return type to void to avoid implying it is consumed.

confidence: 0.72

- skeptic verdict: **FALSE_POSITIVE** (refuted=true, sev→none, conf=0.86)
  The finding's facts are accurate: pruneDanglingEdges() returns result.changes and its doc advertises the count, while the sole production caller (scan.ts:715) discards it. However, this is not a reportable defect. (1) No correctness/security/traceability impact — the function executes the DELETE correctly regardless of whether the count is read; the doc comment is truthful (it DOES return the count). (2) A function exposing a return value that a particular caller chooses not to consume is idiomatic, not a bug — the count remains available for reuse and is exercised by the test mock, so dropping the return type would reduce testability/composability rather than improve the code. (3) Under the adversarial 'reproduces' lens, nothing malfunctions under any realistic trigger: there is no crash, no wrong data, no broken contract — only a subjective preference that a non-zero orphan count COULD be logged. That is optional polish on freshly-landed, correct code, below the bar even for the loop's P2 definition (minor/hygiene/doc implies something to fix; here the code, the doc, and the API shape are all internally consistent and correct). The proposed fix is a discretionary enhancement (add a log line OR void the return type), not the remediation of an actual issue. Classified FALSE_POSITIVE: the framing of correct, deliberate API design as an 'observability gap' does not constitute a finding.
- skeptic verdict: **FALSE_POSITIVE** (refuted=true, sev→none, conf=0.82)
  The finding's factual description is accurate: pruneDanglingEdges() returns result.changes (line 791), documents "Returns the number of edges removed" (line 782), and the sole caller scan.ts:715 discards it. Both lines are new this-session BUG-03 code per git diff 0ef38d58 (so not PRE_EXISTING). However, this is not a reportable defect. (1) It is explicitly NOT a correctness bug — the finder concedes this; the prune executes correctly and no data/behavior is affected. (2) Exposing a count via the return type and leaving it available for callers/tests is idiomatic API design, not a flaw — a `: number` return does not "imply consumption"; it simply makes the count available (and a test mock already references the function). Returning an available-but-currently-unused diagnostic count is a deliberate, defensible shape, not tech debt. (3) The only concrete "harm" cited — "no log line for the prune count" — is a debatable observability preference, not a defect; whether post-scan dangling-edge sweeps warrant a log line is a design judgment call, and silent correct pruning is perfectly acceptable. The lens default is refuted unless a current-code line proves a genuinely outstanding issue; the cited lines prove only an unused return value, which is not an outstanding defect. This is a non-issue dressed as a P2 maintainability finding. Refuted as FALSE_POSITIVE; no remediation warranted. Confidence high — all three cited claims verified directly against current code and the loop's prior-review/remediation context.

**RESULT: REFUTED / ALREADY-FIXED / FALSE-POSITIVE**

---

coverage: Round 9 (maintainability) — independently re-verified all 9 deferred findings against current code + git-diff vs 0ef38d58, and swept the 5 primary changed libs.

DEFERRED FINDINGS — all 9 STILL STAND (none silently fixed, none regressed; verified at current lines):
- CG-002 (numeric ranges unenforced, PARTIAL): out of this round's primary-file scope (lives in tool-arg validation, not the 5 libs); not re-litigated, but no change touched it.
- CG-006 (failed/non-promoted scan reports freshness:'fresh'): CONFIRMED standing — handlers/scan.ts:764-781 computes freshness as `lastPersistedAt ? 'fresh':'empty'` gated on lastPersistedAt (=MAX indexed_at), NOT on scanPromotable; files persist in the loop regardless of promotability.
- CG-007 (read-path mutates via setLastGitHead): CONFIRMED standing — getGraphReadinessSnapshot (ensure-ready.ts:849) calls detectState, which at 496-497 calls setLastGitHead on out-of-scope HEAD advance, contradicting the explicit no-mutation doc at 818-824/836-839.
- CG-008 (candidate-manifest can't detect on-disk-only new files): unchanged (ensure-ready.ts manifest still DB-derived); deferred.
- CG-009 (destructive recover/rollback bypass confirm gate): CONFIRMED — apply-orchestrator.ts confirm gate (415) keys only on hard-stale; file byte-identical to baseline (empty diff).
- CG-010 (failed rollback over-broad status): CONFIRMED — recovery-procedures.ts correctly distinguishes status:ok/restored:false no-op (282-285) from status:failed (300); the over-broad `restored !== true` check lives in apply-orchestrator (unchanged); deferred correctly.
- CG-013 (DB-dir cwd-divergence): CONFIRMED standing AND now accurately documented — readiness-marker.ts:18-21 comment explains the deferral; base dir now matches core/config DATABASE_DIR default (.opencode/skills/system-code-graph/mcp_server/database) but still uses module-load process.cwd() (line 22-24) rather than the workspaceRoot param threaded into writeCodeGraphReadinessMarker, and does not share resolveCanonicalDbDir — exactly the deferred concern.
- CG-020 (per-file unconditional orphan-DELETE): CONFIRMED CLOSED by BUG-03 — code-graph-db.ts replaceEdges now gates the inline prune behind !deferDanglingTargetPrune (764-771); scan.ts full-scan loop passes deferDanglingTargetPrune:true (600) and sweeps ONCE via pruneDanglingEdges() after cross-file resolution (714-716), correctly preserving forward-referenced cross-file IMPORTS edges. Single-file/selective callers (ensure-ready.ts:555) still prune inline. Wiring verified end-to-end.
- CG-037 (dry-run rollback target): CONFIRMED standing — apply-orchestrator.ts:431-444 dry-run branch is operation-agnostic and skips dispatch, so rollback-bad-apply dry-run reports no rollback target; file unchanged.

PRIMARY-FILE CHANGE VERIFICATION:
- diff-parser.ts: DOC-ONLY change. Confirmed via `git show 0ef38d58` that the rangesOverlap BODY is byte-identical; only the comment was corrected from a misleading 'half-open' to the accurate 'inclusive (closed)' that already matched the aLines-1 arithmetic. Single caller (detect-changes.ts:333) passes guarded ≥1-line node ranges + post-image hunk coords; zero-length (pure-delete) path is correct. No finding.
- code-graph-db.ts: BUG-03 (replaceEdges defer + pruneDanglingEdges) correct & fully wired (above). CG-003 removeFile transaction wrapping (859-905) verified safe — all 3 callers (scan.ts:230 via cleanupMissingTrackedFiles, scan.ts:576, ensure-ready.ts:345) run in plain loops outside any open d.transaction(), so no better-sqlite3 nested-transaction throw. Correct & complete.
- tree-sitter-parser.ts: CG-005 tree.delete() now in try/finally with `tree?.delete?.()` (799-832) — frees WASM on both success and in-block throw; correct. Behavior change: quarantine check moved OUT of the SKIP_LIST_ENABLED guard (793-795 check, 838-839 set) — this is a correctness IMPROVEMENT (a B2 WASM heap-corruption fault now quarantines even when skip-list is disabled, per the documented heap-corruption-containment intent at 72-86), not a regression. Metrics now also distinguish 'recovered' outcome (813-817). No finding.
- readiness-marker.ts: path base aligned to skill-local DATABASE_DIR default; CG-013 deferral documented. No new finding beyond the standing CG-013.
- working-set-tracker.ts: serialize/deserialize symbol-persistence + version envelope — see OR-9-02/03.

BUILD/DIST: dist/ is fresh for all 5 libs (mtime newer than source) AND content-verified — dist reflects pruneDanglingEdges/deferDanglingTargetPrune, working-set version envelope + type guard, tree.delete + quarantine decoupling, and the readiness-marker path. Launcher and MCP load from dist, so the fixes are live.

TEST COVERAGE GAPS (not false-confidence regressions — no test passes misleadingly against un-fixed code): (1) handlers/scan.ts BUG-03 wiring has NO assertion that pruneDanglingEdges is called once or that persistIndexedFileResult is called WITH {deferDanglingTargetPrune:true} — code-graph-scan.vitest.ts mocks pruneDanglingEdges (line 44/117) but never asserts on it, and no test exercises real-DB forward-referenced IMPORTS-edge survival. (2) WorkingSetTracker symbol round-trip is only test-exercised against an in-memory instance, with no production persistence path.

Net: the four landed remediations touching these primary files are correct and complete; the 9 deferred findings are all accurately still-deferred. Findings reported are all P2 maintainability/hygiene (lease duplication, dead-in-production tracker, discarded prune count, null-symbols guard gap) — no new P0/P1, no incorrect/incomplete correctness fix among the in-scope changes.

newFindings: 0 (confirmed-real), rawFindings: 4, dimension: maintainability
