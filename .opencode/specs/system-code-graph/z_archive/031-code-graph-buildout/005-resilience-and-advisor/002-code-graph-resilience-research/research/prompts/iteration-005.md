You are running iteration 5 of 7 in a deep-research loop on code-graph resilience.

# Iteration 5 — Exclude-Rule Confidence Tier Definitions

## Research Questions
- Q6: Exclude-rule false-positive rate — for each high-tier pattern (`node_modules/`, etc.), are there real repos where this pattern shouldn't be excluded? What's the tail-risk?

## Required reads
1. Strategy + iterations 1-4 outputs
2. Existing scanner exclude rules (locate via grep for "exclude" in code-graph scan)
3. .gitignore patterns at repo root + nested
4. Sample non-canonical dirs in this repo (`find . -type d` survey)

## What to look for
- Standard ignore patterns from `.gitignore` (these are usually safe to exclude)
- Build-output dirs (`dist/`, `build/`, `out/`)
- Vendored deps (`node_modules/`, `vendor/`, `.venv/`, `__pycache__/`)
- Generated code (often has telltale comments like `// generated by`)
- IDE / OS metadata (`.vscode/`, `.idea/`, `.DS_Store`)
- Test fixtures vs production code (some test fixtures should be indexed for context)
- Repo-specific patterns: any custom-named dirs in this repo that are bloat candidates?

## Output: confidence tier definitions

Define three tiers with at least 5 patterns each:

**High confidence (always-exclude)**: patterns where the tail-risk of false-positive exclusion is near zero. Typical: `node_modules/`, `.git/`, `__pycache__/`.

**Medium confidence (usually-exclude, surface for confirmation)**: patterns where most repos benefit from exclusion but edge cases exist. Typical: `dist/` (sometimes hand-curated docs ship there), `build/`.

**Low confidence (per-repo decision)**: repo-specific patterns that need explicit operator approval. Typical: custom-named dirs found via heuristics.

Each pattern needs:
- pattern (glob)
- tier
- rationale
- false_positive_examples (real repos where this should NOT be excluded)

## Outputs (MANDATORY)

### 1. Iteration markdown
Path: `research/iterations/iteration-005.md`

Required sections: Summary, Tier Definitions, Patterns Per Tier (table with pattern + rationale + false-positive examples), Files Reviewed, Convergence Signals.

### 2. Delta JSON
Path: `research/deltas/iteration-005.json`. Schema with `research_questions_answered: ["Q6"]` plus a `confidence_tiers: { high: [...], medium: [...], low: [...] }` block.

### 3. State log append
JSONL line to `research/deep-research-state.jsonl` with iteration:5.

## Constraints
- Read-only.
- Tail-risk examples must be plausible, not invented.
- At least one repo-specific pattern in the low-tier (sourced from this repo's actual structure).
