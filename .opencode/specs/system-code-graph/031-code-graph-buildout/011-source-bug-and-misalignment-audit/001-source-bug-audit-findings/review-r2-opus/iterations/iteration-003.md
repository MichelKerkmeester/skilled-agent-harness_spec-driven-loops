<!-- deep-review iteration (Opus 4.8, Workflow round 3/10) -->
# Iteration 3 — owner-lease.ts race class + bootstrap rename-claim (DR-001-03/002-03/008-02/008-03) [correctness]

> Opus-4.8 finder + 2 independent adversarial skeptics per finding. A finding is CONFIRMED-REAL only if BOTH skeptics agree it is outstanding in current code.

### OR-3-01 [P2] [correctness] (fix-incomplete) Orphaned empty mutation-lock file (write-failure mid-creation) is permanently unreclaimable, can wedge the server heartbeat

file: `.opencode/skills/system-code-graph/mcp_server/lib/owner-lease.ts:104-148 (tryAcquireOwnerLeaseMutationLock)`

why: tryAcquireOwnerLeaseMutationLock openSync's the lock file with 'wx' FIRST (line 109), then writeFileSync's the pid (110) and fsyncSync's (111). If writeFileSync or fsyncSync throws (e.g. ENOSPC/EIO) after the file already exists on disk, the catch closeSync's the fd but does NOT unlink the now-empty/partial lock file; code !== 'EEXIST' so it re-throws (118-119), leaving an orphan lock file with no parseable pid. Every later acquirer then hits EEXIST, calls readOwnerLeaseMutationLockPid -> null (empty/unparseable content), and the stale-reclaim guard at line 123 requires lockPid !== null, so it is NEVER removed. tryAcquire returns null forever -> refreshOwnerLease() returns false forever (lines 439-440) -> server's heartbeat sees !refreshed and triggers ownerLeaseMismatchShutdownStarted self-shutdown (dist/index.js:39-42). This is a NEW gap introduced by the DR-001-03/002-03 mutation-lock itself; prior reviews predate the lock. Plausible only under a disk/IO failure mid-mutation, hence P2.

fix: On the write/fsync failure path inside tryAcquireOwnerLeaseMutationLock, unlink the just-created lock file before re-throwing (the openSync succeeded so we own it): wrap writeFileSync+fsyncSync so that if they throw, unlinkSync(lockPath) (ignore ENOENT) then close+rethrow. Alternatively, treat lockPid===null (empty/corrupt lock) as reclaimable in the stale path with an mtime-age guard so a wedged empty lock self-heals.

confidence: 0.72 | related: DR-002-03

- skeptic verdict: **CONFIRMED_REAL** (refuted=false, sev→P2, conf=0.82)
  Verified against current code at .opencode/skills/system-code-graph/mcp_server/lib/owner-lease.ts (read directly + git diff 0ef38d58). The finding reproduces and is a genuine fix-incomplete gap introduced by the DR-001-03/DR-002-03 mutation-lock remediation (the entire tryAcquireOwnerLeaseMutationLock block is net-new in the diff; prior reviews predate it, so it is in-scope as a landed fix that is itself incomplete).  Trigger path confirmed line-by-line: - Line 109: openSync(lockPath, 'wx', 0o600) creates the lock file FIRST (file now exists on disk, zero bytes). - Lines 110-111: writeFileSync(fd, pid) + fsyncSync(fd) can throw under ENOSPC/EIO AFTER the file already exists. - Lines 113-119: catch closes the fd (115) but does NOT unlink the just-created file; since code !== 'EEXIST' it re-throws (118-119), leaving a zero-byte/partial orphan lock. - Permanent-wedge confirmed: every subsequent acquirer hits openSync->EEXIST; readOwnerLeaseMutationLockPid (lines 94-102) returns null for empty/unparseable content; the stale-reclaim guard at line 123 requires lockPid !== null, so the null-content orphan is never reclaimed; control falls to return null (143). On the next attempt the loop restarts at attempt===0 and repeats -> tryAcquire returns null forever. - Downstream consequence verified in dist/index.js:39-42: heartbeat calls refreshOwnerLease, which (lib/owner-lease.ts:439-440) acquires the same mutation lock; tryAcquire null -> refreshOwnerLease returns false -> !refreshed sets ownerLeaseMismatchShutdownStarted and self-exits (process.exit(0)). Because the orphan survives restarts, the server re-wedges.  Refutation attempts failed: there is NO mtime/age-based reclaim, NO startup sweep, and NO launcher-side cleanup of the owner-lease .lock anywhere — skill-wide grep shows the .lock file is referenced ONLY inside owner-lease.ts (lines 62, 91, 155), and the only unlink (155, releaseOwnerLeaseMutationLock) runs on the success path, never on the write-failure path. The releaseOwnerLeaseMutationLock helper does delete the lock, but it is unreachable when tryAcquire never returns a lock object.  Severity: finder's P2 is correct/defensible. The trigger is a disk/IO failure (ENOSPC/EIO) in the narrow window after openSync succeeds but before/during the write — it does not occur in normal operation, so it is not P0/P1 under normal use. The persistence-across-restarts pushes it toward P1, but given the abnormal trigger condition required, P2 (edge-case under disk failure) is reasonable; I leave it at P2 rather than upgrade. The proposed fix (unlink the just-created lock on the write/fsync failure path before re-throwing, or treat lockPid===null as reclaimable with an mtime-age guard) is sound and minimal.
- skeptic verdict: **CONFIRMED_REAL** (refuted=false, sev→P2, conf=0.83)
  Verified against current code at .opencode/skills/system-code-graph/mcp_server/lib/owner-lease.ts. The finding's mechanical chain is exact and outstanding:  1. tryAcquireOwnerLeaseMutationLock (lines 104-148) openSync('wx') creates the lock file FIRST (line 109), then writeFileSync the pid (110) + fsyncSync (111). On a write/fsync throw (ENOSPC/EIO) after the file exists, the catch only closeSync's the fd (114-116) and never unlinks; the code is not EEXIST so it re-throws (118-119), leaving a 0-byte/partial orphan lock on disk. CONFIRMED at lines 108-119.  2. readOwnerLeaseMutationLockPid returns null for empty/unparseable content (Number.parseInt('') -> NaN -> not integer -> null, lines 96-98). The stale-reclaim guard requires lockPid !== null (line 123), so a null-pid orphan is NEVER eligible for unlink (line 130). There is no mtime-age fallback. CONFIRMED.  3. I grepped the entire mcp_server tree: the ONLY .lock removal sites are line 130 (stale-reclaim, gated on lockPid !== null && dead) and line 155 (releaseOwnerLeaseMutationLock, reachable only by the acquiring process). An empty orphan matches neither, so it is permanently unreclaimable and survives process restarts (on-disk file).  4. Downstream consequence is real and self-inflicted: tryAcquire returns null forever -> refreshOwnerLease returns false (lines 439-440) -> index.ts heartbeat at lines 48-51 sees !refreshed and triggers ownerLeaseMismatchShutdownStarted self-shutdown via shutdownCodeIndex(...).finally(() => process.exit(0)). CONFIRMED at index.ts:48-51.  NEW-gap verification: git show 0ef38d58:owner-lease.ts has 0 occurrences of tryAcquireOwnerLeaseMutationLock — the mutation lock is entirely new, landed by commits 6f6c6595/8943837b as the DR-001-03/002-03 remediation. So prior reviews could not have flagged it (they predate the lock), and none of the LANDED REMEDIATION items address an empty/null-pid orphan lock. This is review case (b): a landed fix that is itself incomplete. The cleanup paths the remediation added handle dead-pid stale locks but not the write-failure-mid-creation empty-lock case.  Refute lens not satisfied: no landed fix covers this; I cite the exact outstanding lines (108-119 missing unlink-on-write-failure; 123 null-pid exclusion). Not a duplicate, not pre-existing, not already-fixed.  Severity: I keep the finder's P2. The trigger requires a transient disk/IO fault (ENOSPC/EIO) in the narrow window between openSync succeeding and writeFileSync/fsyncSync completing — a rare external precondition, no data loss and no normal-use crash. The aggravating factor (the orphan lock is durable on disk, so it wedges the heartbeat into a repeated self-shutdown loop even after disk recovers and across restarts) is a legitimate argument toward P1, but the rare external-fault precondition keeps it defensibly at P2 as filed. Proposed fix is sound: unlink the just-created lock on the write/fsync failure path before re-throwing (ignore ENOENT), and/or treat lockPid===null as reclaimable under an mtime-age guard for self-heal.

**RESULT: CONFIRMED-REAL**

### OR-3-02 [P2] [correctness] (test-false-confidence) No regression test exercises the new reclaim-path mutation lock under contention; the existing 'concurrent' test cannot interleave and would pass without the lock

file: `.opencode/skills/system-code-graph/mcp_server/tests/lib/owner-lease.vitest.ts:77-94 ('allows exactly one fresh concurrent acquire to win')`

why: The only concurrency test (lines 77-94) runs two acquireOwnerLease calls against an EMPTY dbDir, which take the !existing branch (owner-lease.ts:359-376) using writeOwnerLeaseExclusive ('wx'), NOT the new tryAcquireOwnerLeaseMutationLock reclaim path (378-428). Moreover acquireOwnerLease is fully synchronous and Promise.resolve().then(...) runs each call to completion as an atomic microtask, so there is no real interleaving; single-winner here is guaranteed by exclusive-create and the test would pass even if the mutation lock were a no-op. The DR-001-03 (release under lock + re-read) and the reclaim-path isSameOwnerLease divergence bail (397-407) have NO test that drives a successor-lease write between the pre-lock read and the post-lock re-read. So the new race-class fix has correctness-by-inspection only, no executable regression guard. This is a coverage gap, not a false-passing assertion against un-fixed code, hence reported at modest confidence.

fix: Add a test that pre-writes an existing reclaimable (stale-pid/stale-heartbeat) lease, then injects a successor lease write before the lock's post-acquire re-read (e.g. via a processKill/readParentPid stub that mutates the lease file as a side effect, or by monkey-patching readOwnerLease) and assert acquireOwnerLease returns acquired:false with the successor as holder. Add a releaseOwnerLease test where the lease's ownerPid is swapped to a successor mid-call and assert the successor lease is NOT unlinked.

confidence: 0.6 | related: DR-001-03

- skeptic verdict: **FALSE_POSITIVE** (refuted=true, sev→P2, conf=0.82)
  Verified against current code at mcp_server/lib/owner-lease.ts and mcp_server/tests/lib/owner-lease.vitest.ts (note: source is at mcp_server/lib/, not mcp_server/src/lib/ as cited — minor path error in the finding, but the test import `'../../lib/owner-lease.js'` resolves there).  The finding's factual observations are accurate: (1) the 'concurrent' test (lines 77-94) uses an EMPTY dbDir with no prior writeLease, so both acquireOwnerLease calls take the `!existing` branch (lib/owner-lease.ts:359-376) using writeOwnerLeaseExclusive('wx'), never entering the tryAcquireOwnerLeaseMutationLock reclaim path (378-428); (2) Promise.resolve().then(syncFn) runs each synchronous acquire to completion as an atomic microtask, so there is no preemptive interleaving and single-winner is guaranteed purely by exclusive-create; (3) git show 8943837b confirms the DR-001-03 fix commit touched lib/owner-lease.ts + the launcher but added ZERO changes to owner-lease.vitest.ts; (4) grep across the whole tests/ tree finds no test driving the reclaim divergence-bail (isSameOwnerLease check at 397-407) or release-under-contention (releaseOwnerLease at 463-472). So the DR-001-03 release-under-lock fix and the reclaim-path successor-divergence bail are indeed correctness-by-inspection with no executable regression guard.  HOWEVER, the finding is refuted as a CONFIRMED_REAL/test-false-confidence finding. The only reportable hook here is category (d) — 'a regression test that gives false confidence (passes even against un-fixed code).' The cited test at 77-94 is NOT such a test: it exercises the exclusive-create ('wx') single-winner property, a SEPARATE and genuinely-correct mechanism, and its assertion (exactly one of two fresh acquires on an empty dir wins) is sound and correctly guarded by 'wx' in BOTH fixed and un-fixed code. It was never the regression test for the mutation-lock fix and does not give false confidence about anything it actually asserts. The reviewer explicitly concedes this: 'This is a coverage gap, not a false-passing assertion against un-fixed code.' By their own framing the finding does not satisfy category (d).  What remains is a pure coverage gap: a landed fix lacking a dedicated regression test. Checking the other reportable categories: (b) 'a landed fix that is itself incorrect or incomplete' — I verified the fix CODE is correct: the reclaim path acquires the mutation lock, re-reads `current` under it, bails acquired:false on owner divergence (397-407), re-classifies, and only writes atomically when still reclaimable (424); releaseOwnerLease re-reads under the same lock and only unlinks when holder.ownerPid === ownerPid (466-468). The CAS-under-lock logic is sound, so the fix is not incorrect/incomplete on correctness grounds. The LANDED REMEDIATION list's enumerated new tests (canonical-db-dir, security-hardening, launcher-idle-timeout, status-readiness-snapshot) never promised an owner-lease reclaim/release regression test, so no promised test is missing or broken.  Net: the substantive residual is a minor maintainability/hygiene coverage gap (P2 at most), not a defect in the landed fix and not a false-passing test. The specific claim as filed (test-false-confidence against the cited 77-94 test) does not hold, because that test correctly guards a different invariant and the reviewer mis-attributes a coverage gap to a 'false-passing test.' Refuted.
- skeptic verdict: **DOWNGRADE** (refuted=true, sev→P2, conf=0.9)
  VERIFIED AGAINST CURRENT CODE.  Factual claims in the finding are all TRUE: 1. The concurrent test (owner-lease.vitest.ts:77-94) runs two acquireOwnerLease calls against an EMPTY dbDir. With existing===null it takes the !existing branch (owner-lease.ts:359-376) using writeOwnerLeaseExclusive ('wx'), never the reclaim mutation-lock path (378-428). Confirmed by reading both files. 2. acquireOwnerLease is synchronous; Promise.resolve().then(() => acquireOwnerLease(...)) runs each call to completion as an atomic microtask, so there is no real interleaving. Single-winner here is guaranteed by 'wx' exclusive-create and the test passes even if the mutation lock were a no-op. Confirmed. 3. No test in the entire code-graph test suite drives a successor-lease write between the pre-lock read (existing) and post-lock re-read (current) to exercise the isSameOwnerLease divergence bail (397-407), nor a release-under-lock re-read (DR-001-03, 463-468). Confirmed by grep across tests/ for isSameOwnerLease|MutationLock|successor — zero hits.  WHY THIS IS NOT A SESSION REGRESSION (the refutation): - `git diff 0ef38d58 -- owner-lease.vitest.ts` is EMPTY. The test file's last-touching commit 8c30fb05d4 is an ancestor of baseline (git merge-base --is-ancestor confirms). The pre-existing concurrent test was NOT authored or modified this session and was never claimed as the guard for the new fix. - This is therefore NOT report-category (d) test-false-confidence: that category is for a regression test that PASSES AGAINST UN-FIXED CODE, implying the session added/relied on a test that gives false confidence about its own fix. The remediation list (LANDED REMEDIATION) never claims any owner-lease regression test for DR-001-03 or the reclaim-lock divergence; the four session-added tests cover DR-003-02, DR-008-01, DR-008-04, DR-001-01 only. So nothing claims false coverage of this fix. - The fix ITSELF is correct and complete (the only path to CONFIRMED_REAL under the rubric is the fix being wrong/incomplete): reclaim path 395-428 acquires the lock, re-reads current, bails on isSameOwnerLease divergence (397-407) and on live/symlink/eperm classification (415-421) before writeOwnerLeaseAtomic (424); release 463-472 wraps the unlink in the same lock with an ownerPid re-read. Baseline 0ef38d58 had NEITHER (releaseOwnerLease was a bare unlinkSync with no lock/re-read; no tryAcquireOwnerLeaseMutationLock existed). The serialization plus read-then-act re-read correctly closes the split-brain window. Correct by construction, not just inspection.  NET: A genuine but minor coverage gap (correct-by-inspection fix lacking an executable regression guard) on a fix that works. The finder self-characterized it as 'a coverage gap, not a false-passing assertion against un-fixed code' at modest confidence — accurate. It is a legitimate P2 hygiene note (adding the proposed successor-injection and mid-call-swap tests would harden the suite) but it is NOT an outstanding correctness bug and NOT a session test-false-confidence regression. Hence DOWNGRADE to P2, refuted as a reportable session finding.

**RESULT: REFUTED / ALREADY-FIXED / FALSE-POSITIVE**

---

coverage: Round 3 (correctness) — verified owner-lease.ts mutation-lock race class and launcher bootstrap rename-claim against baseline 0ef38d58 by reading current source + git diff, and confirmed dist/lib/owner-lease.js + dist/index.js match the running code.\n\nCONFIRMED SOUND:\n- DR-001-03 (releaseOwnerLease): now acquires tryAcquireOwnerLeaseMutationLock, re-reads holder while holding it, unlinks only if holder.ownerPid===ownerPid, releases in finally. Closes the read-then-unlink split-brain vs a concurrent reclaim. Correct + complete.\n- refreshOwnerLease + acquire reclaim branch: take the same lock; acquire re-reads `current` under lock and bails via isSameOwnerLease(current,existing) on any divergence (lines 397-407) and re-classifies live/symlink/eperm (409-421) before writeOwnerLeaseAtomic. Closes classify-then-write TOCTOU. reclaimed: current ?? existing handles the current===null (deleted) case. Sound.\n- DR-002-03 (mutation-lock stale cleanup): re-reads pid immediately before unlink and only removes if it still equals the classified dead pid (lines 122-131); attempt<2 cap prevents spin. No successor-lock deletion. Correct.\n- DR-008-02 (bootstrap rename-claim): renameSync(lockDir, staleClaim) atomically claims the inode before rmSync; a losing racer's rename throws and is swallowed by the surrounding catch -> retries mkdir. A fresh successor mkdir creates a new inode the rename cannot touch. No live-lock deletion. Correct.\n- DR-008-03 (launcher clearOwnerLeaseFile / clearOwnerLeaseFileIfOwner): double re-read before unlink; residual sub-syscall window is explicitly documented and, critically, BOUNDED by lifecycle — the launcher only mutates .code-graph-owner.json (a) at startup before the child exists, (b) in respawnAfterDeadSocket under the bootstrap lock AFTER reapOwnerBeforeRespawn confirms the old owner dead + a currentOwner.ownerPid recheck (lines 552-575), or (c) on exit after the child is dead/dying. The server child's heartbeat refreshOwnerLease runs under the mutation lock; launcher and server never write the lease concurrently in the real lifecycle, so the launcher legitimately bypassing owner-lease.ts's mutation lock is not a live bug. No mutation-lock deadlock or leak on any early-return path (all guarded by try/finally; tryAcquire returns null rather than blocking).\n\nNo P0/P1 residual TOCTOU found in the targeted surfaces. Two P2s reported: OR-3-01 (a genuinely NEW unreclaimable-empty-lock gap introduced by the mutation-lock fix, fires only under IO failure mid-creation) and OR-3-02 (the reclaim-path mutation lock + DR-001-03 re-read have no executable contention regression test; the existing 'concurrent' test exercises only the exclusive-create fresh path and cannot interleave). Neither duplicates the prior gpt-5.5 or parallel-Opus reviews.

newFindings: 1 (confirmed-real), rawFindings: 2, dimension: correctness
