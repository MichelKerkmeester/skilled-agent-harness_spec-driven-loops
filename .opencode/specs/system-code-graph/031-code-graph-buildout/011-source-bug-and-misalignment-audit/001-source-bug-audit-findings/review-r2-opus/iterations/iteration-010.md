<!-- deep-review iteration (Opus 4.8, Workflow round 10/10) -->
# Iteration 10 — Adversarial synthesis + completeness critic [correctness]

> Opus-4.8 finder + 2 independent adversarial skeptics per finding. A finding is CONFIRMED-REAL only if BOTH skeptics agree it is outstanding in current code.

### OR-10-01 [P2] [correctness] (fix-incomplete) Legacy-location lease is detected for split-brain prevention, but the IPC bridge cannot connect to a live legacy-location daemon (socket asymmetry across the .spec-kit -> skill-local relocation)

file: `.opencode/bin/mk-code-index-launcher.cjs / .opencode/bin/lib/launcher-ipc-bridge.cjs:launcher 220-228 (legacyLeasePaths) + 588-600 (bridgeOrReportLeaseHeld); bridge 329-333 (maybeBridgeLeaseHolder socketPath resolution)`

why: This session relocated the DB from .opencode/.spec-kit/code-graph/database to skill-local mcp_server/database and, in tandem, repointed legacyLeasePaths() to probe the former .spec-kit PID lease (line 225). isLeaseHeld() therefore correctly detects a still-live old-location daemon and returns a result with legacyPath set (launcher line 488), which prevents a competing writer/split-brain (the data-safety property holds — verified). HOWEVER bridgeOrReportLeaseHeld passes dbDir: resolvedDbDir() (launcher line 594 = the NEW skill-local path) into maybeBridgeLeaseHolder, which computes socketPath = getIpcSocketPath(serviceName, { dbDir }) (bridge line 329) purely from that new path. A live daemon that started before the relocation has its socket at .spec-kit/code-graph/database/daemon-ipc.sock, so the new-location existsSync(socketPath) check (bridge line 330) fails and the launcher emits `LEASE_HELD_BY ... (no-bridge-socket)` instead of bridging. Net effect during the deploy-transition window (the memory note confirms a live daemon currently runs at the old location): secondary MCP sessions/reconnects get a diagnostic line and NO working IPC connection even though a healthy daemon is alive — until the old daemon idles out or is restarted, after which a new launcher re-establishes the daemon skill-local and the gap self-heals. No data loss (dual-writer prevented), hence P2; would edge to P1 only if the old daemon is very long-lived. The session added legacy-lease DETECTION but not the matching legacy-SOCKET bridging, leaving the two halves asymmetric.

fix: When isLeaseHeld() returns a legacy-path hit, derive the bridge socket from the SAME legacy DB dir that the matched lease lives in (e.g. pass the legacy dbDir, or have maybeBridgeLeaseHolder fall back to probing getIpcSocketPath for each legacyLeasePaths() parent dir when the primary-location socket is absent) before reporting no-bridge-socket. Alternatively, gate the legacy-lease relevance to the migration window only. At minimum, document the transition-window no-bridge behavior so it is not mistaken for a daemon outage.

confidence: 0.72 | related: DR-003-01

- skeptic verdict: **FALSE_POSITIVE** (refuted=true, sev→none, conf=0.85)
  The code-level mechanic the finder describes is accurately read, but the issue does not reproduce against current code, and the "asymmetry" is not an incomplete fix — it is the intended safe design.  CURRENT-CODE VERIFICATION: - bridgeOrReportLeaseHeld passes dbDir: resolvedDbDir() (launcher .opencode/bin/mk-code-index-launcher.cjs:594), and maybeBridgeLeaseHolder computes socketPath = getIpcSocketPath(serviceName, { dbDir }) (bridge .opencode/bin/lib/launcher-ipc-bridge.cjs:329 → 58-66: socketDir = options.dbDir, socket = dbDir/daemon-ipc.sock). Launcher does NOT set SPECKIT_IPC_SOCKET_DIR (rg returned no matches in mk-code-index-launcher.cjs), so the socket IS derived from the new dbDir. legacyLeasePaths() probes the former .spec-kit PID lease (launcher:225). So the detection-vs-bridge socket asymmetry the finder cites is mechanically present in the code. That part is correctly read.  WHY IT DOES NOT REPRODUCE (FALSE_POSITIVE): 1. The legacy location does not exist. `ls .opencode/.spec-kit/` → "No such file or directory". The relocation (commit 69e7bf12d1) is deployed and migration-back already completed: the only DB dir is skill-local mcp_server/database/ with a fresh owner lease (.code-graph-owner.json, 16:07) and launcher lease. There is no socket, no PID file, and no .code-graph-owner.json under .spec-kit. 2. With the legacy file absent, leaseHeldFromFile(legacyPath) → readLeaseFile returns null (launcher:456) → { held:false }, so isLeaseHeld() never returns a legacyPath hit and the legacy bridge branch is never entered. The (no-bridge-socket) path the finder describes is unreachable for the legacy case in the current deployed state. 3. The finder's trigger requires a daemon started BEFORE the relocation commit that bound its socket+lease into .spec-kit and is STILL alive when a post-relocation launcher runs. That transition window has closed on this deployment (legacy tree fully removed). The cited "memory note confirms a live daemon currently runs at the old location" is not supported by disk state.  WHY IT IS NOT AN INCOMPLETE FIX (even if it could fire): - The finder itself concedes "No data loss (dual-writer prevented) — the data-safety property holds (verified)". The session's fix ADDED legacy lease DETECTION for split-brain PREVENTION; bridging to a legacy socket was never within that fix's scope. When a held-lease-without-live-socket is hit, emitting LEASE_HELD_BY (no-bridge-socket) is the PRE-EXISTING intended diagnostic design (documented in prior packet 005-opencode-bridge-native-route, where the held-lease/missing-socket path is the known behavior). A live legacy daemon continues serving its own connected session; only a brand-new secondary launcher gets the diagnostic during a window that self-heals. So the fix is correct and complete for its purpose; the absent legacy-socket bridging is by-design and causes no data loss.  Net: hypothetical, transition-window-only, non-reproducing against current code (no .spec-kit tree), data-safety property holds, and the behavior — to the extent it exists — predates this session's fix and is the intended safe path, not a fix that is itself wrong/incomplete. Refuted.
- skeptic verdict: **DOWNGRADE** (refuted=true, sev→none, conf=0.86)
  The socket-asymmetry MECHANIC is real in code, but the finding's data-safety claim is satisfied and the residual is a transient, migration-window-only convenience gap that ground-truth shows has already self-healed — so it does not stand as a "fix-incomplete" defect.  CODE MECHANIC (verified real): bridgeOrReportLeaseHeld passes `dbDir: resolvedDbDir()` (launcher line 594 = new skill-local path) into maybeBridgeLeaseHolder. getIpcSocketPath (bridge lines 58-66) derives socketPath from that dbDir (since neither SPECKIT_IPC_SOCKET_DIR nor SPECKIT_CODE_GRAPH_DB_DIR is set in this env — confirmed via `env`), so existsSync(socketPath) (bridge line 330) probes only the new-location socket and emits `(no-bridge-socket)` rather than bridging to a legacy-location daemon. legacyLeasePaths() (launcher 220-228) probes only the legacy PID lease, not its socket, so detection and bridging are asymmetric. All confirmed against current lines.  WHY IT DOES NOT STAND AS A FIX-INCOMPLETE DEFECT: 1. SAFETY HALF IS CORRECT AND COMPLETE. The landed remediation (DR-002-02) added legacy-lease DETECTION explicitly for split-brain/dual-writer prevention. isLeaseHeld() returning a legacyPath hit blocks a competing writer — the actual data-safety property. The finding itself concedes "the data-safety property holds — verified." legacy-socket BRIDGING was never a safety requirement; it is a connection convenience. The fix did exactly what it was scoped to do. 2. THE IMPACT PRECONDITION DOES NOT HOLD. The finding's harm requires a LIVE pre-relocation daemon at the legacy `.spec-kit` location. Ground-truth: the legacy dir does not exist at all (`NO .spec-kit DIR AT ALL`); there is no daemon-ipc.sock anywhere under .opencode (`find` returns nothing); the only running daemon (PID 8765) launches from skill-local dist/index.js with a current owner lease (.code-graph-owner.json, May 29 16:08) at the new location. legacyLeasePaths() would match nothing. The transition window has already closed on this deployment. 3. SELF-HEALING / TRANSIENT BY THE FINDING'S OWN ADMISSION. The text states the gap "self-heals" once the old daemon idles out or restarts. It is a one-shot migration-boundary artifact, not steady-state behavior. In normal use there is no outstanding crash, data loss, or correctness fault.  Net: real code observation but no outstanding defect against the fix's purpose; impact precondition absent; transient and already healed. Severity falls from P2 to none for steady-state. refuted=true.

**RESULT: REFUTED / ALREADY-FIXED / FALSE-POSITIVE**

---

coverage: Round 10 adversarial/completeness pass on the launcher<->owner-lease<->socket-server<->config DB-dir seam. Read all four primary files at current HEAD and git-diffed each vs 0ef38d58, plus the untouched-by-diff collaborators (readiness-marker.ts, ipc/socket-server.ts resolveIpcSocketPath, launcher-ipc-bridge.cjs getIpcSocketPath/defaultDbDirForService/maybeBridgeLeaseHolder).\n\nTHREE-RESOLVER AGREEMENT (verified SOUND under launcher-driven startup): (1) launcher resolvedDbDir() = canonicalizePath(SPECKIT_CODE_GRAPH_DB_DIR ?? kitDir/mcp_server/database); (2) config.ts DATABASE_DIR = resolveCanonicalDbDir(envDir ?? resolveWorkspaceRoot()/.opencode/skills/system-code-graph/mcp_server/database, workspaceRoot); (3) readiness-marker CODE_GRAPH_READINESS_MARKER_BASE_DIR = resolve(cwd, SPECKIT_CODE_GRAPH_DB_DIR || '.opencode/skills/.../database'). On the normal path the launcher sets SPECKIT_CODE_GRAPH_DB_DIR to the canonical absolute skill-local dir AND spawns the child with cwd=workspace root, so all three collapse to the same path. I confirmed .opencode/skills and system-code-graph are REAL dirs (not symlinks) in this workspace, so config's realpath canonicalization and readiness-marker's non-realpath resolve do not diverge here; the only residual divergence (server launched directly, no env var, CWD != workspace root: config uses module-anchored root, readiness uses cwd) is exactly the explicitly-DEFERRED CG-013 cwd-divergence, and the code comment's 'matches DATABASE_DIR when CWD == workspace root' claim is accurate — not a new finding.\n\nVERIFIED-COMPLETE LANDED FIXES: migration-back ordering is correct (migration block runs BEFORE launchServer() sets the env var, so the `!SPECKIT_CODE_GRAPH_DB_DIR` guard at line 857 is honored; migration targets resolvedDbDir(); formerLocationOwnerLive probes both PID and owner leases via pidLiveAt with correct EPERM=live/ESRCH=dead semantics). The migration copies the former PID lease forward but NOT the owner lease — the copied stale PID lease is correctly classified staleReclaimable (dead PID -> ESRCH) and does not block startup; the empty new-location owner lease is acquired cleanly. acquireOwnerLeaseFile re-read CAS (DR-002-01) is correct in both branches: !existing uses O_EXCL writeOwnerLeaseFileExclusive (true single-winner), reclaim uses write+reread (last-writer-wins, exactly one survives). refreshOwnerLeaseFile reread (DR-002-01), clearOwnerLeaseFile/IfOwner double-read (DR-008-03), bootstrap-lock atomic rename-claim (DR-008-02), DR-003-01 uid check on legacy leases, canonical-db-dir pre-mkdir ancestor realpath escape check (DR-003-02), socket-server pre-existing-dir ownership/writability refusal (DR-008-01), and index.ts shutdown idempotence guard all read correct and complete. index.ts MK_CODE_INDEX_ROOT_DIR doc fix is accurate: writeCodeGraphReadinessMarker uses the arg only for getGraphReadinessSnapshot (body) and writes to the DB-dir-derived CODE_GRAPH_READINESS_MARKER_PATH constant.\n\nONE NEW FINDING (OR-10-01, P2): legacy-lease detection without matching legacy-socket bridging — the session repointed legacyLeasePaths() at .spec-kit so a live old-location daemon is detected (split-brain prevented, data-safe), but bridgeOrReportLeaseHeld always resolves the bridge socket from the NEW skill-local dbDir, so it cannot connect to the old daemon's socket and emits no-bridge-socket during the deploy-transition window. Transient/self-healing, no data loss.\n\nNOTED-NOT-REPORTED: bridge defaultDbDirForService('mk-code-index') (line 47) still returns the old .spec-kit default, but it was NOT changed this session and is latent/dead — the only live caller (maybeBridgeLeaseHolder line 329) always receives an explicit dbDir from the launcher, and no production code calls getIpcSocketPath without dbDir (grep confirmed only spec/log references). Pre-existing maintainability staleness, not a session regression and not live-reachable.

newFindings: 0 (confirmed-real), rawFindings: 1, dimension: correctness
