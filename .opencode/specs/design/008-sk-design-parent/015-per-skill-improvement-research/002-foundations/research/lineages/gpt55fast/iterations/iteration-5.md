SKILL ROUTING: User directed → deep-research. Binding `artifact_dir` directly to the provided fanout lineage directory and limiting writes to that directory.Loading the command/reference contract first, then I’ll initialize the lineage packet in the provided artifact directory.The command contract confirms this is an orchestrated YAML workflow, with the setup already fully bound by the prompt. I’m reading the workflow asset and output schemas now so the lineage packet matches the expected file contract.The workflow includes a `fanout_lineage_artifact_dir` branch matching the user’s instruction; I’m following that branch without invoking the resolver. Next step is source research and packet initialization.The lineage directory is empty, so this is a fresh run. I found the actual foundations skill path as `.opencode/skills/sk-design/design-foundations/`; the user’s `.opencode/skills/sk-design/foundations` appears to refer to the mode name rather than an existing directory.The requested prior research exists, but the stated `014-routing-benchmark` path is not present under the exact parent path. I’m searching by the benchmark signals (`Mode A`, `83`) and reading the foundations corpus next.I’m now grounding the iteration findings in the foundations references and the prior corpus research. The current evidence points toward routing clarity and deliverable scaffolding as likely improvement areas, but I’ll complete the loop before synthesizing.The 009 research shows the previously recommended foundations additions have already largely landed: `data_viz.md`, `adaptation_matrix.md`, and `token_starter.md` are present. That shifts the improvement research from “add missing coverage” to “make the shipped coverage easier to route, execute, and verify.”The local tree confirms `012-foundations-motion-audit` implemented the foundations additions from 009, while the benchmark artifact path still does not exist. I’ll treat the `83/100` score as operator-provided context and flag the missing local artifact in the research evidence.A prior review flagged two foundations-specific hypotheses: underspecified color-role contract and playbook/routing ambiguity. I’m checking those against the current files so the final recommendations only include confirmed issues.Initializing the lineage packet now. I’ll create only the provided lineage directory plus its `iterations`, `deltas`, `prompts`, and `logs` children.Writing the lineage artifacts now: config, state log, six iteration files with deltas, reducer-style strategy/registry/dashboard, resource map, and final synthesis.Artifacts are written. I’m running read-only verification now: JSON/JSONL parsing, required artifact presence, iteration/delta counts, and spec-folder validation if the packet supports it.FANOUT_LINEAGE_COMPLETE:gpt55fast