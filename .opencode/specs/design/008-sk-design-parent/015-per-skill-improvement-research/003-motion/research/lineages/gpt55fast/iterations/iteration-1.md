SKILL ROUTING: User directed → deep-research. I’ll follow the named workflow, bind `artifact_dir` directly to the supplied lineage directory, and keep writes confined to that path.The memory trigger check rejected the provided fan-out session id because it is not a server-managed memory session. I’m retrying the context trigger without binding that id, while preserving it as the deep-research workflow session id.I’m loading the deep-research contract and the CLI executor contract before any workflow execution, because the request names both.The executor contract confirms self-invocation is prohibited inside OpenCode unless this is an explicit detached parallel dispatch. Since this request is a fan-out lineage already executing here, I’ll execute the deep-research loop in-process and won’t spawn `opencode run`.The deep-research contract says the YAML workflow owns state, dispatch, convergence, and synthesis. Because this lineage has an explicit artifact override and a strict write boundary, I’m going to mirror the workflow’s state packet files inside that lineage directory rather than resolving a different artifact root.Phase init starts by classifying the lineage state, then I’ll load the motion-skill evidence corpus and write canonical `deep-research-*` packet files under the supplied lineage directory.The requested `.opencode/skills/sk-design/motion` path is not immediately visible in the broad glob output; I’m checking the actual skill and spec directory layouts before deciding whether this is a path alias or a missing input.Confirmed the local motion skill directory is `.opencode/skills/sk-design/design-motion`; I’ll use that as the concrete target for the user’s `.opencode/skills/sk-design/motion` reference and record the alias explicitly in the research artifacts.The 014 benchmark artifact present in this workspace is for `design-interface`, not `design-motion`; it reports D5 100/100 but aggregate 70/100. I’ll treat the user-provided “motion equals 100 of 100” as an unverified claimed input and record the local evidence contrast in the lineage.The current motion packet is already mature: the prior 009 recommendations for the restraint gate and three reusable motion assets are implemented. The next loop iterations will focus on residual gaps rather than re-proposing completed 009 work.The manual playbook covers eight scenarios but does not appear to include hub-level mode-routing assertions, register-posture variants, or concrete “decorative motion refusal” adversarial prompts. I’ll verify the remaining two scenarios and then start writing iterations.A new evidence point: the motion changelog still mentions `feature_catalog/` and per-packet `graph-metadata.json`, while the nested packet currently has neither. That is a documentation-freshness candidate, not a motion-logic gap.Phase main loop will converge in six evidence iterations rather than exhaust ten: the corpus and live packet agree on most completed work, and the remaining findings are residual, bounded improvements. I’m creating only lineage-local state and output files now.Phase synthesis will emphasize residual improvements: routing/resource contract alignment, advanced-craft top-ups, benchmark/playbook coverage, and documentation freshness. I’m writing the final packet artifacts now.Artifacts are written. I’m doing lineage-local verification now: JSON parsing, JSONL parsing, and file-presence checks for the required research outputs.FANOUT_LINEAGE_COMPLETE:gpt55fast