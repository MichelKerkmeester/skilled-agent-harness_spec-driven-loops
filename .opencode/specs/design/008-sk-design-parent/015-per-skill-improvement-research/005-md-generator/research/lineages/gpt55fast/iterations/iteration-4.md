SKILL ROUTING: User directed → `deep-research`. Spec folder and artifact output root are explicitly provided; I’ll read the deep-research contract first, then write only lineage outputs under the requested `research/lineages/gpt55fast` directory.The deep-research contract requires externalized state and per-iteration evidence. I’m going to gather the target skill, prior corpus, external corpus, and benchmark context first, then create the lineage packet files in the provided artifact directory.Discovery found the actual skill path as `.opencode/skills/sk-design/design-md-generator`, not `.opencode/skills/sk-design/md-generator`. I’ll treat that as the target md-generator skill surface and use the explicitly named corpus paths where they exist.The provided benchmark/external glob did not initially expand, so I’m narrowing by directory and targeted searches instead of assuming those paths are empty.The expected `014-routing-benchmark` directory is present but currently empty except an empty `design-interface/` child; no benchmark file is available there to cite. I’ll record that as a corpus gap and use the user-provided Mode A score as an operator-supplied datum, clearly labeled.There is older lineage material from the reference-expansion run that specifically covered md-generator. I’m using it as prior research, but I’ll re-check against the live skill because some proposals may already be implemented or ruled out.The live md-generator has already incorporated several earlier recommendations: authoring-boundary and source-of-truth router now exist, `--fast` interaction/default-output drift is corrected in the nested path, and v3 deterministic emitters are wired. The improvement focus is shifting from “add missing boundary docs” to routing discoverability, UX wrappers around the pipeline, schema/playbook consistency, and a non-SaaS exemplar.The lineage artifact root exists and contains only an empty `.executor-state/`. I’ll initialize canonical deep-research files there without altering that executor state directory.The declared spec subfolder exists only as a research lineage path; `spec.md`, `plan.md`, `tasks.md`, and `resource-map.md` are absent at that subfolder level. I’ll record `folder_state: no-spec` in the lineage outputs and continue, because the requested artifact root is present and writable.Phase init and evidence gathering are complete. I’m going to write a converged 6-iteration lineage: iteration evidence files, append-only JSONL state, strategy, registry, dashboard, synthesis, and resource map, all under the requested `gpt55fast` artifact root.One tooling inconsistency surfaced: `backend/README.md` documents `backend/package.json`, but the live nested backend path does not contain that file. I’ll include it as an efficiency/setup risk rather than trying to fix it in this research-only lineage.Confirmed: the nested md-generator backend has `package-lock.json` but no `package.json`, despite setup docs requiring `cd backend && npm install` and the backend README listing `package.json`. That makes setup and `npm test` commands non-actionable unless another package file is restored or commands are re-routed.Writing lineage artifacts now. This is research-only output: no skill code, docs, benchmark files, or spec files outside the `gpt55fast` artifact root will be changed.Lineage files are written. I’m verifying the artifact set and JSON/JSONL parseability now.The expected file set is present. I’m doing a final machine parse of JSON/JSONL state and a scoped diff check before returning the requested completion marker.JSON/JSONL validation passed. One final scoped diff check is running to confirm only the requested lineage files were added.`git diff` does not list untracked files, so I’m using a scoped `git status` check for the artifact root.Scoped status shows only the requested `gpt55fast/` lineage root as untracked.FANOUT_LINEAGE_COMPLETE:gpt55fast