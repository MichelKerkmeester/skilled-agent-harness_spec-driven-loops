Act as a senior research convergence + critic agent. READ-ONLY MODE: do NOT write or edit files. Your final message IS the deliverable — return it in the exact markdown skeleton at the end.

# Goal
This is iteration 5 (final convergence) of a deep-research effort to design a new OpenCode/Claude-Code skill `mcp-figma` (PRIMARY = silships figma-cli; OPTIONAL = official Figma Dev Mode MCP via Code Mode). Four prior iterations are complete. Your job: cross-check them for contradictions/gaps, then converge on a recommended PHASED implementation plan, a risk register, success criteria, and the open questions that need a human decision.

# READ these prior-iteration findings (use the Read tool; --dir is repo root)
- .opencode/specs/design/003-mcp-figma-with-direct-cli-support/001-figma-cli-and-mcp-research/research/iterations/iteration-001.md  (figma-cli capabilities)
- .opencode/specs/design/003-mcp-figma-with-direct-cli-support/001-figma-cli-and-mcp-research/research/iterations/iteration-002.md  (Figma MCP + Code Mode)
- .opencode/specs/design/003-mcp-figma-with-direct-cli-support/001-figma-cli-and-mcp-research/research/iterations/iteration-003.md  (skill architecture blueprint)
- .opencode/specs/design/003-mcp-figma-with-direct-cli-support/001-figma-cli-and-mcp-research/research/iterations/iteration-004.md  (install/safety/lifecycle)

# Phasing precedent to mirror (read for the layout pattern)
- The sibling skill mcp-open-design was built under a phase-parent spec (spec 150) whose layout is: phase parent holds only spec.md + description.json + graph-metadata.json; phase children hold the heavy docs. Its children were: 001 research, then implementation phases. Read .opencode/skills/mcp-open-design/ to see the SHIPPED structure (SKILL.md, README.md, references/{od_cli_reference,mcp_wiring,tool_surface}.md, feature_catalog/, manual_testing_playbook/, changelog/, graph-metadata.json).

# Tentative phasing to critique/improve (the orchestrator proposed this — challenge it)
- 151 = phase parent (spec.md + description.json + graph-metadata.json only). Children:
  - 001 = figma-cli + MCP research (DONE — this research)
  - 002 = SKILL.md scaffold + smart router (CLI-primary, optional-MCP)
  - 003 = references (figma_cli_reference.md, tool_surface.md, mcp_wiring.md, code_mode_integration.md, troubleshooting.md)
  - 004 = INSTALL_GUIDE.md + scripts (install.sh, doctor.sh, connect-safe.sh, connect-yolo.sh, daemon.sh, unpatch.sh, print-utcp-snippets.sh)
  - 005 = README + feature_catalog + manual_testing_playbook
  - 006 = registration (descriptions.json, graph-metadata schema-2 + reciprocal sibling edges, skill-advisor keywords) + validate.sh --strict
  - 007 (optional, gated) = live install of figma-cli + optional Code Mode MCP wiring + live verification

# Questions to answer
1. CONTRADICTIONS/GAPS: any conflicts across iterations 001-004? Any missing research (something a builder would need that no iteration covered)? Be specific.
2. PHASING: critique the tentative 7-phase plan. Is the granularity right (too many/few phases)? Should any phases merge or split? Are dependencies/order correct? Propose the FINAL recommended phase list with, per phase: scope (1 line), key deliverables, dependencies, and rough doc-level (spec-kit Level 1/2/3).
3. RISK REGISTER: top risks (e.g. npm-vs-repo binary mismatch, yolo-patch reversibility, official-MCP http-transport unproven, catalog/OAuth gate, doc-drift in figma-cli, design-judgment scope creep). For each: likelihood, impact, mitigation, and which phase owns it.
4. SUCCESS CRITERIA: what does "mcp-figma is done and correct" mean? A concrete acceptance checklist for the whole skill.
5. OPEN QUESTIONS FOR THE HUMAN: the decisions only the operator can make before/at build (e.g. should phase 007 live-install run now or be deferred? support official MCP via http or only mcp-remote bridge? keep the existing Framelink `figma` Code Mode manual as the documented default or switch to official? skill version start at 1.0.0?). List them crisply as questions.

# Output skeleton (return EXACTLY this)
## IT5 — convergence, phasing, risks
### Cross-iteration contradictions & gaps
### Recommended phased plan (final) — per phase: scope / deliverables / deps / level
### Risk register (risk / likelihood / impact / mitigation / owning phase)
### Whole-skill success criteria (acceptance checklist)
### Open questions requiring a human decision
### Confidence + residual unknowns

If a read fails, say so. Prefer concrete recommendations over hedging; where you recommend, say what you'd do and why.
