# Iteration 028 — Track C (deepseek)

## Focus
Track-C consolidated change set (tiered doc/prompt/routing)

## Findings
1. **[P0] C-CONDITIONAL-PHILOSOPHY** — Make named principles (format-spec §1 line 122-128), depth philosophy (§9 line 544), motion philosophy (§10 lines 589-592), and comparative framing (§1 line 130) CONDITIONAL ON EVIDENCE with ABSENT-stamp fallback. Currently these clauses are unconditional — the model MUST fabricate when tokens.json has no backing data. TIER-1. DOC-PROSE.
   - Recommendation: design_md_format.md:§1 line 122-128: prefix 'Named design principles' with 'CONDITIONAL ON EVIDENCE — only include when ≥3 distinct token values support each principle. When absent, stamp: **Design Principles:** ABSENT — insufficient extraction data.' Same pattern for §9 line 544: change 'Named principle — state the depth philosophy upfront...' to 'Named principle — REQUIRED ONLY when shadow/gradient/depth tokens exist. When absent, stamp: **Depth philosophy:** ABSENT — no depth tokens extracted.' §10 lines 589-592: replace fabrication-prone fallback with OBSERVED/RECOMMENDED boundary (see C-FMT-MOTION). §1 line 130: prefix comparative framing trigger with 'CONDITIONAL ON EVIDENCE.' design_md_prompt_template.md:§2 TEMPLATE: add CARDINAL RULE: 'Named principles, philosophy paragraphs, and comparative framing are CONDITIONAL: include only when directly grounded in at least one measurable token value from tokens.json. When no backing data exists, stamp the section ABSENT rather than fabricating.'
   - Evidence: iteration-006.md:7-8 (C3: mandatory-philosophy solicits fabrication); iteration-024.md:13-15 (named-principle SCOPED — evidence-gate, don't abolish); design_md_format.md:§1 lines 122-130, §9 line 544, §10 lines 589-592; design_md_prompt_template.md:§2 lines 40-53
2. **[P0] C-SKILL-NEVER-QUALITATIVE** — SKILL.md §4 NEVER rules (lines 297-304) prohibit only quantitative fabrication (hex, pixel, font-weight, shadow, radius). Zero language prohibits asserting design relationships, causal claims, philosophical interpretations, named principles, or comparative claims about other systems — all equally hallucinatory and unfalsifiable against tokens.json. The Cardinal Fidelity Rule (line 240) covers only quantitative values. TIER-1. DOC-PROSE.
   - Recommendation: SKILL.md §4 NEVER rules: add NEVER #6 after line 304: 'NEVER assert a design relationship, causal explanation, named principle, design philosophy, comparative claim about other systems, or interpretive narrative that cannot be traced directly to a specific measurable value in tokens.json. If tokens.json contains only raw CSS values without intent/rationale, stamp those qualitative sections ABSENT rather than fabricating the WHY.' This creates a single testable rule: every qualitative claim must cite at least one supporting token key.
   - Evidence: iteration-006.md:10-12 (C1: missing anti-fabrication clause in SKILL.md NEVER rules); SKILL.md:§4 lines 240, 289, 297-304; iteration-024.md:12 (focus-consistent is REAL but is a code/extract bug — not Track-C doc scope)
3. **[P0] C-ADD-AP29** — Anti-pattern AP-29 Interpretive Fabrication is absent from anti_patterns.md. The 28 existing anti-patterns include AP-25 Unnamed Principles and AP-26 Convention Assumption which actively ENFORCE fabrication by defining the ABSENCE of fabricated narrative as an error. No anti-pattern covers asserting design relationships, causal intent, or qualitative consistency not present in tokens.json. TIER-1. DOC-PROSE.
   - Recommendation: anti_patterns.md: append AP-29 after AP-28 (after line 721): '### AP-29: Interpretive Fabrication. Description: Asserting a design relationship, causal explanation, design intent, named principle, philosophical meaning, or comparative claim about other systems that is not present in tokens.json. Bad: "The gradient creates atmospheric depth by layering cool navy tones over midnight undertones, establishing a premium, technical feel." — tokens.json contains only `linear-gradient(135deg, #1a1a2e, #16213e)`; the interpretive layer is invented. Correct: state the value and its source token key; do not add interpretive intent, mood descriptors, or design rationale. Detection: for every qualitative claim, check: does this claim cite at least one specific token key from tokens.json?' Re-scope AP-25 and AP-26 to note they apply only when token data IS present.
   - Evidence: iteration-006.md:19-35 (C9: AP-29 Interpretive Fabrication detailed with two canonical examples); anti_patterns.md:700-755 (AP-28 at line 721, summary checklist at line 724)
4. **[P0] C-FMT-MOTION** — design_md_format.md §10 Motion System fallback (lines 586-592) models INVENTION as acceptable output: 'When implementing, consider adding subtle transitions (150ms ease for hover states, 300ms for reveals) to prevent visual jarring.' This intermixes one factual observation with unsolicited recommendation, erasing the boundary between extracted ground-truth and invented advice. TIER-1. DOC-PROSE + PROMPT-TEMPLATE.
   - Recommendation: design_md_format.md §10 lines 586-592: replace the blended fallback with hard-labeled blocks: '### OBSERVED: No transitions, animations, or motion tokens were detected in the extraction data. The site relies on instant state changes. ### RECOMMENDED: When implementing, respect `prefers-reduced-motion`. If motion is later added, a starting point is 150ms ease for hover feedback and 200-300ms ease-out for reveals — but these values were NOT measured from the source and must be validated against design intent.' design_md_prompt_template.md §2: add rule that all RECOMMENDED blocks carry disclaimer 'NOT measured from source.'
   - Evidence: iteration-013.md:7-19 (C4: Motion fallback blurs OBSERVED vs RECOMMENDED — template-level root cause); design_md_format.md:§10 lines 586-592; design_md_prompt_template.md:§2 lines 40-53
5. **[P0] C-SKILL-ESCALATE-ABSENT** — SKILL.md §4 ESCALATE IF (lines 305-311) covers extract failures, dark-mode ambiguity, L2/L3 boundary straddlers, and extractor-bug hex mismatches — but has NO path for when required philosophy/narrative sections have zero backing token data. The prompt template also has no 'if absent, stamp ABSENT' fallback. The model fabricates because 'I have no data for this required section' is not a supported output path. TIER-1. DOC-PROSE + ROUTING.
   - Recommendation: SKILL.md §4 ESCALATE IF: add item 6 after line 311: 'ESCALATE IF a required section (§1 named principles, §6 depth philosophy, §6.5 motion philosophy) lacks any backing token data in tokens.json. Offer to either: (a) stamp the section ABSENT with a note that extraction captured no data for this domain, or (b) manually provide the design rationale.' design_md_format.md §22 Global Formatting Rules: add sub-rule: 'Philosophy and narrative sections: when no backing token data exists, stamp the subsection ABSENT rather than fabricating. Heading must appear; content is a single line: **ABSENT** — extraction captured no [domain] data.' design_md_prompt_template.md §2 TEMPLATE: add rule: 'Sections requiring philosophy/narrative prose are CONDITIONAL ON EVIDENCE. When tokens.json lacks data, write the heading then stamp: **ABSENT** — no [domain] data captured. Do not fabricate.'
   - Evidence: iteration-006.md:36-38 (C10: escalate-if and stamp-ABSENT for missing backing tokens); SKILL.md:§4 lines 305-311; design_md_prompt_template.md:§2 lines 33-57; design_md_format.md:§22
6. **[P0] C-ROUTE-CARDINAL-GATE** — cardinal_rules_card.md is documented as 'a pre-validate self-check' (SKILL.md §5 line 341) but is ABSENT from §2 Resource Loading Levels table (lines 99-107). It is not loaded under ALWAYS, EXTRACT_WRITE, VALIDATE, or any conditional tier. The prompt template inlines six cardinal rules manually (duplicated from the card), creating drift risk. The card is used post-hoc when it should be a pre-write constraint injected into generation. TIER-1. ROUTING.
   - Recommendation: Three coordinated edits: (1) SKILL.md §2 Resource Loading Levels table line 103: add `assets/cardinal_rules_card.md` to the ALWAYS row. (2) SKILL.md §2 RESOURCE_MAP line 132: add `assets/cardinal_rules_card.md` to EXTRACT_WRITE array. (3) design_md_prompt_template.md §2 line 39 (before 'CARDINAL RULES'): insert: 'Read assets/cardinal_rules_card.md — the authoritative fidelity contract. Every rule in that document is non-negotiable and takes precedence over any inline summary.' Then reduce inline rules to a 1-line summary referencing the card.
   - Evidence: iteration-013.md:20-25 (C5: cardinal_rules_card not a pre-write gate); SKILL.md:§2 lines 99-107, 132-134; SKILL.md:§5 line 341; design_md_prompt_template.md:§2 lines 40-53; cardinal_rules_card.md:§2 lines 31-44
7. **[P0] C-FMT-SECTIONS-DEMOTE** — Section count inflated: format spec §1 table (lines 27-48) marks 15 sections as required, but all four gold-standard examples produce only 9-10 sections (Vercel 10, Stripe 9, Linear 9, Supabase ~9). Sections 0 (Brand Context), 7 (Content & Voice), 11 (State Matrix), 12 (Iconography) are frequently absent in practice — their 'required' designation forces fabrication when token data is absent. Verified REAL by iteration-024. TIER-1. DOC-PROSE.
   - Recommendation: design_md_format.md §1 table (lines 27-48): demote §0 Brand Context, §7 Content & Voice, §11 State Matrix, §12 Iconography from 'Required: yes' to 'Required: conditional (data-driven — only when corresponding tokens exist in tokens.json)'. Also change §6.5 Motion System from 'yes' to 'conditional (data-driven — motion.durations.length > 0)'. Nominal section count becomes '10 core + 5 conditional' — honest about capability. This eliminates the header-enforced fabrication pressure for data-poor sections.
   - Evidence: iteration-018.md:22-24 (section count inflation — spec demands 17, examples produce 9-10); iteration-024.md:19-21 (cut 17→9 — REAL, verified across 4 gold-standard examples); iteration-013.md:57-74 (C12: checkSectionCompleteness is header-only, not data-driven); design_md_format.md:27-48
8. **[P0] C-CONTRADICTION** — Direct contradiction: style guide §10 Information Density (line 347) states 'A paragraph with zero values is an opinion. A paragraph with one value is an observation. A paragraph with 2-4 values is documentation.' This is the deletion test — prose without measurable values should be deleted. The format spec simultaneously MANDATES philosophy paragraphs (Depth §9, Motion §10, Visual Theme §1 named principles) that routinely contain zero token-extractable values. Both docs are ALWAYS-loaded. TIER-1. DOC-PROSE.
   - Recommendation: Amend writing_style_guide.md §10 line 347: add exception clause after the deletion test: 'Exception: Required philosophy/narrative sections (§1 named principles, §6 depth philosophy, §6.5 motion philosophy) use an EVIDENCE-BACKED test instead of a value-count test: each claim must cite at least one specific token key from tokens.json. A paragraph with zero token citations is still opinion — stamp ABSENT.' This eliminates the contradiction by making both docs agree that philosophy requires evidence.
   - Evidence: iteration-006.md:13-15 (C11: style-guide/format-spec contradiction); writing_style_guide.md:§10 line 347; design_md_format.md:§9 line 542, §10 lines 589-592, §3 lines 122-130
9. **[P1] C-STYLE-FABRICATE-GATE** — writing_style_guide.md is ALWAYS-loaded and is the most dangerous resource for fabrication: §15 Comparative Framing Pattern provides sentence templates claiming what 'most systems' do (knowledge the model doesn't have from a single-site crawl); §16 Named Principle Pattern declares 'Unnamed decisions get forgotten' with 10+ naming examples; §17 Frequency Interpretation Pattern frames raw frequency as 'noise' and solicits 'narrative of intent'; §18 Intent Narration Pattern explicitly bridges 'the gap between WHAT a system does and WHY.' These four sections actively solicit interpretive invention beyond tokens.json. TIER-2. DOC-PROSE.
   - Recommendation: Prepend a FABRICATION GATE preamble to writing_style_guide.md (before §15 line 529): 'GATE: ALL named principles, comparative framing claims, intent narratives, and frequency interpretations in this document's following sections (§§15-18) are guidance for WHEN evidence exists. They MUST cite at least one specific token key from tokens.json as the basis for every claim. When tokens.json provides raw values but no design rationale, document the values in their reference format and stamp qualitative sections ABSENT. Never assert what another design system or convention does without a reference to extracted data.' This neutralizes the fabrication-incentivizing passages without removing legitimate prose-quality guidance.
   - Evidence: iteration-006.md:16-18 (C2: writing_style_guide.md is the primary fabrication driver); writing_style_guide.md:§15 lines 529-586, §16 lines 589-631, §17 lines 634-661, §18 lines 664-691; SKILL.md:§2 Resource Loading Levels line 103 (ALWAYS row)
10. **[P1] C-ROUTE-FIDELITY-AUDIT** — The smart router (§2) has four intents: EXTRACT_WRITE, VALIDATE, REPORT, STUDY. VALIDATE checks hex accuracy and section structure — purely structural. There is no intent for auditing PROSE fidelity: whether descriptive claims are grounded in observed evidence or fabricated. A FIDELITY_AUDIT intent would load prose-checking resources without requiring re-extraction. This is distinct from VALIDATE because it inspects narrative claims, not structural conformance. TIER-2. ROUTING.
   - Recommendation: SKILL.md §2 SMART ROUTING: (1) INTENT_MODEL after line 129: add 'FIDELITY_AUDIT' with keywords [(fidelity,4), (audit,4), (prose check,4), (claim verification,4), (fabrication,4), (anti-pattern,3), (deep audit,4)]. (2) RESOURCE_MAP after line 139: add 'FIDELITY_AUDIT' with resources [design_md_format.md, writing_style_guide.md, anti_patterns.md, cardinal_rules_card.md]. (3) Resource Loading Levels table after line 107: add row 'CONDITIONAL | FIDELITY_AUDIT intent | writing_style_guide.md, anti_patterns.md, cardinal_rules_card.md'.
   - Evidence: iteration-013.md:37-49 (C7: No first-class fidelity-audit intent — distinct from generic VALIDATE); SKILL.md:§2 lines 99-139
11. **[P1] C-PROMPT-SENTINELS** — Neither the prompt template nor the current cardinal rules require per-claim source traceability. An inline source sentinel mechanism (e.g., `<!-- src: tokens.colorTokens[3].hex -->`) would make individual claims auditable within the document and survive transport. stripHtmlComments() already exists at validate.ts:51-53, so stripping is a working path. Iteration-024 verified this approach is clean (SCOPED, adopt source sentinels as structural provenance check). TIER-2. PROMPT-TEMPLATE.
   - Recommendation: design_md_prompt_template.md §2 CARDINAL RULES: add rule: 'Every non-obvious value claim must include a strippable source sentinel as an HTML comment: <!-- src: tokens.colorTokens[3].hex -->, <!-- src: tokens.fontInfo.fontFaces[0].family -->, <!-- src: tokens.motion.durations[0].value -->. These are stripped before delivery via the existing stripHtmlComments path in validate.ts.'
   - Evidence: iteration-013.md:50-56 (C8: No source-traceable sentinels — prefer inline over sidecar); iteration-024.md:7-9 (checkProseClaims SCOPED — source sentinels clean, should be separate structural check); validate.ts:51-53 (stripHtmlComments already exists)

## Questions Answered
- Which format-spec clauses unconditionally demand philosophy? §§1, 6 (Depth), 6.5 (Motion) — all three require named principles, philosophy paragraphs, or comparative framing regardless of token data presence (iter-006 C3, confirmed by iter-024 as SCOPED: evidence-gate, don't abolish).
- Is the style-guide §10 deletion test contradicted by the format spec's mandatory philosophy? YES — the style guide says zero-value paragraphs are 'opinion' to be deleted; the format spec demands paragraphs that routinely contain zero token values. Both are ALWAYS-loaded. Resolution: amend style guide to accept evidence-backed claims; amend format spec to require evidence (iter-006 C11).
- Does cutting 17 sections to ~9 core meaningfully reduce fabrication pressure? YES — iter-024 confirmed REAL: all 4 gold-standard examples produce 9-10 sections; the validator enforces 15 headers but 5 have zero backing token data, forcing fabrication. Demoting 0/7/11/12 to conditional eliminates header-enforced fabrication pressure.
- Is the focus-consistent bug a Track-C concern? NO — it is an extract/code bug at a11y-extract.ts:101-103 (returns consistent:true on empty interactions). Confirmed REAL by iter-024 but outside Track-C doc/prompt/routing scope.
- Should named principles be abolished entirely? NO — iter-024 explicitly scoped this: evidence-backed principles (e.g., 'shadow-as-border' with real frequency data) are the highest-value prose in a design doc. The fix is evidence-gating, not removal.

## Questions Remaining
- RESERVED: emergent angles/risks (permanently open)
- Implement the ABSENT-stamp mechanism end-to-end: apply to all 17 sections in format spec, wire into prompt template cardinal rules, and add to ESCALATE IF routing path
- Audit the four gold-standard examples (stripe/vercel/linear/supabase DESIGN.md + tokens.json pairs) for: (a) how many named principles actually trace to token keys, (b) how many atmosphere claims have ≥3 backing token values, (c) whether any vibe-paragraph claims contradict observed UI text
- Trace AP-25 (Unnamed Principles) and AP-26 (Convention Assumption) anti-patterns — they currently ENFORCE fabrication solicitation by defining the ABSENCE of fabricated narrative as an error. Should they be rescoped or retired?
- Re-scope cardinal rules numbering: should the anti-fabrication rule read as rule 1 (strongest priming position) rather than appended?
- Benchmark the prompt template with evidence-gated philosophy vs. current unconditional prompt against the same tokens.json to measure fabrication rate per section

## Next Focus
- Implement the ABSENT-stamp mechanism end-to-end: apply to all 17 sections in format spec, wire into prompt template cardinal rules, and add to ESCALATE IF routing path
- Audit the four gold-standard examples (stripe/vercel/linear/supabase DESIGN.md + tokens.json pairs) for: (a) how many named principles actually trace to token keys, (b) how many atmosphere claims have ≥3 backing token values, (c) whether any vibe-paragraph claims contradict observed UI text
- Trace AP-25 (Unnamed Principles) and AP-26 (Convention Assumption) anti-patterns — they currently ENFORCE fabrication solicitation by defining the ABSENCE of fabricated narrative as an error. Should they be rescoped or retired?
- Re-scope cardinal rules numbering: should the anti-fabrication rule read as rule 1 (strongest priming position) rather than appended?
- Benchmark the prompt template with evidence-gated philosophy vs. current unconditional prompt against the same tokens.json to measure fabrication rate per section

## Summary
Making named principles, philosophy paragraphs, and comparative framing CONDITIONAL ON EVIDENCE across both the format spec (design_md_format.md §§1,9,10) and the prompt template, with an ABSENT-stamp fallback and a qualitative fabrication prohibition in SKILL.md's NEVER rules, is the single highest-leverage Track-C change — it removes the unconditional fabrication demand at its root while preserving evidence-backed design narration.
