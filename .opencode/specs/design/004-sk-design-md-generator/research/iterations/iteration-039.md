# Iteration 039 — Track CR (deepseek)

## Focus
Completeness critic — gap list before final synthesis

## Findings
1. **[P0] gap** — ZERO EMPIRICAL VALIDATION — 36 iterations produced ~823 analytical findings but NO experiment, A/B test, prototype, or measurement was ever executed against real data (stripe/vercel/linear/supabase tokens.json or DESIGN.md). The six most-repeated open questions (deltaE threshold test, A/B prompt test, formatters.ts prototype, coverage-election quantification, source-sentinel prototype, prompt-template ABSENT audit) appear as 'Questions Remaining' in 8+ iterations each but were never acted on. The research is 100% analytical with zero empirical ground-truth confirmation.
   - Recommendation: Run a dedicated EMPIRICAL CLOSING WAVE (iters 40-44): (1) deltaE<10 vs deltaE<3 on all 4 gold-standard examples + anobel — measure color palette delta; (2) prototype formatters.ts against stripe/tokens.json — measure % lines replaced + hex fidelity; (3) A/B test evidence-gated vs mandatory-philosophy prompt on 2 examples; (4) implement+quantify coverage-election pre-gate; (5) audit design_md_prompt_template.md for ABSENT-fallback gaps. Without empirical closure, synthesis rests on untested assertions.
   - Evidence: strategy.md contains 50+ carried-forward questions; dashboard shows 0/7 key questions answered; iteration-036 explicitly confirms zero prototypes exist (formatters.ts, dtcg-wrap.ts, emit-css.ts are all 'remaining')
2. **[P0] gap** — PROMPT TEMPLATE ABSENT-FALLBACK AUDIT NEVER EXECUTED — The audit of design_md_prompt_template.md for 'must include' clauses lacking 'when absent, stamp ABSENT' is the most-repeated open question (iter-024, 026, 027, 028, 029, 031, 034, 036). The format spec (design_md_format.md) has been analyzed, but the WRITE-phase prompt template that the LLM actually receives has NOT been audited for fabrication-forcing clauses. This is a critical blind spot: the prompt is the direct fabrication driver.
   - Recommendation: Iteration 40: Execute the prompt template audit. Read design_md_prompt_template.md line-by-line, catalog every clause that demands content when backing tokens may be absent, cross-reference with the 17 DESIGN.md sections, report which clauses lack ABSENT fallback.
   - Evidence: carried-forward questions list (strategy.md lines 198, 205, 221, 231, 243, 252); iter-036 line 48 'audit design_md_prompt_template.md...the plan identifies format spec mandates but the prompt template may have additional fabrication-forcing clauses'
3. **[P0] gap** — UN-AUDITED SCRIPTS: icon-detect.ts, framework-detect.ts, dark-mode-detect.ts, and crawl.ts viewport logic were NEVER directly inspected in any iteration. icon-detect.ts appears once in iter-002 as a brief note about §12 icon-to-text gap but the script internals (library detection accuracy, false-positive surfaces, fabricated icon claims) are unexamined. framework-detect.ts has ZERO coverage across all 36 iterations. dark-mode-detect.ts and crawl.ts viewport handling are only discussed through their output interfaces, never through their source. These are potential fabrication-root enablers.
   - Recommendation: Iteration 41: Audit icon-detect.ts, framework-detect.ts, dark-mode-detect.ts, and crawl.ts for fabrication-enabling patterns (default values on empty data, heuristic fallthroughs, unguarded claims in output). An audit of each script's 'zero-data' return path — parallel to the focus-indicator bug pattern discovered in a11y-extract.ts:101-103.
   - Evidence: Grep for 'icon-detect' returns only iter-002 (surface mention); 'framework-detect' returns zero matches across all 36 iterations; 'dark-mode-detect' returns zero matches; 'crawl.ts' returns zero direct mentions; the pattern in a11y-extract.ts:101-103 (returns fabricated default on empty input) was independently confirmed as still-present by iter-024/036 — same class of bug may exist in un-audited scripts
4. **[P0] gap** — WRITE-PHASE PROMPT INTERNALS UNEXAMINED — The research extensively catalogs format spec and style guide clauses that solicit fabrication, but the ACTUAL WRITE-phase prompt (the instruction the AI receives to generate DESIGN.md from tokens.json) has never been studied. How does the current prompt instruct the AI about thin token sets? Does it say 'write content for all sections' or 'skip empty sections'? The generate-design-md.ts or equivalent prompt-construction logic is never inspected.
   - Recommendation: Iteration 42: Trace the WRITE-phase prompt construction — find where tokens.json is transformed into the AI prompt, audit the system message and section-generation instructions for fabrication-forcing language, compare against the format spec's theoretical requirements.
   - Evidence: No iteration references generate-design-md.ts or prompt-construction code; the SKILL.md §2 WRITE phase description is discussed in routing terms only (iter-028, 030) but the actual prompt text that reaches the LLM is never inspected
5. **[P1] thin** — FORMATTERS.TS ARCHITECTURE UNVERIFIED — The deterministic renderer (iter-030's centerpiece recommendation) partitions 17 sections into DETERMINISTIC/GROUNDED/BOUNDED-PROSE classes and claims it eliminates value fabrication for ~60% of sections. This is the second-highest-ROI recommendation (iter-029) but has NEVER been prototyped against real tokens.json. No benchmark exists for: (a) exact % of DESIGN.md lines that can be replaced, (b) hex/weight/shadow value accuracy improvement, (c) whether token-to-table mapping is 1:1 or requires judgment. The architecture claim is entirely untested.
   - Recommendation: Iteration 43: Prototype formatters.ts minimum-viable (Phase A: §2 Color + §3 Typography only) against stripe/tokens.json. Measure: (a) lines auto-generated vs AI-written, (b) value accuracy delta (how many fabricated fontSize/weight/lineHeight currently slip through), (c) token→table mapping fidelity.
   - Evidence: iter-030 lines 38-49 list formatters.ts prototype as remaining; iter-031 lines 96-109 repeat; iter-034 line 64 confirms it's never been done; iter-036 lines 48-49 flag this as unverified
6. **[P1] thin** — TRACK B THIN ON CLASSIFY-VARIANT/CARD-GEOMETRY FIX VERIFICATION — The 14 Track-B fixes (iter-027) identify real bugs with file:line evidence, but the interaction-data-propagation fixes (item 22: variant classification, card geometry, shadow dedup, hidden-element skew, motion multi-page) are deferred to P1 and have no adversarial verification equivalent to what iter-024 did for Track-A P0s. Specifically: the findInteraction key fix may introduce cross-type false matches, the deltaE<10 may merge distinct brand colors, and the coverage-election <0.3 gate could demote legitimate tokens on multi-page sites. None of these regressions have been tested.
   - Recommendation: Iteration 44: Adversarial verification of the highest-risk Track-B fixes — (a) test findInteraction fix against a known component-rich site to measure false-positive interaction matches, (b) test deltaE<10 against all 4 gold-standard examples to measure color token count delta and lost brand colors, (c) test coverage-election pre-gate against 2+ page extractions to measure L2→L3 demotion count for legitimate infrastructure tokens.
   - Evidence: iter-027 items 5-14 are unverified; iter-036 items 9-10 confirm regression risks exist but no tests run; the anobel example is cited as critical test target but never used
7. **[P1] gap** — UNSURVEYED TOOL CLASSES — (1) W3C DTCG resolver/validator tooling ecosystem: the research adopts DTCG as a concept but never surveys the actual DTCG tooling (format validators, community resolvers, Tokens Studio DTCG implementation details, the DTCG format tools repo). (2) Screenshot-diff visual regression (Percy, Chromatic, BackstopJS): these are the industry standard for detecting visual design drift — never mentioned. (3) ML/perceptual approaches: ML-based color extraction, perceptual hashing for design element detection, neural network approaches to design token classification — never mentioned. The external survey claims 'COMPLETE' (iter-034 line 57) but misses these classes.
   - Recommendation: Accept-open for DTCG tooling (low marginal value — the W3C spec alone is sufficient for adoption planning). Accept-open for ML/perceptual (scope beyond 'harden against fabrication'). Iteration 44b: brief survey of screenshot-diff visual regression tools (Percy, Chromatic) — they directly address the question 'has this design changed since extraction?' and complement proof.ts pixel-sampling. 1 hour of research, not a full iteration.
   - Evidence: iter-034 line 57 claims survey COMPLETE; grep for 'Percy|Chromatic|Backstop|screenshot diff|visual regression|ML|perceptual hash' returns zero matches across iterations
8. **[P1] contradiction** — INCONSISTENT SECTION DEMOTION LIST — iter-028 item 7 says demote §0, §7, §11, §12 plus make §6.5 conditional 'yes→conditional (data-driven — motion.durations.length > 0)' = 5 sections demoted. iter-029 item 1 says demote §0, §7, §9, §11, §12 = 5 sections but INCLUDES §9 (Accessibility) and EXCLUDES §6.5 from the demotion list, while keeping §6.5 conditional separately. The two consolidated track outputs disagree on whether §9 is always-required or conditional. This matters: if §9 is always-required but interaction capture remains default-OFF, fabrication is structurally guaranteed for that section.
   - Recommendation: Reconcile in iteration 40 alongside the prompt audit: explicitly define which sections are conditional and which are always-required, cross-referencing the interaction-ON default change. If interaction capture is default-ON, §9 can remain required; if not, §9 MUST be conditional.
   - Evidence: iter-028 line 26: demote §0,§7,§11,§12,§6.5 (5 sections, does NOT include §9); iter-029 line 7-8: '5 required sections (§0 Brand Context, §7 Content & Voice, §11 State Matrix, §12 Iconography, §9 Accessibility)' includes §9 and excludes §6.5
9. **[P1] gap** — INTERACTION CAPTURE COST ESTIMATE ERROR NEVER CORRECTED IN PLAN — iter-031 line 34 states 'interaction capture adds ~5min worst-case per page (PER_ELEMENT_TIMEOUT=6s × MAX_ELEMENTS=50)' — this is wrong. iter-036 line 25-26 corrects: PAGE_TIMEOUT=30000ms hard-caps per-page cost at 30s, not 5min. The incorrect 5min estimate may have influenced the original decision to default interaction OFF and the risk assessment in integrated plan iter-031. The plan text still carries the wrong number.
   - Recommendation: Correct the '~5min per page' claim to '≤30s per page (PAGE_TIMEOUT cap)' in the integrated plan (iter-031) and in any synthesis document. Re-evaluate whether this error weakened the case for default-ON interaction capture.
   - Evidence: iter-031 line 34 '~5min worst-case per page'; iter-036 lines 25-27 identifies the error and cites interaction-capture.ts:51-53,740 showing PAGE_TIMEOUT=30000
10. **[P2] gap** — LOC CLAIM STILL OVERSTATED — iter-036 found the '~335 LOC' minimal-viable hardening claim is ~550 LOC when individual estimates are summed (a 64% undercount). iter-036 recommended re-baselining to ~430 LOC but no iteration has actually re-baselined the plan or synthesis. The ~335 number is cited in the dashboard and iter-031 summary.
   - Recommendation: Re-baseline in synthesis doc. Accept minor overcount as a cosmetic issue (the scope is correct even if the LOC estimate was wrong). No dedicated iteration needed — correct during final synthesis writing.
   - Evidence: iter-036 lines 7-9 detail the overcount; iter-031 line 90 still uses ~335; dashboard iter-36 summary uses the incorrect number
11. **[P2] gap** — SCOPE CREEP ITEMS — Several recommendations go beyond 'harden against fabrication': (1) MCP token endpoint (iter-029 item 22, P2) — an entirely new feature for agent queryability, (2) tokens.css emission (iter-029 item 12, P1) — a new output artifact, (3) nearest-compliant-shade remediation (iter-029 item 21, P2) — a new remediation feature, (4) DECISIONS + ANTI-PATTERNS output sections (iter-029 item 14, P1) — new DESIGN.md sections. None of these are anti-fabrication. Their presence in the ranked borrow list dilutes focus on the core hardening mission.
   - Recommendation: Accept-open: these are valid feature ideas but should be separated into a 'Phase 2 enhancements' bucket, not mixed with anti-fabrication hardening. Flag in synthesis as out-of-scope additions.
   - Evidence: iter-029 items 12, 14, 21, 22; the research boundaries in strategy.md line 48 explicitly say 'Not shipping changes in this loop' and 'Not researching Figma-only or paid-SaaS internals' — but these items would require shipping new code
12. **[P2] thin** — NO HARD-NUMBER BASELINE FOR FABRICATION RATE — The research asserts fabrication exists (confirmed by iter-024 focus-consistent bug, iter-028 comparative-framing mandate) but never establishes a measured baseline fabrication rate. How many invented claims per DESIGN.md section on average? What % of sentences are interpretive? Without a baseline, the effectiveness of proposed fixes cannot be measured — 'reduction' is asserted, not quantified.
   - Recommendation: Accept-open: a full fabrication audit of 4 gold-standard DESIGN.md files would require manual line-by-line classification (anchored vs interpretive), which is labor-intensive. The research correctly avoids over-quantifying. Flag in synthesis as 'unmeasured but structurally addressed' — the fixes remove fabrication mandates at root rather than patching output.
   - Evidence: iter-006 asks for this audit; iter-028 item 7 confirms it's never been done; no iteration contains a fabrication-rate measurement

## Questions Answered
- D: What do the best external extractors do? ANSWERED — 23 ranked borrow items across iterations 1-34, top 3: data-driven sections (Open Design), deterministic tables (Diez/Supernova), coverage election (design-extract). Survey confirmed COMPLETE for the major design-extraction ecosystem.
- A(partial): What mechanical validator catches prose fabrication? PARTIALLY ANSWERED — banished-word check + source sentinels designed but not implemented. Citation gating designed (iter-030) but deferred to full architecture.
- B(partial): Which extraction gaps force fabrication? MOSTLY ANSWERED — 14 specific bugs identified with file:line evidence (iter-027). Interaction pipeline, a11yAsync dead code, deltaE threshold confirmed real.

## Questions Remaining
- RESERVED: emergent angles/risks (permanently open)
- iter-40: EXECUTE PROMPT TEMPLATE ABSENT-FALLBACK AUDIT — the most-repeated open question. Read design_md_prompt_template.md line-by-line, catalog every 'must include' without fallback. Resolve §9 vs §6.5 demotion inconsistency.
- iter-41: EMPIRICAL CLOSING WAVE PART 1 — Run deltaE<10 threshold change against all 4 gold-standard examples + anobel. Measure color token count delta + brand color loss. Update cluster.test.ts.
- iter-42: EMPIRICAL CLOSING WAVE PART 2 — Prototype formatters.ts Phase A (§2 Colors + §3 Typography) against stripe/tokens.json. Measure auto-generated vs AI-written line ratio, value accuracy delta.
- iter-43: EMPIRICAL CLOSING WAVE PART 3 — A/B test evidence-gated vs mandatory-philosophy prompt on 2 examples. Implement+quantify coverage-election pre-gate against gold-standard tokens.json.
- iter-44: AUDIT UN-INSPECTED SCRIPTS — Audit icon-detect.ts, framework-detect.ts, dark-mode-detect.ts, and crawl.ts for the 'fabricated-default-on-empty-data' bug class (parallel to focus-indicator pattern).

## Next Focus
- iter-40: EXECUTE PROMPT TEMPLATE ABSENT-FALLBACK AUDIT — the most-repeated open question. Read design_md_prompt_template.md line-by-line, catalog every 'must include' without fallback. Resolve §9 vs §6.5 demotion inconsistency.
- iter-41: EMPIRICAL CLOSING WAVE PART 1 — Run deltaE<10 threshold change against all 4 gold-standard examples + anobel. Measure color token count delta + brand color loss. Update cluster.test.ts.
- iter-42: EMPIRICAL CLOSING WAVE PART 2 — Prototype formatters.ts Phase A (§2 Colors + §3 Typography) against stripe/tokens.json. Measure auto-generated vs AI-written line ratio, value accuracy delta.
- iter-43: EMPIRICAL CLOSING WAVE PART 3 — A/B test evidence-gated vs mandatory-philosophy prompt on 2 examples. Implement+quantify coverage-election pre-gate against gold-standard tokens.json.
- iter-44: AUDIT UN-INSPECTED SCRIPTS — Audit icon-detect.ts, framework-detect.ts, dark-mode-detect.ts, and crawl.ts for the 'fabricated-default-on-empty-data' bug class (parallel to focus-indicator pattern).

## Summary
NOT COMPLETE ENOUGH TO SYNTHESIZE. The research has strong analytical coverage across all 4 tracks (823 findings, well-traced to file:line), but has a critical empirical gap: ZERO recommended fixes have been prototyped, tested, or measured against real data. The six most-repeated open questions (deltaE test, A/B prompt test, formatters prototype, coverage-election quantification, source-sentinel prototype, prompt-template ABSENT audit) span 8+ iterations each as 'remaining' but were never executed. Additionally: 4 core scripts (icon-detect.ts, framework-detect.ts, dark-mode-detect.ts, crawl.ts) were never directly inspected; 3 tool classes were un-surveyed (screenshot-diff visual regression, W3C DTCG tooling, ML/perceptual); 2 document-level inconsistencies exist (section demotion list, interaction-capture cost estimate); and ~4 recommendations are scope creep beyond fabrication hardening. A dedicated 5-iteration EMPIRICAL CLOSING WAVE (iters 40-44) must run before synthesis — the research has built the map, but has not walked any of the ground.
