## Agent 07 - Wave 1 Slice A07

- Updated stale README references in the scoped `scripts/` and `shared/` docs to match current module/script behavior.
- Normalized validation command references from `validate-spec.sh` to `spec/validate.sh` in rules and fixture docs.
- Corrected setup and utils docs for current script outputs/options (`FEATURE_DIR`/`AVAILABLE_DOCS`, `--include-tasks`, `--validate-verbose`) and current utility exports (`normalizeInputData`, `validateDataStructure`, 10 modules).
- Updated shared docs to reflect current architecture: `shared/dist/` build output, `normalization.ts` and scoring/utils modules, and active embeddings providers (`voyage`, `openai`, `hf-local`).
- Kept edits within requested scope only.
