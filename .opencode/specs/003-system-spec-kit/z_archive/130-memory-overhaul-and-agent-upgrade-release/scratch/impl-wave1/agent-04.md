# Agent 04 - Wave 1 Slice A04 Notes

- Updated stale module descriptions in 8 lib READMEs to align with post-Spec 126 terminology.
- Added concise references to document-aware behavior (`documentType`, `specLevel`, spec document types) where each module actually intersects indexing/retrieval.
- Kept hardening mentions minimal and relevant (cache invalidation freshness, embedding BM25 fallback continuity).
- Normalized version/date footer in touched README files to `1.8.0` and `2026-02-16`.
