---
title: "Deep Research Strategy: Parent-Hub Routing Out-of-Box Run 3"
description: "Persistent state for seven forced-depth lateral investigations of parent-hub routing."
contextType: research
---

# Deep Research Strategy: Parent-Hub Routing Out-of-Box Run 3

## 1. OVERVIEW

This lineage deliberately questions the routing frame rather than tuning `defaultMode`. Convergence is telemetry only until all seven iterations have completed.

## 2. TOPIC

Parent-hub routing, out-of-box run 3: radical lateral rethinks over the Out-of-Box Agenda in the packet spec.md.

<!-- ANCHOR:key-questions -->
## 3. KEY QUESTIONS (remaining)
- [x] Can Layer 0 safely eliminate hub-local routing while preserving modular packets, local context loading, and explicit authority boundaries?
- [x] What deterministic/adaptive hybrid could learn from routing corrections without making offline replay irreproducible?
- [x] Which no-destination semantics from other routing systems transfer, and what do they imply for abstention, defaults, and failure visibility?
- [x] Can recoverable handoffs or typed dialogue replace one-shot classification without unacceptable latency and context cost?
- [x] What deeper architecture smell does `defaultMode` expose, and what minimal confidence or contract model could replace `INTENT_SIGNALS` plus `RESOURCE_MAP`?

<!-- /ANCHOR:key-questions -->

## 4. NON-GOALS

- Do not change any shipped router, default, benchmark, schema, or packet document.
- Do not re-derive the practical run-1/run-2 verdict as the primary result.
- Do not claim empirical traffic frequencies without an observed corpus.
- Do not treat this lineage as implementation planning.

## 5. STOP CONDITIONS

- Complete exactly seven evidence-bearing iterations, then synthesize with stop reason `maxIterationsReached`.
- Treat earlier convergence as telemetry and broaden the angle instead of stopping.
- Halt only for state corruption, a write-scope violation, or a security-sensitive discovery.

<!-- ANCHOR:answered-questions -->
## 6. ANSWERED QUESTIONS
- Can Layer 0 safely eliminate hub-local routing while preserving modular packets, local context loading, and explicit authority boundaries?
- What deterministic/adaptive hybrid could learn from routing corrections without making offline replay irreproducible?
- Which no-destination semantics from other routing systems transfer, and what do they imply for abstention, defaults, and failure visibility?
- Can recoverable handoffs or typed dialogue replace one-shot classification without unacceptable latency and context cost?
- What deeper architecture smell does `defaultMode` expose, and what minimal confidence or contract model could replace `INTENT_SIGNALS` plus `RESOURCE_MAP`?

<!-- /ANCHOR:answered-questions -->

<!-- MACHINE-OWNED: START -->
<!-- ANCHOR:what-worked -->
## 7. WHAT WORKED
- comparing the canonical hub doctrine with a concrete composite hub and the advisor's only direct-mode projection separated packet containment from routing policy. (iteration 1)
- tracing the existing shadow sink, prompt-safe outcomes, calibration reducer, and projection hash exposed a nearly complete two-plane pattern instead of requiring a hypothetical learner. (iteration 2)
- comparing each system by state and failure-cost semantics, rather than surface vocabulary, separated transferable control mechanisms from decorative analogy. (iteration 3)
- combining SIP's acknowledged referral, A2A's task lifecycle, MCP's schema-bound elicitation, and OAuth's scoped delegation produced one small state machine in which transfer, dialogue, execution, and authority remain distinct. (iteration 4)
- reading the actual confidence and uncertainty formulas before importing calibration vocabulary exposed the score/probability mismatch; selective classification then supplied a precise risk/coverage frame for abstention. (iteration 5)
- starting with field deletion and forcing each candidate through two unlike hubs distinguished duplicated representation from irreducible information. (iteration 6)
- proof-carrying code supplied local verification, Kubernetes supplied dry-run and stale-read rejection, and PostgreSQL exposed the cost of pretending preparation is free. (iteration 7)

<!-- /ANCHOR:what-worked -->

<!-- ANCHOR:what-failed -->
## 8. WHAT FAILED
- broad repository search produced substantial historical and template noise; narrow line-range reads were needed to distinguish current contracts from old assumptions. (iteration 1)
- broad searches mixed runtime shadow scoring, benchmark replay, and unrelated deep-loop replay; narrow source reads were required to separate serving policy, learning telemetry, and CI fixtures. (iteration 2)
- the warm-handoff health-service page failed to open through the browser, so the iteration used accessible Australian government referral guidance and did not depend on the failed page. (iteration 3)
- treating any single protocol as the whole answer failed conceptually—SIP lacks the packet authority model, A2A permits richer history than this router should transfer, MCP governs clarification rather than execution, and token exchange does not define conversational ownership. (iteration 4)
- a universal numeric threshold cannot be justified from contracts or external classifier results because the required router-specific outcome corpus does not exist. (iteration 5)
- the first repository-wide hub summary query assumed the wrong JSON key and failed; a schema-agnostic direct read of the two selected hubs recovered without repeating that call. (iteration 6)
- transaction-wide rollback does not transfer because route targets share no transactional substrate or recovery manager. (iteration 7)

<!-- /ANCHOR:what-failed -->

<!-- ANCHOR:exhausted-approaches -->
## 9. EXHAUSTED APPROACHES (do not retry)
### “Speculate everything, roll back on misroute”: external effects and some generated values are not reproducibly reversible. -- BLOCKED (iteration 7, 1 attempts)
- What was tried: “Speculate everything, roll back on misroute”: external effects and some generated values are not reproducibly reversible.
- Why blocked: Repeated iteration evidence ruled this direction out.
- Do NOT retry: “Speculate everything, roll back on misroute”: external effects and some generated values are not reproducibly reversible.

### A one-field `workflowMode` handoff cannot represent ordered bundles, surfaces, fallback resources, confidence, or defer semantics. Treating it as a complete replacement is a dead end unless the handoff becomes a typed route plan. -- BLOCKED (iteration 1, 1 attempts)
- What was tried: A one-field `workflowMode` handoff cannot represent ordered bundles, surfaces, fallback resources, confidence, or defer semantics. Treating it as a complete replacement is a dead end unless the handoff becomes a typed route plan.
- Why blocked: Repeated iteration evidence ruled this direction out.
- Do NOT retry: A one-field `workflowMode` handoff cannot represent ordered bundles, surfaces, fallback resources, confidence, or defer semantics. Treating it as a complete replacement is a dead end unless the handoff becomes a typed route plan.

### A single-target result with no target role or bundle order. -- BLOCKED (iteration 6, 1 attempts)
- What was tried: A single-target result with no target role or bundle order.
- Why blocked: Repeated iteration evidence ruled this direction out.
- Do NOT retry: A single-target result with no target role or bundle order.

### A solver whose only inputs are requested capability and a mode resource path. -- BLOCKED (iteration 6, 1 attempts)
- What was tried: A solver whose only inputs are requested capability and a mode resource path.
- Why blocked: Repeated iteration evidence ruled this direction out.
- Do NOT retry: A solver whose only inputs are requested capability and a mode resource path.

### A universal “always fail closed” or “always fail open” law is a dead end. The sources instead make the choice conditional on outcome type, destination capability, and the cost of retry versus wrong execution. -- BLOCKED (iteration 3, 1 attempts)
- What was tried: A universal “always fail closed” or “always fail open” law is a dead end. The sources instead make the choice conditional on outcome type, destination capability, and the cost of retry versus wrong execution.
- Why blocked: Repeated iteration evidence ruled this direction out.
- Do NOT retry: A universal “always fail closed” or “always fail open” law is a dead end. The sources instead make the choice conditional on outcome type, destination capability, and the cost of retry versus wrong execution.

### Automatically promoting every bounded calibration proposal: the current proposal contract freezes live weights and requires held-out validation, so automatic promotion would erase an intentional safety boundary. -- BLOCKED (iteration 2, 1 attempts)
- What was tried: Automatically promoting every bounded calibration proposal: the current proposal contract freezes live weights and requires held-out validation, so automatic promotion would erase an intentional safety boundary.
- Why blocked: Repeated iteration evidence ruled this direction out.
- Do NOT retry: Automatically promoting every bounded calibration proposal: the current proposal contract freezes live weights and requires held-out validation, so automatic promotion would erase an intentional safety boundary.

### Claiming atomic router-owned rollback across filesystem or external mutations. -- BLOCKED (iteration 7, 1 attempts)
- What was tried: Claiming atomic router-owned rollback across filesystem or external mutations.
- Why blocked: Repeated iteration evidence ruled this direction out.
- Do NOT retry: Claiming atomic router-owned rollback across filesystem or external mutations.

### Copying domain mechanisms literally—CPU idle loops, routing-table prefixes, DNS TTL syntax, or health-check algorithms—adds machinery without improving the skill-routing contract. -- BLOCKED (iteration 3, 1 attempts)
- What was tried: Copying domain mechanisms literally—CPU idle loops, routing-table prefixes, DNS TTL syntax, or health-check algorithms—adds machinery without improving the skill-routing contract.
- Why blocked: Repeated iteration evidence ruled this direction out.
- Do NOT retry: Copying domain mechanisms literally—CPU idle loops, routing-table prefixes, DNS TTL syntax, or health-check algorithms—adds machinery without improving the skill-routing contract.

### Copying full authority and resource arrays into every route decision when the policy-pinned mode reference can derive them. -- BLOCKED (iteration 6, 1 attempts)
- What was tried: Copying full authority and resource arrays into every route decision when the policy-pinned mode reference can derive them.
- Why blocked: Repeated iteration evidence ruled this direction out.
- Do NOT retry: Copying full authority and resource arrays into every route decision when the policy-pinned mode reference can derive them.

### Copying one universal numerical threshold across hubs without held-out risk/coverage evidence. -- BLOCKED (iteration 5, 1 attempts)
- What was tried: Copying one universal numerical threshold across hubs without held-out risk/coverage evidence.
- Why blocked: Repeated iteration evidence ruled this direction out.
- Do NOT retry: Copying one universal numerical threshold across hubs without held-out risk/coverage evidence.

### Deleting lexical/surface detection rather than compiling it into typed observations. -- BLOCKED (iteration 6, 1 attempts)
- What was tried: Deleting lexical/surface detection rather than compiling it into typed observations.
- Why blocked: Repeated iteration evidence ruled this direction out.
- Do NOT retry: Deleting lexical/surface detection rather than compiling it into typed observations.

### Full-transcript peer handoff: it makes context cost grow with conversation length and transfers unrelated or sensitive material when structured slots and references suffice. -- BLOCKED (iteration 4, 1 attempts)
- What was tried: Full-transcript peer handoff: it makes context cost grow with conversation length and transfers unrelated or sensitive material when structured slots and references suffice.
- Why blocked: Repeated iteration evidence ruled this direction out.
- Do NOT retry: Full-transcript peer handoff: it makes context cost grow with conversation length and transfers unrelated or sensitive material when structured slots and references suffice.

### Fully removing hub-local resolution while leaving Layer 0 unchanged: metadata-routed modes are not generally projected to the advisor, and composite local outcomes would be lost. -- BLOCKED (iteration 1, 1 attempts)
- What was tried: Fully removing hub-local resolution while leaving Layer 0 unchanged: metadata-routed modes are not generally projected to the advisor, and composite local outcomes would be lost.
- Why blocked: Repeated iteration evidence ruled this direction out.
- Do NOT retry: Fully removing hub-local resolution while leaving Layer 0 unchanged: metadata-routed modes are not generally projected to the advisor, and composite local outcomes would be lost.

### Generic rollback after destination mutation: cancellation is not guaranteed even in A2A, so post-execution recovery must be defined by the destination workflow rather than fabricated by the router. -- BLOCKED (iteration 4, 1 attempts)
- What was tried: Generic rollback after destination mutation: cancellation is not guaranteed even in A2A, so post-execution recovery must be defined by the destination workflow rather than fabricated by the router.
- Why blocked: Repeated iteration evidence ruled this direction out.
- Do NOT retry: Generic rollback after destination mutation: cancellation is not guaranteed even in A2A, so post-execution recovery must be defined by the destination workflow rather than fabricated by the router.

### Holding locks, tool leases, or reservations while a proof awaits commit. -- BLOCKED (iteration 7, 1 attempts)
- What was tried: Holding locks, tool leases, or reservations while a proof awaits commit.
- Why blocked: Repeated iteration evidence ruled this direction out.
- Do NOT retry: Holding locks, tool leases, or reservations while a proof awaits commit.

### Letting a confidence controller replace capability validation, resource loading, or packet authority. -- BLOCKED (iteration 5, 1 attempts)
- What was tried: Letting a confidence controller replace capability validation, resource loading, or packet authority.
- Why blocked: Repeated iteration evidence ruled this direction out.
- Do NOT retry: Letting a confidence controller replace capability validation, resource loading, or packet authority.

### Literal two-phase commit with long-lived reservations: lock and recovery-manager costs exceed routing needs. -- BLOCKED (iteration 7, 1 attempts)
- What was tried: Literal two-phase commit with long-lived reservations: lock and recovery-manager costs exceed routing needs.
- Why blocked: Repeated iteration evidence ruled this direction out.
- Do NOT retry: Literal two-phase commit with long-lived reservations: lock and recovery-manager costs exceed routing needs.

### Mutating live weights or router rules immediately after each correction: this makes the effective policy time-dependent and repeats the blocked live-only evaluation direction. -- BLOCKED (iteration 2, 1 attempts)
- What was tried: Mutating live weights or router rules immediately after each correction: this makes the effective policy time-dependent and repeats the blocked live-only evaluation direction.
- Why blocked: Repeated iteration evidence ruled this direction out.
- Do NOT retry: Mutating live weights or router rules immediately after each correction: this makes the effective policy time-dependent and repeats the blocked live-only evaluation direction.

### One proof for a mutating ordered bundle: each commit changes later targets' state. -- BLOCKED (iteration 7, 1 attempts)
- What was tried: One proof for a mutating ordered bundle: each commit changes later targets' state.
- Why blocked: Repeated iteration evidence ruled this direction out.
- Do NOT retry: One proof for a mutating ordered bundle: each commit changes later targets' state.

### Presenting the whole mode registry as a clarification menu; compression is part of the contract. -- BLOCKED (iteration 5, 1 attempts)
- What was tried: Presenting the whole mode registry as a clarification menu; compression is part of the contract.
- Why blocked: Repeated iteration evidence ruled this direction out.
- Do NOT retry: Presenting the whole mode registry as a clarification menu; compression is part of the contract.

### Promoting every nested packet to an independent advisor identity: this contradicts the deliberate single graph identity and makes advisor-invisible evidence packets independently routable. -- BLOCKED (iteration 1, 1 attempts)
- What was tried: Promoting every nested packet to an independent advisor identity: this contradicts the deliberate single graph identity and makes advisor-invisible evidence packets independently routable.
- Why blocked: Repeated iteration evidence ruled this direction out.
- Do NOT retry: Promoting every nested packet to an independent advisor identity: this contradicts the deliberate single graph identity and makes advisor-invisible evidence packets independently routable.

### Replacing deterministic router replay with live-only evaluation: this removes the current offline CI oracle rather than replacing it. -- BLOCKED (iteration 1, 1 attempts)
- What was tried: Replacing deterministic router replay with live-only evaluation: this removes the current offline CI oracle rather than replacing it.
- Why blocked: Repeated iteration evidence ruled this direction out.
- Do NOT retry: Replacing deterministic router replay with live-only evaluation: this removes the current offline CI oracle rather than replacing it.

### Reusing `shadow-deltas.jsonl` as the complete offline oracle. It can compare scores for an HMAC-keyed invocation, but it cannot replay raw feature extraction and does not identify an immutable base/overlay pair. -- BLOCKED (iteration 2, 1 attempts)
- What was tried: Reusing `shadow-deltas.jsonl` as the complete offline oracle. It can compare scores for an HMAC-keyed invocation, but it cannot replay raw feature extraction and does not identify an immutable base/overlay pair.
- Why blocked: Repeated iteration evidence ruled this direction out.
- Do NOT retry: Reusing `shadow-deltas.jsonl` as the complete offline oracle. It can compare scores for an HMAC-keyed invocation, but it cannot replay raw feature extraction and does not identify an immutable base/overlay pair.

### Reusing a proof after any bound input or precondition changes. -- BLOCKED (iteration 7, 1 attempts)
- What was tried: Reusing a proof after any bound input or precondition changes.
- Why blocked: Repeated iteration evidence ruled this direction out.
- Do NOT retry: Reusing a proof after any bound input or precondition changes.

### Transferring load-balancer fail-open behavior to mutation-capable skill requests: routing to a known-unhealthy target trades correctness for availability, a trade that is not safe when a wrong mode can mutate state. -- BLOCKED (iteration 3, 1 attempts)
- What was tried: Transferring load-balancer fail-open behavior to mutation-capable skill requests: routing to a known-unhealthy target trades correctness for availability, a trade that is not safe when a wrong mode can mutate state.
- Why blocked: Repeated iteration evidence ruled this direction out.
- Do NOT retry: Transferring load-balancer fail-open behavior to mutation-capable skill requests: routing to a known-unhealthy target trades correctness for availability, a trade that is not safe when a wrong mode can mutate state.

### Treating `ACCEPTED` as `COMPLETED`: SIP explicitly separates accepting REFER from the later status of the referred action. -- BLOCKED (iteration 4, 1 attempts)
- What was tried: Treating `ACCEPTED` as `COMPLETED`: SIP explicitly separates accepting REFER from the later status of the referred action.
- Why blocked: Repeated iteration evidence ruled this direction out.
- Do NOT retry: Treating `ACCEPTED` as `COMPLETED`: SIP explicitly separates accepting REFER from the later status of the referred action.

### Treating a valid proof as execution authority. -- BLOCKED (iteration 7, 1 attempts)
- What was tried: Treating a valid proof as execution authority.
- Why blocked: Repeated iteration evidence ruled this direction out.
- Do NOT retry: Treating a valid proof as execution authority.

### Treating every no-destination state as the same null/default outcome: the authoritative sources distinguish absence of work, absence of a route, useful negative answers, dependency failure, unhealthy destinations, and wrong initial contact. -- BLOCKED (iteration 3, 1 attempts)
- What was tried: Treating every no-destination state as the same null/default outcome: the authoritative sources distinguish absence of work, absence of a route, useful negative answers, dependency failure, unhealthy destinations, and wrong initial contact.
- Why blocked: Repeated iteration evidence ruled this direction out.
- Do NOT retry: Treating every no-destination state as the same null/default outcome: the authoritative sources distinguish absence of work, absence of a route, useful negative answers, dependency failure, unhealthy destinations, and wrong initial contact.

### Treating prompt-free accepted/corrected outcome rows as per-intent gold fixtures: the telemetry contract explicitly prevents prompt or scenario reconstruction. -- BLOCKED (iteration 2, 1 attempts)
- What was tried: Treating prompt-free accepted/corrected outcome rows as per-intent gold fixtures: the telemetry contract explicitly prevents prompt or scenario reconstruction.
- Why blocked: Repeated iteration evidence ruled this direction out.
- Do NOT retry: Treating prompt-free accepted/corrected outcome rows as per-intent gold fixtures: the telemetry contract explicitly prevents prompt or scenario reconstruction.

### Treating the advisor's `confidence` or top-two margin as a calibrated probability. -- BLOCKED (iteration 5, 1 attempts)
- What was tried: Treating the advisor's `confidence` or top-two margin as a calibrated probability.
- Why blocked: Repeated iteration evidence ruled this direction out.
- Do NOT retry: Treating the advisor's `confidence` or top-two margin as a calibrated probability.

### Unbounded clarification or rerouting: MCP's rate-limit guidance and SIP's hop bound point to explicit budgets, not conversational recursion. -- BLOCKED (iteration 4, 1 attempts)
- What was tried: Unbounded clarification or rerouting: MCP's rate-limit guidance and SIP's hop bound point to explicit budgets, not conversational recursion.
- Why blocked: Repeated iteration evidence ruled this direction out.
- Do NOT retry: Unbounded clarification or rerouting: MCP's rate-limit guidance and SIP's hop bound point to explicit budgets, not conversational recursion.

### Using a default mode as an unlabelled substitute for absent evidence: IP defaults remain real bounded routes, and the absence of both specific and default routes stays visible. -- BLOCKED (iteration 3, 1 attempts)
- What was tried: Using a default mode as an unlabelled substitute for absent evidence: IP defaults remain real bounded routes, and the absence of both specific and default routes stays visible.
- Why blocked: Repeated iteration evidence ruled this direction out.
- Do NOT retry: Using a default mode as an unlabelled substitute for absent evidence: IP defaults remain real bounded routes, and the absence of both specific and default routes stays visible.

<!-- /ANCHOR:exhausted-approaches -->

<!-- ANCHOR:ruled-out-directions -->
## 10. RULED OUT DIRECTIONS
- A one-field `workflowMode` handoff cannot represent ordered bundles, surfaces, fallback resources, confidence, or defer semantics. Treating it as a complete replacement is a dead end unless the handoff becomes a typed route plan. (iteration 1)
- Fully removing hub-local resolution while leaving Layer 0 unchanged: metadata-routed modes are not generally projected to the advisor, and composite local outcomes would be lost. (iteration 1)
- Promoting every nested packet to an independent advisor identity: this contradicts the deliberate single graph identity and makes advisor-invisible evidence packets independently routable. (iteration 1)
- Replacing deterministic router replay with live-only evaluation: this removes the current offline CI oracle rather than replacing it. (iteration 1)
- Automatically promoting every bounded calibration proposal: the current proposal contract freezes live weights and requires held-out validation, so automatic promotion would erase an intentional safety boundary. (iteration 2)
- Mutating live weights or router rules immediately after each correction: this makes the effective policy time-dependent and repeats the blocked live-only evaluation direction. (iteration 2)
- Reusing `shadow-deltas.jsonl` as the complete offline oracle. It can compare scores for an HMAC-keyed invocation, but it cannot replay raw feature extraction and does not identify an immutable base/overlay pair. (iteration 2)
- Treating prompt-free accepted/corrected outcome rows as per-intent gold fixtures: the telemetry contract explicitly prevents prompt or scenario reconstruction. (iteration 2)
- A universal “always fail closed” or “always fail open” law is a dead end. The sources instead make the choice conditional on outcome type, destination capability, and the cost of retry versus wrong execution. (iteration 3)
- Copying domain mechanisms literally—CPU idle loops, routing-table prefixes, DNS TTL syntax, or health-check algorithms—adds machinery without improving the skill-routing contract. (iteration 3)
- Transferring load-balancer fail-open behavior to mutation-capable skill requests: routing to a known-unhealthy target trades correctness for availability, a trade that is not safe when a wrong mode can mutate state. (iteration 3)
- Treating every no-destination state as the same null/default outcome: the authoritative sources distinguish absence of work, absence of a route, useful negative answers, dependency failure, unhealthy destinations, and wrong initial contact. (iteration 3)
- Using a default mode as an unlabelled substitute for absent evidence: IP defaults remain real bounded routes, and the absence of both specific and default routes stays visible. (iteration 3)
- Full-transcript peer handoff: it makes context cost grow with conversation length and transfers unrelated or sensitive material when structured slots and references suffice. (iteration 4)
- Generic rollback after destination mutation: cancellation is not guaranteed even in A2A, so post-execution recovery must be defined by the destination workflow rather than fabricated by the router. (iteration 4)
- Treating `ACCEPTED` as `COMPLETED`: SIP explicitly separates accepting REFER from the later status of the referred action. (iteration 4)
- Unbounded clarification or rerouting: MCP's rate-limit guidance and SIP's hop bound point to explicit budgets, not conversational recursion. (iteration 4)
- Copying one universal numerical threshold across hubs without held-out risk/coverage evidence. (iteration 5)
- Letting a confidence controller replace capability validation, resource loading, or packet authority. (iteration 5)
- Presenting the whole mode registry as a clarification menu; compression is part of the contract. (iteration 5)
- Treating the advisor's `confidence` or top-two margin as a calibrated probability. (iteration 5)
- A single-target result with no target role or bundle order. (iteration 6)
- A solver whose only inputs are requested capability and a mode resource path. (iteration 6)
- Copying full authority and resource arrays into every route decision when the policy-pinned mode reference can derive them. (iteration 6)
- Deleting lexical/surface detection rather than compiling it into typed observations. (iteration 6)
- “Speculate everything, roll back on misroute”: external effects and some generated values are not reproducibly reversible. (iteration 7)
- Claiming atomic router-owned rollback across filesystem or external mutations. (iteration 7)
- Holding locks, tool leases, or reservations while a proof awaits commit. (iteration 7)
- Literal two-phase commit with long-lived reservations: lock and recovery-manager costs exceed routing needs. (iteration 7)
- One proof for a mutating ordered bundle: each commit changes later targets' state. (iteration 7)
- Reusing a proof after any bound input or precondition changes. (iteration 7)
- Treating a valid proof as execution authority. (iteration 7)

<!-- /ANCHOR:ruled-out-directions -->

<!-- ANCHOR:divergence-frontier -->
## 10A. SATURATED DIRECTIONS AND DIVERGENCE FRONTIER
- Completed pivots: 0
- Failed pivots: 0
- Audited overrides: 0
- Saturated: none yet
- Pivot lineage: none yet
- Remaining frontier: none recorded

<!-- /ANCHOR:divergence-frontier -->

<!-- ANCHOR:carried-forward-open-questions -->
## 11A. CARRIED-FORWARD OPEN QUESTIONS
[None yet]

<!-- /ANCHOR:carried-forward-open-questions -->

<!-- ANCHOR:next-focus -->
## 11. NEXT FOCUS
[All tracked questions are resolved]

<!-- /ANCHOR:next-focus -->

<!-- MACHINE-OWNED: END -->

## 12. KNOWN CONTEXT

- The run-3 agenda requires seven non-converging lateral iterations and explicitly invites abolishing hubs, adaptive routing, cross-domain analogies, recoverable handoff, dialogue, confidence-first routing, radical simplification, and a frame-breaking diagnosis. [SOURCE: .opencode/specs/sk-doc/019-sk-doc-router-alignment/021-default-mode-policy-research/spec.md:107]
- The packet forbids implementing router changes; proposals must stay research-only. [SOURCE: .opencode/specs/sk-doc/019-sk-doc-router-alignment/021-default-mode-policy-research/spec.md:59]
- Current hub-router files may already differ from the historical premise, so each iteration must verify the working tree instead of assuming the executive summary is current.
- Likely integration points: hub `SKILL.md` files, `hub-router.json`, `mode-registry.json`, create-skill parent templates, route-gold replay, and the Layer-0 skill advisor.
- `resource-map.md` is absent; the coverage-map gate is skipped.
- Spec Memory and Code Graph were unavailable at initialization; no prior-context cache or structural graph was imported.

## 13. RESEARCH BOUNDARIES

- Max iterations: 7
- Stop policy: max-iterations
- Convergence threshold: 0.05 (telemetry only before iteration 7)
- Per-iteration budget: maximum 12 tool calls
- Progressive synthesis: true
- Allowed writes: this lineage artifact directory only
- Current generation: 1
- Session: fanout-sol-oob-1784345122095-1gku9b
- Started: 2026-07-18T03:31:09Z
