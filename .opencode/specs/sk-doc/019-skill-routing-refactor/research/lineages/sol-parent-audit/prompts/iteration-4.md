DEEP-RESEARCH
Resolved route: mode=research; target_agent=@deep-research; execution=single_iteration; state_source=externalized_files; do_not_switch_mode=true

# Iteration 4

Focus: Audit the parent `routing-before-after.md` against the live tree and current parent `spec.md`/`context-index.md`. Find stale names, old-to-new path drift, obsolete counts, broken links, inaccurate before/after claims, and misleading resume/handoff guidance.

Parallel safety: create `iterations/iteration-004.md` and `deltas/iter-004.jsonl`; do not append the shared state log. The workflow orchestrator will serialize the canonical first delta record into the state log after the seat completes.

Historical artifacts are excluded. Audited docs are read-only. Cite current file:line evidence.
