DEEP-RESEARCH
Resolved route: mode=research; target_agent=@deep-research; execution=single_iteration; state_source=externalized_files; do_not_switch_mode=true

# Iteration 3

Focus: Audit the parent `routing-config-and-advisor-reference.md` against the live parent tree and current parent `spec.md`/`context-index.md`. Find stale packet names, path spellings, counts, workstream assignments, broken cross-references, and resume-routing claims.

Parallel safety: create `iterations/iteration-003.md` and `deltas/iter-003.jsonl`; do not append the shared state log. The workflow orchestrator will serialize the canonical first delta record into the state log after the seat completes.

Historical artifacts are excluded. Audited docs are read-only. Cite current file:line evidence.
