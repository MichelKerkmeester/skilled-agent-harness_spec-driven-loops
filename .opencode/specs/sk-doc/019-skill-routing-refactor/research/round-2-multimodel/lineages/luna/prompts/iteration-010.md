# Iteration 10 prompt

Run the final max-iteration integrity pass. Verify every iteration record and delta, the state JSONL graph, the 15-finding registry, the exclusion boundary, and the required synthesis inputs. Do not add speculative findings. Record only a real integrity defect if one is found; otherwise close the loop at the configured maximum and hand off to phase synthesis.
