# GOAL PROMPT — Anobel Vloot-functie visuals (GLM 5.2 vision-to-code)

**OBJECTIVE.** Produce on-brand dashboard-fragment bento tiles for the 9 Vloot-functie concepts in `concepts.md` — Goedkeuringssysteem, OCI-koppeling, Accountbeheer, Één factuur, Aangepast assortiment, Favorieten, Orders & facturen, Prijzen & condities, Kwartaalcijfers — **5 DISTINCT treatments each** (the per-concept directions in `concepts.md` §2), each generated by GLM 5.2 from the concept's reference shell image, then gated and audited. Total: 45 visuals.

**SPEC FOLDER (Gate 3, established):** `.opencode/specs/anobel.com/004-bento-visuals/`. Outputs → `scratch/faithful-import/dist/<concept>-glm-<1..5>.html` (+ `.notes.md`). Never overwrite the non-`-glm` originals.

**METHOD — per visual, fresh GLM call (no shared context):**
1. **sk-design DECIDES:** register = Product; dials V4 / M2 / D6; restrained palette; contrast inventory (muted text `#4e4e4e`, never `#787878`). Choose the concept's treatment direction from `concepts.md` §2 (one signature per card, grounded in the maritime subject).
2. **GLM 5.2 vision-to-code** via the direct Z.AI Coding Plan API: `POST https://api.z.ai/api/coding/paas/v4/chat/completions`, `Bearer <zai-coding-plan key>`, content = `[text contract + treatment, image_url(base64 of the concept's reference shell)]`, model `glm-5.2`, `max_tokens ~16k` (thinking consumes budget first). NOT opencode `--file` (#20802).
3. **Prompt contract (text):** exact house shell (560×480, radius 22, no border, the house box-shadow; inner `#fefefe` panels 1px `#ececec` tight shadow; title 21/700 `#0a1a2f`; muted `#4e4e4e`; eyebrow "⚓ Vloot-functie"; ghost ↗ bottom-right); palette brand `#06458c`/`#043367`, green `#367e39` status-only, red `#c9140f` alerts-only, **orange = 0**, zero out-of-palette hex; no gradient except navy; reduced-motion guard; Dutch copy from `concepts.md`; "output ONLY the HTML".
4. I write the `.html`, strip fences, snap any stray hex / flatten stray gradients (mechanical fixes only — no redesign).
5. **GATE:** `contrast_check.py` exit 0 on every text pair; `proof_check.py`-style notes; palette/anti-tell greps (orange 0, radius 22, title 700, no `#787878`).
6. **RENDER** headless (2×) for review.
7. **sk-design AUDIT each** (audit mode): accessibility / performance / responsive / theming / anti-slop, with a 0–100 score + confirmed/inferred/not-assessed labels; flag any FIX.

**DISCIPLINE:** adopt-if-better (handover §5.6); promote only clear wins; **no silent caps** — report the per-visual verdict + audit score. Ship adopted winners to OD project `2078899e` (standing "always ship") + refresh `bento-canvas.html`.

**EXECUTION:** **pilot first** — 1 concept × 5 treatments to validate the per-concept pipeline, then fan out the remaining 8 in background batches (GLM dispatch cost = 0, subscription). Deliver a 45-row summary: concept · treatment · gate · audit score · verdict.
