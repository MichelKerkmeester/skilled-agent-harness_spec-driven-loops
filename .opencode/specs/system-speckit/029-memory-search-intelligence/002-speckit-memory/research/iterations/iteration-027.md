# Iteration 27: Round F Synthesis — Corrected Top Ship-First Ranking (post-broadening)

## Focus
Round F synthesis: re-validate the roadmap's Top-10 ship-first picks against ALL broadening verdicts (Rounds A-F override pass-1 where they conflict). Read-only over roadmap.md + the 4 child research.md.

## Corrected ship-first (newInfoRatio 0.40)
**SURVIVES (clean):**
- **#1 Q6-anchor FIX (Deep Loop)** — template-only correctness FIX, untouched by any verdict; the campaign's own driver hit it firsthand.
- **C9 graceful embedder-degrade (Memory)** — graceful-degradation spine untouched; recall THROWS today, keep-lexical substrate exists.
- **C3-A flip temporal-edges (Memory)** — code-complete, reversible; the Q1-C1 risk reclass was Code-Graph-specific. (NB iter-26: this is one of the few near-literal flips.)
- **C5 runtime-empty lane elision (Advisor)** — fixes the confirmed ~13% liveTotal skew; needs baseline capture.
- **Q4-C1 rank-time trust multiplier (Code Graph)** — rank-time-only, fields pre-plumbed, neutral fallback; no migration.
- **Q6-C2 soft generation watermark (Code Graph)** — additive int on the freshness envelope; staged predecessor.

**SURVIVES (trimmed rationale):**
- **C5-B content tie-breaks (Memory)** — Memory-local fix stands, but its "stable-prefix prompt-cache unlock" cross-system rationale weakens (determinism spine is 2-of-4, not unifying).
- **C3 RRF import (Advisor)** — SA is a determinism-confirmed subsystem (shape-compatible), but its "unblocks C1/C2/C6" justification is moot (C1 zero-impact).
- **C2-C graph-expansion gating (Memory)** — not refuted, but gated by C2-A (a BUILD-new classifier) → NOT the standalone "free flip" its #2 rank implied.

**DEMOTED:** C4-A idempotency-default-on (procedural/consolidation leg refuted — infra can't host "wire replay into deferred-save"); C6-A always-on decay (decay-guards already-mitigated, low marginal value); det-impact-order (determinism Code-Graph leg unconfirmed).

**REMOVED:** **C4 Beta-posterior lane auto-tune** (NOT a graduation — estimator→shadow-weight seam missing → real BUILD); **C1 split-conflict re-rank** (conflicts_with DORMANT → zero observable impact).

## Corrected Wave-0 spearhead
**Q6-anchor FIX → Memory {C9, C3-A, C5-B} → Advisor {C5 (+C5a)} → Code Graph {Q4-C1, Q6-C2}.** Dropped from original Wave-0: C6-A (already-mitigated) + Code-Graph det-impact-order (determinism leg unconfirmed).

## Synthesis impact
This corrected ranking IS the broadening's headline deliverable: the original top-10 had **2 removals (C4, C1) and 3 demotions** once verified against live code. The genuine Wave-0 is leaner and honest — a correctness FIX + a few reversible flips/additive fixes; the big architectural items (bi-temporal cluster, C4 gate, D2 keystone) are correctly re-classed as moderate-to-large BUILDs gated on benchmarks, not promotes.

## Next Focus
Apply this corrected ranking + the two meta-spine recalibrations to roadmap.md in the final synthesis (the roadmap re-tag). The broadening campaign's net effect: a hardened, honestly-sequenced, refutation-tested roadmap.
