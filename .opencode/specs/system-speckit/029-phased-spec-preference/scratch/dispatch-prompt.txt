Act as a spec-folder governance analyst with full read-only access to this repository via --dir. Zero prior context exists beyond this message — treat this as the entire briefing; no follow-up turn will arrive.

Spec folder: .opencode/specs/system-speckit/031-phased-spec-preference (pre-approved, skip Gate 3). This dispatch performs no file writes, so Gate 3 is not actually triggered — the line above is informational only.

CONTEXT
This repository ("Public") is a multi-runtime AI-agent project (OpenCode, Claude Code, Codex) governed by the system-spec-kit skill at .opencode/skills/system-spec-kit/, which controls how AI agents document work in spec folders (.opencode/specs/<track>/<###-name>/). A phase-decomposition system already exists: a spec folder becomes a "phase parent" with numbered child phase folders (001-foo/, 002-bar/, ...) once BOTH a complexity score (>= 25/50, scored across five dimensions) AND a documentation level (>= 3) are met — both conditions required; if only one is met, the spec proceeds as a standard non-phased folder.

Every AI agent working in this repo answers a mandatory "Gate 3" question before any file modification, choosing one of five options: A) use an existing spec folder, B) create a new spec folder, C) update a related spec folder, D) skip, E) create a phase folder inside an existing phase parent. Gate 3's decision logic, folder_routing.md, and quick_reference.md currently give no guidance preferring "extend an existing phased packet" over "spin up a new sibling folder."

Two rules, stated verbatim by the repo maintainer, must be reflected in the governing documentation:
Rule 1: "AI's should always prefer multi-phased specs over separate single spec folders, unless the task is small or new from current work."
Rule 2: "Also when they are working inside a phased spec they should prefer adding new phases to that instead of create a new separate spec folder."

Stakeholders: every AI agent instance across all three runtimes that answers Gate 3 after this change ships; the repo maintainer who will manually apply the proposed edits.

Constraints: this is READ-ONLY analysis. Creating, editing, moving, or deleting any file in this repository is off-limits for this task. Invoking opencode run, dispatching sub-agents, or spawning any nested CLI/agent session is also off-limits — produce the analysis directly in this response.

Dependency: the proposal must compose with — never silently override or contradict — the existing phase-qualification thresholds (score >= 25/50 across five dimensions AND documentation level >= 3, both required).

TASK — three phases, in order

Phase 1 — Read (do not skip any item; every claim in the final proposal must trace to something actually read; if a path does not resolve, state the exact path attempted instead of guessing or inventing content):
1. .opencode/skills/system-spec-kit/SKILL.md
2. Every file in .opencode/skills/system-spec-kit/references/structure/ (currently: phase_system.md, phase_definitions.md, folder_routing.md, sub_folder_versioning.md, folder_structure.md)
3. .opencode/skills/system-spec-kit/references/workflows/quick_reference.md and spec_folder_authoring_checklist.md
4. .opencode/skills/system-spec-kit/assets/level_decision_matrix.md and complexity_decision_matrix.md
5. .opencode/skills/system-spec-kit/constitutional/spec-folder-naming.md and gate-enforcement.md
6. Section 2 ("MANDATORY GATES"), specifically GATE 3, of the root CLAUDE.md — this is the only file in the repo that documents Gate 3; the root-level .claude/CLAUDE.md is a short Claude-runtime supplement (tool-routing + logic-sync rules only) and contains NO Gate 3 text — read it anyway for cross-runtime consistency context, but do not search it for Gate 3 wording
7. AGENTS.md at the repository root

Phase 2 — Per-rule proposal. For EACH of Rule 1 and Rule 2 above, independently produce:
a) The exact file(s) and section(s) (heading text or clearly identifiable anchor) to edit, with the exact wording to insert or change, ready to paste — use a fenced block, diff-style (before/after) when modifying existing text, insert-only when adding new text.
b) An explicit statement of how the new preference interacts with the existing phase-qualification thresholds — cover BOTH the case where a task is complex enough to phase-qualify (score >= 25/50 AND level >= 3) and the case where it is not, and state whether/how the new rule changes AI behavior in each case.
c) A concrete, checkable definition of "small" and "new/unrelated to current work" — anchor the definition to concepts that already exist in this repo's documentation (evaluate: the Level 1 "exempt" tier, the LOC-as-soft-guidance table, Gate 3's own exemption language) rather than inventing a new scoring axis; if none of the existing concepts fit cleanly, say so explicitly and explain why before proposing something new.
d) A statement of whether/how Gate 3's five options (A-E) should change — ordering, wording, and/or default — so that option E (phase folder) or "extend the active phased packet" is favored over option B (new spec folder) when applicable; address all five options, not only B and E.
e) A list of any existing passage that already contradicts or partially covers this rule instead of silently duplicating it — check in particular folder_routing.md §9 "BYPASS OPTIONS" → Session Preferences table (it currently maps the literal phrase "new folder" to "Always create new spec folder") and quick_reference.md, plus anything else surfaced during Phase 1 reading.
f) A statement of the risk of applying the new rule too aggressively — e.g., folding genuinely unrelated work into an active phased packet purely because it is open — and an explanation of how the heuristic from (c) specifically guards against that risk.

Phase 3 — Gate 3 option-set synthesis and close-out:
- Summarize, in one paragraph, how Gate 3's A-E options currently read (as actually found in root CLAUDE.md).
- State the specific recommended change to option ordering, wording, and/or default, tying it back to both rules above.
- Under a final "Open Questions / Assumptions" heading, list ONLY items that genuinely cannot be resolved from the files read in Phase 1 — this is a single-shot, non-interactive dispatch with no follow-up turn, so resolve everything resolvable rather than deferring it.

FORMAT — required output structure
Return the complete proposal as the final response text only (no file writes of any kind). Use exactly this structure:

1. RULE 1 PROPOSAL
   1.1 File + Section + Exact Wording
   1.2 Interaction With Existing Thresholds
   1.3 Conflicts / Overlaps With Existing Docs
   1.4 Risks / Guardrails
2. RULE 2 PROPOSAL
   2.1 File + Section + Exact Wording
   2.2 Interaction With Existing Thresholds
   2.3 Conflicts / Overlaps With Existing Docs
   2.4 Risks / Guardrails
3. GATE 3 OPTION-SET CHANGES
   3.1 Current A-E Behavior (as read)
   3.2 Recommended Change
4. OPEN QUESTIONS / ASSUMPTIONS

AUDIENCE, SUCCESS CRITERIA & TIMELINE
Primary audience: the repo maintainer who will manually apply the 1.1/2.1 wording. Secondary audience: every AI agent instance across Claude Code, OpenCode, and Codex that reads the edited documentation after it ships. Success is a maintainer being able to copy the 1.1/2.1 wording directly into the named files with no further research, and every claim being falsifiable against the specific files actually read rather than generic advice. This is a single-shot dispatch — resolve everything resolvable now.
