## Focus

Iteration 5 audited arc `004-code-index-stack`, the CocoIndex code-search stack under `013-embedder-testing-and-architecture`. The pass compared the arc's phase-parent and child packet claims against current `mcp-coco-index` source, docs, benchmark folders, and path history.

## Actions Taken

1. Read the phase-parent `spec.md`, `description.json`, `graph-metadata.json`, and confirmed the parent has no `plan.md`, `tasks.md`, or `implementation-summary.md` because detailed docs live in child packets.
2. Read representative child docs for `009-hybrid-search-bm25-fusion`, `016-query-expansion-identifier-bridging`, `018-rerank-matrix-rebench`, and `023-deep-research-arc-blind-spots`.
3. Spot-checked source files referenced by those docs: `config/config.py`, `config/settings.py`, `retrieval/query.py`, `retrieval/fusion.py`, `retrieval/fts_index.py`, `embedders/registered_embedders.py`, and `rerankers/reranker.py`.
4. Ran grep probes for `COCOINDEX_HYBRID`, `COCOINDEX_RERANK`, `COCOINDEX_QUERY_EXPANSION`, `CodeRankEmbed`, `jina-v3`, `Qwen3`, feature-catalog links, and benchmark folder names.
5. Compared git history for the arc path and `mcp-coco-index`, including `38d4e2d627` (jina-v3 default lock), `a32801b27c` (Qwen default flip), `f35e59505a` (benchmark path rename), and `b430818d55` (023/010 closeout).
6. Checked actual child folders and graph metadata for the phase-parent maps in `004-code-index-stack` and nested `023-deep-research-arc-blind-spots`.
7. Checked prior `iteration-001.md` to avoid re-reporting arc 008 findings; this pass only reports issues found on the arc 004 code-index stack surface.

## Findings

| Finding ID | Category | File:line + grep evidence | Recommended action |
|---|---|---|---|
| f-iter005-001 | MISSED | Parent `004-code-index-stack/spec.md:63-69` still lists only children `001` to `003`, while `004-code-index-stack/graph-metadata.json:6-28` lists children through `023` and `find ... -maxdepth 1` showed actual folders `001` through `020`, `022`, and `023`. The same parent still says `recent_action: Scaffolded three-phase...`, `next_safe_action: Author 001...`, and `completion_pct: 0` at `spec.md:15-26`; graph status remains `planned` at `graph-metadata.json:58-59`. | Refresh the phase-parent control file and graph metadata to the current arc map, or explicitly mark the original 001-003 section as historical and point resume/search to the current child map. |
| f-iter005-002 | DEAD | Parent embedder docs are superseded but still presented as live: `004-code-index-stack/spec.md:40` says the packet swaps to `sbert/jinaai/jina-embeddings-v2-base-code`; current source has `DEFAULT_EMBEDDER_NAME = "sbert/nomic-ai/CodeRankEmbed"` at `registered_embedders.py:255` and `_DEFAULT_MODEL = DEFAULT_EMBEDDER_NAME` at `config/config.py:21`. `description.json:3-9` still describes `"018-code-embedder-coderank"` with stale 2026-05-17 metadata. | Update parent overview/description to the final CodeRankEmbed state, or add a supersession note that later children promoted CodeRankEmbed over the original jina-code target. |
| f-iter005-003 | BUGGED | Query expansion has an env-default contract split: the 016 packet status says shipped with default `COCOINDEX_QUERY_EXPANSION=false` at `016-query-expansion-identifier-bridging/spec.md:46`, and source parses `_DEFAULT_QUERY_EXPANSION` at `config/config.py:778-781` with `_DEFAULT_QUERY_EXPANSION = False` at `config/config.py:32`. But the same spec still requires default `true` at `spec.md:97-101` and `spec.md:138-142`, and `mcp_server/cocoindex_code/README.md:139` documents `COCOINDEX_QUERY_EXPANSION` default as `true`. | Make the 016 requirement table and folder README match the accepted default-off outcome, preserving the failed-bench rationale from the status and implementation summary. |
| f-iter005-004 | DEAD | Hybrid-search rollout docs still say opt-in/default-off even though the stack is default-on now. `009-hybrid-search-bm25-fusion/spec.md:54`, `spec.md:83`, and `spec.md:113-115` say `COCOINDEX_HYBRID=true` is opt-in/default-off; `implementation-summary.md:50`, `implementation-summary.md:58`, and `implementation-summary.md:82-83` repeat that. Current source defaults `COCOINDEX_HYBRID` to true at `config/config.py:722`, and the install guide says default ON at `INSTALL_GUIDE.md:339-345`. | Mark packet 009's opt-in language as superseded by the later promotion, and cross-link the promotion packet or current config source of truth. |
| f-iter005-005 | BUGGED | Reranker model docs are partially stale after the Qwen promotion. Current source sets `DEFAULT_RERANKER_NAME = "Qwen/Qwen3-Reranker-0.6B"` at `registered_embedders.py:255-256` and `_DEFAULT_RERANK_MODEL = DEFAULT_RERANKER_NAME` at `config/config.py:30`. The top-level README agrees in the pipeline table at `README.md:78`, but still says `Cross-encoder rerank ... Local Jina v3 reranker` at `README.md:104`. Git history confirms the later flip in `a32801b27c feat(reranker): flip default jina-v3 -> Qwen3-Reranker-0.6B`. | Replace the stale Jina sentence in the README with Qwen, and mention Jina only as the opt-in fallback. |
| f-iter005-006 | DEAD | Public docs point to files/folders that do not exist. `INSTALL_GUIDE.md:350` links `feature_catalog/hybrid-search.md` and `INSTALL_GUIDE.md:367` links `feature_catalog/reranker.md`; actual files are `feature_catalog/05--search-and-ranking/07-hybrid-search-bm25-rrf.md` and `feature_catalog/05--search-and-ranking/08-reranker-cross-encoder.md`. `INSTALL_GUIDE.md:365` and `INSTALL_GUIDE.md:1088` cite `benchmark-2026-05-20-cocoindex-via-sidecar`, but `rg --files .opencode/skills/mcp-coco-index/mcp_server/benchmarks` only found the sidecar artifacts under `benchmark-2026-05-21`. | Fix the two feature-catalog links and normalize sidecar benchmark references to the actual `benchmark-2026-05-21` folder or restore the missing renamed folder. |
| f-iter005-007 | MISSED | Nested `023-deep-research-arc-blind-spots/spec.md:2-3` says this is an 8-packet follow-on arc and `spec.md:58-67` maps only `001` through `008`. But `023-deep-research-arc-blind-spots/graph-metadata.json:6-16` includes an additional `010-public-repo-docs-alignment` child, `graph-metadata.json:56` sets that child as `last_active_child_id`, and `find .../023-deep-research-arc-blind-spots -maxdepth 2 -name spec.md` found `010-public-repo-docs-alignment/spec.md`. | Update the 023 parent phase map and "8-packet" description to include 010, or mark 010 as an out-of-band child so resume routing and docs agree. |
