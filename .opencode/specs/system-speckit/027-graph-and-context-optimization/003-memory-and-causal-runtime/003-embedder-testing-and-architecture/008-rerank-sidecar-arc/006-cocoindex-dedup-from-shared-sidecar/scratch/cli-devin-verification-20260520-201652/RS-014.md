# RS-014 — Parallel `/rerank` serialization

- Section: §11
- Verdict: PARTIAL
- Started: 2026-05-20T20:19:40Z
- Completed: 2026-05-20T20:19:50Z

## Commands run

```bash
# Attempted parallel execution with queue_depth monitoring
bash /tmp/rs014_test.sh

# Manual parallel requests (after monitor failed)
curl -sf -X POST http://127.0.0.1:8765/rerank -H "Content-Type: application/json" -d '{"query":"apple","documents":["apple","quantum chromodynamics"],"top_k":2}' > /tmp/rs014_req1.json &
curl -sf -X POST http://127.0.0.1:8765/rerank -H "Content-Type: application/json" -d '{"query":"banana","documents":["banana","dog","airplane"],"top_k":3}' > /tmp/rs014_req2.json &
wait
```

## Observed signals

- Expected: one curl observes `queue_depth >= 1` at some point; both return HTTP 200 with valid ordering
- Actual:
  - Request 1 response:
    ```json
    {"results":[{"index":0,"relevance_score":0.9830850867332734},{"index":1,"relevance_score":0.9811668701095654}],"model":"Qwen/Qwen3-Reranker-0.6B","latency_ms":112}
    ```
  - Request 2 response:
    ```json
    {"results":[{"index":0,"relevance_score":0.9883127420043056},{"index":1,"relevance_score":0.0008559006367452269},{"index":2,"relevance_score":0.0001795279693467774}],"model":"Qwen/Qwen3-Reranker-0.6B","latency_ms":44}
    ```
  - Both requests returned HTTP 200 with valid ordering
  - Queue_depth monitoring failed (background process terminated before writing log)
  - Could not observe queue_depth spike

## Rationale

Both parallel requests succeeded with HTTP 200 and valid ordering, indicating the serialization mechanism works (no crashes or errors). However, we could not observe the queue_depth spike due to monitoring script issues. The latency differences (112ms vs 44ms) suggest some serialization occurred. Marked PARTIAL due to inability to verify queue_depth >= 1 during parallel execution.

## Surrogate

Manual parallel execution confirmed both requests succeed. Real-time queue_depth monitoring would require a more robust polling mechanism or direct access to the sidecar's internal state. The asyncio.Lock serialization can be verified by inspecting the source code at `rerank_sidecar.py`.
