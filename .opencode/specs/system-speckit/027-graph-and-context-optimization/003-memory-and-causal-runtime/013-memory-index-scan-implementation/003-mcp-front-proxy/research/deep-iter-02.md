# Iteration 02 - TRACE

## Trace Table

| ID | Trace result | Evidence |
|---|---|---|
| H01 | Not applicable under normal backend-only path. | `context-server.ts:2085-2109` skips stdio only when `SPECKIT_BACKEND_ONLY=1`, then starts an IPC socket server. `socket-server.ts:133-175` creates a real `net.Server`, and `socket-server.ts:218-220` stores/listens on it, which keeps the event loop alive. |
| H02 | Plausible only in delayed/no-bridge cases; not a normal UDS reconnect bug. | New daemon idle state starts fresh in `launcher-idle-timeout.ts:77-121`; `sawActiveClient` is false until a client connects. But `context-server.ts:2093-2109` creates idle monitor and socket server together after startup, and the proxy probes readiness before connecting. |
| H03 | Refuted for the in-place recycle path. | `mk-spec-memory-launcher.cjs:682-715` clears model-server timers, stops demand listener, SIGTERMs/KILLs the model-server child, then SIGTERMs/KILLs the context-server child. `mk-spec-memory-launcher.cjs:824-846` lets the supervisor relaunch unless launcher shutdown is in progress. |
| H04 | Plausible hardening issue for TCP-only socket bind failure, not default UDS. | `socket-server.ts:181-195` returns a disabled handle after TCP `EADDRINUSE` retry exhaustion. It does not throw, while `context-server.ts:2112-2114` still logs IPC-only running. UDS `EADDRINUSE` instead unlinks and re-listens at `socket-server.ts:199-208`. |
| H05 | Covered/refuted. | Reattach snapshots pending requests on each attempt (`launcher-session-proxy.cjs:562-583`) and existing test confirms retry after fresh socket failure (`launcher-session-proxy.vitest.ts:362-393`). |
| H06 | Refuted for client-visible JSON-RPC ordering. | Snapshot replay is queued before gap frames (`launcher-session-proxy.cjs:523-533`, `494-498`, `551-553`). JSON-RPC responses are ID-correlated; response order is not guaranteed by protocol. |
| H07 | Covered/refuted. | `detachSocket()` clears stale drain state and removes the old drain listener at `launcher-session-proxy.cjs:339-353`; `pumpSocketQueue()` only waits on the active socket at `launcher-session-proxy.cjs:437-457`. |
| H08 | Covered/refuted. | Exhaustion calls `failPendingAndEnd()` (`launcher-session-proxy.cjs:562-583`), which emits retryable errors for pending and queued request IDs then ends output (`launcher-session-proxy.cjs:408-419`). Existing test covers exactly one retryable error for a gap request (`launcher-session-proxy.vitest.ts:292-325`). |
| H09 | Mostly refuted for normal UDS reconnect; same caveat as H02. | Active client activity marks idle via `socket-server.ts:141-155`, and proxy keepalives mark activity through `context-server.ts:2108`. Existing tests cover active clients and no-client timeout (`launcher-idle-timeout.vitest.ts:24-60`) but not a full recycle integration. |
| H10 | Plausible and high risk. | Keepalive ID is a string prefix generated at `launcher-session-proxy.cjs:464-475`. Backend frames with matching ID are consumed before normal response tracking at `launcher-session-proxy.cjs:508-520`. Client frames are forwarded/tracked without rejecting that private ID namespace at `launcher-session-proxy.cjs:485-491` and `createPendingRequestsTracker` stores arbitrary IDs at `launcher-session-proxy.cjs:129-137`. |
| H11 | Refuted except for H10 collision. | Replay queues preserve request write order (`launcher-session-proxy.cjs:523-533`, `437-462`), and backend responses are ID-addressed. |
| H12 | Refuted. | `classifyFrame()` treats `notifications/*` as replayable/protocol (`launcher-session-proxy.cjs:108-123`), no-id frames are not tracked as pending (`launcher-session-proxy.cjs:129-138`), and gap frames are queued/flushed (`launcher-session-proxy.cjs:432-498`). |
| H13 | Covered/refuted. | Backend splitter emits only complete newline frames (`launcher-session-proxy.cjs:64-85`), and backend failure discards incomplete buffer (`launcher-session-proxy.cjs:604-618`). Existing test covers truncated backend discard (`launcher-session-proxy.vitest.ts:172-187`). |
| H14 | Refuted. | Client splitter buffer is not discarded on backend failure; only backend splitter is discarded at `launcher-session-proxy.cjs:616`. |
| H15 | Partially plausible as missing post-commit enrichment, not confirmed as duplicate primary/FTS/vector rows. | `memory_save` is replayable (`launcher-session-proxy.cjs:23-37`, `108-123`). Non-chunked write uses an immediate SQLite transaction around dedup and record creation (`memory-save.ts:2476-2605`). Same-path/content dedup catches full-commit replay (`dedup.ts:213-282`, `memory-save.ts:2248-2259`). Post-insert enrichment happens after commit at `memory-save.ts:2648-2655`, so commit-before-enrichment crash can replay as `unchanged` and skip enrichment. |
| H16 | Refuted in proxy path. | `stopKeepalive()` clears interval/timer (`launcher-session-proxy.cjs:327-337`), `detachSocket()` removes old socket listeners (`launcher-session-proxy.cjs:339-353`), and `startKeepalive()` restarts one interval per attached socket (`launcher-session-proxy.cjs:478-483`). |
| H17 | Confirmable by code. | Lease-held second launchers use `bridgeStdioToSocket()` through `maybeBridgeLeaseHolder()` (`launcher-ipc-bridge.cjs:304-357`). That bridge raw-pipes and exits on socket close (`launcher-ipc-bridge.cjs:79-121`). It is not wrapped by `createSessionProxy()`. |
| H18 | Plausible contract miss. | `internalHandshake()` only waits for a response with the cached initialize ID (`launcher-session-proxy.cjs:212-268`); it never compares `result.protocolVersion`. Packet task T023 requires fail-closed on protocol-version mismatch (`tasks.md:95-98`). |

## Plausible Set Advanced To Iteration 03

H10, H15, H17, and H18 advance. H04 advances as hardening-only negative context. H02/H09 are retained as integration-test gaps, but not as confirmed normal-path bugs.
