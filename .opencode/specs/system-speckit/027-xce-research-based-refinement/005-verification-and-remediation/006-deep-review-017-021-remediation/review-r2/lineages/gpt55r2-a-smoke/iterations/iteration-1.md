SKILL ROUTING: User directed -> deep-review. Spec folder and artifact root are provided; I’ll load the workflow contract first, bind `artifact_dir` to the supplied lineage path, then keep all writes inside that directory.The memory trigger gate returned no direct scope memories; it did surface the constitutional deep-skill workflow rule, so I’m staying on the named `deep-review` contract rather than improvising.The scope folder contains only `spec.md`; the lineage root already exists and has `.executor-state/`. I’ll load the review protocol references next, then create the required state, iteration, and synthesis artifacts under the lineage root only.The scope is a review-only audit of `.opencode/skills/system-spec-kit/mcp_server/` search and retrieval code. I’m initializing the lineage packet now; config/state/strategy/dashboard/report and the iteration output will be the only new/updated files, all under the supplied lineage directory.Phase init is bound to the override artifact root; no `resolveArtifactRoot` command was run. I’m now doing the single configured review iteration across the search/retrieval scope.The scope resolves to 80+ search-related TypeScript files plus search handlers. I’m focusing the single iteration on the high-risk seams named by the scope: candidate generation, fusion/weights, confidence calibration, recovery payloads, trigger embedding backfill, and `memory_search` handler routing.The first concrete issue is in the summary-embedding branch: its folder filter uses exact equality while the rest of retrieval treats `specFolder` as subtree scope. I’m checking adjacent summary/vector/lexical paths for a second corroborated finding before writing the iteration artifacts.The second issue is stronger than a simple subtree mismatch: the default-on summary-embedding read path scores only an arbitrary capped prefix of `memory_summaries` and then Stage 1 hydrates those IDs from `memory_index` without the active projection guard used by vector/FTS/trigger paths. I’ll record both as active P1 findings with cited evidence and synthesize a conditional review.Phase main loop found 2 active P1 findings and no P0s. I’m writing the reducer-style state, iteration narrative, dashboard, resource map stub, and final report now.FANOUT_LINEAGE_COMPLETE:gpt55r2-a-smoke