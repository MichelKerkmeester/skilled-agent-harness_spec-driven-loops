Act as a general code-implementation agent (seat BF04). Spec folder: /Users/michelkerkmeester/MEGA/Development/Code_Environment/Public/.opencode/specs/system-spec-kit/027-xce-research-based-refinement/005-verification-and-remediation/007-release-alignment-review/004-code-remediation (PRE-APPROVED at Gate 3 — make the edits below directly; do NOT re-ask Gate 3 and do NOT ask the user anything).

FIRST load .opencode/skills/sk-code/SKILL.md, get its OpenCode surface tag, and follow that surface's standards. Also read .opencode/skills/system-spec-kit/constitutional/comment-hygiene.md.

You fix sk-code misalignments. SURGICAL, behavior-NEUTRAL edits only — comment / type / strict-mode / header edits ONLY. Do not change runtime behavior.

ALLOWED WRITE PATHS (edit ONLY these files):
- .opencode/plugins/mk-skill-advisor.js
- .opencode/plugins/mk-spec-memory.js
- .opencode/plugins/session-cleanup.js
- .opencode/scripts/claude-session-cleanup.sh
- .opencode/scripts/git-hooks/post-commit
BANNED: no delete, no rename, no new files, no edits outside the listed files, NO behavior change, no --no-verify.

FIX RULES by theme:
- Comment-hygiene (ephemeral ids in comments: `FIX RC3-B`, `P4-12`, `packets 006-009`, `SC-002`, ADR-/REQ-/CHK-/task-/finding ids, or a concrete `.opencode/specs/NNN-...` path): DROP the perishable label, KEEP the durable WHY. Never delete an explanatory comment outright — only strip the ephemeral identifier.
- Shell script missing strict mode: add `set -euo pipefail` immediately after the shebang/header.
- `any` / `any[]` in a public/exported type: replace with the real type inferred from usage; it MUST still type-check.
- Missing or non-boxed module/file header: add or normalize to the sk-code OpenCode header convention (cosmetic; in scope).
- catch parameter not typed `unknown`, or exported symbol missing TSDoc: fix only if trivial and behavior-neutral.

DO-NOT-FIX (binding — confirmed false positives; leave untouched):
- `// Feature catalog: <name>` comments are ALLOWED (NOT in comment-hygiene's forbidden list). Do NOT remove them.
- TSDoc `@example` values like `specFolder: 'specs/001-demo'` — illustrative, not real pointers. Leave.
- Any file under a `dist/` directory — build output, regenerated by the build. Do NOT edit.
- Any cited finding that does not reproduce against the live file — mark refuted.

FINDINGS TO FIX (confirm-then-fix each; P0/P1/P2 all in scope):
- [P2] .opencode/plugins/mk-skill-advisor.js:421 — Lowercase comment sentence | sk-code JavaScript style guide §7: all comment text must start with a capital letter, except directives or technical identifiers. | fix: Capitalize the comment or rewrite as a concise WHY comment.
- [P0] .opencode/plugins/mk-spec-memory.js:1 — Missing required JS file structure | sk-code JavaScript checklist P0: file has a box header with component identification and numbered ALL-CAPS sections; JavaScript style guide requires boxed header and section organization. | fix: Add boxed component/purpose header and numbered IMPORTS/CONSTANTS/PLUGIN FACTORY sections.
- [P2] .opencode/plugins/mk-spec-memory.js:210 — Default export lacks JSDoc | sk-code JavaScript quality standards §5 / JavaScript checklist P2: public or exported functions have JSDoc documentation. | fix: Add JSDoc for the plugin context, options, and returned hooks/tools object.
- [P0] .opencode/plugins/session-cleanup.js:1 — Missing required JS file structure | sk-code JavaScript checklist P0: file has a box header with component identification and numbered ALL-CAPS sections; JavaScript style guide requires boxed header and section organization. | fix: Use the OpenCode plugin box header and numbered sections; keep a shebang only if this is actually a CLI entrypoint.
- [P1] .opencode/plugins/session-cleanup.js:17 — CommonJS used in ESM plugin | sk-code JavaScript quality standards §2 and JavaScript checklist P1: .opencode/plugins/* must follow the OpenCode plugin ESM loader path with export default. | fix: Use ESM imports and derive the directory from import.meta.url/fileURLToPath.
- [P2] .opencode/plugins/session-cleanup.js:38 — Lowercase comment sentence | sk-code JavaScript style guide §7: all comment text must start with a capital letter, except directives or technical identifiers. | fix: Capitalize the comment and keep the durable WHY.
- [P0] .opencode/scripts/claude-session-cleanup.sh:1 — Missing shell strict mode | Shell Quality Standards P0 Strict Mode: every shell script must enable `set -euo pipefail`; alignment verifier enforces SH-STRICT-MODE. | fix: Add `set -euo pipefail` near the top before `exec`.
- [P0] .opencode/scripts/git-hooks/post-commit:20 — Hook uses partial strict mode | Shell Quality Standards P0 Strict Mode: every shell script must enable `set -euo pipefail`. | fix: Use full strict mode and guard intentional non-blocking commands.

After editing, re-read each edited line. Your final message MUST be exactly one fenced json block:
```json
{"seat":"BF04","fixed":[{"file":"path","theme":"","after":""}],"refuted":[{"file":"path","why":""}]}
```
