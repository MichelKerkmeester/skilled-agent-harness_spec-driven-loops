# Iteration 020: RQ-M1 specialized-agent mode-checklists

**Focus:** RQ-M1 specialized-agent mode-checklists  
**Executor:** cli-opencode `openai/gpt-5.5-fast` --variant high (read-only). **Status:** complete. **newInfoRatio:** 0.05.  
**Raw output:** prompts/iteration-020.out  ·  **Prompt:** prompts/iteration-020.prompt

### FINDINGS
| Sub-feature | gem-team mechanism (file:line) | spec-kit equivalent or GAP (file:line) | Verdict | Effort(S/M/L) | Net-new value |
|---|---|---|---|---|---|
| Behavior-preserving simplification order | gem-code-simplifier:51-52 safe-order: unused imports→dead code→rename→flatten→extract→reduce complexity→consolidate duplicates | GAP. `removal_plan.md` (sk-code-review) covers dead-code identification/deferred removal classification but not a concrete safe execution order for simplification work. `code_quality_checklist.md:96-108` flags maintainability signals (magic literals, nesting, naming) but does not prescribe an operation sequence. | ADAPT | S | Moderate — ordered simplification discipline is a reusable quality asset |
| Chesterton's Fence + test-after-each-change | gem-code-simplifier:47-48 (git blame/tests before removal), :55-58 (run tests after each change, fail→revert/escalate) | PARTIAL. `sk-code/SKILL.md:234` "Run surface verification commands and record evidence"; `review.md:162-165` has edge-case/error-handling checks. But no explicit "test after every atomic simplification step" or "revert on failure" contract in review or code quality docs. | ADAPT | S | Low — strengthens existing verify-first principle |
| Code smell taxonomy (feature envy, god class, primitive obsession, magic numbers, long param list) | gem-code-simplifier:72 | PARTIAL. `code_quality_checklist.md:96-108` covers magic literals (:99), nesting (:100), long functions (:103), deep chaining (:104). Missing: feature envy, god class, primitive obsession as named patterns. | REJECT | — | Negligible — existing maintainability signals cover the actionable subset; named taxonomy adds no verification value |
| Refactoring operation catalog (Extract Method/Class, Rename, Introduce Param Object, etc.) | gem-code-simplifier:75 | GAP. No equivalent named-operation catalog exists in sk-code or @review. | REJECT | — | None — these are implementation techniques, not review/quality checklist items; the reviewer doesn't need to prescribe HOW to refactor |
| Public contract protection (exported funcs, API handlers, DB schema, config keys, route paths) | gem-code-simplifier:126 | COVERED. `code_quality_checklist.md:75-91` (contract safety, breaking changes to public APIs, P0 for breaking changes with no migration path). `review.md:176` (event handling follows project patterns). `sk-code-review/SKILL.md:287` (fix-scope classification includes cross-consumer). | REJECT | — | None |
| E2E flow sequence (Open→Precondition→Fixture→Flow→Assert→Evidence→Cleanup) | gem-browser-tester:47-54 | GAP. `mcp-chrome-devtools/SKILL.md` provides screenshot/HAR/DOM tools but no structured E2E flow template. `sk-code/SKILL.md:236` requires "desktop and mobile browser behavior" evidence but no flow-sequence contract. | REJECT | — | Low — E2E flow sequencing is test-framework-specific (Playwright/Cypress), not a universal review checklist; sk-code's browser evidence rule is sufficient |
| Console error/warning + network failure (≥400) capture per page | gem-browser-tester:56-57 | PARTIAL. `mcp-chrome-devtools/README.md:97` ("multi-viewport-test.sh checks for console errors at each viewport"); `sk-code/SKILL.md:191` ("desktop/mobile browser console clean evidence when runtime behavior changes"). Network-failure capture at ≥400 is not explicitly required. | ADAPT | S | Low — console errors already required; network-failure capture is a small addition to browser evidence contract |
| A11y audit at initial load → major UI change → final verification | gem-browser-tester:105 | PARTIAL. `sk-code/references/webflow/implementation/animation_workflows.md:501` ("Required testing for accessibility compliance"); `sk-code/references/motion_dev/performance_and_pitfalls.md:69` (reduced-motion guidance). No structured a11y audit schedule (initial→change→final). Iteration 012 flagged "accessibility_snapshot over screenshots" as a gap (`iteration-012.md:14`). | ADAPT | S | Moderate — structured a11y audit schedule is a concrete quality gate improvement |
| Visual regression: baseline first, compare subsequent (threshold 0.95) | gem-browser-tester:112 | COVERED. `mcp-chrome-devtools/README.md:31` ("multi-viewport visual regression testing"); `mcp-chrome-devtools/examples/multi-viewport-test.sh` exists. `sk-code/manual_testing_playbook` MR-004 captures baseline videos. | REJECT | — | None |
| Browser content UNTRUSTED + Observation-First discipline | gem-browser-tester:108-109 | COVERED. `review.md:326` ("No hallucinated or false-positive issues — all findings traceable to source"); adversarial self-check protocol (:364-392). Observation-first is implicit in the read-before-edit mandate (`AGENTS.md` Four Laws). | REJECT | — | None |
| Deploy approval gate (requires_approval OR security_sensitive OR production) | gem-devops:49-54 | GAP. No deploy-approval gate exists in spec-kit. `sk-code/SKILL.md:181` lists DEPLOYMENT intent but `RESOURCE_MAP` has no deployment resource mapping. `AGENTS.md:253` does not mention deployment gates. | REJECT | — | None — spec-kit is a spec/workflow framework, not a deployment tool; deployment approval belongs in infra tooling, not review checklists |
| Pre-Deploy checklist (tests, code review, env vars, migrations, rollback plan) | gem-devops:107 | GAP. No pre-deploy checklist. Closest: `review.md:198-218` PR-specific checks (title, description, breaking changes, tests included) — but these are PR review, not deploy-gate items. | REJECT | — | None — deploy checklists are deployment-pipeline concerns, not code-review concerns |
| Post-Deploy checklist (health check OK, monitoring, old pods terminated, documented) | gem-devops:107 | GAP. No post-deploy checklist in spec-kit. | REJECT | — | None — same rationale as pre-deploy |
| Production Readiness checklist (secrets, JSON logging, health check, pinned versions, resource limits, SSL/TLS, CVE scan, CORS, rate limiting, security headers, rollback tested, runbook) | gem-devops:107 | PARTIAL. `sk-code-review/references/security_checklist.md` covers secrets/auth/injection. `code_quality_checklist.md` covers correctness. Missing: pinned versions, resource limits, SSL/TLS, CVE scan, CORS, rate limiting, security headers (CSP/HSTS/X-Frame-Options), runbook. | REJECT | — | Low — security checklist covers the code-review-relevant subset; infra-specific items (SSL, CVE scan, headers) belong in deployment tooling |
| Health check contracts (GET /health → { status: "ok" }, detailed: deps, uptime, version) | gem-devops:87-88 | GAP. No health-check contract template in spec-kit. | REJECT | — | None — health check contracts are application/deployment concerns, not spec-kit scope |
| Deployment strategies (Rolling, Blue-Green, Canary) | gem-devops:69 | GAP. Not in spec-kit. | REJECT | — | None — infrastructure strategy, not code/spec framework concern |
| Docker best practices (specific tags, multi-stage, non-root, HEALTHCHECK, resource limits) | gem-devops:73-75 | GAP. Not in spec-kit. | REJECT | — | None — container best practices belong in infra/deployment tooling |
| K8s probes (liveness, readiness, startup) | gem-devops:79 | GAP. Not in spec-kit. | REJECT | — | None |
| Feature flag lifecycle (Create→Enable→Canary→25%→50%→100%→Remove) | gem-devops:101-103 | COVERED (different domain). `feature_catalog/17--governance/249-feature-flag-governance.md` + `19--feature-flag-reference/` govern spec-kit's own feature flags with sunset audits, graduation, and dead-code removal. `feature_catalog.md:4112` documents Sprint 8 flag graduation + dead code removal. Gem's lifecycle model is for app-level feature flags (canary rollout); spec-kit's is for search/pipeline flags. Different domains, both covered. | REJECT | — | None |
| Idempotent operations | gem-devops:57,163 | COVERED. `027/008-caura-memclaw` research extensively covers idempotency for spec-kit memory (T1 idempotency receipts, CAS, content-hash reuse). For code review: `code_quality_checklist.md:86` ("Are implicit behavioral guarantees — ordering, timing, idempotency — preserved?"). | REJECT | — | None |
| Rollback strategies (K8s, Vercel, Docker) | gem-devops:95-97 | PARTIAL. `sk-code/references/webflow/deployment/webflow_staging_production.md:146,166` covers Webflow rollback (`?v` flip). Generic rollback not in spec-kit. | REJECT | — | None — stack-specific rollback is deployment tooling |

**[F-020-01]** The simplification-mode checklist (ordered safe operations + behavior-preservation contract) is the strongest net-new candidate. `sk-code-review/references/removal_plan.md` identifies WHAT to remove but not HOW to simplify safely. An ADAPT as a lightweight `assets/opencode/checklists/simplification_mode.md` — loaded as an optional overlay when `sk-code` intent is `CODE_QUALITY` + `REMOVAL` — would fill a real gap without adding agents. Effort: S.

**[F-020-02]** The a11y audit schedule (initial load → major UI change → final verification) is a concrete quality gate improvement. `sk-code` requires browser evidence but does not structure WHEN a11y audits should fire. An ADAPT adding a one-line a11y audit trigger to `sk-code`'s WEBFLOW verification section (`SKILL.md:236`) would be sufficient. Iteration 012's "accessibility_snapshot over screenshots" finding (`iteration-012.md:14`) supports this.

**[F-020-03]** 17 of 20 sub-features are REJECT because spec-kit already covers them (7) or because they are infrastructure/deployment concerns outside spec-kit's scope (10). Spec-kit is deliberately a spec + workflow + review framework, not a deployment pipeline or E2E test runner. The DEPLOYMENT intent in `sk-code`'s `INTENT_SIGNALS` (`references/smart_routing.md:45`) exists as a keyword hook but intentionally has no `RESOURCE_MAP` entry — the gap is by design, not oversight.

**[F-020-04]** The console-error + network-failure capture item is a marginal ADAPT. Console errors are already required by `sk-code/SKILL.md:191`. Adding explicit network-failure capture (≥400 status) as a one-line addition to the WEBFLOW verification contract would close the gap at negligible cost. The `mcp-chrome-devtools` scripts already capture HAR traces that include network failures — the gap is in the verbal contract, not the tooling.

### NEGATIVE / RULED-OUT
- Adding gem-code-simplifier, gem-browser-tester, or gem-devops as new agents: REJECT. The spec-kit's `@review` + `sk-code` + `sk-code-review` baseline+surface-evidence model already handles code quality, browser evidence, and review. New agents would fragment the skill routing surface.
- Deploy approval gates in spec-kit: REJECT. Spec-kit governs specs and code quality, not deployment pipelines. Approval gates belong in CI/CD tooling (GitHub Actions, etc.).
- Docker/K8s/container best practices: REJECT. Out of scope for a spec+workflow framework.
- Feature flag lifecycle adoption from gem-devops: REJECT. Spec-kit already has its own feature-flag governance (`249-feature-flag-governance.md`) with sunset audits. Gem's canary-rollout model is for application feature flags, a different domain.

### OPEN QUESTIONS
- Should `sk-code`'s WEBFLOW verification section explicitly require network-failure capture alongside console errors? Current wording is ambiguous ("browser console clean evidence" — does this include network tab?).
- Is there a case for a `DEPLOYMENT` resource map entry in `sk-code` for Webflow staging/production workflows that already exist at `references/webflow/deployment/`?
- Should the "accessibility_snapshot over screenshots" preference from iteration 012 be codified in `sk-code`'s verification contract or left as implicit guidance in `mcp-chrome-devtools`?

### METRICS
newInfoRatio: 0.05
novelty: Only 2 of 20 sub-features (simplification-mode checklist, a11y audit schedule) are genuine gaps; the remaining 18 are either already covered or outside spec-kit scope by design.
status: complete
focus: RQ-M1 specialized-agent mode-checklists
