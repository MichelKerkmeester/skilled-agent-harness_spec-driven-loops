---
title: "Tasks: Manual Playbook Sweep Findings Remediation [template:level_2/tasks.md]"
description: "One task per confirmed FAIL finding; Phase 1 (plan) done per-finding as discovered, Phase 2 (fix) deferred to a future implementation session."
trigger_phrases:
  - "playbook sweep findings remediation tasks"
importance_tier: "important"
contextType: "implementation"
---
# Tasks: Manual Playbook Sweep Findings Remediation

<!-- SPECKIT_LEVEL: 2 -->

---

<!-- ANCHOR:notation -->
## Task Notation

| Prefix | Meaning |
|--------|---------|
| `[ ]` | Pending |
| `[x]` | Completed |
| `[P]` | Parallelizable |
| `[B]` | Blocked |

**Task Format**: `T### Description (scenario id / file path)`

<!-- /ANCHOR:notation -->
---

<!-- ANCHOR:phase-1 -->
## Phase 1: Plan (done incrementally as the sweep confirms findings)

- [x] T001 Scaffold this remediation packet (spec.md, plan.md, tasks.md, checklist.md)
- [x] T002 Draft root-cause hypothesis + fix direction for the 26 findings confirmed as of manifest 170/485 done
- [ ] T003 [P] Append new finding entries as the sweep confirms more FAILs (recurring, not a one-time task)

<!-- /ANCHOR:phase-1 -->
---

<!-- ANCHOR:phase-2 -->
## Phase 2: Fix (deferred — one task per finding, not yet started)

> **Process note (2026-07-02)**: 0030 and 0033 both self-reported `VERDICT: FIXED` with an empty `git diff`. Root cause was NOT dispatcher/edit-persistence failure (independently ruled out via a controlled diagnostic dispatch on an untracked scratch file, which persisted correctly). Both findings' underlying bugs were already fixed by unrelated prior packets (029, 030) that landed in this repo before the fix dispatcher ran — the dispatch contract's OUTPUT CONTRACT lacked an `ALREADY_CORRECT` verdict tag distinct from `FIXED` (implying a change was made), so the model reused the closest label. **Going forward**: an empty `git diff` on a `FIXED` verdict is not automatically a failure — read the dispatch's own reasoning log before reclassifying; if it cites specific existing code matching the fix, treat as `ALREADY_CORRECT` (done, evidence-backed) rather than `[B]` blocked/retry.

- [x] T-0003 Fix BM25 re-index gate trigger-mutation detection (0003) — FIXED: added getBm25EngineStatus() check so syncChangedRows/retry-manager only treat 0-synced-rows as a real problem when the in-memory engine was actually warm, not when it's a no-op. Verified via git diff (19 insertions/4 deletions across bm25-index.ts + retry-manager.ts)
- [x] T-0004 Restore bounded-graph trace fields in result envelope (0004) — FIXED: added maxDepth/depth/path/paths/seedIds/reachedIds/bounded to MemoryResultTrace + sanitizer helpers in search-results.ts (36 insertions), verified via git diff
- [x] T-0015 Fix trigger-phrase cache reload/index (0015) — FIXED: added `trigger_phrases IS NOT NULL AND != '[]' AND != ''` filter to TRIGGER_CACHE_LOADER_SQL in trigger-matcher.ts (3 insertions). Verified via git diff
- [x] T-0016 Fix `memory_context` specFolder/intent mismatch (0016) — FIXED: gated resume-mode inference behind `!explicitIntent` in resolveEffectiveMode() (memory-context.ts, 3 lines). Verified via git diff
- [x] T-0030 Fix `memory_list` filter/specFolder path (0030) — RECLASSIFIED, ALREADY CORRECT: dispatch's `git diff` was empty because `buildSpecFolderCandidates()` (memory-crud-list.ts:36-60) plus the `spec_folder = ? OR spec_folder LIKE ?candidate/%'` predicate (line 158-165) already implements the exact specs-root-relative-vs-`.opencode/specs/`-prefixed fallback the finding described. Landed in commit `caeb3f61e1` (packet 030, "land phase 011, deep-review remediation, and dist-freshness enforcement") — predates this session's fix dispatcher. Verified via direct read + `git blame`. No code change needed; original sweep FAIL was stale relative to already-shipped packet-030 work.
- [ ] T-0032 [B] Re-verify `session_health` with daemon warm before fixing (0032) — blocked on confirming this isn't the recurring env gap
- [x] T-0033 Fix `session_resume` phase-parent redirect ordering (0033) — RECLASSIFIED, ALREADY CORRECT: `synthesizeResult()` (resume-ladder.ts:804-818) already spreads `...(phaseParent ? { phaseParent } : {})` as the FIRST key in the result object, ahead of `source`/`summary`/child-continuity fields. Landed in commit `4913ddf6f9` (packet 029, "phase-parent resume redirect"). Verified via direct read + `git blame`. No code change needed; original sweep FAIL was stale relative to already-shipped packet-029 work.
- [x] T-0039 Fix `detectRuntimeMismatch` count/assertion drift (0039) — ALREADY_CORRECT: implementation (startup-checks.ts:47-55) and focused test (startup-checks.vitest.ts:53-63) already agree (checks only ABI/platform/arch, expects exactly `['platform','architecture']`). Verified via `npm test -- --run tests/startup-checks.vitest.ts -t detectRuntimeMismatch` (1 passed) + empty `git diff`. No change needed.
- [x] T-0040 Fix spec-doc exclusion under `qualityGateMode: 'warn-only'` (0040) — FIXED: memory-index.ts's `runIndexScan` now unions `specDocFiles`+`graphMetadataFiles` into `specDocKeySet`, collects unchanged spec docs into `returnedUnchangedSpecDocs`, and merges them into the returned `files` array instead of silently dropping them (30 insertions/1 deletion). Verified via git diff.
- [x] T-0048 Fix needs-rebuild sentinel not clearing on successful boot rebuild (0048) — FIXED: `repairNeedsRebuildSentinel()` (checkpoints.ts) now re-checks `hasNeedsRebuildSentinel()` after `clearNeedsRebuildSentinelForDatabase()` and returns an explicit error state if the sentinel is still present instead of trusting the clear call silently succeeded. `context-server.ts` needed no change (already consumes the repair result correctly). Verified via git diff (10 insertions).
- [ ] T-0056 [B] Add `scope` to `memory_causal_stats` accepted parameters, or correct scenario expectation (0056) — NEEDS_MORE_INFO: dispatch found the real rejecting schema lives outside its allowed write paths (`mcp_server/handlers/**/*.ts` was too narrow) and correctly stopped rather than making a speculative handler-only change. Needs a follow-up dispatch with the actual schema file in scope.
- [x] T-0059 Fix `memory_get_learning_history` over-filtering (0059) — ROOT_CAUSE_DIFFERENT, FIXED: real bug was raw `spec_folder = ?` exact-match over-filtering in session-learning.ts's `handleGetLearningHistory` (same class as 0030, different file). Added `buildSpecFolderCandidates()`/`buildSpecFolderWhereClause()` mirroring 0030's pattern, applied to both the history query and the summary aggregate query. Verified via git diff (43 insertions/4 deletions).
- [ ] T-0062 [B] Re-run scenario with stricter evidence requirement before proposing a fix (0062)
- [x] T-0064 Fix eval reporting dashboard 0-eval-runs issue (0064) — FIXED: `getDb()` in reporting-dashboard.ts was falling through to `initEvalDb()` with no dir override, defaulting to the empty compiled `dist/database/speckit-eval.db` shadow instead of the real `mcp_server/database/speckit-eval.db` (9 metric snapshots, 808 channel rows, 930 queries). Added `resolveDefaultEvalDbDir()` to detect dist-vs-source and point at the real package database dir. Verified: dispatch's own before/after query counts (0 runs from dist DB -> 2 runs/2 sprint groups from real DB), `npm run typecheck` passed, 58 vitest tests passed, git diff confirmed (16 insertions/1 deletion).
- [x] T-0074 Replace remaining unsafe `Math.max/min` spread call sites (0074) — FIXED: replaced remaining `Math.max(...arr)`/`Math.min(...arr)` spreads with reduce-based extrema across 6 production files (shadow-evaluation-runtime.ts, rank-metrics.ts, k-value-analysis.ts, composite-scoring.ts, graph-lifecycle.ts, intent-classifier.ts) + 11 test files. Verified via git diff (26 files touched total across the wave, cross-checked each against every dispatch's own self-reported file list to isolate this fix's real scope — see process note below), `npm run typecheck` passed, 336+3 targeted vitest passed, scoped ESLint passed.
- [x] T-0077 Fix sha-256 dedup lookup predicate (canonical-path resolution before dedup check) (0077) — FIXED: create-record.ts's `findSamePathExistingMemory` replaced the `(canonical_file_path = ? OR file_path = ?)` OR-shape with `COALESCE(NULLIF(canonical_file_path, ''), file_path) = ?` (single resolved path before comparing), refactored scope matching into `buildScopeWhereClauses`, and fixed an anchor_id NULL/empty-string comparison inconsistency. Added a divergent-alias regression test. Verified via git diff (59 lines changed).
- [x] T-0079 Fix inert/active logger-gate toggle default (0079) — FIXED: `isConsumptionLogEnabled()` in consumption-logger.ts now requires `SPECKIT_CONSUMPTION_LOG` to explicitly equal `'true'`/`'1'` before consulting `isFeatureEnabled()`, instead of defaulting to graduated-ON. Verified via git diff (8 lines changed) — legitimate, narrow fix.

> **Process note (2026-07-02, wave-1 rerun)**: cross-checking every file in this wave's combined diff against each dispatch's own self-reported file list surfaced 2 files with unattributed content — `mcp_server/tests/generated-metadata-integrity.vitest.ts` (32 insertions) and `mcp_server/tests/graph-metadata-schema.vitest.ts` (53 insertions). Neither appears in 0074/0077/0079's own claimed file lists; 0074's own dispatch log explicitly noted "I observed other dirty files under mcp_server that were not part of this patch; I'm leaving them untouched." Content matches the unrelated, still-unimplemented 011-create-sh-parent-corruption-fix/012-derive-status-explicit-bypass-fix packets (references "T2-P1-002", "gpt-followup-audit"). A genuinely separate concurrent session (PID 53528/53522, distinct session ID, working a `deep-loops` packet) was also confirmed running in this same repo — most likely origin. These 2 files were deliberately excluded from every commit in this batch; not ours to commit.
- [x] T-0085 Wire eval DB write path for `memory_search`/`memory_context` events (0085) — ALREADY_CORRECT: both handlers already call `logSearchQuery`/`logFinalResult`/`logChannelResult` (memory-search.ts:1073,1780,1790; memory-context.ts:1625,2055,2067), writing into `eval_queries`/`eval_channel_results`/`eval_final_results` via eval-logger.ts. Verified via `npx vitest run tests/memory-search-eval-channels.vitest.ts tests/memory-context-eval-channels.vitest.ts tests/eval-logger.vitest.ts` (34 passed) + empty git diff on allowed paths. No change needed.
- [ ] T-0087 [B] Re-run int8 quantization benchmark, update no-go decision record (0087) — NEEDS_MORE_INFO: dispatch found current data refutes a simple no-go reaffirmation, but the canonical decision record lives outside the allowed write paths (`.opencode/specs/**/*.md` was too broad/wrong target). Needs a follow-up dispatch with the exact decision-record path and the refuting data summarized in the brief.
- [x] T-0110 Fix `SPECKIT_GRAPH_UNIFIED` Stage-2 flag propagation/cache invalidation (0110) — ROOT_CAUSE_DIFFERENT, FIXED: real bug was `buildCacheArgs()` in search-utils.ts not including the flag's live state in the cache key, so a toggle didn't invalidate a cached computation. Added `graphUnifiedEnabled: isGraphUnifiedEnabled()` to the cache-key args. Verified via git diff (2 lines: import + key field) — matches the Group A shared flag-propagation pattern.
- [x] T-0113 Fix adaptive-ranking `adaptiveShadow` proposal emission gate (0113) — ROOT_CAUSE_DIFFERENT, FIXED: real bug was `buildAdaptiveShadowProposal` sometimes receiving result IDs as numeric strings, which silently missed the number-keyed signal-delta lookup, so recorded adaptive deltas never applied. Added `normalizeAdaptiveMemoryId()` to coerce/validate IDs before all downstream lookups. Verified via git diff (26 lines changed, single file).
- [x] T-0126 Correct scenario doc's stale Vitest baseline count (350 → current), confirm no real regression first (0126) — DOC_FIX: updated stale baseline 350->365 in scoring-and-fusion-corrections.md (test suite grew, no regression). Verified via git diff.
- [x] T-0129 Fix Stage-2 score sync for non-hybrid path / confirm routing (0129) — FIXED: requests weren't mis-routed (isHybrid correctly derives from config.searchType), but `syncScoreAliasesInPlace` in stage2-fusion.ts only synced rows with a finite `row.score`, leaving `intentAdjustedScore` unset for vector/non-hybrid rows whose score resolved via `similarity`/`rrfScore` instead. Now falls back through `resolveEffectiveScore`. Verified via git diff (single file, 6 lines) + typecheck/vitest/eslint passed per dispatch log.
- [x] T-0133 Apply `QUALITY_FLOOR` as a hard filter before min-representation selection (0133) — FIXED: `analyzeChannelRepresentation` in channel-representation.ts now pre-filters each channel's results to `score >= QUALITY_FLOOR` before determining under-represented channels and promotion candidates, so sub-floor results (0.004/0.001) can no longer force a representation slot. Updated matching doc comments in channel-enforcement.ts. Verified via git diff (both files).
- [x] T-0134 Wire `confidenceTruncation` trace metadata into real query execution path (0134) — ROOT_CAUSE_DIFFERENT, FIXED: real gap was `executePipeline` (orchestrator.ts) never calling `truncateByConfidence` at all on the real query path, only exercised in synthetic tests. Now calls it when `isConfidenceTruncationEnabled()`, builds `ConfidenceTruncationMetadata` (new type in types.ts), attaches it per-row via `traceMetadata`, records it in `PipelineResult.metadata.confidenceTruncation`, and adds a trace entry. Verified via git diff (orchestrator.ts + types.ts, both within 0134's own allowed paths).
- [x] T-0156 Gate `encoding_intent` writes behind first-time-index check in `indexMemoryDeferred` (0156) — FIXED: one-argument-order flip in vector-index-mutations.ts, `COALESCE(?, encoding_intent)` (incoming value always wins) -> `COALESCE(encoding_intent, ?)` (existing value wins, only fills when unset). Added regression test (encoding-intent.vitest.ts, 40 lines). Verified via git diff.
- [x] T-0174 Fix `E_SESSION_SCOPE` on `memory_context` folder-routing check (0174) — FIXED: `handleMemoryContext` was always setting `options.sessionId = effectiveSessionId` even when no session was actually requested, tripping session-scoped routing unintentionally. Now gated behind `requestedSessionId` truthiness. Verified via git diff (4 lines, memory-context.ts) + test update.
- [x] T-0175 [P0] Fix `vec_memories`/`vec_768` dual-write desync and blob-encoding mismatch (0175) — FIXED, verified by direct diff read (not just trusting the verdict): reindex.ts unified `writeVectors`/`writeVectorsToKnn` into one `writeVectorsToShardTables()` running BOTH inserts inside a single `db.transaction()`, using the same `to_embedding_buffer(embedding)` result for both `vec_<dim>` and `vec_memories` (previously two separate transactions, the root cause of the row-count desync). factory.ts's shard-fallback filename now uses `createProfileSlug(provider, manifest.name, dim)` — the same slug function real shard creation uses — instead of raw unsanitized name interpolation, fixing lookup failures for names with special characters (e.g. `nomic-embed-text:v1.5`). Dispatch ran typecheck + 5 targeted tests but did NOT re-run the original finding's KNN self-match distance check directly; compensated by reading the full diff myself given this is P0 data-integrity code.
- [x] T-0176 Fix quality-check retry loop not honoring `maxRetries` override (0176) — ROOT_CAUSE_DIFFERENT, FIXED: `runQualityLoop` (quality-loop.ts) had an early `break` that ended the retry loop after the first attempt where `fixResult.fixed.length === 0`, regardless of `maxRetries`. Removed the break so the loop runs the full configured retry count. Updated quality-loop.vitest.ts. Verified via git diff (4-line removal + test update).
- [x] T-0183 Fix DB hot-rebinding: invalid tier "scratch" + missing `.db-updated` marker + degraded health (0183) — ROOT_CAUSE_DIFFERENT, FIXED, both parts: (1) `scratch` was never a valid tier (confirmed against both CLI/MCP tier allowlists) — corrected the scenario doc to use the real tier `temporary`, code untouched. (2) The marker-detection/rebind path already existed correctly; the real gap was the standalone `bulk-delete` CLI command never writing `DB_UPDATED_FILE` after mutating. Added `notifyDatabaseUpdated()` to cli.ts, called after a successful delete. Verified via git diff (cli.ts + scenario doc) + typecheck/eslint/5 vitest tests passed per dispatch log.
- [x] T-0185 [P0] Audit/fix DB-path construction so all consumers use the shared resolver (0185) — ROOT_CAUSE_DIFFERENT, FIXED, verified by direct diff read (not just trusting the verdict): confirmed root cause was narrower than the brief — eval consumers derived their default DB *directory* independently instead of directly duplicating `context-index.sqlite` construction. eval-db.ts now resolves via `resolveDatabasePaths()` (core/config.ts, the canonical resolver) both at module load and per-call instead of independent env-var precedence logic; reporting-dashboard.ts's dist-vs-source workaround (0064's earlier narrower patch) was consolidated into this same fix and simplified back to a plain `initEvalDb()` call; memory-state-baseline.ts replaced `resolveActiveProfileDbPath()` + a duplicate MEMORY_DB_PATH check with the same canonical resolver. 33 targeted tests + typecheck + alignment drift passed per dispatch log.
- [x] T-0188 Confirm embedding model catalog change is intentional; fix scenario doc or revert (0188) — DOC_FIX: confirmed via grep of shared/embeddings/registry.ts that the model-default change (nomic-embed-text-v1.5 / nomic-ai/nomic-embed-text-v1.5) is intentional and current; corrected the scenario doc's stale expected IDs (unsloth/bge-base-en-v1.5-GGUF, onnx-community/bge-base-en-v1.5-ONNX) and flipped its verdict FAIL->PASS. Also resolves the recurring doc-model-refs drift advisory seen on every prior commit in this batch. Verified via git diff.
- [x] T-0190 Confirm shadow-period gate is working-as-designed or misconfigured for learned-trigger persistence (0190) — DOC_FIX: confirmed working-as-designed (intentional 7-day log-only shadow period before triggers apply). Corrected scenario doc's Expected/Pass-Fail sections to require `reason: "shadow_period"` as the correct outcome instead of immediate persistence; flipped verdict FAIL->PASS. Verified via git diff.
- [x] T-0192 Remove remaining V1 pipeline code path and `isPipelineV2Enabled()` flag check (0192) — ROOT_CAUSE_DIFFERENT, FIXED: the executable V1 path and `isPipelineV2Enabled()` were already fully removed; the scenario's "V1 references" hits were stale comments referencing the already-deleted code. Removed the dead comments from memory-search.ts and pipeline-v2.vitest.ts. Verified via git diff — comment-only change, no behavior change.
- [ ] T-0193 [B] Re-verify lineage backfill rollback drill with fuller evidence capture (0193)
- [ ] T-0194 [B] Re-verify lineage state asOf/projection coverage with fuller evidence capture (0194)
- [x] T-0200 Fix `ENABLE_BM25` default-enabled inversion in `bm25-index.ts` (0200) — FIXED: `isBm25Enabled()` returned `true` when `ENABLE_BM25` was unset, contradicting the documented default-disabled/opt-in behavior. Flipped the unset-value default to `false`. Verified via git diff (1 line).
- [x] T-0205 Fix `SPECKIT_STRICT_SCHEMAS=false` not relaxing strict validation in `tool-schemas.ts` (0205) — ROOT_CAUSE_DIFFERENT, FIXED: every tool schema hardcoded `additionalProperties: false`, ignoring the flag entirely regardless of its value. Added module-level `ALLOW_UNKNOWN_PARAMETERS = process.env.SPECKIT_STRICT_SCHEMAS === 'false'` and replaced every hardcoded `false` (~40 tool schemas) with this constant. Verified via git diff — mechanical, consistent substitution across the whole file, no scope creep.
- [ ] T-0208 [B] Investigate tri-daemon respawn-lock/reap-divergence before fixing (0208) — needs careful multi-daemon lifecycle tracing first
- [ ] T-0211 Fix causal boost never applying despite default-on -- check shared Group A flag-reading root cause first (0211) — queued for next fix-dispatch round, not yet in the running batch
- [ ] T-0212 Fix community-search fallback never surfacing despite default-on -- check shared Group A flag-reading root cause first (0212) — queued for next fix-dispatch round, not yet in the running batch

<!-- /ANCHOR:phase-2 -->
---

<!-- ANCHOR:phase-3 -->
## Phase 3: Verification (deferred to Phase 2 completion)

- [ ] T900 Re-run each fixed scenario's own playbook file to confirm PASS
- [ ] T901 Run the affected subsystem's Vitest suite with each fix, confirm no regressions
- [ ] T902 Update this packet's `implementation-summary.md` with final fix outcomes

<!-- /ANCHOR:phase-3 -->
---

<!-- ANCHOR:completion -->
## Completion Criteria

<!-- /ANCHOR:phase-2-appendix -->
<!-- ANCHOR:phase-2-appendix -->
### Phase 2 Appendix: findings from the resumed sweep (post wave-7 remediation close-out)

- [ ] T-0305 Add 17 undocumented `SPECKIT_*` flags to the feature-flags governance table (0305) — new finding from the resumed sweep, doc-only. Mechanical source/table comparison between `lib/search/search-flags.ts` and the documented feature-flags table (`ENV_REFERENCE.md`) found 17 `SPECKIT_*` flags defined in source that are missing from the governance table entirely (existing documented rows all have correct governance columns -- this is a completeness gap, not a malformed-row issue). Identify the 17 missing flags (re-run the scenario's own source/table diff command) and add governance table rows for each.
- [ ] T-0300 Confirm/fix `pnpm`-based invariance test command in template-composition-system scenario doc (0300) — new finding from the resumed sweep, likely doc-only. Scenario's documented invariance-test command invokes `vitest` via `pnpm`, but `pnpm` reports vitest not found -- every other dispatch this session has consistently used `npm run typecheck`/`npm test`/`npx vitest`, with no other evidence this repo uses pnpm as an alternate package manager. Confirm whether pnpm is genuinely supported here (env setup gap) or the scenario doc should reference `npm`/`npx` like everywhere else (simple doc fix). The "strict validation failed for every generated packet" part of this FAIL is the same pervasive existing-packet-issue pattern seen across nearly every dispatch this session, not a separate new issue.
- [ ] T-0284 Fix stale script path in session-capturing-pipeline-quality-coverage scenario doc (0284) — new finding from the resumed sweep, doc-only. Scenario's documented alignment-drift command is `python3 .opencode/skills/sk-code/scripts/verify_alignment_drift.py --root ...` (missing the `assets/` segment) -- confirmed the real path is `.opencode/skills/sk-code/assets/scripts/verify_alignment_drift.py` (used successfully by multiple fix dispatches this session, e.g. 0074/0175/0185). Fix the scenario doc's command path. The rest of this scenario's FAIL (npm run lint/check, spec validate.sh) is downstream of the already-tracked T-0240 lint backlog, not a separate issue.
- [ ] T-0280 Fix or confirm strict validation exit code for review-level packets (0280) — new finding from the resumed sweep. Scenario expects a `Level: review` packet to correctly waive heavy-doc requirements (plan.md/tasks.md/implementation-summary.md) AND exit 0 under `--strict`. Against the real packet `.opencode/specs/system-speckit/028-memory-search-intelligence/007-dark-flag-graduation/010-dark-flag-validation`: the review-level detection and heavy-doc waiver DID work correctly (no missing-doc complaints), but the overall run still exited `2`/`RESULT: FAILED` with 3 warnings. Needs disambiguation: are those 3 warnings genuine, unrelated pre-existing issues in this specific packet (meaning the scenario picked an imperfect example, doc-only fix -- point it at a cleaner review packet or note the caveat), or does strict-validate incorrectly hard-fail on ANY warning for review-level packets when it should only hard-fail on the heavy-doc requirements it's designed to waive (real bug in the exit-code logic)? Read strict-validate's review-level branch and the 3 specific warnings before concluding.
- [ ] T-0337 Confirm `MCP_MAX_MEMORY_TOKENS` defaults/validation keys are documented and retrievable (0337) — new finding from the resumed sweep, lower confidence/needs triage. A `memory_search` query for MCP configuration guardrails/defaults returned a weak result (`evidenceGap: true`, `requestQuality.label: "gap"`, `responsePolicy.requiredAction: "broaden_or_ask"`) and did not surface `MCP_MAX_MEMORY_TOKENS` defaults or validation setting keys. Could be a real documentation gap (the setting isn't documented anywhere indexed) or just a query-recall issue specific to this scenario's search terms -- confirm by grepping source/docs directly for `MCP_MAX_MEMORY_TOKENS` before treating as a real doc gap.
- [ ] T-0358 Confirm retired-runtime-shims scenario doc uses current symbol names (0358) — new finding, likely doc staleness. Scenario expects `calculateNoveltyBoost`/`noveltyBoostApplied`/`SPECKIT_NOVELTY_BOOST` symbols (with explicit "not applied" reporting) and an `RSF`/`SPECKIT_RSF` gate for adaptive fusion, but none of these exist in current source -- adaptive fusion is correctly gated by `SPECKIT_ADAPTIVE_FUSION` instead. Given this scenario's whole purpose is confirming deprecated features were properly retired, this reads like the doc still references pre-rename symbol names rather than a real functional gap. Confirm via git history whether novelty-boost/RSF were renamed to their current equivalents (doc-only fix) or genuinely removed without replacement (then the scenario's Expected section needs correcting to match, not the code).
- [ ] T-0357 Fix stale `sed` line-window reference in lazy-loading-migration scenario doc (0357) — new finding, doc-only. Targeted Vitest passed (2 files, 395 tests), but the scenario's documented `sed` line-range now prints graph-context code instead of the expected lazy-startup branch -- source has been refactored since the scenario doc's line numbers were written. Update the scenario doc's line range to point at the current lazy-startup branch location.
- [ ] T-0361 Fix compact-merger-assembly test output missing required evidence (0361) — new finding from the resumed sweep. All three documented vitest commands passed (1 passed, 4 passed), but the reporter output lacked the scenario's required section-header strings, allocation/token-estimate values, and deduplication metadata -- the tests pass but don't emit/log the diagnostic evidence the scenario contract expects to observe. Find the compact-merger test/reporter setup and confirm whether this evidence should be logged during the test run (missing instrumentation) or the scenario's expected command needs updating to a different verbosity flag/reporter.
- [ ] T-0366 Fix precompact-hook test coverage missing tailFile/extractTopics/mergeCompactBrief/cachedAt assertions (0366) — new finding from the resumed sweep, same recurring meta-pattern as T-0314/T-0361 (tests pass, 6/6, but don't actually assert the specific documented behaviors). Add explicit assertions for `tailFile`, `extractTopics`, `mergeCompactBrief`, and `cachedAt` signals, or confirm they're covered elsewhere. **Cross-cutting note**: T-0314, T-0361, T-0366, and now 0373 (session-start-compact: Vitest passes 10/1-skipped but stdout never prints the expected `Recovered Context`/`Recovery Instructions`/`updateState()`/`/speckit:resume` evidence) all show the same shape -- a passing test suite that doesn't demonstrate the specific behavior its own scenario/contract requires. Worth a single consolidated coverage-audit pass across these four areas rather than four independent fixes, since the root pattern (incomplete assertions/missing stdout instrumentation, not broken code) may be the same everywhere.
- [ ] T-0365 Fix passive-enrichment `hints` missing from non-memory tool responses (0365) — new finding from the resumed sweep. A known trigger phrase passed to `advisor_recommend` (a non-memory tool) returned no `hints` section at all, but the documented contract expects passive-context-enrichment hints on every tool response, not just memory-tool responses. Related to the response-hints module already flagged in the T-0314/0322 cluster (that cluster found `response-hints` IS exported from the hooks barrel, so the module exists -- this finding is about its application SCOPE, i.e. it may only be wired into memory-tool response paths, not universally applied to all MCP tools like `advisor_recommend`). Find where response-hints gets attached to responses and confirm whether non-memory tool handlers call it at all.
- [ ] T-0367 Fix query-intent classifier misrouting structural/hybrid queries to semantic (0367) — new finding from the resumed sweep. `memory_context` telemetry envelope is present and correct, but structural and hybrid query types both routed to `semantic` instead of the expected structural/code-graph route; additionally, a paraphrase of the same structural query changed `paraphraseGroup`, breaking the expected stability guarantee. Find the query-intent classifier and confirm whether structural/hybrid detection heuristics need tuning, or whether the routing decision downstream of classification is discarding the classified intent.
- [ ] T-0372 [P1] Fix `session_resume` strict-mode sessionId mismatch silently accepted + missing codeGraph fields (0372) — new finding from the resumed sweep, two issues, first is security-adjacent: (1) strict-mode with a mismatched `sessionId` returned `status: "ok"` instead of rejecting -- a mode specifically meant to enforce session-identity strictness is silently permissive on a real mismatch; (2) `codeGraph.available`/`codeGraph.binaryPath` fields are absent from the `session_resume({})` response entirely. **CAUTION -- first fix attempt reverted, confirmed regression**: a fix dispatch self-reported FIXED by removing the `callerCtx?.sessionId` truthiness guard entirely (rejecting whenever `requestedSessionId` is set but `callerSessionId` is null), but this broke an EXISTING, previously-passing test (`session-resume-auth.vitest.ts` "proceeds when callerContext.sessionId is null", line ~203) that encodes a deliberate, documented carve-out: under stdio transport, `callerCtx.sessionId` is always empty per the MCP SDK (single-UID subprocess, no cross-trust boundary, per the original code's own now-deleted comment), so a caller-supplied `sessionId` with no caller-context session to corroborate it was intentionally allowed through, not a bug. Verified via `npx vitest run tests/session-resume-auth.vitest.ts` (1 failed before revert, 9/9 pass after `git checkout` reverted the file). **CONFIRMED already-working**: a separate existing test ("throws on mismatched sessionId in default strict mode") shows the genuine two-sided-mismatch case (both `requestedSessionId` and `callerSessionId` present and different) was ALREADY correctly rejected before this fix touched anything -- meaning the original T-0372 scenario's FAIL may have been observed specifically under stdio transport (where the carve-out is intentional), not a real gap. **Next attempt must**: preserve the null-`callerSessionId` carve-out (do not touch that branch), and first confirm via the scenario's own dispatch environment/transport whether the originally-observed FAIL was genuinely over HTTP/WS (real gap, if so it's already fixed per the passing "throws on mismatched" test -- re-verify against current code before assuming a gap still exists) or stdio (intentional, scenario doc needs correcting instead of code). (2) codeGraph fields gap is untouched by the reverted fix, still needs its own separate investigation.
- [ ] T-0381 Fix `doctor` deep-loop convergence graph signals always empty (0381) — new finding from the resumed sweep. A genuinely qualifying target packet (24 research iterations) returned completely empty convergence graph signals: `totalNodes:0`, `totalEdges:0`, `signals:null`, `graph_signals_json:{}`, `trace:[]`, `graph_convergence_score:0`. Find the deep-loop convergence graph-signal computation in the doctor command and confirm whether it's reading from the wrong source (e.g. wrong iteration/state file glob) or the graph-building step is never actually invoked for real convergence checks.
- [ ] T-0424 Fix missing `convergence.cjs` call in deep-loop YAML workflow before stop logic (0424) — new finding from the resumed sweep. The scenario's specified YAML line ranges (deep-loop workflow) do not contain the expected `convergence.cjs` call before the stop-logic branch. **Caveat**: `convergence.cjs` is the exact file a separate concurrent session has been actively modifying this whole sweep (sliding-window-convergence-mode work, deliberately excluded from all commits in this packet) -- this FAIL may reflect that other session's in-progress, uncommitted state rather than a genuine gap in already-shipped code. Re-verify against the current committed state of the YAML workflow (not the live working tree) before treating this as a real bug.
- [x] T-0444 [P0] Skill-advisor classification bench regressed due to stale corpus gold-labels after a skill merge, not a real classifier quality drop (0444) — FIXED AND VERIFIED. Two automated fix-dispatch attempts timed out at 15 min each (real work, killed mid-verification/mid-edit, not a wrong approach); completed manually: relabeled the remaining stale rows in `labeled-prompts.jsonl` (confirmed all 53 rows now read `deep-loop-workflows`, file re-validated as well-formed JSONL), wired `mergedSkillForAlias()` into `advisor-validate.ts`'s `evaluateRows()` matching (required a small type-narrowing fix at the `skillMatchesAlias()` call site — guard on the derived `topForMatch`/`expectedForMatch` variables, not the original `result.topSkill`/`expected`, since TS can't infer the connection through the ternary). Verified: `tsc --noEmit` clean; full vitest suite shows identical 11 pre-existing failures with and without the fix (confirmed via `git stash` baseline, zero regressions introduced); real bench re-run via `node .opencode/bin/skill-advisor.cjs advisor_validate` shows **full-corpus 51.3%→77.7%** (now passes the live 0.75 threshold) and **`deep-loop-workflows` category 0/53→48/53 (90.6%)**, confirming the P0 driver is resolved. Holdout improved 42.5%→62.5% but still fails its 72.5% threshold (residual gap, not the P0 driver); `unknown_count` still 14 vs targetMax 10 (separate, minor, pre-existing signal). ROOT CAUSE CONFIRMED via earlier investigation. Documented Phase 027 baseline: 80.5% full-corpus top-1, 77.5% holdout top-1, unknown count <=10. Observed: full corpus **0.513** (99/193), holdout **0.425** (17/40), unknown count **14**. Per-skill breakdown shows the drop is concentrated: `sk-deep-research` 0/34 and `sk-deep-review` 0/19 (complete zero-match), `system-spec-kit` 28/55 (50.9%); every OTHER skill in the corpus is at or above the original 80.5% baseline (71/85 = 83.5% excluding those three). Confirmed cause: commit `ce9e313e7f` (2026-06-15, "merge five deep-loop skills into one mode-routed skill") deleted the standalone `deep-research`/`deep-review` skill nodes in favor of a single `deep-loop-workflows` hub skill (routed via `workflowMode`) — the live scorer correctly returns `deep-loop-workflows` now (per its own `routing-parity-deep-skills.vitest.ts`), but the bench corpus at `mcp_server/scripts/routing-accuracy/labeled-prompts.jsonl` still has 34 rows labeled `skill_top_1: "sk-deep-research"` and 19 labeled `"sk-deep-review"`, unchanged since the Phase 027 snapshot. `evaluateRows()` in `handlers/advisor-validate.ts` (~line 279-291) resolves aliases via `skillMatchesAlias()`/`canonicalSkillId()` in `lib/scorer/aliases.ts`, which still maps `sk-deep-research → deep-research` (the pre-merge legacy id) — `deep-loop-workflows !== deep-research`, so all 53 rows fail every time. `aliases.ts` already has the correct helper (`mergedSkillForAlias()`) but it's wired only into `advisor-recommend.ts`, never into the bench path. Precedent: an earlier rename (`mcp-coco-index → system-code-graph`, commit `d5166b5cd8`) DID get a corpus label sed-sweep; the June 15 merge skipped that step. `system-spec-kit`'s 28/55 is partially the same pattern (>=8/55 rows invoke now-separately-routed command-bridge skills like `/memory:save`/`/speckit:resume`, not covered by any alias group) but may retain a real residual gap in the rest. The `unknown_count`/`gold_none_false_fire_count` drift is a separate, minor, unexplained signal — not the P0 driver. **Fix**: relabel the 53 stale rows in `labeled-prompts.jsonl` to `deep-loop-workflows`, retag the spec-kit command-bridge rows to their current skill ids, and wire `mergedSkillForAlias()` into `advisor-validate.ts`'s matching so future skill renames/merges don't require a manual corpus sed.
- [ ] T-0451 Fix OpenCode plugin bridge not producing native Advisor brief (0451) — new finding from the resumed sweep. Direct bridge invocation returned `status: "skipped"`, `metadata.route: "native"`, `freshness: "unavailable"` instead of producing the expected native `Advisor:` brief output. Disable-flag behavior worked correctly (`route: "disabled"` when disabled). Find the OpenCode plugin bridge's native-route handling and confirm why it skips brief generation instead of producing one when route is "native" -- possibly related to T-0444's skill-advisor accuracy regression (same subsystem), confirm independently before assuming shared root cause.
- [ ] T-0327 Fix `progressiveDisclosure.continuation` never populating for pagination (0327) — new finding from the resumed sweep. A broad `memory_search({ query, limit: 20 })` call returned full results with `data.progressiveDisclosure` present, but `progressiveDisclosure.continuation` was `null`, meaning cursor pagination could not be exercised even though results were substantial enough to expect a continuation cursor. Find the progressive-disclosure response builder and confirm whether continuation cursor generation is wired for real (non-test) queries.
- [ ] T-0330 Fix result-explain-v1 schema/flag mismatches (0330) — new finding from the resumed sweep, three distinct issues: (1) documented `debug.enabled=true` parameter is rejected by schema validation (schema doesn't accept a documented flag); (2) the accepted debug mode's output is missing `channelContribution`; (3) with the feature flag OFF and no env override, `bypassCache:true` still returns the `why` explanation field when it should be absent -- same "flag doesn't gate behavior" pattern as the Group A findings (0110/0113/0200 etc.) fixed earlier this session. Check whether this shares the same root-cause class (flag state not reaching this code path) before fixing independently.
- [ ] T-0318 Fix empty-result-recovery contract mismatches (0318) — new finding from the resumed sweep, `SPECKIT_EMPTY_RESULT_RECOVERY` feature, three distinct mismatches: (1) `no_results` status never appeared for a genuinely nonexistent query (only `low_confidence`/`knowledge_gap` did); (2) a narrow nonexistent `specFolder` produced no recovery guidance at all when it should have; (3) `partial` status appeared but with `reason: "knowledge_gap"`/`recommendedAction: "broaden_or_ask"` instead of the expected `spec_filter_too_narrow` action-list contract for a narrow-existing-folder case. Find the empty-result-recovery classification logic and confirm whether these are genuinely missing status branches or a classification-priority bug (e.g. always falling through to knowledge_gap before checking narrower/more-specific conditions).
- [ ] T-0314 Add real test coverage for atomic-save postMutationHooks/partial-indexing/duplicate-suppression (0314) — new finding from the resumed sweep. The targeted save-path test suite passes, but its assertions only cover planner-output wording, not the actual mechanisms the scenario (and presumably the feature contract) require: atomic-save `postMutationHooks` parity, partial-indexing guidance, and duplicate/unchanged-hook suppression. Confirm whether these are covered indirectly elsewhere (context-server.vitest.ts static assertions or implementation source were the closest indirect signals found) or genuinely untested; add explicit assertions for the missing behaviors if truly uncovered. **Confirming instance (0322, hooks-barrel-readme-synchronization)**: `hooks/index.ts` (the public barrel export) only re-exports `mutation-feedback` and `response-hints` -- it does NOT export `MutationHookResult` or `postMutationHooks` at all, and the README's documented `rg` command for them returns no output. This suggests postMutationHooks may not be wired into the public API surface at all, not just under-tested -- check whether the feature is implemented internally but never exported, or genuinely missing. **Third confirming instance (0325, mutation-response-ux-payload-exposure)**: another passing regression suite (1 file, 3 tests) whose assertions only cover planner-response text, not typed `postMutationHooks` exposure or duplicate/no-op suppression with cache-left-unchanged guidance. Three independent scenarios (0314, 0322, 0325) all point at the same conclusion: `postMutationHooks`/`MutationHookResult` is either unimplemented or implemented-but-never-wired/exported/tested anywhere in the observable surface.
- [ ] T-0312 [P1] Claude Code comment-hygiene hook doesn't emit warning on a simulated violation (0312) — new finding from the resumed sweep, notable severity: the comment-hygiene rule is a hard-block enforcement mechanism (never embed spec-path/task-id references in code comments). Hook-wiring check confirmed `True` (the hook IS registered), but a shell simulation of an actual comment-hygiene VIOLATION exited `0` with no `COMMENT HYGIENE WARNING` emitted -- meaning a real violation would silently pass. The non-violating control case also exited 0 (correct, but doesn't rule out the checker just never firing at all). Investigate the Claude Code hook's trigger/matching logic directly against the violation fixture used in the scenario to confirm whether detection is broken or the simulation itself didn't reproduce a real violation pattern.
- [ ] T-0264 Fix stale `MODULE_MAP.md` entry for `scoring/negative-feedback.ts` (0264) — new finding from the resumed sweep. `mcp_server/lib/MODULE_MAP.md` lists `scoring/negative-feedback.ts` as a module, but it does not exist in the actual `mcp_server/lib/scoring/` directory. Needs disambiguation: was this file intentionally removed/merged elsewhere (doc just needs updating) or was it accidentally deleted (real regression -- check git history for the file's deletion commit and whether its functionality was actually preserved elsewhere)?
- [ ] T-0251 Fix missing `FEATURE_CATALOG.md` at repo root + invalid annotation name "Known-item ground truth corpus" (0251) — new finding from the resumed sweep, two distinct issues: (1) the scenario's documented H3 extraction command fails outright because `FEATURE_CATALOG.md` is absent at repo root -- confirm whether it moved/was renamed or genuinely needs restoring; (2) the lowercase catalog cross-reference surfaced one invalid annotation name, "Known-item ground truth corpus", which doesn't conform to the expected naming convention. Fix the annotation name and resolve the missing-catalog-file question (relocate the doc reference or restore the file).
- [ ] T-0253 Fix grep-traceability check always erroring on missing `.spec-memory-owner.json` (0253) — new finding from the resumed sweep. Every run of the feature-catalog-code-references grep traceability check emits a real traversal error because `.opencode/skills/system-spec-kit/mcp_server/database/.spec-memory-owner.json` is confirmed absent, even though the actual annotation hits themselves satisfy the multi-layer requirement. Either this ownership-lock file should exist at runtime and something isn't creating it, or the grep scope shouldn't include the `database/` directory at all. Check what's supposed to create `.spec-memory-owner.json` and why it's missing, or narrow the grep's search scope if it's not meant to be searched.
- [ ] T-0252 Missing test file `tests/code-graph-degraded-sweep.vitest.ts` referenced by graph-degraded-stress-cell-isolation scenario (0252) — new finding from the resumed sweep. Scenario documents and expects `cd mcp_server && npx vitest run tests/code-graph-degraded-sweep.vitest.ts` to pass 5 tests (4 fallback-decision buckets + a live-DB-isolation guard test), but that file does not exist anywhere in the repo (confirmed via find, no similarly-named file either — only unrelated `stress_test/search-quality/w7-degraded-*.vitest.ts` files exist). Either the test was never actually written (documented-but-unimplemented gap) or was deleted/renamed without updating the scenario doc. Check git history for the file path to determine which, then either restore/write the test or correct the scenario doc.
- [ ] T-0247 Confirm whether `embedder_set` is missing a `dryRun` option (0247) — new finding from the resumed sweep, partially entangled with the recurring stale-dist environmental gap but with one concrete non-environmental claim: the dispatch reports `embedder_set` "does not expose or implement `dryRun`" at all (not just blocked by stale dist), meaning the valid embedder path would queue a REAL reindex with no dry-run option to preview first. Needs confirmation: is dryRun genuinely missing from the handler's accepted parameters (real gap, worth adding given the real-mutation risk), or does it exist but wasn't reachable in this dispatch's degraded environment? Check the embedder_set handler's schema/implementation directly before concluding.
- [ ] T-0245 Fix debug-delegation-scaffold-generator: directory-vs-file check, absolute `packet_pointer`, missing YAML marker (0245) — new finding from the resumed sweep, three concrete mismatches: (1) the documented step 3/4 checks target the spec folder directory itself, producing `exit=1`/`Is a directory` instead of checking a file inside it; (2) the generated `packet_pointer` is an absolute path (`/private/tmp/scaf-test/...`) rather than a repo-relative/canonical one; (3) YAML prompt-marker grep returned only 2 hits, expected at least 3 -- the generator may be omitting a marker it should emit. Find the scaffold-generator script, fix the file-vs-directory check target, make packet_pointer relative, and find the missing marker emission.
- [ ] T-0244 Confirm `activeProvider` dead-code removal is actually complete (0244) — new finding from the resumed sweep. Scenario expects zero live references to `activeProvider` (a symbol that should have been fully retired), but it's still referenced in `mcp_server/lib/embedders/reindex.ts`. Representative memory/search regression flows ran without missing-reference errors, so this isn't breaking anything today, but the "fully removed" claim doesn't hold. Needs disambiguation: either finish removing the remaining reference, or the scenario doc is stale if `activeProvider` is legitimately still needed there (check history/intent before deleting).
- [ ] T-0246 Fix or confirm `embedder_list({})` response field names (`dim`/`backend` vs expected `dimensions`/`provider`) (0246) — new finding from the resumed sweep. `embedder_list({})` returns one active embedder using `dim`/`backend` keys, but the scenario (and presumably API consumers) expect `dimensions`/`provider`. Needs disambiguation: either this is a real API contract drift (real bug, restore the expected field names or update all consumers), or the field rename was intentional and only the scenario doc is stale. Check git history on the embedder_list response builder before changing anything.
- [ ] T-0240 [B] Repo-wide `npm run lint` backlog (35 problems: 30 errors, 5 warnings) causing code-standards-alignment scenario FAIL (0240) — NOT a new regression: multiple fix dispatches this session (0074, 0175) already independently hit and noted this exact same pre-existing lint baseline, explicitly scoping their own lint checks to only their changed files to work around it. This is known, pre-existing lint debt (unused vars, CJS require imports, explicit `any`, import-order mismatches), not something introduced by this remediation batch. Out of scope for a single-bug fix dispatch — would need its own dedicated cleanup effort (likely file-by-file or rule-by-rule) if prioritized. Deferred, not blocking.
- [ ] T-0241 Fix `check-completion.sh` reporting `EVIDENCE_MISSING` on the compliant Level 3 test fixture (0241) — new finding from the resumed sweep. `063-template-compliant-level3` (a fixture whose name and role imply it's a maintained, fully-compliant example) returns `status: EVIDENCE_MISSING, passed: false` from `check-completion.sh` instead of the expected `COMPLETE`. Level 1 advisory path (exit 0) worked correctly. Investigate whether the fixture itself has drifted out of compliance with the checker's current evidence rules (fixture-maintenance bug) or the checker has a false-positive in its evidence-detection logic (real code bug) -- read both `check-completion.sh`'s evidence rules and the fixture's current file contents to determine which.
- [ ] T-0237 Fix warm-only probe churn returning non-retryable status for 20/60 probes (0237) — new finding from the resumed sweep. Under warm-only-mode probe churn, launcher count stayed correctly stable (`9 -> 9`, no leak), but the scenario expected ALL probes hitting a not-yet-warm daemon to return the retryable exit code 75 (`non-75 probes: 0 / 60` expected); actual observed was `20 / 60` non-75. Suggests an inconsistent/racy readiness-signaling path under churn -- some probes get a definitive (non-retryable) response instead of the expected "not ready yet, retry" signal. Find the warm-only probe/launcher readiness-check path and confirm whether it's a genuine race (some probes land in a window where readiness state is ambiguous) or a wrong status code on a specific rejection branch.
- [ ] T-0232 Fix matrix-adapter-runner `summary.tsv` missing aggregate rows (0232) — new finding from the resumed sweep. The runner computes and prints "Pass rate by feature" and "Pass rate by executor" aggregate tables to stdout, but `summary.tsv` (the persisted output file) contains only per-cell rows -- the aggregate rows never get written to the file, only shown in transient stdout. Anything consuming `summary.tsv` programmatically (rather than scraping stdout) silently loses the aggregate view. Find the runner script that writes `summary.tsv` and the aggregate-computation step; append the aggregate rows to the file (or a companion file) instead of stdout-only.
- [ ] T-0231 Fix spec-memory CLI count-pipeline crash on stale-dist output + skill-advisor cwd-independent parity failure (0231) — new finding from the resumed sweep, distinct from the routine "dist is stale" environmental note. Root condition (stale dist in this isolated dispatch environment) is the known recurring gap, but the CLI's behavior under that condition is a real bug: when dist is stale, the spec-memory CLI prints a human-readable warning ("`@spec-kit/mcp-server dist is stale. Run: ... npm run build`") to the SAME stream a downstream count-pipeline step then tries to `json.load()`, causing an unhandled `JSONDecodeError` crash instead of a clean degraded/error response. Separately, `skill-advisor-cli-parity.vitest.ts` failed cwd-independent resolution with `expected null to be 'sk-code'` — needs its own root-cause check, may or may not share a cause with the JSON-parse crash. Fix: the CLI should emit valid JSON (with an explicit `staleDistWarning` field or similar) even when dist is stale, never mix a plain-text warning into a JSON-only output stream; investigate the skill-advisor parity null separately. **Third confirming instance (0239, cli-warm-only-no-spawn)**: under warm-only mode, `code-index` and `skill-advisor` correctly return the expected exit-75 (retryable) "backend unavailable" contract, but `spec-memory` exits `69` on stale-dist instead of `75` — spec-memory's stale-dist path doesn't follow the same exit-code convention the other two CLIs implement correctly. Fix should align spec-memory's stale-dist exit code with the established 75 (retryable) convention, likely the same code path as the JSON-crash fix above.
- [ ] T-0222 Fix `SPECKIT_RESPONSE_TRACE=true` cached response not exposing trace fields (0222) — new finding from the resumed sweep. `memory_search` with `includeTrace:true` correctly exposes trace fields, and env-unset correctly omits them, but `SPECKIT_RESPONSE_TRACE=true` (without explicit `includeTrace`) returned a CACHED non-trace response instead of trace-enabled fields (`scores`/`source`/`trace` all missing). Same pattern class as 0110 (SPECKIT_GRAPH_UNIFIED cache invalidation) — likely the response/query cache key doesn't account for the env-forced trace flag the way it now does for graphUnifiedEnabled. Check the cache-key construction for the `memory_search` response cache and confirm whether `SPECKIT_RESPONSE_TRACE` state is included.
- [ ] T-0476 Fix `advisor_validate` retry-once giving up on `MCP error -32001: backend recycled; retry` (0476) — new finding from the resumed sweep, distinct signature from the routine DB-locked/stale-dist environmental gaps. Both real `advisor_validate({"skillSlug":null})` attempts (initial call + the one retry the tool response itself instructed) returned the identical `backend recycled; retry` error, leaving no baseline/ablation slice to evaluate. Suggests either the retry fires before the recycled backend has actually finished restarting (no backoff/delay between attempts), or the recycle-then-retry contract needs more than one retry to reliably succeed. Find the skill-advisor MCP server's backend-recycle handling and the client-side retry-once logic; consider a short backoff or a bounded multi-retry instead of a single immediate retry.
- [ ] T-0481 Fix skill-advisor bench-runner `warm_p95` gate failing its own envelope (0481) — new finding from the resumed sweep, python-compat lane. The bench command ran cleanly and produced its JSON report, but `gates.warm_p95` was `false` (observed in-process warm p95 `70.0421` ms vs the expected `<= 50` ms envelope), so `overall_pass` was `false`. Either this is a genuine warm-path perf regression in the skill-advisor CLI/daemon, or the 50ms envelope itself is miscalibrated for the current dispatch environment — needs a real profiling pass to distinguish the two before deciding whether to fix perf or adjust the gate.
- [ ] T-0484 Fix skill-advisor stdin-mode top result reporting `source: "local"` instead of expected `"native"` (0484) — new finding from the resumed sweep, python-compat lane. The prompted-run and empty-stdin behaviors both matched expected (top skill returned, `[]` for empty stdin), but the first entry's `source` field was `"local"` when the scenario expected `"native"`, with advisor availability also observed unavailable at the time. Suggests the force-native compat path isn't actually taking effect, or is silently falling back to local scoring without surfacing that as a warning. Check the CLI's native-vs-local routing decision and whether a fallback-to-local case is meant to relabel `source` or should have hard-failed instead.
- [ ] T-0485 Fix skill-advisor threshold-flag not differentiating output + non-JSON preface line breaking machine parseability (0485) — new finding from the resumed sweep, python-compat lane, two distinct issues in one scenario. (a) All three different threshold values produced the identical two-entry output — the threshold flag does not appear to actually filter/gate results, matching the established Group A flag-propagation pattern (flag exists and is accepted, but its value never reaches the code path that should act on it). (b) Every command additionally printed a non-JSON preface line before the JSON array on stdout, which would break any downstream consumer doing a naive `json.parse(stdout)`. Fix: trace the threshold flag from CLI arg parsing to the actual filter/sort step; separately, move the preface line to stderr or drop it so stdout is pure JSON.
- [ ] T-0483 Fix skill-advisor python-compat regression suite failing 20/100 evaluated cases (0483) — new finding from the resumed sweep, python-compat lane, likely the umbrella signal for T-0481/T-0484/T-0485 in this same scenario cluster. Suite exited code `1`, `overall_pass` was `false`; across the checked-in 50-row JSONL dataset run through both `inprocess` and `subprocess` execution modes (100 evaluated cases total), 20 cases failed. Given the other three python-compat findings surfaced in the same wave (bench `warm_p95` gate failure, native/local `source` mislabeling, threshold-flag not filtering), investigate whether this suite's 20 failures share a root cause with those, or are a distinct set — a single fix to the native/local routing or threshold-flag wiring may resolve several of these at once. Start from the suite's per-case failure detail (not yet captured in this dispatch's evidence) to map failures to root causes.


- [ ] All Phase 1 findings have a Phase 2 fix task
- [ ] No `[B]` blocked tasks remaining (0032, 0062 currently blocked pending re-verification)
- [ ] All Phase 2 tasks either `[x]` fixed-and-verified or explicitly deferred with operator approval
- [ ] `checklist.md` fully verified

<!-- /ANCHOR:completion -->
---

<!-- ANCHOR:cross-refs -->
## Cross-References

- **Specification**: See `spec.md`
- **Plan**: See `plan.md`
- **Checklist**: See `checklist.md`
- **Parent packet (sweep execution)**: See `../tasks.md`

<!-- /ANCHOR:cross-refs -->
