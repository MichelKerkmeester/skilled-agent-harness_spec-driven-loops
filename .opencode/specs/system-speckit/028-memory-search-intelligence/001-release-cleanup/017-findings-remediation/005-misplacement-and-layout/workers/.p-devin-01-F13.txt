Repository remediation worker. Repo root: /Users/michelkerkmeester/MEGA/Development/Code_Environment/Public
Spec folder pre-approved (skip Gate 3). Make ONLY the change described. Touch NOTHING else.
Do not run git commands that mutate state except where explicitly permitted. Do not commit.
A concurrent session is editing this repo; never stage, revert or touch files outside your task.
If your target sits under benchmark/, changelog/ or archive/, read that directory README first —
retention there is usually a documented contract and renaming or deleting violates it.

Return EXACTLY:
FINDING: <id>
REVERIFIED: <command + its real output>
STATUS: APPLIED | STALE | BLOCKED
CHANGE: <files touched, what changed>
EVIDENCE: <command proving the change is correct>
RISK: <what this could break and how you checked>

FINDING devin-01-F13
TASK: In .opencode/skills/sk-doc/SKILL.md the Layout block (around line 130) lists packet directories including `create-feature-catalog/` and `create-manual-testing-playbook/`, but omits the separate top-level `feature-catalog/` directory that exists at the hub root (.opencode/skills/sk-doc/feature-catalog). Add `feature-catalog/` to that Layout block so the diagram matches what is on disk. Match the surrounding indentation and style exactly. Change nothing else. Verify by diffing the layout block entries against `ls -d .opencode/skills/sk-doc/*/`.
