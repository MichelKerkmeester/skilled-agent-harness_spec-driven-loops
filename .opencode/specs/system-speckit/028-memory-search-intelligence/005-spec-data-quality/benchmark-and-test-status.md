# 005 Spec Data-Quality: Benchmark and Test Status

Single-pane benchmark-and-test record for the whole 005-spec-data-quality program. All 28 phases (001 through 028) sit in one table below, grouped by tier. This is a supplementary record, not a canonical spec doc.

> **STATUS: SPECIFIED, NOT RUN.** Every row below is what a phase *declares* it would measure, never a result. The whole program is research-only. No code has landed, nothing is built and nothing is measured. Read each Benchmark and Test cell as a contract a future build would have to clear, not as evidence it cleared. The single measured-class item in the parent narrative (A4, phase 004) is also still SPECIFIED here because no build run has happened in this program.

## The truncation-law spine

The production default search route never cuts below a 3-result minimum and returns up to 20. `DEFAULT_MIN_RESULTS = 3` is a never-cut-below-3 floor guarantee, not a cap, so a contribution that lands past rank 3 is not mechanically invisible at prod. Two stages can trim the tail above that floor. Confidence truncation is cliff-conditional and returns 3..20, cutting only on a relevance gap past rank 3. Token-budget truncation is the real prod-limiting stage. What splits how the phases declare their metric is not a hard top-3 window but the eval-vs-prod fidelity gap, because the eval path skips truncation while the prod path runs it.

- **Retrieval-class phases (014, 015, 016, 017, 018)** route their metric through the phase-015 prod-mode `completeRecall@3` gate. Phase 015 builds that gate, a `run-eval-v2.mjs` read of the spec corpus through the production route, and phases 014, 016, 017 and 018 settle their keep-or-drop verdict on it. The eval path forces all channels and skips truncation while the prod path runs the routed truncation-active default, so a recall number measured on the unfloored eval path does not by itself prove a prod-path keep.
- **Write-time and detector phases** use conformance-count, catch-rate, swap-precision or byte-identity metrics instead. They run upstream of retrieval truncation at the write seam or in a report pass, so a recall number would be the wrong measure and a count or a precision is the honest one.

The program inherits the production-truth finding from the parent `028-memory-search-intelligence/benchmark-status.md`: no Tier-C retrieval candidate earns a prod-path keep until the phase-015 prod read moves, and the floor minimum itself does not move until the phase-027 experiment sweeps settings 5/8/10 and shows results past rank 3 are signal rather than truncation noise. Until both run, no retrieval claim carries measured support. The direction survives the magnitude correction: retrieval stays gated on a prod-path read and write-time ships on cost.

## Benchmark and test table

| Phase | Benchmark (metric : pass/regress threshold) | Test (file : key assertion) | Default-safety |
|-------|----------------------------------------------|------------------------------|----------------|
| **Tier A: on-write (001-010)** | | | |
| 001 a1-extend-quality-loop-authored | CONTENT_QUALITY scorer, report-only (no numeric threshold), byte-identity on metadata JSONs | vitest unit + validate.sh legacy-corpus : legacy corpus with CONTENT_QUALITY warn rule exits 0 | CONTENT_QUALITY rule default OFF (no env flag on core H1 scoring) |
| 002 a2-trigger-propagation-description | subset coherence assertion (no numeric threshold) : trigger_phrases superset of curated frontmatter, description differs from title | regeneration pass on 005/description.json + subset coherence : idempotent second pass identical | n/a, always-on write-time field population |
| 003 a3-enum-constrain-schemas | enum swap-precision on planted fixtures : catch-rate 1.00 (every out-of-enum rejected), false-reject-rate 0.00 | enum-constrain-schemas.vitest.ts + derive-then-parse round trip : in/out-of-enum fixtures, legacy-corpus conformance baseline | default OFF + SPECKIT_SCHEMA_ENUM_ENFORCE |
| 004 a4-schema-warn-to-error | pre-flip dry-run census conformance-to-zero + validate.sh --strict : every graph-metadata.json passes the target schema before the warn-to-error flip | validate.sh --strict : valid corpus exit 0, corrupted scratch packet exit 2, grep proves zero legacy_grandfathered | n/a, always-on error-tier gate after backfill-to-zero (the one unconditional GO) |
| 005 a5-trigger-coherence-assertion | trigger-coherence catch-rate 1.0 over >=3 planted-divergence fixtures, false-positive count 0 | trigger-coherence.vitest.ts + dry-run over .opencode/specs : subset coherence passes, capped 12-entry derived stays silent | default OFF + SPECKIT_TRIGGER_COHERENCE (warn severity) |
| 006 a6-hvr-style-autofix | swap-precision 1.0 (swaps fire inside prose range only) + planted-violation catch-rate 1.0 | hvr-style.vitest.ts + non-prose byte-diff : fence/frontmatter exclusion fixtures, idempotency guard | default OFF + SPECKIT_HVR_STYLE_AUTOFIX |
| 007 a7-ears-constraints-req-coverage | REQ_COVERAGE catch-rate 1.0 / false-positive 0.0, EARS_LINT swap-precision on planted free-form rows | check-req-coverage.test.sh + check-ears-lint.test.sh + validate.sh --strict : flags-off byte-identity via flag-ceiling | default OFF + SPECKIT_REQ_COVERAGE + SPECKIT_EARS_LINT |
| 008 a8-surface-provenance-fields | conformance-to-zero after backfill : provenance fields populate and stale causal_summary clears the freshness warn, zod parse fixtures pass | vitest zod parse + freshness predicate + generate-context write seam : stale causal_summary flagged | n/a, always-on write-time field addition (warn-tier coherence check default-off) |
| 009 a9-content-hash-integrity | planted-mismatch catch-rate 100% (corrupt-body rows in contentHashMismatches), clean-corpus false-positive 0 | content-hash-integrity.vitest.ts + flag-off case : planted-corruption scratch DB, re-read proves body and hash untouched | default OFF + SPECKIT_CONTENT_HASH_INTEGRITY |
| 010 a10-per-surface-gates | planted-mismatch catch-rate 1.0 across all five gates, clean-fixture swap-precision zero findings | per-surface-gates.vitest.ts + route-validate 28-doc census + canary set : warn-tier on planted, zero on clean | default OFF + SPECKIT_GATE_SKILL_GRAMMAR + _ROUTE_CONTRACT + _WORKFLOW_YAML + _SKILLGRAPH_DRIFT + _TRIGGER_CANARY |
| **Tier B: automation (011-013)** | | | |
| 011 b1-scheduled-dq-sweep | planted-mismatch catch-rate 100% on frozen fixture, apply precision 1.0 | scripts/tests/dq-sweep.vitest.ts : safe-class filter selects only fixClass safe, second apply empty | default OFF + SPECKIT_DQ_SWEEP |
| 012 b2-doctor-dq-route | route-manifest conformance : route-validate.py exit 0 with data-quality row, DB-path apply halts with mutation-violation | route-validate.py + scratch-packet runs (3 flag states) + B1 engine suite : report-only default vs gated safe-apply | n/a, diagnostic-default route, apply pinned to safe class |
| 013 b3-retrieval-feedback-edge | flag-off no-op : search output and timing unchanged with flag off | detect-retrieval-gaps.vitest.ts : edge classification, report-only governance | default OFF + SPECKIT_RETRIEVAL_GAP_DETECT |
| **Tier C: retrieval (014-018), metric routed through the 015 prod-mode completeRecall@3 gate** | | | |
| 014 c1-chunk-prefix | [via 015 gate] prod-mode completeRecall@3 RISES vs 015 baseline, full re-embed at 100% coverage under new strategy version | vitest : prefix-builder determinism (byte-identical prefixes), dual-key miss on strategy bump (no silent no-op), coverage guard (prod read refuses mixed regime) | default OFF, strategy flag not named in docs |
| 015 c2-prodmode-recall-gate | prod-mode completeRecall@3 via run-eval-v2.mjs at the 3-result floor : threshold = stored baseline, PROMOTION asserts rise, REGRESSION asserts non-fall | run-spec-recall-gate.mjs : verdict input is prod profile, flipped lens fails, degraded scratch exits a recall-verdict code distinct from the crash code, PROMOTION fails on unchanged | n/a, this IS the prod-mode gate other Tier-C phases route through |
| 016 c3-answerable-questions-tags | [consumer via 015 gate] fusion score moves only within bound for tagged rows, flag-off baseline parity | vitest parser + fusion unit + CLI flag-off parity : extractors round-trip both fields, flag-off prod matches baseline | default OFF, consumer flag not named in docs (write-time tag generation ships on cost) |
| 017 c4-metadata-fusion | [via 015 gate] alpha-sweep emits prod-mode completeRecall@3 per alpha setting, flag-off byte-identical to baseline | run-eval-v2.mjs alpha-sweep + vitest fusion + flag-off byte-identity : blend stays bounded, one prod@3 number per alpha | default OFF, lane flag not named in docs (double-gated on C1 floor-move + C2 prod@3 rise) |
| 018 c5-llm-judge-scorer | [consumer via 015 gate] judge quality_score in [0,1] persists, form-vs-judge marginal-value report, flag-off byte-identical | vitest scorer + form-vs-judge harness + flag-off byte-identity : value clamped to [0,1], unchanged content_hash not re-scored | default OFF, consumer flag not named in docs (governance value ships on cost) |
| **Novel (019-025)** | | | |
| 019 novel-contradiction-detection | plantedMismatchCatchRate == 1.0 (every planted contradiction and stale claim emits a finding), false-positive floor 0 | scripts/tests/contradiction-detector.vitest.ts : catch-rate 1.0, false-positive 0 | default OFF + SPECKIT_CONTRADICTION_DETECTOR (report-only, not 015-routed) |
| 020 novel-embedding-drift-monitor | mixed-regime alert detection : two live regimes fire an alert, single regime fires none | vitest over detect-embedding-drift.ts : seeded two-regime corpus fires, single-regime corpus does not | default OFF + SPECKIT_EMBEDDING_DRIFT_MONITOR (report-only) |
| 021 novel-example-test-generation | GEN_COVERAGE : un-covered REQ count to zero, REQUIREMENTS bytes-changed 0, fenced-decoy false-positive 0 | scripts/tests/gen-examples-tests.vitest.ts : per-REQ coverage N/N, REQUIREMENTS byte-identity, fence-decoy false-positive 0 | default OFF + SPECKIT_GEN_EXAMPLES (write-time, floor-bypassing) |
| 022 novel-context-budget-assembler | duplicate-rate reduction + token-per-relevant-row improvement (no numeric threshold, similarity threshold and budget default undecided), floor minimum count preserved | scripts/tests/context-budget-assembler.vitest.ts : duplicate-rate drop with flag on, byte-for-byte equality with flag off | default OFF + SPECKIT_CONTEXT_BUDGET_ASSEMBLER (post-floor density, not retrieval-class) |
| 023 novel-typed-relation-kg | relation conformance : zero CHECK-constraint failures, LLM edges partition by distinct provenance, ranking unchanged | vitest unit + fixture integration : in-vocabulary lands, out-of-vocabulary drops cleanly, provenance separates LLM from deterministic edges, ranking unaffected | default OFF + SPECKIT_LLM_GRAPH_BACKFILL (navigation/provenance, not retrieval) |
| 024 novel-freshness-decay-queue | plantedDecayCatchRate == 1.0 (every sub-threshold doc queued), swapPrecision == 1.0 (zero pinned/fresh queued) | mcp_server/tests/freshness-decay.vitest.ts : at-threshold docs never queued, pinned retrievability 1.0 never queued | default OFF + SPECKIT_FRESHNESS_DECAY_QUEUE (report-only queue rows) |
| 025 novel-per-doc-quality-slas | SLA catch-rate == 1.0 (every below-SLA doc files a ticket), swap-precision 0 (no false-positive on at-or-above docs) | mcp_server/tests/quality-sla.vitest.ts : exactly one report-only ticket per flagged doc | default OFF + SPECKIT_QUALITY_SLA (report-only, needs host queue pre-existence) |
| **Infra (026-028)** | | | |
| 026 shared-safe-fix-engine | deny-by-default + INV-1 invariant : a body-mutating detector cannot be granted safe, an apply run touches only safe-class targets | vitest pure-runner unit + dirty scratch fixture integration : INV-1 guard, body-mutating class never grantable safe | n/a, shared engine, deny-by-default by construction |
| 027 retrieval-floor-experiment | prod-lens completeRecall@3 delta per floor setting vs the C2 baseline : signal-or-noise verdict against a pre-stated threshold | run-floor-experiment.mjs over a C2 harness copy DB + floor-experiment-report.md : sweep settings 5/8/10, verdict reads the prod column not eval-at-K | default OFF + SPECKIT_FLOOR_OVERRIDE (env-gated read of DEFAULT_MIN_RESULTS and token budget, not persisted) |
| 028 governance-rollout | rollout-manifest edge-violation count zero across five inviolable edges, 18-item NO-GO enumeration, runbook-import resolution per gate phase | governance-rollout-manifest.vitest.ts (topological-sort check, planted out-of-order 1-of-1 catch) + validate.sh strict : NO-GO count 18 | n/a, governance read-time docs, no SPECKIT flag introduced |
| **Meta (029, executed)** | | | |
| 029 vague-query-model-benchmark | model-behavior characterization across a 4-model by 12-query by 3-sample matrix : no promotion gate, a per-model behavioral profile plus a cross-model verdict-stability finding | scripts/run-benchmark.mjs + scripts/extract-metrics.mjs : read-only bare-query dispatch, every metric traced to a raw stream, launch-race gaps reported not dropped | n/a, read-only benchmark, no flag and no memory write |

## Reading the table

- **Benchmark** is the named metric and its declared pass-or-regress threshold. `[via 015 gate]` marks a metric that only counts once it moves the phase-015 prod-mode `completeRecall@3` read, because a recall number measured on the unfloored eval path does not by itself prove a prod-path keep.
- **Test** is the named test file and its key assertion.
- **Default-safety** is the default-OFF posture and the env flag where the phase declares one. `n/a` means the phase is always-on write-time or read-time work with no flag, by design.
- Where a Tier-C phase declares a consumer, lane or strategy flag but never names it in its spec or plan, the cell says "flag not named in docs" rather than inventing a name.
- Nothing in the 001-028 lever rows has run. Each threshold is a target a future build would have to clear, never a measured outcome. The 029 meta-benchmark row is the one exception, an executed read-only characterization whose measured results live in its own phase docs.
