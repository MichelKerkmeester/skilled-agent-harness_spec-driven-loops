You are a hostile, skeptical reviewer doing READ-ONLY adversarial verification. REFUTE or DOWNGRADE; do not agree by default. Output ONLY a concise markdown ledger — no preamble, no restating teachings in full. Keep total output under ~700 words. Do NOT write files.

# WHAT system-spec-kit Memory ALREADY HAS (the bar a teaching must clear)
FSRS-based decay + co-activation (more adaptive than fixed half-lives); causal edges with `contradicts`/`supersedes`; semantic + lexical trigger matching; importance tiers; a self-maintaining incremental index; shadow-first learning-feedback reducers; spec-folder continuity ladder (handover.md / continuity frontmatter); 37-tool MCP surface; single-user / local; ollama-nomic-768d vector store; `memory_health` / `memory_index_scan` / `memory_embedding_reconcile` / retention sweep.

# THE 14 CANDIDATE TEACHINGS (from synthesis) — verify each
1. Actual-pipeline `why_ranked` explanation (not display-only scoring) — ADAPT
2. Scrub secrets before indexing/hashing/embeddings/FTS/persistence — ADOPT
3. Stage auto-mined memories AND dedup as proposals, never silent durable mutation — ADOPT
4. Surface contradiction/supersession warnings inline with retrieval (not just graph-debug) — ADAPT
5. Narrow atomic `learn/reinforce` MCP contract with capability discovery beside `memory_save` — ADAPT
6. Normalized provenance source-kind + on-demand batch provenance lookup — ADAPT
7. Slim audit snapshots + deletion lineage/tombstones that survive cleanup — ADAPT
8. Explicit vector availability/degradation state (no silent lexical-only fallback) — ADAPT
9. Treat vector/ANN indexes as rebuildable derived surfaces keyed by model/dim — ADOPT
10. Numbered migrations with checksums + baseline sentinel — ADAPT
11. Explicit FTS-rebuild migrations that repopulate source + recreate triggers — ADAPT
12. Bounded emergency context envelopes + low-tech PreCompact snapshot — ADAPT
13. Lexical/trigger deficit detection → semantic rescue — ADAPT
14. Maintenance observability (last-run counters, stale-candidate reports), not a heavy janitor — ADAPT

The full per-teaching evidence lives in `specs/system-spec-kit/027-xce-research-based-refinement/research/010-openltm-memory-architecture-teachings/iterations/iteration-001.md` … `iteration-010.md` — read a specific one ONLY if you must check a claim; do not read all ten.

# OUTPUT — one table only
`# | Adversarial verdict | Ground | One-line reason`
- Adversarial verdict ∈ {UPHELD, DOWNGRADE→<new verdict>, REFUTED}.
- Ground ∈ {already-covered, doesn't-transfer, weak-evidence, cost>benefit, none}.
- UPHOLD the genuinely strong ones; REFUTE/DOWNGRADE the weak ones. Be decisive.

Then two one-line summary rows:
- **Survivors:** comma-separated teaching numbers that are UPHELD or only mildly downgraded.
- **Killed/overstated:** numbers REFUTED or strongly downgraded, each with a 3-word reason.
