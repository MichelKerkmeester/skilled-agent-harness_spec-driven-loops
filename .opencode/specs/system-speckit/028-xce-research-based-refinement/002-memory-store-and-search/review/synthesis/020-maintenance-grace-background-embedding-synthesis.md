# 020 Review Synthesis

**Phase:** `020-maintenance-grace-background-embedding` (027 XCE / 002-memory-store-and-search track)
**Scope:** the shared reference-counted maintenance marker + extending grace to the post-scan background-embedding queue. Shipped files verified against source at cited lines: `mcp_server/lib/storage/maintenance-marker.ts`, `mcp_server/handlers/memory-index.ts` (scan IIFE), `mcp_server/lib/providers/retry-manager.ts` (`runBackgroundJob`), `mcp_server/tests/maintenance-marker.vitest.ts`; launcher-reader contract checked in `.opencode/bin/lib/model-server-supervision.cjs`; atomic-write contract in `mcp_server/lib/storage/transaction-manager.ts`.
**Lineages:** 10 (2 deepseek-v4-pro, 2 mimo-v2.5-pro, 2 kimi-k2.7, 4 opus-4.8). 1 iteration each. Per-lineage verdicts: 8 PASS, 2 CONDITIONAL (`p020-deepseek-1`, `p020-kimi-2`).
**Raw findings collected:** ~22 finding-instances across all lineages. **0 P0** anywhere. **4 P1 finding-instances raised** (deepseek-1 ×1, kimi-2 ×3) — every one verified FALSE-POSITIVE against the cited code. All surviving findings are P2 (cosmetic / doc-accuracy / robustness-advisory / test-hygiene); none is a runtime defect.

This synthesis is READ-ONLY: every confirmed/rejected verdict below was checked against the cited source at the cited line.

---

## Verdict

**PASS** (advisory remediation only).

Confirmed-findings-only mapping:
- **Confirmed P0: 0** → no FAIL trigger.
- **Confirmed P1: 0** → no CONDITIONAL trigger. The two CONDITIONAL verdicts (`deepseek-1`, `kimi-2`) each rest on a P1 that VERIFICATION REFUTES — `atomicWriteFile` never throws (it returns `false`; transaction-manager.ts:196-205), so neither the "increment-before-throwable-write state drift" (deepseek-1) nor the "refresh-timer crashes the daemon" (kimi-2) failure path exists; the "unprotected synchronous foreground scan" (kimi-2) is correct-by-design and was affirmed as such by both deepseek lineages. Independently, `p020-opus-3` raised this same increment-before-write hypothesis as a P0 candidate and **refuted it itself** by reading the same atomic-write code.
- **Confirmed P2 (high-agreement, ≥2 models): 3** (D1 stale labels, D3 embedding refresh-free, D9 test-reset leak).
- **Confirmed P2 (singleton / low-value): 6** (D2, D4, D5, D10, D11, D12/D13 doc-and-test gaps).

The phase ships working, tested code; every REQ (REQ-001..004) is behaviorally satisfied. The PASS carries an advisory doc/robustness cleanup list — no code change is required for correctness.

---

## Confirmed Findings

| Severity | File:line | Agreement (lin / models) | Issue | Verification evidence (what was read) | Remediation |
|----------|-----------|--------------------------|-------|---------------------------------------|-------------|
| P2 (high) | `maintenance-marker.ts:72-81` | 6 lin / 3 models (mimo-1, deepseek-2, opus-1, opus-2, opus-3, opus-4) | A non-final `end()` decrements the in-memory ref count but does NOT re-serialize the marker file, so the on-disk `labels[]` can still list an already-ended holder until the next begin/refresh/20s-timer write. | CONFIRMED, cosmetic. `end()` (marker.ts:75-77) updates `activeCount`/`activeLabels` in memory only; `writeMarker()` is not called on a non-zero-count `end()`. Launcher reader consumes ONLY `childPid` + `activeUntilMs` (model-server-supervision.cjs:619, 636-637), never `labels` → zero runtime impact; the unit test documents this as intentional. | Optional: re-serialize in `end()` when count stays >0, OR add a one-line comment that `labels[]` is best-effort diagnostic, not authoritative. Not required. |
| P2 (high) | `retry-manager.ts:1038-1041` | 3 lin / 1 model (opus-1, opus-2, opus-4) | The embedding tick calls `beginMaintenance('embedding-queue')` once and never calls `refresh()` during the drain (unlike the scan, which refreshes at each phase boundary); marker freshness depends on the 20s `setInterval` firing, which only happens if the batch yields the event loop. | CONFIRMED, forward-compat only. `runBackgroundJob` (retry-manager.ts:1038) holds the handle across `await processRetryQueue(batchSize)` (L1041) with no explicit refresh. Safe today: `processRetryQueue` awaits a network-bound `retryEmbedding`/`generateDocumentEmbedding` per row, yielding between items so the 20s timer fires well inside the 180s TTL. Risk only if a future synchronous/CPU-bound embedding backend blocks >180s. | Add a one-line comment at retry-manager.ts:1038 noting the drain is refresh-free because it yields per `await`; add a periodic `refresh()` only if a synchronous embedding backend is ever introduced. |
| P2 (high) | `maintenance-marker.ts:87-91` | 2 lin / 1 model (kimi-1, kimi-2) | `__resetMaintenanceMarkerForTest()` clears the timer + `activeCount` + `activeLabels` but does not remove the on-disk `.maintenance-active.json`, so a future test reusing `DATABASE_DIR` could see a stale-marker false positive. | CONFIRMED, test-hygiene. Reset helper (marker.ts:87-91) does `clearInterval` + zeroes in-memory state; no `rmSync(markerPath())`. Benign today (each test uses a fresh tmpdir torn down in `afterEach`), but the reset contract is incomplete. | Add `try { rmSync(markerPath(), { force: true }); } catch {}` inside `__resetMaintenanceMarkerForTest` before clearing in-memory state. |
| P2 (singleton) | `maintenance-marker.ts:60`, `:76-77` | 1 lin / 1 model (mimo-2; mimo-1 surfaces same mechanism as a test gap) | `activeLabels.push(label)` is unconditional and `end()` removes via `indexOf` (first match only); two concurrent holders with the SAME label leave a duplicate entry on disk until the next write. | CONFIRMED, cosmetic. Both current callers use distinct labels (`index_scan`, `embedding-queue`), so the duplicate path is not exercised today; if it were, ref-count + file removal still behave correctly. Launcher ignores `labels`. | Optional: dedup with a `Map<label, count>` or `[...new Set(activeLabels)]` in `writeMarker()`. Low priority. |
| P2 (singleton) | `retry-manager.ts:1032-1041`, `1052-1060`; cross-ref `memory-index.ts:1540` | 1 lin (active) / 1 model (opus-1 P2-3, opus-4 F004; opus-2 raised then refuted the severity) | Inter-tick / scan-to-first-tick marker gap: the scan `end()`s when `runIndexScan` resolves; the embedding queue marks only on a tick that finds pending rows, and `end()`s in `finally` after one `batchSize` drain. With the stock 300s `SPECKIT_RETRY_INTERVAL_MS` the daemon holds NO marker between batches. | CONFIRMED-REAL, scope-acknowledged + low severity. `processRetryQueue` (retry-manager.ts:884) drains one batch then returns; `runBackgroundJob` `finally` ends the handle (L1055) → `activeCount` hits 0 → marker removed. Stock interval default is 300000ms / batch 5 (L364-365). **Severity bounded by the launcher predicate:** `shouldAdoptDespiteProbe` only matters when a probe FAILS, and requires `childLiveness === 'alive'` (supervision.cjs:638); between ticks the idle daemon is probe-responsive, so it is reaped only if it ALSO wedges in that gap — narrow. Spec §6 / plan acknowledge it; no data loss (deferred rows drain on a later pass). | None for this packet. The cooperative-heavy-phases follow-on (021) is the proper home; if zero-gap coverage is ever required, hold the marker across the whole drain rather than per tick. |
| P2 (singleton) | `spec.md:108`, `implementation-summary.md` "schema unchanged" claims | 1 lin / 1 model (opus-2; opus-4 examined and ruled it not-a-defect) | Docs say the marker schema is "unchanged" vs 019, but the writer payload changed: 019 emitted `jobId`; the new writer emits `labels: string[]` and drops `jobId`. | CONFIRMED, doc-accuracy nuance. maintenance-marker.ts:44-50 writes `{ childPid, activeUntilMs, labels, refreshedAtIso }` — no `jobId`. Consumer-compatible: launcher reads only `childPid` + `activeUntilMs` (supervision.cjs:619, 636-637), so the LAUNCHER-READ contract is genuinely unchanged. The auxiliary payload is what changed. | Reword to: "the launcher-read fields (`childPid`, `activeUntilMs`) and the file/TTL/dir are unchanged; the auxiliary payload changed `jobId` → `labels[]` to support ref-counted overlapping sources." (Note: this is inherited 019 drift — the field changed in 019's writer; 020 reuses it.) |
| P2 (singleton) | `maintenance-marker.ts:44-51`, refresh path `:63` | 1 lin / 1 model (opus-3 P2-D1-01) | `writeMarker()` discards the `boolean` that `atomicWriteFile` returns; on a persistent write failure the ref count increments and the timer runs while no marker file exists on disk. | CONFIRMED-with-correction, very low value. `atomicWriteFile` returns `false` and `console.warn`s on failure (transaction-manager.ts:196-205) — it does NOT throw. So the failure is observable (via transaction-manager) and the direction is FAIL-SAFE: a daemon that cannot write its marker should be reapable, which is exactly what happens (marker absent → normal reap). | Optional: log in `writeMarker()` when `atomicWriteFile` returns `false`. Not required. |
| P2 (singleton) | `maintenance-marker.ts:76-77` | 1 lin / 1 model (deepseek-2) | `activeLabels` removal is O(n) `indexOf` + `splice`. | CONFIRMED, trivial. Bounded by 2-3 concurrent labels in practice; no measurable cost. | No action (or fold into the dedup `Map` suggestion above). |
| P2 (singleton) | `memory-index.ts:1488-1494`; `maintenance-marker.vitest.ts` | 2 lin / 2 models (deepseek-2 G, mimo-1 F002); related kimi-2 H at `implementation-summary.md:88-92` | Test/doc gaps: no unit test asserts the synchronous foreground scan path produces NO marker, and no test covers the duplicate-label path; the impl-summary verification table has no scope note that protection covers only the background scan + embedding queue. | CONFIRMED, low value. The foreground path (`!args.background → runIndexScan(args, {})`, memory-index.ts:1493-1494) is correct by design (see Rejected D8); a regression test pinning that it stays unmarked, plus a one-line impl-summary scope note, would aid traceability. | Optional: add a `maintenance-marker.vitest.ts` case for the duplicate-label path; add a `memory-index` test asserting the foreground branch writes no marker; add a one-line scope note to the impl-summary verification table. None required. |

---

## Rejected / False-Positive / Already-Resolved

| Claim (lineage) | Severity claimed | Why rejected |
|-----------------|------------------|--------------|
| **D6 — `beginMaintenance` increments `activeCount`/`activeLabels` BEFORE `writeMarker()`; a throw leaves state drifted and the caller with no handle to `end()`** (deepseek-1 P1; opus-4 P2; opus-3 raised as P0 candidate) | P1 / P0-cand | **FALSE-POSITIVE.** The throw path does not exist. `writeMarker()` → `atomicWriteFile` wraps all fs work (mkdirSync/writeFileSync/renameSync) in a single try/catch, returns `false`, never throws (transaction-manager.ts:177-205); it even creates the dir via `mkdirSync({recursive:true})` (L183-184). `beginMaintenance`'s only other ops are an int increment + array push, which cannot throw. opus-3 independently read this code and refuted its own P0 candidate. No state drift, no missing-handle leak. |
| **D7 — the 20s refresh timer (`setInterval(writeMarker)`) raises an unhandled exception on a transient disk/permission failure and crashes the daemon** (kimi-2 P1) | P1 | **FALSE-POSITIVE.** Same root as D6: `atomicWriteFile` never throws (transaction-manager.ts:196-205), so the timer callback cannot raise. A write failure is fail-open and self-healing — the marker simply isn't written and is retried on the next tick. |
| **D8 — the synchronous foreground `memory_index_scan` path (`runIndexScan(args, {})`) acquires no marker and could be reaped mid-scan** (kimi-2 P1) | P1 | **FALSE-POSITIVE / correct-by-design.** memory-index.ts:1493-1494: `if (!args.background) return runIndexScan(args, {})` is a synchronous request-response path that blocks its caller; it is not a fire-and-forget daemon burst the launcher could reap as "wedged-but-busy". The spec scope ("the scan IIFE refactored onto the shared module") targets the background path. Both deepseek lineages explicitly affirmed this path as correct-by-design; no other lineage cross-confirmed it as a bug. |
| **D-ord — in `end()` the label `splice` runs BEFORE the `activeCount` decrement, risking a timer-serialized marker with the label removed but count still holding** (mimo-2 P2-002) | P2 | **FALSE-POSITIVE on ordering.** Code is the opposite: `activeCount = Math.max(0, activeCount - 1)` is line 75, the `indexOf`/`splice` is lines 76-77 — decrement precedes splice. The described inversion does not exist. (Even if it did, the launcher ignores `labels`, so impact would be nil.) |
| **REFUTED-D1-leak — refcount leak permanently shields a wedged daemon, defeating the 180s TTL** (opus-3, self-refuted) | P0-cand | Already refuted by the raising lineage (same atomic-write evidence as D6). Recorded for completeness. |
| **"idle embedding tick leaves a lingering marker" / "end() double-call throws" / "schema change breaks the 019 launcher"** (opus-4 "Ruled Out") | — | Correctly ruled out by opus-4. `beginMaintenance('embedding-queue')` is gated behind the empty-queue early-return (retry-manager.ts:1032-1038) so an idle tick never marks (REQ-004 satisfied); `end()` has an `ended` guard + `rmSync({force:true})` in try/catch; the launcher reads only `childPid`/`activeUntilMs` so the `labels` payload is read-compatible. |

### Doc-drift cross-check (per the 019-sibling concern)
- **`mcp_server/bin/...` non-existent-path drift:** NOT present in 020's docs. 020's spec/plan/tasks/impl-summary cite only real paths (`lib/storage/maintenance-marker.ts`, `handlers/memory-index.ts`, `lib/providers/retry-manager.ts`). Confirmed via grep over the 020 phase folder.
- **`jobId`-vs-`labels[]` schema drift:** 020's docs describe the `labels[]` shape consistently with the shipped writer (the only residual is the inherited "schema unchanged" wording — D5 above, a 019-origin field change 020 reuses). 020 does NOT introduce the 019 `jobId` drift.
- **"per-tick, gaps between batches" embedding-queue limitation:** REAL (D4) — confirmed in code (one `batchSize` drain per `runBackgroundJob`, marker ended in `finally`, stock 300s interval gap) — but already scope-acknowledged in spec §6 / plan and low severity given the probe-liveness gate.

---

## Remediation Outline

All remediation is advisory; the phase passes. Tasks ordered by severity (highest first); none blocks completion.

**Doc reconciliation (P2, ~10 min, no code risk):**
1. `spec.md` / `implementation-summary.md` — reword the "marker schema unchanged" claim to scope it to the launcher-read fields (`childPid`, `activeUntilMs`, file/TTL/dir) and note the auxiliary payload changed `jobId` → `labels[]` for ref-counted overlap (D5). This is inherited 019 wording — reconcile both phases' docs together.
2. `implementation-summary.md` verification table — add a one-line scope note that marker protection covers the background scan + the post-scan embedding queue, not the synchronous foreground scan (which is request-bound by design) (D13).

**Code hardening (P2, optional, low value):**
3. `lib/storage/maintenance-marker.ts:87-91` — `__resetMaintenanceMarkerForTest()` should `rmSync(markerPath(), { force: true })` before clearing in-memory state so the test-reset contract is complete (D9). *(Highest-value of the code items — closes a real test-hygiene gap.)*
4. `lib/providers/retry-manager.ts:1038` — add a one-line comment that the embedding drain is refresh-free because it yields per `await`; introduce a periodic `refresh()` ONLY if a synchronous/CPU-bound embedding backend is ever added (D3).
5. `lib/storage/maintenance-marker.ts:72-81` — optionally re-serialize the marker on a non-final `end()` OR comment that `labels[]` is best-effort diagnostic (D1); optionally dedup `activeLabels` via a count map (D2 + D11).
6. `lib/storage/maintenance-marker.ts:44-51` — optionally log in `writeMarker()` when `atomicWriteFile` returns `false` (D10).

**Test coverage (P2, optional):**
7. `tests/maintenance-marker.vitest.ts` — add a duplicate-label case; add a `memory-index` test asserting the synchronous foreground branch writes no marker (D12).

**Do NOT do** (rejected as false-positives — see table): add try/catch rollback around `beginMaintenance`'s increment (D6), add an uncaughtException handler for the refresh timer (D7), or wrap the synchronous foreground scan in a marker (D8). All three target failure modes that the code already precludes (`atomicWriteFile` never throws; the foreground path is request-bound by design).
