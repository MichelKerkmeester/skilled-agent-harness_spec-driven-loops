Act as a general code-implementation agent (seat BF09). Spec folder: /Users/michelkerkmeester/MEGA/Development/Code_Environment/Public/.opencode/specs/system-spec-kit/027-xce-research-based-refinement/005-verification-and-remediation/007-release-alignment-review/004-code-remediation (PRE-APPROVED at Gate 3 — make the edits below directly; do NOT re-ask Gate 3 and do NOT ask the user anything).

FIRST load .opencode/skills/sk-code/SKILL.md, get its OpenCode surface tag, and follow that surface's standards. Also read .opencode/skills/system-spec-kit/constitutional/comment-hygiene.md.

You fix sk-code misalignments. SURGICAL, behavior-NEUTRAL edits only — comment / type / strict-mode / header edits ONLY. Do not change runtime behavior.

ALLOWED WRITE PATHS (edit ONLY these files):
- .opencode/skills/system-code-graph/mcp_server/lib/structural-indexer.ts
- .opencode/skills/system-code-graph/mcp_server/lib/tree-sitter-parser.ts
- .opencode/skills/system-code-graph/mcp_server/plugin_bridges/mk-code-graph-bridge.mjs
- .opencode/skills/system-code-graph/mcp_server/tools/code-graph-tools.ts
- .opencode/skills/system-skill-advisor/mcp_server/handlers/advisor-validate.ts
BANNED: no delete, no rename, no new files, no edits outside the listed files, NO behavior change, no --no-verify.

FIX RULES by theme:
- Comment-hygiene (ephemeral ids in comments: `FIX RC3-B`, `P4-12`, `packets 006-009`, `SC-002`, ADR-/REQ-/CHK-/task-/finding ids, or a concrete `.opencode/specs/NNN-...` path): DROP the perishable label, KEEP the durable WHY. Never delete an explanatory comment outright — only strip the ephemeral identifier.
- Shell script missing strict mode: add `set -euo pipefail` immediately after the shebang/header.
- `any` / `any[]` in a public/exported type: replace with the real type inferred from usage; it MUST still type-check.
- Missing or non-boxed module/file header: add or normalize to the sk-code OpenCode header convention (cosmetic; in scope).
- catch parameter not typed `unknown`, or exported symbol missing TSDoc: fix only if trivial and behavior-neutral.

DO-NOT-FIX (binding — confirmed false positives; leave untouched):
- `// Feature catalog: <name>` comments are ALLOWED (NOT in comment-hygiene's forbidden list). Do NOT remove them.
- TSDoc `@example` values like `specFolder: 'specs/001-demo'` — illustrative, not real pointers. Leave.
- Any file under a `dist/` directory — build output, regenerated by the build. Do NOT edit.
- Any cited finding that does not reproduce against the live file — mark refuted.

FINDINGS TO FIX (confirm-then-fix each; P0/P1/P2 all in scope):
- [P2] .opencode/skills/system-code-graph/mcp_server/lib/structural-indexer.ts:12 — Type-only imports are mixed into value imports | sk-code TypeScript style: type-only imports use import type and are separated in the final import group. | fix: Move type-only imports to separate import type statements after value imports.
- [P2] .opencode/skills/system-code-graph/mcp_server/lib/structural-indexer.ts:894 — Large module mixes parser, resolver, finalizer, and indexing responsibilities | sk-code universal patterns and code organization: keep modules small, behavior-focused, and single-purpose; files over recommended size should be split by concern. | fix: Split parser selection, module resolution, finalization, and scan orchestration into focused modules.
- [P2] .opencode/skills/system-code-graph/mcp_server/lib/tree-sitter-parser.ts:41 — Internal any interop lacks justification comment | sk-code TypeScript quality: permitted any usage must include a justification comment. | fix: Prefer a narrow parser interface; otherwise add a durable interop justification above each suppression.
- [P0] .opencode/skills/system-code-graph/mcp_server/plugin_bridges/mk-code-graph-bridge.mjs:1 — JavaScript file lacks required boxed header | JavaScript checklist P0: JavaScript and .mjs files must have a boxed component header identifying the component. | fix: Add the required component/purpose boxed header after the shebang.
- [P1] .opencode/skills/system-code-graph/mcp_server/plugin_bridges/mk-code-graph-bridge.mjs:2 — Strict directive present in ESM file | JavaScript style guide: do not add `'use strict'` to `.mjs`; ES modules are strict by definition. | fix: Remove `'use strict';` from the .mjs bridge.
- [P2] .opencode/skills/system-code-graph/mcp_server/tools/code-graph-tools.ts:17 — Type-only import is not last | TypeScript style guide: imports follow Node built-ins, third-party, local modules, then type-only imports last with separation. | fix: Move the `import type` after runtime local imports.
- [P1] .opencode/skills/system-skill-advisor/mcp_server/handlers/advisor-validate.ts:175 — catch parameter not explicitly typed unknown | sk-code TypeScript checklist P1: catch blocks use `unknown` type and narrow with instanceof | fix: Change catch blocks in this file to `catch (error: unknown)`.
- [P2] .opencode/skills/system-skill-advisor/mcp_server/handlers/advisor-validate.ts:16 — type-only import not last | sk-code TypeScript style guide: type-only imports are separate and always last | fix: Move type-only imports after all runtime imports, or split mixed schema imports into value imports plus `import type`.

After editing, re-read each edited line. Your final message MUST be exactly one fenced json block:
```json
{"seat":"BF09","fixed":[{"file":"path","theme":"","after":""}],"refuted":[{"file":"path","why":""}]}
```
