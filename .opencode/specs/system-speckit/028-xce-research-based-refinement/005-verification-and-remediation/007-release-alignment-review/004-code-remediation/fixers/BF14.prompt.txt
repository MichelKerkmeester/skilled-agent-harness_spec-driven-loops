Act as a general code-implementation agent (seat BF14). Spec folder: /Users/michelkerkmeester/MEGA/Development/Code_Environment/Public/.opencode/specs/system-spec-kit/027-xce-research-based-refinement/005-verification-and-remediation/007-release-alignment-review/004-code-remediation (PRE-APPROVED at Gate 3 — make the edits below directly; do NOT re-ask Gate 3 and do NOT ask the user anything).

FIRST load .opencode/skills/sk-code/SKILL.md, get its OpenCode surface tag, and follow that surface's standards. Also read .opencode/skills/system-spec-kit/constitutional/comment-hygiene.md.

You fix sk-code misalignments. SURGICAL, behavior-NEUTRAL edits only — comment / type / strict-mode / header edits ONLY. Do not change runtime behavior.

ALLOWED WRITE PATHS (edit ONLY these files):
- .opencode/skills/system-spec-kit/mcp_server/lib/search/folder-discovery.ts
- .opencode/skills/system-spec-kit/mcp_server/lib/search/hybrid-search.ts
- .opencode/skills/system-spec-kit/mcp_server/lib/search/pipeline/orchestrator.ts
- .opencode/skills/system-spec-kit/mcp_server/lib/search/pipeline/ranking-contract.ts
- .opencode/skills/system-spec-kit/mcp_server/lib/search/pipeline/stage1-candidate-gen.ts
BANNED: no delete, no rename, no new files, no edits outside the listed files, NO behavior change, no --no-verify.

FIX RULES by theme:
- Comment-hygiene (ephemeral ids in comments: `FIX RC3-B`, `P4-12`, `packets 006-009`, `SC-002`, ADR-/REQ-/CHK-/task-/finding ids, or a concrete `.opencode/specs/NNN-...` path): DROP the perishable label, KEEP the durable WHY. Never delete an explanatory comment outright — only strip the ephemeral identifier.
- Shell script missing strict mode: add `set -euo pipefail` immediately after the shebang/header.
- `any` / `any[]` in a public/exported type: replace with the real type inferred from usage; it MUST still type-check.
- Missing or non-boxed module/file header: add or normalize to the sk-code OpenCode header convention (cosmetic; in scope).
- catch parameter not typed `unknown`, or exported symbol missing TSDoc: fix only if trivial and behavior-neutral.

DO-NOT-FIX (binding — confirmed false positives; leave untouched):
- `// Feature catalog: <name>` comments are ALLOWED (NOT in comment-hygiene's forbidden list). Do NOT remove them.
- TSDoc `@example` values like `specFolder: 'specs/001-demo'` — illustrative, not real pointers. Leave.
- Any file under a `dist/` directory — build output, regenerated by the build. Do NOT edit.
- Any cited finding that does not reproduce against the live file — mark refuted.

FINDINGS TO FIX (confirm-then-fix each; P0/P1/P2 all in scope):
- [P0] .opencode/skills/system-spec-kit/mcp_server/lib/search/folder-discovery.ts:1069 — Spec roots named in TSDoc comment | system-spec-kit constitutional/comment-hygiene.md forbids spec-folder paths in code comments. | fix: Describe configured workspace spec roots without literal spec-folder paths.
- [P2] .opencode/skills/system-spec-kit/mcp_server/lib/search/hybrid-search.ts:8 — Import groups are not in sk-code TypeScript order | TypeScript import order: Node built-ins, third-party packages, local modules, then type-only imports last. | fix: Group third-party imports before local imports and move type-only imports to the final import block.
- [P2] .opencode/skills/system-spec-kit/mcp_server/lib/search/hybrid-search.ts:9 — Imports are not in sk-code TypeScript order | sk-code references/opencode/typescript/style_guide.md requires built-ins, third-party, local modules, then type-only imports last. | fix: Reorder imports into the four standard groups and keep type-only imports last.
- [P1] .opencode/skills/system-spec-kit/mcp_server/lib/search/pipeline/orchestrator.ts:72 — Catch parameters are not typed as unknown | sk-code TypeScript quality_standards.md:518-540 and typescript_checklist.md:231-248 require catch blocks to use unknown and narrow safely. | fix: Change each catch to catch (err: unknown) while keeping the existing instanceof narrowing.
- [P2] .opencode/skills/system-spec-kit/mcp_server/lib/search/pipeline/orchestrator.ts:22 — Type-only imports are not grouped last | sk-code code_organization.md:247-270 and typescript_checklist.md:305-320 require type-only imports to use import type and be grouped after runtime imports. | fix: Move import type statements below runtime imports in each affected file, with a blank line separating groups.
- [P1] .opencode/skills/system-spec-kit/mcp_server/lib/search/pipeline/orchestrator.ts:72 — Catch parameter is not explicitly typed unknown | TypeScript checklist P1: catch blocks use `unknown` and narrow with instanceof. | fix: Use `catch (err: unknown)` for all catch blocks in this file.
- [P2] .opencode/skills/system-spec-kit/mcp_server/lib/search/pipeline/ranking-contract.ts:31 — Probable ephemeral fix label in comment | Likely comment-hygiene.md:36-38 finding-id rule; if A1 is not a stable external standard, it should not appear in a code comment. | fix: Remove A1 FIX and keep the durable explanation that canonical score resolution keeps sorting/filtering aligned.
- [P0] .opencode/skills/system-spec-kit/mcp_server/lib/search/pipeline/stage1-candidate-gen.ts:766 — Ephemeral fix identifier in code comment | Comment hygiene hard block: comments must not embed task/finding ids; keep durable WHY only. | fix: Remove `FIX #7`; keep the Promise.allSettled rationale.

After editing, re-read each edited line. Your final message MUST be exactly one fenced json block:
```json
{"seat":"BF14","fixed":[{"file":"path","theme":"","after":""}],"refuted":[{"file":"path","why":""}]}
```
