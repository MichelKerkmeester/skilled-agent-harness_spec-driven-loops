Act as a markdown documentation-fixer (seat AF01). Spec folder: /Users/michelkerkmeester/MEGA/Development/Code_Environment/Public/.opencode/specs/system-spec-kit/027-xce-research-based-refinement/005-verification-and-remediation/007-release-alignment-review/003-readme-remediation (PRE-APPROVED at Gate 3 — make the edits below directly with the Edit/Write tools; do NOT re-ask Gate 3 and do NOT ask the user anything).

You fix STALE content in README files so they match current repo reality. SURGICAL edits only — fix the cited claims, keep everything else intact. Find the correct current value by reading the cited file PLUS its SKILL.md / source / config before editing.

ALLOWED WRITE PATHS (edit ONLY these files):
- .opencode/skills/cli-claude-code/README.md
- .opencode/skills/cli-codex/README.md
- .opencode/skills/cli-opencode/README.md
- .opencode/skills/deep-loop-runtime/README.md
- .opencode/skills/deep-loop-runtime/database/README.md
BANNED: no delete, no rename, no new files, no edits outside the listed files, no --no-verify.

DO-NOT-FIX (binding — confirmed false positives; leave untouched):
- The CLI front-door "37 tools" figure: `spec-memory.cjs` CLI is genuinely 37; only the MCP-server/daemon surface is 39. Fix a "37" ONLY when the sentence is about the MCP server/daemon, NOT the CLI front door.
- Any `// Feature catalog:` comments and TSDoc `@example` values.
- Any cited claim that does NOT actually reproduce against the live file — mark it refuted; never invent a fix.

FINDINGS TO FIX (confirm-then-fix each; P0/P1/P2 all in scope, including cosmetic/style):
- [P1] .opencode/skills/cli-claude-code/README.md:108 — Claude agent roster points at dead surface | stale: Claude Code agents live in `.claude/agents/*.md` and the README documents nine agents including `handover`, `research`, `speckit`, and `write`. -> correct: Current repo-local agent references point at `.opencode/agents/<name>.md`, and the actual roster includes current agents such as `markdown`, `deep-context`, `deep-improvement`, `prompt-improver`, and `code`; the README roster/path is stale.
- [P1] .opencode/skills/cli-codex/README.md:120 — Codex profile model is stale | stale: Profiles live in `.codex/config.toml` under `[profiles.<name>]`, and the README documents six profiles. -> correct: The current repo config declares `[agents.*]` entries that point to `.codex/agents/*.toml`; the active Codex-side roster is larger and includes agents such as `code`, `deep-context`, `deep-improvement`, `deep-research`, `deep-review`, `markdown`, `orchestrate`, and `prompt-improver`.
- [P1] .opencode/skills/cli-opencode/README.md:74 — Agent flag guidance contradicts current routing | stale: The README says to omit `--agent` from every dispatch and later says never to pass a top-level `--agent`. -> correct: The current cli-opencode reference documents `--agent` as a supported core flag, uses a default dispatch shape with per-use-case `--agent <agent-slug>`, and routes concrete tasks through `--agent context|orchestrate|review|debug|ai-council`; only `general` and some small-model paths carry narrower omit-agent caveats.
- [P1] .opencode/skills/cli-opencode/README.md:119 — Provider matrix omits Kimi K2.7 | stale: Seven providers are documented: `opencode-go`, `deepseek`, `minimax-coding-plan`, `minimax`, `xiaomi-token-plan-ams`, `xiaomi`, and `openai`. -> correct: The active small-model registry includes Kimi For Coding via provider `kimi-for-coding` with live slug `kimi-for-coding/k2p7`, and the cli-opencode model reference lists it as a dispatchable model path.
- [P1] .opencode/skills/deep-loop-runtime/README.md:58 — Convergence output shape is stale | stale: The quick start says convergence output is a JSON object with decision, signals, blockers and stopBlocked. -> correct: convergence.cjs emits a wrapper with status/data plus workflow-facing fields graph_decision, graph_signals_json, graph_blockers_json, graph_stop_blocked, graph_trace_json and graph_convergence_score.
- [P1] .opencode/skills/deep-loop-runtime/README.md:91 — Scorer and signal names are wrong | stale: The README says bayesian-scorer interprets novelty signals into typed convergence decisions, and coverage-graph-signals extracts novelty rate, claim-support ratio and contradiction count. -> correct: bayesian-scorer only exports computeScore and shouldDemote; convergence.cjs builds decisions, and coverage-graph-signals exposes typed research/review/context metrics including questionCoverage, claimVerificationRate, contradictionDensity, sliceCoverage, reuseCatalogCoverage and agreementRate.
- [P1] .opencode/skills/deep-loop-runtime/README.md:134 — Loop type troubleshooting omits current modes | stale: The troubleshooting row says input validation should check --loop-type is review or research. -> correct: The runtime scripts validate exactly research, review, council or context; improvement is host-driven and not a runtime loopType.
- [P1] .opencode/skills/deep-loop-runtime/database/README.md:12 — Database README omits council graph store | stale: The database README says persistence is read/written via lib/coverage-graph and lists only deep-loop-graph.sqlite. -> correct: The database folder also contains council-graph.sqlite, owned by lib/council/council-graph-db.ts, and scripts switch to council graph modules for loopType=council.
- [P1] .opencode/skills/deep-loop-runtime/database/README.md:23 — Writer-lock implementation is misattributed | stale: The database lifecycle says it is backed by atomic-write and flock locking via lib/deep-loop/atomic-state. -> correct: Graph writer locking is implemented by scripts/lib/cli-guards.cjs acquireWriterLock using wx lock-file acquisition, stale-lock reclamation and nonce ownership; atomic-state is not the DB writer-lock implementation.

After editing, re-read each edited line to verify. Your final message MUST be exactly one fenced json block:
```json
{"seat":"AF01","fixed":[{"file":"path","claim":"what was stale","after":"what it now says"}],"refuted":[{"file":"path","why":"why skipped"}]}
```
