Act as a markdown documentation-fixer (correction seat COR03). Spec folder: /Users/michelkerkmeester/MEGA/Development/Code_Environment/Public/.opencode/specs/system-spec-kit/027-xce-research-based-refinement/005-verification-and-remediation/007-release-alignment-review/003-readme-remediation (PRE-APPROVED at Gate 3 — make the edits directly; do NOT re-ask Gate 3 or ask anything).

An accuracy audit found that a PRIOR automated fix wrote WRONG or imprecise values in the file(s) below. Apply each audit-verified correction SURGICALLY: replace the wrong text with the actual truth. The audit already verified the truth against the cited evidence — confirm at the evidence path, then correct.

ALLOWED WRITE PATHS (edit ONLY these):
- .opencode/skills/deep-loop-workflows/SKILL.md
BANNED: no delete, no rename, no new files, no edits outside the listed files, no --no-verify.

CORRECTIONS (for each: find the wrong text, replace with the actual truth; verify at evidence first):
1. WRONG (now in file): the hub and commands resolve from it
   ACTUAL TRUTH: The registry is declarative and advisor projections are drift-guarded, but command files are static routers with hardcoded assets/mode routing; they do not resolve from `mode-registry.json`.
   EVIDENCE: .opencode/commands/deep/context.md:80-87; .opencode/commands/deep/research.md:107-116; .opencode/skills/system-skill-advisor/mcp_server/tests/routing-registry-drift-guard.vitest.ts:5-10
2. WRONG (now in file): ALWAYS resolve hub and command modes through `mode-registry.json`; advisor projection maps may be hardcoded only when the drift guard proves they match the registry projection.
   ACTUAL TRUTH: Advisor projection maps are hardcoded and drift-guarded against the registry; command mode routing is still hardcoded in the command files and does not resolve through `mode-registry.json`.
   EVIDENCE: .opencode/commands/deep/context.md:80-87; .opencode/commands/deep/research.md:107-116; .opencode/skills/system-skill-advisor/mcp_server/tests/routing-registry-drift-guard.vitest.ts:80-88

Important nuance to honor: spec-memory CLI front door = 37 tools; MCP server = 39. Validation registry = 38 rules (36 default non-strict + strict-only). Do not re-introduce the wrong value.

After editing, re-read each corrected line. Final message MUST be one fenced ```json block:
```json
{"seat":"COR03","corrected":[{"file":"","to":""}],"skipped":[{"file":"","why":""}]}
```
