Using the provided `spec_folder` as the documentation scope and the provided lineage directory as the only writable artifact root. SKILL ROUTING: User directed → `deep-review`; I’m loading that workflow contract before executing it.The memory trigger lookup rejected the supplied lineage `session_id` because it is not a Spec Memory server-managed session; I’m treating that as non-blocking because the supplied ID is the deep-review lineage ID, not a memory retrieval session. The workflow contract is loaded; I’m now reading the packet state and review resources, then I’ll write only under the lineage artifact directory.Resource loading surfaced the key constraint: this is a command-owned fan-out lineage, but this invocation is already the isolated lineage body with `artifact_dir` pre-bound. I’m skipping only the forbidden `resolveArtifactRoot` command as instructed, and preserving the deep-review state-machine files in the lineage directory.The target scope folder contains only `spec.md`; no prior lineage state exists except executor bookkeeping. I’m treating this as a fresh lineage and using the scope spec as the review target.Prior sibling lineage files exist, but every finding still needs direct evidence from shipped code before it can be recorded. I’m now reading the live search/retrieval implementation around scoped fallback and candidate-generation seams.The code graph is stale (`git HEAD changed`, 442 stale files), so I’m using graphless fallback: exact Grep/Glob plus direct file reads, and I’ll record that trust state in the review artifacts.I have two evidence-backed candidates: community fallback appends unscoped members, and summary embeddings perform global top-K before scoped filtering. I’m checking downstream filters and primary retrieval semantics now before assigning severity.Downstream checks confirm the community path only applies tenant/user/agent governance scope, not `specFolder`; the formatter only maps received rows. I’m recording that as a P1 scoped retrieval defect, with the summary-embedding top-K/scope issue as a second P1.Phase init is ready. I’m creating the required lineage subdirectories, then adding the config, JSONL state, registry, strategy, prompt, iteration, delta, dashboard, resource map, and synthesis report under the bound artifact root only.The single iteration will stop by `maxIterationsReached` with verdict `CONDITIONAL`: two active P1 retrieval-scope findings, no P0. I’m writing the state packet now with typed claim-adjudication packets for both P1 findings.Artifact files are written. I’m running a local integrity check for JSON/JSONL parsing, required file presence, and the mandatory iteration final-line contract.FANOUT_LINEAGE_COMPLETE:gpt55r2-a-4