Using the provided spec folder and lineage artifact directory as the write scope. I’ll load the deep-review contract first, then run the single-iteration review loop and write only under the specified lineage directory.The memory trigger probe rejected the supplied fan-out `session_id` because it is not a server-managed memory session, so I’m retrying without that memory session binding while preserving the user-supplied review `session_id` for artifacts.SKILL ROUTING: User directed → `deep-review`. Reading the workflow contract and scope inputs before any artifact writes.The skill contract requires command-owned state artifacts and reducer-style outputs. I’ll inspect the existing lineage packet and review resources, then initialize or continue the packet in place without resolving a different artifact root.The lineage directory is fresh except for executor metadata. I’m initializing the required state packet and will focus iteration 001 on the highest-risk dimension, correctness/numeric behavior, across the declared search/retrieval scope.Code graph is stale, so structural claims will be graphless and verified by direct `Glob`/`Grep`/`Read` evidence instead of graph traversal.I found a concrete cross-scope candidate issue in the community fallback path and am checking whether downstream formatting re-applies `specFolder` or `limit` before recording it.Downstream formatting preserves whatever rows it receives; it does not re-filter by `specFolder`. I’m recording this as a P1 because a scoped `memory_search` can append community members from other folders when the primary result set is weak.The single iteration will record one active P1 finding and one P2 advisory. I’m creating the lineage packet files now, keeping all writes inside the supplied `gpt55r2-a-2` artifact directory.Artifacts are written. I’m doing a local artifact integrity check now: JSON/JSONL parse, required file presence, and the mandatory final verdict line.FANOUT_LINEAGE_COMPLETE:gpt55r2-a-2