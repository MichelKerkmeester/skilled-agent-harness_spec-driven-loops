Binding the artifact directory to the provided fan-out lineage path and loading the deep-review workflow contract before running the single-iteration review loop.The provided fan-out `session_id` is workflow-local, not a Spec Memory managed session, so I’m using it only for deep-review artifacts and omitting it from memory lookup.SKILL ROUTING: User directed → deep-review.The workflow contract requires command-owned state files plus per-iteration markdown and JSONL deltas; I’ll create only those files under the provided lineage directory.Loading the required protocol resources and scope packet now, then I’ll bind the lineage artifact root directly without calling the resolver.The scope packet is minimal (`spec.md` only) and the lineage directory currently contains only executor state, so phase_init will be a fresh lineage initialization.Scope is server infrastructure only: handlers/providers/daemon/IPC under `system-spec-kit`, excluding the search pipeline and store/index/lifecycle surfaces. I’m enumerating those target files for the single review pass.The scope lists `context-server.js`, but the exact root glob did not find it; I’m checking the MCP server tree for its actual location before making any finding.The runtime entry point is `context-server.ts` rather than the stale `context-server.js` name in the scope note; I’ll review the live TypeScript source and the CLI/IPC daemon files.The daemon supervision helper is under `.opencode/bin/lib/model-server-supervision.cjs`, not under the MCP server tree; I’m including it because the launcher imports it directly.The service-local IPC file is a thin re-export, so I’m opening the shared IPC implementation it delegates to before assessing IPC security or stale-socket behavior.I found a concrete TCP IPC drift candidate: launcher bridge honors `tcp://`, but the shared server-side resolver appears to pass the same env value through `path.resolve`, which would turn it into a filesystem path. I’m verifying that behavior before recording it.The CLI itself correctly preserves `tcp://` in `getIpcSocketPath`; this strengthens the server-side resolver drift rather than disproving it. Next I’m checking handler path boundaries and lifecycle code for independent issues.A second concrete candidate is checkpoint scope matching: the handler’s scope matcher treats missing checkpoint metadata as matching any requested tenant/user/agent scope, which may let scoped callers list/restore unscoped checkpoints. I’m opening the storage layer to confirm whether it is also filtered there or only in the handler.Storage confirms the same wildcard semantics: `checkpointMetadataMatchesScope` returns true when checkpoint metadata omits a requested scope field, and list/get/restore/delete all consume that predicate. This is a stronger isolation finding.Two confirmed findings are enough for this single-iteration fan-out pass: one P1 checkpoint scope-isolation issue and one P1 TCP IPC resolver drift. I’m creating the lineage state and synthesis artifacts under the bound artifact directory only.Writing the final lineage packet now: config/state JSONL, findings registry, strategy/dashboard, one iteration file, resource map, and synthesis report.Verifying the emitted lineage packet for JSON/JSONL parseability, required artifact presence, and the exact iteration verdict final line.Artifact validation passed: config/registry JSON parse, state JSONL has 4 records, required markdown outputs exist, and `iteration-001.md` ends with the required `Review verdict: CONDITIONAL` line.FANOUT_LINEAGE_COMPLETE:gpt55r2-c-1