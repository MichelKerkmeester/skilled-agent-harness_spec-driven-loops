DEEP-RESEARCH
Resolved route: mode=research; target_agent=@deep-research; execution=single_iteration; state_source=externalized_files; do_not_switch_mode=true

# Deep-Research Iteration 7

STATE SUMMARY: Iteration 7 of 10; all key questions have provisional evidence-backed answers; `stopPolicy=max-iterations` requires continued adversarial validation.

Focus: Perform a read-only collision and legacy-dependency inventory. Determine whether tracked or generated content can exist only under legacy `specs/`, whether canonical and legacy identities are persisted differently, which tests cover root precedence, and which collision/duplicate cases remain untested. Use git/tree/config evidence without altering or removing the live symlink.

Read the packet state and relevant prior iterations first. Write only `iterations/iteration-007.md`, one canonical append to `deep-research-state.jsonl`, and `deltas/iter-007.jsonl` under the active research packet. Execute exactly one LEAF iteration with 3-5 focused actions and at most 12 tool calls. Do not modify investigated code, symlinks, git state, or reducer-owned files. Cite each finding with exact file:line or command-result evidence and label inferences.

Include exact route proof, `iteration: 7`, `run: 7`, and every required status/novelty/question/source field in both canonical records. Verify all three outputs and packet scope before returning.
