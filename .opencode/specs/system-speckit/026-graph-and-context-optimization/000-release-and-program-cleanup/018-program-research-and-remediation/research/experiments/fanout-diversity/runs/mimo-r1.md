Now I have enough evidence to compile the report.

### Q1: Self-maintaining memory-index move-reconciliation under rapid concurrent spec-folder moves across sessions
- Findings:
  1. `reconcileMoves()` matches vanished→new paths by grandparent directory + basename uniqueness, gated by `packet_id` from `graph-metadata.json` at the new location. Evidence: `.opencode/skills/system-spec-kit/mcp_server/lib/storage/incremental-index.ts:520-535` [HIGH]
  2. The function requires exactly one candidate in `toDelete` matching the grandparent+basename criterion (`siblingDeleted.length !== 1` → skip), preventing ambiguous multi-match. Evidence: `.opencode/skills/system-spec-kit/mcp_server/lib/storage/incremental-index.ts:575` [HIGH]
  3. A DB uniqueness guard re-reads `memory_index` for the old path and requires exactly one live row (`rows.length !== 1` → skip). Evidence: `.opencode/skills/system-spec-kit/mcp_server/lib/storage/incremental-index.ts:581-582` [HIGH]
  4. Document-type cross-verification prevents mismatched repointing when `storedDocType` disagrees with the new path's doc type. Evidence: `.opencode/skills/system-spec-kit/mcp_server/lib/storage/incremental-index.ts:588-591` [HIGH]
  5. The reconciliation runs inside `memory_index_scan` which acquires an atomic scan lease with heartbeat refresh (lease expiry/3 interval), preventing concurrent scans from racing. Evidence: `.opencode/skills/system-spec-kit/mcp_server/handlers/memory-index.ts:351-408` [HIGH]
  6. Save operations are serialized per-spec-folder by `withSpecFolderLock` using both an in-process promise chain and an inter-process `mkdir`-based lock with 5-minute stale timeout. Evidence: `.opencode/skills/system-spec-kit/mcp_server/handlers/save/spec-folder-mutex.ts:14,54-86` [HIGH]
  7. The reconciliation update is a single `UPDATE ... WHERE id = ? AND embedding_status != 'failed'` statement, not wrapped in an explicit transaction with the preceding `SELECT`, creating a TOCTOU window if two scans race on the same row. Evidence: `.opencode/skills/system-spec-kit/mcp_server/lib/storage/incremental-index.ts:603-614` [MEDIUM]
  8. Cross-session rapid moves (session A moves folder X→Y, session B moves Y→Z before A's scan completes) would fail reconciliation for the second move because the first move's reconciled path would already be consumed, causing the second to fall through to delete+reindex. Evidence: inferred from logic at lines 616-618 [MEDIUM]
- Verdict: The move-reconciliation is correct for single-session sequential moves and adequately protected against concurrent scans by the lease mechanism. The remaining race exposure is narrow: a TOCTOU between the uniqueness SELECT and the UPDATE within a single scan, and cross-session rapid-chaining of moves which degrades gracefully to delete+reindex rather than producing corruption. [HIGH confidence]

### Q2: Shared-daemon lease model when the lease-holder launcher dies ungracefully
- Findings:
  1. The spec-kit memory launcher uses a dual-layer lease: a PID file (`.mk-spec-memory-launcher.json`) and an owner lease file (`.spec-memory-owner.json`) with `ownerPid`, `ppid`, `lastHeartbeatIso`, and `ttlMs=60000`. Evidence: `.opencode/bin/mk-spec-memory-launcher.cjs:84-85,314-323` [HIGH]
  2. The owner lease heartbeat runs at `ttlMs/2` (30 seconds). If `refreshOwnerLeaseFile` fails (e.g. another launcher reclaimed), the launcher self-terminates. Evidence: `.opencode/bin/mk-spec-memory-launcher.cjs:427-443` [HIGH]
  3. Lease classification (`classifyOwnerLease`) checks in order: PID liveness via `process.kill(pid, 0)`, ppid-1-orphan detection, and heartbeat staleness (`now - heartbeat > ttlMs * 2` = 2 minutes). Evidence: `.opencode/bin/mk-spec-memory-launcher.cjs:346-363` [HIGH]
  4. On SIGKILL/crash, the PID file remains. A secondary launcher detects stale PID via ESRCH, reclaims the owner lease with `writeOwnerLeaseFileExclusive` or atomic rename, reaps the dead owner's process tree, and respawns. Evidence: `.opencode/bin/mk-spec-memory-launcher.cjs:365-402,651-668` [HIGH]
  5. The code-graph subsystem uses an identical owner-lease model with mutation locks (`openSync('wx')` + fsync), stale-PID reclaim, ppid-1-orphan detection, and wedged-lock self-healing for unparseable PIDs. Evidence: `.opencode/skills/system-code-graph/mcp_server/lib/owner-lease.ts:119-208,358-384` [HIGH]
  6. The mutation lock has identity-checked stale removal: it re-reads the lock PID immediately before unlink to avoid deleting a successor's live lock. Evidence: `.opencode/skills/system-code-graph/mcp_server/lib/owner-lease.ts:156-163` [HIGH]
  7. Re-election latency: if the owner dies, the next launcher to attempt acquisition detects `stale-pid` or `stale-heartbeat-reclaim` and reclaims immediately in the same `acquireOwnerLease` call — no polling loop or delay. Evidence: `.opencode/skills/system-code-graph/mcp_server/lib/owner-lease.ts:393-406` [HIGH]
  8. For heartbeat-stale reclaim, worst-case latency is `ttlMs * 2` = 120 seconds (owner dies between heartbeats, secondary checks before staleness threshold). For PID-dead reclaim, latency is the time to the next `acquireOwnerLease` call. Evidence: `.opencode/skills/system-code-graph/mcp_server/lib/owner-lease.ts:314-318` [HIGH]
  9. A race exists between two launchers simultaneously reclaiming a stale lease: the mutation lock serializes them, but the loser gets `acquired: false` and must retry. Evidence: `.opencode/skills/system-code-graph/mcp_server/lib/owner-lease.ts:438-468` [MEDIUM]
- Verdict: When the lease-holder dies ungracefully, a secondary launcher reclaims the lease immediately upon next access (PID-dead) or within 2 minutes (heartbeat-stale). The re-election latency is effectively zero for SIGKILL/crash (next caller wins) and bounded by 2×TTL for silent hangs. The mutation lock prevents split-brain. The main remaining exposure is the 2-minute heartbeat window during which the daemon is dead but not yet reclaimable if the owner process is alive but hung. [HIGH confidence]

### Q3: Code-graph multi-file union queries (`unionMode:'multi'` and `hotFileBreadcrumb`)
- Findings:
  1. `unionMode:'multi'` is exposed in the tool schema as a `blast_radius` parameter: "Blast-radius subject handling mode; use multi to union subject + subjects". Evidence: `.opencode/skills/system-code-graph/mcp_server/tool-schemas.ts:54` [HIGH]
  2. When `unionMode === 'multi'`, the handler merges `subject` and `args.subjects` into a single `rawSubjects` array and resolves each independently, producing a union of source files. Evidence: `.opencode/skills/system-code-graph/mcp_server/handlers/query.ts:1319-1321` [HIGH]
  3. The test suite exercises `unionMode:'multi'` with two source files (`src/a.ts` + `src/x.ts`), verifying `multiFileUnion: true`, correct `sourceFiles` array, and merged `depthGroups`. Evidence: `.opencode/skills/system-code-graph/mcp_server/tests/code-graph-query-handler.vitest.ts:983-1073` [HIGH]
  4. `buildHotFileBreadcrumbs()` computes top-10% degree nodes (minimum threshold of 20), querying `graphDb.queryFileDegrees()` for the resolved file paths. Evidence: `.opencode/skills/system-code-graph/mcp_server/handlers/query.ts:932-962` [HIGH]
  5. The `HotFileBreadcrumb` type is `{ degree: number; changeCarefullyReason: string }` — a simple high-degree warning annotation. Evidence: `.opencode/skills/system-code-graph/mcp_server/lib/shared/shared-payload.ts:59-62` [HIGH]
  6. The test verifies that `src/c.ts` (degree 5) gets a `hotFileBreadcrumb` while lower-degree files do not, and that the breadcrumb appears in `nodes`, `affectedFiles`, and `hotFileBreadcrumbs` arrays. Evidence: `.opencode/skills/system-code-graph/mcp_server/tests/code-graph-query-handler.vitest.ts:1021-1072` [HIGH]
  7. The `hotFileBreadcrumb` is also integrated into the shared-payload system (`SharedPayloadSection.hotFileBreadcrumb`), indicating it flows through to context assembly for the memory MCP. Evidence: `.opencode/skills/system-code-graph/mcp_server/lib/shared/shared-payload.ts:80` [HIGH]
  8. Graceful degradation: when one subject in a multi-union fails to resolve, already-resolved seeds are preserved in a partial result rather than discarding all work. Evidence: `.opencode/skills/system-code-graph/mcp_server/handlers/query.ts:1354-1392` [HIGH]
- Verdict: Both features are actively implemented, tested, and earn their complexity. `unionMode:'multi'` enables blast-radius analysis across multiple changed files in a single query (essential for PR review workflows), and `hotFileBreadcrumb` provides actionable "change carefully" warnings for high-degree hub files. The test coverage is concrete and the graceful degradation for partial resolution adds robustness. [HIGH confidence]

### Q4: Skill-graph enhancement-edge propagation (skill advisor)
- Findings:
  1. `detectInboundEnhances()` scores candidate edges using three composite rules: family-inference (max 0.45), asset-shape (max 0.30), and sibling-transitivity (max 0.15). Candidates below `minConfidence` are filtered. Evidence: `.opencode/skills/system-skill-advisor/mcp_server/lib/cross-skill-edges/detect-inbound-enhances.ts:70-166,177-231` [HIGH]
  2. The `propagateInboundEnhances()` orchestrator loads all skill metadata, runs detection, and optionally applies candidates by patching `graph-metadata.json` files with `auto_added_at` and `auto_added_reason` metadata. Evidence: `.opencode/skills/system-skill-advisor/mcp_server/lib/cross-skill-edges/index.ts:26-69` [HIGH]
  3. The `graph_causal` scorer lane traverses the skill graph with BFS up to depth 2, using `EDGE_MULTIPLIER` weights where `enhances: 0.55` is the strongest positive signal. Evidence: `.opencode/skills/system-skill-advisor/mcp_server/lib/scorer/lanes/graph-causal.ts:13-19,55-87` [HIGH]
  4. The graph-causal lane is part of the live fusion scorer with default weight 0.13 (13% of final score). Evidence: `.opencode/skills/system-skill-advisor/mcp_server/lib/scorer/lane-registry.ts:10` [HIGH]
  5. Tests verify that the propagation system: (a) detects cross-family enhances edges, (b) applies them to `graph-metadata.json`, (c) handles malformed JSON gracefully, and (d) respects `skill_has_files` asset-shape rules. Evidence: `.opencode/skills/system-skill-advisor/mcp_server/tests/cross-skill-edges.vitest.ts:319-348,354-380,382+` [HIGH]
  6. No evidence exists of A/B testing, telemetry, or quantitative measurement of whether enhances-edge propagation improves unprompted skill discovery rates. The mechanism is structural-only. Evidence: grep for "unprompted", "discover", "measure.*improvement" returned zero relevant hits. [HIGH]
  7. The propagation is a maintenance tool (`skill_graph_propagate_enhances`), not an automatic runtime behavior. It must be explicitly invoked (report/propose/apply modes). Evidence: `.opencode/skills/system-skill-advisor/mcp_server/handlers/skill-graph/propagate-enhances.ts:34-72` [HIGH]
- Verdict: The mechanism works as designed: it detects missing `enhances` edges via composite scoring and can apply them to `graph-metadata.json`, which then feeds the `graph_causal` scorer lane (13% of recommendation weight). However, there is no measurement evidence that it improves unprompted discovery — the system is purely structural with no A/B test or telemetry backing. The value proposition is plausible (more edges → better graph traversal → better recommendations) but unquantified. [MEDIUM confidence]

### Q5: Doctor command coverage gaps
- Findings:
  1. The `/doctor` router (`_routes.yaml`) defines 7 targets: `memory`, `embeddings`, `causal-graph`, `code-graph`, `deep-loop`, `skill-advisor`, `skill-budget`. Evidence: `.opencode/commands/doctor/_routes.yaml:27-147` [HIGH]
  2. Companion commands: `/doctor:mcp` (install/debug) and `/doctor:update` (multi-subsystem orchestrator). Evidence: `.opencode/commands/doctor/_routes.yaml:156-193` [HIGH]
  3. The `/doctor:update` subsystem contract lists 8 subsystems: code-graph, context-index, vector-index, causal-edges, skill-graph, advisor, deep-loop-graph, and **eval**. Evidence: `.opencode/commands/doctor/update.md:209-221` [HIGH]
  4. **eval** (`speckit-eval.db`) has no dedicated `/doctor` diagnostic target. It is only reachable through `/doctor:update`'s dependency DAG. Evidence: `_routes.yaml` has no eval target; `update.md:220` shows eval as an update-only subsystem. [HIGH]
  5. **Spec-kit Memory MCP server internals** (embedding pipeline, vector-index health, FTS shadow integrity, causal-edges table) lack a dedicated `/doctor` target — `memory` is a read-only diagnostic, and `/doctor:update` handles the rebuild, but there is no `/doctor embeddings` for the vector store or `/doctor vector-index` specifically. Evidence: `_routes.yaml:45-56` shows `embeddings` exists but is provider/model-server status only. [MEDIUM]
  6. **IPC/launcher subsystem** (socket server, bridge, session proxy, respawn lock) has no `/doctor` target. Failures in the IPC bridge or launcher lifecycle go undiagnosed unless `/doctor:mcp debug` catches them at the MCP transport level. Evidence: no IPC/launcher target in `_routes.yaml`. [HIGH]
  7. **Skill-graph** has no isolated `/doctor` target — it is bundled into `skill-advisor` (which covers both the graph and the scorer). Evidence: `_routes.yaml:114-134` shows `skill-advisor` covers `skill_graph_scan` and `skill_graph_query`. [LOW — this is by design, not a gap]
  8. **Session management** (session snapshots, session health, context-metrics) has no `/doctor` target. Evidence: no session-related target in `_routes.yaml`. [MEDIUM]
- Verdict: The primary coverage gaps are: (1) **eval/speckit-eval.db** — no diagnostic target, only reachable through `/doctor:update`; (2) **IPC/launcher infrastructure** — socket bridge, session proxy, and respawn lock have no diagnostic surface; (3) **session management** — session snapshots and health are not independently diagnosable. The `embeddings` target covers provider status but not vector-store internals. These gaps mean that IPC bridge failures, eval DB corruption, and session snapshot issues require manual investigation or running the full `/doctor:update` orchestrator. [HIGH confidence]
