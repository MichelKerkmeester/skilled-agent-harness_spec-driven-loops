<!-- deep-review iteration (Opus 4.8, Workflow round 5/10) -->
# Iteration 5 — Regression-test validity (false-confidence audit) [correctness]

> Opus-4.8 finder + 2 independent adversarial skeptics per finding. A finding is CONFIRMED-REAL only if BOTH skeptics agree it is outstanding in current code.

### OR-5-01 [P2] [correctness] (test-false-confidence) Both new ensure-ready auto-establish tests give false confidence — they pass against the un-fixed gate

file: `.opencode/skills/system-code-graph/mcp_server/tests/ensure-ready.vitest.ts:375-398 (auto-establishes ...) and 400-420 (does NOT auto-establish ...)`

why: The two new tests claim to verify the BUG-03/CG-020 first-time auto-establish gate (the new isDefaultEndUserScope(...) branch added at lib/ensure-ready.ts:668-670). I empirically confirmed they do NOT distinguish the fix: I neutralized the new gate (set `const firstTimeAutoEstablish = false;`) and BOTH tests still passed (2 passed). Reason: for these fixtures the pre-existing evaluateGuardedFullScan -> shouldAutoRescan path already produces the identical decision. Test 1 (default scope, empty graph): stored fingerprint = active fingerprint (both default 'skills=none...') and parse backlog 0, so shouldAutoRescan returns {allowed:true} at baseline too -> scan runs, inlineIndexPerformed:true, autoRescanSafety:'allowed'. Test 2 (INDEX_SKILLS=true, stored=default): active fingerprint (skills=all) != stored (skills=none) -> scope_mismatch -> blocked at baseline too -> action:'full_scan', inlineIndexPerformed:false, autoRescanSafety:'blocked'. A regression in isDefaultEndUserScope (always-true or always-false) would not be caught: Test 1's matching fingerprint still allows via the fallback; Test 2's scope mismatch still blocks via the fallback. The end-to-end behavior IS correct, but the tests assert nothing the old code didn't already satisfy.

fix: Make each test isolate the new branch from the fallback. For the auto-establish test, set stored scope != active scope (e.g. getStoredCodeGraphScopeMock returns a NON-default/mismatched fingerprint) so evaluateGuardedFullScan would BLOCK (scope_mismatch) — then only the isDefaultEndUserScope empty-graph branch can produce autoRescanSafety:'allowed'/inlineIndexPerformed:true. For the no-auto-establish test, keep stored==active (matching fingerprint, 0 backlog) so the fallback would ALLOW, and rely on isDefaultEndUserScope=false (because INDEX_SKILLS opt-in) to be the sole reason for the block; assert autoRescanBlockReason is the guard/scope reason. With these fixtures, neutralizing firstTimeAutoEstablish would flip both results and the tests would fail against the un-fixed code.

confidence: 0.9 | related: CG-020

- skeptic verdict: **CONFIRMED_REAL** (refuted=false, sev→P2, conf=0.95)
  The finder's empirical claim reproduces exactly. I confirmed by mutation testing: setting `firstTimeAutoEstablish = false` (neutralizing the entire BUG-03/CG-020 fix) leaves both new regression tests passing (2 passed). The tests therefore give FALSE CONFIDENCE — they assert nothing that the pre-existing evaluateGuardedFullScan -> shouldAutoRescan fallback didn't already produce for these specific fixtures. Test 1's stored==active default fingerprint + 0 backlog makes the fallback ALLOW (so the empty-graph auto-establish branch is indistinguishable from the fallback's allow). Test 2's INDEX_SKILLS=true creates a scope_mismatch that makes the fallback BLOCK (so the isDefaultEndUserScope=false outcome is indistinguishable from the fallback's block). A regression in isDefaultEndUserScope (always-true or always-false) would not be caught. The PRODUCTION behavior at ensure-ready.ts:668-677 is correct and complete — this is purely a test-quality / mutation-sensitivity gap, hence P2, matching report category (d). The proposed fix is sound: give Test 1 a mismatched stored fingerprint so the fallback would block and only the auto-establish branch can yield allowed; keep Test 2 stored==active+0 backlog so the fallback would allow and only isDefaultEndUserScope=false can produce the block; then neutralizing the gate flips both and the tests fail. This is a NEW finding not present in either prior review (these are the freshly-landed session regression tests). Confidence 0.95: the empirical mutation run is definitive; the small residual is only that 2 of the 4 newly-landed tests were named in the prior remediation list as covering DR-001-01/DR-008-* (different files) — but the OR-5-01 tests are specifically the two auto-establish tests in ensure-ready.vitest.ts, which I verified directly.
- skeptic verdict: **CONFIRMED_REAL** (refuted=false, sev→P2, conf=0.95)
  The finder's claim is empirically true. I reproduced it directly: setting `firstTimeAutoEstablish = false` (neutralizing the new BUG-03/CG-020 gate at lib/ensure-ready.ts:668-670) leaves BOTH new regression tests passing (2 passed), because the pre-existing evaluateGuardedFullScan -> shouldAutoRescan fallback independently produces the identical decision for each fixture. Test 1's matching default fingerprints + 0 backlog yield {allowed:true} via the fallback; Test 2's INDEX_SKILLS=true scope_mismatch yields blocked via the fallback. Neither test forces the new isDefaultEndUserScope branch to be the SOLE cause of its asserted outcome, so a regression in that branch (always-true or always-false) would not be caught. This squarely matches review-prompt category (d): a regression test that gives false confidence by passing against the un-fixed code. It is a genuinely NEW issue — prior reviews flagged the underlying BUG-03/CG-020 deferral, not the test-isolation quality of these two newly-landed tests. ADVERSARIAL LENS (already-mitigated): the finding is NOT closed by any LANDED REMEDIATION item. The LANDED list only states these tests were "added"; it makes no claim that they isolate the gate from the fallback. So the already-mitigated refutation does not hold. SEVERITY: P2 is correct, not P0/P1. The production gate itself is correct and was NOT refuted — there is no data loss, crash, or runtime bug. The defect is purely test hygiene: the tests provide false confidence in a fix that is in fact correct. The finder's proposed fix (Test 1: set stored != active so the fallback would block, leaving only the empty-graph isDefaultEndUserScope branch to allow; Test 2: set stored == active + 0 backlog so the fallback would allow, leaving INDEX_SKILLS opt-in as the sole block reason) would correctly isolate the branch and fail against the un-fixed code. confidence 0.95: empirically verified against current code with a clean restore; the only residual uncertainty is whether the loop wishes to treat correct-but-weakly-tested fixes as in-scope, which the prompt explicitly does via category (d).

**RESULT: CONFIRMED-REAL**

---

coverage: Round 5 (correctness / regression-test validity). Read every focus test AND its code-under-test, git-diffed each vs baseline 0ef38d58, and for the decisive case ran the suite against a simulated un-fixed gate. VERIFIED-SOUND (test genuinely fails against un-fixed code): (1) canonical-db-dir DR-003-02 — pre-fix mkdir'd the symlink-escape leaf before rejecting; the existsSync(outside/code-graph/database)===false assertion fails at baseline. (2) security-hardening DR-008-01 — baseline startIpcSocketServer had no post-mkdir perms check, so binding under a 0o777 dir would not throw /group\\/world-writable/; the skipIf(getuid) guard is correct for portability. (3) launcher-idle-timeout DR-008-04 — baseline left stopped=true + timer cleared on a thrown onIdle, so the second advanceTimersByTimeAsync never re-fires; toHaveBeenCalledTimes(2) fails at baseline (verified fake-timer math: 60ms timeout, 1000ms checkInterval). (4) status-readiness-snapshot DR-001-01 — confirmed via git show that baseline's `const storedScope = graphDb.getStoredCodeGraphScope()` was OUTSIDE any try/catch, so the mocked throw rejects handleCodeGraphStatus entirely and the JSON.parse/assertions never run; post-fix returns the readiness_unavailable degraded envelope. (5) tool-args-validation BUG-04 — baseline validateToolArgs body only checked tool-name + object-shape (git-show confirmed); enum/additionalProperties/minLength assertions and the limit/maxDepth 1000/20 ceiling assertions (baseline was 200/10) all fail at baseline. The 'accepts well-formed', 'throws unknown tool', 'throws non-object' assertions pass both pre/post but are acceptable non-regression coverage, not false confidence for a landed fix. (6) query-handler BUG-01 x3 + BUG-07 — baseline shouldBlockReadPath keyed on action==='full_scan' && !inlineIndexPerformed; all three BUG-01 fixtures (stale/action:none, failed selective_reindex, fresh+verificationGate:fail) had action!=='full_scan' or freshness==='fresh', so baseline answered (queryEdges called) instead of blocking — assertions fail at baseline. BUG-07 status 'error'->'blocked' is a real assertion flip. (7) launcher-lease — real launcher spawn (not mocked); path swap and the legacy-lease integration test correctly track the verified database-location reversal (baseline PRIMARY=.spec-kit/code-graph/database with legacy=skills/...; post-fix PRIMARY=skills/system-code-graph/mcp_server/database with legacy=.spec-kit/... at launcher line 225, which the test plants and detects). (8) default-scope predicate — tests the new isDefaultEndUserScope predicate directly (import fails at baseline where the export does not exist); narrow but not false confidence (assertions match implementation incl. the allow-list case via resolveIncludedSkillsList). All 53 focus tests pass post-fix; ensure-ready 17/17 pass after I restored the file. ONE finding (OR-5-01): the two new ensure-ready auto-establish tests are false-confidence — empirically proven to pass with the new isDefaultEndUserScope gate neutralized, because the pre-existing evaluateGuardedFullScan scope-fingerprint path already yields the same allow/block decision for those fixtures. Side observation (not reported as a finding — launcher source, out of test-validity scope, and documented as intentional): legacyLeasePaths line 226 probes 'skill' (singular) as a known pre-rename typo dir.

newFindings: 1 (confirmed-real), rawFindings: 1, dimension: correctness
