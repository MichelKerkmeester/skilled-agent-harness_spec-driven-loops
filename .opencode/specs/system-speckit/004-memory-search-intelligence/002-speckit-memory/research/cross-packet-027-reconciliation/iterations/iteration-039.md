# Iteration 39 (Round N adversarial): Completeness + honesty audit at ~140 iterations

## Focus
Adversarial completeness/honesty audit of the 005 revisit. Read-only.

## Findings (newInfoRatio 0.45)
**HONESTY-VERDICT: YES — net-deflationary + self-correcting** (Q2 hole refuted + candidate withdrawn, Q4 flip tested+falsified, Q6/Q7 killed, Q5 non-issue, C8 escalated-then-re-deflated, Q3 H/gating→hygiene). **No seat fabricated a benefit NUMBER** (grep zero); all leverage categorical.
**OVER-CLAIMS caught:**
- **skip-closed (iter-012/028) graded "TOP gating blocker, LEVERAGE H" → refuted to theoretical hygiene by iter-032.** The over-claim PROPAGATED into roadmap-edit #2 ("promote C3-D to HARD gating prereq") which is now STALE — must retract in synthesis.
- iter-022 C8 "REAL render-gap, stronger than assumed" asserted on an unread renderer → corrected by iter-031 (seam wrong + threat-model-gated).
- LEVERAGE/EFFORT tags are asserted, never build-measured (soft form of the 028-parent lesson) — treat every H/S as inferred.
**MISS:** 028 C9 embedder-degrade-on-recall ↔ 027's embedder-reconcile/swap subsystem (`memory_embedding_reconcile`) was set aside (iter-007), never mapped. Mild genuine gap → Round O.
**STRUCTURED-ARTIFACTS LAG:** findings-registry records no withdrawn/downgraded disposition; dashboard stale (shows Q1-Q11 unchecked, NEXT FOCUS=Q1). A reader of only the registry/dashboard misses every self-correction → research.md must be the authoritative source of truth.

## Highest residual unknown
The real-world threat model of the unescaped `getTieredContent` HOT-tier render (`memory-triggers.ts:706-707`→`:721,784`): is "who can write spec/memory files" a real injection vector? That decides C8 must-fix vs no-op. Runner-up: C3-C "Current" additive-M vs replace-L.

## Next Focus
Round O: resolve the threat model (O1) + map C9↔embedder-reconcile (O2); synthesis retracts the stale skip-closed gate framing + makes research.md authoritative.
