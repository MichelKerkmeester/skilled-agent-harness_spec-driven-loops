Act as a markdown documentation-fixer (seat AF14). Spec folder: /Users/michelkerkmeester/MEGA/Development/Code_Environment/Public/.opencode/specs/system-spec-kit/027-xce-research-based-refinement/005-verification-and-remediation/007-release-alignment-review/003-readme-remediation (PRE-APPROVED at Gate 3 — make the edits below directly with the Edit/Write tools; do NOT re-ask Gate 3 and do NOT ask the user anything).

You fix STALE content in README files so they match current repo reality. SURGICAL edits only — fix the cited claims, keep everything else intact. Find the correct current value by reading the cited file PLUS its SKILL.md / source / config before editing.

ALLOWED WRITE PATHS (edit ONLY these files):
- .opencode/skills/system-skill-advisor/mcp_server/lib/scorer/lanes/README.md
- .opencode/skills/system-skill-advisor/mcp_server/lib/skill-graph/README.md
- .opencode/skills/system-skill-advisor/mcp_server/schemas/README.md
- .opencode/skills/system-skill-advisor/mcp_server/scripts/README.md
- .opencode/skills/system-skill-advisor/mcp_server/tests/README.md
BANNED: no delete, no rename, no new files, no edits outside the listed files, no --no-verify.

DO-NOT-FIX (binding — confirmed false positives; leave untouched):
- The CLI front-door "37 tools" figure: `spec-memory.cjs` CLI is genuinely 37; only the MCP-server/daemon surface is 39. Fix a "37" ONLY when the sentence is about the MCP server/daemon, NOT the CLI front door.
- Any `// Feature catalog:` comments and TSDoc `@example` values.
- Any cited claim that does NOT actually reproduce against the live file — mark it refuted; never invent a fix.

FINDINGS TO FIX (confirm-then-fix each; P0/P1/P2 all in scope, including cosmetic/style):
- [P1] .opencode/skills/system-skill-advisor/mcp_server/lib/scorer/lanes/README.md:29 — Lane README omits BM25 shadow lane | stale: The folder has 5 code files and lists only `derived.ts`, `explicit.ts`, `graph-causal.ts`, `lexical.ts` and `semantic-shadow.ts`. -> correct: The folder now also contains an active `bm25.ts` BM25F lexical shadow lane used by `lexical.ts` and registered as a shadow lane.
- [P0] .opencode/skills/system-skill-advisor/mcp_server/lib/skill-graph/README.md:48 — Validation command uses unsupported Vitest flag | stale: Run `npm test -- --runInBand`. -> correct: The package test script is Vitest; current serial execution uses `--no-file-parallelism --maxWorkers=1`, and `--runInBand` is rejected as `CACError: Unknown option --runInBand`.
- [P1] .opencode/skills/system-skill-advisor/mcp_server/schemas/README.md:18 — Schemas README uses removed skill_advisor path | stale: `skill_advisor/schemas/` defines contract shapes. -> correct: Schemas live directly under `mcp_server/schemas/`; handlers import `../schemas/advisor-tool-schemas.js`.
- [P2] .opencode/skills/system-skill-advisor/mcp_server/schemas/README.md:18 — Schema README uses legacy pseudo-path | stale: `skill_advisor/schemas/` defines contract shapes. -> correct: The current schema sources are under `mcp_server/schemas/`.
- [P2] .opencode/skills/system-skill-advisor/mcp_server/scripts/README.md:18 — Scripts README uses legacy pseudo-path | stale: `skill_advisor/scripts/` holds command-line utilities. -> correct: The current scripts are under `mcp_server/scripts/`.
- [P2] .opencode/skills/system-skill-advisor/mcp_server/scripts/README.md:31 — Scripts tree omits current maintenance scripts | stale: The directory tree lists older scripts and omits current doc-frontmatter and zombie-soak scripts. -> correct: `check-skill-doc-frontmatter.mjs`, `check-skill-doc-frontmatter.sh` and `verify-zombie-soak.sh` are current scripts in this package.
- [P2] .opencode/skills/system-skill-advisor/mcp_server/tests/README.md:18 — Tests README uses legacy pseudo-path | stale: `skill_advisor/tests/` is the regression surface. -> correct: The current tests live under `mcp_server/tests/`.
- [P2] .opencode/skills/system-skill-advisor/mcp_server/tests/README.md:97 — Tests directory tree is materially incomplete | stale: The directory tree lists only a small older set of package-level test files. -> correct: Current package-level tests include newer daemon, launcher, CLI, parity, graph and harvest suites not represented in the tree, including the env-gated tri-daemon drill.

After editing, re-read each edited line to verify. Your final message MUST be exactly one fenced json block:
```json
{"seat":"AF14","fixed":[{"file":"path","claim":"what was stale","after":"what it now says"}],"refuted":[{"file":"path","why":"why skipped"}]}
```
