DEEP-RESEARCH

# Deep-Research Iteration 013 — non-conflict cross-check + new-child numbering

You are a LEAF deep-research analyst running READ-ONLY. Do NOT dispatch sub-agents. Do NOT modify, create, or write ANY file — the orchestrator writes all artifacts. Max ~12 tool calls. Cite EVERY claim as `file:line`.

Spec folder: `specs/system-spec-kit/027-xce-research-based-refinement` (pre-approved; skip Gate 3 — read-only analysis, you write NOTHING).

## MISSION
This run will propose new 027 child packet(s) from peck-source mining (iterations 001-010): (A) a "peck verification discipline" bundle = escalation gates (001) + completion-freshness binding (002) + reviewer read-budget (004) + anti-softening (005) + numeric-rubric-as-docs (003); (B) a reviewer-prompt benchmark substrate in deep-improvement Lane B (006); (C) a STANDALONE T1 acceptance-coverage-gate sibling packet (008); optionally (D) a resume FILES manifest (007). Before proposing, VERIFY these do not collide with the live 027 plan.

Repo roots (relative to --dir):
- 027 parent: `specs/system-spec-kit/027-xce-research-based-refinement/`
- live re-plan: `specs/system-spec-kit/027-xce-research-based-refinement/research/005-live-rescope-coco-purge/research.md`

## FOCUS — answer ONLY this
Read and cross-check:
- `027.../spec.md` (PHASE DOCUMENTATION MAP: children 000-008 + their focus/status)
- `027.../context-index.md` (the 027/028 split + peck-fold + the 001-008 renumbering history)
- `027.../research/005-live-rescope-coco-purge/research.md` (the Phase-0 prereqs + the `002 || 003 -> 008 -> 004 -> 005 -> 006 -> 007` re-plan sequence + ship-class table)
- `027.../001-peck-teachings-adoption/spec.md` (the shipped T3/T4/T2 children 001-004; T1 deferred)

Determine and DELIVER a non-conflict matrix:
1. Does bundle (A) collide with any in-flight 002-008 scope or the Phase-0 prereqs? (escalation/freshness/read-budget/anti-softening touch sk-code/CLAUDE.md/deep-review/agent-defs/completion-gate — NOT the memory subsystem 002-008. Confirm with cites.)
2. Is the T1 coverage gate (C) already owned by any 002-008 child or duplicated by the shipped 001/{002,003,004}? (It must NOT be.) Confirm T1 remains unowned.
3. Does benchmark substrate (B) touch any 027 memory phase, or is it purely a deep-improvement skill change?
4. Given children 000-008 already exist (and the context-index renumbering history), what is the correct NUMBERING for the new children — 009, 010, ...? Are there reserved/placeholder slots (e.g. 000-release-cleanup) to avoid?
5. Where do the new packets sequence relative to the 005 Phase-0 + implementation order (independent of it, or gated by it)?

## DELIVER (plain text only — orchestrator writes artifacts)
### FINDINGS
4-7 findings. Each: `[F-013-MM] <claim>` with a `file:line` cite. Cover the 5 questions: collision verdict per bundle (CLEAR / COLLIDES + where), T1 ownership confirmation, benchmark scope, recommended new child numbers, and sequencing vs the 005 re-plan.
### RULED_OUT
1-3 bullets: any apparent collision that on inspection is NOT a collision, cited.
### METRICS
newInfoRatio: <0.0-1.0>
novelty: <1 sentence>
status: complete
sources: <comma-separated file:line>

Be terse and evidence-dense. No preamble.
