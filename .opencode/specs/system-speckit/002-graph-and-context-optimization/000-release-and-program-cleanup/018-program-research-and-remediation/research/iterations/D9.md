I have gathered enough evidence. Let me now compile the research notes for the six assigned angles.

### Angle 27: Routing accuracy on a held-out corpus of real user phrasings
- Framing: The shipped 200-prompt "labeled-prompts.jsonl" is treated as an in-the-wild top-1 accuracy gate, but its construction (synthetic vs real) is the key test.
- Repo evidence: `mcp_server/scripts/routing-accuracy/labeled-prompts.jsonl` exists with 193 rows (not 200 as the README and `score-routing-corpus.py:6` docstring imply); the `source_type` field is enumerated as `synthetic-realistic`, `synthetic-command`, `synthetic-edge`, `paraphrased-realistic` — none is `real` or `actual-user`. `mcp_server/scripts/routing-accuracy/score-routing-corpus.py:141` (`score_advisor`) is the only scorer and it calls `analyze_prompt` with hard-coded `confidence_threshold=0.8, uncertainty_threshold=0.35`. Default semantic lane is disabled unless `--enable-semantic` is passed (`score-routing-corpus.py:243-245`). The regression corpus is the 50-row `fixtures/skill_advisor_regression_cases.jsonl`, which `skill_advisor_regression.py:80-89` evaluates in-process and subprocess.
- Findings: There is no held-out *real-user* corpus in this repo. Both corpora are author- or model-authored and curated. The `--enable-semantic` ablation switch exists but the current top-1 metric is a single conf/unc pair on this single 193-row mix. "Real" coverage depends entirely on whether the authour-labelled buckets (true_write, true_read_only, memory_save_resume, deep_loop_prompts, mixed_ambiguous, skill_routing_prompts) represent the actual prompt distribution the advisor sees in production. There is no per-bucket break-down published; `score-routing-corpus.py` only prints an aggregate `accuracy` plus `missed_skill_fires / wrong_skill_fires / false_fires_on_none`. The `validate.sh`-style `gates` of `min_top1_accuracy=0.92, max_command_bridge_fp_rate=0.05, min_p0_pass_rate=1.0` (`skill_advisor_regression.py:248-252`) gate the 50-row regression set, not the 193-row corpus.
- Next experiment: Tag a sample of in-the-wild prompts (e.g. from the orchestrator's actual user-input stream, last 30 days) and run `score-routing-corpus.py` against them; split the report by `bucket` and by the 6 `source_type` values so a true held-out top-1 can be read off a slice the corpus never trained on. Add a `--buckets` filter to `score-routing-corpus.py` and require per-bucket min-accuracy in `gates` alongside the aggregate.
- Verdict: LOW CONFIDENCE — angle 27 is *not yet measured in the wild*. The shipped scorer is a *synthetic* proxy; the only structural way to be more honest is to run the corpus against the real prompt stream and report bucket-level numbers. Recommendation: do not assert a "top-1" number in release notes from the synthetic corpus alone.

### Angle 28: Semantic routing lane vs lexical ablation
- Framing: How often does the semantic lane change the advisor's top-1 vs the lexical-only baseline, and is the delta worth the dependency?
- Repo evidence: `mcp_server/scripts/routing-accuracy/score-routing-corpus.py:243-245` sets `SKILL_ADVISOR_DISABLE_BUILTIN_SEMANTIC=true` when `--enable-semantic` is *not* passed, so the lexical-only run is the default. The semantic-on path is gated by an explicit opt-in flag. `mcp_server/scripts/skill_advisor_regression.py:108` always sets `SKILL_ADVISOR_DISABLE_BUILTIN_SEMANTIC=1` for the subprocess runner. `skill_advisor.py:1259` notes that "to reach 0.8 threshold with intent boost: need score >= 2.0", indicating the semantic lane contributes a non-zero weighted component. The semantic lane in the production MCP path is the `withSemanticShadowPromptEmbedding` shadow wrapper in `mcp_server/handlers/advisor-recommend.ts:268`, which runs in parallel (shadow) and reports `delta` but does **not** change the live top-1.
- Findings: There is a permanent ablation toggle (`--enable-semantic`/`SKILL_ADVISOR_DISABLE_BUILTIN_SEMANTIC`), but no published per-row top-1 delta. The live recommender uses a fused score (`scoreAdvisorPrompt` with `DEFAULT_SHADOW_SCORER_LANE_WEIGHTS` from `lane-registry.js`), and the shadow lane's contribution is recorded via `recordShadowDelta` (handler line 301) but never surfaces as a top-1 swap metric. A direct ablation answer is one rerun of the corpus away: `score-routing-corpus.py` already supports both modes; the missing piece is the diff report (top-1-swap count, MRR delta, Jaccard@3).
- Next experiment: Add `--compare-semantic` to `score-routing-corpus.py` that runs both passes on the same rows and emits `{row_id, lexical_top1, semantic_top1, swapped, semantic_present}`. Run it on the 193-row corpus; report the top-1 swap rate and the bucket-level breakdown. This produces a falsifiable "semantic lane changes top-1 X% of the time" number on the actual corpus.
- Verdict: LOW CONFIDENCE — the toggle exists, the diff does not. Recommendation: ship the diff report (one afternoon) before deciding whether the semantic lane is carrying its weight.

### Angle 29: Routing-confidence calibration of the 0.8 invoke threshold
- Framing: Is the 0.8 confidence threshold (with 0.35 uncertainty) well-calibrated, or does it over/under-trigger skill invocations?
- Repo evidence: The threshold is hard-coded in `mcp_server/scripts/skill_advisor.py:19` (`--threshold 0.8`), echoed in `skill_advisor.py:479-480` as the default, and re-asserted in `mcp_server/scripts/skill_advisor_regression.py:248-249` (CLI default) and `mcp_server/scripts/routing-accuracy/score-routing-corpus.py:154-156` (corpus default). `skill_advisor.py:1088-1102` shows the dual-threshold gate that bumps uncertainty +0.15 on conflict. `skill_advisor_regression.py:285-289` reports `top1_accuracy`, `command_bridge_fp_rate`, and `p0_pass_rate` as gates but no precision/recall at the 0.8 cut. The shadow lane (`withSemanticShadowPromptEmbedding` in `advisor-recommend.ts:268`) records `delta = shadowScore − liveScore` and `recordShadowDelta` writes it, but the file is not exposed for calibration analysis here.
- Findings: The threshold is enforced uniformly (0.80 confidence + 0.35 uncertainty, dual-gate). There is no calibration curve, no precision/recall-vs-threshold sweep, and no histogram of `confidence` on accepted vs rejected recs. The shadow-delta log is the right raw input: it is already recording per-row live score, shadow score, and delta. The cost of being wrong on calibration is also asymmetric — over-trigger floods the user with skill invocations; under-trigger means the user keeps doing the work unaided. The shipped default of 0.8 is justified in code comments as the "routing threshold" (line 19) and as a regression gate (`min_top1_accuracy=0.92`), but neither establishes it as the *optimal* cut on a calibration curve.
- Next experiment: Drain the `shadow-delta` records (likely `.opencode/skills/system-skill-advisor/mcp_server/scripts/shadow/sink`) into a CSV; bucket by `confidence` in 0.05-wide bins; for each bin compute acceptance rate and (where ground truth exists) precision; plot a calibration curve. Re-run `skill_advisor_regression.py --threshold 0.7 0.75 0.8 0.85 0.9` and compare pass rate vs `--max-command-bridge-fp-rate`; pick the threshold that maximises pass rate subject to a hard FP cap.
- Verdict: LOW CONFIDENCE — 0.8 is the *policy default*, not the *empirical optimum*. Recommendation: produce a one-time calibration report from the existing shadow-delta data; defer changing the default until the report is in hand.

### Angle 30: Skill-graph enhancement-edge propagation
- Framing: Does `enhances`-edge propagation measurably change which skills are surfaced in advisor recommendations the user did *not* explicitly ask for?
- Repo evidence: `mcp_server/handlers/skill-graph/propagate-enhances.ts:11` calls `propagateInboundEnhances`; the implementation lives in `mcp_server/lib/cross-skill-edges/detect-inbound-enhances.ts:1-9` (composite-scoring detection). The propagation handler takes `mode: 'report' | 'propose' | 'apply' | dry-run` (line 17-25), so the system can *detect* missing edges and either report or apply them. `handlers/skill-graph/status.ts:21` and `validate.ts:17` set the `enhances` weight band to `[0.3, 0.7]`, the only published confidence range. There is no A/B harness comparing advisor output with vs without propagated edges.
- Findings: The propagation pipeline is implemented and shippable (it has a "report" mode that does not write), but its effect on the *advisor's surfaced set* is not measured. The advisor's `recommend` call (`advisor-recommend.ts:268`) does not consult `skill_graph_enhanced_by` for the requested prompt; it relies on the explicit `enhances` edges baked into the skill's `graph-metadata.json`. The propagation tool therefore changes the *graph* (raw data) but not the *advisor's runtime behaviour* unless the change is *applied* and the skill graph is re-scanned. Even after application, there is no test corpus comparing rec lists.
- Next experiment: Pick 50 prompts from the 193-row corpus. Run `advisor_recommend` for each prompt: (a) before propagation, (b) after a `mode='apply'` propagation pass. Compute Jaccard@3 of the surfaced skill sets and the rank change for any skill that newly appears. The "unprompted discovery" claim becomes falsifiable: it is positive if the new edges cause skills to surface in the top-3 that did not before, in a non-trivial fraction of prompts.
- Verdict: LOW CONFIDENCE — the pipeline is built, the measurement is not. Recommendation: pair the propagation with an A/B run on a 50-prompt slice and only enable `apply` mode in CI if the discovery delta is positive.

### Angle 46: Verify-first scouted-bugfix batches — false-positive rate
- Framing: Across the 5 scouted-bugfix batches, what fraction of the scout's "loud headlines" were REFUTED, and how often was the loud framing wrong while a real lesser defect survived?
- Repo evidence: `002-graph-and-context-optimization/000-release-and-program-cleanup/010-scouted-bugfix-train/001.../implementation-summary.md:55-58` (5 targets: 2 REFUTED on the loud headline, 5 real defects confirmed); `002.../implementation-summary.md:55` (15 targets: 4 CONFIRMED, 9 partial-but-real, 2 REFUTED — 2/15 = 13% strict); `003.../implementation-summary.md:55-58` (20 targets: 7 CONFIRMED, 9 partial-but-real, 4 REFUTED — 4/20 = 20% strict, plus 1 EXCLUDED as a product-decision); `004.../implementation-summary.md:3` (9 confirmed, 11 excluded — none refuted); `005.../implementation-summary.md:3` (3 confirmed, 1 deduped to 130/014, 1 re-confirmed). The verify-first gate is the `gpt-5.5-fast` deep-dive parallel pass documented at `001.../implementation-summary.md:87`.
- Findings: Strict refuted rate across the 65 scouted headlines: 2/5 + 2/15 + 4/20 = 8/40 (40% of the explicitly-classified ones; batch 4 and 5 reported only confirmed/excluded, so 65 is the unweighted denominator). Headline-wrong-but-real-defect (refuted + partial-but-real) hits roughly 26/40 = 65% — a remarkably high "the framing was wrong, the area was real" rate, and the program's stated win. By contrast, `015-docs-drift-review/review/review-report.md:18` shows the *same model* over-reporting in the opposite direction: ~70 raw candidate findings, ~45 rejected by the orchestrator as false positives (64% over-report rate), driven by over-applying the advisor-scoped embedder removal to mk-spec-memory (review-report §5).
- Next experiment: Mine the implementation summaries for an explicit `headline_classification` table (refuted/partial/confirmed/excluded) per scouted target, then publish the per-batch confusion matrix. Add a CI metric on the next scout run: "fraction of CONFIRMED-first-pass" — the verify-first gate's value is to push this number above ~50%; a falling ratio would be a regression on the discipline.
- Verdict: The verify-first *discipline* is delivering on its stated purpose (refuting loud-but-wrong headlines is the headline of the program), with ~20% strict refuted + ~45% partial-but-real on the deeply-investigated batches, and only ~27% strictly confirmed. The 015 docs-drift run shows the same model has an opposing failure mode (over-reporting) that the orchestrator currently catches — that should be tracked as a separate metric, not folded into this one.

### Angle 47: Spec-folder derived-metadata staleness half-life
- Framing: How fast does `graph-metadata.json` / `description.json` drift out of sync with the source docs they were derived from, and is there a TTL-style mechanism to keep them fresh without manual backfill?
- Repo evidence: `mcp_server/lib/graph/graph-metadata-parser.ts:1251-1296` (`refreshGraphMetadataForSpecFolder`) re-derives metadata, then `graphMetadataEqualIgnoringVolatile` skips the write if nothing content-relevant changed — but it uses a content-equality check, not a time threshold. `mcp_server/lib/graph/graph-metadata-schema.ts:40-59` defines `derived.last_save_at` and `derived.last_active_at` but no `stale_after` or `ttl` field. `scripts/graph/backfill-graph-metadata.ts:193-237` (`runBackfill`) refreshes every spec folder unconditionally — there is no `older-than` filter, no `mtime` check, and no `--since` argument. `scripts/validation/continuity-freshness.ts:13` defines `CONTINUITY_STALENESS_THRESHOLD_MS = 10 * 60 * 1000` as a *one-sided* heuristic that only warns when `graph.last_save_at` is **newer** than `implementation-summary.continuity.last_updated_at` (lines 233-265); the converse delta is treated as "benign clock drift" and a pass. `scripts/core/workflow.ts:1705` calls `refreshGraphMetadata` synchronously on every canonical save with `saveLineage: 'same_pass'`, so save-time freshness is the only path that auto-refreshes. `scripts/spec/validate.sh:831` runs `run_continuity_freshness_check` per folder.
- Findings: There is no generic "TTL refresh" mechanism for `graph-metadata.json` or `description.json`; the only freshness budgets are: (a) the 10-minute continuity-vs-graph-metadata one-sided policy (warns when graph is newer than the implementation-summary frontmatter, ignores the converse as clock drift), and (b) save-time `refreshGraphMetadata` triggered by `workflow.ts:1705`. Any spec folder that is *not* re-saved through the canonical workflow drifts indefinitely — `backfill-graph-metadata.ts` is a manual operator action, not a periodic job. Git history (`git log --since='2026-06-01' -- .opencode/specs/**/graph-metadata.json`) shows 63 distinct commits touching these files in the last 4 days, 193 in 10 days, 617 in 30 days, and 1095 in 50 days — but this is *manual* "flush" commits (`chore(specs): refresh graph metadata + minor 026 content edits` patterns), not an automated TTL. The 015 docs-drift-review report (`review/review-report.md:9`) — 21 real drift items found in a single session of 10 partitioned passes — is itself the empirical evidence that user-facing derived surfaces drift on the same cadence.
- Next experiment: Add a `stale_after_days` field to `graphMetadataDerivedSchema` (default 7) and have `backfill-graph-metadata.ts --auto` skip folders whose `derived.last_save_at` is newer than `now - stale_after_days` AND whose underlying `spec.md`/`plan.md`/`tasks.md` mtime is older than `last_save_at`. Add a `validate.sh` rule `GRAPH_METADATA_STALE` that warns on `now - last_save_at > stale_after_days AND last_save_at < mtime(spec.md)`, symmetric to `CONTINUITY_FRESHNESS`. The current 10-minute budget only fires one way; the missing check is the converse.
- Verdict: There is no working TTL today. The only automatic refresh is the save-time `refreshGraphMetadata` in `workflow.ts:1705`; everything else is operator-driven backfills. The 10-minute `CONTINUITY_FRESHNESS` budget is the closest thing to a TTL, but it is one-sided and silent when the graph-metadata is *older* than the source — exactly the drift mode that the angle is asking about. Recommendation: add a symmetric `stale_after_days` + mtime check to `backfill-graph-metadata.ts` and a `GRAPH_METADATA_STALE` rule to `validate.sh`; track the half-life empirically from `last_save_at` deltas across the recent 1095 commits.
