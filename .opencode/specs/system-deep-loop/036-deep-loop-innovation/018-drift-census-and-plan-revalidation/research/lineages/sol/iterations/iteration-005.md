# Iteration 005: Current-Runtime Capability Census for Phases 006-012

## Focus

Inspect the current `system-deep-loop` runtime for already-shipped event/ledger, receipts/effects, compatibility/shadow/rollback, durable fan-out/fan-in, novelty/claims/projections, convergence/health, and shared-contract capabilities, then distinguish reusable substrate from each phase's full acceptance contract.

## Findings

The comparison head is `e4b242c3940c26b47950534e1a149c7e037e71fd`. Introducing commits below may predate baseline `0ce43ff589`; they are cited because the iteration contract asks for the commit that introduced each overlapping capability, not only post-baseline drift. No phase is invalidated: every shipped overlap is narrower than the phase's full core purpose.

| Phase | Current capability overlap | Overlap classification | Introducing commit | Current path:line evidence | Verdict impact |
|---|---|---|---|---|---|
| 006 | A versioned observability envelope appends immutable JSONL rows, and fan-out has a status ledger, but neither is a typed authoritative event ledger with replay fingerprints or fail-closed transition authorization. | **Supporting/partial** | `f8be972a2fad` introduced the observability envelope. | The envelope has schema version, event id, producer, stream, subject, event, status, timestamp, and native payload at `.opencode/skills/system-deep-loop/runtime/lib/deep-loop/observability-events.cjs:19-20,89-110`; append is direct JSONL at lines 126-140. The phase requires envelope + typed append-only ledger + replay fingerprint + authorization gateway at `.opencode/specs/system-deep-loop/036-deep-loop-innovation/006-transition-authorized-ledger-core/spec.md:64-67`. | **needs refinement, unchanged**. Reuse the observability envelope and producer adapters, but retain all four planned children; current recording is explicitly observability, not authorized state transition authority. [SOURCE: .opencode/skills/system-deep-loop/runtime/lib/deep-loop/observability-events.cjs:89-140] |
| 007 | Signed dispatch receipts, hard budget caps, progress gauges, durable loop locks, and continuity threading already cover substantial pieces of five of seven services. The evidence contract remains optional/advisory, and there is no shared effect-recovery gateway, sealed-artifact service, or blinded adjudication service. | **Supporting/partial; scope-changing** | `a1de03dd59c1` introduced deterministic resumable receipt keys; `841c15dbf45f` introduced atomic loop-lock persistence. | Receipt keys reproduce from run secret + dispatch id at `.opencode/skills/system-deep-loop/runtime/lib/deep-loop/receipt-crypto.ts:22-33`; fan-out enforces per-lineage and aggregate caps at `runtime/scripts/fanout-run.cjs:666-768` and emits gauges at lines 1213-1257; locks use atomic write/fsync/rename at `runtime/lib/deep-loop/loop-lock.ts:215-261`. The seven-service acceptance surface is `.opencode/specs/system-deep-loop/036-deep-loop-innovation/007-shared-evidence-and-control-services/spec.md:62-68`. | **needs refinement, changed from provisionally still valid**. Recast receipts, budgets, gauges, locks, and continuity as hardening/hoisting existing implementations; preserve missing effect recovery, sealing, blinded adjudication, and ledger-fold semantics. [SOURCE: .opencode/skills/system-deep-loop/runtime/lib/deep-loop/receipt-crypto.ts:22-33] |
| 008 | Runtime compatibility is limited to local schema aliases and lifecycle normalization. No shared event upcaster, dual-read/single-write adapter, legacy projection, shadow-parity harness, in-flight disposition engine, or cross-mode rollback drill was found. | **Absent at core bridge level** | `c844ea42a00b` introduced the shared lifecycle taxonomy, the nearest compatibility substrate. | Shared active states and stop reasons exist at `.opencode/skills/system-deep-loop/runtime/lib/deep-loop/lifecycle-taxonomy.cjs:24-49`, while the phase requires five executable bridge contracts at `.opencode/specs/system-deep-loop/036-deep-loop-innovation/008-compatibility-shadow-and-rollback-bridge/spec.md:64-68`. Exact runtime searches found compatibility aliases and mode-local rollback surfaces, not the required dark-vs-legacy bridge. | **still valid, confidence raised**. Existing alias normalization is reusable implementation technique but does not change the phase's starting assumptions or acceptance boundary. [SOURCE: .opencode/specs/system-deep-loop/036-deep-loop-innovation/008-compatibility-shadow-and-rollback-bridge/spec.md:64-68] |
| 009 | The runtime already has a capped concurrent pool, append-only orchestration status, retries/salvage, wait checkpoints, partial-result summaries, budgets, and deterministic cross-lineage merge. It explicitly rejects waves and dependency/touch metadata; results are not canonical ledger envelopes and no branch leases exist. | **Supporting/partial, already acknowledged** | `a78e877ca95e` introduced the flat-pool guard and explicit wave/conflict-safety rejection; `f038ff140e5d` introduced conflict-preserving merge records. | Wave and write-set metadata are rejected at `.opencode/skills/system-deep-loop/runtime/scripts/fanout-run.cjs:331-411`; resumable wait checkpoints are at lines 810-997; merge preserves conflicting same-id records at `runtime/scripts/fanout-merge.cjs:294-317`. The phase explicitly generalizes this shipped pool into six durable contracts at `.opencode/specs/system-deep-loop/036-deep-loop-innovation/009-fanout-fanin-durable-orchestration/spec.md:52-67`. | **still valid, unchanged**. The phase already scopes the shipped pool as substrate; canonical ledger dispatch/results, branch leases/waves, conditional fan-in, explicit partial-failure policy, and provenance-weighted reduction remain unshipped. [SOURCE: .opencode/skills/system-deep-loop/runtime/scripts/fanout-run.cjs:331-411] |
| 010 | The coverage graph computes claim verification, contradiction density, graph-observed novelty, and observation counts; fan-out merge emits typed `CONTRADICTS` markers and stable conflict ids. These are real claim/novelty primitives, but not semantic communities, ledger contradiction/supersession events, stable claim lifecycles, typed next-focus, or transactional projections. | **Supporting/partial; scope-changing** | `9a413237f723` introduced research claim/coverage signals; `f038ff140e5d` introduced conflict markers in fan-in. | Claim verification and contradiction density are computed at `.opencode/skills/system-deep-loop/runtime/lib/coverage-graph/coverage-graph-signals.ts:580-629`; graph novelty is at lines 715-783; fan-in contradiction records are at `runtime/scripts/fanout-merge.cjs:294-317`. The full five-child contract remains `.opencode/specs/system-deep-loop/036-deep-loop-innovation/010-novelty-claims-continuity-and-projections/spec.md:64-68`. | **needs refinement, changed from provisionally still valid**. Treat graph metrics and conflict ids as existing adapters/inputs, then narrow new work to semantic communities, typed ledger relations, durable claim identity, focus semantics, and atomic derived views. [SOURCE: .opencode/skills/system-deep-loop/runtime/lib/coverage-graph/coverage-graph-signals.ts:580-629] |
| 011 | Generic research/review/context convergence already computes path-oriented coverage, claim verification, contradiction, source diversity, evidence depth, blockers, snapshots, momentum, graph novelty, and minimum-observation guards. This directly contradicts the premise that the shipped anchor is only council-local, although cycle detection, independent stopping clocks, value-of-computation allocation, and generic degeneration detection remain absent. | **Supporting/partial; premise-changing** | `107c522599d6` introduced generic composite convergence; `9a413237f723` introduced graph-backed research coverage signals. | Cross-loop composite scoring is at `.opencode/skills/system-deep-loop/runtime/scripts/convergence.cjs:303-337`; graph-backed research evaluation and persisted snapshots are at lines 723-825; question coverage is at `runtime/lib/coverage-graph/coverage-graph-signals.ts:550-578`. The phase premise and five children are at `.opencode/specs/system-deep-loop/036-deep-loop-innovation/011-convergence-termination-and-health/spec.md:52-66`. | **needs refinement, changed from provisionally still valid**. Replace the council-only premise and preserve existing generic convergence/coverage as substrate; retain cycle detection, clocks, adaptive allocation, and degeneration health as core work. [SOURCE: .opencode/skills/system-deep-loop/runtime/scripts/convergence.cjs:723-825] |
| 012 | A declarative shared mode registry already freezes `workflowMode`, `runtimeLoopType`, `backendKind`, tool surfaces, packets, commands, agents, and artifact roots; contract compilation/drift checks and shared behavior fixtures also exist. It does not define the planned substrate interface, mixed-version ledger fixtures, or executable phase-013 write-set conflict graph. | **Supporting/partial; scope-changing** | `ce9e313e7f46` introduced the mode registry; post-baseline `708d25acf04a` added its resource-contract version. | The shared discriminators are frozen at `.opencode/skills/system-deep-loop/mode-registry.json:1-25`, with per-mode interfaces beginning at lines 30-54. The required shared substrate interface, closures, mixed-version fixtures, and write-set graph remain at `.opencode/specs/system-deep-loop/036-deep-loop-innovation/012-shared-mode-contracts-and-fixtures/spec.md:63-66`. | **needs refinement, changed from provisionally still valid**. Extend rather than duplicate the mode registry/contract compiler and explicitly limit new fixtures and conflict graph to the phase-006-through-011 substrate and phase-013 migration write sets. [SOURCE: .opencode/skills/system-deep-loop/mode-registry.json:1-54] |

## Verdict Impact

- No phase is `invalidated`; no inspected capability satisfies a phase's full core purpose.
- Phase 006 remains `needs refinement`, now with a concrete reuse boundary around observability events and status producers.
- Phases 007, 010, 011, and 012 change from provisional `still valid` to `needs refinement` because shipped overlap changes their starting assumptions or implementation scope.
- Phases 008 and 009 remain `still valid`; phase 008 lacks its bridge-level capabilities, while phase 009 explicitly acknowledges and generalizes the shipped flat pool.
- The complete matrix refinement set is now provisionally 003, 006, 007, 010, 011, 012, and 013; all others remain still valid pending the authority/retirement and whole-system-gate checks.

## Sources Consulted

- Phase specifications 006-012, especially each phase map and handoff contract.
- Current runtime event, receipt, lock, fan-out, fan-in, coverage-graph, convergence, lifecycle, and mode-registry implementations.
- `git blame` history for each cited overlap, including `f8be972a2fad`, `a1de03dd59c1`, `841c15dbf45f`, `c844ea42a00b`, `a78e877ca95e`, `f038ff140e5d`, `9a413237f723`, `107c522599d6`, `ce9e313e7f46`, and post-baseline `708d25acf04a`.
- Prior matrix at `iterations/iteration-004.md`.

## Assessment

`newInfoRatio: 0.34`. The iteration converts seven low/medium-confidence provisional rows into acceptance-contract comparisons and changes four verdicts based on concrete shipped substrate. Novelty continues downward (`0.90 -> 0.78 -> 0.64 -> 0.46 -> 0.34`) because most capabilities predate the baseline, but the new information is decision-relevant: current phase scopes must reuse rather than recreate substantial runtime primitives. Convergence remains `CONTINUE`; phases 014-016 still have authority, retirement, and executable-gate gaps.

## Reflection

- Worked: comparing each phase's complete child map against current symbols prevented filenames such as `receipt-crypto` or `convergence` from being mistaken for full phase completion.
- Worked: `git blame` separated capability introduction from post-baseline drift and exposed the one directly relevant post-baseline shared-contract change (`708d25acf04a`).
- Ruled out: invalidating phase 011 merely because generic convergence exists; four of its five complementary contracts remain materially incomplete.
- Ruled out: classifying mode-local aliases or rollback utilities as the cross-mode compatibility/shadow/rollback bridge required by phase 008.
- Ruled out: treating the current flat-pool status ledger as canonical phase-006/009 authority; waves and conflict metadata are explicitly rejected and the state ledger is not transition-authorized.

## Recommended Next Focus

Inspect phase-014 authority selectors and phase-015 legacy-writer/archival-reader surfaces, then determine whether either phase's cutover or retirement candidate set has already partially shipped.
