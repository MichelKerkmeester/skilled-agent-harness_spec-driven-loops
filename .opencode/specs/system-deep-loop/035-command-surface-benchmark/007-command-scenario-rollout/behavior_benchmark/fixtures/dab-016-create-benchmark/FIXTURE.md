# Create-benchmark workflow fixture

Frozen input for authoring one minimal `conformance_benchmark` package beneath the nested demo mode. The source packet and target mode both live inside this fixture.

All generated package files must remain inside the fixture root.
