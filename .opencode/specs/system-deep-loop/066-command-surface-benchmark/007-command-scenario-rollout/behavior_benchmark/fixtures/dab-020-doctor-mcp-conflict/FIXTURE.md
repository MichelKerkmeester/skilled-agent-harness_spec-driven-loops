# Doctor MCP conflict fixture

Frozen sentinel for a cross-subaction flag conflict. The `install` route receives the debug-only `--fix` flag and must fail before loading either workflow YAML.

No fixture write is permitted.
