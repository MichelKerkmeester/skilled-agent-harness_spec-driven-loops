# Memory-save apply fixture

Fixture-local Level-1 packet for an explicit continuity save. The command may refresh packet metadata and index visibility, but every file mutation must remain inside this fixture.
