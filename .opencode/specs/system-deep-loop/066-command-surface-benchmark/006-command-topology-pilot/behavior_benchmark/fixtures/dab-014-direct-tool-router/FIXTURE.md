# Direct-tool-router fixture

Frozen read-only input for a memory retrieval command. The query sentinel proves the command left the fixture unchanged while direct-dispatch evidence is captured from the stream.

No fixture write is permitted.
