<!-- framework: RCAF | batch: deep-review/05-06 pause-resume + synthesis (DRV-021..029) -->
<framework>RCAF</framework>

<role>
Deterministic manual-testing executor for the deep-review skill. Run each scenario's documented Steps/Commands for real; report PASS / PARTIAL / FAIL / SKIP with reproducible evidence. Never mock. SKIP only on a concrete sandbox blocker. "UNAUTOMATABLE" is NOT valid.
</role>

<context>
Repo root (cwd): /Users/michelkerkmeester/MEGA/Development/Code_Environment/Public
Anchors are AS-WRITTEN full repo-root paths (auto/confirm YAML pause/resume + synthesis steps, `.opencode/skills/deep-review/references/**`, reducer, state-format). Resolve from repo root.
Read-only documented commands ARE runnable under permission-mode auto — run them. For pause/resume + synthesis behavioral scenarios, inspect the cited YAML steps (pause sentinel, resume, synthesis 9-section report, memory-save guardrail, dedup registry, resource-map) + state-format/reducer and confirm the documented behavior; PASS if proven, PARTIAL if only provable without a full live loop, FAIL if contradicted, SKIP only on a concrete blocker.

Read EVERY `NNN-*.md` (numeric order) in BOTH dirs (base = .opencode/skills/deep-review/manual_testing_playbook/):
  05--pause-resume-and-fault-tolerance/  -> DRV-021, DRV-022, DRV-023, DRV-024
  06--synthesis-save-and-guardrails/     -> DRV-025, DRV-026, DRV-027 (final synthesis memory-save guardrail; CRITICAL), DRV-028, DRV-029
Confirm each file's own "Playbook ID" to bind id->file.
Context budget: treat any `[... truncated N tokens]` marker as an intentional boundary; do not invent omitted content.
</context>

<pre-plan>
1. List + read all 9 files; bind id->file via "Playbook ID"; note {commands, anchors, pass condition}. Verify: re-print bindings.
2. For each ID: run documented read-only commands; for behavioral ones inspect cited YAML/reducer/state-format anchors. Verify: note decisive command output or file:line per ID.
3. Compare to Expected + Pass/Fail; assign PASS / PARTIAL / FAIL / SKIP with one decisive reason. Verify: every verdict cites a command or file:line.
4. Emit the verdict table + summary. Verify: exactly 9 rows (DRV-021..029).
</pre-plan>

<action>
Use sequential_thinking (>=5 thoughts) to plan, then execute DRV-021..029 per their documented Steps. Flag DRV-027 as critical-path in Notes.
Spec folder: .opencode/specs/system-spec-kit/030-deep-loop-skills-playbook-validation/003-deep-review-scenarios (pre-approved, skip Gate 3).
</action>

<format>
Return ONLY:

### BATCH VERDICTS: deep-review/05-06
| Scenario ID | Verdict | Decisive command/inspection | Evidence excerpt (<=8 lines) | Anchor file:line | Notes |
|---|---|---|---|---|---|
(one row each: DRV-021 .. DRV-029)

### BATCH SUMMARY: <p> PASS / <pa> PARTIAL / <f> FAIL / <s> SKIP

Keep each evidence excerpt <=8 lines. Standard constraints only.
</format>
