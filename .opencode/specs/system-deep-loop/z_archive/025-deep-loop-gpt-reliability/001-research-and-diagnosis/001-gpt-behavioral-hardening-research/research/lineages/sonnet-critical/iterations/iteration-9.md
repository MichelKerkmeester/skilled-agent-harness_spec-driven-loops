The `sonnet-critical` fan-out lineage is complete: 10/10 iterations, `stopPolicy: max-iterations`, all outputs written under `research/lineages/sonnet-critical/` only.

**What the critical re-review found**, correcting for GPT self-assessment bias in the prior round:
- Phase 005's "inconclusive" label undersells a 4/4 FAIL result — the real gap is an unexercised native/Claude baseline, not ambiguous evidence.
- **Mode D** (GPT reading advisory prose as a hard gate) upgraded from glm-max's inferred hypothesis to a confirmed, already-fired instance: the `GENERAL AGENT REQUIRED` self-check present in all 7 `/deep:*` command files is the exact, cited cause of one of phase 005's real failures.
- The "ai-council naming drift" both prior lineages described is actually a code-traced, deterministic emitter/validator self-contradiction inside one YAML file — it will fail for any model and must be fixed before any GPT-vs-Claude benchmark's ai-council leg means anything.
- KQ4's top-ranked, "full agreement" recommendation ("orchestrate dispatches `@deep` and STOP") **violates orchestrate.md's own documented nesting-depth cap** in its literal reading; a corrected, NDP-safe fix is supplied.
- KQ9's "wait on FIX-5" verdict **survives** the bias check — it's independently reached by the non-GPT `glm-max` lineage and reinforced by this round's own finding that FIX-5 wouldn't even fix 2 of the 4 confirmed symptoms.
- A self-adversarial pass (iteration 9) applied the same scrutiny to this round's own findings and downgraded four of them from "certain" to appropriately hedged confidence levels.

Four implementation-ready diffs and a full confirmed/sharpened/overturned table per original KQ are in `research.md`. Next step per the charter is the cross-lineage merge with `glm-critical` once it completes.

FANOUT_LINEAGE_COMPLETE:sonnet-critical
