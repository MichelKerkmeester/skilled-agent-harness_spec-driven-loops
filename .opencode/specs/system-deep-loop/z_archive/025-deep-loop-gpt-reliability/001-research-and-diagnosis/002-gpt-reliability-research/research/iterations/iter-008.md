Reading the three specified workflow assets and extracting line-cited workload/checkpoint evidence.
The specified files show both per-iteration artifacts and full-session loop obligations; I’m checking the named benchmark cells/watchdog wording only enough to align proposal effects.
### FINDINGS

1. One deep-review LEAF iteration carries substantial mandatory output: target 9 tool calls with hard max 13, write all findings to files, and produce three artifacts: narrative markdown, canonical state-log JSONL, and per-iteration delta JSONL. Standard/complex scopes add v2 target-selection, search-coverage, and search-ledger fields. `.opencode/skills/deep-loop-workflows/deep-review/assets/prompt_pack_iteration.md.tmpl:57-59`, `.opencode/skills/deep-loop-workflows/deep-review/assets/prompt_pack_iteration.md.tmpl:75-117`

2. The review prompt mandates per-iteration persistence, but not true incremental “write as you go” checkpoints. The counted state record is a complete `"type":"iteration"` record, and missing/malformed artifacts fail validation; a budget-killed run before those final writes is not partially complete. `.opencode/skills/deep-loop-workflows/deep-review/assets/prompt_pack_iteration.md.tmpl:79-83`, `.opencode/skills/deep-loop-workflows/deep-review/assets/prompt_pack_iteration.md.tmpl:105-117`

3. The watchdog would be reset by either visible output or artifact `mtime`, but the review template only guarantees end-of-iteration artifacts, so a GPT executor that reasons silently can still trip no-progress even while doing useful work. `.opencode/skills/deep-loop-workflows/shared/behavior-benchmark/framework.md:198-205`

4. Lane A’s atomic proposal/evaluation unit is heavy: read charter/boundary/profile/target, scan integration, write exactly one candidate, score it, benchmark it, append results, and reduce state. Init adds runtime setup and baseline recording; promotion remains separately gated. `.opencode/skills/deep-loop-workflows/deep-improvement/SKILL.md:247-263`, `.opencode/skills/deep-loop-workflows/deep-improvement/SKILL.md:289-294`

5. One `:auto` invocation expands that into a full session: init, up to 5 loop iterations by default, and synthesis. Each loop iteration includes scan, candidate generation, journaling, lineage, scoring, mutation coverage, benchmark materialization/run, repeatability, trade-off detection, ledger append, reduce, stop check, legal-stop event, and optional blocked-stop event. `.opencode/commands/deep/assets/deep_agent-improvement_auto.yaml:41-42`, `.opencode/commands/deep/assets/deep_agent-improvement_auto.yaml:117-218`, `.opencode/commands/deep/assets/deep_agent-improvement_auto.yaml:219-234`

6. Improvement has many checkpoint-like writes, but current release forbids real resume: every invocation is `new`, existing runtime must be archived/replaced, and resume/restart/fork are explicitly not wired. So partial files help diagnosis and watchdog liveness, not continuation. `.opencode/skills/deep-loop-workflows/deep-improvement/SKILL.md:382-388`, `.opencode/commands/deep/assets/deep_agent-improvement_auto.yaml:120-126`

### PROPOSALS

1. **Tag**: optimize. **Change**: Add explicit heartbeat/checkpoint records to review, council, and improvement prompts: create artifact skeletons early, append `progress` JSONL after setup/target-selection/candidate-generated/seat-started/seat-completed, and reserve `"type":"iteration"` or `session_end` for final completion only. **Expected effect**: IMB-001-high gets resumable evidence before budget death; CXB-004 unaffected except faster setup-halt visibility; ACB-004/ACB-005 avoid 480s silence by seat artifact `mtime` updates. **Effort**: M. **Risk**: More schema surface and possible reducer noise unless partial records are explicitly ignored by completion reducers.

2. **Tag**: re-approach. **Change**: Split heavy autonomous runs into resumable sub-invocations: `setup`, `work --one-iteration`, and `synthesize`, all sharing session id, lock, ledger, and lineage. Remove or narrow the “fresh new session only” gate for this phase mode. **Expected effect**: IMB-001-high no longer has to fit init + candidate + scoring + benchmark + synthesis in one budget window; CXB-004 still halts at setup; ACB-004/ACB-005 can similarly split seat generation from convergence synthesis. **Effort**: L. **Risk**: Changes session semantics and requires careful lock/reducer/replay compatibility.

3. **Tag**: simplify. **Change**: Trim repeated per-iteration work: cache integration scan/profile unless target or manifest changes, materialize benchmark fixtures once per session, and defer repeatability/trade-off analysis to synthesis or only after a candidate clears a score threshold. **Expected effect**: IMB-001-high has a better chance of natural completion under existing caps; CXB-004 unaffected; ACB-004/ACB-005 may gain some time but still need liveness checkpoints for watchdog stalls. **Effort**: M. **Risk**: Stale cache bugs or later discovery of trade-offs if invalidation is too loose.

4. **Tag**: optimize. **Change**: Add GPT-specific pacing contracts to prompt packs: first artifact/output within 60s, candidate/seat artifact by a fixed budget fraction, score/round artifact by the next fraction, and finalizer triggered before hard cap. **Expected effect**: IMB-001-high preserves time for natural synthesis instead of spending the whole cap on candidate/scoring; CXB-004 should ask the setup question immediately; ACB-004/ACB-005 get visible council progress before the watchdog window. **Effort**: S. **Risk**: Too-aggressive pacing can reduce analysis quality unless thresholds are model/profile-specific.

5. **Tag**: optimize. **Change**: Increase budgets only for `timeout_latency` cells with visible progress or artifact writes; do not use budget increases for `stuck_no_progress`. For IMB-001-high, a modest cap bump is justified if checkpoint/simplification still leaves GPT near completion; for ACB watchdog stalls, fix artifact progress; for CXB-004, fix setup routing/halting, not time. **Expected effect**: IMB-001-high may complete naturally with minimal workflow change; CXB-004 liveness unchanged by budget; ACB-004/ACB-005 require checkpoints because a bigger cap does not reset no-progress. **Effort**: S. **Risk**: Higher cost and masking template inefficiency if used before simplifying/checkpointing.
