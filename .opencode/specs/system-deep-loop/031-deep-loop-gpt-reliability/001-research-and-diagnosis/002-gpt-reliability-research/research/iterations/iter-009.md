### FINDINGS

1. `/deep:review:auto` resolves through this chain, with 14 instruction-bearing files before the first real review iteration dispatch on the default native path.

```text
.opencode/commands/deep/review.md -> thin router; forbids dispatch here -> defers presentation + selected YAML asset. Cites: .opencode/commands/deep/review.md:9,13-17,78-84,90-98
.opencode/commands/deep/assets/deep_review_presentation.txt -> owns prompts/displays and :auto setup resolution -> defers shared auto contract and auto YAML. Cites: .opencode/commands/deep/assets/deep_review_presentation.txt:3,13-24,338-343
.opencode/skills/system-spec-kit/references/workflows/auto_mode_contract.md -> shared resolve/targeted-ask/fail-fast semantics -> defers per-command default table and YAML load. Cites: .opencode/skills/system-spec-kit/references/workflows/auto_mode_contract.md:17,31-39,42-56
.opencode/commands/deep/assets/deep_review_auto.yaml -> executable workflow -> defers mode skill, references, templates, agent file, runtime scripts. Cites: .opencode/commands/deep/assets/deep_review_auto.yaml:58-80,119-130,765-929
.opencode/skills/deep-loop-workflows/mode-registry.json -> maps workflowMode=review to packet=deep-review, runtimeLoopType=review, agent=deep-review -> defers packet/runtime interpretation. Cites: .opencode/skills/deep-loop-workflows/mode-registry.json:50-63
.opencode/skills/deep-loop-workflows/SKILL.md -> hub says registry is source of truth, but /deep commands remain static routers -> defers mode packet. Cites: .opencode/skills/deep-loop-workflows/SKILL.md:36-57,82-87
.opencode/skills/deep-loop-workflows/deep-review/SKILL.md -> per-mode rules, forbidden invocation patterns, resource loading -> defers quick/protocol/state/convergence refs, templates, scripts. Cites: .opencode/skills/deep-loop-workflows/deep-review/SKILL.md:43-63,83-90,378-380,426-432
.opencode/agents/deep-review.md -> LEAF-only single-iteration contract -> defers actual review work to one bounded iteration and its state files. Cites: .opencode/agents/deep-review.md:34-39,124-139,193-200
```

2. Count split: 14 files must be read as instruction/source contracts before the first native review iteration can be safely dispatched; the auto YAML additionally names 21 unique static external file/library/script references across the full workflow. The pre-dispatch 14 are command doc, presentation, auto-mode contract, auto YAML, registry, hub SKILL, mode SKILL, three YAML-listed references, two templates, agent file, and prompt-pack template; script/library refs such as artifact resolution, lock, upsert, convergence, executor-config, prompt-pack renderer, audit, validator, reducer, fanout, and save are invoked or conditionally used from YAML. Cites: `.opencode/commands/deep/assets/deep_review_auto.yaml:60-67`, `.opencode/commands/deep/assets/deep_review_auto.yaml:74`, `.opencode/commands/deep/assets/deep_review_auto.yaml:130`, `.opencode/commands/deep/assets/deep_review_auto.yaml:176`, `.opencode/commands/deep/assets/deep_review_auto.yaml:253-267`, `.opencode/commands/deep/assets/deep_review_auto.yaml:451`, `.opencode/commands/deep/assets/deep_review_auto.yaml:542`, `.opencode/commands/deep/assets/deep_review_auto.yaml:769-805`, `.opencode/commands/deep/assets/deep_review_auto.yaml:929`, `.opencode/commands/deep/assets/deep_review_auto.yaml:1021`, `.opencode/commands/deep/assets/deep_review_auto.yaml:1138`, `.opencode/commands/deep/assets/deep_review_auto.yaml:1495`.

3. The highest gate-inversion risk is split ownership of setup: command Markdown says Markdown owns setup and YAML must not load until required values bind, presentation owns `:auto` resolution, and the shared auto contract defines the three-tier semantics. A literal executor has to merge three authorities before touching YAML. Cites: `.opencode/commands/deep/review.md:68-75`, `.opencode/commands/deep/assets/deep_review_presentation.txt:13-24`, `.opencode/skills/system-spec-kit/references/workflows/auto_mode_contract.md:31-56`.

4. The partial-rendering risk is concentrated in placeholder-heavy YAML plus external templates: the YAML explicitly fails on unresolved placeholders, then later renders config, strategy, and prompt-pack templates through separate files and libraries. Cites: `.opencode/commands/deep/assets/deep_review_auto.yaml:193-200`, `.opencode/commands/deep/assets/deep_review_auto.yaml:380`, `.opencode/commands/deep/assets/deep_review_auto.yaml:429`, `.opencode/commands/deep/assets/deep_review_auto.yaml:783-786`.

5. The absorbed-dispatch risk comes from repeated negative/positive dispatch boundaries: command Markdown says do not dispatch from Markdown, YAML owns dispatch, the skill says never hand-roll or Task-dispatch the LEAF agent, and the agent says it is one iteration only. These are correct but scattered. Cites: `.opencode/commands/deep/review.md:13-17`, `.opencode/commands/deep/review.md:70-74`, `.opencode/commands/deep/assets/deep_review_auto.yaml:765-817`, `.opencode/skills/deep-loop-workflows/deep-review/SKILL.md:43-63`, `.opencode/agents/deep-review.md:34-39`.

### PROPOSALS

1. **Tag**: simplify. **Change**: Generate a compiled `/deep:review:auto` execution contract that inlines command routing, presentation setup, the relevant auto-mode excerpt, resolved default table, YAML start condition, skill summary, agent boundary, and a typed external-file manifest. **Expected effect**: Reduces gate inversion RVB-008, partial rendering RVB-002, and absorbed steps RVB-007 by giving executors one authoritative artifact. **Effort**: M. **Risk**: Source/compiled drift; require checksum headers and CI diff guard.

2. **Tag**: optimize. **Change**: Add generated labels to every YAML external reference: `read_contract`, `render_template`, `invoke_script`, `dynamic_target`, `conditional_fanout`, `post_loop_save`. **Expected effect**: Reduces absorbed steps RVB-007 and partial rendering RVB-002 by telling literal executors what to open versus what to invoke. **Effort**: S. **Risk**: Source/compiled drift if labels are handwritten; derive labels during build.

3. **Tag**: simplify. **Change**: Inline the shared `:auto` contract’s Tier 1/Tier 2/Tier 3 rules into the compiled command artifact while keeping `auto_mode_contract.md` as the maintainer source. **Expected effect**: Reduces gate inversion RVB-008 by eliminating a setup-semantics hop at invocation time. **Effort**: S. **Risk**: Source/compiled drift; block release when compiled excerpt differs from source hash.

4. **Tag**: re-approach. **Change**: Move setup resolution into a deterministic loader that parses args, binds defaults, validates placeholders, and emits one hydrated execution packet before the model sees the workflow. **Expected effect**: Reduces all three classes, especially partial rendering RVB-002, because unresolved placeholders fail before model execution. **Effort**: L. **Risk**: Source/compiled drift plus loader bugs; mitigate with trace fixtures for Tier 1, Tier 2, Tier 3, and confirm invariance.

5. **Tag**: optimize. **Change**: Include a compiled routing header: `workflowMode=review`, `packet=deep-review`, `runtimeLoopType=review`, `agent=deep-review`, `backendKind=runtime-loop-type`, `commandRouter=static`. **Expected effect**: Reduces absorbed steps RVB-007 by preventing the executor from re-deriving or confusing registry, command, skill, and agent routes. **Effort**: S. **Risk**: Source/compiled drift; generate directly from `mode-registry.json` plus command asset paths.
