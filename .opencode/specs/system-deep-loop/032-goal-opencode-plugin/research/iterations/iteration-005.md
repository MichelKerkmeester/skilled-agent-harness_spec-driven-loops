# Focus

Do other skills' own `SKILL.md`, `references/`, and `assets/` mention `mk-goal.js`, `/goal`, or `mk_goal`, and are those mentions still accurate post-remediation?

# Actions Taken

1. Read the current deep-research strategy and state log before research actions. [SOURCE: `.opencode/specs/deep-loops/032-goal-opencode-plugin/research/deep-research-strategy.md:56`-`.opencode/specs/deep-loops/032-goal-opencode-plugin/research/deep-research-strategy.md:71`; `.opencode/specs/deep-loops/032-goal-opencode-plugin/research/deep-research-state.jsonl:14`-`.opencode/specs/deep-loops/032-goal-opencode-plugin/research/deep-research-state.jsonl:17`]
2. Reconfirmed the live command filename: `.opencode/commands/*goal*.md` resolves to `.opencode/commands/goal_opencode.md`, while `.opencode/commands/goal.md` has no live file. The live command markdown itself still carries `# /goal` and delegates to `mk_goal` / `mk_goal_status`. [SOURCE: `.opencode/commands/goal_opencode.md:1`-`.opencode/commands/goal_opencode.md:15`]
3. Ran exact goal-plugin sweeps across `.opencode/skills/**/SKILL.md` and `.opencode/skills/**/*.md` for `mk-goal.js`, `mk_goal`, `goal_opencode`, exact `/goal`, `goal plugin`, `usage_limited`, `store_health`, `recordProviderUsageLimit`, `archiveGoalStateFile`, `pruneArchive`, and `sweepOrphanedActiveStates`.
4. Read candidate README and env-reference surfaces: root `README.md`, `.opencode/plugins/README.md`, `system-skill-advisor/README.md`, `system-spec-kit/mcp_server/ENV_REFERENCE.md`, `system-spec-kit/mcp_server/plugin_bridges/README.md`, and the packet review-archive README. [SOURCE: `README.md:1230`-`README.md:1233`; `.opencode/plugins/README.md:44`-`.opencode/plugins/README.md:50`; `.opencode/skills/system-skill-advisor/README.md:42`-`.opencode/skills/system-skill-advisor/README.md:85`; `.opencode/skills/system-spec-kit/mcp_server/ENV_REFERENCE.md:646`-`.opencode/skills/system-spec-kit/mcp_server/ENV_REFERENCE.md:660`; `.opencode/skills/system-spec-kit/mcp_server/plugin_bridges/README.md:32`-`.opencode/skills/system-spec-kit/mcp_server/plugin_bridges/README.md:69`; `.opencode/specs/deep-loops/032-goal-opencode-plugin/review_archive/2026-07-01-plugin-implementation-review/README.md:7`-`.opencode/specs/deep-loops/032-goal-opencode-plugin/review_archive/2026-07-01-plugin-implementation-review/README.md:18`]
5. Ran negative-claim sweeps for markdown still claiming `usage_limited` is dead/unimplemented or goal-state never gets cleaned up, and cross-checked current source for the remediation truth. [SOURCE: `.opencode/plugins/mk-goal.js:40`-`.opencode/plugins/mk-goal.js:42`; `.opencode/plugins/mk-goal.js:825`-`.opencode/plugins/mk-goal.js:895`; `.opencode/plugins/mk-goal.js:1354`-`.opencode/plugins/mk-goal.js:1368`; `.opencode/plugins/mk-goal.js:1602`-`.opencode/plugins/mk-goal.js:1647`]

# Findings

- **[F-iter005-001, P1] Root README likely points users at the wrong OpenCode command invocation name.** `README.md` tells users to run `/goal <condition>` and manage show/pause/clear/complete through `mk_goal` tools, but the live command file is `.opencode/commands/goal_opencode.md`, not `.opencode/commands/goal.md`, and the runtime-specific constitutional note says OpenCode users must check the live `*goal*.md` filename and never expect bare `/goal` to reach the OpenCode plugin. This is a repo-level README, not one of the already-updated docs named in the prompt. [SOURCE: `README.md:1230`-`README.md:1233`; `.opencode/commands/goal_opencode.md:1`-`.opencode/commands/goal_opencode.md:15`; `.opencode/skills/system-spec-kit/constitutional/goal-prompting-runtime-specific.md:55`-`.opencode/skills/system-spec-kit/constitutional/goal-prompting-runtime-specific.md:64`]
- **[F-iter005-002, P1] `system-skill-advisor/README.md` still contradicts its updated feature catalog on live OpenCode tool verification.** The README says live OpenCode-run tool invocation is "still under investigation," while the updated feature catalog says a real `opencode serve` run listed `mk_goal`/`mk_goal_status` and a live model turn called `mk_goal` and persisted state. [SOURCE: `.opencode/skills/system-skill-advisor/README.md:85`; `.opencode/skills/system-skill-advisor/feature_catalog/07--hooks-and-plugin/goal-opencode-plugin.md:41`]
- **[F-iter005-003, P1] `ENV_REFERENCE.md` is still incomplete for the three cleanup/archive env vars.** Its OpenCode goal plugin table lists older `MK_GOAL_*` controls but omits `MK_GOAL_STATE_ARCHIVE_RETENTION_DAYS`, `MK_GOAL_STATE_ACTIVE_RETENTION_DAYS`, and `MK_GOAL_STATE_SWEEP_INTERVAL_MS`, all of which are live constants in `mk-goal.js`. [SOURCE: `.opencode/skills/system-spec-kit/mcp_server/ENV_REFERENCE.md:646`-`.opencode/skills/system-spec-kit/mcp_server/ENV_REFERENCE.md:660`; `.opencode/plugins/mk-goal.js:40`-`.opencode/plugins/mk-goal.js:42`]
- **[F-iter005-004, P2] The packet review-archive README is a stale README-class artifact if archives are in remediation scope.** The review extraction README still names `.opencode/commands/goal.md` as the command surface even though that file is absent and the live file is `goal_opencode.md`. Because it lives under `review_archive/`, classify it separately from live operator docs before fixing. [SOURCE: `.opencode/specs/deep-loops/032-goal-opencode-plugin/review_archive/2026-07-01-plugin-implementation-review/README.md:7`-`.opencode/specs/deep-loops/032-goal-opencode-plugin/review_archive/2026-07-01-plugin-implementation-review/README.md:18`; `.opencode/commands/goal_opencode.md:1`-`.opencode/commands/goal_opencode.md:15`]
- **[O-iter005-001] Other skills' own `SKILL.md` files are mostly clear.** Exact `SKILL.md` grep for goal-plugin identifiers found only `system-spec-kit/SKILL.md`; it did not surface goal-plugin mentions in `cli-opencode`, `cli-claude-code`, `sk-code`, `sk-prompt-models`, or `deep-loop-workflows` own `SKILL.md` files. The `system-spec-kit/SKILL.md` hits point to the current `goal_opencode.md` file and the hook reference, so they look accurate. [SOURCE: `.opencode/skills/system-spec-kit/SKILL.md:91`; `.opencode/skills/system-spec-kit/SKILL.md:154`; `.opencode/skills/system-spec-kit/SKILL.md:433`; `.opencode/skills/system-spec-kit/SKILL.md:437`]
- **[O-iter005-002] `.opencode/plugins/README.md` is behavior-accurate at a high level but incomplete for the new cleanup/status/output contract.** It correctly says `mk-goal.js` owns per-session state, injects active goals, exposes `mk_goal`/`mk_goal_status`, accounts usage over `event()`, and default-off continuation, but it does not mention archive/sweep cleanup, `store_health`, `mutation=`, or the new cleanup env vars. This is a documentation-depth gap rather than a direct contradiction. [SOURCE: `.opencode/plugins/README.md:44`-`.opencode/plugins/README.md:50`; `.opencode/plugins/mk-goal.js:825`-`.opencode/plugins/mk-goal.js:895`; `.opencode/plugins/mk-goal.js:1602`-`.opencode/plugins/mk-goal.js:1647`]
- **[O-iter005-003] The plugin-bridges README remains accurate.** It says `mk-goal.js` is intentionally absent from `plugin_bridges/` because it is a standalone local OpenCode plugin, not a daemon bridge; current source and docs still support that boundary. [SOURCE: `.opencode/skills/system-spec-kit/mcp_server/plugin_bridges/README.md:32`-`.opencode/skills/system-spec-kit/mcp_server/plugin_bridges/README.md:69`]
- **[O-iter005-004] No current skill/README doc found saying goal-state never gets cleaned up.** The targeted negative sweep found only current research prompts/iteration artifacts asking the question, not a live operator README or skill doc making that stale claim. Current source implements `archiveGoalStateFile`, `pruneArchive`, and `sweepOrphanedActiveStates`. [SOURCE: `.opencode/plugins/mk-goal.js:825`-`.opencode/plugins/mk-goal.js:895`]
- **[O-iter005-005] Stale `usage_limited` dead/unimplemented claims persist in archived research, not in current related-skill docs.** The negative sweep found archived implementation-audit artifacts saying `usage_limited` was dead/unwired, but current source now has `recordProviderUsageLimit` setting `status: 'usage_limited'`. Treat these as historical archive drift unless the remediation scope includes rewriting archives. [SOURCE: `.opencode/specs/deep-loops/032-goal-opencode-plugin/research_archive/2026-07-01-plugin-implementation-audit/iterations/iteration-006.md:16`-`.opencode/specs/deep-loops/032-goal-opencode-plugin/research_archive/2026-07-01-plugin-implementation-audit/iterations/iteration-006.md:64`; `.opencode/plugins/mk-goal.js:1354`-`.opencode/plugins/mk-goal.js:1368`]

# Questions Answered

- **Is `.opencode/commands/goal_opencode.md` still the live command filename?** Yes. The only live command file found by the focused command glob is `.opencode/commands/goal_opencode.md`; `.opencode/commands/goal.md` is absent.
- **Do other skills' own `SKILL.md` files mention the goal plugin?** Only `system-spec-kit/SKILL.md` surfaced exact goal-plugin hits; its current mentions look accurate.
- **Do README files describe the goal plugin/command and contain stale or incomplete claims?** Yes. Root `README.md` likely has stale bare-`/goal` invocation guidance; `system-skill-advisor/README.md` has stale live-tool verification wording; `.opencode/plugins/README.md` is not contradictory but omits cleanup/env/status/output details; a packet review-archive README still names old `goal.md`.
- **Is `ENV_REFERENCE.md` complete for the three new env vars?** No.
- **Does any current skill/README doc still claim `usage_limited` is unimplemented/dead or goal-state never gets cleaned up?** No current skill/README hit found. Historical archive research still contains `usage_limited` dead/unimplemented claims.

# Questions Remaining

- Should packet-local archives under `research_archive/` and `review_archive/` be remediated for stale command filenames and old `usage_limited` claims, or left as historical records?
- Should top-level `README.md` document the runtime-specific distinction between Claude Code native `/goal` and the OpenCode plugin command file `goal_opencode.md`, instead of presenting bare `/goal <condition>` as universal?
- Should `.opencode/plugins/README.md` remain a high-level entrypoint table, or should it include the three new cleanup env vars, `store_health`, and `mutation=` output fields?

# Next Focus

Rotate to repo-level and packet-local README/spec archive classification: determine which stale `goal.md`, bare `/goal`, and `usage_limited dead` statements are historical records to preserve versus active documentation that should be remediated.
