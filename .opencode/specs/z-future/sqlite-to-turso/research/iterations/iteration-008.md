# Iteration 8: C8 spec-kit touchpoint reconciliation

## Focus
Reconciled the Phase A deep-context report (`context/context-report.md`, sections 1–2 and 5–7) against the vendored Turso v0.7.0-pre.6 ground truth (COMPAT.md, JS bindings source) and the live spec-kit mcp_server trees, building on iterations 1–6. Produced a corrected blocker list separating REAL blockers from claims the context report's single-model seat pool asserted but the vendored tree refutes, and audited packet-027 storage additions (mutation-ledger, causal tombstones, idempotency receipts, statediff, OpenLTM observability DBs, semantic triggers) for NEW gaps absent from the 16-gap catalog in `research/003 - gaps-and-workarounds-sqlite-to-turso.md`.

## Findings
- **NEW | REFUTED — "Turso does not support PRAGMA database_list"** (context-report.md:127, `embedding-cache.ts::hasActiveVectorShard`): COMPAT.md:158 lists `PRAGMA database_list ✅`. All four call-site families survive: [SOURCE: .opencode/skills/system-spec-kit/mcp_server/lib/cache/embedding-cache.ts:134], [SOURCE: lib/triggers/semantic-trigger-matcher.ts:222] (an 027 surface), [SOURCE: lib/embedders/reindex.ts:308,318], [SOURCE: lib/embedders/embedding-reconcile.ts:148].
- **NEW | REFUTED — "Turso has no embedded integrity check / quick_check may be unsupported"** (context-report.md:141, :49): COMPAT.md:176 `integrity_check ✅`, COMPAT.md:191 `quick_check ✅`. Code-graph recovery CG-RP-001 and skill-advisor `checkSqliteIntegrity` need no redesign on this axis.
- **NEW | REFUTED — "PRAGMA table_info introspection path differs"** (context-report.md:56): COMPAT.md:202 `table_info ✅`; ALTER TABLE ✅ (COMPAT.md:86). `migrateLearnedTriggers`/`rollbackLearnedTriggers` DDL paths are compatible.
- **KNOWN | REFUTED — "Turso has no ATTACH"** (context-report.md:36–37, 46–47, 97): COMPAT.md:88 `ATTACH DATABASE ✅`. The 3-DB shard migration (`db-shard-migration.ts:243`) and vector-shard ATTACH/DETACH (`vector-index-store.ts:1122`) survive architecturally (extension/vec0 parts remain blocked separately).
- **KNOWN | REFUTED — "VACUUM INTO unsupported"** (context-report.md:41–42, 120–121, 147): COMPAT.md:130 "VACUUM INTO supported; plain in-place VACUUM is experimental". The v2 checkpoint snapshot mechanism (`checkpoints.ts:2281` `VACUUM main INTO`, `:2284` `VACUUM active_vec INTO`) survives as-is, including the attached-shard variant since ATTACH works.
- **KNOWN | REFUTED — "Turso/libSQL lacks triggers"** (context-report.md:22–23, 103): COMPAT.md:95 `CREATE TRIGGER ✅`, :102 `DROP TRIGGER ✅`, :260 `RAISE(ABORT,'msg') ✅`. The 027 mutation-ledger append-only triggers — `BEFORE UPDATE/DELETE … BEGIN SELECT RAISE(ABORT, 'mutation_ledger is append-only'); END` [SOURCE: lib/storage/mutation-ledger.ts:134-136; lib/storage/checkpoints.ts:186-188] — are supported syntax. Residual holes confirmed: `INSTEAD OF` ❌ (COMPAT.md:135), `recursive_triggers` ❌ (:193), `changes()`/`total_changes()` Partial in trigger/UPDATE contexts (:269, :316).
- **KNOWN | REFUTED — "libSQL has no .pragma() method"** (context-report.md:108, 110, 123, 126, 128, 139): established in iterations 1–6 (gap 3 refuted; Turso JS SDK has `.pragma()`). Per-pragma engine gaps persist: `synchronous` Partial — OFF/FULL only, NORMAL missing (COMPAT.md:201); `mmap_size` ❌ (:183); `optimize` ❌ (:185); `wal_checkpoint` Partial — param form gap (:215).
- **KNOWN | REFUTED — "every sync transaction must become async"** (context-report.md:21, 24, 26–29, 53, 158): better-sqlite3-compatible SYNC `./compat` export with `transaction(fn)` incl. deferred/immediate/exclusive (iterations 1–6, gap 16 mostly resolved). `insertEdge`/`insertEdgesBatch`/`rollbackWeights`/`updateEdge`, `persistIndexedFileResult`, lease `acquireSkillGraphLease`, and `runInTransaction` in `causal/sweep.ts` need no async rewrite under the compat driver.
- **KNOWN | REFUTED-as-blocker — lease single-writer redesign** (context-report.md:20): multi-process WAL via `.tshm` sidecar is experimental opt-in; the local-file lease model survives for embedded deployments (iterations 1–6).
- **NEW | CHANGED — plain in-place VACUUM is a partial blocker, not covered by the report's VACUUM INTO claims**: `db-shard-migration.ts:274` and `:282` run bare `VACUUM`; COMPAT.md:130 marks in-place VACUUM experimental. Maps to catalog gap 9 (CHANGED) — smallest unit: `migrateLegacySingleDbToShardSync` post-copy compaction; remediation is dropping the call or a VACUUM INTO+swap.
- **NEW — the single genuine 027-induced gap ABSENT from the 16-gap catalog: `run().changes` counter reliability**. COMPAT.md:269/:316 mark `changes()`/`total_changes()` Partial — "Still need to support update statements and triggers" — and the JS bindings wire `RunResult.changes` to that same core connection counter [SOURCE: external/turso-main/bindings/javascript/src/lib.rs:521-532; sync/packages/common/run.ts:233]. 027 surfaces depend on it for *correctness*, not telemetry: optimistic retry claim `claimed: result.changes > 0` [SOURCE: lib/providers/retry-manager.ts:305,326], skill-advisor lease release/heartbeat guards `result.changes === 0` [SOURCE: system-skill-advisor/.../daemon/lease.ts:350-359], causal tombstone sweep accounting [SOURCE: lib/causal/sweep.ts:206], session dedup send-gating [SOURCE: lib/session/session-manager.ts:609]. None of gaps 1–16 covers change-count semantics. Confidence medium: the COMPAT row sits in the SQL-functions section, so the driver-level counter may handle plain UPDATEs — needs one targeted compat-driver test; mitigation if real: rewrite claims on `RETURNING` (✅ COMPAT.md:113).
- **KNOWN | UNCHANGED — REAL blocker: vec0/sqlite-vec/loadExtension** (gaps 1/4). Smallest units: `vector-index-store.ts::sqliteVec.load`+`initialize_db`, `reindex.ts:370::writeVectorsToKnn`, `vector-index-queries.ts::vec_distance_cosine`, `retry-manager.ts::retryEmbedding` (vec_memories), skill-graph `ensureVecTableForDim`. Maps to: loadExtension absent + no native ANN → f32 BLOB linear scan (sqlite-vec-compatible format) + quantized two-stage.
- **KNOWN | UNCHANGED — REAL blocker: FTS5 module absent** (gaps 2/13). Smallest units: `vector-index-schema.ts::memory_fts`+sync triggers, `bm25-index.ts::isMemoryFtsAvailable`/`shouldUseSqliteLexicalEngine`, `checkpoints.ts:1606/1930` ('delete-all'/'rebuild'), `memory-retention-sweep` optimize, `schema-downgrade.ts::createFtsArtifacts`, `hybrid-search.ts::ftsSearch`. Maps to: Tantivy experimental `index_method` (fts_match/fts_score/fts_highlight) with no fts5-virtual-table/bm25()/special-command compatibility; NoMergePolicy maintenance; MVCC mutually exclusive with Tantivy.
- **KNOWN | UNCHANGED — REAL blocker: WITH RECURSIVE hard-rejected** (gap 6). Smallest units: `causal-boost.ts::causal_walk`/`getNeighborBoosts`, `memo.ts::WITH RECURSIVE dependents` (collectDependents, wouldCreateCycle). Maps to: recursive-CTE absence → app-side iterative BFS.
- **KNOWN | CHANGED — REAL but small: SQLITE_BUSY retry predicate** — `daemon/state-mutation.ts::runDaemonStateMutation` string-matching must move to Busy/BusySnapshot + native `busy_timeout` ✅ (COMPAT.md:147).
- **PARTIAL — wal_checkpoint(TRUNCATE)** (context-report.md:19, 30): COMPAT.md:215 Partial (param form). Units: code-graph `recovery-procedures.ts::rollbackBadApply`, `vector-index-store.ts::reopenActiveDatabase` — guarded fallback for local files, not a hard blocker.
- **PARTIAL — file-copy restore** (`checkpoints.ts:2689-2708::restoreCheckpointV2`, context-report.md:40): valid for local embedded Turso; breaks only for Turso Cloud remote — deployment-conditional, not a capability gap.
- **NEW — 027 additions confirmed gap-free**: idempotency receipts (upsert ✅ COMPAT.md:108–109, `datetime('now')` ✅ :390), statediff (driver-neutral per context-report.md:94), OpenLTM session-analytics/eval DB init pragmas (WAL/busy_timeout/foreign_keys all ✅ :177/:147/:165), near-duplicate + retry-manager standard SQL, learned-triggers ALTER TABLE paths (✅ :86).

## Ruled Out
- `PRAGMA optimize`, `mmap_size`, `defer_foreign_keys`, `synchronous`, and `WITHOUT ROWID` as literal SQL strings in any of the three mcp_server trees — grep returned zero hits, so COMPAT's ❌ rows for these don't bite via that form (caveat below).
- A triggers-vs-MVCC NEW gap from the mutation-ledger: catalog gap 8 already covers it; outside MVCC opt-in the triggers work.
- Gap 15 (FTS5 parameterized insert bug) as relevant to the Turso-rewrite path — it's libsql-only and FTS5 is absent in Turso anyway.

## Dead Ends
- Two Bash compound/glob commands were sandbox-blocked (`&&`-joined ls+rg; `rg -rn` with glob path) — recovered by splitting; no evidence lost but cost ~2 of the budget.
- Could not re-audit the 8-pragma `.pragma('…')` startup block in `vector-index-store.ts::initialize_db` for `synchronous=NORMAL`/`mmap_size` usage (the literal-`PRAGMA` grep wouldn't match `.pragma()` call syntax) — budget exhausted; iterations 1–6 already flag these engine gaps generically.
- Web revalidation skipped this iteration: budget went to local verification, and post-vendor web state was covered in iterations 1–6.

## Sources Consulted
- `.opencode/specs/z_future/sqlite-to-turso/context/context-report.md` (full, lines 1–172)
- `.opencode/specs/z_future/sqlite-to-turso/external/turso-main/COMPAT.md` (lines 86–215, 260, 269, 316, 390, 433–460)
- `.opencode/specs/z_future/sqlite-to-turso/research/003 - gaps-and-workarounds-sqlite-to-turso.md` (gap headings 1–16)
- `.opencode/specs/z_future/sqlite-to-turso/external/turso-main/bindings/javascript/src/lib.rs:515-532`; `sync/packages/common/run.ts:233`; `remote-write-statement.ts:79`
- Live trees: `system-spec-kit/mcp_server/lib` (checkpoints.ts, mutation-ledger.ts, db-shard-migration.ts, retry-manager.ts, causal/sweep.ts, session-manager.ts, embedding-cache.ts, semantic-trigger-matcher.ts, reindex.ts, embedding-reconcile.ts), `system-skill-advisor/.../daemon/lease.ts`, `system-code-graph/mcp_server/lib`

## Reflection
- What worked: COMPAT.md's pragma/statement tables gave line-citable verdicts for nearly every context-report claim in two grep passes; sweeping the live trees for `.changes` consumers surfaced the one genuinely new 027 gap that the catalog and the context report both miss.
- What failed: shell-approval blocks on compound commands burned budget; the `.pragma()`-syntax pragma audit and a runtime test of compat-driver `run().changes` on UPDATE had to be deferred.
- Confidence: high for the refuted/real blocker split (every verdict carries a COMPAT.md or source line); medium for the changes-counter NEW gap's real-world severity (needs one targeted driver test).

## Recommended Next Focus
Run (or plan) a targeted compat-driver probe verifying `run().changes` after UPDATE and on trigger-bearing tables in the vendored v0.7.0-pre.6 bindings, since four 027 correctness-critical call sites (retry claim, lease guards, sweep accounting, dedup gating) hinge on it.
