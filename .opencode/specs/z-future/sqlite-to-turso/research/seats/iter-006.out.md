Web tools are permission-denied in this seat, so post-vendor web state can't be verified this iteration — I'm at the tool budget anyway and have strong vendored-tree evidence for all three questions. Final report below.

# Iteration 6: C6 ops and lifecycle

## Focus
Revalidated three ops/lifecycle claims from the baseline (written against Turso v0.5.0) against the vendored v0.7.0-pre.6 tree: gap 9 (VACUUM unsupported; file-copy checkpoint snapshots as the workaround), gap 10 (CDC unstable/preview, unfit to replace mutation-ledger triggers), and the beta-stability record since v0.5.0 (corruption-class fixes, release cadence, 1.0 signals). Evidence came from `COMPAT.md`, `CHANGELOG.md`, and `cli/manuals/cdc.md` in the vendored tree, cross-checked against the baseline gaps doc and the live-code context report; web verification of post-vendor state was blocked by tool permissions.

## Findings
- **NEW — CHANGED (gap 9, C6a):** VACUUM is no longer "not supported." COMPAT.md now marks VACUUM 🚧 Partial: "VACUUM INTO supported; plain in-place VACUUM is experimental" [SOURCE: .opencode/specs/z_future/sqlite-to-turso/external/turso-main/COMPAT.md:130]; the VDBE `Vacuum` opcode is Partial [SOURCE: ...external/turso-main/COMPAT.md:957]. The baseline claim "Turso's COMPAT.md marks VACUUM as unsupported" [SOURCE: .opencode/specs/z_future/sqlite-to-turso/research/003 - gaps-and-workarounds-sqlite-to-turso.md:472] is outdated.
- **NEW — REFUTED (context-report HARD blockers, C6a):** The live-code blocker map asserts "Turso lacks ATTACH and VACUUM INTO" for the checkpoints.ts v2 snapshot path (`VACUUM main INTO` at checkpoints.ts:2281, `VACUUM active_vec INTO` at :2284) [SOURCE: .opencode/specs/z_future/sqlite-to-turso/context/context-report.md:41-42,120-121,147]. At 0.7.0-pre.6, VACUUM INTO is supported with regression tests including a VACUUM-INTO-panic test, strict-table tests, encrypted-DB integration test, and a post-VACUUM-INTO compaction test [SOURCE: ...external/turso-main/CHANGELOG.md:23,91,94,100,107], and ATTACH exists ("Improve ATTACH to properly initialize fresh databases"; canonical-schema tests for attached DBs) [SOURCE: ...external/turso-main/CHANGELOG.md:55,71]. These blockers downgrade from HARD to verify-only; the attached-schema form `VACUUM active_vec INTO` specifically still needs a live test.
- **PARTIAL — UNCHANGED with new caveats (C6a, file-copy snapshots):** File-copy checkpoint snapshots remain valid for local file databases: Turso keeps the SQLite-compatible on-disk format with WAL + checkpointing (per the vendored agent guide's "B-tree/pages … SQLite-compatible format" and "WAL/durability … checkpointing" entries in external/turso-main/CLAUDE.md "Where to Look"), `PRAGMA journal_mode` is ✅ [SOURCE: ...external/turso-main/COMPAT.md:177] and `sqlite3_wal_checkpoint`/`_v2` are ✅ [SOURCE: ...external/turso-main/COMPAT.md:705-706]. Caveats: `PRAGMA wal_checkpoint` is only 🚧 Partial with the note flagging the with-parameter form [SOURCE: ...external/turso-main/COMPAT.md:215], so `wal_checkpoint(TRUNCATE)` as used by `rollbackBadApply` [SOURCE: ...context/context-report.md:19] may not accept its argument — copying only the .sqlite file is safe only after a successful checkpoint/quiesce; and new engine modes (experimental MVCC logical log, initial multi-process access [SOURCE: ...external/turso-main/CHANGELOG.md:45], encryption [SOURCE: ...external/turso-main/CHANGELOG.md:100]) would invalidate naive single-file copies if enabled.
- **PARTIAL (C6a, plain VACUUM trajectory):** In-place VACUUM gained initial support during the 0.6.0 cycle ("Add initial support of `vaccum` op"; "reenable disabled vacuum tests") [SOURCE: ...external/turso-main/CHANGELOG.md:29,134]; `auto_vacuum` write still needs `--experimental-autovacuum` [SOURCE: ...external/turso-main/COMPAT.md:145] and `incremental_vacuum` is still ❌ [SOURCE: ...external/turso-main/COMPAT.md:172]. Baseline workaround W9a (we never call VACUUM, so do nothing) remains valid — impact UNCHANGED, capability CHANGED.
- **NEW — CHANGED (gap 10, C6b):** The CDC pragma has been de-unstabled: canonical name is now `capture_data_changes_conn` (per-connection, returns `(mode, table, version)`, default `off`), with `unstable_capture_data_changes_conn` retained only as an alias whose "name may change" [SOURCE: ...external/turso-main/COMPAT.md:224-225]. The baseline's "the word 'unstable' is literally in the API name" [SOURCE: ...research/003 - gaps-and-workarounds-sqlite-to-turso.md:503] no longer holds.
- **NEW (C6b, CDC maturity):** CDC now ships a full manual: modes `off/id/before/after/full`, custom capture table (default `turso_cdc`), defined table schema (`change_id`, `change_time`, `change_type` 1/0/-1, `before`/`after`/`updates` BLOBs), transaction-boundary semantics (entries written only on COMMIT, none on rollback), schema-change capture in `full` mode, and independent per-connection configs [SOURCE: ...external/turso-main/cli/manuals/cdc.md:13-30,74-85,157-185].
- **PARTIAL — UNCHANGED (C6b, fit verdict):** CDC is still not a drop-in replacement for the mutation-ledger triggers/audit trail: it is per-connection opt-in with default `off` [SOURCE: ...external/turso-main/COMPAT.md:224], so any writer that doesn't enable it silently bypasses capture — unlike DB-resident triggers that fire for all writers; `before`/`after`/`updates` are BLOBs requiring the decode adapter the baseline anticipated (W10c) [SOURCE: ...research/003 - gaps-and-workarounds-sqlite-to-turso.md:521-524]; and CDC is rejected on WITHOUT ROWID tables [SOURCE: ...external/turso-main/COMPAT.md:134]. Baseline W10a ("keep triggers, layer CDC later") remains the correct posture, now with a clearer stabilization trend.
- **NEW (C6c, cadence):** Release cadence from the vendored changelog: 0.1.0 2025-06-30 → 0.2.0 2025-10-03 → 0.3.0 2025-10-30 → 0.4.0 2026-01-05 → 0.5.0 2026-03-04 → 0.6.0 2026-05-14, with 0.7.0-pre.6 cut within ~1 month of 0.6.0 — a steady ~2-month minor-release cadence over the last three cycles [SOURCE: ...external/turso-main/CHANGELOG.md:3,490,1130,1454,1620,2619].
- **PARTIAL (C6c, stability record):** Corruption/panic-class bugs were still being found and fixed in the 0.6.0 cycle: savepoint-rollback cache-invalidation fix with DST coverage [SOURCE: ...external/turso-main/CHANGELOG.md:26], pager panic after savepoint rollback with overflow [:20], "index cursor should have a record" regression [:22], VACUUM INTO panic [:23], `pragma_table_list()` panic [:24] — alongside a deepening verification stack (TLA+ spec for WAL-mode transactions [:118], MVCC shuttle tests [:119], simulator yield injections [:83], SQLite TCL conformance in CI [:59], TPC-C [:74]). Net read: rapidly maturing, but corruption-class fixes are recent enough that beta posture persists at 0.7.0-pre.6.
- **PARTIAL (C6c, 1.0 signal):** A production-readiness FAQ exists in-tree and was explicitly "update[d] … to reflect current state" in 0.6.0 [SOURCE: ...external/turso-main/CHANGELOG.md:147], but its content was not read this iteration (budget) and the changelog carries no 1.0 date; 1.0 timeline remains UNKNOWN from vendored evidence, and post-vendor web state could not be checked (see Dead Ends).

## Ruled Out
- "Turso/libSQL does not support VACUUM INTO" — the context report's 3/3-agreed HARD blocker framing for checkpoints.ts:2281/2284 is disproven at 0.7.0-pre.6 (supported, with regression and encryption tests).
- "Turso lacks ATTACH" as an absolute — ATTACH exists with fresh-database initialization and schema tests at 0.7.0-pre.6 (degree of parity still unverified).
- "CDC's API name is literally unstable_" — the canonical pragma is now `capture_data_changes_conn`; only the alias keeps the prefix.

## Dead Ends
- WebSearch and WebFetch were both permission-denied in this seat, so post-vendor (after ~June 2026) releases, open corruption issues, and 1.0 announcements could not be verified.
- Two compound Bash commands were blocked by the sandbox approval gate (no data; cost 2 of the 12-call budget).
- The production-readiness FAQ file itself was located only by changelog reference, not read.

## Sources Consulted
- `.opencode/specs/z_future/sqlite-to-turso/external/turso-main/COMPAT.md` — lines 130, 134, 145, 172, 177, 215, 224-225, 705-706, 859, 957, 962 (via targeted grep)
- `.opencode/specs/z_future/sqlite-to-turso/external/turso-main/CHANGELOG.md` — lines 1-150 read; version headers at 3, 490, 1130, 1454, 1620, 1929-3535 (grep)
- `.opencode/specs/z_future/sqlite-to-turso/external/turso-main/cli/manuals/cdc.md` — full file (185 lines)
- `.opencode/specs/z_future/sqlite-to-turso/external/turso-main/CLAUDE.md` — agent guide ("Where to Look": storage format, WAL/checkpointing)
- `.opencode/specs/z_future/sqlite-to-turso/research/003 - gaps-and-workarounds-sqlite-to-turso.md` — lines 461-544, 736-737 (grep)
- `.opencode/specs/z_future/sqlite-to-turso/context/context-report.md` — lines 19, 30, 38-45, 67, 103, 119-123, 147, 156 (grep)
- Web: none (permission-denied)

## Reflection
- What worked: triangulating COMPAT.md status rows against CHANGELOG 0.6.0 entries and the dedicated CDC manual gave decisive, line-citable verdicts on both gaps, including a clean refutation of two HARD blockers in the context report.
- What failed: WebSearch/WebFetch permission denials killed all post-vendor verification (C6c is vendored-evidence-only), and two compound Bash commands bounced off the sandbox, wasting budget that could have read the production-readiness FAQ.
- Confidence: high for C6a/C6b (direct vendored-source citations at the exact pinned version); medium for C6c (cadence and fix record are solid, but open-issue state and 1.0 timeline are unverified without web access).

## Recommended Next Focus
Read the in-tree production-readiness FAQ and `core/translate/pragma.rs` to pin down `wal_checkpoint(TRUNCATE)` parameter behavior and the attached-schema `VACUUM <db> INTO` form, then retry web verification of post-0.6.0 releases and open corruption issues from a seat with WebSearch/WebFetch granted.
