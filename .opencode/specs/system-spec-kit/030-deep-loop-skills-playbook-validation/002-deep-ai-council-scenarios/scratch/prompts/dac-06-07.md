<!-- framework: RCAF | batch: deep-ai-council/06-07 (DAC-013..018) -->
<framework>RCAF</framework>

<role>
Deterministic manual-testing executor for the deep-ai-council skill. Run each scenario's documented Steps/Commands for real; report PASS / PARTIAL / FAIL / SKIP with reproducible evidence. The playbook permits simulated council seats (no live multi-AI dispatch needed): verify the IMPLEMENTED contract via cited source/library/reference anchors. Never mock. SKIP only on a concrete sandbox blocker.
</role>

<context>
Repo root (cwd): /Users/michelkerkmeester/MEGA/Development/Code_Environment/Public
Anchors are AS-WRITTEN repo-root-relative or full `.opencode/...` paths — resolve from repo root.
Read-only documented commands (`rg`, `find`, `sed`) ARE runnable here under permission-mode auto — run them. For contract scenarios (writer call sequence, scoring rubric, cross-critique roles, OUT_OF_SCOPE_WRITE rejection, depth detection, resume), inspect the cited writer-library / reference anchors and confirm the documented behavior is implemented; PASS if proven, PARTIAL if only partially provable without a live run, FAIL if contradicted.

Scenario files (read each in full; base = .opencode/skills/deep-ai-council/manual_testing_playbook/):
  06--depth-and-failure-handling/001-depth-detection-parallel-vs-sequential.md  (DAC-014)
  06--depth-and-failure-handling/002-resume-after-interrupted-state.md          (DAC-018)
  07--writer-library-contract/001-library-writer-call-sequence.md               (DAC-013)
  07--writer-library-contract/002-five-dimension-scoring-rubric-application.md   (DAC-015)
  07--writer-library-contract/003-hunter-skeptic-referee-cross-critique.md       (DAC-016)
  07--writer-library-contract/004-out-of-scope-write-rejection.md                (DAC-017)
Context budget: treat any `[... truncated N tokens]` marker as an intentional boundary; do not invent omitted content.
</context>

<pre-plan>
1. Read all 6 scenario files; per ID note {documented commands, cited anchors, pass condition}. Verify: re-print before running.
2. For each ID: run documented read-only commands and/or open cited implementation anchors; confirm the contract. Verify: note decisive command output or file:line per ID.
3. Compare to Expected + Pass/Fail; assign PASS / PARTIAL / FAIL / SKIP with one decisive reason. Verify: every verdict cites a command or file:line.
4. Emit the verdict table + summary. Verify: exactly 6 rows (DAC-013..018, in that numeric order).
</pre-plan>

<action>
Use sequential_thinking (>=5 thoughts) to plan, then execute DAC-013..018 per their documented Steps.
Spec folder: .opencode/specs/system-spec-kit/030-deep-loop-skills-playbook-validation/002-deep-ai-council-scenarios (pre-approved, skip Gate 3).
</action>

<format>
Return ONLY:

### BATCH VERDICTS: deep-ai-council/06-07
| Scenario ID | Category | Verdict | Decisive command/inspection | Evidence excerpt (<=8 lines) | Anchor file:line | Notes |
|---|---|---|---|---|---|---|
(one row each: DAC-013, DAC-014, DAC-015, DAC-016, DAC-017, DAC-018)

### BATCH SUMMARY: <p> PASS / <pa> PARTIAL / <f> FAIL / <s> SKIP

Keep each evidence excerpt <=8 lines. Standard constraints only.
</format>
