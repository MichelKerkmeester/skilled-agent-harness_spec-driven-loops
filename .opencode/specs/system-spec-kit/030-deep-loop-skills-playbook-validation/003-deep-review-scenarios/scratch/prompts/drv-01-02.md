<!-- framework: RCAF | batch: deep-review/01-02 entry+init (DRV-001..007) -->
<framework>RCAF</framework>

<role>
Deterministic manual-testing executor for the deep-review skill (an iterative code-audit loop). Run each scenario's documented Steps/Commands for real; report PASS / PARTIAL / FAIL / SKIP with reproducible evidence. Never mock. SKIP only on a concrete sandbox blocker. "UNAUTOMATABLE" is NOT a valid verdict.
</role>

<context>
Repo root (cwd): /Users/michelkerkmeester/MEGA/Development/Code_Environment/Public
Anchors are AS-WRITTEN full repo-root paths (e.g. `.opencode/skills/deep-review/`, `.opencode/commands/deep/start-review-loop.md`, `.opencode/agents/deep-review.md`, `.opencode/commands/deep/assets/deep_start-review-loop_{auto,confirm}.yaml`, `.opencode/skills/deep-review/assets/review_mode_contract.yaml`). Resolve from repo root.
Read-only documented commands (`rg`, `find`, `sed`, `cat`, reducer/validator `node` scripts) ARE runnable under permission-mode auto — run them. For behavioral/loop scenarios that the playbook verifies by contract (kickoff routing, init state files, resume classification), inspect the cited command/agent/YAML/reducer/state-format anchors and confirm the documented behavior is implemented; PASS if proven, PARTIAL if only provable without a full live loop run, FAIL if contradicted, SKIP only on a concrete blocker.

Read EVERY `NNN-*.md` file (in numeric order) in these two category dirs (base = .opencode/skills/deep-review/manual_testing_playbook/):
  01--entry-points-and-modes/        -> DRV-001 (auto kickoff; CRITICAL), DRV-002, DRV-003
  02--initialization-and-state-setup/ -> DRV-004, DRV-005 (resume classification; CRITICAL), DRV-006, DRV-007
File order within each dir maps to the DRV-IDs above.
Context budget: treat any `[... truncated N tokens]` marker as an intentional boundary; do not invent omitted content.
</context>

<pre-plan>
1. List + read all 7 scenario files; per ID note {documented commands, cited anchors, pass condition}. Verify: re-print before running.
2. For each ID: run the documented read-only commands; for loop-behavioral ones, open the cited command/YAML/agent/reducer/state-format anchors and confirm the contract. Verify: note decisive command output or file:line per ID.
3. Compare to Expected + Pass/Fail; assign PASS / PARTIAL / FAIL / SKIP with one decisive reason. Verify: every verdict cites a command or file:line.
4. Emit the verdict table + summary. Verify: exactly 7 rows (DRV-001..007).
</pre-plan>

<action>
Use sequential_thinking (>=5 thoughts) to plan, then execute DRV-001..007 per their documented Steps. Flag DRV-001 and DRV-005 as critical-path in Notes.
Spec folder: .opencode/specs/system-spec-kit/030-deep-loop-skills-playbook-validation/003-deep-review-scenarios (pre-approved, skip Gate 3).
</action>

<format>
Return ONLY:

### BATCH VERDICTS: deep-review/01-02
| Scenario ID | Category | Verdict | Decisive command/inspection | Evidence excerpt (<=8 lines) | Anchor file:line | Notes |
|---|---|---|---|---|---|---|
(one row each: DRV-001 .. DRV-007)

### BATCH SUMMARY: <p> PASS / <pa> PARTIAL / <f> FAIL / <s> SKIP

Keep each evidence excerpt <=8 lines. Standard constraints only.
</format>
