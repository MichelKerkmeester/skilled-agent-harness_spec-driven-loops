# Deep Review Report: Storage Adapter Ports

## Executive Summary
Verdict: CONDITIONAL

Active findings: P0=0, P1=1, P2=1. The implementation is broadly sound and the narrow storage-port tests pass, but the VectorStore contract does not yet prove ID parity between the better-sqlite adapter and fake implementation. `hasAdvisories=true` because F002 remains as a P2 advisory.

## Planning Trigger
Route to remediation planning or a focused follow-up patch for F001. A clean PASS requires either documenting generated-ID semantics explicitly and aligning the fake, or adding a contract assertion that original caller-supplied IDs behave consistently across implementations.

## Active Finding Registry
| ID | Severity | Dimension | Title | Evidence | Status |
|----|----------|-----------|-------|----------|--------|
| F001 | P1 | traceability | VectorStore contract suite misses caller-supplied ID parity | `.opencode/skills/system-spec-kit/mcp_server/tests/storage-ports-contract.vitest.ts:276-304`; `.opencode/skills/system-spec-kit/mcp_server/lib/storage/ports/vector-store.ts:175-187`; `.opencode/skills/system-spec-kit/mcp_server/lib/storage/ports/vector-store.ts:236-260`; `.opencode/skills/system-spec-kit/mcp_server/tests/fakes/storage-ports.ts:188-197` | active |
| F002 | P2 | correctness | VectorStore.clear semantics are broader than the interface wording | `.opencode/skills/system-spec-kit/mcp_server/lib/storage/ports/vector-store.ts:79-97`; `.opencode/skills/system-spec-kit/mcp_server/lib/storage/ports/vector-store.ts:264-271` | active |

## Remediation Workstreams
| Workstream | Findings | Action |
|------------|----------|--------|
| VectorStore contract parity | F001 | Decide whether `VectorRecord.id` is caller-owned or generated by the adapter, then update `VectorStore`, `FakeVectorStore`, and `storage-ports-contract.vitest.ts` to enforce the chosen behavior. |
| Vector clear semantics | F002 | Rename/document `clear()` as full memory-index clear or narrow the adapter implementation to vector rows only if generic callers need that semantic. |

## Spec Seed
- Clarify REQ-002 with explicit VectorStore ID semantics: caller-supplied stable IDs or generated memory IDs.
- If generated IDs are intended, note that `upsert(record.id)` is an input label, not the persisted lookup key.

## Plan Seed
- Add a failing contract case that upserts `{ id: 'left' }` and verifies `get('left')` / `delete('left')` behavior for both fake and better-sqlite, or explicitly updates the fake to generated-ID semantics and tests the generated ID path.
- Re-run `npx vitest run tests/storage-ports-contract.vitest.ts tests/memo-storage.vitest.ts --testTimeout 60000`.

## Traceability Status
| Protocol | Status | Gate | Evidence | Notes |
|----------|--------|------|----------|-------|
| spec_code | partial | hard | `spec.md:113-116`; `tests/storage-ports-contract.vitest.ts:276-304` | REQ-002 contract validation is incomplete for VectorStore IDs. |
| checklist_evidence | partial | hard | `tasks.md:69-88`; `implementation-summary.md:143-174` | Completion evidence is real, but does not prove F001. |
| feature_catalog_code | pass | advisory | `lib/storage/ports/index.ts:5-49` | Five ports are exported. |
| playbook_capability | partial | advisory | `tests/storage-ports-contract.vitest.ts:422-439` | Contract tests run but miss original-ID parity. |

## Deferred Items
- F002 can remain advisory if no generic production caller uses `VectorStore.clear` and legacy full-clear semantics are intentional.
- Broad full-suite failures recorded in `implementation-summary.md` were not replayed in this lineage.

## Audit Appendix
| Iteration | Focus | New Findings | Ratio | Verdict |
|-----------|-------|--------------|-------|---------|
| 001 | correctness | F002 | 1.0000 | PASS |
| 002 | security | none | 0.0000 | PASS |
| 003 | traceability | F001 | 0.8333 | CONDITIONAL |
| 004 | maintainability | none | 0.0000 | PASS |
| 005 | stabilization | none | 0.0000 | CONDITIONAL |

Replay: all four dimensions covered; required traceability protocols ran; one stabilization pass found no new P0/P1. Active P1 produces `CONDITIONAL`. Narrow verification command passed: `npx vitest run tests/storage-ports-contract.vitest.ts tests/memo-storage.vitest.ts --testTimeout 60000` with 2 files and 30 tests.
