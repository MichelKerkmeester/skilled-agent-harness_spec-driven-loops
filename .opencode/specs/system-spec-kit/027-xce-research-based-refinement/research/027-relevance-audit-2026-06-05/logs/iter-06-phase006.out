I’ll keep this read-only and audit Phase 006 against current code plus recent git history, focusing on whether a `statediff` mechanism exists and whether the recent pipeline commits supersede the phase premise.
I’m reading the Phase 006 docs, current write-path files, and recent history in parallel, then I’ll cross-check the named commits and symbols.
The initial pass shows Phase 006 is explicit about a missing `lib/storage/statediff.ts` engine, while current `memory-index.ts` still has inline categorization, deletion, invalidation hooks, alias detection, and divergence reconciliation. I’m checking the two named commits and the save pipeline details before deciding whether that is just partially addressed or still fully pending.
There are zero current TypeScript hits for `statediff`, `DiffAction`, target-sink, or action-subscriber terminology, and no `lib/storage/*statediff*` or `*target*` files. The named async-save commit changed enrichment timing after commit, but the durable write path still enters direct transactions and post-mutation hooks.
The recent save-pipeline work does affect the graph/enrichment timing assumption: post-insert enrichment is now default-on and async/deferred after row commit. It does not introduce statediff planning or subscriber-based cache/hygiene dispatch, so I’m treating that as a rescope detail rather than completion.
## 006-write-path-reconciliation — VERDICT: NEEDS-RESCOPE
- Evidence: `0060a097b3` changed `memory-save.ts` to async/deferred post-insert enrichment; current code still commits durable rows imperatively in `memory-save.ts:2535-2636`, invalidates directly at `memory-save.ts:2703-2704`, and schedules async enrichment at `memory-save.ts:2690-2702`.
- Evidence: `01d666985c` only synced docs; `handlers/save/README.md:178-186` still describes `create record; post-insert enrichment scheduled async` followed by `response builder and mutation hooks`, not statediff planning.
- Evidence: Current search found zero `statediff`, `DiffAction`, target-sink, or action-subscriber TypeScript hits; no `lib/storage/*statediff*` or `*target*` files exist.
- Evidence: `memory-index.ts` remains inline: categorization at `memory-index.ts:665-670`, direct stale delete at `memory-index.ts:838-845`, broad post-mutation hooks plus inline alias/divergence handling at `memory-index.ts:926-945`.
- Per-item: `lib/storage/statediff.ts` engine — STILL-RELEVANT.
- Per-item: `memory_index` / embedding / FTS / BM25 target sinks — STILL-RELEVANT.
- Per-item: `memory_index_scan` plan-before-write — STILL-RELEVANT.
- Per-item: `memory_save` conversion — NEEDS-RESCOPE: keep durable-row statediff work, but model post-insert enrichment as async/pending-marker-driven rather than same-response graph writes.
- Per-item: Cache invalidation subscribers — STILL-RELEVANT; current hooks are still broad operation hooks in `mutation-hooks.ts:21-127`.
- Reasoning: No statediff engine exists, and both save and scan still mutate through direct branches/transactions rather than a desired/prior action plan. The async pipeline commits partially shift the graph/enrichment timing assumption, but they do not supersede Phase 006’s core reconciliation model.
- Recommended action: Rescope narrowly: keep Phase 006, but update graph-edge/enrichment target wording to account for async post-insert enrichment and pending-marker replay; do not drop the phase.
- Confidence: high.
