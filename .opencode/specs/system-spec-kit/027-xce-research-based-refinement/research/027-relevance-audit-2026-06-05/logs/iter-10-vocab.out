I’ll audit this read-only against the phase docs, recent git history, and current causal/search code, then return one verdict for the constants/vocabulary stability assumption.
I’m checking the relevant 027 phase docs plus current symbols and recent causal/search history, without modifying files.
Current grep already shows all three anchors exist. I’m narrowing whether recent commits changed their shapes or only adjacent causal registry/backfill behavior.
The phase docs explicitly depend on the current vocabulary and constants: 005 excludes vocabulary expansion, and 008 children import the existing coverage targets and state limits rather than copying them.
The named June 3-4 unlink commits did not touch the vocabulary/constants files; they only exposed/registered the unlink tool. I’m checking the relation-coverage history now because that is where the recent causal balancing work actually changed defaults.
One material mismatch surfaced: `DEFAULT_RELATION_TARGETS` is not the same vocabulary as `RELATION_TYPES`; it omits valid `enabled`/`derived_from` and includes invalid `produced`/`cited_by`. I’m tracing whether that mismatch is new or inherited.
The mismatch is inherited, not introduced by the named unlink commits: `RELATION_TYPES` dates to Feb 11, `STATE_LIMITS` to Feb 28, while the current coverage targets date to May 8 and were not vocabulary-aligned by the June backfill commits.
No planned implementation artifacts for 004/005/008 reducers are present (`sweep.ts`, promoter, causal reducer, retention reducer all absent), so the question is not “already implemented”; it’s whether the assumptions need rescope.
## Cross-Cutting Assumption: relation vocabulary + constants stability — VERDICT: NEEDS-RESCOPE
- Evidence: `RELATION_TYPES` is still stable/current at `.opencode/skills/system-spec-kit/mcp_server/lib/storage/causal-edges.ts:18-25`, and schema still permits the same six values at `lib/search/vector-index-schema.ts:719-721`.
- Evidence: `DEFAULT_RELATION_TARGETS` exists at `lib/causal/relation-coverage.ts:41-48`, but includes `produced`/`cited_by` and omits valid `enabled`/`derived_from`.
- Evidence: `STATE_LIMITS` exists at `lib/search/pipeline/stage4-filter.ts:68-74`, but is only exposed through `__testables.STATE_LIMITS` at `stage4-filter.ts:362-368`, not as a production named export.
- Evidence: Recent commits `deee30b319` and `23ba7ea08e` only exposed/registered `memory_causal_unlink`; they did not change `RELATION_TYPES`, `DEFAULT_RELATION_TARGETS`, or `STATE_LIMITS`. Relation coverage behavior changed in `c7eb1f6454`, `d32d90c3f1`, and `b834150fe5`, but the target list itself was inherited from `50ea801c2b`.

- Per-item: `RELATION_TYPES` — STILL-RELEVANT. Stable and still matches the causal edge schema.
- Per-item: `DEFAULT_RELATION_TARGETS` — NEEDS-RESCOPE. Do not use it as a proxy for the valid causal relation vocabulary until it is aligned with `RELATION_TYPES` or explicitly documented as coverage-only.
- Per-item: `STATE_LIMITS` — NEEDS-RESCOPE. Constants are stable, but 008.4 should add/use a production export rather than importing through `__testables` or redeclaring.

- Reasoning: The core vocabulary was not invalidated by recent unlink/backfill commits, but 027’s assumption that coverage targets are a stable vocabulary surface is wrong. The 008 causal reducer plan is especially affected because it emits `ENABLED` edges while `DEFAULT_RELATION_TARGETS` has no `enabled` target. The retention reducer amendment is directionally sound, but the import surface needs a small adjustment.
- Recommended action: Rescope narrowly: keep the phases, but amend 004/005/008 to treat `RELATION_TYPES`/schema as the vocabulary source, align or validate `DEFAULT_RELATION_TARGETS` before reducer use, and add a production export for `STATE_LIMITS`.
- Confidence: high

Review status: COMMENTED
