# Prompt Pack

This fan-out lineage was executed in-process from the user-provided parameters. The artifact root was bound directly from `config.fanout_lineage_artifact_dir`; no resolve-artifact-root node command was run.
