SKILL ROUTING: User directed -> deep-review. Artifact root is bound directly to the provided lineage override; I will only write review outputs under that lineage directory.phase_init is starting against the provided lineage root. I am checking whether this lineage already has state so the loop can initialize or resume safely.phase_main_loop is starting. The target docs identify the changed code and tests; I will review one dimension per iteration and write only lineage-local state/output files.The review found one active correctness issue: the repair job atomically replaces the shard file while the live SQLite connection remains attached to the old shard handle, so the current process can stay on an empty/stale `active_vec` after rebuild. I am writing the lineage artifacts with a conditional verdict.phase_synthesis artifacts are written. I am running read-only validation of the lineage packet structure and the target spec validation gate before the final completion signal.FANOUT_LINEAGE_COMPLETE:gpt-3