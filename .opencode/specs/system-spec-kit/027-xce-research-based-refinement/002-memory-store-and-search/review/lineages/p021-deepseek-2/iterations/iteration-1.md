Now let me check for resource-map.md and read the implementation files mentioned in the spec.Now let me set up the artifact directory and proceed with the review phases. I'll read the key implementation sections in parallel.Now creating all initialization artifacts.Phase 1 complete. Now Phase 2: MAIN LOOP - executing 1 iteration (D1 Correctness).Let me verify the `onPhase` to `maintenance.refresh()` mapping before finalizing the iteration.Now writing the iteration file with the correctness review findings.Now updating state files and proceeding to synthesis.Now updating strategy, findings registry, and dashboard.Now finalizing state and completing.FANOUT_LINEAGE_COMPLETE:p021-deepseek-2