# Review Resource Map

- Touched entries: `spec.md`, `description.json`, `graph-metadata.json`, `resource-map.md`, `timeline.md`, `changelog/README.md`, and `011-command-presentation-workflow-separation/spec.md`.
- Untouched entries (`expected-by-scope` vs `gap`): existing resource-map entries for peck and system-spec-kit skill files were expected-by-scope for the parent-aggregate map, but not all were re-read because this lineage focused on parent inventory drift.
- Implementation paths absent from the map: `011-command-presentation-workflow-separation/` and its four child-family phase parents are absent from the reviewed parent `resource-map.md`, tracked as F002.
