# Review Resource Map: gpt-4 Lineage

- Entries touched: parent `spec.md`, `description.json`, `graph-metadata.json`, `resource-map.md`, `context-index.md`, `timeline.md`, `changelog/README.md`, child `011-command-presentation-workflow-separation/spec.md`, sampled child status specs for `002`, `008`, and `009`.
- Untouched entries (`expected-by-scope` vs `gap`): deep child implementation code and most nested child phase docs were expected-by-scope omissions for this parent-control review; the parent resource-map omission of `011-command-presentation-workflow-separation/` is a gap tracked as F003.
- Implementation paths absent from the map: `011-command-presentation-workflow-separation/` and its four command-family child parents are absent from the parent resource map reviewed at `.opencode/specs/system-spec-kit/027-xce-research-based-refinement/resource-map.md:70-82`.
