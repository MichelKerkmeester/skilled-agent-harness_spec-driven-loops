Using the provided spec folder and lineage artifact root; loading the deep-review contract before writing any lineage artifacts.SKILL ROUTING: User directed → `deep-review`; advisor also surfaced `deep-loop-workflows` with confidence 0.95. Memory trigger lookup initially rejected the supplied fan-out session id because it is not server-managed, then succeeded without that session id.Binding `artifact_dir` directly to the provided lineage directory and treating existing scope files as read-only review inputs. The lineage directory currently only contains `.executor-state/`, so this is a fresh lineage state packet.Starting the single allowed review iteration with a correctness-first pass over the declared search/retrieval surface, using direct Glob/Grep/Read evidence because this audit explicitly permits plain file inspection if MCPs are unreliable.Initial pass found one concrete seam worth checking further: `memory_search` appears to pass recovery/confidence context to the formatter, so I’m tracing the formatter before deciding whether it is a real defect or just delegated behavior.The formatter trace shows the evidence-gap detector annotates rows and metadata, but recovery policy uses a different field name. I’m checking whether that can make a weak/gap result appear citable before recording it.There is an existing formatter test for `evidenceGap: true`, but the live handler only forwards `evidenceGapWarning`; that supports the field-name mismatch as a real integration gap rather than an intended formatter contract.Writing the lineage packet now: config/state, one iteration with two active findings, reducer-style registry/dashboard, resource map, and final report, all under the provided lineage directory only.Verifying the lineage artifacts now by parsing JSON/JSONL and checking the mandatory final iteration verdict line.FANOUT_LINEAGE_COMPLETE:gpt55r2-a-9