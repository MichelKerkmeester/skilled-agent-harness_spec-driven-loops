# Plan — code vs sk-code remediation (Track B)

**Source review:** `../002-code-vs-sk-code-opencode/review/findings-all.json` (154 findings: 66 P0 / 47 P1 / 41 P2) + `../synthesis.md`.
**Executor (per operator):** `cli-opencode` → `openai/gpt-5.5-fast` `--variant high`, dispatched as a **general code-implementation agent** (role + `sk-code` load stated in the prompt body).
**Goal:** fix the CONFIRMED sk-code misalignments only. The raw 66 P0s are ~3x inflated by false positives — fixing them blindly would strip valid comments and waste churn.

## Scope — CONFIRMED only

| # | Theme | Fix | Verified basis |
|---|-------|-----|----------------|
| C1 | **Ephemeral ids in code comments** (comment-hygiene HARD BLOCK) | Drop the perishable label, KEEP the durable WHY. `// FIX RC3-B: Intent confidence floor — …` → `// Intent confidence floor — …`; `// P4-12/P4-19: Incremental index …` → `// Incremental index …`; `# …packets 006-009 … SC-002` → plain-language. | comment-hygiene.md forbidden list; 3/3 spot-checks were real |
| C2 | **Missing shell strict-mode** | Add `set -euo pipefail` immediately after shebang/header. | sk-code `opencode/shell/quality_standards.md:51` "Every script must enable strict mode" |
| C3 | **`any[]` in public DB type** (`lib/storage/write-provenance.ts:25`) | Replace with the real row type (infer from usage; verify with tsc). | sk-code `code_quality_standards.md:92` "no any" |
| C4 | **Real spec-path mentions in comments** (audit, not example values) | file-watcher.ts:201, folder-discovery.ts:1069 — if a concrete `.opencode/specs/NNN-…` path is embedded, replace with durable WHY. | comment-hygiene.md |

### DO NOT FIX (refuted / downgraded — editing corrupts correct code)
- **`// Feature catalog: <name>` comments** (~18-20 "P0s") — NOT in comment-hygiene's forbidden list (spec-paths / packet-phase / ADR-REQ-CHK-task-finding ids); gate-allowed; committed. **Leave them.**
- **TSDoc `@example` values** like `specFolder: 'specs/001-demo'` — illustrative API examples, not ephemeral pointers. **Leave them.**
- **`scripts/dist/**`** "stale compiled" — build output (gitignored, regenerated by `npm run build`). Out of scope; rebuild instead of editing.
- **"non-boxed / dashed header"** P0s — downgraded to P2 style. OPTIONAL cosmetic pass only if a seat has spare budget; never a blocker, never a forced rewrite.
- **P1 style nits** (catch-param-not-`unknown`, missing-TSDoc-on-exported) — valid but low priority; batch only the ones a seat can fix without behavior change, defer the rest.

## Executor design (dispatch-ready)

- **Slicing:** by the review's code areas, BUT only seats that own confirmed findings get a fixer. Group the confirmed C1/C2/C3/C4 hits into ~6-8 fixer seats by directory cluster (handlers, lib/search, lib/storage+ops, shared+context-server, advisor scripts, code-graph, deep-loop scripts, bin+hooks+scripts).
- **Concurrency:** pool of 10, staggered 3s, `</dev/null`, `gtimeout 1200`.
- **Isolation (RM-8 — this is the 44-file-deletion class):** ALL Track-B fixers run with `--dir <worktree-B>` (fresh git worktree off the branch, disjoint file scopes). NEVER `--dangerously-skip-permissions` against the live tree. Diff-reviewed before merge.
- **Per-seat brief contract:**
  - `Spec folder: <this packet> (pre-approved at Gate 3 — make the edits below; do not re-ask).`
  - `FIRST load sk-code, get the OpenCode surface tag, and follow its standards.`
  - `ALLOWED WRITE PATHS:` exact file list. `BANNED: no delete, no rename, no new files, no edits outside the listed files, no --no-verify, no behavior changes — comment/type/strict-mode edits ONLY.`
  - The seat's confirmed findings (file:line + the C-theme + the exact transformation).
  - `comment-hygiene: drop the perishable id, KEEP the durable WHY. Do NOT delete an explanatory comment — only strip the ephemeral label.`
  - `CONFIRM-THEN-FIX: re-read each cited line; skip + log refuted if it does not match. The DO-NOT-FIX list above is binding.`
  - `VERIFY: report a per-finding fixed/refuted table; do not claim a fix you did not make.`

## Verification (orchestrator, post-merge — baseline-and-delta)
1. **Baseline first:** capture `npm run typecheck` + affected `vitest` + `check-comment-hygiene.sh` results on the clean branch BEFORE merge.
2. After merging worktree-B: re-run the WHOLE gate — `npm run typecheck` (0 errors), `check-comment-hygiene.sh` (0 violations — the C1/C4 target), `bash -n` on edited shell + strict-mode present (C2), affected vitest suites (no new failures vs baseline), tsc clean for the `any[]`→typed change (C3).
3. Report the delta vs baseline; never claim "no regressions" without the baseline numbers.
4. Diff-review worktree-B (confirm only the listed files changed, additive/surgical); scoped commit.

## Tasks
- [ ] Build the confirmed-finding briefs (filter findings-all.json to C1-C4; drop refuted clusters).
- [ ] Capture the pre-merge baseline (tsc + hygiene gate + affected tests).
- [ ] Create worktree-B; verify clean baseline.
- [ ] Dispatch ~6-8 general code-fixer seats (pool 10, staggered, sk-code-loaded).
- [ ] Collect per-seat fixed/refuted tables; salvage empties.
- [ ] Orchestrator verification: re-run the whole gate, report delta.
- [ ] Diff-review worktree-B; merge + scoped commit; rebuild dist if any compiled source changed; update implementation-summary.
