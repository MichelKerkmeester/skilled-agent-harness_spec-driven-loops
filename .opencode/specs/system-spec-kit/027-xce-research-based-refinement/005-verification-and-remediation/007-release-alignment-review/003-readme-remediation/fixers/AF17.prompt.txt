Act as a markdown documentation-fixer (seat AF17). Spec folder: /Users/michelkerkmeester/MEGA/Development/Code_Environment/Public/.opencode/specs/system-spec-kit/027-xce-research-based-refinement/005-verification-and-remediation/007-release-alignment-review/003-readme-remediation (PRE-APPROVED at Gate 3 — make the edits below directly with the Edit/Write tools; do NOT re-ask Gate 3 and do NOT ask the user anything).

You fix STALE content in README files so they match current repo reality. SURGICAL edits only — fix the cited claims, keep everything else intact. Find the correct current value by reading the cited file PLUS its SKILL.md / source / config before editing.

ALLOWED WRITE PATHS (edit ONLY these files):
- .opencode/skills/system-spec-kit/mcp_server/hooks/README.md
- .opencode/skills/system-spec-kit/mcp_server/lib/search/README.md
- .opencode/skills/system-spec-kit/mcp_server/scripts/README.md
- .opencode/skills/system-spec-kit/mcp_server/tools/README.md
- .opencode/skills/system-spec-kit/references/cli/daemon_cli_reference.md
BANNED: no delete, no rename, no new files, no edits outside the listed files, no --no-verify.

DO-NOT-FIX (binding — confirmed false positives; leave untouched):
- The CLI front-door "37 tools" figure: `spec-memory.cjs` CLI is genuinely 37; only the MCP-server/daemon surface is 39. Fix a "37" ONLY when the sentence is about the MCP server/daemon, NOT the CLI front door.
- Any `// Feature catalog:` comments and TSDoc `@example` values.
- Any cited claim that does NOT actually reproduce against the live file — mark it refuted; never invent a fix.

FINDINGS TO FIX (confirm-then-fix each; P0/P1/P2 all in scope, including cosmetic/style):
- [P2] .opencode/skills/system-spec-kit/mcp_server/hooks/README.md:60 — Hook tree omits warm CLI fallback helpers | stale: The hooks directory tree lists runtime folders and helper modules but omits the warm daemon CLI fallback helpers. -> correct: hooks/ contains spec-memory-cli-fallback.ts, code-index-cli-fallback.ts, and warm-cli-fallback-envelope.ts for bounded warm-only CLI recovery.
- [P1] .opencode/skills/system-spec-kit/mcp_server/lib/search/README.md:83 — Search README still names cross-encoder/local reranking gates | stale: *rerank*.ts contains cross-encoder, local, and score reranking gates. -> correct: The model-based cross-encoder/local-GGUF reranking layer is removed; Stage 3 now performs algorithmic MMR diversity and MPAB chunk-collapse only.
- [P1] .opencode/skills/system-spec-kit/mcp_server/scripts/README.md:27 — Resume slash command name is stale | stale: The wrapper docs say /spec_kit:resume restores packet context. -> correct: The current command surface is /speckit:resume, registered by system-spec-kit and backed by .opencode/commands/speckit/resume.md.
- [P1] .opencode/skills/system-spec-kit/mcp_server/tools/README.md:92 — Lifecycle tool routing understated | stale: lifecycle-tools.ts routes async ingest start, status and cancel calls. -> correct: lifecycle-tools.ts routes memory_index_scan, memory_index_scan_status, memory_index_scan_cancel, task learning tools, async ingest tools, eval tools, and session tools.
- [P1] .opencode/skills/system-spec-kit/references/cli/daemon_cli_reference.md:26 — Spec-memory CLI tool count is stale | stale: spec-memory.cjs exposes 37 tools; later lines also expect spec-memory=37. -> correct: spec-memory exposes 39 tools; list-tools parity tests assert 39 and the registry includes memory_index_scan_status and memory_index_scan_cancel.

After editing, re-read each edited line to verify. Your final message MUST be exactly one fenced json block:
```json
{"seat":"AF17","fixed":[{"file":"path","claim":"what was stale","after":"what it now says"}],"refuted":[{"file":"path","why":"why skipped"}]}
```
