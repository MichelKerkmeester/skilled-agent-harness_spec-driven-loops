# 027 Release-Alignment Review — Synthesis (round-1 fan-out + round-2 disposition)

**Date:** 2026-06-18
**Method:** 36 read-only gpt-5.5-fast xhigh seats (18/track), pool of 10 concurrent, staggered spawn, orchestrator-written state. 36/36 completed, 0 empty.
**Round-2:** adversarial verification of the dominant clusters against the **authoritative source docs** (comment-hygiene.md, sk-code OpenCode standards) plus representative per-finding spot-checks — more reliable than re-dispatching seats that would re-interpret the same docs.

> Raw itemized findings: `001-readmes-vs-027/review/findings-all.json` (Track A) and `002-code-vs-sk-code-opencode/review/findings-all.json` (Track B). Treat those as a **triage queue**, not a confirmed-defect list — the false-positive clusters below are tagged.

## Raw counts (round-1)

| Track | Seats ok | P0 | P1 | P2 | Total |
|-------|----------|----|----|----|-------|
| A — READMEs vs 027 | 18/18 | 4 | 122 | 39 | 165 |
| B — code vs sk-code | 18/18 | 66 | 47 | 41 | 154 |

## Round-2 disposition

### Track A — READMEs vs 027 reality: **NOT aligned — substantial real drift**
Confirmed real (representative, verified against current files):
- Stale `/speckit:resume` command spelling across multiple skill READMEs (templates, shared, hook_system).
- Embedding defaults documented as cloud-first / BGE-GGUF where the live cascade is local-first (`shared/README.md`, feature_catalog, manual_testing_playbook, local-LLM README).
- Deep-loop roster omits the **improvement** mode in several skill READMEs (deep-context/research/review/ai-council).
- Removed cross-encoder / local-rerank gates still documented as present (`mcp_server/lib/search/README.md`, SKILL.md).
- Dead cross-skill relative links in deep-loop-workflows `scripts/README.md` files.
- Advisor README/SKILL misclassifies the 9th tool / propagate trust-gating; code-graph README has wrong blast-radius fields and stale markdown-in-default-scan claims.

False-positive / nuance to triage out:
- **Tool-count findings conflate two surfaces:** the CLI front door is genuinely **37** tools (`spec-memory.cjs`), while the **MCP server** is **39**. A "37 is stale" finding against a *CLI* reference is wrong; against an *MCP* reference it is right. Verify the surface before fixing.
- A18 (repo root / AGENTS / bin / install_guides) emitted 4 P1s with empty title/line — low-confidence, re-run if needed.

### Track B — code vs sk-code OpenCode standards: **largely aligned; raw P0 count inflated by false positives**
The 66 raw P0s are noise-heavy. After round-2:

**Refuted (not defects):**
- `// Feature catalog: <name>` comments (~18-20 findings across handlers, pipeline, storage, shared) — comment-hygiene.md forbids spec paths / packet-phase numbers / ADR-REQ-CHK-task-finding ids, **not** feature-catalog category tags; the enforced gate does not flag them and they are committed.
- TSDoc `@example` values like `specFolder: 'specs/001-demo'` — illustrative API examples, not ephemeral artifact pointers.
- `scripts/dist/**` "stale compiled" — build output (gitignored, regenerated by `npm run build`); out of review scope.

**Downgraded P0 → P2 (style, not blocker):**
- "non-boxed / dashed header" findings — sk-code defines a module-header convention but the boxed-vs-dashed format is a style preference, not a correctness gate.

**Confirmed real (worth fixing):**
- **Comment-hygiene violations** — genuine ephemeral ids in comments: `// FIX RC3-B:` (memory-search.ts), `// P4-12/P4-19:` (context-server.ts), `# ...packets 006-009 ... SC-002` (verify-zombie-soak.sh), plus real spec-path mentions to audit (file-watcher.ts:201, folder-discovery.ts:1069). These slipped a narrower gate regex but violate the prose rule.
- **Missing shell strict-mode** (`set -euo pipefail`) in several scripts/hooks (worktree-guard.sh, claude-session-cleanup.sh, install-hooks.sh, partial in git-hooks) — sk-code mandates it.
- **`any[]` in a public DB type** (`write-provenance.ts:25`) — sk-code "no any" rule.
- Scattered P1 style nits (catch params not typed `unknown`, exported fns lacking TSDoc) — valid sk-code style, low priority.

## Bottom line
- **READMEs are materially out of date with 027** — a real remediation queue (mostly P1 staleness), once tool-count surface-conflation is triaged out.
- **Code is well-aligned with sk-code**; the actionable set is small: comment-hygiene ephemeral-id cleanup, shell strict-mode, one `any[]`. The 66 raw P0s overstate it ~3x due to the Feature-catalog / TSDoc-example false positives.

## Recommended next step
A scoped remediation packet for: (A) README currency sweep (surface-aware tool counts, resume-command spelling, embedding defaults, deep-loop roster, dead links); (B) the confirmed code subset (ephemeral-id comments, shell strict-mode, `any[]`). The refuted clusters should NOT be fixed.
