# Wave 1 - CODEX-A5: Memory System & MCP Handlers
Date: 2026-03-21
Agent: Codex CLI GPT-5.4 (read-only sandbox)

## Findings

FINDING-CODEX-A5-001 | HIGH | SILENT_FAILURE | .opencode/skill/system-spec-kit/scripts/memory/generate-context.ts:372 | `--stdin`/`--json` mode skips the loader’s schema validation and normalization, so the documented JSON shape can produce incomplete memories without any error. | `parseStructuredModeArguments()` only `JSON.parse`s and spreads the payload into `collectedData` with `_source: 'file'` at `generate-context.ts:372-389`; the documented JSON example still uses snake_case fields like `user_prompts`/`recent_context` at `generate-context.ts:105-133`; file-based mode does call `validateInputData()` and `normalizeInputData()` at `data-loader.ts:519-527`; `runWorkflow()` then reads camelCase fields like `collectedData.userPrompts` at `workflow.ts:1348-1351` and `workflow.ts:1637-1663`, so unnormalized snake_case input is silently dropped. | Run structured payloads through `validateInputData()` and `normalizeInputData()` before setting `collectedData`, and reject JSON-mode payloads that fail normalization instead of treating them as trusted `_source: 'file'` input.
FINDING-CODEX-A5-002 | HIGH | VALIDATION | .opencode/skill/system-spec-kit/mcp_server/handlers/v-rule-bridge.ts:44 | The new V-rule bridge is fail-open: any load failure disables V-rule enforcement and lets writes continue with only a warning. | `loadModule()` swallows all errors and logs `V-rule checks disabled` at `v-rule-bridge.ts:44-55`; the exported bridge functions then return `null` at `v-rule-bridge.ts:62-74`; `memory-save` only enforces V-rules when `vRuleResult` is truthy at `memory-save.ts:410-447`, so a broken/missing dist artifact bypasses the hard-block and write-skip-index dispositions entirely. | Fail closed for save-time validation: surface a handler error when the bridge cannot load, or eagerly verify bridge availability at startup and refuse `memory_save` until validation rules are available.
FINDING-CODEX-A5-003 | HIGH | RACE_CONDITION | .opencode/skill/system-spec-kit/mcp_server/handlers/memory-save.ts:1120 | `atomicSaveMemory()` is not safe under concurrent saves of the same file because every caller writes to the same deterministic `_pending` path with no file-level lock. | `atomicSaveMemory()` derives one shared `pendingPath` via `transactionManager.getPendingPath(file_path)` at `memory-save.ts:1120`; that helper always returns `<base>_pending<ext>` at `transaction-manager.ts:87-92`; the handler then `writeFileSync`s, deletes, and `renameSync`s that shared path across retries at `memory-save.ts:1147-1148`, `1161`, `1181`, `1204`, and `1228`, so overlapping saves can overwrite or delete each other’s pending file. | Use a unique pending filename per request/attempt (for example include `requestId`/`randomUUID()`), and add an OS-visible file lock keyed by final path before writing or promoting pending files.
FINDING-CODEX-A5-004 | HIGH | DATA_LOSS | .opencode/skill/system-spec-kit/mcp_server/handlers/memory-save.ts:571 | The normal save path rewrites the source file with plain `writeFile` after the DB commit, so a crash or competing writer can leave truncated/stale disk content while the DB already reflects the new memory state. | After indexing succeeds, the handler rewrites fixed content with `fs.promises.writeFile()` in both the chunked branch at `memory-save.ts:565-572` and the non-chunked branch at `memory-save.ts:723-727`; the only mutex is the in-process `SPEC_FOLDER_LOCKS` map at `memory-save.ts:125-141`, which does not protect cross-process writers; unlike `atomicSaveMemory()`, these paths do not use temp-file rename semantics. | Replace both post-commit rewrites with the pending-file + rename flow (or `transaction-manager.atomicWriteFile()`), and take a file-path lock rather than only a process-local spec-folder lock so DB/file state cannot diverge under concurrent saves or mid-write crashes.
FINDING-CODEX-A5-005 | MEDIUM | VALIDATION | .opencode/skill/system-spec-kit/mcp_server/handlers/quality-loop.ts:96 | The quality-loop threshold allows structurally incomplete memories to pass even when they have no trigger phrases and no anchors. | `scoreTriggerPhrases()` returns `0` for zero triggers at `quality-loop.ts:96-109`; `scoreAnchorFormat()` gives missing anchors a neutral `0.5` at `quality-loop.ts:149-152`; the weighted total is computed at `quality-loop.ts:385-398`; `runQualityLoop()` passes anything `>= 0.6` at `quality-loop.ts:562` and `586-594`. A long, headed, in-budget document with no triggers and no anchors scores exactly `0*0.25 + 0.5*0.30 + 1*0.20 + 1*0.25 = 0.60`, so it is accepted despite lacking key retrieval metadata. | Add per-dimension floors or hard-fail rules for zero trigger phrases and missing required anchors, or raise the global threshold so those combinations cannot pass accidentally.
FINDING-CODEX-A5-NNN | SEVERITY | CATEGORY | file:line | description | evidence | recommended fix
