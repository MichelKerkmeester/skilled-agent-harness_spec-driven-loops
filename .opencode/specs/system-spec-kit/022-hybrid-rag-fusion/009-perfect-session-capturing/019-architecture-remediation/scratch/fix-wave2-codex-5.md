Moved `buildSessionActivitySignal` into [session-activity-signal.ts](/Users/michelkerkmeester/MEGA/Development/Opencode%20Env/Public/.opencode/skill/system-spec-kit/scripts/lib/session-activity-signal.ts#L1), switched [folder-detector.ts](/Users/michelkerkmeester/MEGA/Development/Opencode%20Env/Public/.opencode/skill/system-spec-kit/scripts/spec-folder/folder-detector.ts#L32) to import from `../lib/session-activity-signal`, and updated the extractor surface to stay backward-compatible through [extractors/index.ts](/Users/michelkerkmeester/MEGA/Development/Opencode%20Env/Public/.opencode/skill/system-spec-kit/scripts/extractors/index.ts#L14) plus the shim at [extractors/session-activity-signal.ts](/Users/michelkerkmeester/MEGA/Development/Opencode%20Env/Public/.opencode/skill/system-spec-kit/scripts/extractors/session-activity-signal.ts#L1). That breaks the `extractors ↔ spec-folder` cycle because `spec-folder` now depends on `lib`, not `extractors`.

Verification: `rg -n "session-activity-signal|buildSessionActivitySignal" .opencode/skill/system-spec-kit/scripts` shows all remaining references resolving through either the new `lib` file or the legacy shim, and `npm run lint` in `.opencode/skill/system-spec-kit/scripts` passed. `npm run test -- tests/auto-detection-fixes.vitest.ts` still has 1 unrelated pre-existing failure in the saved-memory path (`Cannot find module '../../shared/config'` via `mcp_server/dist/core/config.js`); the `buildSessionActivitySignal` coverage in that file was not the failing case.