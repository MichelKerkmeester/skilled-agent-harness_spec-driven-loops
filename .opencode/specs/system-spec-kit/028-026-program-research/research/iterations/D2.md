Now let me gather evidence for each assigned angle in parallel.
Let me get more targeted evidence for specific angles.
Now I have enough evidence. Here are the research notes:

---

### Angle 3: Causal relation inference generalization beyond spec-document lineage
- **Framing:** The causal relation backfill can promote edges from signals other than spec-document chains (commit history, code-graph, test references), and this would materially increase coverage beyond the current ~43% ceiling.
- **Repo evidence:** `relation-backfill.ts:7-11` — "Infers typed causal edges from STRONG, EXISTING deterministic signals only — never a speculative semantic/ML heuristic." Current collectors: spec-document chains (`memory_lineage`), cached `related_memories` cosine neighbors (opt-in `similarity:true`), and structural supersession via `memory_lineage.superseded_by_memory_id` (opt-in `contradicts:true`). `relation-coverage.ts:122` — autonomous backfill hint mentions "lineage links (bounded, created_by='auto', idempotent)" and "Additional similarity-derived 'supports' and structural-supersession 'contradicts' edges are available via the OPT-IN collectors."
- **Findings:** The backfill architecture is explicitly bounded to three signal sources, all scoped to spec-document metadata. There is zero wiring to commit history, code-graph edges, or test references. The `created_by='auto'` guard (MAX_AUTO_STRENGTH=0.5, per-relation window cap) is already designed to accommodate new collectors safely. The 39.91→43.59% coverage gain came from the default-on lineage+chain collector alone; the opt-in similarity and contradicts collectors add edges but are off by default. A code-graph importer (`code_edges` where `edge_type='IMPORTS'`) or git-commit co-change signal could produce `supports` or `derived_from` edges between spec-docs that reference overlapping code, but no such collector exists.
- **Next experiment:** Write a bounded collector that reads `code_graph_query({operation:'blast_radius'})` results for spec-docs that reference code files, and emits `supports` edges between co-dependent spec-docs. Measure coverage delta and false-positive rate on a labeled 50-pair sample.
- **Verdict:** Current inference is narrow by design (spec-doc lineage only). Generalization to code-graph and commit signals is architecturally feasible (same guardrails apply) but unimplemented. The coverage ceiling without external signals is likely ~50-55%. **Recommend:** add a code-graph-derived collector as a low-risk extension.

---

### Angle 6: Reconsolidation opt-in quality delta and risk justification
- **Framing:** The empirical quality delta of reconsolidation (merge/deprecate near-duplicates) is measurable and positive, but the destructive risk justified reverting it to opt-in after an in-range attempt to default it on.
- **Repo evidence:** `search-flags.ts:140-144` — `isOptInEnabled('SPECKIT_RECONSOLIDATION_ENABLED')` (default OFF). `review-report.md:61` — "Reverted to opt-in — `isSaveReconsolidationEnabled()` → `isOptInEnabled('SPECKIT_RECONSOLIDATION_ENABLED')`." Commit `0060a097b3` flipped three gates default-on; commit `a2d1a9bc9e` reverted reconsolidation only. `feature_catalog.md:338` — similarity ≥0.88 triggers merge (content combined, `importance_weight` boosted); 0.75–0.88 deprecates older row + creates `supersedes` edge. `review-report.md:25` — "the destructive path is gated on a 'pre-reconsolidation' checkpoint existing for the spec folder — no checkpoint → skipped." `review-report.md:78-79` — post-deploy incident: "concurrent cross-process writers violated single-writer discipline and corrupted overflow pages" during a one-time enrichment backfill (not reconsolidation itself, but the same destructive-merge risk surface).
- **Findings:** Reconsolidation was reverted because it gates a destructive merge/deprecate path that can permanently alter rows. The review found that even with checkpoint gating, the risk of a concurrent supersede racing a deferred enrichment (finding B1) meant default-on was premature. The quality delta is real: without reconsolidation, near-duplicate spec-docs accumulate (similarity ≥0.88 pairs stay separate), degrading search precision. The assistive reconsolidation layer (`SPECKIT_ASSISTIVE_RECONSOLIDATION`, default ON) provides a safe middle ground — it classifies and logs recommendations without destructive action. The empirical quality gap: no measured retrieval-precision delta exists between reconsolidation-on vs off.
- **Next experiment:** Run a controlled A/B: index 100 spec-docs with reconsolidation OFF, measure search recall@10 on 20 natural-language queries; repeat with ON. Compare duplicate-pair count and retrieval precision.
- **Verdict:** The opt-in decision was correct — destructive merge requires explicit intent. The assistive layer provides partial value (classification + logging). The unmeasured quality gap is the real open question. **Recommend:** measure retrieval precision delta before reconsidering the default.

---

### Angle 18: Generalize transparent daemon-child recycle to mk-code-index and mk-skill-advisor
- **Framing:** The transparent daemon-child recycle pattern (front-proxy + session proxy) can be generalized from mk-spec-memory to mk-code-index and mk-skill-advisor, but the design cost and risk differ because those servers exit on child SIGTERM rather than recycling in place.
- **Repo evidence:** `mk-spec-memory-launcher.cjs:15,203-210` — `createSessionProxy` imported and used; `:1053` — `recycleDaemonInPlace()` sends SIGTERM to context-server child, waits, respawns. `mk-code-index-launcher.cjs:965 lines, 0 occurrences of sessionProxy/createSessionProxy/recycle/transparent` — confirmed by `iteration-015.md:62`: "code-index/advisor have NO transparent-recycle workflow (grep for `recycle|SIGTERM child|transparent`: 0 matches)." `iteration-015.md:75` — "The proxy only adds recycle-survival, needed solely by spec-memory's transparent dist-recycle workflow." `iteration-002.md:86,92` — code-index HAS owner-lease but lacks sessionProxy; advisor has NO owner-lease. `deploy-mcp.sh` behavior: `--recycle` only recycles mk-spec-memory; code-graph and advisor are "not SIGTERM-recycled (their launchers exit, so fresh dist loads on next start)."
- **Findings:** The asymmetry is intentional and reviewed as correct. mk-spec-memory needs transparent recycle because it's a shared long-lived daemon (one per user, many sessions). mk-code-index and mk-skill-advisor are per-session processes — they start fresh for each session and exit when done, so a "recycle" is just "kill and let next session spawn fresh." Adding a session-proxy to code-index/advisor would only help if they too became shared daemons, which is not the current architecture. The design risk is real: code-index has 965 lines, advisor 700 lines vs spec-memory's 1470 — the proxy adds ~300 lines of reconnect/replay logic. The real generalization question is: should code-index/advisor become shared daemons? That's a different architectural decision.
- **Next experiment:** Measure code-index cold-start latency (spawn + index scan). If >2s, consider a warm-pool or shared-daemon mode; the session-proxy would then be a prerequisite. If <500ms, the current per-session model is fine.
- **Verdict:** The session-proxy gap is a feature asymmetry, not a defect. Generalizing the recycle pattern requires first generalizing to shared-daemon architecture, which is a prerequisite decision. **Recommend:** measure cold-start before investing. LOW CONFIDENCE on whether shared-daemon mode is worth it for code-index/advisor.

---

### Angle 24: CocoIndex decoupling — capability loss and re-addition value
- **Framing:** Decoupling CocoIndex from the code-graph lost specific capabilities (Python-based indexing, cross-encoder reranking, semantic search) that are either replaced, abandoned, or worth re-adding.
- **Repo evidence:** `changelog-002-001-touchpoint-research.md:29` — "D1 accepts the loss of opt-in cross-encoder reranking in memory (default path unaffected, positional fallback). D2 adopts a HYBRID search policy (Grep plus code-graph structural) as the post-coco semantic-search replacement." `changelog-002-013-post-deprecation-deep-review.md:27` — post-deprecation review found 7 residual items (1 P0: GEMINI.md routing to deleted `mcp__cocoindex_code__search`; 2 P1: memory:manage still declares removed `ccc` subcommands). `changelog-002-deprecate-coco-index-root.md` — 7 child phases from touchpoint research through residue cleanup. `027-xce-research-based-refinement/004-causal-edge-tombstones/spec.md:61-67` — CocoIndex's Rust core has lifecycle invariants (tombstones, child existence state, generation counters) being adapted into spec-kit-memory. `027/006-write-path-reconciliation/spec.md:62-67` — CocoIndex's `statediff.py` (DiffAction = insert/upsert/replace/delete) being ported as a storage reconciliation layer.
- **Findings:** Three capability classes were lost: (1) **Cross-encoder reranking** — opt-in in memory, positional fallback exists, explicitly accepted as a loss in D1. No measured retrieval-precision impact documented. (2) **Python-based incremental indexing** — CocoIndex's Rust/Python indexer had memoization, stable-path keys, and component-level cache-hit guards. These ideas are being re-imported into spec-kit-memory via 027 packets (tombstones, statediff, memoization) — so the capability isn't lost, it's being rebuilt in TypeScript. (3) **Semantic search** — replaced by HYBRID policy (Grep + code-graph structural), which is a deliberate simplification. The post-deprecation deep review found residue (P0/P1 items) but no behavioral regressions.
- **Next experiment:** Run the same 20-query retrieval benchmark against (a) the current HYBRID search, (b) a hypothetical code-graph + cosine-similarity hybrid. Measure recall@10 delta.
- **Verdict:** No material capability was permanently lost. Cross-encoder reranking was a marginal opt-in feature. Python indexer patterns are being re-imported. Semantic search was replaced by a deliberate HYBRID policy. The real cost was the 7-phase deprecation effort (touchpoint research + residue cleanup), not capability loss. **Recommend:** no re-addition needed; continue the 027 pattern import for incremental indexing.

---

### Angle 26: Multi-file union queries — usage and value analysis
- **Framing:** `unionMode:'multi'` and `hotFileBreadcrumb` are first-class features in the code-graph query handler, but their in-the-wild usage and value are unmeasured.
- **Repo evidence:** `query.ts:35` — `unionMode?: 'single' | 'multi'` on the query input type. `query.ts:1300-1302` — `const rawSubjects = args.unionMode === 'multi' ? [subject, ...(args.subjects ?? [])] : [subject]` — multi mode unions the primary subject with additional subjects. `query.ts:932-962` — `buildHotFileBreadcrumbs()` queries file degrees, identifies the top 10% highest-degree files (threshold min 20), and emits `changeCarefullyReason` advisories. `tool-schemas.ts:54` — schema exposes `unionMode` as enum `['single', 'multi']`. `changelog-004-001-code-graph-runtime-upgrades.md:30-31` — "Explicit `unionMode: 'multi'` merges multiple source files without duplicating results" and "High-degree files now surface an advisory `hotFileBreadcrumb` warning." Test coverage: `code-graph-query-handler.vitest.ts:1004` exercises `unionMode: 'multi'` with assertions on `hotFileBreadcrumbs`. Playbook `022-code-graph-query-blast-radius.md` explicitly tests multi-subject union and transitive expansion.
- **Findings:** The features are well-implemented and tested, but the tool schema describes `unionMode` as blast-radius-specific ("Blast-radius subject handling mode; use multi to union subject + subjects"). There is no telemetry or usage counter to measure how often `unionMode:'multi'` is actually called by real agents. The `hotFileBreadcrumb` is advisory-only (no authority score, no behavioral gate) — it's a signal, not a decision. The design rationale from the code-graph upgrades changelog is sound: multi-file work was previously relying on implicit merging, and hot-file breadcrumbs help operators avoid rippling changes through high-degree files. But without usage data, the value is asserted, not measured.
- **Next experiment:** Add a telemetry counter to `code_graph_query` for `unionMode:'multi'` invocations and `hotFileBreadcrumb` emissions. Run for 2 weeks of normal agent usage. Report call count, unique subject pairs, and whether agents that see `hotFileBreadcrumb` actually change behavior.
- **Verdict:** Well-designed features with solid test coverage but zero empirical usage data. The hot-file breadcrumb is a pure advisory with no behavioral gate — it can only help, never hurt. Union mode's value depends on multi-file blast-radius queries being common, which is plausible but unproven. **Recommend:** add lightweight telemetry before declaring value-proven.
