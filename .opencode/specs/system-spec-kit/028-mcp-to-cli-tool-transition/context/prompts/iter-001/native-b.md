# Seat Prompt: native-b

Gather-subject: MCP-to-CLI transition context for `system-code-graph`, `system-skill-advisor`, and `system-spec-kit`.

Shared focus: stable tool IDs, ownership boundaries, schema registries, dispatcher seams, plugin/hook bridge patterns, and false-safe readiness invariants.

Known context: target packet is `specs/system-spec-kit/028-mcp-to-cli-tool-transition`; seats are read-only and return structured findings only.

Output schema: JSON with `findings[]` using kinds `reuse_candidate`, `integration_point`, `convention`, `dependency`, or `gap`.
