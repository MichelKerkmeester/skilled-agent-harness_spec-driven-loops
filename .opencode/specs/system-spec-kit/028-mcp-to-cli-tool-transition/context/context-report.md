# Context Report: MCP to CLI Tool Transition

> Generated by `deep-context` recovery. Ships verified pointers, not source bodies; open the cited `file:symbol` to read code just-in-time. Findings carry confidence from native-seat agreement and freshness labels.

**Scope**: `.opencode/skills/system-code-graph`, `.opencode/skills/system-skill-advisor`, `.opencode/skills/system-spec-kit` for `specs/system-spec-kit/028-mcp-to-cli-tool-transition`  
**Executors**: native-a + native-b  
**Iterations**: 1  
**Stop**: `recovery_synthesized` after native-only sweep; not a full default heterogeneous CLI pool run

## 1. REUSE Catalog (highest value)

| id | symbol (`file:line`) | signature | reuse | confidence | agree | freshness | notes |
|----|----------------------|-----------|-------|------------|-------|-----------|-------|
| reuse-001 | `CODE_GRAPH_TOOL_SCHEMAS` (`.opencode/skills/system-code-graph/mcp_server/tool-schemas.ts:186`) | `export const CODE_GRAPH_TOOL_SCHEMAS: ToolDefinition[]` | import | 0.98 | 2/2 | direct-read verified | Canonical Code Graph tool manifest; preserve the stable `code_graph_*` and `detect_changes` vocabulary for CLI parity. |
| reuse-002 | `TOOL_DEFINITIONS` (`.opencode/skills/system-skill-advisor/mcp_server/tools/index.ts:37`) | `export const TOOL_DEFINITIONS: ToolDefinition[]` | import | 0.97 | 2/2 | direct-read verified | Canonical Skill Advisor and Skill Graph registry for `advisor_*` and `skill_graph_*` CLI parity. |

## 2. Integration Points

- **[HARD]** `shouldBlockReadPath` (`.opencode/skills/system-code-graph/mcp_server/handlers/query.ts:865`) — CLI reads must preserve the false-safe block when graph freshness is not fresh or the verification gate fails.
- **[HARD]** `handleTool` (`.opencode/skills/system-code-graph/mcp_server/tools/code-graph-tools.ts:62`) — route CLI-parsed Code Graph calls through this dispatcher or keep behavior equivalent to its schema validation and handler switch.
- **[HARD]** `TOOL_DEFINITIONS` (`.opencode/skills/system-spec-kit/mcp_server/tool-schemas.ts:645`) — Spec Kit records Code Graph schema ownership as moved out while IDs stay unchanged; do not reacquire Code Graph/Skill Graph registries in `mk-spec-memory`.
- **[soft]** `AdvisorRecommendInputSchema` (`.opencode/skills/system-skill-advisor/mcp_server/schemas/advisor-tool-schemas.ts:135`) — CLI parsing should preserve strict public input shape and avoid exposing internal affordance scoring.

## 3. Touch List

1. `.opencode/skills/system-code-graph/mcp_server/tool-schemas.ts` — source of stable Code Graph tool inventory for CLI manifest generation.
2. `.opencode/skills/system-code-graph/mcp_server/tools/code-graph-tools.ts` — reuse or mirror dispatch/validation behavior for CLI calls.
3. `.opencode/skills/system-skill-advisor/mcp_server/tools/index.ts` — source of stable Skill Advisor and Skill Graph tool inventory.
4. `.opencode/skills/system-skill-advisor/mcp_server/plugin_bridges/mk-skill-advisor-bridge.mjs` — safe subprocess/timeout/fail-open bridge precedent.
5. `.opencode/skills/system-spec-kit/mcp_server/lib/code-graph-boundary.ts` — caller-side process-boundary seam for Code Graph tool calls.
6. `.opencode/skills/system-spec-kit/mcp_server/tool-schemas.ts` — ownership-boundary documentation and memory-tool registry.

## 4. Conventions

- **Stable runtime shims**: `.opencode/skills/system-spec-kit/mcp_server/hooks/claude/user-prompt-submit.ts:5` keeps old runtime settings while delegating implementation to `system-skill-advisor`.
- **Subprocess bridge with timeout and cleanup**: `.opencode/skills/system-skill-advisor/mcp_server/plugin_bridges/mk-skill-advisor-bridge.mjs:234` calls `mk_skill_advisor` through a bounded MCP subprocess and parses the response envelope.
- **Direct-import bridge is not the pattern**: `.opencode/skills/system-code-graph/mcp_server/plugin_bridges/mk-code-graph-bridge.mjs:33` initializes runtime internals directly; the changelog records why that class was reverted.

## 5. Pruned Dependency Subgraph

- `system-spec-kit/mcp_server/lib/code-graph-boundary.ts:callCodeGraphTool -> mk-code-index launcher` — Spec Kit caller boundary invokes Code Graph by stable tool name.
- `system-skill-advisor/mcp_server/tools/index.ts:TOOL_DEFINITIONS -> skill-graph-tools` — Advisor registry includes skill graph tools through the standalone advisor package.
- `system-spec-kit/mcp_server/tool-schemas.ts:TOOL_DEFINITIONS -/-> code graph schemas` — Code Graph schemas are intentionally absent from Spec Kit Memory registry after extraction.

## 6. Prior Art / Decisions

- **2026-06-07** Code Graph skill rule: stable tool IDs remain `code_graph_*` and `detect_changes`; live MCP namespace is `mk-code-index` (`.opencode/skills/system-code-graph/SKILL.md:321`).
- **2026-06-07** Skill Advisor skill rule: stable public IDs are `advisor_*` and `skill_graph_*`; namespace supplies the boundary (`.opencode/skills/system-skill-advisor/SKILL.md:276`).
- **2026-06-07** Code Graph OpenCode bridge precedent: a direct-import fix was reverted because it bypassed the daemon single-writer lease (`.opencode/skills/system-spec-kit/changelog/v3.5.0.1.md:35`).

## 7. Gaps & Unknowns

- `buildSkillGraphTopologySummary` (`.opencode/skills/system-spec-kit/mcp_server/handlers/session-bootstrap.ts:221`) — bootstrap no longer proxies skill graph topology through `mk-spec-memory`; migration plans must call `mk_skill_advisor` directly for topology.
- `ensureRuntimeReady` (`.opencode/skills/system-code-graph/mcp_server/plugin_bridges/mk-code-graph-bridge.mjs:33`) — this bridge is unsafe as a migration template; use IPC/launcher-backed transport instead.
- **Verify before full planning**: repair or refresh the Code Graph gold-query battery; current verification points at old paths such as `.opencode/skills/system-spec-kit/mcp_server/code_graph/...`, which blocks graph-backed outline queries despite a completed broad reindex.
- **Not run**: default CLI seats (`mimo`, `gpt`, `deepseek`) were not executed in this recovery packet.

---

## Methodology & Confidence

- Per-executor attribution: `native-a`, `native-b`; agreement threshold 2; relevance gate 0.55.
- Reducer output: 12 deduped findings, 12 gated findings, 9 agreement-eligible, 0 low-confidence, 0 contradictions.
- Convergence signals at stop: sliceCoverage 1.00, reuseCatalogCoverage 1.00, agreementRate 0.83 in state log / 0.75 reducer metric, relevanceFloor 1.00, dependencyCompleteness 1.00.
- Stop decision: `recovery_synthesized`. This is a corrected artifact recovery, not proof that the original command loop completed normally.
- Freshness: file pointers are direct-read verified. Code Graph structural verification is blocked until the stale gold-query battery is corrected.
