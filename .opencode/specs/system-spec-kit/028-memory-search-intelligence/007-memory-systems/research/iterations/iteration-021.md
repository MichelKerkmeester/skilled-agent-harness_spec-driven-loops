# Iteration 21 (Opus lineage — NATIVE): Graphiti resolve_extracted_edge dedup + verify iter-15 invalidation

> Model: **Opus 4.8 NATIVE** (read-only). Orchestrator-written. newInfoRatio **0.5**. Task A mines Graphiti's edge-resolution; Task B verifies the two iter-15 invalidation candidates against live internal code (both **GO**). Path note: `temporal-edges.ts` lives at `lib/graph/`, not `lib/storage/`.

## Task A — Graphiti resolve_extracted_edge
- **GR-semantic-fact-dedup-merge → NET-NEW (H leverage / H effort — Wave-2).** LLM-judged duplicate collapse over *hybrid-search-retrieved related edges* (beyond exact composite-key): exact fast-path, then candidate facts → `resolve_edge` LLM → `duplicate_facts` indices → reuse existing edge identity (append provenance) vs inserting a near-dup. `edge_operations.py:684-749`; candidates from `EDGE_HYBRID_SEARCH_RRF` (:392-405). Maps to `causal-edges.ts:348-359,441-452` — dedups ONLY on exact `(source_id,target_id,relation,source_anchor,target_anchor)`; **0 grep hits for semantic/fuzzy/embedding** across the insert path. CAVEAT: high friction — memory-ID graph, no LLM-in-insert-path, no per-edge embeddings; insertEdge is a synchronous SQLite txn. Defer to a consolidation-time pass; **feeds the "semantic edge layer" initiative.**
- **GR-fact-text-reconciliation → NO-TRANSFER.** Provenance accrual (append episode UUID to surviving edge) is meaningless without an episode model; the attribute-merge half is already approximated by our COALESCE/strength update. `edge_operations.py:751-809`.
- **Plain edge dedup → REFUTED** (already stated): our superset composite key (`causal-edges.ts:348-359`) strictly supersets Graphiti's exact fast-path key (`edge_operations.py:686-691`).

## Task B — verify iter-15 (both GO)
- **GR-temporal-ordering-invalidation → GO** (confirmed additive). `contradiction-detection.ts:38-42` (CONFLICTING_RELATIONS) + `:95-123` (detection loop has ONLY `supersedes` + `relationsConflict` — **no valid_at comparison**); `temporal-edges.ts:111-145` exposes valid_at but nothing consumes ordering. Graphiti template: `edge_operations.py:564-571` (`edge_valid_at < resolved_valid_at ⇒ invalid_at = resolved.valid_at`). **Refinement:** scope the rule to *conflicting/superseding relation pairs* (Graphiti gates on LLM-flagged contradiction candidates), NOT pure timestamp ordering on every same-pair edge — else you'd invalidate co-valid non-conflicting edges. Cheap (valid_at columns already exist).
- **GR-semantic-invalidation-discovery → GO** (confirmed net-new). `contradiction-detection.ts:85-93` queries strictly same `source_id + target_id`; no cross-pair / semantic discovery (0 grep hits). Graphiti gathers `edge_invalidation_candidates` via `EDGE_HYBRID_SEARCH_RRF` across the graph (`edge_operations.py:407-418`), endpoint-agnostic. Same caveat as GR-semantic-fact-dedup-merge — needs a semantic/embedding edge layer → **pairs into the "semantic edge layer" initiative.**

## Net effect
Both iter-15 invalidation candidates hold as GO (with the relation-pair scoping refinement on the temporal-ordering rule). Graphiti's edge resolution adds one Wave-2 NET-NEW (semantic-fact-dedup-merge) and one NO-TRANSFER. The recurring theme: every Graphiti edge-intelligence delta (semantic dedup, cross-pair invalidation, fact-embedding) requires the **same underlying "semantic edge layer"** (per-edge embeddings + semantic edge retrieval) our memory-ID graph lacks — so they consolidate into one high-effort initiative rather than separate ships.

## Next Focus
Consolidate GR-semantic-fact-dedup-merge + GR-semantic-invalidation-discovery + GR-fact-embedding-on-edge + CG-edge-vector-index + CG-edge-aware-triplet-search into the single "semantic edge layer" Wave-2 initiative for synthesis/06.
