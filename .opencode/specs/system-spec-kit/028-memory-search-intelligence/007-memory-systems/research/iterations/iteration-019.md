# Iteration 19 (DeepSeek lineage): Cognee ECL ingest + graph_completion retriever depth (Q5)

> Model: **DeepSeek v4 Pro** (read-only, via `opencode run`, explicit-path access — worked). Orchestrator-written. newInfoRatio **0.7**. The Cognee ECL/ingest vein was NOT saturated — 6 candidates, several net-new + a cheap win. Two feed the new **"semantic edge layer"** Wave-2 initiative.

## Candidates
1. **CG-declarative-regex-entity-config → NET-NEW (L/S — cheap win).** JSON-driven entity-extraction patterns (10 built-in types, configurable regex + metadata) loadable without code changes. `regex_entity_config.json:1-62`; `regex_entity_extractor.py:1-80`. Maps to `lib/extraction/entity-extractor.ts:82-121` (hard-coded 5 regex rules). A JSON config layer makes entity types extensible without code edits. **Best lev/eff in this wave.**
2. **CG-content-hash-reprocessing-trigger → NET-NEW (M/M — needs-verify).** On re-ingestion, compares content hashes and *resets pipeline_status* → triggers downstream re-extraction/re-embedding, vs simple dedup-skip. `ingest_data.py:109-112,155-175`. Maps to `handlers/save/dedup.ts` (skip-if-unchanged, but no auto-reset of derived artifacts on change). VERIFY against the reindex path (the memory-index scan may already re-derive on change).
3. **CG-global-context-summary-hierarchy → NET-NEW (M/M).** Two-tier persistent hierarchical summary (root "world summary" + top-k subsections), prepended as a grounding prelude *before* retrieved graph context. `global_context.py:1-73`; `graph_completion_retriever.py:78-79`. Maps to — our summary stage1 channel is flat (single collection); no hierarchical root+sub index, no pre-fetch-and-prepend. Extends the summary-channel work with hierarchy.
4. **CG-edge-vector-index → NET-NEW (M/M) — feeds "semantic edge layer".** Edge relationship names stored in a dedicated vector collection (`EdgeType_relationship_name`), searched alongside node collections, contributing to triplet scoring via separate edge distances. `brute_force_triplet_search.py:270`; `node_edge_vector_search.py:12,131-145`. Maps to `lib/storage/causal-edges.ts` (SQLite only, no edge vector index, edges never inform ranking).
5. **CG-edge-aware-triplet-search → EXTENDS community-detection (H/L) — feeds "semantic edge layer".** Query across 5+ vector collections (entities/summaries/chunks/entity-types/edge-relations); project subgraph; score triplets combining node AND edge vector distances + topology. `brute_force_triplet_search.py:263-287`; `CogneeGraph.py:469-511`. EXTENDS (needs edge vectors + subgraph projection + triplet scoring).
6. **CG-graph-neighborhood-projection → EXTENDS CG-neighborhood-rescore-ranking (H/L — needs-verify).** Query-time k-hop BFS from vector-matched seeds, re-score expansion nodes via ID-filtered vector search so they join ranking. `brute_force_triplet_search.py:67-72,185-199`. DeepSeek claimed "no seam," but internal `enableCausalBoost` (2-hop causal expansion) + the banked `CG-neighborhood-rescore-ranking` likely cover part of this — **verify before treating as net-new.**

## Net effect
The Cognee ingest/retrieval surface yielded real net-new (vein not saturated): one cheap win (declarative-regex-entity-config, L/S), one summary-hierarchy extension, and two candidates (edge-vector-index + edge-aware-triplet-search) that — with iter-21's findings — form the coherent Wave-2 **"semantic edge layer"** initiative (per-edge embeddings → semantic dedup + cross-pair invalidation + triplet retrieval). content-hash-reprocessing + graph-neighborhood-projection need verify against existing internal reindex/causal-boost.

## Next Focus
Verify CG-content-hash-reprocessing-trigger + CG-graph-neighborhood-projection vs the reindex/enableCausalBoost paths. Group edge-vector-index + edge-aware-triplet-search into the semantic-edge-layer Wave-2 initiative.
