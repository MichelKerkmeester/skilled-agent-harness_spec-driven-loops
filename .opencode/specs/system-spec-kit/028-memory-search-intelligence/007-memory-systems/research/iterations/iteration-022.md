# Iteration 22 (Opus lineage — NATIVE): blast-radius scoping of the top-3 GOs

> Model: **Opus 4.8 NATIVE** (read-only). Orchestrator-written. newInfoRatio **0.5** (no new candidate — deflates optimistic finder lev/eff against real call-site reality, iter-10-style). **Materially corrects two top-7 effort estimates.**

## Scoped (size against actual seams + callers)
1. **CG-iterative-context-extension → SIZE M — SMALLEST/SAFEST SHIP.** Seam: a new `case 'deep-iterative'` in the `executeStrategy` switch (`memory-context.ts:1298-1311`) + `executeDeepIterativeStrategy()` modeled on `executeDeepStrategy` (:992-1020) + a `CONTEXT_MODES` key (:914). **Purely additive** — `CONTEXT_MODES` is a Record, `default` already falls through to focused, so **zero edits to existing strategies**; single dispatch site (`:1684`), opt-in routing. Reuse: `buildDeepQueryVariants`/stage1 already does one round of answer-style expansion; hyde.ts/query-expander.ts for next-query gen. **NO convergence/saturation primitive exists** (0 grep hits) → the one net-new algorithm. Trap: unbounded/non-converging loop cost (budget enforcer trims the *final* payload only, no per-iteration guard) → ship with a hard iteration cap + real stop condition, flag default-off. **Ready-to-roadmap as M.**
2. **CG-agentic-tool-loop → SIZE L (CORRECTION: not the H/L "best lev/eff" of iters 11/17).** Same clean new-`case` seam, BUT a ReAct loop injects an LLM into the **synchronous deterministic** retrieval hot path (search is pure better-sqlite3, `hybrid-search.ts:1365-1366`), adding model latency + cost + non-determinism. **No agent-loop step/cost governor exists anywhere** — controller, step cap, cost ceiling, stop condition are all greenfield. Trap: unbounded loop + cost/latency explosion + non-determinism regressing every caller if routing leaks. **Needs its own design packet — NOT a slot-in.** (Mitigant: iter-20's `LT-tool-rule-memory-chain` is a ready loop-governor template.)
3. **MEM-fused-summary-channel → SIZE L (CORRECTION: not M/M).** NOT single-site: touches (1) the RRF `lists.push` site (`hybrid-search.ts:~1394-1495`); (2) the `ChannelName` union + **5 hardcoded channel-list sites** (`query-router.ts:36/68/74/106-107`, `routing-telemetry.ts:17`, `hybrid-search.ts:1310/951`); (3) the adaptive-weight model (`artifact-routing.ts`) — collapsed buckets `{semanticWeight,keywordWeight,graphWeight,recencyWeight}` with **no per-channel slot** for community; (4) retiring the 2 existing inject paths (community fallback `memory-search.ts:1158-1228`, summary stage1 `:1304-1326`) to avoid double-counting. Strong data-side reuse (summary embedding + `searchCommunities` already exist). Trap: double-counting + weight-model mismatch + an **ablation-tuned retune obligation** (inline weights are ablation-derived). **Roadmap-worthy but needs a baseline-and-delta retune plan.**

## SMALLEST-SHIP
**CG-iterative-context-extension** — the only one of the three that is a one-site, flag-gated, fully-additive change with a single contained net-new algorithm (convergence-stop) and a fully-bounded trap (iteration cap).

## Net effect (roadmap correction)
Blast-radius scoping deflated the top-7's optimism: `CG-agentic-tool-loop` is **L effort** (greenfield loop+LLM governance), not the H/L "best lev/eff" the finders implied; `MEM-fused-summary-channel` is **L** (cross-cutting fusion + retune), not M/M. The genuinely smallest safe ships remain `MEM-fact-invalidation-event-time` (H/S, iter-10) and `CG-iterative-context-extension` (M, this iter). This is why each GO needs a blast-radius pass before it's truly roadmap-ready — finder lev/eff is optimistic until checked against call sites.

## Next Focus
Update the consolidated roadmap (research.md) with the corrected sizes. Remaining: blast-radius the other GOs (MEM-tiered-recall-budget, the two new initiatives) if continuing; else synthesis/06.
