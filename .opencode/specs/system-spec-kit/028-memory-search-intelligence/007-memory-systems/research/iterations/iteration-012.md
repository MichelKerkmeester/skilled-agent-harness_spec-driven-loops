# Iteration 12 (MiMo lineage): Letta tiers/budgeting + Cognee summary pipeline

> Model: **MiMo v2.5 Pro** (`xiaomi/mimo-v2.5-pro --variant high`, 1M ctx, read-only, via `opencode run`). Orchestrator-written. newInfoRatio **0.6**. 5 candidates — 2 NET-NEW, 2 EXTENDS, 1 dup. MiMo independently noticed our internal `tier-classifier.ts` already mirrors Letta (`summaryFallbackLength=150`) — a good "partially-borrowed-already" honesty signal.

## Candidates
1. **CG-iterative-retrieval-extension → DUP of banked CG-context-extension-convergence.** Same Cognee retriever (`graph_completion_context_extension_retriever.py:92-129`, QueryState.check_convergence). MiMo re-found the iter-8 candidate. Not new — re-confirm only. (Cross-cut payoff: iter-14 maps this to **deep-loop**, see below.)
2. **LT-per-component-recall-budget → NET-NEW (H/M).** Letta's `ContextWindowCalculator` breaks the window into 9 independently-budgeted sections (system_prompt, core_memory, memory_filesystem, tool_rules, external_memory_summary, summary_memory, messages, functions, directories) with per-section async token counts → targeted eviction (trim core_memory first). `context_window_calculator.py:319-350,354-384`. Maps to `lib/cognitive/pressure-monitor.ts` (single ratio, no breakdown) + `memory-context.ts` `assembleResult` (flat token estimate). **NET-NEW: no per-section budget accounting.**
3. **CG-summary-retrieval-channel → NET-NEW (M/M) — CONVERGES with GR-community-search-channel.** Cognee maintains a dedicated `TextSummary_text` vector collection populated at ingest, searched directly as a fast-path. `summaries_retriever.py:55-57`; `tasks/summarization/summarize_text.py:68-81`. Maps to `memory-search.ts` (channels = vector/bm25/fts5/graph/trigger — no summary channel). **⚠ Convergence:** iter-13 confirmed the internal system DOES compute `community_summaries` but only **injects them as a weak-result fallback** (`memory-search.ts:1158`), never as a fused channel. So this candidate + GR-community-search-channel + iter-13's finding all point at the SAME consolidated gap: **summaries exist but are not a first-class fused RRF channel.**
4. **LT-tiered-compression-in-recall → EXTENDS LT-sliding-window-keep (M/S).** Per-tier content density: HOT/WARM full text, COLD truncated summary (150 chars), DORMANT/ARCHIVED metadata-only — applied at assembly. `summarizer/tier-classifier.py:60-71`. **Internal `lib/cognitive/tier-classifier.ts:60-71 already has TIER_CONFIG (maxHot=5/maxWarm=10) + summaryFallbackLength=150 — but for result COUNTS, not per-result content density.** EXTENDS: add tier-driven per-result truncation in recall assembly.
5. **LT-eviction-percentage-ratchet → EXTENDS LT-compaction-fallback-ladder (M/S).** Progressive eviction ratchet: start 30%, count post-compaction tokens, +10% and retry until under goal, scanning backward to a safe assistant-message boundary. `summarizer_sliding_window.py:148-191`. EXTENDS the banked fallback ladder with progressive sizing.

## Net effect
2 NET-NEW (per-component-recall-budget = strong; summary-retrieval-channel = converges with the community finding), 2 EXTENDS of banked Letta candidates, 1 dup. The Letta tiering candidates land on an internal cognitive layer that **already borrows from MemGPT** — so they're refinements (per-result density, progressive sizing, per-section budgets) on an existing tier system, not greenfield.

## Next Focus
Consolidate CG-summary-retrieval-channel + GR-community-search-channel into one "fused summary/community channel" candidate. Verify LT-per-component-recall-budget against pressure-monitor.ts.
