# 007 Dark Flag Graduation: Benchmark and Test Status

Single-pane record for the dark-flag-graduation suite. One row per child benchmark phase. This is a supplementary record, not a canonical spec doc.

> **STATUS: COMPLETE.** All eight benchmark phases ran against the real corpus or graph on the production path and returned verdicts. No production default was flipped inside this program. Each graduate or refine recommendation is evidence for a separate flip decision, not the flip itself.

## The truncation-law finding, resolved

The 001 benchmark settled the long-standing question. The documented claim that the search appends are held off because the production route truncates to a three-result floor is a **myth in its stated form**. The data shows three facts. First, `DEFAULT_MIN_RESULTS = 3` is a never-cut-below-three minimum, not a cap, and the production path returns the full requested limit of ten. Second, the real reason the appends never reach the reader on the production path is structural, not budgetary: Stage-1 sets `stopAfterFusion`, which skips the enrich branch where the append stages live, so flipping the flags is a confirmed no-op on prod. Third, on the legacy path where the appends do run, token-budget truncation strips the below-baseline appended rows first. The recall opportunity is real (multi-target queries under-recall at 0.4375), so the fix is a structural rewire, not a result-budget change.

## Benchmark and verdict table

| Phase | Dark flag(s) | Benchmark result | Verdict |
|-------|--------------|------------------|---------|
| 001 multihop-tail-appends | `SPECKIT_DETERMINISTIC_MULTIHOP`, `SPECKIT_LANE_CHAMPION_BACKFILL` | completeRecall@K off vs on, prod executePipeline path, 8 labeled multi-target queries. Diagnosis: appends never ran on prod (Stage-1 stopAfterFusion skipped enrichFusedResults), three-result floor is a minimum not a cap, truncation-myth busted. Rewire: post-Stage-4 tail-append stage gated behind the flags, extends the capped baseline past the limit so appended rows are exempt from the final cap. After rewire completeRecall@20 0.5625 to 0.9375 and @12 0.5625 to 0.625 (84 rows appended), @3/@5/@8 flat at 0.4375 (never evicts a baseline hit), zero variance. Flag-off is a proven strict no-op, tsc clean, 25 pipeline+flag and 10 additive-tail-recall tests pass. | **GRADUATE for deep-K readers** (no-op for shallow-K), flags ship behind their default-off, the flip is a separate decision | default OFF, flag-off byte-identical (strict no-op proven across 8 queries x 3 repeats) |
| 002 retrieval-class-weights | `SPECKIT_RETRIEVAL_CLASS_ROUTING` | single-hop precision@1 0.90 to 0.80 (delta -0.10), multi-hop recall 0.75 unchanged, suppression hurts and the cost it guards does not exist | **CUT** unless made density-aware |
| 003 true-citation-ledger | `SPECKIT_TRUE_CITATION_EMITTER` | after refinement: sessionId now threads into the shown write and the detector keys on the memory title anchor (coverage 0.0724 to 0.1579, 9 prose-count false positives suppressed), the emitter is sound. Live density is still zero today because the 1711 existing shown rows are null-session and predate the fix | **REFINE** sound now, graduation gated on session-carrying traffic accumulating, not on more code |
| 004 save-reconsolidation | `SPECKIT_RECONSOLIDATION_ENABLED` | merge precision 0.017 (32 true dupes vs 1816 distinct false positives), no threshold rescues it, gate machinery sound 12/12 | **CUT** replace cosine-band with a content-hash exact-dup merge |
| 005 codegraph-seeded-ppr | `SPECKIT_CODE_GRAPH_SEEDED_PPR_RANKING` | PPR ties the flat walk on every metric at every K (deltas 0.0), no damping beats it, uniform-weight CALLS edges give centrality nothing to differentiate, flag already removed in source | **CUT** confirmed by measurement |
| 006 codegraph-edge-lifecycle | staleness `REVERSE_DEP_FORCE_PARSE`, `EDGE_BITEMPORAL_READS`, `EDGE_GOVERNANCE_VOCAB` | after refinement: degree-capped force-parse drops a 30-importer rename from 30 reparses to 0 (scan 37ms to 21ms) while low-fan-in still rebinds, correctness held 3/3. Bitemporal close-and-insert writer plus as-of reader pass the proving query (past returns old target, current returns new, edge closed not deleted). Both byte-identical off, 21 focused tests pass | **GRADUATE** staleness (degree-cap) · **GRADUATE** bitemporal (writer built) · **CUT** governance |
| 007 advisor-rrf-fusion | `SPECKIT_ADVISOR_RRF_FUSION`, conflict-rerank, `SPECKIT_ADVISOR_SELF_RECOMMENDATION_GUARD` | widened 42-prompt set with self_guard and conflict bands and an in-memory conflicts_with overlay. RRF top-1 37/42 to 38/42 (+1, zero regressions, 0.976 agreement, deterministic). Self-guard moves 0/4 top-1, behaviorally redundant with the generic explainer floor and the un-flagged audit penalty. Conflict seam fed real mass corrects 1/5 (4/5 to 5/5) and repairs a regression plain RRF introduces. | **GRADUATE** RRF core with the conflict-rerank seam (they ship together, the seam is RRF's regression net), **CUT** the self-recommendation guard. Default OFF byte-identical across 42 prompts, no production edit, overlay merged in-memory only. |
| 008 deeploop-finding-dedup | `SPECKIT_FANOUT_NEAR_DUP_DEDUP`, lag-ceiling, progress-heartbeat | dedup precision 1.0 and distinct-finding recall 1.0 (0 false collapses, 7/17 noise removed, strongest severity kept), lag fires one warning, heartbeat 43 steady records, all byte-identical off | **GRADUATE** all three |

## Graduation roadmap (after the refinement pass)

The refinement pass lifted three of the four refine clusters to graduate and left the fourth sound but data-gated.

- **Graduate-ready (4 clusters, 9 flags):** the deep-loop finding dedup with the lag and heartbeat gauges (clean from the start), the multi-hop appends (rewired, completeRecall@20 up 0.375 for deep-K readers), the code-graph staleness repair and bitemporal reads (refined to working), and the advisor RRF core paired with the conflict-rerank seam (they ship together, the seam is RRF's regression net). All byte-identical off, the flip is a separate decision.
- **Refine, gated on data not code (1 cluster):** the true-citation ledger. Both code blocks are fixed and the signal is sound, but live density needs session-carrying traffic to accumulate before it can clear the reranker bar.
- **Cut (5 flags):** the retrieval-class weights, save reconsolidation (the cosine band, not the idea), the seeded PageRank, the edge governance vocab, and the advisor self-recommendation guard (redundant with logic already running un-flagged).
