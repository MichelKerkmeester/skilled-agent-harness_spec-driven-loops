[0m
> build · glm-5.2
[0m
[0m→ [0mRead .opencode/bin/mk-code-index-launcher.cjs
[0m→ [0mRead .opencode/bin/lib/launcher-ipc-bridge.cjs
```json
{
  "angle": "socket-liveness-probe",
  "insights": [
    "The probe you want ALREADY EXISTS: launcher-ipc-bridge.cjs:probeDaemon({deepProbe:true}) (L150-236) connects to the UDS, sends JSON-RPC initialize, and only returns 'alive' on a matching result/error frame. A wedged daemon (event loop blocked, 0% CPU) accepts the connection but never replies → timeout → 'dead'. connect-ok-only is explicitly NOT trusted for the reap/bridge decision (see the comment L155-157). This is the exact semantics the incident needs; the gap is WHERE it is invoked.",
    "probeDaemon is only called today inside maybeBridgeLeaseHolder (L365-442), which runs AFTER a lease file is confirmed held. In the incident the lease file was GONE, so neither acquireOwnerLeaseFile (L474) nor isLeaseHeld (L612) entered the bridge path; launcherMain fell straight through to writeLeaseFile()+launchServer() (L1182-1189) and spawned a SECOND writer against a DB still occupied by the wedged first daemon → orphaned 17MB WAL.",
    "processLiveness (L423-433) is process.kill(pid,0) — it cannot distinguish 'serving' from 'wedged-but-alive'. Every classification in the file (classifyOwnerLease L455, leaseHeldFromFile L582, pidLiveAt L565, removeStaleBootstrapLock L890) is PID-only. The 2-min stale-heartbeat-reclaim (L467, ttlMs*2) is the only wedge detector, and it ONLY fires if the owner lease file PERSISTS; a deleted/corrupt lease file short-circuits it entirely (readOwnerLeaseFile returns null → acquireOwnerLeaseFile treats it as fresh acquire).",
    "Error-code semantics from probeDaemon are already diagnostic: connect ENOENT = socket file absent (no server ever bound, or cleaned up); ECONNREFUSED = stale socket file left behind by a dead server (server gone, fs entry remains — must unlink before bind or the new server hits EADDRINUSE); timeout = wedged server holding the socket AND the DB (the dangerous case); ECONNREFUSED-with-no-PID = safe to reap-and-spawn. The incident was timeout-or-ENOENT-with-orphaned-WAL: PID alive, no socket, lease gone.",
    "The unfixable-without-extra-discovery problem: when the lease file is gone, you have no PID to reap. probeDaemon returning 'timeout' tells you SOMETHING is listening on that path, but you need the PID to kill it. The lease-leasePath/writeLeaseFile (L756) writes socketPath into the PID lease, and buildOwnerLease (L410) writes it into the owner lease — but if both are deleted, the socket path must be recomputed via getIpcSocketPath + you must enumerate the holder (lsof/fuser on the socket inode, or scan for node processes running dist/index.js with the matching dbDir).",
    "probeLeaseHolderWithRetries (L340-363) already requires N consecutive deep-probe failures (resolveLeaseProbeAttempts, default 2) before declaring dead, specifically to avoid a transiently-busy owner (mid-FTS-merge) being false-reaped by a sibling. Any new call site must reuse this retry policy, not a single probe, or you regress the false-reap guard."
  ],
  "recommendations": [
    {
      "change": "Add probeExistingService(socketPath, {deepProbe:true}) wrapper that returns {status:'alive'|'dead', reason, kind} where kind distinguishes 'enoent' (no socket file), 'econnrefused' (stale socket, server gone), 'timeout' (wedged), 'json-rpc-reply' (healthy). Map the raw error.code onto kind in probeDaemon's error/close handlers before settling.",
      "where": ".opencode/bin/lib/launcher-ipc-bridge.cjs:probeDaemon (L150) — enrich the finish() calls with a kind field; export a thin classify wrapper.",
      "why": "Callers (launcherMain, classifyOwnerLease) need to branch on WHY the probe failed, not just dead/alive. ENOENT vs ECONNREFUSED vs timeout demand different recovery actions (spawn vs unlink-socket-then-spawn vs reap-PID-then-spawn)."
    },
    {
      "change": "Insert a socket probe into launcherMain's no-lease-held path. After the strictSingleWriter block determines no owner/PID lease exists (around L1097-1100, just before acquireBootstrapLock at L1102), compute socketPath=getIpcSocketPath('mk-code-index',{dbDir}) and run probeLeaseHolderWithRetries with deepProbe:true. If kind==='alive' → a daemon IS serving but lost its lease files; bridge to it (call bridgeOrReportLeaseHeld with a synthesized leaseResult) instead of spawning a second writer. If kind==='econnrefused' → unlink the stale socket file before launchServer so the new bind succeeds. If kind==='timeout' → a wedged daemon holds the DB; MUST discover and reap its PID (see next rec) before spawning, else double-writer corruption. If kind==='enoent' → clean slate, proceed to spawn.",
      "where": "mk-code-index-launcher.cjs:launcherMain (L1039), insert between the strictSingleWriter decision block exit (L1100) and acquireBootstrapLock (L1102).",
      "why": "This is the exact incident path: lease file gone → no reclaim → blind spawn into an occupied DB. A socket probe here is the only thing that catches a wedged daemon whose lease files were deleted."
    },
    {
      "change": "Add discoverSocketHolderPid(socketPath) that resolves the PID owning the listening socket when no lease file records it. On darwin/linux use `lsof -t -c node <socketPath>` (or `fuser <socketPath>` on linux) filtered to processes whose argv contains the code-graph dist/index.js and whose cwd/env dbDir matches resolvedDbDir(). Fall back to null when unavailable. Feed the discovered PID into reapOwnerBeforeRespawn (L640) which already handles SIGTERM-grace-then-SIGKILL. If discovery returns null and probe is timeout, HARD BLOCK the spawn (log + exit 0 like the reclaimedOrphanPid EPERM path at L1177) rather than risk a double writer — require manual recovery.",
      "where": "mk-code-index-launcher.cjs: new helper near readParentPid (L435); invoke from the new launcherMain probe branch; route output to reapOwnerBeforeRespawn (L640).",
      "why": "A wedged daemon with no lease file is unreapable without PID discovery. Spawning alongside it corrupts SQLite (the orphaned 17MB WAL in the incident). When you cannot prove the holder is dead, blocking is safer than racing two writers."
    },
    {
      "change": "Strengthen classifyOwnerLease so a PID-alive owner is not classified 'live-owner' until the socket deep-probe confirms it is SERVING. Currently processLiveness=='alive' (L456) gates straight to the heartbeat check; a wedged owner with a fresh heartbeat returns 'live-owner' and blocks acquireOwnerLeaseFile, deferring detection to the next secondary's bridge-probe. Add: if liveness==='alive', run probeLeaseHolderWithRetries on lease.socketPath (or recomputed path); timeout/ECONNREFUSED → return 'wedged-owner' (a new classification) so acquireOwnerLeaseFile reclaims immediately instead of waiting for the 2-min heartbeat TTL.",
      "where": "mk-code-index-launcher.cjs:classifyOwnerLease (L455-472), add the socket probe after the ppid-1-orphan check (L463) and before the heartbeat check (L465); make classifyOwnerLease async OR run the probe synchronously-via-spawnSync-lsof+short-socket-read if async is not viable at this callsite.",
      "why": "Cuts wedge-detection latency from ttlMs*2 (~2min) to one probe round (~5s). The bridge path already does this, but only for secondaries; the reclaim path trusts PID liveness alone."
    },
    {
      "change": "After a timeout-classified probe, before respawning, run wal_checkpoint(TRUNCATE) on the DB (or document that launchServer's child must do it on cold start when it detects no concurrent writer). The incident's 17MB orphaned WAL indicates the wedged daemon never checkpointed. A pre-spawn checkpoint (gated on exclusive ownership after successful reap) prevents a respawned daemon inheriting a bloated WAL that inflates every read.",
      "where": "mk-code-index-launcher.cjs:respawnAfterDeadSocket (L668), after clearOwnerLeaseFileIfOwner (L698) and before buildIfNeeded (L708); or in the new launcherMain probe branch after a confirmed reap.",
      "why": "The WAL is a symptom; reclaiming the lease without checkpointing leaves the new daemon replaying a large WAL under load."
    }
  ],
  "risks": [
    "Adding a deep probe to every launcherMain cold path adds up to resolveLeaseProbeAttempts()*timeoutMs (~10s worst case for a wedged daemon) before spawn. Mitigate: short-circuit on ENOENT (socket file absent → no probe, proceed immediately); only deep-probe when fs.existsSync(socketPath) is true. The healthy case stays O(connect+initialize-reply) ≈ tens of ms.",
    "classifyOwnerLease is currently synchronous; making it async ripples into acquireOwnerLeaseFile and launcherMain (already async, low cost) but also into emitLeaseMetric/staleReclaimed paths. If async is undesirable, the socket probe there must be a blocking spawnSync('node', probe-script) — slower and uglier. Prefer making acquireOwnerLeaseFile async and leaving classifyOwnerLease as a pure-PID pre-filter, with the socket probe as a second async gate inside acquireOwnerLeaseFile after the PID-alive branch.",
    "lsof/fuser PID discovery is non-portable and may be absent in sandboxes; if discovery returns null on a timeout-probe, the launcher must HARD BLOCK (not spawn) — which converts a self-healing daemon into a manual-recovery-required state for that edge. Document the recovery command (kill the node holding the socket, wal_checkpoint TRUNCATE, rm the socket) so operators aren't guessing.",
    "TCP socket mode (SPECKIT_IPC_SOCKET_DIR=tcp://) leaves no filesystem artifact, so the ENOENT-vs-ECONNREFUSED distinction collapses to ECONNREFUSED/timeout; the stale-socket-unlink step (recommendation 2) is a no-op there and the bind-collision risk is lower (OS reuses ports), but PID discovery via lsof-on-a-file is impossible — must lsof on host:port, which may match unrelated processes.",
    "Race window between probe-alive and spawn: the daemon could die after the probe returns alive but before the bridge attaches. The existing session-proxy (bridgeStdioThroughSessionProxy, L207) already handles reattach-on-reconnect, so a client bridged to a just-died daemon will be replayed on respawn — but only if the respawn path fires. Ensure the bridge's onError (L437) triggers the same respawn-decision as a fresh launcherMain probe, not just a diagnostic line.",
    "wal_checkpoint(TRUNCATE) from the launcher requires a SQLite handle and must run AFTER the wedged PID is provably dead (reapOwnerBeforeRespawn success) and BEFORE the new child opens the DB — otherwise it either fails (DB locked) or races the child's open. Safest: have the child run PRAGMA wal_checkpoint(TRUNCATE) as its first DB statement on cold start when it detects it is the sole writer (no live lease), keeping the launcher out of SQLite entirely."
  ]
}
```
