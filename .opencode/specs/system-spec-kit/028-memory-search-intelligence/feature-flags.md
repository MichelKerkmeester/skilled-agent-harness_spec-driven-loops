# Packet 028 Feature Flags: The Five Switches That Survived

> Packet 028 added a lot of new search and memory behavior behind environment-variable switches, and almost every switch was meant to ship in the OFF position until a real before-and-after benchmark earned it a flip. The flag-resolution reckoning closed that experiment. Every default-off switch was reopened, simulated under a fair real-world load and given a final keep-or-delete decision per flag. Five switches earned default-ON and stayed. Ten did not earn their keep and their code was removed from the tree. This document explains the five that survived: what each feature is, what turning it ON changes and why it earned its place. The ten that were deleted are listed at the end so a reader who remembers them knows where they went.

---

## THE BIG IDEA

028 shipped its new machinery for correctness and safety first, then asked each switch to prove it was worth turning on. The bar was deliberate. A switch that only changed output without improving a real metric did not earn a default-ON flip, and a switch that did nothing measurable for live recall did not earn a place in the tree at all. The reckoning measured each one under a real-world simulation rather than a synthetic harness, then made a final per-flag decision. Five switches cleared the bar and ship default-ON. The other ten were deleted along with their code, so the tree no longer carries dead switches that a future reader has to reason about.

The five survivors split by the kind of evidence that earned them. Two are unqualified wins where a real metric moved in the right direction. Two are no-harm guarantees where the switch adds protection or grounding without dropping a real result. One earns its keep on prod-path displacement protection, a graph-additive reorder that protects the truncated prod top-3 from being evicted by low-signal graph-only candidates rather than on the eval-mode edge-hop recall the prod truncation floor cuts. Each survivor routes through the shared `isFeatureEnabled` resolver, so the environment override still works and a user can force any of them off with `SPECKIT_<FLAG>=false`.

A note on phases. Packet 028 is split into phase folders. Phase `001-speckit-memory` is the memory and search engine. Phase `002-code-graph` is the code structure index. Phase `003-skill-advisor` is the skill recommender. Phase `004-deep-loop` is the multi-agent loop runtime. Four of the five surviving switches live in `001-speckit-memory`. The fifth, the confidence-calibration switch, rides a pre-028 switch (027/017) whose promotion was decided during 028.

---

## 1. THE FIVE DEFAULT-ON SWITCHES

| env var | default | what it does (plain English) | why it earned the flip | 028 phase |
| --- | --- | --- | --- | --- |
| `SPECKIT_DERIVED_ID_PROVENANCE` | ON (`true`) | Gives every auto-generated link a stable id computed from its own content, so the same generated link always gets the same id and is traceable. | Unqualified correctness win. Content-addressed identity passed every check: stable ids 50 of 50, reproducible on replay 3 of 3, near-duplicates kept distinct 50 of 50, zero collisions. | 001 |
| `SPECKIT_CONFIDENCE_CALIBRATION` | ON (`true`) | Fine-tunes a recall confidence score through a fitted isotonic model so the number it reports means what a reader thinks it means. | Unqualified win. Held-out ECE moved from 0.184 to 0.023 across all folds with a shipped isotonic model, and a label-decoupling fix removed the earlier overfit where the model was fitted and evaluated on the same set. | pre-028 (027/017), promoted during 028 |
| `SPECKIT_RETENTION_FORGETTING_V1` | ON (`true`) | Adds conservative guardrails so forgetting only ever drops genuinely spare items and never drops something that other live memories still point to. | Safety and no-harm guarantee, not a precision win. It spares 386 keep-set rows the OFF path would delete with a dropRecall delta of 0. The keep-versus-drop labels are circular because they derive from the reducer's own thresholds, so it earns the flip as a guardrail rather than a measured precision gain. | 001 |
| `SPECKIT_WORLD_SUMMARY_PRELUDE` | ON (`true`) | Adds a coarse-to-fine warm-up step. Before the detailed search runs it reads a high-level summary of the whole memory to get its bearings, then appends that grounding to the results. | No-displacement grounding aid, not a recall-quality win. In append placement it recovers 11 targets with 0 regressions by construction because it never displaces a baseline row. Its apparent gain is partly a self-recall and an append-by-construction artifact, so it earns the flip as a grounding aid. | 001 |
| `SPECKIT_TEMPORAL_EDGES` | ON (`true`) | Adds a time-aware graph lane that reorders the graph-additive candidates before the result is truncated, so the prod top-3 is protected from being displaced by low-signal graph-only hits. | Prod-path displacement protection, not the +0.083 edge-hop recall. That +0.083 is an eval-mode artifact the 3-result prod truncation floor cuts to a 0.000 delta. The keep rests upstream of truncation: the graph-additive reorder keeps higher-scored lexical and vector hits in the truncated top-3 instead of letting graph-only candidates evict them, measured on 3 of 12 golden queries with 0 regressions. | 001 |

---

## 2. HOW TO TURN ONE OFF

These five switches ship ON. Each is read from the environment through the shared resolver, so a user can force any of them off and restart so the server picks it up. For example:

```bash
export SPECKIT_TEMPORAL_EDGES=false
# then restart the MCP server, or start a fresh session
```

The accepted "off" values are `false`, `0`, `no`, `off` and `disabled`. The accepted "on" values are `true`, `1`, `yes`, `on` and `enabled`, and these are the defaults for all five. A coupling note applies to the calibration pair. `SPECKIT_CONFIDENCE_CALIBRATION` was fitted on the value distribution that `SPECKIT_ABSOLUTE_RELEVANCE_CALIBRATION` (also default-ON) produces, so the two are coupled by construction. Turning the absolute lever off while the calibration lever stays on feeds the model an input it never saw and silently mis-calibrates. Keep the pair in its shipped state unless you understand the coupling.

---

## 3. WHY ONLY FIVE

The whole point of the reckoning was to stop carrying machinery the system never uses. A switch that adds a knob is only worth keeping when flipping it improves a real metric or guarantees no harm. The five above clear that bar. Two move a real number in the right direction, two add protection or grounding without dropping a real result, and one protects the truncated prod top-3 from graph-channel displacement. Everything else was measured under a fair real-world simulation, found not worthwhile and removed from the tree along with its code, so the surviving surface is exactly the set of switches a reader can trust.

---

## 4. THE TEN DELETED SWITCHES (FOR THE RECORD)

The reckoning deleted ten default-off switches and their code after a fair real-world simulation showed each one was not worthwhile. They are listed here so a reader who remembers them knows they were resolved and removed, not silently left off. The per-flag deciding evidence lives in [`benchmark-status.md`](./benchmark-status.md) and [`001-speckit-memory/022-kept-off-flag-resolution/`](./001-speckit-memory/022-kept-off-flag-resolution/).

| deleted flag | one-line deciding evidence | 028 phase |
| --- | --- | --- |
| `SPECKIT_PROCEDURAL_RELIABILITY_RECALL` (with `SPECKIT_PROCEDURAL_OUTCOME_EMITTER`) | Shadow-only with an empty outcome ledger and an eval rankDelta of 0, the multiplier moves only synthetic near-ties. | 001 |
| `SPECKIT_SUMMARY_FUSION_LANE` | Displacement-only, Recall@20 delta -0.036, the lane only pushes a real channel hit out of the result list. | 001 |
| `SPECKIT_CARDINALITY_PENALTY` | Recall@20 movement 0.0000, the degree-lane cap is too small to be decisive at K=20. | 001 |
| `SPECKIT_SLEEPTIME_CONSOLIDATION` | Net -1.67pp, the dedup pass hurts recall rather than helping it. | 001 |
| `SPECKIT_CODE_GRAPH_SEEDED_PPR_RANKING` | Negative on the real forward-CALLS graph, the uniform edges make PPR equal to the prior ranking. | 002 |
| `SPECKIT_SEMANTIC_EDGE_LAYER` (the edge family: `SPECKIT_EDGE_VECTOR_INDEX`, `SPECKIT_EDGE_TRIPLET_SEARCH`, `SPECKIT_EDGE_SEMANTIC_DEDUP`, `SPECKIT_EDGE_SEMANTIC_INVALIDATION`) | The fact-text is generic relation-template boilerplate carrying no pair identity, recall-inert at K=20 with a single-item +0.083 that does not generalize. | 001 |
| `SPECKIT_ADVISOR_OUTCOME_WEIGHTED_RERANK` | MRR within noise on an empty ledger, every skill resolves to neutral so the order never moves. | 003 |
| `SPECKIT_BITEMPORAL_RECALL` | Zero callers, no point-in-time consumer reads the validity window. | 001 |
| `SPECKIT_EDGE_PRESENCE_CURRENTNESS` | A correct integrity reconciliation pass that repairs 0 on the live graph, not a recall lever. | 001 |
| `SPECKIT_AGENTIC_RECALL` | Oracle ceiling +0.344 but the live reasoner nets zero with regressions at 51s per query and has no production consumer. | 001 |

The path-and-value knobs that only existed to serve a deleted feature were removed with it. The retrieval-class routing flag and the calibration and parser value knobs that serve surviving or always-on behavior remain, documented in `mcp_server/ENV_REFERENCE.md`.

---

## 5. THE TRACK B BUILT-BUT-HELD SWITCHES

The deleted ten taught the campaign why a lever fails to move live recall, and those teachings drove a research pass that found four new candidates. One of the four was a measurability tool rather than a switch. eval-v2 was built and kept as standing infrastructure: it adds three non-self-recall classes so a feature cannot win by recalling the query back to itself (`thematic_multi_target`, `causal_chain` and `hard_negative`), a completeRecall@K metric at K of 3, 5 and 8 that scores whether the full target set is recovered, and a dual-mode eval-vs-prod fidelity measurement. The headline it exposed is the eval-saturation that had hidden the deleted features: eval-mode completeRecall@8 is 0.212 against prod-mode 0.036, a +0.176 fidelity gap. eval-v2 is not a flag and ships as kept infrastructure regardless of any feature decision.

The other three candidates were built default-off, benchmarked in prod mode and held by a fresh-Opus gate. None flips on prod-mode evidence. They stay in the tree as honest shadows with a measured reason and a next step each, routed through the shared `isFeatureEnabled` resolver like every other switch.

| env var | default | what it does (plain English) | why it is held (prod-mode evidence) | next step | 028 phase |
| --- | --- | --- | --- | --- | --- |
| `SPECKIT_DETERMINISTIC_MULTIHOP` | OFF (`false`) | Appends a second-hop set of related memories to the result so an edge-hop target gets a recall path. | Prod completeRecall delta 0.000. The appended hop-2 docs land at the tail and prod confidence-truncation cuts them, so the multi-hop content never reaches the prod result window. | A scoped truncation-exemption probe on the causal_chain class with displacement accounting. It earns the flip on that class or proves tail-recall structurally doomed at prod K. | 001 |
| `SPECKIT_LANE_CHAMPION_BACKFILL` | OFF (`false`) | Reserves a backfill slot for the top hit of each retrieval lane so no lane is shut out of the result set. | 0.000 delta, structurally redundant with RRF which already absorbs every lane champion. Reserving a slot duplicates work the fuser already does. | Retire the investment. It has no path to a flip and is redundant with the fuser. | 001 |
| `SPECKIT_TRUE_CITATION_EMITTER` | OFF (`false`) | Emits a clean default-off shadow that produces the corpus's missing negative-citation labels for evaluation. | The shadow is clean and produces the missing negatives, but its positive label depends on the assistant literally echoing the memory id, so positives are under-counted and the measured signal cannot credit it yet. | Fix the positive label with content-attribution, then run a one-shot offline mining pass before any collection decision. | 001 |

The full method, the per-feature prod-mode numbers and the append-not-displace truncation finding live in [`001-speckit-memory/023-new-feature-research-build/`](./001-speckit-memory/023-new-feature-research-build/).

---

## 6. TRACK C: THE PROPOSED DATA-QUALITY FLAGS (NOT BUILT)

The five switches in section 1 survived a real reckoning. The flags in this section are the exact opposite status and a reader should not confuse the two. None of them exists in the tree. None has been built, benchmarked or measured. They are the indicative names a research-only program proposed for a data-quality slate that has not landed a single line of code. Track C is the new `005-spec-data-quality` lineage. A multi-lineage deep-research pass produced 28 data-quality recommendations and scaffolded each as a planned phase, but every phase is PLANNED with no implementation and no number taken. The env var names below are placeholders chosen to show the shape of the proposal. Read every row as a would-be, not a will-be.

| proposed env var (indicative, not built) | default | what it would do (plain English) | status | gate |
| --- | --- | --- | --- | --- |
| `SPECKIT_QUALITY_LOOP_AUTHORED_SURFACE` | would-be OFF | Extends the live default-ON memory-save quality-loop scorers and auto-fix to the authored spec-doc and metadata-JSON write surface, so the same checks that already run on saved memory would also run when a spec doc or a `description.json` is written. | PROPOSED, not built, not measured | Keystone of the Tier-A on-write program (phases 001-010). Reuse-first and write-time, so it would sit upstream of the prod truncation stages. Would ship on cost, not on a measured recall win. |
| `SPECKIT_SCHEDULED_DQ_SWEEP` | would-be OFF | Runs a standing scheduled data-quality sweep over the existing corpus with guarded auto-fix, so drift that landed before any on-write check existed would still be caught and repaired. | PROPOSED, not built, not measured | Tier-B retroactive automation (phase 011). Would ride the shared safe-fix engine (phase 026) behind its guarded auto-fix safety model. Would ship on cost. |
| `SPECKIT_EARS_LINT` | would-be OFF | Lints authored requirement lines against the EARS shape at write time and warns on requirements that do not parse. | PROPOSED, not built, not measured | Tier-A on-write (phases 001-010). Write-time validation, so it would bypass the prod truncation stages. Would ship on cost. |
| `SPECKIT_RETRIEVAL_GAP_DETECT` | would-be OFF | Watches retrieval results for gaps and feeds each miss back as a learning edge, so a query that should have surfaced a doc but did not would leave a trace the system can act on. | PROPOSED, not built, not measured | Tier-B retrieval-learning feedback edge (phase 013). Retrieval-facing, so it inherits the truncation law and is gated on the phase-015 prod-mode completeRecall@3/@5/@8 gate plus an order-sensitive NDCG@K with a top1 guard before any flip. |
| `SPECKIT_CHUNK_PREFIX` | would-be OFF | Prepends a context prefix to each chunk before embedding, so a chunk would carry its parent-doc identity into the vector. | PROPOSED, not built, not measured | Tier-C retrieval (phases 014-018), default-off. Gated on the phase-015 prod-mode completeRecall@3/@5/@8 gate plus an order-sensitive NDCG@K with a top1 guard before any flip. |
| `SPECKIT_ANSWERABLE_QUESTIONS` | would-be OFF | Generates the questions a doc can answer and indexes those next to the doc, so a question-shaped query would have a question-shaped target to match. | PROPOSED, not built, not measured | Tier-C retrieval. Gated on the phase-015 prod-mode completeRecall@3/@5/@8 gate plus an order-sensitive NDCG@K with a top1 guard before any flip. |
| `SPECKIT_METADATA_FUSION` | would-be OFF | Fuses structured metadata signals into the retrieval score, so doc type, freshness and provenance could lift or sink a candidate. | PROPOSED, not built, not measured | Tier-C retrieval. Gated on the phase-015 prod-mode completeRecall@3/@5/@8 gate plus an order-sensitive NDCG@K with a top1 guard before any flip. |
| `SPECKIT_LLM_JUDGE` | would-be OFF | Adds an LLM re-rank pass that judges candidate relevance before the result is returned. | PROPOSED, not built, not measured | Tier-C retrieval. Gated on the phase-015 prod-mode completeRecall@3/@5/@8 gate plus an order-sensitive NDCG@K with a top1 guard before any flip. |

The reason the whole retrieval tier sits behind a gate is the truncation law this packet documents in [`benchmark-status.md`](./benchmark-status.md) and [`before-vs-after.md`](./before-vs-after.md). Token-budget truncation is the real prod-limiting stage and confidence truncation only guarantees a never-cut-below-3 minimum (it returns 3 to 20, cliff-conditional), so an eval-mode recall gain that lands past the prod window does not transfer to the prod reader. A retrieval-class candidate cannot earn a keep on eval-mode numbers. It can only earn one by moving the prod window that phase 015 measures. That is why every Tier-C flag above is gated on the phase-015 prod-mode completeRecall@3/@5/@8 gate plus an order-sensitive NDCG@K with a top1 guard (`015-c2-prodmode-recall-gate`) before any flip, and why the phase-027 floor experiment that would raise the retrieval budget and measure whether results 4 through 10 are signal or noise is itself only proposed, not run. Until that experiment runs there is no measured basis to flip any retrieval flag.

The nearest-to-real win in the whole slate is not a flag at all. It is the A4 JSON-schema warn-to-error promotion (`004-a4-schema-warn-to-error`), unconditional as a DECISION though its error flip is gated on a backfill-to-zero, which would turn the existing JSON-schema shape rules from a warning into a hard error. A real `graphMetadataSchema` run fails 24 files today (16 excluding archives), not the 11 an earlier JSON-parse subcount reported, so the flip waits until the re-measure reads zero. It needs no environment switch because it tightens a check that already runs, and it is validation not ranking, so the prod truncation stages never touch it. Even so it has not shipped. Everything else in Track C either would ship on cost as an on-write or retroactive convenience or stays frozen behind the prod-mode gate, and none of it has been built or measured yet. The full program tiering, the per-phase plans and the truncation-law reasoning live under [`005-spec-data-quality/`](./005-spec-data-quality/).

---

## 7. THE 005 DATA-QUALITY GENERATION: THIRTEEN BUILT SWITCHES, TWELVE KEPT, ONE DELETED

Section 6 listed the Track C data-quality slate as indicative names with no code behind them. That has changed for one lineage. The `005-spec-data-quality` build landed thirteen real switches across the descriptions cache, the metadata generators and the search-verdict path. Every one exists in the tree today and routes through the shared `isFeatureEnabled` resolver. Phase 039 restamped the tree, phase 040 ran the graduation benchmark, a migration re-run then backfilled the freshness fields, and a fixture re-benchmark measured the four verdict and render flags on their proper cases, so this generation has now been fully through the reckoning the original 028 switches faced. Twelve earned their place and ship on or enforcing, and one, grounding-signal, was deleted as purely informational, the same earn-or-delete bar the original generation cleared. They are not the section 6 placeholders. They are the first concrete code from that lineage and each carries the verdict its benchmark earned.

The reason almost every switch ships off, or ships on but report-only, reduces to the three causes the 028 discipline already knows. Migration safety is the first. The existing tree carries `description.json` and `graph-metadata.json` files written under the old prose-status, prefixed-path and wall-clock-stamp shapes the new contract rejects or would rewrite, so a switch that enforces the new shape cannot flip until those files are restamped. Benchmark-gating is the second. A switch that changes search-verdict behavior graduates to ON only after a measured before-and-after earns it, the same earn-or-delete bar the kept five cleared. Reversibility is the third. A behavioral switch dark-launches off so a regression is one env var away from being undone.

One switch ships default-ON because it cannot mass-fail. `SPECKIT_GENERATED_METADATA_Z_EXCLUSION` keeps `z_*` archive and staging folders out of the descriptions cache and is safe on by construction. A second, `SPECKIT_GENERATED_METADATA_GRANDFATHER`, shipped ON in report-only mode during the migration window so the metadata-integrity rule narrated `info` instead of failing strict while legacy files were still on the old shape. Its graduation is the inverse of the others. It has now flipped OFF to a hard error, because phase 039 restamped the tree clean and phase 040 confirmed zero violations.

| env var | default | what it does (plain English) | why it is in this state | graduation gate | phase |
| --- | --- | --- | --- | --- | --- |
| `SPECKIT_IDENTITY_MERGE_SAFETY` | ON (`true`) | Resolves one specs-root-relative spec-folder identity across both generators and preserves a non-null parent plus unions children on a merge re-derive, so a scoped re-derive cannot erase lineage. | Shipped. Phase 039 restamped every `graph-metadata.json` to the new shape, so the lineage-preserving re-derive now reads a tree written under the new merge. | Graduated at phase 040. The migrate driver verify pass read 2049 folders with 0 violations on the restamped tree, then this flipped ON. | 033 |
| `SPECKIT_IDEMPOTENT_DESCRIPTION_WRITES` | ON (`true`) | Skips a per-folder description write whose only delta is the volatile stamp and gates the aggregate-cache write on a real member delta with a targeted upsert. | Shipped. Phase 039 normalized the wall-clock stamps, so turning it on no longer mass-rewrites the tree on the first save. | Graduated at phase 040. A double generate held deterministic on 61 of 61 folders, then this flipped ON without a mass rewrite. | 035 |
| `SPECKIT_GENERATED_METADATA_DRIFT_GATE` | ON (`true`) | Derives both `description` and `causal_summary` from one shared synopsis extractor, persists `source_doc_hashes` as the freshness key and fails strict on a drift report. | Shipped. A migration re-run stamped `source_doc_hashes` across the tree, so the gate has a freshness baseline. | Graduated at the re-benchmark. The validator with the gate on read 2049 folders with 0 violations once the hashes were present, then this flipped ON. | 037 |
| `SPECKIT_GENERATOR_HARDENING` | ON (`true`) | Persists a graph-metadata `source_fingerprint`, routes phase-parent classification and the derived-children list through one `listPhaseChildren` enumeration and moves access and freshness telemetry to the index layer so a read no longer dirties the JSON. | Shipped. A migration re-run stamped the `source_fingerprint` across the tree, so the enforcing integrity rule has the field to check. | Graduated at the re-benchmark. The verify pass fell from mass-failing 2049 of 2049 to 0 violations once the fingerprints were present, then this flipped ON. | 038 |
| `SPECKIT_GENERATED_METADATA_GRANDFATHER` | OFF (`false`), enforcing | Generated-metadata integrity violations, including a missing or mismatched `source_fingerprint`, now fail strict as hard errors. | Shipped enforcing, the inverse graduation. Phase 039 restamped the tree clean, so the rule no longer narrates `info`. | Graduated at phase 040. The verify pass confirmed 0 violations across 2049 folders, so it was set `false` and violations now fail strict. | 036 |
| `SPECKIT_GENERATED_METADATA_Z_EXCLUSION` | ON (`true`), opt-out | Excludes `z_*` archive and staging folders from the descriptions.json cache. | Reversibility on cost. It cannot mass-fail anything, so it ships ON with a one-var opt-out to restore the prior scanner. | None needed. It ships on cost and stays ON unless a reader opts out. | 034 |
| `SPECKIT_LEXICAL_GROUNDING` | ON (`true`) | A lexical-grounding floor on the search verdict for low-overlap results. | Shipped. The lead graduation candidate cleared its bar, the off-corpus false-confirm rate fell from 0.833 to 0. | Graduated at phase 040. The benchmark confirmed the rate holds at 0 on live data, then this flipped ON. | 026 |
| `SPECKIT_FALSE_CONFIRM_MAX_RATE` | ceiling 0, enforcing | Caps the tolerated off-corpus false-confirm rate for the eval fixture CI gate. | Shipped. The rate holds at 0 with lexical grounding on, so the ceiling is set to 0 and the gate now fails on any off-corpus false confirm. | Graduated at phase 040. The CI gate exits 0 with the verdict flags on and exits 1 with them off, so the ceiling was set and the gate enforces. | 025 |
| `SPECKIT_NOISE_FLOOR_SUBTRACTION` | ON (`true`) | Subtracts a measured per-embedder corpus noise-floor from absolute relevance before the request-quality band read, floored at zero. An embedder with no measured floor fails closed to the raw band. | Shipped. The measured before-and-after drove the off-corpus false-confirm rate from 0.833 to 0, the same win lexical grounding cleared. | Graduated at phase 040. The benchmark confirmed the rate holds at 0 on live data, then this flipped ON. | 028-scoring-hardening |
| `SPECKIT_CITE_WITH_CAVEAT` | ON (`true`) | Adds a `cite_with_caveat` tier between `cite_results` and `do_not_cite_results`, so a weak verdict whose top hit is still grounded is hedged rather than dropped. | Shipped. A borderline-grounded fixture showed it recovers a hedged-but-usable result the binary policy drops, and fires only on the borderline tier. | Graduated on the fixture re-benchmark. It recovered one borderline result the binary policy dropped to do_not_cite, a measurable effect, then this flipped ON. | 028-scoring-hardening |
| `SPECKIT_EVIDENCE_GAP_VERDICT` | ON (`true`) | Reads the Stage 4 `evidenceGapDetected` signal into the verdict so a true gap caps a good verdict at weak. | Shipped. A Stage-4 gap fixture showed it caps an otherwise-good verdict to weak on a real gap, a safety improvement, and leaves a no-gap verdict unchanged. | Graduated on the fixture re-benchmark. It capped one otherwise-good verdict to weak on a detected gap, a measurable effect, then this flipped ON. | 028-scoring-hardening |
| `SPECKIT_ENVELOPE_FIDELITY` | ON (`true`) | Emits the `data.envelopeRender` pre-rendered verdict fragment and promotes `requestQuality` and `citationPolicy` to conditionally-mandatory render slots in the command contract. | Shipped. A captured render corpus showed the fidelity checker flags every dropped or altered verdict render a no-checker baseline passes silently while passing every faithful render. | Graduated on the fixture re-benchmark. It flagged 3 of 3 dropped renders and passed 3 of 3 faithful ones, a measurable effect, then this flipped ON. | 027 |

Every switch above reads through the same `isFeatureEnabled` resolver as the five survivors, so the environment override works identically and the accepted off and on values are the set documented in section 2. This generation has now been through the reckoning the original generation faced. Phase 039 was the full-repo migration that restamped every `description.json` and `graph-metadata.json` in the whole tree, including `z_archive` and `z_future`, to the new shape, which earned the migration-gated flips and the grandfather flip to error. Phase 040 was the benchmark that measured each behavioral switch's before-and-after on live data and graduated the earners to default-ON under the same earn-or-delete discipline that produced the kept five. Of the thirteen, twelve are kept and one was deleted. A migration re-run backfilled the source_doc_hashes and source_fingerprint fields, graduating the drift-gate and generator-hardening flags, and a fixture re-benchmark built the borderline, gap and render-corpus fixtures the off-corpus class never produced, graduating cite-with-caveat, evidence-gap and envelope-fidelity on a measured effect. Grounding-signal set its display field correctly but moved no retrieval metric, so it was deleted with its code under the earn-or-delete bar. The per-flag verdicts and the measured evidence live in [`benchmark-status.md`](./benchmark-status.md).
