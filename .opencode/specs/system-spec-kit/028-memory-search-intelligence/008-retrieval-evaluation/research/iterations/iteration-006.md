# Iteration 6 (A5): re-measure cold-tier surfacing post-015 admission

> Model: **Opus 4.8 via claude2** (acct#2, read-only; relaunched at 1200s + read-cap after a 600s over-exploration timeout). Orchestrator-written. newInfoRatio **0.6**. **Honest result: post-015 admission does shift the distribution, but the re-measurement is BLOCKED by two preconditions — so 016's "inert" holds *operationally* (un-observable), not *disproven*.** Refuted two of the brief's premises.

## Candidates
1. **A5-tier-tagged-golden-lane → NET-NEW (H/M).** The harness **cannot** measure cold surfacing today: golden relevances are keyed by `memoryId` only (`ground-truth-data.ts:42`), with NO tier label (`complexityTier` is *query* difficulty; categories have `hard_negative` but no `cold`/`archived`). **The brief's premise "the golden set has cold IDs — use them" is UNSUPPORTED** — tier membership is a live-row property (`importance_tier`), discoverable only by joining relevances against the DB at eval time. Fix = a harness extension that tags each golden relevance by live `importance_tier` + emits a cold-appearance-rate + precision metric. (Different missing lane than A1's gate-verdict lane + a data-model gap.)
2. **A5-admission-liveness-split → EXTENDS 028/007 reindex-gate (H/L).** Admission splits into two lanes of different liveness. **LEXICAL+TRIGGER = live now**, default-on, no embeddings (`sqlite-fts.ts:171-188`, `isArchivedRetrievalIncludedByDefault` `search-flags.ts:647`). **VECTOR = inert** — the projection backfill admits a cold orphan only when its logical key has no active winner (`search-flags.ts:663-668`), but ~25% of cold rows are un-embedded; the gate-zero reindex is still deferred (`vector-index-store.ts:1879` `embedding_status='pending'`). So cold surfacing is **measurable in the lexical lane today, dark in the vector lane until reindex**.
3. **A5-fsrs-demotion-is-wired → EXTENDS 016 (refutation of "reopen FSRS").** 016's "tuning only fights the scheduler" is **confirmed in code**: retrievability demotes cold rows in fusion (`stage2-fusion.ts:567-582,879,1036` → `recencyFusionBoosted`) AND as one of five composite weights (`composite-scoring.ts:650-678`). Cold rows are already temperature-demoted in the real ranking path — reopening FSRS *tuning* would fight an active scheduler. 016 was right to decline.
4. **A5-basement-is-category-mismatch → NO-TRANSFER.** The brief's "tier basement" lead is a category error: `edge-tier-basement.ts:4-10` is explicitly **STAGED + not wired**, and it's a *retention/deletion* floor, not a *retrieval-ranking* basement — it does not influence what surfaces.

## Key finding
Post-015 admission genuinely shifts the distribution 016 measured against, but the re-measurement is **blocked by two independent preconditions**: (1) the gate-zero reindex is deferred, so the *vector-lane* cold admission is dark (~25% un-embedded); (2) the golden set carries **no tier label** on its `memoryId`-keyed relevances, so even the *lexical-lane* admission (live now) can't be scored without first joining to live `importance_tier` + re-grading. The actionable move is **NOT reopening FSRS tuning** (retrievability demotion is already wired in both fusion and composite scoring — 016 correctly declined) — it is a **harness extension: a cold-tier surfacing lane** (tier-tag golden relevances → measure cold appearance-rate + precision in the lexical lane now; defer the vector half behind the same reindex precondition). Until both land, 016's "inert" stands as **un-observable, not disproven** — and crucially, *not* vindicated as "harmless."

## Next focus
The cold-tier lane (A5-tier-tagged-golden-lane) joins A1's gate-verdict lane + A2's ECE lane as the third "accuracy/coverage lane the harness lacks" — all three are harness extensions feeding A8's methodology. Adversarial-verify the A5 premise-refutations (no tier label; basement staged) before treating them as load-bearing.
