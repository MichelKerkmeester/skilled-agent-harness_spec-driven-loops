# Iteration 5 (A3): A/B the default-on levers 017 shipped unmeasured

> Model: **Opus 4.8 via claude2** (acct#2, read-only). Orchestrator-written. newInfoRatio **0.7**. **Two findings that alone justify the campaign: a harness blind-spot for S5, and a confirmed silent-regression mechanism in a shipped default-on lever.**

## Candidates
1. **C1-s5-evalmode-blindspot → NET-NEW (H/S).** The wave-1 harness *structurally cannot measure S5*: `reorderTopNByCosine` runs only in the `else` of `if (evaluationMode)` (`hybrid-search.ts:1992` vs the reorder call `:2023-2024`); the comment `:2021-2022` says it's "Skipped in evaluationMode so the labeled-set top-K window stays exactly as fused." A labeled-set eval is the canonical place to pass `evaluationMode:true` → it exercises the *pre-reorder* order and reports S5 as a **no-op**. The A/B searchFn MUST set `evaluationMode:false` + toggle `SPECKIT_COSINE_TOPN_REORDER`; metric = **nDCG@1** (`computeNDCG(...,1)`) + top-1 precision (reorder permutes the ≤10-deep head, never membership).
2. **C2-s5-demotes-fused-nonvector-hits → EXTENDS A4 (H/S) — the sharp A3 question answered: YES.** `reorderTopNByCosine` sorts by `resolveAbsoluteRelevance` (`:2440`), which returns real cosine **only for rows with `.similarity`**, else falls back to RRF magnitude ~0.01-0.05 (`types.ts:89-96`). So a hit RRF-ranked-high via fts/graph/bm25 multi-channel agreement but **missed by the vector lane** gets relevance ≈0.03 and **sinks below every vector hit in the head**. cosine-low/RRF-high-but-right is exactly the demoted class; root is the A4 scale-split (`resolveSearchScore` reads `.score`/RRF first, `memory-search.ts:493-494`). Most-exposed strata: **cross_document (n=29) + graph_relationship (n=12)** queries where non-vector channels carry the answer. Instrument: per query, capture pre- vs post-reorder rank of each head row; flag `post>pre` AND row∈GT-relevant AND no `.similarity`.
3. **C3-s2-gate-verdict-accuracy-lane → EXTENDS A1 (H/M).** S2's top-dominant verdict (`assessRequestQuality confidence-scoring.ts:355-398`, `TOP_DOMINANT_THRESHOLD=0.8`) is doubly invisible to ranking metrics: it changes only the good/weak/gap label AND is called *only in the formatter* (`search-results.ts:1056`), never in the ablation searchFn; eval-db has no verdict table. Derive a citability label from graded relevance (≥1 grade-3 in top-K ⇒ should-be `good`; **hard_negative category, n=11**, ⇒ should-be `gap`); the regression cell = **false-`good` on hard_negatives** (the 0.8 path firing on a spurious cosine = over-citing). Specifies the exact labels + confusion cell A1 left abstract.
4. **C4-s3-stratified-recall-ab → NET-NEW (M/M).** S3 down-routes simple-tier (≤3 terms) to a channel-reduced path; only `isLowSignalShortQuery` escalates them back (`query-classifier.ts:157-166`). Flag OFF ⇒ everything full-pipeline = the recall ceiling. Golden set already tags `complexityTier` (n=37 simple). A/B: recall@k on simple-tier, `SPECKIT_COMPLEXITY_ROUTER` on/off, partitioned {escalated, non-escalated}. **Most-likely-net-negative lever:** a precise 3-term technical query (e.g. "FSRS cold tier") is simple-tier, low stop-word ratio, no trigger ⇒ NOT escalated ⇒ eats the channel-reduced route ⇒ recall loss; the escalation heuristic can't rescue content-bearing short queries.

## Key finding
**S5 (cosine top-N reorder) most needs measurement** — ships default-ON, self-describes as a "low-risk lift" (`search-flags.ts:634`) with zero recall evidence, yet (a) the harness can't even see it (skipped in `evaluationMode`, C1), and (b) it has a concrete silent-regression mechanism (demoting RRF-high/cosine-absent hits multi-channel fusion ranked correctly, C2). **The regression test:** golden set with `evaluationMode:false`, A/B `SPECKIT_COSINE_TOPN_REORDER`, report Δ nDCG@1 (all queries) + the relevant-non-vector-demotion count (C2 instrument) on cross_document + graph_relationship. Regression = positive demotion count with flat/negative nDCG@1 (reorder paying top-1 cosine cosmetics by sinking correctly-fused non-vector answers). This lane is the prerequisite for trusting C3/C4 (S5 re-permutes the same head they measure).

## Next focus
C2 is the campaign's sharpest correctness candidate (a shipped default-on lever with a demotion mechanism); flag it for adversarial-verify (does the demotion actually occur on real golden queries, or does the ≤10 depth + vector-lane coverage make it rare?). C1's evalMode blind-spot is a harness-correctness prerequisite for A3/A2 measurement.
