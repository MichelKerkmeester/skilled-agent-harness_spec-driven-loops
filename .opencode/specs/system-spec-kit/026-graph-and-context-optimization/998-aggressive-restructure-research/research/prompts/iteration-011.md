Framework: BUILD

# Iter 011 — Track 6: First-principles 11-surface taxonomy application — apply iter 044's domain decomposition to current 15-child state; identify remaining mismatches

You are a SWE-1.6 deep-research iteration worker (v1.0.4.1 recipe — sequential_thinking mandatory pre-output). Stay read-only EXCEPT for the iter output file. Cite evidence with file:line.

## Pre-planning

**Sequential_thinking mandatory**: call mcp__sequential_thinking__sequentialthinking with ≥ 5 thoughts before producing the output.

**Goal:** First-principles 11-surface taxonomy application — apply iter 044's domain decomposition to current 15-child state; identify remaining mismatches

**Steps:**
1. Read prior iter outputs in this packet (iteration-001.md through iteration-010.md) for context
2. Read relevant 026 child packets via grep/ls
3. Apply the scoped analysis per the iter goal above
4. Cite every claim with <ref_file file="<path>" lines="N-M" /> tags
5. Emit findings + JSONL delta row

**Acceptance:** ≥ 3 file:line citations per major claim; JSONL row appended.

**Stop:** Emit iteration-011.md then exit.

## Research Question

First-principles 11-surface taxonomy application — apply iter 044's domain decomposition to current 15-child state; identify remaining mismatches

Cite all claims via <ref_file lines="N-M" /> tags. Reference prior iter (001 onward) where relevant.

## Output contract

**Write to:** `research/iterations/iteration-011.md`

Required heading structure:

```
# Iter 011 — Track 6: First-principles 11-surface taxonomy application — apply iter 044's domain decomposition to current 15-child state; identify remaining mismatches

## Question
## Evidence (<ref_file> tags)
## Findings (sub-sections per finding type)
## Gaps for next iter
## JSONL delta row
{"iter_id": "011", "timestamp_utc": "<ISO8601>", "executor": "cli-devin", "model": "swe-1.6", "track": <track-int>, "status": "complete", "findings_count": <int>, "primary_evidence_files": [<paths>]}
```

## Context

Packet 998 deeper restructure research after Wave 1 (packet 107). Apply iter contract v1.0.4.1: sequential_thinking → output, citations dense, JSONL row mandatory.
