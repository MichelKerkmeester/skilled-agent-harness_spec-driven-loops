Framework: BUILD

# Iter 002 — Track 1: 000 sub-cluster identification — group 59 children into 5-7 candidate sub-phases (release-readiness vs cleanup vs audit vs post-program vs followup)

You are a SWE-1.6 deep-research iteration worker (v1.0.4.1 recipe — sequential_thinking mandatory pre-output). Stay read-only EXCEPT for the iter output file. Cite evidence with file:line.

## Pre-planning

**Sequential_thinking mandatory**: call mcp__sequential_thinking__sequentialthinking with ≥ 5 thoughts before producing the output.

**Goal:** 000 sub-cluster identification — group 59 children into 5-7 candidate sub-phases (release-readiness vs cleanup vs audit vs post-program vs followup)

**Steps:**
1. Read prior iter outputs in this packet (iteration-001.md through iteration-001.md) for context
2. Read relevant 026 child packets via grep/ls
3. Apply the scoped analysis per the iter goal above
4. Cite every claim with <ref_file file="<path>" lines="N-M" /> tags
5. Emit findings + JSONL delta row

**Acceptance:** ≥ 3 file:line citations per major claim; JSONL row appended.

**Stop:** Emit iteration-002.md then exit.

## Research Question

000 sub-cluster identification — group 59 children into 5-7 candidate sub-phases (release-readiness vs cleanup vs audit vs post-program vs followup)

Cite all claims via <ref_file lines="N-M" /> tags. Reference prior iter (001 onward) where relevant.

## Output contract

**Write to:** `research/iterations/iteration-002.md`

Required heading structure:

```
# Iter 002 — Track 1: 000 sub-cluster identification — group 59 children into 5-7 candidate sub-phases (release-readiness vs cleanup vs audit vs post-program vs followup)

## Question
## Evidence (<ref_file> tags)
## Findings (sub-sections per finding type)
## Gaps for next iter
## JSONL delta row
{"iter_id": "002", "timestamp_utc": "<ISO8601>", "executor": "cli-devin", "model": "swe-1.6", "track": <track-int>, "status": "complete", "findings_count": <int>, "primary_evidence_files": [<paths>]}
```

## Context

Packet 998 deeper restructure research after Wave 1 (packet 107). Apply iter contract v1.0.4.1: sequential_thinking → output, citations dense, JSONL row mandatory.
