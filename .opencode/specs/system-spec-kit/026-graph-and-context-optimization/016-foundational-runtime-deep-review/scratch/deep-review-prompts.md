# Deep-Review Prompts

## H1 Prompt — `session-stop.ts`

Review `.opencode/skill/system-spec-kit/mcp_server/hooks/claude/session-stop.ts` as a producer-side continuity authority with emphasis on missing-session handling, spec-folder retargeting, and autosave durability. Start with `runContextAutosave()` at lines `60-105`, then inspect `processStopHook()` at `220-319`, the CLI timeout wrapper at `331-336`, and `detectSpecFolder()` at `340-417`. Treat the file as a state-mutating hook, not just a parser. I want exact findings, not hypothetical style advice.

Focus on four questions. First, analyze the safety of `const sessionId = input.session_id ?? 'unknown'` at line `240`: can real stop events omit `session_id`, and if they do, does hashing the literal string `"unknown"` collapse unrelated sessions into one shared state file? Second, trace the retargeting path at `281-296`: when `detectedSpec !== stateBeforeStop.lastSpecFolder`, is the overwrite always intended, or can long transcripts with multiple packet references silently move autosave to the wrong spec folder? Third, check the write-then-read sequence at `283` and `308-309`: is there any real guarantee that the state update is durably visible before `runContextAutosave()` reloads state? Fourth, assess fail-open autosave at `85-101`: does the hardcoded `AUTOSAVE_TIMEOUT_MS = 4000` at line `34` make continuity capture materially lossy under cold start or embedding-provider latency?

Cross-check against `mcp_server/tests/hook-session-stop.vitest.ts` and any replay coverage, but emphasize missing tests for `processStopHook()` end-to-end, timeout handling, and the missing-session collision case. Return P0/P1/P2 findings with exact line numbers and propose the narrowest safe fixes.

## H2 Prompt — `hook-state.ts`

Review `.opencode/skill/system-spec-kit/mcp_server/hooks/claude/hook-state.ts` as the authoritative tempdir state store for Claude hook continuity. Start with path derivation at `58-70`, then inspect `loadMostRecentState()` at `109-167`, `saveState()` at `170-181`, `updateState()` at `221-241`, and stale cleanup at `244-263`. Treat this file as an actual persistence layer whose failure semantics shape startup, stop, and compact behavior.

Investigate four risk themes. First, analyze concurrency: `updateState()` is a read-modify-write path with no lock, and `saveState()` uses a deterministic `filePath + '.tmp'` temp name at line `173`. Can two overlapping hook writers for the same session lose data or overwrite each other's temp content before rename? Second, inspect load safety: `JSON.parse(raw) as HookState` at line `87` applies no runtime validation. Determine whether corrupt JSON, truncated files, or stale schema shapes collapse into silent reinitialization instead of explicit quarantine. Third, evaluate scope correctness: `loadMostRecentState()` at `118-129` requires `specFolder` or `claudeSessionId`; does that incorrectly reject legitimate `agentId`/`userId`-scoped lookups used elsewhere in governed flows? Fourth, assess stale cleanup: because `cleanStaleStates()` uses filesystem `mtime` rather than JSON `updatedAt`, can backup restore, `touch`, or external copy operations distort cleanup behavior?

Use `mcp_server/tests/hook-state.vitest.ts` and related integration tests as coverage evidence, but call out missing concurrency, corrupt-state, and schema-evolution tests. Return only concrete findings with exact lines, severity, and the minimum viable hardening changes.

## H3 Prompt — `shared-payload.ts`

Review `.opencode/skill/system-spec-kit/mcp_server/lib/context/shared-payload.ts` as the shared trust/provenance contract layer for graph and continuity payloads. Begin with the value sets and prohibited-key definitions at `14-90` and `193-199`, then examine `detectorProvenanceToParserProvenance()` at `298-307`, `validateStructuralTrustPayload()` at `371-401`, `buildStructuralContextTrust()` at `407-431`, `isStructuralTrustComplete()` at `495-504`, `createSharedPayloadEnvelope()` at `546-589`, and the trust-state helpers at `592-614`.

Concentrate on contract honesty and semantic loss. First, determine whether `isStructuralTrustComplete()` is incorrectly implemented as a throwing predicate because it routes through the throwing construction path. Second, inspect whether `trustStateFromGraphState()` and related helpers collapse semantically distinct states such as `missing`, `empty`, and `stale` in ways that deprive callers of the recovery information they need. Third, verify whether `buildStructuralContextTrust()` has a documented and safe default for unrecognized status values, or whether it silently downgrades them into the all-unknown triple. Fourth, assess runtime validation completeness: `SharedPayloadKind` and `producer` are string-literal unions but do not appear to have exported runtime validator arrays. Decide whether this creates a gap between TypeScript typing and actual runtime safety.

Cross-check with `mcp_server/tests/graph-payload-validator.vitest.ts` and `structural-trust-axis.vitest.ts`, but focus on the missing direct tests for throwing predicates, zero-section envelopes, and unknown statuses. Return concrete findings, exact lines, and recommended narrow fixes that preserve the split trust-axis model instead of collapsing it again.

## H4 Prompt — `graph-metadata-parser.ts`

Review `.opencode/skill/system-spec-kit/mcp_server/lib/graph/graph-metadata-parser.ts` as the integrity authority for `graph-metadata.json`. Start with status normalization and legacy parsing at `115-208` and `223-255`, then inspect key-file resolution at `409-455` and `553-652`, derived key-file/status logic at `791-870`, final derivation at `890-929`, merge behavior at `938-950`, and file write at `969-989`. Treat this file as a parser/migrator/writer, not just a schema helper.

Focus on four questions. First, analyze the JSON-valid-but-schema-invalid path at `223-255`: can a malformed modern JSON file silently re-enter the system as "legacy" content with `ok: true`, and if so, does the caller get any signal that migration happened? Second, inspect `resolveSpecFolderPath()` around `480-494`: is the basename fallback ever safe, or does it fabricate single-segment `spec_folder` values that should instead fail? Third, probe cross-track probing at `553-577` and `622-652`: can same-named files in adjacent spec trees be accepted as valid key files for the wrong packet? Fourth, review concurrency and determinism: `process.pid + Date.now()` temp naming at line `985` and the insertion-order entity cap at line `774` both look fragile.

Use `mcp_server/tests/graph-metadata-schema.vitest.ts` as the direct coverage baseline, but emphasize missing tests for legacy fallback activation, cross-track false positives, basename fallback, concurrent writers, and entity-priority rules. Return exact line-numbered findings plus the smallest safe hardening steps.

## H5 Prompt — `adaptive-fusion.ts`

Review `.opencode/skill/system-spec-kit/shared/algorithms/adaptive-fusion.ts` as the adaptive ranking seam between intent classification and final retrieval order. Begin with the profile definitions at `60-69`, rollout/env logic at `81-115`, adaptive weight computation at `138-188`, fusion core at `203-250`, standard fallback at `370-388`, and the main exported entry path at `356-429` plus export surface at `435-438`.

Investigate contract drift between declared and actual behavior. First, determine whether the `continuity` profile is truly intended to be private even though `INTENT_WEIGHT_PROFILES` is exported publicly; if it is internal-only, assess how easily a future caller could activate it accidentally. Second, trace `graphCausalBias`: it is present in the type and on every profile, but does any ranking path actually consume it? If not, classify the severity of the dead contract and the risk that tests are giving false confidence. Third, inspect env handling: with `SPECKIT_ROLLOUT_PERCENT` parsing at `99-103`, does malformed configuration fail open to full rollout? Fourth, audit fallback honesty: after adaptive-fusion errors, do returned `weights` describe the algorithm that really ran, or do callers see adaptive-looking weights on standard-fusion results?

Cross-check `mcp_server/tests/intent-weighting.vitest.ts` and any adaptive-fusion coverage you find, but focus on missing tests for malformed rollout config, dead causal bias, and mismatch between reported weights and executed path. Return only concrete line-numbered findings and precise, low-risk fixes.

## H6 Prompt — `reconsolidation-bridge.ts`

Review `.opencode/skill/system-spec-kit/mcp_server/handlers/save/reconsolidation-bridge.ts` as the scope-boundary and reconsolidation governor for `/memory:save`. Start with mode classification at `72-94`, requested-scope normalization and candidate filtering at `177-236`, the main reconsolidation flow at `243-444`, and assistive recommendation handling at `453-512`. Treat this as a correctness boundary: a bug here can cross scopes or duplicate persisted memory.

Focus on four concrete questions. First, inspect the over-fetch + scope-filter pattern at `283-294`: when 3x candidates are fetched and all are filtered out, does the caller learn that filtering removed them, or does the bridge silently present "no similar memories" as if there were no candidates? Second, trace the complement path at line `394`: can `reconsolidate()` invoke the provided `storeMemory` callback before the bridge falls through to normal CREATE, producing a duplicate write? Third, analyze the warning/result contract around line `500`: `assistiveRecommendation` is attached as a property on the warnings array. Determine how easily that side channel is lost during serialization, spreading, or downstream normalization. Fourth, assess the misleading exported constant `ASSISTIVE_AUTO_MERGE_THRESHOLD` at `478-481`: the name implies auto-merge/archive behavior, but the visible implementation appears advisory-only.

Use `mcp_server/tests/reconsolidation-bridge.vitest.ts`, `assistive-reconsolidation.vitest.ts`, and save-path tests as evidence, but call out missing tests for zero-survivor filtering, complement double-write, array-property transport loss, and the threshold-name/behavior mismatch. Return exact findings with line numbers and the narrowest trustworthy repairs.

## H7 Prompt — `post-insert.ts`

Review `.opencode/skill/system-spec-kit/mcp_server/handlers/save/post-insert.ts` as the planner-aware post-insert enrichment controller. Start with `shouldRunPostInsertEnrichment()` at `63-65`, then analyze the ordered pipeline at `80-215`, and finish with the deferred wrapper at `217-242`. Treat the file as a workflow contract layer whose return values tell callers whether enrichment actually happened.

I want a truthfulness-focused review. First, inspect every place where `enrichmentStatus` is set to `true` (`99`, `112`, `125`, `133`, `152`, `155`, `179`, `181`, `207`, `208`, and deferred status at `227-234`). Determine whether the returned booleans encode anything more than "this branch finished executing" and whether callers can distinguish success, skip, failure, and defer. Second, analyze the deferred contract at `235`: `followUpAction: 'runEnrichmentBackfill'` appears to promise later recovery. Verify whether any consumer exists; if not, classify this as dead contract or misleading API. Third, trace dependency correctness around `159-161` and `187`: does entity linking run even after entity extraction failed, and does enabling entity linking unintentionally enable graph lifecycle even when graph refresh is meant to stay off? Fourth, inspect observability: the file uses `console.error` for informational success messages, which may create false operational alarms.

Cross-check `mcp_server/tests/post-insert-deferred.vitest.ts` and any save-path tests, but emphasize missing direct tests for deferred truthfulness, follow-up-action consumption, and step-outcome semantics. Return concrete line-numbered findings plus the smallest changes needed to make the contract honest.
