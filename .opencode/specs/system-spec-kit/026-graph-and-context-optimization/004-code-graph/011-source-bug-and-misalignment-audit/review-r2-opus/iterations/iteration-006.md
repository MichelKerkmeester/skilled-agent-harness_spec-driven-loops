<!-- deep-review iteration (Opus 4.8, Workflow round 6/10) -->
# Iteration 6 — DR-010-01 intra-phase cancellation + BUG-06 [correctness]

> Opus-4.8 finder + 2 independent adversarial skeptics per finding. A finding is CONFIRMED-REAL only if BOTH skeptics agree it is outstanding in current code.

### OR-6-01 [P2] [traceability] (test-false-confidence) DR-010-01/BUG-06 abort path has NO regression test despite commit claiming '+ regression tests'

file: `.opencode/skills/system-code-graph/mcp_server/tests/phase-runner.test.ts:211-284 (no signal/abort test); cf. structural-indexer.ts:2138-2144, phase-runner.ts:276-282`

why: Commit 5ec1caf4ee is titled 'land DR-010-01 intra-phase abort + regression tests', but the only test-tree change touching the cancellation surface is a single relaxation in ensure-ready.vitest.ts (line ~253: changed an exact toHaveBeenCalledWith to expect.objectContaining so the new signal option does not break the assertion, using a fully-mocked indexFilesMock that never parses or honors the signal). phase-runner.test.ts has zero coverage of the new signal? parameter / abort-at-phase-boundary throw. No structural-indexer test passes options.signal to exercise the per-file abort in parseCandidates. No test drives indexWithTimeout with a real deadline abort. The fix CODE is correct (verified: per-file check is reachable because parseFile awaits getParser() and yields, persistIndexedFileResult only runs after the race resolves so no partial DB write, and the timeout-message wins the Promise.race over the wrapped PhaseRunnerError so the abort does not mask the real timeout). The gap is purely that a future regression of either guard (e.g. removing the per-file check, or making the loop never yield) would not be caught by any test, and the commit message overstates coverage.

fix: Add two small unit tests: (1) phase-runner.test.ts — runPhases(phases, signal) where an AbortController is aborted before/after the first phase, asserting it throws 'runPhases aborted before phase "..."' and does not invoke later phase bodies; (2) structural-indexer (or a focused ensure-ready/indexWithTimeout test using a real fixture dir with several files) that aborts mid-parse and asserts parseCandidates throws the 'parse-candidates aborted (deadline signal)' error and that no files were persisted. Optionally correct the commit narrative in any continuity/summary doc that repeats the '+ regression tests' claim for DR-010-01.

confidence: 0.86 | related: DR-010-01

- skeptic verdict: **FALSE_POSITIVE** (refuted=true, sev→none, conf=0.93)
  The finding's factual sub-claims are true (no DR-010-01 abort test exists), but its load-bearing thesis — "commit claiming '+ regression tests' overstates coverage" and a "test-false-confidence" traceability defect — is REFUTED by a misreading of the actual commit and doc.  VERIFIED FIX CODE (correct, as the finder concedes): structural-indexer.ts:2142-2144 has the per-file `if (options.signal?.aborted) throw new Error('parse-candidates aborted (deadline signal) after N parsed')` guard at the top of the parse-candidates loop; phase-runner.ts:280-281 has the between-phase `if (signal?.aborted) throw 'runPhases aborted before phase ...'` guard; indexFiles threads `options.signal` into `runPhases(phases, options.signal)` (structural-indexer.ts:2218).  VERIFIED COVERAGE GAP (real, but not what the finding claims): grep of phase-runner.test.ts returns NO matches for signal/abort/AbortController; no test in the entire tests/ tree asserts the 'parse-candidates aborted' string; the only cancellation-surface test change (ensure-ready.vitest.ts:250-254) is a `toHaveBeenCalledWith` -> `expect.objectContaining` relaxation against a fully-mocked indexFilesMock that never honors the signal.  WHY THE THESIS IS REFUTED — the commit DOES NOT overstate coverage: 1. Commit 5ec1caf4ee body verbatim scopes "Regression tests for the cleanly-testable pre-existing fixes" to exactly DR-003-02 / DR-008-01 / DR-008-04 / DR-001-01 — NOT DR-010-01. 2. The same commit body explicitly DISCLAIMS a DR-010-01 test: "Concurrency CAS / lease races and the DR-010-01 mid-phase abort remain hard to cover deterministically (noted honestly in the investigation doc)." 3. The investigation doc (.../011-source-bug-and-misalignment-audit/review/pre-existing-findings-investigation.md, added in the same commit) has a dedicated "Coverage honesty" section stating DR-010-01's per-file abort is "verified by typecheck + manual reasoning + the live launcher smoke, not by a dedicated unit test," and even explains the subtle reason (a pre-aborted signal trips runPhases' between-phase check first).  So the documented coverage is already accurate; the finding's proposed fix to "correct the commit narrative" is moot. The "test-false-confidence" category does not apply: that category requires a test that passes against the un-fixed code, giving false confidence. Here there is an ABSENCE of a test that the authors disclosed in writing — an honestly-documented gap, not false confidence and not a traceability defect. The underlying "add abort unit tests" observation is real but is self-disclosed operator/author WIP, already on the record, not a NEW issue and not a landed-fix-that-is-wrong. It does not meet the bar for a reportable new finding; severity none.
- skeptic verdict: **CONFIRMED_REAL** (refuted=false, sev→P2, conf=0.9)
  Adversarial lens default is "refuted unless I can cite the exact current line proving the issue is genuinely OUTSTANDING." I can, and the LANDED REMEDIATION does NOT close this gap. The remediation list explicitly enumerates new regression tests for DR-001-01, DR-008-01, DR-008-04, and DR-003-02, and conspicuously omits any DR-010-01 abort regression test — confirming the gap rather than closing it.  Every factual claim in OR-6-01 verifies against current code: (1) phase-runner.test.ts has an empty diff since baseline and zero signal/abort coverage; (2) no AbortController appears anywhere in the test tree; (3) the sole cancellation-surface test change is the ensure-ready.vitest.ts assertion relaxation, which runs against a fully-mocked vi.fn() indexFilesMock that never honors the signal; (4) the commit message's '+ regression tests' overstates coverage for DR-010-01 specifically.  This is a legitimate traceability / test-false-confidence finding under reporting criterion (a) (genuinely NEW — prior gpt-5.5 review flagged DR-010-01 as a code finding, now fixed, but never flagged the absence of a regression test) with the spirit of (d) (coverage gives false confidence: a future regression of either the per-file guard or runPhases boundary check would pass undetected). The finding correctly concedes the FIX CODE is correct (I independently confirmed: runPhases honors signal at phase-runner.ts:280-281, the per-file guard at 2138-2144 is reachable because parseFile awaits getParser() and yields, and persistIndexedFileResult only runs after the race resolves so there is no partial DB write), so there is no correctness or security impact — P2 traceability is the right severity, matching the finder's own P2 assignment. No downgrade warranted; no over-statement to correct. The proposed fix (add a runPhases abort-boundary test and a structural-indexer/indexWithTimeout mid-parse abort test, plus correcting any continuity doc that repeats the '+ regression tests' claim) is sound and proportionate.

**RESULT: REFUTED / ALREADY-FIXED / FALSE-POSITIVE**

---

coverage: Round 6 (correctness) focus: DR-010-01 intra-phase cancellation + BUG-06 deadline plumbing across structural-indexer.ts, phase-runner.ts, ensure-ready.ts. Read all three current files and git-diffed each vs baseline 0ef38d58. VERIFIED CORRECT: (a) phase-runner.ts:266-309 — runPhases now takes signal?: AbortSignal and throws at each phase boundary before starting the next phase; cheap and cannot corrupt the pipeline (results are computed phase-by-phase, abort just stops before the next one). (b) structural-indexer.ts:2137-2144 — parseCandidates checks options.signal?.aborted at the top of the per-file loop; this is genuinely reachable because parseFile (line 1226-1263) does `await getParser()` and yields to the event loop, so the indexWithTimeout timer's controller.abort() becomes visible between files (one-file granularity). The all-fresh-skip fast path does not await but is pure-synchronous mtime checks that finish near-instantly, so missing the abort there is harmless. (c) indexFiles (2206-2245) threads options.signal into runPhases and re-throws on error after backfilling the error-outcome histogram. (d) ensure-ready.ts indexWithTimeout (530-569) builds the AbortController, passes controller.signal into indexFiles, and races against an abort-listener promise that rejects with 'Auto-indexing timed out after Nms'. NO partial-DB-leak: persistIndexedFileResult runs ONLY after the race resolves successfully; on timeout/abort the race rejects and the persistence loop is skipped entirely. NO masking of the real timeout: the abort-listener rejects synchronously on abort(), winning the Promise.race over indexFiles' eventually-thrown PhaseRunner-wrapped abort. NO missed-abort window: the listener is registered in the same synchronous race-array construction before any abort can fire. NO masking of real parse errors: per-file parse failures are caught locally (catch {} at 2160) and are orthogonal to the top-of-loop abort guard. Confirmed the ONLY signal-passing caller is indexWithTimeout; handlers/scan.ts:383 calls indexFiles without a signal, so explicit code_graph_scan is never spuriously aborted. The wrapped-error nuance (runPhases re-wraps the parseCandidates abort as PhaseRunnerError('phase-failure')) is benign for current paths since the only signal-bearing caller already wins its race on the timeout message; no finding raised. One finding (OR-6-01, P2 traceability/test-false-confidence): the abort path has no regression test despite the fix commit claiming '+ regression tests' — the sole test change is an assertion relaxation in ensure-ready.vitest.ts using a mocked indexFiles that never honors the signal. Cross-referenced prior reviews: DR-010-01 was the originating finding and the code remediation is correct; this is a coverage/claim gap, not a re-report of the original bug. No new correctness defect found in the abort logic itself.

newFindings: 0 (confirmed-real), rawFindings: 1, dimension: correctness
