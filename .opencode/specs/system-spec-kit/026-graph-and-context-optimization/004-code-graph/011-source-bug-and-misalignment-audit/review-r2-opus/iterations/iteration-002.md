<!-- deep-review iteration (Opus 4.8, Workflow round 2/10) -->
# Iteration 2 — Launcher lease CAS + legacy probing (DR-002-01/02, DR-003-01) [correctness]

> Opus-4.8 finder + 2 independent adversarial skeptics per finding. A finding is CONFIRMED-REAL only if BOTH skeptics agree it is outstanding in current code.

### OR-2-01 [P2] [correctness] (test-false-confidence) Sole concurrency test exercises the wx fresh-lease path, not the DR-002-01 reclaim re-read CAS

file: `.opencode/skills/system-code-graph/mcp_server/tests/launcher-lease.vitest.ts:207-227`

why: The DR-002-01 fix added a re-read-after-write CAS to the RECLAIM branch of acquireOwnerLeaseFile (mk-code-index-launcher.cjs lines 373-386), which runs only when an `existing` stale owner lease is present. The only concurrency regression test, 'lets exactly one concurrent launcher own the lease' (line 207), spawns two launchers from a fresh createWorkspace() (tmpdir, no seeded owner lease, line 24-42). With no `existing` lease, both launchers take the `writeOwnerLeaseFileExclusive` (O_EXCL `wx`) fresh path (lines 360-371), whose single-winner guarantee comes entirely from O_EXCL and predates DR-002-01. The reclaim-path re-read CAS (the actual code DR-002-01 changed) is never reached: 'reclaims a stale-heartbeat owner lease with a live PID' (line 309) and 'reclaims a dead-pid lease' (line 296) each spawn only ONE launcher against a pre-seeded stale lease, so no two processes ever concurrently hit the reclaim branch. The test therefore passes identically against the un-fixed code (delete lines 377-384 and 'exactly one concurrent launcher' still goes green), giving false confidence that the reclaim CAS is verified.

fix: Add a regression test that seeds a stale-heartbeat owner lease (ttlMs small, lastHeartbeatIso > 2*ttl old, ownerPid = a LIVE helper pid so classifyOwnerLease returns 'stale-heartbeat-reclaim'), then spawns TWO launchers simultaneously so both read the same `existing` stale lease and both enter the reclaim branch. Assert exactly one ends up as the recorded owner (readOwnerLeasePid) and the other reports/bridges — this is the only scenario that distinguishes the re-read CAS from plain last-writer-wins.

confidence: 0.83 | related: DR-002-01

- skeptic verdict: **CONFIRMED_REAL** (refuted=false, sev→P2, conf=0.86)
  The finding reproduces against current code. Every structural claim was verified directly: (1) the DR-002-01 re-read CAS lives in the reclaim branch gated on a pre-existing stale `existing` owner lease (cjs:351,373-386); (2) the fresh path is O_EXCL and predates the fix (cjs:360-371); (3) the sole truly-concurrent launcher test seeds NO owner lease (fresh createWorkspace) so both processes take O_EXCL, not reclaim; (4) every reclaim-triggering test spawns exactly one launcher, so two processes never concurrently hit the reclaim branch; (5) the library-level owner-lease test likewise only has a 'fresh' concurrent test and single-caller reclaim tests. Removing CAS lines 377-384 would not turn the test red, so the test gives false confidence that the reclaim CAS is verified — squarely deep-review category (d). It is genuinely NEW: the prior gpt-5.5 review found and tracked the DR-002-01 fix itself (remediation-summary line 119) but never flagged the test-coverage gap; the parallel Opus review's only test-coverage mention is generic P2 boilerplate scoped entirely to ensure-ready.ts. `rg` for launcher-lease/concurrent-reclaim coverage in both prior reports returned nothing. P2 is correct: the DR-002-01 fix CODE is correct (verified at cjs:376-386), so there is no code defect and no production risk — this is purely a test-traceability/false-confidence hygiene gap, exactly as self-rated. Confidence 0.86 rather than higher only because one could argue the library's fresh-concurrent test plus single-caller reclaim assertions give indirect coverage of the underlying overwrite semantics; but neither exercises the concurrent-reclaim race the CAS specifically guards, so the false-confidence characterization stands.
- skeptic verdict: **CONFIRMED_REAL** (refuted=false, sev→P2, conf=0.9)
  I read the actual current code and verified all of the finding's claims hold. (1) The DR-002-01 re-read CAS is confined to the reclaim branch (lines 373-386), reached only when an `existing` stale lease is present; the diff against 0ef38d58 confirms these lines are this session's addition. (2) The sole concurrency test (207) uses a fresh workspace with no seeded owner lease, so both launchers take the O_EXCL fresh path (360-371) whose guarantee predates DR-002-01 — the reclaim CAS is never exercised. (3) Both reclaim tests (296, 309) spawn a single launcher, so the re-read at line 377 trivially sees its own pid; removing the CAS lines keeps them green. Hence no regression test distinguishes the reclaim re-read CAS from plain last-writer-wins, giving false confidence that DR-002-01's reclaim path is verified.  Adversarial 'already-mitigated' lens fails: the LANDED REMEDIATION list enumerates new regression tests for DR-003-02, DR-008-01, DR-008-04, and DR-001-01, but NOT for DR-002-01's reclaim-concurrency path. The stale-heartbeat reclaim test added this session is single-process and does not close the gap. This is genuinely outstanding in current code.  This is a NEW finding (test-false-confidence dimension): prior reviews (gpt-5.5-fast and the parallel Opus daemon-shutdown review) reported the production bugs, all of which are now fixed; none of them flagged that the reclaim CAS lacks a concurrent-reclaim regression test. It is not a duplicate.  Severity P2 is correct: this is a test-quality/false-confidence issue. The production CAS code at lines 376-384 is itself correct and protective for a real two-launcher-race-on-stale-lease scenario; only the test coverage is missing. There is no runtime correctness, security, or data-loss impact — so it does not rise to P1. The proposed fix (seed a stale-heartbeat owner lease with a live ownerPid, then spawn two launchers concurrently, asserting exactly one becomes recorded owner and the other backs off) is the correct and minimal remediation that would distinguish the re-read CAS from last-writer-wins. Confidence 0.9: claims are directly grounded in current-code lines; the only residual uncertainty is whether a maintainer considers untested-but-correct hardening code worth a dedicated test, which is a judgment call rather than a factual dispute.

**RESULT: CONFIRMED-REAL**

---

coverage: Round 2 / correctness / launcher lease CAS + legacy probing. Read the current .opencode/bin/mk-code-index-launcher.cjs in full for the lease subsystem and git-diffed it vs 0ef38d58; cross-checked parity against the canonical .opencode/skills/system-code-graph/mcp_server/lib/owner-lease.ts (acquire/refresh/release + mutation-lock CG-016/017) and traced the bridge socket derivation in .opencode/bin/lib/launcher-ipc-bridge.cjs.\n\nVerified each landed fix is correct AND complete:\n- DR-002-01 acquireOwnerLeaseFile (lines 373-386): atomic tmp+rename write then re-read confirming own pid; two concurrent reclaimers -> exactly one survivor. SOUND. It diverges from canonical (canonical re-classifies `current` while holding the mutation lock at lines 409-421 and aborts if now-live; launcher blindly overwrites then checks only its own pid), but I traced this to be benign: the owner-lease reclaim is backstopped by the PID-lease gate isLeaseHeld() at lines 900-905, which bridges instead of double-spawning whenever the prior launcher pid is still live. The launcher also has NO recurring heartbeat (refreshOwnerLeaseFile is called once at line 778 to transfer ownership to the child pid), so owner leases always go stale-heartbeat after 2*ttl and the PID lease is the real liveness signal by design. No split-brain, no double-daemon, no data loss -> not a P1.\n- DR-002-01 refreshOwnerLeaseFile (389-404): ownership check pre-write, re-read post-write vs nextOwnerPid. SOUND.\n- DR-002-02 legacyLeasePaths (220-228) + formerLocationOwnerLive/pidLiveAt (437-452): probes the former .spec-kit path AND the skill/ typo path; migration liveness check covers BOTH .mk-code-index-launcher.json (pid) and .code-graph-owner.json (ownerPid). pidLiveAt fails safe (live/EPERM->true, ESRCH/missing/corrupt->false); a pid<=0 corrupt lease would spuriously over-block migration (safe direction, P2-only, not worth a finding). Migration never copies a DB a live owner could be writing. SOUND.\n- DR-003-01 leaseHeldFromFile uid check (454-468): foreign-owned legacy lease -> not held / not reclaimable. SOUND. Noted the bridge socket is always derived from resolvedDbDir() (primary) in launcher-ipc-bridge.cjs line 64, not from the legacy lease, so the spoofed-socket vector is already mitigated by socket-dir resolution; the uid check is correct defense-in-depth, not the sole guard.\n- DR-008-02 acquireBootstrapLock rename-claim (lines ~739-749): atomic rename-to-stale-name before rmSync; losing racer's rename ENOENTs into retry. SOUND.\n- DR-008-03 clearOwnerLeaseFile/IfOwner double re-read (406-435): residual sub-syscall window honestly documented. SOUND.\n- respawnAfterDeadSocket (537-585): owner re-acquire runs under requireLock bootstrap lock with writeOwnerLeaseFileExclusive (wx) -> single-winner. SOUND.\n- classifyOwnerLease (328-345): faithful single-dir subset of canonical classifyOwner; lacks 'symlink-alias' but that only triggers when candidateDbDir is passed, which the launcher never does. Not a gap.\n\nNo new correctness/security bugs and no incorrect/incomplete landed fix found in this focus area. The single finding is a precisely-scoped test-false-confidence gap: the reclaim-path CAS that DR-002-01 actually changed is not exercised by any regression test (all concurrency coverage hits the pre-existing O_EXCL fresh-lease path).

newFindings: 1 (confirmed-real), rawFindings: 1, dimension: correctness
