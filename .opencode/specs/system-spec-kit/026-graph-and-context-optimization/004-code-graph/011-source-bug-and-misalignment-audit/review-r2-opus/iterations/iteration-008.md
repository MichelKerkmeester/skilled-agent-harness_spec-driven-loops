<!-- deep-review iteration (Opus 4.8, Workflow round 8/10) -->
# Iteration 8 — Doc/code contract alignment (016f47b4) [traceability]

> Opus-4.8 finder + 2 independent adversarial skeptics per finding. A finding is CONFIRMED-REAL only if BOTH skeptics agree it is outstanding in current code.

### OR-8-01 [P2] [traceability] (fix-incomplete) `absent` still presented as a top-level readiness/contract state in §2 table + ARCHITECTURE, contradicting the remediation's own SKILL.md correction

file: `.opencode/skills/system-code-graph/references/readiness/readiness_and_scope_fingerprint.md:57 (and ARCHITECTURE.md:136)`

why: The remediation's SKILL.md fix (016f47b4) now states explicitly: "Freshness states are `fresh`, `stale`, `empty`, `error` (`absent` is not a freshness state — it is the companion trust-state projection of an `empty` graph)". But two sibling docs were left unchanged and still present `absent` as a top-level readiness/contract state: (a) readiness_and_scope_fingerprint.md §2 "READINESS STATES" table line 57 lists an `absent` row claiming "`code_graph_status` returns `absent`"; (b) ARCHITECTURE.md §136 "Readiness contract ... States are `fresh`, `stale`, `empty`, `error`, `absent`". Verified against code: `GraphFreshness` is only `fresh|stale|empty|error` (readiness-contract.ts:9; ensure-ready.ts assigns only those four — apply-orchestrator.ts:524 enumerates exactly those). `canonicalReadiness` is `ready|stale|missing` (canonicalReadinessFromFreshness: empty→missing, error→missing — never `absent`). `absent` appears ONLY as a `trustState`/`goldVerificationTrust` projection of `empty` (queryTrustStateFromFreshness: empty→absent, readiness-contract.ts:112; getGoldVerificationTrust returns 'absent' only when no verification record, status.ts:173). So `code_graph_status` never returns a top-level `readiness`/`freshness`/`canonicalReadiness` of `absent`; when no DB exists it emits freshness:empty / canonicalReadiness:missing / trustState:absent. This is an incomplete propagation of the same `absent`-vs-freshness cleanup the remediation landed in SKILL.md, leaving the skill's own docs internally inconsistent. Distinct from prior CG-011 (which covered only the §3 trustState verified/unverified/quarantined vocabulary, now correctly fixed to live/stale/absent/unavailable + goldVerificationTrust).

fix: Propagate the SKILL.md framing to the two sibling docs: in readiness §2 either rename the table to make clear it mixes the freshness enum with derived projections, or annotate the `absent` row that it surfaces via `trustState: absent` / `canonicalReadiness: missing` (the top-level freshness for a missing DB is `empty`), not a literal `readiness: "absent"`. In ARCHITECTURE.md §136 change "States are `fresh`, `stale`, `empty`, `error`, `absent`" to the four-value freshness enum, optionally noting `absent` as the empty-graph trust projection.

confidence: 0.72 | related: CG-011

- skeptic verdict: **CONFIRMED_REAL** (refuted=false, sev→P2, conf=0.86)
  Not refuted — confirmed as a fix-incomplete (rubric category b: a landed FIX that is itself incomplete). The session's SKILL.md correction (016f47b4) introduced the precise framing that `absent` is not a freshness state but the companion trust-state projection of `empty`. Two sibling docs were left at the pre-baseline framing that presents `absent` as a top-level readiness/contract state: readiness_and_scope_fingerprint.md:57 (with the demonstrably wrong "code_graph_status returns absent" behavior note) and ARCHITECTURE.md:136. The code-grounded claim is accurate: GraphFreshness is exactly fresh|stale|empty|error, canonicalReadiness maps empty/error→missing, and absent surfaces only via trustState (empty→absent) and goldVerificationTrust (no record). So code_graph_status never emits a top-level readiness/freshness of absent; for a missing DB it emits freshness:empty / canonicalReadiness:missing / trustState:absent.  The underlying absent text in both siblings predates baseline 0ef38d58 (verified: both lines are unchanged context lines in git diff 0ef38d58). That alone would push toward PRE_EXISTING. But the relevant defect here is the internal inconsistency the session itself created by correcting only SKILL.md, and the §2 Read-path Behavior cell ("code_graph_status returns absent") is an active factual inaccuracy in current code. This qualifies as a genuinely outstanding, newly-surfaced traceability gap attributable to the incomplete propagation of the session's own fix — distinct from CG-011 (which covered only §3 trustState vocabulary, now correctly fixed). Severity P2 is correct: pure doc hygiene, zero behavior/correctness/security impact; no code path consumes these doc strings. Confidence 0.86: code facts and git provenance are solid; the only judgment call is the PRE_EXISTING-vs-fix-incomplete framing, and the session-introduced inconsistency plus the live inaccurate behavior note justify CONFIRMED_REAL at P2.
- skeptic verdict: **CONFIRMED_REAL** (refuted=false, sev→P2, conf=0.88)
  Every factual claim verified against current source. The remediation (016f47b4) corrected SKILL.md to state `absent` is not a freshness state, and rewrote §3 of readiness_and_scope_fingerprint.md (the CG-011 trustState fix), but did NOT propagate the framing to (a) the §2 READINESS STATES table line 57, which still presents `absent` as a readiness state and asserts "`code_graph_status` returns `absent`", and (b) ARCHITECTURE.md:136, which still enumerates `absent` in the freshness/readiness state list. Code proves the top-level freshness/canonicalReadiness for a missing DB is `empty`/`missing` with `absent` only as a trustState/goldVerificationTrust projection. The git diff confirms these two lines were untouched, so the "already-mitigated" lens does not hold — this is a genuine incompleteness of the landed doc remediation that leaves the skill's own docs internally inconsistent. It is distinct from CG-011 (which only addressed §3's verified/unverified/quarantined → live/stale/absent/unavailable vocabulary). Severity is correctly P2: a documentation-traceability inconsistency, not a code-correctness bug — the described read-path behavior (blocked + code_graph_scan) remains accurate, so no operator is misled into a wrong action. Held confidence at 0.88 (not higher) because §2's `absent` row is defensible as an operator-facing "no graph yet" concept rather than a strictly false statement; it is the internal-consistency mismatch with the same-session SKILL.md correction, not factual falsehood, that makes it a real fix-incomplete finding.

**RESULT: CONFIRMED-REAL**

---

coverage: Round 8 (traceability — doc/code contract alignment for 016f47b4). VERIFIED CORRECT + COMPLETE: (1) database_path_policy.md migration direction reversed correctly — current canonical = skill-local `system-code-graph/mcp_server/database/`, former = `.opencode/.spec-kit/code-graph/database/` (superseded 2026-05-29). Confirmed real file bytes via `od -c`/`grep -c` (the `.opencode/n/database/` rendering in rg output was a display artifact, not file corruption — actual content is `.opencode/.spec-kit/code-graph/database/`). Matches code: core/config.ts:14-20 defaultDir = skill-local; launcher cjs:5-6,848-859 migrates FROM `.spec-kit` BACK TO skill-local (copy not move), skips when SPECKIT_CODE_GRAPH_DB_DIR set (line 857), targets resolvedDbDir(). (2) naming_conventions.md §5 + DB-dir table row corrected to skill-local. (3) launcher_lease.md PID-file path corrected to skill-local mcp_server/database/. (4) ARCHITECTURE.md Storage path + ADR-003 single-writer wording corrected (status no longer writes readiness marker). (5) README.md SPECKIT_CODE_GRAPH_DB_DIR default + playbook group count (10→9) corrected. (6) SKILL.md coordination-boundary path corrected. (7) INSTALL_GUIDE.md auto-migrate-back wording correct. TOOL COUNTS VERIFIED: tool-schemas.ts CODE_GRAPH_TOOL_SCHEMAS = exactly 8 (codeGraphScan/Query/Status/Context/ClassifyQueryIntent/Verify/Apply/detectChanges); code-graph-tools.ts ALLOWED list + dispatch switch = same 8. ownership_boundary.md fixed: removed the duplicated `code_graph_status`/`code_graph_scan`/`code_graph_verify` tail, now lists the 8 cleanly. Scanned all skill .md for stale tool counts — no `11/10/9/7/6 tools` remain; all say `8 tools` with correct names. The `deep_loop_graph_*` coverage-graph feature_catalog entries correctly marked internal .cjs scripts, not MCP tools (arc-118). READINESS/TRUST AXES VERIFIED against code: readiness §3 now correctly documents trustState (live/stale/absent/unavailable, fresh→live/stale→stale/empty→absent/error→unavailable per readiness-contract.ts queryTrustStateFromFreshness) and goldVerificationTrust (live/stale/absent per status.ts getGoldVerificationTrust:167-194 — absent=no record, stale=non-fresh/old/failed, live=passed+in-window) as two separate axes, and correctly separates parser quarantine into parserHealth — this fully resolves prior CG-011. FIX-009-v3 per-call scope exception (scan-argument trusts stored scope) documented and consistent with the scope-policy intent. One incomplete-propagation finding (OR-8-01, P2): the `absent`-is-not-a-freshness-state correction landed in SKILL.md but not in readiness §2 table or ARCHITECTURE.md §136, which still present `absent` as a top-level readiness/contract state — pre-existing, but now internally contradictory with the fixed SKILL.md. No P0/P1. No new regression-test false-confidence in scope (this round is doc-only).

newFindings: 1 (confirmed-real), rawFindings: 1, dimension: traceability
