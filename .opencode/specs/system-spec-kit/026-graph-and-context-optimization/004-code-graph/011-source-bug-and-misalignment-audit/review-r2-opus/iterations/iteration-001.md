<!-- deep-review iteration (Opus 4.8, Workflow round 1/10) -->
# Iteration 1 — Launcher migration-back (DR-001-02/006-02) [correctness]

> Opus-4.8 finder + 2 independent adversarial skeptics per finding. A finding is CONFIRMED-REAL only if BOTH skeptics agree it is outstanding in current code.

### OR-1-01 [P1] [correctness] (fix-incomplete) DR-001-02/006-02 fix incomplete: migration-back still runs BEFORE any lock; two concurrent launchers can both migrate and the second can clobber the first's now-live target DB

file: `.opencode/bin/mk-code-index-launcher.cjs:857-879 (migration block); guards 883/913 (locks acquired AFTER)`

evidence:

```
Line 857-860 guard: `if (!process.env.SPECKIT_CODE_GRAPH_DB_DIR && !exists(path.join(migrationTarget,'code-graph.sqlite')) && exists(path.join(formerSharedDbDir,'code-graph.sqlite')) && !formerLocationOwnerLive(formerSharedDbDir))`. Line 873: `fs.copyFileSync(src, dst)` (no flags). Line 883: `acquireOwnerLeaseFile()` — AFTER migration. Line 913: `acquireBootstrapLock()` — AFTER migration. `formerLocationOwnerLive(dir)` at 449-452 probes only `dir` = formerSharedDbDir (the source). Prior review review-report.md:48 explicitly recommended "Acquire the bootstrap/owner lease before migrating".
```

why: The prior review's DR-001-02/DR-006-02 recommendation had two parts: (1) honor SPECKIT_CODE_GRAPH_DB_DIR and (2) "Acquire the bootstrap/owner lease BEFORE migrating" so two concurrent launchers cannot both migrate (review-report.md:48). Part (1) is correctly landed (verified: line 857 `!process.env.SPECKIT_CODE_GRAPH_DB_DIR` skip + `migrationTarget = resolvedDbDir()` at 856). Part (2) was NOT implemented — instead the remediation added `formerLocationOwnerLive(formerSharedDbDir)` (line 860), which probes liveness of the SOURCE/former location only. The migration copy at lines 861-878 still executes in the main IIFE BEFORE `acquireOwnerLeaseFile()` (line 883) and BEFORE `acquireBootstrapLock()` (line 913), with no mutual exclusion. Window: two launchers L1/L2 start concurrently into the default skill-local target; both evaluate `!exists(migrationTarget/code-graph.sqlite)` as true (neither has copied yet), both pass the dead-former-owner check, both enter the block. Worse case: L1 finishes its `fs.copyFileSync` (line 873, no COPYFILE_EXCL — unconditional truncate+overwrite, confirmed), then proceeds to win the owner lease (347-376) and launch the daemon, which opens and actively writes the target code-graph.sqlite; L2, slightly behind, having already passed the `!exists` guard before L1's copy created the file, now runs `copyFileSync(former -> target)` and truncates/overwrites the live DB out from under L1's running daemon -> SQLite corruption / data loss on the live writer. `formerLocationOwnerLive` cannot prevent this because it inspects the source (former) location, never the target. This is the exact data-loss hazard class DR-001-02 was meant to eliminate, merely relocated from source-side to target-side.

fix: Move the migration block to run AFTER acquireBootstrapLock() succeeds (only the single bootstrap-lock winner migrates), or guard the copy with COPYFILE_EXCL plus a per-target lock. Lock-before-migrate (the prior review's recommendation) is the robust fix: only one launcher per workspace holds the bootstrap lock, so only it performs the copy, and it does so before opening the DB. At minimum, re-check `!exists(migrationTarget/code-graph.sqlite)` while holding the lock and use `fs.copyFileSync(src, dst, fs.constants.COPYFILE_EXCL)` so a late copier refuses to overwrite an existing (possibly live) target.

confidence: 0.78 | related: DR-001-02

- skeptic verdict: **CONFIRMED_REAL** (refuted=false, sev→P1, conf=0.82)
  The finding is genuinely outstanding and correctly classified as fix-incomplete (report category (b): a landed FIX that is itself incomplete). Verified against current code, not speculation.  Part (1) of the prior review's DR-001-02/DR-006-02 recommendation (honor SPECKIT_CODE_GRAPH_DB_DIR skip + target resolvedDbDir()) landed correctly (857, 856). Part (2) — review-report.md:48 verbatim: "Acquire the bootstrap/owner lease before migrating" — was NOT implemented. The migration copy still executes before acquireOwnerLeaseFile (883) and acquireBootstrapLock (913). The remediation substituted formerLocationOwnerLive(), a SOURCE-side liveness probe, which structurally cannot detect a concurrently-written TARGET.  Reproduction trace (cold-start race, the exact "two launchers can both migrate" hazard the prior review named): L1 and L2 start concurrently after the 2026-05-29 relocation, target DB absent, former DB present, no live former owner. Both evaluate guard 857-860 as true (neither has copied yet, so !exists(target) holds for both, and formerLocationOwnerLive probes only the dead former owner). Both enter. L1 finishes copyFileSync, wins owner lease + bootstrap lock, launches daemon which opens and writes target code-graph.sqlite. L2 — already past the !exists guard before L1's copy created the file — runs copyFileSync(former -> target) at 873 with no COPYFILE_EXCL, truncating/overwriting the live DB under L1's running writer => SQLite corruption / data loss on the live writer. This is a real TOCTOU: guard evaluated before lock, non-exclusive copy, no re-check under any lock.  Distinct from CG-017 (owner-lease.ts refresh TOCTOU, different file/mechanism) and from the source-side hazard the remediation addressed — the hazard was relocated to the target side, not eliminated. The concurrent-launcher scenario is the precisely plausible trigger the single-writer machinery (lease bridging for parallel sessions/MCP reconnects, lines 884-897) exists to handle, so it is not a contrived condition.  Severity P1 (not P0): requires the narrow first-startup-after-relocation window AND two concurrent cold launchers AND no live former owner — a plausible-but-not-normal-use condition, matching the P1 rubric. Outcome is data loss/corruption when it does fire. The proposed fix (move migration after acquireBootstrapLock so only the single lock winner copies, or re-check !exists under lock + COPYFILE_EXCL) is sound and matches the prior review's original recommendation. confidence 0.82: structural claims are exact against current source; the residual uncertainty is purely empirical timing (the race window between L2's guard read and L1's copy is small, and migration is a one-time post-relocation event), but the absence of any mutual exclusion makes the hazard real rather than theoretical.
- skeptic verdict: **CONFIRMED_REAL** (refuted=false, sev→P1, conf=0.85)
  Not refuted. This is finding category (b): a landed fix (DR-001-02/006-02) that is incomplete relative to its own documented recommendation. The prior review explicitly called for "acquire the lease before migrating" to stop two concurrent launchers from both migrating; the remediation only added source-side liveness (`formerLocationOwnerLive`) and the override skip, leaving the concurrent-migrate race unaddressed.  Race is real and reachable: two launchers cold-start into the default skill-local target with a former `.spec-kit` DB present and no live source owner. Both pass `!exists(migrationTarget/code-graph.sqlite)` (858) before either copies, both pass `!formerLocationOwnerLive` (source has no live owner), both enter the unguarded block. L1 copies (873), wins owner lease (883) + bootstrap lock (913), launches the daemon (926) which opens the target SQLite for writing. L2 — having passed line 858 before L1's copy — reaches its own copyFileSync (873, no COPYFILE_EXCL) inside the L1-spawn-to-DB-open window and truncates/overwrites the live DB → SQLite corruption / code-graph data loss. The single-process flow is await-free so the hazard is strictly cross-process, exactly the class the bootstrap lock exists to serialize — but migration runs before that lock.  Severity P1, not P0: the migration block is one-shot (after first success, `exists(target/code-graph.sqlite)` is permanently true and the block never re-runs), so the window exists only on the first concurrent cold-start after the 2026-05-29 relocation on machines still holding the former-location DB. Plausible condition, genuine DB corruption — squarely P1. Proposed fix is sound and matches the prior recommendation: gate migration behind the bootstrap lock (single winner copies), and/or use COPYFILE_EXCL plus a recheck-of-`!exists` while holding the lock. Confidence 0.85: structural facts are certain from current code; the residual uncertainty is empirical race-window width, but the absence of any cross-process exclusion around an overwriting copy makes the hazard well-founded.

**RESULT: CONFIRMED-REAL**

---

coverage: Verified the DR-001-02/DR-006-02 launcher migration-back remediation against baseline 0ef38d58 by reading the current mk-code-index-launcher.cjs and diffing it (git diff 0ef38d58). Confirmed CORRECT and complete: (a) SPECKIT_CODE_GRAPH_DB_DIR override skip — `!process.env.SPECKIT_CODE_GRAPH_DB_DIR` guard at line 857 means an override run no longer auto-seeds the default skill-local DB; (b) migration target resolves via `resolvedDbDir()` (line 856), which also path-asserts within workspace root; (c) `formerLocationOwnerLive()` (449-452) correctly probes BOTH the former PID lease (.mk-code-index-launcher.json/pid) and former owner lease (.code-graph-owner.json/ownerPid) via `pidLiveAt`, with EPERM treated as live and ESRCH/missing as dead — sound source-side liveness; (d) copy-not-move with `.code-graph-owner.json` intentionally excluded from dbFiles (862-868) so the former owner lease is not carried forward — verified the list copies sqlite/shm/wal/readiness/pid-lease only; (e) WAL/SHM copy is safe on the SOURCE side because the dead-source liveness gate guarantees no concurrent writer mutating the former DB during copy. ONE incomplete-fix found (OR-1-01, P1): the prior review's two-part recommendation was only half-implemented. The override half is fixed, but the \"acquire lease BEFORE migrating\" half was replaced by a source-side liveness probe that does not serialize two concurrent launchers; the migration copy still runs pre-lock (before both acquireOwnerLeaseFile at 883 and acquireBootstrapLock at 913), and `fs.copyFileSync` has no COPYFILE_EXCL, so the two-concurrent-migrate window the prior review flagged remains open — and is upgraded to a live-target-clobber path when the first migrator becomes the daemon and a lagging second migrator overwrites the now-open DB. I considered but did NOT report: (i) the copied former PID lease creating a stale/PID-reuse false-held lease at the new target — gated dead-at-copy-time by formerLocationOwnerLive and overwritten by writeLeaseFile at 919, narrow pre-existing window, dominated by OR-1-01; (ii) source-side WAL torn copy — precluded by the dead-source gate. Did not re-report the override fix or resolvedDbDir target since both are correct and complete.

newFindings: 1 (confirmed-real), rawFindings: 1, dimension: correctness
