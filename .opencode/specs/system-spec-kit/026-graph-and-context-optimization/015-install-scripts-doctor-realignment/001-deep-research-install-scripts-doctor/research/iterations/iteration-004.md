# Iteration 4: Q4 systematic 116 deep-skill-evolution impact sweep

## Focus
Systematically sweep install, script, doctor, and directly related advisor/index surfaces for 116 deep-skill-evolution stale references: renamed-away `sk-*` deep skill names, old `sk-ai-council`, and deep-loop runtime DB relocation paths. Prior iteration findings were treated as already-known and excluded unless this pass found a distinct newer drift.

## Actions Taken
Read the deep-research state and strategy, then reviewed iteration 001-003 findings to avoid re-reporting known misses.
Built the current skill inventory with `ls .opencode/skills | sort`; current deep-family skills are `deep-agent-improvement`, `deep-ai-council`, `deep-loop-runtime`, `deep-research`, and `deep-review`, while `sk-ai-council`, `sk-deep-research`, `sk-deep-review`, and `sk-deep-agent-improvement` directories are absent.
Checked rename/relocation history with `git log --oneline --all -- .opencode/skills | rg -i '116|rename|deep-'`, `git log --diff-filter=R --summary -- .opencode/skills`, and targeted history for old `sk-*` deep directories.
Grepped the requested bounded surfaces for old names and paths: `.opencode/skills/*/INSTALL_GUIDE.md`, `.opencode/skills/*/scripts`, `.opencode/skills/*/mcp_server/scripts`, `.opencode/scripts`, `.github/hooks`, `.opencode/commands/doctor`, plus the advisor skill graph/fixtures.
Read hit contexts in advisor regression fixtures, routing-accuracy corpus files, optimizer manifest/scripts, system-spec-kit parity tests, doctor routes/docs, and current deep-loop-runtime docs/source.

## Findings
- **[P1 / STALE-LIVE]** `.opencode/skills/system-skill-advisor/mcp_server/scripts/fixtures/skill_advisor_regression_cases.jsonl:17` — regression cases still expect `sk-deep-research` at lines 17-18, `sk-deep-review` at lines 21-23, and `sk-deep-agent-improvement` at lines 42-45, while current skill directories are `deep-research`, `deep-review`, and `deep-agent-improvement`; `advisor-validate.ts:187-192` loads this fixture as a live validation input, so advisor validation can preserve or score against renamed-away skill ids.
- **[P1 / STALE-LIVE]** `.opencode/skills/system-skill-advisor/mcp_server/scripts/routing-accuracy/labeled-prompts.jsonl:24` — the default routing-accuracy corpus uses `skill_top_1:"sk-deep-research"` for resume/deep-loop rows at lines 24-25, 28, 41-42, and 147-148, and `skill_top_1:"sk-deep-review"` at lines 26, 43-44, 46, 48, 149, 152-153; `score-routing-corpus.py:20-24` makes this file the default dataset, so live corpus scoring can mark canonical `deep-*` advisor output wrong or train reports around old labels.
- **[P2 / STALE-LIVE]** `.opencode/skills/system-spec-kit/scripts/optimizer/optimizer-manifest.json:15` — optimizer tunable `configPaths` still point at `.opencode/skills/sk-deep-research/assets/deep_research_config.json` and `.opencode/skills/sk-deep-review/assets/deep_review_config.json` at lines 15-16, repeated at lines 26-27 and 37-38, while current files exist under `.opencode/skills/deep-research/assets/deep_research_config.json` and `.opencode/skills/deep-review/assets/deep_review_config.json`; `optimizer/search.cjs:33-36` loads this manifest as canonical optimizer input.
- **[P2 / STALE-LIVE]** `.opencode/skills/system-spec-kit/scripts/tests/deep-research-contract-parity.vitest.ts:14` — parity tests still probe `.opencode/skills/sk-deep-research/scripts/runtime-capabilities.cjs` and primary docs at lines 28-34, while `graph-aware-stop.vitest.ts:17` also points its reducer fixture at `../../../sk-deep-research/scripts/reduce-state.cjs`; because those paths no longer exist and the `deep-research` equivalents do, the live test surface silently skips or misses contract coverage for the current skill.
- **[P2 / STALE-LIVE]** `.opencode/skills/system-spec-kit/scripts/tests/deep-review-contract-parity.vitest.ts:15` — the deep-review parity test still probes `.opencode/skills/sk-deep-review/scripts/runtime-capabilities.cjs` and reads primary docs at lines 30-35, while the current resolver and docs live under `.opencode/skills/deep-review`; the optional `describe.skip` guard means the live test suite can skip current deep-review parity instead of validating it.
- **[P1 / STALE-LIVE]** `.opencode/commands/doctor/_routes.yaml:92` — the canonical `/doctor deep-loop` route still sets `gate3_location` to `.opencode/skills/deep-loop-runtime/storage/deep-loop-graph.sqlite`, also repeated in `speckit.md:47`, `update.md:219`, and `update.md:270`, while current `deep-loop-runtime` docs and source place the DB at `database/deep-loop-graph.sqlite` (`deep-loop-runtime/SKILL.md:80`, `README.md:66`, `coverage-graph-db.ts:99-103`); live doctor routing and update docs now point at a stale post-relocation path, distinct from the already-reported `mcp_server/database/deep-loop-graph.sqlite` miss.

## Ruled Out
- **[INFO / CORRECT]** `.opencode/skills/system-skill-advisor/mcp_server/scripts/skill-graph.json:87` — the advisor skill graph itself uses current `deep-agent-improvement`, `deep-ai-council`, `deep-loop-runtime`, `deep-research`, and `deep-review` ids, so the stale `sk-deep-*` issue is in fixtures/corpora, not the live graph file.
- **[INFO / ALREADY-REPORTED]** `.opencode/skills/system-spec-kit/scripts/test-council-matrix.sh:14` — the `sk-ai-council` script casualty remains valid, but iteration 002 already reported it as a Q4 note, so this pass does not count it as new.
- **[INFO / ALREADY-REPORTED]** `.opencode/commands/doctor/assets/doctor_deep-loop.yaml:78` and `.opencode/commands/doctor/assets/doctor_update.yaml:106` — stale `mcp_server/database/deep-loop-graph.sqlite` allow-list entries remain valid, but iteration 003 already reported them; this pass only adds the newly discovered `deep-loop-runtime/storage` to `deep-loop-runtime/database` drift.
- **[INFO / HISTORICAL]** `.opencode/skills/system-spec-kit/scripts/observability/smart-router-measurement-results.jsonl:164` — old `labelSkill` and `advisorTopSkill` rows are measurement output records with generated timestamps, not live routing configuration; keep as historical evidence unless a script treats them as an input corpus.

## Questions Answered
Q4 is substantially answered for the bounded install/scripts/doctor surface set. The systematic sweep found additional 116 casualties beyond iterations 1-3: advisor validation fixtures, routing-accuracy corpus labels, optimizer config paths, deep-research/deep-review parity tests, and doctor deep-loop storage/database path drift.

## Questions Remaining
Q5 remains: cross-surface and 4-runtime consistency should verify whether the same `deep-*` ids, command wrappers, and DB paths are consistent across `.opencode`, `.claude`, `.codex`, and `.gemini` surfaces after these Q4 misses are fixed.

## Next Focus
Q5 cross-surface/4-runtime consistency sweep, starting from the newly expanded casualty list: advisor fixtures/corpora, optimizer manifest, system-spec-kit deep-loop tests, and doctor deep-loop route/docs.
