● Checking my documentation
  └ # GitHub Copilot CLI Documentation

I’m pulling the actual CLI reference so I can give documented flags, not just examples from memory.

✗ Fetching web content https://docs.github.com/api/article/body?pathname=/en/copilot/reference/cli-…
  └ Permission denied and could not request permission from user

● Web Search (MCP: github-mcp-server) · GitHub Copilot CLI reference documented flags -p --model --…
  └ {"type":"output_text","text":{"value":"Here’s a summary of the key docum...

**`-p`** runs a prompt non-interactively so `copilot` can execute a single request and exit, which is useful for scripts and automation.

**`--model`** selects the model used for the run, letting you pin the request to a specific supported model instead of the default.

**`--no-ask-user`** prevents the agent from stopping to request extra user input, making the run fully non-interactive.

PASS


Changes   +0 -0
Requests  1 Premium (26s)
Tokens    ↑ 95.9k • ↓ 998 • 90.4k (cached) • 723 (reasoning)
