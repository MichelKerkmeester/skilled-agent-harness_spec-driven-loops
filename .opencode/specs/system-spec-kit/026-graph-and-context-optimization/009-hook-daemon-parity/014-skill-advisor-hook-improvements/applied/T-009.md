# Applied: T-009
## Source
- Task description: Publish aggregate-vs-runtime threshold semantics in validation outputs for `014-F-005` (`.opencode/skill/system-spec-kit/mcp_server/skill-advisor/handlers/advisor-validate.ts`, `.opencode/skill/system-spec-kit/mcp_server/skill-advisor/schemas/advisor-tool-schemas.ts`, `.opencode/skill/system-spec-kit/mcp_server/skill-advisor/README.md`).
- Priority: P1
- Finding IDs: F-005

## Target Files Modified
| Path | Change Summary | Lines Touched |
| --- | --- | --- |
| `.opencode/skill/system-spec-kit/mcp_server/skill-advisor/handlers/advisor-validate.ts` | Switched validation gates to `0.75 / 0.725 / 0.7`, surfaced them under `thresholdSemantics`, and kept runtime routing at `0.8 / 0.35`. | `56-57`, `248`, `316`, `325`, `354-366` |
| `.opencode/skill/system-spec-kit/mcp_server/skill-advisor/schemas/advisor-tool-schemas.ts` | Added strict schema coverage for `thresholdSemantics`. | `123-135` |
| `.opencode/skill/system-spec-kit/mcp_server/skill-advisor/README.md` | Added the operator-facing explanation of aggregate validation vs prompt-time routing thresholds. | `222-226` |

## Evidence of Change
Before:
```ts
const fullSlice = countSlice(full.correct, corpus.length);
const holdoutSlice = countSlice(holdoutResult.correct, holdout.length);
```

After:
```ts
const fullSlice = countSlice(full.correct, corpus.length, FULL_CORPUS_THRESHOLD);
const holdoutSlice = countSlice(holdoutResult.correct, holdout.length, HOLDOUT_THRESHOLD);
```

Validation output excerpt:
```json
"thresholdSemantics": {
  "aggregateValidation": {"fullCorpusTop1":0.75,"holdoutTop1":0.725,"perSkillTop1":0.7,"unknownCountTargetMax":10},
  "runtimeRouting": {"confidenceThreshold":0.8,"uncertaintyThreshold":0.35,"confidenceOnly":false}
}
```

## Verification
- Syntax / type check / anchor check: `handleAdvisorValidate(...)` smoke returned the new threshold block and the updated slice thresholds in live JSON.
- Test result: `./node_modules/.bin/vitest run ./skill-advisor/tests/handlers/advisor-validate.vitest.ts --reporter=dot` -> `Tests 3 passed (3)`.

## Deferred
- None.
