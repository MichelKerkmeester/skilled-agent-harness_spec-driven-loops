# Applied: T-008
## Source
- Task description: Add explicit workspace selection across public advisor tools for `014-F-004` (`.opencode/skill/system-spec-kit/mcp_server/skill-advisor/schemas/advisor-tool-schemas.ts`, `.opencode/skill/system-spec-kit/mcp_server/skill-advisor/handlers/advisor-recommend.ts`, `.opencode/skill/system-spec-kit/mcp_server/skill-advisor/handlers/advisor-validate.ts`, `.opencode/skill/system-spec-kit/mcp_server/skill-advisor/README.md`).
- Priority: P1
- Finding IDs: F-004

## Target Files Modified
| Path | Change Summary | Lines Touched |
| --- | --- | --- |
| `.opencode/skill/system-spec-kit/mcp_server/skill-advisor/schemas/advisor-tool-schemas.ts` | Added optional `workspaceRoot` input to recommend/validate and required `workspaceRoot` to public outputs. | `25`, `49`, `99`, `121` |
| `.opencode/skill/system-spec-kit/mcp_server/skill-advisor/handlers/advisor-recommend.ts` | Resolved explicit workspace roots before status lookup, caching, and scoring. | `163-170`, `191`, `211-245` |
| `.opencode/skill/system-spec-kit/mcp_server/skill-advisor/handlers/advisor-validate.ts` | Resolved explicit workspace roots before corpus loading and telemetry reads. | `301-310`, `352` |
| `.opencode/skill/system-spec-kit/mcp_server/skill-advisor/README.md` | Updated examples to show `workspaceRoot` on every public tool call that can infer it. | `69-77`, `210` |

## Evidence of Change
Before:
```text
advisor_validate({"skillSlug":null})
```

After:
```text
advisor_validate({"confirmHeavyRun":true,"workspaceRoot":"/absolute/path/to/repo","skillSlug":null})
```

## Verification
- Syntax / type check / anchor check: direct `handleAdvisorRecommend(...)` and `handleAdvisorValidate(...)` smokes both returned the resolved repository root in their public JSON data.
- Test result: `./node_modules/.bin/vitest run ./skill-advisor/tests/handlers/advisor-validate.vitest.ts --reporter=dot` -> `Tests 3 passed (3)`.

## Deferred
- None.
