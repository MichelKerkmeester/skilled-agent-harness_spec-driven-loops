# Applied: T-005
## Source
- Task description: Implement OpenCode effective-threshold unification for `014-F-001` (`.opencode/plugins/spec-kit-skill-advisor.js`, `.opencode/plugin-helpers/spec-kit-skill-advisor-bridge.mjs`, `.opencode/skill/system-spec-kit/mcp_server/skill-advisor/lib/skill-advisor-brief.ts`, `.opencode/skill/system-spec-kit/references/hooks/skill-advisor-hook.md`).
- Priority: P1
- Finding IDs: F-001

## Target Files Modified
| Path | Change Summary | Lines Touched |
| --- | --- | --- |
| `.opencode/plugins/spec-kit-skill-advisor.js` | Aligned the OpenCode plugin default threshold with the shared `0.8` runtime default. | `28` |
| `.opencode/plugin-helpers/spec-kit-skill-advisor-bridge.mjs` | Reused a single `effectiveThresholds` object for native and fallback bridge routes. | `8-9`, `94`, `193-300` |
| `.opencode/skill/system-spec-kit/mcp_server/skill-advisor/lib/skill-advisor-brief.ts` | Centralized threshold resolution under exported shared helpers. | `86-97`, `131-156` |
| `.opencode/skill/system-spec-kit/references/hooks/skill-advisor-hook.md` | Updated the hook reference to document the `0.8 / 0.35` OpenCode bridge contract. | `62`, `82-83`, `138-142` |

## Evidence of Change
Before:
```js
return 0.7;
```

After:
```js
return DEFAULT_CONFIDENCE_THRESHOLD;
```

Bridge runtime excerpt:
```json
"effectiveThresholds":{"confidenceThreshold":0.8,"uncertaintyThreshold":0.35,"confidenceOnly":false}
```

## Verification
- Syntax / type check / anchor check: direct bridge smoke returned `workspaceRoot` plus the unified `effectiveThresholds` object on the active bridge route.
- Test result: `./node_modules/.bin/vitest run ./skill-advisor/tests/compat/plugin-bridge.vitest.ts --reporter=dot` -> `Tests 4 passed (4)`.

## Deferred
- None.
