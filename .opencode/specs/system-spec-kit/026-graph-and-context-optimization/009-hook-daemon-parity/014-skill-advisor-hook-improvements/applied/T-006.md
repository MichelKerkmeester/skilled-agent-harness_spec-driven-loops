# Applied: T-006
## Source
- Task description: Route OpenCode native rendering onto shared brief invariants for `014-F-002` (`.opencode/plugin-helpers/spec-kit-skill-advisor-bridge.mjs`, `.opencode/skill/system-spec-kit/mcp_server/skill-advisor/lib/render.ts`, `.opencode/skill/system-spec-kit/mcp_server/skill-advisor/tests/compat/plugin-bridge.vitest.ts`).
- Priority: P1
- Finding IDs: F-002

## Target Files Modified
| Path | Change Summary | Lines Touched |
| --- | --- | --- |
| `.opencode/plugin-helpers/spec-kit-skill-advisor-bridge.mjs` | Replaced the branch-local native brief formatter with a shared `renderAdvisorBrief(...)` call over a synthetic hook result. | `218-265` |
| `.opencode/skill/system-spec-kit/mcp_server/skill-advisor/lib/render.ts` | Made the renderer accept caller-supplied threshold config so native and fallback branches use the same pass criteria. | `15-19`, `110-121` |
| `.opencode/skill/system-spec-kit/mcp_server/skill-advisor/tests/compat/plugin-bridge.vitest.ts` | Added assertions for the shared threshold metadata that backs the render contract. | `37-69` |

## Evidence of Change
Before:
```js
const brief = renderNativeBrief(data, input.maxTokens);
```

After:
```js
const rendered = modules.renderAdvisorBrief({...}, {
  tokenCap: maxTokens,
  thresholdConfig: effectiveThresholds,
});
```

## Verification
- Syntax / type check / anchor check: direct bridge smoke returned `Advisor: stale; use system-spec-kit 0.95/0.31 pass.` through the shared renderer path.
- Test result: `./node_modules/.bin/vitest run ./skill-advisor/tests/compat/plugin-bridge.vitest.ts --reporter=dot` -> `Tests 4 passed (4)`.

## Deferred
- None.
