# Applied: T-011
## Source
- Task description: Add accepted/corrected/ignored outcome capture for `014-F-007` (`.opencode/skill/system-spec-kit/mcp_server/skill-advisor/lib/metrics.ts`, `.opencode/skill/system-spec-kit/mcp_server/skill-advisor/handlers/advisor-validate.ts`, `.opencode/skill/system-spec-kit/mcp_server/skill-advisor/schemas/advisor-tool-schemas.ts`).
- Priority: P1
- Finding IDs: F-007

## Target Files Modified
| Path | Change Summary | Lines Touched |
| --- | --- | --- |
| `.opencode/skill/system-spec-kit/mcp_server/skill-advisor/lib/metrics.ts` | Added prompt-safe outcome record creation, persistence, and summary helpers. | `39`, `80-96`, `371-424` |
| `.opencode/skill/system-spec-kit/mcp_server/skill-advisor/handlers/advisor-validate.ts` | Accepted `outcomeEvents`, persisted them, and published live totals in the validator output. | `301-310`, `325`, `412-424` |
| `.opencode/skill/system-spec-kit/mcp_server/skill-advisor/schemas/advisor-tool-schemas.ts` | Added strict schemas for `outcomeEvents` input and `telemetry.outcomes` output. | `101-107`, `186-203` |

## Evidence of Change
Before:
```ts
const data = validateAdvisor(AdvisorValidateInputSchema.parse(args));
```

After:
```ts
for (const outcomeEvent of args.outcomeEvents ?? []) {
  persistAdvisorHookOutcomeRecord(workspaceRoot, createAdvisorHookOutcomeRecord({...}));
}
```

Validator output excerpt:
```json
"totals":{"accepted":1,"corrected":1,"ignored":1}
```

## Verification
- Syntax / type check / anchor check: the validator smoke accepted three outcome events and published their totals without exposing prompt text.
- Test result: `handleAdvisorValidate({ ..., outcomeEvents: [...] })` returned `recordedThisRun = 3` and `totals = { accepted: 1, corrected: 1, ignored: 1 }`.

## Deferred
- None.
