# Applied: T-003
## Source
- Task description: Establish explicit workspaceRoot and threshold-semantic baseline assertions for `014-F-004` and `014-F-005` (`.opencode/skill/system-spec-kit/mcp_server/skill-advisor/schemas/advisor-tool-schemas.ts`, `.opencode/skill/system-spec-kit/mcp_server/skill-advisor/handlers/advisor-recommend.ts`, `.opencode/skill/system-spec-kit/mcp_server/skill-advisor/handlers/advisor-validate.ts`, `.opencode/skill/system-spec-kit/mcp_server/skill-advisor/README.md`).
- Priority: P1
- Finding IDs: F-004, F-005

## Target Files Modified
| Path | Change Summary | Lines Touched |
| --- | --- | --- |
| `.opencode/skill/system-spec-kit/mcp_server/skill-advisor/schemas/advisor-tool-schemas.ts` | Added explicit `workspaceRoot`, `effectiveThresholds`, `thresholdSemantics`, and telemetry schema surfaces. | `25`, `49-55`, `99-107`, `121-203` |
| `.opencode/skill/system-spec-kit/mcp_server/skill-advisor/handlers/advisor-recommend.ts` | Normalized `workspaceRoot` selection and published the operator-facing threshold subset in every recommendation response. | `26`, `57-145`, `163-245` |
| `.opencode/skill/system-spec-kit/mcp_server/skill-advisor/handlers/advisor-validate.ts` | Added `workspaceRoot` selection, explicit validation thresholds, and public telemetry/threshold semantics. | `14-25`, `56-57`, `248-412` |
| `.opencode/skill/system-spec-kit/mcp_server/skill-advisor/README.md` | Documented the explicit workspaceRoot inputs and the new public threshold semantics. | `69-77`, `210`, `222-226` |

## Evidence of Change
Before:
```ts
export const AdvisorRecommendInputSchema = z.object({
  prompt: z.string().min(1).max(10_000),
```

After:
```ts
export const AdvisorRecommendInputSchema = z.object({
  workspaceRoot: z.string().min(1).optional(),
  prompt: z.string().min(1).max(10_000),
```

New runtime output excerpt:
```json
"effectiveThresholds": {
  "confidenceThreshold": 0.8,
  "uncertaintyThreshold": 0.35,
  "confidenceOnly": false
}
```

## Verification
- Syntax / type check / anchor check: direct `handleAdvisorRecommend(...)` smoke now returns `workspaceRoot` + `effectiveThresholds` and parses cleanly through the emitted Zod schema.
- Test result: `./node_modules/.bin/vitest run ./skill-advisor/tests/handlers/advisor-validate.vitest.ts --reporter=dot` -> `Tests 3 passed (3)`.

## Deferred
- None.
