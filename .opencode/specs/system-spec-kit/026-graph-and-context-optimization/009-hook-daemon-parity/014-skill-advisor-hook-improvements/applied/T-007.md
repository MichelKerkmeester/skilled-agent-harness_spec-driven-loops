# Applied: T-007
## Source
- Task description: Normalize the Codex prompt-time fast path against the shared builder for `014-F-003` (`.opencode/skill/system-spec-kit/mcp_server/hooks/codex/user-prompt-submit.ts`, `.opencode/skill/system-spec-kit/mcp_server/hooks/codex/prompt-wrapper.ts`, `.opencode/skill/system-spec-kit/mcp_server/skill-advisor/lib/skill-advisor-brief.ts`).
- Priority: P1
- Finding IDs: F-003

## Target Files Modified
| Path | Change Summary | Lines Touched |
| --- | --- | --- |
| `.opencode/skill/system-spec-kit/mcp_server/hooks/codex/user-prompt-submit.ts` | Deleted the native scorer branch and rendered Codex output with the shared threshold config. | `12-24`, `77`, `146-188`, `261-388` |
| `.opencode/skill/system-spec-kit/mcp_server/hooks/codex/prompt-wrapper.ts` | Matched the wrapper fallback to the same timeout + threshold contract as the main Codex hook. | `16-30`, `55`, `99-105`, `131-202`, `242` |
| `.opencode/skill/system-spec-kit/mcp_server/skill-advisor/lib/skill-advisor-brief.ts` | Exported shared threshold helpers used by the Codex runtime surfaces. | `86-97`, `131-186` |

## Evidence of Change
Before:
```ts
async function buildNativeCodexAdvisorBrief(...) { ... }
```

After:
```ts
async function buildCodexAdvisorBrief(
  prompt: string,
  options: { workspaceRoot: string },
): Promise<AdvisorHookResult> {
  return buildSkillAdvisorBrief(prompt, {
    runtime: 'codex',
    workspaceRoot: options.workspaceRoot,
    subprocessTimeoutMs: codexHookTimeoutMs(),
  });
}
```

## Verification
- Syntax / type check / anchor check: Codex direct-hook and prompt-wrapper smoke commands returned prompt-safe fail-open `{}` output and persisted diagnostics, with no branch-local fast-path output shape remaining.
- Test result: runtime smoke commands passed:
  - `node .../dist/hooks/codex/user-prompt-submit.js` -> diagnostic JSON + `{}`.
  - `node .../dist/hooks/codex/prompt-wrapper.js` -> `{}`.

## Deferred
- None.
