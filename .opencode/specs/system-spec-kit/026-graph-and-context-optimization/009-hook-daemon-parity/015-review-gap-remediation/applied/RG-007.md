# Applied: RG-007

## Source
- Severity: P1
- Bucket: `013-missed-files`
- Finding: The central MCP server README still documented `code_graph_status` and `code_graph_context` at the pre-013 contract level, so operators could not see `graphQualitySummary`, blocked-read payloads, or structured `partialOutput` metadata in the main reference.
- References:
  - `.opencode/specs/system-spec-kit/026-graph-and-context-optimization/research/013-014-review-gap-merged-synthesis.md`
  - `.opencode/specs/system-spec-kit/026-graph-and-context-optimization/research/013-014-review-gap-merged-findings.json`
  - `.opencode/skill/system-spec-kit/mcp_server/code-graph/handlers/status.ts`
  - `.opencode/skill/system-spec-kit/mcp_server/code-graph/handlers/context.ts`

## Target Files Modified
| Path | Change Summary |
|---|---|
| `.opencode/skill/system-spec-kit/mcp_server/README.md` | Refreshed the `code_graph_context` and `code_graph_status` tool-reference sections so they describe `status: "blocked"` read-path payloads, `data.metadata.partialOutput`, readiness/trust metadata, and `graphQualitySummary`. |
| `.opencode/specs/system-spec-kit/026-graph-and-context-optimization/009-hook-daemon-parity/015-review-gap-remediation/applied/RG-007.md` | Recorded the remediation scope, evidence, and verification for this packet-local fix. |

## Evidence of Change
```diff
-Get LLM-oriented compact graph neighborhoods... Responses include a `readiness` block...
+Get LLM-oriented compact graph neighborhoods... Successful responses return `status: "ok"` plus readiness/trust metadata ... When the read path determines the graph needs a full scan, the tool returns an explicit `status: "blocked"` payload ...
+`data.metadata.partialOutput` is structured and stable: `isPartial`, `reasons`, `omittedSections`, `omittedAnchors`, and `truncatedText`.
```

```diff
-Report code graph index health: file count, node and edge counts by type, parse health summary, last scan timestamp, DB file size and schema version.
+Report code graph index health: file count, node and edge counts by type, parse health summary, last scan timestamp, DB file size, schema version, readiness/trust fields for the current freshness probe, and `graphQualitySummary` ...
```

## Verification
- `sed -n '1047,1176p' .opencode/skill/system-spec-kit/mcp_server/README.md` to confirm the updated tool-reference text is present and limited to the intended sections.
- `git diff -- .opencode/skill/system-spec-kit/mcp_server/README.md .opencode/specs/system-spec-kit/026-graph-and-context-optimization/009-hook-daemon-parity/015-review-gap-remediation/applied/RG-007.md` to verify the edit scope stayed within the requested files.

## Deferred
- No automated test coverage was added because this remediation changes operator-facing documentation only and does not alter runtime behavior or schema code paths.
