# Applied: RG-020

## Source
- Severity: P1
- Bucket: `014-impl-defects`
- Finding: Durable outcome telemetry was not corruption-tolerant, so one malformed JSONL line could make `advisor_validate` throw while summarizing outcome totals.
- References:
  - `.opencode/specs/system-spec-kit/026-graph-and-context-optimization/research/013-014-review-gap-merged-synthesis.md`
  - `.opencode/specs/system-spec-kit/026-graph-and-context-optimization/research/013-014-review-gap-merged-findings.json`
  - `.opencode/specs/system-spec-kit/026-graph-and-context-optimization/009-hook-daemon-parity/014-skill-advisor-hook-improvements/review/iterations/iteration-06.md`

## Target Files Modified
| Path | Change Summary | Lines Touched |
|---|---|---|
| `.opencode/skill/system-spec-kit/mcp_server/skill-advisor/lib/metrics.ts` | Added a safe JSONL parse helper plus outcome-record shape validation, then filtered malformed/invalid outcome rows instead of letting `JSON.parse` abort the validation path. | 250-256, 399-427 |
| `.opencode/skill/system-spec-kit/mcp_server/skill-advisor/tests/handlers/advisor-validate.vitest.ts` | Added a regression that seeds corrupt durable outcome telemetry, asserts `advisor_validate` still returns `status: "ok"`, and verifies only valid records contribute to totals while restoring prior sink state after the test. | 5-15, 43-79 |

## Evidence of Change
```diff
+function tryParseJsonLine(line: string): unknown | null {
+  try {
+    return JSON.parse(line) as unknown;
+  } catch {
+    return null;
+  }
+}
...
-    .map((line) => JSON.parse(line) as AdvisorHookOutcomeRecord);
+    .map((line) => tryParseJsonLine(line))
+    .filter(validateAdvisorHookOutcomeRecord);
```

```diff
+  it('drops malformed durable outcome telemetry instead of crashing validation', async () => {
+    writeFileSync(outcomesPath, [
+      '{"timestamp":"2026-04-24T10:08:00.000Z","runtime":"codex","outcome":"accepted","skillLabel":"system-spec-kit"}',
+      '{"timestamp":"2026-04-24T10:08:01.000Z","runtime":"codex","outcome"',
+      '{"timestamp":"2026-04-24T10:08:02.000Z","runtime":"codex","outcome":"corrected","skillLabel":"system-spec-kit","correctedSkillLabel":"sk-code-opencode"}',
+      '{"timestamp":"2026-04-24T10:08:03.000Z","runtime":"codex","outcome":"mystery","skillLabel":"system-spec-kit"}',
+    ].join('\n'), 'utf8');
+    ...
+    expect(data.telemetry.outcomes.totals).toEqual({ accepted: 1, corrected: 1, ignored: 0 });
+  });
```

## Verification
- `npm exec vitest run skill-advisor/tests/handlers/advisor-validate.vitest.ts`
- Result: passed (`1` test file, `4` tests passed).
- `npm run typecheck -- --pretty false`
- Result: failed on pre-existing out-of-scope errors in other files, including `code-graph/lib/structural-indexer.ts` and multiple hook/legacy render tests that reject the broader `"stale"` status union. No RG-020-specific type error surfaced before those unrelated failures stopped the run.

## Deferred
- `.opencode/skill/system-spec-kit/mcp_server/skill-advisor/handlers/advisor-validate.ts` was read for scope confirmation but did not require a code change in this pass; its existing `summarizeAdvisorHookOutcomeRecords(...)` call becomes safe once durable outcome parsing drops malformed lines instead of throwing.
- Diagnostics JSONL parsing remains outside this single-finding pass. RG-020 only required hardening the durable outcome telemetry path identified in the review evidence.
