# Applied: RG-010

## Source
- Severity: P1
- Bucket: `013-missed-files`
- Finding: The public tool-schema descriptions still described the old `code_graph_status` and `code_graph_context` outputs, so generated tool docs and downstream consumers inherited stale contract text.

## Target Files Modified
| Path | Change Summary | Lines Touched |
|---|---|---|
| `.opencode/skill/system-spec-kit/mcp_server/tool-schemas.ts` | Refreshed the public schema descriptions so `code_graph_status` now advertises readiness plus `graphQualitySummary`, and `code_graph_context` now documents the blocked-read contract and `metadata.partialOutput` truncation metadata. | 590-597 |

## Evidence of Change
```diff
-  description: '[L7:Maintenance] Report code graph index health. Returns totalFiles, totalNodes, totalEdges, freshness, lastScanAt, lastGitHead, dbFileSize, schemaVersion, nodesByKind, edgesByType, and parseHealth. Token Budget: 500.',
+  description: '[L7:Maintenance] Report code graph index health. Returns totalFiles, totalNodes, totalEdges, freshness, readiness, canonicalReadiness, trustState, lastScanAt, lastPersistedAt, lastGitHead, dbFileSize, schemaVersion, nodesByKind, edgesByType, parseHealth, and graphQualitySummary. Token Budget: 500.',
```

```diff
-  description: '[L6:Analysis] Get LLM-oriented compact graph neighborhoods. ... Modes: neighborhood (1-hop calls+imports), outline (file symbols), impact (reverse callers). Token Budget: 1200.',
+  description: '[L6:Analysis] Get LLM-oriented compact graph neighborhoods. ... Modes: neighborhood (1-hop calls+imports), outline (file symbols), impact (reverse callers). When readiness requires a full scan, returns an explicit blocked payload with requiredAction `code_graph_scan`, readiness metadata, and lastPersistedAt instead of degraded graph answers. Successful responses include metadata.partialOutput for deadline/budget truncation details (reasons, omittedSections, omittedAnchors, truncatedText). Token Budget: 1200.',
```

## Verification
- `npx tsc --noEmit --pretty false --module nodenext --moduleResolution nodenext --target es2022 --skipLibCheck tool-schemas.ts` in `.opencode/skill/system-spec-kit/mcp_server` -> passed
- `npm run typecheck` in `.opencode/skill/system-spec-kit/mcp_server` -> failed in `code-graph/lib/structural-indexer.ts:1340` because `IndexFilesResult` now requires `preParseSkippedCount` but an empty-array cast still omits that field

## Deferred
- No paired regression test was added because RG-010 is a schema-description contract-doc fix only; runtime behavior and handler coverage already live in the code-graph handler suites.
- Package-wide typecheck remediation for `code-graph/lib/structural-indexer.ts` was deferred because the user constrained this pass to RG-010's target files plus the applied report.
