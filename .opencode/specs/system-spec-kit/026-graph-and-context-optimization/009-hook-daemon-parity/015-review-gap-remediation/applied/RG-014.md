# Applied: RG-014

## Source
- Severity: P1
- Bucket: `013-missed-files`
- Source audit: `030-gap:F-008`
- Finding summary: The storage/query playbook still used the retired `tests/code-graph-indexer.vitest.ts` path and validated `code_graph_status` as counts-only, so the operator scenario lagged packet 013's live status contract.

## Target Files Modified
| Path | Change Summary | Lines Touched |
|---|---|---|
| `.opencode/skill/system-spec-kit/manual_testing_playbook/22--context-preservation-and-code-graph/254-code-graph-scan-query.md` | Repointed the storage/query scenarios at the live `mcp_server/code-graph/tests/...` suites, corrected the scan-path references, and refreshed the query/status scenario so operators now validate `graphQualitySummary.detectorProvenanceSummary` plus `graphQualitySummary.graphEdgeEnrichmentSummary`. | 23-31, 45-50, 74-79, 103-120, 132-154, 161-166 |

## Evidence of Change

Before:
```md
- **Prompt**: `... npx vitest run tests/code-graph-indexer.vitest.ts ...`
...
- `code_graph_status` returns counts (files indexed, nodes, edges)
```

After:
```md
- **Prompt**: `... npx vitest run code-graph/tests/code-graph-indexer.vitest.ts code-graph/tests/code-graph-query-handler.vitest.ts code-graph/tests/code-graph-siblings-readiness.vitest.ts code-graph/tests/code-graph-scan.vitest.ts tests/unit-path-security.vitest.ts ...`
...
- `code_graph_status` returns counts (files indexed, nodes, edges) plus `graphQualitySummary.detectorProvenanceSummary` and `graphQualitySummary.graphEdgeEnrichmentSummary`
```

The query/status scenario now points to the handler suites that exercise the live packet 013 contract:
- `code-graph/tests/code-graph-query-handler.vitest.ts` for outline, calls, and imports behavior
- `code-graph/tests/code-graph-siblings-readiness.vitest.ts` for canonical readiness plus `code_graph_status` payload parity

## Verification
- Existence check: `rg --files .opencode/skill/system-spec-kit/mcp_server | rg 'code-graph/(tests/code-graph-(indexer|scan|query-handler|siblings-readiness)\\.vitest\\.ts)$|tests/unit-path-security\\.vitest\\.ts$'`
- Targeted suite run: `cd .opencode/skill/system-spec-kit/mcp_server && npx vitest run code-graph/tests/code-graph-indexer.vitest.ts code-graph/tests/code-graph-query-handler.vitest.ts code-graph/tests/code-graph-siblings-readiness.vitest.ts code-graph/tests/code-graph-scan.vitest.ts tests/unit-path-security.vitest.ts`
  - Result: `4` suites passed and `code-graph/tests/code-graph-scan.vitest.ts` failed at `handleCodeGraphScan > clears the persisted edge-enrichment summary when a later scan reports no summary` because `setLastGraphEdgeEnrichmentSummaryMock` was never called as the test expected.

## Deferred
- No additional code or test files were changed in this pass; RG-014 was a playbook-drift fix, so the remediation stayed inside the allowed documentation scope while pointing operators at the existing live regression suites.
