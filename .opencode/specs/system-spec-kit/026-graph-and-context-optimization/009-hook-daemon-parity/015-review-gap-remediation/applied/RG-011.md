# Applied: RG-011

## Source
- Severity: P1
- Bucket: `013-missed-files`
- Source audit: `030-gap:F-005`
- Finding summary: The code-graph storage/query feature-catalog leaf still pointed at the pre-move indexer test paths and described `code_graph_status` generically, so the curated reference no longer matched the live `code_graph_status` and `code_graph_context` contract.

## Target Files Modified
| Path | Change Summary | Lines Touched |
|---|---|---|
| `.opencode/skill/system-spec-kit/feature_catalog/22--context-preservation-and-code-graph/08-code-graph-storage-query.md` | Refreshed the frontmatter and overview copy so the leaf names the live readiness, trust, `graphQualitySummary`, blocked-read, and `metadata.partialOutput` behavior; corrected the stale scan/indexer test paths to the current `mcp_server/code-graph/tests/...` location. | 2-3, 11-13, 39-40 |

## Evidence of Change

### `.opencode/skill/system-spec-kit/feature_catalog/22--context-preservation-and-code-graph/08-code-graph-storage-query.md`

Before:
```md
description: "Code graph provides SQLite storage with code_files, code_nodes, code_edges tables and 4 MCP tools for structural queries."
```

After:
```md
description: "Code graph provides SQLite storage plus MCP tools for structural queries, readiness-aware status, and compact graph context."
```

Before:
```md
SQLite database (code-graph.sqlite) stores indexed files, symbol nodes, and relationship edges. 4 MCP tools: code_graph_scan (workspace indexing), code_graph_query (outline/calls/imports), code_graph_status (health), code_graph_context (LLM neighborhoods).
```

After:
```md
SQLite database (`code-graph.sqlite`) stores indexed files, symbol nodes, and relationship edges. 4 MCP tools: `code_graph_scan` (workspace indexing), `code_graph_query` (outline/calls/imports), `code_graph_status` (freshness plus readiness, trust, parse-health, and `graphQualitySummary` reporting), and `code_graph_context` (LLM neighborhoods with readiness metadata, blocked full-scan responses, and `metadata.partialOutput` when budget or deadline limits trim output).
```

Before:
```md
| `mcp_server/tests/code-graph-scan.vitest.ts` | Scan flow, persistence, and incremental refresh behavior |
| `mcp_server/tests/code-graph-indexer.vitest.ts` | Indexer-to-storage integration and edge persistence coverage |
```

After:
```md
| `mcp_server/code-graph/tests/code-graph-scan.vitest.ts` | Scan flow, persistence, and incremental refresh behavior |
| `mcp_server/code-graph/tests/code-graph-indexer.vitest.ts` | Indexer-to-storage integration and edge persistence coverage |
```

## Verification
- `git diff --check -- .opencode/skill/system-spec-kit/feature_catalog/22--context-preservation-and-code-graph/08-code-graph-storage-query.md .opencode/specs/system-spec-kit/026-graph-and-context-optimization/009-hook-daemon-parity/015-review-gap-remediation/applied/RG-011.md`
- `rg -n "mcp_server/tests/code-graph-(scan|indexer)\\.vitest\\.ts|graphQualitySummary|metadata\\.partialOutput|blocked full-scan responses" .opencode/skill/system-spec-kit/feature_catalog/22--context-preservation-and-code-graph/08-code-graph-storage-query.md`

## Deferred
- No paired executable test was added. RG-011 is a documentation-surface drift finding, and the requested scope limited edits to the catalog leaf plus this applied note. Verification stayed focused on source-alignment and path correction.
