# Applied: RG-018

## Source
- Severity: P1
- Bucket: `014-impl-defects`
- Source audit: `014/review:F-004`
- Finding summary: Packet `014` still built a private advisor brief string inside the producer and then stored that divergent format in the shared payload, while hooks rendered a different prompt-visible contract via `renderAdvisorBrief(...)`.

## Target Files Modified
| Path | Change Summary | Lines Touched |
|---|---|---|
| `.opencode/skill/system-spec-kit/mcp_server/skill-advisor/lib/skill-advisor-brief.ts` | Replaced the producer-local brief formatter with a `renderAdvisorBrief(...)` bridge and threaded the same rendered string into both prompt output and shared-payload summary/section content. | 35-38, 156-173, 197-204, 245-265, 311-319, 495-502 |
| `.opencode/skill/system-spec-kit/mcp_server/skill-advisor/lib/render.ts` | Introduced a minimal `AdvisorBriefRenderableResult` contract so the producer can call the shared renderer directly without depending on the full producer type surface. | 21-33, 95-159 |
| `.opencode/skill/system-spec-kit/mcp_server/skill-advisor/tests/legacy/advisor-brief-producer.vitest.ts` | Added regression assertions for both the normal and ambiguous brief paths to prove the built brief, shared payload summary, and shared payload section content now all share the renderer contract. | 12, 93-156 |

## Evidence of Change

### `.opencode/skill/system-spec-kit/mcp_server/skill-advisor/lib/skill-advisor-brief.ts`

Before:
```ts
function renderBrief(
  recommendations: readonly AdvisorRecommendation[],
  freshness: AdvisorHookFreshness,
  tokenCap: number,
): string | null {
  const top = recommendations[0];
  if (!top) {
    return null;
  }
  const prefix = freshness === 'stale' ? 'Advisor: stale - ' : 'Advisor: ';
  const brief = `${prefix}use ${top.skill} (confidence ${top.confidence.toFixed(2)}, uncertainty ${top.uncertainty.toFixed(2)}).`;
  return capTextByTokenEstimate(brief, tokenCap);
}
```

After:
```ts
function renderSharedBrief(
  recommendations: readonly AdvisorRecommendation[],
  freshness: AdvisorHookFreshness,
  tokenCap: number,
  thresholdConfig: AdvisorThresholds | undefined,
): string | null {
  const renderableResult: AdvisorBriefRenderableResult = {
    status: 'ok',
    freshness,
    recommendations,
    metrics: { tokenCap },
    sharedPayload: null,
  };
  return renderAdvisorBrief(renderableResult, {
    tokenCap,
    thresholdConfig,
  });
}
```

Before:
```ts
  const sections: SharedPayloadSection[] = args.brief
    ? [{
      key: 'advisor-brief',
      title: 'Advisor Brief',
      content: args.brief,
      source: 'advisor-runtime',
    }]
    : [];
  return createSharedPayloadEnvelope({
    kind: 'resume',
    sections,
    summary: args.brief ?? `Advisor ${args.status} (${args.freshness})`,
```

After:
```ts
  const sections: SharedPayloadSection[] = args.renderedBrief
    ? [{
      key: 'advisor-brief',
      title: 'Advisor Brief',
      content: args.renderedBrief,
      source: 'advisor-runtime',
    }]
    : [];
  return createSharedPayloadEnvelope({
    kind: 'resume',
    sections,
    summary: args.renderedBrief ?? `Advisor ${args.status} (${args.freshness})`,
```

### `.opencode/skill/system-spec-kit/mcp_server/skill-advisor/lib/render.ts`

Added a renderer-facing result shape so the producer can reuse the same formatter without importing the full producer module:

```ts
export interface AdvisorBriefRenderableResult {
  readonly status: 'ok' | 'stale' | 'skipped' | 'degraded' | 'fail_open';
  readonly freshness: 'live' | 'stale' | 'absent' | 'unavailable';
  readonly recommendations: readonly AdvisorRecommendation[];
  readonly metrics?: {
    readonly tokenCap?: number;
  } | null;
  readonly sharedPayload?: {
    readonly metadata?: {
      readonly skillLabel?: string | null;
    } | null;
  } | null;
}
```

### `.opencode/skill/system-spec-kit/mcp_server/skill-advisor/tests/legacy/advisor-brief-producer.vitest.ts`

Added paired regressions:

```ts
    expect(result.brief).toBe('Advisor: live; use sk-code-opencode 0.91/0.10 pass.');
    expect(result.brief).toBe(rendered);
    expect(result.sharedPayload?.summary).toBe(rendered);
    expect(result.sharedPayload?.sections).toEqual([
      expect.objectContaining({
        key: 'advisor-brief',
        content: rendered,
      }),
    ]);
```

```ts
    expect(result.brief).toBe(
      'Advisor: live; ambiguous: sk-code-opencode 0.91/0.10 vs sk-doc 0.89/0.11 pass.',
    );
    expect(result.brief).toBe(rendered);
    expect(result.sharedPayload?.summary).toBe(rendered);
```

## Verification
- `npm exec -- vitest run skill-advisor/tests/legacy/advisor-brief-producer.vitest.ts` in `.opencode/skill/system-spec-kit/mcp_server` -> passed (`1` file, `16` tests)
- `npm run typecheck` in `.opencode/skill/system-spec-kit/mcp_server` -> still fails on pre-existing out-of-scope error in `code-graph/lib/structural-indexer.ts:1340` (`TS2352` on `IndexFilesResult` / `preParseSkippedCount`), but no remaining type errors from the RG-018 changes

## Deferred
- The package-level typecheck still has an unrelated `code-graph` failure outside the RG-018 target files, so this pass did not attempt to clear that broader workspace error.
