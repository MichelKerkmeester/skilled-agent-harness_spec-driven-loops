# Applied: RG-008

## Source
- Severity: P1
- Bucket: `013-missed-files`
- Source audit: `030-gap:F-002`
- Finding summary: The code-graph subsystem README still presented the older readiness/status surface and omitted the post-013 `graphQualitySummary`, blocked full-scan payload, and startup structured-context metadata.

## Target Files Modified
| Path | Change Summary | Lines Touched |
|---|---|---|
| `.opencode/skill/system-spec-kit/mcp_server/code-graph/README.md` | Expanded the public contract section so the subsystem README documents `code_graph_status` quality fields, the `code_graph_context` blocked-read/success metadata, and startup/shared-payload transport details alongside the existing readiness overview. | 42-44, 122-164 |

## Evidence of Change

### `.opencode/skill/system-spec-kit/mcp_server/code-graph/README.md`

Added contract coverage for:
- `code_graph_status` fields: `readiness`, `canonicalReadiness`, `trustState`, `parseHealth`, `graphQualitySummary`
- `code_graph_context` blocked-read payload: `status:"blocked"`, `requiredAction`, `graphAnswersOmitted`, readiness/timestamp fields
- Structured context metadata: `metadata.partialOutput`, `metadata.freshness`, `graphMetadata.detectorProvenance`, and anchor provenance fields
- Startup surfaces: `graphQualitySummary`, `sharedPayload`, `sharedPayloadTransport`, and shared-payload `provenance`

Representative excerpt:
```md
| **`code_graph_context` (blocked path)** | `status: "blocked"` | `blocked`, `degraded`, `graphAnswersOmitted`, `requiredAction`, `blockReason`, `readiness`, `canonicalReadiness`, `trustState`, `lastPersistedAt` | Returned when readiness requires a full scan and inline full scans are disallowed; callers should run `code_graph_scan` before retrying |
```

```md
| `sharedPayloadTransport` | compact JSON with `kind`, `summary`, `provenance`, `sectionKeys` | Lightweight startup digest for transports that do not need full section bodies |
```

## Verification
- `git diff --check -- .opencode/skill/system-spec-kit/mcp_server/code-graph/README.md .opencode/specs/system-spec-kit/026-graph-and-context-optimization/009-hook-daemon-parity/015-review-gap-remediation/applied/RG-008.md`
- `bash .opencode/skill/system-spec-kit/scripts/spec/validate.sh .opencode/specs/system-spec-kit/026-graph-and-context-optimization/009-hook-daemon-parity/015-review-gap-remediation --strict`
- No paired automated test was added because `RG-008` is a documentation-surface drift fix; the live behavior already exists in the referenced handlers/builders.

## Deferred
- None.
