# Applied: RG-009

## Source
- Severity: P1
- Bucket: `013-missed-files`
- Finding: The install guide only explained freshness/full-scan behavior, so operators did not see the newer `graphQualitySummary` and blocked-read/context vocabulary they now encounter on code-graph surfaces.

## Target Files Modified
| Path | Change Summary | Lines Touched |
|---|---|---|
| `.opencode/skill/system-spec-kit/mcp_server/INSTALL_GUIDE.md` | Extended the code-graph overview, verification guidance, troubleshooting table, and operator notes so the install guide now documents `graphQualitySummary`, the blocked read-path payload (`status: "blocked"`, `requiredAction: "code_graph_scan"`), and the `code_graph_context` metadata operators should inspect (`graphMetadata.detectorProvenance`, `metadata.partialOutput`). | 125-130, 458-462, 805, 915-921 |

## Evidence of Change
```diff
+- startup/bootstrap payloads may also include `graphQualitySummary` so operators can see detector provenance and edge-enrichment quality from the last persisted scan
+- `code_graph_status` returns counts plus `readiness`, `canonicalReadiness`, `trustState`, and `graphQualitySummary`
+- read-path tools do not run inline full scans; when the graph is empty or too stale they return a blocked payload with `status: "blocked"`, `requiredAction: "code_graph_scan"`, and `blockReason: "full_scan_required"`
+- successful `code_graph_context` responses include `metadata.partialOutput` and `graphMetadata.detectorProvenance`
```

```diff
+| `code_graph_query` reports `full_scan`, or `code_graph_context` / `code_graph_query` returns `status: "blocked"` with `requiredAction: "code_graph_scan"` | The graph is empty or too stale for bounded read-path repair | Run `code_graph_scan`, then retry the structural read |
+
+### Code Graph Status and Context Signals
+Use `code_graph_status` as the quickest operator probe for structural health.
+`graphQualitySummary` summarizes what kind of detector provenance and edge-enrichment evidence the last persisted scan produced.
+`code_graph_context` can refuse broad repair on the read path and returns `blockReason: "full_scan_required"` when that happens.
```

## Verification
- `rg -n "graphQualitySummary|status: \"blocked\"|requiredAction: \"code_graph_scan\"|partialOutput|graphMetadata\\.detectorProvenance" .opencode/skill/system-spec-kit/mcp_server/INSTALL_GUIDE.md` -> passed
- `git diff --check -- .opencode/skill/system-spec-kit/mcp_server/INSTALL_GUIDE.md .opencode/specs/system-spec-kit/026-graph-and-context-optimization/009-hook-daemon-parity/015-review-gap-remediation/applied/RG-009.md` -> passed

## Deferred
- No automated tests were added or run because RG-009 is a documentation-only contract propagation fix; there was no executable regression surface in the requested target files.
