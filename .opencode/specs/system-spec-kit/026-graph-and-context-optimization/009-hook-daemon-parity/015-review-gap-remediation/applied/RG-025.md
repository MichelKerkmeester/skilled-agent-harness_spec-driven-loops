# Applied: RG-025

## Source
- Severity: P1
- Bucket: `014-missed-files`
- Source audit: `031-gap:F-004`
- Finding summary: The native `advisor_recommend` manual scenario still validated only the recommendation payload and did not assert the public 014 `workspaceRoot` and `effectiveThresholds` fields.

## Target Files Modified
| Path | Change Summary | Lines Touched |
|---|---|---|
| `.opencode/skill/system-spec-kit/mcp_server/skill-advisor/manual_testing_playbook/01--native-mcp-tools/001-native-recommend-happy-path.md` | Extended the happy-path manual expectations so operators explicitly validate the 014 output contract for `workspaceRoot` and `effectiveThresholds` in addition to the recommendation payload. | 47, 54-61 |

## Evidence of Change

Before:
```md
2. Save the JSON response.
3. Inspect `data.recommendations[0]`.
```

After:
```md
2. Save the JSON response.
3. Inspect `data.workspaceRoot`, `data.effectiveThresholds`, and `data.recommendations[0]`.
```

Before:
```md
- Response envelope has `status: "ok"`.
- `data.freshness` is `live` or `stale`.
```

After:
```md
- Response envelope has `status: "ok"`.
- `data.workspaceRoot` is the absolute repository root for the current checkout.
- `data.effectiveThresholds` is present and, for this exact call with no overrides, equals:
  {
    "confidenceThreshold": 0.8,
    "uncertaintyThreshold": 0.35,
    "confidenceOnly": false
  }
- `data.freshness` is `live` or `stale`.
```

The handler and schema target files were read as part of the fix and already expose these public 014 fields, so no code-path or schema change was required for RG-025.

## Verification
- `rg -n "workspaceRoot|effectiveThresholds|confidenceThreshold|uncertaintyThreshold|confidenceOnly" .opencode/skill/system-spec-kit/mcp_server/skill-advisor/manual_testing_playbook/01--native-mcp-tools/001-native-recommend-happy-path.md .opencode/specs/system-spec-kit/026-graph-and-context-optimization/009-hook-daemon-parity/015-review-gap-remediation/applied/RG-025.md` -> confirmed the new assertions and the applied report text.
- `npm --prefix .opencode/skill/system-spec-kit/mcp_server run build` -> failed in existing non-RG-025 TypeScript surfaces:
  - `code-graph/lib/structural-indexer.ts(1340,19)` missing `preParseSkippedCount` on an `IndexFilesResult` cast
  - `skill-advisor/handlers/advisor-validate.ts(76,12)` invalid `readonly` modifier usage
  - `skill-advisor/handlers/advisor-validate.ts(354,60)` readonly array passed to a mutable parameter

## Deferred
- None. This finding was a downstream manual-playbook coverage gap; the runtime handler and schema already matched the 014 public contract.
