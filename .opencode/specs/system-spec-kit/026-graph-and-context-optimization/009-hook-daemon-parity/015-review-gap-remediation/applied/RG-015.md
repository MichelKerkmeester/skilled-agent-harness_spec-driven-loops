# Applied: RG-015

## Source
- Severity: P1
- Bucket: `013-missed-files`
- Source audit: `030-gap:F-009`
- Finding summary: The semantic-vs-structural routing playbook still used the old indexer test path and did not mention the 013-era `partialOutput` or blocked-read contract for `code_graph_context`.

## Target Files Modified
| Path | Change Summary | Lines Touched |
|---|---|---|
| `.opencode/skill/system-spec-kit/manual_testing_playbook/22--context-preservation-and-code-graph/255-cocoindex-code-graph-routing.md` | Repointed the routing scenario from the stale `tests/code-graph-indexer.vitest.ts` path to the live `code-graph-context-handler`, `code-graph-query-handler`, and `code-graph-seed-resolver` suites, and expanded the scenario expectations to require blocked-read plus structured `partialOutput` validation. | 17, 26, 34-37, 104, 109, 113, 117, 121, 126 |
| `.opencode/specs/system-spec-kit/026-graph-and-context-optimization/009-hook-daemon-parity/015-review-gap-remediation/applied/RG-015.md` | Recorded the remediation source, doc-only evidence, targeted verification, and the reason no new automated test file changes were needed in this pass. | 1-49 |

## Evidence of Change

### `.opencode/skill/system-spec-kit/manual_testing_playbook/22--context-preservation-and-code-graph/255-cocoindex-code-graph-routing.md`

Before:
```md
- **Objective**: Verify that the CocoIndex bridge correctly routes between semantic and structural queries. The seed resolver (`seed-resolver.ts`) normalizes CocoIndex file:line results to ArtifactRef via a resolution chain (exact symbol, enclosing symbol, file anchor). `code_graph_context` expands resolved anchors in 3 modes: neighborhood (1-hop graph neighbors), outline (file symbol listing), and impact (reverse callers). Budget-aware truncation must apply to context output.
```

After:
```md
- **Objective**: Verify that the CocoIndex bridge correctly routes between semantic and structural queries. The seed resolver (`seed-resolver.ts`) normalizes CocoIndex file:line results to ArtifactRef via a resolution chain (exact symbol, enclosing symbol, file anchor). `code_graph_context` expands resolved anchors in 3 modes: neighborhood (1-hop graph neighbors), outline (file symbol listing), and impact (reverse callers). The post-013 contract must also surface an explicit blocked-read payload when readiness requires a suppressed full scan and structured `metadata.partialOutput` details when deadline or budget pressure omits work.
```

Before:
```md
As a context-and-code-graph validation operator, validate code_graph_context expands in neighborhood, outline, and impact modes against cd .opencode/skill/system-spec-kit/mcp_server && npx vitest run tests/code-graph-indexer.vitest.ts. Verify neighborhood returns 1-hop neighbors, outline returns file symbols, impact returns reverse callers. Return a concise pass/fail verdict with the main reason and cited evidence.
```

After:
```md
As a context-and-code-graph validation operator, validate the live routing contracts against `cd .opencode/skill/system-spec-kit/mcp_server && npm exec -- vitest run code-graph/tests/code-graph-context-handler.vitest.ts code-graph/tests/code-graph-query-handler.vitest.ts code-graph/tests/code-graph-seed-resolver.vitest.ts`. Verify the seed resolver preserves CocoIndex file and filePath seeds, neighborhood returns 1-hop neighbors, outline returns file symbols, impact returns reverse callers, `code_graph_context` returns an explicit blocked payload when readiness requires a suppressed full scan, and `metadata.partialOutput` reports omitted work when bounded execution trims output. Return a concise pass/fail verdict with the main reason and cited evidence.
```

The updated pass criteria now require evidence for:
- real handler/resolver suite coverage instead of the stale indexer path
- `status: "blocked"` plus `requiredAction: "code_graph_scan"` / `graphAnswersOmitted` on blocked context reads
- structured `metadata.partialOutput` fields when bounded execution omits work

## Verification
- `cd .opencode/skill/system-spec-kit/mcp_server && npm exec -- vitest run code-graph/tests/code-graph-context-handler.vitest.ts code-graph/tests/code-graph-query-handler.vitest.ts code-graph/tests/code-graph-seed-resolver.vitest.ts`
- `rg -n "code-graph-context-handler|code-graph-query-handler|code-graph-seed-resolver|partialOutput|requiredAction: \\\"code_graph_scan\\\"" .opencode/skill/system-spec-kit/manual_testing_playbook/22--context-preservation-and-code-graph/255-cocoindex-code-graph-routing.md`
- `rg -n "code-graph-indexer" .opencode/skill/system-spec-kit/manual_testing_playbook/22--context-preservation-and-code-graph/255-cocoindex-code-graph-routing.md` -> expected no matches

## Deferred
- None. RG-015 was a manual-playbook drift fix, and the paired regression coverage already existed in the referenced Vitest suites, so this pass only re-pointed the documentation to the live contracts.
