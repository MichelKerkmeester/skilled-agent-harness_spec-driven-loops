# Applied: RG-024

## Source
- Severity: P1
- Bucket: `014-missed-files`
- Source audit: `031-gap:F-003`
- Finding summary: The `advisor_recommend` feature catalog page still described the pre-014 public output shape and omitted `workspaceRoot` plus `effectiveThresholds`.

## Target Files Modified
| Path | Change Summary | Lines Touched |
|---|---|---|
| `.opencode/skill/system-spec-kit/mcp_server/skill-advisor/feature_catalog/06--mcp-surface/01-advisor-recommend.md` | Expanded the public contract description to include the resolved `workspaceRoot`, the full `effectiveThresholds` object, fail-open envelope behavior, and the rest of the landed response metadata. | 31 |
| `.opencode/skill/system-spec-kit/mcp_server/skill-advisor/README.md` | Tightened the native MCP summary bullet so the README now names the real `advisor_recommend` threshold fields and optional fail-open metadata. | 76 |

## Evidence of Change

### `.opencode/skill/system-spec-kit/mcp_server/skill-advisor/feature_catalog/06--mcp-surface/01-advisor-recommend.md`

Before:
```md
`handlers/advisor-recommend.ts` implements the tool. Input is validated by `schemas/advisor-tool-schemas.ts` (Zod strict). Output carries `recommendations[]`, optional `laneBreakdown[]` when `includeAttribution: true`, `lifecycle` redirect metadata, `cache` state, `trustState`, `warnings`, and `abstainReasons`.
```

After:
```md
`handlers/advisor-recommend.ts` implements the tool. Input is validated by `schemas/advisor-tool-schemas.ts` (Zod strict). The public response always includes the resolved `workspaceRoot` plus `effectiveThresholds`, where `effectiveThresholds` publishes the active `confidenceThreshold`, `uncertaintyThreshold`, and `confidenceOnly` mode after request overrides are merged with defaults.
```

The updated section now also states that:
- fail-open responses still preserve `workspaceRoot` and `effectiveThresholds`
- lifecycle redirect metadata is carried on each recommendation entry
- the envelope includes `freshness`, `trustState`, `generatedAt`, `cache`, optional `warnings`, and optional `abstainReasons`

### `.opencode/skill/system-spec-kit/mcp_server/skill-advisor/README.md`

Before:
```md
- `advisor_recommend` returns `workspaceRoot`, prompt-safe `recommendations[]`, `effectiveThresholds`, cache state, lifecycle redirect metadata, and freshness trust.
```

After:
```md
- `advisor_recommend` returns the resolved `workspaceRoot`, prompt-safe `recommendations[]`, `effectiveThresholds` (`confidenceThreshold`, `uncertaintyThreshold`, `confidenceOnly`), cache state, lifecycle redirect metadata, freshness/trust metadata, and optional warnings or abstain reasons for fail-open or threshold-filtered results.
```

### `.opencode/skill/system-spec-kit/mcp_server/skill-advisor/handlers/advisor-recommend.ts`

Read for contract confirmation. No code change was required because the handler already emits:
- `workspaceRoot` on all parsed outputs
- `effectiveThresholds` with `confidenceThreshold`, `uncertaintyThreshold`, and `confidenceOnly`
- fail-open empty envelopes for disabled or absent freshness states

## Verification
- `(cd .opencode/skill/system-spec-kit/mcp_server && ./node_modules/.bin/vitest run skill-advisor/tests/handlers/advisor-recommend.vitest.ts)`
- `git diff -- .opencode/skill/system-spec-kit/mcp_server/skill-advisor/feature_catalog/06--mcp-surface/01-advisor-recommend.md .opencode/skill/system-spec-kit/mcp_server/skill-advisor/README.md .opencode/specs/system-spec-kit/026-graph-and-context-optimization/009-hook-daemon-parity/015-review-gap-remediation/applied/RG-024.md`

## Deferred
- No handler or test edits were made in this pass because RG-024 is a docs-drift fix; automated contract assertions for the new recommend fields are tracked separately by `F-007`.
