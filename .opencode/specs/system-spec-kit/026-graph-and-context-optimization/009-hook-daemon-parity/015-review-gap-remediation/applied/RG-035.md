# Applied: RG-035

## Source
- Severity: P2
- Bucket: `013-missed-files`
- Source audit: `030-gap:F-012`
- Finding summary: The auto-trigger feature page described stale/empty handling only as helper behavior, not as the explicit blocked `code_graph_query` and `code_graph_context` payloads operators now receive after packet 013.
- Recommended fix: Update the feature page so it teaches the caller-visible blocked query/context payload contract instead of only the helper actions.

## Target Files Modified
| Path | Change Summary | Lines Touched |
|---|---|---|
| `.opencode/skill/system-spec-kit/feature_catalog/22--context-preservation-and-code-graph/15-code-graph-auto-trigger.md` | Expanded the overview from helper-only freshness behavior to the caller-visible blocked query/context contract and refreshed the source inventory to point at the live handlers/tests that define that contract. | 13-22, 40-44 |
| `.opencode/specs/system-spec-kit/026-graph-and-context-optimization/009-hook-daemon-parity/015-review-gap-remediation/applied/RG-035.md` | Recorded the applied remediation evidence, verification, and any deferred work for RG-035. | 1-42 |

## Evidence
- Merged synthesis identifies `RG-035` as a missed feature-page update for the blocked payload contract: `.opencode/specs/system-spec-kit/026-graph-and-context-optimization/research/013-014-review-gap-merged-synthesis.md`
- Merged findings registry records the same summary and recommended fix for `RG-035`: `.opencode/specs/system-spec-kit/026-graph-and-context-optimization/research/013-014-review-gap-merged-findings.json:639`
- `query.ts` returns a blocked `code_graph_query` payload with `status: "blocked"`, `requiredAction: "code_graph_scan"`, and omitted-answer flags when readiness requires a full scan: `.opencode/skill/system-spec-kit/mcp_server/code-graph/handlers/query.ts:629`
- `context.ts` returns the parallel blocked `code_graph_context` payload with readiness metadata and the same `requiredAction`: `.opencode/skill/system-spec-kit/mcp_server/code-graph/handlers/context.ts:138`
- The handler regressions already assert that both read paths surface the explicit blocked contract: `.opencode/skill/system-spec-kit/mcp_server/code-graph/tests/code-graph-query-handler.vitest.ts:162`, `.opencode/skill/system-spec-kit/mcp_server/code-graph/tests/code-graph-context-handler.vitest.ts:126`

Before:
```md
The ensure-ready module checks graph state (fresh/stale/empty) by comparing the current git HEAD against the last indexed HEAD and scanning for stale files. When the graph is empty, a full scan is triggered. When files are stale but below the selective reindex threshold (50 files), only changed files are reindexed. Above the threshold, a full rescan is performed. A 10-second timeout prevents blocking on large codebases. Shared by code_graph_context, code_graph_query, and code_graph_status handlers.
```

After:
```md
The ensure-ready module checks graph state (fresh/stale/empty) by comparing the current git HEAD against the last indexed HEAD and scanning for stale files. When the graph is empty, a full scan is required. When files are stale but below the selective reindex threshold (50 files), only changed files are reindexed inline. Above the threshold, a full rescan is required. A 10-second timeout prevents unbounded inline work.

Packet 013 made this visible to callers instead of leaving it as an internal helper concern. When `code_graph_query` or `code_graph_context` hits a `full_scan` readiness result and inline indexing does not complete, the handlers return an explicit blocked payload...
```

## Verification
- Passed: `git diff --check -- .opencode/skill/system-spec-kit/feature_catalog/22--context-preservation-and-code-graph/15-code-graph-auto-trigger.md .opencode/specs/system-spec-kit/026-graph-and-context-optimization/009-hook-daemon-parity/015-review-gap-remediation/applied/RG-035.md`
  Result: no whitespace or patch-format issues in the edited files.
- Passed: `sed -n '1,80p' .opencode/skill/system-spec-kit/feature_catalog/22--context-preservation-and-code-graph/15-code-graph-auto-trigger.md`
  Result: the feature leaf now documents the blocked query/context payload fields and points at the live handler/test files that implement them.
- Packet validation: `bash .opencode/skill/system-spec-kit/scripts/spec/validate.sh .opencode/specs/system-spec-kit/026-graph-and-context-optimization/009-hook-daemon-parity/015-review-gap-remediation --strict`
  Result: failed on pre-existing packet-level documentation/template gaps in the remediation folder (`plan.md`, `tasks.md`, `checklist.md` missing; `spec.md` missing required template headers/anchors/frontmatter markers). No RG-035-specific markdown or patch hygiene errors were reported.

## Deferred
- None.
