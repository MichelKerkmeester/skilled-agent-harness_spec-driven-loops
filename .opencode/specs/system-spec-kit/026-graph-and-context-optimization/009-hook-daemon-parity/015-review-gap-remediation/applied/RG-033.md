# Applied: RG-033

## Source
- Severity: P2
- Bucket: `013-missed-files`
- Finding: The handler-local README for `code-graph/handlers/` still summarized `status.ts` and `context.ts` generically after packet 013 changed those caller-visible surfaces materially.
- References:
  - `.opencode/specs/system-spec-kit/026-graph-and-context-optimization/research/013-014-review-gap-merged-synthesis.md`
  - `.opencode/specs/system-spec-kit/026-graph-and-context-optimization/research/013-014-review-gap-merged-findings.json`
  - `.opencode/skill/system-spec-kit/mcp_server/code-graph/handlers/query.ts`
  - `.opencode/skill/system-spec-kit/mcp_server/code-graph/handlers/status.ts`
  - `.opencode/skill/system-spec-kit/mcp_server/code-graph/handlers/context.ts`

## Target Files Modified
| Path | Change Summary |
|---|---|
| `.opencode/skill/system-spec-kit/mcp_server/code-graph/handlers/README.md` | Refreshed the handler table so `query.ts`, `status.ts`, and `context.ts` describe the current readiness/trust contract, blocked-read behavior, ambiguity/dangling-edge warnings, seed provenance, and `metadata.partialOutput` surface. |
| `.opencode/specs/system-spec-kit/026-graph-and-context-optimization/009-hook-daemon-parity/015-review-gap-remediation/applied/RG-033.md` | Recorded the scope, evidence, and verification for this packet-local README remediation. |

## Evidence
```diff
-| `query.ts` | `code_graph_query` | Query structural relationships (outline, calls, imports) |
-| `status.ts` | `code_graph_status` | Report graph health and statistics |
-| `context.ts` | `code_graph_context` | LLM-oriented compact graph neighborhoods |
+| `query.ts` | `code_graph_query` | Run structural relationship reads ... with shared readiness/trust metadata, explicit blocked-read payloads ... and warnings for ambiguous subjects or dangling edges |
+| `status.ts` | `code_graph_status` | Probe freshness plus totals ... readiness/trust fields, and `graphQualitySummary` ... |
+| `context.ts` | `code_graph_context` | Build LLM-oriented graph neighborhoods from manual, graph, or CocoIndex seeds ... `metadata.partialOutput` ... blocked payload with `requiredAction: "code_graph_scan"` |
```

```diff
+## Surface Notes
+
+- `query.ts` and `context.ts` both use read-path readiness gating and the shared readiness contract from `../lib/readiness-contract.ts`.
+- `status.ts` is a non-mutating freshness probe: it reports current readiness/trust state but does not perform inline indexing.
+- `context.ts` preserves seed provenance across `manual`, `graph`, and `cocoindex` inputs so returned anchors can keep caller-visible source labels.
```

## Verification
- `sed -n '1,220p' .opencode/skill/system-spec-kit/mcp_server/code-graph/handlers/README.md` to confirm the updated handler descriptions and `Surface Notes` section match the live post-013 handler surface.
- `git diff --check -- .opencode/skill/system-spec-kit/mcp_server/code-graph/handlers/README.md .opencode/specs/system-spec-kit/026-graph-and-context-optimization/009-hook-daemon-parity/015-review-gap-remediation/applied/RG-033.md` to verify the patch stays within scope and introduces no whitespace errors.

## Deferred
- No automated tests were added because RG-033 is a documentation-only remediation; runtime behavior already lives in the handler implementations and their existing suites.
