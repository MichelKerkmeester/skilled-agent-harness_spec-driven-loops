# Applied: RG-003

## Source
- Severity: P1
- Bucket: `013-impl-defects`
- Finding: The packet preserved CocoIndex seed fidelity only when callers sent `seed.file`; `seed.filePath` fell through the generic seed path and dropped provider-specific metadata.

## Target Files Modified
| Path | Change Summary | Lines Touched |
|---|---|---|
| `.opencode/skill/system-spec-kit/mcp_server/code-graph/handlers/context.ts` | Normalized the CocoIndex handler branch to accept `seed.filePath` the same way it already accepted `seed.file`, so provider-specific `file`, `range`, `score`, and `snippet` data stay on the CocoIndex seed path. | 164-178 |
| `.opencode/skill/system-spec-kit/mcp_server/code-graph/tests/code-graph-context-handler.vitest.ts` | Added a direct regression that sends a CocoIndex seed via `filePath` and asserts the handler still forwards a CocoIndex-shaped seed plus preserves the returned anchor metadata. | 228-299 |

## Evidence of Change
```diff
+      const cocoindexFile = seed.file ?? seed.filePath;
-
-      if (seed.provider === 'cocoindex' && seed.file) {
+      if (seed.provider === 'cocoindex' && cocoindexFile) {
         return {
           provider: 'cocoindex' as const,
-          file: seed.file,
+          file: cocoindexFile,
```

```diff
+  it('preserves cocoindex seed fidelity when callers send filePath', async () => {
+    const result = await handleCodeGraphContext({
+      queryMode: 'neighborhood',
+      seeds: [{
+        provider: 'cocoindex',
+        filePath: 'src/example.ts',
+        range: { start: 42, end: 44 },
+        score: 0.83,
+        snippet: 'handleThing();',
+        source: 'semantic-search',
+      }],
+    });
+    expect(mocks.buildContext).toHaveBeenCalledWith(expect.objectContaining({
+      seeds: [expect.objectContaining({
+        provider: 'cocoindex',
+        file: 'src/example.ts',
+      })],
+    }));
+  });
```

## Verification
- `npm exec -- vitest run code-graph/tests/code-graph-context-handler.vitest.ts` in `.opencode/skill/system-spec-kit/mcp_server` -> passed
- `npm run typecheck` in `.opencode/skill/system-spec-kit/mcp_server` -> passed

## Deferred
- No broader code-graph suite rerun was added in this pass; the fix stayed within the RG-003 target files and targeted verification scope.
