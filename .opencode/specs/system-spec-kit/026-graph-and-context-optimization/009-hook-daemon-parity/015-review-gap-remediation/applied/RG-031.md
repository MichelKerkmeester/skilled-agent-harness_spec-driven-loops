# Applied: RG-031

## Source
- Severity: P2
- Bucket: `013-impl-defects`
- Source audit: `013/review:F-009`
- Finding summary: The context handler test suite covered partial-output metadata, but it did not run a multi-anchor deadline case through `handleCodeGraphContext(...)`, so the earlier `omittedAnchors` undercount bug stayed unguarded at the handler payload surface.

## Target Files Modified
| Path | Change Summary | Lines Touched |
|---|---|---|
| `.opencode/skill/system-spec-kit/mcp_server/code-graph/tests/code-graph-context-handler.vitest.ts` | Added a handler-surface regression that routes the real context builder through `handleCodeGraphContext(...)`, forces a timeout after the first anchor, and asserts `metadata.partialOutput.omittedAnchors` equals the number of skipped anchors left out of `graphContext`. | 414-503 |

## Evidence

### Handler-path timeout regression

Added:
```ts
  it('surfaces omittedAnchors for multi-anchor deadline timeouts through the handler payload', async () => {
    const { buildContext: actualBuildContext } = await vi.importActual<typeof import('../lib/code-graph-context.js')>(
      '../lib/code-graph-context.js',
    );

    mocks.buildContext.mockImplementation((args) => actualBuildContext(args));
```

The new test drives:
- three resolved anchors
- a bounded `performance.now()` sequence that expires during the first anchor expansion
- the real `buildContext(...)` implementation behind the mocked handler dependency

It then proves the handler payload stays aligned with skipped-anchor reality:
```ts
      expect(parsed.data.metadata.partialOutput).toEqual({
        isPartial: true,
        reasons: ['deadline'],
        omittedSections: 1,
        omittedAnchors: 2,
        truncatedText: false,
      });
      expect(parsed.data.metadata.partialOutput.omittedAnchors).toBe(
        parsed.data.anchors.length - parsed.data.graphContext.length,
      );
```

## Verification
- `./.opencode/skill/system-spec-kit/mcp_server/node_modules/.bin/vitest run --config .opencode/skill/system-spec-kit/mcp_server/vitest.config.ts .opencode/skill/system-spec-kit/mcp_server/code-graph/tests/code-graph-context-handler.vitest.ts`

## Deferred
- RG-031 only required handler-surface regression coverage. No production code changes were needed in this pass because the remaining gap was missing coverage, not missing runtime logic.
