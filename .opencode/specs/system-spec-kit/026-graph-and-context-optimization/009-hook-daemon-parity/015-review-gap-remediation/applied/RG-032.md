# Applied: RG-032

## Source
- Severity: P2
- Bucket: `013-impl-defects`
- Source audit: `013/review:F-010`
- Finding summary: The scan regression suite mocked `indexFiles()` in a way that bypassed the real `skipFreshFiles` handoff, so the no-op incremental path from `F-004` was not explicitly guarded.
- Recommended fix: Add a no-op incremental regression where `indexFiles()` returns an empty result set and assert summary preservation plus correct skipped-file accounting.

## Target Files Modified
- `.opencode/skill/system-spec-kit/mcp_server/code-graph/tests/code-graph-scan.vitest.ts`
  Tightened the no-op incremental regression so the mocked indexer returns an empty result set through the real `skipFreshFiles` option contract, then asserted `filesScanned`, `filesSkipped`, and persisted-summary preservation.
- `.opencode/specs/system-spec-kit/026-graph-and-context-optimization/009-hook-daemon-parity/015-review-gap-remediation/applied/RG-032.md`
  Recorded the remediation evidence and focused verification for this single finding.

## Evidence
- Merged synthesis lists `RG-032` and the intended fix at `.opencode/specs/system-spec-kit/026-graph-and-context-optimization/research/013-014-review-gap-merged-synthesis.md:69`.
- Merged findings registry records the same summary and recommended fix at `.opencode/specs/system-spec-kit/026-graph-and-context-optimization/research/013-014-review-gap-merged-findings.json:591-603`.
- The updated regression now lives at `.opencode/skill/system-spec-kit/mcp_server/code-graph/tests/code-graph-scan.vitest.ts:210-241`.

Before:
```ts
    mocks.indexFilesMock.mockResolvedValueOnce(withPreParseSkippedCount([], 3));
```

After:
```ts
    mocks.indexFilesMock.mockImplementationOnce(async (_config, options) => {
      expect(options).toEqual({ skipFreshFiles: true });
      return withPreParseSkippedCount([], 3);
    });
```

Added assertions:
```ts
    expect(payload.data.filesScanned).toBe(0);
    expect(payload.data.filesSkipped).toBe(3);
    expect(mocks.indexFilesMock).toHaveBeenCalledWith(expect.any(Object), { skipFreshFiles: true });
    expect(mocks.isFileStaleMock).not.toHaveBeenCalled();
    expect(mocks.setLastGraphEdgeEnrichmentSummaryMock).not.toHaveBeenCalled();
    expect(mocks.clearLastGraphEdgeEnrichmentSummaryMock).not.toHaveBeenCalled();
```

## Verification
- Passed: `node node_modules/vitest/vitest.mjs run mcp_server/code-graph/tests/code-graph-scan.vitest.ts`
  Working directory: `.opencode/skill/system-spec-kit/mcp_server`
  Result: `1` test file passed, `4` tests passed.

## Deferred
- None.
