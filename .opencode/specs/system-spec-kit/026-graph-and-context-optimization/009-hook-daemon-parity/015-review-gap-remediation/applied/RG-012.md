# Applied: RG-012

## Source
- Severity: P1
- Bucket: `013-missed-files`
- Finding: The CocoIndex bridge feature page still pointed at the old seed-resolver test path and did not capture the richer post-013 context contract.

## Target Files Modified
| Path | Change Summary | Lines Touched |
|---|---|---|
| `.opencode/skill/system-spec-kit/feature_catalog/22--context-preservation-and-code-graph/09-cocoindex-bridge-context.md` | Updated the bridge overview to describe CocoIndex seed-fidelity across `file` and `filePath`, explicit blocked-read behavior when `code_graph_context` suppresses a full scan, structured `metadata.partialOutput`, and corrected the stale test reference by pointing at the live code-graph test locations. | 1-38 |

## Evidence of Change
```diff
-Seed resolver normalizes CocoIndex file:line results to ArtifactRef via resolution chain: exact symbol, near-exact symbol, enclosing symbol, file anchor. `code_graph_context` expands resolved anchors in 3 modes: neighborhood (1-hop), outline (file symbols), and impact (reverse callers plus reverse imports). Budget-aware truncation.
+Seed resolver normalizes CocoIndex file:line results to `ArtifactRef` via resolution chain: exact symbol, near-exact symbol, enclosing symbol, file anchor. `code_graph_context` preserves CocoIndex seed fidelity across both `file` and `filePath` inputs so provider metadata such as `source`, `score`, `snippet`, and `range` survive into resolved anchors. When readiness requires a suppressed full scan, the handler returns an explicit blocked payload instead of degraded graph answers. Successful responses still expand resolved anchors in 3 modes: neighborhood (1-hop), outline (file symbols), and impact (reverse callers plus reverse imports), and now expose structured `metadata.partialOutput` fields for deadline or budget omissions.
```

```diff
-| `mcp_server/tests/code-graph-seed-resolver.vitest.ts` | Seed resolution across exact-symbol, enclosing-symbol, and file-anchor fallbacks |
+| `mcp_server/code-graph/tests/code-graph-seed-resolver.vitest.ts` | Seed resolution across exact-symbol, enclosing-symbol, and file-anchor fallbacks |
+| `mcp_server/code-graph/tests/code-graph-context-handler.vitest.ts` | CocoIndex seed fidelity, blocked full-scan responses, and structured `partialOutput` metadata |
```

## Verification
- `sed -n '1,220p' .opencode/skill/system-spec-kit/feature_catalog/22--context-preservation-and-code-graph/09-cocoindex-bridge-context.md` -> confirmed the feature page now names seed fidelity, blocked-read behavior, and `partialOutput`.
- `rg --files .opencode/skill/system-spec-kit/mcp_server/code-graph/tests | rg 'code-graph-(seed-resolver|context-handler)\\.vitest\\.ts$'` -> confirmed both referenced test paths exist.
- `sed -n '1,260p' .opencode/skill/system-spec-kit/mcp_server/code-graph/tests/code-graph-context-handler.vitest.ts` -> confirmed the linked suite covers blocked full-scan responses, CocoIndex seed fidelity, and structured `partialOutput` metadata.

## Deferred
- No new paired test coverage was added because RG-012 is a documentation-drift finding and the underlying regression coverage already exists in `mcp_server/code-graph/tests/code-graph-context-handler.vitest.ts`; this pass only reconciled the feature page with the landed contract.
