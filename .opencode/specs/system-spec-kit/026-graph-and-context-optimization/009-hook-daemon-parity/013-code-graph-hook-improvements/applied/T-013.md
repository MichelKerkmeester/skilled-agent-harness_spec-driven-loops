# Applied: T-013
## Source
- Task description: Address `013-ZC-F-004` by verifying the operation-aware resolver against sibling `handle*` shadow patterns in `.opencode/skill/system-spec-kit/mcp_server/code-graph/handlers/query.ts` and `.opencode/skill/system-spec-kit/mcp_server/code-graph/tests/code-graph-query-handler.vitest.ts`
- Priority: P2
- Finding IDs: `013-ZC-F-004`

## Target Files Modified
| Path | Change Summary | Lines Touched |
|---|---|---|
| `.opencode/skill/system-spec-kit/mcp_server/code-graph/handlers/query.ts` | Applied the operation-aware resolver to both fq-name and plain-name ambiguity paths. | `218-345` |
| `.opencode/skill/system-spec-kit/mcp_server/code-graph/tests/code-graph-query-handler.vitest.ts` | Added sibling `handle*` shadow regressions for both `calls_from` and `calls_to`. | `337-447` |

## Evidence of Change
Before: sibling shadow patterns like re-export wrappers and implementation handlers were treated as equivalent.
After: the resolver verifies CALLS-oriented behavior against realistic `handle*` wrapper-shadow scenarios.

## Verification
- Syntax / type check / anchor check: targeted `eslint` and `tsc --noEmit` passed.
- Test result: targeted Vitest packet suite passed.

## Deferred
- None.
