# Applied: T-006
## Source
- Task description: Address `013-ZC-F-003` by replacing first-candidate ambiguity assertions with wrapper-vs-function `calls_from` and `calls_to` regressions in `.opencode/skill/system-spec-kit/mcp_server/code-graph/tests/code-graph-query-handler.vitest.ts`
- Priority: P1
- Finding IDs: `013-ZC-F-003`

## Target Files Modified
| Path | Change Summary | Lines Touched |
|---|---|---|
| `.opencode/skill/system-spec-kit/mcp_server/code-graph/tests/code-graph-query-handler.vitest.ts` | Replaced deterministic-first-match assertions with wrapper-shadow CALLS regressions for both directions. | `337-447` |

## Evidence of Change
Before: the test suite normalized the wrong behavior by expecting the first candidate.
After: the suite expects `calls_from` and `calls_to` to resolve to the real implementation symbol in sibling `handle*` shadow scenarios.

## Verification
- Syntax / type check / anchor check: targeted `eslint` and `tsc --noEmit` passed.
- Test result: targeted Vitest packet suite passed.

## Deferred
- None.
