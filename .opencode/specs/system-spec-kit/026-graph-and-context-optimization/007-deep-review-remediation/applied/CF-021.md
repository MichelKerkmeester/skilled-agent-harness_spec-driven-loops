# Applied: CF-021

## Source
- Severity: P1
- Flagging phases: 006, 009
- Hook lifecycle naming was inconsistent across the phase 006 implementation summary, the shared hook-system reference, and the fallback/plugin tests, so operator-facing surfaces did not line up with the tested runtime contract.

## Target Files Modified
| Path | Change Summary | Lines Touched |
|---|---|---|
| `.opencode/specs/system-spec-kit/026-graph-and-context-optimization/006-search-routing-advisor/006-smart-router-remediation-and-opencode-plugin/implementation-summary.md` | Replaced obsolete OpenCode lifecycle names with the tested plugin keys. | 119-123 |
| `.opencode/skill/system-spec-kit/references/config/hook_system.md` | Added the canonical runtime hook vocabulary table and updated Gemini/OpenCode lifecycle naming in the lifecycle, matrix, and fallback sections. | 24-45, 68-78 |
| `.opencode/skill/system-spec-kit/mcp_server/tests/cross-runtime-fallback.vitest.ts` | Added a canonical runtime vocabulary map and rewrote Gemini fallback expectations to preserve native Gemini names while matching the fixture's fallback boundary. | 9-34, 51-61, 90-93, 135-146 |
| `.opencode/skill/system-spec-kit/mcp_server/tests/spec-kit-skill-advisor-plugin.vitest.ts` | Froze the canonical OpenCode plugin key set and asserted that legacy lifecycle names stay absent. | 16-25, 136-145 |

## Evidence of Change

```diff
- `onSessionStart` initializes a per-session prompt cache
- `onUserPromptSubmitted` sends the prompt to the bridge over stdin and returns `{ additionalContext }`
- `onSessionEnd` clears the session cache
+ `event` handles per-session cache lifecycle, including startup and cleanup events
+ `experimental.chat.system.transform` sends the prompt to the bridge over stdin and appends the advisor brief to `output.system`
+ `tool.spec_kit_skill_advisor_status` reports settings, readiness, cache counts, and bridge state
```

```diff
- | Gemini | yes (`UserPromptSubmit`) | yes (`SessionStart`) | yes (`PreCompact`) | yes (`Stop`) |
- | OpenCode | yes (`experimental.chat.system.transform`) | yes (`event` handlers) | yes (compact plugin) | yes (event cleanup) |
+ | Gemini | yes (`BeforeAgent`) | yes (`SessionStart`) | yes (`PreCompress`) | yes (`SessionEnd`) |
+ | OpenCode | yes (`experimental.chat.system.transform`) | yes (`event` startup handlers) | yes (`event` compact handlers / compact plugin) | yes (`event` cleanup handlers) |
```

```diff
- it('gemini-cli uses tool_fallback', () => {
+ it('gemini-cli falls back when the fixture does not load repo-root BeforeAgent/SessionStart config', () => {
    setRuntimeEnv('gemini-cli');
+   expect(CANONICAL_RUNTIME_HOOK_VOCABULARY['gemini-cli'].lifecycle).toBe('SessionStart');
    expect(getRecoveryApproach()).toBe('tool_fallback');
  });
```

```diff
- expect(hooks).toHaveProperty('event');
- expect(hooks).toHaveProperty('experimental.chat.system.transform');
- expect(hooks).toHaveProperty('tool');
- expect(hooks).not.toHaveProperty('onSessionStart');
- expect(hooks).not.toHaveProperty('onUserPromptSubmitted');
- expect(hooks).not.toHaveProperty('onSessionEnd');
+ for (const key of CANONICAL_OPENCODE_PLUGIN_SURFACE) {
+   expect(hooks).toHaveProperty(key);
+ }
+ for (const key of LEGACY_OPENCODE_LIFECYCLE_KEYS) {
+   expect(hooks).not.toHaveProperty(key);
+ }
```

## Verification
- `npm exec vitest run tests/cross-runtime-fallback.vitest.ts tests/spec-kit-skill-advisor-plugin.vitest.ts -- --reporter=dot` from `.opencode/skill/system-spec-kit/mcp_server` passed: 2 files, 50 tests.
- `bash .opencode/skill/system-spec-kit/scripts/spec/validate.sh .opencode/specs/system-spec-kit/026-graph-and-context-optimization/006-search-routing-advisor/006-smart-router-remediation-and-opencode-plugin --strict` ran and reported one pre-existing integrity error outside CF-021 scope: stale `023-smart-router-remediation-and-opencode-plugin` metadata in that packet's `implementation-summary.md`.
- `bash .opencode/skill/system-spec-kit/scripts/spec/validate.sh .opencode/specs/system-spec-kit/026-graph-and-context-optimization/007-deep-review-remediation/006-integrity-parity-closure --strict` ran after creating this report and reported existing packet-level Level 3 gaps outside CF-021 scope: missing `decision-record.md` and the corresponding Level 3 consistency failure.
- Markdown anchors in the touched phase 006 spec folder remained valid per the same strict validator run.

## Deferred
- The strict spec validator still reports stale Spec Folder metadata in `.opencode/specs/system-spec-kit/026-graph-and-context-optimization/006-search-routing-advisor/006-smart-router-remediation-and-opencode-plugin/implementation-summary.md`. I did not change that metadata because the user restricted this pass to CF-021 sections only, and the validator issue is unrelated to the hook-vocabulary parity fix.
- The destination phase folder `.opencode/specs/system-spec-kit/026-graph-and-context-optimization/007-deep-review-remediation/006-integrity-parity-closure` is declared Level 3 but is still missing `decision-record.md`. I did not create that file because the task allowed edits only to the CF-021 target files and the applied report.
