# Applied: CF-014

## Source
- Severity: P0
- Flagging phases: 004
- Finding summary: Child-phase deep-research runs still split artifacts across phase-local and packet-root research paths.

## Target Files Modified
| Path | Change Summary | Lines Touched |
|---|---|---|
| `.opencode/skill/system-spec-kit/shared/review-research-paths.cjs` | Expanded the shared resolver to return the canonical packet and archive directories so child-phase research runs have one runtime-owned artifact root. | 45-75, 102-125 |
| `.opencode/command/spec_kit/assets/spec_kit_deep-research_auto.yaml` | Routed prompt rendering and every executor prompt read through `{artifact_dir}/prompts`, created that directory during init, and updated the artifact-root note to match the live contract. | 95-133, 154, 488, 525, 531, 556, 571 |
| `.opencode/skill/sk-deep-research/references/state_format.md` | Replaced the phase-local packet description with the canonical `{artifact_dir}` contract for root and child-phase runs. | 24-29 |

## Evidence of Change
`.opencode/skill/system-spec-kit/shared/review-research-paths.cjs`

```diff
- * @returns {{ rootDir: string, subfolder: string|null }} Resolved packet root and child-phase subfolder
+ * @returns {{ rootDir, subfolder, artifactDir, artifactArchiveRoot }} Resolved packet root plus canonical packet/archive directories
```

```diff
-    return { rootDir, subfolder: null };
+    return { rootDir, subfolder: null, artifactDir: rootDir, artifactArchiveRoot: ... };
```

`.opencode/command/spec_kit/assets/spec_kit_deep-research_auto.yaml`

```diff
-  # step_resolve_artifact_root plus the shared resolver, so child phases write into the
-  # spec tree root's research/ folder with a phase-path subfolder when needed.
+  # step_resolve_artifact_root plus the shared resolver, so child phases write every
+  # workflow-owned artifact into one root-level packet directory when needed.
```

```diff
-            output_path: "{spec_folder}/research/prompts/iteration-{current_iteration}.md"
+            output_path: "{state_paths.prompt_dir}/iteration-{current_iteration}.md"
```

`.opencode/skill/sk-deep-research/references/state_format.md`

```diff
-Research mode stores its runtime packet under `{spec_folder}/research/`, with iteration findings under `{spec_folder}/research/iterations/` and canonical synthesis at `{spec_folder}/research/research.md`.
+Research mode stores its runtime packet under the canonical `{artifact_dir}` resolved by `resolveArtifactRoot(specFolder, 'research')` ... prompts, state, deltas, iteration findings, and canonical synthesis all live under that same packet directory.
```

## Verification
- Syntax check (code): `node --check .opencode/skill/system-spec-kit/shared/review-research-paths.cjs` passed.
- YAML validity: `ruby -e "require 'yaml'; YAML.load_file('.opencode/command/spec_kit/assets/spec_kit_deep-research_auto.yaml'); puts 'YAML_OK'"` returned `YAML_OK`.
- Markdown validity: `python3` anchor-pair check on `.opencode/skill/sk-deep-research/references/state_format.md` returned `MD_ANCHORS_OK`; frontmatter remained intact.
- Spec validation: `bash .opencode/skill/system-spec-kit/scripts/spec/validate.sh .opencode/specs/system-spec-kit/026-graph-and-context-optimization/007-deep-review-remediation/006-integrity-parity-closure --strict` ran and failed with pre-existing packet-structure errors: missing `spec.md`, `plan.md`, `tasks.md`; warning for missing `graph-metadata.json`; warning-level section-count expectations derived from those missing docs.
- No unrelated content deleted: `git diff --unified=1 --` for the three target files showed only the contract and prompt-path hunks above.

## Deferred
- `.opencode/command/spec_kit/assets/spec_kit_deep-research_confirm.yaml` still uses the pre-fix child-local prompt path pattern, but it was outside the allowed target-file set for CF-014 in this task, so it was not edited here.
- Packet-level strict validation remains failing because the remediation subfolder is missing baseline spec-kit documents and `graph-metadata.json`. Creating those files was out of scope for this task and explicitly disallowed by the request.
