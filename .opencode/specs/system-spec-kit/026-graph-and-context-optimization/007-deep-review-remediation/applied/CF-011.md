# Applied: CF-011

## Source
- Severity: P1
- Flagging phases: 003
- The code-graph public schemas lag the runtime handlers, so validated inputs cannot reach `unionMode` or the richer seed shapes accepted by the implementation.

## Target Files Modified
| Path | Change Summary | Lines Touched |
|---|---|---|
| `.opencode/skill/system-spec-kit/mcp_server/schemas/tool-input-schemas.ts` | Added `code_graph_query.unionMode`, expanded `codeGraphSeedSchema` to admit handler-supported seed fields, and allowed `unionMode` through the public parameter allowlist. | `452-480`, `668-669` |
| `.opencode/skill/system-spec-kit/mcp_server/tool-schemas.ts` | Added public `unionMode` documentation for `code_graph_query` and exposed the richer `code_graph_context` seed fields already accepted by the handler. | `576-579`, `613-622` |

## Evidence of Change
```diff
-  subjects: optionalStringArray,
+  subjects: optionalStringArray,
+  unionMode: z.enum(['single', 'multi']).optional(),
...
-  provider: z.enum(['cocoindex', 'manual', 'graph']).optional(),
+  provider: z.enum(['cocoindex', 'manual', 'graph']).optional(),
+  source: z.string().optional(),
...
+  snippet: z.string().optional(),
...
+  kind: z.string().optional(),
+  nodeId: z.string().optional(),
...
-  code_graph_query: ['operation', 'subject', 'subjects', 'edgeType', 'limit', 'includeTransitive', 'maxDepth'],
+  code_graph_query: ['operation', 'subject', 'subjects', 'unionMode', 'edgeType', 'limit', 'includeTransitive', 'maxDepth'],
```

```diff
-      subjects: { type: 'array', items: { type: 'string' }, description: 'Optional additional file paths or symbols for blast-radius union mode' },
+      subjects: { type: 'array', items: { type: 'string' }, description: 'Optional additional file paths or symbols for blast-radius union mode' },
+      unionMode: { type: 'string', enum: ['single', 'multi'], description: 'Blast-radius subject handling mode; use multi to union subject + subjects' },
...
+            source: { type: 'string', description: 'Optional provenance label surfaced on resolved anchors' },
...
+            snippet: { type: 'string', description: 'CocoIndex snippet text preserved with the seed' },
+            kind: { type: 'string', description: 'Manual seed kind metadata' },
+            nodeId: { type: 'string', description: 'Graph seed node identifier' },
```

Handler parity confirmed against the existing runtime implementations without widening them:
- `query.ts` already declares and uses `unionMode` in `QueryArgs` and the `blast_radius` branch at `.opencode/skill/system-spec-kit/mcp_server/code-graph/handlers/query.ts:28` and `:690-728`.
- `context.ts` already accepts and consumes `source`, `snippet`, `kind`, and `nodeId` at `.opencode/skill/system-spec-kit/mcp_server/code-graph/handlers/context.ts:16-30` and `:122-164`.

## Verification
- `npm exec vitest run tests/tool-input-schema.vitest.ts` from `.opencode/skill/system-spec-kit/mcp_server` -> passed (`73` tests).
- `npm exec -- tsc --noEmit --project tsconfig.json` from `.opencode/skill/system-spec-kit/mcp_server` -> passed.
- Reviewed the runtime handlers to confirm the schema additions match already-supported fields rather than introducing new behavior.
- Markdown structure and anchors were preserved; no existing spec docs were modified outside this applied note.

## Deferred
- No deferred work inside this finding scope. Handler code was intentionally left unchanged because the runtime already supports the fields added to the public schemas.
