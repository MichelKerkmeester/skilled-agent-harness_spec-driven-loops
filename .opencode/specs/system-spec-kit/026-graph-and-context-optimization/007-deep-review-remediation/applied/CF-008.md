# Applied: CF-008

## Source
- Severity: P2
- Flagging phases: 002
- Finding summary: The continuity packet still had packet-local traceability drift in its research path and flattened numbering, which weakened discovery and search alignment.

## Target Files Modified
| Path | Change Summary | Lines Touched |
|---|---|---|
| `.opencode/specs/system-spec-kit/026-graph-and-context-optimization/002-continuity-memory-runtime/spec.md` | Repointed the parent packet's research context to the packet-local canonical research artifact. | 165 |
| `.opencode/specs/system-spec-kit/026-graph-and-context-optimization/002-continuity-memory-runtime/004-memory-save-rewrite/spec.md` | Made `004-memory-save-rewrite` the primary flattened packet identity in discovery metadata and clarified that packet-local snapshot folders live inside the flattened packet. | 5-6, 75-76, 149 |
| `.opencode/specs/system-spec-kit/026-graph-and-context-optimization/002-continuity-memory-runtime/004-memory-save-rewrite/implementation-summary.md` | Added the flattened packet id to discovery metadata, normalized the `Spec Folder` field to `004`, and clarified that `013/014/015` snapshot labels are historical sub-artifact sequence numbers. | 6, 47, 93-96 |

## Evidence of Change
### Parent packet research pointer

Before:
```md
Canonical findings live in `.opencode/specs/system-spec-kit/026-graph-and-context-optimization/002-continuity-memory-runtime/research/002-continuity-memory-runtime-pt-01/research.md`
```

After:
```md
Canonical findings live in the packet-local `research/research.md`
```

### Flattened packet numbering

Before:
```yaml
trigger_phrases:
  - "014-memory-save-rewrite"
```

After:
```yaml
trigger_phrases:
  - "004-memory-save-rewrite"
  - "014-memory-save-rewrite"
```

Before:
```md
| **Spec Folder** | 014-memory-save-rewrite |
```

After:
```md
| **Spec Folder** | `004-memory-save-rewrite` (historical alias: `014-memory-save-rewrite`) |
```

### Snapshot traceability clarification

Before:
```md
The packet carries the key source artifacts so a reader can inspect them without leaving the folder:
```

After:
```md
The packet carries the key source artifacts so a reader can inspect them without leaving the folder:

- all snapshot directories below are packet-local to `004-memory-save-rewrite`; `013`, `014`, and `015` are historical snapshot sequence labels, not active packet ids
```

## Verification
- `bash .opencode/skill/system-spec-kit/scripts/spec/validate.sh .opencode/specs/system-spec-kit/026-graph-and-context-optimization/002-continuity-memory-runtime --strict --no-recursive` returned `RESULT: FAILED (strict)` because the packet already carries pre-existing warnings outside CF-008 scope: one custom `research-context` anchor/header deviation in `spec.md` and one missing citation warning in `research/research.md`.
- `bash .opencode/skill/system-spec-kit/scripts/spec/validate.sh .opencode/specs/system-spec-kit/026-graph-and-context-optimization/002-continuity-memory-runtime/004-memory-save-rewrite --strict --no-recursive` returned `RESULT: FAILED` because the packet already has broad pre-existing `SPEC_DOC_INTEGRITY` link failures across `checklist.md`, `plan.md`, and `tasks.md` for missing snapshot-doc references outside the allowed target-file set.
- `rg -n "002-continuity-memory-runtime-pt-01" ...` returned no matches across the three target docs, confirming the stale parent research pointer was removed.
- `rg -n '^\\| \\*\\*Spec Folder\\*\\* \\| 014-memory-save-rewrite \\|$' .../004-memory-save-rewrite/implementation-summary.md` returned no matches, confirming the unaliased stale packet id no longer appears in the metadata table.
- `rg -n '^  - "004-memory-save-rewrite"$|^\\| \\*\\*Phase Folder\\*\\* \\| `004-memory-save-rewrite` \\(historical alias: `014-memory-save-rewrite`\\) \\|$|^\\| \\*\\*Spec Folder\\*\\* \\| `004-memory-save-rewrite` \\(historical alias: `014-memory-save-rewrite`\\) \\|$' .../004-memory-save-rewrite/spec.md .../004-memory-save-rewrite/implementation-summary.md` matched the new flattened packet identity in both target child docs.

## Deferred
- Historical session identifiers, branch names, snapshot directory names, and generated metadata files such as `description.json` were left unchanged because CF-008 scope was limited to the three target docs plus this applied report.
