# Applied: CF-003

## Source
- Severity: P1
- Flagging phases: 001, 006
- Search-routing efficacy is still backed mostly by predictive or observe-only telemetry, so the current routing quality story lacks operational proof.

## Target Files Modified
| Path | Change Summary | Lines Touched |
|---|---|---|
| `.opencode/skill/system-spec-kit/scripts/observability/smart-router-telemetry.ts` | Added evidence-source tagging plus JSONL parsing/reading helpers so live-wrapper records can be distinguished from static prediction records. | 16-26, 58-60, 78-101, 270, 294, 315-380 |
| `.opencode/skill/system-spec-kit/scripts/observability/smart-router-measurement.ts` | Kept static telemetry on the static stream, added live-readiness summarization from wrapper telemetry, and made report language/readiness depend on live observed reads. | 16-21, 78, 97-103, 122, 173-202, 644-705, 723-776, 801 |
| `.opencode/specs/system-spec-kit/026-graph-and-context-optimization/006-search-routing-advisor/007-deferred-remediation-and-telemetry-run/implementation-summary.md` | Updated the phase summary to document the static-vs-live split and the live-readiness gate. | 48, 52, 56, 101, 133 |

## Evidence of Change
```diff
-import { recordSmartRouterCompliance } from './smart-router-telemetry.js';
+import {
+  readSmartRouterComplianceJsonl,
+  recordSmartRouterCompliance,
+  type ComplianceClass,
+  type ComplianceRecord,
+} from './smart-router-telemetry.js';
```

```diff
-        actualReads: [],
-      }, {
-        outputPath: options.liveStream
-          ? undefined
-          : path.resolve(workspaceRoot, options.telemetryOutputPath ?? DEFAULT_STATIC_COMPLIANCE_PATH),
+        actualReads: [],
+        evidenceSource: 'static_prediction',
+      }, {
+        outputPath: path.resolve(workspaceRoot, options.telemetryOutputPath ?? DEFAULT_STATIC_COMPLIANCE_PATH),
       });
...
+  const liveReadiness = summarizeLiveReadiness(
+    readSmartRouterComplianceJsonl(liveTelemetryPath),
+    liveTelemetryPath,
+  );
```

```diff
-Phase 024 now has the measurement machinery that Phases 020-023 deferred: a static 200-prompt harness, an observe-only live-session wrapper, and a JSONL analyzer. The static report is deliberately honest: it measures predicted routes and advisor top-1 labels, not live AI behavior.
+Phase 024 now has the measurement machinery that Phases 020-023 deferred: a static 200-prompt harness, an observe-only live-session wrapper, and a JSONL analyzer. The static report remains deliberately honest, and the measurement path now keeps static telemetry separate from live-wrapper evidence so readiness stays blocked until actual reads are captured.
```

## Verification
- `npm run typecheck` in `.opencode/skill/system-spec-kit/scripts`
- `npm run typecheck` in `.opencode/skill/system-spec-kit/mcp_server`
- `npx vitest run tests/smart-router-measurement.vitest.ts tests/smart-router-analyze.vitest.ts` in `.opencode/skill/system-spec-kit/mcp_server`
- Anchor check: `what-built`, `decisions`, and `limitations` anchors still present in `implementation-summary.md`

## Deferred
- No runtime adapter session was executed in this pass, so the live-readiness gate will remain `BLOCKED` until wrapper telemetry writes records with non-empty `actualReads`.
- The `.codex` registration blockage documented by Phase 007 remains unchanged because it is outside the allowed target-file scope for CF-003.
