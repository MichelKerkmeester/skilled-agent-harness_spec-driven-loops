# Applied: CF-013

## Source

- Severity: `P2`
- Phases: `003`
- Summary: Residual code-graph cleanup debt remained in dangling-edge reconciliation and in post-migration docs and regression coverage.

## Target Files Modified

| File | Change |
| --- | --- |
| `.opencode/skill/system-spec-kit/mcp_server/code-graph/lib/structural-indexer.ts` | Reordered post-parse finalization so node dedup runs before `TESTED_BY` synthesis and reconciles edges whose source nodes were removed. |
| `.opencode/skill/system-spec-kit/mcp_server/code-graph/README.md` | Updated the package contract to describe the dual parser backends, staged persistence helper, shared regression surfaces, and post-dedup edge reconciliation behavior. |
| `.opencode/skill/system-spec-kit/mcp_server/tests/tree-sitter-parser.vitest.ts` | Added focused regression coverage for edge reconciliation after cross-result dedup and for `TESTED_BY` synthesis from retained test nodes only. |

## Evidence of Change

### Before

```ts
// structural-indexer.ts
// Cross-file TESTED_BY edges (heuristic, confidence 0.6)
for (const result of results) {
  if (!testPattern.test(result.filePath)) continue;
  ...
}

const globalSeenIds = new Set<string>();
for (const result of results) {
  const dedupedNodes: CodeNode[] = [];
  ...
}
```

```md
This folder contains the structural code graph subsystem. It walks a workspace,
parses source files via tree-sitter, and persists nodes...
```

### After

```ts
// structural-indexer.ts
const globalSeenIds = new Set<string>();
for (const result of results) {
  const dedupedNodes: CodeNode[] = [];
  ...
  const retainedSourceIds = new Set(dedupedNodes.map((node) => node.symbolId));
  result.edges = result.edges.filter((edge) => retainedSourceIds.has(edge.sourceId));
}

for (const result of results) {
  if (!testPattern.test(result.filePath)) continue;
  ...
}
```

```md
This folder contains the structural code graph subsystem. It walks a workspace,
parses source files via tree-sitter or the shipped regex fallback, and persists nodes...
```

## Verification

- PASS: `node node_modules/vitest/vitest.mjs run tests/tree-sitter-parser.vitest.ts` from `.opencode/skill/system-spec-kit/mcp_server` -> `1` file, `17` tests passed.
- PASS: `npm run typecheck --workspace=@spec-kit/mcp-server` from `.opencode/skill/system-spec-kit` -> exited `0`.

## Deferred

- This pass did not change DB-level edge insertion behavior in `code-graph-db.ts`; the scoped fix only closes the post-dedup reconciliation gap in the structural indexer and its docs/tests.
