# Applied: CF-028

## Source
- Severity: P2
- Flagging phases: 010
- Finding summary: Memory-indexer hardening still lacked direct regression proof for null `canonical_file_path` fallback and tenant/session-scoped `fromScan` flows.

## Target Files Modified
| Path | Change Summary | Lines Touched |
|---|---|---|
| `.opencode/skill/system-spec-kit/mcp_server/tests/pe-orchestration.vitest.ts` | Added a regression that proves PE orchestration falls back to `file_path` when `canonical_file_path` is `null`, preserves same-file `UPDATE`, and keeps scope filters wired into candidate lookup. | 161-239 |
| `.opencode/skill/system-spec-kit/mcp_server/tests/handler-memory-index.vitest.ts` | Tightened the scan-originated and non-scan scoped save regressions so they assert tenant/session scope reaches the reconsolidation bridge directly while preserving the `fromScan` bypass versus normal recheck split. | 783-826, 884-927 |

## Evidence of Change
For `.opencode/skill/system-spec-kit/mcp_server/tests/pe-orchestration.vitest.ts`:

Before:
```ts
  it('downgrades cross-file reinforce decisions to CREATE before they can reuse a sibling row', () => {
```

After:
```ts
  it('falls back to file_path when canonical_file_path is null and preserves scoped same-file updates', () => {
    ...
    expect(mocks.findSimilarMemoriesMock).toHaveBeenCalledWith(
      expect.any(Float32Array),
      expect.objectContaining({
        tenantId: 'tenant-scan',
        userId: 'user-scan',
        agentId: 'agent-scan',
        sessionId: 'session-scan',
        excludeFilePath: tasksPath,
        excludeCanonicalFilePath: tasksPath,
      }),
    );
    expect(result.earlyReturn).toMatchObject({
      status: 'updated',
      id: 123,
      pe_action: 'UPDATE',
    });
  });
```

For `.opencode/skill/system-spec-kit/mcp_server/tests/handler-memory-index.vitest.ts`:

Before:
```ts
        expect(harness.runReconsolidationIfEnabledMock).toHaveBeenCalledTimes(1);
        expect(harness.findScopeFilteredCandidatesMock).not.toHaveBeenCalled();
```

After:
```ts
        expect(harness.runReconsolidationIfEnabledMock).toHaveBeenCalledWith(
          expect.any(Object),
          expect.objectContaining({
            filePath,
            specFolder: 'system-spec-kit/026-graph-and-context-optimization/010-memory-indexer-lineage-and-concurrency-fix',
          }),
          filePath,
          true,
          expect.any(Float32Array),
          {
            tenantId: 'tenant-scan',
            userId: 'user-scan',
            agentId: 'agent-scan',
            sessionId: 'session-scan',
          },
          expect.any(Object),
        );
        expect(harness.findScopeFilteredCandidatesMock).not.toHaveBeenCalled();
```

Before:
```ts
        expect(harness.runReconsolidationIfEnabledMock).toHaveBeenCalledTimes(1);
        expect(harness.findScopeFilteredCandidatesMock).toHaveBeenCalledTimes(1);
```

After:
```ts
        expect(harness.runReconsolidationIfEnabledMock).toHaveBeenCalledWith(
          expect.any(Object),
          expect.objectContaining({
            filePath,
            specFolder: 'system-spec-kit/026-graph-and-context-optimization/010-memory-indexer-lineage-and-concurrency-fix',
          }),
          filePath,
          true,
          expect.any(Float32Array),
          {
            tenantId: 'tenant-direct',
            userId: 'user-direct',
            agentId: 'agent-direct',
            sessionId: 'session-direct',
          },
          expect.any(Object),
        );
        expect(harness.findScopeFilteredCandidatesMock).toHaveBeenCalledTimes(1);
```

## Verification
- `./node_modules/.bin/vitest run tests/pe-orchestration.vitest.ts` from `.opencode/skill/system-spec-kit/mcp_server` -> passed (`1` file, `3` tests)
- `./node_modules/.bin/vitest run tests/handler-memory-index.vitest.ts` from `.opencode/skill/system-spec-kit/mcp_server` -> passed (`1` file, `28` tests)
- Inspected `.opencode/skill/system-spec-kit/mcp_server/handlers/save/pe-orchestration.ts` before editing; no code change was required because the fallback logic already existed and the gap was missing regression proof.

## Deferred
- No production-code change in `.opencode/skill/system-spec-kit/mcp_server/handlers/save/pe-orchestration.ts`; CF-028 closed by making the existing hardening behavior explicit in regression coverage.
