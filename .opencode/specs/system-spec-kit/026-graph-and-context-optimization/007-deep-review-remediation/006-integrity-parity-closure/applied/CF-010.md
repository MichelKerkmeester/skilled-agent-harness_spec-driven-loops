# Applied: CF-010

## Source
- Severity: P1
- Flagging phases: 001, 003
- Code-graph trust metadata is internally inconsistent because parser provenance, freshness labels, and detector vocabulary drift across query, context, startup, and doc surfaces.

## Target Files Modified
| Path | Change Summary | Lines Touched |
|---|---|---|
| `.opencode/skill/system-spec-kit/mcp_server/lib/context/shared-payload.ts` | Added a shared detector-to-parser structural-trust mapper and routed `buildStructuralContextTrust()` through it so startup/context trust carriers can consume the same provenance bridge when detector provenance is available. | `623-638`, `737-767` |
| `.opencode/skill/system-spec-kit/mcp_server/code-graph/lib/readiness-contract.ts` | Centralized persisted detector provenance lookup and made one helper emit both `graphMetadata` and structural trust from the same detector-provenance input. | `31-49`, `129-200` |
| `.opencode/skill/system-spec-kit/mcp_server/code-graph/handlers/query.ts` | Removed the handler-local AST default and switched query payload assembly to the shared readiness trust metadata helper. | `15-18`, `206-223` |
| `.opencode/skill/system-spec-kit/mcp_server/ENV_REFERENCE.md` | Documented that parser-provenance carriers use the shared detector-to-parser mapping instead of assuming AST. | `96` |

## Evidence of Change
```diff
-function buildQueryStructuralTrust(readiness: ReadyResult) {
-  if (readiness.freshness === 'fresh') {
-    return { parserProvenance: 'ast', evidenceStatus: 'confirmed', freshnessAuthority: 'live' } as const;
-  }
-  if (readiness.freshness === 'stale') {
-    return { parserProvenance: 'ast', evidenceStatus: 'probable', freshnessAuthority: 'stale' } as const;
-  }
-  return { parserProvenance: 'unknown', evidenceStatus: 'unverified', freshnessAuthority: 'unknown' } as const;
-}
+const { graphMetadata, structuralTrust } = buildQueryTrustMetadata(readiness);
+const withTrust = attachStructuralTrustFields(base, structuralTrust, { label });
```

```diff
+export function buildStructuralTrustFromProvenance(input: {
+  evidenceStatus: EvidenceStatus;
+  freshnessAuthority: FreshnessAuthority;
+  detectorProvenance?: DetectorProvenance | null;
+  fallbackParserProvenance?: ParserProvenance;
+}): StructuralTrust {
+  const parserProvenance = input.detectorProvenance
+    ? detectorProvenanceToParserProvenance(input.detectorProvenance)
+    : input.fallbackParserProvenance ?? 'unknown';
+  return makeStructuralTrust({
+    parserProvenance,
+    evidenceStatus: input.evidenceStatus,
+    freshnessAuthority: input.freshnessAuthority,
+  });
+}
```

```diff
+function buildQueryTrustArtifacts(
+  readiness: ReadyResult,
+  detectorProvenance: DetectorProvenance | null = getPersistedDetectorProvenance(readiness),
+): QueryTrustMetadata {
+  const graphMetadata = detectorProvenance
+    ? { detectorProvenance, detectorProvenanceSource: 'last-persisted-scan' }
+    : undefined;
+  ...
+  structuralTrust: buildStructuralTrustFromProvenance({
+    detectorProvenance,
+    fallbackParserProvenance: 'unknown',
+    evidenceStatus: 'confirmed',
+    freshnessAuthority: 'live',
+  }),
+}
```

```diff
-| `SPECKIT_PARSER` | `treesitter` | string | Structural parser backend: `treesitter` (AST-accurate via WASM) or `regex` (lightweight fallback). |
+| `SPECKIT_PARSER` | `treesitter` | string | Structural parser backend: `treesitter` (AST-accurate via WASM) or `regex` (lightweight fallback). Detector provenance is surfaced separately on code-graph metadata; when a parser-provenance carrier is required, the shared trust mapper translates persisted detector provenance (for example, `structured -> regex`) instead of assuming AST. |
```

## Verification
- `npm run typecheck` from `.opencode/skill/system-spec-kit/mcp_server` -> passed.
- `npx vitest run tests/readiness-contract.vitest.ts tests/structural-trust-axis.vitest.ts code-graph/tests/code-graph-query-handler.vitest.ts` from `.opencode/skill/system-spec-kit/mcp_server` -> passed (`36` tests).
- `bash .opencode/skill/system-spec-kit/scripts/spec/validate.sh .opencode/specs/system-spec-kit/026-graph-and-context-optimization/007-deep-review-remediation/006-integrity-parity-closure --strict` -> failed on a pre-existing packet issue: missing required Level 3 `decision-record.md`; anchor and frontmatter checks passed.
- Reviewed the flagging-phase research and confirmed the new query path now derives parser provenance from the same detector-provenance bridge already defined in `shared-payload.ts`.
- Markdown structure and anchors were preserved; only the requested target files plus this applied note were edited.

## Deferred
- Startup and session producers outside the allowed target list were not edited in this pass. `buildStructuralContextTrust()` now shares the same provenance mapper, but emitting persisted detector provenance into every startup/context producer would require follow-up edits in non-target handler files.
- Packet-level strict validation remains blocked by the existing missing `decision-record.md`, which was outside the allowed edit scope for this finding.
