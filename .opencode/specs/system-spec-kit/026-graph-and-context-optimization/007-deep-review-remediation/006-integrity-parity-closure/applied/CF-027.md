# Applied: CF-027

## Source
- Severity: P1
- Flagging phases: 009
- Hook-runtime readiness detection and operator guidance were still inconsistent across runtimes: Codex could report `live` from weak local signals, Gemini docs lagged checked-in event names, Copilot readiness keyed off generic hook files, and `advisor_status` treated freshness artifacts as daemon proof.

## Target Files Modified
| Path | Change Summary | Lines Touched |
|---|---|---|
| `.opencode/skill/system-spec-kit/mcp_server/lib/codex-hook-policy.ts` | Made Codex `live` require the documented `codex_hooks` feature flag plus a valid `hooks.json` registration file; downgraded weaker states to `partial` with explicit reasons. | `9`, `45-50`, `128-203`, `285-305` |
| `.opencode/skill/system-spec-kit/references/config/hook_system.md` | Added a normalized runtime-hook vocabulary, updated Gemini to native event names, documented shared startup-state inputs, and clarified Codex/Copilot readiness prerequisites. | `24-45`, `71-78` |
| `.opencode/skill/system-spec-kit/mcp_server/code-graph/lib/runtime-detection.ts` | Replaced Copilot’s generic `.github/hooks` probe with Spec Kit wrapper detection in `.claude/settings.local.json`; Gemini detection now recognizes native and normalized hook names. | `6`, `59-141` |
| `.opencode/skill/system-spec-kit/mcp_server/hooks/gemini/session-prime.ts` | Forwarded optional `specFolder` and `cwd`-derived spec scope into the shared startup brief so Gemini can use the same continuity inputs as Claude. | `9`, `129-159`, `273-295` |
| `.opencode/skill/system-spec-kit/mcp_server/hooks/copilot/session-prime.ts` | Forwarded optional `sessionId` and `specFolder` scope into the shared startup brief so Copilot startup priming uses the same continuity inputs as Claude. | `35-42`, `79-93`, `171-183`, `211-213` |
| `.opencode/skill/system-spec-kit/mcp_server/skill-advisor/handlers/advisor-status.ts` | Stopped deriving daemon availability from freshness alone; now requires a live PID/process signal and emits an operator-visible error when only freshness artifacts exist. | `30-47`, `105-128` |

## Evidence of Change

Before:
```ts
const hooks: CodexHookAvailability = settingsValid ? 'live' : 'partial';
```

After:
```ts
const hasFeatureFlag = hasCodexHooksFeatureEnabled(configPath);
const hasHooksRegistration = hasValidHookRegistration(hooksPath);
const hooks: CodexHookAvailability = hasFeatureFlag && hasHooksRegistration ? 'live' : 'partial';
```

Before:
```ts
const hooksDir = resolve(process.cwd(), '.github', 'hooks');
...
const sessionStartHooks = parsed?.hooks?.sessionStart;
if (Array.isArray(sessionStartHooks) && sessionStartHooks.length > 0) {
  return 'enabled';
}
```

After:
```ts
const settingsPath = resolve(process.cwd(), '.claude', 'settings.local.json');
...
const hasPromptWrapper = hasCopilotWrapper(hooks, 'UserPromptSubmit', '/hooks/copilot/user-prompt-submit.js');
const hasStartupWrapper = hasCopilotWrapper(hooks, 'SessionStart', '/hooks/copilot/session-prime.js');
return hasPromptWrapper && hasStartupWrapper ? 'enabled' : 'disabled_by_scope';
```

Before:
```ts
const daemonAvailable = generation.state !== 'unavailable';
```

After:
```ts
const daemonPid = parseDaemonPid();
const daemonAvailable = hasRunningDaemon(daemonPid);
if (!daemonAvailable && generation.state !== 'unavailable') {
  errors.push('advisor_status found freshness artifacts, but no live daemon PID/process evidence');
}
```

Before:
```md
| Gemini | yes (`UserPromptSubmit`) | yes (`SessionStart`) | yes (`PreCompact`) | yes (`Stop`) |
```

After:
```md
| Gemini | yes (`BeforeAgent`) | yes (`SessionStart`) | yes (`PreCompress`) | yes (`SessionEnd`) |
```

## Verification
- `npm run --workspaces=false typecheck` in `.opencode/skill/system-spec-kit/mcp_server` passed.
- Targeted smoke verification passed with `node_modules/vitest/vitest.mjs run --config /tmp/cf027-vitest.config.mjs`, covering:
  - Codex `partial -> live` transition only when `codex_hooks` and `hooks.json` are both present.
  - Copilot remaining `disabled_by_scope` for unrelated `.github/hooks` files and turning `enabled` only with Spec Kit wrapper fields in `.claude/settings.local.json`.
  - Gemini reporting `enabled` when native `BeforeAgent` / `PreCompress` / `SessionEnd` hook names are present.
  - `advisor_status` returning `unavailable` plus an explicit error when freshness artifacts exist without live daemon PID/process evidence.
- Markdown structure check: existing headings and section order in `hook_system.md` were preserved; no frontmatter or anchor blocks were removed.

## Deferred
- No deferred items inside the requested target-file scope.
