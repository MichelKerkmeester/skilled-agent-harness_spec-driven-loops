# Applied: CF-025

## Source
- Severity: P0
- Flagging phases: 008
- Finding summary: The non-native executor runtime is still fail-by-contract because canonical iteration records omit executor metadata while validation requires it, and timeout/crash helpers are not wired into live dispatch.

## Target Files Modified
| Path | Change Summary | Lines Touched |
|---|---|---|
| `.opencode/skill/system-spec-kit/mcp_server/lib/deep-loop/executor-audit.ts` | Added a shared audited executor runner that converts timeout and crash paths into typed `dispatch_failure` events before validator checks run. | 3-4, 152-225 |
| `.opencode/skill/sk-deep-research/assets/prompt_pack_iteration.md.tmpl` | Updated the iteration contract so non-native executor provenance is workflow-owned and failure events stay workflow-emitted instead of agent-authored. | 42-48 |
| `.opencode/command/spec_kit/assets/spec_kit_deep-research_auto.yaml` | Wrapped every non-native CLI dispatch in the audited runner and moved `record_executor_audit` ahead of `post_dispatch_validate` so the first canonical iteration record carries executor metadata before validation. | 517-733 |

## Evidence of Change
`.opencode/skill/system-spec-kit/mcp_server/lib/deep-loop/executor-audit.ts`

```diff
+export function runAuditedExecutorCommand(input: RunAuditedExecutorCommandInput): number {
+  const result = spawnSync(input.command, input.args, { ... });
+  emitDispatchFailure(input.stateLogPath, input.executor, 'timeout' | 'crash', input.iteration, ...);
+}
```

`.opencode/skill/sk-deep-research/assets/prompt_pack_iteration.md.tmpl`

```diff
-{"type":"iteration","iteration":<n>,"newInfoRatio":<0..1>,"status":"<string>","focus":"<string>","graphEvents":[/* optional */]}
+{"type":"iteration","iteration":<n>,"newInfoRatio":<0..1>,"status":"<string>","focus":"<string>","graphEvents":[/* optional */],"executor":{/* workflow-owned for non-native runs */}}
```

```diff
+For non-native CLI executors, the workflow owns executor provenance ... before `post_dispatch_validate` runs.
```

`.opencode/command/spec_kit/assets/spec_kit_deep-research_auto.yaml`

```diff
-            codex exec \
-              --model "{config.executor.model}" \
+            node --experimental-strip-types --input-type=module <<'EOF'
+            import { runAuditedExecutorCommand } from './.opencode/skill/system-spec-kit/mcp_server/lib/deep-loop/executor-audit.ts';
```

```diff
-        post_dispatch_validate:
+        record_executor_audit:
+          skip_when: "config.executor.kind == 'native'"
+        post_dispatch_validate:
```

## Verification
- Syntax check (code): `node --experimental-strip-types --check .opencode/skill/system-spec-kit/mcp_server/lib/deep-loop/executor-audit.ts` passed.
- Focused tests: `.opencode/skill/system-spec-kit/mcp_server/node_modules/.bin/vitest run .opencode/skill/system-spec-kit/mcp_server/tests/deep-loop/executor-audit.vitest.ts .opencode/skill/system-spec-kit/mcp_server/tests/deep-loop/dispatch-failure.vitest.ts .opencode/skill/system-spec-kit/mcp_server/tests/deep-loop/post-dispatch-validate.vitest.ts --config .opencode/skill/system-spec-kit/mcp_server/vitest.config.ts` passed (`3` files, `25` tests).
- YAML validity: `ruby -e "require 'yaml'; YAML.load_file('.opencode/command/spec_kit/assets/spec_kit_deep-research_auto.yaml'); puts 'YAML_OK'"` returned `YAML_OK`.
- Markdown/schema validity: the prompt-pack template remained plain markdown text with no frontmatter or anchors added; a quick Python read check passed (`PROMPT_OK`).
- Spec validation: `bash .opencode/skill/system-spec-kit/scripts/spec/validate.sh .opencode/specs/system-spec-kit/026-graph-and-context-optimization/007-deep-review-remediation/006-integrity-parity-closure --strict` failed with pre-existing packet-structure/template issues outside CF-025 scope, including a missing `decision-record.md`, missing required anchors/template headers in existing packet docs, and existing references to missing `cross-phase-synthesis.md`.
- No unrelated content deleted: the CF-025 hunks above only add the audited runner, the workflow-owned provenance note, and the audit-before-validate reorder. The YAML file already contained separate pre-existing working-tree edits outside these line ranges, and they were preserved untouched.

## Deferred
- `.opencode/skill/system-spec-kit/mcp_server/lib/deep-loop/post-dispatch-validate.ts` was read for CF-025 but did not require a code change in this pass. Its existing `dispatch_failure_logged` short-circuit and `executor_missing` enforcement already matched the intended contract once the workflow started emitting typed failures and writing executor metadata before validation.
