# Applied: CF-016

## Source

- Packet: `system-spec-kit/026-graph-and-context-optimization/007-deep-review-remediation/006-integrity-parity-closure`
- Finding: `CF-016`
- Flagging phase: `004`
- Recommended fix: codify the deep-research governance boundary and add explicit validation hooks where the workflow mutates authored spec markdown

## Target Files Modified

| File | Change |
| --- | --- |
| `AGENTS.md` | Narrowed the generic markdown governance rule to authored spec docs and documented the deep-research workflow-owned packet exemption plus required validation behavior. |
| `.opencode/skill/system-spec-kit/SKILL.md` | Mirrored the same distributed-governance clarification so skill guidance matches policy. |
| `.opencode/command/spec_kit/assets/spec_kit_deep-research_auto.yaml` | Added targeted strict `validate.sh` checkpoints after deep-research `spec.md` mutations and documented why the rule bundle is scoped. |

## Evidence of Change

### `AGENTS.md`

Before:

```md
- Any agent writing spec folder docs (*.md) MUST use templates from .opencode/skill/system-spec-kit/templates/level_N/, run `bash .opencode/skill/system-spec-kit/scripts/spec/validate.sh [spec_folder] --strict` after each file write, and route continuity updates through /memory:save. @deep-research retains exclusive write access for research/research.md; @debug retains exclusive write access for debug-delegation.md.
```

After:

```md
- Any agent writing authored spec folder docs (`spec.md`, `plan.md`, `tasks.md`, `checklist.md`, `implementation-summary.md`, `decision-record.md`, `handover.md`, `review-report.md`, `debug-delegation.md`) MUST use templates from .opencode/skill/system-spec-kit/templates/level_N/, run `bash .opencode/skill/system-spec-kit/scripts/spec/validate.sh [spec_folder] --strict` after each file write, and route continuity updates through /memory:save. Deep-research workflow-owned packet markdown (`research/iterations/*.md`, `research/deep-research-*.md`, and progressive `research/research.md` loop updates) is exempt from that generic per-write rule; `/spec_kit:deep-research` must instead run targeted strict validation after every `spec.md` mutation it performs. @deep-research retains exclusive write access for `research/research.md`; @debug retains exclusive write access for `debug-delegation.md`.
```

### `.opencode/skill/system-spec-kit/SKILL.md`

Before:

```md
Any agent writing spec folder docs (*.md) MUST use templates from .opencode/skill/system-spec-kit/templates/level_N/, run `bash .opencode/skill/system-spec-kit/scripts/spec/validate.sh [spec_folder] --strict` after each file write, and route continuity updates through /memory:save. @deep-research retains exclusive write access for research/research.md; @debug retains exclusive write access for debug-delegation.md.
```

After:

```md
Any agent writing authored spec folder docs (`spec.md`, `plan.md`, `tasks.md`, `checklist.md`, `implementation-summary.md`, `decision-record.md`, `handover.md`, `review-report.md`, `debug-delegation.md`) MUST use templates from .opencode/skill/system-spec-kit/templates/level_N/, run `bash .opencode/skill/system-spec-kit/scripts/spec/validate.sh [spec_folder] --strict` after each file write, and route continuity updates through /memory:save. Deep-research workflow-owned packet markdown (`research/iterations/*.md`, `research/deep-research-*.md`, and progressive `research/research.md` loop updates) is exempt from that generic per-write rule; `/spec_kit:deep-research` must instead run targeted strict validation after every `spec.md` mutation it performs. @deep-research retains exclusive write access for `research/research.md`; @debug retains exclusive write access for `debug-delegation.md`.
```

### `.opencode/command/spec_kit/assets/spec_kit_deep-research_auto.yaml`

Before:

```yaml
      step_preinit_spec_branch:
        action: "Run the bounded pre-init spec branch required by spec_check_protocol"
        protocol_reference: ".opencode/skill/sk-deep-research/references/spec_check_protocol.md"
```

```yaml
      step_writeback_spec_findings:
        action: "Write or replace the bounded generated findings block in spec.md before config completion"
```

After:

```yaml
      step_validate_preinit_spec_doc:
        action: "Run targeted strict validation after any deep-research pre-init spec.md mutation"
        condition: "validate_spec_doc"
        command: "SPECKIT_RULES=FRONTMATTER_VALID,LEVEL_DECLARED,TEMPLATE_SOURCE,TEMPLATE_HEADERS,PLACEHOLDER_FILLED,SECTIONS_PRESENT,ANCHORS_VALID,SPEC_DOC_INTEGRITY,TOC_POLICY bash .opencode/skill/system-spec-kit/scripts/spec/validate.sh {spec_folder} --strict"
```

```yaml
      step_validate_post_synthesis_spec_doc:
        action: "Run targeted strict validation after deep-research writes back into spec.md"
        command: "SPECKIT_RULES=FRONTMATTER_VALID,LEVEL_DECLARED,TEMPLATE_SOURCE,TEMPLATE_HEADERS,PLACEHOLDER_FILLED,SECTIONS_PRESENT,ANCHORS_VALID,SPEC_DOC_INTEGRITY,TOC_POLICY bash .opencode/skill/system-spec-kit/scripts/spec/validate.sh {spec_folder} --strict"
```

## Verification

- `SPECKIT_RULES=FRONTMATTER_VALID,LEVEL_DECLARED,TEMPLATE_SOURCE,TEMPLATE_HEADERS,PLACEHOLDER_FILLED,SECTIONS_PRESENT,ANCHORS_VALID,SPEC_DOC_INTEGRITY,TOC_POLICY bash .opencode/skill/system-spec-kit/scripts/spec/validate.sh .opencode/specs/system-spec-kit/026-graph-and-context-optimization/007-deep-review-remediation/006-integrity-parity-closure --strict` -> pass
- `bash .opencode/skill/system-spec-kit/scripts/spec/validate.sh .opencode/specs/system-spec-kit/026-graph-and-context-optimization/007-deep-review-remediation/006-integrity-parity-closure --strict` -> fail on existing packet-level `decision-record.md` / Level 3 completeness gaps outside CF-016 scope
- `ruby -e "require 'yaml'; YAML.load_file('.opencode/command/spec_kit/assets/spec_kit_deep-research_auto.yaml'); puts 'YAML_OK'"` -> `YAML_OK`
- `git diff --check -- AGENTS.md .opencode/skill/system-spec-kit/SKILL.md .opencode/command/spec_kit/assets/spec_kit_deep-research_auto.yaml .opencode/specs/system-spec-kit/026-graph-and-context-optimization/007-deep-review-remediation/006-integrity-parity-closure/applied/CF-016.md` -> pass

## Deferred

- No change was made to `sk-deep-research` reference docs or tests in this pass because the task scope was limited to the three target files plus the applied report.
