# Applied: CF-005

## Source
- Severity: P0
- Flagging phases: 002
- Finding summary: Routed full-auto saves can lose writes because merge preparation happens before the folder lock.

## Target Files Modified
| Path | Change Summary | Lines Touched |
|---|---|---|
| `.opencode/skill/system-spec-kit/mcp_server/handlers/save/atomic-index-memory.ts` | Added an optional locked re-prepare hook and moved effective-path/original-state capture into the locked section before promotion. | `66-90`, `302-421` |
| `.opencode/skill/system-spec-kit/mcp_server/handlers/memory-save.ts` | Recomputed canonical routed save preparation while the spec-folder lock is held for full-auto atomic saves. | `3059-3132` |
| `.opencode/skill/system-spec-kit/mcp_server/tests/handler-memory-save.vitest.ts` | Extended the harness to mock the spec-folder mutex and added a concurrency regression proving both routed writes survive lock-ordered execution. | `644-647`, `792-795`, `993-994`, `1207-1289` |

## Evidence of Change
`.opencode/skill/system-spec-kit/mcp_server/handlers/save/atomic-index-memory.ts`

```diff
- const effectiveFilePath = prepared.persistedFilePath ?? file_path;
- const pendingPath = (dependencies.getPendingPath ?? defaultPendingPath)(effectiveFilePath);
+ if (dependencies.prepareLocked) {
+   const preparedLocked = await dependencies.prepareLocked(lockedContext);
+ }
+ effectiveFilePath = ready.persistedFilePath ?? file_path;
```

`.opencode/skill/system-spec-kit/mcp_server/handlers/memory-save.ts`

```diff
+ prepareLocked: async ({ ready, params: currentParams }) => {
+   const canonicalPrepared = await buildCanonicalAtomicPreparedSave(currentParams, database);
+ },
```

`.opencode/skill/system-spec-kit/mcp_server/tests/handler-memory-save.vitest.ts`

```diff
+ specFolderMutexModuleFactory?: () => unknown | Promise<unknown>;
+ vi.doMock('../handlers/save/spec-folder-mutex', options.specFolderMutexModuleFactory as any);
+ it('rebuilds canonical routed merges after acquiring the spec-folder lock', async () => {
```

## Verification
- Syntax check (for code changes): `npx tsc --noEmit --composite false -p tsconfig.json` in `.opencode/skill/system-spec-kit/mcp_server` exited `0`.
- Syntax check (behavioral regression): `node node_modules/vitest/vitest.mjs run tests/handler-memory-save.vitest.ts --config vitest.config.ts -t "routes canonical atomic saves into the target implementation summary document|rebuilds canonical routed merges after acquiring the spec-folder lock"` exited `0` with `2 passed`.
- Markdown/schema validity (for docs): applied report created as plain Markdown with the requested heading and section structure; no frontmatter or anchors were introduced or modified.
- No unrelated content deleted: changes are limited to the three target code files plus this applied report.

## Deferred
- None.
