# Applied: CF-001

## Source
- Severity: P0
- Flagging phases: 001
- Finding summary: The archived root roadmap and convergence labels are stale enough to mis-sequence follow-on work because shipped substrate is still represented as missing or unfinished.

## Target Files Modified
| Path | Change Summary | Lines Touched |
|---|---|---|
| `.opencode/specs/system-spec-kit/026-graph-and-context-optimization/spec.md` | Added a root packet state model that distinguishes `implemented`, `narrowed`, `reopened`, and `still-open` work, and clarified that archived research convergence does not equal operational acceptance. | 107-130 |
| `.opencode/specs/system-spec-kit/026-graph-and-context-optimization/001-research-and-baseline/research/archive/v-pre-20260423/deep-research-dashboard.md` | Recast the archive status and progress labels as research-only convergence signals and marked the hidden-prereq row as historical rather than a live blocker ledger. | 4-25 |

## Evidence of Change
For `.opencode/specs/system-spec-kit/026-graph-and-context-optimization/spec.md`:

Before:
```md
> Active children are the nine thematic wrappers below. Historical phases `001` through `029` are preserved as direct child folders under each wrapper and mapped in `merged-phase-map.md`.
```

After:
```md
> Active children are the nine thematic wrappers below. Historical phases `001` through `029` are preserved as direct child folders under each wrapper and mapped in `merged-phase-map.md`. The `Status` column tracks wrapper activity only; use the current-state model below to distinguish implemented, narrowed, reopened, and still-open follow-on work.
```

Before:
```md
| 009 | `009-hook-daemon-parity/` | Skill graph daemon, hook parity, plugin/runtime parity, and parity remediation | `009-hook-daemon-parity/001-skill-advisor-hook-surface/`, `009-hook-daemon-parity/002-skill-graph-daemon-and-advisor-unification/`, `009-hook-daemon-parity/003-hook-parity-remediation/` | In Progress |
```

After:
```md
### Current State Model

| `implemented` | Shipped substrate exists and should not be treated as missing prerequisite work. | Shared trust axes and readiness primitives, planner-first memory-save substrate, Codex hook injection, search-routing measurement scaffolding |
```

For `.opencode/specs/system-spec-kit/026-graph-and-context-optimization/001-research-and-baseline/research/archive/v-pre-20260423/deep-research-dashboard.md`:

Before:
```md
**Status:** ✅ **COMPLETE** — 18 master iterations complete ...
| Convergence flag | **CONVERGED** (v1: question-coverage + composite; v2: rigor-lane stable) |
| Public hidden prereqs (iter-13) | 11 |
```

After:
```md
**Status:** ✅ **RESEARCH COMPLETE** — 18 master iterations complete ... This archived dashboard records research convergence, not a clean operational-acceptance verdict for later implementation packets.
| Research convergence flag | **CONVERGED** (v1: question-coverage + composite; v2: rigor-lane stable) |
| Historical hidden prereqs snapshot (iter-13) | 11 in the archived corpus; do not treat this row as the current blocker ledger |
```

## Verification
- Syntax check (for code changes): Not applicable; documentation-only change.
- Markdown/schema validity (for docs): Confirmed frontmatter remains intact in `spec.md`; added content stays within existing markdown sections and preserves anchor pairs; archive dashboard structure remains valid markdown.
- No unrelated content deleted

## Deferred
- None.
