# Applied: CF-024

## Source
- Severity: P1
- Flagging phases: 007
- Session-completion governance still permitted false completion and false routing because status extraction could auto-complete without explicit proof and `/complete` still advertised `@debug` while dispatching a generic agent.

## Target Files Modified
| Path | Change Summary | Lines Touched |
|---|---|---|
| `.opencode/skill/system-spec-kit/scripts/extractors/collect-session-data.ts` | Added explicit-completion evidence detection and required that evidence for clean-repo, JSON-mode, and high-activity completion paths. | 411-435, 449-474, 508-528 |
| `.opencode/command/spec_kit/assets/spec_kit_complete_confirm.yaml` | Aligned the debug escalation dispatch target with the documented `@debug` path. | 929 |

## Evidence of Change
```diff
-  ) && !hasUnresolvedNextSteps && !hasPendingWorkIndicators) {
+  ) && !hasUnresolvedNextSteps && !hasPendingWorkIndicators && hasExplicitCompletionEvidence) {
     return 'COMPLETED';
   }
```

```diff
-    // If explicit JSON data has summary + decisions + next steps, session is complete
-    // But if there are pending nextSteps, downgrade to partial (IN_PROGRESS)
+    // Require explicit completion evidence before JSON-mode data can auto-complete.
     if (saveMode === SaveMode.Json && hasSessionSummary && (hasKeyDecisions || hasNextSteps)) {
       if (hasUnresolvedNextSteps) {
         return 'IN_PROGRESS';
       }
-      return 'COMPLETED';
+      if (hasExplicitCompletionEvidence) {
+        return 'COMPLETED';
+      }
     }
```

```diff
-  if (hasNoBlockers && highActivity && !hasPendingWorkIndicators) {
+  if (hasNoBlockers && highActivity && !hasPendingWorkIndicators && hasExplicitCompletionEvidence) {
     return 'COMPLETED';
   }
```

```diff
       debug_dispatch:
-        subagent_type: "general-purpose"
+        subagent_type: "debug"
         timeout_ms: 120000
```

## Verification
- `node mcp_server/node_modules/vitest/vitest.mjs run scripts/tests/collect-session-data.vitest.ts --config mcp_server/vitest.config.ts` from `.opencode/skill/system-spec-kit` passed: `1` file, `14` tests.
- `./node_modules/.bin/tsc --noEmit --composite false -p scripts/tsconfig.json` from `.opencode/skill/system-spec-kit` passed.
- `ruby -e "require 'yaml'; YAML.safe_load(File.read('.opencode/command/spec_kit/assets/spec_kit_complete_confirm.yaml'), permitted_classes: [], aliases: true); puts 'YAML_OK'"` passed.
- Markdown/frontmatter anchors were preserved because no frontmatter or anchor-bearing markdown content was edited in the target files.

## Deferred
- None.
