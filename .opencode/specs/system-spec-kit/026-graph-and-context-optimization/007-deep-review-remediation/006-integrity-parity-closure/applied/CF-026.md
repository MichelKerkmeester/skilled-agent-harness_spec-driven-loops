# Applied: CF-026

## Source
- Severity: P1
- Flagging phases: 008
- Finding summary: CLI executor hardening was incomplete because `sandboxMode` and permission controls were not consistently executable, Copilot's large-prompt wrapper only had string-shape proof, and failure-path verification stayed mostly unit-level.

## Target Files Modified
| Path | Change Summary | Lines Touched |
|---|---|---|
| `.opencode/skill/system-spec-kit/mcp_server/lib/deep-loop/executor-config.ts` | Added shared runtime helpers for codex sandbox selection, gemini sandbox mapping, claude permission mapping, and Copilot large-prompt wrapper selection so the YAML and tests use one contract. | 17, 43-67 |
| `.opencode/command/spec_kit/assets/spec_kit_deep-research_auto.yaml` | Replaced hardcoded permission values with shared helper calls for codex, gemini, and claude; centralized Copilot wrapper prompt selection; documented Copilot's fixed broad posture. | 523, 548, 569-573, 607, 616, 635, 661, 677, 701 |
| `.opencode/skill/system-spec-kit/mcp_server/tests/deep-loop/cli-matrix.vitest.ts` | Updated command-shape expectations to follow the live permission mappings and added subprocess smoke tests for the Copilot wrapper plus crash/timeout `dispatch_failure` logging. | 1-15, 37, 52, 62, 72, 79-306 |
| `.opencode/specs/system-spec-kit/026-graph-and-context-optimization/008-runtime-executor-hardening/002-sk-deep-cli-runtime-execution/002-runtime-matrix/implementation-summary.md` | Narrowed packet claims so the doc matches the new executable mappings and subprocess smoke coverage, and documented the remaining vendor-CLI proof gap. | 3, 10-15, 62-67, 82, 143, 186, 199, 210-212 |

## Evidence of Change
```diff
-                  'workspace-write',
+                  resolveCodexSandboxMode(executor.sandboxMode),
...
-                  'none',
+                  resolveGeminiSandboxMode(executor.sandboxMode),
...
-              'acceptEdits',
+              resolveClaudePermissionMode(executor.sandboxMode),
```

```diff
-            const promptArg =
-              Buffer.byteLength(prompt, 'utf8') <= 16384
-                ? prompt
-                : `Read the instructions in @${promptPath} and follow them exactly. Do not deviate.`;
+            const promptArg = resolveCopilotPromptArg(promptPath, prompt);
```

```diff
+  it('exercises the large-prompt wrapper with a real subprocess and artifact writes', () => {
+  it('records a crash failure event from a real subprocess exit', () => {
+  it('records a timeout failure event from a real subprocess timeout', () => {
```

```diff
-1. **No live-dispatch smoke tests**: ...
+1. **No vendor-CLI end-to-end smoke tests**: The matrix now has subprocess smoke coverage for the Copilot wrapper plus audited crash/timeout failure paths, but it still does not prove a real `copilot`, `gemini`, `claude`, or `codex` binary can complete the full iteration artifact contract in this packet's automated suite.
```

## Verification
- `npx tsc --noEmit --composite false -p tsconfig.json` in `.opencode/skill/system-spec-kit/mcp_server` exited `0`.
- `npx vitest run tests/deep-loop/cli-matrix.vitest.ts tests/deep-loop/executor-config.vitest.ts tests/deep-loop/dispatch-failure.vitest.ts` exited `0` with `3` files and `37` tests passing.
- The new subprocess smoke emitted `wrapper-ok`, confirming the large-prompt Copilot wrapper path exercised a real temp-file handoff.
- `bash .opencode/skill/system-spec-kit/scripts/spec/validate.sh .opencode/specs/system-spec-kit/026-graph-and-context-optimization/008-runtime-executor-hardening/002-sk-deep-cli-runtime-execution/002-runtime-matrix --strict` exited `2`, but only with packet-level warnings in strict mode after this fix (`RESULT: FAILED (strict)` from existing anchor/header and AI-protocol deviations).
- `bash .opencode/skill/system-spec-kit/scripts/spec/validate.sh .opencode/specs/system-spec-kit/026-graph-and-context-optimization/007-deep-review-remediation/006-integrity-parity-closure --strict` exited `2` because the packet is already missing required Level 3 file `decision-record.md`, which is outside the allowed write scope for this task.

## Deferred
- Real vendor-CLI end-to-end smoke was not added. This pass adds local subprocess proof for wrapper/failure behavior only; the packet still needs an environment-owned smoke gate with actual `copilot`, `codex`, `gemini`, and `claude` binaries.
- The strict spec-validation failures were not fully cleared because the allowed scope excluded creating the missing `decision-record.md` in `006-integrity-parity-closure` and excluded broader template/header cleanup outside the listed target files.
