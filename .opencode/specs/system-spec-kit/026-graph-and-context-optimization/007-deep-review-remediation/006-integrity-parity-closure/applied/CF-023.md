# Applied: CF-023

## Source
- Severity: P1
- Flagging phases: 007
- Deep-review remediation status surfaces were stale against landed child work and current `009` parity truth: shipped packets still read as `Spec Ready` or `Draft`, while the older `007` cleanup ledger no longer matched the live `009` backlog.

## Target Files Modified
| Path | Change Summary | Lines Touched |
|---|---|---|
| `.opencode/specs/system-spec-kit/026-graph-and-context-optimization/007-deep-review-remediation/spec.md` | Rebuilt the parent remediation ledger from current child state and refreshed the `009` parity dependency language. | `64-67`, `76-90`, `101-126`, `153-165` |
| `.opencode/specs/system-spec-kit/026-graph-and-context-optimization/007-deep-review-remediation/002-cli-executor-remediation/tasks.md` | Synced the Phase 020 task ledger to the shipped R1-R11 state and isolated deferred/admin-only follow-up. | `10-13`, `21`, `44-125` |
| `.opencode/specs/system-spec-kit/026-graph-and-context-optimization/007-deep-review-remediation/004-r03-post-remediation/spec.md` | Promoted Phase 026 out of draft status and replaced future-tense dispatch instructions with the real remaining verification/admin steps. | `14-24`, `38`, `108-115` |
| `.opencode/specs/system-spec-kit/026-graph-and-context-optimization/007-deep-review-remediation/004-r03-post-remediation/tasks.md` | Narrowed the open work to packet-level verification/admin closeout and marked summary/checklist sync as complete. | `3`, `10`, `75-76` |
| `.opencode/specs/system-spec-kit/026-graph-and-context-optimization/009-hook-daemon-parity/spec.md` | Reconciled the phase map so child `001` no longer over-claims completion and the `010`/`011` reapply backlog is explicit. | `106`, `115-116` |

## Evidence of Change
```diff
-| 2 | `002-cli-executor-remediation/` | Spec Ready | Feature Specification: Phase 020 Research-Findings Remediation |
-| 4 | `004-r03-post-remediation/` | Draft | Feature Specification: Phase 026 — R03 Post-Remediation Remediation |
+| 2 | `002-cli-executor-remediation/` | Complete | Phase 020 shipped R1-R11 with 116/116 scoped tests green; remaining follow-up is limited to deferred R12/Q4/admin closeout. |
+| 4 | `004-r03-post-remediation/` | Phase-local complete, parent gates pending | Phase 026 closed T01-T12 with evidence; remaining work is verification/admin closeout sync. |
```

```diff
-    recent_action: "Tasks scaffolded"
-    next_safe_action: "Begin T-AUD-01"
+    recent_action: "Synced shipped R1-R11 work into the task ledger"
+    next_safe_action: "Treat remaining unchecked items as deferred or packet-admin closeout only"
...
- [ ] **T-AUD-01** [A] Add `writeFirstRecordExecutor(stateLogPath, executor, iter)` to `executor-audit.ts`.
+ [x] **T-AUD-01** [A] Add `writeFirstRecordExecutor(stateLogPath, executor, iter)` to `executor-audit.ts`.
...
+Wave G remains the explicit deferred branch from the implementation summary. These tasks stay unchecked because the packet records R12 as out of scope rather than shipped in this pass.
```

```diff
-| **Status** | Draft |
+| **Status** | Phase-local complete, parent gates pending |
...
-Sequential driver script `run-phase-026.sh`:
-1. For each task T01..T12:
-   - Build task-specific prompt with iteration file + fix instructions
-   - Fresh `copilot -p "..." --model gpt-5.4 --allow-all-tools --no-ask-user` invocation
+The sequential per-finding cli-copilot dispatch is no longer the open work for this packet.
+Remaining packet-local follow-up is limited to:
+1. Run `npm --prefix ../../../../../skill/system-spec-kit/mcp_server run build`.
+2. Run the focused Phase 025 baseline plus the T06/T10/T11-added tests.
```

```diff
-| 1 | `001-skill-advisor-hook-surface/` | Complete | Feature Specification: Skill-Advisor Hook Surface |
+| 1 | `001-skill-advisor-hook-surface/` | In Progress | Skill-Advisor Hook Surface implementation is shipped, but the child checklist and closeout surfaces are still scaffolded and need reconciliation before this row can claim complete. |
```

## Verification
- `bash .opencode/skill/system-spec-kit/scripts/spec/validate.sh .opencode/specs/system-spec-kit/026-graph-and-context-optimization/007-deep-review-remediation --strict`
  Result: edited parent anchors/frontmatter still parse, but the command exits non-zero on pre-existing recursive issues in untouched handover/session docs and older child packets.
- `bash .opencode/skill/system-spec-kit/scripts/spec/validate.sh .opencode/specs/system-spec-kit/026-graph-and-context-optimization/007-deep-review-remediation/002-cli-executor-remediation --strict`
  Result: task-sheet edits are structurally accepted; command still fails on pre-existing anchor/template debt elsewhere in the packet.
- `bash .opencode/skill/system-spec-kit/scripts/spec/validate.sh .opencode/specs/system-spec-kit/026-graph-and-context-optimization/007-deep-review-remediation/004-r03-post-remediation --strict`
  Result: the status-sync edits parse cleanly; command still fails on pre-existing frontmatter/template deviations elsewhere in that packet.
- `bash .opencode/skill/system-spec-kit/scripts/spec/validate.sh .opencode/specs/system-spec-kit/026-graph-and-context-optimization/009-hook-daemon-parity --strict`
  Result: the parent phase-map change passes packet-level checks; recursive validation continues to report older child-packet issues outside this finding's scope.
- `bash .opencode/skill/system-spec-kit/scripts/spec/validate.sh .opencode/specs/system-spec-kit/026-graph-and-context-optimization/007-deep-review-remediation/006-integrity-parity-closure --strict`
  Result: the new applied report parses cleanly; the packet still fails on pre-existing Level 3 debt because `decision-record.md` is absent in the packet.

## Deferred
- `002-cli-executor-remediation/spec.md` was left untouched because it is outside the allowed target-file list, even though its status surface also lags the shipped implementation summary.
- `001-deep-review-and-remediation/tasks.md` was not rewritten in this fix; the parent `007/spec.md` now flags it as a historical ledger whose remaining live `009` item is `009/001`, but a fuller backlog prune would require a separate scoped change.
