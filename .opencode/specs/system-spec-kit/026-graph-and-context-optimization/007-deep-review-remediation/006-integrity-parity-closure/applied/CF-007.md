# Applied: CF-007

## Source
- Severity: P1
- Flagging phases: 002, 010
- Save and index concurrency safeguards still relied on partial contracts: save locking was process-local, the scan bypass remained a soft boolean, and routed/scoped regressions were missing.

## Target Files Modified
| Path | Change Summary | Lines Touched |
|---|---|---|
| `.opencode/skill/system-spec-kit/mcp_server/handlers/save/spec-folder-mutex.ts` | Added filesystem-backed interprocess lock acquisition, stale-lock reaping, timeout handling, and retained in-process queueing. | 6-112 |
| `.opencode/skill/system-spec-kit/mcp_server/handlers/save/types.ts` | Added internal `IndexingOrigin` type so scan provenance is modeled as an internal contract rather than a public tool arg. | 250-252 |
| `.opencode/skill/system-spec-kit/mcp_server/handlers/memory-save.ts` | Normalized scan handling through `IndexingOrigin`, added `indexMemoryFileFromScan()`, kept legacy `fromScan` as compatibility input, and switched the transactional bypass check to origin-based logic. | 1977-2015, 2556-2624, 3291-3315 |
| `.opencode/skill/system-spec-kit/mcp_server/tests/handler-memory-save.vitest.ts` | Added export coverage for the scan wrapper and a regression proving the mutex retries through a filesystem-backed lock. | 25-27, 41-47, 98-153 |
| `.opencode/skill/system-spec-kit/mcp_server/tests/handler-memory-index.vitest.ts` | Added scoped scan/non-scan regressions and updated the harness scope normalizer so governed scope reaches the guard path. | 306-316, 783-914 |

## Evidence of Change
```diff
- const SPEC_FOLDER_LOCKS = new Map<string, Promise<unknown>>();
+ const SPEC_FOLDER_LOCKS = new Map<string, Promise<unknown>>();
+ const LOCK_ROOT = path.join(os.tmpdir(), 'spec-kit-memory-save-locks');
+ async function acquireInterprocessLock(specFolder: string): Promise<InterprocessLockHandle> { ... }
+ releaseInterprocessLock(interprocessLock);
```

```diff
- if (!fromScan && embedding) {
+ const indexingOrigin = resolveIndexingOrigin({ origin, fromScan });
+ if (indexingOrigin !== 'scan' && embedding) {
```

```diff
- async function indexMemoryFile(filePath: string, { ..., fromScan = false, ... } = {})
+ async function indexMemoryFile(filePath: string, { ..., ...originOptions } = {})
+ async function indexMemoryFileFromScan(filePath: string, options: BaseIndexMemoryFileOptions = {})
```

```diff
+ it('retries through a filesystem-backed lock when another process already holds the spec-folder lock', async () => { ... })
+ it('keeps tenant and session scoped scan-originated saves on the scan-only wrapper path', async () => { ... })
+ it('keeps the scoped transactional recheck active when scan origin is omitted', async () => { ... })
```

## Verification
- `npm exec -- vitest run tests/handler-memory-save.vitest.ts --testNamePattern="spec-folder mutex hardening"`: passed
- `npm exec -- vitest run tests/handler-memory-index.vitest.ts --testNamePattern="scan-originated save guard regressions"`: passed
- `npm run typecheck` in `.opencode/skill/system-spec-kit/mcp_server`: failed on pre-existing unrelated error `hooks/gemini/session-prime.ts(290)`
- `npm exec -- vitest run tests/handler-memory-save.vitest.ts tests/handler-memory-index.vitest.ts`: all CF-007-targeted assertions passed, but the suite still has an unrelated existing failure in `handler-memory-save.vitest.ts` (`fails open to natural Tier 2 routing when Tier 3 transport throws`, expected 1 fetch call, got 2)

## Deferred
- Full removal of the legacy `fromScan` boolean from production callers was not applied because `.opencode/skill/system-spec-kit/mcp_server/handlers/memory-index.ts` was outside the allowed target set for this packet and still calls `indexMemoryFile(..., { fromScan: true })`.
- No additional live acceptance rerun was recorded here; this packet was limited to code hardening and regression coverage in the listed target files.
