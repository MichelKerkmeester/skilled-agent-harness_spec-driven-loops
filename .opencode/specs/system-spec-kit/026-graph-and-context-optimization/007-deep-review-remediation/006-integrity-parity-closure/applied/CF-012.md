# Applied: CF-012

## Source
- Severity: P1
- Flagging phases: 003
- Finding summary: Code-graph refresh performance is still under-hardened because selective reindexing walks the full tree and the short readiness debounce is not keyed by workspace or options.

## Target Files Modified
| Path | Change Summary | Lines Touched |
|---|---|---|
| `.opencode/skill/system-spec-kit/mcp_server/code-graph/lib/ensure-ready.ts` | Keyed readiness debounce entries by resolved `rootDir` plus inline-scan options, and routed selective refresh through explicit stale-file indexing instead of `includeGlobs` overloading. | 10, 184-194, 256-380 |
| `.opencode/skill/system-spec-kit/mcp_server/code-graph/lib/structural-indexer.ts` | Added `specificFiles` support so targeted refresh can parse an explicit stale-file list without walking the whole workspace; factored shared post-processing into a helper. | 39, 1236-1347 |

## Evidence of Change
```diff
-let lastCheckAt = 0;
-let lastCheckResult: ReadyResult | null = null;
+const readinessDebounce = new Map<string, ReadyDebounceEntry>();
+
+function buildReadinessDebounceKey(rootDir: string, allowInlineIndex: boolean, allowInlineFullScan: boolean): string {
+  return JSON.stringify({ rootDir: resolve(rootDir), allowInlineIndex, allowInlineFullScan });
+}
```

```diff
-      // F048: Convert absolute stale file paths to rootDir-relative globs
-      config.includeGlobs = state.staleFiles.map(f => relative(rootDir, f));
-      await indexWithTimeout(config, AUTO_INDEX_TIMEOUT_MS);
+      await indexWithTimeout(config, AUTO_INDEX_TIMEOUT_MS, { specificFiles: state.staleFiles });
```

```diff
+export interface IndexFilesOptions {
+  skipFreshFiles?: boolean;
+  specificFiles?: string[];
+}
...
+if (options.specificFiles && options.specificFiles.length > 0) {
+  candidateFiles = collectSpecificFiles(config.rootDir, options.specificFiles, config.maxFileSizeBytes);
+  console.info(`[structural-indexer] refreshed ${candidateFiles.length} specific file(s)`);
+} else {
+  // existing glob-based workspace scan
+}
```

## Verification
- TypeScript compile check passed: `./.opencode/skill/system-spec-kit/node_modules/.bin/tsc -p .opencode/skill/system-spec-kit/mcp_server/tsconfig.json --noEmit`
- Targeted regression test passed: `./mcp_server/node_modules/.bin/vitest run mcp_server/tests/ensure-ready.vitest.ts --config mcp_server/vitest.config.ts`
- Strict spec validation ran and failed at the packet level: `bash .opencode/skill/system-spec-kit/scripts/spec/validate.sh .opencode/specs/system-spec-kit/026-graph-and-context-optimization/007-deep-review-remediation/006-integrity-parity-closure --strict` reported missing required Level 3 file `decision-record.md`

## Deferred
- None.
