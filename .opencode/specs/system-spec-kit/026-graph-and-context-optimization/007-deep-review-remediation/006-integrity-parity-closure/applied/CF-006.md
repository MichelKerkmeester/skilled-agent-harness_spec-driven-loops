# Applied: CF-006

## Source
- Severity: P1
- Flagging phases: 002
- Finding summary: The resume ladder contract had drifted across code and packet docs on precedence, archive fallback, and parent-handover traversal.

## Target Files Modified
| Path | Change Summary | Lines Touched |
|---|---|---|
| `.opencode/skill/system-spec-kit/mcp_server/lib/resume/resume-ladder.ts` | Updated the contract comment to describe freshness arbitration between folder-local handover and continuity, and added an explicit hint when both signals are compared. | `600-606`, `680-688` |
| `.opencode/skill/system-spec-kit/mcp_server/handlers/memory-context.ts` | Exposed the shipped resume selection policy in the resume payload and rewrote the public hint text to match folder-local traversal plus no archived tier. | `902-919` |
| `.opencode/specs/system-spec-kit/026-graph-and-context-optimization/002-continuity-memory-runtime/003-continuity-refactor-gates/004-gate-d-reader-ready/spec.md` | Rewrote Gate D resume-contract prose to match shipped behavior: folder-local handover and continuity are compared first, spec docs are the only fallback, and archived resume fallback is disabled. | `27-29`, `58-61`, `70-85`, `101-103`, `124`, `137`, `144-150`, `169`, `173`, `180`, `185`, `192`, `203`, `213-224` |
| `.opencode/specs/system-spec-kit/026-graph-and-context-optimization/002-continuity-memory-runtime/003-continuity-refactor-gates/005-gate-e-runtime-migration/spec.md` | Removed the parent-handover claim from Gate E and aligned the packet to folder-local handover traversal plus freshness arbitration before spec-doc fallback. | `27-29`, `79-80`, `111`, `162`, `191`, `200` |

## Evidence of Change
`.opencode/skill/system-spec-kit/mcp_server/lib/resume/resume-ladder.ts`

```diff
- * Preference order is `handover.md` first, then `_memory.continuity` inside
+ * The helper reads packet-local `handover.md` and `_memory.continuity` inside
+ * `implementation-summary.md`, promotes whichever of those two sources is
+ * fresher when both exist, then falls back to canonical spec documents
...
+    hints.push(`Compared folder-local handover.md and _memory.continuity; selected ${primary.source} as the fresher resume source.`);
```

`.opencode/skill/system-spec-kit/mcp_server/handlers/memory-context.ts`

```diff
+        selectionPolicy: 'freshest_handover_or_continuity_then_spec_docs',
+        handoverTraversal: 'resolved-folder-only',
+        freshnessWinner: ladder.freshnessWinner === 'spec-docs' ? 'spec_docs' : ladder.freshnessWinner,
-      'Resume mode reads canonical docs directly: handover.md -> _memory.continuity -> spec docs.',
+      'Resume mode reads folder-local canonical docs directly: handover.md and _memory.continuity are compared first, then spec docs.',
```

`.opencode/specs/.../004-gate-d-reader-ready/spec.md`

```diff
- **Key Decisions**: keep retrieval topology intact while changing source semantics; treat archived rows as measured fallback only during D0.
+ **Key Decisions**: keep retrieval topology intact while changing source semantics; compare the folder-local handover document and `_memory.continuity`, promote the fresher of the two, then fall through to canonical spec docs with no archived resume tier.
...
- **Given** spec docs are stale or unavailable, **when** resume runs and archived fallback is allowed, **then** archived rows appear as last-resort support and the fast-path miss is recorded.
+ **Given** handover and continuity are both unavailable, **when** resume runs, **then** the ladder falls through to direct spec-doc reads; if canonical docs are also missing, it reports that no recovery context was found.
```

`.opencode/specs/.../005-gate-e-runtime-migration/spec.md`

```diff
- The operator-facing recovery surface is `/spec_kit:resume`. Canonical continuity is sourced from packet artifacts in this order: `../handover.md`, `_memory.continuity`, then the canonical spec docs.
+ The operator-facing recovery surface is `/spec_kit:resume`. Canonical continuity is sourced from folder-local packet artifacts: the handover document and `_memory.continuity` are read directly, the fresher of the two wins, then the canonical spec docs.
...
- 2. **Given** `/spec_kit:resume` is the operator recovery surface, **when** continuity guidance is read, **then** it points to `../handover.md`, `_memory.continuity`, and canonical spec docs in that order.
+ 2. **Given** `/spec_kit:resume` is the operator recovery surface, **when** continuity guidance is read, **then** it points to the folder-local handover document and `_memory.continuity`, promotes the fresher of the two, and falls back to canonical spec docs.
```

## Verification
- Syntax check: `npx tsc --noEmit --composite false -p tsconfig.json` in `.opencode/skill/system-spec-kit/mcp_server` exited `0`.
- Behavioral regression: `node node_modules/vitest/vitest.mjs run tests/resume-ladder.vitest.ts --config vitest.config.ts -t "uses handover as the happy-path source|falls back to continuity when handover is missing|falls through to spec docs when continuity is malformed|merges mixed handover and continuity state while preferring the freshest signal"` exited `0` with `4 passed`.
- Packet validation: `bash .opencode/skill/system-spec-kit/scripts/spec/validate.sh .opencode/specs/system-spec-kit/026-graph-and-context-optimization/002-continuity-memory-runtime/003-continuity-refactor-gates/004-gate-d-reader-ready --strict` exited `2` because strict mode treats the pre-existing `CONTINUITY_FRESHNESS` warning as a failure; the validator reported `0` errors after the contract wording fix.
- Packet validation: `bash .opencode/skill/system-spec-kit/scripts/spec/validate.sh .opencode/specs/system-spec-kit/026-graph-and-context-optimization/002-continuity-memory-runtime/003-continuity-refactor-gates/005-gate-e-runtime-migration --strict` exited `2` for the same pre-existing `CONTINUITY_FRESHNESS` warning; the validator reported `0` errors after the parent-handover wording was removed.
- Packet validation: `bash .opencode/skill/system-spec-kit/scripts/spec/validate.sh .opencode/specs/system-spec-kit/026-graph-and-context-optimization/007-deep-review-remediation/006-integrity-parity-closure --strict` exited `2` because the remediation packet is already missing `decision-record.md`; that packet-structure issue was outside the allowed edit scope for CF-006.

## Deferred
- Test file edits were not applied because the allowed scope for this fix was limited to the four listed target files plus this applied report; the existing resume-ladder regression already matches the shipped freshness-winner behavior.
