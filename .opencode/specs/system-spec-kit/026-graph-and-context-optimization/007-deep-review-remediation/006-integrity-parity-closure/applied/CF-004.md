# Applied: CF-004

## Source
- Severity: P1
- Flagging phases: 001, 007, 009
- Cross-runtime hook parity remains incomplete because Copilot wrapper/schema reapply work is still reverted and the remediation backlog has drifted behind current 009 truth.

## Target Files Modified
| Path | Change Summary | Lines Touched |
|---|---|---|
| `.claude/settings.local.json` | Reapplied the top-level Copilot wrapper fields on all four matcher objects and restored the live Copilot writer commands on `UserPromptSubmit` and `SessionStart`. | 25-69 |
| `.opencode/specs/system-spec-kit/026-graph-and-context-optimization/007-deep-review-remediation/spec.md` | Updated the root backlog language so `007` now points at the current `009` parity map instead of the stale cleanup framing. | 63-67, 75-90, 99-126, 149-165 |
| `.opencode/specs/system-spec-kit/026-graph-and-context-optimization/009-hook-daemon-parity/010-copilot-wrapper-schema-fix/implementation-summary.md` | Converted packet 010 from reverted/reapply-needed wording to restored live-state wording with current verification notes. | 2-63 |
| `.opencode/specs/system-spec-kit/026-graph-and-context-optimization/009-hook-daemon-parity/011-copilot-writer-wiring/implementation-summary.md` | Converted packet 011 from reverted/reapply-needed wording to restored live-state wording with current verification notes. | 2-102 |

## Evidence of Change
Before:
```diff
-        "hooks": [
```

After:
```diff
+        "type": "command",
+        "bash": "cd \"$(git rev-parse --show-toplevel 2>/dev/null || pwd)\" && node .opencode/skill/system-spec-kit/mcp_server/dist/hooks/copilot/user-prompt-submit.js",
+        "timeoutSec": 5,
         "hooks": [
```

Before:
```diff
-| 1 | `001-deep-review-and-remediation/` | In Progress | Remediation Plan: 120-Iteration Deep Review Findings |
```

After:
```diff
+| 1 | `001-deep-review-and-remediation/` | In Progress | Remediation Plan: 120-Iteration Deep Review Findings, now keyed to the active `009` parity map: `009/001` remains live, `009/003` is complete, `009/006` stays in progress, and `009/010` plus `009/011` are restored in config. |
```

Before:
```diff
-    status: "reverted-needs-reapply"
```

After:
```diff
+    status: "implemented"
```

## Verification
- `python3 -m json.tool .claude/settings.local.json` passes.
- `jq '.hooks.UserPromptSubmit[0] | {type, bash, timeoutSec}' .claude/settings.local.json` shows the restored top-level Copilot writer wrapper.
- `jq '.hooks.SessionStart[0] | {type, bash, timeoutSec}' .claude/settings.local.json` shows the restored top-level Copilot writer wrapper.
- `bash .opencode/skill/system-spec-kit/scripts/spec/validate.sh '.opencode/specs/system-spec-kit/026-graph-and-context-optimization/007-deep-review-remediation/006-integrity-parity-closure' --strict` fails with `Missing 1 required file(s) - decision-record.md` and `Required file missing for Level 3: decision-record.md`.
- Markdown anchors and frontmatter blocks in the edited spec documents were preserved; only finding-specific sections were updated.

## Deferred
- Cross-runtime smoke reruns are still deferred. CF-004 scope required reapplying the config and restoring backlog parity wording; the live smoke matrix belongs to follow-on packet verification.
- Packet-local strict validation remains red because the approved Level 3 packet is missing `decision-record.md`, and the task rules limited edits to the four target files plus this applied report.
