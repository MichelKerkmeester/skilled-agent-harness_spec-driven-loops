# Applied: CF-017

## Source
- Severity: P0
- Flagging phases: 005
- Finding summary: The release-cleanup manual-testing contract is internally inconsistent because the playbook forbids UNAUTOMATABLE while the runner and packet evidence depend on it as a core result class.

## Target Files Modified
| Path | Change Summary | Lines Touched |
|---|---|---|
| `.opencode/skill/system-spec-kit/manual_testing_playbook/manual_testing_playbook.md` | Replaced the contradictory execution-policy sentence so `UNAUTOMATABLE` is a valid documented scenario status and `PARTIAL` is scoped to packet summaries. | 9 |
| `.opencode/skill/system-spec-kit/scripts/tests/manual-playbook-runner.ts` | Added a status-contract comment clarifying that runner output includes `UNAUTOMATABLE` and that packet-level `PARTIAL` remains outside the runner enum. | 8-11 |
| `.opencode/specs/system-spec-kit/026-graph-and-context-optimization/005-release-cleanup-playbooks/003-playbook-and-remediation/002-full-playbook-execution/tasks.md` | Added a verification note above the results matrix defining canonical scenario statuses versus the packet-summary `PARTIAL` overlay. | 92-94 |

## Evidence of Change
### `.opencode/skill/system-spec-kit/manual_testing_playbook/manual_testing_playbook.md`
Before:
```md
> **EXECUTION POLICY**: ... The only acceptable classifications are PASS, FAIL, or SKIP ... "UNAUTOMATABLE" is not a valid status.
```
After:
```md
> **EXECUTION POLICY**: ... Valid scenario classifications are `PASS`, `FAIL`, `SKIP` ... or `UNAUTOMATABLE` ... Packet-level summaries may additionally use `PARTIAL` ...
```

### `.opencode/skill/system-spec-kit/scripts/tests/manual-playbook-runner.ts`
Before:
```ts
type ScenarioStatus = 'PASS' | 'FAIL' | 'SKIP' | 'UNAUTOMATABLE';
```
After:
```ts
// Runner output stays aligned with the playbook contract...
type ScenarioStatus = 'PASS' | 'FAIL' | 'SKIP' | 'UNAUTOMATABLE';
```

### `.opencode/specs/system-spec-kit/026-graph-and-context-optimization/005-release-cleanup-playbooks/003-playbook-and-remediation/002-full-playbook-execution/tasks.md`
Before:
```md
## Phase 3: Verification

| Category | Total | PASS | PARTIAL | FAIL | SKIP | UNAUTOMATABLE |
```
After:
```md
## Phase 3: Verification

Result-class contract for this packet: `PASS`, `FAIL`, `SKIP`, and `UNAUTOMATABLE` ...
| Category | Total | PASS | PARTIAL | FAIL | SKIP | UNAUTOMATABLE |
```

## Verification
- Syntax check (for code changes): no local `tsc` binary is available in this workspace (`command -v tsc` returned no result, and there is no root `node_modules/.bin/tsc`), so no compiler-backed parse check could be run. The code edit is comment-only and does not change executable TypeScript syntax.
- Markdown/schema validity (for docs): confirmed the modified Markdown files keep existing frontmatter and anchors intact; no frontmatter structure was changed.
- Spec validation: `bash .opencode/skill/system-spec-kit/scripts/spec/validate.sh .opencode/specs/system-spec-kit/026-graph-and-context-optimization/005-release-cleanup-playbooks/003-playbook-and-remediation/002-full-playbook-execution --strict` still returns `RESULT: FAILED (strict)` because `CONTINUITY_FRESHNESS` was already stale (`2026-04-12T00:00:00Z` vs graph metadata `2026-04-21T13:00:00Z`); this fix did not modify continuity metadata.
- Applied-folder validation: `bash .opencode/skill/system-spec-kit/scripts/spec/validate.sh .opencode/specs/system-spec-kit/026-graph-and-context-optimization/007-deep-review-remediation/006-integrity-parity-closure --strict` returns `RESULT: FAILED` because the folder is not a standalone spec packet and intentionally lacks `spec.md`, `plan.md`, and `tasks.md`.
- No unrelated content deleted: only the targeted contract lines plus this applied report were added or updated.

## Deferred
- The runner still defaults to the retired `006-canonical-continuity-refactor` report root, but that is CF-018/P1 scope and was intentionally left untouched here.
