# Applied: CF-020

## Source
- Severity: P1
- Flagging phases: 006
- The live search-routing pipeline had contract drift: continuity fusion intent was only inferred through internal overrides, the direct hybrid path bypassed adaptive-fusion behavior, graph calibration only bounded part of the attributed graph delta, and 2-3 result rerank skips left bad Stage 2 orderings uncorrected.

## Target Files Modified
| Path | Change Summary | Lines Touched |
|---|---|---|
| `.opencode/skill/system-spec-kit/mcp_server/lib/search/intent-classifier.ts` | Added a public continuity-query detector without widening the seven-intent classifier enum. | `652-668`, `696` |
| `.opencode/skill/system-spec-kit/mcp_server/lib/search/search-utils.ts` | Centralized fusion-intent resolution so cache keys and callers share the same continuity-aware contract. | `11`, `60-78`, `163-187`, `240` |
| `.opencode/skill/system-spec-kit/mcp_server/lib/search/hybrid-search.ts` | Resolved fusion intent through the shared helper, inferred dominant document type, and routed adaptive fusion through the shared algorithm surface. | `11`, `48`, `746-770`, `1250-1310` |
| `.opencode/skill/system-spec-kit/shared/algorithms/adaptive-fusion.ts` | Extended adaptive fusion to accept graph and passthrough lists so the live hybrid path can use the same weighted fusion behavior. | `58-61`, `208-255` |
| `.opencode/skill/system-spec-kit/mcp_server/lib/search/graph-calibration.ts` | Added per-signal graph calibration helpers that cap total graph contribution while preserving attributed sub-signals. | `126-131`, `359-385`, `454-469` |
| `.opencode/skill/system-spec-kit/mcp_server/lib/search/pipeline/stage2-fusion.ts` | Replaced surrogate graph calibration with per-signal calibration and switched recency fusion to the shared adaptive intent contract. | `78-94`, `353-397`, `1008-1023` |
| `.opencode/skill/system-spec-kit/mcp_server/lib/search/pipeline/stage3-rerank.ts` | Lowered the rerank floor so only 0-1 result sets skip reranking. | `49-50` |

## Evidence of Change
```diff
- const intent = options.intent || classifyIntent(query).intent;
+ const intent = resolveFusionIntentContract({
+   detectedIntent,
+   adaptiveFusionIntent: options.intent ?? null,
+   query,
+ }) ?? detectedIntent;
```

```diff
- const fused = fuseResultsMulti(fusionLists);
+ const fused = adaptiveEnabled
+   ? adaptiveFuse(..., {
+       graphResults: ...,
+       additionalLists: passthroughLists,
+     })
+   : fuseResultsMulti(fusionLists);
```

```diff
- const calibrated = applyCalibrationProfile({
-   graphWeightBoost: originalGraphSignalDelta,
-   n2aScore: originalGraphSignalDelta,
-   n2bScore: originalGraphSignalDelta,
-   communityBoost: originalCommunityDelta,
- });
+ const calibrated = applyGraphContributionCalibration({
+   causalDelta,
+   coActivationDelta,
+   graphSignalDelta,
+   communityDelta,
+ });
```

```diff
- const MIN_RESULTS_FOR_RERANK = 4;
+ const MIN_RESULTS_FOR_RERANK = 2;
```

## Verification
- `npm exec --prefix .opencode/skill/system-spec-kit/shared -- tsc --noEmit --composite false -p .opencode/skill/system-spec-kit/shared/tsconfig.json`
- `npm exec --prefix .opencode/skill/system-spec-kit/mcp_server -- tsc --noEmit --composite false -p .opencode/skill/system-spec-kit/mcp_server/tsconfig.json`
- `npm exec --prefix mcp_server -- vitest run --config mcp_server/vitest.config.ts mcp_server/tests/adaptive-fusion.vitest.ts mcp_server/tests/intent-classifier.vitest.ts mcp_server/tests/stage2-fusion.vitest.ts`
- Stage 3 stale expectation noted: `mcp_server/tests/stage3-rerank-regression.vitest.ts` still asserts the retired 4-result skip rule, so it now fails until the packet updates that regression to the new parity contract.
- `bash .opencode/skill/system-spec-kit/scripts/spec/validate.sh .opencode/specs/system-spec-kit/026-graph-and-context-optimization/007-deep-review-remediation/006-integrity-parity-closure --strict`
  - Result: `FAILED`
  - Errors: missing `decision-record.md` and corresponding Level 3 mismatch in the packet
  - Warning: Level 3 section-count warning tracks the same missing decision-record surface

## Deferred
- No code-path deferrals inside CF-020 scope.
- Test updates for `mcp_server/tests/stage3-rerank-regression.vitest.ts` were not applied because the task scope allowed edits only in the listed target files plus this applied report.
- Packet-level spec remediation for the missing Level 3 `decision-record.md` was not applied because the task scope disallowed edits outside the listed target files and `applied/`.
