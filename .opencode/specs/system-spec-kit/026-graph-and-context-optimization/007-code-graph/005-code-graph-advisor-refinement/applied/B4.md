# Fix-up Batch B4

## Scope

Closed findings:

- R3-P1-002: PR-7 settings parity test coverage gap.
- R4-P1-002: Hook command path resolution security.
- R3-P2-001: Hook registration documentation freshness.

## Applied Changes

### R3-P1-002

Updated `.opencode/skill/system-spec-kit/mcp_server/skill-advisor/tests/hooks/settings-driven-invocation-parity.vitest.ts` so the `.claude/settings.local.json` shape assertions run in every runtime, not only when Claude-specific environment variables are present.

Added command anchoring assertions that require each Claude hook command to:

- point at the expected Claude adapter script;
- avoid `hooks/copilot/`, `hooks/codex/`, and `hooks/gemini/`;
- anchor execution to the canonical repo root;
- use a pinned Node binary instead of ambient `node`;
- avoid `git rev-parse` and `|| pwd` cwd fallback behavior.

### R4-P1-002

Updated `.claude/settings.local.json` hook commands to use the canonical project root:

`/Users/michelkerkmeester/MEGA/Development/Code_Environment/Public`

The commands now invoke `/opt/homebrew/bin/node` directly and no longer resolve the project root from ambient cwd or nested Git repositories.

### R3-P2-001

Updated `.opencode/skill/system-spec-kit/mcp_server/hooks/copilot/README.md` to remove the stale mixed `.claude/settings.local.json` top-level wrapper example.

The Copilot registration guidance now states that `.claude/settings.local.json` remains Claude-only, and Copilot should run the compiled Copilot writer scripts through a Copilot-supported command surface.

## Verification

- `cd .opencode/skill/system-spec-kit/mcp_server && npx vitest run skill-advisor/tests/hooks/settings-driven-invocation-parity.vitest.ts`
  - Passed: 1 file, 40 tests.
- `cd .opencode/skill/system-spec-kit/mcp_server && npx tsc --noEmit`
  - Passed.
- `bash .opencode/skill/system-spec-kit/scripts/spec/validate.sh .opencode/specs/system-spec-kit/026-graph-and-context-optimization/007-code-graph/005-code-graph-advisor-refinement --strict`
  - Failed strict validation with 0 errors and 3 warnings: Level 3 phase-count complexity warning, template header deviations, and continuity freshness lag.
