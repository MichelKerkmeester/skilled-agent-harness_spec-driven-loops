# Iteration 1: Concept-Layer Mapping (Q8)

## Focus

Confirm or refine the six-layer concept model for the memory→spec-doc-continuity rename. Anchor the next 9 iterations on a concept-layer table that pairs each layer with its current `memory_*` identifiers, the Anthropic-clash risk for that layer, and 2–3 candidate replacement axes. The strategy.md §11 Next Focus mandates two angles: (1) layer enumeration verified against primary sources (MCP tool/handler/SQL surface), and (2) naming-axis survey with Anthropic Claude Memory collision triage.

## Findings

1. **Layer 1 (SQLite + embeddings index) has 8 distinct `memory_*` tables, matching the strategy §12 informal count.** Verified table set: `memory_index`, `memory_conflicts`, `memory_corrections`, `memory_fts`, `memory_summaries`, `memory_entities`, `memory_lineage`, `vec_memories`. The constants `MEMORY_LINEAGE_TABLE` and `LEGACY_MEMORY_LINEAGE_TABLE` in the schema confirm a prior rename precedent (the legacy `hydra_memory_lineage` → `memory_lineage` migration). The migration history (V8→V9 added `memory_corrections`, V12 unified `memory_conflicts`, V20 added `memory_summaries` + `memory_entities`) shows the table set has grown over time, so any rename framework must accommodate further growth without re-prefixing each table individually. [SOURCE: .opencode/skill/system-spec-kit/mcp_server/lib/search/vector-index-schema.ts:47-49, 246-261, 756, 1010, 1030, 1392, 2426, 2445] [SOURCE: .opencode/skill/system-spec-kit/mcp_server/lib/storage/schema-downgrade.ts:205]

2. **Layer 1+5 (storage + retrieval pipeline) exposes 21 public `memory_*` MCP tools, not 9.** The spec.md §3 and strategy.md §12 both cite 9, but `tool-schemas.ts` enumerates 21: `memory_context`, `memory_search`, `memory_quick_search`, `memory_match_triggers`, `memory_save`, `memory_list`, `memory_stats`, `memory_health`, `memory_delete`, `memory_update`, `memory_validate`, `memory_bulk_delete`, `memory_drift_why`, `memory_causal_link`, `memory_causal_stats`, `memory_causal_unlink`, `memory_index_scan`, `memory_get_learning_history`, `memory_ingest_start`, `memory_ingest_status`, `memory_ingest_cancel`. This 2.3× undercount means the Q3 alias-window decision must price 21 deprecation aliases, not 9, and the REQ-008 advisor regression bundle has more trigger phrases to keep balanced than the spec assumes. The discrepancy is an actionable spec-error finding, not a research artifact. [SOURCE: .opencode/skill/system-spec-kit/mcp_server/tool-schemas.ts:47, 54, 196, 214, 221, 228, 234, 240, 280, 295, 301, 323, 416, 423, 429, 435, 494, 500, 506, 529, 542]

3. **Layer 4 (session resume / recovery surfaces) has a single canonical phrasing already in production: `handover.md → _memory.continuity → spec docs`.** The same string appears verbatim in 5 separate template/README files (`templates/README.md:82`, `templates/core/README.md:31`, `templates/level_3/README.md:120`, `templates/handover.md:127`, `templates/changelog/README.md:27`). REQ-004 mandates a single canonical recovery-ladder phrasing — this finding establishes that the canonical phrasing already exists; the rename's job is to preserve the ladder structure while replacing the middle token (`_memory.continuity`). Renaming the chain from three components to three new components (or two, if the YAML key collapses with the surrounding doc) must keep the ladder verbally compact, because every doc currently quotes it inline. [SOURCE: .opencode/skill/system-spec-kit/templates/README.md:82] [SOURCE: .opencode/skill/system-spec-kit/templates/core/README.md:31] [SOURCE: .opencode/skill/system-spec-kit/templates/level_3/README.md:120] [SOURCE: .opencode/skill/system-spec-kit/templates/handover.md:127] [SOURCE: .opencode/skill/system-spec-kit/templates/changelog/README.md:27]

4. **Layer 3 (generated metadata + frontmatter) uses `_memory:` as a top-level YAML key with `continuity` as its only documented subkey, in 9 template files.** Files: `templates/level_3/spec.md`, `templates/level_3/plan.md`, `templates/level_3/tasks.md`, `templates/level_3/checklist.md`, `templates/level_3/decision-record.md`, `templates/level_3/implementation-summary.md`, `templates/level_3/README.md`. The Q4 question (rename in-frontmatter vs. parser-fallback alias) reduces to: do we change a single top-level YAML key in 9 templates plus every existing implementation-summary doc in `.opencode/specs/`, or do we accept a parser-fallback that reads both `_memory:` and a new key (e.g. `_continuity:`) and warns on the legacy path. The narrow scope of the schema (one top-level key, one nested subkey) makes a parser-fallback far cheaper than the spec implies and effectively risk-free; aggressive in-frontmatter rename across the spec corpus is unnecessary. [SOURCE: .opencode/skill/system-spec-kit/templates/level_3/spec.md:12] [SOURCE: .opencode/skill/system-spec-kit/templates/level_3/plan.md:12] [SOURCE: .opencode/skill/system-spec-kit/templates/level_3/tasks.md:11] [SOURCE: .opencode/skill/system-spec-kit/templates/level_3/checklist.md:11] [SOURCE: .opencode/skill/system-spec-kit/templates/level_3/decision-record.md:12] [SOURCE: .opencode/skill/system-spec-kit/templates/level_3/implementation-summary.md:11] [SOURCE: .opencode/skill/system-spec-kit/templates/level_3/README.md:10]

5. **Anthropic's Claude Memory tool primary terminology is narrow and well-bounded.** Verbatim noun phrases used by Anthropic: "memory tool", "memory file directory", "memory directory", "memory operations", "memory commands" (the six are `view`, `create`, `str_replace`, `insert`, `delete`, `rename`), "memory artifacts", "memory store", "memory backend", "/memories" (the canonical container path), "memory_20250818" (the API beta tool type identifier), and "memory tool helpers". The conceptual layers are: (1) the LLM tool surface (`tools: [{type: "memory_20250818", name: "memory"}]`), (2) the client-side handler implementation (`BetaAbstractMemoryTool` / `betaMemoryTool`), and (3) the on-disk container (`/memories`). Anthropic does NOT use the terms `continuity`, `spec doc`, `packet`, `handover`, `recovery ladder`, `causal graph`, or `lineage` — none of our internal vocabulary collides downward. The collision surface is bidirectional and asymmetric: any of our identifiers that contain the bare token `memory` (especially `memory_save`, `memory_search`, `memory_context`, `memory_match_triggers`, `/memory:save`, `/memory:search`, `_memory.continuity`) will read in our READMEs as if they refer to Anthropic's tool. The reverse is not true — Anthropic's `memory_20250818` is dated and clearly Anthropic-branded. This means the rename's collision-mitigation work falls entirely on the local side; we do NOT need a defensive Anthropic-side disambiguation strategy beyond a one-line positioning callout. [SOURCE: https://platform.claude.com/docs/en/docs/agents-and-tools/tool-use/memory-tool]

6. **Concept-layer table (the iteration deliverable).** With primary-source-verified identifiers per layer:

| Layer | One-line definition | Current `memory_*` identifiers | Anthropic clash | Candidate replacement axes |
|---|---|---|---|---|
| **L1: Storage substrate** | SQLite + sqlite-vec index, FTS5, BM25, causal graph, FSRS state | 8 tables (`memory_index`, `memory_conflicts`, `memory_corrections`, `memory_fts`, `memory_summaries`, `memory_entities`, `memory_lineage`, `vec_memories`) + storage handlers in `mcp_server/lib/storage/` and `mcp_server/lib/search/` | Low (table names not user-facing) | `continuity_*`, `spec_doc_*`, `packet_*`. Recommend `continuity_*` (matches Layer 3+4) OR keep `memory_*` for tables since they're internal (Q3 stop-at-API-surface answer) |
| **L2: Canonical spec-doc markdown** | `spec.md`, `plan.md`, `tasks.md`, `implementation-summary.md`, `decision-record.md`, `handover.md` at packet root | None directly named `memory_*` (these are markdown files, not identifiers). The container term is `spec folder` / `packet`. | None | Already named correctly; no rename needed. Glossary should explicitly note: spec docs are NOT memory |
| **L3: Generated metadata** | `description.json`, `graph-metadata.json`, `_memory.continuity` YAML frontmatter | `_memory.continuity` YAML key in 9 templates + every `implementation-summary.md` ever written | Medium (collides with Anthropic's "memory file directory" mental model) | `_continuity:`, `_spec_doc:`, `_handover:`. Recommend `_continuity:` with parser-fallback alias from `_memory:` |
| **L4: Session resume / recovery surfaces** | `/spec_kit:resume`, `session_bootstrap`, `session_health`, the `handover.md → _memory.continuity → spec docs` ladder | `session_bootstrap`, `session_resume`, `session_health` (already non-memory-prefixed) | Low (already clean) | Already named correctly. Only the ladder middle-token (`_memory.continuity`) needs to track the L3 rename |
| **L5: Hybrid retrieval pipeline** | 5-channel fusion (vector + BM25 + FTS + causal graph + degree) + CocoIndex bridge + 21 public `memory_*` tools | 21 MCP tools (all `memory_*`-prefixed), `mcp_server/lib/search/`, `mcp_server/lib/scoring/` | High (this is the user-facing surface; every README quoting `memory_search` or `/memory:search` reads like Anthropic's tool) | `continuity_*`, `recall_*`, `spec_doc_*`, `lookup_*`. Recommend `continuity_*` for retrieval/save tools; `recall_*` rejected as too cognitive-science |
| **L6: Constitutional / always-surface rules** | `/memory:learn` slash command, never-decaying tier, 3.0× search boost | `/memory:learn` (1 command) + storage in `memory_index` with `importance_tier='constitutional'` | Medium (the word "memory" in `/memory:learn` reads colloquially as Anthropic's memory) | `/continuity:learn`, `/rule:learn`, `/principle:learn`. Recommend `/continuity:learn` for namespace consistency; the underlying tier name (`constitutional`) stays unchanged |

7. **Naming-axis survey: 4 candidate top-level vocabularies, 1 clear winner.** Candidate axes considered: (a) `continuity_*` — already has internal traction (the entire `handover.md → _memory.continuity → spec docs` ladder), aligns with the system's actual function (preserving spec-doc continuity across sessions), and is a single-word rename with no Anthropic collision; (b) `spec_doc_*` — accurate but verbose, makes tool names like `spec_doc_search` clunky compared to `continuity_search`; (c) `packet_*` — too abstract, "packet" is a folder concept, not an information-flow concept; (d) per-layer naming (e.g. `continuity_*` for L5, `spec_doc_*` for L2 markdown ops, `metadata_*` for L3 generated files) — increases mental load, contradicts REQ-001's "no two layers collapse but ALL layers share a coherent vocabulary" reading. The winner is **single top-level `continuity` with per-layer suffixes only where the layer adds a distinct verb**: `continuity_save` (L1+L3+L5 write), `continuity_search` (L5 read), `continuity_resume` aliased to existing `session_resume` (L4), `_continuity:` frontmatter key (L3), `/continuity:save` slash command (L5 user surface), with the L2 spec-doc markdown layer simply called "spec docs" in prose. This collapses to a Q1 answer: single top-level concept name. [INFERENCE: based on findings 3, 4, 5, 6 above + spec.md §11 US-001 acceptance criteria]

## Ruled Out

- **Renaming the SQL table set** (Layer 1) was deprioritized for iteration 1 because finding 1 + finding 6 together show the tables are internal-only. They never appear in `tool-schemas.ts` user-facing identifiers, are mentioned only in `mcp_server/lib/` storage implementation, and have a precedent (V11 `hydra_memory_lineage` → `memory_lineage`) that shows table renames are possible but high-blast-radius. Iteration 1 deliberately defers the Q3 SQL-table decision to iteration 2 with a recommendation: "stop at public-API surface" is the parsimony-favored path.
- **Per-layer naming with one new prefix per layer** (Q1's per-layer arm) — finding 7 explains this would inflate the mental load and contradict the practical reality that L2 spec-doc markdown doesn't need a code-prefix at all. Documented as a non-preferred Q1 arm; iteration 2 should sanity-check by enumerating the precise tool list each axis would generate.

## Dead Ends

None definitive yet — no approach has been *proven* fundamentally unviable in this iteration. Q1 per-layer naming is non-preferred but not eliminated; iteration 2 (or human reviewer) should confirm before adding it to "Exhausted Approaches."

## Sources Consulted

- `.opencode/skill/system-spec-kit/mcp_server/tool-schemas.ts:47-542` — 21 `memory_*` MCP tool names
- `.opencode/skill/system-spec-kit/mcp_server/lib/search/vector-index-schema.ts:47-49, 246, 756, 1010, 1030, 1392, 2426, 2445` — 8 SQL tables and migration history
- `.opencode/skill/system-spec-kit/mcp_server/lib/storage/schema-downgrade.ts:205` — `memory_fts` cross-reference
- `https://platform.claude.com/docs/en/docs/agents-and-tools/tool-use/memory-tool` — Anthropic Claude Memory tool primary documentation (after 1×404 + 2×redirect chain from `docs.anthropic.com`)
- `.opencode/skill/system-spec-kit/templates/level_3/{spec,plan,tasks,checklist,decision-record,implementation-summary,README}.md` — 9 files using `_memory:` frontmatter
- `.opencode/skill/system-spec-kit/templates/{handover.md, README.md, core/README.md, level_3/README.md, changelog/README.md}` — 5 files quoting the canonical `handover.md → _memory.continuity → spec docs` ladder
- `.opencode/skill/system-spec-kit/templates/debug-delegation.md:127` — secondary continuity-ladder reference
- spec.md §1-12 — primary spec source (read for context, not as evidence of itself)

## Assessment

- **New information ratio**: 0.85
- **Questions addressed**: Q1 (per-layer vs. single top-level), Q3 (SQL-table rename scope), Q4 (`_memory.continuity` migration path), Q8 (concept-layer mapping)
- **Questions answered**: Q8 fully answered (concept-layer table delivered with primary-source citations). Q1 has a strong recommended answer (single `continuity_*` top-level) backed by finding 7. Q3 has a strong recommended answer (stop at public-API surface) backed by finding 1. Q4 has a strong recommended answer (parser-fallback alias from `_memory:`, no in-frontmatter rewrite) backed by finding 4.

Justification: 7 findings, all backed by primary sources. Findings 1, 2, 5, 6, 7 are fully new (not in spec.md or strategy.md). Findings 3 and 4 are partially new (the canonical ladder phrasing existed in strategy.md as a concept; this iteration enumerated the 5+9 file-level instances). Calculation: (5 fully-new + 0.5×2 partially-new) / 7 = 0.857.

## Reflection

- **What worked and why**: Hitting `tool-schemas.ts` and the SQL schema directly with grep produced primary-source identifier counts in two tool calls, and discovered the spec.md "9 tools" count is a 2.3× undercount of the actual 21. Reading 5 templates simultaneously via grep confirmed the canonical recovery ladder is already standardized — that's a finding the spec did not flag and changes Q4's risk profile. The Anthropic docs WebFetch worked despite a 404 + 2 redirects; persistence on the canonical platform.claude.com URL paid off.

- **What did not work and why**: The first two Anthropic URLs (`docs.claude.com/en/docs/build-with-claude/memory` and `www.anthropic.com/news/memory-and-context-management-for-claude`) both 404'd. Lesson: Anthropic restructured docs.claude.com → platform.claude.com mid-cycle; future iterations should hit `platform.claude.com/docs/en/docs/agents-and-tools/tool-use/memory-tool` directly. I burned 2 of my 3 retries on the chain.

- **What I would do differently**: Cap WebFetch retries at 1, fail-over to a different angle (e.g. `docs.anthropic.com/en/docs/agents-and-tools/tool-use/tool-reference` or the Anthropic engineering blog on long-running agents) faster. Also: for iteration 2's Q9 work (legacy `memory_*` tool names with hard-coded external integrations), enumerate the 21-tool list from this iteration's finding 2 against `.opencode/specs/**/handover.md` references to find which are actually quoted in committed packet docs.

## Recommended Next Focus

**Iteration 2 — Q9 alias-window scoping with full 21-tool blast radius.** Now that we know there are 21 (not 9) `memory_*` MCP tools, enumerate which of those 21 names are actually quoted by name in (a) committed `.opencode/specs/**/handover.md` files, (b) `.claude/agents/*.md` and `.opencode/agent/*.md` agent definitions, (c) `CLAUDE.md` + `AGENTS.md` + the two siblings, and (d) the `opencode.json` MCP config. Tools with high external-quotation count get permanent aliases; tools with zero external quotations can drop in one release. Output: a 21-row table with quotation-count + alias-window recommendation per tool. This directly answers Q2 (alias window length) and Q3 (rename surface) at higher resolution than iteration 1.

(Defer Q5 glossary/lint-rule and Q6 README positioning callout to iterations 3-4; defer Q7 FSRS loanwords to iteration 5; defer Q10 OSS precedent to iteration 6 once internal vocabulary is converged.)
