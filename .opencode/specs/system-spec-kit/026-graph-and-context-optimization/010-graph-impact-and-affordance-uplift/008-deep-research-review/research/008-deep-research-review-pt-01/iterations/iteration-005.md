---
_memory:
  continuity:
    next_safe_action: "Iteration 006 should drill 010/005 trust-badge merge behavior, age allowlist scope, dbGetter dependency injection, and cache invalidation against strategy.md."
---
# Iteration 005 — 010/004 affordance denylist + conflicts_with + debug counters

**Focus:** Audit denylist comprehensiveness, conflicts_with reject reachability, debug counter parity, lane routing.
**Iteration:** 5 of 10
**Convergence score:** 0.62

## Section A verdicts (denylist comprehensiveness)
- **A1:** TS uses one instruction denylist regex plus privacy regexes. `affordance-normalizer.ts:123-124` defines `INSTRUCTION_PATTERN` as `/(?:(?:^|\s|\b)(?:(?:ignore|disregard|forget|skip|bypass|override)\s+(?:the\s+)?(?:previous|prior|earlier|above|all|any)(?:\s+(?:and\s+)?(?:previous|prior|earlier|above|all|any))*\s+(?:instruction|instructions|prompt|prompts|directions?|rule|rules|context|guidance|message|messages|system\s+prompt)|override\s+system\s+(?:prompt|message|instructions)|new\s+instructions?\s*:|reveal\s+(?:the\s+)?(?:system|developer)\s+(?:prompt|instruction|instructions)|system\s*:|developer\s*:|assistant\s*:|user\s*:|human\s*:|execute\s*:|instruction\s*:)|\[\s*(?:system|developer|assistant|user|human|inst|instruction)\s*\]|<\s*(?:system|developer|assistant|user|human)\s*>)/i`; `EMAIL_PATTERN`, `URL_PATTERN`, and `TOKEN_PATTERN` are at `affordance-normalizer.ts:125-127`.
- **A2:** Python mirrors the denylist in `skill_graph_compiler.py:90-109` as `AFFORDANCE_INSTRUCTION_PATTERN = re.compile(...)` with the same branch sequence: command synonyms at `skill_graph_compiler.py:93`, directional variants at `skill_graph_compiler.py:94-95`, protected nouns at `skill_graph_compiler.py:96-97`, override/new/reveal/role prefixes at `skill_graph_compiler.py:98-103`, bracket markers at `skill_graph_compiler.py:105`, angled markers at `skill_graph_compiler.py:106`, and `re.IGNORECASE` at `skill_graph_compiler.py:108`.
- **A3:** They are semantically equivalent for the ASCII fixture corpus, but not character-for-character equivalent. Divergences are regex host syntax (`/.../i` in TS vs `re.compile(..., re.IGNORECASE)` in Python), Python string segmentation across `skill_graph_compiler.py:91-107`, and possible engine-level Unicode casefold/whitespace differences. No material ASCII branch divergence found.
- **A4:** Covered fixture shapes: ignore/disregard/forget/skip/bypass + previous/prior/above/all/any instructions/context/rules (`affordance-injection-fixtures.json:5-12`), override system prompt/message/instructions (`:13-15`), new instructions (`:16`), reveal system/developer prompt (`:17-18`), role prefixes system/developer/assistant/user/human/execute/instruction (`:19-25`), bracket markers `[INST]`, `[system]`, `[developer]`, `[instruction]` (`:26-29`), angled markers `<system>`, `<assistant>`, `<human>` (`:30-32`), benign near-misses (`:35-45`), and privacy stripping (`:47-64`). Not covered: typo/fuzzy command words, punctuation-separated letters, Unicode homoglyph/small-cap substitutions, HTML/entity encoded words, zero-width separators, Base64/URL encoded instructions, non-English instruction equivalents, Markdown hidden comments, YAML/frontmatter role labels, and nested tags like `<SYSTEM/>`.
- **A5:** Reachable misses exist, but they are sanitized rather than raw-forwarded. The regex runs before normalization in TS at `affordance-normalizer.ts:135-140` and Python at `skill_graph_compiler.py:405-409`; if it misses, the phrase is cleaned and returned at `affordance-normalizer.ts:140-147` / `skill_graph_compiler.py:410-415`. Not covered examples: `ignor previous instructions`, `ovreride system prompt`, `I*g*n*o*r*e prior instructions`, `&#105;gnore previous instructions`, `ɪɢɴᴏʀᴇ previous instructions`, and multilingual equivalents. Control and non-ASCII characters are stripped later, but the input is not rejected as unsafe.

## Section B verdicts (conflicts_with reject)
- **B1:** Python excludes conflict affordances by omission from `AFFORDANCE_RELATION_FIELDS`; allowed relation fields are only `dependsOn`, `depends_on`, `enhances`, `siblings`, `prerequisiteFor`, and `prerequisite_for` at `skill_graph_compiler.py:61-68`.
- **B2:** Reserved fields are explicit: `AFFORDANCE_REJECTED_RELATION_FIELDS = frozenset({"conflictsWith", "conflicts_with"})` at `skill_graph_compiler.py:72-75`.
- **B3:** `validate_derived_affordances` starts at `skill_graph_compiler.py:526`; it intersects raw keys with the rejected set at `skill_graph_compiler.py:543` and emits `declares reserved field(s) ... conflict edges require an authoritative reciprocal declaration in edges.conflicts_with and cannot be derived from affordances` at `skill_graph_compiler.py:545-550`.
- **B4:** The reject is reachable from a normal skill author editing `derived.affordances`: an author could reasonably mirror top-level `edges.conflicts_with` into affordance metadata and hit this validation error. It is not exclusively an attack defense; it also enforces the contract that conflicts require bilateral authoritative declarations (`skill_graph_compiler.py:49-60`).
- **B5:** Python has the explicit test at `test_skill_advisor.py:1539-1555`; it only checks `conflicts_with`, not camelCase `conflictsWith`. I found no TS-side reject test. More importantly, TS still includes `conflicts_with` in `AffordanceRelation` at `affordance-normalizer.ts:5-10`, accepts `conflictsWith`/`conflicts_with` inputs at `affordance-normalizer.ts:24-25`, and normalizes them in `relationEdges` at `affordance-normalizer.ts:184-190`.

## Section C verdicts (debug counters)
- **C1:** TS declares and initializes the counters at `affordance-normalizer.ts:81-87`:
  `{ received: 0, accepted: 0, dropped_unsafe: 0, dropped_empty: 0, dropped_unknown_skill: 0 }`.
- **C2:** Python declares the same object at `skill_graph_compiler.py:136-142`:
  `{"received": 0, "accepted": 0, "dropped_unsafe": 0, "dropped_empty": 0, "dropped_unknown_skill": 0}`.
- **C3:** Conceptual events match for four counters. TS increments `received` at `affordance-normalizer.ts:217`, `dropped_unknown_skill` at `:220`, `dropped_empty` at `:226`, and `accepted` at `:229`; Python increments `received` at `skill_graph_compiler.py:465`, `dropped_unknown_skill` for non-dict and missing skill at `:468` and `:476`, `dropped_empty` at `:498`, and `accepted` at `:500`. `dropped_unsafe` fires in neither runtime; both comments say it is retained for future phrase-level sanitizer accounting (`affordance-normalizer.ts:67-70`, `skill_graph_compiler.py:130-133`).
- **C4:** Getters are exported/available: TS exports `getAffordanceNormalizerCounters` at `affordance-normalizer.ts:89-91`; Python exposes `get_affordance_normalizer_counters` at `skill_graph_compiler.py:145-147`.
- **C5:** Reset helpers are exported/available: TS exports `resetAffordanceNormalizerCounters` at `affordance-normalizer.ts:93-99`; Python exposes `reset_affordance_normalizer_counters` at `skill_graph_compiler.py:150-153`.
- **C6:** Module-load initialization is correct in both runtimes (`affordance-normalizer.ts:81-87`, `skill_graph_compiler.py:136-142`).
- **C7:** TS concurrency caveat is low risk for normal Node execution: `normalize()` is synchronous, increments are plain same-thread mutations, and there is no `await` or callback interleaving inside the loop (`affordance-normalizer.ts:214-239`). Counters are process/module-instance local, so worker threads or multiple processes would not share a single aggregate.

## Section D verdicts (lane routing)
- **D1:** Derived lane consumes affordance evidence through the existing `derived_generated` path: it filters affordances by skill at `derived.ts:10-15`, flattens `derivedTriggers` at `derived.ts:29-31`, scores matched affordance triggers at `derived.ts:39-45`, pushes only `affordance.evidenceLabel` at `derived.ts:46`, and emits lane `derived_generated` at `derived.ts:59-64`.
- **D2:** Graph-causal lane weights affordance edges by appending them to the existing adjacency map at `graph-causal.ts:34-45`; it then uses the shared `EDGE_MULTIPLIER` table at `graph-causal.ts:13-19` and propagates evidence as `edge:source->target:type` at `graph-causal.ts:68-76`.
- **D3:** No new lane was added. `ScorerLane` is still the five-lane union at `types.ts:7-12`, and `SCORER_LANES` remains `explicit_author`, `lexical`, `graph_causal`, `derived_generated`, `semantic_shadow` at `weights-config.ts:22-28`. Fusion routes affordances into existing derived and graph-causal calls at `fusion.ts:148-155`.
- **D4:** Raw affordance phrases do not leak through affordance attribution. `derived.ts:46` records `affordance.evidenceLabel`, not the matched phrase; recommendation payloads include lane evidence at `fusion.ts:260-308`; the regression test asserts `evidence: ['affordance:mcp-chrome-devtools:0']` and no raw phrase in recommendations at `affordance-normalizer.test.ts:94-118`. The hook renderer is stricter: it ignores free-form reasons/descriptions/stdout/prompt text at `render.ts:104-110` and emits only skill label plus scores at `render.ts:155-157`.

## Section E verdicts (test coverage)
- **E1:** `affordance-normalizer.test.ts` has 6 `it(...)` cases: allowlisted fields/privacy/instruction stripping/raw phrase leak checks at `:55-120`, plus shared fixture injection, benign, and privacy coverage at `:126-167`.
- **E2:** `lane-attribution.test.ts` has 1 `it(...)` case. It proves affordance evidence routes through derived and graph-causal lanes and asserts no lane named `affordance` appears (`lane-attribution.test.ts:30-59`).
- **E3:** `routing-fixtures.affordance.test.ts` has 2 `it(...)` cases: recall improves for affordance-only prompts at `:30-62`, and affordance evidence does not overpower explicit author triggers at `:64-82`.
- **E4:** TS counter values are not asserted. Search found exported counter helpers in the library, but no test imports or calls `getAffordanceNormalizerCounters` / `resetAffordanceNormalizerCounters`; the affordance tests import only `normalize`, `scoreAdvisorPrompt`, and projection helpers at `affordance-normalizer.test.ts:9-12`.
- **E5:** Python counter values are also not asserted. The test file covers compiler normalization and fixtures, but the R-007-P2-8/R-007-8 block calls `normalize_affordance_input` and `validate_derived_affordances` without reading `get_affordance_normalizer_counters` (`test_skill_advisor.py:1539-1633`).

## New findings
- **F13 (P2, test-add/code-fix, RQ3/RQ5): Obfuscated prompt-injection variants are outside the denylist and fixture corpus.** Evidence: the shared fixture enumerates direct ASCII variants only (`affordance-injection-fixtures.json:5-32`), the denylist executes before normalization (`affordance-normalizer.ts:135-140`, `skill_graph_compiler.py:405-409`), and missed strings are cleaned then returned rather than rejected (`affordance-normalizer.ts:140-147`, `skill_graph_compiler.py:410-415`). Remediation: add tests for typo, punctuation-separated, Unicode homoglyph/small-cap, HTML entity, zero-width, and multilingual variants; decide whether to canonical-fold/entity-decode before the denylist or document these as out of scope.
- **F14 (P2, code-fix/test-add, RQ2/RQ3): `conflicts_with` is rejected only by the Python compiler path; the TS normalizer still accepts it.** Evidence: Python reserves `conflictsWith`/`conflicts_with` at `skill_graph_compiler.py:72-75` and errors at `skill_graph_compiler.py:543-550`, while TS includes `conflicts_with` in the affordance relation type and normalizes both spellings at `affordance-normalizer.ts:5-10`, `:24-25`, and `:184-190`. Remediation: either remove conflict affordance support from TS and add a TS regression test, or document that runtime-supplied affordances may express negative edges while compiled graph metadata may not.
- **F15 (P2, test-add/observability, RQ2/RQ5): Debug counters are exported but not value-tested, and `dropped_unsafe` is permanently zero by design.** Evidence: `dropped_unsafe` is initialized in TS/PY (`affordance-normalizer.ts:81-87`, `skill_graph_compiler.py:136-142`) but has no increment site; both comments say it is currently unused (`affordance-normalizer.ts:67-70`, `skill_graph_compiler.py:130-133`). Tests cover fixture behavior but do not assert counter state (`affordance-normalizer.test.ts:55-167`, `test_skill_advisor.py:1539-1633`). Remediation: add TS/PY tests for received, accepted, dropped_empty, dropped_unknown_skill, reset behavior, and either increment `dropped_unsafe` per scrubbed unsafe phrase/input or rename/document it as reserved.

## Negative findings
- **HANDLED:** TS and PY denylist branches match for the shared ASCII adversarial fixture; no direct branch omission found between `affordance-normalizer.ts:123-124` and `skill_graph_compiler.py:90-109`.
- **HANDLED:** Privacy substrings are stripped before instruction matching in both runtimes (`affordance-normalizer.ts:135-138`, `skill_graph_compiler.py:405-407`) and covered by shared fixture tests (`affordance-normalizer.test.ts:156-167`, `test_skill_advisor.py:1614-1631`).
- **HANDLED:** Python `conflicts_with` rejection has an explicit, operator-readable error message rather than a generic unsupported-field error (`skill_graph_compiler.py:539-550`).
- **HANDLED:** Affordance evidence is routed through existing `derived_generated` and `graph_causal` lanes; no separate affordance lane exists (`types.ts:7-12`, `weights-config.ts:22-28`, `fusion.ts:148-155`).
- **HANDLED:** Raw matched affordance phrases are replaced by stable `affordance:<skill>:<index>` labels in derived-lane evidence (`derived.ts:39-47`) and covered by a regression test (`affordance-normalizer.test.ts:94-118`).
- **HANDLED:** TS counter mutations are synchronous and safe under ordinary single-threaded Node calls (`affordance-normalizer.ts:214-239`).

## RQ coverage cumulative through iter 5
- **RQ1:** Partial. Iterations 1-4 found no new P0; iteration 5 found no P0/P1 on 010/004, only P2 boundary/test gaps.
- **RQ2:** Covered for this slice. T-D conflict rejection is real server-side but TS parity is incomplete; T-F counters exist but need value assertions.
- **RQ3:** Covered for this slice. Denylist handles direct variants but misses obfuscation classes; conflict declarations are fail-closed only in Python compiler validation.
- **RQ4:** Partial. No new umbrella-doc drift investigated in iteration 5 beyond code/test contract comments.
- **RQ5:** Covered for this slice. Existing tests cover core affordance routing and shared fixtures, but not obfuscated variants, TS conflict rejection, or counter values.

## Next iteration recommendation
Iter 006 should drill 010/005 trust-badge merge + age allowlist + dbGetter DI + cache invalidation per strategy.md.
