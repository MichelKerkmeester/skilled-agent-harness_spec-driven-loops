# RS-020 — Sidecar crash mid-session

- Section: §12
- Verdict: PASS
- Started: 2026-05-20T20:20:40Z
- Completed: 2026-05-20T20:20:55Z

## Commands run

```bash
# Start sidecar
bash .opencode/skills/system-rerank-sidecar/scripts/start.sh > /tmp/rerank-sidecar-rs020.log 2>&1 & disown
sleep 3 && curl -sf -X POST http://127.0.0.1:8765/warmup

# Baseline search (sidecar active)
COCOINDEX_RERANK_VIA_SIDECAR=true .opencode/skills/mcp-coco-index/mcp_server/.venv/bin/ccc search "registry of available embedding backends" --limit 3

# Kill sidecar
pkill -KILL -f rerank_sidecar
pgrep -f rerank_sidecar

# Rerun same search (sidecar gone)
COCOINDEX_RERANK_VIA_SIDECAR=true .opencode/skills/mcp-coco-index/mcp_server/.venv/bin/ccc search "registry of available embedding backends" --limit 3
```

## Observed signals

- Expected: second call falls back to bundled adapter, no MCP crash
- Actual:
  - Baseline search (sidecar active): reranker scores in 0.99 range (0.998, 0.994, 0.993)
  - Sidecar killed successfully (pgrep returns exit code 1)
  - Second search (sidecar gone): reranker scores in different range (6.312, 5.125, 4.938)
  - Both searches returned results successfully
  - No CLI crash or error in output
  - Results still show cross_encoder_rerank in rankingSignals (bundled adapter still reranking)

## Rationale

Second call returned results successfully after sidecar crash, with reranker scores in a different range indicating the bundled CrossEncoderRerankerAdapter took over. No CLI crash or error occurred. The fallback mechanism worked correctly. Marked PASS.

## Surrogate

Not applicable (fully executable via CLI). The CLI does not expose RetrievalDiagnostics to confirm the exact reranker_fallback_reason, but the score range difference (6.312 vs 0.998) clearly indicates a different adapter was used (bundled vs sidecar).
