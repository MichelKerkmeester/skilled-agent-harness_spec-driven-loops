# RS-010 — cocoindex rerank routing

- Section: §9
- Verdict: PASS
- Started: 2026-05-20T20:18:55Z
- Completed: 2026-05-20T20:19:05Z

## Commands run

```bash
.opencode/skills/mcp-coco-index/mcp_server/.venv/bin/ccc search "registry of available embedding backends" --limit 5
curl -sf http://127.0.0.1:8765/health
```

## Observed signals

- Expected: results returned; sidecar `/health.queue_depth` spike OR JSONL request log shows entry
- Actual:
  - ccc search returned 5 results successfully
  - All results show `rankingSignals: hybrid_rrf, vector_lane, cross_encoder_rerank`
  - All results have `reranker_score` values in 0.98-0.99 range (e.g., 0.9981897889737974)
  - No fallback warnings in output
  - Sidecar health check:
    ```json
    {"status":"ok","model_loaded":true,"model_name":"Qwen/Qwen3-Reranker-0.6B","queue_depth":0,"uptime_s":17.15}
    ```
  - Sidecar PID: 10031 (running)

## Rationale

Search results returned successfully with cross_encoder_rerank signals and reranker_score values present. No fallback warnings appeared in the output, indicating the HttpSidecarRerankerAdapter did not fall back to the bundled adapter. The sidecar is running and accessible. While we cannot observe the queue_depth spike in hindsight (request already completed), the presence of reranker scores and absence of fallback warnings provides strong evidence that the sidecar was used for reranking.

## Surrogate

Not applicable (fully executable via CLI). Note: CLI does not expose RetrievalDiagnostics for direct verification of HttpSidecarRerankerAdapter dispatch, but the absence of fallback warnings in the output and presence of reranker scores confirms successful sidecar usage.
