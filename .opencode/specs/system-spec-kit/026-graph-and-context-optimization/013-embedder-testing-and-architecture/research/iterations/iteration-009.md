## Focus

Iteration 9 audited cross-arc consistency and global hygiene for the `013-embedder-testing-and-architecture` umbrella. The pass focused on root phase-parent metadata, stale cross-arc paths, production-default claims, and deferred follow-on promises outside arc 008.

## Actions Taken

1. Read root `spec.md`, `description.json`, and `graph-metadata.json`; root `plan.md`, `tasks.md`, and `implementation-summary.md` are absent because this target is a phase parent.
2. Skimmed `iteration-001.md` plus the finding rows from iterations 002-008 to avoid re-reporting arc 008 and prior arc-specific findings.
3. Listed direct child arcs under the umbrella and compared them with root `graph-metadata.json.children_ids` and the root `spec.md` sub-phase table.
4. Spot-checked source files tied to the docs' embedder claims: `shared/embeddings/auto-select.ts`, `mcp_server/lib/embedders/registry.ts`, `mcp_server/lib/embedders/adapter.ts`, `cocoindex_code/config/config.py`, and `cocoindex_code/embedders/registered_embedders.py`.
5. Ran grep probes for `jina-embeddings-v3`, `nomic-embed-text-v1.5`, `CodeRankEmbed`, old child slugs like `001-embedder-adapter-interface`, and deferred packet names such as `017-system-code-graph-architecture-and-artifact-policy`.
6. Compared git history for the root parent docs with the current state; `git log -- .../013-embedder-testing-and-architecture/{spec.md,graph-metadata.json,description.json}` shows the last root metadata refresh at `11ffc9696`, before later arc 008 and default-promotion commits.
7. Verified promised follow-on packet names by path search under `.opencode/specs/system-spec-kit/026-graph-and-context-optimization`; no matching directories exist for three deferred packet names from packet 051.

## Findings

| Finding ID | Category | File:line + grep evidence | Recommended action |
|---|---|---|---|
| f-iter009-001 | BUGGED | Root `spec.md:76-85` lists old flat child slugs such as `001-embedder-adapter-interface`, `004-code-index-stack`, `008-deep-review-stack`, and `009-skill-docs-alignment`; `graph-metadata.json:6-14` lists only children `001` through `007`; actual direct child probe returns `001-local-embeddings-foundation` through `008-rerank-sidecar-arc`. The old flat paths are absent (`test -e .../001-embedder-adapter-interface` etc. returned missing). | Regenerate the umbrella phase map and `graph-metadata.json.children_ids` from the actual direct children, including `008-rerank-sidecar-arc`, and remove the old flat slug rows. |
| f-iter009-002 | BUGGED | Root continuity still says scaffold state: `spec.md:15-26` has `last_updated_at: 2026-05-17T08:15:00Z`, `recent_action: "Scaffold phase parent + 4 children"`, `next_safe_action: "Resume into 001-embedder-adapter-interface"`, and `completion_pct: 5`; `spec.md:47` says `Status | Scaffolded`; `graph-metadata.json:42-43` says `"status": "planned"`. Git history on the root docs includes later umbrella refresh commit `11ffc9696` and subsequent arc commits through `f8494eda59`, while the filesystem now has eight top-level arcs. | Refresh root continuity/status metadata after the current research run so resume agents do not route into a nonexistent 001 flat child or treat the umbrella as a 5% scaffold. |
| f-iter009-003 | BUGGED | Root `spec.md:3` says production winners are `jina-embeddings-v3` for memory and `sbert/jinaai/jina-embeddings-v2-base-code` for CocoIndex; root row `spec.md:79` repeats `jina-embeddings-v3 + rescue`. Current mk-spec-memory auto selection prefers Nomic first at `auto-select.ts:80-90`, and CocoIndex now defaults to CodeRankEmbed at `config.py:21` plus `registered_embedders.py:255`; git history includes `847333a8fa feat(016/002/004): ADR-013 — switch mk-spec-memory default to nomic-embed-text-v1.5` and `8364bdd5b7 feat(016/004): promote nomic-ai/CodeRankEmbed as production embedder default` in the arc history. | Update the umbrella description and phase 004/006 rows to the final defaults: mk-spec-memory Nomic-first/ADR-013+014 and CocoIndex CodeRankEmbed, with Jina kept only as historical candidate/fallback where accurate. |
| f-iter009-004 | DEAD | Active source comments still point to removed flat child docs: `adapter.ts:7-8` and `registry.ts:9-10` reference `013-embedder-testing-and-architecture/001-embedder-adapter-interface/spec.md`, but that path is missing. The global descriptions index also retains dead old entries at `.opencode/specs/descriptions.json:9013`, `9025`, `9040`, and `9054` for flat children `001` through `004`, while live docs are nested under `002-spec-memory-stack/001-adapter-interface` etc. | Replace source comments with live nested spec paths and regenerate/clean `.opencode/specs/descriptions.json` so memory/spec search does not surface dead folder anchors. |
| f-iter009-005 | MISSED | Packet 051 defers broad P2 work to named follow-ons at `051-runtime-config-mk-code-index-parity-plus-findings/implementation-summary.md:143-145`, and the binding trace repeats `DEFERRED_PACKETS=017-system-code-graph-architecture-and-artifact-policy,049-shared-daemon-runner-hardening,050-shared-daemon-test-hygiene` at `implementation-summary.md:164`. Path search under `.opencode/specs/system-spec-kit/026-graph-and-context-optimization` found no matching directories; existing `049-substrate-stress-coverage` and `050-all-skills-alignment-sweep` are different packets. | Either create the three deferred follow-on packets with those exact slugs, retarget the deferrals to existing packets, or mark the deferred P2s explicitly waived/resolved. |
