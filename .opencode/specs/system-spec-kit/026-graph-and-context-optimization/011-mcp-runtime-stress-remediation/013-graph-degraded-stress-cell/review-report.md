<!--
Generated by orchestrator dispatch on 2026-04-27.
Reviewer:  copilot CLI 1.0.36 — model gpt-5.5 — effort high
Mode:      read-only review (no production code or tests executed)
Scope:     packet 013-graph-degraded-stress-cell (test-only, closes packet 005 v1.0.2 NEUTRAL verdict)

Pre-dispatch scope verification (orchestrator-side, before reviewer ran):
  $ git status --short -- .opencode/skill/system-spec-kit/mcp_server/tests/
    ?? .opencode/skill/system-spec-kit/mcp_server/stress_test/code-graph-degraded-sweep.vitest.ts
    (other untracked test files belong to sibling packets 012/014/015)

  $ git diff --stat HEAD -- .opencode/skill/system-spec-kit/mcp_server/
    Modified files all attributable to sibling packets (012 / 014 / 015), NOT 013.
    Confirmed packet 013's footprint is exactly:
      * mcp_server/stress_test/code-graph-degraded-sweep.vitest.ts  (untracked, new)
      * 013-graph-degraded-stress-cell/{spec,plan,tasks,checklist,implementation-summary}.md
      * 013-graph-degraded-stress-cell/{description,graph-metadata}.json

CLI errors: none. Single dispatch on gpt-5.5 / high; no fallback to gpt-5.4 needed.
-->

# Review Report: packet 013-graph-degraded-stress-cell

## Verdict
APPROVE WITH NITS

The packet matches the research-directed test-only approach: the implementation adds one new vitest sweep plus the packet docs, exercises the empty/broad-stale/readiness-error/fresh fallback branches end-to-end, and includes a live-DB sha256 byte-equality guard. I found no P0 blockers and one P1 brittleness concern: the broad-stale bucket hard-codes `STALE_FILE_COUNT = 60` instead of referencing the non-exported `SELECTIVE_REINDEX_THRESHOLD = 50`.

## Finding Summary
- P0: 0
- P1: 1
- P2: 0

## Per-Check Results

### Check 1 — Plan adherence
**Verdict:** PASS  
**Evidence:** `research.md:131-149`, `spec.md:67-77`, `plan.md:50-52`, `implementation-summary.md:67-80`, `code-graph-degraded-sweep.vitest.ts:23-35`, git untracked packet listing.  
**Findings:** none

The packet follows research.md §4.3 Approach #1: isolated DB sweep, end-to-end `handleCodeGraphQuery`, test-only, live DB untouched. The test covers A / A' / B / C buckets at `code-graph-degraded-sweep.vitest.ts:195-217`, `228-277`, `296-326`, and `336-373`.

### Check 2 — Coverage of the four `fallbackDecision` buckets
**Verdict:** PASS  
**Evidence:** `query.ts:791-808`, `code-graph-degraded-sweep.vitest.ts:195-217`, `228-277`, `296-326`, `336-373`.  
**Findings:** none

Each bucket asserts the exact emitted shape with `toEqual` where `fallbackDecision` exists: empty graph and broad-stale expect `{ nextTool: 'code_graph_scan', reason: 'full_scan_required', retryAfter: 'scan_complete' }`, while readiness exception expects `{ nextTool: 'rg', reason: 'scan_failed' }`. Fresh state asserts the property is absent, not null or loosely falsy.

### Check 3 — Live-DB byte-equality protection
**Verdict:** PASS  
**Evidence:** `code-graph-degraded-sweep.vitest.ts:77-83`, `104-108`, `141-149`, `301-309`, `378-384`; `code-graph-db.ts:153-186`.  
**Findings:** none

`LIVE_DB_PATH` resolves from the tests directory to `../database/code-graph.sqlite`, i.e. `mcp_server/database/code-graph.sqlite`. `hashFile` reads full file bytes via `readFileSync` and hashes with sha256. The `afterAll` equality assertion runs unconditionally, and the sanity test prevents a missing live DB from becoming a null-vs-null no-op. Bucket B calls `initDb(tempDir)` before installing the `getDb` spy, so any unmocked singleton access remains tmpdir-scoped.

### Check 4 — Bucket B mechanism deviation (`vi.spyOn(getDb)` vs corrupt-file)
**Verdict:** PASS  
**Evidence:** `implementation-summary.md:88-100`, `query.ts:1087-1121`, `ensure-ready.ts:149`, `ensure-ready.ts:329-341`, `code-graph-degraded-sweep.vitest.ts:280-326`.  
**Findings:** none

The spy exercises the same handler-level readiness-crash branch required for `nextTool: 'rg'`: `detectState()` begins with `getDb()` at `ensure-ready.ts:149`; the spy throws at `code-graph-degraded-sweep.vitest.ts:305-309`; `ensureCodeGraphReady()` does not catch that pre-state exception before returning; `handleCodeGraphQuery` catches it and emits `fallbackDecision: buildFallbackDecision(crashReadiness)` at `query.ts:1093-1117`.

Branches not covered by the spy: failures after a DB object is returned, such as `d.prepare(...)` throwing, or auto-index failures caught inside `ensure-ready.ts:423-440`. Those are different failure taxonomy surfaces, but not different for the handler-level `rg` fallback contract when readiness probing itself throws.

### Check 5 — `initDb(tempDir)` vs `SPEC_KIT_DB_DIR` isolation seam
**Verdict:** PASS  
**Evidence:** `implementation-summary.md:96-101`, `code-graph-db.ts:153-186`, `code-graph-db.vitest.ts:46-48`, `skill-graph-handlers.vitest.ts:55-99`, `crash-recovery.vitest.ts:887-988`, `regression-010-index-large-files.vitest.ts:25-534`, `handler-helpers.vitest.ts:17-22`, `cli.vitest.ts:34-37`.  
**Findings:** none

Both isolation seams are valid in this corpus. `SPEC_KIT_DB_DIR` is used where modules or child processes re-read environment-derived config. Direct `initDb(tempDir)` is the correct seam here because this is an in-process read-path integration test that needs deterministic singleton placement before invoking `handleCodeGraphQuery`.

### Check 6 — Production code mutation
**Verdict:** PASS  
**Evidence:** `spec.md:73-90`, `spec.md:102-106`, `implementation-summary.md:67-80`, `code-graph-degraded-sweep.vitest.ts:31-35`, `code-graph-degraded-sweep.vitest.ts:62-71`, git status / untracked listing.  
**Findings:** none

Packet-attributable files are the new test plus the 7 packet docs. The test directly imports `code-graph-db.js`, `indexer-types.js`, and `query.js`; those are the intended read-only seams. Current workspace status includes sibling production changes, but they are not packet 013 artifacts per the supplied attribution context.

### Check 7 — Determinism
**Verdict:** PASS  
**Evidence:** `code-graph-degraded-sweep.vitest.ts:160-177`, `188-201`, `228-248`, `296-303`, `336-359`; `ensure-ready.ts:200-213`.  
**Findings:** none

Tmpdirs use OS-managed entropy via `mkdtempSync`. `pinCwd(tempDir)` runs before `initDb` and before each handler call in all four buckets. The broad-stale 24h skew is safe because `ensureFreshFiles` compares stored mtime to actual disk mtime exactly, and the test intentionally stores "now" after setting disk mtime to 24h earlier. No `setTimeout`, sleeps, watchers, or random UUID dependencies appear in the test.

### Check 8 — Cleanup
**Verdict:** PASS  
**Evidence:** `code-graph-degraded-sweep.vitest.ts:145-158`, `160-177`, `188-190`, `305-318`; `code-graph-db.ts:190-196`.  
**Findings:** none

Cleanup order is correct: `closeDb()` releases better-sqlite3 handles before `vi.restoreAllMocks()` and tmpdir removal. `vi.restoreAllMocks()` covers both `process.cwd` and `codeGraphDb.getDb`; Bucket B also manually restores the spy before assertions so `afterEach` can close the real tmp singleton. The test does not set or mutate `SPEC_KIT_DB_DIR`.

### Check 9 — `SELECTIVE_REINDEX_THRESHOLD` boundary brittleness
**Verdict:** CONCERN  
**Evidence:** `ensure-ready.ts:55-56`, `ensure-ready.ts:200-213`, `code-graph-degraded-sweep.vitest.ts:221-236`, `implementation-summary.md:121-126`.  
**Findings:**
- **P1 — Hard-coded threshold assumption.** `STALE_FILE_COUNT = 60` is hard-coded in the test while `SELECTIVE_REINDEX_THRESHOLD = 50` is a non-exported production constant. The limitation is documented, but the test still depends on a duplicated boundary assumption. Recommended fix: export a test-visible threshold constant from `ensure-ready.ts`, or document the assumption directly adjacent to the test with a required update note if exporting is intentionally avoided.

### Check 10 — Scope discipline
**Verdict:** PASS  
**Evidence:** git status / untracked listing; `spec.md:67-90`; `implementation-summary.md:67-80`.  
**Findings:** none

The dirty workspace includes sibling packet production changes in `code_graph/`, plus sibling packet tests and folders. Packet 013's scoped untracked artifacts are only `mcp_server/stress_test/code-graph-degraded-sweep.vitest.ts` and the 7 files under `013-graph-degraded-stress-cell/`. No skill/template changes or docs outside the packet folder are attributable to packet 013.

## Top 3 Findings (most impactful)
1. **[P1] Hard-coded stale threshold assumption** — `STALE_FILE_COUNT = 60` duplicates the non-exported `SELECTIVE_REINDEX_THRESHOLD = 50`; export the constant or document the maintenance contract inline. `code-graph-degraded-sweep.vitest.ts:221-236`, `ensure-ready.ts:55-56`.

## Bucket B Equivalence Judgment
`vi.spyOn(codeGraphDb, 'getDb')` is contractually equivalent to a real corrupt-SQLite readiness-probe failure for the purpose of exercising the `nextTool: 'rg'` branch. The branch condition is "readiness check throws before a `ReadyResult` is returned," not a specific SQLite error class: `detectState()` starts with `getDb()` at `ensure-ready.ts:149`, `ensureCodeGraphReady()` calls `detectState(rootDir)` at `ensure-ready.ts:341`, and `query.ts:1093-1117` catches the thrown error and emits `buildFallbackDecision(crashReadiness)`, which maps `freshness: 'error'` to `{ nextTool: 'rg', reason: 'scan_failed' }` via `query.ts:791-796`.

The spy does not cover failures after `getDb()` succeeds, such as `d.prepare(...)` throwing, nor auto-index failures handled inside `ensure-ready.ts:423-440`. Those are separate failure-mode taxonomy gaps, not gaps in the handler fallback contract under review.

## SELECTIVE_REINDEX_THRESHOLD Reference Audit
The value is not referenced from source. `SELECTIVE_REINDEX_THRESHOLD` is a non-exported constant at `ensure-ready.ts:55-56`; the test hard-codes `STALE_FILE_COUNT = 60` at `code-graph-degraded-sweep.vitest.ts:235-236`. Recommended fix: export a stable test-visible constant, or add an explicit inline maintenance note tying `60` to "must remain > SELECTIVE_REINDEX_THRESHOLD" if the production constant intentionally remains private.

## Per-Bucket Branch Trace

**A — Empty graph**
- Setup: tmpdir created, `process.cwd()` pinned, `initDb(tempDir)` creates schema with zero rows. `code-graph-degraded-sweep.vitest.ts:195-202`
- Handler: `handleCodeGraphQuery({ operation: 'calls_from' })` invokes readiness with inline full scan disabled. `code-graph-degraded-sweep.vitest.ts:203-207`, `query.ts:1087-1092`
- Branch: `detectState()` sees `code_nodes` count `0`, returns `action: 'full_scan'`. `ensure-ready.ts:151-154`
- Assertion: blocked payload contains exact `code_graph_scan` fallback object. `code-graph-degraded-sweep.vitest.ts:209-216`

**A' — Broad-stale**
- Setup: tmpdir pinned and initialized; 60 tracked files are backdated on disk but stored with "now" mtime and one node each. `code-graph-degraded-sweep.vitest.ts:228-263`
- Handler: `calls_from` query enters readiness with `allowInlineFullScan: false`. `code-graph-degraded-sweep.vitest.ts:265-269`, `query.ts:1087-1092`
- Branch: stale count exceeds `SELECTIVE_REINDEX_THRESHOLD`, so `detectState()` returns `action: 'full_scan'`. `ensure-ready.ts:200-213`
- Assertion: exact `code_graph_scan` fallback object is asserted. `code-graph-degraded-sweep.vitest.ts:271-277`

**B — Readiness exception**
- Setup: tmpdir pinned, `initDb(tempDir)` runs before the `getDb` spy, then the spy throws `readiness probe failed: simulated db locked`. `code-graph-degraded-sweep.vitest.ts:296-309`
- Handler: `outline` query calls `ensureCodeGraphReady`; `detectState()` first calls `getDb()`, which throws. `ensure-ready.ts:149`, `ensure-ready.ts:329-341`
- Branch: `query.ts` catch block builds crash readiness and emits `fallbackDecision`. `query.ts:1093-1117`
- Assertion: exact `{ nextTool: 'rg', reason: 'scan_failed' }` object plus `code_graph_not_ready` message. `code-graph-degraded-sweep.vitest.ts:320-326`

**C — Fresh**
- Setup: tmpdir pinned and initialized; a real file is written, stored with matching mtime/content hash, and seeded with one node. `code-graph-degraded-sweep.vitest.ts:336-359`
- Handler: `outline` query enters readiness and graph query path. `code-graph-degraded-sweep.vitest.ts:361-365`, `query.ts:1087-1092`
- Branch: `detectState()` finds tracked files fresh and returns `action: 'none'`. `ensure-ready.ts:172-197`
- Assertion: response is `ok` and `fallbackDecision` property is absent. `code-graph-degraded-sweep.vitest.ts:367-372`

## Sign-Off Recommendation
APPROVE WITH NITS
