## Focus

Iteration 3 audited arc `002-spec-memory-stack`, the mk-spec-memory TypeScript MCP embedder stack under `016-embedder-testing-and-architecture`. The pass focused on parent/child metadata consistency, ADR-014 local-first default claims, and source alignment for the adapter, auto-select, factory, registry, and reindex paths.

## Actions Taken

1. Skimmed the arc parent `spec.md`, `description.json`, and `graph-metadata.json`; confirmed parent-level `plan.md`, `tasks.md`, and `implementation-summary.md` are absent because the target is a phase parent.
2. Listed all direct child phase folders and compared that list with `graph-metadata.json.children_ids`.
3. Read selected child docs for phases 004, 015, 016, and 019, including ADR evidence and implementation summaries.
4. Spot-checked source files referenced by the docs: `shared/embeddings/auto-select.ts`, `shared/embeddings/factory.ts`, `shared/embeddings/profile.ts`, `shared/embeddings/providers/hf-local.ts`, `mcp_server/lib/embedders/registry.ts`, `mcp_server/lib/embedders/schema.ts`, and `mcp_server/lib/embedders/reindex.ts`.
5. Ran grep probes for `HF_LOCAL_MODEL`, `HF_EMBEDDINGS_MODEL`, `BAAI/bge-base-en-v1.5`, `nomic-ai/nomic-embed-text-v1.5`, `mxbai`, `cat-24/409`, `ADR-014`, `local-first`, `children_ids`, and stale `llama-cpp` references.
6. Compared path history with `git log -- .opencode/specs/.../002-spec-memory-stack` and inspected commit stats for `8bc0d7c0b` (ADR-014) and `db12a824f9` (019 repair runner).
7. Read iteration 1 to avoid re-reporting arc 008 rerank-sidecar findings.

## Findings

| Finding ID | Category | File:line + grep evidence | Recommended action |
|---|---|---|---|
| f-iter003-001 | UNSHIPPED | Phase 015 says the hf-local fallback was changed to nomic: `.opencode/specs/.../002-spec-memory-stack/015-cascade-reorder-and-nomic-hf-local-default/spec.md:73` and `implementation-summary.md:63-65`. `auto-select.ts` does use `HF_LOCAL_MODEL = 'nomic-ai/nomic-embed-text-v1.5'` at `.opencode/skills/system-spec-kit/shared/embeddings/auto-select.ts:80-83`, but other runtime paths still default to BGE: `factory.ts:143-146`, `profile.ts:185-195`, and `providers/hf-local.ts:13,211-212`. | Align every hf-local default surface to `nomic-ai/nomic-embed-text-v1.5`, or narrow ADR-014/docs to say only the `EMBEDDINGS_PROVIDER=auto` bootstrap path changed. Add a regression test for explicit `EMBEDDINGS_PROVIDER=hf-local` with no `HF_EMBEDDINGS_MODEL`. |
| f-iter003-002 | MISSED | The parent has 19 direct child folders, including `018-constitutional-quality-gate-exemption` and `019-lineage-and-metadata-repair-runner` (`find ... | nl`: rows 18-19), but parent `graph-metadata.json` lists children only through 017 at lines 6-23. The same metadata reports `derived.status = "planned"` at line 51 despite shipped child commits through `db12a824f9` and `implementation-summary.md` for 019 reporting completion at lines 41-45. | Regenerate the parent graph metadata after 018/019, include all direct children, and set derived status from child closeout state instead of leaving the phase parent as `planned`. |
| f-iter003-003 | BUGGED | Parent `spec.md` phase map is structurally corrupted: the table header has three columns at lines 70-71, but rows 76-79 contain four cells and shift phase titles/status/evidence into the wrong columns. Example: line 76 maps `015-cascade-reorder...` to title `016-reindex...` and line 77 maps `016-reindex...` to title `017-factory...`. | Rebuild the parent phase map from direct child `spec.md` metadata, with one row per child and exactly `Phase / Title / Status` cells. Add a validator check for phase-parent table cell counts. |
| f-iter003-004 | BUGGED | Parent `description.json` does not describe the parent arc: lines 2-17 say "Migration runner: normalize importance_tier enum..." and keywords are `migration`, `runner`, `graph-metadata`, `repair`, while parent `spec.md:55-62` describes the mk-spec-memory embedder stack. This looks copied from phase 019, not generated from the parent. | Regenerate `description.json` from the parent `spec.md` and add a post-save sanity check that parent descriptions do not inherit a child packet's title/keywords. |
| f-iter003-005 | UNSHIPPED | Parent `spec.md` claims the stack shipped "mxbai swap + cat-24/409 closure" at lines 3, 62, and 75. The bake-off decision record says the mxbai job failed at 0 rows, active pointer stayed baseline, and "cat-24/409 ... cannot be closed by this packet" at `004-spec-memory-embedder-bake-off/decision-record.md:50-56`; the retry still failed at lines 108-123. The closure narrative says cat-24/409 was closed via nomic plus retrieval-rescue at lines 7, 16-19, and explicitly gives mxbai `2/10` with `ADR-001 ROLLBACK` at lines 36-45. | Rewrite the parent summary and phase 004 row to say the bake-off ultimately closed cat-24/409 through nomic/jina plus retrieval-rescue, while mxbai was a failed/rolled-back candidate. |
