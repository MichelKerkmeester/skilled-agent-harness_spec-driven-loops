## Focus

Iteration 7 audited arc `006-mcp-launcher-concurrency`, especially the late 010-012 bridge/socket follow-ons and the parent arc metadata. The pass checked whether the arc's "complete" claims still line up with current launcher, daemon, plugin, and spec-folder state.

## Actions Taken

1. Read the arc parent `spec.md` and `graph-metadata.json`; `plan.md`, `tasks.md`, and `implementation-summary.md` are absent at the parent, which matches phase-parent discipline.
2. Skimmed prior iteration 1 only to avoid re-reporting arc 008 findings.
3. Listed the arc's child docs and read phase 011 and 012 `spec.md`, `tasks.md`, and `implementation-summary.md` because those are the latest committed follow-ons in the path history.
4. Spot-checked source files referenced by the docs: `.opencode/bin/mk-skill-advisor-launcher.cjs`, `.opencode/bin/mk-code-index-launcher.cjs`, `.opencode/bin/mk-spec-memory-launcher.cjs`, `.opencode/skills/system-skill-advisor/mcp_server/lib/daemon/lease.ts`, `.opencode/plugins/mk-skill-advisor.js`, and the plugin bridge.
5. Ran grep probes for `SPECKIT_IPC_SOCKET_DIR`, `LEASE_HELD_BY`, `no-bridge-socket`, `skill_graph_daemon_lease`, `DEFAULT_BRIDGE_TIMEOUT_MS`, `ADVISOR_MCP_TIMEOUT_MS`, and proposed follow-on slug `013-plugin-bridge-timeout-tune`.
6. Compared arc history with `git log --oneline -- <arc path>`; the path includes commits through `d5dbd39bc2 feat(016/006/012): daemon-side bridge socket for skill-advisor + code-index`.
7. Checked current git status for arc/source divergence; source docs under this arc are clean, while runtime DB/lease files are dirty from active local state and were not treated as source findings.

## Findings

| Finding ID | Category | File:line + grep evidence | Recommended action |
|---|---|---|---|
| f-iter007-001 | BUGGED | Parent `spec.md:55-68` lists 12 completed children, but parent `spec.md:3` still says "5-phase arc", `description.json:3` says "4-phase", `description.json:17` still has `"specId": "012"`, and `graph-metadata.json:54` still says "4-phase arc" despite `children_ids` through 012 at `graph-metadata.json:6-18`. | Regenerate or manually repair the parent `description.json` and `graph-metadata.json` after the 006 rename and 012 follow-on, and update parent prose to describe the 12-child arc rather than the old 4/5-phase shape. |
| f-iter007-002 | MISSED | Parent `spec.md:88-93` says only two passive operator items remain and anything else belongs to a separate follow-on arc. But phase 011 leaves `REQ-006` open at `011.../spec.md:124-129`, phase 012 leaves `T018` open at `012.../tasks.md:82-85`, and no `013-*` child exists under the arc parent (`find ... -maxdepth 1 -type d -name '013-*'` returned no rows). | Either add a tracked follow-on child for daemon SQLite dead-PID reclaim, or update the parent "What Needs Done" section so it does not imply the only remaining work is the zombie-soak operator task. |
| f-iter007-003 | BUGGED | The dead-PID follow-on is now underspecified against code: phase 011 says the robust fix is adding `process.kill(pid, 0)` before honoring a `skill_graph_daemon_lease` row at `011.../spec.md:85` and `011.../spec.md:159`; current `lease.ts:226-247` already performs that liveness probe in `readLeaseHeldAt()`. The remaining acquisition path at `lease.ts:300-317` still gates takeover only on `heartbeat_at` staleness and does not read `pid`, so the promised fix target is stale/partial. | Reframe the follow-on around acquisition-time reclaim semantics: decide whether `acquireSkillGraphLease()` should reclaim dead rows immediately, add tests for dead-but-fresh heartbeat rows, and revise 011/012 docs to stop asking for a probe that already exists in the launcher preflight path. |
| f-iter007-004 | BUGGED | Phase 012 claims the OpenCode plugin does not need changes once daemon sockets bind at `012.../spec.md:96-100`, but the same packet says plugin timeout may still break real bridges at `012.../spec.md:170` and `012.../implementation-summary.md:221`. Current plugin code keeps `DEFAULT_BRIDGE_TIMEOUT_MS = 1000` at `.opencode/plugins/mk-skill-advisor.js:33` and kills the bridge at `mk-skill-advisor.js:463-474`, while the bridge itself allows `ADVISOR_MCP_TIMEOUT_MS = 8000` at `mk-skill-advisor-bridge.mjs:46` and `:242-246`. | Treat the plugin timeout as an unresolved bug, not a vague open question: raise the default or configure `MK_SKILL_ADVISOR_BRIDGE_TIMEOUT_MS` to at least the bridge's 8s MCP timeout, then add a regression test for successful bridge completion under the new timeout. |
| f-iter007-005 | UNSHIPPED | Phase 012 task closeout claims all tasks complete at `012.../tasks.md:80`, but `T015` is checked `[x]` with evidence still saying `(pending -- run after this file is authored)` at `012.../tasks.md:67-72`. The implementation summary likewise records strict validate as an expected command/result, not observed output, at `012.../implementation-summary.md:189-195`. | Backfill actual strict-validate evidence for phase 012 or reopen `T015` until the command output is captured. This is a documentation closeout gap rather than missing launcher code. |
