## Focus

Iteration 2 audited arc `001-local-embeddings-foundation`, with emphasis on the phase-parent claims, child registration metadata, and current local embedding runtime behavior. The pass compared the arc's llama-cpp/EmbeddingGemma-era documentation against the current Ollama/hf-local implementation and the live child-folder inventory.

## Actions Taken

1. Read the deep-research state, strategy, and iteration-001 report to avoid re-reporting arc 008 findings.
2. Skimmed arc 001 parent `spec.md` and `graph-metadata.json`; parent `plan.md`, `tasks.md`, and `implementation-summary.md` are absent by phase-parent design.
3. Listed live child phase folders and compared them against the parent phase map and `graph-metadata.json.children_ids`.
4. Ran grep probes for `EMBEDDINGS_PROVIDER`, `MEMORY_AUTO_MIGRATE_HF_TO_LLAMA`, `llama-cpp`, `hf-local`, `ollama`, `runMigration`, `migrate-embeddings-to-llama-cpp`, `contextSize`, `mk-spec-memory`, and `mk_code_index`.
5. Spot-checked source files referenced by arc docs: `.opencode/skills/system-spec-kit/shared/embeddings/factory.ts`, `.opencode/skills/system-spec-kit/shared/embeddings/profile.ts`, `.opencode/skills/system-spec-kit/mcp_server/lib/embedders/execution-router.ts`, and `.opencode/skills/system-spec-kit/mcp_server/ENV_REFERENCE.md`.
6. Spot-checked phase docs for `018-llama-cpp-auto-migration` and `028-local-llm-feature-test-suite`, then compared their claims against current scripts and tests.
7. Ran `git log --oneline -- .opencode/specs/.../001-local-embeddings-foundation`, which showed only broad metadata/renumbering commits for this path (`db12a824f9`, `656542344e`, `0aeb35be61`, `d8c6036792`), not a clean arc-local closeout commit trail.

## Findings

| Finding ID | Category | File:line + grep evidence | Recommended action |
|---|---|---|---|
| f-iter002-001 | BUGGED | Parent `spec.md:49` says `Status` is `Complete`, but the same parent map marks `037-llama-cpp-embedding-worker-deep-dive` as `Pending` at `spec.md:117`; `graph-metadata.json:93` also derives `"status": "in_progress"`. | Reconcile the parent status with live child status, or mark the parent as an active coordination umbrella instead of complete. |
| f-iter002-002 | MISSED | Parent `spec.md:80-82` says its scope includes the child phase manifest, but the phase map only lists rows `001-012` and `037-039` at `spec.md:104-119`; `find .../001-local-embeddings-foundation -maxdepth 1` shows many omitted child folders including `013-v4-cleanup`, `018-llama-cpp-auto-migration`, `028-local-llm-feature-test-suite`, and `055-cli-devin-deep-loop-alignment`. | Regenerate the parent phase map from the filesystem or graph metadata so resume/coverage tooling sees the real arc. |
| f-iter002-003 | MISSED | `graph-metadata.json:19-56` lists children but skips live child directories `024-post-remediation-v2-re-review`, `026-post-batch-11-re-review`, `027-post-batch-12-final-re-review`, `030-post-029-final-re-review`, and `031-post-batch-15-final-re-review`; `comm -23 <live dirs> <graph children>` returned exactly those missing children. | Regenerate `graph-metadata.json.children_ids` and `derived.last_active_child_id` from the live child set. |
| f-iter002-004 | BUGGED | Parent `spec.md:72` claims `EMBEDDINGS_PROVIDER=auto` cascades `Voyage -> OpenAI -> llama-cpp -> hf-local`; current `factory.ts:66` and `factory.ts:97` only support `voyage`, `openai`, `hf-local`, `ollama`, and `auto`, while `factory.ts:612-647` resolves active Ollama, then Voyage, then OpenAI, then hf-local. `rg "llama-cpp" shared/embeddings/factory.ts` returns no provider support. | Update arc 001 docs to the current Ollama/hf-local cascade or restore a real llama-cpp provider before treating the arc claim as current. |
| f-iter002-005 | UNSHIPPED | Phase 018 requires `runMigration()`, startup wiring, and `MEMORY_AUTO_MIGRATE_HF_TO_LLAMA` handling at `018-llama-cpp-auto-migration/spec.md:78-83` and `spec.md:95-99`; its summary says this was built at `implementation-summary.md:51-65`. Current source probe: `test -f .opencode/skills/system-spec-kit/scripts/migrate-embeddings-to-llama-cpp.ts` exits `1`, and `rg "MEMORY_AUTO_MIGRATE_HF_TO_LLAMA|runMigration|MIGRATION_PENDING|runAutoMigrationIfNeeded" .opencode/skills/system-spec-kit/scripts .opencode/skills/system-spec-kit/mcp_server/context-server.ts .opencode/skills/system-spec-kit/shared -g '!**/node_modules/**'` finds only `.opencode/skills/system-spec-kit/shared/README.md:343`, not live startup integration. | Either restore the source-level migration gate and tests or mark phase 018 as superseded/decommissioned by the current Ollama cascade. |
| f-iter002-006 | DEAD | `execution-router.ts:29` still includes `llama-cpp` in `SIDECAR_LOCAL_PROVIDERS`, so auto sidecar routing has a branch for that provider; current factory validation rejects any provider outside `SUPPORTED_PROVIDERS` at `factory.ts:97` and `factory.ts:209-214`, where `llama-cpp` is absent. | Remove the dead `llama-cpp` sidecar branch or add a live provider adapter and factory support. |
| f-iter002-007 | UNSHIPPED | Phase 028 promises a comprehensive local-LLM suite with 10 functional groups and 4 perf benchmarks at `028-local-llm-feature-test-suite/spec.md:66-134`, and REQ-001 requires all 10 groups to exist and pass at `spec.md:172`; actual `find mcp_server/tests/local-llm-features -type f` shows 5 functional test files and 3 perf files, with no `migration-throughput.bench.ts`. The phase has no `implementation-summary.md`, and its `tasks.md:53-87` is still unchecked template scaffolding. | Close or re-scope phase 028: either implement the missing groups/benchmark and evidence docs, or change the spec to match the partial current suite. |
