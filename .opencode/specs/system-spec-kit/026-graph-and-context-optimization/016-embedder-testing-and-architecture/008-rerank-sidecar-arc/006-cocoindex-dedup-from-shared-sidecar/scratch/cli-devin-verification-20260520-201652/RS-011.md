# RS-011 — cocoindex opt-out

- Section: §9
- Verdict: PARTIAL
- Started: 2026-05-20T20:19:05Z
- Completed: 2026-05-20T20:19:15Z

## Commands run

```bash
COCOINDEX_RERANK_VIA_SIDECAR=false .opencode/skills/mcp-coco-index/mcp_server/.venv/bin/ccc search "registry of available embedding backends" --limit 5
```

## Observed signals

- Expected: results returned; sidecar receives no request from this call (queue stays 0)
- Actual:
  - ccc search returned 5 results successfully
  - Results still show `rankingSignals: hybrid_rrf, vector_lane, cross_encoder_rerank`
  - Results still have `reranker_score` values in 0.98-0.99 range
  - No fallback warnings in output
  - Sidecar health check after:
    ```json
    {"status":"ok","model_loaded":true,"model_name":"Qwen/Qwen3-Reranker-0.6B","queue_depth":0,"uptime_s":25.32}
    ```

## Rationale

Results returned successfully with reranker scores still present, which is expected behavior (opt-out is from sidecar to bundled adapter, not from reranking entirely). The bundled CrossEncoderRerankerAdapter is still performing reranking. However, we cannot definitively prove the sidecar did not receive the request without real-time queue_depth monitoring or request logs. The queue_depth is 0 after the call, but this is expected for a completed request regardless of which adapter handled it. Marked PARTIAL due to inability to verify the sidecar did not receive the request during the call.

## Surrogate

Not applicable (fully executable via CLI). To fully verify this scenario, real-time queue_depth monitoring during the search call or sidecar request logging would be required. The CLI does not expose this level of observability.
