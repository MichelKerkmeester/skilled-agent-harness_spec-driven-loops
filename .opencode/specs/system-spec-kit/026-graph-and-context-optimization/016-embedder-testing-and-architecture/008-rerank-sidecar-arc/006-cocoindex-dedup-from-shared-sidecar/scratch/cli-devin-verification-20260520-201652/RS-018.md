# RS-018 — Port collision

- Section: §12
- Verdict: PASS
- Started: 2026-05-20T20:20:25Z
- Completed: 2026-05-20T20:20:35Z

## Commands run

```bash
# Bind port 8765 with nc
nc -l 127.0.0.1 8765 > /tmp/nc_port_collision.log 2>&1 &

# Verify port is bound
lsof -iTCP:8765 -sTCP:LISTEN

# Attempt to start sidecar
bash .opencode/skills/system-rerank-sidecar/scripts/start.sh 2>&1

# Cleanup
pkill -f "nc -l 127.0.0.1 8765"
```

## Observed signals

- Expected: start.sh exits non-zero with clear error message mentioning EADDRINUSE or port already in use
- Actual:
  - lsof showed nc listening on localhost:8765
  - start.sh output:
    ```
    INFO:     Started server process [13606]
    INFO:     Waiting for application startup.
    INFO:     Application startup complete.
    ERROR:    [Errno 48] error while attempting to bind on address ('127.0.0.1', 8765): address already in use
    INFO:     Waiting for application shutdown.
    INFO:     Application shutdown complete.
    ```
  - Exit code: 1 (non-zero)

## Rationale

start.sh exited with non-zero exit code (1) and clear error message "address already in use" with Errno 48 (EADDRINUSE on macOS). Error message is human-readable and actionable. Port collision detected and reported correctly. Marked PASS.

## Surrogate

Not applicable (fully executable). Used nc to bind the port before attempting sidecar startup, simulating port collision scenario.
