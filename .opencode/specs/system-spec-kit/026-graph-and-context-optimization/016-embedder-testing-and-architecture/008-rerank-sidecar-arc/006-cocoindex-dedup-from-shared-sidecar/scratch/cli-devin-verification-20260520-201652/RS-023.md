# RS-023 — Env-var leak audit

- Section: §13
- Verdict: PASS
- Started: 2026-05-20T20:21:05Z
- Completed: 2026-05-20T20:21:15Z

## Commands run

```bash
# Export fake secret
export FAKE_SECRET=token

# Start sidecar
cd /Users/michelkerkmeester/MEGA/Development/Code_Environment/Public && bash .opencode/skills/system-rerank-sidecar/scripts/start.sh > /tmp/rerank-sidecar-rs023.log 2>&1 & disown

# Get sidecar PID
sleep 3 && pgrep -f rerank_sidecar

# Inspect environment
ps -E -p <pid>
```

## Observed signals

- Expected: `FAKE_SECRET` is visible in sidecar env
- Actual:
  - Sidecar PID: 14486
  - Environment dump includes: `FAKE_SECRET=token`
  - Sidecar started successfully

## Rationale

FAKE_SECRET=token is visible in the sidecar's environment dump. Parent shell secrets are inherited by the sidecar process. This is a documented advisory scenario (open advisory) where the operator notes the leak surface and plans the RERANK_* allowlist follow-on. Scenario executed and behavior recorded. Marked PASS per playbook criteria (PASS = scenario executed and behavior recorded).

## Surrogate

Not applicable (fully executable). Used ps -E on macOS to inspect the sidecar process environment. On Linux, /proc/<pid>/environ would be used instead.
