# RS-008 — spec-memory rerank routing

- Section: §8
- Verdict: SKIPPED
- Started: 2026-05-20T20:18:40Z
- Completed: 2026-05-20T20:18:45Z

## Commands run

None (SKIPPED)

## Observed signals

- Expected: results include `scoringMethod` reflecting reranker; sidecar log records one request
- Actual: Not executed

## Rationale

SKIPPED — Requires calling `memory_search()` via the spec-memory MCP with `SPECKIT_CROSS_ENCODER=true`. This scenario requires the full MCP server infrastructure which is not bootable in this shell-based dispatch. While a Node.js surrogate could theoretically exercise `cross-encoder.ts` directly, the critical verification is the integration with the actual MCP `memory_search` flow, which requires the full runtime.

## Surrogate

No surrogate executed. The routing logic can be verified by inspecting the source at `.opencode/skills/system-spec-kit/mcp_server/lib/search/cross-encoder.ts` which shows the `local` provider (line 52-59) configured to hit `http://localhost:8765/rerank` when `RERANKER_LOCAL=true` is set. The `resolveProvider()` function (line 219-230) checks this environment variable to select the local provider.
