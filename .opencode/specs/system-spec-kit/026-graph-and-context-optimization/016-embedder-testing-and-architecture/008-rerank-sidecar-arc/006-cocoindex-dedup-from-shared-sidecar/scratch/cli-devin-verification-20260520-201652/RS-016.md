# RS-016 — Race-bind under parallel cold start

- Section: §11
- Verdict: SKIPPED
- Started: 2026-05-20T20:20:05Z
- Completed: 2026-05-20T20:20:10Z

## Commands run

None (SKIPPED)

## Observed signals

- Expected: exactly one sidecar PID
- Actual: Not executed

## Rationale

SKIPPED — Requires kicking off both mk-spec-memory launcher and mcp-coco-index MCP launch within < 100 ms of each other. This scenario requires the full MCP server infrastructure for both consumers, which is not bootable in this shell-based dispatch. The spec-memory ensure helper requires TypeScript execution environment. While we could potentially run the cocoindex ensure surrogate in parallel with itself, the meaningful test requires two different consumers racing to bind the port.

## Surrogate

No surrogate executed. The race-bind logic can be verified by inspecting the source code:
- `.opencode/skills/system-spec-kit/mcp_server/bin/lib/ensure-rerank-sidecar.cjs` (spec-memory)
- `.opencode/skills/system-rerank-sidecar/scripts/ensure_rerank_sidecar.py` (cocoindex)
Both helpers probe /health before spawning and rely on port-bind EADDRINUSE as the atomicity primitive. The FastAPI/uvicorn --host 127.0.0.1 binding should reject the second bind attempt with EADDRINUSE.
