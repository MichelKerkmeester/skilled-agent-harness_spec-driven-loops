# Local-LLM Legacy Review Report

## 1. Stop Reason
Reported request label: `max_iterations_reached` (20/20). Artifact-backed stop state: the available review packet contains 10 iteration files (`iteration-001.md` through `iteration-010.md`), and `deep-review-state.jsonl` records a run resume that reduced `maxIterations` from 20 to 10. This report therefore synthesizes the 10 available markdown iterations as source of truth.

## 2. Iteration Count
10 artifact-backed iterations. The requested count of 20 is not supported by the iteration files present on disk.

## 3. Dimension Coverage
| Dimension | Deduped Finding Count |
|---|---:|
| correctness | 37 |
| traceability | 46 |
| maintainability | 33 |

## 4. Severity Counts
| Severity | Deduped Total |
|---|---:|
| P0 | 2 |
| P1 | 83 |
| P2 | 31 |

## 5. Verdict
FAIL; hasAdvisories=true. The P0 findings mean release readiness fails until runtime default selection is remediated. P1 and P2 findings show broad doc, config, template, fixture, and generated-asset drift.

## 6. Release-Readiness
Canonical post-014 default state is not consistent across the repo. The intended state is hf-local EmbeddingGemma q8 768d for Memory MCP, sentence-transformers `google/embeddinggemma-300m` bf16 768d for CocoIndex, and `llama-cpp` as explicit opt-in. The iteration evidence shows executable provider resolution still selecting `llama-cpp` in the auto path, runtime configs still documenting llama-cpp/cloud-provider defaults, and many docs/templates/tests preserving MiniLM, Voyage, Nomic, OpenAI, generic sqlite, and rejected ONNX backend assumptions.

## 7. Top P0 Findings
| Severity | Dimension | File:Line | Iterations | Evidence | Recommendation |
|---|---|---|---:|---|---|
| P0 | correctness | `.opencode/skills/system-spec-kit/shared/embeddings/factory.ts:819` | 1 | "const llamaCppAvailability = getLlamaCppAvailability();" | Remove `llama-cpp` from the implicit `auto` no-key path; only select it when `EMBEDDINGS_PROVIDER=llama-cpp` is explicit. |
| P0 | correctness | `.opencode/skills/system-spec-kit/shared/embeddings/factory.ts:822` | 1 | "name: 'llama-cpp'," | Change the no-key default branch to return `hf-local` with `onnx-community/embeddinggemma-300m-ONNX`, `q8`, 768 dims. |

## 8. Top P1 Findings
| Severity | Dimension | File:Line | Iterations | Evidence | Recommendation |
|---|---|---|---:|---|---|
| P1 | correctness | `.claude/mcp.json:19` | 1 | "\"_NOTE_3_EMBEDDINGS_PROVIDER\": \"Options: auto \| llama-cpp \| hf-local \| voyage \| openai. Default 'auto' resolves to llama-cpp; set EMBEDDINGS_PROVIDER=hf-local to opt out.\"," | Align Claude MCP config note with post-014 provider resolution and remove the opt-out framing. |
| P1 | correctness | `.codex/config.toml:23` | 1 | "_NOTE_3_EMBEDDINGS_PROVIDER = \"Options: auto \| llama-cpp \| hf-local \| voyage \| openai. Default 'auto' resolves to llama-cpp; set EMBEDDINGS_PROVIDER=hf-local to opt out.\"" | Update Codex config notes so `auto` resolves to `hf-local` without API keys and `llama-cpp` is explicit opt-in. |
| P1 | correctness | `.gemini/settings.json:36` | 1 | "\"_NOTE_3_EMBEDDINGS_PROVIDER\": \"Options: auto \| llama-cpp \| hf-local \| voyage \| openai. Default 'auto' resolves to llama-cpp; set EMBEDDINGS_PROVIDER=hf-local to opt out.\"," | Align Gemini config note with `auto -> hf-local` and make `llama-cpp` opt-in only. |
| P1 | traceability | `.opencode/install_guides/README.md:671` | 1 | "export VOYAGE_EMBEDDINGS_MODEL=voyage-3.5  # Default" | Replace the stale Voyage default example with canonical hf-local defaults; keep Voyage only as optional explicit provider setup. |
| P1 | traceability | `.opencode/install_guides/README.md:678` | 1 | "export HF_EMBEDDINGS_MODEL=nomic-ai/nomic-embed-text-v1.5  # Default" | Update HF Local example to `onnx-community/embeddinggemma-300m-ONNX`, `q8`, 768 dims. |
| P1 | traceability | `.opencode/skills/mcp-coco-index/INSTALL_GUIDE.md:101` | 1 | "\| **Embedding** \| Configurable (default: all-MiniLM-L6-v2 local, recommended: Voyage Code 3) \|" | Update CocoIndex install guide to `sentence-transformers` + `google/embeddinggemma-300m`, `bf16`, 768 dims as the default. |
| P1 | traceability | `.opencode/skills/mcp-coco-index/SKILL.md:271` | 1 | "\| `voyage/voyage-code-3` (primary) \| Cloud via LiteLLM \| 1024 \| `VOYAGE_API_KEY` required \| Higher quality code search \|" | Remove `primary` from Voyage Code 3 and document it only as optional LiteLLM/cloud configuration. |
| P1 | maintainability | `.opencode/skills/mcp-coco-index/assets/config_templates.md:75` | 1 | "\"_NOTE_2\": \"Default embedding: all-MiniLM-L6-v2 (local, no API key needed)\"," | Refresh config templates so generated client configs do not reintroduce the MiniLM default. |
| P1 | traceability | `.opencode/skills/mcp-coco-index/references/settings_reference.md:43` | 1 | "\| `embedding.model`    \| string        \| `sentence-transformers/all-MiniLM-L6-v2`   \| Model identifier passed to the provider          \|" | Replace MiniLM defaults with `google/embeddinggemma-300m` and 768-dimensional expectations. |
| P1 | traceability | `.opencode/skills/system-spec-kit/mcp_server/ENV_REFERENCE.md:445` | 1 | "In `EMBEDDINGS_PROVIDER=auto`, the runtime prefers Voyage `voyage-4` (1024 dims) when `VOYAGE_API_KEY` is present, then OpenAI `text-embedding-3-small` (1536 dims) when `OPENAI_API_KEY` is present" | Rewrite the embedding default reference around the canonical hf-local default; keep cloud providers as explicit/optional fallback details. |
| P1 | traceability | `.opencode/skills/system-spec-kit/references/memory/embedding_resilience.md:23` | 1 | "\| Voyage Provider \| `shared/embeddings/providers/voyage.ts` \| Primary embedding provider \|" | Change the architecture table to name `hf-local` as the default provider and Voyage as optional/fallback. |
| P1 | traceability | `.opencode/skills/system-spec-kit/references/memory/embedding_resilience.md:48` | 1 | "│ 1. VOYAGE AI (Primary)                                            │" | Replace the fallback-order diagram with the post-014 resolver order and remove Voyage-as-primary language. |
| P1 | correctness | `opencode.json:29` | 1 | "\"_NOTE_3_EMBEDDINGS_PROVIDER\": \"Options: auto \| llama-cpp \| hf-local \| voyage \| openai. Default 'auto' resolves to llama-cpp; set EMBEDDINGS_PROVIDER=hf-local to opt out.\"," | Align OpenCode config note with `auto -> hf-local` for no-key startup. |
| P1 | traceability | `.opencode/skills/mcp-coco-index/INSTALL_GUIDE.md:437` | 2 | "**Primary (recommended):** `voyage/voyage-code-3` via LiteLLM provider -- best code search quality." | Rewrite the CocoIndex model section so sentence-transformers + `google/embeddinggemma-300m` is primary/default. |
| P1 | traceability | `.opencode/skills/mcp-coco-index/README.md:76` | 2 | "`sentence-transformers/all-MiniLM-L6-v2` (local, no API key)" | Change the documented default model to `google/embeddinggemma-300m` with 768-dimensional expectations. |
| P1 | traceability | `.opencode/skills/mcp-coco-index/README.md:77` | 2 | "`voyage/voyage-code-3` via LiteLLM (1024-dim, requires `VOYAGE_API_KEY`)" | Remove `voyage-code-3` as the primary model and present it only as optional LiteLLM configuration if retained. |
| P1 | traceability | `.opencode/skills/system-spec-kit/README.md:361` | 2 | "Compares meaning via embeddings (Voyage AI 1024d)" | Update the hybrid-search channel docs to name hf-local EmbeddingGemma q8 768d as the Memory MCP default. |
| P1 | traceability | `.opencode/skills/system-spec-kit/README.md:691` | 2 | "Recommended. Best retrieval quality. Requires `VOYAGE_API_KEY`" | Remove the recommendation from the provider table and mark Voyage as optional/fallback. |
| P1 | traceability | `.opencode/skills/system-spec-kit/mcp_server/README.md:46` | 2 | "The default `auto` cascade is cloud key when configured, then `llama-cpp` when the local GGUF runtime is available, then `hf-local` as the local fallback." | Rewrite provider resolution docs so `auto` falls through to hf-local when no API keys are present; document `llama-cpp` as explicit opt-in only. |
| P1 | traceability | `.opencode/skills/system-spec-kit/mcp_server/README.md:52` | 2 | "`EMBEDDINGS_PROVIDER=auto` resolves to `llama-cpp` after cloud providers." | Remove the auto-to-llama-cpp claim and describe synchronous migration only for explicit `EMBEDDINGS_PROVIDER=llama-cpp`. |
| P1 | traceability | `.opencode/skills/system-spec-kit/shared/README.md:340` | 2 | "`HF_EMBEDDINGS_MODEL` ... `nomic-ai/nomic-embed-text-v1.5`" | Replace the stale HF model default with `onnx-community/embeddinggemma-300m-ONNX`, q8, 768 dims. |
| P1 | traceability | `.opencode/skills/system-spec-kit/shared/README.md:345` | 2 | "Auto-detection: Voyage if `VOYAGE_API_KEY` exists (recommended)" | Align provider precedence with post-014 behavior and remove the Voyage recommendation. |
| P1 | traceability | `README.md:139` | 2 | "# Option A: Voyage AI (recommended - best quality)" | Update the root quick start to lead with hf-local EmbeddingGemma defaults and move Voyage to optional explicit setup. |
| P1 | traceability | `README.md:517` | 2 | "- **Voyage AI** - Set `VOYAGE_API_KEY` env var. Best quality, recommended." | Remove Voyage-as-recommended wording from the embedding provider feature list. |
| P1 | traceability | `README.md:828` | 2 | "Semantic code search via vector embeddings (Voyage Code 3 and All-MiniLM-L6-v2 models)" | Replace the CocoIndex model summary with `google/embeddinggemma-300m` / sentence-transformers / bf16 / 768 dims. |
| P1 | maintainability | `.opencode/skills/mcp-coco-index/INSTALL_GUIDE.md:354` | 3 | `"_NOTE_2_EMBEDDING": "Default: all-MiniLM-L6-v2 (local, no API key needed)",` | Refresh the OpenCode install-guide config sample so copied configs do not reintroduce MiniLM. |
| P1 | maintainability | `.opencode/skills/mcp-coco-index/INSTALL_GUIDE.md:409` | 3 | `"_NOTE_2_EMBEDDING": "Default: all-MiniLM-L6-v2 (local, no API key needed)",` | Refresh the Claude install-guide config sample to the canonical CocoIndex EmbeddingGemma default. |
| P1 | maintainability | `.opencode/skills/mcp-coco-index/INSTALL_GUIDE.md:429` | 3 | `_NOTE_2 = "Default embedding: all-MiniLM-L6-v2 (local, no API key needed)"` | Refresh the Codex install-guide config sample to the canonical CocoIndex EmbeddingGemma default. |
| P1 | maintainability | `.opencode/skills/mcp-coco-index/assets/config_templates.md:140` | 3 | `"_NOTE_2": "Default embedding: all-MiniLM-L6-v2 (local, no API key needed)",` | Update the Claude MCP config template to advertise `google/embeddinggemma-300m`, bf16, 768 dims as the CocoIndex default. |
| P1 | maintainability | `.opencode/skills/mcp-coco-index/assets/config_templates.md:160` | 3 | `_NOTE_2 = "Default embedding: all-MiniLM-L6-v2 (local, no API key needed)"` | Update the Codex config template note to the post-014 EmbeddingGemma default. |
| P1 | correctness | `.claude/mcp.json:17` | 4 | `"_NOTE_1_DATABASE": "Default DB: context-index__llama-cpp__unsloth-embeddinggemma-300m-gguf__768__q8.sqlite (auto-derived from provider+model+dim+dtype; hf-local ONNX q8 is the fallback).",` | Align the Claude MCP default database note with post-014 hf-local defaults and remove llama-cpp-as-default wording. |
| P1 | correctness | `.codex/config.toml:21` | 4 | `_NOTE_1_DATABASE = "Default DB: context-index__llama-cpp__unsloth-embeddinggemma-300m-gguf__768__q8.sqlite (auto-derived from provider+model+dim+dtype; hf-local ONNX q8 is the fallback)."` | Update the Codex MCP config note so the default database is the hf-local EmbeddingGemma ONNX q8 profile; keep llama-cpp as explicit opt-in only. |
| P1 | correctness | `.gemini/settings.json:34` | 4 | `"_NOTE_1_DATABASE": "Default DB: context-index__llama-cpp__unsloth-embeddinggemma-300m-gguf__768__q8.sqlite (auto-derived from provider+model+dim+dtype; hf-local ONNX q8 is the fallback).",` | Align the Gemini MCP config note with the hf-local EmbeddingGemma default database profile. |
| P1 | correctness | `.opencode/skills/system-spec-kit/mcp_server/lib/eval/memory-state-baseline.ts:65` | 4 | `return path.resolve(DEFAULT_DB_DIR, CONTEXT_DB_FILENAME);` | Stop defaulting baseline capture to `context-index.sqlite`; derive the current Memory MCP profile path unless an explicit override is supplied. |
| P1 | correctness | `.opencode/skills/system-spec-kit/scripts/evals/map-ground-truth-ids.ts:34` | 4 | `const DB_PATH = path.join(DB_DIR, 'context-index.sqlite');` | Resolve the active Memory MCP database from the current embedding profile instead of hardcoding the deleted generic sqlite filename. |
| P1 | correctness | `.opencode/skills/system-spec-kit/scripts/evals/run-ablation.ts:50` | 4 | `const PROD_DB_PATH = path.join(DB_DIR, 'context-index.sqlite');` | Use the profile-derived post-014 sqlite path for ablation runs so evaluation reads the active hf-local EmbeddingGemma store. |
| P1 | correctness | `.opencode/skills/system-spec-kit/scripts/evals/run-bm25-baseline.ts:39` | 4 | `const PROD_DB_PATH = path.join(DB_DIR, 'context-index.sqlite');` | Replace the hardcoded generic database path with the active profile database resolver. |
| P1 | correctness | `.opencode/skills/system-spec-kit/scripts/memory/cleanup-index-scope-violations.ts:77` | 4 | `new URL('../../../mcp_server/database/context-index__voyage__voyage-4__1024.sqlite', import.meta.url),` | Make the cleanup script target the active profile database or require an explicit `--db` path; do not hardcode the old Voyage 1024 store. |
| P1 | correctness | `.opencode/skills/system-spec-kit/shared/embeddings.ts:868` | 4 | `export const DEFAULT_MODEL_NAME: string = 'nomic-ai/nomic-embed-text-v1.5';` | Change the legacy facade fallback to `onnx-community/embeddinggemma-300m-ONNX` with q8/768 expectations, or remove the fallback if factory profiles are now the only source of truth. |
| P1 | correctness | `.opencode/skills/system-spec-kit/shared/embeddings/profile.ts:79` | 4 | "return \`${baseDir}/context-index.sqlite\`;" | Remove or quarantine the legacy generic sqlite filename branch so all active Memory MCP profiles use filename-keyed `context-index__<profile>.sqlite` paths. |
| P1 | correctness | `opencode.json:27` | 4 | `"_NOTE_1_DATABASE": "Default DB: context-index__llama-cpp__unsloth-embeddinggemma-300m-gguf__768__q8.sqlite (auto-derived from provider+model+dim+dtype; hf-local ONNX q8 is the fallback).",` | Align the OpenCode MCP config note with the hf-local EmbeddingGemma default database profile. |
| P1 | traceability | `.opencode/install_guides/README.md:489` | 5 | "- **Voyage** (recommended if you have `VOYAGE_API_KEY`). Best retrieval quality, cloud embeddings" | Remove recommended wording from the install guide and lead with hf-local EmbeddingGemma defaults. |
| P1 | traceability | `.opencode/install_guides/README.md:634` | 5 | "\| **Voyage** \| Recommended, best quality \| 1024 \| `VOYAGE_API_KEY` \|" | Rewrite the provider table so hf-local q8 768d is the default row and Voyage is optional. |
| P1 | traceability | `.opencode/install_guides/README.md:643` | 5 | "- If `VOYAGE_API_KEY` set + `EMBEDDINGS_PROVIDER=voyage`: uses Voyage (recommended, 8% better retrieval)" | Remove the quantified recommendation and frame explicit Voyage selection as opt-in only. |
| P1 | traceability | `.opencode/install_guides/README.md:675` | 5 | "export OPENAI_EMBEDDINGS_MODEL=text-embedding-3-small  # Default" | Replace the default export example with canonical hf-local model/dtype/dims, leaving OpenAI as optional setup if documented. |
| P1 | traceability | `.opencode/skills/mcp-coco-index/INSTALL_GUIDE.md:619` | 5 | "\| **Default**     \| all-MiniLM-L6-v2 (local, no API key, 384 dimensions)                       \|" | Replace the CocoIndex summary default with `sentence-transformers` + `google/embeddinggemma-300m`, bf16, 768 dims. |
| P1 | traceability | `.opencode/skills/system-spec-kit/feature_catalog/19--feature-flag-reference/05-5-embedding-and-api.md:23` | 5 | "\| `EMBEDDING_DIM` \| _(provider default)_ \| number \| `lib/search/vector-index-store.ts`, `shared/embeddings/factory.ts` \| Compatibility check and startup override for the stored vector dimension. Any positive explicit `EMBEDDING_DIM` value is honored first; otherwise runtime dimension selection comes from the active provider profile (Voyage 1024, OpenAI 1536, local 768 fallback). \|" | Update the feature-flag reference to make 768d hf-local EmbeddingGemma the default profile and cloud dimensions optional. |
| P1 | traceability | `.opencode/skills/system-spec-kit/feature_catalog/19--feature-flag-reference/05-5-embedding-and-api.md:24` | 5 | "\| `EMBEDDINGS_PROVIDER` \| `'auto'` \| string \| `shared/embeddings/factory.ts` \| Selects the embedding provider. Valid values include `'auto'`, `'openai'`, `'hf-local'`, and `'voyage'`. In `'auto'` mode, resolution precedence is explicit `EMBEDDINGS_PROVIDER` -> `VOYAGE_API_KEY` -> `OPENAI_API_KEY` -> local fallback. \|" | Align the documented `auto` precedence with post-014 resolution and keep cloud providers out of the default narrative. |
| P1 | traceability | `.opencode/skills/system-spec-kit/references/config/environment_variables.md:46` | 5 | "2. `VOYAGE_API_KEY` detected (auto-selects Voyage)" | Rework provider-selection docs around `auto -> hf-local` when no keys are present and explicit opt-in for cloud providers. |
| P1 | traceability | `README.md:1257` | 5 | "- **`VOYAGE_API_KEY`** (optional) - Voyage AI embeddings (recommended)" | Remove the recommendation and describe Voyage only as an optional cloud provider. |
| P1 | traceability | `README.md:1261` | 5 | "Default repo-local database path: `.opencode/skills/system-spec-kit/mcp_server/database/context-index.sqlite`" | Replace the generic sqlite path with the derived hf-local EmbeddingGemma profile filename or profile-derived directory guidance. |
| P1 | traceability | `README.md:1289` | 5 | "- **Paths** - The checked-in configs default to `.opencode/skills/system-spec-kit/mcp_server/database/context-index.sqlite`. If a runtime cannot write inside the repo, override `MEMORY_DB_PATH` (and, when relevant, `SPEC_KIT_DB_DIR`) to a writable location." | Update the checked-in config path description to say the sqlite filename is derived from provider, model, dims, and dtype. |
| P1 | traceability | `README.md:1434` | 5 | "- **[→ Voyage AI](https://www.voyageai.com/)** - Recommended embedding provider" | Change the external-resource label so hf-local is the default and Voyage is optional/fallback. |
| P1 | maintainability | `.opencode/skills/mcp-coco-index/mcp_server/pyproject.toml:51` | 6 | "Default install (`pip install -e .`) routes through LiteLLM (Voyage AI etc.) only." | Align package dependency guidance with the post-014 CocoIndex default: sentence-transformers plus `google/embeddinggemma-300m`, with LiteLLM/Voyage as explicit opt-in. |
| P1 | correctness | `.claude/mcp.json:18` | 7 | "\"_NOTE_2_PROVIDERS\": \"Auto cascade: VOYAGE_API_KEY -> OPENAI_API_KEY -> llama-cpp (GGUF Q8_0 + Metal) -> hf-local (ONNX q8 fallback).\"" | Align the Claude MCP provider note with post-014 resolution and remove `llama-cpp` from the implicit auto cascade. |
| P1 | correctness | `.claude/mcp.json:21` | 7 | "\"_NOTE_5_GET_VOYAGE_KEY\": \"Get Voyage key: https://dash.voyageai.com/api-keys (recommended, 8% better than OpenAI)\"," | Remove the Voyage recommendation from the Claude MCP config note and avoid unsupported quality claims in defaults guidance. |
| P1 | correctness | `.codex/config.toml:22` | 7 | "_NOTE_2_PROVIDERS = \"Auto cascade: VOYAGE_API_KEY -> OPENAI_API_KEY -> llama-cpp (GGUF Q8_0 + Metal) -> hf-local (ONNX q8 fallback).\"" | Rewrite the Codex provider note so `auto` falls through to `hf-local` when no API keys are present, with `llama-cpp` documented only as explicit opt-in. |
| P1 | correctness | `.codex/config.toml:25` | 7 | "_NOTE_5_GET_VOYAGE_KEY = \"Get Voyage key: https://dash.voyageai.com/api-keys (recommended, 8% better than OpenAI)\"" | Remove the Voyage recommendation from the Codex runtime config note; keep Voyage only as optional explicit provider setup. |
| P1 | correctness | `.gemini/settings.json:35` | 7 | "\"_NOTE_2_PROVIDERS\": \"Auto cascade: VOYAGE_API_KEY -> OPENAI_API_KEY -> llama-cpp (GGUF Q8_0 + Metal) -> hf-local (ONNX q8 fallback).\"" | Align the Gemini MCP provider note with the canonical hf-local default path. |
| P1 | correctness | `.gemini/settings.json:38` | 7 | "\"_NOTE_5_GET_VOYAGE_KEY\": \"Get Voyage key: https://dash.voyageai.com/api-keys (recommended, 8% better than OpenAI)\"," | Remove Voyage-as-recommended wording from the Gemini config note. |
| P1 | correctness | `.opencode/skills/system-spec-kit/mcp_server/scripts/migrations/create-checkpoint.ts:37` | 7 | "path.resolve(process.cwd(), '.opencode/skills/system-spec-kit/mcp_server/database/context-index.sqlite')," | Resolve checkpoint source DBs through the active embedding profile path instead of hardcoded legacy `context-index.sqlite` candidates. |
| P1 | correctness | `.opencode/skills/system-spec-kit/mcp_server/scripts/migrations/restore-checkpoint.ts:36` | 7 | "path.resolve(process.cwd(), '.opencode/skills/system-spec-kit/mcp_server/database/context-index.sqlite')," | Resolve restore target/source DBs through the active embedding profile path instead of hardcoded legacy `context-index.sqlite` candidates. |
| P1 | correctness | `.opencode/skills/system-spec-kit/scripts/install-llama-cpp.sh:83` | 7 | "log_pass \"llama-cpp embeddings ready. Default auto mode can now use llama-cpp.\"" | Change the installer success message to say `llama-cpp` is available for explicit `EMBEDDINGS_PROVIDER=llama-cpp`, not default auto mode. |
| P1 | correctness | `opencode.json:28` | 7 | "\"_NOTE_2_PROVIDERS\": \"Auto cascade: VOYAGE_API_KEY -> OPENAI_API_KEY -> llama-cpp (GGUF Q8_0 + Metal) -> hf-local (ONNX q8 fallback).\"" | Align the OpenCode MCP provider note with `auto -> hf-local` for no-key startup and explicit-only `llama-cpp`. |
| P1 | correctness | `opencode.json:31` | 7 | "\"_NOTE_5_GET_VOYAGE_KEY\": \"Get Voyage key: https://dash.voyageai.com/api-keys (recommended, 8% better than OpenAI)\"," | Remove Voyage-as-recommended wording from the OpenCode config note. |
| P1 | traceability | `.mcp.json:22` | 8 | "\"_NOTE_5_GET_VOYAGE_KEY\": \"Get Voyage key: https://dash.voyageai.com/api-keys (recommended, 8% better than OpenAI)\"," | Remove the Voyage recommendation and quality claim from the root MCP config note; keep Voyage only as optional explicit-provider setup. |
| P1 | traceability | `.opencode/install_guides/README.md:1344` | 8 | "sqlite3 .opencode/skills/system-spec-kit/mcp_server/dist/database/context-index.sqlite \"SELECT COUNT(*) FROM memory_index;\"" | Change troubleshooting commands to resolve or show the active profile-derived Memory MCP sqlite file instead of the deleted generic database. |
| P1 | traceability | `.opencode/skills/mcp-coco-index/INSTALL_GUIDE.md:620` | 8 | "\| **Recommended** \| Voyage Code 3 via LiteLLM (API key required, best code search quality)      \|" | Remove the Voyage recommendation from the install-guide summary and set EmbeddingGemma as the default/recommended local setup. |
| P1 | traceability | `.opencode/skills/mcp-coco-index/INSTALL_GUIDE.md:843` | 8 | "- **Default Embedding**: [all-MiniLM-L6-v2](https://huggingface.co/sentence-transformers/all-MiniLM-L6-v2)" | Replace the external-resource default link with `google/embeddinggemma-300m` or remove the stale default label. |
| P1 | traceability | `.opencode/skills/mcp-coco-index/README.md:160` | 8 | "The local model (`all-MiniLM-L6-v2`) requires no API key and works offline, making it the right default for most projects." | Update the CocoIndex README narrative to make `google/embeddinggemma-300m` the local default and remove MiniLM-as-default guidance. |
| P1 | traceability | `.opencode/skills/mcp-coco-index/README.md:276` | 8 | "model: all-MiniLM-L6-v2              # or voyage/voyage-code-3" | Refresh the global settings YAML sample to use `google/embeddinggemma-300m` and keep Voyage as an explicit alternate. |
| P1 | traceability | `.opencode/skills/mcp-coco-index/references/settings_reference.md:127` | 8 | "\| `sentence-transformers/all-MiniLM-L6-v2` \| sentence-transformers \| None \| 384 \| Local, offline, current default \|" | Replace the MiniLM current-default row with `google/embeddinggemma-300m`, bf16, 768 dimensions. |
| P1 | traceability | `.opencode/skills/mcp-coco-index/references/settings_reference.md:47` | 8 | "### Example: Voyage Code 3 (recommended)" | Lead the settings reference with the canonical sentence-transformers `google/embeddinggemma-300m` default; mark Voyage Code 3 as optional LiteLLM setup. |
| P1 | traceability | `.opencode/skills/system-spec-kit/README.md:146` | 8 | "\| Embedding API \| None (HuggingFace local) \| Voyage AI recommended for best quality \|" | Update the requirements table to state hf-local EmbeddingGemma is the default and cloud providers are optional. |
| P1 | traceability | `.opencode/skills/system-spec-kit/README.md:699` | 8 | "\| `VOYAGE_API_KEY`     \| Recommended \| Voyage AI embeddings (1024d, best retrieval quality) \|" | Replace the recommended Voyage env row with canonical hf-local defaults and move Voyage into an optional cloud-provider section. |
| P1 | traceability | `.opencode/skills/system-spec-kit/README.md:720` | 8 | "\"VOYAGE_API_KEY\": \"your-key-here\"" | Change the generic MCP server configuration example so it works without a Voyage key and reflects `EMBEDDINGS_PROVIDER=auto` falling through to hf-local. |
| P1 | traceability | `.opencode/skills/system-spec-kit/mcp_server/INSTALL_GUIDE.md:113` | 8 | "\| `.opencode/skills/system-spec-kit/mcp_server/database/context-index.sqlite` \| Default repo-local memory database used by the checked-in configs \|" | Update the runtime-path table to describe profile-derived sqlite filenames, with hf-local EmbeddingGemma q8 768d as the default profile. |
| P1 | traceability | `.opencode/skills/system-spec-kit/mcp_server/INSTALL_GUIDE.md:116` | 8 | "local fallback stays on `context-index.sqlite`, while Voyage and OpenAI profiles get their own profile-specific filenames in the same directory." | Remove the claim that the local fallback keeps the legacy generic filename; document the filename-keyed hf-local default. |
| P1 | traceability | `.opencode/skills/system-spec-kit/shared/README.md:198` | 8 | "│       ├── voyage.ts           # Voyage AI (recommended)" | Rename the provider-tree annotation so `voyage.ts` is optional/fallback, not recommended. |
| P1 | correctness | `.mcp.json:20` | 10 | `"_NOTE_3_EMBEDDINGS_PROVIDER": "Current: 'auto' (VOYAGE_API_KEY -> OPENAI_API_KEY -> hf-local). Options: 'auto', 'voyage', 'openai', 'hf-local'",` | Align the root MCP config note with the canonical hf-local default and present cloud providers as explicit/optional, not the visible `auto` cascade. |
| P1 | correctness | `.opencode/skills/system-spec-kit/scripts/setup/install.sh:195` | 10 | `"_NOTE_3_CLOUD_PROVIDERS": "In auto mode: VOYAGE_API_KEY selects Voyage embeddings + rerank-2.5, OPENAI_API_KEY selects OpenAI, otherwise HF local fallback stays active",` | Update the installer-generated config note so new configs lead with `auto -> hf-local` for no-key startup and keep cloud providers as explicit opt-in guidance. |
| P1 | correctness | `.opencode/skills/system-spec-kit/scripts/setup/install.sh:253` | 10 | `local local_fallback_db_path="${canonical_db_dir}/context-index.sqlite"` | Replace the legacy generic sqlite probe with active-profile database resolution, or remove the probe and only report the derived database directory. |
| P1 | correctness | `.opencode/skills/system-spec-kit/shared/embeddings/factory.ts:145` | 10 | `const LLAMA_CPP_INSTALL_HINT = "Run 'bash .opencode/skills/system-spec-kit/scripts/install-llama-cpp.sh' to enable the faster default.";` | Change the runtime hint to say the installer enables explicit `EMBEDDINGS_PROVIDER=llama-cpp`; hf-local remains the default no-key profile. |

## 9. Top P2 Findings (advisories)
| Severity | Dimension | File:Line | Evidence | Recommendation |
|---|---|---|---|---|
| P2 | maintainability | `.opencode/skills/mcp-coco-index/feature_catalog/03--indexing-pipeline/04-embedding-provider-selection.md:23` | `Default user settings choose sentence-transformers/all-MiniLM-L6-v2.` | Regenerate the feature-catalog current-reality section to name `sentence-transformers` + `google/embeddinggemma-300m`. |
| P2 | maintainability | `.opencode/skills/mcp-coco-index/manual_testing_playbook/03--configuration/001-default-model-verification.md:42` | `embedding.model matches a documented model such as sentence-transformers/all-MiniLM-L6-v2 or voyage/voyage-code-3` | Regenerate the per-scenario CFG-001 file with the post-014 default model and dimensions. |
| P2 | maintainability | `.opencode/skills/mcp-coco-index/manual_testing_playbook/manual_testing_playbook.md:301` | `embedding.model matches a documented model such as the default local sentence-transformers/all-MiniLM-L6-v2 or a Lit... | Regenerate CFG-001 playbook text so manual verification checks for `google/embeddinggemma-300m` instead of accepting stale defaults. |
| P2 | maintainability | `.opencode/skills/mcp-coco-index/tests/test_config.py:49` | `assert "all-MiniLM-L6-v2" in config.embedding_model` | Update the env-derived config test to assert the canonical EmbeddingGemma model. |
| P2 | maintainability | `.opencode/skills/mcp-coco-index/tests/test_settings.py:50` | `assert "all-MiniLM-L6-v2" in s.embedding.model` | Update the default settings test to assert `google/embeddinggemma-300m`. |
| P2 | maintainability | `.opencode/skills/system-spec-kit/mcp_server/tests/fixtures/sample-memories.json:77` | `"content": "Vector embeddings use 384 dimensions for semantic search",` | Update fixture content to 768 dimensions or make it model-neutral so tests do not preserve MiniLM assumptions. |
| P2 | maintainability | `.opencode/skills/system-spec-kit/mcp_server/tests/fixtures/similarity-test-cases.json:139` | `"embeddingModel": "all-MiniLM-L6-v2",` | Update similarity fixture metadata to EmbeddingGemma and 768 dimensions, or remove model-specific metadata if not required. |
| P2 | maintainability | `.opencode/skills/mcp-coco-index/feature_catalog/08--configuration/01-user-settings.md:23` | "The default user settings use the local sentence-transformers provider and `all-MiniLM-L6-v2`." | Regenerate the feature catalog entry so the default model is `google/embeddinggemma-300m` with 768-dimensional expectations. |
| P2 | maintainability | `.opencode/skills/mcp-coco-index/feature_catalog/feature_catalog.md:681` | "The default user settings use the local sentence-transformers provider and `all-MiniLM-L6-v2`." | Regenerate the aggregate feature catalog after updating the canonical per-feature entry. |
| P2 | maintainability | `.opencode/skills/system-spec-kit/manual_testing_playbook/23--doctor-commands/323-doctor-memory-fresh-install.md:22` | "Exact command sequence: create disposable workspace -> confirm `mcp_server/database/context-index.sqlite` is absent ... | Regenerate the doctor-memory fresh-install playbook to validate profile-derived sqlite filenames, not the legacy generic path. |
| P2 | maintainability | `.opencode/skills/system-spec-kit/manual_testing_playbook/23--doctor-commands/327-doctor-memory-disk-pressure.md:43` | "free space is less than `2 * (context-index.sqlite bytes + context-index__voyage__voyage-4__1024.sqlite bytes)`." | Update disk-pressure fixtures to calculate space against the active hf-local profile DB and optional explicit-provider DBs, not the old Voyage 1024... |
| P2 | maintainability | `.opencode/skills/system-spec-kit/mcp_server/tests/embeddings.vitest.ts:257` | "expect(profile.model).toBe('nomic-ai/nomic-embed-text-v1.5');" | Update the no-key startup-profile test to assert `onnx-community/embeddinggemma-300m-ONNX`, q8, 768 dims. |
| P2 | maintainability | `.opencode/skills/system-spec-kit/scripts/tests/fixtures/manual-playbook-fixture.ts:324` | "const dbPath = path.join(dbDir, 'context-index.sqlite');" | Derive the fixture database filename from the active embedding profile instead of preserving the deleted generic sqlite filename. |
| P2 | maintainability | `.opencode/skills/system-spec-kit/scripts/tests/memory-pipeline-regressions.vitest.ts:111` | "expect(embeddings.MODEL_NAME).toBe('nomic-ai/nomic-embed-text-v1.5');" | Update the regression expectation to the canonical hf-local ONNX EmbeddingGemma model, or make the assertion profile-driven. |
| P2 | maintainability | `.opencode/skills/system-spec-kit/scripts/tests/test-embeddings-behavioral.js:242` | "assertEqual(emb.MODEL_NAME, 'nomic-ai/nomic-embed-text-v1.5', 'EB-003: MODEL_NAME is nomic default');" | Refresh the behavioral test constants and labels so they lock the post-014 hf-local default instead of the old Nomic default. |
| P2 | maintainability | `.opencode/skills/system-spec-kit/scripts/tests/test-embeddings-factory.js:46` | "model: 'nomic-ai/nomic-embed-text-v1.5'," | Replace the fixture profile model with the canonical hf-local ONNX EmbeddingGemma identifier. |
| P2 | correctness | `.opencode/skills/system-spec-kit/package.json:39` | "\"onnxruntime-node\": {" | Remove or justify the explicit `onnxruntime-node` package metadata after the ONNX runtime backend rejection; keep only dependencies required by the... |
| P2 | correctness | `.opencode/skills/system-spec-kit/scripts/setup/check-native-modules.sh:63` | "# Probe onnxruntime-node (optional, may not be installed)" | Drop the native-module probe for the rejected ONNX runtime backend, or gate it behind an explicit supported backend mode. |
| P2 | correctness | `.opencode/skills/system-spec-kit/shared/embeddings/providers/hf-local.ts:309` | "console.error('[hf-local] 1. npm rebuild onnxruntime-node sharp');" | Update hf-local recovery guidance so it does not tell users to rebuild the rejected `onnxruntime-node` backend. |
| P2 | maintainability | `.opencode/skills/mcp-coco-index/SKILL.md:8` | "voyage-code-3, all-MiniLM-L6-v2" | Refresh the skill keyword metadata so routing/search hints name `google/embeddinggemma-300m` and do not keep MiniLM/Voyage as stale default-era anc... |
| P2 | maintainability | `.opencode/skills/mcp-coco-index/feature_catalog/feature_catalog.md:293` | "Default user settings choose `sentence-transformers/all-MiniLM-L6-v2`." | Regenerate the aggregate CocoIndex feature catalog after updating the source entry so the generated asset reflects the EmbeddingGemma default. |
| P2 | maintainability | `.opencode/skills/mcp-coco-index/references/tool_reference.md:400` | "`embedding.model` -- embedding model name (e.g., `all-MiniLM-L6-v2`, `gemini/text-embedding-004`)" | Update the tool-reference example to `google/embeddinggemma-300m` or make it provider-neutral so examples do not preserve the removed MiniLM model. |
| P2 | maintainability | `.opencode/skills/system-spec-kit/feature_catalog/16--tooling-and-scripts/21-eval-runner-cli.md:28` | "The script targets the production memory database at `mcp_server/database/context-index.sqlite`." | Regenerate the eval-runner catalog entry to describe profile-derived Memory MCP sqlite paths rather than the deleted generic database filename. |
| P2 | maintainability | `.opencode/skills/system-spec-kit/feature_catalog/16--tooling-and-scripts/29-setup-native-module-health-and-mcp-installation.md:32` | "attempts to load `better-sqlite3` plus optional `onnxruntime-node` and `sharp` installs" | Regenerate the native-module health catalog entry so it no longer documents probing the rejected ONNX runtime backend. |
| P2 | maintainability | `.opencode/skills/system-spec-kit/feature_catalog/16--tooling-and-scripts/31-evaluation-benchmark-and-import-policy-tooling.md:25` | "`run-ablation.ts` is the controlled channel-ablation CLI. It requires `SPECKIT_ABLATION=true`, opens the production ... | Refresh the benchmark/import-policy catalog so ablation guidance resolves the active hf-local EmbeddingGemma profile DB. |
| P2 | maintainability | `.opencode/skills/system-spec-kit/feature_catalog/16--tooling-and-scripts/31-evaluation-benchmark-and-import-policy-tooling.md:31` | "`map-ground-truth-ids.ts` is the read-only ground-truth provenance helper. It opens the production `context-index.sq... | Update the generated provenance-helper description to use the profile-derived database path instead of the legacy generic sqlite file. |
| P2 | maintainability | `.opencode/skills/system-spec-kit/manual_testing_playbook/23--doctor-commands/325-doctor-memory-long-pole-rebuild.md:41` | "`test -s .opencode/skills/system-spec-kit/mcp_server/database/context-index.sqlite`" | Regenerate the long-pole rebuild playbook so preconditions and snapshot checks use the active profile-derived sqlite filename. |
| P2 | maintainability | `.opencode/skills/system-spec-kit/manual_testing_playbook/23--doctor-commands/326-doctor-memory-sigint-cancellation.md:46` | "`shasum .opencode/skills/system-spec-kit/mcp_server/database/context-index__voyage__voyage-4__1024.sqlite`" | Replace the required Voyage 1024 checksum fixture with the hf-local EmbeddingGemma default DB, leaving Voyage only as explicit-provider optional co... |
| P2 | maintainability | `.opencode/skills/system-spec-kit/scripts/tests/test-utils.js:142` | "* @returns {Array} 384-dimensional embedding vector" | Update the shared test helper to generate/document 768-dimensional vectors, or make the dimension profile-driven to avoid locking in MiniLM-era ass... |
| P2 | correctness | `.opencode/skills/system-spec-kit/mcp_server/package.json:58` | `"onnxruntime-common": "^1.21.0",` | Remove or justify the MCP server's direct `onnxruntime-common` dependency so package manifests do not preserve rejected-backend residue. |
| P2 | correctness | `.opencode/skills/system-spec-kit/package.json:50` | `"onnxruntime-common": "^1.21.0"` | Remove or justify the direct `onnxruntime-common` dependency after the ONNX runtime backend rejection; keep only dependencies required by the accep... |

## 10. Recommendation
Scaffold 022-local-llm-legacy-remediation packet.

Top batch-able remediation groups:
- Provider resolution: remove implicit `llama-cpp` from Memory MCP `auto` no-key selection and fix runtime hints that call it the faster/default path.
- Runtime config notes: align `.codex`, `.claude`, `.gemini`, `.mcp.json`, and `opencode.json` database/provider/key guidance with hf-local as canonical default.
- Database path handling: replace generic `context-index.sqlite` and hardcoded Voyage profile paths in eval, checkpoint, setup, cleanup, and generated docs with profile-derived database resolution.
- CocoIndex defaults: update README, install guide, settings reference, templates, feature catalogs, tests, and playbooks from MiniLM/Voyage defaults to `google/embeddinggemma-300m` bf16 768d.
- Memory MCP docs and fixtures: update README/reference/install-guide/manual-testing/catalog/test fixture surfaces from Voyage, Nomic, OpenAI, 384d, generic sqlite, and rejected ONNX backend residue.
- Generated assets: regenerate feature catalogs and manual testing playbooks after source defaults are corrected.

## Appendix A. Excluded as historical context
The iteration files did not provide file:line rows for historical exclusions. The following source-authored notes were not counted in the main tables.

| Iteration | Excluded as historical context |
|---:|---|
| 1 | 5 |
| 2 | 4 |
| 2 | Additional stale CocoIndex MiniLM/Voyage references remain in README/install-guide/settings-reference/feature-catalog clusters; this iteration sampled distinct user-facing claims while avoiding duplicate flags from iteration 001. Not flagged: Qwen mentions in `cli-opencode` provider docs, Voyage in `CONTRIBUTING.md` example PR text, optional Narsil Voyage key comments, and feature-specific `node-llama-cpp` reranker playbook entries. |
| 3 | 6 |
| 3 | Prior iteration findings in root README, System Spec Kit README/shared README, ENV_REFERENCE, settings_reference, SKILL.md, and the known 014 migration narrative were skipped. Active fallback/provider-registry code, explicit Voyage/OpenAI fallback tests, doctor provider-detection references, and the current review packet were treated as intentional or out of scope. |
| 4 | 8 |
| 4 | Saturation is starting in docs/templates, but correctness residue remains in executable eval/cleanup scripts and legacy embedding facades. I did not flag the intentional Voyage/OpenAI fallback provider registry, explicit llama-cpp provider implementation, regression tests for fallback behavior, the 014 migration narrative, review-packet artifacts, code-graph/spec-memory rows, CocoIndex's already-canonical `default_user_settings()`, or generic `context-index.sqlite` test fixtures. |
| 5 | 10 |
| 5 | Fresh traceability residue remains in root README, install guides, feature-flag references, and CocoIndex guide summaries. The scan intentionally skipped this review packet, 014 migration history, doctor route provider-detection text, fallback-chain code, and old spec/graph metadata rows. |
| 6 | 7 |
| 7 | 5 |
| 7 | Skipped the 014 migration narrative, the 015 review packet, the living provider fallback factory findings already reported in iteration 001, Qwen prompt-registry opt-in entries, and fallback-path regression tests. No saturation yet; correctness residue remains concentrated in runtime config notes, setup/migration scripts, and native backend cleanup. |
| 8 | 8 |
| 8 | Traceability residue is now mostly duplicate-shaped around the same stale defaults, but still present in distinct user-facing surfaces. I treated changelog/release-note mentions, optional provider support without default language, Qwen model references unrelated to embeddings, package-lock dependency rows, and the 014/015 packet areas as non-findings. No P0 traceability finding surfaced in this pass. |
| 9 | 4 |
| 10 | 7 |
| 10 | Correctness is near saturation. Remaining unflagged hits were mostly optional provider code, backward-compat tests, Qwen registry entries kept for opt-in, package-lock transitive dependencies, local reranker `node-llama-cpp` references, and the intentional 014 migration/spec history. |
