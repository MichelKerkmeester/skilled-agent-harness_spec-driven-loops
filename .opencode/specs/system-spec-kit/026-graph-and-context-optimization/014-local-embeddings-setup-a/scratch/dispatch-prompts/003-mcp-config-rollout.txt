You are dispatched by Claude Code main-agent via cli-opencode for sub-phase 003 of packet 014-local-embeddings-setup-a. Single-shot. No sub-agent dispatch. No SpawnAgent. Spec folder pre-approved (Gate 3 = option A existing).

═══════════════════════════════════════════════════════════════════
SUB-PHASE 003 — MCP CONFIG ROLLOUT (4 RUNTIMES) (Level 1)
═══════════════════════════════════════════════════════════════════

PACKET FOLDER:
.opencode/specs/system-spec-kit/026-graph-and-context-optimization/014-local-embeddings-setup-a/003-mcp-config-rollout/

CONTEXT
Sub-phases 001 + 002 are complete (prefix registry shipped; models downloaded). Now wire the env vars into all 4 MCP runtime configs so EVERY runtime (Claude Code, OpenCode, Codex, Gemini) uses local embeddings. Remove `VOYAGE_API_KEY` so factory auto-detect can't re-prefer Voyage.

═══════════════════════════════════════════════════════════════════
PER-RUNTIME CONFIG PATCHES
═══════════════════════════════════════════════════════════════════

Read EACH config file first to learn its exact schema (env-block key may differ: `env` vs `environment`; key path may differ). Don't guess — pattern-match against existing MCP server entries.

| Runtime | Path | Format |
|---|---|---|
| Claude Code | `~/.claude.json` | JSON |
| OpenCode | `<repo>/opencode.json` | JSON |
| Codex CLI | `<repo>/.codex/config.toml` | TOML |
| Gemini CLI | `<repo>/.gemini/settings.json` | JSON |

Locate the `spec-kit-memory` (or similarly-named) and `cocoindex-code` (or similarly-named) MCP server entries in each file. Add/update their env block with:

**spec-kit-memory env additions:**
```
EMBEDDINGS_PROVIDER=hf-local
HF_EMBEDDINGS_MODEL=google/embeddinggemma-300m
EMBEDDING_DIM=768
```

**cocoindex-code env additions:**
```
COCOINDEX_CODE_EMBEDDING_MODEL=sbert/Qwen/Qwen3-Embedding-4B
```

**CRITICAL SAFETY STEP:** also REMOVE or null out `VOYAGE_API_KEY` from EVERY config (spec-kit-memory section). Setting to empty string `""` is equivalent to removing for factory.ts:327 detection logic.

**Note on fallback (if 002 triggered model fallback):** If sub-phase 002's implementation-summary.md indicates fallback to `mixedbread-ai/mxbai-embed-large-v1` or `Snowflake/snowflake-arctic-embed-l-v2.0` for memory, USE THAT model+dim instead of EmbeddingGemma. Similarly check if Qwen3-4B was swapped to arctic-l for code. Always read 002's summary first.

═══════════════════════════════════════════════════════════════════
PER-FILE VALIDATION (after edits)
═══════════════════════════════════════════════════════════════════

After each edit, validate syntax:
- JSON: `python3 -c "import json; json.load(open('<file>'))"` — exit 0
- TOML: `python3 -c "import tomllib; tomllib.load(open('<file>','rb'))"` — exit 0 (needs Python 3.11+)

═══════════════════════════════════════════════════════════════════
DOCS — fill scaffolded files in this packet
═══════════════════════════════════════════════════════════════════

- `spec.md`: 4 runtimes, env vars added, VOYAGE_API_KEY removed
- `plan.md`: per-runtime steps, schema differences observed
- `tasks.md`: 4-runtime checklist (before/after env block per file)
- `implementation-summary.md`: actual env blocks added, schema variances, validation results, any runtime where config file didn't exist (Codex/Gemini may be absent — record as KNOWN_GAPS, don't create)

═══════════════════════════════════════════════════════════════════
VERIFY
═══════════════════════════════════════════════════════════════════

1. After all 4 (or however many exist) files are patched + validated, run strict-validate on this packet:
   `bash .opencode/skills/system-spec-kit/scripts/spec/validate.sh .opencode/specs/system-spec-kit/026-graph-and-context-optimization/014-local-embeddings-setup-a/003-mcp-config-rollout --strict` — must exit 0.

═══════════════════════════════════════════════════════════════════
ALLOWED WRITE PATHS
═══════════════════════════════════════════════════════════════════

- ~/.claude.json (if exists)
- <repo>/opencode.json (if exists)
- <repo>/.codex/config.toml (if exists)
- <repo>/.gemini/settings.json (if exists)
- .opencode/specs/system-spec-kit/026-graph-and-context-optimization/014-local-embeddings-setup-a/003-mcp-config-rollout/**

═══════════════════════════════════════════════════════════════════
BANNED OPERATIONS
═══════════════════════════════════════════════════════════════════

- Touch source code (hf-local.ts, factory.ts, shared.py — 001 owns these)
- Touch any other spec folder
- Touch vec stores
- Run model downloads / npm / pip
- Spawn sub-agents

═══════════════════════════════════════════════════════════════════
RESPONSE CONTRACT
═══════════════════════════════════════════════════════════════════

BINDING: I have read the 003 MCP CONFIG ROLLOUT contract. I patched all existing MCP runtime configs with the new env vars, removed VOYAGE_API_KEY, validated JSON/TOML syntax, filled in this packet's docs, and strict-validated.
AGENT_RECEIVED: cli-opencode openai/gpt-5.5-fast --variant high
SPAWN_AGENT_USED: no
FILES_MODIFIED:
  - <each runtime config + brief env-block summary>
RUNTIMES_PATCHED:
  - Claude Code: <yes/no/file-absent>
  - OpenCode: <yes/no/file-absent>
  - Codex: <yes/no/file-absent>
  - Gemini: <yes/no/file-absent>
VOYAGE_API_KEY_REMOVED_FROM:
  - <each runtime where it was present before, now absent/empty>
SYNTAX_VALIDATION_EXIT_CODES:
  - claude: 0/1
  - opencode: 0/1
  - codex: 0/1
  - gemini: 0/1
STRICT_VALIDATE_EXIT_CODE: 0/1/2
KNOWN_GAPS: <e.g. Gemini config absent on this machine>
