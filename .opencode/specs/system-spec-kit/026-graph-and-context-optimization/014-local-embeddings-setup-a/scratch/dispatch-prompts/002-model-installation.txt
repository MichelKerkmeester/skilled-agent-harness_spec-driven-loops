You are dispatched by Claude Code main-agent via cli-opencode for sub-phase 002 of packet 014-local-embeddings-setup-a. Single-shot. No sub-agent dispatch. No SpawnAgent. Spec folder pre-approved (Gate 3 = option A existing).

═══════════════════════════════════════════════════════════════════
SUB-PHASE 002 — MODEL INSTALLATION & COMPATIBILITY (Level 1)
═══════════════════════════════════════════════════════════════════

PACKET FOLDER (already exists, scaffolded):
.opencode/specs/system-spec-kit/026-graph-and-context-optimization/014-local-embeddings-setup-a/002-model-installation-and-compat/

CONTEXT
Migrating from Voyage AI (paid API) to local embeddings. Need to pre-download two models so first-call latency doesn't block reindex. **Highest-risk unknown**: whether `@huggingface/transformers` v3.8.1 (the transformers.js npm pkg used by HfLocalProvider) actually loads `google/embeddinggemma-300m` — Gemma3 architecture support in transformers.js is recent and the ST-config-on-Gemma3 path may be unsupported. Smoke-test before committing.

═══════════════════════════════════════════════════════════════════
TASKS
═══════════════════════════════════════════════════════════════════

1) Pre-flight checks (read-only):
   - `which huggingface-cli` — must be available. If missing, instruct user via KNOWN_GAPS (don't try to install).
   - `du -sh ~/.cache/huggingface/hub/ 2>/dev/null` — record current HF cache size.
   - `df -h ~ | tail -1` — confirm ≥20GB free.
   - `python3 -c "import sentence_transformers, torch; print(sentence_transformers.__version__, torch.__version__, torch.backends.mps.is_available())"` — confirm Python env is ready (Setup A's CocoIndex side uses sentence-transformers).
   - `node -e "const m = require('@huggingface/transformers'); console.log(m.env.version || 'unknown')"` from `.opencode/skills/system-spec-kit/mcp_server/` dir — confirm transformers.js is installed.

2) Pre-download models (resumable, can take 5-10 min):
   - `huggingface-cli download Qwen/Qwen3-Embedding-4B` (~8GB)
   - `huggingface-cli download google/embeddinggemma-300m` (~620MB, includes ONNX variants)
   - Verify cache: `du -sh ~/.cache/huggingface/hub/` should grow to 9-10GB.

3) **RISK-GATE SMOKE TEST — EmbeddingGemma via transformers.js**:
   Write `002-model-installation-and-compat/scratch/test-embeddinggemma.mjs`:
   ```js
   import { pipeline } from '@huggingface/transformers';
   process.env.HF_HUB_OFFLINE = '1';  // force offline; model must already be cached
   const start = Date.now();
   const pipe = await pipeline('feature-extraction', 'google/embeddinggemma-300m', { dtype: 'fp32' });
   const loadMs = Date.now() - start;
   const out = await pipe('hello world', { pooling: 'mean', normalize: true });
   console.log(JSON.stringify({ load_ms: loadMs, dims: Array.from(out.dims), first5: Array.from(out.data.slice(0, 5)) }));
   ```
   Run from `.opencode/skills/system-spec-kit/mcp_server/`: `node /Users/michelkerkmeester/MEGA/Development/Code_Environment/Public/.opencode/specs/system-spec-kit/026-graph-and-context-optimization/014-local-embeddings-setup-a/002-model-installation-and-compat/scratch/test-embeddinggemma.mjs`
   **PASS criteria**: dims `[1, 768]`, finite floats in `first5`, load_ms < 30000.
   **FAIL → document fallback in implementation-summary.md**: switch memory side to `mixedbread-ai/mxbai-embed-large-v1` (1024d, no-prefix already in registry from 001). Record the failure mode (error message verbatim) and update plan.md to reflect the model swap.

4) **MIRROR SMOKE TEST — Qwen3-Embedding-4B via sentence-transformers**:
   Write `002-model-installation-and-compat/scratch/test-qwen3-4b.py`:
   ```python
   import os
   os.environ['HF_HUB_OFFLINE'] = '1'
   from sentence_transformers import SentenceTransformer
   import time
   t0 = time.time()
   m = SentenceTransformer('Qwen/Qwen3-Embedding-4B', device='mps')
   load_s = time.time() - t0
   v = m.encode('def hello(): return "world"', normalize_embeddings=True)
   import json
   print(json.dumps({'load_s': round(load_s, 2), 'dim': int(v.shape[0]), 'first5': v[:5].tolist()}))
   ```
   Run: `python3 002-model-installation-and-compat/scratch/test-qwen3-4b.py`
   **PASS criteria**: `dim` is 2560, finite floats, `load_s` < 60.
   **FAIL → fallback to `Snowflake/snowflake-arctic-embed-l-v2.0`** (1024d, also in 001's registry). Same documentation discipline.

5) Document outcomes in the packet's files:
   - `spec.md`: confirmed chosen models + dims + reasons
   - `plan.md`: download steps + smoke-test commands + fallback decisions
   - `tasks.md`: checklist of completed steps with results
   - `implementation-summary.md`: actual disk usage, load times, smoke-test results, any deviation from contract, fallback model (if any)

6) Strict-validate: `bash .opencode/skills/system-spec-kit/scripts/spec/validate.sh .opencode/specs/system-spec-kit/026-graph-and-context-optimization/014-local-embeddings-setup-a/002-model-installation-and-compat --strict` — must exit 0.

═══════════════════════════════════════════════════════════════════
ALLOWED WRITE PATHS
═══════════════════════════════════════════════════════════════════

- ~/.cache/huggingface/** (model downloads — implicit via huggingface-cli)
- .opencode/specs/system-spec-kit/026-graph-and-context-optimization/014-local-embeddings-setup-a/002-model-installation-and-compat/**

═══════════════════════════════════════════════════════════════════
BANNED OPERATIONS
═══════════════════════════════════════════════════════════════════

- Touch ANY source code (no edits to hf-local.ts, factory.ts, shared.py — that's 001's job, running in parallel)
- Edit MCP runtime configs (~/.claude.json, opencode.json, .codex/config.toml, .gemini/settings.json) — 003's job
- Touch vec stores (.cocoindex_code/**, .opencode/skills/system-spec-kit/mcp_server/database/**)
- Run npm install / pip install (use existing env; if a dep is missing, document in KNOWN_GAPS — don't install)
- Edit any other spec folder (only 002-model-installation-and-compat)
- Spawn sub-agents (no SpawnAgent / Task tool / nested cli-* / opencode run / opencode-skills)
- Modify the parent 014 wrapper

═══════════════════════════════════════════════════════════════════
RESPONSE CONTRACT — these fields MUST appear at the end of your final response
═══════════════════════════════════════════════════════════════════

BINDING: I have read the 002 MODEL INSTALLATION contract. I ran pre-flight checks, pre-downloaded both models, smoke-tested EmbeddingGemma via transformers.js, smoke-tested Qwen3-Embedding-4B via sentence-transformers, recorded results in the packet docs, and strict-validated the packet.
AGENT_RECEIVED: cli-opencode openai/gpt-5.5-fast --variant high
SPAWN_AGENT_USED: no
PREFLIGHT:
  huggingface-cli: <yes/no>
  hf_cache_before: <size>
  disk_free: <size>
  python_st_version: <x.y.z>
  python_torch_version: <x.y.z>
  python_mps: <true/false>
  transformersjs_version: <x.y.z>
MODELS_DOWNLOADED:
  Qwen/Qwen3-Embedding-4B: <yes/no, on-disk size>
  google/embeddinggemma-300m: <yes/no, on-disk size>
SMOKE_TEST_RESULTS:
  embeddinggemma: PASS/FAIL — dims=<...>, load_ms=<...>, error=<verbatim if fail>
  qwen3-4b: PASS/FAIL — dim=<...>, load_s=<...>, error=<verbatim if fail>
FALLBACK_TRIGGERED: <none / EmbeddingGemma→mxbai-large / Qwen3-4B→arctic-l>
HF_CACHE_AFTER: <size>
STRICT_VALIDATE_EXIT_CODE: <0/1/2>
KNOWN_GAPS: <anything missing or deferred>
