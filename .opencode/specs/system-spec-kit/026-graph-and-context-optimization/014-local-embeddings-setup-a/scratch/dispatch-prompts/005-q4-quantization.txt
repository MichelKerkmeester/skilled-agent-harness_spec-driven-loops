You are dispatched by Claude Code main-agent via cli-opencode for sub-phase 005 of packet 014-local-embeddings-setup-a. Single-shot. No sub-agent dispatch. No SpawnAgent. Spec folder pre-approved (Gate 3 = option A existing).

═══════════════════════════════════════════════════════════════════
SUB-PHASE 005 — Q4 QUANTIZATION (Level 2)
═══════════════════════════════════════════════════════════════════

PACKET FOLDER:
.opencode/specs/system-spec-kit/026-graph-and-context-optimization/014-local-embeddings-setup-a/005-q4-quantization/

CONTEXT
Sub-phases 001-004 are complete. EmbeddingGemma is running at fp32 (~620MB resident). Google's QAT (quantization-aware training) means Q4 should drop footprint to ~190MB with <1% quality loss. We need to (a) confirm Q4 ONNX is published, (b) plumb a dtype env var through HfLocalProvider, (c) benchmark recall fp32 vs Q4.

═══════════════════════════════════════════════════════════════════
TASKS
═══════════════════════════════════════════════════════════════════

1) Inspect HF model card / cache for `google/embeddinggemma-300m` ONNX variants:
   `ls ~/.cache/huggingface/hub/models--google--embeddinggemma-300m/**/*.onnx 2>/dev/null`
   Record which dtypes are available: fp32, fp16, q4, q4_matmul, int8, etc.

2) Source change: extend `HfLocalProvider` to honor `HF_EMBEDDINGS_DTYPE` env var.
   - File: `.opencode/skills/system-spec-kit/shared/embeddings/providers/hf-local.ts`
   - Find the `pipeline()` call (uses hardcoded `{ dtype: 'fp32' }` or similar)
   - Replace with `{ dtype: getDtypeFromEnv() }` where `getDtypeFromEnv()` reads `process.env.HF_EMBEDDINGS_DTYPE` and falls back to `'fp32'`. Supported values: `'fp32' | 'fp16' | 'q4' | 'q4f16' | 'int8' | 'auto'`. Validate input against the supported list; unknown values fall back to fp32 with a console.warn.
   - Build: `cd .opencode/skills/system-spec-kit/mcp_server && npm run build && npx tsc --noEmit` — both exit 0 required.

3) Config update: set `HF_EMBEDDINGS_DTYPE=q4` (or `q4f16` if q4 isn't available) in all 4 runtime configs (find them via 003's implementation-summary.md). Same file paths and validation rules as 003. Skip configs that don't exist.

4) Benchmark recall fp32 vs Q4:
   - Use a fixed 20-query battery (write it inline at `005-q4-quantization/scratch/benchmark-queries.json` — 20 known-result memory queries: trigger phrases from existing memory entries, e.g., "save context", "deep review", "spec folder", "Gate 3", "cli-opencode", "Voyage", "embeddings", etc.)
   - Restart MCP child process (user runtime restart required — coordinate via main agent if you can't restart yourself). Document in KNOWN_GAPS if you can't restart.
   - Alternatively, write a standalone Node script that loads `EmbeddingGemma` at both dtypes and computes cosine-similarity against a small reference corpus from the 5k memory rows — produces a comparable accuracy proxy without requiring MCP restart.
   - Record top-1 accuracy at fp32 and at Q4. Accept if Δaccuracy < 2%.
   - If Δaccuracy ≥ 2%: roll back to fp16 (smaller savings, less quality loss). If fp16 also unacceptable: stay on fp32 and document.

5) Measure resident RAM via `ps aux | grep -E 'context-server|opencode|node' | head` before and after MCP restart (or document expected savings if restart isn't possible in this dispatch).

═══════════════════════════════════════════════════════════════════
DOCS — fill scaffolded files in this packet
═══════════════════════════════════════════════════════════════════

- `spec.md`: chosen dtype + reason
- `plan.md`: source change + benchmark plan + restart instructions
- `tasks.md`: completed steps
- `checklist.md`: L2 verification — assert dtype env honored, dist rebuilt, ram savings measured/projected, benchmark recorded
- `implementation-summary.md`: actual fp32 vs Q4 accuracy + RAM measurements + final chosen dtype

═══════════════════════════════════════════════════════════════════
VERIFY
═══════════════════════════════════════════════════════════════════

- Build + tsc exit 0
- Benchmark JSON written to `005-q4-quantization/scratch/benchmark-results.json`
- Strict-validate: `bash .opencode/skills/system-spec-kit/scripts/spec/validate.sh .opencode/specs/system-spec-kit/026-graph-and-context-optimization/014-local-embeddings-setup-a/005-q4-quantization --strict` — exit 0

═══════════════════════════════════════════════════════════════════
ALLOWED WRITE PATHS
═══════════════════════════════════════════════════════════════════

- .opencode/skills/system-spec-kit/shared/embeddings/providers/hf-local.ts
- .opencode/skills/system-spec-kit/mcp_server/dist/**
- ~/.claude.json, opencode.json, .codex/config.toml, .gemini/settings.json (adding HF_EMBEDDINGS_DTYPE only)
- .opencode/specs/system-spec-kit/026-graph-and-context-optimization/014-local-embeddings-setup-a/005-q4-quantization/**

═══════════════════════════════════════════════════════════════════
BANNED OPERATIONS
═══════════════════════════════════════════════════════════════════

- Touch any source file besides hf-local.ts
- Touch any other spec folder
- Touch vec stores
- Spawn sub-agents

═══════════════════════════════════════════════════════════════════
RESPONSE CONTRACT
═══════════════════════════════════════════════════════════════════

BINDING: I have read the 005 Q4 QUANTIZATION contract. I confirmed ONNX dtype availability, plumbed HF_EMBEDDINGS_DTYPE through hf-local.ts, rebuilt dist, configured the env var in available runtimes, benchmarked recall, and strict-validated.
AGENT_RECEIVED: cli-opencode openai/gpt-5.5-fast --variant high
SPAWN_AGENT_USED: no
ONNX_VARIANTS_AVAILABLE: <list dtypes found in HF cache>
SELECTED_DTYPE: <fp32|fp16|q4|q4f16|int8>
BUILD_EXIT_CODE: 0/non-zero
TSC_NOEMIT_EXIT_CODE: 0/non-zero
BENCHMARK:
  fp32_top1: <0.0-1.0>
  q4_top1: <0.0-1.0>
  delta_pp: <number>
RAM_BEFORE_AFTER: <fp32 ~MB | selected ~MB>
RUNTIMES_PATCHED: <list>
STRICT_VALIDATE_EXIT_CODE: 0/1/2
KNOWN_GAPS: <if restart not possible, etc>
