Now I have enough evidence to write up the 5 research notes. Let me compile them:

### Angle 35: Cross-model research diversity (meta, tested by this very run)
- **Framing:** When the same deep-research question is run on MiMo, DeepSeek, and MiniMax, the three models either surface meaningfully different findings (real diversity value) or converge on the same answer (fan-out is pure parallelism, not diversity).
- **Repo evidence:** `.opencode/skills/deep-loop-runtime/feature_catalog/09--fanout/fanout-merge.md:20-32` (research merge dedup-by-findingId + cross-lineage `_lineages` attribution; review merge uses strongest-restriction P0 rollup). `.opencode/skills/deep-loop-runtime/scripts/fanout-merge.cjs:91-170, 183-252` (the actual merge impl). `.opencode/specs/system-spec-kit/028-026-program-research/research/iterations/D1.md–D10.md` (this very run's 10 iterations, each with a research note per assigned angle). `.opencode/specs/system-spec-kit/028-026-program-research/research/iterations/D4.md:8` (the open next-experiment that asks for a 3-lineage fan-out to measure Jaccard diversity). `.opencode/skills/sk-prompt-small-model/benchmarks/005-mimo-minimax-capability-discrimination/synthesis.md:9-15` (only direct cross-model head-to-head in the repo: M3 vs MiMo on 8 hard fixtures, 32/32 cells, M3 perfect / MiMo 0.89 — discriminator is reliability not framework). `.opencode/skills/sk-prompt-small-model/benchmarks/004-mimo-prompt-framework/eval/synthesis-m3-vs-mimo.md:88-93` (cross-model finding: "both agree COSTAR is the high-reasoning winner, but for opposite reasons" — M3 = format-robust, MiMo = framing-sensitive).
- **Findings:** The merge plumbing (`fanout-merge.cjs`) was built to track cross-lineage attribution — `keyFindings` deduped by id with a `_lineages` array, and `coverageBySources` exists as a placeholder (always empty per iteration D4). However, **no cross-model research diversity experiment has actually been run.** Iteration D4 explicitly proposes it as a next experiment. The only cross-model evidence in the repo is the capability-discrimination benchmark (M3 vs MiMo on hard coding fixtures) which surfaces a *reliability* gap (MiMo 1-in-5 catastrophic miss), not a *diversity* gap. The 10 iterations in this packet show stylistic and content variation (D1/D2 are exhaustive, D6 is moderate, D7 is brief) but these are caused by different assigned angle counts and note shapes, not by model identity — a single model is responsible for each iteration, and the per-iteration model is not stamped on the output. The structural fact is that fan-out has never been exercised with heterogeneous models: the parallel test uses two identical `o4-mini` executors (D4). The "this very run" framing is misleading because the 028 packet is a *single-lineage* deep-research pass, not a fan-out. The first real test of cross-model research diversity would require restructuring the deep-research YAML to spawn 3 lineages, one per model, and compute Jaccard on the resulting `keyFindings`.
- **Next experiment:** Run a 3-lineage deep-research on a fixed spec (e.g. the 028 packet) with `cli-codex o4-mini`, `cli-opencode deepseek-v4-pro --variant high`, and `cli-opencode xiaomi-token-plan-ams/mimo-v2.5-pro --variant high`. Compute Jaccard similarity on `keyFindings.id` between lineage pairs; if Jaccard < 0.6 the models surface meaningfully different findings, if > 0.8 they converge. Threshold from the existing D4 proposal.
- **Verdict:** **UNKNOWN — no measurement exists.** The infrastructure (`fanout-merge.cjs` + cross-lineage attribution) is in place but has never been exercised with heterogeneous models. The 028 packet itself is single-lineage, so it cannot answer the meta-question. **LOW CONFIDENCE** that fan-out produces materially different findings until measured. The capability benchmark (M3 vs MiMo) shows models *can* diverge on the same input (reliability gap, format gap) but the research-finding diversity question is a different axis.

### Angle 36: Per-model prompt-craft frameworks (RCAF/COSTAR/TIDD-EC) — empirically validated vs convention defaults
- **Framing:** Of the 7 frameworks in `sk-prompt/references/patterns_evaluation.md` (RCAF, COSTAR, RACE, CIDI, TIDD-EC, CRISPE, CRAFT), only RCAF and COSTAR and TIDD-EC have model-specific empirical benchmarks; the other 4 + RCAF/COSTAR/TIDD-EC on most small models are convention defaults with no measurement.
- **Repo evidence:** `.opencode/skills/sk-prompt/references/patterns_evaluation.md:30-38` (the 7-framework table, each with a "Success Rate" of 87-94% — these rates are presented as if measured but no methodology, no fixture set, no sample size is cited anywhere in the file or in `sk-prompt/`. I searched the doc for "case study", "study", "empirical", "validate", "measure" — none appear adjacent to the success rates). `.opencode/skills/sk-prompt-small-model/assets/model-profiles.json:35-49, 95-109, 111-162, 164-208, 210-259, 300-315, 361-376, 419-437` (`recommended_frameworks.evidence` per model — only `minimax-2.7` and `minimax-m3` (carried) and `mimo-v2.5-pro` have `benchmark: "003"` / `"004"` with non-null scores; the other 6 models record `benchmark: null, primary_score: null, sample: "no model-specific benchmark", confidence: "low", status: "default-unverified"`). `.opencode/skills/sk-prompt-small-model/references/pattern-index.md:47-48` (the index itself labels the framework picks as "shipped (benchmark 003)" and "shipped (benchmark 004)" — only 2 benchmarks exist in `.opencode/skills/sk-prompt-small-model/benchmarks/`, 003 and 004, plus 005 which is capability-discrimination not framework-comparison). `.opencode/skills/sk-prompt-small-model/benchmarks/003-minimax-prompt-framework/eval-loop/synthesis.md:9-19` (the 7-variant x 7-fixture rig that gave 0.7671 TIDD-EC vs 0.7419 RCAF on M2.7). `.opencode/skills/sk-prompt-small-model/benchmarks/004-mimo-prompt-framework/eval/synthesis.md:14-20` (5-framework x 2-fixture rig on MiMo). `.opencode/skills/sk-prompt-small-model/references/models/swe-1.6.md:96-111` (SWE-1.6 RCAF is "contract basis, no head-to-head run" with `Confidence: low`). The 7-framework `Success Rate` column in `patterns_evaluation.md` is presented without provenance.
- **Findings:** The mapping from the 7 frameworks to per-model picks is **partial**: 2 of 7 frameworks (TIDD-EC, COSTAR) have *any* empirical model-specific data (MiniMax → TIDD-EC+dense on 2 of 7 small-model profiles, MiMo → COSTAR+lean on 1 of 7); the other 5 (RCAF, RACE, CIDI, CRISPE, CRAFT) and RCAF-as-fallback on most rotation models are pure convention. RCAF is the convention default for 6 of 7 small models (SWE-1.6, DeepSeek-v4-pro, Kimi-k2.6, Qwen3.6, GLM-5.1) — all `status: "default-unverified"` with `benchmark: null, confidence: "low"`. The `Success Rate` column in the canonical patterns_evaluation.md table is unannotated: there is no fixture, no sample size, no provenance, and the rates (87-94%) are clustered too tightly to be a real measurement (90 ± 4% on what tasks?). Of the 7 frameworks, CRISPE and CRAFT have *zero* benchmark coverage in the entire `sk-prompt-small-model` rotation. The MiniMax TIDD-EC result (benchmark 003) was 7 fixtures x 7 variants = 49 dispatches with single sample per cell (per the synthesis caveat: "Single sample per (variant, fixture); margins of 0.008-0.03 are above the ~0.02 fixture-noise floor but a re-run would tighten confidence"). The MiMo COSTAR result (benchmark 004) was 2 fixtures x 5 frameworks with 1 sample per cell — the synthesis explicitly labels COSTAR-vs-RACE as "statistical tie" and "single-sample noise". The 5 framework x 8 fixture grid in benchmark 005 capability-discrimination measured **capability** (correctness), not framework choice. So the empirical base for the entire framework-pick layer is: ~49 dispatches for MiniMax/M2.7, ~10 dispatches for MiMo, 0 dispatches for the other 6 models.
- **Next experiment:** Build a 5-framework x 3-fixture x 2-model cross-bake-off (SWE-1.6 + MiniMax-M3 + MiMo + Kimi-k2.6 → 120 dispatches) on a tiered fixture set (3 easy + 3 hard + 3 adversarial, per the 127 framework's tiered-fixtures plan referenced in `synthesis-m3-vs-mimo.md:100`). Measure: (a) per-cell pass rate with N=5 sample for confidence intervals, (b) format adherence as a tiebreak. Report which model-framework pairs are now evidence-backed vs still convention. This would lift ~80% of the rotation out of `default-unverified`.
- **Verdict:** **The framework-pick layer is ~70% convention default, ~30% empirically validated, and the validation is itself narrow (small N, narrow fixture coverage).** RCAF on 5/6 small models is convention with no measurement; CRISPE and CRAFT have zero empirical coverage anywhere. The "Success Rate" column in `patterns_evaluation.md` is unannotated and should either be sourced to a benchmark or moved out of the table. **LOW CONFIDENCE** on the framework-pick layer's accuracy outside the two benchmarks that exist.

### Angle 37: sk-prompt forkability — is the hub-to-cli seam truly decoupled
- **Framing:** After the 2.1.0.0 "card relocation" release, `sk-prompt` is supposed to be cleanly fork-portable as a generic prompt-framework engine. The question is whether residual cross-refs into the `sk-prompt-small-model` hub or into cli-* skills would break an extraction.
- **Repo evidence:** `.opencode/skills/sk-prompt/changelog/v2.1.0.0.md:1-19` (the explicit "forkable" release: `cli_prompt_quality_card.md` was moved out via git rename to `sk-prompt-small-model/assets/`; `graph-metadata.json` cleaned of `enhance_when`/`key_files`/`entities`/`source_docs` references; asset folder "verified to carry only generic engine assets"). `.opencode/skills/sk-prompt/SKILL.md:2-4` (frontmatter: `name: sk-prompt`, no model or CLI references). `.opencode/skills/sk-prompt/SKILL.md:437` (the only CLI mention: "the `cli-*` skills that use the prompt quality card before dispatch" — generic class reference, no specific skill, no path). `.opencode/skills/sk-prompt/assets/` contains only `format_guide_json.md`, `format_guide_markdown.md`, `format_guide_yaml.md`, `framework-registry.json` — all generic (the registry is data-only scaffolds for the 7 frameworks, no model/CLI/provider names). `.opencode/skills/sk-prompt/references/` contains only `depth_framework.md` and `patterns_evaluation.md` — both generic. `grep -rn "minimax\|MiniMax\|deepseek\|mimo\|MiMo\|cli-opencode\|cli-devin\|cli-codex" .opencode/skills/sk-prompt/{references,assets}/` returns no matches; `grep -rn "sk-prompt-small-model" .opencode/skills/sk-prompt/{references,assets,SKILL.md}/` returns no matches. The only intra-`sk-prompt` cross-reference to the hub is the `changelog/` directory (3 files mentioning `sk-prompt-small-model` in their prose). The `manual_testing_playbook/` contains references to `sk-prompt` (the skill's own name) in scenario prose but no paths into the hub. **Outgoing cross-refs from `sk-prompt`:** zero paths into the hub or any cli-* skill in the live content. **Incoming cross-refs into `sk-prompt` from cli-* skills:** the four sibling cli-* skills' changelogs (`cli-opencode/v1.3.11.0.md`, `cli-claude-code/v1.1.11.0.md`, `cli-codex/v1.4.8.0.md`, `cli-devin/v1.0.10.0.md`) all say "the canonical CLI prompt quality card moved out of sk-prompt … so this skill no longer points at a file that left its old home" — these are post-relocation mechanical repoints; the cli-* skills themselves still name "sk-prompt" generically in their SKILL.md (`cli-opencode/SKILL.md:312, 314-315` point to `sk-prompt-small-model` and `sk-prompt` as the canonical source for "framework definitions"; `cli-claude-code/SKILL.md:348-351` mirrors).
- **Findings:** `sk-prompt` is **cleanly forkable as the v2.1.0.0 release claimed.** The skill body has zero outgoing path cross-references into the `sk-prompt-small-model` hub or into any cli-* skill. The only `sk-prompt` mentions in live content are: (a) the skill's own frontmatter `name` field, (b) one generic class reference ("the cli-* skills that use the prompt quality card") with no paths, (c) the `changelog/` history. The `assets/`, `references/`, and `graph-metadata.json` carry no model names, provider names, executor names, or paths to the hub. The asset folder is restricted to generic engine assets (4 files: 3 format guides + 1 framework registry). The `manual_testing_playbook/` contains self-references (e.g. `rg 'STEP 0' .opencode/skills/sk-prompt/SKILL.md`) which are about the skill itself and would resolve fine in an extraction. The `v2.1.1.0` and `v2.1.2.0` releases post-cleaned the only remaining stale card references in `SKILL.md` and `manual_testing_playbook/`. **The one residual coupling is the skill's name `sk-prompt`** — an extracted copy would be renamed (the changelog `v3.4.1.0` notes `sk-improve-prompt` → `sk-prompt` is one prior rename), but the extraction target's identity is operator-chosen, not a forkability blocker. The pre-relocation cross-refs (the cli-* skills' SKILL.md generic mentions of "sk-prompt" as the framework source) are *outgoing* from the cli-* skills into the soon-to-be-extracted `sk-prompt` — meaning after extraction, those four cli-* skills would need to be repointed to the extracted copy or to a different framework source. The fork extraction is therefore `sk-prompt` clean but the consumer side (cli-* skills) would need a coordinated rename/repoint.
- **Next experiment:** Perform the actual extraction: copy `.opencode/skills/sk-prompt/` to `/tmp/sk-prompt-extracted/`, then run a vitest suite against the extracted copy in isolation. If the test suite passes (CLEAR scoring, framework selection, DEPTH processing, format guide rendering) without any path-resolution errors, the extraction is mechanically clean. Then attempt to import the extracted skill from a sibling `cli-opencode` instance to verify the consumer side is also clean.
- **Verdict:** **`sk-prompt` is cleanly fork-portable** — zero outgoing path cross-references into the hub or any cli-* skill in live content. The `v2.1.0.0` "card relocation" plus `v2.1.1.0`/`v2.1.2.0` cleanups achieved the stated goal. The fork would require only a coordinated rename of the four cli-* skills' generic "sk-prompt" mentions to point at the extracted copy or its new name. **HIGH CONFIDENCE** in the forkability claim based on direct grep evidence.

### Angle 38: Open-model cost/quality frontier — DeepSeek vs MiniMax vs MiMo on a standardized task suite
- **Framing:** The three "open"/accessible small models (DeepSeek-v4-pro, MiniMax-M3, MiMo-V2.5-Pro) have a measurable cost/quality frontier — for each task class (review, RCA, code generation, format-strict coding), one model is the best price/performance and the others trail.
- **Repo evidence:** `.opencode/skills/sk-prompt-small-model/benchmarks/005-mimo-minimax-capability-discrimination/synthesis.md` (parent) and `eval/capability-discrimination/synthesis.md:7-15` (3 samples × 4 computational fixtures = 12 cells) and `eval/strict-validation/synthesis.md:5-21` (5 samples × 4 validation fixtures = 20 cells) — M3 is **32/32 gate-eligible at 1.00**, MiMo is **~0.89 on both runs, gate-ineligible**, gap is reliability not capability. `.opencode/skills/sk-prompt-small-model/benchmarks/004-mimo-prompt-framework/eval/synthesis.md:14-20, 38-49` (MiMo at default reasoning: 100% pass on tractable fixtures, format adherence is the discriminator — COSTAR 100%, RACE 100%, CIDI/RCAF/TIDD-EC 50% on the harder fixture). `.opencode/skills/sk-prompt-small-model/benchmarks/004-mimo-prompt-framework/eval/synthesis-m3-vs-mimo.md:16-22, 82-93` (M3 vs MiMo on the same 2-fixture rig at `--variant high`: M3 = 244.8s/10 dispatches, MiMo = 160.5s/10 dispatches — MiMo is **~1.5× faster** but M3 keeps every framework format-clean; MiMo needs the lean COSTAR scaffold to stay format-clean; M3 wins on consistency). `.opencode/skills/sk-prompt-small-model/assets/model-profiles.json:23-31, 287-296, 405-411` (strengths: DeepSeek = "complex reasoning, root-cause debugging, multi-step implementation, architecture trade-off analysis"; MiniMax-M3 = "guardrail-heavy TIDD-EC framing + dense pre-planning, subscription billing"; MiMo = "1M-token context, strongly agentic (1000+ tool calls, SWE-bench Pro 57.2), token-efficient (40-60% fewer tokens/trajectory vs frontier)"). The 028 packet D4 next-experiment proposes adding `cli-codex o4-mini` and `cli-opencode gpt-5.5-fast` to a 3-lineage fan-out but that is review diversity, not cost/quality.
- **Findings:** The frontier is **partially mapped** for two of the three pairs (M3 vs MiMo via benchmark 005 + 004; MiniMax vs MiMo via benchmark 003 vs 004 indirectly) but **DeepSeek-v4-pro is missing from the cross-model matrix entirely.** Benchmark 005 measures only M3 and MiMo; the model-profiles.json registry lists DeepSeek-v4-pro as the "escalation target for complex review, RCA, and architecture trade-off analysis" but no cross-model comparison has been run. The empirical findings for M3 vs MiMo are: (1) M3 wins on **reliability** (32/32 vs 0.89, M3 never emits a fully-failing answer, MiMo has ~1-in-5 catastrophic failure on hard validation), (2) MiMo wins on **latency** (160.5s vs 244.8s on the same 10-dispatch rig, ~1.5× faster), (3) MiMo wins on **token efficiency** (40-60% fewer tokens per trajectory, per registry), (4) MiMo wins on **context window** (1M tokens vs M3's unverified-but-tighter limit), (5) on **format adherence** MiMo is more framing-sensitive (5/5 frameworks clean on M3 vs 1/5 clean on MiMo at high reasoning). The cost dimension is **not measured** — the registry lists quota pools (`minimax-token-plan`, `xiaomi-token-plan`, `cognition-pro`, `deepseek-api`, `opencode-go`) but no per-dispatch dollar cost or tokens-per-task metric is captured. The task-class dimension is **not separated** — benchmarks 003/004/005 all use tractable pure-function coding fixtures; there is no benchmark for review/RCA/architecture (DeepSeek's declared strengths) or for 1M-context tasks (MiMo's declared strength). The published recommendation is "MiniMax-M3 is the more reliable pick over MiMo-V2.5-Pro — on reliability/consistency, not raw capability" (capability-discrimination synthesis:8), but this is a binary pick, not a per-task-class frontier.
- **Next experiment:** Build a 3-model × 3-task-class × 5-fixture × 5-sample = 225-dispatch bake-off covering (a) hard validation (existing ipv4/roman fixtures), (b) multi-file review (3 hand-written 500-line codebases with intentional defects), (c) 1M-context stress (MiMo's strength, impossible for the others). For each cell record: correctness, format adherence, wall-clock seconds, total tokens, and approximate cost from the quota-pool billing model. Compute the Pareto frontier per task class. DeepSeek-v4-pro must be included — it is the missing third vertex of the triangle.
- **Verdict:** **The frontier is partially mapped for M3 vs MiMo on coding; DeepSeek-v4-pro is unmeasured against either.** M3 is the reliable pick; MiMo is the fast/token-efficient pick; DeepSeek is the unverified escalation target. The recommendation "use M3 for hard validation, MiMo for token-bounded high-volume coding, DeepSeek for RCA/review" is *plausible from declared strengths* but **not measured**. **LOW CONFIDENCE** on the full frontier; the per-task-class mapping is a research project, not a shipped benchmark.

### Angle 39: "--variant high" reasoning-effort mapping — is it actually forwarded for each provider
- **Framing:** The cli-* dispatchers forward `--variant high` to each provider. For some providers (MiMo, OpenAI, gpt-5.5-fast, MiniMax-M3) the flag is verified accepted; for others (DeepSeek direct API, MiniMax legacy, some pool paths) it is flagged "unverified — omitted by default." The question is which providers actually honor the flag, which silently ignore it, and which reject it.
- **Repo evidence:** `.opencode/skills/cli-opencode/references/cli_reference.md:239-251` (the canonical provider-variant matrix: opencode-go = `--variant` accepted, effect model-dependent; deepseek (deepseek-v4-pro) = reasoning effort accepted; deepseek (deepseek-v4-flash) = non-reasoning, `--variant` ignored; minimax-coding-plan/minimax (M3/M2.7) = **"behavior unverified — omitted by default; confirm against the MiniMax API before relying on it"**; xiaomi-token-plan-ams (mimo-v2.5-pro) = `--variant` maps to MiMo reasoning effort, **"always use `--variant high`" — confirmed accepted on opencode 1.15.13**). `.opencode/skills/sk-prompt-small-model/assets/model-profiles.json:280-286, 340-349, 395-403` (per-model `capability.variant_status`: MiMo = `"confirmed"`; M2.7 = `"omitted-by-default-historically"`; M3 = `"omitted-by-default-historically"`). `.opencode/skills/sk-prompt-small-model/references/models/minimax-m3.md:135-139` (dispatch table: `variant_flag: --variant` with `variant_status: omitted-by-default-historically` — "omit --variant until accepted behaviour on this provider is verified"). `.opencode/skills/deep-improvement/scripts/model-benchmark/dispatch-model.cjs:411` (the dispatch script pushes `--variant` for cli-opencode **unconditionally if `variant` is set**, with no provider-aware filtering; same at line 427 for cli-claude-code via `--effort`, line 435 for cli-codex via `-c model_reasoning_effort=...`). `.opencode/skills/deep-improvement/scripts/skill-benchmark/live-executor.cjs:83` (provider-aware exception: `if (variant && !/minimax/i.test(model)) args.push('--variant', variant);` — MiniMax is the ONE provider where `--variant` is deliberately suppressed). `.opencode/skills/sk-prompt-small-model/benchmarks/004-mimo-prompt-framework/eval/synthesis-m3-vs-mimo.md:5, 119-123` (the smoking gun: "both models confirmed to accept `--variant high` … pre-flight probes returned a clean text part + `step-finish/stop`, empty stderr; MiMo probe showed `reasoning:41` tokens, confirming the high variant engages reasoning. … `MiniMax-M3` … **`--variant high` is confirmed ACCEPTED for `MiniMax-M3` — every dispatch ran under `--variant high` with clean exits; the pre-flight probe returned a valid text part + `step-finish/stop`. This contradicts the current 'unverified' note for the MiniMax `--variant` row.** … 20 clean dispatches"). `.opencode/skills/sk-prompt-small-model/benchmarks/004-mimo-prompt-framework/eval/synthesis-m3-vs-mimo.md:103` ("`--variant high` semantics are whatever opencode 1.15.13 maps the flag to per provider; both models confirmed it accepted (clean exits, valid event stream; MiMo showed reasoning tokens), but the exact per-provider reasoning-token budget is not introspected here"). `.opencode/skills/cli-opencode/changelog/v1.3.13.0.md:11` (per benchmark 004 result: "20 clean benchmark dispatches, `--variant high` accepted" for `minimax-coding-plan/MiniMax-M3`).
- **Findings:** The "unverified" label on MiniMax `--variant` is **stale and contradicted by 20 clean dispatches on 2026-06-02.** The benchmark 004 m3-vs-mimo synthesis explicitly recommends updating the MiniMax `--variant` row from "unverified — omitted by default" to "**`--variant high` confirmed accepted for `MiniMax-M3` on opencode 1.15.13 (126/004); standing high default applies**" (line 123), but as the synthesis notes "These are recommendations only — the user applies any doc change to cli-opencode themselves." For the other providers: (1) **MiMo** — verified accepted, reasoning tokens observed; the `live-executor.cjs:83` hardcodes the `!/minimax/i.test(model)` exception but does NOT exclude MiMo, so MiMo gets `--variant` forwarded. (2) **DeepSeek-v4-pro (via deepseek-api)** — registered as "reasoning effort accepted" but no benchmark evidence of `--variant` actually being parsed; the `--pure` flag is required for tool names with `:` (per `deepseek-v4-pro.md:154`). (3) **DeepSeek-v4-flash** — registered as "non-reasoning, `--variant` ignored" — flag is accepted but inert. (4) **Kimi-k2.6 / GLM-5.1 via opencode-go** — `--variant` accepted, effect model-dependent (no per-model verification). (5) **OpenAI chat models (gpt-5.5 family)** — verified accepted (`deep-improvement/changelog/v1.10.0.0.md:48`: "Live Mode B confirmed against `openai/gpt-5.5-fast --variant high`"). (6) **Anthropic (Claude via cli-claude-code)** — mapped to `--effort` flag at `dispatch-model.cjs:427`, not `--variant`; the variant name is passed through unchanged. The dispatch path that is *most* broken: the `live-executor.cjs:83` MiniMax exception. **After 20 clean dispatches confirming `--variant high` is accepted on MiniMax-M3, this exception is now incorrectly suppressing the flag** — every MiniMax-M3 dispatch via `live-executor.cjs` runs at default reasoning effort, missing the high-reasoning improvement. The `dispatch-model.cjs:411` path (the model-benchmark harness) does NOT have the same exception, so it correctly forwards `--variant` to MiniMax. The two scripts disagree. The MiMo flag is verified + working; the MiniMax flag is verified + the exception in `live-executor.cjs` is now stale and silently downgrades MiniMax reasoning effort; DeepSeek/OpenAI flags are unmeasured but the path is correct (forwarded unconditionally); Anthropic uses a different flag.
- **Next experiment:** Run a 3-cell sanity test: (a) `live-executor.cjs --model minimax-coding-plan/MiniMax-M3 --variant high` and inspect the spawned `opencode run` argv (should contain `--variant high` per the `dispatch-model.cjs` contract, NOT contain it per the `live-executor.cjs:83` exception — the bug surfaces here). (b) Run the same dispatch through `dispatch-model.cjs` and compare argv. (c) Run a real M3 dispatch at `--variant high` vs default variant on a 5-fixture hard-validation rig and measure correctness delta. If M3 at high beats M3 at default by > 5%, the `live-executor.cjs` exception is causing real silent quality loss in production.
- **Verdict:** **The "several flagged unverified" framing in angle 39 is now stale: 1 of 3 was unverified and is now confirmed-accepted (MiniMax-M3, 20 clean dispatches); 1 was confirmed-accepted (MiMo, reasoning tokens observed); 1 has the flag but the live executor is now incorrectly suppressing it (MiniMax via `live-executor.cjs:83`).** The `dispatch-model.cjs` and `live-executor.cjs` paths disagree on MiniMax `--variant` forwarding. **Recommend: (1) update the `cli-opencode` cli_reference.md to remove the "unverified" caveat for M3; (2) remove the `!/minimax/i.test(model)` exception in `live-executor.cjs:83`; (3) add a per-provider smoke test in the dispatch harness that asserts `--variant high` reaches argv.** The angle's premise "several flagged unverified" is now only accurate for DeepSeek/Kimi/GLM-via-opencode-go, which lack any per-provider smoke test. **MEDIUM CONFIDENCE** based on the strong 126/004 evidence and the clear code divergence between the two dispatch scripts.
