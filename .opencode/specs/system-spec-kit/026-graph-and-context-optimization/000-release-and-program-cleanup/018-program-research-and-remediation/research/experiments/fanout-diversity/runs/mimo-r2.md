Now I need to deep-read key implementation files for precise evidence. Let me read the critical files for each question.
Now let me check for non-test/non-spec usage of `unionMode:'multi'` and the launcher lease TTL values.
I have now gathered sufficient evidence across all five questions. Here is the report.

---

### Q1: Self-maintaining memory-index move-reconciliation under rapid concurrent spec-folder moves across sessions
- Findings:
  1. `reconcileMoves()` is scan-time-only, invoked after `memory_index_scan` categorizes vanished/appeared paths. It does not run during real-time file-watcher events. Evidence: `.opencode/skills/system-spec-kit/mcp_server/handlers/memory-index.ts`:688-700 [HIGH]
  2. Move matching requires a unique sibling relationship: same grandparent directory + same basename + exactly one `toDelete` candidate + exactly one live DB row. If two vanished paths share a basename under the same grandparent (e.g., parallel renames), the match silently skips. Evidence: `.opencode/skills/system-spec-kit/mcp_server/lib/storage/incremental-index.ts`:569-582 [HIGH]
  3. The file-watcher handles renames as two separate events: `unlink` (removes old DB rows via `removeIndexedMemoriesForFile`) then `add` (re-indexes new path). These are debounced independently. Under rapid concurrent renames, the `unlink` handler deletes the row before a subsequent scan can pair it, destroying the embedding. Evidence: `.opencode/skills/system-spec-kit/mcp_server/lib/ops/file-watcher.ts`:548-583 [HIGH]
  4. The scan lease is atomic via SQLite transaction (`BEGIN IMMEDIATE`), preventing two concurrent scans from both running `reconcileMoves`. Evidence: `.opencode/skills/system-spec-kit/mcp_server/core/db-state.ts`:442 [HIGH]
  5. The re-read-after-write guard in `acquireOwnerLeaseFile` (line 391-400) and the exclusive-write path prevent two launchers racing on a stale lease from both acquiring ownership. Evidence: `.opencode/bin/mk-spec-memory-launcher.cjs`:391-400 [HIGH]
  6. A race exists when the watcher fires `unlink` for a renamed file BEFORE the next scan cycle: the row is deleted, then the new file is indexed fresh (re-embedding required). The scan-time reconciliation only helps if both paths are still in the categorized lists at scan time. Evidence: `.opencode/skills/system-spec-kit/mcp_server/context-server.ts`:1637-1670 (watcher delete path) [HIGH]
  7. The lineage system tolerates stale `logical_key` after an in-place move by re-checking the predecessor's current row identity. Evidence: `.opencode/skills/system-spec-kit/mcp_server/lib/storage/lineage-state.ts`:736-754 [HIGH]
  8. Search description-map cache has a 60-second TTL, meaning folder renames are "eventually picked up" but not immediately. Evidence: `.opencode/skills/system-spec-kit/mcp_server/lib/search/hybrid-search.ts`:2164-2169 [HIGH]
  9. Tests cover: single rename, concurrent renames across multiple files, burst rename chains, and the uniqueness guard. Evidence: `.opencode/skills/system-spec-kit/mcp_server/tests/file-watcher.vitest.ts`:478-636 [HIGH]
  10. The `packet_id` read only happens from the NEW location's `graph-metadata.json`; the old location may already be gone, so the old row's packet_id is never checked. This is correct by design. Evidence: `.opencode/skills/system-spec-kit/mcp_server/lib/storage/incremental-index.ts`:521-531 [HIGH]
- Verdict: The scan-time reconciliation is correct for its design scope — it preserves embeddings during sibling-folder renames detected at scan time. The remaining race exposure is the watcher-vs-scan gap: if a rename fires `unlink` before the next scan, the row is deleted and the new file must be re-embedded. Under rapid concurrent moves across sessions (each session running its own watcher), this window is real but bounded by the scan cooldown interval. No data corruption occurs — at worst, embeddings are regenerated. [HIGH]

### Q2: Shared-daemon lease model — ungraceful lease-holder death and re-election latency
- Findings:
  1. The owner lease uses a file-based JSON record with `ownerPid`, `lastHeartbeatIso`, and `ttlMs` (default 60000ms). The heartbeat refreshes at `ttlMs / 2` (30 seconds). Evidence: `.opencode/bin/mk-spec-memory-launcher.cjs`:314-324, 430-442 [HIGH]
  2. `classifyOwnerLease()` detects stale leases via three paths: `processLiveness(pid) === 'dead'` (ESRCH), `ppid === 1` orphan detection, or heartbeat expiry > `ttlMs * 2` (2 minutes). Evidence: `.opencode/bin/mk-spec-memory-launcher.cjs`:346-363 [HIGH]
  3. When the lease-holder dies ungracefully, a secondary launcher's `bridgeOrReportLeaseHeld()` path detects the dead socket via a deep JSON-RPC probe, then calls `respawnAfterDeadSocket()`. This acquires a respawn lock, reaps the dead owner PID (SIGTERM → grace → SIGKILL), reaps the child PID, clears the old lease, writes a new exclusive owner lease, and relaunches. Evidence: `.opencode/bin/mk-spec-memory-launcher.cjs`:671-751 [HIGH]
  4. The respawn lock (`mk-spec-memory-respawn.lock`) is fd-based exclusive, preventing two secondaries from both attempting respawn. Evidence: `.opencode/bin/mk-spec-memory-launcher.cjs`:696-700 [HIGH]
  5. The re-read-after-write check at line 703-707 and 710-714 verifies the owner lease and launcher lease haven't changed during the lock wait, preventing a superseded respawn. Evidence: `.opencode/bin/mk-spec-memory-launcher.cjs`:703-714 [HIGH]
  6. If `processLiveness()` returns `unknown-eperm` (EPERM — process exists but sandbox prevents signal), the lease is treated as live-owner and NOT reclaimed. This is a conservative safety choice but means a sandboxed dead holder cannot be reclaimed. Evidence: `.opencode/bin/mk-spec-memory-launcher.cjs`:349, 371 [HIGH]
  7. The heartbeat timer is `.unref()`'d, so it won't keep the event loop alive if the launcher is otherwise idle. A launcher that hangs (not crash) will have its heartbeat expire after `ttlMs * 2`. Evidence: `.opencode/bin/mk-spec-memory-launcher.cjs`:442 [HIGH]
  8. On clean exit, `clearOwnerLeaseFile()` double-checks the lease file still points to this PID before unlinking (preventing deletion of a newly-acquired lease by a racing launcher). Evidence: `.opencode/bin/mk-spec-memory-launcher.cjs`:450-464 [HIGH]
  9. The dead-socket respawn can be disabled via `SPECKIT_BRIDGE_RESPAWN_DISABLED=1`, in which case the secondary reports the diagnostic and exits. Evidence: `.opencode/bin/mk-spec-memory-launcher.cjs`:672-675 [HIGH]
  10. There is no "re-election" mechanism — the architecture uses single-writer leases with stale reclaim, not leader election. The first secondary to successfully acquire the respawn lock and write a new exclusive lease becomes the new owner. Evidence: `.opencode/bin/mk-spec-memory-launcher.cjs`:671-751 [HIGH]
  11. The IPC bridge module performs a deep JSON-RPC probe (not just socket existence) to detect hung daemons that hold the socket but cannot serve requests. Evidence: `.opencode/bin/lib/launcher-ipc-bridge.cjs`:127 [HIGH]
  12. The RSS watchdog recycles the daemon in-place (SIGTERM → grace → SIGKILL → relaunch) when memory ceilings are sustained, keeping the launcher lease alive across the recycle. Evidence: `.opencode/bin/mk-spec-memory-launcher.cjs`:1089-1101 [HIGH]
- Verdict: On ungraceful lease-holder death, the worst-case re-acquisition latency is `ttlMs * 2` (2 minutes) for heartbeat-based detection, or faster if a secondary attempts to bridge and detects the dead socket via deep probe. The respawn path is serialized via an exclusive lock and includes supersession checks, so no two launchers can both become owner. The `unknown-eperm` case is the main gap: a sandboxed dead process cannot be reclaimed via PID liveness, requiring the heartbeat TTL to expire. Overall the model is sound for single-machine deployment. [HIGH]

### Q3: Code-graph multi-file union queries — usage and value of `unionMode:'multi'` and `hotFileBreadcrumb`
- Findings:
  1. `unionMode:'multi'` merges `subject` with `subjects[]` into `rawSubjects` at query time. Each subject is resolved independently; unresolved/ambiguous subjects degrade gracefully with `failureFallback`. Evidence: `.opencode/skills/system-code-graph/mcp_server/handlers/query.ts`:1319-1394 [HIGH]
  2. `buildHotFileBreadcrumbs()` queries file degrees from the DB, computes a top-10% threshold (minimum 20), and emits `{ degree, changeCarefullyReason }` for files above threshold. Evidence: `.opencode/skills/system-code-graph/mcp_server/handlers/query.ts`:932-962 [HIGH]
  3. `hotFileBreadcrumb` is attached to both affected files and seed nodes in blast-radius responses. Evidence: `.opencode/skills/system-code-graph/mcp_server/handlers/query.ts`:1148-1158 [HIGH]
  4. The only non-test, non-spec usage of `unionMode:'multi'` in the codebase is the handler implementation itself and the tool schema definition. No production agent code, command files, or skill files invoke it. Evidence: grep across `.opencode/` for `unionMode.*multi` excluding tests/specs — only `query.ts:1319` and `tool-schemas.ts:54` [HIGH]
  5. The test at `code-graph-query-handler.vitest.ts:1004` exercises `unionMode:'multi'` with assertions on `multiFileUnion` and `hotFileBreadcrumbs`. Evidence: `.opencode/skills/system-code-graph/mcp_server/tests/code-graph-query-handler.vitest.ts`:1004 [HIGH]
  6. The manual testing playbook `022-code-graph-query-blast-radius.md` explicitly tests multi-subject union and documents the expected behavior. Evidence: `.opencode/skills/system-code-graph/manual_testing_playbook/06--mcp-tool-surface/022-code-graph-query-blast-radius.md`:13, 38 [HIGH]
  7. `hotFileBreadcrumb` is advisory-only — it carries no authority score, no behavioral gate, and does not block or modify any operation. Evidence: `.opencode/skills/system-code-graph/mcp_server/handlers/query.ts`:957-960 [HIGH]
  8. The blast-radius response also includes `multiFileUnion: true` only when `unionMode === 'multi'` AND multiple distinct source files resolved. Evidence: `.opencode/skills/system-code-graph/mcp_server/handlers/query.ts`:1498 [HIGH]
  9. `hotFileBreadcrumbs` are also emitted on `calls_from`, `calls_to`, `imports_from`, `imports_to` relationship queries, not just blast_radius. Evidence: `.opencode/skills/system-code-graph/mcp_server/handlers/query.ts`:1628, 1654 [HIGH]
  10. There is no telemetry or usage counter for `unionMode:'multi'` invocations or `hotFileBreadcrumb` emissions, so actual usage cannot be measured. Evidence: absence of telemetry code in `query.ts` handler [HIGH]
- Verdict: `unionMode:'multi'` is well-implemented and tested, but has zero detected production usage — no agent, command, or skill file invokes it. The `hotFileBreadcrumb` is a sound advisory signal for high-degree files, also emitted on relationship queries, but its behavioral impact is unmeasured. Both features earn their implementation complexity for correctness (multi-file blast radius is a real need), but their value is currently latent rather than proven. [HIGH]

### Q4: Skill-graph enhancement-edge propagation — mechanism and evidence of impact
- Findings:
  1. `detectInboundEnhances()` iterates all skill pairs, skips existing edges, and scores via three composite rules: family-inference (max 0.45), asset-shape (max 0.30), sibling-transitivity (max 0.15). Confidence threshold gates which candidates are returned. Evidence: `.opencode/skills/system-skill-advisor/mcp_server/lib/cross-skill-edges/detect-inbound-enhances.ts`:177-231 [HIGH]
  2. Family-inference requires the source to already enhance ≥3 peers in the target's family, with ≥50% family-share. Evidence: `.opencode/skills/system-skill-advisor/mcp_server/lib/cross-skill-edges/detect-inbound-enhances.ts`:70-105 [HIGH]
  3. Asset-shape checks if the target has files matching the source's `enhance_when` rules (`skill_has_asset` or `skill_has_files`). Evidence: `.opencode/skills/system-skill-advisor/mcp_server/lib/cross-skill-edges/detect-inbound-enhances.ts`:111-138 [HIGH]
  4. Sibling-transitivity checks if source enhances B and B has target as sibling. Evidence: `.opencode/skills/system-skill-advisor/mcp_server/lib/cross-skill-edges/detect-inbound-enhances.ts`:144-166 [HIGH]
  5. The compiled `skill-graph.json` contains actual `enhances` edges: `cli-devin→sk-prompt-models`, `cli-opencode→sk-prompt-models`, `deep-loop-runtime→deep-review`, `deep-loop-runtime→deep-research`, `mcp-chrome-devtools→sk-code`, `mcp-click-up→sk-code`, `sk-code→sk-code-review`, `sk-code→system-spec-kit`. Evidence: `.opencode/skills/system-skill-advisor/mcp_server/scripts/skill-graph.json`:1 [HIGH]
  6. In the graph-causal scoring lane, `enhances` has the highest positive edge multiplier (0.55) among all edge types, with BFS propagation up to depth 2 and decay `1/(depth+1)`. Evidence: `.opencode/skills/system-skill-advisor/mcp_server/lib/scorer/lanes/graph-causal.ts`:13-19, 70 [HIGH]
  7. The `skill_graph_propagate_enhances` MCP tool is gated to trusted callers only and defaults to `dryRun: true`. Evidence: `.opencode/skills/system-skill-advisor/mcp_server/handlers/skill-graph/propagate-enhances.ts`:4-5 [HIGH]
  8. The Python skill advisor script iterates `enhances` edges in graph scoring, confirming the edges feed into actual recommendation decisions. Evidence: `.opencode/skills/system-skill-advisor/mcp_server/scripts/skill_advisor.py`:518-531, 961-970 [HIGH]
  9. Applied edges are marked with `auto_added_at` and `auto_added_reason` for auditability. Evidence: `.opencode/skills/system-skill-advisor/mcp_server/lib/cross-skill-edges/apply-graph-metadata-patch.ts`:35-84 [HIGH]
  10. Validation checks reciprocal weight parity (diff > 0.1 triggers `WEIGHT-PARITY` warning). Evidence: `.opencode/skills/system-skill-advisor/mcp_server/handlers/skill-graph/validate.ts`:229-282 [HIGH]
  11. There is no A/B test or before/after measurement comparing skill discovery rates with and without enhances edges. The mechanism is structurally integrated but its marginal contribution to unprompted skill discovery is unquantified. Evidence: absence of comparative telemetry [HIGH]
- Verdict: The enhances-edge propagation mechanism is real and active — 8 `enhances` edges exist in the compiled graph, they carry the highest scoring multiplier (0.55) in the graph-causal lane, and they influence BFS-propagated recommendation scores. The detection logic (three composite rules) is sound and conservative. However, no comparative measurement exists to prove it "measurably improves" unprompted discovery versus a baseline without these edges. The mechanism works; the value is structurally plausible but empirically unproven. [HIGH]

### Q5: Doctor command coverage gaps — which subsystems lack diagnostic targets
- Findings:
  1. The `/doctor` router defines 7 primary targets: `memory`, `embeddings`, `causal-graph`, `code-graph`, `deep-loop`, `skill-advisor`, `skill-budget`. Evidence: `.opencode/commands/doctor/_routes.yaml`:27-147 [HIGH]
  2. Two MCP sub-routes exist: `install` and `debug`. One standalone orchestrator: `/doctor:update`. Evidence: `.opencode/commands/doctor/_routes.yaml`:156-193 [HIGH]
  3. The `mcp-code-mode` skill has its own `doctor.sh` install-health script but is NOT a `/doctor` router target — it has no diagnostic YAML, no MCP tools, and no trigger phrases in the route manifest. Evidence: `.opencode/skills/mcp-code-mode/scripts/doctor.sh` exists; absence from `_routes.yaml` [HIGH]
  4. The `system-spec-kit` MCP server has a `doctor.sh` but no dedicated `/doctor` target for its IPC socket server, file-watcher, or session management subsystems — these are subsumed under `memory`. Evidence: `.opencode/skills/system-spec-kit/scripts/doctor.sh` [MEDIUM]
  5. The `system-code-graph` MCP server has a `doctor.sh` and is covered by the `code-graph` target. Evidence: `.opencode/skills/system-code-graph/scripts/doctor.sh` and `_routes.yaml`:74-95 [HIGH]
  6. The `system-skill-advisor` MCP server has a `doctor.sh` and is covered by the `skill-advisor` target. Evidence: `.opencode/skills/system-skill-advisor/scripts/doctor.sh` and `_routes.yaml`:114-134 [HIGH]
  7. No `/doctor` target exists for: the file-watcher subsystem (chokidar-based), the IPC socket bridge, the launcher lease infrastructure, the model-server supervisor (hf-model-server), or the spec-folder save mutex. Evidence: absence from `_routes.yaml` [HIGH]
  8. The `embeddings` target only checks `embedder_status` — it does not diagnose the hf-model-server process lifecycle (crash-loop guard, idle eviction, demand listener). Evidence: `_routes.yaml`:45-56, `doctor_embeddings.yaml` [HIGH]
  9. No `/doctor` target covers the `deep-improvement` or `deep-ai-council` skills, though these are `sk-util` family members without their own MCP servers. Evidence: absence from `_routes.yaml` [HIGH]
  10. The `deep-loop` target is script-only (no MCP tools) and depends on `deep-loop-runtime/scripts/{status,query,convergence}.cjs`. If those scripts are missing or broken, there is no fallback diagnostic. Evidence: `_routes.yaml`:97-112 [HIGH]
  11. The Codex runtime (`.codex/`) has no doctor command copy — only `.opencode/` and `.claude/` have the doctor router. Evidence: absence of `.codex/commands/doctor/` [MEDIUM]
  12. The `/doctor:update` orchestrator covers cross-subsystem rebuilds but is the only target that touches launcher/lease infrastructure, and only during version migration, not as a standalone diagnostic. Evidence: `_routes.yaml`:184-193 [HIGH]
- Verdict: The main coverage gaps are: (1) IPC socket bridge and launcher lease infrastructure — no standalone diagnostic for lease contention, socket staleness, or bridge readiness; (2) hf-model-server process lifecycle — the `embeddings` target checks status but not crash-loop, idle eviction, or demand listener health; (3) `mcp-code-mode` has no `/doctor` target despite having its own `doctor.sh`; (4) file-watcher health has no diagnostic target. These failure modes require manual log inspection or the generic `/doctor:mcp debug` path. [HIGH]
