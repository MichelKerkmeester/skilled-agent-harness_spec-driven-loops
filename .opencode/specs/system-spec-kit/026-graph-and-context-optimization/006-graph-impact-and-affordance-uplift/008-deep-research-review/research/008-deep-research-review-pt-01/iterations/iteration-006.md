---
_memory:
  continuity:
    next_safe_action: "Iter 007 should audit 010/006 umbrella documentation against live code and test reality, especially README/SKILL/INSTALL_GUIDE claims."
---
# Iteration 006 — 010/005 trust-badge + cache invalidation audit

**Focus:** Audit trust-badge merge, age-label allowlist, dbGetter DI, R-007-12 cache invalidation generation counter.
**Iteration:** 6 of 10
**Convergence score:** 0.74

## Section A verdicts (merge-per-field)
- A1 — HANDLED. `mergeTrustBadges` is implemented as a per-field overlay: explicit non-null `confidence` wins, explicit non-`never` age strings win, boolean fields fall through when `null`, and missing required booleans omit the whole badge (`.opencode/skill/system-spec-kit/mcp_server/formatters/search-results.ts:351`, `.opencode/skill/system-spec-kit/mcp_server/formatters/search-results.ts:357`, `.opencode/skill/system-spec-kit/mcp_server/formatters/search-results.ts:367`).
- A2 — HANDLED. For `{ confidence: null, extractionAge: 'never', orphan: null, ... }`, the merge uses derived values for confidence/ages/booleans when a derived badge exists; if no derived badge exists and booleans remain undefined, it returns `undefined` and omits the badge (`.opencode/skill/system-spec-kit/mcp_server/formatters/search-results.ts:357`, `.opencode/skill/system-spec-kit/mcp_server/formatters/search-results.ts:360`, `.opencode/skill/system-spec-kit/mcp_server/formatters/search-results.ts:363`, `.opencode/skill/system-spec-kit/mcp_server/formatters/search-results.ts:367`).
- A3 — HANDLED. `{ confidence: 0.9 }` is accepted as a partial: normalization clamps only confidence, derives missing age labels to `never`, and leaves booleans as `null`; merge then uses `0.9` plus derived fields if available, or omits the badge if derived booleans are unavailable (`.opencode/skill/system-spec-kit/mcp_server/formatters/search-results.ts:301`, `.opencode/skill/system-spec-kit/mcp_server/formatters/search-results.ts:304`, `.opencode/skill/system-spec-kit/mcp_server/formatters/search-results.ts:309`, `.opencode/skill/system-spec-kit/mcp_server/formatters/search-results.ts:357`).
- A4 — HANDLED. Derived `undefined` plus incomplete explicit data cannot ship a half-formed badge because `orphan === undefined || weightHistoryChanged === undefined` returns `undefined` (`.opencode/skill/system-spec-kit/mcp_server/formatters/search-results.ts:367`).
- A5 — GAP. I found no test exercising a partial caller-supplied badge with derived fallback. The current trust-badge tests cover SQL derivation and full explicit pass-through only (`.opencode/skill/system-spec-kit/mcp_server/tests/memory/trust-badges.test.ts:91`, `.opencode/skill/system-spec-kit/mcp_server/tests/memory/trust-badges.test.ts:132`, `.opencode/skill/system-spec-kit/mcp_server/tests/memory/trust-badges.test.ts:166`).

## Section B verdicts (age-label allowlist)
- B1 — HANDLED. Exact regex: `/^(?:never|today|yesterday|\d{1,6}\s+(?:day|days|week|weeks|month|months)\s+ago)$/` (`.opencode/skill/system-spec-kit/mcp_server/formatters/search-results.ts:276`).
- B2 — HANDLED by code inspection. The regex accepts `never`, `today`, `yesterday`, `1 day ago`, `1 days ago`, `1 week ago`, and `999999 months ago`; it rejects `xx`, `1day ago`, `tomorrow`, `1 yr ago`, and `\x07 days ago` because the grammar requires one of the listed tokens or 1-6 digits + whitespace + allowed unit + whitespace + `ago` (`.opencode/skill/system-spec-kit/mcp_server/formatters/search-results.ts:276`).
- B3 — HANDLED. The 32-character cap is `MAX_AGE_LABEL_LENGTH = 32`, enforced before the regex test (`.opencode/skill/system-spec-kit/mcp_server/formatters/search-results.ts:277`, `.opencode/skill/system-spec-kit/mcp_server/formatters/search-results.ts:282`).
- B4 — HANDLED. Rejected explicit age strings fall through to `formatAgeString` using `fallbackIso` when it is a string, otherwise `null`; raw rejected strings are not surfaced (`.opencode/skill/system-spec-kit/mcp_server/formatters/search-results.ts:285`, `.opencode/skill/system-spec-kit/mcp_server/formatters/search-results.ts:288`).

## Section C verdicts (dbGetter DI seam)
- C1 — HANDLED. `fetchTrustBadgeSnapshots(results, dbGetter)` includes an optional `dbGetter` parameter (`.opencode/skill/system-spec-kit/mcp_server/formatters/search-results.ts:402`, `.opencode/skill/system-spec-kit/mcp_server/formatters/search-results.ts:404`).
- C2 — HANDLED. The default is `dbGetter: () => ReturnType<typeof requireDb> = requireDb` (`.opencode/skill/system-spec-kit/mcp_server/formatters/search-results.ts:404`).
- C3 — HANDLED. `rg` found production usage only at `formatSearchResults`, which calls `fetchTrustBadgeSnapshots(results)` without a custom getter; only `tests/memory/trust-badges.test.ts` passes `testDbGetter`, so the seam is genuinely test-only today (`.opencode/skill/system-spec-kit/mcp_server/formatters/search-results.ts:751`, `.opencode/skill/system-spec-kit/mcp_server/tests/memory/trust-badges.test.ts:115`, `.opencode/skill/system-spec-kit/mcp_server/tests/memory/trust-badges.test.ts:150`).
- C4 — HANDLED. The bind-type fix exists as `const boundIds = resultIds.map((id) => String(id));`, with the SQL call using `.all(...boundIds)` (`.opencode/skill/system-spec-kit/mcp_server/formatters/search-results.ts:451`, `.opencode/skill/system-spec-kit/mcp_server/formatters/search-results.ts:455`, `.opencode/skill/system-spec-kit/mcp_server/formatters/search-results.ts:492`).
- C5 — GAP. The test file directly exercises the SQL pipeline in two cases, not three: connected metadata and orphan metadata. The third trust-badge case is the public formatter explicit-pass-through path and the comment states it does not depend on the database (`.opencode/skill/system-spec-kit/mcp_server/tests/memory/trust-badges.test.ts:91`, `.opencode/skill/system-spec-kit/mcp_server/tests/memory/trust-badges.test.ts:132`, `.opencode/skill/system-spec-kit/mcp_server/tests/memory/trust-badges.test.ts:166`, `.opencode/skill/system-spec-kit/mcp_server/tests/memory/trust-badges.test.ts:167`).

## Section D verdicts (cache invalidation generation counter)
- D1 — HANDLED. `causalEdgesGeneration` is declared as module state with R-007-12 comments (`.opencode/skill/system-spec-kit/mcp_server/lib/storage/causal-edges.ts:150`, `.opencode/skill/system-spec-kit/mcp_server/lib/storage/causal-edges.ts:159`).
- D2 — HANDLED. `bumpCausalEdgesGeneration` increments/wraps the counter, and `invalidateDegreeCache` calls it before degree/signal cache clearing; mutators call `invalidateDegreeCache` after successful insert, batch insert, bulk insert, update, delete, delete-for-memory, and rollback (`.opencode/skill/system-spec-kit/mcp_server/lib/storage/causal-edges.ts:161`, `.opencode/skill/system-spec-kit/mcp_server/lib/storage/causal-edges.ts:177`, `.opencode/skill/system-spec-kit/mcp_server/lib/storage/causal-edges.ts:351`, `.opencode/skill/system-spec-kit/mcp_server/lib/storage/causal-edges.ts:405`, `.opencode/skill/system-spec-kit/mcp_server/lib/storage/causal-edges.ts:460`, `.opencode/skill/system-spec-kit/mcp_server/lib/storage/causal-edges.ts:688`, `.opencode/skill/system-spec-kit/mcp_server/lib/storage/causal-edges.ts:707`, `.opencode/skill/system-spec-kit/mcp_server/lib/storage/causal-edges.ts:728`, `.opencode/skill/system-spec-kit/mcp_server/lib/storage/causal-edges.ts:945`).
- D3 — HANDLED. `getCausalEdgesGeneration` returns the module counter and is exported (`.opencode/skill/system-spec-kit/mcp_server/lib/storage/causal-edges.ts:169`, `.opencode/skill/system-spec-kit/mcp_server/lib/storage/causal-edges.ts:1039`).
- D4 — HANDLED. `CacheArgsInput` accepts `causalEdgesGeneration`, and `buildCacheArgs` includes it only when `enableCausalBoost === true` and the value is finite (`.opencode/skill/system-spec-kit/mcp_server/lib/search/search-utils.ts:58`, `.opencode/skill/system-spec-kit/mcp_server/lib/search/search-utils.ts:66`, `.opencode/skill/system-spec-kit/mcp_server/lib/search/search-utils.ts:181`, `.opencode/skill/system-spec-kit/mcp_server/lib/search/search-utils.ts:213`).
- D5 — HANDLED. `memory-search.ts` reads the generation only for causal boost and threads it into `buildCacheArgs` (`.opencode/skill/system-spec-kit/mcp_server/handlers/memory-search.ts:847`, `.opencode/skill/system-spec-kit/mcp_server/handlers/memory-search.ts:852`, `.opencode/skill/system-spec-kit/mcp_server/handlers/memory-search.ts:887`).
- D6 — HANDLED. When `enableCausalBoost` is false, `causalEdgesGenerationForCache` is `undefined`, and `buildCacheArgs` also gates inclusion on `enableCausalBoost === true`; non-causal callers avoid needless invalidation (`.opencode/skill/system-spec-kit/mcp_server/handlers/memory-search.ts:852`, `.opencode/skill/system-spec-kit/mcp_server/lib/search/search-utils.ts:181`, `.opencode/skill/system-spec-kit/mcp_server/lib/search/search-utils.ts:213`).
- D7 — HANDLED with a boundary caveat. In the single-process synchronous mutation path, successful mutations bump before returning from their mutator, so later causal searches read the new generation. A request that already read generation N can still race with a mutation before its async pipeline completes, but any cache entry written under N is abandoned by future N+1 cache keys; this is possible current-response staleness, not persistent cross-generation cache poisoning (`.opencode/skill/system-spec-kit/mcp_server/handlers/memory-search.ts:852`, `.opencode/skill/system-spec-kit/mcp_server/handlers/memory-search.ts:949`, `.opencode/skill/system-spec-kit/mcp_server/handlers/memory-search.ts:1199`).

## Section E verdicts (TrustBadgeFetchResult shape)
- E1 — HANDLED. `TrustBadgeFetchResult` shape is `{ snapshots, attempted, derivedCount, failureReason }` via readonly fields: `snapshots: Map<number, TrustBadgeSnapshot>`, `attempted: boolean`, `derivedCount: number`, and `failureReason: 'no_db' | 'no_results' | 'query_error' | null` (`.opencode/skill/system-spec-kit/mcp_server/formatters/search-results.ts:380`, `.opencode/skill/system-spec-kit/mcp_server/formatters/search-results.ts:388`).
- E2 — HANDLED. Branches set `failureReason: null` for no numeric IDs, `'no_db'` when `dbGetter` throws, `'no_results'` when a query returns no snapshots, `null` when derived snapshots exist, and `'query_error'` when the query throws (`.opencode/skill/system-spec-kit/mcp_server/formatters/search-results.ts:410`, `.opencode/skill/system-spec-kit/mcp_server/formatters/search-results.ts:427`, `.opencode/skill/system-spec-kit/mcp_server/formatters/search-results.ts:511`, `.opencode/skill/system-spec-kit/mcp_server/formatters/search-results.ts:518`).
- E3 — HANDLED. `MemoryResultTrace.trustBadgeDerivation` is declared with `attempted`, `derivedCount`, and structured `failureReason`, then stamped onto traces when `includeTrace` is true (`.opencode/skill/system-spec-kit/mcp_server/formatters/search-results.ts:168`, `.opencode/skill/system-spec-kit/mcp_server/formatters/search-results.ts:814`).
- E4 — HANDLED. Callers are consistent with the harmonized return shape: production uses `trustBadgeFetch.snapshots.get(resultId)`, and tests use `fetchResult.snapshots.get(...)`; no remaining bare `fetchTrustBadgeSnapshots(...).get(...)` or `snapshots.get(...)` mismatch was found by `rg` (`.opencode/skill/system-spec-kit/mcp_server/formatters/search-results.ts:786`, `.opencode/skill/system-spec-kit/mcp_server/tests/memory/trust-badges.test.ts:119`, `.opencode/skill/system-spec-kit/mcp_server/tests/memory/trust-badges.test.ts:154`).

## New findings
- F16 (P2, test-add, RQ5): R-007-11 lacks a regression test for partial explicit trust badges merging with derived fallback. Remediation: add a `formatSearchResults` or exported testable case where explicit `{ confidence: 0.9 }` overlays a derived badge and preserves derived age/booleans. Evidence: merge supports the behavior (`.opencode/skill/system-spec-kit/mcp_server/formatters/search-results.ts:357`, `.opencode/skill/system-spec-kit/mcp_server/formatters/search-results.ts:364`), but tests cover only SQL derivation and full explicit pass-through (`.opencode/skill/system-spec-kit/mcp_server/tests/memory/trust-badges.test.ts:91`, `.opencode/skill/system-spec-kit/mcp_server/tests/memory/trust-badges.test.ts:166`).
- F17 (P2, test-add / closure-integrity, RQ2/RQ5): If the 010/007/T-E closure claims three SQL-pipeline cases, the test evidence does not match; only two tests pass `testDbGetter` into `fetchTrustBadgeSnapshots`, while the third explicitly avoids DB dependence. Remediation: either correct the closure claim or add a third SQL-pipeline case, such as `query_error` or no-weight-history table fallback. Evidence: SQL cases at `.opencode/skill/system-spec-kit/mcp_server/tests/memory/trust-badges.test.ts:115` and `.opencode/skill/system-spec-kit/mcp_server/tests/memory/trust-badges.test.ts:150`; formatter-only comment at `.opencode/skill/system-spec-kit/mcp_server/tests/memory/trust-badges.test.ts:167`.
- F18 (P2, test-add, RQ3/RQ5): R-007-P2-10 age allowlist is implemented, but the trust-badge suite has no explicit accepted/rejected boundary tests for malformed age labels and fallback derivation. Remediation: add table tests for `never`, `today`, `1 days ago`, `999999 months ago`, `tomorrow`, `1day ago`, `1 yr ago`, and control-character input with `extractedAt` fallback. Evidence: sanitizer exists (`.opencode/skill/system-spec-kit/mcp_server/formatters/search-results.ts:276`, `.opencode/skill/system-spec-kit/mcp_server/formatters/search-results.ts:282`, `.opencode/skill/system-spec-kit/mcp_server/formatters/search-results.ts:288`), but current trust-badge tests only exercise full valid labels (`.opencode/skill/system-spec-kit/mcp_server/tests/memory/trust-badges.test.ts:178`, `.opencode/skill/system-spec-kit/mcp_server/tests/memory/trust-badges.test.ts:180`).

## Negative findings
- HANDLED: Trust-badge merge is per-field, not wholesale replacement.
- HANDLED: Incomplete explicit badges are omitted when no derived badge can complete required booleans.
- HANDLED: Age-label allowlist rejects arbitrary strings and derives from timestamp fallback.
- HANDLED: `dbGetter` defaults to `requireDb` and production callers do not inject custom DB getters.
- HANDLED: SQL bind-type fix uses `resultIds.map(String)` before the `VALUES` bind.
- HANDLED: Causal-edge generation is omitted from non-causal cache keys.
- HANDLED: Cache generation is bumped by successful causal-edge mutators and exported for memory-search cache keys.
- HANDLED: `TrustBadgeFetchResult` shape is harmonized across production and tests.

## RQ coverage cumulative through iter 6
- RQ1 — Covered for 010/001-005 focus surfaces: no new P0/P1 found in this iteration; prior P1/P2 context remains from iters 1-5.
- RQ2 — Extended: F17 adds a P2 closure-integrity precision gap for the T-E test claim, alongside prior F12 and F14-iter5.
- RQ3 — Extended: trust-badge sanitization and cache invalidation boundaries are code-backed; F18 adds missing adversarial age-label tests.
- RQ4 — Pending primary drill in iter 007; no umbrella-doc conclusion drawn here.
- RQ5 — Extended: F16, F17, and F18 are all regression-test sufficiency gaps.

## Next iteration recommendation
Iter 007 should drill 010/006 umbrella docs vs code reality per `strategy.md`, especially root README, system-spec-kit SKILL.md, mcp_server README/INSTALL_GUIDE, feature catalog entries, and the five baseline manual-testing playbook scenarios.

EXIT_STATUS=DONE | findings=3 | convergence=0.74 | checklist=23/2 | next=iter-007
