Independent Opus cross-check complete. I read the actual source for all three files in scope (checkpoints.ts v2 create/restore/journal/sentinel/pruning/catalog-merge; vector-index-store.ts boot recovery + shard attach + reopen; context-server.ts gate/serverInfo/cache-invalidation), traced the crash-safety interleavings, and confirmed the ground-truth facts. Findings:

[P1] Swap-failure rollback path does not demote journal swap-done→swap-pending → main-DB data-loss on crash | .opencode/skills/system-spec-kit/mcp_server/lib/storage/checkpoints.ts:2645 | evidence: `swapFn` writes the `swap-done` journal as its last step (checkpoints.ts:2639) and runs inside `reopen()` BEFORE `initialize_db()` (vector-index-store.ts:1600-1601). When `initialize_db` throws AFTER swapFn completed (dimension-mismatch FATAL throws vector-index-store.ts:1489/1502/1514, schema error on snapshot, or listener-rebind failure thrown at vector-index-store.ts:750), control enters the catch at 2645 with journal=swap-done. That handler's rollback lambda (restoreBackups + clearRestoreJournal) does NOT demote the journal — unlike the sibling in-process handler at 2699-2706 which explicitly demotes for exactly this reason. restoreBackups does `fs.rmSync(liveMainPath)` then `fs.renameSync(backup→live)` (2515-2516); a crash in that gap leaves live missing + journal swap-done, and boot recovery's swap-done branch `fs.rmSync`s the backup (vector-index-store.ts:921) → both copies destroyed | fix: write swap-pending journal before the rollback reopen in the 2645 handler, identical to 2699-2706; optionally drop the rm-before-rename in restoreBackups (POSIX rename atomically replaces)

[P2] createCheckpointV2 publishes snapshot dir before catalog INSERT; failed INSERT orphans dir and wedges same-name re-creates | checkpoints.ts:2264 | evidence: `fs.renameSync(tmpDir, finalDir); published=true` (2264-2265) precedes the INSERT transaction (2277-2310), which is NOT in the busy-retry loop (unlike v1). Catch cleans tmpDir only `if (!published)` (2316-2318), so a failed INSERT leaves finalDir with no catalog row; the next same-name create's `fs.renameSync(tmpDir, finalDir)` fails ENOTEMPTY against the non-empty orphan, never swept (not a `.tmp-` dir) | fix: on post-publish failure, rm finalDir in the catch; or wrap the INSERT in the busy-retry loop

[P2] swap-done crash before mergeCheckpointCatalogRows loses post-snapshot checkpoint catalog rows | checkpoints.ts:2665 | evidence: `liveCheckpointCatalogRows` is in-memory only (2548); on a swap-done crash before the merge, boot recovery deletes the `.bak` (vector-index-store.ts:921) without re-merging, so checkpoints created after the snapshot lose their catalog rows while their snapshot dirs persist → orphaned/unrestorable | fix: persist catalog rows in the journal, or re-merge from `.bak` in swap-done recovery before deleting it

[P2] Pruned snapshot dirs removed after transaction commit → orphaned dirs on crash | checkpoints.ts:2312 | evidence: `removeCheckpointSnapshotDirs(prunedSnapshotPaths)` runs after the commit returns (2312, also v1 path 2447); a crash between commit and removal leaks pruned dirs that are never reclaimed (not `.tmp-`) | fix: best-effort sweep of catalog-less snapshot dirs at boot/startup-scan

[P2] v1 restore omits needs-rebuild sentinel on incomplete derived rebuild (asymmetry vs v2) | checkpoints.ts:3130 | evidence: `runPostRestoreRebuilds(database, checkpointSpecFolder)` return value is ignored; v2 writes the `.needs-rebuild` sentinel when `!isDerivedRebuildComplete` (2667-2677). v1 is legacy/in-place so impact is low | fix: capture the summary and write the sentinel symmetrically

Verified clean: two-phase journal write (temp+fsync+rename+dir-fsync, 810-818) and swap-pending boot recovery (vector-index-store.ts:934-958); atomic sentinel write/clear with no-overwrite recovery guard; full DB-consumer rebind after reopen via connection-change listener → rebindDatabaseConsumers (db-state.ts:145-181), no stale-handle gap; VACUUM-INTO busy retry, vec_memories-only v2 gate, embedder-slug + schema-downgrade guards, ALLOWED_TABLE_NAMES allowlist, SAVEPOINT merge atomicity; serverInfo 1.8.0 (context-server.ts:1014) and SPECKIT_BACKEND_ONLY stdio gate (2126-2131).

VERDICT: CONDITIONAL (P0=0 P1=1 P2=4)
