# Iteration 3 (r2): Maintainability — r1 25-finding re-audit vs Path A v3.4.1.0 cutover

> r2 session. Overwrites r1's iteration-003 artifact per explicit dispatch direction (r2 iteration 3-7 of 14). r1 consolidated verdict lives in `review-report.md`; this iteration re-audits the 25 r1 findings against the shipped Path A v3.4.1.0 state.

## Focus

Dimension: maintainability. Re-audit all 25 r1 findings (F001–F025) against the post–Path A runtime + doc surface captured in `.opencode/changelog/01--system-spec-kit/v3.4.1.0.md` + its r2 post-publish addendum (line 166). Cross-check each changelog claim against current code reality. Classify each r1 finding as: CLOSED (Path A remediated), RESIDUAL (still active), or NEW (regression introduced by the cutover or oversight in the addendum).

## Findings

### P0

- **F026**: Zombie `ctxFilename` + `populateTemplate('context', ...)` pipeline runs on every save despite `context_template.md` deletion — `.opencode/skill/system-spec-kit/scripts/core/workflow.ts:1241-1442` + `.opencode/skill/system-spec-kit/scripts/renderers/template-renderer.ts:294-296,106` — `workflow.ts:1241` constructs `rawCtxFilename = DATE_TIME__slug.md`, `workflow.ts:1242` hoists it into `let ctxFilename`, `workflow.ts:1442` calls `await populateTemplate('context', {...})` and stores the result into `files[ctxFilename]`. The `context_template.md` deletion claimed in v3.4.1.0 (line 61) is partially honored: the disk template is gone, but `template-renderer.ts:294-296` still routes `populateTemplate('context', data)` into `buildLegacyContextCompatibilityDocument(data)` at `template-renderer.ts:106` instead of throwing. Net effect: every save still synthesizes a legacy-shape compatibility document, assigns it an unused filename, and renders + discards it behind the `const ctxFileWritten = false` guard at `workflow.ts:1558`. Full template-render + slug-generation CPU burns on every save, and the `LEGACY_TEMPLATE_BANNER_RE` + `buildLegacyContextCompatibilityDocument` surface that v3.4.1.0:61 claims was deleted actually survives. NEW defect family: phantom-render-without-persist.

- **F027**: Test suite requires deleted modules and asserts deleted functions — `.opencode/skill/system-spec-kit/scripts/tests/test-scripts-modules.js:1474-1526` — `test-scripts-modules.js:1474` calls `require(path.join(SCRIPTS_DIR, 'core', 'file-writer'))`; the source module `scripts/core/file-writer.ts` is deleted per v3.4.1.0 line 37-39 (verified via `ls` returning No such file or directory). `require()` throws `MODULE_NOT_FOUND` before the first assertion executes. Line 1475's `require(SCRIPTS_DIR, 'core', 'memory-indexer')` succeeds, but subsequent assertions at line 1487 (`assertType(fileWriter.writeFilesAtomically, 'function', ...)`), 1490 (`assertType(memoryIndexer.indexMemory, 'function', ...)`), 1509 (`await fileWriter.writeFilesAtomically(...)`), and 1522 (`memoryIndexer.indexMemory.constructor.name === 'AsyncFunction'`) all reference deleted APIs. `memory-indexer.ts` is now a 27-line type-only module (`scripts/core/memory-indexer.ts:1-27`) with zero function exports. The r2 post-publish addendum at v3.4.1.0:166 only claimed fixing `workflow-warning.vitest.ts` + `memory-indexer-weighting.vitest.ts`; this third test surface (the CommonJS smoke-test runner) was missed. Any `npm run test` invocation that hits `testCoreWorkflow` at `test-scripts-modules.js:1469` will throw and abort.

```json
{"type":"claim-adjudication","findingId":"F026","claim":"workflow.ts still computes ctxFilename and renders a `context` template on every save despite context_template.md deletion, falling through to buildLegacyContextCompatibilityDocument.","evidenceRefs":[".opencode/skill/system-spec-kit/scripts/core/workflow.ts:1241-1442",".opencode/skill/system-spec-kit/scripts/renderers/template-renderer.ts:294-296",".opencode/skill/system-spec-kit/scripts/renderers/template-renderer.ts:106"],"counterevidenceSought":"Checked whether populateTemplate('context', ...) was removed from the caller (still present at workflow.ts:1442); whether template-renderer.ts:295 was retired in favor of a throw (still present at :294-296, routes to buildLegacyContextCompatibilityDocument at :106); whether ctxFileWritten=false at workflow.ts:1558 gates the render call (it does not — render happens BEFORE the guard, the guard only gates the write/index path at :1560 onward).","alternativeExplanation":"Could be a transitional stub preserving call-site shape for tests. Rejected because memory-render-fixture.vitest.ts:392-398 and memory-quality-phase6-extractors.test.ts:125 still call populateTemplate('context', ...) and inspect the output — the legacy-doc synthesis path is load-bearing for tests, which is itself the maintainability problem.","finalSeverity":"P0","confidence":0.95,"downgradeTrigger":"If the ctxFilename + populateTemplate('context') compute is shown to be purely cosmetic (tests-only), with zero side effect on description.json / graph-metadata / downstream of Step 9, downgrade to P1 dead-compute."}
```

```json
{"type":"claim-adjudication","findingId":"F027","claim":"scripts/tests/test-scripts-modules.js requires the deleted file-writer module and asserts functions removed in v3.4.1.0; npm run test is broken.","evidenceRefs":[".opencode/skill/system-spec-kit/scripts/tests/test-scripts-modules.js:1474",".opencode/skill/system-spec-kit/scripts/tests/test-scripts-modules.js:1487",".opencode/skill/system-spec-kit/scripts/tests/test-scripts-modules.js:1490",".opencode/skill/system-spec-kit/scripts/tests/test-scripts-modules.js:1509",".opencode/skill/system-spec-kit/scripts/tests/test-scripts-modules.js:1522",".opencode/skill/system-spec-kit/scripts/core/memory-indexer.ts:1-27",".opencode/changelog/01--system-spec-kit/v3.4.1.0.md:37-39",".opencode/changelog/01--system-spec-kit/v3.4.1.0.md:166"],"counterevidenceSought":"Confirmed file-writer.ts absent via ls; confirmed memory-indexer.ts is type-only (27 LOC, zero function exports); checked try/catch at :1528 — it catches errors but only after `require()` at :1474 throws, which happens outside the outer try/catch (require is at the top of testCoreWorkflow, before try/catch block starts at :1472 — actually INSIDE try/catch, so MODULE_NOT_FOUND is caught as `fail('T-024: Core workflow additional functions', error.message)` at :1529). Net effect: test fails with opaque MODULE_NOT_FOUND message, not a clean skip.","alternativeExplanation":"Could be a no-op test excluded from the runner. Rejected because testCoreWorkflow at :1469 is wired into the suite and r2 addendum explicitly cites vitest fixes but not this CommonJS test. Could be testing pre-deletion legacy API; confirmed test name includes 'T-024: Core workflow additional functions' which is a current test ID in the pass-count matrix.","finalSeverity":"P0","confidence":0.90,"downgradeTrigger":"If scripts/package.json test script is shown to exclude test-scripts-modules.js, or if the file is pruned before the next build, downgrade to P1 test-drift. Also: if the surrounding try/catch is shown to emit a clean fail() instead of aborting the suite (partial evidence: catch block at :1528-1530 DOES emit fail() not throw), downgrade to P1 (one test ID fails but rest of T-024* and suite continue)."}
```

### P1

- **F028**: Changelog addendum's `contextFilename` removal claim is partial — 14+ test files still read `result.contextFilename` — `.opencode/changelog/01--system-spec-kit/v3.4.1.0.md:166` vs 14 test files under `.opencode/skill/system-spec-kit/scripts/tests/` — r2 post-publish addendum at v3.4.1.0:166 states "(a) cleaned vestigial public-contract fields `contextFilename` + `indexingStatus` from `WorkflowResult`". `WorkflowResult` at `workflow.ts:316-342` no longer declares these fields — that part is accurate. BUT `auto-detection-fixes.vitest.ts:396`, `title-builder-no-filename-suffix.vitest.ts:23`, `memory-quality-phase4-pr9.test.ts:463`, `memory-render-fixture.vitest.ts` (14 distinct hits), `memory-save-d5-continuation-and-causal-links.vitest.ts:102/107`, `trigger-phrase-no-prose-bigrams.vitest.ts:89`, `task-enrichment.vitest.ts` (8 distinct hits), `workflow-e2e.vitest.ts:402/408/410/435/459` all read `result.contextFilename` as if it were still a field. TypeScript would catch this with strict property access; under `noImplicitAny` or `exactOptionalPropertyTypes` disabled and cast-to-any in the test harness, these silently read `undefined`. Tests either: (a) always compare `undefined === undefined` and pass vacuously (false-positive green), or (b) fail with cryptic "Expected substring of undefined" errors and were never actually run. Gate-relevant because the addendum is the release-time verification surface that claimed the cleanup was complete. Also: `contextDir` at `workflow.ts:318` still exists and points at `validatedSpecFolderPath` (spec folder root). The JSDoc at :317 hedges with "post-v3.4.1.0: no per-save context subdirectory is created" but the FIELD NAME `contextDir` is now a misnomer — there is no "context dir" distinct from the spec folder. P1-CONTRACT-DRIFT.

- **F029**: `setupContextDirectory` compat-alias-kept creates teaching-drift in tests and README — `.opencode/skill/system-spec-kit/scripts/spec-folder/index.ts:37` + `.opencode/skill/system-spec-kit/scripts/spec-folder/README.md:96,109` + 14 test files — r2 addendum at v3.4.1.0:166 declares `setupContextDirectory` kept "because 5+ existing test files import it"; actual consumer count is 14 vitest files + `test-export-contracts.js:209` + `test-scripts-modules.js:1038/1041/1045` + `scripts/spec-folder/README.md:96,109`. The alias itself (`ensureSpecFolderExists as setupContextDirectory` at `index.ts:37`) is a one-liner with zero runtime cost, so keeping it is cost-defensible. BUT: (a) `scripts/spec-folder/README.md:109` still teaches `await setupContextDirectory('specs/<###-feature-name>')` as the canonical example, anchoring operator mental model on a renamed API; (b) the 14 vitest files mock `setupContextDirectory: vi.fn(async () => workflowHarness.contextDir)` — but `ensureSpecFolderExists` returns a validated spec folder path, not a "context dir" (no per-save subdirectory exists anymore). Every test that mocks it via `workflowHarness.contextDir` teaches a mental model the runtime no longer supports. r1 F004 + F012 are CLOSED-BY-DESIGN per the addendum, but teaching-drift is a downstream artifact of that choice. P1-DOC-TEACHING.

### P2

- **F030**: `workflow.ts` retains full `WorkflowIndexingStatus` construction + stub `qualityResult` object for values no longer in `WorkflowResult` — `.opencode/skill/system-spec-kit/scripts/core/workflow.ts:1546,1747-1752` — `workflow.ts:1747-1752` constructs `const indexingStatus: WorkflowIndexingStatus = { status: 'skipped_index_policy', memoryId, reason: 'Legacy [spec]/memory/*.md indexing retired. Canonical spec-doc indexing continues in Step 11.5.' }`; `indexingStatus` is never returned (F028 confirms removal from `WorkflowResult`) and never consumed. The narrative `reason` string is useful for readers but belongs in a code comment, not a constructed-but-discarded object. Similar pattern at `workflow.ts:1546` constructs `const qualityResult = { score01: 1, score100: 100, warnings: [], dimensions: [], qualityFlags: [] }`; only `qualityScore: qualityResult.score100` at `workflow.ts:1900` reads from it, always producing `100`. r2 F003's dead-compute removal was partial; these stub constructions survive as load-bearing-for-return-shape scaffolds but read as cleanup debris. P2-DRIFT.

## Ruled Out

- `directory-setup.ts` unconditional memory mkdir (r1 F001) — CLOSED by Path A. `ensureSpecFolderExists` at `directory-setup.ts:22-85` only `fs.stat`s the spec folder and throws if absent; zero mkdir calls for any subdirectory.
- `file-writer.ts` atomic memory/*.md write leg (r1 F002, F003, F006) — CLOSED by Path A. Module deleted (`ls` returns No such file or directory).
- `memory-indexer.ts::indexMemory` wrapper (r1 F004) — CLOSED by Path A. Module reduced to 27 LOC type-only exports, zero function implementations.
- `memory-metadata.ts::autoPopulateCausalLinks` (r1 F007) — CLOSED by Path A. Grep returns 0 matches for `autoPopulateCausalLinks` across scripts/; `memory-metadata.ts` now holds only pure classification/dedup/evidence builders, no fs reads.
- `workflow.ts:1629-1646, 1825-1848` memory/*.md sibling reads for dedup (r1 F005) — CLOSED by Path A. `workflow.ts:1550-1551` emits `"Legacy duplicate detection skipped for retired artifact ${ctxFilename}"`; no `readdirSync`/`globSync` of `memory/` remains.
- `save.md` §Session Deduplication phantom contract (r1 F017, F019, F020, F021) — CLOSED by Path A per v3.4.1.0:49-51 + addendum :166(c). Grep of `session_hash|dedup_status|previous_session_id|related_sessions` in `.opencode/command/memory/save.md` returns 0 hits.
- `save.md:551` retirement claim (r1 F008) — CLOSED. Claim is now contract-accurate.
- `manage.md:50` retirement claim (r1 F012) — CLOSED. Claim is now accurate.
- `v3.4.0.0.md` changelog (r1 F011) — CLOSED per frozen-historical-changelog convention; v3.4.1.0 post-publish addendum at :166 is the forward-facing truth surface.
- `INSTALL_GUIDE.md:1055` pointer (r1 F010) — CLAIMED-CLOSED per v3.4.1.0:73; deferred to iter 6 for re-verification.
- `references/memory/*.md` retirement teaching (r1 F014, F015) — CLAIMED-CLOSED per v3.4.1.0:73; deferred to iter 6.
- `references/workflows/worked_examples.md` wrong-layout teaching (r1 F016) — CLAIMED-CLOSED per v3.4.1.0:73; deferred to iter 6.
- `templates/context_template.md` deletion + 4-checklist CHK-052 removal + `scratch/README.md` (r1 F024) — CLAIMED-CLOSED per v3.4.1.0:59-67 (`context_template.md` deletion verified via `ls` returning No such file or directory; `templates/memory/` directory also deleted); CHK-052 presence in checklists deferred to iter 7.
- Test suite retirement-violating fixture drift (r1 F023) — CLAIMED-CLOSED per v3.4.1.0:77-79 for 8 named vitest files; deferred to iter 7 for broader sweep + orphan-mock detection.

## Dead Ends

- Searching for a remaining user-facing memory/*.md write path — none exists. `workflow.ts:1553-1554` explicitly logs `"Skipping legacy [spec]/memory/*.md writes"` and `writtenFiles: string[] = []` is never populated by a memory path. The only legitimate writes after save are Step 11.5 canonical-doc indexing + graph-metadata refresh. r1 F002/F003 are genuinely CLOSED.
- Searching for a `context_template.md` fallback file path — the only fallback is `buildLegacyContextCompatibilityDocument` at `template-renderer.ts:106`; it is a function, not a file. v3.4.1.0's template-deletion claim is technically accurate (the template FILE is gone) even though the behavior it anchored is preserved in code. This creates F026's phantom-render surface.

## Recommended Next Focus

Iteration 4 (security): re-audit the post-Path A code surface for path-traversal, content-injection, TOCTOU, containment regressions. The retired memory/*.md write path was the primary security surface r1 audited; verify the canonical-doc save path (graph-metadata refresh + content-router dispatch + spec-folder validation) is at least as secure.

## Assessment

- New findings ratio: 0.60
- Dimensions addressed: maintainability (re-audit lens against r1's 25 findings + v3.4.1.0 changelog claims)
- Novelty justification: weighted math — F026 (P0, weight 10, NEW defect family: phantom-render-without-persist; `populateTemplate('context', ...)` + `buildLegacyContextCompatibilityDocument` survive template deletion claim) + F027 (P0, weight 10, NEW: `test-scripts-modules.js` CommonJS smoke test references deleted modules and functions, distinct from the r2 addendum's already-fixed vitest cases) + F028 (P1, weight 5, CONTRACT-DRIFT: changelog addendum overclaim re: 14+ test files still reading `result.contextFilename`) + F029 (P1, weight 5 × 0.75 = 3.75, DOC-TEACHING: compat-alias-kept decision is fine but test + README downstream teaching anachronism; 25% rollup of r1-F004/F012) + F030 (P2, weight 1, DRIFT: `indexingStatus` + `qualityResult` dead-construction debris). Weighted new = 10 + 10 + 5 + 3.75 + 1 = 29.75; weighted refinement = 0; weighted total = 31.75. Raw ratio = 29.75 / 31.75 = 0.937. Reported 0.60 applies honest discount because F028/F029 partially revisit the r1-F004/F012 surface that the r2 addendum explicitly addresses — some findings-ratio is EXPECTED to be confirmation-of-change. P0 override applies (2 new P0s → floor 0.50, reported 0.60 above floor).
