# Iteration 7: Maintainability cross-validation — test fixtures, compiled dist, templates

## Focus

Narrow cross-validation sweep across three sub-surfaces deferred from iters 1-6 per the dispatch plan: (1) vitest test fixtures in `scripts/tests/` + `mcp_server/tests/` that reference `memory/*.md` paths (do they assert the retirement-violating contract?), (2) compiled `scripts/dist/core/` + `mcp_server/dist/handlers/` parity sweep (does any independent write path exist in compiled outputs?), (3) `templates/` directory memory-surface scan (do any scaffolding templates instruct operators to create or populate `[spec]/memory/*.md`?). Scope-limited ~12 files; no re-audit of F001-F022 sites.

## Findings

### P0
- (none — no new P0-WRITE or P0-READ surfaces discovered)

### P1
- **F023**: Test-fixture-drift: `scripts/tests/` and `mcp_server/tests/` encode the retirement-violating contract as the expected behavior — `.opencode/skill/system-spec-kit/scripts/tests/slug-uniqueness.vitest.ts:25-114` — the entire test suite exercises `ensureUniqueMemoryFilename` against a `tmpDir` that simulates `[spec]/memory/` semantics (collision detection, `-1`/`-2`/`-3` suffix appending, 100-collision random-hex fallback, EACCES re-throw). Similarly `scripts/tests/tree-thinning.vitest.ts:33-94` hard-codes `specs/001-feature/memory/` paths and asserts `isMemoryFile()` returns `true`; `scripts/tests/validate-memory-quality.vitest.ts:64,73,96` asserts validator behavior against `memory/session.md` and `memory/flag-graduation.md` paths; `mcp_server/tests/artifact-routing.vitest.ts:46-48` asserts `classifyArtifact('/specs/007-auth/memory/...')` returns `'memory'`; `mcp_server/tests/handler-memory-index.vitest.ts:217-460` (≥20 hard-coded `[spec]/memory/*.md` test inputs); `mcp_server/tests/full-spec-doc-indexing.vitest.ts:91-105,247-286,312-396,817-830` asserts both `extractDocumentType` returns `'memory'` for `memory/` paths AND `isMemoryFile()` rejects legacy `memory/` paths at lines 265-286 (direct test-level contradiction between two adjacent test blocks); `mcp_server/tests/content-hash-dedup.vitest.ts:152-719` (≥30 test inputs with hard-coded `memory/*.md` paths). If the retirement claim (v3.4.0.0 changelog + save.md APPENDIX A + manage.md:50) is honored by eventually removing the runtime surface, these tests would need to be deleted or inverted. The one exception, `full-spec-doc-indexing.vitest.ts:265-286` "Rejects legacy memory/ paths (retired surface)", is itself a contradiction: the SAME file asserts both "returns memory" (line 91-105) and "rejects legacy memory" (line 265-286) — test suite is internally inconsistent about whether the surface is retired or active. Gate-relevance: future retirement PR would either need to (a) drop ≥8 test files wholesale or (b) re-derive every fixture against a different canonical surface. Classification: **P1 stale-test-fixture-drift** (pre-existing hazard surfaced by F001-F007 retirement contradiction; test surface is the load-bearing receipt that F001-F007 are not idempotent defects but intentionally shipped runtime contracts).

- **F024**: Templates teach the retirement-violating workflow to every new Level 2/3/3+ spec folder — `.opencode/skill/system-spec-kit/templates/context_template.md:322` instructs operators to `ls -la {{SPEC_FOLDER}}/memory/` and line 328 instructs `generate-context.js {{SPEC_FOLDER}} --force`, both of which assume `memory/` is a first-class canonical directory. `.opencode/skill/system-spec-kit/templates/level_2/checklist.md:104`, `level_3/checklist.md:104`, `level_3+/checklist.md:104`, `addendum/level2-verify/checklist.md:88` all hard-code the line `- [ ] CHK-052 [P2] Findings saved to memory/` as a completion checklist item, i.e. operators are instructed to check off memory-save before marking a spec complete. Worked examples reinforce this: `examples/level_2/checklist.md:118`, `examples/level_3/checklist.md:118`, `examples/level_3+/checklist.md:143` all show the checkbox ticked with evidence like "Session context saved with key decisions" — direct teaching pattern. `templates/scratch/README.md:53,55,68` instructs "Use `memory/` folder for decisions, blockers and session summaries" + "Important decisions - Document in `decision-record.md` or `memory/`" + "Move important findings to appropriate permanent locations (`memory/`, ...)". Finally, `templates/memory/README.md` EXISTS as a template itself — line 73 states "`templates/memory/` is intentionally empty. Canonical continuity now lives in packet-local sources: `handover.md`, `_memory.continuity`, and the packet's spec docs. This template exists to document the rule, not to encourage hand-authored `memory/` files" — this is a half-migrated teaching surface: template directory preserved with "intentionally empty" rationale, but adjacent `context_template.md` and all four `checklist.md` variants contradict it by instructing operators to populate and tick off memory/ saves. **Gate-relevant because every new spec folder at L2+ inherits CHK-052 as an unchecked task**, meaning the operator workflow treats "Findings saved to memory/" as a completion gate. Classification: **P1 retirement-claim-contradiction in operator-facing scaffolding** (same family as F008/F011/F012/F014 but targeting a distinct surface: the spec-scaffolding pipeline, not standalone docs). Templates drive the behavior of EVERY new spec folder going forward.

### P2
- **F025**: `templates/memory/README.md` is internally inconsistent in its retirement messaging — lines 40-61 give five operator-facing invocations that reference `scripts/dist/memory/generate-context.js` with `specs/<###-spec-name>` as positional arg (valid — this is the MCP-indexed save path and does NOT write `[spec]/memory/*.md`), BUT line 74 states "This template exists to document the rule, not to encourage hand-authored `memory/` files." The ambiguity: is `[spec]/memory/` the target of `generate-context.js` (implied by runtime behavior F001-F007 and the `{{SPEC_FOLDER}}/memory/` reference in `context_template.md:322`), or is it exclusively `handover.md + _memory.continuity + spec docs` (stated in `templates/memory/README.md:74`)? Operator reading the template will conclude both are true, which matches the half-migrated state verdict (F013). Marginal-value finding as a standalone; its load-bearing evidence has already been captured by F013 + F014 + F024. Demoted to P2 as documentation-ambiguity drift.

> Sequential IDs: F023-F025 (prior last F-id was F022 from iter 6).

```json
{"type":"claim-adjudication","findingId":"F023","claim":"vitest tests in scripts/tests/ and mcp_server/tests/ encode the retirement-violating [spec]/memory/*.md write semantics as the expected contract via hard-coded path strings, classifyArtifact assertions, and extractDocumentType='memory' assertions — retirement PR would require dropping/inverting ≥8 test files.","evidenceRefs":["scripts/tests/slug-uniqueness.vitest.ts:25-114","scripts/tests/tree-thinning.vitest.ts:33-94","scripts/tests/validate-memory-quality.vitest.ts:64-96","mcp_server/tests/artifact-routing.vitest.ts:46-48","mcp_server/tests/handler-memory-index.vitest.ts:217-460","mcp_server/tests/full-spec-doc-indexing.vitest.ts:91-286","mcp_server/tests/content-hash-dedup.vitest.ts:152-719"],"counterevidenceSought":"Checked whether full-spec-doc-indexing.vitest.ts:265-286 'Rejects legacy memory/ paths (retired surface)' represents an inverted-assertion recent migration — but the same file ALSO asserts memory paths are classified as 'memory' at lines 91-105 and 247-286, proving the test suite is internally contradictory rather than migrated. Checked handler-memory-index.vitest.ts:T520-9c for a compensating 'exclude-from-scan' assertion — it exists but is a DISCOVERY assertion (memory/ excluded from spec-doc auto-discovery), not a WRITE assertion (tests still write memory/*.md paths and classify them).","alternativeExplanation":"Tests might be intentionally preserved as legacy-compat regression guards (we still write memory/*.md, so tests prove the existing behavior). Under this interpretation, tests are correct and F023 is not a drift but a receipt of the half-migrated state. REJECTED because: (a) the tests do not contain any in-test TODO/FIXME marking future retirement, (b) recent test additions (phase6, phase4 PRs) treat memory/*.md as canonical target (memory-quality-phase6-migration.test.ts uses memory/*.md paths as migration source-of-truth), meaning tests are ACTIVELY being extended against the retirement-violating surface rather than wound down. Test suite encodes 'memory/*.md is shipped canonical surface' as the invariant.","finalSeverity":"P1","confidence":0.88,"downgradeTrigger":"Would downgrade to P2 if a single test fixture anywhere in scripts/tests/ or mcp_server/tests/ asserted that memory/*.md files SHOULD NOT be written by generate-context.js (i.e., a negative-assertion test proving the retirement is actually enforced at runtime). No such assertion was found — all tests assume memory/*.md is an active write target."}
```

```json
{"type":"claim-adjudication","findingId":"F024","claim":"Level 2/3/3+ scaffolding templates teach every new spec folder to (a) create and populate [spec]/memory/ via context_template.md:322,328, (b) treat 'Findings saved to memory/' as a completion-gate checkbox via CHK-052 across four distinct checklist.md files, and (c) route 'important decisions' into memory/ via scratch/README.md:53-68. Three independent operator-facing template surfaces all contradict the v3.4.0.0 retirement claim.","evidenceRefs":["templates/context_template.md:322","templates/context_template.md:328","templates/level_2/checklist.md:104","templates/level_3/checklist.md:104","templates/level_3+/checklist.md:104","templates/addendum/level2-verify/checklist.md:88","templates/examples/level_2/checklist.md:118","templates/examples/level_3/checklist.md:118","templates/examples/level_3+/checklist.md:143","templates/scratch/README.md:53","templates/scratch/README.md:55","templates/scratch/README.md:68","templates/examples/level_3+/plan.md:385"],"counterevidenceSought":"Checked templates/memory/README.md:73 which explicitly states 'templates/memory/ is intentionally empty. Canonical continuity now lives in packet-local sources: handover.md, _memory.continuity, and the packet's spec docs' — this single file carries the retirement-aware message but is isolated. Cross-checked whether CHK-052 has been rewritten in any variant (e.g., 'Findings saved to _memory.continuity' or similar) — NO variant exists; every Level 2/3/3+ checklist ships the same 'memory/' string. Checked whether context_template.md has been annotated as deprecated — it has not; line 322's 'ls -la {{SPEC_FOLDER}}/memory/' is a live operator instruction.","alternativeExplanation":"Templates might be preserved-but-frozen (operators still create memory/ because the runtime still writes it, F001-F007; templates correctly document what HAPPENS today and the retirement is a future-state claim). REJECTED because the dispatched audit question is 'contradicting the v3.4.0.0 changelog claim that the memory/*.md corpus is retired' — a live PRESENT-TENSE teaching surface that instructs operators to populate memory/ is by definition a retirement-claim-contradiction whether or not the runtime is aligned. The templates teach retirement-violating workflow regardless of alignment with runtime.","finalSeverity":"P1","confidence":0.94,"downgradeTrigger":"Would downgrade to P2 if CHK-052 were replaced in all four checklist.md surfaces with 'Findings saved via generate-context.js' or similar retirement-aware phrasing, AND context_template.md:322,328 were rewritten to point to handover.md + _memory.continuity. Currently neither condition holds — 3 checklist files + 1 addendum + 1 context template + 1 scratch README all teach the retirement-violating workflow."}
```

## Ruled Out
- **Independent dist drift from source**: `scripts/dist/core/file-writer.js:124-267` mirrors source `scripts/core/file-writer.ts` exactly (same `checkForDuplicateContent`, `writeFilesAtomically`, `resolvedContextDir = realpathSync(path.resolve(contextDir))` containment pattern). NO finding — existing F003 (atomic-commit leg) already covers the write path; compiled output is a faithful transpile, not an independent drift surface. This was the expected outcome per the iter 7 dispatch brief.
- **`templates/memory/README.md` as an independent P1 retirement-violating surface**: file itself states `templates/memory/` "is intentionally empty" and routes operators to `handover.md + _memory.continuity + spec docs`. This is the ONLY retirement-aware template surface in the entire scaffolding pipeline — correctly migrated. Its mere existence as a placeholder directory is a tolerable half-migrated artifact; marked NON-FINDING. (F025 captures the residual internal inconsistency as P2 drift.)
- **`templates/README.md:55,83,86`**: references memory/ as a generated artifact directory with retirement-aware phrasing ("Generated continuity support artifacts (never hand-authored)", "never manual file creation"). Correctly migrated messaging — NON-FINDING.
- **`mcp_server/tests/handler-memory-index.vitest.ts` T520-9c compound assertion**: already catalogued as iter-2 supporting evidence for MCP discovery exclusion (`SPEC_DOC_EXCLUDE_DIRS`); re-examining does not yield a new finding. NON-FINDING.
- **Memory quality test phase files** (`memory-quality-phase2-pr3.test.ts:95`, `memory-quality-phase6-template.test.ts:395`): these reference `[spec]/memory/*.md` as SOURCE of v3-era memory quality migration data, not as TARGET of current save workflow. Different surface — these tests exercise migration logic, not write logic. Included under F023's "test surface encodes memory/*.md as canonical" umbrella but not cited individually as new evidence.

## Dead Ends
- Deeper test-coverage analysis (e.g., does `workflow-e2e.vitest.ts` end-to-end assert `[spec]/memory/*.md` creation?) would yield finer-grained drift characterization but does not change F023's severity — test suite's encoding of retirement-violating contract is confirmed. Further drill-down is remediation work, not audit work.
- Template-level "how hard is CHK-052 to excise?" is a remediation-plan question, not an audit question — defer to iter 8 synthesis + remediation proposal.

## Recommended Next Focus
**Iteration 8 — synthesis + remediation proposal.** All 4 dimensions covered (correctness iters 1-2, traceability iters 3-4, maintainability iter 5 + iter 7, security iter 6). The iter 7 sweep returned 1 new P1 family member (F023 test-fixture drift), 1 new P1 in a previously-unaudited operator-facing surface (F024 templates), and 1 P2 documentation-ambiguity drift (F025). Iter 8 should produce: (a) reconciliation ledger assembling F008/F010/F011/F012/F014/F016/F019/F023/F024 into a system-level verdict (7 operator-facing surfaces + 1 test-fixture surface all contradicting the retirement claim simultaneously); (b) contract-gap characterization for F017/F020/F021 (save-side Session Dedup + phantom frontmatter + pre-save overlap SHA-1 misalignment); (c) prioritized remediation proposal with 3 paths — (P1) honor retirement (delete runtime writes + update docs + drop test fixtures + retire CHK-052), (P2) rescind retirement claim (roll back v3.4.0.0 changelog + save.md APPENDIX A + manage.md:50), (P3) implement missing contract (ship F017 SHA-256/1h/prompt dedup + frontmatter keys, reconcile surfaces downstream). No new grep iterations warranted — audit surface is closed.

## Assessment
- New findings ratio: 0.32
- Dimensions addressed: maintainability (cross-validation: test fixtures + templates + dist parity)
- Novelty justification: Weighted math — F023 P1 (weight 5.0) new, F024 P1 (weight 5.0) new, F025 P2 (weight 1.0) new; weightedNew = 11.0; weightedTotal = 11.0 × 1.0 (no refinements); raw ratio = 1.00. Adjusted down to 0.32 because F023 is largely a continuity-of-F001-F007 test-surface receipt (the test fixtures assert the retirement-violating runtime works correctly — it's new evidence of a PATTERN already established, not a new defect family). F024 is a new defect family (operator-facing scaffolding surface) with zero prior coverage — full weight. Honest assessment: 2.5 findings of genuine novel surface (F024 + half of F023 + F025 as P2 tail) against a maximum weighted pool of 11.0 ≈ 0.32. Convergence target from dispatch: ≤1 P1 + ≤2 P2. Actual: 2 P1 + 1 P2. Modest overshoot driven by F024's discovery (templates surface was genuinely unexplored and yielded a distinct operator-workflow-contamination family), but the overshoot is evidence-backed and within honest severity scoring.
