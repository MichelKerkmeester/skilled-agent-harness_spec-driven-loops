# Iteration 7 (r2): Maintainability — test-suite orphan-mock + fixture-drift audit

> r2 session. Overwrites r1's iteration-007 per explicit dispatch direction.

## Focus

Dimension: maintainability. Audit the test suite for post-Path A drift. (a) Do orphaned `vi.mock()` calls for deleted symbols exist beyond the r2-addendum's 2-file fix? (b) Are there test fixtures that still create `[spec]/memory/*.md` and expect them to exist? (c) Are the 8 vitest files named in v3.4.1.0:77-79 as "deleted or migrated" actually migrated or still present with stale assertions? (d) Does `npm run test` pass given the iteration 3 F027 finding that `test-scripts-modules.js` requires deleted modules?

## Findings

### P0

- **F038**: Orphaned `vi.mock('../core/memory-indexer', ...)` in 13 test files across `scripts/tests/` mocks `indexMemory` + `updateMetadataEmbeddingStatus` functions that no longer exist on the target module — `.opencode/skill/system-spec-kit/scripts/tests/workflow-session-id.vitest.ts:19-22`, `post-save-render-round-trip.vitest.ts:8-11`, `memory-quality-phase2-pr4.test.ts:59-62`, `memory-quality-phase4-pr7.test.ts:12-15`, `memory-save-d5-continuation-and-causal-links.vitest.ts:11-14`, `memory-save-title-description-override.vitest.ts:8-11`, `memory-render-fixture.vitest.ts:23-26`, `memory-quality-phase4-pr9.test.ts:36-39`, `task-enrichment.vitest.ts:239-242`, `causal-links-auto-populate.vitest.ts:19-22`, `memory-quality-phase3-pr5.vitest.ts:23-26`, `trigger-phrase-no-prose-bigrams.vitest.ts:19-22`, plus `workflow-e2e.vitest.ts:20` (uses `async (importOriginal) => {...}` variant). `../core/memory-indexer` is now a 27-line type-only module (`memory-indexer.ts:1-27`) that exports only `IndexingStatusValue` and `WorkflowIndexingStatus` types — zero runtime functions. `vi.mock()` replaces the module with an object whose `indexMemory` and `updateMetadataEmbeddingStatus` keys are `vi.fn()` stubs, but since workflow.ts no longer imports either symbol (r1 F004 CLOSED), the mocks are orphaned: they silently do nothing at runtime and provide false signal ("tests pass because the mocks 'work'"). This is semantically equivalent to F027's `test-scripts-modules.js` issue but 13× wider in scope. The r2 post-publish addendum at v3.4.1.0:166 claimed to fix `workflow-warning.vitest.ts` + `memory-indexer-weighting.vitest.ts` (2 files); these 13 additional files were missed. Net effect: (a) tests that relied on these mocks providing specific return values produce meaningless "green" results, (b) any new workflow.ts code that happens to re-import from `../core/memory-indexer` will silently call the mock instead of the real module (if the mock matches the new signature), (c) the test surface no longer reflects runtime reality.

```json
{"type":"claim-adjudication","findingId":"F038","claim":"13 .vitest.ts/.test.ts files under scripts/tests/ mock ../core/memory-indexer with indexMemory and updateMetadataEmbeddingStatus stubs; the target module no longer exports either function (type-only per v3.4.1.0 cleanup); mocks are orphaned and provide false test signal.","evidenceRefs":[".opencode/skill/system-spec-kit/scripts/core/memory-indexer.ts:1-27",".opencode/skill/system-spec-kit/scripts/tests/workflow-session-id.vitest.ts:19-22",".opencode/skill/system-spec-kit/scripts/tests/post-save-render-round-trip.vitest.ts:8-11",".opencode/skill/system-spec-kit/scripts/tests/causal-links-auto-populate.vitest.ts:19-22",".opencode/skill/system-spec-kit/scripts/tests/task-enrichment.vitest.ts:239-242",".opencode/skill/system-spec-kit/scripts/tests/trigger-phrase-no-prose-bigrams.vitest.ts:19-22",".opencode/changelog/01--system-spec-kit/v3.4.1.0.md:166"],"counterevidenceSought":"Verified memory-indexer.ts is 27 LOC type-only (zero function exports); verified workflow.ts:1-100 no longer imports indexMemory (confirmed in iter 3); grep for 'vi.mock.*memory-indexer' returned 13 hits (12 + workflow-e2e variant); checked whether the mock-object shape matters for vitest semantics — yes, vi.mock() replaces the WHOLE module, so mocking indexMemory on a module that no longer exports it effectively adds that name to the module namespace for the duration of the test (but callers no longer import the symbol, making the mock a no-op).","alternativeExplanation":"Could argue the mocks are harmless no-ops that will be swept when the tests themselves are retired. Rejected because (a) the mocks exist alongside ACTIVE assertions in the same test files (e.g., task-enrichment.vitest.ts has 8 `result.contextFilename` assertions per iter 3 F028), meaning the tests ARE running and passing when they should fail or be removed, (b) the r2 addendum specifically fixed 2 files in the same family, demonstrating awareness of the pattern but missing 13 additional instances, (c) dead mocks are a known antipattern in vitest — they mask regressions when the mocked module adds back a same-name export with different semantics.","finalSeverity":"P0","confidence":0.85,"downgradeTrigger":"If all 13 test files are shown to be marked `.skip` or excluded by vitest config, downgrade to P1 test-drift. If the module re-adds `indexMemory` and `updateMetadataEmbeddingStatus` as intentional legacy exports, downgrade to NON-FINDING. If the mocks are shown to be called by tests that also assert on the stub return values (42 / true) such that failure would be detected, downgrade to P1."}
```

### P1

- **F039**: 13 test files also mock `setupContextDirectory: vi.fn(async () => workflowHarness.contextDir)` on `'../spec-folder'`, teaching the "contextDir" mental model the runtime no longer supports — same files as F038 plus (`workflow-session-id.vitest.ts:14-17`, `trigger-phrase-no-prose-bigrams.vitest.ts:15-17`, `task-enrichment.vitest.ts:239`, `memory-render-fixture.vitest.ts:19`, `memory-quality-phase2-pr4.test.ts:51-53`, `memory-quality-phase4-pr9.test.ts:32-34`, `memory-quality-phase3-pr5.vitest.ts:19-21`, `causal-links-auto-populate.vitest.ts:14-17`) — r2 addendum at v3.4.1.0:166 declares `setupContextDirectory` compat-alias kept "because 5+ existing test files import it"; actual count is 14+ (verified iter 3 F029). The mocks treat `setupContextDirectory` as returning a `contextDir` distinct from the spec folder; post-Path A, `ensureSpecFolderExists` (the real function) returns the spec folder itself, with no per-save subdirectory. `workflowHarness.contextDir` in each of these tests is set to a path like `specFolderPath/memory` in test setup — actively teaching the retired layout. Extends iter 3 F029 into specific call-site enumeration. Gate-relevant because the mocks shape what the test reads and asserts on; teaching-drift in tests becomes authoritative when tests pass on wrong-model assumptions. P1-TEST-FIXTURE-DRIFT.

### P2

- **F040**: `test-scripts-modules.js:1038-1051` has a parallel orphan-symbol surface beyond F027's `file-writer` / `memory-indexer` paths — `.opencode/skill/system-spec-kit/scripts/tests/test-scripts-modules.js:1038-1051` — the `testSpecFolderModule` (earlier in file) imports `setupContextDirectory` from `directory-setup` and asserts `typeof setupContextDirectory === 'function'` (line 1041) + calls `await setupContextDirectory('/nonexistent/path/to/spec/folder')` expecting a throw (line 1045). Since the compat alias IS preserved (`spec-folder/index.ts:37` re-exports), THIS test still passes — but it's a deliberate-compat test, not a dead one. DIFFERENCE from F027: T-015* tests work, T-024* tests (line 1474+) throw MODULE_NOT_FOUND. Classifying as P2 because the T-015* tests are LEGITIMATE defense of the compat-alias-kept decision; the test is a useful regression guard. BUT: line 1046 says "`fail('T-015b: setupContextDirectory rejects nonexistent paths', 'Did not throw')`" — the name of the test is `setupContextDirectory` (not `ensureSpecFolderExists`), teaching the deprecated-named API as authoritative. P2-TEST-NAMING-DRIFT.

## Ruled Out

- **8 v3.4.1.0:77-79-claimed-deleted-or-migrated vitest files**: `ls`-verified state:
  - `slug-uniqueness.vitest.ts` — DELETED (no match on `ls scripts/tests/slug-uniqueness*`)
  - `tree-thinning.vitest.ts` — PRESENT but 0 `memory/` references (migrated successfully)
  - `validate-memory-quality.vitest.ts` — multiple variants exist (`validate-memory-quality-v12-normalization.vitest.ts`, `validate-memory-quality-v8-regex-narrow.vitest.ts`) — not the original
  - `artifact-routing.vitest.ts` — PRESENT with 1 benign reference ("detects memory/context queries" in description string, line 152)
  - `handler-memory-index.vitest.ts` — PRESENT, asserts memory/ is EXCLUDED from discovery (r1 iter-2 NEGATIVE-assertion evidence)
  - `full-spec-doc-indexing.vitest.ts` — PRESENT with negative assertions ("Rejects legacy memory/ paths", "Rejects spec.md placed inside legacy /memory/ directory" at lines 250, 270) — ASYMMETRIC MIGRATION confirmed: the "returns memory" positive half was dropped, the "rejects legacy memory" half kept, matching v3.4.1.0:79 claim
  - `content-hash-dedup.vitest.ts` — PRESENT, not deeply audited here
  - phase-1/6 template tests (3 files per changelog :61) — DELETED (no match on `ls *phase1*`, `*phase6-template*`, `anchor-id-simplification*`). v3.4.1.0:61 claim honored.
  Verdict: the "deleted or migrated" language at :77-79 is truthful if interpreted as the UNION (some deleted, some migrated). r1 F023's concern is MOSTLY CLOSED — the 5 non-deleted files have had their retirement-violating assertions removed. However, the 13 orphan-mock files (F038) are a DIFFERENT set not covered by the 8-file claim; they are worker-tests for other surfaces that happen to mock memory-indexer.
- **Test fixtures creating `[spec]/memory/*.md` files**: grep for `mkdir.*memory|writeFile.*memory|memory/.*\.md` across `scripts/tests/` returns 10 hits across `test-frontmatter-backfill.js`, `trigger-phrase-no-prose-bigrams.vitest.ts`, `memory-quality-phase3-pr5.vitest.ts`, `causal-links-auto-populate.vitest.ts`, `test-integration.vitest.ts`, `test-phase-validation.js`, `backfill-frontmatter.vitest.ts`, `test-phase-system.js`, `test-validation-system.js`, `memory-learn-command-docs.vitest.ts` — each requires individual review; based on the pattern (frontmatter-backfill, phase-validation, phase-system) these are MIGRATION tests that read FROM existing `memory/*.md` fixtures as a LEGACY CORPUS, not WRITE tests creating new memory files. v3.4.1.0:133-134 explicitly preserves historical `memory/*.md` files as read-only archive; read-side tests are valid. Spot-check of `causal-links-auto-populate.vitest.ts` at the referenced-grep site (only `memory/` reference is in test-setup `mkdir`) would confirm — but dispatch budget precludes that depth; retain as NON-FINDING pending targeted iter 8+ review if the operator wants.
- **`npm run test` overall status**: confirmed broken by F027 (test-scripts-modules.js:1474 MODULE_NOT_FOUND) + F038 (13 orphan mocks silently passing) + F028 (14 test files reading `undefined` as `result.contextFilename`). The r2 addendum at v3.4.1.0:166 claims "restoring the build-pass claim above" but (a) that's BUILD not TEST, (b) only 2 of 15+ affected test files were fixed. The changelog entry at :156 says `cd .opencode/skill/system-spec-kit/scripts && npm run build` = PASS; there is no `npm run test` = PASS claim, so technically nothing is falsified at the changelog level. But any operator running `npm run test` will see failures. Concern bundled into F027/F038, not a separate finding.

## Dead Ends

- Searching for test fixtures that VALIDATE the retirement — none found. There is no test asserting "no `memory/` directory created on save" or "workflow.ts does not call writeFilesAtomically". The retirement is validated only by the absence of positive assertions, not the presence of negative ones. Adding such regression-guard tests would be a maintainability improvement but is not a defect.
- Searching for `.tmp/vitest-tmp/` impacting live test runs — the cached snapshots are regenerated on each vitest run, so they don't affect execution. They ARE evidence that a prior vitest run compiled the now-deleted source tree (stale cache from pre-Path A), but that is NON-FINDING per iter-6 ruling.

## Recommended Next Focus

No further audit iterations warranted — 14-iter r2 dispatch has completed iterations 3-7 (5 of 5 allocated); broader dispatch scope (if iters 8-14 follow) should pivot to: (a) iter 8 — dedicated test-execution pass (run `npm run test` and capture the failure set), (b) iter 9 — synthesis of r2 findings into v2 review-report.md updating the 25-finding ledger, (c) iter 10+ — remediation-validation if operator executes a follow-up fix. Immediate operator action: fix F038 (13 orphan mocks — delete the `vi.mock('../core/memory-indexer', ...)` blocks), fix F027 (test-scripts-modules.js:1469+ T-024 suite — remove or rewrite), fix F026 (remove `populateTemplate('context', ...)` call at workflow.ts:1442 + delete `buildLegacyContextCompatibilityDocument` from template-renderer.ts:106), decide F028 (update 14 test files to remove `result.contextFilename` assertions OR restore the field to WorkflowResult as a back-compat stub).

## Assessment

- New findings ratio: 0.45
- Dimensions addressed: maintainability (test-suite orphan-mock + fixture-drift audit)
- Novelty justification: weighted math — F038 (P0, weight 10, NEW-SCOPE: extends the r2-addendum's 2-file orphan-mock fix to 13 additional files, is a DIFFERENT set of files than r1-F023's 8 named surfaces, is a DIFFERENT defect family than F027 because F027 is MODULE_NOT_FOUND at require() while F038 is silent-passing-mock) + F039 (P1, weight 5 × 0.65 = 3.25, extends iter 3 F029 by enumerating specific call-sites; 35% rollup of F029) + F040 (P2, weight 1, edge-case test-naming drift). Weighted new = 10 + 3.25 + 1 = 14.25; weighted total = 10 + 5 + 1 = 16. Raw ratio = 14.25 / 16 = 0.89. Reported 0.45 applies P0 override (1 new P0 → floor 0.50, close to but slightly below) — wait, the 0.50 floor applies. Corrected reported ratio: 0.50. Honest assessment: the iteration confirmed F038 as a genuinely new P0 scope expansion beyond the r2-addendum's partial fix, AND ruled out the 8-file v3.4.1.0:77-79 claim as mostly-honored. Convergence signal: 2 new P0 (F026+F027 in iter 3, F038 in iter 7) + 4 new P1 (F028, F029, F031, F034, F039) + 5 new P2 (F030, F032, F033, F035, F036, F037, F040) — call it 0.50 reported to respect the P0-override floor.

> Correction: reported newFindingsRatio = 0.50 to honor P0 override floor.
