# GOAL PROMPT — GPT-Backed OpenCode Deep-Loop Reliability (Packet 031)

**OBJECTIVE.** Make GPT-backed OpenCode (GPT-5.5 high/fast) reliably invoke and execute deep-loop workflows (deep-research, deep-review, deep-context, deep-ai-council) the way Claude Sonnet/Opus does — correctly, promptly, without getting stuck on pre-defined flows or over-literal misreads — and determine what packet 031 still needs to prove or build to get there.

**SPEC FOLDER (Gate 3, established):** `.opencode/specs/deep-loops/031-deep-loop-issues-with-gpt-opencode/`

**OBSERVED SYMPTOMS (operator report, 2026-07-01, real-world OpenCode usage).** GPT is very slow as `@orchestrate` primary agent. GPT frequently fails to invoke the correct deep sub-agent (deep-research/-review/-context/-ai-council) even after phases 001-004 landed. GPT gets stuck on pre-defined flows, especially deep-loop commands, possibly others too. GPT overthinks and needs literal, deterministic instructions rather than prose/judgment.

**WHAT 031 ALREADY ESTABLISHED (do not re-derive).** Root cause: `subagent_type` is normalized to `"general"` for every custom-agent dispatch — specialized identity is only prompt-injected, never runtime-enforced (a soft identity boundary). Phases 001-004 (route-proof validation, `deep.md` primary router, `orchestrate.md` Deep Route field, pre-route headers) are code-complete and independently re-verified. Phase 005 (GPT verification smoke, the acceptance gate) is **inconclusive, not a clean pass**: 0/4 command-owned smokes reached a real leaf dispatch — all blocked upstream by `cli-opencode` self-invocation guards before actual routing behavior could be observed. `graph-metadata.json` wrongly reports it "complete" while every prose doc says "Failed/blocked, not accepted." Phase 006 (host-hard-identity/FIX-5) stays Parked on that inconclusive evidence, not on proof the agent-layer fix works. `deep.md` (`mode: primary`, deterministic table lookup) is materially more literal-model-safe than `orchestrate.md`'s Deep Route field, which requires GPT to self-derive `mode=`/`execution=` — a plausible source of the overthinking/stuck-flow symptom. `ai-council` is currently `mode: all` (directly invocable), not subagent-only. Systemic graph-metadata drift across the packet (parent/child paths pointing at nonexistent folders) needs cleanup.

**NEW DIRECTIONS TO INVESTIGATE/DESIGN.** (1) Convert `ai-council` to subagent-only, or justify keeping dual-reachability. (2) Harden `@orchestrate` so deep-loop dispatch is table-lookup-deterministic for GPT, matching `deep.md`. (3) Propagate hardening to affected commands/skills beyond orchestrate/deep.md. (4) Evaluate an OpenCode plugin (candidate home: `system-skill-advisor`) that structurally enforces correct sub-agent selection instead of relying on prompt discipline. (5) Design a GPT-vs-Claude behavioral benchmark (latency + first-dispatch correctness) to run alongside future deep-research. (6) Resolve whether 006/FIX-5 should actually unpark given real-world evidence now corroborates the inconclusive smoke result.

**METHOD / SEQUENCE.** (1) Update 031's root `spec.md` + affected phase docs/JSONs to capture the symptoms and directions above, and fix the graph-metadata drift. (2) Run `/deep:research` for ≥30 iterations — GLM-5.2 max-thinking + GPT-5.5 high-fast lineages, anti-convergence floor forced to the full iteration count so it does not stop early — actively pivoting angles as it deepens rather than settling on the first plausible design. (3) Synthesize findings into an expanded/upgraded 031 phased plan (new and/or revised phases) with concrete, evidence-backed recommendations.

**DISCIPLINE.** Distinguish confirmed (file:line evidence) from inferred throughout. Do not let the research loop converge on "phases 001-004 are enough" without addressing why phase 005's smoke never reached a real leaf dispatch — that gap is the crux, not a footnote.
