SKILL ROUTING: User directed -> deep-research. Spec folder and detached artifact boundary are explicitly provided, so I’ll follow the named workflow and keep writes inside the lineage directory.The skill contract requires command-owned state, append-only JSONL, per-iteration markdown, and reducer-owned summary artifacts. I’m reading the loop/state references and the CLI executor contract before creating lineage-local outputs.The live command runner is not exposed as a slash-command tool here, so I’m executing the lineage state machine directly as the detached lineage worker: initialize state, write 30 sequential iteration artifacts, append JSONL evidence, then synthesize. I’ll only write under the provided lineage artifact directory.The existing packet confirms this research is a follow-up to unresolved phase 005 smoke evidence and the parked FIX-5 trigger. I’m narrowing the source read to the parent/phase specs, orchestrate/deep/ai-council agents, and deep command surfaces that control dispatch.The key contrast is now concrete: `deep.md` uses a registry table plus consistency gates, while `orchestrate.md` still asks a general primary agent to decompose and derive the route before delegation. Next I’m checking the command YAML and plugin hook surfaces to ground the proposed benchmark and enforcement options.I have enough evidence to write the lineage artifacts. The synthesis will recommend: external-shell smoke first, orchestrate-to-deep routing unification next, a prompt-transform enforcement plugin as a guardrail, and FIX-5 unpark only if external smoke/benchmark shows route-proof failures or persistent latency/stuck regressions.Writing the lineage-local state packet now: config, JSONL, reducer summaries, 30 iteration narratives, deltas, resource map, and final synthesis under `research/lineages/gpt-fast-high`.Core state files are in place. I’m adding the write-once iteration narratives and per-iteration delta records next, then the final synthesis.The full lineage packet is written. I’m running local artifact integrity checks and the packet strict validator before returning the completion sentinel.FANOUT_LINEAGE_COMPLETE:gpt-fast-high