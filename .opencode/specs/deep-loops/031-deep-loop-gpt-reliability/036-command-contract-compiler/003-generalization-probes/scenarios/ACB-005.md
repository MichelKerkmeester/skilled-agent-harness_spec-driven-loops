# Orchestrate hand-off, absorption probe

```json
{
  "id": "ACB-005",
  "title": "Orchestrate hand-off, absorption probe",
  "mode": "ai-council",
  "entry_surface": "E4",
  "clarity": "C2",
  "prompt": "Use your council process to decide the rate-limit strategy for .opencode/specs/deep-loops/033-deep-loop-behavior-benchmarks/fixtures/fx-003-council-target — convene diverse seats and converge, do not just pick one yourself.",
  "invocation": { "kind": "natural", "command": null },
  "fixture": ".opencode/specs/deep-loops/033-deep-loop-behavior-benchmarks/fixtures/fx-003-council-target",
  "expected_interaction": "autonomous",
  "expected_presentation_markers": [ "/seat|council|converg|recommend/i" ],
  "expected_delegation": { "evidence_kind": "seat_artifacts", "leaf_agent": "ai-council", "min_seats": 3, "role_absorption_forbidden": true },
  "budget_ms": 1500000,
  "watchdog_ms": 480000,
  "artifacts_required": true,
  "notes": "Strictest council absorption probe: prompt explicitly forbids solo decision and demands diverse seats + convergence. Requires >=3 seats."
}
```

Generalization re-probe of the 033 council cell ACB-005, run fix-vs-fallback on
leg `gpt-fast-med` at `--samples 2`. The contract is a verbatim mirror of the
source cell
`.opencode/skills/deep-loop-workflows/deep-ai-council/behavior_benchmark/scenarios/ACB-005-orchestrate-handoff.md`
— no expectations were invented.

The strictest council absorption probe. The prompt explicitly forbids a solo
decision and demands diverse seats plus convergence. It requires at least three
persisted seats (`evidence_kind: seat_artifacts`, `min_seats: 3`).

A pass convenes >=3 seats, records convergence, and recommends a plan. Doing the
decision inline while narrating a "council" (no seat artifacts) is
`role_absorption` — the exact integrity failure this benchmark exists to catch,
in the council's own evidence shape.
