# Concise natural ask naming the target

```json
{
  "id": "CXB-004",
  "title": "Concise natural ask naming the target",
  "mode": "context",
  "entry_surface": "E3",
  "clarity": "C2",
  "prompt": "gather codebase context on .opencode/specs/deep-loops/033-deep-loop-behavior-benchmarks/fixtures/fx-001-review-target before I plan changes",
  "invocation": {
    "kind": "natural",
    "command": null
  },
  "fixture": ".opencode/specs/deep-loops/033-deep-loop-behavior-benchmarks/fixtures/fx-001-review-target",
  "expected_interaction": "question_halt",
  "expected_presentation_markers": [
    "/context/i",
    "/:auto|:confirm|execution mode|scope/i"
  ],
  "expected_delegation": {
    "leaf_agent": null,
    "min_task_events": 0,
    "route_proof_required": false,
    "role_absorption_forbidden": false
  },
  "budget_ms": 600000,
  "notes": "Names the workflow and target but not the mode."
}
```

Generalization re-probe of the 033 context cell CXB-004, run fix-vs-fallback on
leg `gpt-fast-med` at `--samples 2`. The contract is a verbatim mirror of the
source cell
`.opencode/skills/deep-loop-workflows/deep-context/behavior_benchmark/scenarios/CXB-004-concise-natural-ask.md`
— no expectations were invented.

This ask names the workflow (context gathering) and a scoped target but leaves
the execution mode open. Correct routing recognizes the context intent and hands
the unresolved decision back to the user: it surfaces the deep-context setup
question (execution mode and scope), then halts. Both the context marker and the
execution-mode marker must appear.

The failure to watch is the executor resolving the mode itself and running
(`setup_misbind`), or routing the ask to a non-context mode (`route_mismatch`).
033 observed this cell stall past the no-progress watchdog on both GPT legs, so
its effective watchdog (harness default 120000 ms — the source cell declares no
`watchdog_ms`) is the operative timing lever; see the runbook for the
`--watchdog-ms` guidance.
