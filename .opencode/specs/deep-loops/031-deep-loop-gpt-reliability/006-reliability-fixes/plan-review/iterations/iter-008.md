</think>
</think>
</think>
</think>
</think>
</think>
</think>
</think>
</think>
</think>
</think>
## GAPS

### GAP-G8-01 — Feature-flagged rollout mechanism dropped from 034 synthesis to 035 plan
**Severity:** blocker
**Evidence:** `034/.../research/synthesis.md:51` §5.4 mandates *"root-policy and profile changes behind per-command opt-in with fallback to current injection"*; `034/.../research/findings-registry.md:52` F-044 lists *"feature-flagged rollout"* as a verification-process gap to fold into implementation; `034/.../research/iterations/iter-015.md:49` states *"No explicit rollback/compatibility plan… Add a feature-flagged rollout proposal with per-command opt-in, benchmark gates, and fallback to current injection"*; `034/.../implementation-summary.md:24` records synthesis *"hands off 5 verification-process items (…feature-flagged rollout)"* to a future implementation packet. In 035, `spec.md:60-66` scope, `002/spec.md:73-77` REQ-003 (*"wire command-scoped runs to receive it first"*), `010/spec.md:73-76` REQ-002 (*"defer … for autonomous command runs"*), and `spec.md:91-97` parent Risks table contain zero references to feature flags, per-command opt-in, or fallback paths.
**Gap:** Synthesis §5.4's per-command opt-in + fallback-to-current-injection is the safety belt for the highest-blast-radius re-wires (Gate-3 bridge, autonomous-profile wiring, root-policy dedupe). 035 implements all three as forced re-wires with no opt-out. A regression in 002 (e.g., the iter-015 reasoning-effort interaction, GAP-04 in `plan-review/gap-registry.md:12`) or in 010 (root-policy dedupe breaking the bridge, GAP-09/GAP-G8-04 below) ships immediately to every command-scoped GPT run with no kill switch. The parent Risks table names Claude-native-green + ordering + design drift and is silent on rollout.
**Amendment:** Add a new phase (e.g. `011-rollout-mechanism`) or fold into 002 REQ-0X: define (a) feature-flag key naming convention, (b) per-command opt-in manifest wired to `executionMode`, (c) fallback path = byte-identical pre-035 injection, (d) a CI verification that the fallback reproduces the 033 scorecard's Claude-native buckets exactly, (e) default state on/off and the promotion rule (e.g., three consecutive green GPT-leg runs per command before flag flips to on).

### GAP-G8-02 — 002 → 003 → 004 absorption window: receipt-mitigation not lifted out, autonomous profile lands first
**Severity:** blocker
**Evidence:** `034/.../research/synthesis.md:28` §2 designs a deliberate two-stage mitigation for the absorption class: *"**Prompt-pack absorption guard** — first-line abort ('producing findings without a dispatch receipt is role absorption') while receipts are built (S, lands in parallel with the S quick-wins, before receipts) → structural HMAC receipts (M, phase 004)"*; F-010 `findings-registry.md:18` anchors the abort line; the existing `plan-review/gap-registry.md:10` GAP-02 already records *"035 collapses to one stage — the interim guard is dropped, leaving the 3 absorption cells with zero mitigation in the window between phase 002 and phase 004."* Phase 002 `spec.md:75` REQ-003 (*"wire command-scoped runs to receive [the 8-rule profile] first"*) un-blocks the Gate-3 halt on every autonomous run. Phase 003 scope (`spec.md:63-66`) is presentation-asset-only — does NOT touch the LEAF prompt packs. Phase 004 REQ-004 (`004/spec.md:76`) bundles the abort-line as a sub-requirement of the receipts phase. After 002 lands, the three absorption cells RVB-007 / RSB-005 / RSB-007 (cells named in `004/spec.md:89`) are reachable by autonomous GPT runs with no halt gate and no abort guard for the full duration of 003.
**Gap:** The synthesis explicitly named this two-stage design to prevent an absorption-flush window; 035 collapses it. The autonomous profile lands first and clears the halt, then the next phase (003) does nothing about prompt-pack absorption, then 004 finally ships both the abort line AND the structural receipt. An executor that absorbs between 002 and 004 (i.e., the precise failure mode RVB-007/RSB-005/RSB-007 measured at med) cannot be detected because the prompt packs have not yet refused.
**Amendment:** Split F-010's first-line abort rule out of 004 and lift it into 002 REQ-005 (or add a 002a sub-phase): *"Add the prompt-pack first-line abort rule ('producing findings without a dispatch receipt is role absorption; stop and write no findings') to the two LEAF prompt packs BEFORE re-classifying any autonomous run as Gate-3-satisfied."* Add a 002 acceptance sub-test: re-run RVB-007/RSB-005/RSB-007 cells at med after the abort line lands, assert classification is `prompt_pack_aborted` (not `role_absorption`), and update 004 REQ-004 to scope only the HMAC structural mechanism (the abort line is then the durable guard, not an "interim").

### GAP-G8-03 — Classifier `satisfiedBy` / `requiresGate3Prompt` API extension has no caller-migration plan
**Severity:** major
**Evidence:** `002/spec.md:74` REQ-002: *"Extend the classifier to return satisfiedBy ('prebound_spec_folder' | 'prior_answer' | null) and requiresGate3Prompt, with the executionMode/boundSpecFolder/commandContract inputs and the 6 test cases incl. interactive-confirm-still-asks."* F-040 `findings-registry.md:48` says *"backward-compatible options"* (synthesis carries the same wording). The existing `plan-review/gap-registry.md:24-30` GAP-16..22 cover the LOGIC of the new API (undefined validator, `writeBoundary`, parent-folder pollution, `/doctor` precedence, `:confirm` vocabulary) — none cover CALLER MIGRATION. Verified callers of `classifyPrompt` (rg scope): `.opencode/skills/system-spec-kit/scripts/tests/gate-3-classifier.vitest.ts` calls `classifyPrompt('xxx').triggersGate3` at lines 143, 149, 153, 157, 163, 169, 177, 183, 188, 194, 213, 221, 227, 231, 237, 243, 247, 262, 272, 287, 294, 295, 305, 309, 319, 329, 339, 344, 350, 356 (30+ existing test cases, none pass `executionMode`/`boundSpecFolder`/`commandContract`); `.opencode/skills/system-skill-advisor/mcp_server/scripts/routing-accuracy/gate3-corpus-runner.mjs:7,29` `const result = classifyPrompt(row.prompt)` (positional only, no ctx); `.opencode/commands/speckit/assets/speckit_implement_auto.yaml:25` and `speckit_implement_confirm.yaml:25` declare `machine_contract: ".opencode/skills/system-spec-kit/shared/gate-3-classifier.ts (classifyPrompt)"`.
**Gap:** REQ-002 names the new API shape and 6 test cases but does not enumerate the existing 30+ vitest cases, the corpus-runner caller, or the YAML `machine_contract` references. Two failure modes: (1) if the signature adds REQUIRED positional args, every existing caller breaks at first compile/test run; (2) if the ctx is fully optional (synthesis's "backward-compatible options"), then `satisfiedBy:null` is the default and the autonomous-precedence rule must distinguish "ctx absent → re-halt" from "ctx present + autonomous + bound → skip" — neither asserted in REQ-002 nor in the 6-test matrix. The brief's "config/versioning: changing the classifier API (satisfiedBy) — who are the existing callers, and does the plan account for updating all of them?" maps directly here; the answer is *"the plan does not account for them."*
**Amendment:** Extend `002/spec.md:74` REQ-002 with (a) explicit signature declaration (e.g. `classifyPrompt(prompt: string, ctx?: {executionMode: 'autonomous'|'interactive'; boundSpecFolder?: BoundSpecFolder; commandContract?: CommandContract}): ClassificationResult` where `satisfiedBy`/`requiresGate3Prompt` are added fields and `triggersGate3` is retained), (b) a migration table enumerating every caller and its new call shape (vitest file 30+ lines + `gate3-corpus-runner.mjs:29` + both YAML `machine_contract` refs), (c) a "ctx-absent → `satisfiedBy:null` AND `requiresGate3Prompt:true`" test in the 6-test matrix (locking the backward-compat invariant), (d) a vitest helper that all 30+ existing cases opt into with `as PromptCtx` so the regression surface is testable per-call.

### GAP-G8-04 — 002 ↔ 010 injection-surface collision: 002's flips are non-durable against 010's last-landing dedupe + deferral
**Severity:** major
**Evidence:** `002/spec.md:75` REQ-003 (*"Add the 8-rule autonomous execution profile reference and wire command-scoped runs to receive it first"*) mutates the same per-session command-run injection path that `010/spec.md:74-75` REQ-001 (*"Dedupe root policy at injection (hash check → one canonical copy + mirror note)"*) and REQ-002 (*"defer command-irrelevant sections (spec levels, memory-save, completion mechanics, agent routing, quick reference) for autonomous command runs"*) later mutate. `010/spec.md:43` declares predecessor `009-pacing-and-resume` (not `002`). `010/spec.md:92-96` Risks row is the verbatim boilerplate (*"Re-run the baseline leg; it must stay green after every change"*). The existing `plan-review/gap-registry.md:19` GAP-09 records the structural collision but not the cell-level regression test that would catch it.
**Gap:** 002 flips RVB-008/RSB-008/ACB-004/IMB-004/IMB-005 by injecting the autonomous profile + bridge BEFORE the slimmed root policy. 010 lands last and can silently revert those flips if (a) the dedupe logic on the bridge clause mishandles the mirror note, (b) the autonomous-profile deferral lumps the bridge clause into "command-irrelevant sections" wrongly, or (c) the slimmed policy still reaches the executor in an order that re-introduces the priority inversion F-028 (`findings-registry.md:36`) names. There is no cell-level regression gate tying 010's success to the durability of 002's flips — only the boilerplate "baseline leg green" instruction.
**Amendment:** Change `010/spec.md:43` predecessor from `009-pacing-and-resume` to `002-gate3-precedence`; add `010/spec.md` REQ-003: *"Re-run 002's five Gate-3 cells (RVB-008/RSB-008/ACB-004/IMB-004/IMB-005) as a regression acceptance gate; any flip revert = 010 fail."* Add a phase-specific Risks row to 010: *"10 can silently regress 002's flips if autonomous-profile deferral or root-policy dedupe breaks the bridge clause's injection order — gate on 002's five cells."*

### GAP-G8-05 — "Baseline leg green" is an operational instruction with no CI enforcement; partially impossible-as-stated
**Severity:** major
**Evidence:** Parent `spec.md:72` REQ-003 (*"The 033 behavior benchmark is the acceptance harness; each phase re-runs its cells (gpt-fast-med + gpt-fast-high) and keeps the Claude-native baseline leg green"*) and `spec.md:93` Risks row (*"Baseline benchmark leg re-run and kept green after every phase"*). The boilerplate repeats verbatim in every child phase (001/002/003/004/005/006/010) and in their plan.md/tasks.md (e.g. `001/spec.md:96`, `001/plan.md:66`, `001/plan.md:75`, `001/tasks.md:58,74`, `003/plan.md:75`, `003/tasks.md:74`, `010/plan.md:66,75`). There is no CI step, no PR-blocking check, no automated harness action. Existing `plan-review/gap-registry.md`: GAP-05 (ACB-005 baseline contradiction: `iterations/iter-001.md:51-59`), GAP-37 (broken-runner baselines carry forward), GAP-39 (D-007 host-confound). `033/.../005-scorecard-and-integration/scorecard.md:16` records the council Claude-native baseline as *"2 pass, 2 partial/inline, 1 confirm-halt"* — a real cell that REQ-003 promises to keep green but which is currently NOT green.
**Gap:** "Baseline green" is an aspiration with no machine-checkable enforcement. Three concrete failure modes make it non-mechanizable as the plan stands: (1) ACB-005's Claude-native baseline already includes a confirm-halt (not green), so REQ-003 is impossible-as-stated for phase 005 ACB-005; (2) non-affected cells (23 cells × 3 legs ≈ 69 results) carry forward scores produced by the pre-F-014 / pre-F-025 runner that under-detected rewrite-heavy absorption and leaked path tokens — a "pass baseline" may be a previously-misclassified `role_absorption` (GAP-37); (3) the Claude baseline runs a different host binary (`claude -p` ≠ opencode), so D5 latency ratios cannot be cleanly attributed to model vs host, and phases 005 / 009's latency claims (IMB-001-high natural completion, pacing) implicitly need D5 evidence that the plan does not redefine (GAP-39).
**Amendment:** Amend parent `spec.md:72` REQ-003 to (a) concretely define *"baseline green"* as *"Claude-native leg's bucket state on the affected cells matches the pre-phase scorecard baseline, with a documented exception list for cells the scorecard already shows as non-green (e.g. ACB-005 confirm-halt dimension)"*, (b) add a CI gate script under `.opencode/specs/deep-loops/035-gpt-reliability-fixes/ci/` that runs `behavior-bench-run.cjs` against the pre-phase fixture set + Claude-native leg on every PR touching a phase's source files, (c) restate D-007 on every latency-claim phase (`005/spec.md:97-99`, `009/spec.md`), (d) execute GAP-37's full 32 × 3 cells × 3 legs re-score as a phase 001 REQ-003a sub-step (one-shot before any phase 002 flip) so the baseline isn't broken-runner.

### GAP-G8-06 — `:confirm` flow unaddressed across all 10 phases despite REQ-003's "green" promise
**Severity:** major
**Evidence:** `033` ran only `:auto`; every deep command has a `_confirm.yaml` (e.g. `speckit_implement_confirm.yaml` cited above; the same pattern holds across the deep-loop commands per the iter-015 reasoning). F-008 `findings-registry.md:16` scopes confirm's setup-render as a presentation-layer constraint (`deep_review_confirm.yaml:26-37, validates 179-186`). Synthesis §6 cell list omits any `:confirm` cell. `002/spec.md:76` REQ-004 says *"state in the presentation contracts that Tier-1 auto-setup resolution satisfies Gate 3, mirrored across all five commands"* — implies both `:auto` and `:confirm`, but no `:confirm` cell is named in `002/spec.md:87` acceptance (RVB-008/RSB-008/ACB-004/IMB-004/IMB-005 are all `:auto`). `003/spec.md:63` scope is *"The five deep command presentation assets + command docs"* but does not enumerate the five `_confirm.yaml` paths or the `render.no_suffix`/`render.confirm` blocks. `005/spec.md:74-76` REQ-002 / REQ-004 cover per-round liveness in `:auto` only. `033/.../005-scorecard-and-integration/scorecard.md:16` (council Claude-native baseline row): *"2 pass, 2 partial/inline, 1 confirm-halt"* — the confirm-halt dimension is real, measured, and unaddressed.
**Gap:** The Claude-native baseline that REQ-003 promises to keep green includes a confirm-halt dimension (ACB-005 partial/inline and 1 confirm-halt cell). 035 has no phase that owns (a) `:confirm` flow setup-rendering (no `render.confirm` block defined anywhere), (b) classifier `:confirm` vocabulary (existing `gap-registry.md:30` GAP-22 already records this — *":confirm absent from classifier; autonomous-confirm+bound untested"*), or (c) confirm-gate validator behavior. The "baseline green" claim for ACB-005 is therefore impossible-as-stated.
**Amendment:** Amend `002/spec.md:76` REQ-004 and `003/spec.md:63` scope to enumerate the five `_confirm.yaml` paths + the `render.no_suffix`/`render.confirm` blocks; either (a) add a `:confirm`-leg acceptance cell from the council row (e.g. ACB-005-confirm-leg flip to pass), or (b) explicitly carve the confirm-halt dimension out of REQ-003 with a written *"confirm-halt dimension is pre-existing out-of-scope; baseline-green applies only to non-confirm cells"* exception recorded in `spec.md:72`. Lift GAP-22's `:confirm` classifier vocabulary into 002 REQ-002 as a 7th test (autonomous-confirm+bound → defined verdict, given F-008's already-bound contract).

### GAP-G8-07 — Boilerplate Risks rows hide phase-specific regression surfaces
**Severity:** minor
**Evidence:** Every child phase Risks row is verbatim identical text: 001 `spec.md:96-98`, 002 `spec.md:97-99`, 003 `spec.md:97-99`, 004 `spec.md:97-99`, 005 `spec.md:97-99`, 006 `spec.md:97-99`, 010 `spec.md:97-99`. The three rows are: *"Regressing the Claude-native path | Re-run the baseline leg; it must stay green after every change"*; *"Phase ordering | Depends on the parent dependency order; phase 002 (Gate-3) must land before P1 phases are verified"*; *"Design drift from the 034 designs | The 034 iter-011/012/013/014 designs are the reference; verify quoted current-text before applying."* None of the phase-specific risks cataloged in `plan-review/gap-registry.md` (GAP-08/09/12/13/14/15/16/17/18/19/20/22/23/24/25/26/27/28/29/32/35/36, lines 16-46) appear in any phase's Risks row. For example, 004's Risks row does not name HMAC-key channel transit (GAP-23), wrapper-unreachable CLI branches across copilot/claude-code/opencode × review/research/context/ai-council (GAP-24), native-receipt child-identity (GAP-25), receipt-write I/O failure (GAP-26), route-field-demotion consumers in 4 YAMLs (GAP-27), key lifecycle for resumable runs (GAP-28), or receipt-log DoS surface (GAP-29). 002's Risks row does not name classifier caller migration (GAP-G8-03 above), absorb-window exposure (GAP-G8-02), agent-file F-020 interaction (GAP-12), `writeBoundary` non-enforcement (GAP-17), `prior_answer` cross-task leak (GAP-20), phase-parent pollution (GAP-21), orchestrator re-classify child path (GAP-18), or `/doctor` precedence (GAP-19).
**Gap:** The boilerplate risks row is a checklist item, not a phase-specific regression analysis. It satisfies *"we wrote a Risks row"* without identifying the actual surfaces each phase will mutate — exactly the surfaces the red-team has already enumerated in `plan-review/gap-registry.md`. The parent `spec.md:91-97` Risks table does the same at packet level. The brief's central question — *"for each P0/P1 phase, the specific way it could REGRESS Claude's correct behavior or a real interactive session — and whether the plan's risk table actually mitigates it or just names it"* — the answer is *"just names it."*
**Amendment:** Delete the boilerplate Risks rows from all 10 phase specs; replace each with the phase-specific subset of `plan-review/gap-registry.md` GAP-IDs that apply (e.g. 002 inherits GAP-G8-02, GAP-G8-03, GAP-12, 16, 17, 18, 19, 20, 21, 22; 004 inherits GAP-23, 24, 25, 26, 27, 28, 29; 010 inherits GAP-G8-04 and GAP-09; etc.). Update the parent `spec.md:91-97` Risks table to a cross-cutting rollup with explicit pointers to the per-phase rows so reviewers can trace any concrete regression surface back to its owning phase.
