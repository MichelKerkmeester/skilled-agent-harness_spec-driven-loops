# Findings Registry — 034 GPT Reliability Research

Deduped on (surface, proposal). Every entry requires file:line evidence, a tag
(simplify | optimize | re-approach), expected reliability effect, effort (S/M/L),
and the iteration(s) that produced it. Orchestrator-verified before entry.

| ID | Angle | Surface | Finding + evidence | Proposal (tag) | Expected effect | Effort | Iter |
|---|---|---|---|---|---|---|---|
| F-001 | A1 | CLAUDE.md Gate 3 | Gate 3 is worded as an unconditional priority hard stop ("Overrides Gates 1-2", "Ask first, then act", CLAUDE.md:126-138); the VIOLATION RECOVERY block (CLAUDE.md:176-181) contains the VERBATIM halt template GPT reproduced in benchmark cells ("Before I proceed, I need to ask about documentation:") | Add explicit autonomous-command precedence rule: a command contract with autonomous execution + validly bound spec_folder IS the Gate-3 answer (simplify) | Flips RVB-008, RSB-008, ACB-004, IMB-004/005 | S | 001 |
| F-002 | A1 | gate-3-classifier.ts | The machine contract ITSELF lists `/deep:review`, `/deep:research`, `/deep:ai-council`, `:auto` (with /deep:), `deep-loop`, `convergence` as Gate-3 resume triggers (ts:108-141, 197-200) — a literal executor reading the classifier concludes autonomous deep commands REQUIRE the question | Extend classifyPrompt() to return "triggered-but-satisfied" (satisfiedBy: prebound_spec_folder) when executionMode=autonomous + boundSpecFolder valid (optimize) | Same 5 cells; fixes the machine-contract root | M | 001 |
| F-003 | A1 | deep_*_auto.yaml | Autonomous YAMLs genuinely mandate writes ("Never hold findings in memory; write everything to files", deep_review_auto.yaml:16; approvals: none:6) so the write-risk a literal executor perceives is REAL, not hallucinated — the halt is a rational reading of conflicting contracts | (evidence context for F-001/F-002; no standalone proposal) | n/a | n/a | 001 |
| F-004 | A1 | deep_*_presentation.txt | The `:auto` three-tier setup binds spec_folder (Tier 1 pre-bound answers / flags / defaults) and says "do not emit the consolidated prompt" — but NEVER states that auto-setup binding satisfies Gate 3; the text tolerates Claude's reading without licensing it (presentation.txt:11-23, 27-68) | Name Gate 3 explicitly in the presentation contracts: Tier-1 resolution sets gate3_status=satisfied_by_auto_setup; Tier-2 narrows to the spec_folder field only; mirror across all 5 command surfaces (simplify) | RVB-008 + RSB-008 direct; siblings when mirrored | M | 001 |
| F-005 | A1 | CLAUDE.md Gate 3 (semantic) | The gate's intent is WRITE OWNERSHIP, but its letter mandates a QUESTION; Claude follows intent, GPT follows letter — the precedence bridge between "ownership established autonomously" and "question asked" does not exist anywhere | Reword Gate 3 from "ask the spec-folder question before writes" to "establish write ownership before writes" with enumerated valid ownership sources incl. pre-bound autonomous setup (re-approach) | All 5 cells; broadest + highest regression risk (needs classifier tests) | M | 001 |
| F-006 | A2 | presentation assets + command docs | The setup prompt reaches the executor as a REFERENCED asset it must voluntarily load and copy (review.md:15 instructs the load; presentation.txt:3 declares ownership) — no structural enforcement at halt time; GPT reconstructs from prose instead | Inline a halt-render rule in the command docs: "output the named setup block verbatim; do not summarize; stop" (simplify, S) | RVB-002/CXB-002 presentation 2/2 | S | 002 |
| F-007 | A2 | presentation assets | The consolidated prompt is a fenced HUMAN-INSTRUCTION block ("EXECUTE THIS SINGLE CONSOLIDATED PROMPT", review presentation.txt:94-99, questions 163-202) not a machine-bound render template — Claude copies by convention, GPT paraphrases/omits | Wrap blocks in explicit SETUP_PROMPT_START/END markers + "render only the marked block" (optimize, S) | Same cells; copy/fill contract replaces convention | S | 002 |
| F-008 | A2 | confirm YAML + router | deep_review_confirm.yaml only consumes already-bound setup values (confirm.yaml:26-37, validates 179-186); YAML loads AFTER presentation resolution (review.md:94-97) so no YAML-side fix can force full halt-time rendering — the fix must land in the presentation layer or command doc | (constraint finding — scopes where fixes can work) | n/a | n/a | 002 |
| F-009 | A2 | auto_mode_contract + presentation assets | Setup behavior is assembled from MULTIPLE prose sources (presentation asset + shared auto_mode_contract Tier rules 47-56/116-130 + router doc) with genuine cross-file tension the executor must reconcile (e.g. full-setup-block vs Tier-3 no-questions fail-fast); registry caveat: iter-002 mis-mapped one benchmark cell here, structural point verified | Longer-term: single machine-readable presentation source per command (questions, required_fields, render.*) generating the display docs (re-approach, L); or per-command pre-YAML setup asset (M) | Fewer cross-file contradictions; RVB-002-class + IMB-003 class | L | 002 |
| F-010 | A3 | prompt_pack templates | Dual-audience wording licenses absorption: tmpl:6 addresses "the @deep-review LEAF agent (native executor) OR a CLI executor", then tmpl:57 assigns "You are a LEAF agent" — a literal executor concludes IT is the LEAF and does the work inline | Strict boundary line in both prompt packs: "If you are reading this without a workflow-supplied dispatch receipt, stop and write no findings" + first-line abort rule naming absorption (optimize, S) | RVB-007/RSB-005/RSB-007 at med | S | 003 |
| F-011 | A3 | deep_*_auto.yaml dispatch step | Dispatch proof is post-hoc, not a pre-work receipt: step_dispatch_iteration has a native dispatch branch (yaml:765-816) but post_dispatch_validate runs only after artifacts exist (928-940); the CLI branch runs opencode directly with the LEAF prompt (904-914) — nothing distinguishes "dispatched then worked" from "worked inline" | Hard pre_dispatch_receipt step: receipt produced by the dispatch MECHANISM (native Task event id, or audited child-process receipt), required by the validator before state-log append (re-approach, M) | Same 3 cells; the structural fix | M | 003 |
| F-012 | A3 | route-proof schema + validator | Route proof is SELF-ATTESTED: the model-authored JSONL schema includes target_agent/agent_definition_loaded/resolved_route (tmpl:79-83) and post-dispatch-validate.ts only checks presence/equality (619-665) — exactly the forgery RVB/RSB-007-med produced | Remove route-proof authorship from LEAF prompts; fields become workflow-owned, injected from the dispatch receipt; validator compares receipt-derived values (simplify, M) | Kills the fabrication path outright | M | 003 |
| F-013 | A3 | executor-audit + CLI branch | runAuditedExecutorCommand exists (executor-audit.ts:475-614) but the auto-YAML CLI branches bypass it — no dispatch_started/completed events bind the run to a child process | Route all CLI executor launches through the audited wrapper; validator requires the events for non-native executors (re-approach, M) | Same 3 cells via unforgeable CLI receipts | M | 003 |
| F-014 | A3 | behavior-benchmark runner | Harness gap found by GPT: fixtureGained counts only NEW files (runner:754-756) — a run that only REWRITES existing fixture artifacts under-detects as absorption evidence | Compare before/after content hashes, not just new-file presence (optimize, S) — harness improvement for the 033 suite | Classification consistency on rewrite-heavy cells | S | 003 |
| F-015 | A4 | council loop protocol | The protocol LICENSES the dark window: "Dispatch or simulate distinct seats within one CLI round" then critique then persist (loop_protocol:22-27, 57-65, 98-120) — per-seat writes only at final persistence, so minutes of silent internal simulation are contract-compliant; the watchdog then kills compliant behavior | Stepwise contract: produce AND persist Seat A, then B, then C, then critique, then referee (simplify, S) | ACB-004/ACB-005 liveness | S | 004 |
| F-016 | A4 | council auto YAML | Orchestration validates only AFTER round_completed (yaml:119-131, assert_jsonl_record_type: round_completed) — no round_started/seat_started/seat_completed liveness records required mid-round | Require per-round liveness JSONL records (round_started, seat_started/completed, critique_started, adjudication_started) in post_dispatch_validate (optimize, M) | Same cells; observable progress resets watchdogs | M | 004 |
| F-017 | A4 | deep_context_auto.yaml | step_parallel_sweep is an explicit BARRIER-JOIN ("host waits for ALL seats before merging", yaml:408-416) with findings returned as JSON and written only post-sweep (455-470) — one stalled seat starves the whole run of observable progress until watchdog kill | Settle-as-you-go: write each seat's JSON as it returns, merge survivors after timeout; degraded completion beats silent death (re-approach, M) | CXB-004 completes with partial agreement | M | 004 |
| F-018 | A4 | council convergence rules | Auto mode layers adjudicator-verdict stability across rounds on top of the reference's lightweight convergence (auto yaml:10-11, 102-103; convergence_signals:54-58) — invites an internal evaluator loop before any observable write, exactly on the absorption-probe cells | Single bounded referee pass per persisted round (two-of-three + max-round escape); adjudicator-stability math behind an optional flag (simplify, S) | ACB-005 hidden-loop avoidance | S | 004 |
| F-019 | A5 | agent .md files (all 3 sampled) | Hard contracts are POSITION-buried: orchestrate.md is 817 lines/74 headings with the Gate-3 HARD BLOCK at line 387 (after routing/decomposition/examples); deep-research.md's dispatcher-facing output contract starts at 462/584; ai-council.md splits output/persistence across 5 non-adjacent sections (75-81, 385-421, 424-496, 500-568, 624-729) | Top-of-file `## EXECUTOR CONTRACT` block: 7-10 numbered hard rules (authority, first action, forbidden, required output, verification, write boundary, stop condition) immediately after frontmatter in all agent files (simplify, M) | Gate-3 inversion + partial presentation + absorption classes | M | 005 |
| F-020 | A5 | orchestrate.md | Gate 3 is named abstractly early (line 51) but the operative hard block sits at 387-404 — layout matches the measured priority-inversion behavior (executor acts on what it read first) | Hoist Gate 3 to "Step 0: file-modification gate" within the first 60 lines; line 51 points at it (optimize, S) | Gate-3 cells on E4/orchestrate-routed paths | S | 005 |
| F-021 | A5 | deep-research.md | The "return this exact summary" completion-report template lives at 462-495 with verification checklist at 499-532, in the final quarter — the shape most vulnerable to GPT dropping the output contract after executing the workflow | Move the completion-report template into the early contract block; later section becomes a pointer (optimize, S) | RSB presentation dims | S | 005 |
| F-022 | A5 | agent rule sections | ALWAYS/NEVER/ESCALATE rules mix with narrative prose late in each file — salience competition between style guidance and hard blockers; GPT overweights early/enumerated material | Machine-readable rules header ({id, phase, must, fail_status}) with prose as explanation below (simplify→re-approach, L) | Literal-adherence class across all modes | L | 005 |
