Researching the three specified contract files and packet 033 benchmark evidence read-only, then I’ll synthesize the GPT-safe profile with line-cited findings.
The archived `033-ux-deep-analysis` packet surfaced by memory is not the benchmark packet described in the brief, so I’m widening the read-only search to benchmark artifacts and model-outcome logs.
Found the active GPT-reliability packet and the A10 brief. I’m reading its synthesis, registry, and related iteration outputs to ground the measured-outcome claims instead of relying only on the prompt.
The 033 scorecards confirm the class split: GPT handles many step-list runs after startup but repeatedly fails at Gate-3 prose precedence and structured-mode dark windows. I’m reading the specific contract line ranges that explain those differences.
### FINDINGS

1. Step-list YAML is the safest pattern for GPT once execution starts. `deep_review_auto.yaml` gives concrete state paths (`.opencode/commands/deep/assets/deep_review_auto.yaml:95-110`), an iteration loop (`.opencode/commands/deep/assets/deep_review_auto.yaml:478-493`), a dispatch step (`.opencode/commands/deep/assets/deep_review_auto.yaml:765-816`), and post-dispatch artifact assertions (`.opencode/commands/deep/assets/deep_review_auto.yaml:928-940`). Measured outcome matches: GPT-high reached 5/8 deep-review passes while GPT-med reached 1/8, with high eliminating silent stalls in that dispatch-mode slice (`.opencode/specs/deep-loops/033-deep-loop-behavior-benchmarks/002-pilot-deep-review/scorecard.md:32-41`).

2. GPT fails when prose hard-blocks claim priority over the local command contract. `CLAUDE.md` says Gate 3 “Overrides Gates 1-2” and “Ask first, then act” (`CLAUDE.md:126-138`), then gives the exact violation-recovery halt template (`CLAUDE.md:178-180`). Packet 033 records GPT halting on that spec-folder question in every mode and both reasoning efforts (`.opencode/specs/deep-loops/033-deep-loop-behavior-benchmarks/005-scorecard-and-integration/scorecard.md:23-25`).

3. GPT stalls on multi-step prose protocols that allow long dark windows. The council protocol says to “Dispatch or simulate distinct seats within one CLI round,” then critique, then persist artifacts only in phase 4 (`.opencode/skills/deep-loop-workflows/deep-ai-council/references/integration/loop_protocol.md:22-27`, `.opencode/skills/deep-loop-workflows/deep-ai-council/references/integration/loop_protocol.md:98-120`). Packet 033 shows GPT-high still silently stalled on ACB-004 and ACB-005, while Claude passed ACB-004 (`.opencode/specs/deep-loops/033-deep-loop-behavior-benchmarks/004-rollout-council-improvement/scorecard.md:18-19`, `.opencode/specs/deep-loops/033-deep-loop-behavior-benchmarks/004-rollout-council-improvement/scorecard.md:42-50`).

4. “Should produce” is weaker than “write X to Y.” Council seats “should produce” recommendations, trade-offs, risks, evidence, and conditions, but no per-seat file target is attached (`.opencode/skills/deep-loop-workflows/deep-ai-council/references/integration/loop_protocol.md:57-65`). The A4 registry finding ties that deferred-persistence shape directly to ACB-004/ACB-005 liveness failures (`.opencode/specs/deep-loops/034-gpt-reliability-research/research/findings-registry.md:23-26`).

5. Cross-file, untyped indirection is a GPT-fidelity loss multiplier. The A9 registry traced 14 instruction-bearing files before first review dispatch, 21 external YAML refs, and three setup authorities; it recommends a compiled execution contract because each hop invites gate inversion, partial rendering, or absorption (`.opencode/specs/deep-loops/034-gpt-reliability-research/research/findings-registry.md:43-46`). This is the structural opposite of a compact, local step list.

### PROPOSALS

1. **Authority-First**. **Change:** Codify in a new `gpt-safe-contract-authoring.md` reference and retrofit `CLAUDE.md` Gate 3 first: local autonomous command contracts with a bound `spec_folder` satisfy write-ownership and outrank generic documentation questions. **Expected effect:** Flips RVB-008, RSB-008, ACB-004, IMB-004/005 Gate-3 halts (`scorecard.md:23`, `CLAUDE.md:126-138`). **Effort:** M. **Risk:** Medium; must not weaken real interactive write-scope safety.

2. **Artifact-Bound Imperatives**. **Change:** Require every executor-facing imperative to name an artifact path and validator, following `deep_review_auto.yaml:928-940`; retrofit `loop_protocol.md` seats from “should produce” to `write seat-{N}.md/json to ai-council/round-{N}/`. **Expected effect:** Converts ACB-004/005 from dark-window stalls into visible progress. **Effort:** S-M. **Risk:** Low; adds artifact noise if overused.

3. **Stepwise Liveness**. **Change:** Add heartbeat/progress records after each seat, critique, and referee step; retrofit council protocol and auto YAML before changing budgets. **Expected effect:** Addresses measured structured-mode stalls where high effort did not help (`004-rollout-council-improvement/scorecard.md:42-50`). **Effort:** M. **Risk:** Medium; partial states need cleanup semantics.

4. **Compiled Contract**. **Change:** Codify a build-time “one hydrated execution packet” rule for GPT executors: command setup, presentation text, YAML excerpt, bound defaults, and typed references in one file; retrofit `/deep:review` first. **Expected effect:** Reduces gate inversion, presentation gaps, and absorption caused by 14-file/21-reference chains (`findings-registry.md:43-46`). **Effort:** L. **Risk:** Medium-high; drift guard required.

5. **Low Conditional Depth**. **Change:** Replace prose `unless/except/when` ladders with decision tables or machine outputs; retrofit `CLAUDE.md` Gate 3’s write/read-only/continuity branches (`CLAUDE.md:129-138`). **Expected effect:** Makes GPT choose the intended branch instead of the highest-salience hard block. **Effort:** M. **Risk:** Medium; edge cases must be covered by classifier tests.

6. **Top-Loaded Executor Contract**. **Change:** Require first-screen contract blocks: authority, first action, forbidden actions, required artifacts, validation, stop condition. Retrofit root gates and deep-loop agent docs before late prose rules. **Expected effect:** Addresses position-burial and root-policy salience found in A7/A5 (`findings-registry.md:27-38`). **Effort:** M. **Risk:** Low-medium; duplicated summaries can drift.

7. **Mode-Scoped GPT Guidance**. **Change:** In deep-loop command docs and `cli-opencode` GPT profiles, state: high effort is required for dispatch modes, but high does not fix Gate-3 precedence or structured-mode stalls. **Expected effect:** Prevents GPT-med absorption while directing structured-mode fixes toward artifacts/liveness, matching 033’s mode-specific findings (`005-scorecard-and-integration/scorecard.md:25-31`). **Effort:** S. **Risk:** Low; higher GPT cost in dispatch modes.
