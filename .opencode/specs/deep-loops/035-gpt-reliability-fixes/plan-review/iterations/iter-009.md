</think>
</think>
## GAPS

**G1. Phase 002 "M" is actually L** — Effort mis-estimate, `035-gpt-reliability-fixes/spec.md:117`, `../002-gate3-precedence/spec.md:43-77`, `../034.../findings-registry.md:8-13`.
   - **Gap:** 002 closes **7** findings (F-001/002/004/005/028/030/040), not the 3-4 typical of an M. REQ-001 mutates the root gate; REQ-002 adds a new classifier API (`satisfiedBy` + `requiresGate3Prompt`), 6 new tests, and 3 new inputs; REQ-003 wires the 8-rule prelude into "command-scoped runs" (i.e. every deep-loop workflow + per-command prompt-pack hook); REQ-004 mirrors the Gate-3 satisfaction statement across **all five** presentation contracts. The synthesis already flagged this as "broadest + highest regression risk (needs classifier tests)" (`findings-registry.md:13`). No per-call-site migration plan (the 34 vitest positions + corpus-runner + 2 YAML `machine_contract` refs already enumerated in `gap-registry.md:48`).
   - **Severity:** major.
   - **Amendment:** Re-rate 002 as **L**. Either split it into 002a (root policy + bridge + 5-presentation mirror) and 002b (classifier API + tests + caller-migration), or add a `002-callsites` sub-phase that owns the migration table before REQ-003 wires anything command-scoped.

**G2. Phase 010 "S-M" is actually M (probably M-L)** — Effort mis-estimate, `../035.../spec.md:125`, `../034.../findings-registry.md:35-38`.
   - **Gap:** 010 closes F-027 (engine-level double-injection dedupe: hash check, canonical copy, mirror note — engine-level, not content) and F-029 (deferral of *six identified sections* of the root policy for autonomous command runs). The blast radius is **every session, every executor, every command**: an injection-pipeline regression silently degrades the four prior phases' cells. The synthesis explicitly marks F-027 as doubling the root-policy load (~16-20K tokens × 2 → halved) and F-029 as moving a complex slice of root-policy content behind a per-run-mode flag — that is engine work, not content editing. S-M is sized for a two-line doc tweak; 010 mutates the injector.
   - **Severity:** major.
   - **Amendment:** Re-rate 010 as **M**. Add a "blast-radius" line to REQ-001/002 mandating that every accepted fix must round-trip through the post-002 six GPT cells (RVB-008/RSB-008/ACB-004/IMB-004/IMB-005, repeated) so a regressed injector fails acceptance rather than silently degrading.

**G3. Phase 008 is research-sized work mis-labeled as an implementation phase** — Shippability, `../035.../spec.md:123`, `../034.../synthesis.md:34-35`, `../034.../findings-registry.md:43-46`.
   - **Gap:** 008 (L) collapses F-009 + F-035-F-038 into a build-time compiled-contract + deterministic setup loader. The 034 findings-registry confirms: **no design iteration exists** for any of these (the ready-to-apply designs are iter-011→002, iter-012→004, iter-013→003, iter-014→005 — there is **no iter-N for 008**). The synthesis is candid: F-009 is "longer-term: single machine-readable presentation source per command (L)"; F-038 is the "longest L, integration risk." Building a `build/compile` step, typed 21+ external refs, checksum drift guards, **and** a deterministic setup loader that emits a hydrated packet before model execution — in one phase — is a research campaign, not a packet-implementation phase.
   - **Severity:** blocker (as-phased).
   - **Amendment:** Carve 008 out of 035 into a dedicated `036-compiled-contract` packet whose first deliverable IS the design (build contract, ref-type taxonomy, drift-guard CI, loader spec). The 035 phase 008 should become a single REQ-001 stub ("adopt 036's compiled contract once shipped") that can be deferred or dropped without blocking 009.

**G4. Phase 009 is research-sized work mis-labeled, with one acceptance cell doing all the verification lifting** — Shippability, `../035.../spec.md:124`, `../034.../synthesis.md:36,39`, `../034.../findings-registry.md:40-42`.
   - **Gap:** 009 closes F-032 (split the heaviest command into 3 sub-invocations — explicit "large workflow/API change"), F-033 (cross-iteration cache system), F-034 (conditional budget policy). **No design iteration exists** for F-032/033/034 (registry:40-42), unlike F-040/041/042/043 which had iter-011/012/013/014 ready-to-apply designs. The synthesis itself hedges: "Best answer to budget-edge deaths … large workflow/API change." IMB-001-high is the only acceptance cell; everything else (resumability across sub-invocations, per-mode cache behavior, conditional budget dispatch) has no measurable cell. The "L" effort band is the only one allocating the design+build budget — but a phase whose three REQs are each unbounded R&D with one terminal cell cannot be shipped the same way a 003 (S, real design in hand) is shipped.
   - **Severity:** blocker (as-phased).
   - **Amendment:** Same pattern as G3: split 009 into a 036b/036c-style design packet (or rename 036 design + 009 implementation). Phases 009 must contain: (a) a defined resume contract (shared ledger schema, parent-sessionId propagation, see also `gap-registry.md:13,28` about HMAC key lifecycle), (b) per-sub-invocation acceptance cells (init → work → synthesize each gating a 034 cell or sub-cell), (c) a measurable cache-hit-rate metric. Today the single cell hides unbounded work.

**G5. Feature-flag / fallback / opt-in mechanism — synthesis-mandated, no phase creates it** — Hidden prerequisite, `../034.../synthesis.md:51`, `../035.../spec.md:91-97`, every child `Risks` row.
   - **Gap:** Synthesis §5 item 4 mandates "Rollout safety: root-policy and profile changes behind per-command opt-in with fallback to current injection; Claude-native legs must stay green at every step (the receipt design explicitly preserves the native path)." F-044 also lists this as a required handoff item. The 035 parent **Risks table has zero feature-flag/opt-in/fallback entries** (`spec.md:91-97`), every child `Risks` row is verbatim-identical boilerplate (002/004/006/009/010 — three lines, no opt-in, no fallback, no kill switch). The three highest-blast-radius rewrites (002 root-gate bridge, 003 autonomous-profile wiring, 010 root-policy dedupe) therefore ship as forced re-wires. A regression in 002 hits every command-scoped GPT run on the next invocation; there is no fallback to the pre-035 injection. This is the **hidden prerequisite** that gates safe rollout of phases 002/003/004/006/007/010 — it must exist before 002 lands, not after 010.
   - **Severity:** blocker (prerequisite for phases 002/003/004/007/010 to land safely).
   - **Amendment:** Insert a new **phase 011 — rollout mechanism** sequenced BEFORE 002 (or fold the mechanism into 001 as a sub-REQ): define a per-command opt-in key convention, a fallback hash that reproduces the pre-035 injection byte-identically, and a CI check asserting the fallback reproduces the 033 Claude-native bucket. Without this, "baseline leg green" after each phase is unfalsifiable and a regressed 002/010 is un-rollable-back.

**G6. Quick-win ordering is broken: 006 is serialized behind two M phases and the first-line-abort quick-win was dropped** — Quick-win mismatch, `../034.../synthesis.md:24-27`, `../035.../spec.md:121`, `../035.../006-routing-offer/spec.md:43`.
   - **Gap:** Synthesis §5.1 places routing offer **in the FIRST parallel S-wave with render markers (003), before receipts (004)**. Phase 006 in 035 is serialized behind **005** (predecessor), which itself follows 004 — three upstream phases before a different surface (Gate 2 + `skill_advisor.py`, sharing zero files with 004/005) ships. 006's three acceptance cells (ACB-003/IMB-003/RSB-004) never hit Gate-3 (they're inline vague-ask answers), so the 002-masking rule barely constrains them; the delay is pure sequencing convenience. Separately, synthesis §2 explicitly designs a TWO-stage absorption mitigation: an S interim first-line-abort prompt-pack guard ("producing findings without a dispatch receipt is role absorption") **while** the M receipts ship; 035 collapses this into phase 004 alone, leaving RVB-007/RSB-005/RSB-007 with **no mitigation between phases 002 and 004** — two M-phase windows where the 3 absorption cells are unmasked. The 034 quick-win set = "Gate-3 package + render markers + routing offer + council stepwise persistence + prompt-pack abort line"; 035 implements 4 of those 5.
   - **Severity:** major (impacts both effort-to-impact and gap coverage).
   - **Amendment:** (a) Set 006's predecessor to **002** (or 001 at most) and mark `003 ∥ 006` parallel per synthesis §5.1. (b) Add the first-line-abort prompt-pack rule as an explicit REQ in phase 003 (or as a sub-REQ inside 004), scoped interim until receipts ship, with a release-on-004-success toggle. Both are cheap, both restore the synthesis quick-win ordering.

**G7. Phase 008 has no acceptance cells of its own — false independence** — Independence/false-independence, `../035.../spec.md:62,123`, `../035.../008-compiled-contract/spec.md:81-87`.
   - **Gap:** The parent spec claims every phase "ships and verifies independently" (`spec.md:62`) and requires acceptance cells "moved to the expected verdict." Phase 008's success row says "Gate-inversion + render + absorbed-step classes together" — those are **002/003/004 cells** (RVB-008/RSB-008/ACB-004/IMB-004/IMB-005 + RVB-002/CXB-002/IMB-003 D2 + RVB-007/RSB-005/RSB-007). 008 doesn't own any cell that it didn't inherit; it can only be accepted as a non-regression after the upstream phases move them. Same shape as `gap-registry.md:10` for 007 and 010, but more acute here because 008's whole purpose is to **fix a recurrence class**, not move cells. If 002/003/004 ship and cells flip, 008 is non-regression; if 002/003/004 ship and cells don't flip, 008 cannot rescue them. The compounding with G3 (no design) makes 008 the highest-risk, lowest-testable phase in the packet.
   - **Severity:** major.
   - **Amendment:** Restate 008's success criterion as: (a) drift-guard CI green with a typed-ref compile passing (engine-level), (b) loader unit-test coverage asserting unresolved placeholders fail before model execution (engine-level), (c) a **dedicated** "compiled-contract-present" assertion on at least one 033 cell that 003/004 moves (proving the compiled artifact was read, not the layered sources). Independently of the cells 002/003/004 own. Same amendment template applies to 007 (per-agent contract-presence assertion) and 010 (single-injection + token-budget assertion) per `gap-registry.md:10`.
