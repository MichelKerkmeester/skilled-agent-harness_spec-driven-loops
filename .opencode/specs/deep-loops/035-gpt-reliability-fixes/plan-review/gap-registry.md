# Gap Registry — 035 Plan Red-Team (GLM-5.2-max)

Deduped on (target, gap). Each entry: what's missing/wrong/risky in the 035 plan,
evidence (which phase/finding/file), severity (blocker | major | minor), and the
recommended plan amendment. Orchestrator-verified before entry.

| ID | Angle | Target | Gap (what we missed) | Severity | Recommended amendment | Iter |
|---|---|---|---|---|---|---|
| GAP-01 | coverage | RSB-001 (fixture strict-validation nondeterminism) | Scorecard §4 ranks "[P2] Fixture strict-validation nondeterminism" (fail-close vs dispatch on full-run cells); no 035 phase names RSB-001 as an acceptance cell, and F-014/F-025 (phase 001) are different confounds (content-hash, path-token). A measured, ranked item nobody owns — inflates the "all cells covered" completeness claim. | major | Add a phase-001 requirement to make the toy research fixture strict-valid or classify the fail-close as an intended probe; name RSB-001 as a phase-001 acceptance cell. | 001 |
| GAP-02 | coverage | F-010 absorption class (RVB-007, RSB-005, RSB-007) | Synthesis §2 designed a deliberate TWO-stage mitigation: interim first-line-abort prompt-pack guard (S, lands in parallel with the S quick-wins, before receipts) → structural HMAC receipts (M, phase 004). 035 collapses to one stage — the interim guard is dropped, leaving the 3 absorption cells with zero mitigation in the window between phase 002 and phase 004. | major | Add the first-line-abort rule as a fast sub-requirement in phase 003 or 004 (same prompt-pack templates), scoped interim until receipts ship; or insert a lightweight phase between 002 and 004. | 001 |
| GAP-03 | coverage | ACB-004 (claimed by BOTH phase 002 and phase 005) | ACB-004 fails via Gate-3 halt at med AND stall at high; parent map lists it as an acceptance cell in both phase 002 (Gate-3) and phase 005 (progress records). REQ-003 says "re-run at both efforts" but gives no primary/secondary cause attribution — synthesis §5.2 flagged this exact double-claim risk; 035 implements no mitigation. Both phases could claim the full flip, or neither prove its share. | major | Effort-scope the acceptance: phase 002 owns ACB-004-med (halt→execute), phase 005 owns ACB-004-high (stall→liveness). Add the primary/secondary-cause attribution field to REQ-003's rerun protocol per synthesis §5.2. | 001 |
| GAP-04 | coverage | High-reasoning-effort mandate (dispatch modes) | Scorecard §4 item 2 "[P1] Mandate high reasoning effort … scoped" is the cheapest measured remediation. 035 folds it into F-039's 7-rule authoring profile, which phase 007 codifies "as a reference" — documentation only. No phase adds an enforceable config point (prompt-pack `reasoning_effort`, YAML effort binding, CLI-dispatch default). Verified: the only `reasoning_effort` string anywhere in 035 is inside this critique — no phase enforces it. | major | Add a requirement to phase 004 (audited CLI wrapper) or 007 to set high as the dispatch-mode default in the prompt-pack templates and/or wrapper, with a documented fallback where high is unavailable. | 001 |
| GAP-05 | coverage | ACB-005 confirm-halt dimension + REQ-003 baseline frame | Scorecard:16 shows the council Claude-native baseline is "2 pass, 2 partial/inline, 1 confirm-halt" — NOT green, contradicting REQ-003's "keep the Claude-native baseline leg green." Phase 005 treats ACB-005 as a pure stall (progress records fix dark windows, not confirm-gate halts); the confirm-gate dimension (F-008 territory) is unaddressed. [Caveat: that ACB-005 specifically is THE confirm-halt cell is GLM's inference from the mode-level row; the contradiction holds at council-mode level regardless.] | major | Either (a) scope phase-005 ACB-005 acceptance to the GPT stall leg only and document the baseline confirm-halt as pre-existing/out-of-scope in success criteria, or (b) add a confirm-flow presentation fix (the F-008 layer) to phase 003/005. | 001 |
| GAP-06 | coverage | Contested stall cells (CXB-004, ACB-004-high, ACB-005) — sampling | Scorecard §4 item 3 "[P1] … 3-sample the contested stalls … CXB-004 owes a 3-sample pass before quoting a stall rate." Phase 001's stated job is "make the acceptance harness trustworthy," but it adds no multi-sample/stall-rate methodology. Phase 005's claim that CXB-004 flips stall→complete is unfalsifiable against a baseline the scorecard itself flags as under-sampled (single-run cells). | major | Add an N≥3 requirement for contested stall cells to phase 001's scope, or a methodology gate in REQ-003 requiring ≥3 stable samples before any stall-cell flip can be claimed. | 001 |
| GAP-07 | coverage | F-003 classification (autonomous YAML write-mandate) | F-003 evidence is real (`deep_review_auto.yaml:23`: "Never hold findings in memory; write everything to files"). 035 REQ-004 files it non-actionable alongside F-008/F-044. But unlike F-008 (a genuine constraint), the YAML wording is independently clarifiable defense-in-depth: a literal executor reading the YAML still sees an unconditional write mandate conflicting with Gate 3, regardless of whether the root-policy bridge lands. | minor | Reclassify F-003 as actionable (S) and assign to phase 002 or phase 010 as a YAML-mandate wording clarification ("after Gate 3 is satisfied" / "writes go to the pre-bound spec_folder"). | 001 |
| GAP-08 | dependencies | Phase 001 harness scope (missing instrumentation) | Synthesis §5 item 3 requires three harness additions — presentation snapshot assertions, budget-edge integration checks, advisor vague-ask telemetry — that later phases' acceptance depends on (003 render assertions, 005/009 budget/pacing checks, 006 routing telemetry). 001 scope is only `behavior-bench-run.cjs` + the vague-ask contracts (F-014/F-025); none of the three §5 instrumentation pieces is assigned to any phase. Distinct from GAP-06 (that is sampling COUNT; this is missing measurement CAPABILITY). | major | Expand 001 to add the three §5 instrumentation pieces (or per-phase harness-extension sub-reqs for 003/005/006/009), and restate 001's purpose as trustworthy for ALL later cell-flips, not just the two 034 confounds. | 002 |
| GAP-09 | dependencies | 002 ↔ 010 injection-surface collision | 002 REQ-003 gives the 8-rule autonomous profile first position in the command-scoped injection; 010 REQ-002 defers/slims that same payload and REQ-001 dedupes the (002-edited) root policy. Both mutate the same per-session command-run injection, but 010's predecessor is 009 (not 002) and its risks row never names the 002 interaction. 010 lands last and can silently regress the exact cells 002 flipped (RVB-008/RSB-008/ACB-004/IMB-004/IMB-005). | major | Set 010's predecessor to 002; add a 010 requirement to PRESERVE the 002 precedence bridge + 8-rule-profile-first position, with a regression re-run of 002's five cells as a 010 acceptance gate. | 002 |
| GAP-10 | dependencies | 007/008/010 independence claim (no unique cells) | Parent spec:62 claims "each phase ships and verifies independently"; success #1 requires "acceptance cells moved." But child acceptance rows: 007 "measured indirectly," 008 "classes together," 010 "indirect (supports Gate-3 + presentation cells)" — none owns a cell. So they can only be accepted as non-regression AFTER 002/003/004 move those cells → not independent. (Thematic sibling of GAP-03 attribution problem.) | major | Restate independence as "001–006 verify independently; 007/008/010 verify as non-regression gated on the owning P0/P1 phase," OR give each a dedicated structural acceptance cell (contract-presence assertion for 007, drift-guard CI + loader unit test for 008, single-injection + token-budget assertion for 010). | 002 |
| GAP-11 | dependencies | 006 mis-serialized behind 004/005 | 006's predecessor is 005, but 006's scope (Gate 2 + `skill_advisor.py`) shares no file with 004/005, and its cells (ACB-003/IMB-003/RSB-004) are inline vague-ask answers that never hit Gate-3, so the 002-masking rule barely constrains them. Synthesis §5.1 puts routing offer in the FIRST parallel S-wave with render markers (003), before receipts. Serializing 006 behind two M phases invents a dependency and delays a top impact/effort win. | major | Set 006's predecessor to 001 (or 002 at most); mark 003 ∥ 006 parallel per §5.1; change the parent §8 header to separate TRUE hard deps (001; 002→P1 verification) from sequencing convenience. | 002 |
| GAP-12 | dependencies | 002 Gate-3 cells co-owned by 007 (P2 gates P0) | F-020 (closed by 007) locates the operative Gate-3 hard block in `orchestrate.md:387-404`; 002's five acceptance cells route through the orchestrator, but 002 scope touches only root policy + classifier, NOT orchestrate.md. If any cell weights the agent-file Gate-3 position over the root bridge (F-020's priority-inversion), 002 alone can't flip it — 007 co-owns. [GLM self-caveat: 002's profile-first MAY overcome the positioning; verify at execution.] | major | Add the F-020 interaction to 002's risks + an acceptance caveat: a cell that won't flip after 002 on an orchestrate-routed path is jointly owned by 002+007; 007 must not re-claim it as a clean independent flip. | 002 |
| GAP-13 | dependencies | 009 undocumented dependency on 004 receipts | 009 REQ-001 splits the heaviest command into resumable sub-invocations (setup / work --one-iteration / synthesize) sharing one session id + ledger = N dispatches. 004 REQ-001/002 emit a PER-DISPATCH HMAC receipt and gate every state-log append on it. Whether per-dispatch HMAC composes across a shared-ledger multi-dispatch run is unexamined; 009 names only 005 as a cross-phase dep. Order is safe (004<009) but the coupling is invisible, so 009's ledger protocol could be designed to fail 004's validator. | major | Add 004 as a documented dependency of 009; require each resumable sub-invocation to emit+consume a 004-style receipt over the shared ledger, with a resume-mid-run acceptance test asserting all sub-invocations validate. | 002 |
| GAP-14 | dependencies | 001 path-free rewrite re-scores 006's baseline | 001 REQ-002 rewrites the vague-ask prompts (ACB-003/IMB-003/RSB-004) path-free and REQ-003 re-scores them — but those ARE 006's acceptance cells, and F-026 says path tokens can route AWAY from the intended skill, so removing them can move the routing verdict before 006's scorer change lands. 006 would then measure against a 001-moved baseline with no attribution. Unflagged in both children. (Input-change confound; distinct from GAP-08's capability gap.) | major | Require 001 to record the post-rewrite re-score for the three cells as the NEW baseline handed to 006; restate 006 acceptance as "route/offer vs the post-001 baseline," and add the coupling to 006's risks. | 002 |
| GAP-15 | dependencies | 004 ↔ 005 co-edit shared surfaces | 004 (review prompt pack, `post-dispatch-validate.ts`, deep_*_auto.yaml dispatch steps) and 005 (review prompt pack, per-round liveness in the same validator, REQ-004 review/improvement progress) mutate the same prompt pack, validator, and auto-YAML region with no cross-reference. Order (004<005) is correct so not a blocker, but 005's plan must rebase onto 004's validator + prompt-pack edits or the receipt-require and liveness-require collide in one function. | minor | Add a coordination note to 005's risks naming the three shared surfaces; gate 005's plan authoring on rebasing against 004's landed `post-dispatch-validate.ts` + review prompt-pack changes. | 002 |
| GAP-16 | gate3-safety | **Undefined validator** — `validated` is a caller-supplied boolean | The P0 satisfaction rule (iter-011.md:97-107) grants the Gate-3 skip on `options.boundSpecFolder?.validated` with NO call to any validator; iter-011.md:64-68 defines `BoundSpecFolder{path,source,validated}` but nothing produces `validated:true`, and 002 REQ-002 specifies the API shape + inputs + 6 tests but no validation contract. `allowedSpecFolderSources` includes `'flags'`, so a literal executor passing `--spec-folder ../` (source `flags`, `validated:true`) skips the gate → any write. The whole package's safety rests on an undefined boolean; also swallows stale/deprecated/metadata-missing folders. | **blocker** | Ship `validateSpecFolderBinding()` with a testable predicate the rule CALLS (not trusts): path under `.opencode/specs/` or `specs/`; exists; has mandatory metadata files; Status ≠ Deprecated/Superseded; is a leaf (GAP-21). Add tests: out-of-tree `flags` path → not satisfied; ambiguous target-path (>1 candidate) → not satisfied. | 003 |
| GAP-17 | gate3-safety | `writeBoundary` declared but never enforced | iter-011.md:74 declares `writeBoundary?: string` on `CommandContract` and the prose (iter-011.md:36) makes "remains inside that command's declared write boundary" a satisfaction CONDITION — but the satisfaction rule (97-107) never references `writeBoundary`, and no post-gate enforcement is specified. A command binds `specs/123-x`, the gate is skipped at classify time, and the executor writes `src/parser.ts` unimpeded. | major | Require a non-empty `writeBoundary` in the rule, RETURN it on the result, and add a runtime invariant: any write whose resolved path is outside `writeBoundary` re-opens Gate 3 (violation-recovery). Test: bound folder + out-of-boundary write → `requiresGate3Prompt` flips back true. | 003 |
| GAP-18 | gate3-safety | Orchestrate/E4 child-agent re-classify re-halts (sharpens GAP-12) | 002 closes F-001/002/004/005/028/030/040 but NOT F-019/F-020 (007 owns them); the operative Gate-3 block for E4 lives in `orchestrate.md:387` (F-020), which 002 doesn't touch. A dispatched child agent re-classifies the same prompt: if it gets `boundSpecFolder` via the Task prompt but NOT the `commandContract` object, `classifyPrompt` returns `requiresGate3Prompt:true` and re-halts — regression on the very F-020 path the package claims to clear. The 6-test matrix never exercises the parent→child options hand-off. | major | Add a 7th test: child re-classify with `boundSpecFolder` present, `commandContract:null` → assert the intended verdict (propagate the contract, or treat bound-folder+autonomous-parent as sufficient). Add a §6 note: 002's bridge is unsafe on E4 until 007 propagates `boundSpecFolder`+`commandContract` to children. | 003 |
| GAP-19 | gate3-safety | `/doctor` router clause vs autonomous bridge — no precedence | The After block (iter-011.md:37-40) preserves the Router-commands clause verbatim ("mutates routes require the same spec-folder discipline… before asking or acting") alongside the bridge's "Do NOT emit the generic A-E prompt." For an autonomous `/doctor:<mutates>` with a bound folder, a literal executor reads two applicable clauses with opposite imperatives and no precedence sentence — precisely the literal-executor failure mode this packet targets. No `/doctor`/router test exists. | major | Add a precedence line ("Router-commands clause governs per-route mutation class; the autonomous bridge applies only to routes the manifest marks autonomous-and-bound") + a test: autonomous `/doctor` on a `mutates` route, bound vs unbound → distinct verdicts. | 003 |
| GAP-20 | gate3-safety | `prior_answer` branch has no mode/task-boundary gate → binding leaks (also Claude regression) | The `prior_answer` satisfaction branch (iter-011.md:97-99) fires on `triggersGate3 && boundSpecFolder.validated && source==='prior_answer'` with NO executionMode gate — test 5 confirms it satisfies an INTERACTIVE `save context`. Combined with the unchanged "answer applies for the ENTIRE session — re-ask ONLY when user says 'new task'" rule, an autonomous binding of `specs/100-foo` in run 1 can suppress Gate-3 for an unrelated interactive write in run 2, landing writes in the wrong folder. Defeats the "command-scoped" promise AND regresses Claude's correct re-ask-on-new-task behavior. | major | Constrain `prior_answer` satisfaction to an INTERACTIVE Gate-3 answer (or explicit carry-over), never an autonomous `prebound_spec_folder` re-tagged `prior_answer`; test cross-task reuse (autonomous binding → new interactive write) → `requiresGate3Prompt:true`. | 003 |
| GAP-21 | gate3-safety | Phase-parent binding pollutes lean-trio | `BoundSpecFolder` has no `isLeaf`/`resolvedChildId`; the rule has no parent check. Binding a phase parent (e.g. `specs/035-gpt-reliability-fixes`) is path-shaped and passes a naïve validator, but writing `plan.md`/`tasks.md` there violates AGENTS.md §3's lean-trio policy (heavy docs live in the child, resolved via `last_active_child_id`). An autonomous write command can pollute a phase parent. (This is why GAP-16's validator must read graph-metadata, not just `exists()`.) | major | Add `isLeaf`/`resolvedChildId` to `BoundSpecFolder`; the validator must, for a phase parent, resolve `last_active_child_id` (or fail) and bind the child. Test: bind a phase-parent path → auto-resolves to active child or NOT satisfied. | 003 |
| GAP-22 | gate3-safety | `:confirm` absent from classifier; autonomous-confirm+bound untested | `gate-3-classifier.ts:119/198` handle `:auto` but there is NO `:confirm` entry, so `/deep:review:confirm` matches only via the `/deep:review` substring — the classifier never sees confirm's "already-bound, no re-render" semantics (F-008). The 6-test matrix covers interactive-confirm-still-asks but not autonomous-confirm+bound, so that verdict is emergent, not asserted. | minor | Add `:confirm` to the vocabulary with documented semantics + a test for autonomous-confirm+bound → defined verdict (satisfied, given F-008's already-bound contract). | 003 |
| GAP-23 | receipts-threat | **HMAC key transits a command-string channel** — forgeability collapses | iter-012.md:61 claims the key is "never passed through `buildExecutorDispatchEnv`" (env-protected), but line 191 interpolates `receiptKey: '{dispatch_receipt.key}'` INSIDE the `if_cli_opencode` `command: |` heredoc (node script body → `EOF`), so the engine materializes the key into the executed command string — the exact ancestry/cmdline surface `executor-audit.ts:408/495 detectFromAncestry(ancestryCmdlines)` already reads. No crypto/hmac exists in executor-audit.ts (design, not built). A compromised executor reading `ps`/`/proc` (or the temp script file) recovers the key and self-issues valid receipts, defeating F-010/011/012. | **blocker** | Keep the key in the engine process: compute the HMAC in the YAML-engine step from pre-dispatch facts; have the wrapper RETURN post-dispatch facts (child pid/exit/sessionId) for the engine to countersign — never pass `receiptKey` into any `command:` template. Add a threat-model section asserting the key's only residence is engine heap. | 004 |
| GAP-24 | receipts-threat | `runAuditedExecutorCommand` unreachable from any CLI branch — REQ-003 is a 12-command migration | F-013's "wrapper bypassed" is real: `if_cli_copilot` (deep_review_auto.yaml:863) calls `spawnSync('copilot', built.argv)` directly, `if_cli_claude_code` and `if_cli_opencode` shell directly; no branch calls the wrapper (it exists only in the iter-012 design). REQ-003/iter-012 redesign only the `cli_opencode` branch — copilot (`@PROMPT_PATH` rewrite) and claude-code (`CLAUDE_CONFIG_DIR`) are undesigned, across review/research/context/ai-council = ~9-12 branch rewrites. 004 is "M" with no per-branch tasking and names none of this in risks. | major | Scope REQ-003 per-branch × per-YAML ({copilot, claude-code, opencode} × {review, research, context, ai-council}); spec the wrapper input contract for all three arg styles (positional / `@path` / `-p`); add a regression cell asserting identical dispatch behavior post-wrap per branch. | 004 |
| GAP-25 | receipts-threat | Native Task receipt can't bind child identity — proves intent, not work | The receipt is written pre-dispatch (iter-012.md:100-114); the native branch `if_native: dispatch: agent: deep-review` (deep_review_auto.yaml:812) gets its child sessionId/pid from the runtime only after the Task executes — unknowable at receipt-write time — yet the schema requires `child.sessionId` + `sessionIdSource`. So a native receipt attests "engine intended dispatch X," strictly weaker than the "unforgeable + child-bound" framing (spec 004 §2, F-041); success #2 ("native dispatch pass") defines no test binding the receipt to the actual subagent session. | major | Split into a pre-dispatch INTENT record (engine-signed, no child id) + a post-dispatch COMPLETION countersign the engine writes after the Task returns the real sessionId/pid; validator binds the two. Test: native dispatch whose post-completion child id differs → rejected. Redefine/remove `sessionIdSource:'native_dispatch'` (unachievable pre-dispatch). | 004 |
| GAP-26 | receipts-threat | No failure mode for receipt-write I/O failure — transient error → rejection | REQ-001 specifies no atomic-write/lock/retry/idempotency for `writeDispatchReceipt` appending to `dispatch-receipts.jsonl`; success #1 ("inline work without a receipt is rejected") gives no carve-out for a dispatch that occurred but whose receipt write failed (disk full, EACCES, concurrent-append interleave from parallel seats per F-016/F-017). iter-012 makes a missing receipt a hard `dispatch_receipt_missing`. Both outcomes are wrong: hard-fail aborts legitimate completed work, soft-continue rejects it — no recovery path. | major | REQ atomic write (single `O_APPEND` of a pre-serialized line, or temp+rename under per-dispatch lock) with idempotent retry + a distinct `dispatch_receipt_write_failed` class ≠ `dispatch_receipt_missing`. Define concurrent-seat behavior: per-dispatch receipt files keyed by dispatchId, or an append lock. | 004 |
| GAP-27 | receipts-threat | Demoting route fields breaks 4 YAMLs + validator (undocumented consumers) | LIVE-VERIFIED: `deep_review_auto.yaml:932 assert_jsonl_fields` REQUIRES `target_agent, agent_definition_loaded, resolved_route`; `post-dispatch-validate.ts:619 validateRouteProofRecord` enforces presence+equality (`route_proof_missing`/`route_proof_mismatch`); all 4 auto-YAMLs (review/research/context/ai-council) carry the block. REQ-002 "demote to advisory" + iter-012 only patch the validator's check + one excerpt — leaving the fields required in 4 YAMLs turns every post-004 dispatch into a jsonl/route-proof failure. | major | Migration REQ enumerating every `assert_jsonl_fields` + every reader of the three route fields (grep-verified), removing them from required lists in all 4 YAMLs, and dropping/repointing `validateRouteProofRecord` to receipt-derived values. Acceptance: a dispatch omitting the model-written fields passes when a valid receipt exists. | 004 |
| GAP-28 | receipts-threat | No key lifecycle for resumable/multi-dispatch runs (sharpens GAP-13 on the key axis) | `post_dispatch_validate` needs `receipt_key` to verify (iter-012.md:216); the key is "workflow-private … never written to state" = in-memory for one process. Phase 009 (F-032) splits the heaviest command into N resumable sub-invocations across process boundaries; a resumed run must re-read the key to validate earlier receipts. In-memory-only → resumed run sees all prior receipts as invalid; persisted-to-disk → the child (r/w on artifact_dir) reads and forges it. iter-012 assumes single-process lifetime; spec 004 names neither storage nor derivation. | major | Derive the key as HMAC(dispatchId, run-master-key held by engine) so receipts are re-verifiable on resume without persisting per-dispatch keys; OR a sealed parent-owned key store (0600) the child can't read + a resume re-open protocol. Add 004 as an explicit dependency of 009 with a resume-mid-run acceptance test. | 004 |
| GAP-29 | receipts-threat | Receipt log shares child's r/w artifact_dir — delete/truncate DoS + non-atomic append | `receiptLogPath: "{artifact_dir}/dispatch-receipts.jsonl"` (iter-012.md:58) sits in the dir the child writes iteration files to. The child can't forge a MAC but CAN `unlink`/truncate the file → `dispatch_receipt_missing` (silent DoS masquerading as absorption). Parallel seats (F-016 council, F-017 context sweep) appending to one JSONL risk non-atomic line interleave that breaks the validator's line parse; the replay test's `previousStateLogSize` binding is child-influenceable. | major | Write receipts to a parent-owned, child-unwritable path (`{artifact_dir}/.receipts/` mode 0700) or one file per dispatchId; validator tolerates a missing file only when a sibling intent+completion pair exists. Acceptance: a child that deletes its receipt is classified `dispatch_receipt_destroyed` (failure), not silently absorbed. | 004 |
| GAP-30 | progress-rootcause | Reducer safety allowlist not lifted from design into an enforceable REQ | iter-014.md:92 DELIVERABLE 3 mandates "completion reducers must use an explicit allowlist for completion-bearing records and ignore `type:"progress"`/`event:"progress_record"` for completion math." 005 REQ-001 defines the schema + step-transition semantics but never requires the allowlist guard, and success:84 states only the negative invariant ("stalled cells still classified as stalls") with no mechanism. Review's reducer already does this (only `type==='iteration'` counts), but a new council/context reducer keying on type-prefix would misclassify heartbeat records → false completions. | major *(MiniMax: blocker; design already specifies the rule + review implements it, so gap is enforcement coverage for new reducers, not a broken design)* | Add a REQ mandating the allowlist/schema-discriminator check in every reducer/validator deriving completion from `*-state.jsonl`; add a checklist row feeding a reducer only `type:'progress'` lines and asserting null for completion math. | 005 |
| GAP-31 | progress-rootcause | IMB-001-high / improvement unreachable from 005's declared scope | 005 scope (spec:64) is "council + context auto YAML + loop protocol; the review prompt pack" and explicitly out-of-scopes budget/pacing to phase 009 — but REQ-004 (:76) says "Review/improvement emit progress" and success (:87) claims "IMB-001-high partial credit." F-031 (registry:39) is anchored only at the REVIEW prompt pack; iter-014 shows no improvement-prompt-pack insertion point; the improvement budget-edge death is the F-032/033/034 cluster owned by 009. So the IMB-001 cell 005 claims is produced by nothing 005 touches. | major | Remove "IMB-001-high partial credit" from 005 success and move it to 009, OR expand 005 scope to the improvement prompt pack AND produce the missing iter-014 insertion point (a real design gap to author or escalate). | 005 |
| GAP-32 | progress-rootcause | "Council persists each seat stepwise" has no writer path vs persist-artifacts.cjs | 005 REQ-002 requires council to persist each seat stepwise, but `persist-artifacts.cjs:255-268 parseCouncilReport` requires ALL of Composition/Per-seat/Recommended Plan/Plan Confidence in one input and `writeArtifacts` emits the seat set as one transaction — a single-seat stepwise call fails validation (exit 1). iter-014's after-block shows stepwise records but points to `state_paths.seat_dir/{label}.json`, a different file than the markdown artifact persist-artifacts.cjs produces; 005 is silent on which path is the stepwise target. | major | REQ-002 must name the writer (a new `append-seat.cjs`, an `--incremental` flag that re-validates per seat, or a distinct `seat_dir/*.json` contract) + a contract test asserting the harness consumes the chosen path as a per-seat settle event. | 005 |
| GAP-33 | progress-rootcause | F-018 (convergence rule) is scope-creep inside a liveness-records phase | 005 REQ-002 bundles F-015/F-016/F-018, but F-018 (registry:26) is "single bounded referee pass per round (two-of-three + max-round escape); adjudicator-stability math behind a flag" — a convergence-rule change to `deep_ai-council_auto.yaml:10-11,102-103` + `convergence_signals.md:54-58`, NOT a liveness record. 005's success is written entirely in liveness terms, so F-018's acceptance cell (ACB-005 hidden-loop) is unspecified. | major | Split F-018 out of 005 (to a new phase or to 007, the natural home for council-mode rule changes), OR add a sub-success-criterion naming the convergence artifact and asserting the ACB-005 hidden-loop detector flips. | 005 |
| GAP-34 | progress-rootcause | The 60s threshold is asserted, not derived | 005 REQ-001's ">60s" trigger has no rationale: the benchmark watchdog is 120s no-progress (033 001-framework spec:73); observed stalls are council 12-14min, context 4-5min (scorecards) — none references 60s as a natural unit, and iter-014:24 gives no ratio derivation. 30s or 90s would yield the same cell-level behavior; the value is free-floating. | minor | Derive the threshold from the watchdog window (e.g. threshold = (watchdog − safety_margin)/2 = 60s at watchdog=120s) OR, if it's a usability floor, cite a measured LLM sub-step latency distribution. | 005 |
| GAP-35 | progress-rootcause | "Step transitions only, no timers" is unenforceable at schema level — no-op heartbeats mask stalls | The iter-014:5-20 schema is a free-form `type:"progress"` record with `status:started\|completed`; nothing distinguishes transition/timer/no-op. 005 REQ-001 asserts "step-transition-only semantics" but the validator has no field to check it. A literal executor can wrap a 1-second no-op in a started/completed pair, satisfying the 60s rule and resetting the watchdog (mtime check, iter-014:96) on an otherwise-hung run — GAP-30's allowlist protects completion math but not the watchdog. (Partly brief-seeded, developed with a schema-enforceability mechanism.) | major | Add a schema field anchoring records to real work — `unit_id` + a `progress_delta` (seat/evidence count, bytes written) the validator cross-checks against the produced artifact; reject started/completed pairs whose delta is zero or whose `artifact_path` is absent. Make it a REQ, not a design note. | 005 |
| GAP-36 | progress-rootcause | In-CLI council seat simulation yields no per-seat dispatch boundary for records | `loop_protocol.md:25` licenses "Dispatch or simulate distinct seats within one CLI round." When seats are simulated in-CLI, the dispatch is a single internal model action producing all seats' markdown in one write — no per-seat host event to bracket a started/completed pair. `deep_ai-council_auto.yaml:765-816` has a single dispatch step, not per-seat sub-steps, so a literal executor emits one seat_started/seat_completed for all of STEP 2, losing the per-seat liveness granularity 005 REQ-002 intends. Synthesis §5 explicitly flags in-CLI council as a distinct surface from dispatch modes. | major | REQ that in-CLI council MUST break STEP 2 into per-seat sub-steps with explicit started/completed records (or an internal iterator emitting the JSONL between simulated seats) + a contract test asserting record count == seats_per_round; if unguaranteeable for in-CLI, restrict stepwise-liveness to CLI-dispatched mode and accept watchdog-only bounding for in-CLI. | 005 |
| GAP-37 | verification-methodology | Non-affected cells carry forward BROKEN-runner baselines | F-014 (registry:22, discovered iter-003) and F-025 (registry:33, discovered iter-006) were found MID-program, after phases 002-004 were already scored. The 035 plan re-runs only "affected cells" per phase, so the other ~23 cells × 3 legs (~69 results) keep scores produced by the pre-fix runner that under-detected rewrite-heavy absorption (F-014) and leaked path tokens (F-025). A cell the plan trusts as a "pass baseline" may be a previously-misclassified absorption. | major | Amend phase 001 REQ-003: one-shot re-run of ALL 32 cells × 3 legs (~96 runs) with the corrected runner BEFORE any phase-002+ flip re-run; publish a delta-classification report (`previously-pass-now-X`); cells whose bucket changes invalidate the "baseline green" claim for that cell. | 006 |
| GAP-38 | verification-methodology | No per-mode stall-RATE comparison — watchdog can't tell "halt fixed" from "stall induced" | The 120s no-progress watchdog + 15/25min budgets (033 framework) fire on both Gate-3 halts (~1s `phase0_block`) and stalls, but the plan requires no pre/post per-mode stall-rate histogram. Scorecard §1: council gpt-fast-high 2/5 stalls, context 1/6 — the same bucket phase 005 claims to fix. A single cell-flip can't show whether structured-mode stalling actually decreased or just moved. | major | Amend phase 001 REQ-003 to record a pre/post per-mode `stuck_no_progress` rate per leg; phase 005 success must read "structured-mode stall rate per leg decreased by ≥Y," not a single cell-flip. | 006 |
| GAP-39 | verification-methodology | Claude baseline host confound (D-007) carries forward — latency acceptance can't attribute model vs host | `decision-record D-007`: the Claude baseline runs a DIFFERENT host binary (`claude -p`), so host overhead (session bootstrap, tool wiring) is folded into every D5 latency ratio — "to be cited on every latency ratio this leg anchors." Yet every 035 phase uses "baseline leg green," and phases 005/009 make latency claims (IMB-001-high natural completion, pacing) that implicitly need D5 evidence that can't be cleanly attributed to model vs host. | major | Add a "D5 baseline-confound restatement" line to the Risks table of any phase naming a latency-sensitive cell (005, 009, 010); define a latency acceptance rule comparing the GPT leg's `tTerminal` against ITSELF pre/post (paired), never against the Claude host baseline. | 006 |
| GAP-40 | verification-methodology | Success framed as "GPT reliability fixed" but coverage is bounded to 32 authored scenarios | 033 spec's 32 scenarios are author-chosen, weighted to vague/concise on bare/vague entry surfaces; F-024 notes the trigger inventory lacks natural phrasings ("best way to handle," "make the agent better," "research how"). The 035 parent success §5:1 claims reliability improvement writ large, but any prompt outside the 32 has no acceptance evidence — by design, but unflagged. | minor | Append "(scoped to the 32-scenario behavior-benchmark suite; no evidence for prompts outside that set)" to parent success §5:1; add a `coverage_outside_32: unmeasured` row to the Risks table. | 006 |
| GAP-41 | unaddressed-surfaces | 3 sibling improvement lanes (model-benchmark, skill-benchmark, ai-system-improvement) unbenchmarked + unphased | 033's 5×3 grid measured only IMB (agent-improvement); mode-registry enumerates 4 improvement lanes, each with its own `deep_{model,skill,ai-system}-*_{auto,confirm}.yaml` + presentation (verified: `deep_model-benchmark_auto.yaml` etc. exist). Phases 003/004/005/007/009 all say "review/improvement" but never enumerate the 3 sisters, which share the same Gate-3 halt, prompt-pack absorption, and budget-edge surfaces as review. | major *(MiniMax: blocker)* | Scope-amend 003/004/005/007/009 (or add a phase) to list the 3 lanes and either re-run the fix packages against them or record the deferral with a target date in spec §3. | 007 |
| GAP-42 | unaddressed-surfaces | `:confirm` path uniformly out of scope across all 10 phases | 033 ran only `:auto`; every deep command has a `_confirm.yaml` (verified) and F-008 scoped confirm's setup-render as a presentation-layer constraint. Phase 002's bridge leaves `:confirm` still asking Gate 3, but no phase names what the consolidated setup prompt must RENDER on `:confirm`. (Broader than GAP-22's classifier-vocabulary point — this is the presentation/setup-render axis.) | major *(MiniMax: blocker)* | Amend phase 003 scope to name the five `_confirm.yaml` paths and the `render.no_suffix`/`render.confirm` blocks in each presentation asset, not just the bare-auto halt. | 007 |
| GAP-43 | unaddressed-surfaces | 11 of 14 agent files unaudited by phase 007 | F-019 explicitly says "(all 3 sampled)" — only `orchestrate.md`, `deep-research.md`, `ai-council.md` were examined for the position-buried Gate-3/output-contract defect. `.opencode/agents/` has 14 files (verified); phase 007 REQ-001 says "in each agent file" without enumeration, leaving 11 files (code, context, debug, deep-context, deep-improvement, deep-review, deep, design, markdown, prompt-improver, review) unscanned for the same defect class. | major *(MiniMax: blocker)* | Amend phase 007 REQ-001 to enumerate all 14 files + add a sub-finding scan of the 11 unsampled ones for Gate-3 hard-block position, completion-template position, dual-audience wording; insert the retrofits found. | 007 |
| GAP-44 | unaddressed-surfaces | ~35 non-deep-loop commands share the auto/confirm/Gate machinery but get no acceptance evidence | `.opencode/commands/` has ~35 commands (doctor/, speckit/, memory/, create/, design/, root routers) — many with the same `:auto`/`:confirm` + presentation shape (e.g. `create_*_auto.yaml`). Phase 002 (Gate-3), 003 (render), 006 (routing), 010 (injection) apply GLOBALLY via AGENTS.md but never name these surfaces, so the fixes land in code paths the 033 cells never exercise. | major *(MiniMax: blocker)* | Add a phase (or scope-amend 002/003/006/010) that names the non-deep-loop surfaces and either extends the fix packages to them or records a written exclusion boundary in spec §3. | 007 |
| GAP-45 | unaddressed-surfaces | "GPT-safe" validated only on gpt-5.5-fast — 5 other opencode executors unmeasured | 033 legs are all `gpt-5.5-fast` variants; the "Claude-shaped" thesis + every fix is validated against one non-Claude model. AGENTS.md §8 (`sk-prompt-models`) covers DeepSeek-v4-pro, Kimi-k2.7-code, MiniMax-M3, MiMo-V2.5-Pro, GLM-5.2 — none in the synthesis verification map. Phase 010's "no single guaranteed flip" salience claim is model-generality-unverified. (This campaign's own iters 001-004=GLM, 005+=MiniMax show models diverge.) | major | Add one non-GPT executor leg (cheapest: DeepSeek or Kimi) to one phase's acceptance harness — phase 010 already disclaims a guaranteed flip — to check "executor-safe" == "GPT-safe." | 007 |
| GAP-46 | unaddressed-surfaces | MCP/hook injection paths beyond root policy not slimmed by phase 010 | F-029 names session-prime (2000t), continuity brief (2400ch), advisor brief (2048) as bounded, with root policy dominant. Phase 010 scopes root-policy dedupe + session-prime/plugins deferral, leaving `session_bootstrap`/`session_resume` payloads, `code_graph_status` context blocks, `advisor_recommend` brief, `memory_match_triggers` tier injection, and code-mode/MCP transport briefs unaddressed — all compete for salience under autonomous command runs. | minor | Amend phase 010 scope to enumerate and either slim or defer each of these injection paths for autonomous command runs. | 007 |
| GAP-47 | regression-rollback | Feature-flagged rollout / opt-in / fallback safety-belt dropped from synthesis to plan | Synthesis §5 item 4 mandates "Rollout safety: root-policy and profile changes behind per-command opt-in with fallback to current injection"; F-044 + iter-015:49 + impl-summary open_questions all list feature-flagged rollout as a required handoff item. 035 has ZERO feature-flag/opt-in/fallback references across all 10 phases (verified). All three highest-blast-radius rewrites (Gate-3 bridge, autonomous-profile wiring, root-policy dedupe) ship as forced re-wires with no kill switch — a regression in 002 or 010 hits every command-scoped GPT run immediately. | major *(MiniMax: blocker)* | Add a `011-rollout-mechanism` phase (or fold into 002): feature-flag key convention, per-command opt-in manifest wired to executionMode, fallback = byte-identical pre-035 injection, a CI check that the fallback reproduces the 033 Claude-native buckets, and a promotion rule (N consecutive green GPT-leg runs before flag flips on). | 008 |
| GAP-48 | regression-rollback | Classifier `satisfiedBy` API extension has no caller-migration plan | 002 REQ-002 adds `satisfiedBy`/`requiresGate3Prompt` + new inputs but enumerates no existing callers. Verified callers: 34 `classifyPrompt` calls in `gate-3-classifier.vitest.ts` (all positional, `.triggersGate3` only), `gate3-corpus-runner.mjs` (positional, no ctx), and `speckit_implement_{auto,confirm}.yaml` declare it as a `machine_contract`. If new args are required → every caller breaks; if ctx is optional → `satisfiedBy:null` default means the rule must distinguish "ctx absent → re-halt" from "ctx present+autonomous+bound → skip", asserted nowhere in the 6-test matrix. | major | Extend REQ-002 with an explicit signature (ctx optional, `triggersGate3` retained), a migration table enumerating every caller (vitest 34 + corpus-runner + 2 YAML machine_contract refs), and a "ctx-absent → satisfiedBy:null AND requiresGate3Prompt:true" backward-compat test. | 008 |
| GAP-49 | regression-rollback | "Baseline green" has no CI enforcement and is impossible-as-stated for some cells | REQ-003's "keep the Claude-native baseline leg green" is only ever an operational instruction (the verbatim boilerplate Risks row) — no CI step, no PR-blocking check, no automated harness action. And it's impossible-as-stated in three ways already in the registry: ACB-005's Claude baseline is a confirm-halt not green (GAP-05), ~69 non-affected cells carry broken-runner baselines (GAP-37), and the Claude leg is a different host binary so latency can't attribute model-vs-host (GAP-39). Consolidates those under one enforcement gap. | major | Define "baseline green" concretely (bucket-state match on affected cells + a documented non-green exception list e.g. ACB-005); add a CI gate script running `behavior-bench-run.cjs` on the pre-phase fixtures + Claude leg on every PR touching a phase's source; restate D-007 on latency phases; run GAP-37's full 32×3 re-score as a phase-001 sub-step. | 008 |
| GAP-50 | regression-rollback | Boilerplate Risks rows hide phase-specific regression surfaces | Every child phase's Risks row is verbatim-identical (3 rows: Claude-native-green, phase-ordering, design-drift) — verified across 001/002/004/010. None names the concrete regression surface each phase mutates: 004's row omits HMAC-key transit (GAP-23), wrapper-unreachable branches (GAP-24), route-field-demotion consumers (GAP-27), etc.; 002's omits caller migration (GAP-48), the absorb window (GAP-02), writeBoundary (GAP-17), prior_answer leak (GAP-20). The risk analysis is a checklist item, not a phase-specific analysis. | minor | Replace each phase's boilerplate Risks rows with the phase-specific subset of registry GAP-IDs that apply (002 ← GAP-02/12/16-22/48; 004 ← GAP-23-29; 010 ← GAP-09/47); make the parent Risks table a cross-cutting rollup pointing to the per-phase rows. | 008 |
| GAP-51 | effort-realism | Phase 002 effort "M" is under-rated (actually L) | 002 closes 7 findings (F-001/002/004/005/028/030/040) — not the 3-4 typical of M. It mutates the root gate (REQ-001), adds a new classifier API + 6 tests + 3 inputs (REQ-002), wires the 8-rule prelude into every command-scoped run (REQ-003), and mirrors the satisfaction statement across all 5 presentation contracts (REQ-004), plus the 34-caller migration (GAP-48). Synthesis itself flags it "broadest + highest regression risk." Under-rating hides the true sequencing cost of the FIRST phase. | major | Re-rate 002 as L; split into 002a (root policy + bridge + 5-presentation mirror) and 002b (classifier API + tests + caller migration), or add a `002-callsites` sub-phase owning the migration table before REQ-003 wires anything command-scoped. | 009 |
| GAP-52 | effort-realism | Phase 010 effort "S-M" is under-rated (actually M) | 010 closes F-027 (engine-level double-injection dedupe: hash check + canonical copy + mirror note) and F-029 (deferral of six root-policy sections for autonomous runs) — both mutate the INJECTOR, not doc content, with blast radius = every session/executor/command. S-M is sized for a two-line doc tweak; an injection-pipeline regression silently degrades the four prior phases' cells. | major | Re-rate 010 as M; add a blast-radius REQ that every accepted 010 fix round-trips through the post-002 six GPT cells (RVB-008/RSB-008/ACB-004/IMB-004/IMB-005) so a regressed injector fails acceptance instead of silently degrading. | 009 |
| GAP-53 | effort-realism | Phase 008 is research-sized (no design iteration exists) mis-labeled as implementation | VERIFIED: design passes were iter-011→002, 012→004, 013→003, 014→005; iter-015 was ranking-only. F-035/036/037/038 + F-009 (008's findings) have NO design iteration — they appear only in ranking notes ("L effort, higher integration risk," "deterministic setup can replace them later"). 008 asks for a build/compile step + typed 21+ refs + drift guards + a deterministic setup loader — design AND build — in one phase, unlike 003 which ships a ready design. | major *(MiniMax: blocker as-phased)* | Carve 008 into a dedicated `036-compiled-contract` packet whose FIRST deliverable is the design (build contract, ref-type taxonomy, drift-guard CI, loader spec); leave a stub REQ in 035 ("adopt 036 once shipped") that can defer/drop without blocking 009. | 009 |
| GAP-54 | effort-realism | Phase 009 is research-sized (no design iteration) + one cell does all verification | VERIFIED: F-032/033/034 (009's findings) have NO design iteration (only ranking notes: "large workflow/API change," "resumable subcommands are the larger redesign"). 009 closes F-032 (split heaviest command into 3 sub-invocations), F-033 (cross-iteration cache), F-034 (conditional budget) — three unbounded R&D REQs with IMB-001-high as the ONLY acceptance cell; resumability, cache behavior, budget dispatch have no measurable cell. | major *(MiniMax: blocker as-phased)* | Split 009 into a design packet (036b/c) + implementation; 009 must define (a) a resume contract (shared-ledger schema, parent-sessionId propagation — see GAP-13/28 key lifecycle), (b) per-sub-invocation acceptance cells (init→work→synthesize each gating a cell), (c) a measurable cache-hit-rate metric. | 009 |
| GAP-55 | second-order | 008 compiled-contract replaces indirection with a NEW source/compiled drift class | 008 REQ-001 names "checksum drift guards" but specifies no guard contract: when it fires (hard-fail/warn/override), the resolve order between maintained layered sources and the compiled artifact, or the recovery path. The drift guard IS the next 14-file chain — maintainers now keep sources AND the compiled artifact AND the drift CI in sync. Worse than the indirection it replaces because the old indirection was inert when broken; a hard-fail guard breaks every CLI branch, a warn guard is noise. | major | Before 008 lands, ship a written guard contract (enforcement locus, fail/warn UX, recovery command) + a single canonical build target + a non-blocking "compile-only" CI stage; gate 008 on that contract existing (compounds GAP-53). | 010 |
| GAP-56 | second-order | 010 injection-slimming starves first-turn Gate-2 routing (010↔006 interaction) | 010 REQ-002 defers "agent routing" + "quick reference" for autonomous runs, but agent-routing content includes the skill-advisor dispatch tokens + MCP transport briefs the FIRST-turn Gate-2 scorer reads. Meanwhile 006 widens the phrase net with noun-gated boosters — a slimmed injection + a wider phrase net can produce phantom routes the slimmed context can't adjudicate, degrading the exact cells 006 was meant to flip. The deferral criterion conflates per-command relevance with per-session relevance. (Distinct from GAP-09's 002↔010 collision — this is 010↔006.) | major | Replace REQ-002 with a per-command deferral matrix (which sections each command keeps vs defers) + add RSB-004/ACB-003/IMB-003 (006's cells) as 010's non-regression gate: 006's flips must not move after 010 lands. | 010 |
| GAP-57 | second-order | The 8-rule prelude / executor-contract block is itself a Claude-shaped convention GPT will mis-read | 002 REQ-003 wires a ~90-word hard-rule prelude as the first thing a command-scoped run reads; 007 REQ-001 adds a top-of-file EXECUTOR CONTRACT block (7-10 hard rules). But the SAME literalism that made GPT reproduce the Gate-3 halt template verbatim (F-001) will reproduce the new prelude verbatim when it doesn't apply (interactive/ad-hoc), and F-022 names "salience competition between style guidance and hard blockers" as a failure shape. The program replaces one over-literal reading with a second hard-block family — a literal executor seeing the contract in an interactive session skips Gate-2 routing exactly as 002's design intended for autonomous. | major | Collapse the prelude INTO the compiled contract (008), bind it to `executionMode==='autonomous'` AND `commandContract.writeBoundary` presence, ship it typed (not raw injected text), and add a regression cell counting "prelude fired in interactive session." | 010 |
| GAP-58 | second-order | **The simpler root fix the plan doesn't take**: one self-contained command contract subsumes 002/003/007/008/010 | The same defect (executor doesn't see the contract because it's distributed across files + weighted by file position) is fixed in FIVE fragments: 002 prelude, 003 render markers, 005 progress records, 007 executor-contract block, 008 compiled flatten, 010 injection slim. A single build-time artifact — one typed contract per command embedding Gate-3 precedence, setup render block, output template, write boundary, receipts + progress contracts, tool list, as the first ~150 lines of the compiled prompt — subsumes them all: 007 → a pointer, 010 → "inject the contract + one link," 008 → this build step, 002 → one line in the contract, 003 → markers inside it. | major | Before starting any phase past 002, evaluate restructuring the packet from 10 phases to ~3: (a) contract compiler + first command's contract, (b) retrofit remaining 4 commands + fold 002/003/007/010 into contract-author guidelines, (c) acceptance = one run per command on 033 + a checksum-matches-source assertion. Highest-leverage strategic decision in the review. | 010 |
