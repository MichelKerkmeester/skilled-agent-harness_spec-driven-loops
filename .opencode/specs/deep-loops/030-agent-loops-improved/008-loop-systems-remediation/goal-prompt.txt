Autonomously finish the 008-loop-systems-remediation phase tree at .opencode/specs/deep-loops/030-agent-loops-improved/008-loop-systems-remediation/. READ its handover.md FIRST (full state, file:line, gotchas). Already fixed+committed (do NOT redo): 001 rollback hash-guard, 003 model-benchmark ledger, 006 plugin tests; 002 :92 is design-intent.

EXECUTOR: cli-codex gpt-5.5 reasoning=high service_tier=fast, SEQUENTIAL one-file dispatches — the deep-improvement/playbook files OOM codex at xhigh/parallel, so use high effort and tell it to read ONLY the target file. Fallback GLM-5.2 via cli-opencode on OOM/error. Tests need PATH=/opt/homebrew/bin (node v25).

REMAINING FIXES (one phase = one commit):
- 002-deep-improvement-promotion-safety: in deep-loop-workflows/deep-improvement/scripts/shared/promote-candidate.cjs ~L541, narrow the verifyMirrorSync gate so a legitimate IN-SYNC agent-definition promotion passes (only block genuine out-of-sync). Add a fail-before/pass-after regression test. Leave :92 (legacy no-phase canonical mutation) as documented design-intent.
- 004-adversarial-playbook-scenarios (markdown only): author 1 adversarial scenario per fixed cluster under each skill's manual_testing_playbook/, phrased to FAIL on regression, TEST EXECUTION pointing at the real regression test — loop-lock refresh-vs-reclaim split-brain; writeStateAtomic(undefined) throws; concurrent diff-gated append no row loss; deferred-writer flush error surfaces; jsonl no-trailing-newline no-corrupt; mk-goal terminal same-objective revival drops stale usage; injection clamp preserves directive+fence markers; fanout exit-0/no-artifact not fulfilled. Match the existing scenario format.
- 005-tighten-playbook-pass-criteria (markdown only): VERIFY then complete — for deep-loop-runtime manual_testing_playbook scenarios in 04--state-safety, 06--coverage-graph, 09--fanout, 03--validation, change the SCENARIO CONTRACT pass criterion from "inspection OR tests" to REQUIRE the test RUN (EXIT 0) AND source confirms. Audit the 3 source-only scenarios (speckit-autopilot-lifecycle, single-loop-telemetry-heartbeat, coverage-graph-fuzzy-merge): mandate the runnable test or mark inspection-by-nature.
- 006-p2-test-adequacy: make deep-loop-runtime tests/unit/atomic-state.vitest.ts + jsonl-repair.vitest.ts GENUINELY concurrent via a CHILD-PROCESS harness (spawn 2 node procs appending the same JSONL through the real append fn; assert both rows survive; pattern: tests/helpers/spawn-cjs.ts). Do NOT use an in-process barrier (codex's timed out). Plugin tests already done.

PER-PHASE LOOP: read handover + phase spec -> implement scope-locked -> VERIFY (cited test passes, RED-before-fix) -> finalize that phase's spec.md Status=Complete + plan/tasks/implementation-summary -> regen metadata (generate-description.js + backfill-graph-metadata.ts; base .opencode/specs/skilled-agent-orchestration) -> validate.sh <phase> --strict -> commit ONE phase (explicit paths, trailers) -> push (rebase if HEAD moved; a concurrent session also commits this branch). Then finalize the still-unstatused/Review specs (001 Review->Complete etc.) and regen the 009 parent + 156 parent.

DISCIPLINE: no "done" without --strict pass + green tests; comment hygiene (never embed spec/phase/REQ ids in code comments); never git add -A (explicit paths); IGNORE the skill-benchmark/design-token-lint.vitest.ts failure (a concurrent session's WIP, not ours).

DONE WHEN: all 6 009 children Status=Complete and validate --strict clean; validate.sh 008-loop-systems-remediation --recursive green; deep-loop-runtime suite >=545 green AND 6/6 plugin tests AND deep-improvement suite green (excluding design-token-lint); everything committed and pushed to system-speckit/028-memory-search-intelligence.
