Both web tools (WebSearch and WebFetch) are permission-blocked in this session, so post-vendor web state goes in as unverified cutoff knowledge. I have sufficient repo evidence to complete the re-score. Final result below.

# Iteration 7: C7 strategy re-score: paths A/B/C and P0-P4

## Focus
Re-score the three migration paths from 002 §7 (A: SQLite→libSQL→Turso; B: SQLite+LanceDB hybrid; C: wait for 1.0) and the P0–P4 priority ladder (002 §4) against the iteration 1–6 verdict set and the context-report blocker map, using the vendored v0.7.0-pre.6 compat layer as primary evidence. The decisive new facts are: (1) the shipped better-sqlite3 sync compat mode collapses the async-migration risk that originally justified the libSQL stepping stone, (2) the hard-blocker set has narrowed to FTS5/Tantivy, vector index, and WITH RECURSIVE, and (3) the production coupling surface has grown to ~127 files across three MCP servers (027 statediff/tombstones/incremental-index included), which invalidates the baseline 500–800 LOC adapter scope.

## Findings

- **NEW | CHANGED — Path A demoted from "Recommended" to "conditional, cloud-sync-only":** the libSQL stepping stone's core sale in 002 §7 was preserving a synchronous better-sqlite3-shaped API while waiting for Turso [SOURCE: research/002:177-184]. Turso's compat layer now provides that directly — `transaction(fn)` with default/deferred/immediate/exclusive modes implemented as BEGIN/COMMIT/ROLLBACK wrappers [SOURCE: external/turso-main/bindings/javascript/packages/common/compat.ts:116-146] and a working `pragma()` that forwards to `PRAGMA` statements [SOURCE: compat.ts:148-166]. Migrating better-sqlite3→libsql→Turso-compat is now two API hops where better-sqlite3→Turso-compat is one; the intermediate leg no longer reduces the risk it was designed to reduce. Path A retains an edge only if multi-device cloud sync is needed before Turso 1.0 (libSQL embedded replicas are production-proven; Turso CDC/sync is per-connection opt-in and experimental per iteration-6 verdicts).
- **NEW (unverified web) — Path A's libSQL leg carries maintenance-mode risk:** per knowledge cutoff (Jan 2026), Turso's engineering focus moved to the Rust rewrite after the Limbo→Turso transition, with libSQL in maintenance posture. WebSearch and WebFetch were both permission-blocked this session, so June-2026 status is UNVERIFIED — flagging, not asserting. If true, Phase 2.5 of Path A (libSQL native DiskANN vectors, 002:181) targets a feature-frozen engine.
- **KNOWN | UNCHANGED — Path B's trigger condition is unchanged but its cost rose:** Turso still has no native vector index (linear scan only; gap 1 unchanged per iterations 1–6), so LanceDB remains the only embedded ANN option if vector p95 breaches at >10k memories [SOURCE: research/002:118]. But 027 raised hybrid cost: statediff reconciliation enumerates single-DB targets (memory_index, embedding_cache, fts, bm25) [SOURCE: context/context-report.md:94] and tombstone lifecycle runs read→insert→delete in one transaction [SOURCE: context-report.md:21]; splitting vectors into a second store breaks the single-transaction reconciliation assumptions, adding split-brain handling that 002's "1-2 weeks" estimate [SOURCE: research/002:208] did not include.
- **NEW | CHANGED — Path C upgrades to the winner, in a modified form ("C-prime: adapter + de-SQLite-ism pre-work + compat pilot"):** the codebase already contains the two fallback engines a Turso compat pilot needs — `BM25_ENGINE_VALUES` routes between FTS5 and an in-memory BM25 engine [SOURCE: context-report.md:96; lib/search/bm25-index.ts], and per-dimension blob tables (`vec_768` via `vector_table_name_for_profile`) exist as non-vec0 vector storage [SOURCE: context-report.md:101], with the f32 blob format sqlite-vec-compatible (iteration 1–6 verdict). A local-file compat pilot is therefore feasible without waiting for 1.0, by accepting in-memory BM25 + blob linear scan as pilot posture. This option did not exist at baseline.
- **NEW — Revised decision matrix (winners per criterion):**

  | Criterion | A (libSQL first) | B (LanceDB hybrid) | C′ (adapter + compat pilot) | Winner |
  |---|---|---|---|---|
  | Migration risk | Medium (was Low: double API hop + maintenance-mode leg) | Medium-High (statediff/tombstone split-brain) | Low | **C′** |
  | Sync-API preservation | Yes (libsql pkg) | n/a | Yes (compat `transaction`/`pragma`) | **C′** (no longer A-exclusive) |
  | Time to first value | 2–4 days swap, value reduced | 2-3 weeks+ | 2–3 days (adapter slice 1) | **C′** |
  | FTS continuity | FTS5 preserved until final hop | FTS5 preserved | FTS5 preserved; pilot uses in-memory BM25 | **A/B tie** (C′ pilot degrades weighted bm25()) |
  | Vector performance | Same; DiskANN option (unverified engine status) | Best (real ANN) | Same; pilot = linear scan | **B** |
  | Cloud sync path | Mature (embedded replicas) | None | Experimental (CDC/sync, .tshm) | **A** |
  | Operational complexity | Same | Higher (two stores) | Same | **A/C′ tie** |
  | 027-surface compatibility (statediff/tombstones/incremental) | Touches all | Worst | Best (no backend change until ready) | **C′** |
  | Future flexibility | Reduced (dead-end leg) | High | Highest (adapter keeps every option) | **C′** |

  C′ wins 5 of 9 criteria and loses only cloud-sync (A) and vector performance (B), both of which remain conditional triggers rather than current needs.
- **PARTIAL | REFUTED (internal consistency) — the context-report blocker map overstates blockers and must be read with iteration 1–6 corrections:** it asserts "Turso/libSQL lacks triggers" [context-report.md:22-23], "no ATTACH" [:36-37], "no VACUUM INTO" [:41-42], "libSQL lacks .pragma()" [:108]. Iterations 1–6 refuted all four against the vendored tree (CREATE TRIGGER default-on, ATTACH/DETACH supported, VACUUM INTO works, .pragma() exists in SDK and compat). The surviving HARD blockers from that map are exactly three: FTS5 surface (bm25() weights-at-query-time, 'rebuild'/'delete-all'/'optimize' commands) [:35,38,39,111,146], vec0/loadExtension [:34,37,109,117,125,142], and WITH RECURSIVE (causal-boost 2-hop walk, memo.ts dependents closure) [:143-149].
- **NEW | CHANGED — compat mode's not-implemented set is now precisely known and bounds the swap effort:** `backup()`, `serialize()`, `function()` (UDFs), `aggregate()`, `table()`, and `loadExtension()` all throw "not implemented" [SOURCE: compat.ts:168-194]. Consequences: sqlite-vec can never load under compat (vec0 swap is mandatory, not optional); checkpoint v2 survives via VACUUM INTO + file-copy (no serialize() dependency); no UDF escape hatch exists for emulating bm25() or recursive traversal in-engine.
- **NEW | CHANGED — C7b: P0 DatabaseAdapter is still the right first move, but its job and size changed:** 002 scoped ~500–800 LOC wrapping three files (vector-index-store, vector-index-queries, sqlite-fts) [SOURCE: research/002:65-73,57]. Measured today: ~127 production TS files reference better-sqlite3 across the three MCP servers (spec-kit ≈113, skill-advisor ≈8, code-graph ≈6), with hotspots statediff.ts (34 token hits), causal/sweep.ts (31), vector-index-store.ts (29), vector-index-schema.ts (28), memory-retention-sweep.ts (27), memory-index.ts handler (22) [SOURCE: rg counts, this iteration]. Because compat preserves sync `transaction`/`pragma`, the adapter's primary job is no longer sync→async bridging but **fencing five divergence ports**: (1) extension/vec0 KNN SQL, (2) FTS5 SQL + maintenance commands, (3) WITH RECURSIVE → app-side BFS behind a traversal port, (4) snapshot/recovery mechanics (WAL triplet, wal_checkpoint(TRUNCATE), backup→VACUUM INTO), (5) busy/retry predicate (SQLITE_BUSY → Busy/BusySnapshot). Revised adapter estimate: **1,200–2,000 LOC, 1–2 weeks** for the spec-kit server slice (≈85% of weight); do NOT wrap all 127 files — hotspots + ports only, long tail keeps a raw handle alias.
- **NEW | CHANGED — revised effort estimates for the four moves:** (a) adapter: 1,200–2,000 LOC / 1–2 weeks (was 500–800 LOC); (b) libsql swap: 2–4 days mechanical across ~127 prod files (was "1–2 days, ~120 imports" [research/002:180]) but strategic value down per Path A demotion; (c) Turso compat-mode local-file swap: **2–3 weeks** = days of import swap + FTS reroute to existing in-memory BM25 + vec0→blob-table reroute + app-side BFS for the two recursive CTEs (2–4 days) + retry-predicate rewrite (1–2 days), accepting linear-scan/unweighted-BM25 pilot posture; (d) full async conversion: **4–8 weeks** (baseline said "80+ files" [research/002:124]; now ~127 prod + ~150 test files, and 027 added transaction-dense surfaces — sweep tombstones, mutation-hooks statediff, incremental-index v31–v34) — but compat mode makes full async **deferrable**, needed only for remote/sync deployment, no longer a local-migration precondition.
- **NEW — P0–P4 ladder re-score:** P0 adapter: KEEP, reshape to ports model, upsize (above). P1 "evaluate libSQL Q2 2026": REPLACE with "Turso compat-mode pilot behind adapter" — 002's libSQL evaluation questions [research/002:79-83] are now answered for Turso directly (sync mode: yes via compat; sqlite-vec loading: permanently no). NEW P1.5: app-side BFS rewrite of causal-boost + memo recursive CTEs under current SQLite with equivalence tests — path-independent prep, required by every Turso end-state, ~2–4 days. P2 vector benchmark: KEEP gated (still no native index; add quantized two-stage variant to the benchmark set). P3 full-migration gates at 1.0: KEEP, add four explicit gates — Tantivy out of experimental AND coexisting with MVCC (currently mutually exclusive), WITH RECURSIVE or completed BFS, window-function parity audit, CDC maturity for audit-trigger replacement. P4 LanceDB hybrid: KEEP conditional, same trigger (>10k memories / p95 breach), cost note raised for statediff/tombstone split-brain.

## Ruled Out
- Path A as the default recommendation — the compat layer removes its unique advantage (sync-API preservation) and the stepping stone adds an API hop; only a near-term cloud-sync requirement re-elevates it.
- Treating full async conversion as a migration precondition — compat mode makes it deferrable to the remote/sync phase.
- The baseline 500–800 LOC adapter scope — refuted by measured coupling (~127 production files, three servers, 027 hotspots).
- Immediate LanceDB adoption (P4 promotion) — current scale (~2k memories) plus the established quantized two-stage mitigation keeps Path B's trigger unfired.

## Dead Ends
- WebSearch and WebFetch are both permission-blocked in this session — June-2026 libSQL maintenance status and Turso post-vendor releases could not be verified; flagged as cutoff-knowledge where used.
- Sandbox rejected `$(...)` expansion, `awk` pipelines, and `rg --stats`, so token counts were gathered via plain `rg -c`/`rg -l` (per-file counts eyeball-summed; treat file counts as exact, match totals as approximate).

## Sources Consulted
- `.opencode/specs/z_future/sqlite-to-turso/research/002 - recommendations-sqlite-to-turso-migration.md` (§3 :36-59, §4 :65-112, §5 :116-127, §7 :177-214)
- `.opencode/specs/z_future/sqlite-to-turso/context/context-report.md` (:19-30 reuse catalog, :34-50 HARD integration points, :94-103 conventions, :139-158 open gaps, :132-134 prior art)
- `.opencode/specs/z_future/sqlite-to-turso/external/turso-main/bindings/javascript/packages/common/compat.ts` (:116-146 transaction, :148-166 pragma, :168-194 not-implemented set)
- Repo-wide `rg` sweeps over `.opencode/skills/{system-spec-kit,system-code-graph,system-skill-advisor}/mcp_server` for better-sqlite3 / `.pragma(` / `.transaction(` / WITH RECURSIVE / statediff / tombstone coupling
- Iteration 1–6 verdict set (provided in-prompt; not re-derived)

## Reflection
- What worked: the vendored compat.ts read was high-yield — its exact implemented/not-implemented split converts several baseline unknowns into hard effort bounds; the coupling census directly falsified the 002 adapter scope.
- What failed: all web verification (WebSearch/WebFetch permission-blocked), leaving libSQL's mid-2026 maintenance status — the single biggest open input to Path A's score — unverified; several sandbox rejections cost ~3 tool calls.
- Confidence: **medium-high** — high on C7b and the matrix rows grounded in vendored code and repo measurements; medium on Path A's demotion magnitude, since it partially rests on the unverified libSQL maintenance-mode claim (though the double-API-hop argument holds even if libSQL is actively maintained).

## Recommended Next Focus
Verify current libSQL maintenance/release status and any post-vendor Turso releases (vector index, Tantivy stabilization, 1.0 signals) via web in a session where WebSearch/WebFetch are permitted, since that is the only remaining input that could re-elevate Path A.
