Spec folder: skilled-agent-orchestration/135-skill-readme-standardization/022-system-skill-advisor-readme (pre-approved, skip Gate 3). READ-ONLY: do not write, create or edit any file. Return findings as your final assistant message only.

Role: You are verifying exact facts about the `.opencode/skills/system-skill-advisor/` skill so a README rewrite cites them correctly. This pass locks the precise, citable details and finds stale facts.

Context: Read `.opencode/skills/system-skill-advisor/SKILL.md` in full, its current `README.md`, `ARCHITECTURE.md`, `INSTALL_GUIDE.md`, the `references/` files, `mcp_server/lib/scorer/lane-registry.ts` (live lane weights), `mcp_server/tools/` and `mcp_server/handlers/` (the live tool list and dispatch), `mcp_server/schemas/` (response contracts), and `mcp_server/scripts/skill_advisor.py` (the compat shim). Verify against real file contents, not memory. This skill is the standalone Gate 2 routing engine: a five-lane scorer, a SQLite skill graph, a freshness and trust contract, runtime hooks across Claude / Codex / Gemini / Devin / OpenCode, plus the `mk_skill_advisor` MCP server.

Action: Report under exactly these six headings, every claim cited to a real file path:

1. EXACT TOOLS & INVOCATION — the precise MCP tool names and their key inputs/options, quoted from the tool descriptors or handlers. Confirm there are nine tools and the split (four advisor: `advisor_recommend`, `advisor_rebuild`, `advisor_status`, `advisor_validate`; five skill-graph: `skill_graph_scan`, `skill_graph_query`, `skill_graph_status`, `skill_graph_validate`, `skill_graph_propagate_enhances`). List the `skill_graph_query` queryTypes (depends_on, dependents, enhances, enhanced_by, family_members, conflicts, transitive_path, hub_skills, orphans, subgraph) and the `advisor_recommend` options (topK, includeAttribution). Note `skill_graph_propagate_enhances` is trusted-caller gated.
2. SCORER LANES & WEIGHTS — the five lanes and their LIVE weights from `lane-registry.ts` (and the separate shadow weights if present), each cited. State which lanes are live. Confirm whether the README's weight table (explicit_author 0.42, lexical 0.28, graph_causal 0.13, derived_generated 0.12, semantic_shadow 0.05) matches the source.
3. FRESHNESS & TRUST CONTRACT — the trust-state vocabulary (`live`, `stale`, `absent`, `unavailable`), what each means and the caller action, plus how the daemon watches `SKILL.md` and `graph-metadata.json` and bumps generation. Cite the freshness/daemon source.
4. KEY FILES — a table of the real files and subfolders (path + one-line role): SKILL.md, ARCHITECTURE.md, INSTALL_GUIDE.md, each `references/` subfolder, `hooks/` per runtime, `mcp_server/` (note `advisor-server.ts`, `tools/`, `handlers/skill-graph/`, `schemas/`, `lib/scorer/lane-registry.ts`, `scripts/skill_advisor.py`, `database/`), feature_catalog/, manual_testing_playbook/.
5. TROUBLESHOOTING & FAQ — the concrete failure modes (stale/absent/unavailable trust state, ambiguous top-2, `advisor_validate` baseline drift and the exact corpus top-1 baseline number if stated, newly-added skill not picked up, Devin hook `MK_SKILL_ADVISOR_HOOK_DISABLED` vs `SPECKIT_SKILL_ADVISOR_HOOK_DISABLED` precedence) and the 3 to 5 questions a user most likely asks, with short grounded answers (including why the advisor is a separate MCP server and its relationship to system-spec-kit).
6. STALE FACTS IN CURRENT README — list every claim in the current `README.md` that disagrees with SKILL.md, the lane registry, the tool descriptors or the real files (tool count/names, lane weights, version, env var names, baseline number, embedder cascade details, paths). Write "none found" if clean.

Format: One structured markdown report under those six numbered headings. Cite real file paths verbatim. Mark anything unverifiable as UNKNOWN. No preamble.
