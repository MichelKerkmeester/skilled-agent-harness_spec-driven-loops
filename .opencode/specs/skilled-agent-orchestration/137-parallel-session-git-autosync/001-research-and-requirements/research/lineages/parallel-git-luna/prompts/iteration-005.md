# Deep-Research Iteration 005 Prompt

Resolved route: mode=research target_agent=deep-research. Agent definition loaded: yes.

Focus: adversarial architecture review and final acceptance matrix. Compare optimistic auto-rebase/retry, a serialized local publisher/daemon, a remote merge queue/Gerrit, and a hybrid. Reconcile the five prior evidence passes into a complete RQ1–RQ7 answer, including crash windows, ambiguous push acknowledgments, object reachability, rollback, IDE freshness, and the boundary between saved uncommitted files and unsaved editor buffers. This is the fifth evidence pass; do not synthesize early in the loop, but prepare a complete evidence-backed recommendation for phase_synthesis.

Research boundary: write only under `.opencode/specs/skilled-agent-orchestration/137-parallel-session-git-autosync/001-research-and-requirements/research/lineages/parallel-git-luna`. Prefer primary Git and official service documentation and cite every load-bearing claim with `[SOURCE: URL]`.

Required negative knowledge: state precisely which parts of the user’s “zero blockers/no loss” contract are impossible for arbitrary overlapping edits or unsaved editor buffers, and define the strongest achievable contract.

Executor contract: `cli-codex`, model `gpt-5.6-luna`, reasoning effort `max`, service tier `fast`, sandbox mode `workspace-write`. This is a detached child lineage; do not spawn a nested same-kind Codex executor.
