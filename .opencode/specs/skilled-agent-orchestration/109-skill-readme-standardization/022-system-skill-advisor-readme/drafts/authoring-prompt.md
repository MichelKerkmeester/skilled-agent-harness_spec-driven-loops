Spec folder: skilled-agent-orchestration/z_archive/109-skill-readme-standardization/022-system-skill-advisor-readme (pre-approved, skip Gate 3). OUTPUT-ONLY task: do NOT write, create or edit any file. Return ONLY the finished README markdown in a single fenced ```markdown block as your final message. No preamble, no commentary.

Role: You are a technical writer rewriting the README for the `system-skill-advisor` skill in a specific house voice.

Context you must use:
- The factual map is in `.opencode/specs/skilled-agent-orchestration/z_archive/109-skill-readme-standardization/022-system-skill-advisor-readme/context/context-report.md`. Read it. Every fact, path and command must come from it (or from re-reading `.opencode/skills/system-skill-advisor/` to confirm). The current README is mostly accurate, so preserve the facts and change the voice and structure. Three facts were corrected (see Hard requirements).
- The required structure and voice are in `.opencode/skills/sk-doc/assets/skill/skill_readme_template.md`. Read it and follow the fillable scaffold exactly.
- The golden example is `.opencode/skills/sk-git/README.md`. Read it; yours should read like it.
- The voice rules are in `.opencode/skills/sk-doc/references/global/hvr_rules.md`. Read and obey them.

Hard requirements on facts:
- There are nine MCP tools on the `mk_skill_advisor` server (namespace `mcp__mk_skill_advisor__*`): four advisor tools (`advisor_recommend`, `advisor_rebuild`, `advisor_status`, `advisor_validate`) and five skill-graph tools (`skill_graph_scan`, `skill_graph_query`, `skill_graph_status`, `skill_graph_validate`, `skill_graph_propagate_enhances`). The last is trusted-caller gated (internal). `advisor_recommend` options are `topK`, `includeAttribution` and `includeAbstainReasons` (name all three, or describe them as a set; do NOT list only the first two). `advisor_validate` requires `confirmHeavyRun: true`. `skill_graph_query` has ten query types: `depends_on`, `dependents`, `enhances`, `enhanced_by`, `family_members`, `conflicts`, `transitive_path`, `hub_skills`, `orphans`, `subgraph`.
- The scorer has five live lanes with these live weights: `explicit_author` 0.42, `lexical` 0.28, `graph_causal` 0.13, `derived_generated` 0.12, `semantic_shadow` 0.05. These are the live fusion weights from `lane-registry.ts`.
- Trust state has four values: `live`, `stale`, `absent`, `unavailable`. State the caller action for each. A daemon watches every `SKILL.md` and `graph-metadata.json`, bumps a generation counter on change and invalidates the cache, but does NOT auto-rebuild; only `advisor_rebuild` mutates the SQLite database.
- Embedder: the `semantic_shadow` lane uses a pluggable embedder layer shared with mk-spec-memory, with a registry of text-tuned embedding models that defaults via a local-first cascade to a local model. Do NOT say "single shipped manifest" and do NOT pin a model count (it drifts). Point a reader to `INSTALL_GUIDE.md` §12 for the model registry and swap workflow.
- Boundary: the advisor recommends a skill, it does not replace the target skill (sk-code, sk-doc, sk-git, mcp-code-mode do the work). Memory and spec folders stay in system-spec-kit. Structural code search stays in system-code-graph. The only dependency on system-spec-kit is the shared embeddings stack and the IPC bridge; other code is isolated as of v0.2.0.
- Do NOT cite a version number (the README does not, and the narrative voice omits version lines). Do NOT pin drift-prone counts (feature-catalog group totals, scenario totals, manifest counts). You may state there are nine MCP tools and five scorer lanes (both stable). Every cited path must resolve. In RELATED DOCUMENTS link the `changelog/` directory rather than a single old version file.

Structure and voice:
- Numbered ALL-CAPS H2 sections with `---` dividers, a numbered OVERVIEW section, sequential numbering. No table of contents.
- Frontmatter (title `system-skill-advisor`, one-sentence description, trigger_phrases such as "skill advisor", "gate 2 routing", "advisor_recommend", "which skill should handle this", "route this request"), then H1, then a one-line `>` blockquote pitch.
- Section 1 = AT A GLANCE (4-row table). Section 2 = OVERVIEW with "Why This Skill Exists" (problem-first) then "What It Does". Then QUICK START, HOW IT WORKS, INTEGRATION & NAVIGATION, TROUBLESHOOTING, FAQ, an optional VERIFICATION, and a final RELATED DOCUMENTS section. Drop any section that does not fit and renumber.
- Voice: no em dashes, no semicolons, no Oxford commas, no banned words, no setup phrases. Lead with the reader. Prose carries the explanation; tables only for genuine 4-plus-item lookups (the tool list, the lane table, the trust-state table all qualify). Vary subsection counts.
- Lead the distinctive value: this is standalone Gate 2 routing that answers "which skill should handle this prompt" with a calibrated, prompt-safe score, and refuses to answer on a stale or absent index rather than fabricate a recommendation the runtime would trust. In QUICK START show an `advisor_status` then `advisor_recommend` then (conditional) `advisor_rebuild` sequence with expected output. In HOW IT WORKS cover the five-lane scorer and prompt-safe attribution, the freshness and trust contract with the daemon, and the SQLite skill graph. INTEGRATION states the boundary with the target skills, with system-spec-kit and with system-code-graph.
- Target length roughly 240 to 340 lines. Every command shows its expected output.

Return the complete README as one ```markdown fenced block. Nothing else.
