Spec folder: skilled-agent-orchestration/z_archive/109-skill-readme-standardization/022-system-skill-advisor-readme (pre-approved, skip Gate 3). READ-ONLY: do not write, create or edit any file, and do not run the skill or its MCP tools. Return findings as your final assistant message only.

Role: You are a documentation analyst gathering an accurate factual map of one skill so its README can be rewritten.

Context: The skill lives at `.opencode/skills/system-skill-advisor/`. Read its `SKILL.md` in full, its current `README.md`, `ARCHITECTURE.md`, `INSTALL_GUIDE.md` and the files under `references/` (scoring, graph, runtime, config, hooks, decisions). Stay within that skill directory. This skill is the standalone Gate 2 routing surface for Spec Kit: it scores a non-trivial prompt against indexed skill metadata, hook signals and a SQLite skill graph, then returns calibrated, prompt-safe recommendations the runtime can trust. It runs as its own MCP server named `mk_skill_advisor` (namespace `mcp__mk_skill_advisor__*`). Note the boundary with the target skills it routes to (sk-code, sk-doc, sk-git, mcp-code-mode) and with system-spec-kit (memory, spec folders) only to describe it.

Action: Produce a factual map for a README rewrite. Report under exactly these eight headings:

1. PURPOSE — one sentence: what the skill does.
2. PROBLEM — 2 to 4 sentences: the human pain point it removes (problem-first, not a feature list). Why an agent guessing which skill to use is unreliable, why ambiguous prompts get silently mis-routed without calibrated confidence, why routing must be safe to log from hooks, and why a routing index that has gone stale and answers anyway is dangerous.
3. MODES & CAPABILITIES — the five-lane scorer, prompt-safe attribution, daemon-backed freshness, the trust-state vocabulary, the SQLite skill graph with auto-propagated enhances, the multi-runtime hooks, the Python compatibility shim, the standalone process boundary, each one line.
4. INVOCATION — the nine MCP tools (the four advisor tools `advisor_recommend`, `advisor_rebuild`, `advisor_status`, `advisor_validate` and the five skill-graph tools `skill_graph_scan`, `skill_graph_query`, `skill_graph_status`, `skill_graph_validate`, `skill_graph_propagate_enhances`) and the documented Gate 2 routing flow (recommend, then invoke the top skill, then surface ambiguity when top scores are close). Note the freshness gate.
5. KEY FILES — a table of the real files and subfolders (path + one-line purpose): SKILL.md, ARCHITECTURE.md, INSTALL_GUIDE.md, each `references/` subfolder, `hooks/` per-runtime folders, `mcp_server/` (note `lib/scorer/lane-registry.ts`, `tools/`, `handlers/`, `scripts/skill_advisor.py`, `database/`), feature_catalog/, manual_testing_playbook/.
6. BOUNDARIES — what it does NOT do: it recommends a skill, it does not replace the target skill; memory and spec folders stay in system-spec-kit; structural code search is system-code-graph; it depends on system-spec-kit only for the embeddings stack (semantic_shadow lane) and the IPC bridge.
7. TROUBLESHOOTING & FAQ MATERIAL — common failure modes and gotchas (stale or absent or unavailable trust state, ambiguous top-2 candidates, validate baseline drift, a newly-added skill not yet picked up, the Devin hook env-var precedence) and the 3 to 5 questions a user actually asks.
8. STALE FACTS — anything in the current README.md that is inaccurate versus SKILL.md and the real files (tool count, lane weights, version, env var names, the advisor_validate corpus baseline number, embedder details, paths). Write "none found" if clean.

Format: Return one structured markdown report under those eight numbered headings. Cite real file paths verbatim. Mark anything you cannot verify as UNKNOWN. No preamble, no closing commentary.
