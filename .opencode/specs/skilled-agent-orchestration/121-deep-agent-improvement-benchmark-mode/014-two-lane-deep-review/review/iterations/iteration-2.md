[{"id":"security-2-1","severity":"P0","dimension":"security","title":"Auto workflow shell-injects user fields","file":".opencode/commands/deep/assets/deep_start-model-benchmark-loop_auto.yaml","line":147,"evidence":"The workflow command interpolates raw placeholders into a shell string: `--profile {profile} --outputs-dir {spec_folder}/improvement/benchmark-outputs --scorer {scoring_method} --grader {grader}`. Those fields are user-bound inputs in the same YAML.","fix":"Do not render user-bound values into shell commands. Execute via an argv array or a Node wrapper/config JSON, validate scorer/grader enums, and shell-escape or reject unsafe profile/spec paths before any Bash fallback."},{"id":"security-2-2","severity":"P0","dimension":"security","title":"Confirm workflow shell-injects user fields","file":".opencode/commands/deep/assets/deep_start-model-benchmark-loop_confirm.yaml","line":178,"evidence":"The interactive workflow uses the same raw shell interpolation for `--profile {profile}`, `{spec_folder}`, `{scoring_method}`, and `{grader}` in the benchmark command.","fix":"Use structured argv/config execution for every command field in this workflow, including journal/reducer/promote steps; never concatenate untrusted placeholders into shell strings."},{"id":"security-2-3","severity":"P0","dimension":"security","title":"Criteria shell execution defaults permissive","file":".opencode/skills/deep-agent-improvement/SKILL.md","line":276,"evidence":"The Lane B hardening note says `DEEP_AGENT_ALLOW_CRITERIA_EXEC=0` refuses criteria-driven shell execution and `DEEP_AGENT_GRADER_CACHE_RAW=0` redacts raw grader output, but both default to the permissive value for backward compatibility.","fix":"Invert both defaults: deny criteria shell execution and redact raw grader cache unless an explicit trusted-profile allowlist or per-run opt-in enables them."},{"id":"security-2-4","severity":"P1","dimension":"security","title":"Fixture regexes can ReDoS scoring","file":".opencode/skills/deep-agent-improvement/scripts/model-benchmark/run-benchmark.cjs","line":107,"evidence":"`compilePatterns` converts profile/fixture strings directly with `new RegExp(value, 'i')`; `scoreFixture` then runs those regexes against output content without a safety check, length cap, or timeout.","fix":"Reject unsafe regexes during profile/fixture load, cap pattern length/count, or use RE2/bounded string matching so malicious benchmark profiles cannot hang the runner."},{"id":"security-2-5","severity":"P1","dimension":"security","title":"Prompt text can become CLI flags","file":".opencode/skills/deep-agent-improvement/scripts/model-benchmark/dispatch-model.cjs","line":159,"evidence":"For `cli-gemini`, `buildSpawnSpec` constructs `const args = [promptText, '-m', model, '-y', '-o', 'text'];` after reading `promptText` from `opts.prompt_file`; prompt content beginning with `-` can be parsed as CLI options by common argument parsers.","fix":"Pass prompts through stdin or a prompt file for every executor, or insert a `--` sentinel and put all fixed options before it; add tests with prompt text starting `--help` and `--model`."}]