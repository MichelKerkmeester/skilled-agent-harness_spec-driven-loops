# Seat B — sk-interface-design de-vendor + uniqueness + Open Design integration (READ-ONLY)

You are a skill-architecture and software-licensing analyst. Determine how to evolve the `sk-interface-design` skill to be MORE UNIQUE, de-vendored from the `ui-ux-pro-max` reference repo, and INTEGRATED with the new `mcp-open-design` skill — by sourcing design data live from the installed Open Design app.

## Verified context (read the real files)
- `sk-interface-design` at `/Users/michelkerkmeester/MEGA/Development/Code_Environment/Public/.opencode/skills/sk-interface-design`. It currently vendors 9 MIT-licensed CSVs from the `ui-ux-pro-max` repo under `assets/data/` (`styles, colors, typography, ui-reasoning, products, landing, ux-guidelines, charts, app-interface`), queried by `scripts/design_search.py` + `design_search_core.py` (zero-dep BM25). It carries `LICENSE-ui-ux-pro-max.txt` (verbatim MIT) + `THIRD-PARTY-NOTICES.md` + SKILL.md license frontmatter "Apache-2.0 (principles) plus MIT (data and search)". Its base lineage is Anthropic's `frontend-design` skill (Apache-2.0). READ: `SKILL.md`, `references/*.md`, `assets/data/*` (headers), `THIRD-PARTY-NOTICES.md`, `LICENSE*.txt`, `changelog/`.
- **Open Design** bundles ~150 design-systems at `/Applications/Open Design.app/Contents/Resources/open-design/design-systems` (each a structured DESIGN.md system; READ `README.md`, `_schema/`, and 3 examples e.g. `apple`, `airbnb`, plus one more) and ~139 skills at `.../open-design/skills`, plus 111 `design-templates`. These are reachable from a terminal via the new `mcp-open-design` skill (`od mcp` tools `get_file`/`search_files`/`list_files`, and `od tools design-systems read`).
- The operator's intent: drop the `ui-ux-pro-max` attribution ("the terms of service thing from the external repo we used as reference") and make `sk-interface-design` better + more unique, integrated with open-design.

## Tasks
1. **OD data model.** Characterize Open Design's design-system format (read `_schema` + examples): does a DESIGN.md system carry tokens, components, type scales, reasoning? How does an agent consume one? Same brief for an OD `skill`. Note what is RICHER or DIFFERENT vs sk-interface-design's CSVs.
2. **De-vendor + uniqueness plan.** Can `sk-interface-design` REPLACE its static `ui-ux-pro-max` CSV inventory by sourcing design-system/pattern data LIVE from Open Design via `mcp-open-design`? Map each current CSV → an OD equivalent OR original content we author. Spell out what becomes genuinely OURS/unique (the anti-default judgment + the live OD integration) and which third-party dependency is dropped. Keep `design_principles.md` (the Apache-2.0 authority) intact.
3. **Integration design.** How should `sk-interface-design` route to `mcp-open-design` (e.g. when an OD design system matches the brief, reuse it; how this folds into the existing `claude_design_parity.md` loop). Keep the skill lean and anti-default (no style-chooser, no generator).
4. **LICENSING sequence (precise + legally careful).** MIT requires retaining the copyright/permission notice WHILE the verbatim compilation ships. Produce the exact ordered steps: (a) replace/remove the vendored CSV data + the search script if it only serves that data; (b) THEN remove `LICENSE-ui-ux-pro-max.txt` + `THIRD-PARTY-NOTICES.md` + the MIT clause in the SKILL.md frontmatter; (c) what MUST STAY (the Apache-2.0 Anthropic `frontend-design` base + `LICENSE.txt` + its attribution). Flag any content that is NOT cleanly replaceable and where attribution must remain.

## Method & constraints
- READ-ONLY analysis (sk-interface-design files + OD bundle). NO mutating commands; do NOT actually delete or edit anything.
- **Spec folder pre-approved (skip Gate 3).** Shared working tree: you may WRITE exactly ONE file — `/Users/michelkerkmeester/MEGA/Development/Code_Environment/Public/.opencode/specs/skilled-agent-orchestration/150-open-design-terminal-and-interface-integration/001-terminal-control-and-integration-research/research/seats/seat-b.findings.md`. No other writes; no `git`.

## Output
Write your COMPLETE structured findings (markdown) to that one file: sections per tasks 1–4, plus an ordered "DE-VENDOR CHECKLIST" and a "RISKS / KEEP-ATTRIBUTION CASES" list. Put a 1-paragraph summary in your final reply.
