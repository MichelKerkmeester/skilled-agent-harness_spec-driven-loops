# Iter-3 — ALL skill READMEs + internal references review

## Role
Senior deep-reviewer. Read-only.

## Scope
Walk every skill at `.opencode/skills/*/README.md` + `.opencode/skills/*/SKILL.md` + `.opencode/skills/*/references/**/*.md`.

### Checks
1. NO live mention of `deep-ai-council` outside the explicit historical-narrative allow-list (changelogs v1+earlier of sk-ai-council; skill-advisor changelog v0.3.0.md).
2. NO live mention of `sk-small-model` outside the historical allow-list (sk-ai-small-model/changelog/v0.1+v0.2; cli-devin/cli-opencode changelog history).
3. Renamed skill paths `.opencode/skills/sk-ai-council/` and `.opencode/skills/sk-ai-small-model/` are referenced consistently.
4. Skill cross-references (in references/*.md) point at the new paths.

## Output
JSON FINDINGS + NARRATIVE. End.
