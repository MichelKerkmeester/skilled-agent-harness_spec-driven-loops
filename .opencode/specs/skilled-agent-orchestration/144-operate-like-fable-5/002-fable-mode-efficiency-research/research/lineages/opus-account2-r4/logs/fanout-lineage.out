The `opus-account2-r4` fan-out lineage ran to completion. All outputs are confined to the lineage dir; everything outside it was read-only (source materials + repo surface verification).

**Loop summary**
- **phase_init** — bound `artifact_dir` directly to the `config.fanout_lineage_artifact_dir` override (did not run `resolveArtifactRoot`); wrote `deep-research-config.json`, `deep-research-strategy.md` (with the Non-Goals + Stop-Conditions charter), and seeded `deep-research-state.jsonl`.
- **phase_main_loop** — 5 iterations to `maxIterations` (binding stop; would also converge): EXTRACT-1 (F1–F15) → EXTRACT-2 (G1–G9) → SURFACE MAP → OPTIMIZE (12 ranked recs) → FOLLOW-UPS+GAPS. newInfoRatio descended monotone **0.95 → 0.78 → 0.58 → 0.22 → 0.14**; 5/5 key questions answered; each iteration produced a non-empty `iteration-NNN.md` + a JSONL delta carrying `type/run/newInfoRatio/status/focus` + novelty justification + ruled-out directions.
- **phase_synthesis** — wrote `research.md`, `deep-research-dashboard.md`, `deep-research-findings-registry.json`, and appended the `synthesis_complete` event. JSON/JSONL all validate (jq).

**This lineage's genuine, source-read findings** (not copied — read every source in full myself), with independent contributions beyond a plain restatement:
- The **thermostat is proven, not hypothetical** — the `UserPromptSubmit` hook fired on *this* research session and carried a constitutional reminder (empirical ride-along for rec #1).
- The AGENTS.md "dead pointer" is a **hyphen/underscore naming drift** (`skill-advisor-hook.md` cited; `skill_advisor_hook.md` exists) → fix is a one-char-class edit + a pointer-resolution test, not authoring a doc.
- Rec #12 is **"one comparison away"**: `executor-audit.ts` already records the actual model and self-escalates SIGTERM→SIGKILL; it just never compares requested-vs-actual or fails loud.
- Both carried round-1 follow-ups confirmed **still-open by grep-absence**.

**Efficiency core (land-first cluster):** #1 one-paragraph governor on the live hook, #2 mutation-check/claim-falsifier into sk-code, #3 recursion-control rule + xhigh briefs — all Tier-B, low-cost, low-blast, and (per G4/G5) buying *efficiency* (median words 47→18, tool:text 1.41→3.91), not capability.

**Most likely to be wrong:** the OpenCode/Codex per-turn-hook read-reliability (rated "runtime-dep" — only the Claude wiring was verified). Logged as gap #1 and merge-question Q-merge-1 for the parent orchestrator.

FANOUT_LINEAGE_COMPLETE:opus-account2-r4
