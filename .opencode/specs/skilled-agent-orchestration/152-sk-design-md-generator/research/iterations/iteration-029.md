# Iteration 029 — Track D (mimo)

## Focus
Track-D ranked borrow list (ADOPT-NOW/SCOPED/DEFER, top-3 composable)

## Findings
1. **[P0] Section requirements data-driven (17→10 core + 5 conditional)** — Open Design 9-section schema. Iteration-024 verdict: REAL. 5 required sections (§0 Brand Context, §7 Content & Voice, §11 State Matrix, §12 Iconography, §9 Accessibility) are never written in practice across 4 gold-standard examples (Vercel 10, Stripe 9, Linear 9, Supabase ~9). Validator enforces 15 section headers via checkSectionCompleteness header-only string-match, forcing AI to fabricate content for data-poor sections. Adoption cost: LOW (~40 LOC in validate.ts + design_md_format.md table edit).
   - Recommendation: In design_md_format.md §1 table (lines 27-48): demote §0, §7, §11, §12 to 'Required: conditional (data-driven)'. In validate.ts checkSectionCompleteness (line 241): make requirements data-driven — §2.5 requires darkMode.supported===true, §6.5 requires motion data, §12 requires iconSystem.detected===true. File: tool/scripts/validate.ts + tool/resources/design_md_format.md. Phase: VALIDATE + FORMAT.
   - Evidence: iteration-024 P0 (section-count inflation REAL), iteration-018 P0 (fabrication-surface analysis), iteration-008 P0 (Section 0/1/6/7 as primary fabrication vectors), validate.ts:241-285
2. **[P0] Coverage-based token election (additive pre-gate)** — design-extract (Manavarya09, 3.3k stars). Iteration-024 verdict: SCOPED-additive. Current classifyColorStability (cluster.ts:400-435) uses 5-signal additive score where pageCoverage contributes only ±30 of max ~120. A one-off hero gradient on 1/8 pages (12.5% coverage) with 500+ element-frequency can reach L2. Coverage election as HARD PRE-GATE (pagesCoverage <0.3 → cap at L3) prevents single-page token leakage. Adoption cost: LOW (~25 LOC pre-gate in cluster.ts:569).
   - Recommendation: Add pre-classification HARD GATE in clusterTokens() (cluster.ts:569) BEFORE stability pass: if totalPages >= 2 and pagesCoverage < 0.3 → force L3. Fix single-page coverage inflation: when totalPages === 1, compute coverage as frequencyPercentile. File: tool/scripts/cluster.ts. Phase: EXTRACT Phase 1 output assembly.
   - Evidence: iteration-024 P1 (coverage-election SCOPED-additive), iteration-003 P0, iteration-007 P0, cluster.ts:400-435, cluster.ts:738
3. **[P0] Deterministic value-table rendering (Sections 2-8 auto-render)** — Diez DocsGen, Supernova, Backlight, Zeroheight, Knapsack, Style Dictionary. Current pipeline AI-writes ALL 17 sections including pure value-tables (colors, typography, shadows, radii, spacing) — validate.ts catches phantom hex but NOT fabricated fontSize/fontWeight/lineHeight. Deterministic formatters eliminate the primary hallucination surface for numeric sections. Adoption cost: MEDIUM (~200 LOC new formatters.ts).
   - Recommendation: New tool/scripts/formatters.ts with formatColorTable(), formatTypographyTable(), formatShadowTable(), formatComponentTable(). Split WRITE phase: (1) AUTO-RENDER Sections 2-8 from tokens.json (zero AI), (2) AI-ANNOTATE Sections 1, 9-16 where judgment required. File: tool/scripts/formatters.ts (new), SKILL.md WRITE phase orchestration. Phase: WRITE phase refactor.
   - Evidence: iteration-011 P0, iteration-017 P0 (doc-as-token-view), iteration-022 P0 (f), iteration-023 P0, iteration-024 P1 (DTCG SCOPED — deterministic tables higher anti-fabrication ROI)
4. **[P0] Evidence-gated named principles (conditional on ≥3 tokens)** — Superposition/CSS Stats thin-evidence omission pattern. Iteration-024 verdict: SCOPED — evidence-gating, not abolition. Current format mandates named principles even with sparse data (AP-25 treats missing principle as defect). Iteration-024 confirmed evidence-backed principles (e.g., 'shadow-as-border' with real frequency data) ARE highest-value prose. Fix: require ≥3 distinct backing tokens across ≥2 pages. Adoption cost: LOW (~30 LOC in design_md_format.md + prompt template).
   - Recommendation: In design_md_format.md §9 line 544: change unconditional 'Named principle' to 'REQUIRED ONLY when ≥3 shadow/gradient/depth-related tokens exist across ≥2 crawled pages. When absent, stamp: ABSENT — insufficient tokens.' Apply same gate to §1 design principles and §3 named strategies. File: tool/resources/design_md_format.md + assets/design_md_prompt_template.md. Phase: FORMAT + WRITE.
   - Evidence: iteration-024 P1 (named-principle SCOPED), iteration-003 P1, iteration-008 P0, iteration-021 P0, design_md_format.md:534-575
5. **[P0] Focus indicator bug fix (a11y-extract.ts:101-103)** — a11y-extract.ts:101-103 returns {consistent: true} when focusStyles.length === 0. Interaction capture is OFF by default (extract.ts:81 noInteraction=true). validate.ts never checks focus prose against tokens. Iteration-024 verdict: REAL. Adoption cost: LOW (~20 LOC across 3 files).
   - Recommendation: Three changes: (1) add captured:boolean to A11yTokens.focusIndicator type (types.ts:271-274), (2) return {style:null, consistent:null, captured:false} when zero focusStyles (a11y-extract.ts:101-103), (3) add checkA11yFocusProvidence to validate.ts that fails focus prose when captured===false. Also flip extract.ts:81 default to interaction capture ON. File: a11y-extract.ts, types.ts, validate.ts, extract.ts. Phase: EXTRACT + VALIDATE.
   - Evidence: iteration-024 P0 (focus-consistent REAL), iteration-014 P1, a11y-extract.ts:101-103, extract.ts:81
6. **[P0] Source sentinels for prose claims (inline provenance)** — Zeroheight cite-the-token pattern. Iteration-024 verdict: SCOPED — source sentinels are clean structural check (WARNING-tier). Inline <!-- source: tokens.x --> sentinel parsed via existing stripHtmlComments (validate.ts:51-53). Adoption cost: LOW (~30 LOC in validate.ts).
   - Recommendation: Add checkSourceSentinels in validate.ts: parse <!-- source: tokens.x --> comments from raw MD, resolve dotted paths against DesignTokens, verify non-empty/non-null. WARNING-tier only. Keep separate from banned-word heuristic. File: tool/scripts/validate.ts. Phase: VALIDATE.
   - Evidence: iteration-024 P1 (checkProseClaims SCOPED), iteration-017 P1, iteration-023 P1, validate.ts:51-53
7. **[P1] Per-token confidence surfacing to writer** — Already exists in schema — ColorToken.confidence and TypographyLevel.confidence computed in cluster.ts:752,911 as 'high'|'medium'|'low' from frequency + page-coverage. Field populated but DESIGN.md writer does not surface it. Adoption cost: LOW (plumbing only, ~20 LOC in WRITE prompt).
   - Recommendation: Plumb to DESIGN.md writer: strong claims ('The primary brand color is...') only from high-confidence tokens. Low-confidence tokens → stamp 'ABSENT — insufficient evidence'. File: assets/design_md_prompt_template.md (add confidence-gating rule). Phase: WRITE.
   - Evidence: iteration-025 P0, iteration-020 P1, cluster.ts:752,911, types.ts:424,439
8. **[P1] Hybrid static-computed CSS var resolution** — project-wallace/CSS Stats hybrid pattern. dom-collector.ts captures --var names and raw authored values but never resolves var() chains. cluster.ts:662-675 cross-references only direct color strings — var()-referenced tokens produce zero matches. Adoption cost: LOW-MEDIUM (~40 LOC in dom-collector.ts resolution pass).
   - Recommendation: Add resolution pass in dom-collector.ts after stylesheet walk: for each CSSVariable, call getComputedStyle(document.documentElement).getPropertyValue(name) to capture resolved value. Store in new resolvedValue field on CSSVariable type. In cluster.ts:662-675, use resolvedValue (falling back to raw) for color matching. File: tool/scripts/dom-collector.ts, tool/scripts/types.ts, tool/scripts/cluster.ts. Phase: EXTRACT.
   - Evidence: iteration-012 P0, dom-collector.ts:105-143, cluster.ts:662-675, types.ts:44-49
9. **[P1] Deterministic role assignment (classifyColorRole)** — Token Studio group hierarchy, Style Dictionary CTI transform. Current role assignment is entirely LLM-decided in WRITE phase. color_role_taxonomy.md defines 19 functional roles with deterministic identification signals but cluster.ts doesn't execute them algorithmically. Adoption cost: MEDIUM (~80 LOC new classifyColorRole() in cluster.ts).
   - Recommendation: Add classifyColorRole() in cluster.ts: rank colors by usage-as dimension dominance (most-frequent bgColor+interactiveContext → primary, most-frequent text → text-primary, most-frequent border → border). CSS variable name match overrides algorithmic assignment. Reserve LLM for narrating WHY, not DECIDING role. File: tool/scripts/cluster.ts. Phase: EXTRACT.
   - Evidence: iteration-021 P0 (Step 5 role naming), iteration-022 P0 (h), color_role_taxonomy.md §1-§2, anti_patterns.md AP-03
10. **[P1] Occurrence-band pre-clustering** — CSS Stats occurrence-based clustering. Current OKLCH perceptual clustering (cluster.ts:698-724) can merge a true brand color (freq=2000) with a one-off decorative color (freq=4) at deltaE<3. Two-pass: bucket by frequency percentile, then perceptual-cluster within bands. Adoption cost: LOW-MEDIUM (~30 LOC refactor of clustering loop).
   - Recommendation: Refactor cluster.ts:574-724: pre-partition colors into frequency bands (top 30%, middle 40%, bottom 30%). Run perceptual clustering within each band. Cross-band merge only at tighter threshold (deltaE < 1.0). File: tool/scripts/cluster.ts. Phase: EXTRACT.
   - Evidence: iteration-012 P1, cluster.ts:698-724, cluster.ts:400-537
11. **[P1] Semantic component tagging (className/ARIA role pre-pass)** — Locofy/Builder.io pattern. dom-collector.ts:169-171 captures className, role, ariaLabel, nearestLandmark — but cluster.ts:1134-1230 uses NONE for detection, relying on brittle geometric thresholds. Adoption cost: LOW (~30 LOC pre-pass in cluster.ts §7).
   - Recommendation: In cluster.ts §7 Component Identification (line 1134): add pre-pass matching element.className against known patterns (/.btn/, /.card/, /.badge/, /.nav/, /.input/) and element.role against ARIA widget roles BEFORE geometric fallback. File: tool/scripts/cluster.ts. Phase: EXTRACT.
   - Evidence: iteration-015 P0, cluster.ts:1164-1229, dom-collector.ts:193-263
12. **[P1] tokens.css output (CSS custom properties)** — extract-design-system (arvindrk) dual-output pattern, anobel.com/004-bento-visuals (120+ CSS vars proven in production). Trivial transform from tokens.json → :root { --color-primary: #533afd; }. Directly importable by frontend agents. Adoption cost: LOW (~50 LOC new emit-css.ts).
   - Recommendation: New tool/scripts/emit-css.ts: read tokens.json, emit tokens.css with :root {} block grouped by category (--color-*, --font-*, --shadow-*, --radius-*, --spacing-*). Include semantic aliases. Run as Phase 1b after extract.ts. File: tool/scripts/emit-css.ts (new). Phase: EXTRACT output.
   - Evidence: iteration-023 P1, extract-design-system pattern, anobel.com/004-bento-visuals
13. **[P1] Section evidence grading (per-section completeness)** — Already partially exists — validate.ts graded score (0-100) with critical gating. Next step: per-section evidence-completeness comparing writer claims against backing tokens.json. Adoption cost: MEDIUM (~60 LOC new checkEvidenceCompleteness in validate.ts).
   - Recommendation: Add checkEvidenceCompleteness() to validate.ts: for each DESIGN.md section, count backing tokens from tokens.json and flag sections making claims with ZERO backing data as FABRICATED, thin data as PARTIAL, full backing as VERIFIED. File: tool/scripts/validate.ts. Phase: VALIDATE.
   - Evidence: iteration-025 P0, iteration-024 P0 (section-cut REAL), validate.ts:318-349
14. **[P1] DECISIONS + ANTI-PATTERNS sections in DESIGN.md** — Open Design 9-section includes DECISIONS (provenance-anchored) and ANTI-PATTERNS (baked into output). Our anti_patterns.md is a writer-side resource doc, not output. DECISIONS section doesn't exist — rationale scattered in prose. Adoption cost: LOW-MEDIUM (~40 LOC format spec + prompt update).
   - Recommendation: Add Section ~8.5 'Design Decisions & Provenance': decision name, claim, evidence (≥2 token values), tradeoff, anti-pattern link. Promote top 5-8 system-specific anti-patterns FROM anti_patterns.md INTO DESIGN.md output. File: tool/resources/design_md_format.md. Phase: FORMAT.
   - Evidence: iteration-018 P0, mcp-open-design/SKILL.md:224, stripe/DESIGN.md:17, vercel/DESIGN.md:12
15. **[P1] Semantic aliasing with CSS-variable provenance** — Specify SDTF, DTCG $ref. CSS variable names (--color-text-primary) carry author intent that is discarded after cross-referencing. Create named alias entries with provenance tags. Adoption cost: MEDIUM (~60 LOC in cluster.ts alias pass).
   - Recommendation: Add aliases section to tokens.json: when cssVariableNames contains --color-text-primary, create alias {key: 'text.primary', $ref: '#/colorTokens/hex:...', provenance: '--color-text-primary', confidence: 'css-var'}. When no CSS var exists, create inferred aliases from usedAs dominance with confidence: 'heuristic'. File: tool/scripts/cluster.ts. Phase: EXTRACT output.
   - Evidence: iteration-011 P1, iteration-016 P1, Specify SDTF composition/aliasing, DTCG §3.4
16. **[P1] Element-level selectorAnchors on tokens** — MiroMiro pattern. dom-collector captures full per-element styles but cluster.ts aggregates by RGBA key, discarding selector/element provenance. Add 'where-used' anchors. Adoption cost: LOW (~20 LOC in cluster.ts addColor()).
   - Recommendation: Extend ColorEntry in cluster.ts with Set<string> selectorAnchors. In addColor(), push '${el.tag}.${el.className}' from source element. Surface as selectorAnchors: string[] on ColorToken. Cap at 5. Apply to ShadowToken, RadiusToken. File: cluster.ts:576-607, types.ts:409-426. Phase: EXTRACT.
   - Evidence: iteration-020 P0, MiroMiro hover-to-inspect, cluster.ts:576-607
17. **[P1] Real DOM contrast pairs replacing synthetic cross-product** — a11y-extract.ts:265-304 already computes real (el.color, el.backgroundColor) pairs with usage counts. cluster.ts:798-831 caps at top-20 and computes synthetic cross-product pairs that never co-occur. §9 should use measured data. Adoption cost: LOW (source swap, ~10 LOC).
   - Recommendation: Replace §9 Contrast Ratios source from cluster.ts colorRelationships.contrastPairs to a11yTokens.contrastPairs. Add top-N cap (15). File: WRITE phase §9 source routing. Phase: WRITE.
   - Evidence: iteration-014 P0, a11y-extract.ts:265-304, cluster.ts:798-831
18. **[P1] Role-typed CSS variable dedup (prefix-grouped extraction)** — get-custom-properties-style pattern. Current cssVariables[] is flat concat of all --custom-properties with no role grouping. Prefix-group (--color-*, --spacing-*, --font-*) maps directly to DESIGN.md sections. Also captures component-level vars missed by :root-only scope. Adoption cost: MEDIUM (~50 LOC refactor of dom-collector.ts:105-143).
   - Recommendation: Replace flat extraction in dom-collector.ts:105-143 with prefix-grouped extractor partitioning by role prefix. Remove :root-only selector scope to capture all selectors. Each group feeds corresponding token builder. File: tool/scripts/dom-collector.ts. Phase: EXTRACT.
   - Evidence: iteration-012 P1, dom-collector.ts:118-126, types.ts:44-49
19. **[P2] DTCG $type/$value schema (parallel output first)** — W3C DTCG 2025.10, Style Dictionary v4, Tokens Studio, Specify. Iteration-024 verdict: SCOPED high-cost — does NOT directly fix prose fabrication (prompt/writer discipline issue). Real value is downstream interop + cleaner validator. ~200 LOC in cluster.ts + types.ts + validate.ts. Phased: parallel tokens.dtcg.json first.
   - Recommendation: Phase 1: emit tokens.dtcg.json alongside tokens.json (zero breaking changes). Phase 2: type-gated validation in validate.ts reading DTCG file. Phase 3: native migration once ecosystem adopts. File: tool/scripts/dtcg-wrap.ts (new), tool/scripts/types.ts, tool/scripts/validate.ts. Phase: EXTRACT output (parallel).
   - Evidence: iteration-024 P0 (DTCG SCOPED high-cost), iteration-007 P0, iteration-011 P0, iteration-022 P0 (b), W3C DTCG 2025.10
20. **[P2] CIEDE2000 contrast alongside WCAG ratio** — Contrast, Polypane, axe-core. WCAG ratio for text accessibility; CIEDE2000 for visual-distinction validation. Enables auto-flagging near-identical colors. Adoption cost: LOW-MEDIUM (~30 LOC in css-analyzer.ts).
   - Recommendation: Add CIEDE2000 to contrastPairs as complementary metric alongside WCAG ratio. File: tool/scripts/css-analyzer.ts. Phase: EXTRACT.
   - Evidence: iteration-022 P2 (g), iteration-014 P1, CIEDE2000 CIE standard
21. **[P2] Nearest-compliant-shade remediation** — Stark's algorithm. Given failing contrast pair, compute nearest OKLCh adjustment achieving ≥4.5:1. Uses existing culori/deltaE infrastructure. Adoption cost: MEDIUM (~60 LOC new remediation module).
   - Recommendation: Add nearestCompliantShade() to a11y-extract.ts or new remediation.ts. Binary-search OKLCh lightness. Clearly label RECOMMENDED (computed) not OBSERVED. File: tool/scripts/a11y-extract.ts or remediation.ts (new). Phase: WRITE §9.
   - Evidence: iteration-014 P1, cluster.ts:348-354 (deltaE OKLCh), cluster.ts:678 (culori)
22. **[P2] MCP token endpoint for structured agent queries** — Supernova AGENT-READY MCP endpoint. CLI fallback (query-token.ts) gives 80% of value. Lower priority since agents already read tokens.json directly. Adoption cost: MEDIUM (~80 LOC new script).
   - Recommendation: Phase 1: add tool/scripts/query-token.ts CLI (--key path, --format json|css). Phase 2 if demand: MCP tool registration. File: tool/scripts/query-token.ts (new). Phase: new tool.
   - Evidence: iteration-017 P2, iteration-023 P2, Supernova AGENT-READY MCP
23. **[P2] Structural-region confidence boost** — StyleSniff live DOM sampling with structural awareness. structuralRegion captured per element in dom-collector.ts:60-74 but never fed into stability classification. Adoption cost: LOW (~10 LOC in classifyColorStability).
   - Recommendation: In classifyColorStability(): add signal — if source elements >=80% in nav/header/footer (infrastructure regions), add +15 score. File: tool/scripts/cluster.ts:400-435. Phase: EXTRACT.
   - Evidence: iteration-020 P1, StyleSniff, dom-collector.ts:60-74, cluster.ts:400-435

## Questions Answered
- What are the ranked borrowable anti-fabrication techniques from ~20 external tools, deduplicated across 15 Track-D iterations?
- How do iteration-24 adversarial verdicts (DTCG=SCOPED high-cost, section-cut=REAL, election=SCOPED-additive, named-principle=SCOPED) affect tier assignment?
- Do the top 3 borrows (data-driven sections, coverage election, deterministic tables) compose into one coherent architecture?
- Which techniques are already partially implemented (confidence field, stability gating, proof.ts) and only need plumbing?

## Questions Remaining
- RESERVED: emergent angles/risks (permanently open)
- Prototype DTCG parallel output (tokens.dtcg.json) against stripe/tokens.json to measure migration effort
- A/B test: generate DESIGN.md with current mandatory-philosophy prompt vs evidence-gated prompt — measure fabrication rate per section
- Implement coverage-election pre-gate in cluster.ts, run against gold-standard examples to quantify L2→L3 demotion count
- Audit WRITE-phase prompt template for all 'must include' clauses lacking 'when absent, stamp ABSENT' fallback

## Next Focus
- Prototype DTCG parallel output (tokens.dtcg.json) against stripe/tokens.json to measure migration effort
- A/B test: generate DESIGN.md with current mandatory-philosophy prompt vs evidence-gated prompt — measure fabrication rate per section
- Implement coverage-election pre-gate in cluster.ts, run against gold-standard examples to quantify L2→L3 demotion count
- Audit WRITE-phase prompt template for all 'must include' clauses lacking 'when absent, stamp ABSENT' fallback

## Summary
Top 3 borrows: (1) data-driven section requirements (17→10+5 conditional, eliminates fabrication pressure for data-poor sections — P0/LOW cost), (2) deterministic value-table rendering for Sections 2-8 (AI writes only narrative sections — P0/MEDIUM cost), (3) coverage-based token election as additive pre-gate (P0/LOW cost). They COMPOSE into a coherent architecture: coverage election cleans token input → deterministic formatters render value-tables → evidence-gated prose with source sentinels handles narrative → data-driven section requirements remove obligations when data is absent.
