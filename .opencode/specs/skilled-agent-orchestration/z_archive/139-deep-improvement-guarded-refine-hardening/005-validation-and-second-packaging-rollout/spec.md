# Phase 005: Validation + Second-Packaging Rollout

**Parent:** 139-deep-improvement-guarded-refine-hardening | **Status:** Implemented | **Teachings:** T12 + all | **Depends:** 001-004

## Goal

Prove the hardened system end to end on the pilot, then demonstrate the contract generalizes by onboarding a second packaging — closing the loop the council demanded: no autonomy claims without live evidence.

## Work items

1. **Copywriter re-validation.** With phases 001-004 landed: run `/deep:start-packaging-refine-loop` on the Copywriter (live, N=3, full fixture set, held-out per the 004 convention) through to either a **genuine live promotion-accept** (operator then merges the worktree branch deliberately) or multi-signal convergence with journaled evidence. Record the phantom-gap trend across iterations — the council's "gap shrinking" signal — in the run report.
2. **Red-team gauntlet as regression suite.** The Phase 001 fixtures (frozen-surface touch, held-out regression, synonym-laundering attempt against the code linter, interactive held-out fixture) become a scripted gauntlet that every future Lane D change must pass; wire into the manual testing playbook and, where dispatch-free, into vitest.
3. **Second packaging pilot.** Onboard one more Barter system (candidate: Barter deals — closest sibling, same HVR superset) to the `_loop/loop.py` contract using the Phase 001 conformance checklist: freeze its scoring surface, make its copies derivable, define gradeable fixtures + held-out, dry-run, then one guarded live iteration. Every friction point becomes a contract-doc or template fix — the goal is that packaging #3 needs no code changes.
4. **Close-out.** Update deep-improvement changelog + feature catalog with measured results; update Barter project memory (`copywriter-autorefine-loop.md`) and `Copywriter/SYNC.md`; file follow-up specs for anything deliberately deferred (e.g. the Copywriter's self-MEQT leniency fix in the ai-systems area; full deep-loop-runtime Bayesian convergence adoption if Phase 001 chose the mirror-the-contract route).

## Acceptance

- One journaled live `promote_accept` on Copywriter with held-out non-regression, or documented convergence with all gates green.
- Red-team gauntlet: 100% of attack fixtures rejected/halted, repeatably.
- Second packaging completes a guarded live iteration with zero deep-improvement code changes (contract + docs only).

## Status (2026-06-09)

Complete. Both live runs documented in live-revalidation-evidence.md (Copywriter: guarded blockedStop on a real HVR violation, all gates green; deals: decline-when-clean at 23.67/25, phantom gap +0.67). Barter deals onboarded to the contract with ZERO deep-improvement changes (frozen DEAL surface D3/E4/A3/L3, empty derived set, harness + grader + 2 fixtures, gauntlet 10/10); Copywriter + deals live runs complete through the full command stack.
