I’ll keep this scoped to the six named profile files and the registry read only. I’m loading the relevant workflow skills now because this edits `.opencode/skills` content and small-model prompt profiles.
I’m going to read the six profiles plus the registry data first, then patch only unsupported claims and the §5 ownership phrasing.
I found the unsupported claims exactly where expected: the registry has default-unverified RCAF/medium/no benchmark for SWE/DeepSeek/Kimi/Qwen/GLM, and empirical TIDD-EC vs RCAF only for MiniMax-M2.7. I’m patching only those profile statements and the dispatch-gotcha ownership wording now.
The first patch was rejected before applying because one SWE-1.6 context hunk did not exist verbatim. I’m reapplying with smaller, exact-context patches.
One verification search still found the word `outperforms` in a negated DeepSeek sentence; I’m removing the comparative wording entirely so text checks won’t flag it.
I also found three command-fragment remnants in §5 dispatch gotchas. They’re being converted to wrapper-neutral pointers so the profiles do not own invocation syntax.
I’m running scope and content checks now: unsupported-claim search, wrapper-fragment search, then repo/spec validation.
The worktree has many unrelated existing changes. The six targeted profile paths are currently under an untracked `references/models/` directory, so `git diff` does not show them as tracked modifications; I’m continuing to verify the file contents directly and will not touch unrelated changes.
The approved spec folder is a phase parent; I’m validating it with the strict spec validator and then checking target-file content/scope.
Strict spec validation passed for the parent and all phase children. I’m running a focused markdown/profile sanity check next, without touching any other files.
The available quick validator is skill-wide, not per-profile, so it may reflect unrelated existing skill changes. I’ll still run it and report any scope caveat.
Updated only the six requested profile files.

**Edits Per File**
- `deepseek-v4-pro.md`: Rephrased TIDD-EC/no-measured-gain and RCAF comparison claims to registry-default/no model-specific benchmark wording; updated §5 to say flags come from `model-profiles.json` and wrappers live in `cli-devin`/`cli-opencode`.
- `kimi-k2.6.md`: Softened TIDD-EC/COSTAR language to “not specified/no benchmark”; removed §5 wrapper snippet and pointed to executor-owned wrappers.
- `qwen3.6.md`: Removed the unsourced “80% general-purpose framework” claim; updated §5 wrapper ownership wording and links.
- `glm-5.1.md`: Removed the unregistered “plain Role + Action” fallback wording; updated §5 wrapper ownership wording and removed wrapper snippet.
- `minimax-2.7.md`: Removed RACE from the scored comparison; removed the invented `15+ fixtures` confidence threshold; updated §5 wrapper ownership wording and removed wrapper snippet.
- `swe-1.6.md`: Marked BUILD and dense-plan/strict-bundle cautions as profile-level `cli-devin` guidance, not registry `fallback`/`avoid` data; updated §5 wrapper ownership wording and removed wrapper snippet.

**Verification**
- `validate.sh .opencode/specs/skilled-agent-orchestration/130-prompt-knowledge-layering --strict`: passed for parent and all phase children.
- `quick_validate.py .opencode/skills/sk-prompt-models --json`: valid, with one skill-description length warning outside the requested profile-file scope.
- Focused searches found no remaining targeted unsupported phrases or wrapper snippets in the six files.
