# Iteration 5 — Synthesis + Cross-Cutting

## Verdict
CONDITIONAL

## Summary
All 11 edge cases from `edge-cases.md` are resolved: filename embeds, JSON keys, regression fixtures, observability JSONL, and hardcoded skill IDs are fully rotated in active scope. Advisor routing returns `sk-prompt` top-1 (confidence 0.9262). Final scoped grep returns 0 active-scope hits. No P0 blockers exist. However, the documentation layer has a systematic SPECKIT_LEVEL contradiction spanning phases 004-006 (both spec.md and implementation-summary.md) that prior iterations missed, plus the unresolved P1 FRONTMATTER_MEMORY_BLOCK on `resource-map.md` that contradicts Phase 006's claim of "0 errors, 0 warnings." The rename is operationally complete; the remaining issues are documentation hygiene only.

## Findings

### P0 (Blockers)
None.

### P1 (Required)
1. **Parent validation fails: `FRONTMATTER_MEMORY_BLOCK: 1 issue`** — `resource-map.md:1-13` — First reported in Iteration 1, still unresolved. The `resource-map.md` frontmatter is missing `trigger_phrases`, `importance_tier`, and `contextType` fields. Compare with `spec.md:5-13` which has all three. `validate.sh --strict` exits with 1 error at the phase-parent level. This directly contradicts `006-advisor-and-validate/implementation-summary.md:95` which claims "Strict validate parent + 6 children (recursive) | PASS — 0 errors, 0 warnings."

   **Carried forward from:** Iterations 1, 3, 4. **Status:** Unresolved across all 5 iterations.

### P2 (Suggestions)
1. **`graph-metadata.json` `derived.status` is `"planned"`** — `graph-metadata.json:44` — Stale despite code-complete (all 6 phases done). Auto-generated by `generate-context.js` on next memory save.

   **Carried forward from:** Iteration 1.

2. **`graph-metadata.json` `derived.last_active_child_id` is `null`** — `graph-metadata.json:222` (verified via python3 extraction) — Expected `"skilled-agent-orchestration/082-sk-improve-prompt-rename/006-advisor-and-validate"`. Auto-regenerated on next memory save.

   **Carried forward from:** Iteration 1.

3. **Phase 002 implementation-summary frontmatter template contamination** — `002-skill-folder-rename/implementation-summary.md:2,5-10,36,48` — Title line 2 contains template literal `"[template:level_1/implementation-summary.md]"`. Trigger phrases lines 5-8 are generic defaults (`"implementation"`, `"summary"`, `"template"`, `"impl summary core"`). `importance_tier` is `"normal"` (vs `"important"` in all other phases). `contextType` is `"general"` (vs `"implementation"`). `<!-- SPECKIT_LEVEL: 1 -->` at line 36 contradicts metadata table `| **Level** | 2 |` at line 48. The `_memory.continuity` block itself is intact.

   **Carried forward from:** Iteration 3.

4. **SPECKIT_LEVEL vs metadata Level contradiction across phases 004, 005, 006 (systematic)** — The HTML comment `<!-- SPECKIT_LEVEL: 1 -->` conflicts with the metadata table `| **Level** | 2 |` in ALL THREE phases' `spec.md` AND `implementation-summary.md` files (6 files total). Prior iterations (specifically Iteration 3) caught Phase 002's instance but missed that the same pattern exists across phases 4-6.

   **Evidence (spec.md):**
   - `004-runtime-mirrors/spec.md`: `<!-- SPECKIT_LEVEL: 1 -->` vs `| **Level** | 2 |`
   - `005-root-and-config/spec.md`: `<!-- SPECKIT_LEVEL: 1 -->` vs `| **Level** | 2 |`
   - `006-advisor-and-validate/spec.md`: `<!-- SPECKIT_LEVEL: 1 -->` vs `| **Level** | 2 |`

   **Evidence (implementation-summary.md):**
   - `004-runtime-mirrors/implementation-summary.md:32`: `<!-- SPECKIT_LEVEL: 1 -->` vs `:44`: `| **Level** | 2 |`
   - `005-root-and-config/implementation-summary.md:32`: `<!-- SPECKIT_LEVEL: 1 -->` vs `:44`: `| **Level** | 2 |`
   - `006-advisor-and-validate/implementation-summary.md:33`: `<!-- SPECKIT_LEVEL: 1 -->` vs `:45`: `| **Level** | 2 |`

   **Root cause:** Per `deep-review-strategy.md:53`, phases 4-6 are intentionally declared Level 1 (mechanical/verification-only). The `SPECKIT_LEVEL: 1` comment matches this intent. The metadata table `| **Level** | 2 |` was either scaffolded from Level 2 precedent (Phase 003) and never adjusted, or copied from the Phase 002/003 template without adjusting the table row. Phases 1-3 are correctly Level 2 with both comment and table aligned. The `validate.sh` does not flag this because it reads the metadata table value, not the HTML comment.

   **Finding class:** consistency. **Severity:** P2 — no operational impact, only documentation self-consistency. **New for this iteration.**

## Verification Evidence

### Final scoped grep (active scope)
```bash
rg -l 'sk-improve-prompt' .opencode .claude .codex .gemini *.md *.json \
  --glob '!**/z_archive/**' --glob '!**/z_future/**' \
  --glob '!**/054-*/**' --glob '!**/055-*/**' --glob '!**/059-agent-implement-code/**' \
  --glob '!**/061-*/**' --glob '!**/063-*/**' --glob '!**/067-*/**' \
  --glob '!**/070-*/**' --glob '!**/079-*/**' \
  --glob '!**/081-cli-copilot-deprecation*/**' \
  --glob '!**/026-graph-and-context-optimization/**' \
  --glob '!**/082-sk-improve-prompt-rename/**' \
  --glob '!**/.git/**'
```
**Result: 0 hits** — active scope is clean.

### Advisor probe
```bash
python3 .opencode/skills/system-spec-kit/mcp_server/skill_advisor/scripts/skill_advisor.py "improve my prompt" --threshold 0.0
```
**Result:** top-1 = `sk-prompt`, confidence 0.9262, score 0.7935, dominant_lane = `explicit_author`. Routing confirmed.

### Strict validate (parent)
```bash
bash .opencode/skills/system-spec-kit/scripts/spec/validate.sh .opencode/specs/skilled-agent-orchestration/082-sk-improve-prompt-rename --strict
```
**Result: FAILED** — Errors: 1 (FRONTMATTER_MEMORY_BLOCK: 1 issue on resource-map.md). Warnings: 0.

### Skill folder rename
- `.opencode/skills/sk-prompt/` exists (8 entries). ✓
- `.opencode/skills/sk-improve-prompt` does NOT exist. ✓
- Changelog symlink: `.opencode/changelog/sk-prompt -> ../skill/sk-prompt/changelog`. ✓
- `.opencode/changelog/sk-improve-prompt` does NOT exist. ✓

### Edge case re-tests (all 11 from edge-cases.md)

| # | Edge Case | Result |
|---|-----------|--------|
| 1 | Filename embeds | `find . -name "*sk-improve-prompt*"` returns only `./barter/coder/.opencode/skills/sk-improve-prompt`, `./barter/coder/.opencode/changelog/14--sk-improve-prompt` (out of scope, separate Barter repo nested copy), and `./.opencode/specs/skilled-agent-orchestration/082-sk-improve-prompt-rename` (packet self, excluded from grep). ✓ |
| 2 | JSON keys in skill-graph.json | All 5 locations use `sk-prompt`. 0 residual `sk-improve-prompt`. ✓ |
| 3 | Changelog symlink | Renamed to `.opencode/changelog/sk-prompt` pointing to `../skill/sk-prompt/changelog`. ✓ |
| 4 | URL/path links | `rg '\.opencode/skills/sk-improve-prompt/'` in active scope returns 0 hits. ✓ |
| 5 | Hardcoded skill IDs (scorer lanes) | `explicit.ts`, `lexical.ts`, `fusion.ts`, `skill_advisor.py`: 0 `sk-improve-prompt` hits. ✓ |
| 6 | Regression fixtures | `labeled-prompts.jsonl`, `skill_advisor_regression_cases.jsonl`: 0 `sk-improve-prompt` hits. ✓ |
| 7 | Smart-router observability | `smart-router-measurement-results.jsonl`, `smart-router-measurement-report.md`: 0 `sk-improve-prompt` hits. ✓ |
| 8 | Memory database | `descriptions.json`: 0 `sk-improve-prompt` hits. ✓ |
| 9 | Code-graph node IDs | Scorer/advisor source files clean. CocoIndex needs re-index deferred to Phase 006. ✓ |
| 10 | Root instruction docs | `AGENTS.md`: 1 `sk-prompt`, 0 `sk-improve-prompt`. ✓ |
| 11 | Hidden runtime mirrors | `.claude/`, `.codex/`, `.gemini/`: 0 `sk-improve-prompt` hits. ✓ |

### CLI mirror prompt quality cards
| Mirror | sk-prompt | sk-improve-prompt |
|--------|-----------|-------------------|
| cli-claude-code | 1 | 0 |
| cli-codex | 1 | 0 |
| cli-gemini | 1 | 0 |
| cli-opencode | 1 | 0 |

All 4 mirror cards correctly reference `sk-prompt`. ✓

### Active changelogs
| Changelog | sk-improve-prompt |
|-----------|-------------------|
| `system-spec-kit/changelog/v3.4.0.0.md` | 0 hits |
| `agent-orchestration/v2.4.0.0.md` | 0 hits |

### Root docs
| File | Reports |
|------|---------|
| `README.md` | 4 `sk-prompt` hits |
| `AGENTS.md` | 1 `sk-prompt` hit |
| `.opencode/skills/README.md` | 4 `sk-prompt` hits |
| `.opencode/install_guides/README.md` | 3 `sk-prompt` hits |
| `.opencode/install_guides/SET-UP - AGENTS.md` | 2 `sk-prompt` hits |

All root docs reference `sk-prompt`. ✓

## Adversarial Self-Check

- **Did prior iterations miss the SPECKIT_LEVEL contradiction in phases 4-6?** Yes. Iteration 3 caught Phase 002's instance (line 36 vs 48) but did not extend the check to phases 4-6. The pattern is identical across all 4 phases (002, 004, 005, 006). This is the primary cross-cutting finding of this synthesis iteration.

- **Are my P0 findings actually blockers?** There are 0 P0 findings. The rename succeeds on every operational dimension: grep gate (0 active hits), advisor routing (top-1 = sk-prompt), skill folder (exists as sk-prompt), changelog symlink (renamed), cross-skill refs (all rotated). No operational surface is broken.

- **Is the FRONTMATTER_MEMORY_BLOCK a true blocker or a docs polish issue?** It is a validator error that contradicts Phase 006's implementation-summary claim of "0 errors, 0 warnings." However, it was flagged as P1 (not P0) in Iteration 1 because it does not block operational functionality — the skill routes correctly, the grep is clean, and all source surfaces are rotated. It is a spec-doc frontmatter compliance issue that predates the strict validator enforcement. Remains correctly classified as P1.

- **Could the `barter/coder/` paths be a leak vector?** No. `barter/coder/` is a nested third-party submodule or copy tree that is not part of the canonical `.opencode/` skill framework. The edge-cases.md (line 3) explicitly notes these as "outside canonical root .opencode/ surfaces." The spec.md scope (lines 114-121) limits rename to canonical root. No operational impact.

- **Is the SPECKIT_LEVEL contradiction actually systematic, or just copy-paste from Phase 002?** It is systematic — it appears in spec.md AND implementation-summary.md across 3 phases (004, 005, 006), plus Phase 002's implementation-summary.md. Phases 001 and 003 are correctly aligned (Level 2 in both comment and table). The pattern suggests the Level 2 metadata table row was inherited from a Level 1 template that was scaffolded after Phase 003, with the HTML comment lowered to Level 1 but the table cell left at Level 2.

- **Did I check for any `sk-improve-prompt` residuals in directories that the scoped grep excludes but might still be active?** The excluded scope (z_archive, z_future, completed packets, 082 packet self, .git) are all explicitly declared as out-of-scope per the spec's frozen-continuity rules. Iteration 4 confirmed no leaks into these excluded zones. My edge case re-tests covered all 11 edge case categories without relying on the scoped grep. No blind spots.

- **Could the `graph-metadata.json` stale fields mask other issues?** No. `status` and `last_active_child_id` are auto-generated metadata that do not affect any gate. They are regenerated on next `generate-context.js` call during memory save. The stale values are cosmetic.

- **Final self-challenge: what would I find if I did a 6th iteration?** The remaining issues are exclusively documentation metadata (frontmatter fields, template contamination, SPECKIT_LEVEL table alignment). No operational surface is broken. A 6th iteration would find 0 new operational issues. The review is converged — the FINDINGS section accurately captures all remaining actionable issues.

## Convergence Assessment

| Metric | Value |
|--------|-------|
| Active P0 | 0 across all 5 iterations |
| Active P1 | 1 (FRONTMATTER_MEMORY_BLOCK — unresolved since iteration 1) |
| Active P2 | 4 (graph-metadata stale × 2, Phase 002 template contamination, SPECKIT_LEVEL contradiction) |
| New P0 findings this iteration | 0 |
| New P1 findings this iteration | 0 |
| New P2 findings this iteration | 1 (systematic SPECKIT_LEVEL contradiction — first cross-cutting discovery) |
| NewFindingsRatio (severity-weighted) | ~0.08 (1 new P2 out of ~12 weighted finding-iterations) |

Convergence threshold (< 0.10) is met. The review has exhausted all dimensions and is stable.

## Remediation Path (if accepted)

| Finding | Fix |
|---------|-----|
| P1: resource-map.md frontmatter | Add `trigger_phrases:`, `importance_tier: "important"`, `contextType: "implementation"` to resource-map.md YAML frontmatter. Fix Phase 006 implementation-summary verification row from "PASS — 0 errors, 0 warnings" to reflect reality. |
| P2: graph-metadata.json staleness | Run `generate-context.js` to auto-regenerate `status` and `last_active_child_id`. |
| P2: Phase 002 template contamination | Customize `title`, `trigger_phrases`, `importance_tier`, `contextType` to match Phase 001/003 patterns. Fix `SPECKIT_LEVEL` comment to `2`. |
| P2: SPECKIT_LEVEL contradictions (phases 4-6) | Align metadata table `| **Level** | 2 |` to `| **Level** | 1 |` in both spec.md and implementation-summary.md for phases 004, 005, 006 (6 edits total). Or, if the phases are truly Level 2, update the `SPECKIT_LEVEL` comment to `2` and add `checklist.md` — but per `deep-review-strategy.md:53`, Level 1 is the documented intent. |
