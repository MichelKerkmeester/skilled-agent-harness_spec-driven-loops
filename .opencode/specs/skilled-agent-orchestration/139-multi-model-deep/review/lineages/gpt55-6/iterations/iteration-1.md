I’ll follow the specified deep-review workflow, bind the artifact directory directly to the provided override, and confine writes to the lineage directory.Memory trigger lookup refused the provided lineage session id because it is not a server-managed memory session; I’ll use that id only for the review lineage artifacts, as requested. Skill routing is user-directed to `deep-review`.I’m loading the deep-review protocol resources and inspecting the lineage directory before any writes, because the workflow requires state-first execution and the output path may already contain partial state.The lineage directory is fresh except for `.executor-state/`; `resource-map.md` is absent in the target spec folder, so the coverage gate will be recorded as skipped.`memory_context` also refused due server session binding, so init will record prior context as unavailable and continue from the packet files. Next I’m reading the spec artifacts to resolve the review scope.The spec packet is still scaffold/template content, so scope must be inferred from repository state and likely daemon/portability terms rather than from populated spec task metadata.The root fan-out config defines the real review target as a 12-file files target; I’ll run this lineage’s single pass against that scope, using direct file evidence rather than the scaffold-only packet docs.I found two high-confidence launcher correctness issues and one bounded test hardening issue. I’m going to create the lineage-local state packet, iteration, delta, dashboard, resource map, and synthesis report now.I’ll run lightweight read-only verification on the reviewed script/config surfaces so the synthesis has concrete validation evidence, not just file reads.The lightweight checks passed: `node --check` on the launcher/proxy/bridge/supervision/code-index scripts, `bash -n` on session cleanup, and JSON parsing for the three hook configs.Writing the finalized lineage packet now; all files are under `.opencode/specs/skilled-agent-orchestration/139-multi-model-deep/review/lineages/gpt55-6`.The lineage packet is written. I’m validating the generated JSON/JSONL and the mandatory iteration final verdict line before returning the completion marker.FANOUT_LINEAGE_COMPLETE:gpt55-6