## CONTEXT
READ-ONLY deep-research seat. Do NOT write/edit files — analysis text only. Spec folder ...146-sk-code-ponytail-based-refinement pre-approved (Gate 3); read-only regardless.

NOTE: ponytail source is GITIGNORED (search/glob will NOT find external/ponytail-main). The relevant ponytail benchmark is EMBEDDED below. For sk-code / deep-improvement, read on-disk files normally.

### EMBEDDED PONYTAIL SOURCE — benchmark harness
- Structure (promptfooconfig.yaml): 3 providers (claude-haiku-4-5, claude-sonnet-4-6, claude-opus-4-8); 3 arms (baseline / caveman / ponytail) as prompt files; 5 tests (email validator, JS debounce, CSV sum, React countdown, FastAPI rate-limit); a `defaultTest.assert` of type javascript `file://loc.js` with metric `code_loc` (deterministic LOC counted from fenced code blocks); run `npx promptfoo eval -c promptfooconfig.yaml --repeat 10`, median reported.
- Method (prompts.json): "One fresh Claude Code subagent per task x config, same model, no file outputs. Metrics from task telemetry: total tokens (includes thinking), duration. Code lines counted from fenced blocks."
- Result claim (README): vs baseline, ponytail writes 80-94% less code, costs 47-77% less, runs 3-6x faster. LOC is the deterministic metric; cost/latency from API telemetry. Note: correctness is NOT gated in ponytail's harness (it only measures size/cost/latency).

## OBJECTIVE (iteration 006 of 10)
Angle: "Benchmark / eval harness" — could sk-code adopt an OBJECTIVE over-engineering/simplicity benchmark (does loading sk-code reduce LOC / over-engineering WITHOUT breaking correctness?), modeled on ponytail's PromptFoo arms and/or the repo's deep-improvement sweep benchmark (which GATES correctness)?

## STEPS (read these on-disk files)
- .opencode/skills/sk-code/README.md   (run: `rg -ni benchmark .opencode/skills/sk-code` — does sk-code already cite a benchmark?)
- .opencode/skills/deep-improvement/scripts/skill-benchmark/   (list contents; is there an existing sk-code benchmark + a sweep-benchmark.cjs where correctness is a gate?)
- `rg -l "sweep-benchmark|skill-benchmark|over-engineer|code_loc" .opencode/skills/deep-improvement .opencode/skills/sk-code`

## ANSWER
1. What would a sk-code simplicity benchmark MEASURE (LOC, over-engineering markers, correctness-GATED unlike ponytail's)? Arms = no-skill vs sk-code-loaded. How does ponytail's LOC-from-fenced-blocks + arm structure inform it?
2. Does sk-code / the repo ALREADY have this (deep-improvement skill-benchmark, sweep-benchmark.cjs, sk-code live-final reports)? If yes, what does ponytail's add that's missing — specifically an over-engineering/LOC-reduction dimension with a correctness gate? Be skeptical; do NOT recommend a duplicate harness.
3. WHERE would it live + how invoked (a new arm/metric in deep-improvement's sweep-benchmark.cjs vs a standalone harness)? Name the path.
4. Negative knowledge: benchmark harnesses are heavy + ponytail's omits a correctness gate (dangerous to copy). Is the ROI there, or "nice but skip / fold one LOC metric into the existing sweep"?

## FORMAT
Terse, cite file/section. End with a fenced ```json block:
```json
{"seat":"gpt-3","lens":"benchmark-eval-harness","findings":[{"id":"F1","title":"","ponytail_source":"external/ponytail-main/benchmarks/<file>","target":"sk-code:<file> or deep-improvement:<path>","mechanism":"","recommendation":"","value":"high|medium|low","effort":"S|M|L","integration_risk":"","redundant_with":"","confidence":"high|medium|low"}],"newInfoRatio":0.0,"negative_knowledge":[""]}
```
