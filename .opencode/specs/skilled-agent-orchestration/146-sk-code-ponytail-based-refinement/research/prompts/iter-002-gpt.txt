## CONTEXT
READ-ONLY deep-research analysis seat. Do NOT write/edit any file — produce analysis text only. Spec folder .opencode/specs/skilled-agent-orchestration/146-sk-code-ponytail-based-refinement is pre-approved (Gate 3 satisfied); read-only regardless.

Two skills we want to improve:
- sk-code (.opencode/skills/sk-code): surface-aware code-standards router with one existing hook — a PostToolUse "comment-hygiene" warning (scripts/hooks/claude-posttooluse.sh, fail-safe exit 0) backed by scripts/check-comment-hygiene.sh.
- sk-code-review (.opencode/skills/sk-code-review): findings-first P0/P1/P2 review baseline (no hooks).

Reference project "ponytail" (external/ponytail-main) ships a richer hook system: a hooks.json manifest wiring SessionStart (activate mode, write a .ponytail-active flag file, inject the ruleset as hidden context) and UserPromptSubmit (detect /ponytail commands, persist mode), plus an OpenCode plugin that injects the ruleset into the system prompt every turn.

## OBJECTIVE (iteration 002 of 10)
Angle: "Hook architecture parity" — which ponytail hook points could sk-code / sk-code-review adopt, and how do they map onto our existing hook surface?

## STEPS (read these specific files; do not broad-search)
- external/ponytail-main/hooks/hooks.json
- external/ponytail-main/hooks/ponytail-activate.js
- external/ponytail-main/hooks/ponytail-mode-tracker.js
- external/ponytail-main/hooks/ponytail-runtime.js
- external/ponytail-main/hooks/ponytail-config.js
- external/ponytail-main/.opencode/plugins/ponytail.mjs
- .opencode/skills/sk-code/scripts/hooks/claude-posttooluse.sh
- .opencode/skills/sk-code/scripts/check-comment-hygiene.sh   (skim)
- .opencode/skills/sk-code/references/opencode/shared/hooks.md   (our hook-entrypoint contract: which events are wired in settings.json)

## ANSWER THESE
1. Map each ponytail hook point (SessionStart mode-activate + flag file, UserPromptSubmit rule-injection, per-turn system-prompt injection) to a concrete opportunity in sk-code or sk-code-review. For each: what NEW behavior would it enable (e.g. SessionStart that surfaces the active surface's verification commands; UserPromptSubmit that injects the matched surface's standards; a flag file that remembers "verification intensity")?
2. Which of these is genuinely additive vs already covered by our PostToolUse hygiene hook or the 4 wired Claude entrypoints (user-prompt-submit, compact-inject, session-prime, session-stop) named in references/opencode/shared/hooks.md?
3. Name the exact target file/path each new hook would live in (e.g. .opencode/skills/sk-code/scripts/hooks/<new>.sh + the settings.json wiring it needs).
4. Integration-risk: hooks are runtime-loaded via settings.json, NOT imported — flag the maintenance/portability cost across the 3 runtimes (.opencode/.claude/.codex).

## TONE / FORMAT
Terse, evidence-led, cite file or file#section. Consider hook opportunities NOT listed above (negative space). End your response with a fenced ```json block EXACTLY in this shape:
```json
{
  "seat": "gpt-1",
  "lens": "hook-architecture-parity",
  "findings": [
    {
      "id": "F1",
      "title": "<short>",
      "ponytail_source": "external/ponytail-main/<path>",
      "target": "sk-code:<file> or sk-code-review:<file>",
      "mechanism": "<what ponytail's hook does>",
      "recommendation": "<concrete new hook for our skill + where it wires>",
      "value": "high|medium|low",
      "effort": "S|M|L",
      "integration_risk": "<risk + 3-runtime cost>",
      "redundant_with": "<existing hook or 'none'>",
      "confidence": "high|medium|low"
    }
  ],
  "newInfoRatio": 0.0,
  "negative_knowledge": ["<what NOT to adopt for this angle, and why>"]
}
```
newInfoRatio in [0,1] = fraction of findings genuinely new vs obvious.
