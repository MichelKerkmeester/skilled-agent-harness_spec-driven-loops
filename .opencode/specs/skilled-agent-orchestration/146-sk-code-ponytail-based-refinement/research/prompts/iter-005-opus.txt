ROLE: Senior AI-tooling architect, READ-ONLY deep-research. Do NOT write/edit files — analysis text only. Spec folder ...146-sk-code-ponytail-based-refinement pre-approved (Gate 3); read-only regardless.

CRITICAL OUTPUT RULE: print your COMPLETE narrative AND the full JSON findings block to STDOUT as your final assistant message. Do NOT write to any plan file. Do NOT call ExitPlanMode or AskUserQuestion. Stdout is the ONLY deliverable — anything you place in a plan file or elsewhere is LOST.

ANGLE (iteration 005 of 10): "Rule-invariant validation" — ponytail ships scripts/check-rule-copies.js, a guard that verifies load-bearing rule PHRASES (e.g. "ONE runnable check", "input validation at trust boundaries") appear identically across all its adapter copies (AGENTS.md + .cursor/.windsurf/.clinerules/etc.), failing CI on drift. Could sk-code / sk-code-review adopt a similar canary-phrase guard?

CONTEXT TO TEST: the repo already has (a) a sk-code router-sync drift guard (.opencode/skills/deep-improvement/scripts/skill-benchmark/tests/sk-code-router-sync.vitest.ts) that fails if RESOURCE_MAP paths drift from disk, and (b) a 3-runtime mirror convention where deep-loop agents exist as identical bodies in .opencode/agents/*.md + .claude/*.md + .codex/*.toml. Determine what a canary-phrase guard would add ON TOP of these.

FILES TO READ (specific):
- external/ponytail-main/scripts/check-rule-copies.js   (the invariant list + comparison logic)
- .opencode/skills/sk-code/references/smart_routing.md   (§11 RESOURCE_MAP + drift-guard note ~line 300)
- .opencode/skills/sk-code/SKILL.md   (the Iron Law wording — a candidate invariant)
- .opencode/skills/sk-code-review/SKILL.md   (the final-status-line contract + severity contract — candidate invariants)
- .opencode/skills/sk-code/scripts/check-comment-hygiene.test.sh   (an existing test style to mirror)

ANSWER:
1. WHICH load-bearing phrases in sk-code / sk-code-review are drift-prone and worth guarding (e.g. "No completion without verification evidence"; "Review status: APPROVED|REQUESTED_CHANGES|COMMENTED"; the P0/P1/P2 definitions)? List concrete strings + the files they must stay consistent across.
2. Does a canary guard add value beyond the existing router-sync drift guard (paths) — i.e. it guards WORDING not paths? Where would it live + wire (CI workflow + test file path)?
3. Cross-runtime angle: should the guard also assert the .opencode/.claude/.codex skill mirrors (if any) stay phrase-identical?
4. Negative knowledge: over-guarding wording can block legitimate edits; what should NOT be canary-locked?

OUTPUT: concise evidence-led narrative, then a fenced ```json block:
```json
{"seat":"opus-3","lens":"rule-invariant-validation","findings":[{"id":"F1","title":"","ponytail_source":"external/ponytail-main/<path>","target":"sk-code:<file> or sk-code-review:<file>","mechanism":"","recommendation":"","value":"high|medium|low","effort":"S|M|L","integration_risk":"","redundant_with":"","confidence":"high|medium|low"}],"newInfoRatio":0.0,"negative_knowledge":[""]}
```
