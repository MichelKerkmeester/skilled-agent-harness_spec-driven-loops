Act as a senior technical researcher analyzing an MCP-server integration for a skill-authoring project. READ-ONLY MODE: do NOT write, create, or edit any files. Your final message IS the deliverable — return it as markdown in the exact skeleton at the end.

# Context
We are building an OpenCode / Claude-Code "skill" called `mcp-figma`. PRIMARY tool = `figma-cli` (silships). SECONDARY, OPTIONAL = the official Figma MCP server, invoked through this project's "Code Mode" MCP system. This iteration researches the Figma MCP landscape AND how to wire/call it via Code Mode. It is the analog of the "MCP-via-Code-Mode" half of the existing `mcp-chrome-devtools` skill.

# Known facts (verify, correct, extend)
- The official Figma Dev Mode MCP server has a REMOTE endpoint https://mcp.figma.com/mcp (hosted, no desktop app, broadest features) and a LOCAL desktop endpoint http://127.0.0.1:3845/mcp (runs via the Figma desktop app in Dev Mode).
- Claude Code wiring example: `claude mcp add --transport http figma-desktop http://127.0.0.1:3845/mcp`.
- This project's Code Mode registers external MCP tools in `.utcp_config.json` and calls them via `call_tool_chain()`, with discovery via `search_tools()` / `list_tools()`. Tool naming convention: `{manual_name}.{manual_name}_{tool_name}`.

# LOCAL files to READ for the exact project pattern (use the Read tool; --dir is the repo root)
- .opencode/skills/mcp-code-mode/SKILL.md
- .opencode/skills/mcp-chrome-devtools/SKILL.md  (especially its "MCP Approach (Fallback) — Chrome DevTools via Code Mode" section)
- .opencode/skills/mcp-chrome-devtools/INSTALL_GUIDE.md  (how the MCP half is installed/registered)
- .utcp_config.json  (the ACTUAL registered-manuals shape; how chrome_devtools is registered — copy the real structure)

# Web sources to fetch for depth on the official Figma MCP (note any that fail)
- https://developers.figma.com/docs/figma-mcp-server/
- https://developers.figma.com/docs/figma-mcp-server/local-server-installation/
- https://developers.figma.com/docs/figma-mcp-server/remote-server-installation/

# Questions to answer precisely (with evidence)
1. OFFICIAL FIGMA MCP — remote vs local/desktop: exact endpoints, transport (http / SSE / stdio), authentication (OAuth? token? Dev Mode requirement? paid/Org/Enterprise plan gate?), and the TOOL SURFACE it exposes (e.g. get_code, get_variable_defs, get_image, get_code_connect_map, get_metadata — list what each version actually provides). Which one is the better default for an OPTIONAL Code-Mode path, and why.
2. COMMUNITY ALTERNATIVES: are there notable third-party Figma MCP servers (e.g. Framelink / GLips figma-context-mcp, or others) worth supporting? Brief pros/cons vs the official server. Give a verdict: support none / official-only / official+one community.
3. CODE MODE REGISTRATION: the EXACT shape of a `.utcp_config.json` entry to register the Figma MCP over http transport. Produce a concrete, paste-ready example modeled on how chrome_devtools is actually registered in the real file. Note whether http-transport MCPs are even supported by this Code Mode (vs stdio/command manuals) and how.
4. CODE MODE INVOCATION: the exact `call_tool_chain()` TypeScript pattern to call a Figma MCP tool, using the `{manual}.{manual}_{tool}` naming. Provide a runnable example (e.g. get code + variables for the current selection), including discovery (`search_tools`/`list_tools`) and cleanup if relevant.
5. CLI-vs-MCP ROUTING LINE: when should `mcp-figma` prefer the figma-cli vs the optional MCP? The chrome-devtools skill draws CLI = fast/token-efficient/self-documenting, MCP = multi-tool chaining/parallel/type-safe. State the figma analog crisply (e.g. CLI = create/modify/export in Figma; MCP = pull design context / code / variables FROM Figma into the agent for code generation).
6. GOTCHAS: desktop server needs Figma desktop + Dev Mode + an open file; remote-server auth; port 3845 conflicts; token cost of MCP responses vs CLI; any rate limits or plan gates.

# Output skeleton (return EXACTLY this structure)
## IT2 — Figma MCP landscape + Code Mode integration
### Official Figma MCP (remote vs desktop): endpoints, transport, auth, tool surface
### Community alternatives + verdict
### Code Mode registration (.utcp_config.json entry — concrete paste-ready example)
### Code Mode invocation (call_tool_chain example)
### CLI-vs-MCP routing line for mcp-figma
### Gotchas & constraints
### Confidence + unknowns

For every non-obvious claim, cite the source URL or local file path. If a fetch or read fails, state it explicitly and mark that section accordingly.
