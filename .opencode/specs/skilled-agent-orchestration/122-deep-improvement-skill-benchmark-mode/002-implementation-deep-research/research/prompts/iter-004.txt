[Rendered reference copy of iteration 4's prompt. run_one.sh substitutes <model> + iter at dispatch and writes to research/<model>/iterations/iteration-004.md.]

You are @deep-research, a LEAF research agent doing ONE iteration (iteration 4 of 5) of an autonomous deep-research loop. Fresh context. Working directory is the repo root: /Users/michelkerkmeester/MEGA/Development/Code_Environment/Public

RESEARCH TOPIC: Determine the BEST WAY TO IMPLEMENT the deep-improvement rename and Lane C ("skill-benchmark"). The DESIGN is already converged (see the 001 research.md cited below); THIS loop produces a build-ready IMPLEMENTATION PLAYBOOK.

THIS ITERATION'S FOCUS: IQ6 — scorer + report-builder: concrete computation of D1-D5 (reuse the advisor 5-lane scorer for D1 inter12+intra13; Hit@k/Recall@k/MRR for D2; call/token counts for D3; pluggable-grader ablation for D4; static connectivity scan as the D5 hard-gate); render report.md FROM report.json (anti-drift); bottleneck ranking by funnel attrition (largest single-stage drop-off = headline); the remediation taxonomy mapping finding-class -> (file, locus, one-line fix, handoffLane).

FIXED CONSTRAINT (do not re-litigate): one subdir per lane + shared/. Decide only shared vs Lane-C-specific. Rename scope fixed as NARROW.

GROUND IN THE REPO (read what's relevant; do NOT modify anything):
- .opencode/specs/skilled-agent-orchestration/122-deep-improvement-skill-benchmark-mode/001-skill-benchmark-deep-research/research/research.md (AUTHORITATIVE design: scoring weights, funnel, report schema, remediation taxonomy)
- .opencode/skills/deep-agent-improvement/scripts/model-benchmark/scorer/ (Lane B deterministic checks + grader harness + cache to reuse)
- .opencode/skills/deep-agent-improvement/references/model-benchmark/evaluator_contract.md (Lane B report schema)
- .opencode/skills/system-skill-advisor/ (the 5-lane advisor scorer + advisor_validate corpus for D1)
Also use web search/fetch for prior art (Ragas, retrieval metrics, SARIF, LLM-as-judge rubrics).

CONSTRAINTS: LEAF — no sub-agents. Max 12 tool calls. Cite every finding. Report findings only.

OUTPUT — do BOTH:
(A) Write narrative to research/<model>/iterations/iteration-004.md with headings: Focus | Actions Taken | Findings (each cited) | Recommendations (build-ready) | Open Questions. Write research/<model>/state-parts/iter-004.jsonl with one line: {"type":"iteration","iteration":4,"model":"<model>","newInfoRatio":0.6,"status":"complete","focus":"iter4"} and research/<model>/deltas/iter-004.jsonl with that line plus one {"type":"finding",...} per key finding.
(B) THEN paste the COMPLETE iteration narrative markdown again in your final reply (mandatory).
