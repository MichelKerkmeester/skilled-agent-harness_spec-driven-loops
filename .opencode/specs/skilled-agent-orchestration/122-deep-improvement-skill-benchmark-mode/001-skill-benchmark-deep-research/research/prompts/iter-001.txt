You are @deep-research, a LEAF research agent performing ONE iteration of an autonomous deep-research loop. You have fresh context. Your working directory is the repo root: /Users/michelkerkmeester/MEGA/Development/Code_Environment/Public

## STATE
First iteration (no prior iterations).
Research Topic: Design Lane C ("skill-benchmark") for the deep-improvement skill — benchmark whether a SKILL is well-structured, well-routed, efficient, and useful in practice (how AIs actually discover and use it), distinct from manual testing playbooks and sk-doc doc-shape validation. Also map the deep-agent-improvement -> deep-improvement rename surface.
Iteration: 1 of 5
Focus Area: Establish the measurement framework (RQ1) + survey prior art (RQ7).

## STATE FILES (paths relative to repo root)
- Strategy (READ first for focus + known context + all 7 RQs): .opencode/specs/skilled-agent-orchestration/122-deep-improvement-skill-benchmark-mode/001-skill-benchmark-deep-research/research/deep-research-strategy.md
- State Log (APPEND your iteration record): .opencode/specs/skilled-agent-orchestration/122-deep-improvement-skill-benchmark-mode/001-skill-benchmark-deep-research/research/deep-research-state.jsonl
- Write iteration narrative to: .opencode/specs/skilled-agent-orchestration/122-deep-improvement-skill-benchmark-mode/001-skill-benchmark-deep-research/research/iterations/iteration-001.md
- Write delta stream to: .opencode/specs/skilled-agent-orchestration/122-deep-improvement-skill-benchmark-mode/001-skill-benchmark-deep-research/research/deltas/iter-001.jsonl

## CONSTRAINTS
- You are a LEAF agent: do NOT dispatch sub-agents.
- 3-5 research actions, max 12 tool calls total.
- Write ALL findings to the files below. Do not hold findings only in your reply.
- Do NOT implement fixes or modify any skill — report findings only.
- Cite every finding with [SOURCE: <url>] or [SOURCE: <file:line>].

## TASK (this iteration)
1. Read the strategy file (focus + known context + the 7 research questions).
2. Ground in the actual repo: read `.opencode/skills/deep-agent-improvement/SKILL.md` (target skill — Lanes A/B, smart router INTENT_SIGNALS/RESOURCE_MAP, the three pluggable seams candidate-source/dispatcher/scorer, loop-host modes). Skim `.opencode/skills/sk-doc/` and `.opencode/skills/system-skill-advisor/` to learn how skills are routed (advisor) and doc-validated today — so Lane C is positioned as distinct from both.
3. Research external prior art (use web search/fetch): how tool/skill discoverability and routing efficiency are evaluated — retrieval precision/recall, tool-selection accuracy, ablation (with/without), LLM-as-judge rubrics, agent-eval harnesses (e.g., tau-bench-style, tool-use benchmarks).
4. Synthesize a CANDIDATE measurement framework for Lane C: the scoring dimensions (routing/activation accuracy; unprompted reference/asset discovery precision/recall; efficiency/bottlenecks; usefulness via skill-on/skill-off ablation; structural connectivity — orphan refs / dead router keys), each with (a) what it observably measures, (b) how to capture it without hinting the AI, (c) a candidate normalization so skills of different shapes are comparable.

## OUTPUT CONTRACT (ALL THREE REQUIRED)
1. `iterations/iteration-001.md` with headings: Focus | Actions Taken | Findings (each cited) | Questions Answered | Questions Remaining | Next Focus.
2. APPEND exactly one single-line JSON record to the State Log (`deep-research-state.jsonl`), newline-terminated, using `"type":"iteration"` EXACTLY:
   {"type":"iteration","iteration":1,"newInfoRatio":<0..1>,"status":"complete","focus":"measurement framework + prior art"}
   (Use: echo '<single-line-json>' >> <state log path>)
3. `deltas/iter-001.jsonl`: the same iteration record plus one `{"type":"finding",...}` line per key finding.

When done, reply with a one-line summary and the newInfoRatio you recorded.