SKILL ROUTING: User directed → `deep-research`. I’m loading the workflow contract now and will keep all generated lineage artifacts confined to the requested `gpt55fast` directory.The skill contract confirms this must follow the deep-research loop state model: read state first, write iteration markdown plus JSONL deltas, check convergence each iteration, then synthesize. I’ll inspect only the requested lineage directory state before writing.The lineage directory has no existing loop state besides an empty `.executor-state`, so this is a fresh lineage. I’m reading the command contract and packet context needed to initialize the lineage correctly without writing outside the lineage directory.Because this is already a `cli-opencode` fan-out lineage, I won’t recursively invoke `opencode run`; I’ll execute the lineage directly and produce the workflow artifacts the parent process expects. Next I’m reading the packet’s research objective so the strategy and iterations are evidence-based.The packet objective is clear: produce one gpt-5.5-fast lineage for Claude Design parity across `sk-interface-design` and `mcp-magicpath`; no implementation changes. I found no existing parent `resource-map.md`, so the lineage config will record `resource_map_present: false`.I have enough evidence for the five research focuses: Claude Design baseline, `sk-interface-design`, `mcp-magicpath`, parity recommendations, and convergence synthesis. I’m creating the lineage packet files and directories now, all under the requested `gpt55fast` artifact directory.Lineage artifacts are written. I’m running a read-only artifact verification now: JSON/JSONL parse checks, required file presence, and iteration/delta count consistency.Artifact verification passed for all five iterations and synthesis state. I’m running one final whitespace check on the lineage diff before returning the required completion marker.FANOUT_LINEAGE_COMPLETE:gpt55fast