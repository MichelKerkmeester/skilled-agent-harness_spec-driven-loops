# Reference Map — sk-prompt-small-model → sk-prompt-models

Inventory deliverable for the rename. Counts captured 2026-06-28.

## Totals
- Files containing `sk-prompt-small-model`: **826**
- Occurrences: **4,596**
- Files containing the older legacy `sk-small-model`: 124 (separate name — NOT renamed)

## Classification policy

### TEXT-REPLACE (the bulk)
Every file containing `sk-prompt-small-model` **except** the exclusions below gets a literal token replace `sk-prompt-small-model` → `sk-prompt-models`. The token is unique, so one swap covers absolute paths, relative back-links, `name:`/`skill_id`, prose, and config.

Replace set = `rg -l sk-prompt-small-model` MINUS the exclusions. Command (run AFTER `git mv`):
```
comm -23 <(rg -l 'sk-prompt-small-model' | sort) <(rg -l 'sk-small-model' | sort) \
  | grep -v '/158-sk-prompt-models-rename/' \
  | grep -v 'system-skill-advisor/mcp_server/database/skill-graph.json' \
  | grep -v 'system-skill-advisor/mcp_server/scripts/skill-graph.json' \
  | xargs perl -pi -e 's/sk-prompt-small-model/sk-prompt-models/g'
```

### GIT-MV
`git mv .opencode/skills/sk-prompt-small-model .opencode/skills/sk-prompt-models` (history preserved; untracked new files — `references/models/glm-5.2.md`, `benchmarks/008-*` — move with it).

### REGENERATE (never string-edit)
- `system-skill-advisor/mcp_server/{database,scripts}/skill-graph.json` (compiled index) + `skill-graph.sqlite` → advisor `--force-refresh` + `skill_graph_compiler.py`.
- `.opencode/specs/descriptions.json` (generated specs index) → regenerated by spec tooling / memory scan.
- spec-memory `context-index.sqlite` + vectors → `memory_index_scan`.
- The renamed skill's `description.json` / `graph-metadata.json` derived blocks → regenerated.
- `*.sqlite` everywhere — auto-excluded (rg skips binary; not in the replace set).

### HISTORY-CARE / FROZEN (excluded from TEXT-REPLACE)
**Rule: any file that ALSO contains `sk-small-model` is historical (documents the earlier rename) — leave it frozen so its `sk-small-model → sk-prompt-small-model` narrative stays true.** 51 files, incl:
- `sk-prompt-small-model/changelog/v0.3.0.0.md` (THE earlier-rename changelog), `v0.1.0.0.md`, `v0.2.0.0.md`
- `deep-loop-workflows/deep-ai-council/changelog/v1.2.0.0.md`
- `system-spec-kit/references/workflows/rename_pattern.md` (uses the rename as a worked example)
- `z_archive/114-small-ai-model-optimization/**` (the entire archived original-rename packet) + `z_archive/116-.../115-arc-review/**`
- `.opencode/specs/descriptions.json` (also REGENERATE)
- The **158 packet** (27 files) — self-referential (names the rename subject).

A NEW changelog entry under the renamed skill documents THIS rename (sk-prompt-small-model → sk-prompt-models) and forward-points from the frozen history.

## High-risk hardcoded references to verify individually (must not be missed)
- `system-skill-advisor/mcp_server/scripts/check-prompt-quality-card-sync.sh` — `H = f"{ROOT}/.opencode/skills/sk-prompt-small-model"` (the card-sync gate).
- `commands/deep/assets/deep_model-benchmark_{auto,confirm}.yaml` — benchmark_root/report/mkdir/outputs-dir/promote paths.
- `commands/deep/assets/deep_context_{auto,confirm}.yaml` — `prompt_framing` + loaders.
- `deep-loop-workflows/.../benchmark-profiles/reviewer-regression.json` — `outputsDir`.
- `system-spec-kit/mcp_server/tests/secret-scrubber.vitest.ts` — fixture string (passes through the replace).
- `deep-loop-runtime/lib/deep-loop/executor-config.ts` — prose comment.
- `sk-prompt-models/assets/model_profiles.json` — 5 `profile_ref` strings.
- `sk-prompt-models/references/models/*.md`, `_index.md`, `pattern_index.md` — `../../../sk-prompt-small-model/...` back-links.

## Done-criteria reconciliation
After the rename, `rg -c sk-prompt-small-model` will be **non-zero only in the frozen HISTORY-CARE set + the 158 packet**. That residue is intentional and enumerated here; zero live-wiring references remain.
