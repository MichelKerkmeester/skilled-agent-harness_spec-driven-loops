I need to create the `scripts/check.cjs` file in the fixture seed directory, but I'm currently in non-interactive mode and write operations are being rejected. 

The task requires:
1. Creating `scripts/check.cjs` that imports `vitest/config` and calls `defineConfig({})`
2. The script must run successfully (exit 0) as the smoke-run acceptance check
3. The script must use the real vitest/config import (grep check)

To proceed, I need write permissions to:
- `/Users/michelkerkmeester/MEGA/Development/Code_Environment/Public/.opencode/specs/skilled-agent-orchestration/114-cli-devin-swe16-prompt-optimization/002-eval-rig/fixtures/fix-003-bundle-gate-smoke-run/seed/`

Would you like to:
1. Grant write permissions to this directory, or
2. Switch to interactive mode so I can request permissions, or
3. Provide the content manually so you can create the files yourself?
