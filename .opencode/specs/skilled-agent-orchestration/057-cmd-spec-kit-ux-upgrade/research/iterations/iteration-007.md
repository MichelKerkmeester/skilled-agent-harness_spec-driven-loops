# Focus

Ranking pass: order the SPAR-Kit adoption findings by impact x adoption-cost, assign follow-on packet names in the `058-*` through `06X-*` range, and verify every ranked finding cites both the external SPAR surface and the internal system-spec-kit surface.

# Actions Taken

1. Read the active strategy, state-log tail, and findings registry. The registry is still empty, so this pass treated iteration narratives 1-6 and the state-log graph events as the source of truth.
2. Read `iteration-001.md` through `iteration-006.md` to collect installer, instruction-file, command/skill granularity, template architecture, tool-discovery, and persona/tone findings.
3. Checked for existing follow-on packet folders in `.opencode/specs/skilled-agent-orchestration/` matching `058-*` through `06X-*`; no collisions were found.
4. Ran a compatibility scan for `:auto`, `:confirm`, `:with-phases`, `:with-research`, `skill-advisor`, and Gate 3 references. The scan was broad and noisy, but it confirmed the command-mode surface is spread across `.opencode/command`, `.opencode/skill/system-spec-kit`, and this packet's spec docs, so Q2 still needs a dedicated matrix pass.

# Findings

Ranking scale: impact is the expected UX/orchestration value; adoption cost is implementation and compatibility risk. The final ordering prioritizes high impact with low-to-medium cost. Higher-cost items can still rank high when they unlock multiple axes.

| Rank | Prior IDs | Verdict | Impact | Cost | Follow-on packet | Finding |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | F-014, iter-5 #5 | `adapt` | High | Low | `058-gate-copy-and-question-budget` | Adopt SPAR's blocker vs optional question split as `Required To Proceed` and `Optional Refinements`. External evidence: SPAR `spar-plan` recommendations and `spar-specify`/`spar-plan` skill prompts separate key follow-ups from optional exploration. Internal evidence: `AGENTS.md` already requires consolidated questions, and `.opencode/command/spec_kit/assets/spec_kit_complete_auto.yaml` already caps clarification at high-impact items. This is the fastest win because it improves Gate 3 and planning UX without changing command semantics. |
| 2 | F-003, F-012 | `adapt` | High | Low-Medium | `059-phase-boundary-copy-pass` | Add SPAR-style phase-boundary language to internal command docs. External evidence: `external/install-root/skills/spar-specify/SKILL.md`, `spar-plan/SKILL.md`, `spar-act/SKILL.md`, and `spar-retain/SKILL.md` clearly state what each phase should and should not do. Internal evidence: `.opencode/command/spec_kit/plan.md`, `implement.md`, `complete.md`, and `.opencode/skill/system-spec-kit/SKILL.md` contain stronger machinery but less immediate phase framing. This should be copy and guardrail placement, not behavior change. |
| 3 | F-002, F-012 | `adapt` | High | Medium | `060-command-surface-matrix` | Use Specify -> Plan -> Act -> Retain as navigation copy over the existing command set, not as a command collapse. External evidence: SPAR's root docs and phase skills expose a compact lifecycle. Internal evidence: `.opencode/command/spec_kit/`, `.opencode/command/memory/`, `.opencode/command/create/`, and `.opencode/specs/.../057-cmd-spec-kit-ux-upgrade/spec.md` show asymmetric modes and suffixes. Q2 remains open until this packet builds the real mode x scope x lifecycle matrix. |
| 4 | F-004, F-006, F-011 | `adapt` | Very High | Medium-High | `061-declarative-ownership-manifest` | Build a declarative ownership manifest using SPAR's policy vocabulary. External evidence: `external/install-root/targets/*.json` and `external/lib/repo-bootstrap.mjs` use `managed_block`, `seed_if_missing`, `replace`, and `replace_managed_children` to make ownership explicit. Internal evidence: `AGENTS.md`, `.codex/AGENTS.md`, `.opencode/skill/system-spec-kit/templates/`, `.opencode/command/`, and `.opencode/agent/` are cross-runtime surfaces without one manifest of ownership. This unlocks instruction sync, template policy, and runtime placement, but it needs careful design. |
| 5 | F-007, F-009, F-013 | `adapt` | High | Medium | `062-tool-discovery-ledger` | Add an operator-visible tool ledger/dashboard that complements live routing. External evidence: SPAR seeds `.spar-kit/.local/tools.yaml` and `spar-init` updates installed/version/reason state after checking local tools. Internal evidence: `.opencode/skill/system-spec-kit/references/hooks/skill-advisor-hook.md`, `.opencode/skill/system-spec-kit/mcp_server/tool-schemas.ts`, `.opencode/skill/mcp-code-mode/SKILL.md`, and `.opencode/command/doctor/skill-advisor.md` provide strong routing and health systems but no single local inventory. The ledger must be diagnostic, not authoritative. |
| 6 | F-003(iter-3), F-004(iter-3), F-006(iter-3), F-015 | `adapt-narrowly` | Medium-High | Medium | `063-template-inventory-and-manifest` | Inventory templates by source input, generated output, validation-critical consumer artifact, example, and optional asset. External evidence: SPAR has two authored templates plus package prep that syncs optional skill assets only where present. Internal evidence: `.opencode/skill/system-spec-kit/templates/`, `scripts/templates/compose.sh`, and validation references already support a richer CORE + ADDENDUM architecture. The adoption is source-layer clarity, not consumer-layer deletion. |
| 7 | F-001 | `adapt` | Medium-High | Medium-High | `064-runtime-target-manifest` | Prototype runtime target manifests for `.opencode`, `.claude`, `.codex`, and `.gemini` placement before any installer work. External evidence: SPAR's `external/lib/cli.mjs`, `external/lib/install-engine.mjs`, and `external/install-root/targets/{default,codex,opencode}.json` show a compact multi-target install model. Internal evidence: `.opencode/agent/README.txt`, `.opencode/skill/system-spec-kit/ARCHITECTURE.md`, hook docs, and runtime agent directories show heavier cross-runtime reality. This ranks below the ownership manifest because install mechanics should follow ownership semantics. |
| 8 | iter-5 #1, #2, #7, F-014 | `adapt` | Medium | Low-Medium | `065-persona-evaluation-fixtures` | Use SPAR personas as evaluation fixtures and audience maps, not runtime identities. External evidence: `external/Research/Personas/Personas.md` includes Vera, Pete, Tess, Maya, Max, and Consultant Cass. Internal evidence: `.codex/AGENTS.md`, `AGENTS.md`, `.opencode/agent/orchestrate.md`, `.opencode/agent/improve-prompt.md`, and `.opencode/agent/ultra-think.md` are voice, role, and reasoning-lens driven. The low-cost move is to test command UX against these audiences. |
| 9 | F-008, F-010 | `reject-with-rationale` plus `take-inspiration` | Medium | Low | `066-tool-discovery-authority-boundary` | Reject static `tools.yaml` as the source of truth for MCP discovery while preserving the agent-routing vs operator-inventory distinction. External evidence: SPAR's `tools.yaml` tracks a small CLI-focused prerequisite list. Internal evidence: `tool-schemas.ts`, Code Mode discovery docs, and skill-advisor hooks are dynamic and broader than a static registry. This packet should document authority boundaries so the ledger from `062-*` cannot drift into false authority. |
| 10 | F-005, F-015 | `reject-with-rationale` | Medium | Low | `067-generated-block-budget` | Reject a hard 60-line cap for full instruction files; adapt it only as a generated-block budget. External evidence: SPAR's shipped `install-root/AGENTS.md` and `install-root/CLAUDE.md` are tiny boot pointers. Internal evidence: Public `AGENTS.md`, `.codex/AGENTS.md`, and repo-level instruction files carry hard gates, memory save rules, validation, code-search routing, and runtime policy. The adoption should warn when generated managed blocks exceed a scan-friendly budget. |
| 11 | iter-5 #6 | `reject-with-rationale` | Medium | Low | `068-runtime-persona-boundary` | Reject named SPAR personas inside `/spec_kit:*` runtime prompts. External evidence: SPAR's persona-backed tone is useful for product-design posture. Internal evidence: `.codex/AGENTS.md` defines the senior-engineer voice, while `.opencode/agent/*.md` define role contracts. Injecting Vera/Pete/Tess/Maya/Max/Cass into execution prompts would add a competing identity layer. Keep personas in evaluation and design docs. |
| 12 | F-005(iter-3), F-015 | `reject-with-rationale` plus `adapt-narrowly` | Medium | Medium | `069-template-compression-boundary` | Reject SPAR's two-template architecture as a consumer-facing target; adapt source-layer compression only. External evidence: `external/templates/spec.md` and `external/templates/plan.md` are viable because SPAR's artifact contract is intentionally small. Internal evidence: system-spec-kit validators and templates require `spec.md`, `plan.md`, `tasks.md`, `implementation-summary.md`, `checklist.md`, `decision-record.md`, phase-parent docs, and resume/memory anchors. The packet should define what can be compressed behind the composer without removing required artifacts. |

Coverage check: each ranked finding cites at least one external SPAR surface and at least one internal system-spec-kit surface. The only unresolved source gap remains the concrete `fs-enterprises` instruction-root path from Q1; the Public and Barter evidence is enough for ranking, but final Q1 closure still needs that path.

# Questions Answered

- Q8 is answered for ranking: the strongest evidence-backed adoption sequence is question budget, phase-boundary copy, lifecycle navigation, declarative ownership, tool ledger, template inventory, runtime target manifest, and persona fixtures.
- Q6 is refined: npm install itself should not lead. Runtime target manifest work belongs after the declarative ownership manifest.
- Q5 is refined: SPAR's local ledger helps operator inventory, but live skill-advisor and MCP schemas remain authoritative for routing and capability discovery.
- Q4 and Q7 are refined: personas belong in evaluation; question-budget discipline belongs in runtime intake copy.

# Questions Remaining

- Q1 still needs the real `fs-enterprises` instruction root before the managed-block subset can be validated against the full triad.
- Q2 still needs a dedicated command-mode matrix. The broad compatibility scan confirms the surface is large and noisy; it does not prove that a 2-axis model is safe.
- Q3 still needs a file-by-file template classification before manifest implementation.
- Q5 still needs an ownership decision for the future ledger: `/doctor:tools`, install/init, or explicit-save-only.

# Next Focus

Iteration 8 should build the Q2 compatibility matrix from actual command docs and YAML assets: `/spec_kit`, `/create`, `/doctor`, `/improve`, and `/memory`; suffixes `:auto`, `:confirm`, `:with-phases`, `:with-research`; plus skill-advisor and Gate 3 touchpoints. The goal is to decide whether the ranking's `060-command-surface-matrix` can safely model mode x scope x lifecycle or needs a different taxonomy.
