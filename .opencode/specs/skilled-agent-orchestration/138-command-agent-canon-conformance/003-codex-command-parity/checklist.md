---
title: "Verification Checklist: Codex command parity — sync-prompts.cjs + thin router-prompts"
description: "Verifier contract for the Codex command parity phase: generator structural mirror, 37/37 prompt generation, --check GREEN, fail-closed drift, thin-router (no body duplication), repo-tracking, and the deferred ~/.codex install."
trigger_phrases:
  - "codex command parity checklist"
  - "sync-prompts verification"
  - "codex prompt drift gate check"
importance_tier: "important"
contextType: "verification"
_memory:
  continuity:
    packet_pointer: "skilled-agent-orchestration/138-command-agent-canon-conformance/003-codex-command-parity"
    last_updated_at: "2026-07-14T19:20:00Z"
    last_updated_by: "claude"
    recent_action: "Verified 37/37 --check GREEN; thin-router audited; committed c1771fbbf3"
    next_safe_action: "Orchestrator runs validate.sh --strict on this child"
---
# Verification Checklist: Codex command parity — sync-prompts.cjs + thin router-prompts

<!-- SPECKIT_LEVEL: 2 -->
<!-- SPECKIT_TEMPLATE_SOURCE: checklist | v2.2 -->

---

<!-- ANCHOR:protocol -->
## Verification Protocol

| Priority | Handling | Completion Impact |
|----------|----------|-------------------|
| **P0** | HARD BLOCKER | Cannot claim done until complete |
| **P1** | Required | Must complete OR get user approval |
| **P2** | Optional | Can defer with documented reason |

<!-- /ANCHOR:protocol -->
---

<!-- ANCHOR:pre-impl -->
## Pre-Implementation

- [x] CHK-001 [P0] Premise corrected before building; evidence: `spec.md` §2 records that Codex commands are markdown prompts (invoked `/<name>`, `$ARGUMENTS`), not TOML — TOML is the Codex agent format owned by phase 002.
- [x] CHK-002 [P0] Mirror precedent identified; evidence: `.opencode/skills/system-spec-kit/scripts/codex/sync-agents.cjs` exists and supplies the structural shape (`REPO_ROOT` via `__dirname`, `--check` drift mode) that `sync-prompts.cjs` mirrors.
- [x] CHK-003 [P1] Source set + exclusions defined; evidence: `sync-prompts.cjs` `EXCLUDED_DIRECTORIES = {assets, scripts, fixtures}` and the `README.md` / `*.contract.md` skips resolve `.opencode/commands/**/*.md` to 37 files.

<!-- /ANCHOR:pre-impl -->
---

<!-- ANCHOR:code-quality -->
## Code Quality

- [x] CHK-010 [P0] `sync-prompts.cjs` is a structural mirror of `sync-agents.cjs`; evidence: `REPO_ROOT = path.resolve(__dirname, '../../../../..')`, requires only `node:fs` / `node:path`, and exposes a `--check` drift mode — matching the agent-sync shape.
- [x] CHK-011 [P0] Generator produces thin routers, not body copies; evidence: `renderPrompt()` emits a generated-by header, a pointer to `.opencode/commands/<relativePath>`, and a `$ARGUMENTS` passthrough — no command body is read or inlined.
- [x] CHK-012 [P1] Flat-hyphenated naming matches convention; evidence: `toFlatName()` maps `design/motion.md` → `design-motion.md`, matching the existing `~/.codex/prompts/` flat convention and the repo's hyphen-naming direction.
- [x] CHK-013 [P1] Comment hygiene preserved; evidence: `sync-prompts.cjs` carries no ephemeral spec/packet/REQ/task ids in code comments — only the durable `Generated by sync-prompts.cjs … do not edit by hand.` header text.

<!-- /ANCHOR:code-quality -->
---

<!-- ANCHOR:testing -->
## Testing

- [x] CHK-020 [P0] `--check` returns GREEN when in sync; evidence: `node .opencode/skills/system-spec-kit/scripts/codex/sync-prompts.cjs --check` printed `[codex-prompt-sync] PASS: 37 prompts are in sync.` and exited 0.
- [x] CHK-021 [P0] Fail-closed on drift; evidence: `checkOutputs()` pushes `MISSING`/`STALE`/`EXTRA` and, when `drift.length > 0`, `main()` sets `process.exitCode = 1`; the implementing session's hand-edit reproduced `STALE` + exit 1.
- [x] CHK-022 [P1] Thin-router forwarding verified on a sample; evidence: `.codex/prompts/design-motion.md` references `.opencode/commands/design/motion.md` as the single source of truth and ends with `User request: $ARGUMENTS`, with no command body inlined.
- [x] CHK-023 [P1] Count parity confirmed; evidence: `.codex/prompts/*.md` = 37 files, equal to the in-scope command count reported by `--check`.

<!-- /ANCHOR:testing -->
---

<!-- ANCHOR:fix-completeness -->
## Fix Completeness

- [x] CHK-FIX-001 [P0] The deliverable matches the stated finding class (durable, gated cross-runtime parity); evidence: the derived tree is repo-tracked and a `--check` gate protects it, mirroring the agent-sync contract.
- [x] CHK-FIX-002 [P0] Producer inventory complete; evidence: the generator walks the whole `.opencode/commands/**` tree recursively (`listCommandFiles()`), so every in-scope command produces exactly one prompt.
- [x] CHK-FIX-003 [P1] Extra/orphan coverage exists; evidence: `--check` reports `EXTRA` for any `.codex/prompts/*.md` without a matching source, and default `writeOutputs()` unlinks extras — the tree cannot silently accrete orphans.
- [x] CHK-FIX-004 [P1] Evidence pinned to this session's commands and diff; evidence: `implementation-summary.md` records the `--check` output, the sampled prompt, and commit `c1771fbbf3`.

<!-- /ANCHOR:fix-completeness -->
---

<!-- ANCHOR:security -->
## Security

- [x] CHK-030 [P0] No secrets added; evidence: `sync-prompts.cjs` and the `.codex/prompts/` tree contain only generated routing text and canonical paths — no credentials or tokens.
- [x] CHK-031 [P1] No unsafe home-dir mutation performed; evidence: the `~/.codex/prompts/` install and `create` symlink repair are DEFERRED (ADR-002); nothing outside the repo worktree was written.
- [x] CHK-032 [P2] Generator path handling is contained; evidence: source/output roots are derived from `__dirname` and confined to `.opencode/commands` and `.codex/prompts`; only `.md` files are read/written.

<!-- /ANCHOR:security -->
---

<!-- ANCHOR:docs -->
## Documentation

- [x] CHK-040 [P1] Spec, plan, tasks, checklist, decision record, and implementation summary are synchronized; evidence: all six docs under `003-codex-command-parity/` describe the same commit `c1771fbbf3` in-repo-done + install-deferred state.
- [x] CHK-041 [P1] The deferred install is documented, not silently dropped; evidence: `spec.md` §7 Open Questions and `decision-record.md` ADR-002 record the `~/.codex/prompts/` install + symlink repair as operator-gated.
- [x] CHK-042 [P2] The broken symlink is characterized precisely; evidence: `~/.codex/prompts/create -> ../../.opencode/command/create` (singular "command", target absent) is recorded verbatim in `decision-record.md` ADR-002.

<!-- /ANCHOR:docs -->
---

<!-- ANCHOR:file-org -->
## File Organization

- [x] CHK-050 [P1] New files live in the correct trees; evidence: generator at `.opencode/skills/system-spec-kit/scripts/codex/sync-prompts.cjs`; prompts under `.codex/prompts/`.
- [x] CHK-051 [P1] No stray files in the main tree; evidence: all work is in the worktree `.worktrees/0041-skilled-command-agent-canon`; commit `c1771fbbf3` scopes the generator + prompts.
- [x] CHK-052 [P2] Derived tree is regenerable, not hand-authored; evidence: every prompt carries the `Generated by sync-prompts.cjs … do not edit by hand.` header and `--check` re-derives them.

<!-- /ANCHOR:file-org -->
---

<!-- ANCHOR:summary -->
## Verification Summary

| Category | Total | Verified |
|----------|-------|----------|
| P0 Items | 8 | 8/8 |
| P1 Items | 10 | 10/10 |
| P2/N/A Items | 4 | 4/4 |

**Verification Date**: 2026-07-14

<!-- /ANCHOR:summary -->
