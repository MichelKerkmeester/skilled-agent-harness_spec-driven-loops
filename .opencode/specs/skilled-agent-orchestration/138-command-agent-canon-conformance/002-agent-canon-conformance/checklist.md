---
title: "Verification Checklist: agent create-agent canon conformance + Codex TOML parity gate"
description: "Verifier contract for the 26 agent .md create-agent gate, the sanctioned ## 0. numbering warning, behavior preservation, and the restored Codex agent-TOML --check parity gate."
trigger_phrases:
  - "agent canon conformance checklist"
  - "codex toml parity verification"
  - "create-agent validator checklist"
importance_tier: "important"
contextType: "verification"
_memory:
  continuity:
    packet_pointer: "skilled-agent-orchestration/138-command-agent-canon-conformance/002-agent-canon-conformance"
    last_updated_at: "2026-07-14T19:30:00Z"
    last_updated_by: "claude"
    recent_action: "All checklist items verified with evidence; awaiting orchestrator validate.sh --strict"
    next_safe_action: "Orchestrator runs validate.sh --strict on this child"
    blockers: []
    key_files:
      - ".opencode/agents/*.md"
      - ".claude/agents/*.md"
      - ".codex/agents/*.toml"
      - ".opencode/skills/system-spec-kit/scripts/codex/sync-agents.cjs"
---
<!-- SPECKIT_TEMPLATE_SOURCE: checklist | v2.2 -->
# Verification Checklist: agent create-agent canon conformance + Codex TOML parity gate

<!-- SPECKIT_LEVEL: 2 -->

---

<!-- ANCHOR:protocol -->
## Verification Protocol

| Priority | Handling | Completion Impact |
|----------|----------|-------------------|
| **P0** | HARD BLOCKER | Cannot claim done until complete |
| **P1** | Required | Must complete OR get user approval |
| **P2** | Optional | Can defer with documented reason |
<!-- /ANCHOR:protocol -->

---

<!-- ANCHOR:pre-impl -->
## Pre-Implementation

- [x] CHK-001 [P0] Create-agent gate and scope documented in `spec.md`; evidence: `spec.md` sections 2-5 define the validator gate, the `## 0.` interaction, and the Codex parity scope.
- [x] CHK-002 [P0] Approach and phases defined in `plan.md`; evidence: `plan.md` section 4 defines Assess → Resync → Verify with the frozen-`.md` constraint.
- [x] CHK-003 [P1] Gate tooling identified and available; evidence: `plan.md` section 6 lists `validate_document.py --type agent` and `sync-agents.cjs --check`.
<!-- /ANCHOR:pre-impl -->

---

<!-- ANCHOR:code-quality -->
## Code Quality

- [x] CHK-010 [P0] Generated TOMLs are deterministic from source and produced only by the sync tool; evidence: the three changed `.codex/agents/*.toml` were regenerated by the worktree's `sync-agents.cjs`, not hand-edited.
- [x] CHK-011 [P0] Regenerated TOMLs parse and the parity gate accepts them; evidence: `node .opencode/skills/system-spec-kit/scripts/codex/sync-agents.cjs --check` prints "PASS: 13 agents are in sync." and exits 0.
- [x] CHK-012 [P1] No agent `.md` edited — parity resync only; evidence: `git show --stat e893d9adde` lists only `.codex/agents/{ai-council,deep-alignment,markdown}.toml` (3 files, +9/-4); no `.md` in the diff.
- [x] CHK-013 [P1] Changes follow project patterns and comment hygiene; evidence: `git show --stat e893d9adde` lists only `.codex/agents/*.toml` (generated content); no code comments authored.
<!-- /ANCHOR:code-quality -->

---

<!-- ANCHOR:testing -->
## Testing

- [x] CHK-020 [P0] Every agent `.md` in both runtimes exits 0 on the create-agent gate; evidence: `validate_document.py --type agent` sweep over 26 files returned 26/26 exit 0; each has `## 1. CORE WORKFLOW`.
- [x] CHK-021 [P0] Codex agent-TOML parity gate is GREEN; evidence: `sync-agents.cjs --check` exit 0, "PASS: 13 agents are in sync." (baseline was RED with 4 stale TOMLs before regen).
- [x] CHK-022 [P1] The sanctioned `## 0.` numbering warning is characterized, not silenced; evidence: 22/26 files emit exactly one non-blocking `non_sequential_numbering` warning ("expected 1, found 0") from the canon-required `## 0.` hard block; renumbering was deliberately NOT done.
- [x] CHK-023 [P1] The two exempt agents' 0/0 result is explained; evidence: `deep-improvement` and `prompt-improver` lack `## 0.` (4/26 files at 0 issues) — a pre-existing conformance nuance, left as-is at the exit-0 bar.
<!-- /ANCHOR:testing -->

---

<!-- ANCHOR:fix-completeness -->
## Fix Completeness

- [x] CHK-FIX-001 [P0] Each surface has a finding class: agent `.md` = `verify-only` (confirmed conformant); `.codex/agents/*.toml` = `regenerate` (parity drift); evidence: implementation summary lists the surfaces and classes.
- [x] CHK-FIX-002 [P0] Full producer inventory completed; evidence: all 13 agents enumerated across all three runtimes (13 opencode `.md` + 13 claude `.md` + 13 codex `.toml`).
- [x] CHK-FIX-003 [P0] Consumer inventory completed for the changed generated surface; evidence: `.codex/agents/*.toml` is consumed only by the Codex runtime and produced only by `sync-agents.cjs`; the `--check` gate is the sole drift consumer.
- [x] CHK-FIX-004 [P1] Fail-closed coverage confirmed; evidence: `sync-agents.cjs --check` is a fail-closed gate — it exited 1 (RED) on drift before regen and exits 0 (GREEN) only at full parity.
- [x] CHK-FIX-005 [P1] Evidence pinned to explicit diff and command outputs; evidence: commit `e893d9adde` stat and the two `--check` runs (RED→GREEN) are recorded in `implementation-summary.md`.
<!-- /ANCHOR:fix-completeness -->

---

<!-- ANCHOR:security -->
## Security

- [x] CHK-030 [P0] No secrets added; evidence: `git show --stat e893d9adde` = 3 `.codex/agents/*.toml` (+9/-4), generated routing content only, no credentials.
- [x] CHK-031 [P1] No permission/behavior contract weakened; evidence: no agent `.md` frontmatter (`permission:`/`tools:`) changed — the `.md` surface is frozen and the TOMLs are regenerated from it.
<!-- /ANCHOR:security -->

---

<!-- ANCHOR:docs -->
## Documentation

- [x] CHK-040 [P1] Spec, plan, tasks, checklist, decision record, and implementation summary are synchronized; evidence: `spec.md`, `plan.md`, `tasks.md`, `checklist.md`, `decision-record.md`, `implementation-summary.md` all authored with consistent completion state.
- [x] CHK-041 [P1] The validator numbering-gap and `## 0.` backfill nuance are recorded as ADRs; evidence: `decision-record.md` ADR-001 (validator gap, deferred to operator) and ADR-002 (`## 0.` backfill open question).
- [x] CHK-042 [P2] Out-of-canon surface flagged, not force-fit; evidence: root `AGENTS.md` is noted in `spec.md` section 7 and `decision-record.md` as outside create-agent canon.
<!-- /ANCHOR:docs -->

---

<!-- ANCHOR:file-org -->
## File Organization

- [x] CHK-050 [P1] Child doc set is complete and correctly located; evidence: `spec.md`, `plan.md`, `tasks.md`, `checklist.md`, `implementation-summary.md`, `decision-record.md` under `002-agent-canon-conformance/`.
- [x] CHK-051 [P1] No stray or orphaned files introduced; evidence: `ls 002-agent-canon-conformance/` shows exactly the six spec-docs; the three regenerated files live in `.codex/agents/`; no temp artifacts.
<!-- /ANCHOR:file-org -->

---

<!-- ANCHOR:summary -->
## Verification Summary

| Category | Total | Verified |
|----------|-------|----------|
| P0 Items | 9 | 9/9 |
| P1 Items | 10 | 10/10 |
| P2 Items | 1 | 1/1 |

**Verification Date**: 2026-07-14
<!-- /ANCHOR:summary -->
