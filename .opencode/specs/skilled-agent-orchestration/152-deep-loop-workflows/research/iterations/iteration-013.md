# Deep-Research Iteration 013: Migration sequencing and phase decomposition

> Engine: openai/gpt-5.5 --variant xhigh (read-only seat) · orchestrator-written.

Spec memory trigger lookup failed with `MCP error -32000: Connection closed`; I’ll use the available local docs and skill routing, keeping this iteration read-only.
SKILL ROUTING: `deep-loop-runtime` recommended at confidence `0.8448`; I’m loading it and then reading the requested source documents before synthesizing.
I’m reading the verified Context Report first as requested, then I’ll cross-check the command/agent/runtime specs that constrain phase sequencing.
Context Report read in full. I’m checking the two precedent specs for parity and Lane D constraints before deciding the phase graph.
The runtime rules confirm the backend-extension tripwire: adding merged workflow consumers to runtime support requires a new ownership ADR, and stdout JSON/MCP-free invariants must not change. I’m checking the target spec folder shape so phase names don’t collide with existing children.
The target packet currently has only `context/` and `research/`; proposed implementation phase children can start at `001-*` without colliding.
One historical naming conflict is visible: the Lane D precedent spec names `/deep:start-packaging-refine-loop`, while the verified Context Report and the prompt scope use `/deep:start-non-dev-ai-system-loop`; I’ll treat the Context Report as current for command naming and use the Lane D spec for adapter constraints.
Current command files confirm the current Lane D command is `start-non-dev-ai-system-loop.md`; the historical packaging name is stale for command naming. I’m also checking council assets because it is outside the `start-*loop` glob.
Council has the expected markdown plus auto/confirm YAML; no `deep_start-non-dev-ai-system-loop_*.yaml` assets matched, so the command surface needs a pre-migration inventory gate instead of assuming all 8 commands have paired YAML.
I’ll read the current Lane D command entrypoint to avoid baking a wrong YAML assumption into the phase plan.
**Recommendation**
Use 9 implementation phase children. This is the minimum decomposition that keeps each blast radius independently shippable while preserving byte-identical single-executor parity per mode. Do not combine advisor, governance, docs, and deletion into one tail phase; those are distinct failure domains. The migration should proceed as a staged cutover: freeze baselines, legalize runtime ownership, promote generic backend pieces, stage the merged workflow skill, repoint commands, repoint native agents, collapse advisor identity, consolidate governance, rewrite framework docs, then delete the old five skill folders only after full-surface validation passes.

**Evidence Used**
Confirmed surfaces from the Context Report: commands are 8 commands with 12 YAML and 6 presentation assets, with roughly 270 skill-path occurrences in command YAMLs (`context-report.md:66-75`). Native agents are 5 agents across 3 runtimes, 15 real files, not symlinks (`context-report.md:77-84`). Cross-repo docs are a separate surface (`context-report.md:86-98`). Skill-advisor migration is constrained by folder-name-bound `skill_id` and finite inbound edges (`context-report.md:100-105`). Governance has the largest mechanical source-file path surface and count-guard drift (`context-report.md:107-116`). The accepted parity bar is byte-identical single-executor artifacts modulo timestamps (`006-command-surface-docs-parity/spec.md:34-36`). Runtime section 4 requires an ownership ADR for a new consumer skill and forbids MCP reintroduction or stdout JSON contract changes (`deep-loop-runtime/SKILL.md:188-212`).

**Gating Decisions**
| Gate | Decision | Gates |
|---|---|---|
| Agent rename | Closed: keep `deep-context`, `deep-research`, `deep-review`, `deep-improvement`, and `ai-council`. No unified `@deep-loop` agent. | Phase 005, Phase 008, Phase 009 |
| Runtime ownership | Must be decided first through an ownership ADR because `deep-loop-workflows` becomes a new runtime consumer beyond the wording in `deep-loop-runtime/SKILL.md:207-210`. | Phase 002 onward |
| Backend promotions | Promote only generic runtime pieces before workflow path repoints: runtime-capabilities resolver, loop-lock CLI adapter, `resolveArtifactRoot`, terminal journal taxonomy contract. No new MCP, no stdout JSON changes. | Phase 003, Phase 004 |
| Advisor migration | Collapse public advisor identity only after the merged skill exists and command/agent paths can dispatch it. Use one skill ID plus mode discriminator aliases. | Phase 006, Phase 008, Phase 009 |
| Governance | Consolidate after the final mode-packet layout is stable, because `SOURCE-FILES` tables need final paths. | Phase 007, Phase 008, Phase 009 |
| Lane D | Treat `/deep:start-non-dev-ai-system-loop` as current command naming. The older `/deep:start-packaging-refine-loop` spec is binding for adapter constraints, not current command name. No Barter contract bump unless argv/env/journal/kill-switch semantics change. | Phase 004, Phase 009 |

**Phase Children**
| Order | Phase Folder | Scope | Deliverables | Validation | Rollback | Parallelizable |
|---:|---|---|---|---|---|---|
| 1 | `.opencode/specs/skilled-agent-orchestration/152-deep-loop-workflows/001-parity-baseline-and-runtime-adr` | Freeze pre-migration behavior and legalize runtime ownership. | Baseline single-executor artifact snapshots for context, research, review, council, and improvement lanes A/B/C/D; normalization rules for timestamps and run IDs; ownership ADR allowing `deep-loop-workflows` to consume promoted runtime helpers; command/agent/advisor/governance inventories. | Run every current mode through the same single-executor fixture matrix and record expected artifact digests; verify current command inventory includes `ask-ai-council.md` and no Lane D YAML assumption; strict spec validation for the phase. | Drop baseline artifacts and ADR draft; no production path changes should exist in this phase. | Inventory work can split by surface: commands, agents, advisor, governance, docs. Baseline execution should stay serialized enough to avoid shared state collisions. |
| 2 | `.opencode/specs/skilled-agent-orchestration/152-deep-loop-workflows/002-runtime-backend-promotions` | Promote only generic backend pieces into frozen `deep-loop-runtime`. | Parameterized runtime-capabilities resolver; loop-lock CLI adapter; shared `resolveArtifactRoot`; terminal journal taxonomy contract with adapters; runtime tests for JSON-only stdout, lock handling, and no MCP registration. | Runtime test suite; direct script JSON contract checks; old workflow commands still produce byte-identical artifacts through their existing paths or thin local adapters; no SQLite schema change unless explicitly ADR-approved. | Revert promoted runtime additions and restore local research/review/context copies; because commands are not repointed yet, rollback is contained. | Runtime helper implementation, tests, and documentation can proceed in parallel after the ADR is accepted. |
| 3 | `.opencode/specs/skilled-agent-orchestration/152-deep-loop-workflows/003-workflows-hub-and-mode-packets` | Build the new user-facing workflow skill without changing command dispatch yet. | `.opencode/skills/deep-loop-workflows/SKILL.md` as thin command-first/mode-first hub; direct mode packets under `.opencode/skills/deep-loop-workflows/context`, `research`, `review`, `ai-council`, and `improvement`; per-mode manifests; old subtrees copied or moved verbatim with hash comparison; improvement retains lane substructure. | Hash/diff checks proving moved mode packets are content-identical except required path roots and metadata; hub router tests for mode selection; old commands still pass baseline parity; no permanent compatibility wrappers. | Remove `.opencode/skills/deep-loop-workflows`; keep old five skills authoritative. | Mode packet staging can split by mode after backend APIs are stable. Hub router should be single-owner to avoid keyword-collision drift. |
| 4 | `.opencode/specs/skilled-agent-orchestration/152-deep-loop-workflows/004-command-surface-repoint` | Repoint the 8 `/deep:*` commands and command assets from five skill paths to the merged skill. | Rewritten command markdown frontmatter and path bodies; 12 YAML asset path rewrites; 6 presentation text rewrites; inline Lane D `loop-host.cjs` path rewrite in `start-non-dev-ai-system-loop.md`; command seed/prose updates from old skill terms to mode terms; verifier moved to workflow validation. | Repoint one command cluster at a time and run byte-identical single-executor parity after each cluster: context, research, review, council, improvement A/B/C/D. Also run YAML path verifier and grep that runtime paths still point to `.opencode/skills/deep-loop-runtime/...`. | Restore command markdown/YAML/presentation files to old paths; merged skill remains staged but unused. | Context, research, review, and council command rewrites can be parallel after common rewrite rules are frozen. Improvement’s four-command cluster should be one owner because `loop-host.cjs --mode` and Lane D adapter semantics are coupled. |
| 5 | `.opencode/specs/skilled-agent-orchestration/152-deep-loop-workflows/005-agent-mirror-repoint` | Repoint native agents while keeping agent names. | Updates to `.opencode/agents/{deep-context,deep-research,deep-review,deep-improvement,ai-council}.md`, `.claude/agents/*.md`, and `.codex/agents/*.toml`; internal skill references point to `deep-loop-workflows` mode packets; ai-council literal script paths updated in lockstep with output schema references. | Three-way mirror parity for all 5 agents, preserving TOML-specific fields and whitelisting only known path-convention differences; command dispatch smoke per native agent; no agent rename. | Restore 15 agent files to old skill references; commands can still run via CLI paths from Phase 004 if agent smoke fails. | Repoint per agent in parallel, but ai-council should be isolated because it has the densest executable path coupling. |
| 6 | `.opencode/specs/skilled-agent-orchestration/152-deep-loop-workflows/006-advisor-graph-mode-routing` | Collapse advisor identity to one canonical skill while preserving mode-level routing. | One `.opencode/skills/deep-loop-workflows/graph-metadata.json` with `skill_id: deep-loop-workflows`; old five IDs removed from rankable advisor graph; mode alias/discriminator layer for context, research, review, council, agent-improvement, model-benchmark, skill-benchmark, non-dev-ai-system-refine; inbound edge repoints with merged edge contexts; routing fixtures rewritten. | `skill_graph_scan`; `skill_graph_validate`; rejected edges must be `0`; grep for `UNKNOWN-TARGET`; advisor routing parity with at least 3 phrasing variants per mode; explicit deep-context coverage added because it was absent from aliases. | Restore old graph metadata and advisor alias fixtures; rescan graph; commands/agents remain functional from direct path routing. | Edge repoints and fixture updates can split, but final scan/validate must be single-owner. |
| 7 | `.opencode/specs/skilled-agent-orchestration/152-deep-loop-workflows/007-governance-tree-consolidation` | Consolidate `feature_catalog` and `manual_testing_playbook` into the merged skill with mode partitioning. | Unified lowercase `feature_catalog/` and `manual_testing_playbook/` roots under `deep-loop-workflows`; top-level mode partitions; council uppercase catalog normalized; mode-qualified ID handling for collisions; count guards rewritten; duplicated sandbox setup deduped; roughly 198 `SOURCE-FILES` path tables rewritten to final paths. | Catalog/playbook count guard tests; anchor integrity tests; source-file path grep; no flattening of mode-specific category models; pre-existing drift recorded and reconciled rather than blindly copied. | Restore per-mode governance roots from old skill folders or git; leave command/agent/advisor migration intact if governance-only validation fails. | Per-mode governance migration can run in parallel after final path layout is stable. Count aggregation and collision policy should be single-owner. |
| 8 | `.opencode/specs/skilled-agent-orchestration/152-deep-loop-workflows/008-framework-docs-sweep` | Rewrite public docs from five workflow skills to two-skill architecture. | Root `README.md`; `.opencode/skills/README.md`; `CLAUDE.md` and `AGENTS.md` paired edits; `deep-loop-runtime/README.md`; constitutional deep-skill workflow rule; narrow related-skill references in `system-spec-kit` and `cli-opencode`; no changelog or historical rewrite. | Grep old live skill IDs with exclusions for changelog/archive/historical spec folders; verify `CLAUDE.md` and `AGENTS.md` remain mirrored where expected; validate runtime README says one workflow consumer with internal modes plus frozen backend. | Revert docs only; no dispatch behavior changes should be tied to this phase. | Docs can split by file family after advisor/governance naming is final. |
| 9 | `.opencode/specs/skilled-agent-orchestration/152-deep-loop-workflows/009-old-skill-removal-final-validation` | Delete the old five workflow skill dirs and prove the full surface is clean. | Remove `.opencode/skills/deep-context`, `.opencode/skills/deep-research`, `.opencode/skills/deep-review`, `.opencode/skills/deep-ai-council`, `.opencode/skills/deep-improvement`; final graph rebuild; final parity report; final implementation summary. | Full mode matrix byte-identical parity; all command smoke tests; all agent mirror checks; advisor scan/validate/replay; governance count guards; runtime tests; strict spec validation; grep old paths excluding intentional history; no broken symlinks in `.claude/.codex` skill/command dirs. | Restore old five directories from git if any final gate fails; do not keep permanent compatibility directories. | Final validation can parallel by surface, but deletion and final pass/fail decision should be single-owner. |

**Dependency Graph**
```text
001-parity-baseline-and-runtime-adr
  -> 002-runtime-backend-promotions
  -> 003-workflows-hub-and-mode-packets
  -> 004-command-surface-repoint
  -> 005-agent-mirror-repoint
  -> 006-advisor-graph-mode-routing
  -> 007-governance-tree-consolidation
  -> 008-framework-docs-sweep
  -> 009-old-skill-removal-final-validation
```

Parallel windows:
```text
After 001: command inventory, agent inventory, advisor inventory, governance inventory, docs inventory can run concurrently.
After 002: mode-packet staging can run by mode.
After 003: 004 command repoint and 005 agent repoint can partially overlap if file ownership is separated.
After 003: 007 governance migration can run in parallel with 004/005, but final governance source paths must use the command-approved layout.
After 006 and 007: 008 docs sweep can split by doc family.
During 009: validation can fan out by surface, but deletion and final acceptance remain serial.
```

**Incremental Parity Rule**
Every behavior-touching phase must preserve the Phase 001 baseline for each mode before the phase can close. The required mode matrix is `context`, `research`, `review`, `council`, `agent-improvement`, `model-benchmark`, `skill-benchmark`, and `non-dev-ai-system-refine`. The compare should normalize timestamps, transient run IDs, and intentionally external Lane D packaging paths, but not normalize state shape, final report wording, verdict lines, journal stop reasons, candidate registry records, or command presentation blocks.

**No Pass-1 Amendment**
I do not recommend changing the Pass-1 architecture. The only clarification is sequencing-related: use `ai-council` as the direct mode-packet folder for path continuity, while the router/advisor discriminator may expose `council` with aliases for `ai-council` and `deep-ai-council`.
