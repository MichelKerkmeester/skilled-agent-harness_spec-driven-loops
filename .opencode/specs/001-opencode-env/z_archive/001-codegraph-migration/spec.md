# Spec: CodeGraph Migration in AGENTS.md

## Overview
Update AGENTS.md to replace all semantic_search MCP references with codegraph MCP, and add two new skills to the available skills list.

## Scope
- Replace `mcp-semantic-search` with `mcp-codegraph` throughout AGENTS.md
- Update tool routing section with new codegraph tool names
- Add `mcp-codegraph` skill entry
- Add `workflows-create-plugin` skill entry

## Out of Scope
- Changes to actual MCP configuration files
- Changes to skill files themselves
- Any other documentation updates

## Success Criteria
- All semantic_search references replaced with codegraph equivalents
- Skills list contains both new skills
- Document remains valid markdown
