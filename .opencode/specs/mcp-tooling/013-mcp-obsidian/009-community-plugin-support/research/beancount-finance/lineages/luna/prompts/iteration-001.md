# Iteration 1 prompt pack

Focus: verify the plugin-owned contract at source level: persisted settings, commands, structured vault layout, directive writers, and subprocess boundary.

Constraints: use the 2.3.1 source ref where available; distinguish verified source behavior from Beancount semantics; do not write outside the luna lineage artifact directory; retain the missing compiled `main.js` as an explicit evidence gap.

Required outputs: exact setting keys/defaults, command IDs, files/includes, supported writes, runtime-only state, and concrete edge cases for file-layer automation.
