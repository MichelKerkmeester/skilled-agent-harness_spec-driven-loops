# Iteration 003 prompt record

Treat apparent convergence as telemetry only. Broaden into failure modes: validation gaps, malformed directives, account lifecycle, currency and price failures, lot syntax, stale query data, output limits, CSV import, reconciliation, and rollback. Distinguish source-confirmed behavior from a recommended AI protocol.
