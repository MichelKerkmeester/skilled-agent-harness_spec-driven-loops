# Iteration 002 prompt record

Reconstruct the Beancount ledger model and the plugin’s serialized directive forms. Inspect BQL block/inline execution, named queries, BeanQuery grammar, and dashboard query families. Turn source observations into executable file-layer recipes, citing the source of each behavior.
