# Iteration 019: Adversarial Recheck

## Finding Verdicts

| ID | original severity | verdict | evidence | reasoning |
|---|---|---|---|---|
| P1-001-T79 | P1 | DOWNGRADE | `scripts/extractors/collect-session-data.ts:362-371` treats any non-empty `nextSteps` or `Next Steps` observation as unresolved; tests and normalization only model pending next-step strings, not completed checkbox items (`scripts/tests/runtime-memory-inputs.vitest.ts:323-352`, `scripts/utils/input-normalizer.ts:344-358`). | The code still does not filter completed `[x]` text, but I found no live docs/tests showing `nextSteps` is a checklist field. In current reality `nextSteps` means pending follow-up work, so the reported `[x]` case is an edge-case robustness gap, not a strong P1 correctness defect. |
| P1-002-1 | P1 | CONFIRMED | Stage 1 calls `hybridSearch.collectRawCandidates(...)` for hybrid paths (`mcp_server/lib/search/pipeline/stage1-candidate-gen.ts:543-556`, `591-603`, `644-648`). `collectRawCandidates()` forces `skipFusion: true` (`mcp_server/lib/search/hybrid-search.ts:1384-1429`), while Stage 2 assumes hybrid rows already arrived with fused scores (`mcp_server/lib/search/pipeline/stage2-fusion.ts:35-40`). | This is not just an intentional raw-candidate helper. The live 4-stage pipeline hands Stage 2 already-deduped raw rows, but Stage 2 does not perform RRF/adaptive fusion itself. Cross-channel evidence is reduced to raw candidate merging before Stage 2. |
| P1-002-2 | P1 | CONFIRMED | Live Stage 3 elects `bestChunk`, reassembles parent content, and sorts, but never calls `computeMPAB()` or assigns an aggregated parent score (`mcp_server/lib/search/pipeline/stage3-rerank.ts:489-523`, `533-639`). The documented/runtime helper still defines MPAB scoring as `sMax + 0.3 * sum(remaining) / sqrt(N)` (`mcp_server/lib/search/README.md:114-116`, `mcp_server/lib/scoring/mpab-aggregation.ts:17-25`, `78-125`), and tests still expect that formula (`mcp_server/tests/mpab-quality-gate-integration.vitest.ts:160-175`). | This is not a false positive and not obviously by design. The current Stage 3 implementation collapses chunks, but it does not apply the documented MPAB bonus formula that the scoring module and tests still describe. |
| P1-002-3 | P1 | CONFIRMED | Community injection adds rows with only `id`, `score`, and `_communityBoosted` (`mcp_server/lib/graph/community-detection.ts:527-557`; also tested in `mcp_server/tests/community-detection.vitest.ts:518-526`). Stage 2 injects those rows without hydrating state (`mcp_server/lib/search/pipeline/stage2-fusion.ts:817-830`). Stage 4 resolves missing `memoryState` to the caller's `minState` fallback (`mcp_server/lib/search/pipeline/stage4-filter.ts:87-97`, `146-162`). | Injected community rows can pass min-state filtering without carrying a real state value. The fallback was added to avoid false-negative empties, but in this path it also lets synthetic partial rows bypass the intended threshold. |
| P1-002-4 | P1 | DOWNGRADE | Stage 3 explicitly rebuilds results as `diversified embedded rows first`, then `non-embedded rows` in original relative order (`mcp_server/lib/search/pipeline/stage3-rerank.ts:204-223`). | The ranking effect is real, but the code comment shows it is intentional diversity behavior for the embedded subset after fixing silent drops. Because `SPECKIT_MMR` is optional and the tradeoff is deliberate rather than accidental data loss, this reads as a lower-severity ranking-quality issue, not a P1 correctness bug. |
| SEC-001 | SEC | CONFIRMED | `resolveAdminActor()` is documented as validating format only, with transport auth required elsewhere (`mcp_server/handlers/shared-memory.ts:35-40`, `40-85`). Tests only cover empty/ambiguous IDs, not caller binding (`mcp_server/tests/shared-memory-handlers.vitest.ts:247-264`). Feature docs describe actor identity as the shared-memory access boundary (`feature_catalog/02--mutation/07-namespace-management-crud-tools.md:18-20`, `feature_catalog/17--governance/04-shared-memory-rollout-deny-by-default-membership-and-kill-switch.md:28`). | I did not find evidence that the current MCP layer cryptographically or process-level binds `actorUserId` / `actorAgentId` to the caller. Given shipped shared-memory governance features, treating caller-supplied actor IDs as trusted is still a live issue, not something I can dismiss as harmless local-only plumbing. |
| SEC-002 | SEC | CONFIRMED | Checkpoint schemas expose only `name`, `specFolder`, `limit`, `clearExisting`, and `confirmName` (`mcp_server/tool-schemas.ts:323-339`). Handler args likewise carry no `tenantId`, `userId`, `agentId`, or `sharedSpaceId` (`mcp_server/handlers/checkpoints.ts:39-58`, `105-205`). Storage falls back to `SELECT * FROM memory_index` when `specFolder` is absent (`mcp_server/lib/storage/checkpoints.ts:1102-1118`), and restore scope is derived from checkpoint contents, not caller governance scope (`mcp_server/lib/storage/checkpoints.ts:1248-1280`). | In a governed/shared-memory deployment, checkpoint creation and restore still operate outside tenant or actor isolation. This is not rebutted by the current code. A single-user local deployment lowers practical risk, but the handler surface is still unscoped. |
| SEC-003 | SEC | DOWNGRADE | Scoped trigger matching validates trusted session IDs up front (`mcp_server/handlers/memory-triggers.ts:207-225`), but the post-filter catch still logs and returns unscoped results on scope-filter failure (`mcp_server/handlers/memory-triggers.ts:287-322`). | The fail-open path is real, but it only activates after an internal scope-filter error. Because exploitation requires a secondary runtime/schema failure and the path is logged, I would lower severity rather than dismiss it. It is still not ideal for governed retrieval. |
| P1-004-1 | P1 | DOWNGRADE | The current 012 packet's `review-report.md` still tracks the issue (`001-hybrid-rag-fusion-epic/012-pre-release-fixes-alignment-preparation/review-report.md:69`), but there are no live `spec.md` or `plan.md` matches for `fusion-lab.js` inside the 012 packet. A global spec/plan search only found a retired-status mention in `015-manual-testing-per-playbook/011-scoring-and-calibration/spec.md:126`. | The specific claim that active spec/plan artifacts still track `fusion-lab.js` is no longer well supported. The issue persists mostly as review-history text, not as an active packet plan/spec dependency. |
| P1-014-1 | P1 | CONFIRMED | Live command result: `find .opencode/specs/02--system-spec-kit/022-hybrid-rag-fusion -type d -name "[0-9]*" | wc -l` => `123`. The 012 packet still claims `119 spec directories` in `spec.md:27` and `plan.md:131`. | This mismatch is still present exactly as reported. The packet metadata is stale against the live directory count. |

## Summary

Adversarially rechecking the live code cuts the list down to 6 solid findings, 4 downgraded findings, and 0 fixed/false-positive findings.

The strongest confirms are the Stage 1 fusion gap, the missing live MPAB formula in Stage 3, the community/min-state bypass, the shared-space actor-trust gap, the unscoped checkpoint surface, and the `119` vs `123` directory-count drift.

The best downgrade candidates are the T79 checkbox interpretation, the MMR non-embedded ordering complaint, the trigger fail-open path, and the `fusion-lab.js` packet-tracking claim. In each of those cases, the underlying code path exists, but the original severity or framing overstates the live impact.

## JSONL

{"id":"P1-001-T79","originalSeverity":"P1","verdict":"DOWNGRADE","evidence":"collect-session-data.ts:362-371; runtime-memory-inputs.vitest.ts:323-352","reasoning":"Current code treats any nextSteps as unresolved, but live tests/docs model nextSteps as pending actions, not completed checkbox items."}
{"id":"P1-002-1","originalSeverity":"P1","verdict":"CONFIRMED","evidence":"stage1-candidate-gen.ts:543-556,591-603,644-648; hybrid-search.ts:1384-1429; stage2-fusion.ts:35-40","reasoning":"Stage 1 feeds Stage 2 raw candidates with skipFusion=true while Stage 2 assumes hybrid fusion already happened."}
{"id":"P1-002-2","originalSeverity":"P1","verdict":"CONFIRMED","evidence":"stage3-rerank.ts:489-523,533-639; search/README.md:114-116; mpab-aggregation.ts:17-25,78-125","reasoning":"Live Stage 3 collapses chunks but does not apply the documented MPAB formula still described in docs and helper code."}
{"id":"P1-002-3","originalSeverity":"P1","verdict":"CONFIRMED","evidence":"community-detection.ts:527-557; stage2-fusion.ts:817-830; stage4-filter.ts:87-97,146-162","reasoning":"Injected community rows lack memoryState and inherit the minState fallback, bypassing real state filtering."}
{"id":"P1-002-4","originalSeverity":"P1","verdict":"DOWNGRADE","evidence":"stage3-rerank.ts:204-223","reasoning":"Non-embedded rows are intentionally appended after diversified embedded rows as an MMR tradeoff, so this is lower-severity ranking drift."}
{"id":"SEC-001","originalSeverity":"SEC","verdict":"CONFIRMED","evidence":"shared-memory.ts:35-40,40-85; shared-memory-handlers.vitest.ts:247-264","reasoning":"The handler explicitly validates format only and no caller-to-actor binding was found in the live MCP layer."}
{"id":"SEC-002","originalSeverity":"SEC","verdict":"CONFIRMED","evidence":"tool-schemas.ts:323-339; handlers/checkpoints.ts:39-58,105-205; lib/storage/checkpoints.ts:1102-1118,1248-1280","reasoning":"Checkpoint tools still have no tenant/user/agent/shared-space inputs and default to unscoped snapshots/restores."}
{"id":"SEC-003","originalSeverity":"SEC","verdict":"DOWNGRADE","evidence":"memory-triggers.ts:207-225,287-322","reasoning":"The fail-open path exists, but only after an internal scope-filter error, so severity is lower than a normal-path auth bypass."}
{"id":"P1-004-1","originalSeverity":"P1","verdict":"DOWNGRADE","evidence":"012 review-report.md:69; 015-manual-testing-per-playbook/011-scoring-and-calibration/spec.md:126","reasoning":"Current active spec/plan references are largely gone; the finding survives mainly as review-history text and a retired-status mention."}
{"id":"P1-014-1","originalSeverity":"P1","verdict":"CONFIRMED","evidence":"spec.md:27; plan.md:131; directory-count command => 123","reasoning":"The 012 packet still claims 119 directories while the live numbered-directory count is 123."}
