# Wave 1 - CODEX-A1: Core Pipeline Logic
Date: 2026-03-21
Agent: Codex CLI GPT-5.4 (read-only sandbox)

## Findings

FINDING-CODEX-A1-001 | HIGH | LOGIC_ERROR | .opencode/skill/system-spec-kit/scripts/core/workflow.ts:679 | Tree-thinning is making decisions on a 500-character excerpt, not on the real file size, so medium and large files can be undercounted and merged as if they were tiny. | `resolveTreeThinningContent()` returns `readFileSync(candidatePath, 'utf8').slice(0, 500)`; those excerpts are passed at `const thinningResult = applyTreeThinning(thinFileInputs)`; `applyTreeThinning()` classifies with `estimateTokenCount(file.content)`. | Pass full content or a separate full-token-count into thinning, and only truncate later when rendering merge notes.
FINDING-CODEX-A1-002 | HIGH | LOGIC_ERROR | .opencode/skill/system-spec-kit/scripts/core/workflow.ts:1137 | The sufficiency gate is told that every file is spec-relevant, which can let cross-spec or synthetic file evidence count as valid support and mask contamination. | Snapshot builder hardcodes `files: files.map((file) => ({ path: file.FILE_PATH, description: file.DESCRIPTION, specRelevant: true }))`; the sufficiency engine explicitly only discounts synthetic files when `!file.specRelevant`. | Preserve real relevance/provenance on `FileChange` and propagate `_specRelevant` instead of forcing `true`.
FINDING-CODEX-A1-003 | MEDIUM | LOGIC_ERROR | .opencode/skill/system-spec-kit/scripts/core/workflow.ts:1937 | `KEY_FILES` is built from the pre-thinning file list, so the rendered memory can still advertise files that tree-thinning deliberately collapsed out of `FILES`. | Workflow computes `const effectiveFiles = applyThinningToFileChanges(enhancedFiles, thinningResult)` and then immediately does `const keyFiles = buildKeyFiles(enhancedFiles, specFolder)`; the helper itself is defined as `buildKeyFiles(effectiveFiles, specFolderPath)`. | Build key files from `effectiveFiles`, or make the divergence explicit if it is intentional.
FINDING-CODEX-A1-004 | MEDIUM | EDGE_CASE | .opencode/skill/system-spec-kit/scripts/core/workflow.ts:418 | The stateless alignment safety check silently degrades to keyword-only matching whenever spec-folder extraction fails, which weakens the contamination barrier without surfacing that the stronger path is gone. | `resolveAlignmentTargets()` wraps `extractSpecFolderContext(...)` in `try { ... } catch (_error) { // Fall back to keyword-only alignment when spec docs are unavailable. }`; the earlier stateless gate then uses those reduced targets to decide whether to abort. | Log degraded alignment mode at minimum, and fail closed for stateless saves when explicit spec context cannot be loaded.
FINDING-CODEX-A1-005 | HIGH | LOGIC_ERROR | .opencode/skill/system-spec-kit/scripts/core/workflow.ts:976 | The workflow lock only serializes runs inside one Node process; concurrent CLI invocations still race on shared files like `description.json`, so memory sequence/history updates can be lost. | Locking is just `let workflowRunQueue: Promise<void> = Promise.resolve()` plus `await priorRun`; later the code does `const existing = loadPFD(...)`, mutates `existing.memorySequence`, then `savePFD(existing, specFolderAbsolute)` with no filesystem lock or CAS. | Use a lockfile or atomic compare-and-swap around per-spec-folder metadata updates, not only an in-memory promise queue.
FINDING-CODEX-A1-006 | MEDIUM | TYPE_SAFETY | .opencode/skill/system-spec-kit/scripts/core/quality-scorer.ts:129 | There are two exported `scoreMemoryQuality` APIs with the same name but incompatible signatures and scoring models, which is a footgun for callers and keeps test coverage split across two different quality lanes. | Core scorer exports `scoreMemoryQuality(content, triggerPhrases, keyTopics, files, observations, ...)`; `workflow.ts` imports the extractor scorer as `scoreMemoryQualityV2` from `../extractors/quality-scorer`; tests still import the legacy core scorer directly. | Rename or remove the legacy scorer, or wrap both behind one canonical object-based API.
FINDING-CODEX-A1-007 | MEDIUM | LOGIC_ERROR | .opencode/skill/system-spec-kit/scripts/core/memory-indexer.ts:143 | The indexer’s “recency” term is not actually recency-based; every document gets the same +0.2 boost, so the importance formula is mislabeled and cannot distinguish new from old memories. | `const recencyFactor = IMPORTANCE_RECENCY_WEIGHT`; `const importanceWeight = Math.round((lengthFactor + anchorFactor + recencyFactor + IMPORTANCE_BASE_WEIGHT) * 100) / 100`. | Derive recency from a real timestamp or remove/rename the factor so the weight matches what it actually measures.
FINDING-CODEX-A1-008 | MEDIUM | EDGE_CASE | .opencode/skill/system-spec-kit/scripts/core/memory-indexer.ts:107 | `indexMemory()` has no runtime normalization for null or malformed trigger arrays, so JS callers can crash it or accidentally index character-by-character “phrases.” | It does `if (preExtractedTriggers.length > 0)` and later `for (const phrase of manualPhrases)` without `Array.isArray(...)`; the function is exported and callable from untyped JS paths. | Normalize both inputs with `Array.isArray` and filter to non-empty strings before use.
FINDING-CODEX-A1-009 | LOW | TYPE_SAFETY | .opencode/skill/system-spec-kit/scripts/core/config.ts:125 | Config validation says these are integer/count fields, but it accepts fractional numbers, which downstream code silently floors during slicing and truncation. | The section is labeled `Positive integer fields`; validation only rejects non-finite or `<= 0`; consumers later do `observations.slice(0, CONFIG.MAX_OBSERVATIONS)` and `allFiles.slice(0, CONFIG.MAX_FILES_IN_MEMORY)`. | Enforce `Number.isInteger` for count/limit fields, or round once during config load so behavior is explicit.
FINDING-CODEX-A1-010 | LOW | LOGIC_ERROR | .opencode/skill/system-spec-kit/scripts/core/tree-thinning.ts:34 | The TS threshold fix to `150` is not fully propagated to stale JS artifacts, so JS-only test runs can still assert the old `300` behavior and mask drift. | Source sets `memoryThinThreshold: 150`; stale artifact `scripts/tests/tree-thinning.vitest.js` still says `Memory-specific thresholds (300/100 tokens)` and `expect(tree_thinning_1.DEFAULT_THINNING_CONFIG.memoryThinThreshold).toBe(300)`. | Regenerate or delete stale JS artifacts and make CI verify TS source and JS outputs stay in sync.
