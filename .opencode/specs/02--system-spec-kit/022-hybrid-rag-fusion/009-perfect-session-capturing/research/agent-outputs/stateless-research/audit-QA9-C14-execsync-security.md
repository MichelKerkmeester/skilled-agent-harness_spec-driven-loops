## Audit QA9-C14: execSync Security — Copilot Cross-Cutting
### P0 Blockers: 0 — None. No user-controlled input reaches the `execSync` command strings in either file: `git-context-extractor.ts:51-57,109-113,133`; `session-extractor.ts:134-139`. The commands are static git invocations, and `projectRoot` / `CONFIG.PROJECT_ROOT` are used only as `cwd`, not interpolated into the shell command.
### P1 Required: 1 — `session-extractor.ts:134-139` uses `execSync` twice in `getChannel()` without a `timeout`, so a hung or pathological git invocation can block the extractor indefinitely even though the surrounding `try/catch` prevents process-level crashes.
### P2 Suggestions: 2 — `git-context-extractor.ts:118-185` contains failures with a top-level `try/catch` and returns `emptyResult()`, which is graceful for pipeline stability but drops all partial git enrichment and emits no diagnostic signal; add lightweight error reporting if auditability matters. `git-context-extractor.ts:51-57` and `session-extractor.ts:134-139` currently avoid injection because the command strings are fixed, but both wrappers use shell-based `execSync(string)`; migrating to `execFileSync('git', [...args])` would harden future path/argument handling by construction.
### Score: 88
