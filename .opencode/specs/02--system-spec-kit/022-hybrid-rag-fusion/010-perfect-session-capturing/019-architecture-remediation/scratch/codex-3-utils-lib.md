Findings first, ordered by severity. I did not modify any files.

- `CODEX3-001` | `High` | `Dangling import` | File: [ast-parser.ts:8](/Users/michelkerkmeester/MEGA/Development/Opencode%20Env/Public/.opencode/skill/system-spec-kit/scripts/memory/ast-parser.ts#L8) | Imports `@spec-kit/shared/lib/structure-aware-chunker`, even though `structure-aware-chunker.ts` was deleted. Evidence: repo-wide search found this as the remaining live code import, so loading this module will fail at resolution time. Fix: repoint to the current shared chunker implementation or inline the supported chunking helpers, then remove the stale README mention too.

- `CODEX3-002` | `Medium` | `Slug collision / logic` | File: [anchor-generator.ts:241](/Users/michelkerkmeester/MEGA/Development/Opencode%20Env/Public/.opencode/skill/system-spec-kit/scripts/lib/anchor-generator.ts#L241) | `wrapSectionsWithAnchors()` throws away `category`, `specNumber`, and the richer `generateAnchorId()` path, then uses only `generateSemanticSlug(headingText)`. Evidence: a runtime probe on `## Implemented auth flow` and `## Discovered auth flow` produced `auth-flow` and `auth-flow-2`, so anchor IDs are collision-prone and order-dependent. Fix: generate wrapped anchors via `generateAnchorId(headingText, category, specNumber, sectionContext)` instead of the bare semantic slug.

- `CODEX3-003` | `Medium` | `Validation gap` | File: [data-validator.ts:59](/Users/michelkerkmeester/MEGA/Development/Opencode%20Env/Public/.opencode/skill/system-spec-kit/scripts/utils/data-validator.ts#L59) | `ensureArrayOfObjects()` lets malformed arrays through unchanged, and presence flags are only ever set to `true`, never recomputed to `false`. Evidence: runtime on `{ HAS_DESCRIPTION: true, DESCRIPTION: '', PROS: [42] }` returned `{"HAS_DESCRIPTION":true,"DESCRIPTION":"","PROS":[42],"HAS_PROS_CONS":true}`. Fix: coerce every `PROS`/`CONS` entry into `{ PRO/CON: string }` and recompute all mapped flags both ways from normalized content.

- `CODEX3-004` | `Medium` | `Input edge case / validation gap` | File: [input-normalizer.ts:199](/Users/michelkerkmeester/MEGA/Development/Opencode%20Env/Public/.opencode/skill/system-spec-kit/scripts/utils/input-normalizer.ts#L199) | `transformKeyDecision()` assumes `decisionItem.alternatives` is an array and does `.map(...)`, but `validateInputData()` never validates that nested shape. Evidence: runtime with `{ decision: 'x', alternatives: 'oops' }` threw `TypeError: ...map is not a function`. Fix: guard with `Array.isArray(decisionItem.alternatives)` and extend validation to nested decision objects.

- `CODEX3-005` | `Medium` | `ReDoS / config hardening` | File: [content-filter.ts:113](/Users/michelkerkmeester/MEGA/Development/Opencode%20Env/Public/.opencode/skill/system-spec-kit/scripts/lib/content-filter.ts#L113) | `compileNoisePatterns()` turns arbitrary config strings into live `RegExp` objects, and `filterNoise()` executes them against every prompt. Evidence: there is no safety screening before `new RegExp(...)`, so a catastrophic pattern in `filters.jsonc` can hang filtering. Fix: validate patterns with a safe-regex gate, reject risky constructs, and cap input length per regex evaluation.

- `CODEX3-006` | `Medium` | `Dedup false positive` | File: [content-filter.ts:376](/Users/michelkerkmeester/MEGA/Development/Opencode%20Env/Public/.opencode/skill/system-spec-kit/scripts/lib/content-filter.ts#L376) | `calculateSimilarity()` is only same-position character agreement over the first 200 chars, so distinct prompts with a long shared prefix are dropped as duplicates. Evidence: a runtime probe on two different auth-fallback sentences scored `0.857...`, and `deduplicate()` kept only one item at the default threshold band. Fix: replace this with token-shingle/Jaccard, cosine, or normalized edit-distance similarity.

- `CODEX3-007` | `Medium` | `Slug collision` | File: [slug-utils.ts:262](/Users/michelkerkmeester/MEGA/Development/Opencode%20Env/Public/.opencode/skill/system-spec-kit/scripts/utils/slug-utils.ts#L262) | `generateContentSlug()` skips generic-name screening for the fallback string, so it can emit known-generic slugs. Evidence: runtime showed `isGenericContentTask('Session context') === true` while `generateContentSlug('', 'Session context')` returned `session-context`. Fix: run the fallback through the same generic/contamination checks and fall back to a hash or spec-derived slug when it is generic.

- `CODEX3-008` | `Medium` | `Time handling` | File: [message-utils.ts:90](/Users/michelkerkmeester/MEGA/Development/Opencode%20Env/Public/.opencode/skill/system-spec-kit/scripts/utils/message-utils.ts#L90) | `formatTimestamp()` adds `TIMEZONE_OFFSET_HOURS` to the epoch and still emits a `Z`-suffixed ISO string, which changes the instant while claiming UTC. Evidence: the code builds `new Date(d.getTime() + offsetMs)` and then returns `toISOString()` for `iso`; this is wrong whenever the configured offset is non-zero. Fix: only apply offsets to human-readable/date-only formats, or emit an offset/local string instead of `Z`.

- `CODEX3-009` | `Low` | `Dead export` | File: [message-utils.ts:191](/Users/michelkerkmeester/MEGA/Development/Opencode%20Env/Public/.opencode/skill/system-spec-kit/scripts/utils/message-utils.ts#L191), [index.ts:89](/Users/michelkerkmeester/MEGA/Development/Opencode%20Env/Public/.opencode/skill/system-spec-kit/scripts/utils/index.ts#L89) | `extractKeyArtifacts()` is explicitly marked “Dead code — zero production importers” but is still exported directly and through the barrel. Evidence: non-test repo hits are only its own module and the barrel export. Fix: stop re-exporting it, leave one deprecation point if needed, then remove it.

- `CODEX3-010` | `Low` | `Logic / dead parameter` | File: [ascii-boxes.ts:134](/Users/michelkerkmeester/MEGA/Development/Opencode%20Env/Public/.opencode/skill/system-spec-kit/scripts/lib/ascii-boxes.ts#L134), [decision-tree-generator.ts:166](/Users/michelkerkmeester/MEGA/Development/Opencode%20Env/Public/.opencode/skill/system-spec-kit/scripts/lib/decision-tree-generator.ts#L166) | `formatOptionBox()` accepts `isChosen` but never uses it, so chosen and non-chosen options render identically. Evidence: runtime check returned `true` for `formatOptionBox(opt, true) === formatOptionBox(opt, false)`. Fix: use `isChosen` to alter the header/border/marker, or remove the parameter and caller expectation.

No live dangling imports showed up inside the 19 requested lib/utils files themselves. The only stale non-code reference I found is [lib/README.md:43](/Users/michelkerkmeester/MEGA/Development/Opencode%20Env/Public/.opencode/skill/system-spec-kit/scripts/lib/README.md#L43), which still lists `structure-aware-chunker.ts`.