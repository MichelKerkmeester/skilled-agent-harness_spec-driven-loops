# Wave 1 - CODEX-A2: Extractor System & JSON-Primary Flow
Date: 2026-03-21
Agent: Codex CLI GPT-5.4 (read-only sandbox)

## Findings

FINDING-CODEX-A2-001 | HIGH | DATA_LOSS | .opencode/skill/system-spec-kit/scripts/utils/input-normalizer.ts:433 | `normalizeInputData()` drops canonical JSON-primary `sessionSummary`/`keyDecisions` semantics before collection, so explicit completion metadata can disappear and later stages misclassify finished saves as in-progress. | evidence: fast path `if (data.userPrompts || ... ) { ... return cloned; }` only backfills `nextSteps`, trigger phrases, spec folder, technical context, and tier; slow path converts `sessionSummary`/`keyDecisions` into observations but never preserves raw fields; `collect-session-data.ts:350-362` checks `collectedData.sessionSummary` and `collectedData.keyDecisions` directly. | recommended fix: always persist canonical `sessionSummary`, `keyDecisions`, and `nextSteps` on normalized data, then have `collectSessionData()` consume those canonical fields.
FINDING-CODEX-A2-002 | HIGH | DATA_LOSS | .opencode/skill/system-spec-kit/scripts/extractors/session-extractor.ts:352 | Structured JSON-primary `toolCalls` and `exchanges` are effectively ignored downstream; the system reconstructs tool/activity state from observation text instead of consuming the richer structured fields. | evidence: `session-types.ts:140-143` declares `toolCalls?: ToolCallSummary[]` and `exchanges?: ExchangeSummary[]`; `session-extractor.ts` says tool counts “should derive from the structured toolCalls array directly” but still regex-counts `observations`; `conversation-extractor.ts:132-165` rebuilds tool calls from `observations[].facts`. | recommended fix: thread `toolCalls`/`exchanges` through normalization and make `session-extractor`/`conversation-extractor` prefer structured fields before text reconstruction.
FINDING-CODEX-A2-003 | MEDIUM | TYPE_SAFETY | .opencode/skill/system-spec-kit/scripts/types/session-types.ts:147 | `nextSteps` has a schema mismatch: collected data types allow `Array<Record<string, unknown>>`, but normalization/rendering treat it as `string[]`, so object-form next steps collapse into `[object Object]`. | evidence: `session-types.ts` declares `nextSteps?: Array<Record<string, unknown>>`; `input-normalizer.ts:412-416` accepts the array unchecked; `input-normalizer.ts:310-325` does `narrative: nextSteps.join(' ')` and interpolates `firstStep`/`remainingSteps` into strings. | recommended fix: normalize next-step objects into strings explicitly or narrow the type contract to `string[]` end-to-end.
FINDING-CODEX-A2-004 | HIGH | PARITY | .opencode/skill/system-spec-kit/scripts/loaders/data-loader.ts:403 | Codex session targeting is implemented but dropped at the loader boundary, so a requested `sessionId` never reaches the Codex capture selector. | evidence: loader interface declares `captureCodexConversation(maxExchanges, projectRoot)` and calls `captureCodexConversation(20, projectRoot)`; actual capture signature is `captureCodexConversation(..., sessionId?)` in `codex-cli-capture.ts:322-326`, and `resolveTranscript()` filters on `sessionId` at `codex-cli-capture.ts:306-310`. | recommended fix: update `CodexCliCaptureMod` to include `sessionId` and pass `options.sessionId` through `attemptNativeCapture()`.
FINDING-CODEX-A2-005 | MEDIUM | PARITY | .opencode/skill/system-spec-kit/scripts/loaders/data-loader.ts:370 | Session targeting is asymmetric across providers: Claude gets exact-id/time-window hints, while Copilot, Gemini, and OpenCode just pick the newest/current matching session, which can bind recovery to the wrong concurrent session. | evidence: Claude passes `buildClaudeSessionHints(sessionId)` and has ambiguity logic in `claude-code-capture.ts:327-392`; Copilot returns `matches[0]` in `copilot-cli-capture.ts:197-201`; Gemini returns `newestMatch` in `gemini-cli-capture.ts:227-261`; OpenCode uses `getCurrentSession(projectId)` in `opencode-capture.ts:740-743`. | recommended fix: standardize a provider-neutral session-target contract with exact id, optional time window, and explicit ambiguity errors for all captures.
FINDING-CODEX-A2-006 | MEDIUM | LOGIC_ERROR | .opencode/skill/system-spec-kit/scripts/extractors/contamination-filter.ts:69 | The contamination filter misses current tool-path leakage shapes because its “tool title with path” regex only matches `Read/Edit/... tool ... /path`, while the normalizer emits titles like `Read src/file.ts` and `Edit src/file.ts`. | evidence: denylist uses `\\b(?:Read|Edit|Write|Grep|Glob|Bash)\\s+tool...`; `input-normalizer.ts:693-717` generates `Read ${shortPath}`, `Edit ${shortPath}`, `Write ${shortPath}`, `Bash ${shortPath}`. | recommended fix: expand contamination patterns to cover current normalized title forms or filter tool-path leakage from structured tool metadata before title rendering.
FINDING-CODEX-A2-007 | MEDIUM | LOGIC_ERROR | .opencode/skill/system-spec-kit/scripts/extractors/contamination-filter.ts:33 | The denylist is broad enough to delete legitimate content because it strips generic phrases like `Let me check`, `First, I'll`, and `Next, I'll` from every cleaned field, not just orchestration-only metadata. | evidence: patterns at `contamination-filter.ts:33-55`; workflow applies `cleanContaminationText()` to `observation.title`, `observation.narrative`, `fact.text`, `SUMMARY`, `file.DESCRIPTION`, and `message.prompt` in `core/workflow.ts:1508-1601` and `1642-1653`. | recommended fix: restrict these regexes to orchestration-only fields or require stronger anchors/context before removal.
FINDING-CODEX-A2-008 | MEDIUM | LOGIC_ERROR | .opencode/skill/system-spec-kit/scripts/core/quality-scorer.ts:123 | The project keeps two incompatible `scoreMemoryQuality` implementations with the same name, so tests/calibration exercise a different scoring model than runtime. | evidence: runtime imports extractor scorer in `core/workflow.ts:39-42`; extractor scorer takes `scoreMemoryQuality(inputs)` and uses contamination caps only (`extractors/quality-scorer.ts:159-180`); legacy core scorer takes positional args and also subtracts contamination penalties (`core/quality-scorer.ts:264-279`); tests still import the core scorer. | recommended fix: remove or rename the legacy scorer, or make it a thin adapter over the extractor scorer so test calibration matches production behavior.
FINDING-CODEX-A2-009 | LOW | DEAD_CODE | .opencode/skill/system-spec-kit/scripts/extractors/collect-session-data.ts:732 | The null-data simulation fallback is effectively dead in the real loader flow and would silently fabricate session data if the collector were called directly with `null`. | evidence: `loadCollectedData()` throws `RECOVERY_MODE_REQUIRED` or `NO_DATA_AVAILABLE` at `data-loader.ts:559-597`, but `collectSessionData()` still does `return getSimFactory().createSessionData(...)` when `!collectedData`. | recommended fix: delete the branch or guard it behind an explicit simulation mode flag instead of silent fallback.
FINDING-CODEX-A2-010 | LOW | DEAD_CODE | .opencode/skill/system-spec-kit/scripts/extractors/gemini-cli-capture.ts:286 | `toolCallIndexById` is write-only dead state in the Gemini capture path. | evidence: the map is created at line 286 and only written with `toolCallIndexById.set(callId, toolCalls.length)` at line 300; there is no corresponding `get()` or merge path anywhere in the file. | recommended fix: remove the map or implement the missing reconciliation logic if Gemini can emit follow-up tool-result events.
