# Review Iteration 036
## Dimension: D3 Traceability
## Focus: F021-F033 remediation verification

## New Findings
None.

## Remediation Verification
- F021: FIXED — `code_graph_scan` now enters the DB through `graphDb.getLastGitHead()` before scanning, and `getDb()` lazily calls `initDb(DATABASE_DIR)`; `initDb()` creates schema/version state on first use instead of requiring prior setup (`.opencode/skill/system-spec-kit/mcp_server/handlers/code-graph/scan.ts:71-72`; `.opencode/skill/system-spec-kit/mcp_server/lib/code-graph/code-graph-db.ts:112-145`).
- F022: PARTIAL — the Claude compaction path now calls `autoSurfaceAtCompaction()` from `memory-surface.ts`, but the cached payload still only renders constitutional memories; `triggered` stays empty in `mergeInput` and is never emitted into the compact payload (`.opencode/skill/system-spec-kit/mcp_server/hooks/claude/compact-inject.ts:19`; `.opencode/skill/system-spec-kit/mcp_server/hooks/claude/compact-inject.ts:212-217`; `.opencode/skill/system-spec-kit/mcp_server/hooks/claude/compact-inject.ts:231-249`; `.opencode/skill/system-spec-kit/mcp_server/hooks/memory-surface.ts:396-412`).
- F023: FIXED — schema migration is now guarded by `ensureSchemaMigrations()`, and `initDb()` clears the singleton on setup failure by closing/resetting `db`/`dbPath` before rethrowing, preventing poisoned reuse (`.opencode/skill/system-spec-kit/mcp_server/lib/code-graph/code-graph-db.ts:96-109`; `.opencode/skill/system-spec-kit/mcp_server/lib/code-graph/code-graph-db.ts:115-138`).
- F024: FIXED — both `replaceNodes()` and `replaceEdges()` now wrap delete+insert sequences in SQLite transactions, so a failed insert no longer leaves the file graph half-wiped (`.opencode/skill/system-spec-kit/mcp_server/lib/code-graph/code-graph-db.ts:213-243`; `.opencode/skill/system-spec-kit/mcp_server/lib/code-graph/code-graph-db.ts:247-265`).
- F025: FIXED — DR-004 is explicitly marked superseded, updated from 4 phases to 16 phases, and points readers to DR-011/DR-016 (`.opencode/specs/02--system-spec-kit/024-compact-code-graph/decision-record.md:53-63`).
- F026: FIXED — transitive traversal now enforces `item.depth >= maxDepth` before expansion and deduplicates both visited nodes and emitted results via `visited` + `resultSymbolIds`, preventing over-depth leakage and converging-path duplicates (`.opencode/skill/system-spec-kit/mcp_server/handlers/code-graph/query.ts:44-46`; `.opencode/skill/system-spec-kit/mcp_server/handlers/code-graph/query.ts:49-96`; `.opencode/skill/system-spec-kit/mcp_server/handlers/code-graph/query.ts:139-151`).
- F027: FIXED — session filenames are no longer sanitized aliases; they are SHA-256-derived names from the full `sessionId`, which removes the old lossy filename collision path (`.opencode/skill/system-spec-kit/mcp_server/hooks/claude/hook-state.ts:41-45`; `.opencode/skill/system-spec-kit/mcp_server/tests/hook-state.vitest.ts:51-57`).
- F028: FIXED — hook state now creates the directory with `0o700` and writes the temp JSON with `0o600` before atomic rename, and tests assert both permissions (`.opencode/skill/system-spec-kit/mcp_server/hooks/claude/hook-state.ts:48-50`; `.opencode/skill/system-spec-kit/mcp_server/hooks/claude/hook-state.ts:66-73`; `.opencode/skill/system-spec-kit/mcp_server/tests/hook-state.vitest.ts:65-76`; `.opencode/skill/system-spec-kit/mcp_server/tests/hook-state.vitest.ts:101-108`).
- F029: STILL_ACTIVE — direct coverage improved in some areas (`handleCodeGraphScan` is directly tested and several hook helpers are directly imported), but I found no direct test imports for `handleCodeGraphQuery`, `handleCodeGraphContext`, `handleCccFeedback`, or `session-stop` at current HEAD; coverage is still uneven versus the original gap (`.opencode/skill/system-spec-kit/mcp_server/tests/code-graph-scan.vitest.ts:35`; `.opencode/skill/system-spec-kit/mcp_server/tests/hook-state.vitest.ts:9-18`; `.opencode/skill/system-spec-kit/mcp_server/tests/hook-session-start.vitest.ts:6-14`; `.opencode/skill/system-spec-kit/mcp_server/handlers/code-graph/query.ts:99-189`; `.opencode/skill/system-spec-kit/mcp_server/handlers/code-graph/context.ts:85-186`; `.opencode/skill/system-spec-kit/mcp_server/handlers/code-graph/ccc-feedback.ts:16-71`).
- F030: PARTIAL — the main spec now matches the single unscoped SessionStart registration used in settings, but the research table still says "SessionStart has `source=compact` matcher," so the spec remains internally inconsistent (`.opencode/specs/02--system-spec-kit/024-compact-code-graph/spec.md:40-44`; `.claude/settings.local.json:17-26`; `.opencode/specs/02--system-spec-kit/024-compact-code-graph/spec.md:135-135`).
- F031: STILL_ACTIVE — the handler still writes caller-controlled `resultFile`/`comment` directly to disk, and the published tool schema still has no length bounds for those fields, so oversized input is still accepted pre-write (`.opencode/skill/system-spec-kit/mcp_server/handlers/code-graph/ccc-feedback.ts:37-45`; `.opencode/skill/system-spec-kit/mcp_server/tool-schemas.ts:713-725`).
- F032: FIXED — `session-prime.ts` no longer carries a private budget helper; it imports and uses shared `calculatePressureAdjustedBudget()` from `shared.ts` (`.opencode/skill/system-spec-kit/mcp_server/hooks/claude/session-prime.ts:10-15`; `.opencode/skill/system-spec-kit/mcp_server/hooks/claude/session-prime.ts:191-201`; `.opencode/skill/system-spec-kit/mcp_server/hooks/claude/shared.ts:112-123`).
- F033: STILL_ACTIVE — `includeTrace` is still accepted and forwarded, but neither `ContextResult` nor the handler response exposes any trace payload; the response only returns anchors/graphContext/text/metadata (`.opencode/skill/system-spec-kit/mcp_server/handlers/code-graph/context.ts:29-31`; `.opencode/skill/system-spec-kit/mcp_server/handlers/code-graph/context.ts:138-169`; `.opencode/skill/system-spec-kit/mcp_server/lib/code-graph/code-graph-context.ts:14-23`; `.opencode/skill/system-spec-kit/mcp_server/lib/code-graph/code-graph-context.ts:25-39`; `.opencode/skill/system-spec-kit/mcp_server/lib/code-graph/code-graph-context.ts:103-117`).

## Iteration Summary
- New findings: 0
- Verified fixes: 8
- Remaining active: 5 (2 partial, 3 still active)
