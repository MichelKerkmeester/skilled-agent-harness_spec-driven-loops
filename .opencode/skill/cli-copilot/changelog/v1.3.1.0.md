This update warns Copilot not to call itself through this skill. It prevents circular delegation and points people to the built-in Copilot features they already have. The goal is to make the workflow easier to follow and easier to trust.

---

#### Stop circular delegation

A Copilot session could try to delegate back into Copilot through this same skill, which adds no value and creates confusion. The main guide now states clearly that Copilot should use its native features instead of calling itself through this wrapper. The workflow is simpler and avoids unnecessary loops.

---

## Files Changed

| File | Change | Why |
| ---- | ------ | --- |
| `.opencode/skill/cli-copilot/SKILL.md` | Updated | Added a clear self-invocation guard and native-tool guidance. |

---

## Upgrade

No migration is required. This is a rule-only documentation update.
