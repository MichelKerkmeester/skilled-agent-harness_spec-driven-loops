This release shortens the cli-claude-code SKILL.md by 38% via hybrid extraction (helper bodies + Memory Handback procedure moved to two new shared references) and normalizes the playbook root filename to lowercase. Sibling cli-* skills received the same treatment in coordinated commits so the family stays consistent.

> Spec folder: N/A (cross-cutting cli-* family hygiene; no spec packet)

---

## What Changed

#### SKILL.md shortened from 657 → 409 LOC

The SKILL.md helper bodies (`score_intents`, `select_intents`, `route_*_resources`, `_task_text`, `_guard_in_skill`, `discover_markdown_resources`) are byte-for-byte identical across all 5 cli-* sibling skills. Same goes for the 7-step Memory Handback Protocol procedure. Both blocks were extracted to two new shared references under `system-spec-kit/references/cli/` (`shared_smart_router.md` + `memory_handback.md`).

The cli-claude-code SKILL.md retains its file-specific dictionaries (`INTENT_SIGNALS`, `RESOURCE_MAP`, `LOADING_LEVELS`, `UNKNOWN_FALLBACK_CHECKLIST`) inline plus a 6-line stub naming the helper call sequence and a one-line cross-reference. Verbose `ALWAYS` / `NEVER` / `ESCALATE` prose was compressed; the redundant Section 8 collapsed to a 5-line stub pointing back to Section 5.

#### Self-invocation guard hardening preserved

The earlier (commit `3f9d4ed1a`) uniform self-invocation guard hardening — top-of-file CRITICAL admonition, "You ARE Claude Code already" first bullet in Section 1, and the 3-layer detect_self_invocation pseudocode (env / process ancestry / state lockfile) — all stayed inline. Compression target was wrapper boilerplate only; all three layered checks and the `refuse(...)` call remain visible.

#### Root playbook filename normalized

Renamed `MANUAL_TESTING_PLAYBOOK.md` → `manual_testing_playbook.md` to match the lowercase canonical convention. All cross-references in scenario files updated.

---

## Files Changed

| File | What changed |
| --- | --- |
| `.opencode/skill/cli-claude-code/SKILL.md` | 657 → 409 LOC; smart-router helper bodies + Memory Handback extracted to shared refs; verbose prose compressed; Section 8 collapsed to 5-line stub |
| `.opencode/skill/cli-claude-code/manual_testing_playbook/manual_testing_playbook.md` | Renamed from MANUAL_TESTING_PLAYBOOK.md (lowercase canonical) |
| `.opencode/skill/system-spec-kit/references/cli/shared_smart_router.md` | New (146 LOC) — 6 helper functions + ALWAYS/CONDITIONAL/ON_DEMAND loading-level semantics + UNKNOWN_FALLBACK return contract; shared across all 5 cli-* siblings |
| `.opencode/skill/system-spec-kit/references/cli/memory_handback.md` | New (49 LOC) — 7-step Memory Epilogue extraction + generate-context.js handback procedure with 6 caveat paragraphs; shared across all 5 cli-* siblings |
| All cli-claude-code playbook scenarios | Bulk reference update from `MANUAL_TESTING_PLAYBOOK.md` to `manual_testing_playbook.md` |

---

## Verified

| Check | Result |
| --- | --- |
| Anchor parity | 8/8 open / 8/8 close |
| CRITICAL admonition preserved | Yes |
| "You ARE Claude Code already" bullet preserved | Yes |
| 3-layer detect_self_invocation preserved | Yes (env / ancestry / state lockfile + refuse() call) |
| Advisor scoring (`mcp__spec_kit_memory__advisor_recommend "delegate to claude code for deep reasoning"`) | top-1 unchanged, score within ±0.001 of baseline |
| `mcp__spec_kit_memory__skill_graph_validate` | PASS (22 nodes / 79 edges / 0 errors) |
| `npm build` | PASS (`tsc --build` clean) |

---

## Upgrade

No script changes required. The skill's external interface (frontmatter, Keywords HTML comment, advisor-consumed metadata) is unchanged. Agents that read the SKILL.md body see compressed but equivalent guidance plus two new shared references that can be loaded on demand for full helper-body or Memory Handback caveat detail.
