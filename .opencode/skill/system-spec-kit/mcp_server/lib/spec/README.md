---
title: "Spec Helpers"
description: "Spec-level detection helpers for runtime modules."
trigger_phrases:
  - "spec level"
  - "spec helpers"
---

# Spec Helpers

## 1. OVERVIEW

`lib/spec/` currently contains `spec-level.ts`, a small helper that infers packet documentation level from `spec.md` or sibling files without reaching into handler-only code.

## 2. RELATED

- `../parsing/README.md`
- `../../handlers/README.md`
