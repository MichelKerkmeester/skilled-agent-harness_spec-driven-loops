This release hybrid RAG Fusion Workstream A delivered as the Unified Context Engine baseline - The memory MCP search pipeline now runs tri-channel retrieval , adaptive fusion, evidence-gap detection, and diversity reranking with explicit regression coverage. It explains the main changes in simple language so the release is easier to understand.

---

## What Changed

#### Pipeline wiring completed

This release updates Pipeline wiring completed. Pipeline wiring completed: hybrid search now enables graph retrieval by default, applies intent-aware adaptive fusion, and runs co-activation plus MMR in the main flow. This matters because the release is clearer and easier to work with.

&nbsp;

#### Evidence-gap signaling added

This release updates Evidence-gap signaling added. Evidence-gap signaling added: memory search now surfaces explicit low-confidence warnings using TRM scoring from evidence gap detector. This matters because the release is clearer and easier to work with.

&nbsp;

#### Deep-mode query expansion added

This release updates Deep-mode query expansion added. Deep-mode query expansion added: query expander provides deterministic synonym variants for multi-query retrieval in deep mode. This matters because the release is clearer and easier to work with.

&nbsp;

#### Foundation modules created for 4

The team needed stronger proof that Foundation modules created for 4 worked as expected. Foundation modules created for Phase 4: pagerank and structure aware chunker were implemented and tested, but are still intentionally not wired into ingest. This matters because the team has clearer proof that the change works.

&nbsp;

#### New module suites passed

The team needed stronger proof that New module suites passed worked as expected. New module suites passed: MMR, TRM evidence-gap, query expansion, pagerank, and structure-aware chunking test suites all passed. This matters because the team has clearer proof that the change works.

&nbsp;

#### Pipeline and handler regressions passed

The team needed stronger proof that Pipeline and handler regressions passed worked as expected. Pipeline and handler regressions passed: wiring changes in hybrid search and memory search were validated, including opt-out behavior and integration pipeline checks. This matters because the team has clearer proof that the change works.

&nbsp;

#### Global stability maintained

The team needed stronger proof that Global stability maintained worked as expected. Global stability maintained: latest verification in spec evidence reports 4,546 passed, 19 skipped, 0 failed. This matters because the team has clearer proof that the change works.

&nbsp;

#### Consolidation completed

People needed clearer guidance about Consolidation completed. Consolidation completed: this folder was synchronized as the main active RAG track, with supplemental command-alignment and non-skill-graph consolidation notes indexed. This matters because the release notes and instructions are easier to read and trust.

---

## Files Changed

| File | What Changed |
| ---- | ------------ |
| `.opencode/specs/system-spec-kit/022-hybrid-rag-fusion/002-hybrid-rag-fusion/` | Spec folder referenced by this release. |
| `.opencode/skill/system-spec-kit/mcp_server/lib/search/mmr-reranker.ts` | New MMR reranker module. |
| `.opencode/skill/system-spec-kit/mcp_server/lib/search/evidence-gap-detector.ts` | New TRM evidence-gap detector. |
| `.opencode/skill/system-spec-kit/mcp_server/lib/search/query-expander.ts` | New deterministic query expansion module. |
| `.opencode/skill/system-spec-kit/mcp_server/lib/manage/pagerank.ts` | New PageRank authority module (not yet wired). |
| `.opencode/skill/system-spec-kit/scripts/lib/structure-aware-chunker.ts` | New structure-aware chunker (not yet wired). |
| `.opencode/skill/system-spec-kit/mcp_server/lib/search/hybrid-search.ts` | Graph/adaptive-fusion/co-activation/MMR wiring. |
| `.opencode/skill/system-spec-kit/mcp_server/handlers/memory-search.ts` | Evidence-gap warnings, deep query expansion, rerank default. |
| `.opencode/skill/system-spec-kit/mcp_server/tests/integration-138-pipeline.vitest.ts` | New integration coverage for production adapters. |

---

## Upgrade

- No schema migration is required (v15 SQLite unchanged) and no new runtime dependencies were introduced for the shipped path. Restart the MCP runtime to pick up new pipeline behavior. Note that PageRank ingest wiring, structure-aware ingest chunking, and empirical 120ms latency profiling remain deferred follow-up items.
