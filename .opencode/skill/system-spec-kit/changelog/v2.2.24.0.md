This release hybrid memory retrieval rollout completion and verification closure - This release documents every shipped feature from spec 136, including typed retrieval trace, artifact-aware routing, adaptive fusion, mutation ledger, telemetry, extraction/config hardening, and final deferred-test closure. It focuses on fixing important problems and explaining the results in clearer terms. The goal is to make the workflow easier to follow and easier to trust.

---

## What Changed

#### Typed retrieval trace shipped

There was a reliability problem around Typed retrieval trace shipped. Typed retrieval trace shipped: every retrieval now returns a typed ContextEnvelope with stage trace (candidate, filter, fusion, rerank, fallback), degraded-mode expected behavior, and retrieval metadata. This matters because it reduces confusing failures and makes the system more dependable.

&nbsp;

#### Artifact-aware routing shipped

This release updates Artifact-aware routing shipped. Artifact-aware routing shipped: retrieval strategy now routes across 9 artifact classes with deterministic weight tables. This matters because the release is clearer and easier to work with.

&nbsp;

#### Adaptive hybrid fusion shipped

There was a reliability problem around Adaptive hybrid fusion shipped. Adaptive hybrid fusion shipped: intent-aware fusion profiles now support fix_bug, add_feature, understand, refactor, security_audit, find_spec, and find_decision. This matters because it reduces confusing failures and makes the system more dependable.

&nbsp;

#### Dark-run comparison mode available

This release updates Dark-run comparison mode available. Dark-run comparison mode available: adaptive behavior can be compared against deterministic fusion without changing live retrieval output. This matters because the release is clearer and easier to work with.

&nbsp;

#### Feature-flag safety model retained

This release updates Feature-flag safety model retained. Feature-flag safety model retained: SPECKIT_ADAPTIVE_FUSION remains default-off and SPECKIT_EXTENDED_TELEMETRY remains default-on for controlled rollout behavior. This matters because the release is clearer and easier to work with.

&nbsp;

#### Append-only mutation ledger shipped

There was a reliability problem around Append-only mutation ledger shipped. Append-only mutation ledger shipped: create/update/delete/reindex operations are recorded through SQLite trigger-enforced immutable entries with hash-chain linkage. This matters because the release is clearer and easier to work with.

&nbsp;

#### Extended telemetry shipped

There was a reliability problem around Extended telemetry shipped. Extended telemetry shipped: retrieval now captures latency, mode, fallback, and quality-proxy dimensions across memory_search and memory_context. This matters because it reduces confusing failures and makes the system more dependable.

&nbsp;

#### Extraction and provenance hardening shipped

There was a reliability problem around Extraction and provenance hardening shipped. Extraction and provenance hardening shipped: deterministic memory_id fallback for extraction, bounded causal-seed selection, and main boost import-path normalization. This matters because it reduces confusing failures and makes the system more dependable.

&nbsp;

#### Configuration safety hardening shipped

The team needed stronger proof that Configuration safety hardening shipped worked as expected. Configuration safety hardening shipped: cognitive config parsing now uses Zod validation and working-memory eviction now enforces deterministic LRU ordering. This matters because the team has clearer proof that the change works.

&nbsp;

#### Embedding-provider architecture drift closed

The team needed stronger proof that Embedding-provider architecture drift closed worked as expected. Embedding-provider architecture drift closed: legacy lib/interfaces/embedding-provider references were removed from active test paths and aligned to shared provider expected behaviors/factory. This matters because the team has clearer proof that the change works.

&nbsp;

#### Deferred suites activated

People needed clearer guidance about Deferred suites activated. Deferred suites activated: api key validation.vitest, api validation.vitest, and lazy loading.vitest now run as active suites. This matters because the release notes and instructions are easier to read and trust.

&nbsp;

#### Targeted closure verification

People needed clearer guidance about Targeted closure verification. Targeted closure verification: 3 passed files, 15 passed tests, 0 skipped. This matters because the release notes and instructions are easier to read and trust.

&nbsp;

#### All-features regression verification

People needed clearer guidance about All-features regression verification. All-features regression verification: 142 passed files (142 total), 4415 passed tests, 19 skipped (4434 total). This matters because the release notes and instructions are easier to read and trust.

&nbsp;

#### Root and addendum docs synchronized

People needed clearer guidance about Root and addendum docs synchronized. Root and addendum docs synchronized: spec 136 root docs and package 007-documentation-alignment now reflect current totals and closure evidence. This matters because the release notes and instructions are easier to read and trust.

&nbsp;

#### CHK-336 closed

People needed clearer guidance about CHK-336 closed. CHK-336 closed: added Last Verified rows in module-count/stat tables for skill root README, MCP server README, and MCP lib README. This matters because the release notes and instructions are easier to read and trust.

&nbsp;

#### Dark-launch verification completed

This release updates Dark-launch verification completed. Dark-launch verification completed: baseline run with all new feature flags disabled preserved expected deterministic behavior (99.65% pass rate, 15 expected cognitive-feature failures). This matters because the release is clearer and easier to work with.

&nbsp;

#### Staged rollout completed

This release updates Staged rollout completed. Staged rollout completed: feature enablement progressed through 10%, 50%, and 100% gates with monotonic failure reduction (20 to 7 to 1) and GO decisions. This matters because the release is clearer and easier to work with.

&nbsp;

#### Governance packets completed

This release updates Governance packets completed. Governance packets completed: technical, data, and product approval artifacts were produced, with 13 success criteria passing and 23 requirements mapped to evidence. This matters because the release is clearer and easier to work with.

&nbsp;

#### Outcome confirmation completed

This release updates Outcome confirmation completed. Outcome confirmation completed: 14-day KPI monitoring closure recorded 12/12 measurable KPIs passing with margin, and capability truth-matrix review confirmed zero drift. This matters because the release is clearer and easier to work with.

&nbsp;

#### Wave 3 survey framework included

People needed clearer guidance about Wave 3 survey framework included. Wave 3 survey framework included: administrative user-survey framework artifacts were included in outcome confirmation documentation. This matters because the release notes and instructions are easier to read and trust.

&nbsp;

#### Skill version bump

This release updates Skill version bump. Skill version bump: SKILL updated from 2.2.23.0 to 2.2.24.0. This matters because the release is clearer and easier to work with.

---

## Files Changed

| File | What Changed |
| ---- | ------------ |
| `.opencode/specs/system-spec-kit/020-mcp-working-memory-hybrid-rag/` | Spec folder referenced by this release. |
| `.opencode/skill/system-spec-kit/mcp_server/lib/{contracts,search,storage,telemetry,extraction,cognitive,config}/**` | Retrieval, ledger, telemetry, extraction, and config hardening runtime features. |
| `.opencode/skill/system-spec-kit/mcp_server/handlers/{memory-search,memory-context,memory-save}.ts` | Handler wiring for adaptive retrieval, telemetry, and mutation logging. |
| `.opencode/skill/system-spec-kit/mcp_server/tests/{api-key-validation,api-validation,lazy-loading,embeddings}.vitest.ts` | Deferred drift closure and active coverage updates. |
| `.opencode/specs/system-spec-kit/020-mcp-working-memory-hybrid-rag/{implementation-summary,test-results,checklist}.md` | Root verification sync for full feature set and closure evidence. |
| `.opencode/specs/system-spec-kit/020-mcp-working-memory-hybrid-rag/007-documentation-alignment/*.md` | Addendum closure sync, CHK-336 closure, and status normalization. |
| `.opencode/skill/system-spec-kit/{README.md,SKILL.md}` | Last verified table updates and skill version bump. |
| `.opencode/skill/system-spec-kit/mcp_server/{README.md,lib/README.md}` | Module/stat table verification-date updates. |

---

## Upgrade

- No migration is required. Pull latest to get active coverage for the three formerly deferred suites and synchronized verification documentation.
