This release 30-agent audit bug fixes - comprehensive remediation of 34 bugs discovered by 30 parallel agents auditing Spec Kit v1.2.x. All P0/P1/P2 issues resolved. It focuses on fixing important problems and explaining the results in clearer terms. The goal is to make the workflow easier to follow and easier to trust.

---

## What Changed

#### Core Infrastructure (Agent 1)

There was a reliability problem around Core Infrastructure (Agent 1). The release fixes the issue in core infrastructure (agent 1). This matters because it reduces confusing failures and makes the system more dependable.

&nbsp;

#### Schema version mismatch

There was a reliability problem around Schema version mismatch. Updated schema version from 9 to 11 in vector index:108. This matters because it reduces confusing failures and makes the system more dependable.

&nbsp;

#### Duplicate error codes

There was a reliability problem around Duplicate error codes. QUERY_TOO_LONG changed from E040 to E042, QUERY_EMPTY from E041 to E043 (now unique). This matters because it reduces confusing failures and makes the system more dependable.

&nbsp;

#### Scoring Modules (Agent 2)

There was a reliability problem around Scoring Modules (Agent 2). The release fixes the issue in scoring modules (agent 2). This matters because it reduces confusing failures and makes the system more dependable.

&nbsp;

#### Tier weight inconsistency

There was a reliability problem around Tier weight inconsistency. Aligned folder scoring TIER_WEIGHTS with authoritative importance tiers. This matters because it reduces confusing failures and makes the system more dependable.

&nbsp;

#### Invalid tier names

There was a reliability problem around Invalid tier names. Removed non-existent "high" and "low" tiers from composite scoring multipliers. This matters because it reduces confusing failures and makes the system more dependable.

&nbsp;

#### Test alignment

There was a reliability problem around Test alignment. Updated five factor scoring.test to use valid tier names. This matters because it reduces confusing failures and makes the system more dependable.

&nbsp;

#### Extractors (Agent 3)

There was a reliability problem around Extractors (Agent 3). The release fixes the issue in extractors (agent 3). This matters because it reduces confusing failures and makes the system more dependable.

&nbsp;

#### Null pointer crashes

There was a reliability problem around Null pointer crashes. Added defensive null checks for collected_data and observations in file extractor:61-63. This matters because it reduces confusing failures and makes the system more dependable.

&nbsp;

#### Process termination

There was a reliability problem around Process termination. Replaced process.exit(1) with throw new Error() in file extractor:31 and diagram extractor:22,28. This matters because it reduces confusing failures and makes the system more dependable.

&nbsp;

#### Templates (Agents 4 and 5)

People needed clearer guidance about Templates (Agents 4 and 5). The release fixes the issue in templates (agents 4 and 5). This matters because the release notes and instructions are easier to read and trust.

&nbsp;

#### Section numbering

There was a reliability problem around Section numbering. Fixed spec section 10 to 7 (OPEN QUESTIONS). This matters because it reduces confusing failures and makes the system more dependable.

&nbsp;

#### Progressive inheritance

People needed clearer guidance about Progressive inheritance. Level 3 templates now correctly inherit L2 addendum content. This matters because the release notes and instructions are easier to read and trust.

&nbsp;

#### L3+ content removal

There was a reliability problem around L3+ content removal. Removed L3+ sections from checklist (belongs only in level_3+). This matters because it reduces confusing failures and makes the system more dependable.

&nbsp;

#### Double separators

People needed clearer guidance about Double separators. Fixed ------ to --- in L2 and L3 templates. This matters because the release notes and instructions are easier to read and trust.

&nbsp;

#### Template comments

People needed clearer guidance about Template comments. Updated template source comments to reflect inheritance chain. This matters because the release notes and instructions are easier to read and trust.

&nbsp;

#### Memory Commands (Agent 6)

There was a reliability problem around Memory Commands (Agent 6). The release fixes the issue in memory commands (agent 6). This matters because it reduces confusing failures and makes the system more dependable.

&nbsp;

#### Tool prefix standardization

There was a reliability problem around Tool prefix standardization. All memory commands now use full spec_kit_memory_ prefix. This matters because it reduces confusing failures and makes the system more dependable.

&nbsp;

#### Files updated

There was a reliability problem around Files updated. Context, manage, save, learn (MCP Matrix tables and allowed-tools). This matters because it reduces confusing failures and makes the system more dependable.

&nbsp;

#### SKILL Documentation (Agent 7)

People needed clearer guidance about SKILL Documentation (Agent 7). The release fixes the issue in skill documentation (agent 7). This matters because the release notes and instructions are easier to read and trust.

&nbsp;

#### Tool table expansion

There was a reliability problem around Tool table expansion. Added 8 missing tools (14 to 22): memory_context, task_preflight, task_postflight, memory_drift_why, memory_causal_link, memory_causal_stats, memory_causal_unlink, memory_get_learning_history. This matters because it reduces confusing failures and makes the system more dependable.

&nbsp;

#### LOC counts

People needed clearer guidance about LOC counts. Updated template line counts to match actual sizes (L1: 450, L2: 890, L3: 890, L3+: 1080). This matters because the release notes and instructions are easier to read and trust.

&nbsp;

#### Path fix

There was a reliability problem around Path fix. Corrected decision format path to validation/ subfolder. This matters because it reduces confusing failures and makes the system more dependable.

&nbsp;

#### Error Handling (Agent 8)

There was a reliability problem around Error Handling (Agent 8). The release fixes the issue in error handling (agent 8). This matters because it reduces confusing failures and makes the system more dependable.

&nbsp;

#### False positive reduction

There was a reliability problem around False positive reduction. Added hasWholeWord() and areTermsInSameContext() helpers to prediction error gate for better contradiction detection. This matters because it reduces confusing failures and makes the system more dependable.

&nbsp;

#### Error message clarity

There was a reliability problem around Error message clarity. Improved workflow:279-281 error message with actionable guidance. This matters because it reduces confusing failures and makes the system more dependable.

&nbsp;

#### Shared Utilities (Agent 9)

There was a reliability problem around Shared Utilities (Agent 9). The release fixes the issue in shared utilities (agent 9). This matters because it reduces confusing failures and makes the system more dependable.

&nbsp;

#### Deduplication logic

There was a reliability problem around Deduplication logic. Trigger extractor:448-499 now prefers longer, more specific phrases over shorter ones. This matters because it reduces confusing failures and makes the system more dependable.

&nbsp;

#### Embedding Providers (Agent 10)

There was a reliability problem around Embedding Providers (Agent 10). The release fixes the issue in embedding providers (agent 10). This matters because it reduces confusing failures and makes the system more dependable.

&nbsp;

#### Provider identification

There was a reliability problem around Provider identification. Added getProviderName() method to all 3 providers. This matters because it reduces confusing failures and makes the system more dependable.

&nbsp;

#### Documentation accuracy

People needed clearer guidance about Documentation accuracy. Fixed README API documentation to match actual process_with_retry() parameters. This matters because the release notes and instructions are easier to read and trust.

&nbsp;

#### Fixed

There was a reliability problem around Fixed. All fixes independently verified: schema version 11 confirmed at vector index:108, error codes E042/E043 confirmed unique at core:40-41, zero process.exit() calls in extractors, all 3 providers have getProviderName() method, memory commands use full tool prefixes. This matters because it reduces confusing failures and makes the system more dependable.

---

## Files Changed

| File | What Changed |
| ---- | ------------ |
| `Audit Spec: specs/003-memory-and-spec-kit/084-speckit-30-agent-audit/` | Updated in this release. |
| `Fix Spec: specs/003-memory-and-spec-kit/085-audit-fixes/` | Updated in this release. |
| `Completion: 34/34 bugs fixed (100%)` | Updated in this release. |
