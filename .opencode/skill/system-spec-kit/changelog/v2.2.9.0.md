This release agent Routing Documentation Update - Updated SKILL and README to document agent routing changes from spec 014 (Command Agent Utilization Audit). Added Agent Dispatch table, debug delegation exception, and :with-research / :auto-debug mode suffixes. It focuses on fixing important problems and explaining the results in clearer terms. The goal is to make the workflow easier to follow and easier to trust.

---

## What Changed

#### Agent Exclusivity exceptions

There was a reliability problem around Agent Exclusivity exceptions. Agent Exclusivity exceptions: Added debug delegation to @debug agent only (alongside existing @handover and @research exceptions). This matters because it reduces confusing failures and makes the system more dependable.

&nbsp;

#### Agent Dispatch in Commands

This release updates Agent Dispatch in Commands. Agent Dispatch in Commands: New subsection documenting all agent routing in spec_kit commands:. This matters because the release is clearer and easier to work with.

&nbsp;

#### @debug

There was a reliability problem around @debug. @debug: implement/complete at failure >= 3. This matters because it reduces confusing failures and makes the system more dependable.

&nbsp;

#### @review

There was a reliability problem around @review. @review: implement/complete dual-phase (Mode 2 Pre-Commit + Mode 4 Gate Validation), debug post-fix (advisory). This matters because it reduces confusing failures and makes the system more dependable.

&nbsp;

#### @research

This release updates @research. @research: plan at confidence < 60%. This matters because the release is clearer and easier to work with.

&nbsp;

#### @handover

This release updates @handover. @handover: plan/implement/complete/research at session end. This matters because the release is clearer and easier to work with.

&nbsp;

#### Mode Suffixes table expanded

People needed clearer guidance about Mode Suffixes table expanded. Mode Suffixes table expanded: Added :with-research and :auto-debug flags with "Applies To" column showing /spec_kit:complete exclusivity. This matters because the release notes and instructions are easier to read and trust.

&nbsp;

#### Recent Changes section

People needed clearer guidance about Recent Changes section. Recent Changes section: Added spec 014 agent routing compliance summary. This matters because the release notes and instructions are easier to read and trust.

---

## Files Changed

| File | What Changed |
| ---- | ------------ |
| `.claude/specs/system-spec-kit/z_archive/002-agent-routing-doc-update/` | Spec folder referenced by this release. |
| `.opencode/skill/system-spec-kit/SKILL.md - Agent Exclusivity + Agent Dispatch subsection` | Updated in this release. |
| `.opencode/skill/system-spec-kit/README.md - Mode Suffixes + Recent Changes` | Updated in this release. |

---

## Upgrade

- No action required. Pull latest to get updated documentation.
