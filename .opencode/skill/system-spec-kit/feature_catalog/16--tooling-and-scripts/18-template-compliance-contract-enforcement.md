---
title: "Template Compliance Contract Enforcement"
description: "3-layer defense-in-depth system ensuring all spec folder documents generated by AI agents pass structural validation on first write. Layer 1 injects compact anchor-to-H2 contracts into @speckit agent definitions. Layer 2 adds post-write validation protocol with fix loops. Layer 3 installs pre-commit hook in block mode."
---

# Template Compliance Contract Enforcement

## 1. OVERVIEW

Template Compliance Contract Enforcement is a 3-layer defense-in-depth system that ensures all spec folder documents generated by AI agents pass structural validation on first write.

The problem: AI agents writing spec folder markdown files frequently produce documents that fail structural validation — wrong heading levels, missing anchors, skipped required sections. Fixing these after the fact wastes cycles and clutters commit history.

The solution uses three complementary layers:

1. **Layer 1 — Agent Knowledge (Prevention):** A compact structural contract is embedded directly in all four CLI `@speckit` agent definitions. Before an agent writes any spec folder document, it already knows the exact heading structure, required anchors, and content minimums for each documentation level. The canonical source of truth is a shared reference file; each agent definition includes a 49-line compact version.

2. **Layer 2 — Post-Write Validation (Detection + Repair):** Immediately after writing any spec folder `.md` file, the agent runs `validate.sh --strict` against the spec folder. Exit codes are parsed (0 = pass, 1 = warnings, 2 = errors). On failure, the agent enters a fix loop of up to 3 attempts before escalating.

3. **Layer 3 — Pre-Commit Gate (Enforcement):** A git pre-commit hook runs a 6-rule fast validation subset on all staged spec folder changes. Configured in block mode via `.speckit-enforce.yaml`, it prevents structurally non-compliant documents from entering the repository. A `created_after` date gate enables progressive rollout to new folders only.

Together, the three layers mean agents know structure before writing, validate after writing, and the commit gate catches anything missed.

---

## 2. CURRENT REALITY

The system comprises 6 components spanning references, agent definitions, configuration, and scripts:

**1. Shared reference file (`references/template-compliance-contract.md`):** Canonical source of truth for structural contracts at all documentation levels (L1, L2, L3, L3+). Generated from `template-structure.js loadTemplateContract()` output. Defines the exact heading hierarchy, required anchors, and enforcement rules that all agents must follow. Versioned at 1.0.0.

**2. Compact structural contract in 4 CLI @speckit agent definitions:** A 49-line compact version of the structural contract is embedded in each of the four `@speckit` agent definitions — Claude Code (`.claude/agents/speckit.md`), Copilot (`.opencode/agent/speckit.md`), ChatGPT (`.opencode/agent/chatgpt/speckit.md`), and Codex (`.codex/agents/speckit.toml`). This gives every agent pre-write knowledge of anchor-to-H2 mappings for each document type at each level.

**3. Content minimums table (SECTION_COUNTS thresholds per level):** The contract defines minimum section counts per document per level. These thresholds are enforced both by the embedded agent contracts (Layer 1 prevention) and by the validation engine (Layer 2/3 detection). Agents reference these minimums when generating content to ensure documents are substantive, not just structurally correct.

**4. Post-Write Validation Protocol:** After writing any spec folder `.md` file, the agent must run `validate.sh --strict` against the spec folder. Exit code semantics: 0 = pass, 1 = warnings (acceptable), 2 = errors (must fix). On exit 2, the agent reads the error output, applies targeted fixes, and re-validates — up to 3 attempts maximum. If validation still fails after 3 attempts, the agent halts and reports the failure.

**5. `.speckit-enforce.yaml` enforcement mode set to `block`:** The configuration file at the repository root controls pre-commit hook behavior. Mode is set to `block` (errors prevent commit, warnings pass through). The `created_after` date gate (`2026-03-22`) limits enforcement to spec folders created on or after that date, enabling progressive rollout without breaking legacy folders. A separate `new_folder_mode` field controls the mode applied to newly created folders.

**6. Pre-commit hook symlink at `.git/hooks/pre-commit`:** The git pre-commit hook is installed as a symlink pointing to `.opencode/skill/system-spec-kit/scripts/spec/pre-commit-spec-validate.sh`. This hook runs automatically on every `git commit` that includes staged spec folder changes, executing the 6-rule fast validation subset defined in `.speckit-enforce.yaml`.

---

## 3. SOURCE FILES

### Implementation

| File | Layer | Role |
|------|-------|------|
| `.opencode/skill/system-spec-kit/references/template-compliance-contract.md` | Reference | Canonical structural contract for L1-L3+ |
| `.claude/agents/speckit.md` | Agent Def | Claude Code @speckit with embedded contract |
| `.opencode/agent/speckit.md` | Agent Def | Copilot @speckit with embedded contract |
| `.opencode/agent/chatgpt/speckit.md` | Agent Def | ChatGPT @speckit with embedded contract |
| `.codex/agents/speckit.toml` | Agent Def | Codex @speckit with embedded contract |
| `.speckit-enforce.yaml` | Config | Enforcement mode (block) and rule subset |
| `.opencode/skill/system-spec-kit/scripts/spec/pre-commit-spec-validate.sh` | Hook | Pre-commit validation gate (Layer 3) |
| `.opencode/skill/system-spec-kit/scripts/utils/template-structure.js` | Engine | Contract extraction CLI |

---

## 4. SOURCE METADATA

- Group: Tooling and scripts
- Source feature title: Template Compliance Contract Enforcement
- Current reality source: direct implementation audit
