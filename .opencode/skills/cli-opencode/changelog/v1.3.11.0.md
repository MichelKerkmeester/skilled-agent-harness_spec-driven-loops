The canonical CLI prompt quality card moved out of sk-prompt and into the sk-prompt-models hub, so this skill no longer points at a file that left its old home. We repointed this skill's references to the new hub location so every link keeps resolving and sk-prompt stays a forkable generic engine. This is a mechanical reference repoint with no change to dispatch behavior.

> Spec folder: `.opencode/specs/skilled-agent-orchestration/130-prompt-knowledge-layering/013-card-relocation-and-guard/` (Level 1)

---

## What Changed

#### Architecture

- **Card pointer repointed** -- the canonical cli_prompt_quality_card.md moved out of sk-prompt to the sk-prompt-models hub, we repointed this skill's references to the new path, so the links keep resolving and sk-prompt stays a forkable generic engine.

## Files Changed

| File | What changed |
| ---- | ------------ |
| `SKILL.md` | Card reference repointed to the hub and version bump |
| `assets/prompt_quality_card.md` | Canonical-card pointer repointed to the hub |

## Upgrade

No migration required.
