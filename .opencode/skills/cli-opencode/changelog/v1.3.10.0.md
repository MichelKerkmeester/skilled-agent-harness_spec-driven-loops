# cli-opencode v1.3.10.0 — align card + references to sk-doc templates

**Released**: 2026-06-03
**Status**: Doc alignment. No dispatch-behavior change.

---

## Summary

Aligned cli-opencode's prompt-quality card and three references to the sk-doc asset/reference templates, and scrubbed ephemeral spec-phase pointers from the references:

- `assets/prompt_quality_card.md` → asset template: added `## 1. OVERVIEW` (Purpose + Usage), ALL-CAPS numbered sections, `## N. RELATED RESOURCES` last. Still delegates the framework + CLEAR tables to the canonical card (not inlined).
- `references/{permissions-matrix,destructive_scope_violations,context-budget}.md` → reference template: `## 1. OVERVIEW` (Purpose + When to Use + Core Principle) first, ALL-CAPS numbered sections; ephemeral spec/packet/phase pointers (ADR ids, `Phase 003`, spec-folder paths) rewritten to durable wording.

---

## What Changed

| File / Path | Change |
|---|---|
| `assets/prompt_quality_card.md` | Asset-template structure (OVERVIEW + ALL-CAPS + RELATED RESOURCES last) |
| `references/permissions-matrix.md` | Reference-template structure + ephemeral-ref scrub |
| `references/destructive_scope_violations.md` | Reference-template structure + ephemeral-ref scrub |
| `references/context-budget.md` | Reference-template structure + ephemeral-ref scrub |
| `SKILL.md` | Version 1.3.10.0 |

---

## Verification

| Check | Result |
|---|---|
| card-sync guard green (no table inlined) | Verified |
| All four docs open with `## 1. OVERVIEW` | Verified |
| No ephemeral spec/phase refs remain | Verified |

---

## Spec folders referenced

- `.opencode/specs/skilled-agent-orchestration/130-prompt-knowledge-layering/012-cli-doc-alignment/`
