---
title: "create-flowchart v1.0.1.0 - Smart-routing conformance normalization"
version: 1.0.1.0
---

# v1.0.1.0 - Smart-routing conformance normalization

**Released:** 2026-07-12

Structural conformance pass so `SKILL.md` satisfies the canonical section contract enforced by the shared skill packager. No creation-workflow behavior changed; existing prose was preserved and only re-homed under an explicit heading.

## What Changed

- Promoted the trailing reference-pointer prose (the pointers to `assets/flowcharts/*`, the `references/README.md` route-map, and the shared `../shared/` markdown standards) out of the Success Criteria section into a dedicated `## 11. References` H2. The packager requires a REFERENCES section by matched substring; the pointer previously existed only as unlabeled prose, so the contract read it as missing.
- Bumped the `SKILL.md` frontmatter version to `1.0.1.0`.

## Notes

The `When To Use + Smart Routing` header was already contract-valid (its Smart Routing substring is space-separated), so it was left intact and not split. Section numbering stays contiguous (References appended as section 11) and no intra-file section cross-reference shifted. The packager `--check` passes and the shared document validator reports zero issues.
