---
name: create-flowchart
description: "SCAFFOLD PLACEHOLDER — sk-doc parent-hub packet. Generate + validate ASCII flowcharts (pattern library + validate_flowchart.sh)."
allowed-tools: [Read, Write, Edit, Bash, Grep, Glob]
version: 0.1.0.0
---

<!-- SCAFFOLD PLACEHOLDER: this workflow packet's body (contract, references, assets, scripts) is populated in a later phase of the sk-doc parent-hub conversion. It is NOT yet functional. No graph-metadata.json here — the single advisor identity lives at the sk-doc hub root. -->

# create-flowchart (packet placeholder)

Generate + validate ASCII flowcharts (pattern library + validate_flowchart.sh).

Registered in `../mode-registry.json` (workflowMode: `create-flowchart`) but not yet built. Conversion plan: `.opencode/specs/skilled-agent-orchestration/125-sk-doc-parent/`.
