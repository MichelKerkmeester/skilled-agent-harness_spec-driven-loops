{
  "_template": "Parent-skill hub-router.json scaffold. Replace every [bracketed] placeholder. Keep routerSignals keys bidirectionally aligned with mode-registry.json modes[].workflowMode. The canonical live example is sk-code/hub-router.json.",
  "_surfaceBundleNote": "The surfaceBundle outcome and its bundleRule are surface-axis only; a pure workflow hub keeps only single, orderedBundle, and defer (checker 5f/5g).",
  "_pathContract": "Two path kinds live in a hub and must never be conflated. A hubLoadAddress is what routerSignals[].resources hold: a packet entrypoint the hub loads (e.g. [workflow-packet-a]/SKILL.md). A leafResourceId is the canonical packet-root-relative resource id (references/... or assets/...) that the second-layer surface router (shared/references/smart-routing.md, scaffolded from parent_skill_smart_routing_template.md) maps each intent to. A raw path is converted to the canonical (workflowMode, leafResourceId) pair at exactly one boundary — the leaf-resource contract library — via a packet-qualified prefix ([packet]/references|assets/...) or an authored shared alias (leaf-aliases.json). Never strip a prefix generically and never infer a shared-tier file into a mode.",
  "_routingPrecisionNote": "Catch-all vocabulary classes (hub-identity here) belong on the DEFAULT mode only — never on specialized modes. Intent scoring is binary (a mode gets its full weight if ANY of its classes matches, else 0) and selection keeps every intent within routerPolicy.ambiguityDelta of the top score, so a class shared across modes makes them all co-fire on hub-generic words: over-emission that fails the route-gold gate. Each specialized mode must fire only on its own vocabulary; hub-generic words then route to the default (or defer). To make one specific signal win alone over the default, give it a weight gap wider than the ambiguity delta rather than a shared class.",
  "skill": "[parent-skill-name]",
  "version": "1.0.0.0",
  "routerPolicy": {
    "defaultMode": "[mode-a]",
    "ambiguityDelta": 1,
    "tieBreak": ["[mode-a]", "[mode-b]", "[mode-c]", "[mode-d]", "[surface-name]", "[transport-name]"],
    "outcomes": {
      "single": "one dominant workflow intent routes to one workflow mode",
      "orderedBundle": "clearly separate workflow intents route to an ordered workflow mode list",
      "surfaceBundle": "CONDITIONAL (declare only if this hub has packetKind:surface packets; delete for a pure workflow hub) — one workflow mode primary plus zero-or-more read-only surface packets as evidence, workflow ordered first",
      "defer": "unclear or contradictory intent asks for disambiguation"
    },
    "defaultResource": ["[shared-or-workflow-resource].md"],
    "bundleRules": [
      {
        "_example": "Optional surfaceBundle rule: DELETE for a pure workflow hub because includeSurfaces names a surface mode that a workflow-only hub lacks (checker 5f). When whenPrimary routes, attach includeSurfaces as read-only evidence. Every named mode must be a real mode-registry.json entry.",
        "name": "[mode-a]-with-surface-evidence",
        "whenPrimary": "[mode-a]",
        "includeSurfaces": ["[surface-name]"],
        "outcome": "surfaceBundle"
      },
      {
        "_example": "Optional orderedBundle rule: when whenAll workflow modes co-occur, emit them as one ordered bundle.",
        "name": "[mode-a]-then-[mode-b]",
        "whenAll": ["[mode-a]", "[mode-b]"],
        "outcome": "orderedBundle"
      }
    ]
  },
  "routerSignals": {
    "[mode-a]": {
      "_note": "This is defaultMode; it alone carries hub-identity so hub-generic words route here. Do NOT add hub-identity to the specialized modes below (see _routingPrecisionNote).",
      "weight": 4,
      "classes": ["[mode-a]-aliases", "[mode-a]-actions", "hub-identity"],
      "resources": ["[workflow-packet-a]/SKILL.md"]
    },
    "[mode-b]": {
      "weight": 4,
      "classes": ["[mode-b]-aliases", "[mode-b]-evidence"],
      "resources": ["[workflow-packet-b]/SKILL.md"]
    },
    "[mode-c]": {
      "weight": 3,
      "classes": ["[mode-c]-aliases"],
      "resources": ["[workflow-packet-c]/SKILL.md"]
    },
    "[mode-d]": {
      "_example": "Command-bridge workflow signal: routed by its command while remaining a registered workflow mode.",
      "weight": 2,
      "classes": ["[mode-d]-aliases"],
      "resources": ["[workflow-packet-d]/SKILL.md"]
    },
    "[surface-name]": {
      "_example": "Surface packet signal: normal router entry, read-only resources, metadata route in mode-registry.json.",
      "weight": 2,
      "classes": ["[surface-name]-aliases", "[surface-name]-runtime"],
      "resources": ["[surface-name]/SKILL.md", "[surface-name]/references/[reference].md"]
    },
    "[transport-name]": {
      "_example": "Transport packet signal: metadata-routed bridge to an external CLI/MCP surface.",
      "weight": 1,
      "classes": ["[transport-name]-aliases", "[transport-name]-runtime"],
      "resources": ["[transport-name]/SKILL.md"]
    }
  },
  "vocabularyClasses": {
    "hub-identity": {
      "keywords": ["[parent-skill-name]", "mode-registry", "hub-router", "workflowmode", "packetkind", "[domain keyword]"]
    },
    "[mode-a]-aliases": {
      "keywords": ["[mode-a]", "[workflow phrase]", "[natural-language alias]"]
    },
    "[mode-a]-actions": {
      "keywords": ["[action verb]", "[workflow action phrase]"]
    },
    "[mode-b]-aliases": {
      "keywords": ["[mode-b]", "[workflow phrase]", "[natural-language alias]"]
    },
    "[mode-b]-evidence": {
      "keywords": ["[evidence phrase]", "[verification phrase]"]
    },
    "[mode-c]-aliases": {
      "keywords": ["[mode-c]", "[workflow phrase]", "[natural-language alias]"]
    },
    "[mode-d]-aliases": {
      "keywords": ["[mode-d]", "[workflow phrase]", "[natural-language alias]"]
    },
    "[surface-name]-aliases": {
      "keywords": ["[surface-name]", "[surface phrase]", "[surface natural-language alias]"]
    },
    "[surface-name]-runtime": {
      "keywords": ["[surface runtime keyword]", "[surface framework keyword]", "[surface evidence keyword]"]
    },
    "[transport-name]-aliases": {
      "keywords": ["[transport-name]", "[transport phrase]", "[transport natural-language alias]"]
    },
    "[transport-name]-runtime": {
      "keywords": ["[transport runtime keyword]", "[external tool keyword]", "[CLI/MCP bridge keyword]"]
    }
  }
}
