# create-skill

SCAFFOLD PLACEHOLDER — nested workflow packet of the `sk-doc` parent hub.

Scaffold OpenCode skills (+ parent hubs via the create-skill-parent mode). Owns skill + parent_skill templates and init_skill/package_skill scripts.

Body populated in a later phase; see the 125-sk-doc-parent spec folder.
