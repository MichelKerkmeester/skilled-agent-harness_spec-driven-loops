deep-review's reference library is now grouped into convergence, state, and protocol subfolders, and the SKILL.md dropped back under the 500-line cap by moving two detailed contracts into references where they belong.

> Spec folder: `.opencode/specs/skilled-agent-orchestration/116-deep-skill-evolution/008-deep-skill-doc-evolution` (Level 2)

---

## What Changed

#### Documentation Structure

- **Grouped references/ into convergence/, state/, and protocol/** -- the seven reference files sat in one flat directory. They now live under three topic subfolders. The smart router discovers references recursively so routing is unchanged, and the hardcoded reducer-schema test paths moved in lockstep with the files.
- **Relocated two detailed contracts out of SKILL.md** -- the Executor Selection Contract (invariants, failure modes, config surface, code-graph readiness surface) moved to `references/protocol/executor_selection.md`, and the finding-deduplication rules moved to `references/state/finding_deduplication.md`. SKILL.md keeps a one-line pointer to each. This brought SKILL.md from 548 to 489 lines, back under the 500-line cap, while the full detail stays one click away.
- **Updated the README structure tree** -- it now shows the three reference subfolders and the two relocated files.
