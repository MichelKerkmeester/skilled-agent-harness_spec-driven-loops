# v1.10.1.0: Reference And Asset Alignment

## 1. OVERVIEW

This release aligns `deep-review` with the shared deep-skill reference model by adding focused convergence and state-output references. The loop behavior is unchanged: `/deep:review` still owns state, dispatch, reducer updates, convergence checks, and synthesis.

> Spec folder: `.opencode/specs/skilled-agent-orchestration/116-deep-skill-evolution/000-release-cleanup/001-deep-loop-runtime/003-reference-asset-alignment/` (Level 3)

---

## What Changed

#### Template Alignment

- Added the missing focused reference family that mirrors the deep-skill shape without importing research or council vocabulary.
- Kept `references/convergence.md` and `references/state_format.md` as hubs while adding narrower files for operator navigation.

#### Reference Changes

- Added `references/convergence_signals.md` for severity-weighted STOP signals, P0 override, legal stop, and sibling threshold contrast.
- Added `references/state_outputs.md` for packet outputs, iteration markdown, dashboard, final report, and resource-map behavior.
- Added `references/state_reducer_registry.md` for reducer ownership, registry semantics, fail-closed behavior, and reconstruction.

#### Asset Changes

- No new review assets were required. Existing config, strategy, dashboard, prompt-pack, review contract, and runtime capability assets remain canonical.

#### Router And README Updates

- Version bumped from `1.10.0.0` to `1.10.1.0`.
- `SKILL.md` routing now loads the focused convergence/state references for setup, iteration, convergence, synthesis, stuck, and on-demand flows.
- README structure and related documents now list the focused references.

No runtime YAML, reducer, script, command, or agent behavior changed.
