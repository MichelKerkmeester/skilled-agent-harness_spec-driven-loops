This release completes the 118 FULL_ISOLATE_NO_MCP migration for deep-review. The review loop no longer depends on deep-loop runtime code living under the system-spec-kit MCP server. Shared runtime libraries, coverage-graph ownership, direct script entry points, SQLite storage, and runtime tests now live in the peer `deep-loop-runtime` skill, so deep-review consumes a clearer boundary without carrying a deleted MCP tool surface forward.

> Spec folder: `.opencode/specs/skilled-agent-orchestration/116-deep-skill-evolution/003-deep-loop-runtime/009-verification-changelog-closeout/` (Level 2)

---

## Architecture

#### Runtime dependency moved

- **Deep-loop runtime boundary** -- Before this arc, deep-review's loop infrastructure lived beside the spec-memory MCP server and exposed four `deep_loop_graph_*` MCP tools. The 118 arc moved the shared runtime into `.opencode/skills/deep-loop-runtime/`, replaced those MCP calls with direct `.cjs` script invocations, updated workflow YAMLs, and split tests by runtime responsibility. This matters because the review workflow now depends on a peer runtime skill rather than an MCP-server-private implementation detail.

#### Historical version repair

- **Clean minor version** -- Phase H of 116/008 bumped `deep-review` from `1.3.2.0` to `1.3.3.0`, but the existing `v1.3.3.0.md` from May 7 described unrelated feature-catalog work. The 118 closeout uses `v1.4.0.0` for the dependency relocation so the version history has a clean marker for the architecture change.

#### Phase impact

- **118 phase spine** -- Phase 001 created the peer runtime skill scaffold. Phases 002 through 005 moved 13 runtime files, added four direct scripts, relocated SQLite ownership, removed MCP handlers and schema entries, and cut workflow YAMLs over to scripts. Phase 006 updated `/doctor` and system-code-graph collateral. Phase 007 split tests by responsibility. Phase 008 verifies the full sweep, updates changelogs, records the deferred 116 resource map, and closes the parent packet.

## Files Changed

| File | What changed |
| ---- | ------------ |
| `.opencode/skills/deep-loop-workflows/review/SKILL.md` | Frontmatter version bumped from `1.3.3.0` to `1.4.0.0`. |
| `.opencode/skills/deep-loop-workflows/review/changelog/v1.4.0.0.md` | Replaced stale May 7 feature-catalog content with the 118 architecture-change release note. |
| `.opencode/skills/deep-loop-runtime/` | Peer runtime dependency now owns shared libraries, scripts, storage, and tests consumed by deep-review and deep-research. |
| `.opencode/commands/deep/assets/deep_review_auto.yaml` | Uses direct runtime script invocation instead of removed deep-loop MCP tools. |
| `.opencode/commands/deep/assets/deep_review_confirm.yaml` | Mirrors the auto workflow cutover. |

## Upgrade

No migration is required for existing deep-review users. Workflow behavior is intended to remain the same, but the runtime dependency path changed from the system-spec-kit MCP server internals to the peer `deep-loop-runtime` skill.

The 118 arc has a visible committed spine through phase 007: `bd77886d0a`, `954702a8f4`, `107c522599`, `e590c12e19`, and `be2e777a4f`. The phase-008 commit handoff records the broader closeout span as `bd77886d0a..<this commit>` per the 118 closeout packet.
