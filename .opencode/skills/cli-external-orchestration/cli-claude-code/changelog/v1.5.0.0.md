# cli-claude-code v1.5.0.0

## Changed

- Rewrote the skill README purpose-first on the refined skill README template. The README now opens with a one-line pitch and a problem-first OVERVIEW, carries the agent roster as a capability table, documents the dispatch lifecycle, the self-invocation guard, auth pre-flight and the Memory Handback, and closes with troubleshooting, FAQ, verification and related documents. The frontmatter version moved from 1.1.0.20 to 1.5.0.0, re-aligning the README with the changelog top.
- Corrected the sibling boundaries table. The old README listed `cli-opencode` twice. The rewrite names all six cli-X siblings once, with the provider each one dispatches to, matching the hub README roster.

## Not Changed

- Dispatch behavior. The default envelope (`claude -p` with `claude-sonnet-4-6` and `--output-format text`), the deep-reasoning override (`claude-opus-4-6 --effort high`), the self-invocation guard, the auth pre-flight and the Memory Handback procedure are preserved from the prior README.
- The agent roster. All 13 `.claude/agents/*.md` definitions and their purposes are carried into the new capability table.
- No `SKILL.md` content, template asset, other skill README, vault file or runtime data moved.
