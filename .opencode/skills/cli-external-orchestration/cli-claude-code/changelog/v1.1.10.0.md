This release aligns cli-claude-code's prompt-quality card to the sk-doc asset template so it matches the structure used across the other CLI skills. No dispatch behavior changed.

> Spec folder: `.opencode/specs/skilled-agent-orchestration/130-prompt-knowledge-layering/012-cli-doc-alignment/` (Level 1)

---

## What Changed

#### Documentation

- **Prompt quality card aligned** -- `assets/prompt-quality-card.md` did not follow the sk-doc asset template. We added `## 1. OVERVIEW` (Purpose plus Usage), recased and renumbered to ALL-CAPS sections (Shared Layer, Model Overrides, Delegation / Precedence, Failure Patterns) and put `## N. RELATED RESOURCES` last, while still delegating the framework plus CLEAR tables to the canonical card (not inlined) and leaving the canonical-card pointer unchanged. The card-sync guard stays green with no table inlined and the card opens with `## 1. OVERVIEW`, verified.

## Files Changed

| File | What changed |
| ---- | ------------ |
| `assets/prompt-quality-card.md` | Asset-template structure (OVERVIEW plus ALL-CAPS plus RELATED RESOURCES last) |
| `SKILL.md` | Version 1.1.10.0 |

## Upgrade

No migration required.
