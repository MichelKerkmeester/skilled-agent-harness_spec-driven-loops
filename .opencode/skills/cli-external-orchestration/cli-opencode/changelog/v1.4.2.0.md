The opencode-go gateway serves GLM 5.3 live (`opencode models opencode-go` lists `opencode-go/glm-5.3`, confirmed 2026-08-14), but `references/providers-and-models.md` documented only DeepSeek V4 Flash and Qwen 3.8 Max under that gateway. This entry documents `opencode-go/glm-5.3` so the catalog matches what the gateway actually offers.

This is a **documentation-only** change: cli-opencode has no code-enforced model allowlist (`--model provider/model-id` is free-form), so `opencode-go/glm-5.3` was already dispatchable — the gap was purely in the catalog. The id was **list-verified only, not dispatch-tested** (operator decision).

---

#### GLM 5.3 documented under opencode-go

`references/providers-and-models.md` §2 adds an `opencode-go/glm-5.3` row (Z.AI GLM 5.3 via the Go gateway) and updates the gateway's family line from "DeepSeek and Qwen" to "DeepSeek, GLM, and Qwen". The gateway also fronts `glm-5.1` / `glm-5.2`, noted as out of this catalog's curated scope.

---

## Files Changed

| File | Change | Why |
| ---- | ------ | --- |
| `.opencode/skills/cli-external-orchestration/cli-opencode/references/providers-and-models.md` | Modified | opencode-go table adds `glm-5.3`; family line adds GLM; frontmatter version 1.0.0.0 → 1.1.0.0 |

Source: `specs/cli-external-orchestration/043-luna-max-deepseek-max-glm-roster/`
