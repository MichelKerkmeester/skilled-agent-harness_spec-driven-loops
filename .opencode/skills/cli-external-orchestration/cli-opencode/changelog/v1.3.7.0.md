# cli-opencode v1.3.7.0 — Thinned prompt_quality_card + 3-tier composition rule in SKILL.md

**Released**: 2026-06-02
**Status**: Documentation trim + cross-link alignment. No dispatch behavior change.

---

## Summary

`assets/prompt_quality_card.md` was carrying duplicated content from the canonical sk-prompt card (framework table) and sk-prompt-models hub (MiniMax/MiMo override paragraphs). This release replaces that duplication with targeted cross-links, reducing the card from 96 to 37 LOC. SKILL.md gains an explicit 3-tier prompt-composition precedence rule so dispatchers know where to look at each tier.

---

## What Changed

| File | Change | Why |
|------|--------|-----|
| `assets/prompt_quality_card.md` | Trimmed 96 → 37 LOC | Removed framework table (delegates to sk-prompt canonical card); replaced MiniMax/MiMo inline override paragraphs with a compact table cross-linking sk-prompt-models hub profiles |
| `SKILL.md` | Added 3-tier prompt-composition precedence rule | Establishes clear lookup order: (1) per-model hub profile via sk-prompt-models, (2) framework guidance via sk-prompt card, (3) skill-local quality card; includes hub cross-link |

## Details

**`assets/prompt_quality_card.md`**
- Framework comparison table removed — duplicated sk-prompt's canonical card; dispatchers should read that card directly.
- Inline MiniMax and MiMo override paragraphs replaced with a two-row table: model slug → framework → hub profile link. No information lost; source of truth moved to sk-prompt-models where it is maintained.
- Card description line updated to reflect its new scope (quality checklist + per-model pointer table, not framework reference).

**`SKILL.md`**
- New section: **Prompt-Composition Precedence (3 tiers)** — defines which source wins when composing a dispatch prompt: per-model hub profile (sk-prompt-models) overrides framework guidance (sk-prompt card), which overrides the local quality card defaults. Cross-link to sk-prompt-models hub included.
