# cli-opencode v1.4.3.0

## Changed

- **Roster retirement + new default** (spec `specs/cli-external-orchestration/056-roster-drop-deepseek-api-pro-terra/`): the direct DeepSeek API provider, every DeepSeek V4 Pro entry (roster rows, cline-pass route, examples, pre-flight trees), and every GPT-5.6 Terra slug (base/fast/pro) were removed. The default dispatch is now `opencode run --model opencode-go/deepseek-v4-flash --variant max --format json --dir <repo-root>` (flash is max-tier-pinned by policy), applied consistently across SKILL.md, README, the roster, cli-reference's pre-flight decision tree (default check moved from `deepseek` to `opencode-go`), agent-delegation, integration-patterns, opencode-tools, prompt-templates, permissions-matrix, context-budget, and the destructive-scope fallback advice.
- The provider auth pre-flight one-shot script now keys the default on `OPENCODE_GO_OK`; the fallback ASK-user trees offer `opencode-go` setup instead of `opencode providers login deepseek` (the direct provider is off-roster).
- Playbooks: CO-011 (deepseek direct API) retired and its feature file deleted; CO-012 keeps flash variant coverage on the opencode-go route. CO-001/CO-014/CO-015/CO-017/CO-034 command sequences now pin the flash default to `--variant max`.
- SKILL.md frontmatter version moved from 1.4.2.0 to 1.4.3.0.

## Not Changed

- The OpenRouter three-model allowlist, cline-pass flash/GLM routes, minimax/xiaomi/openai catalogs, and the incident history in `references/destructive-scope-violations.md` (2026-05-04 narrative kept verbatim).

## Verification

- `grep -rn "deepseek-v4-pro|gpt-5.6-terra|deepseek/deepseek" cli-opencode` over non-changelog, non-benchmark files: zero hits (only intentional incident-history and retired-scenario notes remain).
- Deep-loop unit tests (`executor-config.vitest.ts`, `fanout-run.vitest.ts`, `combo-matrix.vitest.ts`): 205 passed.
