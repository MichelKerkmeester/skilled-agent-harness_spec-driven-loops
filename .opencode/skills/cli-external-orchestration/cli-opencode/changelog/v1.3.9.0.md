# cli-opencode v1.3.9.0 — Pointer-ize Tier-3, per-model card links, discovery triggers

**Released**: 2026-06-03
**Status**: Refinement — closes drift found by the 130 deep research; no dispatch-behavior change.

---

## Summary

- **Tier-3 pointer-ized (C1)** — the deep-path escalation bullet in `SKILL.md` no longer re-enumerates the trigger list (which had drifted from canonical); it points to `sk-prompt/assets/cli_prompt_quality_card.md` §"Tier 3 — Deep path".
- **Per-model card links (C6)** — `assets/prompt_quality_card.md` replaces the directory-only pointer with per-model profile links for deepseek/kimi/qwen/glm, and the intro line now names the opencode-go cluster it dispatches.
- **Discovery triggers (C7)** — `graph-metadata.json` `trigger_phrases` + `key_topics` gain deepseek/kimi/qwen/glm (each dispatched here); `qwen3.6` is no longer unreachable by name. `last_updated_at` refreshed.

---

## What Changed

| File / Path | Change | C-item |
|---|---|---|
| `SKILL.md` | Tier-3 deep-path bullet → pointer to the canonical card (no inlined enumeration) | C1 |
| `assets/prompt_quality_card.md` | Cluster row → per-model profile links; intro broadened to name the opencode-go models | C6 |
| `graph-metadata.json` | Added deepseek/kimi/qwen/glm triggers + key_topics; `last_updated_at` bumped | C7 |

---

## Verification

| Check | Result |
|---|---|
| `check-prompt-quality-card-sync.sh` CHECK 2/4 pass (pointer-only + qwen reachable) | Verified |
| `graph-metadata.json` valid JSON | Verified |

---

## Spec folders referenced

- `.opencode/specs/skilled-agent-orchestration/130-prompt-knowledge-layering/009-refine-hub-cli-connections/`
