This release aligns cli-opencode's prompt-quality card and three reference docs to the sk-doc templates and scrubs ephemeral spec-phase pointers out of them, so the docs stay consistent with the rest of the skill set and do not rot as phases churn. No dispatch behavior changed.

> Spec folder: `.opencode/specs/skilled-agent-orchestration/130-prompt-knowledge-layering/012-cli-doc-alignment/` (Level 1)

---

## What Changed

#### Documentation

- **Prompt quality card aligned** -- `assets/prompt_quality_card.md` did not follow the sk-doc asset template. We added `## 1. OVERVIEW` (Purpose plus Usage), recased and renumbered to ALL-CAPS sections and put `## N. RELATED RESOURCES` last, while still delegating the framework plus CLEAR tables to the canonical card rather than inlining them. The card-sync guard stays green with no table inlined, verified.
- **Three references aligned and scrubbed** -- `references/permissions-matrix.md`, `references/destructive_scope_violations.md` and `references/context-budget.md` did not match the reference template and carried ephemeral spec, packet and phase pointers (ADR ids, `Phase 003`, spec-folder paths) that go stale. Each now opens with `## 1. OVERVIEW` (Purpose plus When to Use plus Core Principle), uses ALL-CAPS numbered sections and has its ephemeral pointers rewritten to durable wording. All four docs were verified to open with `## 1. OVERVIEW` and to retain no ephemeral spec or phase references.

## Files Changed

| File | What changed |
| ---- | ------------ |
| `assets/prompt_quality_card.md` | Asset-template structure (OVERVIEW plus ALL-CAPS plus RELATED RESOURCES last) |
| `references/permissions-matrix.md` | Reference-template structure plus ephemeral-ref scrub |
| `references/destructive_scope_violations.md` | Reference-template structure plus ephemeral-ref scrub |
| `references/context-budget.md` | Reference-template structure plus ephemeral-ref scrub |
| `SKILL.md` | Version 1.3.10.0 |

## Upgrade

No migration required.
