This release revives cli-codex as a nested workflow packet under cli-external-orchestration. It preserves fail-closed availability and self-invocation guards while delegating execution to the existing deep-loop runtime.

> Spec folder: `.opencode/specs/skilled-agent-orchestration/134-cli-codex-revival/003-cli-codex-skill-packet/` (Level 1)

---

## What Changed

#### Runtime Contract

- **Nested mode revived** -- moved the retired skill contract into `cli-external-orchestration/cli-codex/` with nested-packet metadata rules.
- **Fail-closed routing preserved** -- `command -v codex` must succeed before dispatch, and Codex self-invocation remains prohibited.
- **Shared runtime reused** -- orchestrated execution delegates to the existing deep-loop `cli-codex` adapter instead of adding a second process path.

#### Hub Integration

- **Mode registered** -- added `cli-codex` to the hub registry and lexical router.
- **Nested paths normalized** -- operator and reference paths now resolve from the renamed hub topology.

## Files Changed

| File | What changed |
| ---- | ------------ |
| `SKILL.md` | Version 1.5.0.0, hard rules, nested paths, and runtime ownership |
| `README.md` | Nested topology and runtime reuse guidance |
| `references/`, `assets/`, `manual_testing_playbook/` | Revived retired packet resources |

## Upgrade

Route Codex requests through `cli-external-orchestration` using workflow mode `cli-codex`.
