This release condenses `SKILL.md` from 4630 to 3976 words by relocating reference-table duplication into pointers to the ALWAYS-loaded `references/cli-reference.md`. No routing behavior, rule, model, or contract changed. Every dispatch rule, the full four-model roster, the smart-router dictionaries, the self-invocation guard, and the auth pre-flight decision tree are preserved verbatim.

---

## What Changed

#### SKILL.md Condensation

- **Reference tables relocated to pointers** -- the full flag glossary, unique-capabilities table, essential-command examples, and troubleshooting table were removed from `SKILL.md` because they duplicate `references/cli-reference.md`, which is ALWAYS loaded on every invocation. `SKILL.md` now carries only the three dispatch-critical gotchas (read-only sandbox default silent no-op, explicit `service_tier="fast"`, no `--reasoning-effort` flag) plus a pointer.
- **Internal repetition removed** -- the duplicate install/auth block, the repeated per-model ceiling clause, and the duplicated sandbox and fast-mode warnings were consolidated to a single authoritative statement each.
- **Section 8 slimmed** -- the related-resources section no longer restates the Section 5 reference index; it points to it.

#### Preserved Verbatim

- All 16 ALWAYS rules, 5 NEVER rules, 4 ESCALATE conditions, and the Memory Handback protocol.
- The four-model GPT-5.5 / GPT-5.6 roster with per-model reasoning-effort ceilings, and the user-override resolution table.
- The `INTENT_SIGNALS` / `RESOURCE_MAP` / `LOADING_LEVELS` router dictionaries, the self-invocation guard, and the auth pre-flight decision tree.

## Files Changed

| File | What changed |
| ---- | ------------ |
| `SKILL.md` | Version 1.6.1.0; reference tables relocated to `cli_reference.md` pointers, internal repetition consolidated, 4630 to 3976 words |

## Upgrade

No action required. Dispatch behavior, routing, and every rule are unchanged. Full flag, sandbox, session, and troubleshooting reference now lives solely in `references/cli-reference.md` (loaded on every invocation).
