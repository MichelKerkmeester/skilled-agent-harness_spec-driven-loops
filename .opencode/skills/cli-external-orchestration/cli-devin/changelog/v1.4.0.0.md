Devin's live roster (confirmed via `devin models list`, 2026-08-14) exposes per-tier DeepSeek V4 uids and a full GPT-5.6 Luna persona. This skill's curated allowlist adds four Max-tier uids: the DeepSeek Max tiers `deepseek-v4-flash-max` and `deepseek-v4-pro-max`, and the first GPT-5.6 persona uids `gpt-5-6-luna-max` and `gpt-5-6-luna-max-priority`. The curated scope grows from four families to five (GPT-5.6 added). Devin encodes the "Fast" speed tier as the `-priority` suffix, so `gpt-5-6-luna-max-priority` is the Fast variant of Luna Max. All four were **list-verified only, not dispatch-tested** (operator decision).

---

#### DeepSeek Max tiers + GPT-5.6 Luna Max join, four families → five

`DEVIN_SUPPORTED_MODELS` in `executor-config.ts` and its hand-duplicated mirror `DEVIN_ALLOWED_MODELS` in `fanout-run.cjs` both add the four uids, sorted alphabetically: `deepseek-v4-flash-max` and `deepseek-v4-pro-max` slot into the DeepSeek block, and `gpt-5-6-luna-max` / `gpt-5-6-luna-max-priority` form a new GPT-5.6 block between GLM-5.2 and Grok. GLM-5.2, Grok (4.5 and 4.6), SWE-1.7 and the existing `deepseek-v4` / `deepseek-v4-pro` slugs are unchanged. Both arrays and their unit fixtures (`fanout-run.vitest.ts`, plus the `DEVIN_ALLOWED ≡ DEVIN_SUPPORTED` cross-check and `combo-matrix.vitest.ts` derivation) were updated together; the three touched runtime suites (190 tests) pass clean.

#### List-verified, not dispatch-tested; scope limited to the Max tier

`devin models list` on 2026-08-14 printed all four uids verbatim ("DeepSeek V4 Flash Max", "DeepSeek V4 Pro Max", "GPT-5.6 Luna Max Thinking", "GPT-5.6 Luna Max Thinking Fast"). The DeepSeek request was scoped to "max thinking levels", so only the `-max` tier is added — the `-low` / `-high` tiers Devin also exposes are left out. Per operator decision no live `devin -p --model …` dispatch was run; the code comments and docs say so.

#### Docs updated to match

`SKILL.md`, `README.md`, and `references/{cli-reference,providers-and-models}.md` move the curated-family count from four to five and add the new uids to every roster enumeration. `references/providers-and-models.md` carries the fullest account, including the `-priority`-is-Fast note and the dated verification.

---

## Files Changed

| File | Change | Why |
| ---- | ------ | --- |
| `.opencode/skills/system-deep-loop/runtime/lib/deep-loop/executor-config.ts` | Modified | `DEVIN_SUPPORTED_MODELS` adds the 4 Max-tier uids, sorted; doc comment records the 2026-08-14 list-verification, the 4→5-family scope, and the `-priority`=Fast note |
| `.opencode/skills/system-deep-loop/runtime/scripts/fanout-run.cjs` | Modified | `DEVIN_ALLOWED_MODELS` mirror kept in sync |
| `.opencode/skills/system-deep-loop/runtime/tests/unit/fanout-run.vitest.ts` | Modified | Devin allowed-model fixture adds the 4 uids |
| `.opencode/skills/cli-external-orchestration/cli-devin/SKILL.md` | Modified | Curated-family enumerations (incl. the detailed uid list) add DeepSeek Max tiers + GPT-5.6 Luna Max |
| `.opencode/skills/cli-external-orchestration/cli-devin/README.md` | Modified | Multi-model dispatch row: four families → five |
| `.opencode/skills/cli-external-orchestration/cli-devin/references/providers-and-models.md` | Modified | Table rows added (DeepSeek Max + GPT-5.6 Luna), five-family scope, `-priority`=Fast note, dated verification |
| `.opencode/skills/cli-external-orchestration/cli-devin/references/cli-reference.md` | Modified | Curated-family list (§1, `--model` row, §5 footer) → five families |
| `.opencode/skills/cli-external-orchestration/cli-cursor/*` | Modified | See `cli-cursor/changelog/v1.4.0.0.md` for the parallel Cursor-side change |
| `.opencode/skills/cli-external-orchestration/shared/references/smart-routing.md` | Modified | Devin roster mention adds GPT-5.6 Luna Max |

Source: `specs/cli-external-orchestration/043-roster-update-luna-deepseek-glm-gemini/`
