Cursor's live roster added a `cursor-grok-4.6-*` family (confirmed via `cursor-agent --list-models`, 2026-08-12) alongside the still-present `cursor-grok-4.5-*` ids — the vendor has not dropped 4.5. This skill's curated allowlist adds the full 4.6 family alongside the existing 4.5 family, growing from 10 to 18 ids. The 4.6 family carries a fourth thinking tier, `xhigh`, that 4.5 never had. Every `cursor-grok-4.6-*` id was dispatch-tested end to end before landing here, and the parameterized `cursor-grok-4.6[effort=high]` bracket was re-confirmed rejected outright, matching 4.5's behavior. Model rosters and tables across the touched docs are now sorted alphabetically rather than grouped by family.

---

#### Grok 4.6 joins Grok 4.5, allowlist 10 → 18 ids

`CURSOR_SUPPORTED_MODELS` in `executor-config.ts` and its hand-duplicated mirror `CURSOR_ALLOWED_MODELS` in `fanout-run.cjs` both add the 8 `cursor-grok-4.6-*` ids alongside the existing 6 `cursor-grok-4.5-*` ids. Composer and GLM 5.2 are unchanged. Both allowlist arrays and their unit tests (`executor-config.vitest.ts`, `fanout-run.vitest.ts`) were updated together so the enforced set and its coverage stay in sync; the full `runtime` suite (188 tests) and `tsc --noEmit` pass clean.

#### Live verification, not a documentation-only addition

Before any file changed, `cursor-agent --list-models` confirmed the 8 new 4.6 ids exist alongside the still-present 4.5 ids, and `cursor-agent -p --model cursor-grok-4.6-high "..."` / `...-xhigh` both returned a live model response from a trusted scratch workspace. `cursor-grok-4.5-high` was independently re-dispatch-tested too, confirming it still works. The parameterized-bracket rejection (`cursor-grok-4.6[effort=high]` → `Cannot use this model`, exit 1) was re-tested rather than assumed to still hold from the 4.5-era test.

#### Docs updated to match, now sorted alphabetically

`SKILL.md`, `README.md`, `references/{cli-reference,integration-patterns,providers-and-models,agent-delegation}.md`, `assets/{prompt-templates,prompt-quality-card}.md`, and `manual-testing-playbook/manual-testing-playbook.md` all move their Grok tables and allowlist counts (10 → 18) to include both versions. `references/providers-and-models.md` — the single-source catalog — carries the fullest account, including the fact that Cursor still lists 4.5 upstream. Every roster table and enumerated model list in the touched docs is now sorted alphabetically by id (or by family name, then id) instead of grouped by release order.

---

## Files Changed

| File | Change | Why |
| ---- | ------ | --- |
| `.opencode/skills/system-deep-loop/runtime/lib/deep-loop/executor-config.ts` | Modified | `CURSOR_SUPPORTED_MODELS`/`DEVIN_SUPPORTED_MODELS` add Grok 4.6 ids alongside the existing Grok 4.5 ids; lists resorted alphabetically; doc comments updated with the live-confirmation date |
| `.opencode/skills/system-deep-loop/runtime/scripts/fanout-run.cjs` | Modified | `CURSOR_ALLOWED_MODELS`/`DEVIN_ALLOWED_MODELS` mirrors kept in sync with `executor-config.ts` |
| `.opencode/skills/system-deep-loop/runtime/tests/unit/executor-config.vitest.ts` | Modified | Allowlist assertion updated to the 18-id set |
| `.opencode/skills/system-deep-loop/runtime/tests/unit/fanout-run.vitest.ts` | Modified | Allowed model fixtures updated to include both Grok 4.5 and 4.6 ids |
| `.opencode/skills/cli-external-orchestration/cli-cursor/SKILL.md` | Modified | Triggers, dispatch table and RULES section cover both Grok versions, 18-id allowlist |
| `.opencode/skills/cli-external-orchestration/cli-cursor/README.md` | Modified | Guard-rail table, troubleshooting, and FAQ cover both Grok versions |
| `.opencode/skills/cli-external-orchestration/cli-cursor/references/providers-and-models.md` | Modified | Allowlist table rewritten to the 18-id set, sorted alphabetically; live-verification note added |
| `.opencode/skills/cli-external-orchestration/cli-cursor/references/cli-reference.md` | Modified | §5 Model Selection table (alphabetized by family) and selection strategy cover both Grok versions |
| `.opencode/skills/cli-external-orchestration/cli-cursor/references/integration-patterns.md` | Modified | Decision matrix covers both Grok versions |
| `.opencode/skills/cli-external-orchestration/cli-cursor/references/agent-delegation.md` | Modified | Task-type routing table covers both Grok versions |
| `.opencode/skills/cli-external-orchestration/cli-cursor/assets/prompt-templates.md` | Modified | Flag reference covers both Grok versions, 18-id allowlist |
| `.opencode/skills/cli-external-orchestration/cli-cursor/manual-testing-playbook/manual-testing-playbook.md` | Modified | Precondition 7 (enforced model allowlist) covers both Grok versions, 18-id count |
| `.opencode/skills/cli-external-orchestration/cli-devin/*` | Modified | See `cli-devin/changelog/v1.3.0.0.md` for the parallel Devin-side change |
| `.opencode/skills/cli-external-orchestration/shared/references/smart-routing.md` | Modified | Roster mention and DEVIN routing keywords cover both Grok versions |

Source: `specs/cli-external-orchestration/036-grok-4.6-support/`
