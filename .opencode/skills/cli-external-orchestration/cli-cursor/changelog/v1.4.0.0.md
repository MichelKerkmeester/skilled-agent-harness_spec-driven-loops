Cursor's live roster exposes a GPT-5.6 Luna persona alongside Sol and Terra (confirmed via `cursor-agent --list-models`, 2026-08-14). This skill's curated allowlist adds the two Max-tier Luna ids — `gpt-5.6-luna-max` and `gpt-5.6-luna-max-fast` — growing from 18 to 20 ids. These are the first GPT-5.6 persona ids in the curated Cursor scope; only the Max tier is in-scope, and the Sol/Terra personas and the other Luna tiers stay out of scope. Unlike the Grok 4.6 addition, the two Luna ids were **list-verified only, not dispatch-tested** (operator decision), so no dispatch-test claim is made for them.

---

#### GPT-5.6 Luna Max joins the allowlist, 18 → 20 ids

`CURSOR_SUPPORTED_MODELS` in `executor-config.ts` and its hand-duplicated mirror `CURSOR_ALLOWED_MODELS` in `fanout-run.cjs` both add `gpt-5.6-luna-max` and `gpt-5.6-luna-max-fast`, sorted alphabetically (after `glm-5.2-max`). Composer, Grok (4.5 and 4.6) and GLM 5.2 are unchanged. Both allowlist arrays and their unit tests (`executor-config.vitest.ts`, `fanout-run.vitest.ts`) were updated together so the enforced set and its coverage stay in sync; the three touched runtime suites (190 tests) pass clean.

#### List-verified, not dispatch-tested

`cursor-agent --list-models` on 2026-08-14 printed both new ids verbatim ("GPT-5.6 Luna 1M Max" / "GPT-5.6 Luna Max Fast"). Per operator decision this addition stops at list-verification — no live `cursor-agent -p --model gpt-5.6-luna-max` dispatch was run — and the code comments and docs say so rather than claiming a test that did not happen. The ids are still non-fabricated: each was copied from the live listing, not inferred.

#### Docs updated to match

`SKILL.md`, `README.md`, `references/{cli-reference,integration-patterns,providers-and-models,agent-delegation}.md`, `assets/prompt-templates.md`, and `manual-testing-playbook/manual-testing-playbook.md` all move their allowlist count from 18 to 20 and, where they enumerate the curated families, add GPT-5.6 Luna Max. Two pre-existing stale "10"-count claims (left behind when packet 036 grew the roster to 18) were corrected to 20 in the same pass. `references/providers-and-models.md` — the single-source catalog — carries the fullest account and the dated verification note.

---

## Files Changed

| File | Change | Why |
| ---- | ------ | --- |
| `.opencode/skills/system-deep-loop/runtime/lib/deep-loop/executor-config.ts` | Modified | `CURSOR_SUPPORTED_MODELS` adds the 2 Luna Max ids, sorted; doc comment records the 2026-08-14 list-verification and the not-dispatch-tested caveat |
| `.opencode/skills/system-deep-loop/runtime/scripts/fanout-run.cjs` | Modified | `CURSOR_ALLOWED_MODELS` mirror kept in sync |
| `.opencode/skills/system-deep-loop/runtime/tests/unit/executor-config.vitest.ts` | Modified | Allowlist assertion updated to the 20-id set |
| `.opencode/skills/system-deep-loop/runtime/tests/unit/fanout-run.vitest.ts` | Modified | Cursor allowed-model fixture adds the 2 Luna Max ids |
| `.opencode/skills/cli-external-orchestration/cli-cursor/SKILL.md` | Modified | Count 18 → 20; dispatch guidance names GPT-5.6 Luna Max; stale "10" corrected |
| `.opencode/skills/cli-external-orchestration/cli-cursor/README.md` | Modified | Feature table, troubleshooting, FAQ: count 20, family list adds Luna Max |
| `.opencode/skills/cli-external-orchestration/cli-cursor/references/providers-and-models.md` | Modified | Rows 19–20 added, count 20, effort table + dated verification note |
| `.opencode/skills/cli-external-orchestration/cli-cursor/references/cli-reference.md` | Modified | Count 20; stale "10" corrected |
| `.opencode/skills/cli-external-orchestration/cli-cursor/references/integration-patterns.md` | Modified | Count 20; family list adds Luna Max |
| `.opencode/skills/cli-external-orchestration/cli-cursor/references/agent-delegation.md` | Modified | Count 20 |
| `.opencode/skills/cli-external-orchestration/cli-cursor/assets/prompt-templates.md` | Modified | Enforced-allowlist note: count 20, Luna ids listed |
| `.opencode/skills/cli-external-orchestration/cli-cursor/manual-testing-playbook/manual-testing-playbook.md` | Modified | Precondition 7: count 20, Luna ids listed |
| `.opencode/skills/cli-external-orchestration/cli-devin/*` | Modified | See `cli-devin/changelog/v1.4.0.0.md` for the parallel Devin-side change |
| `.opencode/skills/cli-external-orchestration/shared/references/smart-routing.md` | Modified | Devin roster mention adds GPT-5.6 Luna Max |

Source: `specs/cli-external-orchestration/043-luna-max-deepseek-max-glm-roster/`
