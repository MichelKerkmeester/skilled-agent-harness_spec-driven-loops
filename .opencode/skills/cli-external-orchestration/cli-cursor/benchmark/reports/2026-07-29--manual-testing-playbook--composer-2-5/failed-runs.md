# Failed Runs

_Derived after the fact from this run's stored record, not written at run time._

> cli-cursor · live · claude · composer-2.5 · paid

No scenario recorded a FAIL verdict across 1 scenario(s): 1 PASS. Note: the sessionStart injection's absence from the model-visible transcript (0 canary / 0 `[active_goal]` occurrences) is the documented, correct behavior for Cursor's `sessionStart` `agent_message` channel — a recorded-evidence-tier PASS, not a failure. A transcript match here would indicate an undocumented behavior change and would need escalation, not a silent tier upgrade.
