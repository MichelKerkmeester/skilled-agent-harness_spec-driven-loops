# cli-external-orchestration v1.1.0.0

## Changed

- Renamed the parent hub directory and public advisor identity from `cli-external` to `cli-external-orchestration`.
- Repointed hub metadata, advisor integration, hooks, commands, tests, and live documentation to the new identity.
- Kept the nested `cli-opencode` and `cli-claude-code` packet names and behavior unchanged.

## Verification

- Advisor routing projection drift check reports `fresh`.
- Prompt quality-card synchronization guard passes.
- Direct executor delegation resolves `cli-opencode` as the top recommendation.
