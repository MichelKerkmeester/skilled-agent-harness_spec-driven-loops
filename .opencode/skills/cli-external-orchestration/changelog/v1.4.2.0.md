# cli-external-orchestration v1.4.2.0

## Changed

- Migrated the hub's second-stage surface router from `shared/references/smart-routing.md` to a first-class root `ROUTER.md` per the two-state root-router document standard. The root `ROUTER.md` declares `router_state: active` and `skill_pointer: SKILL.md`; the machine-readable `INTENT_SIGNALS` / `RESOURCE_MAP` block is byte-identical to the legacy file (sha256 `8899785a…`), and the prose link to `hub-router.json` was rebased for the root location.
- Repointed `hub-router.json` `routerPolicy.defaultResource` from the legacy path to `ROUTER.md` (the literal legacy entry only; `mode-registry.json` stays in the fallback list). The hub release surfaces align at version 1.4.2.0.
- Updated the hub docs for the root router: `SKILL.md` (v1.4.2.0) gained the two-stage routing pointer, a Surface Router section, the `ROUTER.md` layout entry, two RESOURCE_MAP/leaf-manifest sync rules, and the surface-router reference; `README.md` (v1.4.2.0) gained the stage-two router in the routing chain and the related-documents table; `description.json` aligned to v1.4.2.0.
- Deleted the legacy `shared/references/smart-routing.md` after standalone contract validation; the parent doctor then passed, while compiled-manifest freshness remains the downstream fleet-rebuild gate; the empty `shared/` tree was removed.

## Not Changed

- Routing behavior. The hub still reads `hub-router.json` and `mode-registry.json` with the same `tieBreak` order, outcomes, and `defaultMode`; only the stage-two leaf-router location and the literal legacy default path changed.
- Every pre-existing roster entry, self-invocation guard, and per-mode dispatch contract.
- Historical changelog entries and dated benchmark reports that name the legacy path.
