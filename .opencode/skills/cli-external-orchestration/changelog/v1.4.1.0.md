# cli-external-orchestration v1.4.1.0

## Changed

- Expanded the enforced model rosters of cli-cursor and cli-devin with Gemini 3.7 Flash High (phase 004 of the 043 phase-parent; confirmed present in the live CLI listings AND dispatch-tested end-to-end on 2026-08-15):
  - **cli-cursor** adds `gemini-3.7-flash-high` — the first Gemini id in its curated allowlist (20 → 21 ids). Cursor's display name is "Gemini 3.7 Flash"; the High tier lives in the id suffix. See `cli-cursor/changelog/v1.4.1.0.md`.
  - **cli-devin** adds `gemini-3-7-flash-high` — the first Gemini uid in its curated allowlist, growing the curated scope from five families to six. See `cli-devin/changelog/v1.4.1.0.md`.
- Updated `shared/references/smart-routing.md` so the cli-devin routing description names Gemini alongside the existing families.
- Ran a roster-count honesty sweep across the touched mode docs (cli-cursor "20-id" → "21-id"; cli-devin "five families" → "six"), including amending every "Claude / Gemini / Kimi ids are out of scope" wording to "other Gemini" so the docs do not contradict the new in-scope id.

## Not Changed

- Routing behavior. The hub still reads `hub-router.json` and `mode-registry.json` with the same `tieBreak` order and `cli-opencode` as the default mode.
- Every pre-existing roster entry: DeepSeek, GLM-5.2, GPT-5.6 Luna Max, Grok 4.5/4.6, SWE-1.7, and the opencode-go GLM 5.3 catalog row are unchanged by this phase.
- The enforcement mechanism. New ids were added to both the `executor-config.ts` source-of-truth arrays and their `fanout-run.cjs` mirrors together, kept in sync by the existing guard tests.

Source: `specs/cli-external-orchestration/043-luna-max-deepseek-max-glm-roster/004-gemini-3-7-flash-high/`
