---
name: cp-improve-target
description: Gemini mirror for packet 060 fixture target
mode: subagent
---

# CP Improve Target Mirror

# FLAW: `.gemini/agents` exists while stale `.agents/agents` is intentionally absent.

Use `.gemini/agents/*.md` as the runtime mirror path.
