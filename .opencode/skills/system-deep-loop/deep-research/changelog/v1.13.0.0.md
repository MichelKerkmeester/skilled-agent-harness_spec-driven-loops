# v1.13.0.0: Reference And Asset Alignment

## 1. OVERVIEW

This release records the shared deep-skill reference and asset alignment for `deep-research`. The skill already carried the target resource family from the previous reference split: quick reference, loop protocol, convergence split, state split, prompt-pack template, runtime capability matrix, config, strategy, and dashboard assets. This version bump makes that alignment explicit and keeps operator-facing metadata in sync with `deep-ai-council` and `deep-review`.

> Spec folder: `.opencode/specs/skilled-agent-orchestration/116-deep-skill-evolution/000-release-cleanup/001-deep-loop-runtime/003-reference-asset-alignment/` (Level 3)

---

## What Changed

#### Template Alignment

- README and SKILL metadata now state that `deep-research` is the iterative investigation member of the shared deep-skill resource model.
- The existing sk-doc-shaped reference family remains unchanged in behavior and continues to route through `references/quick_reference.md`, `references/loop_protocol.md`, `references/convergence*.md`, and `references/state*.md`.

#### Reference Changes

- No new deep-research reference files were required; the split references from the prior phase already matched the target family.
- Navigation text was refreshed so operators can distinguish research novelty signals from review severity signals and council agreement signals.

#### Asset Changes

- No new runtime assets were required; existing config, strategy, dashboard, prompt-pack, and runtime capability assets remain canonical.
- Corrected the existing config template archive root to `research_archive` so it matches the current README/archive convention.

#### Router And README Updates

- Version bumped from `1.12.0.0` to `1.13.0.0`.
- README statistics and related-resource language updated for cross-skill alignment.

No runtime YAML, reducer, script, command, or agent behavior changed.
