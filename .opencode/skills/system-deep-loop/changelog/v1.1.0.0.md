# deep-loop-workflows v1.1.0.0 — Parent-Hub Canon Alignment

> Aligns the deep-loop-workflows hub with the canonical parent-skill contract while preserving its three-tier discriminator and the advisor drift-guard projection.

---

## Added

- `description.json` — hub description with the required canon fields (name, description, version, keywords) plus the seven modes and their three backend kinds.
- `hub-router.json` — the hub router the canon requires (one `routerSignals` entry per registry mode, `<mode>-aliases` vocabulary classes derived from the registry `aliases[]`, `hub-identity`, `tieBreak` covering all seven modes, resources pointing at the packet SKILL.md files). Also unblocks `parent-hub-vocab-sync`, which silently no-ops without a router.
- `manual-testing-playbook/` — operator-executable scenarios across mode routing, improvement-lane routing, advisor integration, runtime/backend discrimination, and state/convergence discipline. Every expected route derived from `mode-registry.json`.
- `benchmark/` — first Lane-C skill-benchmark baseline (router mode) plus `benchmark/README.md`.
- `mode-registry.json` `extensions` — declares the two active extensions: `runtime-loop` (the research|review|council convergence backend) and `advisor-projection` (the drift-guard-checked projection maps). These activate in-place fields; they do not relocate anything.

## Changed

- `mode-registry.json` — every mode now carries the canon core fields: `packetKind: "workflow"`, `grandfatheredFolderMismatch: false` (folder == packetSkillName for all modes), and a well-formed `toolSurface` (all seven modes are mutating loop/host orchestrators; research additionally grants WebFetch for outward investigation). The three-tier discriminator and every `advisorRouting` projection block are unchanged.
- `changelog/` — five hub-level per-mode changelog symlinks removed under the real-files-only policy (packet changelogs live in their packets; the `deep-context` symlink was dangling). Real hub changelog files only.
- Version normalized to the four-segment scheme and bumped 1.0.0.0 → 1.1.0.0 across SKILL.md, registry, router, and description.

## Verification

- Advisor drift-guard (`routing-registry-drift-guard.vitest.ts`) — 7/7 GREEN before and after; the additive canon fields do not touch the advisorRouting projection.
- `PARENT_HUB_CHECK_STRICT=1 parent-skill-check .opencode/skills/deep-loop-workflows` — canon invariants pass (3d/3f/5/7a/8a).
- `parent-hub-vocab-sync --skill deep-loop-workflows` — exit 0, no orphan aliases, no collisions.
