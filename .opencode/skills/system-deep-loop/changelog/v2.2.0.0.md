---
title: "system-deep-loop changelog v2.2.0.0"
description: "Additive model-roster expansion in the executor allowlists: cli-cursor gains GPT-5.6 Luna Max, cli-devin gains DeepSeek V4 max tiers plus GPT-5.6 Luna Max, kept in sync with the fanout-run.cjs mirrors and their vitest fixtures."
trigger_phrases:
  - "system-deep-loop v2.2.0.0"
  - "executor allowlist luna max deepseek max"
importance_tier: "normal"
contextType: "general"
version: 2.2.0.0
---

# system-deep-loop v2.2.0.0

## Changed

- Expanded two executor allowlists in `runtime/lib/deep-loop/executor-config.ts` with live-verified, additive model ids (packet 043; every id confirmed present verbatim in the live `cursor-agent --list-models` / `devin models list` output on 2026-08-14):
  - `CURSOR_SUPPORTED_MODELS` 18 → 20: adds `gpt-5.6-luna-max`, `gpt-5.6-luna-max-fast` (the first GPT-5.6 persona in the curated Cursor scope).
  - `DEVIN_SUPPORTED_MODELS` 19 → 23: adds the DeepSeek max tiers `deepseek-v4-flash-max`, `deepseek-v4-pro-max` and the GPT-5.6 persona uids `gpt-5-6-luna-max`, `gpt-5-6-luna-max-priority` (Devin's Fast suffix is `-priority`). Curated scope grows four families → five.
- Kept the hand-duplicated mirrors `CURSOR_ALLOWED_MODELS` / `DEVIN_ALLOWED_MODELS` in `runtime/scripts/fanout-run.cjs` byte-identical to the source arrays, and updated the vitest fixtures (`executor-config.vitest.ts`, `fanout-run.vitest.ts`). The `ALLOWED ≡ SUPPORTED` cross-check and `combo-matrix` derivation tests confirm parity.
- Updated the rationale doc comments to record the 2026-08-14 list-verification and, honestly, that these ids were **list-verified only, not dispatch-tested** (operator decision) — unlike the Grok entries above them.

## Not Changed

- The allowlist enforcement mechanism, sort order, and fail-closed rejection of off-list ids.
- Grok 4.5 / 4.6 entries, GLM-5.2, SWE-1.7, the Pi roster, and every other executor config surface.
- Verification: the touched unit suites pass — `vitest run executor-config.vitest.ts fanout-run.vitest.ts combo-matrix.vitest.ts` → 3 files, 190 tests, all green.

Source: `specs/cli-external-orchestration/043-luna-max-deepseek-max-glm-roster/`
