---
title: "system-deep-loop changelog v2.2.3.0"
description: "Promote the second-stage surface router to a first-class root ROUTER.md while preserving its machine policy and stage-one fallback behavior."
trigger_phrases:
  - "system-deep-loop v2.2.3.0"
  - "system deep loop root router"
importance_tier: "normal"
contextType: "general"
version: 2.2.3.0
---

# system-deep-loop v2.2.3.0

## Changed

- Migrated the second-stage surface router from `shared/references/smart-routing.md` to root `ROUTER.md`.
- Added the explicit `router_state: active` and `skill_pointer: SKILL.md` frontmatter contract.
- Preserved the machine-readable Python fence byte-for-byte at SHA-256 `f9f410c1e3d0e70c7db23d952041811dab95be478237fdd2bf5e3523e7f9d274`.
- Repointed only the literal legacy entry in `hub-router.json` `routerPolicy.defaultResource` to `ROUTER.md`; `mode-registry.json` remains the companion fallback resource.
- Updated the root `SKILL.md`, README, description, feature catalog, and manual-testing index to reference the root router.
- Removed the legacy router after standalone root-contract validation. The parent doctor passes; compiled policy freshness remains gated on the fleet rebuild and promoted sync.

## Not Changed

- The seven workflow modes, routing keywords, intent/resource key sets, tie-break order, outcomes, and default mode.
- Frozen replay and scorer bytes.
- Advisor identity or typed leaf ownership: root `ROUTER.md` is a control-plane document, not a leaf or graph identity.
- Historical changelog and benchmark evidence that names the former path.
