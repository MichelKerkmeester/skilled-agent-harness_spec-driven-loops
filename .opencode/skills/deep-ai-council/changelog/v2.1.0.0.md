---
title: "v2.1.0.0: Release Cleanup"
description: "Release-cleanup pass (packet 131/000/004): sk-doc template + HVR alignment, README rewrite, root FEATURE_CATALOG.md, surgical fixes. Smart Router §3 preserved."
---

# v2.1.0.0: Release Cleanup

Date: 2026-05-24

Release-cleanup pass against current sk-doc templates and HVR standards, run as packet `116-deep-skill-evolution/000-release-cleanup/004-deep-ai-council`. The README is rewritten to a marketing-leaning HVR voice, the missing root feature catalog is authored and surgical alignment fixes close the audit findings. Smart Router (§3) stays untouched. No backward-incompatible changes.

## What Changed

#### Added

- `feature_catalog/FEATURE_CATALOG.md` root inventory: 32 per-feature capability summaries across 9 categories with links and scenario IDs, closing the missing-root-catalog gap (AF-0002).
- Release-cleanup spec folder at `.opencode/specs/skilled-agent-orchestration/116-deep-skill-evolution/000-release-cleanup/004-deep-ai-council` with 4 JSON schemas and `resource-map.yaml`.

#### Changed

- `README.md` full rewrite per `skill_readme_template.md`: HVR-clean, marketing-leaning voice anchored at ~70% of the root README intensity, structural pattern from `system-spec-kit/README.md`.
- `SKILL.md` version frontmatter `2.0.0.0` -> `2.1.0.0`.
- All 32 per-feature `Canonical catalog source` back-links now point to `FEATURE_CATALOG.md` instead of the playbook (AF-0006).

#### Fixed

- `SKILL.md` §7 playbook-coverage count corrected from "18 scenarios across 7 categories" to "32 scenarios across 9 categories" (AF-0001).
- `SKILL.md` §7 removed the stale "Phase 001 spec folder" phase reference (AF-0004, partial).
- `references/scoring_rubric.md` Integration-dimension top-score anchor reworded to "no friction" for HVR compliance (AF-0003).
- `README.md` runtime-parity and changelog references corrected: agent mirrors now point to the actual `ai-council.*` files across four runtimes including Gemini, and the changelog list reflects the current head (AF-0005, AF-0008).

#### Deferred

- Agent-file rename `ai-council.*` -> `deep-ai-council.*` declined by the operator: the files keep the `ai-council` name to avoid breaking live `--agent ai-council` callers.
- Broader `SKILL.md` §7 extraction-era checklist reframing left as a P2 follow-on.
