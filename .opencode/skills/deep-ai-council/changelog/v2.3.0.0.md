deep-ai-council's fifteen reference files are now grouped into five topic subfolders, so a reader reaches convergence, scoring, structure, pattern, and integration docs by area instead of scanning one flat list.

> Spec folder: `.opencode/specs/skilled-agent-orchestration/116-deep-skill-evolution/008-deep-skill-doc-evolution` (Level 2)

---

## What Changed

#### Documentation Structure

- **Grouped references/ into convergence/, scoring/, structure/, patterns/, and integration/** -- the fifteen references sat in one flat directory. They now live under five topic subfolders. The smart router discovers references recursively, so routing behavior is unchanged while navigation gets clearer.
- **Updated the README structure tree and every inbound link** -- the SKILL.md REFERENCES paths and intent map, the README structure diagram, and the @ai-council agent mirrors across four runtimes resolve to the subfoldered paths. A workspace sweep confirmed zero stale flat reference paths in live consumers.
