# v0.9.0.0 — rename from sk-prompt-small-model (2026-06-28)

## Summary

Renamed the skill directory `.opencode/skills/sk-prompt-small-model/` → `.opencode/skills/sk-prompt-models/` and propagated the new identity across every live reference surface repo-wide. The `small` qualifier had become inaccurate — the rotation now includes 1M-context models (GLM-5.2, MiMo-V2.5-Pro), so the grouping is "non-frontier models you dispatch out to," not size. This is a pure identity refactor: skill behavior, model coverage, dispatch matrix, `recommended_frameworks` data, and `enhances` edges are unchanged.

## What changed

- **Directory**: `git mv` (history preserved). Untracked additions (`references/models/glm-5.2.md`, `benchmarks/008-*`) moved with it.
- **Identity**: `SKILL.md` `name:` + Keywords, `graph-metadata.json` `skill_id`, `description.json`.
- **Live references (~750 across the repo)**: cross-skill links (cli-*, deep-loop-*, system-*, sk-prompt), command workflows, the card-sync guard hardcoded path, `reviewer-regression.json` outputsDir, the secret-scrubber test fixture, `executor-config.ts` comment, `model_profiles.json` profile_refs, `references/models/*.md` back-links, specs/READMEs, the `.claude` / `.codex` agent mirrors, `.github/workflows/prompt-card-sync.yml`, and `AGENTS.md`.
- **Derived indexes**: advisor force-refreshed; compiled `skill-graph.json` updated. Per-skill `graph-metadata.json` updated.

## Frozen (history-care — deliberately NOT renamed)

- Changelogs / archived packets that document the EARLIER `sk-small-model → sk-prompt-small-model` rename (this skill's `v0.1`–`v0.3` changelogs, `z_archive/114-small-ai-model-optimization/**`) — left as true historical records so their narrative stays accurate.
- The legacy name `sk-small-model` itself (a separate, older rename generation) — untouched.
- The `158-sk-prompt-models-rename` spec packet — self-referential (it names the rename subject).

## Verification

- `git grep` live residual = **0** references to `sk-prompt-small-model` (only the frozen history + 158 packet + a binary `.sqlite` backup retain the old name, all intentional).
- Card-sync guard exit 0; advisor routing probe surfaces `sk-prompt-models` (confidence 0.95).
- All edited JSON parses; the secret-scrubber fixture passes.

## Known pre-existing (out of scope, flagged)

- `skill_graph_compiler --export-json` fails validation on 5 pre-existing graph asymmetries (mcp-figma / mcp-open-design / sk-design / sk-code-review symmetry) + a weight-band note (the `enhances` 0.8 weight) — all predate this rename. The compiled index was updated by targeted edit; the asymmetries need a separate graph-cleanup pass.
