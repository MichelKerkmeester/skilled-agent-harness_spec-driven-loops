This release repoints the sk-prompt manual-testing-playbook's references to the CLI prompt quality card. The card moved to the model-craft hub in 2.1.0.0, but the playbook still pointed at its old sk-prompt location in nine places, including two `rg` commands that would have found nothing. All nine now resolve. No skill behavior changed.

> Spec folder: `.opencode/specs/skilled-agent-orchestration/130-prompt-knowledge-layering/015-deferred-cleanup-entangled-playbook-assets/` (Level 1)

---

## What Changed

#### Documentation

- **Playbook card paths repointed** -- the manual-testing-playbook referenced `cli_prompt_quality_card.md` at its pre-relocation sk-prompt path in nine places: six source-reference rows using `../../assets/cli-prompt-quality-card.md`, two `rg` verification commands using `.opencode/skills/sk-prompt/assets/cli-prompt-quality-card.md`, and one on-disk resolution entry. After the phase-013 move the card lives in the hub, so those paths were dangling and the two `rg` commands would have matched nothing. All nine now point at `sk-prompt-models/assets/cli-prompt-quality-card.md`, verified to resolve. Scenario prose was left intact; only the broken file paths changed.

## Files Changed

| File | What changed |
| ---- | ------------ |
| `manual-testing-playbook/manual-testing-playbook.md` | On-disk resolution path repointed to the hub |
| `manual-testing-playbook/06--escalation-tiers/*.md` | Source-ref + `rg` card paths repointed to the hub |
| `manual-testing-playbook/05--framework-selection/*.md` | Source-ref card paths repointed to the hub |
| `SKILL.md` | Version 2.1.2.0 |

## Upgrade

No migration required.
