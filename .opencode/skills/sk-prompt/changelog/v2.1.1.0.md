This release finishes the phase-013 card relocation by scrubbing the stale `cli_prompt_quality_card.md` references that were left behind in `sk-prompt/SKILL.md`. The card moved to the model-craft hub in 2.1.0.0, but the skill body still named it in prose, in a broken Core-References link, in a whole FAST-PATH ASSET section, and in the resource list. Those are gone now, so `sk-prompt` documents only what it actually owns.

> Spec folder: `.opencode/specs/skilled-agent-orchestration/130-prompt-knowledge-layering/014-doc-divider-sweep-and-sk-prompt-cleanup/` (Level 1)

---

## What Changed

#### Documentation

- **Stale card references removed** -- after the 2.1.0.0 relocation, `SKILL.md` still pointed at `cli_prompt_quality_card.md` in four places: a §1 activation-trigger sentence, a Core-References bullet whose `./assets/` link was now broken, the entire `## 8. FAST-PATH ASSET` section describing an asset that no longer lives here, and an entry in the §10 resource-discovery list. We reworded the §1 sentence to name the deep-path escalation relationship without the moved filename, dropped the broken Core-References bullet, removed the §8 section, renumbered the following sections (§9 INTEGRATION POINTS and §10 REFERENCES became §8 and §9), and dropped the card from the resource list. The router pseudocode was already correct and was left untouched. A grep confirms no `cli_prompt_quality_card` reference remains in `SKILL.md` and the sections renumber 1 through 9 with no gaps or duplicates.

## Files Changed

| File | What changed |
| ---- | ------------ |
| `SKILL.md` | Removed 4 stale card references; renumbered §8/§9; version 2.1.1.0 |

## Upgrade

No migration required.
