# sk-prompt-models v0.6.1.0 — Hub seam refinement (STAR de-phantom, mechanics de-leak, cluster navigability, canonical checklist)

**Released**: 2026-06-03
**Status**: Refinement — closes hub-side drift found by the 130 deep research; no behavior change to dispatch.

---

## Summary

Tightens the hub so each fact has one home and the four default-unverified models are navigable and discoverable:

- **STAR de-phantomed** — STAR/BUILD are relabeled as cli-devin task-shapes (NOT sk-prompt frameworks); `swe-1.6.md` and `_index.md` no longer present STAR as a registry `fallback` (registry is `fallback: null`).
- **Mechanics de-leaked** — `mimo-v2.5-pro.md` and `minimax-m3.md` drop their embedded `opencode run` wrappers (executor mechanics) for a one-line rule + pointer to `cli-opencode`.
- **Cluster navigability** — the four clones (deepseek/kimi/qwen/glm) gain bidirectional card↔profile links.
- **Canonical adoption checklist** — `pattern-index.md` §4 is now the single, complete new-provider checklist (adds author-profile + `_index` row + dispatch-matrix row); `SKILL.md` §3 points to it instead of keeping a divergent copy.
- **Metadata refresh** — `graph-metadata.json` `causal_summary`/`key_topics` updated to the hub role; minimax/mimo added to `intent_signals` + the cli-opencode `enhances` context; `last_updated_at` refreshed.

---

## What Changed

| File / Path | Change | C-item |
|---|---|---|
| `SKILL.md` | STAR removed from the sk-prompt framework list (relabeled cli-devin task-shape); §3 adoption steps now point to `pattern-index.md` §4 | C3, C8 |
| `references/models/swe-1.6.md` | "registry fallback = STAR" → `fallback: null`; §4 STAR template relabeled "profile-level option" (not "fallback") | C3 |
| `references/models/_index.md` | swe-1.6 row `RCAF; STAR fallback` → `RCAF; no fallback` | C4 |
| `references/models/mimo-v2.5-pro.md`, `minimax-m3.md` | Embedded `opencode run` wrapper → non-TTY rule + pointer to `cli-opencode` | C5 |
| `references/models/{deepseek-v4-pro,kimi-k2.6,qwen3.6,glm-5.1}.md` | Added card↔profile back-links (round-trip) | C6 |
| `references/pattern-index.md` | §4 made the single canonical new-provider checklist (adds profile + `_index` + matrix rows) | C8 |
| `graph-metadata.json` | `causal_summary`/`key_topics`/`intent_signals`/`enhances` refreshed; `last_updated_at` bumped | C10 |

---

## Verification

| Check | Result |
|---|---|
| `check-prompt-quality-card-sync.sh` (extended) exit 0 | Verified |
| No hub surface presents STAR as a sk-prompt framework / registry fallback | Verified |
| 4 clone profiles round-trip card↔profile | Verified |
| `graph-metadata.json` valid JSON | Verified |

---

## Spec folders referenced

- `.opencode/specs/skilled-agent-orchestration/130-prompt-knowledge-layering/009-refine-hub-cli-connections/` — implementing packet
- `.opencode/specs/skilled-agent-orchestration/130-prompt-knowledge-layering/research/research.md` — the converged C1–C10 backlog (decision record)
