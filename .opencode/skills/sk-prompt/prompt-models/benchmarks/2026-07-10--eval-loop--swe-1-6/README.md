# 2026-07-10--eval-loop--swe-1-6

_Derived after the fact from this run's stored record, not written at run time._

This folder is an experiment workspace whose internal shape predates the report convention. Renaming it into the dated grammar does not make its contents comparable with a Lane C run, and nothing here has been reshaped to look as though it were.

## Contents

| Entry |
|---|
| `fixtures/` |
| `iterations/` |
| `scripts/` |
| `state/` |
| `synthesis.md` |
| `variants/` |
