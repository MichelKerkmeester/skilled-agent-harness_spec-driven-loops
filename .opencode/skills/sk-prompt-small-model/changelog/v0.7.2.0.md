# sk-prompt-small-model v0.7.2.0 — owns the canonical cross-CLI prompt quality card

**Released**: 2026-06-03
**Status**: Card relocation. The hub now owns the canonical `cli_prompt_quality_card.md`; no dispatch-behavior change.

---

## Summary

The canonical cross-CLI prompt quality card moved here from `sk-prompt`. The card is CLI-dispatch-specific (model overrides, precedence, the per-cli mirrors), so it belongs with the model-craft hub rather than the forkable generic framework engine.

- `assets/cli_prompt_quality_card.md` — moved in from `sk-prompt/assets/` (git rename).
- The card's internal links to the generic framework definitions were repointed to `../../sk-prompt/references/{patterns_evaluation,depth_framework}.md` (they stay in sk-prompt).
- The hub `SKILL.md` §5 reference now points at the local `./assets/cli_prompt_quality_card.md`.
- The five `cli-*` SKILL.md + cards + `cli-devin/references/context-budget.md` were repointed from `../sk-prompt/assets/...` to `../sk-prompt-small-model/assets/...` (~15 references).

---

## What Changed

| File / Path | Change |
|---|---|
| `assets/cli_prompt_quality_card.md` | Moved in from `sk-prompt`; internal framework-def links repointed to `../../sk-prompt/references/` |
| `SKILL.md` | §5 card reference → local `./assets/`; version 0.7.2.0 |
| (consumers) 5 `cli-*` SKILL.md + cards + `cli-devin/references/context-budget.md` | Card pointer repointed to the hub (mechanical) |

---

## Verification

| Check | Result |
|---|---|
| Card resolves from every consumer; card's own links resolve | Verified |
| No active skill file references the old `sk-prompt/assets/` card path | Verified |
| card-sync guard green (canonical path updated to the hub) | Verified |

---

## Spec folders referenced

- `.opencode/specs/skilled-agent-orchestration/130-prompt-knowledge-layering/013-card-relocation-and-guard/`
