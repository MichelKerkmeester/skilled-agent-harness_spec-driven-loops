# sk-prompt-small-model v0.7.1.0 — benchmark merge + stale-citation repoint

**Released**: 2026-06-03
**Status**: Benchmark layer reconciliation. The renumbered benchmark folders are staged in git, two sibling benchmarks are merged, and every stale citation is repointed.

---

## Summary

- **Rename reconciled.** The benchmark folders had been renamed on disk to a clean `00N` scheme (`113-003→001` … `127-006→006`) without git; this version stages that rename.
- **006 merged into 005.** Benchmarks 005 (capability discrimination) and 006 (strict-validation fixtures) are the same MiniMax-M3-vs-MiMo comparison. 006 became `005/eval/strict-validation/`, 005's original data moved to `005/eval/capability-discrimination/`, and a merged top-level `synthesis.md` ties both. The `006-` folder is removed.
- **Citations repointed.** ~40 stale benchmark citations (`120/003`, `126/004`, `127/004`, `127/006`, `113/003`, `113/005`) across the profiles, `_index.md`, `pattern-index.md`, and `model-profiles.json` now read `benchmark 00N`.
- **Hygiene.** `model-profiles.json` annotation fields are durable WHY with refreshed ids; `pattern-index.md` lost its remaining ephemeral spec-phase refs (the `Phase NNN` column became `Status`, the arc name and deleted-phase line and spec-folder links were removed).

---

## What Changed

| File / Path | Change |
|---|---|
| `benchmarks/` (6 folders) | Renamed (staged); 006 merged into 005 with two eval subsets |
| `benchmarks/005-*/synthesis.md` | New merged top-level synthesis |
| `references/models/*.md` (6) + `_index.md` | Citations → `benchmark 00N` |
| `references/pattern-index.md` | Citations + spec-phase scrub |
| `assets/model-profiles.json` | Evidence ids refreshed; notes hygiened |
| `SKILL.md` | Version 0.7.1.0 |

---

## Verification

| Check | Result |
|---|---|
| Merge structure correct; 006 removed | Verified |
| No stale benchmark/phase ids in active docs | Verified |
| `model-profiles.json` valid JSON | Verified |
| card-sync guard green | Verified |

---

## Spec folders referenced

- `.opencode/specs/skilled-agent-orchestration/130-prompt-knowledge-layering/011-model-profiles-and-benchmark-merge/`
