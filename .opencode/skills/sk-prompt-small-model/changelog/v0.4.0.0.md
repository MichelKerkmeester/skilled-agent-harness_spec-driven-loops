# v0.4.0.0 — sk-prompt-small-model (renamed from prior identity)

**Date**: 2026-05-23
**Phase**: 008 (`.opencode/specs/skilled-agent-orchestration/114-small-ai-model-optimization/008-sk-prompt-small-model-rename/`)
**Type**: Identity-only refactor (no behavioral change)
**Family**: stays `sk-util` (per ADR-001)

---

## Summary

Second-pass rename of the small-model sentinel skill. The current canonical name (`sk-prompt-small-model`) explicitly anchors on prompt-quality scope (matching CLAUDE.md's "Small-model dispatch rule" wording) and aligns with the `sk-prompt` neighbor. Trigger phrases, model coverage, dispatch matrix, allowed-tools, and advice text are unchanged.

## Provenance Chain

- Original sentinel skill shipped in Phase 002 on 2026-05-18 (initial canonical name).
- First identity refactor shipped in Phase 007 on 2026-05-21 (v0.3.0.0) — canonical name evolved one step.
- This refactor (v0.4.0.0) shipped in Phase 008 on 2026-05-23 — canonical name evolved to its current form.
- `git log --follow` traces back through both renames to original Phase 002 creation.
- The phase-007 folder `007-sk-ai-small-model-rename/` is preserved as the only path-string marker of the intermediate canonical name (immutable spec-kit convention).

## History Policy

Phase 007 used preserve-history (historical docs under 114/001-006 left untouched as immutable provenance). Phase 008 adopts the OPPOSITE policy per explicit user directive: REWRITE-ALL. Every content occurrence of the prior canonical name was rewritten to the current name — including phase 007's shipped spec docs (~200 hits), 131-deep-skill-evolution/scratch/115-arc-review/ (~70 hits), deep-ai-council/changelog/v1.2.0.0.md (3 hits), system-spec-kit/references/rename-pattern.md (3 hits). The only acceptable residuals are:
- The immutable phase-007 folder NAME `007-sk-ai-small-model-rename/` (path-only, never content).
- 008/* active-phase docs (semantic-meaning preservation per D-008).

The deliberate cost of this policy is documented in Phase 008's `decision-record.md` ADR-002: phase 007's shipped spec docs now describe a rename endpoint that did not exist in that phase's actual timeline. Future readers must consult ADR-002 to interpret phase-007 docs correctly post-008.

## Verification

| Check | Method | Result |
|---|---|---|
| Skill directory rename | git mv | ✓ old dir gone, new dir present, history preserved |
| Frontmatter `name` | grep | sk-prompt-small-model ✓ |
| graph-metadata.json `skill_id` | jq | sk-prompt-small-model ✓ |
| Family | jq | sk-util (ADR-001) ✓ |
| cli-devin reverse edge | jq | sk-prompt-small-model ✓ |
| cli-opencode reverse edge | jq | sk-prompt-small-model ✓ |
| Aggregator symlink | readlink | ../skills/sk-prompt-small-model/changelog ✓ |
| 4 playbook FILE renames | ls | 4 files with new names ✓ |
| Memory dir | rg | 0 hits ✓ |
| Compiled graph | jq | 23 skills; current name present + prior absent ✓ |
| Strict spec-folder validate | validate.sh --strict | exit 0 ✓ |

## Buckets Executed

1. Skill body rename (dir + frontmatter + body + new v0.4.0.0.md)
2. Sibling reverse edges (cli-devin + cli-opencode)
3. Aggregator symlink rotation
4. 4 manual playbook file renames + content sweep
5. Root markdown sweep
6. Compiled skill-graph regenerated
7. Memory dir sweep
8. Global historical sweep (REWRITE-ALL)

## Operator Notes

- Compiled skill-graph + per-skill metadata reflect the current canonical name correctly.
- Advisor MCP daemon may pick up the rename either at natural respawn or via `pkill -f advisor-server.js` to force a fresh load.
