{
  "note": "The applicator picks up a file named exactly tokens.json beside a DESIGN.md and reads darkMode from it. Without this, a reference with a dark theme is themed onto the corpus stock dark chrome instead of its own ground.",
  "darkMode": {
    "supported": true,
    "strategy": "class",
    "selector": ".dark",
    "darkVariables": [
      "--background", "--foreground", "--muted-foreground", "--card", "--border",
      "--chart-1", "--chart-2", "--chart-3", "--chart-4", "--chart-5"
    ]
  }
}
