---
title: "sk-design v1.1.0.0 - Parent-Hub Canon Alignment"
version: 1.1.0.0
---

# sk-design v1.1.0.0 — Parent-Hub Canon Alignment

> Aligns the sk-design hub with the canonical parent-skill contract: hub companion artifacts, declarative routing metadata, and the first Lane-C benchmark baseline.

---

## Added

- `description.json` — hub description with the required canon fields (name, description, version, keywords), modes, and trigger examples.
- `manual_testing_playbook/` — 21 operator-executable scenarios across 5 categories (mode routing incl. the mode-hint override, advisor integration, transform-verb framing, md-generator pipeline, shared-reference-base discipline). Every expected route derived from `mode-registry.json` / `hub-router.json`; 260 referenced resource paths existence-verified.
- `benchmark/` — first Lane-C skill-benchmark baseline (router mode): 15/15 scored scenarios passed, D1-intra 100, D2 100, D5 connectivity 100; verdict CONDITIONAL 69/100 with D3 documented as a router-mode measurement gap. Includes `benchmark/README.md` with the re-run command.
- `mode-registry.json` `extensions.transform-verbs` — declares the transform-verb routing extension, activating the in-place top-level `transformVerbRouting` block without relocating it.
- `hub-router.json` `routerPolicy.bundleRules` — the prose Bundle Rule for build/UI work is now declared machine-readably as `ui-build-bundle` (`whenAll: interface + foundations → orderedBundle`).
- `hub-router.json` `routerPolicy.defaultResource` — `references/design_proof_token.md` wired in (was orphaned: cited by two shared asset cards but unreachable from any router intent; surfaced by the first benchmark run's connectivity gate).

## Changed

- `mode-registry.json` modes all carry `packetKind: "workflow"` (two-axis canon discriminator).
- `changelog/` — five per-mode symlinks replaced by the real-files-only policy (packet changelogs live in their packets).
- Mode SKILL link depths fixed (audit, foundations, md-generator, motion linked the sk-doc smart-router template one level too shallow) and `design-interface/README.md` cross-skill links repaired.
- `SKILL.md` Bundle Rule section now names its declarative `bundleRules` counterpart.

## Verification

- `PARENT_HUB_CHECK_STRICT=1 parent-skill-check .opencode/skills/sk-design` — 0 failures, 0 warnings.
- Lane-C router-mode benchmark green (D5 hard gate 100/100).
- `check-markdown-links` — zero broken sk-design links.
