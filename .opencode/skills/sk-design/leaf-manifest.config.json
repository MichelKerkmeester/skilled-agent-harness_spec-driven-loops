{
  "workflowMode": "sk-design",
  "packet": ".",
  "leafRoots": [
    "references",
    "assets",
    "manual-testing-playbook"
  ],
  "excludeIndexFiles": true,
  "resourceContractVersion": 1,
  "_note": "Standalone single-mode manifest config. sk-design is a normal skill (no mode-registry.json / hub-router.json); its references/, assets/ and manual-testing-playbook/ docs are the routed leaves the in-SKILL.md INTENT_MODEL + RESOURCE_MAP router selects. assets/tokens.css is a copy-paste artifact rather than a routable markdown leaf, and changelog/ and benchmark/ are records rather than knowledge, so none of them appear as leaves. Consumed by generate-leaf-manifest.cjs to emit leaf-manifest.json; leaf-aliases.json is the derived identity projection of that manifest, so regenerate both together via ci-skill-root-metadata.cjs --fix."
}
