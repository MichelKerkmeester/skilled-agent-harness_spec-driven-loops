# v4.0.0.0

sk-code converted from a flat two-axis skill into a nested parent hub with 5 mode packets (implement, quality, debug, verify, review) over a shared surface-detection router; folds the review doctrine in as the code-review mode. Scaffold phase - packets are skeletons; content relocation follows.
