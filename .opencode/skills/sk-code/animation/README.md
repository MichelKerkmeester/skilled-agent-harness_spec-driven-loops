---
title: animation
description: Read-only Motion.dev surface evidence for the sk-code hub — animation principles, the animate/timeline/scroll API, a decision matrix, and copy-ready snippets. A cross-stack overlay bundled on top of the detected base surface.
trigger_phrases:
  - "motion.dev animation"
  - "cross-stack animation"
  - "animation principles"
version: 1.0.0.0
---

# animation — Motion.dev Evidence (sk-code surface)

## 1. AT A GLANCE

| Aspect | What you get |
|---|---|
| **Kind** | Surface evidence packet (`packetKind: surface`, read-only, advisor-invisible). |
| **Carries** | Motion.dev animation evidence: principles, the `animate()`/timeline/scroll/gesture API, a Webflow-vs-Motion decision matrix, integration patterns, and copy-ready snippets. |
| **Reached by** | The hub bundling it as a **cross-stack overlay** whenever an animation intent fires — on top of whichever base surface (Webflow/OpenCode) is detected. Never a primary. |
| **Mutates** | Nothing. The paired workflow mode owns the build. |

## 2. LAYOUT

- `SKILL.md` — the surface contract, reference map, and standards.
- `references/` — `animation_principles.md`, `quick_start.md`, `animate_and_timelines.md`, `scroll_and_gestures.md`, `integration_patterns.md`, `decision_matrix.md`, `performance_and_pitfalls.md`.
- `assets/` — `install_card.md`, `playbook_entries.md`, and `snippets/` (drop-in JS).
- `changelog/` — release history.

See `SKILL.md` for the full reference map and the non-negotiable motion standards.
