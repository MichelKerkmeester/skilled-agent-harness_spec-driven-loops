# sk-code shared

This placeholder will hold the shared surface-detection router and cross-mode helpers consumed by every sk-code mode. It is non-discoverable, carries no `graph-metadata.json`, and receives real content in a later phase.
