This release reconciles the legal-stop gate model across the `deep-review` documentation surfaces. The authoritative producer is `step_emit_blocked_stop` in both `deep_start-review-loop_{auto,confirm}.yaml`, which emits a 9-gate `gateResults` bundle. The documentation had drifted to describe 6 or 7 gates depending on the surface, omitting `candidateCoverageGate` and `graphlessFallbackGate` (the two v2-rollout gates) and, in one table, `claimAdjudicationGate`. Every surface now enumerates the same 9 gates with the same `Gate`-suffix names, so a reader of any one document sees the model the runtime actually emits. This is a documentation-only release. The reducer (`scripts/reduce-state.cjs`) is gate-name-agnostic and was confirmed unchanged, and the YAML workflows were read-only as the source of truth.

> Spec folder: `.opencode/specs/skilled-agent-orchestration/116-deep-skill-evolution/000-release-cleanup/003-deep-review/001-gate-model-reconciliation/` (Level 2)

---

## Gate Model Reconciliation

Closes the gate-model drift cluster (LG-0013, LG-0016, LG-0031, LG-0032) surfaced by the `003-deep-review` phase-5 deep-research loop. The authoritative 9-gate set, in emission order, is `convergenceGate`, `dimensionCoverageGate`, `p0ResolutionGate`, `evidenceDensityGate`, `hotspotSaturationGate`, `claimAdjudicationGate`, `fixCompletenessReplayGate`, `candidateCoverageGate`, and `graphlessFallbackGate`.

#### convergence.md aligned to 9 gates

The §Section-1 `blocked_stop` event example now includes `candidateCoverageGate` and `graphlessFallbackGate` with the same field shapes the YAML emits (`searchDebt`/`missing` and `mode`/`missing`/`unavailabilityReason`). The prose key listing names all 9 gates and notes the two v2-rollout gates pass trivially when the search path is inactive. The §6 LEGAL-STOP GATE BUNDLE drift note was rewritten from a "known drift" warning into a reconciled mapping that names the YAML producer as authoritative, and the table, the `buildReviewLegalStop` pseudocode, and the recovery-strategy table each gained the two missing gate rows.

&nbsp;

#### loop_protocol.md gate count corrected

The §Step-2 legal-stop description previously named seven gates. It now names all nine and flags `candidateCoverageGate` and `graphlessFallbackGate` as v2-rollout gates.

&nbsp;

#### state_format.md blocked_stop schema completed

The `blocked_stop` event example carried only five gates. It now carries the full 9-gate `gateResults` bundle and a pointer to `convergence.md` §Section-1 as the authoritative shape.

&nbsp;

#### feature_catalog quality-gates entry enumerated

`04--severity-system/quality-gates.md` described gates in passing prose. §2 CURRENT REALITY now carries a table enumerating all 9 gate names with a one-line check description for each, and explains how the 3 contract-level binary gates relate to the 9-gate legal-stop bundle.

&nbsp;

#### DRV-018 playbook clarified

The scenario validates the 3 contract-level binary gates (evidence, scope, coverage). The WHY THIS MATTERS section now explains that those map into the 9 named event-shape gates of the legal-stop bundle, so the 3-gate scenario is not in tension with the 9-gate model. A stale `convergence.md` Section 10.4 anchor was corrected to §6 LEGAL-STOP GATE BUNDLE.
