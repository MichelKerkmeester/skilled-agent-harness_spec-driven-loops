---
title: "mcp-webflow v1.1.0.0 - Depth Upgrade"
version: 1.1.0.0
---

# v1.1.0.0 - Depth Upgrade

**Released:** 2026-08-02

Deepened the packet to the obsidian-mode depth standard after audit, without changing the frozen
safety contract:

## What's New

### SKILL.md (rewritten, ~6x larger)
- Full activation-trigger and when-not-to-use sections.
- Frozen operation-class matrix (RO/DW/DS/PB/DP) with per-class gates and rollback statements.
- Guarded intent router (read/draft/delete/publish/deploy/designer patterns).
- Execution protocol: discovery-first, classification, sk-design pairing boundary, auth posture,
  version-surface reconciliation.
- Complete safety rules (always/never) and negative knowledge.

### references/
- `tool-surface.md`: full 18-module action inventory with per-action classes (sites, pages, cms,
  components, scripts, workflows, webhooks, enterprise, aiChat, comments, rules, dePages,
  deElement, deStyle, deVariable, deComponents, deAsset, localDeMCPConnection).
- `mcp-wiring.md`: scope model, token kinds, rate limits + 429 semantics, Bridge App boundary,
  version-surface contradiction table, diagnostic flow.
- `troubleshooting.md`: expanded failure-mode tables (auth/403 causes, publish queue, Designer
  Bridge App, drift handling, redaction rules).

### scripts/ + assets/
- `doctor.sh` (verify-only checks; token presence as boolean, never values), `install.sh`,
  `scripts/README.md`.
- `assets/utcp-webflow-manual.md` reference shape.

### feature-catalog/
- Index updated (prefix claim corrected to expected-but-unverified) + capability cards:
  `cms.md`, `publish-deploy.md`, `designer.md`, `site-pages-scripts.md`.

### manual-testing-playbook/
- Expanded 6 → 12 scenarios: discovery drift, page reads, rate-limit backoff, deploy gate,
  data-family negative pairing, non-Webflow deferral.

### examples/
- Added destructive-refused and first-session-discovery; staging-publish rewritten.

## Contract Changes

None — classification, gates, pairing, and credential rules are unchanged; the upgrade only adds
coverage and operational depth. Callable-name namespace remains UNVERIFIED until authenticated
discovery (token + test site pending).
