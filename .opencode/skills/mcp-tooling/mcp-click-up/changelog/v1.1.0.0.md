---
title: "mcp-click-up changelog — v1.1.0.0"
description: "Purpose-first README rewrite on the refined skill README template: one-line pitch, problem-first overview, ClickUp Operation Layer capability table, broken INSTALL-GUIDE link fixed, version bumped."
trigger_phrases:
  - "mcp-click-up v1.1.0.0"
  - "click up readme rewrite"
importance_tier: "normal"
contextType: "general"
version: 1.1.0.0
---

# mcp-click-up — v1.1.0.0

Rewrites the skill README to the refined skill README template from the skill-readme-refinement packet, matching the mcp-obsidian pilot standard so the mcp-tooling hub carries one consistent mode README set.

## 1. NEW

- **Purpose-first README** — `README.md` rewritten on the refined template: a one-line pitch blockquote after the H1, an AT A GLANCE table first, a problem-first OVERVIEW that states the reader's situation before any feature list, and a ClickUp Operation Layer capability table covering task statuses, time tracking, documents/goals/OKRs and offline task reads.
- **Broken link fixed** — the RELATED DOCUMENTS and FAQ entries now point to `INSTALL-GUIDE.md` at the packet root, which exists. The old `references/INSTALL-GUIDE.md` target did not resolve.
- **New document links** — RELATED DOCUMENTS now links the `feature-catalog/FEATURE-CATALOG.md` and `manual-testing-playbook/manual-testing-playbook.md` files, both of which exist on disk.

## 2. CHANGED

- `README.md` version field `1.0.0.7` → `1.1.0.0` (frontmatter).
- README body edited to Human Voice Rules: zero em dashes, zero semicolons, zero Oxford commas and zero banned words across the whole file, including table cells and code blocks.
- The cupt health verification command now uses `python3 -m json.tool` instead of a semicolon-based Python one-liner.

## 3. NOT CHANGED

- No `SKILL.md`, reference, script, feature catalog, manual testing playbook, template or vault file touched.
- No routing behavior, command surface or install path changed. Every factual claim from the previous README survives the rewrite, verified section by section.
