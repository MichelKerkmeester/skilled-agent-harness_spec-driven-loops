---
title: "mcp-obsidian changelog v0.11.0.0"
description: "README conformance release from the skill-readme refinement packet: Human Voice Rule fixes in the plugin capability table and version alignment with the skill release track."
trigger_phrases:
  - "mcp-obsidian v0.11.0.0"
  - "obsidian readme conformance"
importance_tier: "normal"
contextType: "general"
version: 0.11.0.0
---

<!-- Reconstructed: the original of this entry was deleted by a concurrent deep-loop containment sweep and was never committed; content restored from the session record. -->

# mcp-obsidian v0.11.0.0

The README conformance pass from the skill-readme refinement packet. The exemplar README cleared the refined template shape, the validator and the link guard, but carried two prose semicolons in the plugin capability table and a version field that lagged the skill release track. This release fixes the voice violations and aligns the field with the changelog head.

## 1. NEW

No new CLI, MCP or plugin surface. The skill behavior is unchanged from v0.10.0.0. This release is the README itself.

## 2. CHANGED

- `README.md` version `0.4.0.0` to `0.11.0.0`. The field never moved during the v0.5.0.0 through v0.10.0.0 releases while the README text did. This release catches the field up to the head of the release track so the frontmatter and the changelog agree.
- `README.md` Plugin Knowledge Layer table. The Git and Outliner rows dropped two semicolons in favor of separate sentences, clearing the Human Voice Rules prose gate.
- `README.md` AT A GLANCE Works on row. Removed a comma before a two-item alternative so the Oxford comma grep returns zero matches.

## 3. NOT CHANGED

- `SKILL.md` stays at `0.10.0.0`. No skill file, reference set, feature-catalog card or playbook scenario moved in this release.
- The released changelog entries `v0.1.0.0` through `v0.10.0.0` stay byte-identical.
- No vault, plugin or runtime data was touched.
