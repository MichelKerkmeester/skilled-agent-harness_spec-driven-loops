## [**1.2.0.0**] - 2026-07-16

Kebab-case naming migration: every file and directory name in this packet now uses hyphens only, per the repo-wide hyphen-naming convention (sk-doc/017). 33 paths were renamed via `git mv` (7 scenario/category directories, 26 files), and every in-packet reference — SKILL.md smart-router RESOURCE_MAP/DEFAULT_RESOURCE/ON_DEMAND lists, playbook root index, feature-catalog tables, scenario `expected_resources` mirrors, scripts, fixtures — plus the `.opencode/specs/mcp-tooling/008-mcp-aside` packet record was updated to the new paths.

#### Kebab-case renames

- Need: The packet shipped with snake_case reference/asset/scenario names (`references/aside_cli_reference.md`, `manual_testing_playbook/recovery_and_failure/`, ...), which the hyphen-naming convention now bans for filesystem names.
- Change: All snake_case directories (`agent_task/`, `cli_lifecycle/`, `intra_routing_recall/`, `mcp_transport/`, `probes_and_gaps/`, `recovery_and_failure/`, `repl_evidence/`) and files (e.g. `aside_cli_reference.md` → `aside-cli-reference.md`, `mcp_wiring.md` → `mcp-wiring.md`, `utcp_aside_manual.md` → `utcp-aside-manual.md`, `discovery_fixture_2026-07-16.json` → `discovery-fixture-2026-07-16.json`, `holdout_repl.md` → `holdout-repl.md`) were renamed with `git mv`; scenario frontmatter `category:` labels and `expected_resources` paths follow the new names.
- Why it matters: One naming dialect across the packet; router path lists, benchmark gold resources, and on-disk names can no longer drift apart on case style.

&nbsp;

#### Tool-mandated carve-out (NOT renamed)

- Need: An evidence check before renaming the two top-level scaffold directories.
- Change: `feature_catalog/` and `manual_testing_playbook/` (the top-level directories) intentionally stay snake_case — they are hardcoded in `sk-doc/create-skill/scripts/package_skill.py` (VERSIONED_DOC_SUBTREES + scaffold checks), `.opencode/commands/doctor/scripts/parent-skill-check.cjs` (6 refs), and the skill-benchmark loaders (`load-playbook-scenarios.cjs`, `playbook-generator.cjs`, `run-skill-benchmark.cjs`). The sk-git pilot renamed them and its package gate fails. Additionally, the two root index FILES `manual_testing_playbook/manual_testing_playbook.md` and `feature_catalog/feature_catalog.md` also keep their names: `load-playbook-scenarios.cjs:375` loads `manual_testing_playbook.md` by exact filename as the playbook root index, and both filenames sit in tool skip-lists (`load-playbook-scenarios.cjs:307`, `validate-playbook-topology.cjs:136`).
- Why it matters: Renaming any of these four names silently changes benchmark-loader shape detection and package-gate scaffold checks; the carve-out keeps the tooling contract intact until the tools themselves migrate.

---

## Files Changed

| File | Change | Why |
| ---- | ------ | --- |
| `references/`, `assets/`, `examples/` snake_case files (7) | Renamed | Kebab-case migration (`aside-cli-reference.md`, `discovery-fixture-2026-07-16.json`, `mcp-wiring.md`, `session-management.md`, `utcp-aside-manual.md`). |
| `feature_catalog/` leaf files (5) | Renamed | Kebab-case migration (`install-and-doctor.md`, `mcp-transport-and-code-mode.md`, `repl-evidence-capture.md`, `agent-tasks.md`, `troubleshooting-recipes.md`). |
| `manual_testing_playbook/` category dirs (7) + scenario files | Renamed | Kebab-case migration (`agent-task/`, `cli-lifecycle/`, `intra-routing-recall/`, `mcp-transport/`, `probes-and-gaps/`, `recovery-and-failure/`, `repl-evidence/`). |
| `SKILL.md` | Updated | Router path lists updated to kebab paths; version 1.1.1.0 → 1.2.0.0. |
| `README.md`, `INSTALL_GUIDE.md`, `scripts/doctor.sh`, `scripts/install.sh`, playbook/catalog root indexes | Updated | Reference paths updated to kebab names. |
| `feature_catalog/`, `manual_testing_playbook/` top-level dirs + their root index files | Kept | Tool-mandated carve-out (see above). |
