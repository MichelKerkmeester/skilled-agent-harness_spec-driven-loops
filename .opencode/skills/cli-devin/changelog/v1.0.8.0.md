# cli-devin v1.0.8.0 — Pointer-ize Tier-3 + drop the framework-choice restatement

**Released**: 2026-06-03
**Status**: Refinement — closes drift found by the 130 deep research.

---

## Summary

- **Tier-3 pointer-ized (C1)** — the deep-path bullet now points to `sk-prompt/assets/cli_prompt_quality_card.md` §"Tier 3 — Deep path" instead of re-enumerating the triggers (cli-devin's copy matched canonical, but is now pointer-only like the others so all five stay in lockstep by construction).
- **Framework choice de-restated (C2)** — the two SWE-1.6 callouts (the `> REQUIRED` block and the §4 ALWAYS rule) drop the inline `(RCAF primary; STAR fallback; BUILD …)` enumeration and keep only the "OWNED by `swe-1.6.md`" pointer. The cli-devin-wide composition guidance (bundle-gate language, pre-planning density) stays — it is executor policy, not a framework-choice restatement.

---

## What Changed

| File / Path | Change | C-item |
|---|---|---|
| `SKILL.md` | Tier-3 deep-path bullet → canonical-card pointer | C1 |
| `SKILL.md` | SWE-1.6 callout + §4 rule drop the RCAF/STAR/BUILD enumeration; keep the profile pointer | C2 |

---

## Verification

| Check | Result |
|---|---|
| `check-prompt-quality-card-sync.sh` CHECK 2 (pointer-only) pass | Verified |
| No `RCAF primary; STAR fallback` enumeration remains in `SKILL.md` | Verified |

---

## Spec folders referenced

- `.opencode/specs/skilled-agent-orchestration/130-prompt-knowledge-layering/009-refine-hub-cli-connections/`
