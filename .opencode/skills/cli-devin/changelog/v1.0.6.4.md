Removed the custom top-level fields that Devin's strict `--agent-config` parser rejects from all three shipped recipes, so deep-loop dispatches stop failing at config-parse time. The fields (`verification_enabled`, `verification_languages`, `bayesian_scoring_enabled`, `bayesian_score_file`, `fallback_chain`) were Phase-004 additions encoding opt-in verification, fallback, and Bayesian-scoring features, but the binary rejects any unknown top-level key — the same constraint that already defers `mcp_servers`. The recipes now match the canonical minimal schema documented in `references/agent-config-recipes.md` §2 and strict-parse against `devin 2026.5.6`.

#### Recipe drift fix

- Stripped `verification_enabled`, `verification_languages`, `bayesian_scoring_enabled`, `bayesian_score_file`, and `fallback_chain` from `assets/agent-config-deep-research-iter.json`, `assets/agent-config-deep-review-iter.json`, and `assets/agent-config-synthesis.json`.
- Removed the three stale `system_instructions` entries that described the now-absent verification, Bayesian-scoring, and fallback contracts, plus the vestigial `agent-state/tool-scores-*` write scopes those contracts required.

#### Documentation realignment

- `references/agent-config-recipes.md` §2: added a "Rejected custom fields" note so the schema table explicitly warns against re-adding the rejected keys.
- `references/output-verification.md`, `references/quota-fallback.md`, and `assets/confidence-scoring-rubric.md`: reframed each "Recipe Contract" section to state the field is deferred at the recipe layer (Devin rejects custom agent-config keys) and describes the intended shape for when a Devin-supported channel exists.
