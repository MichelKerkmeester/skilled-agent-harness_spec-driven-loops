This release finishes correcting the permission-mode typo across cli-devin's docs. Phase 015 fixed `references/cli_reference.md`, but the same duplicate-`dangerous` typo (a phantom third mode) survived in five other places. Devin has exactly 2 permission modes (`auto`/`dangerous`), so every doc now says so consistently. No dispatch behavior changed.

> Spec folder: `.opencode/specs/skilled-agent-orchestration/130-prompt-knowledge-layering/015-deferred-cleanup-entangled-playbook-assets/` (Level 1)

---

## What Changed

#### Documentation

- **Permission-mode typo cleared everywhere** -- the docs repeatedly listed `dangerous` twice (e.g. "Choose `auto`, `dangerous`, or `dangerous`", "approve `dangerous` or `dangerous`", a 3-row table with two `dangerous` rows, "Documents the three permission modes"), implying a phantom middle tier. Devin's CLI has 2 modes: `auto` (default, confirms risky ops) and `dangerous` (auto-approves all, operator approval required). We collapsed every duplicate to the 2 real modes in `README.md` (3 spots), `references/agent_delegation.md` (2 spots), `references/cloud_handoff.md` (2 spots), `references/devin_tools.md` (§5 table 3 rows to 2), and the `02--permission-modes/auto-mode.md` source-ref note. A grep confirms no duplicate-`dangerous` and no "three permission modes" claim remains.

## Files Changed

| File | What changed |
| ---- | ------------ |
| `README.md` | Permission-mode taxonomy, troubleshooting, and FAQ deduped to 2 modes |
| `references/agent_delegation.md` | RULES + troubleshooting deduped to 2 modes |
| `references/cloud_handoff.md` | Permission-mode selection + related-resources deduped to 2 modes |
| `references/devin_tools.md` | §5 cross-family table collapsed from 3 rows to 2 |
| `manual_testing_playbook/02--permission-modes/auto-mode.md` | "three permission modes" source-ref corrected to "two" |
| `SKILL.md` | Version 1.0.13.0 |

## Upgrade

No migration required.
