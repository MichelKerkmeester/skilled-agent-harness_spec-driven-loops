This release aligns cli-devin's prompt-quality card and confidence-scoring rubric to the sk-doc asset template and scrubs ephemeral phase references out of the rubric, so both docs match the rest of the skill set and stay durable. No dispatch behavior changed.

> Spec folder: `.opencode/specs/skilled-agent-orchestration/130-prompt-knowledge-layering/012-cli-doc-alignment/` (Level 1)

---

## What Changed

#### Documentation

- **Prompt quality card aligned** -- `assets/prompt_quality_card.md` did not follow the sk-doc asset template. We added `## 1. OVERVIEW` (Purpose plus Usage), used ALL-CAPS numbered sections and put `## N. RELATED RESOURCES` last, while still delegating the framework plus CLEAR tables to the canonical card and preserving the SWE-1.6 model-selection rows plus the CLEAR-delegation sentence. The card-sync guard stays green with no table inlined, verified.
- **Confidence-scoring rubric aligned and scrubbed** -- `assets/confidence-scoring-rubric.md` did not match the template and carried an ephemeral `Phase 004 Implementation Notes` heading with `Phase 004` mentions inside it that go stale. It now opens with `## 1. OVERVIEW`, uses ALL-CAPS numbered sections, ends with `## N. RELATED RESOURCES` and has the ephemeral references rewritten to durable wording. Both docs were verified to open with `## 1. OVERVIEW` and no `Phase NNN` references remain in the rubric.

## Files Changed

| File | What changed |
| ---- | ------------ |
| `assets/prompt_quality_card.md` | Asset-template structure |
| `assets/confidence-scoring-rubric.md` | Asset-template structure plus ephemeral-ref scrub |
| `SKILL.md` | Version 1.0.9.0 |

## Upgrade

No migration required.
