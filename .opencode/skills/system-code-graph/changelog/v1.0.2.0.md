# v1.0.2.0 — Polish Complete

**Release date**: 2026-05-15
**Source review**: packet 037 — 20-iter cli-devin SWE-1.6 deep-review (`review/review-report.md` verdict 🟡 CONDITIONAL → 🟢 PASS post-remediation)
**Remediation packets**: 038 (active dispatches) + 039 (verification sweep — proved most items closed-by-parallel-work)

---

## Headline

The 037 review surfaced 2 P0 + 25 P1 + ~30 P2 findings on v1.0.0.0. Packets 038 + 039 closed every actionable P1 + both P0s. v1.0.0.0's documentation claim of "zero `from 'system-spec-kit'` imports remain" was an honest miss — the actual 1.0.0.0 had 46 such imports across 23 files. v1.0.1.0 corrected the docs (ERRATUM); v1.0.2.0 reflects the post-refactor reality: 13 of 14 production imports have been removed, the remaining one is `@spec-kit/shared` workspace import (correct cross-skill dependency, not a leak).

## P0 closures

### P0-1 — Isolation honesty (Path 1 + Path 2 combined)

**Path 2 (docs ERRATUM + reverse-direction CI guardrail)** — packet 038, commit `cdc56b7c1`:
- `INSTALL-GUIDE.md` §6 table row replaces the false "zero imports" claim with honest count (46 imports / 23 files broken down 11 production `.ts` + 3 `.d.ts` + 5 tests + 4 stress-tests).
- `changelog/v1.0.0.0.md` appends an ERRATUM preserving the original (false) claim for audit trail + noting v1.0.1.0 corrects it.
- `.github/workflows/isolation-check.yml` adds a non-blocking reverse-direction audit step.

**Path 1 (production import refactor)** — parallel commit `6b6f41214`:
- 13 of 14 production `.ts` imports from `system-spec-kit` removed.
- Remaining import is `@spec-kit/shared` workspace alias (intentional cross-skill type-sharing — not a leak).

### P0-2 — ccc_* fake readiness → real CocoIndex probe

Parallel agent shipped `mcp-server/lib/ccc-readiness-probe.ts` (~9.3 KB) integrated into all 3 ccc_* handlers (`ccc-status`, `ccc-reindex`, `ccc-feedback`). The hardcoded `buildUnavailableReadiness('readiness_not_applicable')` is gone. The CocoIndex bridge is now meaningfully readiness-aware.

## P1 closures (25 of 25)

| Cluster | Coverage | Source |
|---|---|---|
| **A — Docs alignment with sk-doc** (A1..A5) | 5/5 | parallel `058/4a..4c` commits `2b4abb3a1`, `68bce35cb`, `ec98af539` |
| **B — Runtime correctness** (B1..B7) | 7/7 | packet 038 commit `6553e36da` |
| **C — Launcher** (C1) | 1/1 | packet 038 commit `c8f4432c3` (comment) + parallel `35c1892c5` (DB move) |
| **D — Tests** (D1..D3) | 3/3 | parallel `35c1892c5` (new vitests + stress deletions + skip restoration) |
| **F — Build automation** (F1, F2) | 2/2 | parallel `35c1892c5` (scripts + real dist at canonical path) |
| **G — Doctor** (G1) | 1/1 | parallel `35c1892c5` (`mkdir -p "$db_dir"` at `mcp-doctor.sh:537`) |
| **H — Cross-runtime config consistency** (H1..H3) | 3/3 | parallel `058` series (INDEX_* defaults, _NOTE_1_DB, _NOTE_AUTO_MIGRATION) |

Specific anchors:
- B1: `mcp-server/index.ts` got `uncaughtException` + `unhandledRejection` handlers + `MK_CODE_INDEX_ROOT_DIR` env var.
- B2: `scan.ts` `fullReindexTriggered` hardcode replaced with git-HEAD-conditional + redundant `errors.filter` dropped.
- B3: `query.ts` `blast_radius` wrapped in `graphDb.getDb().transaction()`.
- B4: `code-graph-context.ts` Set-based node dedup.
- B5: `status.ts` `GoldVerificationTrust` widened to canonical 8-state.
- B6: `apply-orchestrator.ts` `repair-nodes` op gained `confirm=true` gate.
- B7: dead `recoverPartialScanFailure` import removed.

## Cross-AI verification

037's findings were independently re-verified via 5 cli-codex gpt-5.5 high fast iterations (`037/review/iterations/iteration-021..025.md` + `037/review/verification-addendum.md`):
- 15 VERIFIED
- 9 PARTIAL (defect real, wording needed correction)
- 0 HALLUCINATED

The previously dismissed P0-3 (claimed TS compile error at `apply-orchestrator.ts:342`) was reconfirmed dismissed — `tsc --noEmit` exits clean.

## Deferred to packet 040 if pursued

- `package.json` gitignore policy decision (P1-F1 sub-question — leave gitignored or track). Scripts block exists locally; the gitignore decision is operator preference.
- Devin scenario length style review (P1-A5 cosmetic sub-claim — 164 lines vs ~65 template).
- ~30 P2 nice-to-haves from 037 review — see `037/.../review/iterations/iteration-001..020.md` P2 sections.

## Upgrade notes

No migration required. v1.0.0.0 → v1.0.1.0 → v1.0.2.0 is a polish + correctness chain; no API changes. Cross-runtime config consumers should observe the new `SPECKIT_CODE_GRAPH_INDEX_*="false"` defaults — flip via `SPECKIT_CODE_GRAPH_MAINTAINER_MODE=true` in `.env.local` for maintainer indexing.
