deep-agent-improvement's fifteen reference files are now grouped into four topic subfolders, so scoring, promotion-gate, integration, and workflow guidance is reached by area instead of scanning one flat list.

> Spec folder: `.opencode/specs/skilled-agent-orchestration/116-deep-skill-evolution/008-deep-skill-doc-evolution` (Level 2)

---

## What Changed

#### Documentation Structure

- **Grouped references/ into scoring/, promotion-gates/, integration/, and workflow/** -- the fifteen references sat in one flat directory. They now live under four topic subfolders. The smart router discovers references recursively, so routing behavior is unchanged while navigation gets clearer.
- **Updated the README structure tree and every inbound link** -- the SKILL.md REFERENCES paths and the RESOURCE_MAP intent entries, the README structure diagram, and the command reference resolve to the subfoldered paths. A workspace sweep confirmed zero stale flat reference paths in live consumers.
