{
  "workflowMode": "system-skill-advisor",
  "packet": ".",
  "leafRoots": ["references", "assets", "feature-catalog", "manual-testing-playbook"],
  "excludeIndexFiles": true,
  "resourceContractVersion": 1,
  "_note": "Standalone single-mode manifest config. system-skill-advisor is a normal skill (no mode-registry.json); its references/feature-catalog/playbook docs are the routed leaves that the in-SKILL.md INTENT_SIGNALS + RESOURCE_MAP router selects. The mcp-server/ advisor engine (skill-graph.json, scorer/prompt-policy config, handlers) is NOT a routable leaf and is excluded because it is not under a leafRoot. Consumed by generate-leaf-manifest.cjs to emit leaf-manifest.json; leaf-aliases.json is the derived identity projection of that manifest, so regenerate both together via ci-skill-root-metadata.cjs --fix."
}
