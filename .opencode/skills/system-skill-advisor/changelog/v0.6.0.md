This release keeps the prompt-quality-card drift guard accurate after the canonical card moved skills. The guard's logic never depended on the card's path, so only its documented "canonical location" comment needed to follow the card to its new home. The guard stays green against the relocated card.

> Spec folder: `.opencode/specs/skilled-agent-orchestration/130-prompt-knowledge-layering/013-card-relocation-and-guard/` (Level 1)

---

## What Changed

#### Documentation

- **Guard canonical-location comment updated** -- the canonical `cli_prompt_quality_card.md` moved from `sk-prompt/assets/` to `sk-prompt-small-model/assets/`, which left the `check-prompt-quality-card-sync.sh` header comment pointing at a stale location. The guard's actual logic is path-agnostic (it checks the five cli-* card filenames, table-absence and SKILL.md pointers, not the canonical card's path), so only the header-comment "canonical location" reference was updated to match. CHECK 1-4 semantics, the pre-commit orchestrator wiring and the CI workflow are unchanged, and the guard exits 0 on the clean post-relocation tree, verified.

## Files Changed

| File | What changed |
| ---- | ------------ |
| `mcp_server/scripts/check-prompt-quality-card-sync.sh` | Header canonical-location comment updated to `sk-prompt-small-model/assets/cli_prompt_quality_card.md` |
| `SKILL.md` | Version 0.6.0 |

## Upgrade

No migration required.
