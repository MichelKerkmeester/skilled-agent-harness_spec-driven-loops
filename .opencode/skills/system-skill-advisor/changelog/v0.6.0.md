# system-skill-advisor v0.6.0 — card-sync guard canonical path follows the card to the hub

**Released**: 2026-06-03
**Status**: Guard maintenance. The drift guard's documented canonical-card location was updated after the card moved.

---

## Summary

The canonical `cli_prompt_quality_card.md` moved from `sk-prompt/assets/` to `sk-prompt-small-model/assets/`. The `check-prompt-quality-card-sync.sh` guard's logic is path-agnostic (it checks the 5 cli-* card filenames + table-absence + SKILL.md pointers, not the canonical card's path), so only its header-comment "canonical location" reference needed updating to match.

CHECK 1-4 semantics, the pre-commit orchestrator wiring, and the CI workflow are unchanged. The guard stays green against the relocated card.

---

## What Changed

| File / Path | Change |
|---|---|
| `mcp_server/scripts/check-prompt-quality-card-sync.sh` | Header canonical-location comment → `sk-prompt-small-model/assets/cli_prompt_quality_card.md` |
| `SKILL.md` | Version 0.6.0 |

---

## Verification

| Check | Result |
|---|---|
| Guard exit 0 on the clean tree (post-relocation) | Verified |
| CHECK 1-4 semantics unchanged | Verified |

---

## Spec folders referenced

- `.opencode/specs/skilled-agent-orchestration/130-prompt-knowledge-layering/013-card-relocation-and-guard/`
