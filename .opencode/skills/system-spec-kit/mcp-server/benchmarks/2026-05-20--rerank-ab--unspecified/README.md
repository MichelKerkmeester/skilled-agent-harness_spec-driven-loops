# 2026-05-20--rerank-ab--unspecified

_Derived after the fact from this run's stored record, not written at run time._

This run records metric arms rather than per-scenario verdicts. It has no per-scenario pass or fail list, so no failure file is derived for it; the arms themselves are the result.

## Contents

| Entry |
|---|
| `SOURCE.md` |
| `benchmark-report.md` |
| `per-probe-arm-a.jsonl` |
| `per-probe-arm-b.jsonl` |
| `per-probe.jsonl` |
| `rerank-ab-fixture.json` |
| `results.csv` |
| `scripts/` |
