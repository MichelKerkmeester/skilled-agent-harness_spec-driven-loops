# v3.7.1.0

Adopted the spec-folder authoring docs, relocated from sk-code into their proper home:

- `references/workflows/spec_folder_write_recipe.md` — the step-by-step recipe for writing a new spec folder that satisfies `validate.sh --strict` on the first try.
- `references/workflows/spec_folder_authoring_checklist.md` — the companion checklist for the Level contract, `_memory.continuity` frontmatter, metadata files, and strict validation.

Both are indexed under the COMPLETE intent in the SKILL RESOURCE_MAP, and `/speckit:complete`'s authoring-time cross-skill load now sources the spec-folder recipe and checklist from system-spec-kit directly rather than from sk-code. Spec-folder authoring is system-spec-kit's domain; sk-code keeps only a cross-reference pointer for implementers routed there.
