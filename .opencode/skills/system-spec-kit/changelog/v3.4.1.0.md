# v3.4.1.0 — Now You Can Watch It Run

v3.4.0.0 shipped the substrate. v3.4.1.0 is the release where you can see it working. The code graph found the bug that was costing it most of its symbols. The skill advisor finally ships as a real daemon backing live hooks across every runtime that has them. The CocoIndex daemon stops leaking processes and log spam under load. And the doctor commands, trust badges, freshness indicators, and telemetry surfaces this release adds are all built so a single command can answer "is this thing healthy right now?"

Alongside the hardening work, this release is also a polish pass on names, descriptions, and tests. Three skills and agents were renamed to match what they actually do. Skill and command descriptions got trimmed by 42% to fit back under Claude Code's discovery budget — and a new `/doctor:skill-budget` command keeps them honest. Manual testing playbooks for `sk-code-review`, `sk-git`, and `sk-prompt` ship 66 scenarios in natural-human voice. The Multi-AI Council agent now has a full output-protocol contract. And `cli-copilot` is retired — `cli-codex` is the cost-optimized default external executor going forward.

---

## What's New at a Glance

| | |
|---|---|
| **Memory Continuity** | Legacy folder fully retired. Eight save defects fixed. Planner-first save by default. 17 search-runtime bugs catalogued — three P0 fixed in-phase. |
| **Code Graph** | Tree-sitter parser path fix recovers ~11x more symbols in production. New `/doctor:code-graph`. Freshness tracking, scope tightening, self-contained package. |
| **Edge Explanations and Trust** | Edges carry `reason` and `step`. Memory results carry per-result trust badges. `detect_changes` preflight blocks impact analysis on stale graphs. |
| **Skill Advisor** | Native daemon backs live hooks in Claude, Codex, Copilot, Gemini, and OpenCode. Native TypeScript scorer. New `advisor_*` MCP surface. |
| **Causal Graph Routing** | Graph channel activates on intent and entity-rich short queries. New `graphChannelInvocationRate` telemetry. |
| **Hook Parity** | All four runtimes green again. 578 vitest files, 11,114 tests, zero new regressions. |
| **Resource Maps** | New cross-cutting spec doc. Auto-emitted by deep-research and deep-review at convergence. |
| **Runtime Executor** | Deep-loop skills support `native`, `cli-codex`, `cli-claude-code`, and `cli-gemini` as a first-class YAML branch with Zod-validated config. |
| **Template Levels Redesign** | Level 1/2/3/3+ taxonomy retired. Manifest-driven (`spec-kit-docs.json`) framework with orthogonal capabilities. |
| **CocoIndex Daemon Resilience** | Nine patches. Atomic PID lock. 60 MB log cap. Bounded shutdown. Broken-pipe survival. |
| **Multi-AI Council** | Full output-protocol contract shipped. v1.1 state metadata. Optional memory-save payload. Advisory completion checker. **Post-release**: scoped-write flip — council writes its own `ai-council/**` artifacts; bash + patch stay denied. |
| **Naming and Discovery** | Three skill/agent renames. Plural `.opencode/skills/` (670k occurrences). 42% description trim. New `/doctor:skill-budget` audit. |
| **Testing Playbooks** | New for `sk-code-review` (17 scenarios), `sk-git` (21), `sk-prompt` (28). Natural-human voice across all 16 playbooks. |
| **CLI Cleanup** | `cli-copilot` retired — `cli-codex` is the new default. `cli-opencode` stdin-redirect fix prevents silent hangs. |

---

## Memory Continuity, Now Production-Grade

Half of v3.4.0.0's memory migration was missing in production. Five legacy write paths still touched the old `[spec]/memory/` files. v3.4.1.0 closes the rest, fixes eight defects in the save flow, and catalogues every search-runtime bug found along the way.

#### The migration is finished

Five legacy write paths to `[spec]/memory/*.md` are fully retired. The write surface is now canonical spec docs only. 183 stale memory-file rows from prior runs were pruned, along with 1,141 dependent causal edges that referenced them.

Eight JSON-mode memory defects (D1 through D8) are fixed: truncation boundaries, junk-phrase filtering inside trigger-phrase synthesis, predecessor discovery for causal lineage, post-save verification, and metadata drift on partial-failure paths. Every defect fixture passes.

&nbsp;

#### Planner-first by default

`/memory:save` is now planner-first (non-mutating dry-run) by default with full-auto fallback. Four ancillary subsystems that previously fired on every save are gated behind opt-in env flags.

Six gates run before each save: spec-doc structure validation, anchor parity, trigger-phrase sanity, key-files canonical-path check, status derivation, and continuity-pointer integrity. A failing gate surfaces visibly instead of corrupting the save.

&nbsp;

#### Search runtime bug catalogue

A 17-bug catalogue of memory-search runtime defects shipped alongside the migration. Three P0 fixes landed in-phase: a truncation-wrapper sanity guard, an intent-classifier confidence floor (the classifier was routing at 0.098 confidence in some traces), and forbidden-phrase enforcement on synthesized triggers. The remaining 14 (Clusters 4 through 7, all P1/P2) are documented for a follow-on remediation packet.

&nbsp;

#### Memory indexer review

A 4-iteration deep-review of the memory-indexer packet (Track A lineage, Track B index-scope invariants) ran across 92 files. Verdict: CONDITIONAL. One P1 (constitutional README storage-boundary correction) and 13 P2 findings split across documentation drift, single-source-of-truth derivation, operator observability, and API contract surfaces. A live MCP rescan blocker (CHK-T15) gates the FINAL release until verified on a freshly restarted daemon.

---

## The Code Graph You Can Actually Use

The code graph was missing 60 to 80 percent of the symbols in your codebase. Not because the parser could not see them — because the parser was never running.

#### The tree-sitter parser path fix

The tree-sitter grammar path was relative to the source tree, not the compiled `dist/` location, so production silently fell back to a regex shim. After the fix, broad-scope scans produce 56,843 nodes versus the regex-fallback baseline of 5,000 — roughly 11x more symbols recovered on every scan.

&nbsp;

#### A diagnostic command

`/doctor:code-graph` is a new operator command. `auto` and `confirm` modes are diagnostic-only — they report parser status, scan freshness, scope coverage, and known stale areas. **Post-release `apply` mode** (packet 026/007/013) wires the resilience-research artifacts (28-query gold battery, 3-state staleness model, 3-procedure recovery playbook, 3-tier exclude-rule confidence) into a verification-gated auto-fix workflow with rollback semantics. Apply runs the gold battery before and after each operation; failure reverts the DB triplet to the last known-good snapshot. 25 new vitest cases pass.

&nbsp;

#### Freshness, scope, and a self-contained package

Every code-graph response now carries a freshness status and stale-graph highlighting. Default scope excludes are tightened: `z_future/`, `z_archive/`, and CocoIndex's own internal paths are skipped unless explicitly requested. `.gitignore` entries are honored. Cross-file dedup applies on every multi-file query. The whole code-graph package is now self-contained at `mcp_server/code-graph/`. 34 files migrated with zero regressions on the 52 + 85 + 171 test surfaces.

&nbsp;

#### Backend resilience

Five hardening streams shipped behind a 10-iteration deep-review and a follow-up sk-code-opencode audit: scan idempotence, query-timeout protection, error recovery, diagnostic accuracy, and test-isolation robustness. 5 P0 + 12 P1 findings closed in the first remediation; 1 P0 + 10 P1 closed in the second. Test suite at 99.8% pass.

&nbsp;

#### Hook integration parity

CALLS resolution now disambiguates fully-qualified names instead of failing on symbol-name collisions. Blocked reads return a typed `BlockedResult` with explicit reason instead of an opaque error. CocoIndex ranking fidelity is preserved end-to-end. Graph quality summaries (`graphQualitySummary`) are exposed in startup payloads, and startup-payload parity is verified across all four runtimes.

&nbsp;

#### Real-world usefulness trial

A sandbox usefulness trial ran the code graph across five surfaces in production-shaped traffic — queries, hooks, advisor, Gate 3, and runtime smoke. Outcomes mapped back to deferred cells in the implementation track, closing the loop between "we shipped it" and "it works in the way it gets used."

---

## Edge Explanations and Trust Display

Eight packets shipped under "graph impact and affordance uplift." The headlines:

#### Edge explanations

Every edge can now carry `reason` and `step` metadata, and `blast_radius` returns depth groups, risk levels, confidence filtering, and ambiguity candidates. The output is no longer a flat list of files — it is a navigable explanation of why each file is implicated.

&nbsp;

#### Memory trust display

Memory search results now carry per-result trust badges: confidence, age, and orphan state, all derived from causal-edge data. Orphan results — memory rows with no incoming or outgoing edges — are visibly flagged.

&nbsp;

#### Affordance routing

Tool and resource affordances flow through existing scorer lanes via a sanitization step instead of expanding the graph vocabulary. The advisor can route on "this skill needs CocoIndex" or "this skill writes to the spec folder" without polluting the runtime graph.

&nbsp;

#### A read-only preflight

A new `detect_changes` handler is the read-only preflight for impact analysis. If the graph is stale, it blocks the analysis and tells the operator to rescan — instead of returning silently incorrect impact maps.

&nbsp;

#### Clean-room license posture

A clean-room adaptation audit cleared the governance posture for the remaining external work and recorded PolyForm Noncommercial 1.0.0 as the operating license for the affected packets. Umbrella docs across the root README, MCP server docs, install guide, feature catalogs, and testing-playbook indexes were rolled up against the new shipped capabilities so nothing on the surface still describes the old behavior.

---

## Skill Advisor: Now a Real Daemon

The advisor finally ships as a daemon. v3.4.0.0 cleaned up the routing dictionaries and shipped the graph compiler. v3.4.1.0 is the runtime that uses both.

#### Native daemon, single-writer lease

A daemon now backs the advisor with watcher-driven freshness and a single-writer lease that prevents concurrent processes from corrupting the compiled graph. The previous Python script and OpenCode plugin paths still work — they route through compatibility shims to the native behavior. A new MCP surface — `advisor_recommend`, `advisor_status`, `advisor_validate` — exposes the advisor as a first-class tool instead of a hook-only side effect.

&nbsp;

#### Live hooks across every runtime

Each `UserPromptSubmit` event in Claude, Gemini, Copilot, and Codex now runs `buildSkillAdvisorBrief(prompt, {runtime})` and emits a sanitized skill recommendation with freshness, confidence, and uncertainty fields. The fail-open contract returns `{}` on any error, so a misbehaving advisor never blocks a prompt. Cross-runtime corpus parity: 200 of 200 fixtures match. Cache hit-rate 66.7%, cache hit p95 latency 0.016 ms.

A 650-line reference doc at `references/hooks/skill-advisor-hook.md` (DQI 97/100) documents the contract, and 11 test files plus 10 canonical fixtures lock the behavior in place.

&nbsp;

#### Native TypeScript scorer

The five-lane fusion scorer is rewritten in native TypeScript. Family affinity and graph-derived transitive boosts both flow through the new path. The 52-fixture regression suite still passes at 100% top-1 accuracy through the migration.

&nbsp;

#### Smart router and plugin hardening

The smart router shipped six remediation areas from the upstream research packet: stale-route resource cleanup, static router validation in CI, conservatively-tuned ON_DEMAND keywords (hit rate climbed from 5.5% to 48.0%), explicit UNKNOWN disambiguation in place of silent CLI fallback, observe-only telemetry, and a bridge-backed OpenCode plugin (`spec-kit-skill-advisor`) that imports the same producer and renderer the hooks use.

The plugin itself was hardened across three deferred audit items: per-instance closure state (no shared module state across plugin instances), an in-flight bridge-promise dedup so concurrent identical cache misses share one bridge call, and bounded payloads with cache LRU eviction. Tests grew from 23 to 30.

&nbsp;

#### Setup command, standards alignment, and ambiguity fix

`/doctor:skill-advisor` is a new operator command that analyzes the current skill set, optimizes advisor scoring tables, and re-indexes the skill graph. A standards-alignment pass across the 21 skill metadata files closed routing inconsistencies that accumulated as the advisor evolved.

The ambiguity detector also got a calibration fix: a candidate is now flagged when EITHER score gap ≤ 0.05 OR confidence gap ≤ 0.05, not score-only. 37 scorer tests plus 40 handler tests pass.

---

## Causal Graph Routing: 1,328 Edges, Now in Use

The causal-edge graph holds 1,328 live edges. Before this release, the graph channel only fired at the complex query tier (>8 terms), so those edges sat unused for natural 1–5-term queries.

The router now activates the graph channel for `find_decision` and `find_spec` intents at any tier, plus entity-rich queries that name memory rows with three or more outgoing causal edges. The override sits at the routing layer above the tier classifier, which preserves tier stability for telemetry while expanding the channel set when the new gates fire.

New telemetry: `memory_health.data.routing.graphChannelInvocationRate` exposes the per-channel invocation rate over a 200-decision rolling ring buffer. A `SPECKIT_GRAPH_CHANNEL_PRESERVATION` env flag (default ON) allows opt-out.

p99 routing latency stays under 5 ms. Live runs show graph-channel invocation at 0.625 after the new gates take traffic — well above the 0.30 floor. 25 new tests cover the routing surface.

---

## Hook Parity Across All Four Runtimes

Three of the four runtimes were silently dropping startup payloads in some configurations. v3.4.1.0 puts them back on the same page.

- **OpenCode plugin** emits visible diagnostics on transport parse failure instead of silently dropping the code-graph context. The plugin loader was hardened against unsafe paths in a follow-up review.
- **Codex advisor hook** returns a stale-diagnostic banner on timeout instead of a blank response. Codex's `pre-tool-use` and `prompt-wrapper` hooks share the same builder, timeout, and durable-diagnostics contract as the prompt-submission path.
- **Copilot startup** routes through the correct repo-local script instead of the global one. The wrapper schema bug that blocked tool-call telemetry is fixed, and the writer-wiring fix closed a separate payload-shape regression.
- **PreToolUse denylist** gains alias coverage for the canonical commands.

13 external documentation files were updated to match the new shipped behavior — READMEs, install guides, architecture docs, feature catalogs, and testing playbooks — so nothing on the docs surface still describes pre-fix behavior. Full vitest baseline: 578 passed files, 11,114 passed tests, 31 skipped, zero new regressions.

---

## Resource Maps

A new spec doc joins the canonical set: `resource-map.md`. It is a flat, scannable inventory of files a packet touched.

The template lives at the same tier as `handover.md`, `debug-delegation.md`, `research.md`, and `decision-record.md`. It is auto-emitted by `sk-deep-review` and `sk-deep-research` at convergence — review-shaped maps carry P0/P1/P2 finding counts, research-shaped maps carry per-iteration citation counts. Both shapes consume evidence the loop runtime already captures, so there is no new scan cost.

135 child packets were migrated to local-owner folders for the new template, and 155 canonical references were rewritten across the docs surface. 541 spec-folder packets were refreshed with the new graph metadata.

---

## Runtime Executor: Pick Your Model

Deep-loop skills (`sk-deep-research`, `sk-deep-review`) previously hardcoded `model: opus` and forbade CLI-in-a-loop. The substrate now supports executor selection as a first-class YAML dispatch branch with Zod-validated config.

Available executors: `native`, `cli-codex`, `cli-claude-code`, and `cli-gemini`. Per-kind flag validation rejects invalid combinations (for example, `service-tier` on a non-codex CLI). Markdown prompt packs are now executor-agnostic — model-specific directives are stripped at dispatch time. The native path is byte-for-byte identical to the v3.4.0.0 baseline.

A 30-iteration cli-codex dogfood run produced 12 closed remediation IDs downstream. 116 new tests cover the executor selection surface. A 7-iteration deep-review of the predecessor remediation packet (016) reached a CONDITIONAL verdict with 0 P0, 10 P1, and 18 P2 findings; the four primary P0 composites were re-verified.

---

## Template Levels, Redesigned

The Level 1 / Level 2 / Level 3 / Level 3+ taxonomy is being retired. It conflated three orthogonal concerns — line count, capability set, and template shape — into one flat dimension that did not scale to phase parents, audit packets, or research-only packets.

A 9-iteration deep-research loop (`cli-codex gpt-5.5 high`) ran on the consolidation question. The PARTIAL backward-compat framing was rejected. The converged design is a **C+F hybrid manifest-driven framework**: capabilities and presets live in a single `spec-kit-docs.json` manifest, scaffolders and validators both read from it, and human-authored scaffold docs are distinguished from command/agent-owned lazy addons. Five ADRs formalize the decisions.

A four-phase implementation plan: ADD manifest and resolver (shipped), MODIFY scaffolder, MODIFY validators, DELETE legacy taxonomy code. The validator stays inclusive of legacy phase parents during the transition. 86 source files collapse into roughly 15 manifest files in the new design.

Four sweep packets cleaned up the surface in parallel: stale references in `SKILL.md` and skill-bundled assets (with brief mentions added for current features like `SPECKIT_POST_VALIDATE`, exit codes, path hardening, save locking, batch inline rendering, and phase-mode syntax), command MD/YAML alignment, fleet-wide `SPECKIT_LEVEL` and `SPECKIT_TEMPLATE_SOURCE` marker validation, and a separate sweep over the `z_archive/` corpus.

---

## CocoIndex Daemon Resilience

The `mcp-coco-index` daemon was leaking zombie processes (244 open file descriptors held by stale daemons), burning ~90% CPU formatting tracebacks on every client disconnect, growing logs without bound (23 MB observed before the fix), and spawning duplicate daemons under concurrent-start storms.

Nine patches across five hardening phases.

#### Broken-pipe survival

A `_safe_send_bytes` wrapper now catches the broken-pipe condition and recovers instead of double-crashing the writer. The 564 spurious `BrokenPipeError` lines per run are gone, and the disconnect-loop CPU spike with them.

&nbsp;

#### Log cap

Logs cap at 10 MB per file with 5 backups (60 MB total) instead of growing without bound. A logging double-write bug — every log line was emitted twice through both a stream handler and a file handler — is fixed via a conditional `StreamHandler` that engages only on TTY.

&nbsp;

#### Concurrent-start storms

Concurrent daemon starts now route through an atomic PID-file lock (`fcntl.flock` on POSIX, `msvcrt.locking` on Win32). A sibling-detection block fires inside the lock-held window before the PID write, so two simultaneous starts converge on a single survivor instead of both winning. Listener backlog is also raised from the stdlib default of 1 to 128, so 16-way client storms queue at the OS level instead of failing with connection-refused.

&nbsp;

#### Bounded shutdown

Shutdown is bounded by a 10-second `asyncio.wait_for` cap on task-join. The daemon no longer hangs indefinitely on stuck tasks.

&nbsp;

#### Operator recovery

A single recovery procedure now works whether the daemon is half-stuck, fully stuck, or duplicated. 20 of 20 pytest pass — 9 unit tests and 7 E2E/integration tests, plus 4 covering the storm scenarios.

---

## Multi-AI Council, Productionized

The Multi-AI Council agent now ships a complete output protocol contract instead of an implicit one. Agent body §12 through §17 document the output protocol, invocation contract, state schema, convergence signal, caller-persistence protocol, and summary. Four shared reference files under `system-spec-kit/references/multi-ai-council/` cover folder layout, seat diversity patterns, convergence signals, and JSONL state format.

A v1.1 state metadata schema with `schema_version`, `protocol`, and `producer` fields is forward-compatible (v1 readers still parse it). A new `--memory-save-payload-out` flag writes a memory-save payload only on opt-in. An advisory `advise-council-completion.cjs` script reports missing artifacts, missing reports, and seat-count mismatches, and always exits 0 — so it works in pre-commit hooks without gating strict validation.

The validator regression test confirms the new `ai-council/` artifact folder pattern is tolerated. Four-runtime mirror parity tests prevent the agent body from drifting between Claude, Codex, Gemini, and OpenCode definitions.

**Post-release: scoped-write flip (packet 098).** The council was originally planning-only. After ADR-001/002/003, it was flipped to scoped-write — the four runtime mirrors flipped from `write: deny / edit: deny` to `write: allow / edit: allow`, with `bash: deny` and `patch: deny` preserved. Path-scope to `ai-council/**` is enforced by the writer library (`scripts/multi-ai-council/lib/persist-artifacts.js` throws `OUT_OF_SCOPE_WRITE` for any out-of-bounds target) — OpenCode's permission schema doesn't natively support path-array scoping, so defense lives at the lib layer. The audit-trail schema went additive v1.1 → v1.2 with new `artifact_written` and `rollback` event types. Round-NNN folder is the rollback unit; failed rounds preserved in `failed/round-NNN-<timestamp>/`. 7 new vitest cases pass (permission-scope, runtime-parity, audit-trail, rollback).

---

## Naming and Discovery, Cleaned Up

Three skills and agents were renamed to match what they actually do:

- `sk-improve-prompt` → **`sk-prompt`** — aligns with the single-capability skill family (`sk-code`, `sk-doc`, `sk-git`). 51 active files, ~200 references touched.
- `@improve-prompt` → **`@prompt-improver`** — agent name family is now noun-form (`@code`, `@review`, `@debug`, `@orchestrate`, `@prompt-improver`). ~31 active references.
- `@improve-agent` → **`@deep-agent-improvement`** — completes the family alignment with the underlying `deep-agent-improvement` skill and the `/improve:agent` command. 8 file renames + 54 active references.

The `.opencode/skill/` directory was renamed to plural `.opencode/skills/` (and similarly for sibling runtime mounts). 11k file changes across 670k occurrences over four sub-phases (skills, agents, commands, symlinks). Three subsequent deep-reviews surfaced 22, then 13, then 6 findings; each followed by targeted remediation. Final state: 40 P0+P1 findings resolved, 1 P2 cosmetic deferred.

#### Descriptions trimmed back under the discovery budget

A multi-packet trim arc brought project descriptions from 7,646 → 5,391 chars (42% reduction) while preserving routing keywords. 16 skill plus 20 command frontmatter descriptions trimmed in the first pass (skills avg 251 → 126 chars, commands avg 157 → 104 chars). Six over-soft agent descriptions trimmed across 24 runtime-mirror files. Eight remaining over-soft items trimmed to zero audit findings. Final headroom: +209 chars under the 5,600 ceiling.

#### Guardrails so it stays trimmed

A new `/doctor:skill-budget` audit command reports per-item table, project total, headroom, and suggested trims. `--json` and `--fail-over=N` flags make it scriptable for CI and pre-commit. New soft-target validation in `quick_validate.py` (≤130 skill / ≤110 command) is wired into `/create:sk-skill` and `/create:agent`. Updated sk-doc authoring docs declare the targets explicitly: 1,536-char hard cap, 5,600-char project soft ceiling.

---

## Testing Playbooks That Read Like Humans

Three new manual-testing playbooks shipped this release:

- **`sk-code-review`** — 17 scenarios across 6 categories covering small PR, large refactor, multi-commit, auth, injection, secrets, blockers, class-of-bug, cross-consumer, scope, baseline-vs-surface, test code, re-review, stale architecture, and AI-generated suspect quality. 18 of 18 scenarios pass when executed via `opencode run --model deepseek/deepseek-chat`, including three cross-CLI re-runs.
- **`sk-git`** — 21 scenarios covering the worktree → commit → finish lifecycle.
- **`sk-prompt`** — 28 scenarios across 7 categories covering mode detection, smart routing, depth/clear/loop, CLEAR scoring, framework selection, escalation tiers, and format modes.

All sixteen manual-testing playbooks were also refactored from formulaic "As a {ROLE} ..." prompts to natural-human voice. RCAF-pattern prompts are reserved for cross-CLI delegation, multi-agent dispatch, and safety-refusal flows where role context determines behavior. The prompt-equality contract is preserved byte-for-byte across both locations (SCENARIO CONTRACT and the 9-column scenario table).

---

## CLI Cleanup

`cli-copilot` is retired. GitHub Copilot price increases pushed it above the cost threshold. The entire skill tree is gone — 50+ playbook scenarios, hooks, matrix-runner adapter, and 2 test files — and 101 files across sibling cli-* skills, the advisor, agents, commands, and root docs were scrubbed of references. The advisor never recommends `cli-copilot` again. `cli-codex` is the cost-optimized default external executor going forward.

`opencode run` had a silent-hang bug under stdout/stderr redirect. The CLI reads stdin during startup, and without an explicit EOF the read blocked forever at 0% CPU. The fix is nine characters per dispatch — `</dev/null` — applied to four YAML workflow files (deep-research/deep-review auto/confirm), the `cli-opencode` skill, and two stress-test dispatch scripts. The rule is now documented as a permanent ALWAYS in `cli-opencode` SKILL.md §4 so future automation gets it right at write time. The bug had been costing 12 minutes per dispatch in stuck timeout cycles.

---

## Post-Release Hardening

The release shipped at commit `9a1a587b8`. Four follow-up packets and a config-alignment pass landed afterwards, mostly to close items the original release-note flagged as deferred — and to recover from a vitest baseline that turned out to be overstated. All folded back into v3.4.1.0 because the tag was not yet cut.

#### Vitest baseline recovery

The original "Core test suites — 11,606 passed, 0 net regressions" line was overstated. A full-repo `pnpm vitest run` after the release packet landed reported 11,587 passed / 198 failed across 166 files — pre-existing baseline drift accumulated across the v3.4.x arc that was never captured in CI.

Three follow-up packets recovered the baseline:

- **`026/000/006-vitest-baseline-recovery`** triaged the 198 into four buckets (fixture-drift / runtime-regression / environmental / flaky), fixed 27 fixture-drift cases in-packet, and parked 152 runtime-regression cases as `it.fails.skip` with `// followup-actual:` annotations.
- **`026/000/007-vitest-recovery-followup`** Unit H (no-LOC-cap re-dispatch) closed all 138 parks: 129 stale-assertion updates (test asserted the OLD contract; updated to match shipped behavior), 8 source-fix paths (3 real bugs — see below), 1 deletion (test asserted retired behavior). Zero blocked-on annotations; codex didn't take an exit hatch.
- **`026/000/007` Unit I** closed 11 `it.todo` placeholders: built a shared in-memory SQLite DB-fixture helper at `mcp_server/tests/helpers/memory-db-fixture.ts`, implemented all 9 DB-backed tests against the production schema (real `searchWithFallback`, `processReview`, `accessTracker.flushAccessCounts` paths exercised — not mocks), and exported `buildCompactContext` + `tailFile` from `compact-inject.ts` to address the 2 edge-case todos.

Final vitest baseline: **11,815 passed / 0 failed / 90 skipped (env-gated) / 0 todo**. The 90 skipped are healthy `it.skipIf` / `ctx.skip` patterns conditioned on optional daemons, fixtures, and binaries — they execute when the environment provides them.

&nbsp;

#### Three real source bugs caught by the recovery

Unit H's strict-fix mandate surfaced three genuine source defects that had been silently shipping:

1. **`mcp_server/lib/utils/index-scope.ts`** — code-graph indexer still matched the singular `.opencode/skill/`, `.opencode/agent/`, `.opencode/command/` roots after the plural rename in packet 096. Shipped plural files were treated as out-of-scope. Fix: switched matching to plural roots; verified with code-graph indexer tests.

2. **`mcp_server/lib/architecture/layer-definitions.ts`** — `memory_retention_sweep` was registered as an L4 mutation tool but missing from the L4 tool list, leaving architecture/layer coverage inconsistent. Fix: added to the L4 set; verified with layer-definition tests.

3. **`mcp_server/code_graph/lib/structural-indexer.ts`** — diagnostic logging used `console.info`, which writes to stdout and corrupts MCP stdio responses when invoked through runtime surfaces. The third bug in particular was protocol-level and would have stayed silent if Unit G's blanket-skip had stuck. Fix: moved diagnostics to `console.error`; updated stdio-safety + structural-logging tests.

&nbsp;

#### Codex CLI alignment

Codex CLI 0.130 deprecated `[features].codex_hooks` in favor of `[features].hooks` per the official feature-flags doc. User-level `~/.codex/config.toml` was renamed; project-local `.codex/config.toml` had `[profiles.*]` blocks deleted (Codex ignores those at project level — moved to user-level so `codex exec -p <name>` resolves). Stale `.opencode/skill/` (singular) paths in `~/.codex/hooks.json` and the `spec_kit_memory` MCP server config were updated to plural. The hook-policy detector at `mcp_server/lib/codex-hook-policy.ts` now matches both the new `hooks` and legacy `codex_hooks` keys during the transition. After the migration, the `codex-hook-policy` vitest suite passes 6/6.

&nbsp;

#### Multi-AI Council schema correction

ADR-001 in packet 098 originally proposed `write: { mode: allow, paths: ["ai-council/**"] }` as the YAML form. OpenCode's permission schema rejects path-array on `PermissionActionConfig` — the valid forms are simple `allow|ask|deny` strings or glob-keyed maps. Fix: simple `write: allow / edit: allow` across all four runtime mirrors; path-scope to `ai-council/**` enforced at the writer-library layer (the lib already throws `OUT_OF_SCOPE_WRITE` for any out-of-bounds target). ADR-001's "OpenCode schema note (corrected 2026-05-09)" documents the discovery.

&nbsp;

#### Codex sandbox residue cleanup

cli-codex hit `EPERM` writing to `.codex/agents/multi-ai-council.toml` from within its own sandbox — codex's sandbox refuses self-tree writes by design. Claude (different sandbox) applied the TOML flip after the cli-codex run. Separately, errant double-nested `.opencode/` shell directories (~52 MB of stray `node_modules` + 2 git-tracked files) created by cli-codex runs that briefly thought they were operating in nested CWDs were purged.

---

## Verification Highlights

| Check | Result |
|---|---|
| TypeScript compile | PASS |
| Strict validation across the 026 phase tree | PASS |
| Core test suites (vitest) | PASS — 11,815 passed / 0 failed / 90 pending / 0 todo after 026/000/007 Unit I closed all 11 it.todo placeholders (DB fixture for memory-search integration + exported buildCompactContext for edge-case tests) |
| Code-graph regression floor | PASS — 17 frozen tests |
| Code-graph self-contained package | PASS — 52 + 85 + 171 tests |
| Skill advisor regression | PASS — 52/52, 100% top-1 |
| Skill advisor cross-runtime corpus parity | PASS — 200/200, cache hit p95 0.016 ms |
| Memory quality (D1–D8 fixtures) | PASS |
| Hook parity (4 runtimes) | PASS — 578 vitest files, 11,114 tests |
| CocoIndex daemon | PASS — 20/20 pytest (unit + E2E) |
| Runtime executor hardening | PASS — 116 new tests, byte-for-byte native parity |
| Causal-graph channel routing | PASS — 25 new tests, p99 < 5 ms, live invocation 0.625 |
| Tree-sitter parser-path fix | PASS — ~11x symbol recovery (5,000 → 56,843 nodes) |
| Tree-sitter broad-scan crash floor | PASS — 17.5% → 0.72% (24x improvement), zero B2 cascades on 9,314-file scan |
| Concurrent daemon storm test | PASS — 1 survivor (was 2) |
| Resource-map backfill | PASS — 541 spec-folder packets refreshed |
| `cli-copilot` retirement grep sweep | PASS — 0 residual references in active surfaces |
| `sk-prompt` rename grep sweep | PASS — 51 active files updated, 0 stale refs |
| `.opencode/skills/` plural rename | PASS — 670k occurrences across 11k files, 40 findings closed |
| Description budget audit | PASS — project total 5,391 chars / +209 headroom |
| Multi-AI Council 4-runtime mirror parity | PASS |
| `sk-code-review` playbook execution | PASS — 18/18 via DeepSeek |
| Deep-research / deep-review iterations across this release | 9 (templates) · 10 (graph-impact) · 10 (skill-advisor unification) · 7 (executor hardening) · 5 (daemon resilience) · 4 (memory-indexer review) · 10 (smart-router) · 8-phase rename remediation cycle |

---

## Known Limitations

- **Memory search runtime bugs (Clusters 4–7):** Catalogued, with three P0 fixes shipped in-phase. The remaining P1/P2 items are tracked in `026/003/006-memory-search-clusters-4-7-remediation` (closed for P0/P1; P2 refinement open).
- **Memory indexer FINAL release:** Gated on the live MCP rescan blocker (CHK-T15) — operator restart action.
- **Code-graph usefulness verdict re-evaluation:** Phase 012/007 fixed the tree-sitter broad-scan B1/B2 crash (17.5% → 0.72% crash rate, zero B2 events on a 9,314-file scan). The OVERHEAD verdict from the prior real-world usefulness trial is pending re-evaluation against the new crash floor. Copilot payload-transport and skill-graph stale-cache items previously listed here were closed by the same rerun.
- **Template phase 4 follow-on optimizations:** Validation hot-path batching, manifest schema extensions, cross-session lineage checks, and exit-code taxonomy cleanup (all P2 performance/schema work). The Gate 7 follow-ups and the four-phase legacy-taxonomy deletion (ADD → MODIFY scaffolder → MODIFY validators → DELETE legacy) all shipped this release; only these P2 optimizations remain.
- **Causal-graph live MCP smoke:** Procedure documented in `scratch/post-change.md`; closes SC-001 once the next daemon restart verifies.
- **Codex hooks approval gate:** After any edit to `~/.codex/hooks.json`, Codex CLI shows "5 hooks need review before they can run." The user must run `codex` (TUI) → `/hooks` once to approve. Approval persists until the file changes again.

#### Closed since release (formerly listed here)

- ~~Code-graph apply-mode auto-fix~~ — shipped via `026/007/013-doctor-apply-mode-phase-b`.
- ~~Multi-AI Council artifact writes~~ — shipped via `098-multi-ai-council-write-authority`.
- ~~Plural rename P2-032 cosmetic~~ — closed via `096/009-p2-032-cleanup`.

---

## Upgrade

- **`/spec_kit:start`** is gone (since v3.4.0.0). Use `/spec_kit:plan --intake-only`.
- **`@improve-prompt`** → **`@prompt-improver`**. External callers must update.
- **`@improve-agent`** → **`@deep-agent-improvement`**. External callers must update.
- **`sk-improve-prompt`** → **`sk-prompt`**. External callers must update.
- **`cli-copilot`** is retired. Use `cli-codex` (cost-optimized) or `cli-claude-code`.
- **`.opencode/skill/`** → **`.opencode/skills/`**. External symlinks pointing at the singular form will break (already-known issue, fix is a 5-character path update).
- **`opencode run`** under stdout redirect now requires `</dev/null` to avoid the stdin-blocking hang. Documented in `cli-opencode` SKILL.md §4.
- **No migration required** for memory continuity — the legacy `memory/*.md` files in old spec folders remain readable as archive but no new writes flow there.

---

## Full Details

Per-phase implementation docs, decision records, research artifacts, and the full set of 85 phase changelogs:
`.opencode/specs/system-spec-kit/026-graph-and-context-optimization/`

Skilled-agent-orchestration packets covering renames, descriptions, multi-AI council, and testing playbooks:
`.opencode/specs/skilled-agent-orchestration/080-097/`

The phase-changelog index lives at `.opencode/specs/system-spec-kit/026-graph-and-context-optimization/changelog/` (relative symlinks to per-phase changelogs, one entry per packet, named `changelog-026-<phase>-<slug>.md`).
