deep-research's reference library is now grouped into four thematic subfolders, so operators and the smart router reach convergence, state, protocol, and guide docs by topic instead of scanning one flat list of thirteen files.

> Spec folder: `.opencode/specs/skilled-agent-orchestration/116-deep-skill-evolution/008-deep-skill-doc-evolution` (Level 2)

---

## What Changed

#### Documentation Structure

- **Grouped references/ into convergence/, state/, protocol/, and guides/** -- the thirteen reference files sat in one flat directory. They now live under four topic subfolders. The smart router discovers references recursively, so routing behavior is unchanged while navigation gets clearer.
- **Updated the README structure tree and every inbound link** -- the SKILL.md REFERENCES paths, the README structure diagram, the command and agent-mirror references, and the cross-skill deep-review pointer all resolve to the subfoldered paths. A workspace sweep confirmed zero stale flat reference paths in live consumers.
