---
title: "Post-032 24-- scenario suite evidence"
description: "Blocked preflight for the post-032 15-scenario local-LLM memory substrate suite."
trigger_phrases:
  - "run 2026-05-14b post 032"
  - "24 scenario suite blocked preflight"
  - "kimi-k2.6 suite evidence"
  - "local llm query intelligence evidence"
importance_tier: "important"
---

# Post-032 24-- Scenario Suite Evidence

## Header

| Field | Value |
|-------|-------|
| Executor | cli-codex gpt-5.5 high fast |
| Requested runner | `cli-opencode` with `opencode-go/kimi-k2.6 --variant high` |
| Packet | `.opencode/specs/system-spec-kit/026-graph-and-context-optimization/014-local-llama-cpp/032-substrate-repair-followups/002-rerun-24-scenarios-suite` |
| Evidence date | 2026-05-14 |
| Suite state | BLOCKED before scenario execution |

## Substrate State At Start

The required Memory MCP preflight could not complete. The direct Code Mode MCP bridge in this Codex session returned `user cancelled MCP tool call` for tool discovery, so the intended fallback was to dispatch through OpenCode, which has the project MCP runtime.

OpenCode itself started only after isolating its state database under `/private/tmp/opencode-data`; the default state database failed on `PRAGMA wal_checkpoint(PASSIVE)`. With the temp OpenCode data directory, provider credentials loaded, but the actual preflight still failed before `memory_health` could return:

| Check | Result | Evidence |
|-------|--------|----------|
| `opencode providers list` | PASS | OpenAI, DeepSeek, and OpenCode Go credentials were present. |
| `opencode run --pure -m opencode-go/kimi-k2.6 --variant high ...` | FAIL | Provider call returned `ConnectionRefused` / `FailedToOpenSocket` for `https://opencode.ai/zen/go/v1/chat/completions`. |
| `spec_kit_memory` MCP startup inside OpenCode | FAIL | `ERR_MODULE_NOT_FOUND`: missing package `zod-to-json-schema` imported by `@modelcontextprotocol/sdk/dist/esm/server/zod-json-schema-compat.js`. |
| Direct launcher probe | FAIL | `node .opencode/bin/spec-kit-memory-launcher.cjs --help` failed with the same missing `zod-to-json-schema` error. |
| `npm ls zod-to-json-schema --depth=0` in `system-spec-kit/mcp_server` | FAIL | Returned `(empty)`. |

The scenario suite was not run because the spec requires `memory_health` and a sample `memory_save` round-trip before execution. Running 15 model dispatches without a working Memory MCP would only produce invalid noise.

## Summary Table

| # | Title | Verdict | Key observation |
|---|-------|---------|-----------------|
| 401 | Paraphrase recall | SKIP | Preflight blocked: Memory MCP did not start, so save/search/delete could not be validated. |
| 402 | Synonymy across vocabularies | SKIP | Preflight blocked before query execution. |
| 403 | Code-intent matching (implementation vs docs) | SKIP | Preflight blocked before query execution. |
| 404 | Disambiguation under context | SKIP | Preflight blocked before query execution. |
| 405 | Multi-aspect query synthesis | SKIP | Preflight blocked before query execution. |
| 406 | Specificity ladder | SKIP | Preflight blocked before query execution. |
| 407 | Adversarial near-miss (lexical overlap, semantic distance) | SKIP | Preflight blocked before query execution. |
| 408 | Compound concept synthesis | SKIP | Preflight blocked before query execution. |
| 409 | LLM-made memory recall (round-trip quality) | SKIP | Preflight blocked before query execution. |
| 410 | Query latency + throughput under realistic load | SKIP | Preflight blocked before query execution. |
| 411 | Causal graph link quality | SKIP | Preflight blocked: Memory MCP did not start, so save/search/delete could not be validated. |
| 412 | Causal coverage under bulk save | SKIP | Preflight blocked: Memory MCP did not start, so bulk saves could not be validated. |
| 413 | Drift detection quality | SKIP | Preflight blocked: Memory MCP did not start, so baseline save/search/delete could not be validated. |
| 414 | Cross-AI memory handoff | SKIP | Preflight blocked: Memory MCP and OpenCode provider path were both unavailable. |
| 415 | Concurrent multi-AI safety | SKIP | Preflight blocked: Memory MCP and OpenCode provider path were both unavailable. |

## Per-Scenario Detail

### 401 — Paraphrase recall

VERDICT: SKIP

KEY_METRIC: Memory MCP preflight failed before scenario execution.

DETAIL: This save-heavy scenario requires a real save/search/delete round-trip. The Memory MCP launcher failed with missing `zod-to-json-schema`, so executing the scenario would not test the intended substrate.

### 402 — Synonymy across vocabularies

VERDICT: SKIP

KEY_METRIC: Query substrate unavailable.

DETAIL: The scenario was not attempted because required preflight checks did not pass. No query-quality result was generated.

### 403 — Code-intent matching (implementation vs docs)

VERDICT: SKIP

KEY_METRIC: Query substrate unavailable.

DETAIL: The scenario was not attempted because required preflight checks did not pass. No implementation-vs-docs matching result was generated.

### 404 — Disambiguation under context

VERDICT: SKIP

KEY_METRIC: Query substrate unavailable.

DETAIL: The scenario was not attempted because required preflight checks did not pass. No disambiguation result was generated.

### 405 — Multi-aspect query synthesis

VERDICT: SKIP

KEY_METRIC: Query substrate unavailable.

DETAIL: The scenario was not attempted because required preflight checks did not pass. No synthesis result was generated.

### 406 — Specificity ladder

VERDICT: SKIP

KEY_METRIC: Query substrate unavailable.

DETAIL: The scenario was not attempted because required preflight checks did not pass. No specificity-ladder result was generated.

### 407 — Adversarial near-miss

VERDICT: SKIP

KEY_METRIC: Query substrate unavailable.

DETAIL: The scenario was not attempted because required preflight checks did not pass. No adversarial near-miss result was generated.

### 408 — Compound concept synthesis

VERDICT: SKIP

KEY_METRIC: Query substrate unavailable.

DETAIL: The scenario was not attempted because required preflight checks did not pass. No compound synthesis result was generated.

### 409 — LLM-made memory recall

VERDICT: SKIP

KEY_METRIC: Query substrate unavailable.

DETAIL: The scenario was not attempted because required preflight checks did not pass. No LLM-made memory recall result was generated.

### 410 — Query latency + throughput

VERDICT: SKIP

KEY_METRIC: Query substrate unavailable.

DETAIL: The scenario was not attempted because required preflight checks did not pass. No latency or throughput measurement was generated.

### 411 — Causal graph link quality

VERDICT: SKIP

KEY_METRIC: Memory MCP preflight failed before scenario execution.

DETAIL: This save-heavy scenario requires three real `memory_save` calls and graph/link validation. The Memory MCP launcher failed before tool registration, so the result would not be meaningful.

### 412 — Causal coverage under bulk save

VERDICT: SKIP

KEY_METRIC: Memory MCP preflight failed before scenario execution.

DETAIL: This save-heavy scenario requires 20 real memory saves. The Memory MCP launcher failed before tool registration, so bulk-save coverage could not be measured.

### 413 — Drift detection quality

VERDICT: SKIP

KEY_METRIC: Memory MCP preflight failed before scenario execution.

DETAIL: This scenario requires baseline memory creation before drift explanation. The Memory MCP launcher failed before tool registration, so no drift result was generated.

### 414 — Cross-AI memory handoff

VERDICT: SKIP

KEY_METRIC: Memory MCP and provider dispatch were unavailable.

DETAIL: This cross-AI scenario requires both a working memory substrate and successful external CLI dispatch. OpenCode reported missing `zod-to-json-schema` for `spec_kit_memory`, and `opencode-go/kimi-k2.6` returned provider connection errors.

### 415 — Concurrent multi-AI safety

VERDICT: SKIP

KEY_METRIC: Memory MCP and provider dispatch were unavailable.

DETAIL: This scenario requires concurrent save/search activity through the real MCP tools. The Memory MCP failed during startup, and the provider path did not complete a model call.

## Bottom Line

| Verdict | Count |
|---------|-------|
| PASS | 0 |
| PARTIAL | 0 |
| FAIL | 0 |
| SKIP | 15 |

Overall finding: BLOCKED. The earlier `run-2026-05-14-kimi-k2.6.md` baseline reportedly showed `0 PASS / 1 PARTIAL / 7 FAIL / 1 SKIP` with six save-heavy scenarios skipped due to the old E081 trap; this run cannot provide a meaningful comparison because the Memory MCP and model dispatch path failed before the preflight gate. The important difference is not substrate quality signal; it is a new runner/runtime prerequisite failure: `zod-to-json-schema` is absent from the installed MCP dependency tree, and OpenCode provider calls are unavailable in this sandboxed Codex environment.

## Cleanup

The `_sandbox/032-002-preflight/` directory was never created because the health gate failed first. No per-scenario sandbox directories were created by this executor.
