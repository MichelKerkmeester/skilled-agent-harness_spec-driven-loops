# 24 Local-LLM Memory Substrate Validation — Search-Only Run — 2026-05-14

Executor: cli-opencode / opencode-go/kimi-k2.6 (high variant)
Substrate snapshot at start: llama-cpp/EmbeddingGemma — circuit OPEN, 214 failed + 60 queued embeddings, vector channel lexical-only fallback
Strategy: 9 search-only scenarios (402-410); save-heavy scenarios (401, 411-415) skipped due to substrate circuit-breaker state
Started: 2026-05-14 09:04:51

## Summary
| # | Title | Verdict | Key observation |
|---|-------|---------|------------------|
| 402 | Synonymy across vocabularies | FAIL | top-5 Jaccard 25% (A), 0% (B), 0% (C), 0% (D); vector channel down, CocoIndex errors/timeouts |
| 403 | Code-intent matching | SKIP | CocoIndex daemon over_cap, all 4 queries failed (timeouts/truncation errors), zero results returned |
| 404 | Disambiguation under context | PARTIAL | 2/3 CocoIndex variants disambiguated; variant 3 returned 0 file-system hits |
| 405 | Multi-aspect query synthesis | FAIL | vector channel down → FTS5 lexical-only fallback; CocoIndex top-5 missed Metal (M) entirely; only 1 multi-aspect result vs required ≥2 |
| 406 | Specificity ladder | FAIL | All 3 queries collapsed to the same spec folder (005-q4-quantization); vector channel down, FTS5 lexical-only fallback, CocoIndex daemon failed all 3 queries |
| 407 | Adversarial near-miss | FAIL | 0/3 queries executable; CocoIndex search returns msgspec.DecodeError on all calls, memory falls back to FTS5 lexical-only with evidence-gap warnings |
| 408 | Compound concept synthesis | FAIL   | 0/4 constituents in top-10, evidence gap Z=1.41   |
| 409 | LLM-made memory recall | FAIL | 2/10 in top-3, mean rank ~7.0, FTS5 lexical fallback active on all queries |
| 410 | Query latency and throughput | FAIL | p50 421 ms / p95 ~1045 ms / qps ~1.9, all searches fell back to FTS5 lexical-only |

## Per-scenario detail

### 402 — Synonymy across vocabularies

- Verdict: FAIL
- Key metric: top-5 Jaccard 25% (A), 0% (B), 0% (C), 0% (D); vector channel down, CocoIndex errors/timeouts
- Detail: Under lexical-only fallback, Memory pair A achieved only 25% overlap and pair B achieved 0%. CocoIndex queries for pairs C and D all failed with substrate errors or timeouts, returning zero hits. The open circuit breaker on the vector channel and degraded CocoIndex daemon make cross-vocabulary semantic retrieval impossible to validate.
- opencode exit code: 0

### 403 — Code-intent matching

- Verdict: SKIP
- Key metric: CocoIndex daemon over_cap, all 4 queries failed (timeouts/truncation errors), zero results returned
- Detail: All four mcp__cocoindex_code__search queries failed outright: the first batch returned "Expected object, got int", "Input data was truncated", and an MCP timeout; the second batch all timed out. memory_health confirms the CocoIndex daemon is degraded with logCapState over_cap, the embedding circuit breaker is open with 536 queued jobs, and the vector channel is unavailable. No rank-pair table could be produced because the search substrate produced zero results.
- opencode exit code: 0

### 404 — Disambiguation under context

- Verdict: PARTIAL
- Key metric: 2/3 CocoIndex variants disambiguated; variant 3 returned 0 file-system hits
- Detail: CocoIndex correctly steered variants 1 (Memory MCP, 3/3 A) and 2 (Git, 2/3 B) to intended senses. Variant 3 (file-system JSON) failed — top-3 were all memory-system files (generate-context.js workflows). Memory MCP was lexical-only (FTS5) and could not disambiguate any variant, returning memory-save docs for all queries. Substrate degradation (vector channel down, CocoIndex degraded) impacted retrieval quality.
- opencode exit code: 0

### 405 — Multi-aspect query synthesis

- Verdict: FAIL
- Key metric: vector channel down → FTS5 lexical-only fallback; CocoIndex top-5 missed Metal (M) entirely; only 1 multi-aspect result vs required ≥2
- Detail: Fired compound query across both channels. Memory search truncated to 1 result due to token budget; CocoIndex top-5 covered only A and E, with M absent entirely. The lexical-only fallback collapsed onto high-frequency embedding/provider terms and failed to balance the three distinct concepts — exactly the naive-ranker failure mode this scenario tests for.
- opencode exit code: 0

### 406 — Specificity ladder

- Verdict: FAIL
- Key metric: All 3 queries collapsed to the same spec folder (005-q4-quantization); vector channel down, FTS5 lexical-only fallback, CocoIndex daemon failed all 3 queries
- Detail: Three specificity-level queries all returned the same spec-folder metadata (description.json/spec.md) instead of level-appropriate docs or source code. The vector embedding channel is offline and CocoIndex is degraded, making semantic specificity discrimination impossible. This is a substrate failure, not a ranker algorithm failure.
- opencode exit code: 0

### 407 — Adversarial near-miss

- Verdict: FAIL
- Key metric: 0/3 queries executable; CocoIndex search returns msgspec.DecodeError on all calls, memory falls back to FTS5 lexical-only with evidence-gap warnings
- Detail: Scenario 407 requires semantic ranking to outrank lexical decoys, but the embedding provider is unhealthy (llama-cpp down, 288 pending embeddings), vector search is disabled, and CocoIndex IPC is truncated. All three adversarial queries failed at the search layer before ranking could even be evaluated. This is a genuine substrate degradation finding, not a content issue.
- opencode exit code: 0

### 408 — Compound concept synthesis

- Verdict: FAIL  
- Key metric: 0/4 constituents in top-10, evidence gap Z=1.41  
- Detail: Neither CocoIndex nor memory search surfaced any of the four expected implementation files. CocoIndex hit test scripts and review files; memory search flagged an evidence gap and returned an unrelated Q4 quantization plan. The substrate is degraded with an unhealthy llama-cpp embedding provider and 721 vector/FTS mismatched rows, preventing compound-concept synthesis retrieval.
- opencode exit code: 0

### 409 — LLM-made memory recall

- Verdict: FAIL
- Key metric: 2/10 in top-3, mean rank ~7.0, FTS5 lexical fallback active on all queries
- Detail: Queried 10 sampled LLM-stored memories with their own first trigger phrase; only 2 source memories (IDs 1431, 402) surfaced in top-3. The other 8 were displaced by unrelated lexical matches or absent entirely because the local embedding provider is unhealthy, forcing pure FTS5 fallback. This is a substrate degradation finding, not embedding drift.
- opencode exit code: 0

### 410 — Query latency and throughput

- Verdict: FAIL
- Key metric: p50 421 ms / p95 ~1045 ms / qps ~1.9, all searches fell back to FTS5 lexical-only
- Detail: 14 mixed queries were fired against a degraded substrate with an unhealthy embedding provider and 240 pending retries. Every search used FTS5 lexical fallback, producing median latencies more than double the 200 ms interactive target and throughput far below 5 qps; only the p99 catastrophic-outlier bound was met.
- opencode exit code: 0

## Bottom line
- PASS: 0/9
- PARTIAL: 1/9
- FAIL: 6/9
- SKIP: 1/9
- UNKNOWN: 0/9

Completed: 2026-05-14 09:29:17
