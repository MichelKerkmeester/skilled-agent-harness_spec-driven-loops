# Git Ignore Configuration
# Specifies intentionally untracked files that Git should ignore

.DS_Store

# Development
node_modules/

# Database files (contain personal memory/session data)
skill/system-spec-kit/database/*.sqlite
skill/system-spec-kit/database/*.sqlite-shm
skill/system-spec-kit/database/*.sqlite-wal

# Archive folder (root level only)
/z — Archive/