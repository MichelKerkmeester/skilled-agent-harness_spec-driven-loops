description = "Delegate debugging to a specialized sub-agent with full context handoff."
prompt = """---
description: Delegate debugging to a specialized sub-agent with full context handoff.
argument-hint: "[spec-folder-path]"
allowed-tools: Read, Write, Edit, Bash, Grep, Glob, Task
---

# SpecKit Debug (TOML Runtime Wrapper)

This TOML command is a runtime wrapper for the canonical markdown command.

## Source of Truth

- `.opencode/command/spec_kit/debug.md`

## Execution Rules

1. Read `.opencode/command/spec_kit/debug.md`.
2. Execute it exactly as written.
3. Use this request as the command input (`$ARGUMENTS`).
4. Return the canonical status/output format from the source command.

## Notes

- This wrapper intentionally avoids duplicating the full command body to prevent drift.
- Any future updates should be made in `.opencode/command/spec_kit/debug.md`.

User request: {{args}}"""
