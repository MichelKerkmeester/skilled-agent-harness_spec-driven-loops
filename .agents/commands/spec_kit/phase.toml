description = "Create and manage phase decomposition for complex spec folders"
prompt = """---
description: Create and manage phase decomposition for complex spec folders
argument-hint: "[feature-description] [--phases N] [--phase-names list] [--parent specs/NNN-name/] [:auto|:confirm]"
allowed-tools: Read, Write, Edit, Bash, Grep, Glob, memory_context, memory_search
---

# Spec Kit Phase (TOML Runtime Wrapper)

This TOML command is a runtime wrapper for the canonical markdown command.

## Source of Truth

- `.opencode/command/spec_kit/phase.md`

## Execution Rules

1. Read `.opencode/command/spec_kit/phase.md`.
2. Execute its phases, gates, setup flow, mode routing, and YAML workflow exactly as written.
3. Use this request as the command input (`$ARGUMENTS`).
4. Return the canonical status format from the source command.

## Notes

- This wrapper intentionally avoids duplicating the full command body to prevent drift.
- Any future updates should be made in `.opencode/command/spec_kit/phase.md`.

User request: {{args}}"""
