description = "Research workflow (9 steps) - technical investigation and documentation. Supports :auto and :confirm modes"
prompt = """---
description: Research workflow (9 steps) - technical investigation and documentation. Supports :auto and :confirm modes
argument-hint: "<research-topic> [:auto|:confirm] [--phase-folder=<path>]"
allowed-tools: Read, Write, Edit, Bash, Grep, Glob, Task, webfetch, memory_context, memory_search
---

# SpecKit Research (TOML Runtime Wrapper)

This TOML command is a runtime wrapper for the canonical markdown command.

## Source of Truth

- `.opencode/command/spec_kit/research.md`

## Execution Rules

1. Read `.opencode/command/spec_kit/research.md`.
2. Execute it exactly as written.
3. Use this request as the command input (`$ARGUMENTS`).
4. Return the canonical status/output format from the source command.

## Notes

- This wrapper intentionally avoids duplicating the full command body to prevent drift.
- Any future updates should be made in `.opencode/command/spec_kit/research.md`.

User request: {{args}}"""
