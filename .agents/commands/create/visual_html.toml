description = "Create or verify styled, self-contained visual HTML artifacts from topics, docs, diffs, and recaps using sk-doc-visual - supports :auto and :confirm modes"
prompt = """---
description: Create or verify styled, self-contained visual HTML artifacts from topics, docs, diffs, and recaps using sk-doc-visual - supports :auto and :confirm modes
argument-hint: "<target-or-source> [--mode <auto|create|analyze|verify|custom>] [--artifact <auto|spec|plan|tasks|checklist|implementation-summary|research|decision-record|readme|install-guide>] [--source-file <path>] [--spec-folder <path>] [--traceability] [--include-doc-impact] [--include-doc-health] [--type <architecture|flowchart|sequence|data-flow|er|state|mindmap|table|timeline|dashboard>] [--style <terminal|editorial|blueprint|neon|paper|hand-drawn|ide|data-dense|gradient>] [:auto|:confirm]"
allowed-tools: Read, Write, Edit, Bash, Glob, Grep
---

# Create Visual HTML (TOML Runtime Wrapper)

This TOML command is a runtime wrapper for the canonical markdown command.

## Source of Truth

- `.opencode/command/create/visual_html.md`

## Execution Rules

1. Read `.opencode/command/create/visual_html.md`.
2. Execute its phases, gates, setup flow, mode routing, and YAML workflow exactly as written.
3. Use this request as the command input (`$ARGUMENTS`).
4. Return the canonical status format from the source command.

## Notes

- This wrapper intentionally avoids duplicating the full command body to prevent drift.
- Any future updates should be made in `.opencode/command/create/visual_html.md`.

User request: {{args}}"""
