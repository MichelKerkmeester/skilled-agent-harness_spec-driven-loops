description = "Create or improve AI prompts using proven frameworks, DEPTH thinking, and CLEAR scoring via sk-prompt-improver - supports :auto and :confirm modes"
prompt = """---
description: Create or improve AI prompts using proven frameworks, DEPTH thinking, and CLEAR scoring via sk-prompt-improver - supports :auto and :confirm modes
argument-hint: "<prompt_or_topic> [$mode] [:auto|:confirm]"
allowed-tools: Read, Write, Edit, Bash, Glob, Grep
---

# Create Prompt (TOML Runtime Wrapper)

This TOML command is a runtime wrapper for the canonical markdown command.

## Source of Truth

- `.opencode/command/create/prompt.md`

## Execution Rules

1. Read `.opencode/command/create/prompt.md`.
2. Execute it exactly as written.
3. Use this request as the command input (`$ARGUMENTS`).
4. Return the canonical status/output format from the source command.

## Notes

- This wrapper intentionally avoids duplicating the full command body to prevent drift.
- Any future updates should be made in `.opencode/command/create/prompt.md`.

User request: {{args}}"""
