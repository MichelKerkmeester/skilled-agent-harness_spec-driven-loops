description = "Unified documentation command for folder README and install guide creation"
prompt = """---
description: Unified documentation command for folder README and install guide creation
argument-hint: "[readme|install] <target> [--type <project|component|feature|skill>] [--platforms <list>] [--output <path>] [:auto|:confirm]"
allowed-tools: Read, Write, Edit, Bash, Glob, Grep, TodoWrite
---

# Create Folder README (TOML Runtime Wrapper)

This TOML command wraps the unified documentation workflow.

## Source of Truth

- `.opencode/command/create/folder_readme.md`

## Execution Rules

1. Read `.opencode/command/create/folder_readme.md`.
2. Execute its operation detection, Phase 0, Setup Phase, YAML routing, and validation exactly as written.
3. Use this request as command input (`$ARGUMENTS`).
4. Return canonical status format (`STATUS=OK|FAIL|CANCELLED`).

## Operations

- `readme` (default): Create/update folder README documentation
- `install`: Create/update installation guide

User request: {{args}}"""
