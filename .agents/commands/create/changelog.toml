description = "Create a changelog entry by dynamically detecting recent work, resolving the target component folder, and generating a properly formatted changelog file - supports :auto and :confirm modes"
prompt = """---
description: Create a changelog entry by dynamically detecting recent work, resolving the target component folder, and generating a properly formatted changelog file - supports :auto and :confirm modes
argument-hint: "<spec-folder-or-component> [--bump <major|minor|patch|build>] [:auto|:confirm]"
allowed-tools: Read, Write, Edit, Bash, Glob, Grep
---

# Create Changelog (TOML Runtime Wrapper)

This TOML command is a runtime wrapper for the canonical markdown command.

## Source of Truth

- `.opencode/command/create/changelog.md`

## Execution Rules

1. Read `.opencode/command/create/changelog.md`.
2. Execute it exactly as written.
3. Use this request as the command input (`$ARGUMENTS`).
4. Return the canonical status/output format from the source command.

## Notes

- This wrapper intentionally avoids duplicating the full command body to prevent drift.
- Any future updates should be made in `.opencode/command/create/changelog.md`.

User request: {{args}}"""
