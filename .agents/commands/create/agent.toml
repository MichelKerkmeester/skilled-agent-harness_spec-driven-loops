description = "Create a new OpenCode agent with proper frontmatter, tool permissions, and behavioral rules - supports :auto and :confirm modes"
prompt = """---
description: Create a new OpenCode agent with proper frontmatter, tool permissions, and behavioral rules - supports :auto and :confirm modes
argument-hint: "<agent_name> [agent_description] [:auto|:confirm]"
allowed-tools: Read, Write, Edit, Bash, Glob, Grep, TodoWrite
---

# Create Agent (TOML Runtime Wrapper)

This TOML command is a runtime wrapper for the canonical markdown command.

## Source of Truth

- `.opencode/command/create/agent.md`

## Execution Rules

1. Read `.opencode/command/create/agent.md`.
2. Execute it exactly as written.
3. Use this request as the command input (`$ARGUMENTS`).
4. Return the canonical status/output format from the source command.

## Notes

- This wrapper intentionally avoids duplicating the full command body to prevent drift.
- Any future updates should be made in `.opencode/command/create/agent.md`.

User request: {{args}}"""
