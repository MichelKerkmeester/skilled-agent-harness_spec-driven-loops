description = "Recover session from crash, compaction, or timeout - resume interrupted work"
prompt = """---
description: Recover session from crash, compaction, or timeout - resume interrupted work
argument-hint: "[recovery-mode:auto|manual]"
allowed-tools: Read, Bash, spec_kit_memory_memory_context, spec_kit_memory_memory_search, spec_kit_memory_memory_list, spec_kit_memory_memory_stats
---

# Memory Continue (TOML Runtime Wrapper)

This TOML command is a runtime wrapper for the canonical markdown command.

## Source of Truth

- `.opencode/command/memory/continue.md`

## Execution Rules

1. Read `.opencode/command/memory/continue.md`.
2. Execute it exactly as written.
3. Use this request as the command input (`$ARGUMENTS`).
4. Return the canonical status/output format from the source command.

## Notes

- This wrapper intentionally avoids duplicating the full command body to prevent drift.
- Any future updates should be made in `.opencode/command/memory/continue.md`.

User request: {{args}}"""
