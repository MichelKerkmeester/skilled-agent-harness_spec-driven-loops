description = "Create and manage constitutional memories - always-surface rules that appear at the top of every search result."
prompt = """---
description: Create and manage constitutional memories - always-surface rules that appear at the top of every search result.
argument-hint: "[rule-description] | list | edit <filename> | remove <filename> | budget"
allowed-tools: Read, Write, Edit, Glob, Bash, spec_kit_memory_memory_save, spec_kit_memory_memory_search, spec_kit_memory_memory_stats, spec_kit_memory_memory_list, spec_kit_memory_memory_delete, spec_kit_memory_memory_index_scan
---

# Memory Learn (TOML Runtime Wrapper)

This TOML command is a runtime wrapper for the canonical markdown command.

## Source of Truth

- `.opencode/command/memory/learn.md`

## Execution Rules

1. Read `.opencode/command/memory/learn.md`.
2. Execute it exactly as written.
3. Use this request as the command input (`$ARGUMENTS`).
4. Return the canonical status/output format from the source command.

## Notes

- This wrapper intentionally avoids duplicating the full command body to prevent drift.
- Any future updates should be made in `.opencode/command/memory/learn.md`.

User request: {{args}}"""
