description = "Retrieve context with intent awareness - combines search + load with task-specific weights"
prompt = """---
description: Retrieve context with intent awareness - combines search + load with task-specific weights
argument-hint: "<query> [--intent:<type>]"
allowed-tools: Read, spec_kit_memory_memory_context, spec_kit_memory_memory_search, spec_kit_memory_memory_match_triggers
---

# Memory Context (TOML Runtime Wrapper)

This TOML command is a runtime wrapper for the canonical markdown command.

## Source of Truth

- `.opencode/command/memory/context.md`

## Execution Rules

1. Read `.opencode/command/memory/context.md`.
2. Execute it exactly as written.
3. Use this request as the command input (`$ARGUMENTS`).
4. Return the canonical status/output format from the source command.

## Notes

- This wrapper intentionally avoids duplicating the full command body to prevent drift.
- Any future updates should be made in `.opencode/command/memory/context.md`.

User request: {{args}}"""
