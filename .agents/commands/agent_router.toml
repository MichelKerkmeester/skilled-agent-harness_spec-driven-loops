description = "Route requests to AI Systems with full System Prompt identity adoption"
prompt = """---
description: Route requests to AI Systems with full System Prompt identity adoption
argument-hint: "[system|path:<path>] <request>"
allowed-tools: Read, Write, Edit, Glob, Grep, Bash, WebFetch, WebSearch, AskUserQuestion
---

# Agent Router (TOML Runtime Wrapper)

This TOML command is a runtime wrapper for the canonical markdown command.

## Source of Truth

- `.opencode/command/agent_router.md`

## Execution Rules

1. Read `.opencode/command/agent_router.md`.
2. Execute its routing logic, system prompt loading, and identity adoption exactly as written.
3. Use this request as the command input (`$ARGUMENTS`).
4. Return the output from the routed AI system.

## Notes

- This wrapper intentionally avoids duplicating the full command body to prevent drift.
- Any future updates should be made in `.opencode/command/agent_router.md`.

User request: {{args}}"""
