DEEP-REVIEW
Resolved route: mode=review; target_agent=@deep-review; execution=single_review_iteration; state_source=externalized_files; do_not_switch_mode=true

# Iteration 1 Prompt

STATE SUMMARY: Iteration 1 of 4; dimension=correctness; prior findings P0=0 P1=0 P2=0; stopPolicy=max-iterations.

Review the 163 declared target Markdown paths against the canonical create-skill reference/asset structure. Replay the four xHigh-remediated structural sites, run the generic validator and an independent semantic matrix, and treat variant When-to-Use headings semantically. Target files are read-only. Write only inside the bound `confirm-b` lineage packet.
