# Iteration 6 prompt

Audit authored path references in non-excluded child packet documents. Resolve every current-looking path against the tracked repository, distinguish canonical source trees from untracked generated/build directories, and exclude frozen historical verification artifacts. Focus on the known `smart_routing.md` to `smart-routing.md` rename and the `mcp_server` versus `mcp-server` split. Report exact line evidence and NEW/PRE-EXISTING classification.
