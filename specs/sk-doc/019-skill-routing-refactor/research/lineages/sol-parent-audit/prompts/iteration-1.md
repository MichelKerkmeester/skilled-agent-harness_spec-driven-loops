DEEP-RESEARCH
Resolved route: mode=research; target_agent=@deep-research; execution=single_iteration; state_source=externalized_files; do_not_switch_mode=true

# Iteration 1

State source: the packet-local config, state log, strategy, and registry.

Focus: Build an authoritative inventory of the live direct-child tree and nested phase-parent structure, then compare names, numbering, duplicate-number collisions, and status/structure claims against the parent `spec.md` phase map.

Constraints:
- Leaf-only; no sub-dispatch.
- Audited documents are read-only.
- Exclude frozen historical artifacts.
- Write only `iterations/iteration-001.md`, the single state-log append, and `deltas/iter-001.jsonl`.
- Cite every finding with current file:line evidence.
