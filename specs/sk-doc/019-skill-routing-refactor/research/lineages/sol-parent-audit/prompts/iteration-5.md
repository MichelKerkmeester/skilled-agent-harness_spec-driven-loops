DEEP-RESEARCH
Resolved route: mode=research; target_agent=@deep-research; execution=single_iteration; state_source=externalized_files; do_not_switch_mode=true

# Iteration 5

Focus: Reconcile parent `graph-metadata.json` and `description.json` with the live direct-child tree and current parent content. Check `children_ids`, `derived`, description scope, active-child routing, and fingerprints.

Direct-mode recovery: the current detached executor performs this evidence pass because same-kind nested Codex dispatch is prohibited. Writes remain packet-local.
