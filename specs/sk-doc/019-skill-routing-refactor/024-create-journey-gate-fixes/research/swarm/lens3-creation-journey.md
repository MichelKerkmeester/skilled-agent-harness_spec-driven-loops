Act as an end-to-end workflow reviewer. READ-ONLY — modify nothing (a temp scaffold under /tmp is allowed for testing; delete it after). Scope: does following create-skill's creation guidance actually produce a skill that system-skill-advisor can discover and route to?

Walk both journeys as written:
1. STANDALONE: .opencode/skills/sk-doc/create-skill/SKILL.md "Standalone Skill Creation Workflow" steps + scripts/init_skill.py (run it into /tmp to inspect output) + the class gate step. Then check: is the scaffolded graph-metadata.json advisor-ingestable per mcp-server/lib/skill-graph/skill-graph-db.ts (family, schema_version, required derived fields)? Do the SKILL.md steps ever tell the author to make the skill DISCOVERABLE — keywords/intent_signals quality, trigger phrases, how the advisor scores them? Is there guidance on WHEN the advisor daemon/graph re-ingests (index refresh) after a new skill lands, in either create-skill or system-skill-advisor docs?
2. PARENT HUB: SKILL.md "Parent Hub Creation Workflow" steps 1-30 + init_skill.py --kind parent. Same questions, plus: do the steps cover command-metadata.json authoring (template reference?), advisorRouting fields in the registry, and the description.json keyword guidance the advisor's doctor rules expect?
3. The seam itself: when a NEW skill is created, what tells the skill-advisor system about it? Trace whether anything in either skill documents the ingestion/refresh path (skill-graph scan, daemon restart, doctor). If the journey ends with "gate passes" but the advisor never learns the skill exists without an undocumented step, that is the P0 finding.
OUTPUT: journey-step table (step -> gap/ok), then findings-first `P0|P1|P2 | doc/script | gap | evidence`, REFUTED list, `VERDICT: CLEAN` or `VERDICT: FINDINGS n`. Clean up any /tmp scaffold you created.
