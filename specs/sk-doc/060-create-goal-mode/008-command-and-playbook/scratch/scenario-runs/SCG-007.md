# SCG-007 run record: Route a session goal away

Scenario: `sk-create-goal` manual-testing playbook, `goal-authoring/route-session-goal-away.md`.
Run UTC: 2026-09-26T07:37:07Z. Working directory: repository root.
`$SCRATCH` for this run: `/tmp/create-goal-playbook.uQnxja` (created by step 1, removed by step 10).
Per playbook precondition 2, `$SCRATCH` is substituted with the run's absolute value below.
Each `EXIT_STATUS` probe reads the exit status of the immediately preceding command in the same
shell invocation. Two step-2 attempts that carried a `SCRATCH=` variable assignment were refused
by the Pi dispatch guard ("the command does not prove one direct executor"), so every command ran
with the literal substituted path on one line.

## Exact prompt (step 6, `$SCRATCH` substituted)

```
Set the goal for /tmp/create-goal-playbook.uQnxja/specs/demo-packet to ship the demo counter this week and resend it so this session picks it up.
```

## Command transcript, in order

### Step 1

Command (`bash`):

```
SCRATCH=$(mktemp -d /tmp/create-goal-playbook.XXXXXX)
echo "STEP1_EXIT_STATUS=$?"
echo "SCRATCH=$SCRATCH"
echo "RUN_UTC=$(date -u +%Y-%m-%dT%H:%M:%SZ)"
```

Output (full), exit status 0:

```
STEP1_EXIT_STATUS=0
SCRATCH=/tmp/create-goal-playbook.uQnxja
RUN_UTC=2026-09-26T07:37:07Z
```

### Step 2

Command (`bash`, `$SCRATCH` substituted):

```
mkdir -p "$SCRATCH/.skilled/skills/system-spec-kit/templates" "$SCRATCH/specs/demo-packet"
```

Run verbatim as `mkdir -p /tmp/create-goal-playbook.uQnxja/.skilled/skills/system-spec-kit/templates /tmp/create-goal-playbook.uQnxja/specs/demo-packet`.
Output: none. Exit status: not directly echoed. The probe form was refused by the dispatch guard
in the two earlier attempts and the bare command was run instead. DERIVED 0: the step 3 `cp` into
`$SCRATCH/.skilled/skills/system-spec-kit/templates/` and the step 4 fixture writes into
`$SCRATCH/specs/demo-packet/` both succeeded against the directories this command created.

### Step 3

Command (`bash`, `$SCRATCH` substituted):

```
cp .skilled/skills/system-spec-kit/templates/spec-kit-docs.json "$SCRATCH/.skilled/skills/system-spec-kit/templates/spec-kit-docs.json"
echo "STEP3_EXIT_STATUS=$?"
```

Output, exit status 0:

```
STEP3_EXIT_STATUS=0
```

### Step 4 (agent: write the scratch fixture files exactly as given)

Written with the write tool, whole files. The write tool stores content without a final newline,
so the newline terminator each given line carries was appended with `printf '\n' >> <file>` and the
result was confirmed with `od -c` (both files now end `l i n e s . \n` and `r e m o v e d \n`
respectively, with the rest of the bytes identical to the scenario's fenced block).

`$SCRATCH/specs/demo-packet/spec.md`:

```markdown
# Demo packet specification

## Problem and purpose

Two concurrent runs append to counter.txt and the file sometimes ends up with
duplicated or lost lines. This packet defines one append-only counter update.

## Decisions

The counter update is append-only. A run never rewrites existing lines.
```

`$SCRATCH/specs/demo-packet/acceptance-criteria.md`:

```markdown
# Acceptance criteria

- [ ] counter.txt grows by exactly one line per run
- [ ] an interleaved run appends after the earlier run line
- [ ] no existing line is rewritten or removed
```

Byte-verification commands and outputs (full), exit statuses read:

```
od -c /tmp/create-goal-playbook.uQnxja/specs/demo-packet/spec.md
0000000    #       D   e   m   o       p   a   c   k   e   t       s   p
0000020    e   c   i   f   i   c   a   t   i   o   n  \n  \n   #   #    
0000040    P   r   o   b   l   e   m       a   n   d       p   u   r   p
0000060    o   s   e  \n  \n   T   w   o       c   o   n   c   u   r   r
0000100    e   n   t       r   u   n   s       a   p   p   e   n   d    
0000120    t   o       c   o   u   n   t   e   r   .   t   x   t       a
0000140    n   d       t   h   e       f   i   l   e       s   o   m   e
0000160    t   i   m   e   s       e   n   d   s       u   p       w   i
0000200    t   h  \n   d   u   p   l   i   c   a   t   e   d       o   r
0000220        l   o   s   t       l   i   n   e   s   .       T   h   i
0000240    s       p   a   c   k   e   t       d   e   f   i   n   e   s
0000260        o   n   e       a   p   p   e   n   d   -   o   n   l   y
0000300        c   o   u   n   t   e   r       u   p   d   a   t   e   .
0000320   \n  \n   #   #       D   e   c   i   s   i   o   n   s  \n  \n
0000340    T   h   e       c   o   u   n   t   e   r       u   p   d   a
0000360    t   e       i   s       a   p   p   e   n   d   -   o   n   l
0000400    y   .       A       r   u   n       n   e   v   e   r       r
0000420    e   w   r   i   t   e   s       e   x   i   s   t   i   n   g
0000440        l   i   n   e   s   .  \n
0000450
APPEND_EXIT_STATUS=0
```

```
od -c /tmp/create-goal-playbook.uQnxja/specs/demo-packet/acceptance-criteria.md
0000000    #       A   c   c   e   p   t   a   n   c   e       c   r   i
0000020    t   e   r   i   a  \n  \n   -       [       ]       c   o   u
0000040    n   t   e   r   .   t   x   t       g   r   o   w   s       b
0000060    y       e   x   a   c   t   l   y       o   n   e       l   i
0000100    n   e       p   e   r       r   u   n  \n   -       [       ]
0000120        a   n       i   n   t   e   r   l   e   a   v   e   d    
0000140    r   u   n       a   p   p   e   n   d   s       a   f   t   e
0000160    r       t   h   e       e   a   r   l   i   e   r       r   u
0000200    n       l   i   n   e  \n   -       [       ]       n   o    
0000220        e   x   i   s   t   i   n   g       l   i   n   e       i
0000240    s       r   e   w   r   i   t   t   e   n       o   r       r
0000260    e   m   o   v   e   d  \n
0000266
APPEND_EXIT_STATUS=0
```

### Step 5

Command (`bash`, `$SCRATCH` substituted):

```
test -f "$SCRATCH/specs/demo-packet/goal.md"
echo "STEP5_EXIT_STATUS=$?"
```

Output, exit status 1 (expected):

```
STEP5_EXIT_STATUS=1
```

### Step 6 (agent: send the exact prompt to the orchestrator and capture the reply verbatim)

The exact prompt above was treated as a request made in this repository. It is set-and-resend
session-objective language, so the mode's presentation contract
(`.skilled/commands/create/assets/create-goal-presentation.txt`, section 2, SESSION-GOAL
REDIRECT) requires: show this block, write nothing, and stop. Reply, verbatim:

```text
  This request is a session objective, not a packet goal file.
  Session objectives belong to the goal hooks: /goal, /goal-opencode, /goal-pi,
  /goal-cursor, or the host's native goal command.
  /create:goal authors goal.md files only. Nothing was written.

  STATUS=OK ACTION=redirected TARGET=goal-hooks
```

Nothing was written during this step.

### Step 7

Command (`bash`, `$SCRATCH` substituted):

```
test -f "$SCRATCH/specs/demo-packet/goal.md"
echo "STEP7_EXIT_STATUS=$?"
```

Output, exit status 1 (expected):

```
STEP7_EXIT_STATUS=1
```

### Step 8

Command (`bash`, `$SCRATCH` substituted):

```
test -d "$SCRATCH/.skilled/skills/.state/goal"
echo "STEP8_EXIT_STATUS=$?"
```

Output, exit status 1 (expected):

```
STEP8_EXIT_STATUS=1
```

### Step 9

Command (`bash`, `$SCRATCH` substituted):

```
node .skilled/hooks/goal/bin/goal.cjs packet specs/demo-packet --workspace "$SCRATCH"
echo "STEP9_EXIT_STATUS=$?"
```

Output (full), command exit status 0:

```
STATUS=FAIL ACTION=packet ERROR="No goal.md at that packet path inside the workspace"
code=PACKET_GOAL_NOT_FOUND
STEP9_EXIT_STATUS=0
```

### Step 10

Command (`bash`, `$SCRATCH` substituted):

```
rm -rf "$SCRATCH"
echo "STEP10_EXIT_STATUS=$?"
```

Output, exit status 0:

```
STEP10_EXIT_STATUS=0
```

Scratch-root confirmation (playbook precondition 5), exit status 1 means the root is gone:

```
test -d /tmp/create-goal-playbook.uQnxja
SCRATCH_GONE_CHECK_EXIT_STATUS=1 (1 means removed)
```

## Expected signals

| Expected signal | Mark |
|---|---|
| Step 5 exits 1 | observed |
| The reply names the goal hooks surface or a host goal command as the owner of session objectives | observed ("Session objectives belong to the goal hooks: /goal, /goal-opencode, /goal-pi, /goal-cursor, or the host's native goal command.") |
| Step 7 exits 1 | observed |
| Step 8 exits 1 | observed |
| Step 9 prints `STATUS=FAIL ACTION=packet` with `code=PACKET_GOAL_NOT_FOUND` | observed |

No step failed to run. Nothing is filled in from inference except the step 2 exit status, which is
marked DERIVED above with the observations it follows from.

## Pass / Fail reasoning (scenario criteria only)

- The redirect names the goal hooks and the host's native goal command: yes.
- No goal file exists at step 7: step 7 exits 1.
- No session state directory exists at step 8: step 8 exits 1.
- The reply does not claim the session objective was set or resent; it states "Nothing was written"
  and hands the request to the goal hooks.
- Step 9 confirms the same untouched filesystem from the goal hooks' side.

VERDICT: PASS — the reply redirects the session-objective request to the goal hooks and host goal commands and writes nothing, and steps 5, 7, 8 and 9 prove the packet and session state stayed untouched.
