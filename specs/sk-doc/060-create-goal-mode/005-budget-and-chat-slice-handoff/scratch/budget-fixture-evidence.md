# Budget Fixture Evidence

The level-2 goal was rendered from the shared goal template with the inline renderer. The single-folder fixture has no child-goal detail to remove. Its frontmatter and log are outside the measured slice. The cut shortened decision prose, then shortened all five criteria and their matching objective copy.

Render command:

```bash
bash .skilled/skills/system-spec-kit/runtime/cli/templates/inline-gate-renderer.sh --level 2 --out-dir specs/sk-doc/060-create-goal-mode/scratch/tmp-over-budget-fixture .skilled/skills/system-spec-kit/templates/addons/goal.md.tmpl
```

Measure command:

```bash
node .skilled/hooks/goal/bin/goal.cjs packet specs/sk-doc/060-create-goal-mode/scratch/tmp-over-budget-fixture --workspace "$PWD"
```

Criterion count before the cut: 5
Criterion count after the cut: 5

Each command exited 0.

## Before the cut

packet_durable_chars=5897
packet_budget=over

Full `goal.cjs packet` output:

```text
STATUS=OK ACTION=packet
packet_path="specs/sk-doc/060-create-goal-mode/scratch/tmp-over-budget-fixture"
packet_nested=false
packet_durable_chars=5897
packet_budget=over
packet_slice_hash="sha256:60d47d527ab2c713d2761b08d0f35a96072a49360ddd25746f92780b8a17e4ad"
objective_slice="Execute specs/sk-doc/060-create-goal-mode/scratch/tmp-over-budget-fixture/goal.md.\nDONE WHEN:\n- The intake report records the original request text and the outcome the requester asked for, so a reviewer can compare those two fields without opening another file or interpreting a shortened paraphrase.\n- Each accepted request has one route name, the route's required input and the named output that the next author must produce, with no accepted request left without a route or assigned to two different routes.\n- Each route record includes one completion check that names an observable result and can be evaluated from the generated intake report without opening the route definition or another document.\n- Every unresolved request names the specific information still missing and remains marked unresolved until the requester supplies that information, so a reviewer can distinguish an unanswered question from an accepted route.\n- The final intake report contains the route result, original request, requested outcome, missing information when present and completion check, with five distinct fields available for direct review by a person using the report."
chat_slice="# Goal: Technical Intake Router\n\n> Everything between the frontmatter and the log is the DURABLE SLICE: it is\n> what an operator sets as the session objective, and it must stay true for the\n> life of the packet. The frontmatter above it is bookkeeping and never leaves\n> this file: it is not sent in chat, not injected, not stored in an objective.\n> Keep the slice short. A phase parent or top-level packet has one limit, 4000\n> characters, measured from the frontmatter's closing fence to the log anchor.\n> Up to 4000 passes and past it fails; the runtime goal surfaces cap what they\n> hold, and a truncated objective loses its tail, which is where the criteria\n> live.\n\n## DURABLE DIRECTIVE\n\n**Objective:** Build an intake router that receives technical requests, classifies each request by its requested outcome, assigns every accepted request to one documented route and gives each route enough detail to produce a reviewable result without forcing the next author to reconstruct the requester's purpose from scattered notes or infer the intended output from unrelated examples.\n\nThe router is complete when the following results hold:\n\n- The intake report records the original request text and the outcome the requester asked for, so a reviewer can compare those two fields without opening another file or interpreting a shortened paraphrase.\n- Each accepted request has one route name, the route's required input and the named output that the next author must produce, with no accepted request left without a route or assigned to two different routes.\n- Each route record includes one completion check that names an observable result and can be evaluated from the generated intake report without opening the route definition or another document.\n- Every unresolved request names the specific information still missing and remains marked unresolved until the requester supplies that information, so a reviewer can distinguish an unanswered question from an accepted route.\n- The final intake report contains the route result, original request, requested outcome, missing information when present and completion check, with five distinct fields available for direct review by a person using the report.\n\n### Decisions\n\nFrozen choices. Changing one is an amendment.\n\n| ID | Decision |\n|----|----------|\n| D1 | The intake router records the requested outcome before it selects a route, because routing from a keyword alone can send the same sentence to different destinations when the request changes meaning in context. The selected route must therefore be supported by the full request and must preserve the goal the requester stated. |\n| D2 | Each route has a written input, output and completion check, because a route that names only a destination leaves the next author to guess what should happen there. The router records those three details alongside its route decision so another author can check the decision without searching unrelated files. |\n| D3 | A request with an unclear outcome remains unresolved until its owner supplies enough detail, because an invented default can look like a valid decision after the original request has been separated from its context. The router records the missing detail and keeps the request available for correction. |\n| D4 | The router keeps the original request text with the route result, because a summary can remove a constraint that matters later and a reader needs the original wording to confirm the chosen route. The record also identifies the route rule that accepted the request. |\n\n### Operator copy\n\nThe operator holds this directive as the session objective, and that copy is\nwhat judges completion, not this file. Whenever anything above the log changes\n(objective, a decision, the binding table, a criterion), resend this file's\nchat slice so the operator can update their copy. The chat slice is the\ndurable slice without its frontmatter, HTML comments, anchor markers, `---`\ndividers or heading section numbers, and `goal.cjs packet` prints it as\n`chat_slice`. Never send more than 4000 characters: cut this file first. Keep\nreminding while the copy stays unset, and never stop work for it. A child goal\nchange that alters a parent decision or criterion is an amendment to the\nparent: apply it there first, then resend the parent.\n\n## COMPLETION CRITERIA\n\nThree to seven bullets, each checkable without opening another file. Copy them\nverbatim into the objective: nothing dereferences a path, so criteria left only\nhere are invisible to whatever judges completion.\n\n- [ ] The intake report records the original request text and the outcome the requester asked for, so a reviewer can compare those two fields without opening another file or interpreting a shortened paraphrase.\n- [ ] Each accepted request has one route name, the route's required input and the named output that the next author must produce, with no accepted request left without a route or assigned to two different routes.\n- [ ] Each route record includes one completion check that names an observable result and can be evaluated from the generated intake report without opening the route definition or another document.\n- [ ] Every unresolved request names the specific information still missing and remains marked unresolved until the requester supplies that information, so a reviewer can distinguish an unanswered question from an accepted route.\n- [ ] The final intake report contains the route result, original request, requested outcome, missing information when present and completion check, with five distinct fields available for direct review by a person using the report."
```

## After the cut

packet_durable_chars=3256
packet_budget=ok

The final output contains both `chat_slice` and `objective_slice`.

Full `goal.cjs packet` output:

```text
STATUS=OK ACTION=packet
packet_path="specs/sk-doc/060-create-goal-mode/scratch/tmp-over-budget-fixture"
packet_nested=false
packet_durable_chars=3256
packet_budget=ok
packet_slice_hash="sha256:860e9920889e8740ef974f3fdfef8e1c7fa6d9219b0621cc4af74837d2b59ffa"
objective_slice="Execute specs/sk-doc/060-create-goal-mode/scratch/tmp-over-budget-fixture/goal.md.\nDONE WHEN:\n- The report preserves the request text and requested outcome.\n- Each accepted request has one route, its required input and named output.\n- Each route has one completion check in the intake report.\n- Each unresolved request names missing information and stays unresolved until supplied.\n- The final report records request, outcome, route, missing detail and completion check."
chat_slice="# Goal: Technical Intake Router\n\n> Everything between the frontmatter and the log is the DURABLE SLICE: it is\n> what an operator sets as the session objective, and it must stay true for the\n> life of the packet. The frontmatter above it is bookkeeping and never leaves\n> this file: it is not sent in chat, not injected, not stored in an objective.\n> Keep the slice short. A phase parent or top-level packet has one limit, 4000\n> characters, measured from the frontmatter's closing fence to the log anchor.\n> Up to 4000 passes and past it fails; the runtime goal surfaces cap what they\n> hold, and a truncated objective loses its tail, which is where the criteria\n> live.\n\n## DURABLE DIRECTIVE\n\n**Objective:** Build an intake router that records each technical request, assigns it by requested outcome and produces a reviewable report.\n\nDone when:\n\n- The report preserves the request text and requested outcome.\n- Each accepted request has one route, its required input and named output.\n- Each route has one completion check in the intake report.\n- Each unresolved request names missing information and stays unresolved until supplied.\n- The final report records request, outcome, route, missing detail and completion check.\n\n### Decisions\n\nFrozen choices. Changing one is an amendment.\n\n| ID | Decision |\n|----|----------|\n| D1 | Requested outcome decides the route. |\n| D2 | Every route names its required input, output and completion check. |\n| D3 | A request with missing detail stays unresolved until its owner clarifies it. |\n| D4 | Keep the original request beside the route result. |\n\n### Operator copy\n\nThe operator holds this directive as the session objective, and that copy is\nwhat judges completion, not this file. Whenever anything above the log changes\n(objective, a decision, the binding table, a criterion), resend this file's\nchat slice so the operator can update their copy. The chat slice is the\ndurable slice without its frontmatter, HTML comments, anchor markers, `---`\ndividers or heading section numbers, and `goal.cjs packet` prints it as\n`chat_slice`. Never send more than 4000 characters: cut this file first. Keep\nreminding while the copy stays unset, and never stop work for it. A child goal\nchange that alters a parent decision or criterion is an amendment to the\nparent: apply it there first, then resend the parent.\n\n## COMPLETION CRITERIA\n\nThree to seven bullets, each checkable without opening another file. Copy them\nverbatim into the objective: nothing dereferences a path, so criteria left only\nhere are invisible to whatever judges completion.\n\n- [ ] The report preserves the request text and requested outcome.\n- [ ] Each accepted request has one route, its required input and named output.\n- [ ] Each route has one completion check in the intake report.\n- [ ] Each unresolved request names missing information and stays unresolved until supplied.\n- [ ] The final report records request, outcome, route, missing detail and completion check."
```
