# SCG-006 run record: Detect an unbound phase

Scenario: `sk-create-goal` manual-testing playbook, `goal-authoring/detect-unbound-phase.md`.
Run UTC: 2026-09-26T08:46:04Z. Working directory: repository root.
`$SCRATCH` for this run: `/tmp/create-goal-playbook.QySkqS` (created by step 1, removed by step 7).
Per playbook precondition 2, `$SCRATCH` is substituted with the run's absolute value below, because
each `bash` invocation is its own shell. Each `*_EXIT_STATUS` probe reads the exit status of the
immediately preceding command in the same shell invocation.

## Exact prompt (step 5's request, `$SCRATCH` substituted)

```
Before I sign off the phase parent at /tmp/create-goal-playbook.QySkqS/specs/demo-phase, run the goal check and tell me whether the binding table covers every phase folder.
```

## Command transcript, in order

### Step 1

Command (`bash`):

```
SCRATCH=$(mktemp -d /tmp/create-goal-playbook.XXXXXX)
echo "STEP1_EXIT_STATUS=$?"
echo "SCRATCH=$SCRATCH"
echo "RUN_UTC=$(date -u +%Y-%m-%dT%H:%M:%SZ)"
```

Output (full), exit status 0:

```
STEP1_EXIT_STATUS=0
SCRATCH=/tmp/create-goal-playbook.QySkqS
RUN_UTC=2026-09-26T08:46:04Z
```

### Step 2

Command (`bash`, `$SCRATCH` substituted):

```
mkdir -p /tmp/create-goal-playbook.QySkqS/.skilled/skills/system-spec-kit/templates /tmp/create-goal-playbook.QySkqS/specs/demo-phase/001-alpha /tmp/create-goal-playbook.QySkqS/specs/demo-phase/002-beta
echo "STEP2_EXIT_STATUS=$?"
```

Output, exit status 0:

```
STEP2_EXIT_STATUS=0
```

### Step 3

Command (`bash`, `$SCRATCH` substituted):

```
cp .skilled/skills/system-spec-kit/templates/spec-kit-docs.json /tmp/create-goal-playbook.QySkqS/.skilled/skills/system-spec-kit/templates/spec-kit-docs.json
echo "STEP3_EXIT_STATUS=$?"
```

Output, exit status 0:

```
STEP3_EXIT_STATUS=0
```

### Step 4 (agent: write the scratch fixture files exactly as given)

Written whole with the write tool, five files, each preserving its final newline. Byte sizes and
final bytes verified (`wc -c`, `tail -c 12 | od -c`), exit status 0:

```
     364 spec.md
     169 acceptance-criteria.md
      87 001-alpha/spec.md
      94 002-beta/spec.md
    1246 goal.md
== spec.md
0000000    0   0   2   -   b   e   t   a   `       |  \n
== acceptance-criteria.md
0000000    t   h   i   n       b   u   d   g   e   t  \n
== 001-alpha/spec.md
0000000    n   e       p   e   r       r   u   n   .  \n
== 002-beta/spec.md
0000000    r       r   u   n       l   i   n   e   .  \n
== goal.md
0000000    H   O   R   :   l   o   g       -   -   >  \n
STEP4_VERIFY_EXIT_STATUS=0
```

`$SCRATCH/specs/demo-phase/spec.md`:

```markdown
# Demo phase packet specification

## Problem and purpose

Two concurrent runs append to counter.txt and the file sometimes ends up with
duplicated or lost lines. The fix is split across two phases.

## Decisions

Counter lines are plain text and append-only.

## Phase documentation map

| Phase | Folder |
|---|---|
| alpha | `001-alpha` |
| beta | `002-beta` |
```

`$SCRATCH/specs/demo-phase/acceptance-criteria.md`:

```markdown
# Acceptance criteria

- [ ] both phase folders hold a goal document
- [ ] the binding table lists every phase folder
- [ ] the parent durable slice stays within budget
```

`$SCRATCH/specs/demo-phase/001-alpha/spec.md`:

```markdown
# Alpha phase specification

Prove that counter.txt grows by exactly one line per run.
```

`$SCRATCH/specs/demo-phase/002-beta/spec.md`:

```markdown
# Beta phase specification

Prove that an interleaved run appends after the earlier run line.
```

`$SCRATCH/specs/demo-phase/goal.md` (the seeded parent goal, one binding row missing and the
unbound phase named in the objective prose):

```markdown
---
title: "Goal: Demo phase packet"
description: "The durable directive for the demo phase packet."
---
# Goal: Demo phase packet

---

<!-- ANCHOR:directive -->
## 1. DURABLE DIRECTIVE

**Objective:** Land the demo counter across the alpha and beta phases where 002-beta proves interleaving.

### Decisions

Frozen choices. Changing one is an amendment.

| ID | Decision |
|----|----------|
| D1 | Counter lines are plain text and append-only. |

### Operator copy

The operator holds this directive as the session objective. Whenever anything
above the log changes, resend this file's chat slice so the operator can update
their copy.
<!-- /ANCHOR:directive -->

---

<!-- ANCHOR:binding -->
## 2. BINDING

**Read the child goal before working a phase.**

| Phase | Goal document |
|-------|---------------|
| alpha | `001-alpha/goal.md` |

**Precedence.** Decisions above outrank child detail.

<!-- /ANCHOR:binding -->

---

<!-- ANCHOR:completion -->
## 3. COMPLETION CRITERIA

- [ ] Both phase folders hold a goal document
- [ ] The binding table lists every phase folder
- [ ] The parent durable slice stays within budget
<!-- /ANCHOR:completion -->

---

<!-- ANCHOR:log -->
## 4. LOG

Everything below is VOLATILE.
<!-- /ANCHOR:log -->
```

### Step 5

Command (`bash`, `$SCRATCH` substituted):

```
node .skilled/skills/sk-doc/sk-create-goal/scripts/check-goal.cjs specs/demo-phase --root /tmp/create-goal-playbook.QySkqS
echo "STEP5_EXIT_STATUS=$?"
```

Output (full), exit status 1 (expected):

```
[check-goal] packet=specs/demo-phase
[check-goal] 1/4 FAIL missing-binding-row findings=1
[check-goal] FINDING missing-binding-row specs/demo-phase: no binding-table target row for 002-beta/goal.md
[check-goal] 2/4 PASS placeholder findings=0
[check-goal] 3/4 PASS criteria-count findings=0
[check-goal] 4/4 PASS parent-budget findings=0
[check-goal] RESULT: FAILED (3/4 checks)
STEP5_EXIT_STATUS=1
```

Reply text to the prompt (the agent's answer), verbatim:

```text
  No. The binding table does not cover every phase folder. Do not sign off yet.

  The goal check fails on the binding check alone: no binding-table target row
  for 002-beta/goal.md. The disk folders are 001-alpha and 002-beta and the
  table has one row, alpha -> `001-alpha/goal.md`. The objective prose mentions
  002-beta, which reads like coverage but is not a binding row. Add the row
  | beta | `002-beta/goal.md` | and rerun the check before signing off.
```

### Step 6

Command (`bash`, `$SCRATCH` substituted):

```
grep -c '002-beta' /tmp/create-goal-playbook.QySkqS/specs/demo-phase/goal.md
echo "STEP6_GREP_EXIT_STATUS=$?"
```

Output, exit status 0 (expected):

```
1
STEP6_GREP_EXIT_STATUS=0
```

### Step 7

Command (`bash`, `$SCRATCH` substituted):

```
rm -rf /tmp/create-goal-playbook.QySkqS
echo "STEP7_EXIT_STATUS=$?"
```

Output, exit status 0:

```
STEP7_EXIT_STATUS=0
```

Scratch-root confirmation (playbook precondition 5), exit status 1 means the root is gone:

```
test -d /tmp/create-goal-playbook.QySkqS
SCRATCH_GONE_CHECK_EXIT_STATUS=1 (1 means removed)
```

## Expected signals

| Expected signal | Mark |
|---|---|
| Step 5 exits 1 with `RESULT: FAILED (3/4 checks)` | observed |
| `FAIL missing-binding-row findings=1` | observed |
| A `FINDING` line reads `no binding-table target row for 002-beta/goal.md` | observed |
| `PASS placeholder findings=0`, `PASS criteria-count findings=0`, `PASS parent-budget findings=0` stay green | observed |
| Step 6 prints 1 and exits 0 | observed |

No step failed to run. Nothing in this record is filled in from inference.

## Pass / Fail reasoning (scenario criteria only)

- The binding check fails with exactly one finding naming `002-beta/goal.md`: the step 5 transcript.
- The other three checks pass with zero findings: the step 5 transcript.
- The phase name still appears in the goal prose: step 6 prints 1, the objective sentence names
  `002-beta`, so the prose trap is genuinely tested and the check keys on the table rows and the
  disk folders rather than on the prose.
- The finding names the uncovered folder, not the covered one: `002-beta/goal.md`, while
  `001-alpha` has its row.

VERDICT: PASS — the binding check fails on the uncovered folder `002-beta` with the named finding while the phase name still appears in the goal prose.
