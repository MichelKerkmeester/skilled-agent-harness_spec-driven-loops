You are settling ONE angle of the sk-doc monolith -> WORKFLOW-ONLY parent-hub conversion. Repo root is CWD. Ground claims in file:line. BE FAST: a pre-computed search result file is provided — read it FIRST, then read at most 2 extra files. Do NOT run your own broad grep.

SEED: converting sk-doc (6-mode monolith) into a WORKFLOW-ONLY parent hub with a shared/ doc-quality backbone; ONE graph-metadata.json at the hub; two tiers; NO surface axis; ZERO named extensions (base case); root facade symlinks preserve ~151 external refs for zero external edits. Reference hub: sk-code. Over-decomposition lesson (124/022): fold generic doctrine into shared/.

ANGLE (facade-topology): Design the exact symlink facade set that yields ZERO external edits: which root paths under sk-doc/ must remain as symlink facades (scripts/, references/skill_creation/, assets/frontmatter_templates.md) so every external referencing file resolves unchanged. Enforce canonical-in-shared / symlink-inward; confirm no reverse or cyclic links (mirror the sk-code shared/ pattern).

PRE-COMPUTED SEARCH RESULTS (read this file — it already contains the grep/ls output you would otherwise compute): /Users/michelkerkmeester/MEGA/Development/Code_Environment/Public/.opencode/specs/skilled-agent-orchestration/125-sk-doc-parent/001-research-and-canon/research/precomputed/facade-topology.txt
Extra file to read if useful: .opencode/skills/sk-code/SKILL.md §3 (shared/ + symlink doctrine)

Output ONLY a JSON object: {"angle":"facade-topology","rulings":[{"question":"...","ruling":"...","rationale":"...","evidence":"path:line","confidence":"high|med|low"}],"risks":["..."],"open":["..."]}
Nothing except the JSON.
