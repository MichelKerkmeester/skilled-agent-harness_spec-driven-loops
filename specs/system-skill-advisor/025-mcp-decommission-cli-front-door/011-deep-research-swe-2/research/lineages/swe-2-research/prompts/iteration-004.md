# Iteration 4 dispatch prompt (recorded)

Focus: Q3 — assemble the repeatable checklist for the next transport-to-CLI migration, ordered so the steps preventing the most expensive failures come first. Every step must cite the packet episode that justifies it.

Sources assigned: iterations 1–3 findings (latent mechanisms + residue taxonomy), `003-cli-front-door-parity/parity/report.json`, `runtime/skill-advisor-cli.ts` warm-only/prompt-time defaults, goal.md completion criteria and deviation table.
