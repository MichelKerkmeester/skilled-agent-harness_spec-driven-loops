# Iteration 3 dispatch prompt (recorded)

Focus: Q2 — the classes of residue a transport removal leaves beyond obvious name references. Each class grounded in something actually present in this packet; each surviving hit bucketed as historical record vs live instruction surface; name which class the phase-007 sweep would have missed.

Sources assigned: `007-docs-and-residue-sweep/spec.md`, `008-verification-and-closeout/acceptance-criteria.md`, the archived `009` review report (finding classes), commits `127aef03e7`/`afd10f291f`/`9015d00c79`/`3feab865ea`, live `git grep` bucket counts over the current tree.
