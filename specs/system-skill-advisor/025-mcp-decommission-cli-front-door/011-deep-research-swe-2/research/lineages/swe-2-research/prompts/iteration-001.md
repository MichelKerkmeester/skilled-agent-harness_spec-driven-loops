# Iteration 1 dispatch prompt (recorded)

Lineage: swe-2-research (detached fan-out, executor cli-devin model=swe-2-max, session fanout-swe-2-research-1789189288280-etmi20)

Focus: Packet archaeology — establish what the MCP decommission actually shipped: the deletion/rename/rewire inventory, the ordering invariants, and the fallback-era architecture, before hunting latent failures.

Sources assigned: parent `spec.md`, `goal.md`, `001-transport-and-consumer-inventory/inventory.md`, `002-daemon-transport-decision/{baseline,protocol-contract,warm-mechanism}.md`, `003-cli-front-door-parity/spec.md`, plus `git log`/`git show` on the phase commits named in the goal log.

Constraints: read-only outside the lineage directory; no nested dispatch; cite `[SOURCE: file]` or `[SOURCE: command]` per finding; phase 010 research output excluded.
