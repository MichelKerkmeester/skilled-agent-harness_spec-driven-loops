# Iteration 002 — Live transcript richness

Determine the smallest durable relay vocabulary that exposes Pi's live stream as a useful mobile-native transcript rather than a chat-only mirror. Cover streaming text, summarized/omitted thinking, plan/TODO snapshots, tool-call inputs, file-edit diffs, tool results, and usage. Preserve immutable epochs, sequence replay, redaction, and foreground authority. Compare the vocabulary with Pi RPC and current Claude/OpenAI streaming conventions. State the observable test that proves this is richer and safer than the parity baseline.

