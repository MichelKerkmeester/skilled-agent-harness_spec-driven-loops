The retry policy lives in `.opencode/skills/system-spec-kit/shared/utils/retry.ts`.

It is the canonical copy, moved from `runtime/lib/utils/retry.js` (retry.ts:4). The policy itself is `DEFAULT_CONFIG` (retry.ts:14), three retries, a 1s base delay, a 4s cap and an exponential base of 2. The same module classifies transient versus permanent errors (`classifyError`, retry.ts:147) and exposes the entry points `retryWithBackoff` (retry.ts:265) and `withRetry` (retry.ts:390), with the backoff math in `calculateBackoff` (retry.ts:229). Its tests sit beside it as `retry.test.ts`, and its importers in this skill are the embeddings providers `voyage.ts:7` and `openai.ts:7`.

Nothing is blocked on you.
