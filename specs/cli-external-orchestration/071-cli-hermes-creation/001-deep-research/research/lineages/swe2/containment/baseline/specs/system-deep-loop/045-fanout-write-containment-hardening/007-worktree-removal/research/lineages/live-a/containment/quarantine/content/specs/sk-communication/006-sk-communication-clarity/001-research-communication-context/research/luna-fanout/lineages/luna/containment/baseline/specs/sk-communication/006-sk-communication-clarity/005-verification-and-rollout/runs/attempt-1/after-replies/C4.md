The command failed on purpose, as asked.

```
$ ls no-such-xyz
ls: no-such-xyz: No such file or directory
$ echo "exit: $?"
exit: 1
```

OBSERVED: the error text and the exit status both came from this run, executed from the repo root. Stdout stayed empty and stderr carried the failure.

Nothing was written, and nothing is blocked on you.
