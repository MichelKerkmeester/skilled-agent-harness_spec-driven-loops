# Research: Which parts of AGENTS.md no longer describe reality, and which explain what a delegate already owns

Lineage `staleness` of the root-doc-staleness research packet (`specs/sk-doc/055-governance-doc-alignment`, Stream C, REQ-004). Session `fanout-staleness-1789357391666-zngraa`, generation 1, mode `new`, executor `cli-pi` (glm-5.3-flash), five evidence iterations, 2026-09-14T03:59Z through 04:38Z. Every state record in this lineage went through the append gateway (ledger `deep-research-ledger`, sequences 1-7); every finding below cites file:line read this session.

## 1. The question, and the two failure classes

Class one, not reality: AGENTS.md names a file, folder, script, command, flag, or threshold that does not exist or no longer behaves as described. Class two, redundant detail: AGENTS.md explains at length what a repo rule, a skill, a reference, or an asset already owns, where the binding clause belongs in the delegate and the root should carry a pointer. The two classes stayed apart throughout; every class-two finding carries a three-way case call (case one: a delegate genuinely carries it, reduce to a pointer, quoting the delegate's own line; case two: it must bind when no delegate loads, keep; case three: prompt-time discipline no script or skill can enforce, keep), and every case-one call quotes the delegate.

**Verdict first.** Class one: exactly two failures. (1) The completion rule's `checklist.md` group: the scaffolder never produces the file, the verification checklist lives inside tasks.md, the closure gate is acceptance-criteria.md, and the rule never names the document that decides closure; five places, one cause. (2) The documented skill-advisor invocation exits 69/75 on this working tree because the shim's only compiled runner is absent; the documented daemon-start and Python-scorer fallback are unreachable until the runtime is built. Everything else swept, 39 paths and references: all exist. Class two: confirmed, with the specimen (the memory save rule) and five of the six nominated paragraphs yielding case-one reductions; Gate 3 cleared as a keep; section 8's summaries cleared as the design's deliberate degradation path. The target shape (section 6) governs every recommended rewrite; the irreducible keeps are exactly the clauses no delegate reaches, and they are enumerated, not asserted.

## 2. Method and provenance

- Five iterations, one focus each: (1) the checklist.md failure class; (2) the class-one existence-and-behavior sweep; (3) the class-two specimen plus the sixth, unnamed mandates candidate; (4) the git safety table, the validate.sh subsection, Gate 3; (5) the advisor metadata placement, the MCP routing section, the broadened class-one residue, and the section 6 adjudication. Narratives: `iterations/iteration-001.md` through `iteration-005.md`; deltas: `deltas/iter-001.jsonl` through `deltas/iter-005.jsonl`; per-iteration state records: `deltas/iter-00N.state-record.json`, each recorded through the append gateway (receipts: ledger sequences 2-6, event type `deep-research.ledger.iteration-completed`; the config record at sequence 1, `deep-research.ledger.run-initialized`).
- AGENTS.md (496 lines) was read in full once at 2026-09-14T03:59Z, this working tree; targeted re-reads afterward. Concurrent-edit risk (packet spec §6) is answered by the citations: every finding cites what was actually read, and the read-time is recorded.
- Documented deviations, all recorded in the strategy's WHAT FAILED: the shipped `reduce-state.cjs` resolves its artifact root from a spec folder, so its node steps ran in-process (the same satisfaction the YAML grants the executor-dispatch steps; the resource-map likewise hand-emitted, marked, and receipted below); the iteration-1 prompt was rendered post-hoc (iterations 2-5 rendered first); no continuity save and no validate.sh run, both outside the invocation's contained write surface (continuity save is non-blocking by this loop's success criteria).
- Evidence discipline: OBSERVED (a path, line, or command output read this session) versus INFERRED (marked, with the step that would confirm it) is stated on every finding; the three INFERRED residues that survive are listed in the Open Questions.

## 3. Class one, finding 1: the checklist.md group (five places, one cause)

The failure: the completion rule instructs loading a file the scaffolder never creates, and never names the document that decides closure.

- AGENTS.md:263 — "2. Load `checklist.md` → verify ALL items → mark `[x]` with evidence." (step 2 of the COMPLETION VERIFICATION RULE)
- AGENTS.md:266 — "`plan.md` / `tasks.md` / `checklist.md` evidence rows." (the metadata-reconciliation list)
- AGENTS.md:270 — "Skip: Level 1 tasks (checklist.md is optional at every level)."
- AGENTS.md:303 — the Self-Check: "Claiming completion? `checklist.md` verified?"
- AGENTS.md:463 — the quick-reference claim-completion row, "validate.sh <spec-folder> --strict → checklist all items → reconcile metadata" (the verb form; the fifth place by the invocation's counting).

The reality, from the producer's own words: the scaffolder's level map is "L1: Essential what/why/how - spec, plan, tasks; summary follows lifecycle; L2: +Quality gates, verification - merged into tasks.md; L3: +Architecture guidance - decision-record.md is on-demand" (create.sh:12-16; the L1 help text at :304-308); its contract-doc function adds acceptance-criteria.md "at every level whose contract lists it", because "the closure gate needs this document... Without it the scaffolder would emit packets that fail validation the moment they are created" (create.sh:450-456). The template contract: "acceptance-criteria.md: one row per criterion, each Met, Waived or Superseded before the packet may close; the verification checklist lives in tasks.md", enforced by "AC_CLOSURE fails when acceptance-criteria.md is absent from a packet created after 2026-08-30" (template-mapping.md:159,161). The consumer side: 1203 depth-3 packet directories, 0 files named checklist.md, 283 acceptance-criteria.md, 4185 spec.md as the positive control (this session's census). The requirements authority AGENTS.md:372 routes to, folder-structure.md, never mentions checklist.md; the name survives only in the phase-parent prohibited/legacy list (template-mapping.md:70). The packet's own documents agree: 055's tasks.md carries `## Verification Checklist` (line 109), `## Verification Protocol` (112), `## Testing Checklist` (145), `## Verification Summary` (199); 055's acceptance-criteria.md:3: "The criteria this packet must satisfy before it may be closed"; 055's spec.md:124-125: "Acceptance criteria for these requirements live in `acceptance-criteria.md`, which is the document that decides whether this packet may close."

## 4. Class one, finding 2: the skill-advisor invocation (behavior)

AGENTS.md:99 promises: "run `node .opencode/bin/skill-advisor.cjs advisor_recommend --json '{...}' --format json`... It starts the daemon when needed. When the daemon is unreachable the CLI falls back to a local Python scorer, marks the answer degraded, and the brief shows `Advisor: stale`."

Reality on this working tree: the shim's only runner is the compiled CLI `.opencode/skills/system-skill-advisor/runtime/dist/runtime/skill-advisor-cli.js`, which is absent, and `ensureFreshDist()` fails before any daemon start or fallback: "if (result.status === 'missing' || result.stale) { ... fail(result.message, warmOnly ? EXIT_RETRYABLE : EXIT_PROTOCOL, ...) }" with EXIT_PROTOCOL = 69, EXIT_RETRYABLE = 75 (skill-advisor.cjs:28-30,62-69). The documented one-liner therefore exits 69 (or 75 with --warm-only) with the freshness message. The command name itself is correct: the advisor's ARCHITECTURE.md nine-command contract names the recommendation command `advisor_recommend`. [SOURCES: .opencode/bin/skill-advisor.cjs:28-30,38-44,62-69; .opencode/skills/system-skill-advisor/ARCHITECTURE.md]. INFERRED, with the confirming step: that the runtime failure surfaces as the source predicts, because the invocation was deliberately not executed (its socket state would land in /tmp, outside the lineage); the operator confirms with `node .opencode/bin/skill-advisor.cjs advisor_recommend --json '{"prompt":"probe"}' --format json; echo $?`.

## 5. Class two: the specimen, the sixth, unnamed candidate, and the negative space

- **The memory save rule (AGENTS.md:281-286), the invocation's specimen: case one on three of five bullets, with two keeps.** The delegates: references/memory/save-workflow.md (627 lines) and .opencode/commands/speckit/save.md (92 lines). The compiled path appears FIVE times across the three documents (AGENTS.md:285; save-workflow.md:211,216,222,244 — the last as the preflight `test -f`; save.md:19,68). What the save writes: the root's:285 versus the delegates' own lines, "packet narrative remains in canonical docs rather than a parallel memory note" (save-workflow.md:331), "Canonical spec-doc content is authored in the packet documents themselves; the writer owns the continuity frontmatter and the generated metadata pair" (save.md:61, echoed :90). The post-save quality review: the root's:286 versus "After `generate-context.js` completes, it emits a **POST-SAVE QUALITY REVIEW** block" (save-workflow.md:553) with "| **HIGH** | MUST manually patch via Edit tool" (:559) and "### Common HIGH Issues" (:563), and the command's step 6 (:61). Who composes: the root's:284 versus "AI agent creates structured JSON summary...; 4. AI agent calls `generate-context.js` with JSON data" (save-workflow.md:151-152) under "All paths feed the same canonical save entrypoint" (:24). The case calls: :284 (pointer half) and :286 = case one; :285 = case one with ONE undelgated mechanic (the "Editing the continuity frontmatter directly is a legitimate shortcut when only continuity changed" residual); :282-283 = case three, the session-bound trigger pair (which folder, whether Gate 3 was answered), stays. The duplication's cost, observed: the root's shortcut clause versus the command's "The writer is the only write path. Do not hand-edit generated metadata to stand in for a save" (save.md:25) reconcile only by an unstated scope line.
- **The sixth, unnamed candidate: the Documentation & Honesty mandates (AGENTS.md:475-479), found by the specimen's pattern, partially confirmed.** :477 ("Use 'UNKNOWN' when uncertain") = uncertainty-and-honesty.md:62's "Write `UNKNOWN: <what you don't know>` and add what would resolve it"; :479 (the "I'M UNCERTAIN ABOUT THIS:" prefix) = :72. Both case one. :478 (the 80% clarify threshold) = case two BY THE DELEGATE'S OWN BACK-POINTER: "The scale is the Confidence Thresholds table in `AGENTS.md` §2 and there is exactly one of it; this file carries no second copy" (uncertainty-and-honesty.md:48-49) — the delegate deliberately makes the root carry the scale. The:474 expanded-by pointer already exists; the:477+479 rows reduce to it.
- **The negative space (what did NOT fail), so the next audit does not re-derive it.** 37/37 named paths, directories, commands, and references exist (the probe's else-branch demonstrated the same session). validate.sh's exit contract matches AGENTS.md:262 (the footer's 0/1/2/3; usage = 1, infrastructure = 3, the orchestrator's status propagated via `run_validation || rc=$?; exit "$rc"`). The shared classifier exports `classifyPrompt(prompt, options): ClassificationResult` (gate-3-classifier.ts:859). sk-code carries `## 2. SMART ROUTING` (:50); the spec-kit SKILL.md carries `### Distributed Governance Rule` (:59); both trigger-index scripts exist; recommend-level.sh scores exactly "LOC, file count and risk" (`--loc`, `--files`, `--auth --api --db --architectural`, :8-24). sk-design-md-generator is the sk-design hub's measure mode, reached through the `design` agent (design.md:3,23). figma is registered (.utcp_config.json:86-95, "figma-developer-mcp@latest"), with .claude/mcp.json and .codex/config.toml present. goal.md is the WITH_GOAL lazy addon (create.sh:466-467; 14 packets carry one; 055 does not) — the GOAL POSTURE RULE is conditional, not stale. trustedEvidenceYield is a real, optional wire field (deep-research-ledger-schema.ts:217) whose absence the gateway records as explicitly zero. The retrieval unsupported-features clause (AGENTS.md:315) is the retrieval conventions' own:64 row verbatim, with the no-hit meaning at :40 and the exit contract at :167-173 — case one, the root's row keeps its purpose and its:316-recipes pointer. The goal hook binary (.opencode/hooks/goal/bin/goal.cjs) and the trigger-index data file (runtime/data/trigger-index.json) both exist.

## 6. Class two: the five nominated candidates, adjudicated

| Candidate | Verdict | The delegate's own line (case one) or the keep's reason |
|---|---|---|
| The git safety table (AGENTS.md:318-331) | 7/8 rows case one; the ask-first-worktree row (324) case three, stays | "Owns: git worktree / ... / branch naming allocator / ... Does NOT own: spec folders, memory, continuity, save context (system-spec-kit); code implementation, tests (sk-code)" (SKILL.md:79); the seven-digit ordinal, "minted once in commit order and never reissued" (conventional-commit-workflows.md:32); the hook refusals (:34); the allocator, "allocated by `worktree-naming.sh`, which holds the clone-wide lock" (shared-patterns.md:34,57); the push ask, "The AI must NEVER push a branch to `origin` outside the remote allowlist without a fresh, explicit go-ahead for THAT push" (SKILL.md:301) and the bypass, "set `SPECKIT_ALLOW_REMOTE_PUSH=1` for that one `git push` only" (:307); the hook registration, "All six AI runtimes carry the same shared hook... (registration, delivery,...)" (:314-321); the live-sync leg (:297). The:324-ask: conversational, no delegate (the skill's ask-hits are the push ask; blast-radius.md:72 prices a one-line-revert fork as decide-and-move-on, which is the quoted exception) |
| The validate.sh subsection (272-278) + the:262 step-1 parenthetical | Case one, TWICE | "CLI taxonomy: `0` = success, `1` = user error, `2` = validation error, and `3` = system error. `--strict` selects the rules that only run under strict; a warning stays advice in both modes and never changes the exit code. A rule that should block reports an error itself." (validation-rules.md:35 — the:262 parenthetical, near-verbatim); §14 "INVOKING VALIDATE.SH: FOUR WAYS A RUN LIES" (:757-775), whose item 1 IS "Require an explicit `RESULT: PASSED`" (:765) and whose preamble states its own ownership: "They belong here because this document owns what a validation run means; a copy kept anywhere else goes stale the first time the harness moves" (:758-760). The root's:274-276 IS that sentence, shortened — the demonstration of the(delegate's)own thesis |
| Gate 3 (63-77) | CLEARED, keep | The classifier owns the FIRE vocabulary only: `ClassificationResult { triggersGate3, requiresGate3Prompt, satisfiedBy, writeBoundary, reason, matched }` (:107-119) and the exported `Gate3VocabularySnapshot` ("Serializable JSON snapshot of the vocabulary (for Python / YAML consumers)", :878-882). The A-E option texts appear nowhere in it (0 hits; positive control: the same rg resolves the SpecRoot labels at :127,137,375-376). The:64 fire-sentence is the:66-declared "human-readable form for runtimes that do not" call it (case two); the:67-72 texts and the:76-77 session/child-dispatch clauses are question-time (case three); the:73-74 guards are already pointers-with-why |
| The advisor metadata placement (110-112) | Case one | "Not the spec-folder schema. ... The two are never the same file and never interchangeable." (skill-root-metadata-contract.md:32); the class table, H declares "both" / S declares "neither" (:40-41); the placement table, `description.json`/`mode-registry.json`/`hub-router.json` required-at-H-and-forbidden-at-S, `leaf-manifest.config.json` the reverse (:65-68); "Forbidden is not stylistic. Each forbidden file would create a second source of truth..." (:73,77); the fleet gate, the contract's own subtitle (:5). The:112 two-stage paragraph = the skill-hub-routing rule ("registered is not routed...", REPO RULES.md:68) plus the nested-packets pointer. The root uniquely keeps the §6-cross-reference — a delegate cannot reference the root's sections |
| The MCP routing section (351-359) | MIXED | The:353 second sentence duplicates the skill's own `mcp__code_mode__call_tool_chain` (SKILL.md:4) — reducible. The:357 ("Enumerate at runtime, never from a written list... naming-convention.md owns the prefixing rules and the `cli`-manual exception") is ALREADY the target shape and the in-document exemplar — untouched. The:359 ("Registration is not availability... Never promise that a manual named in the config is live") is reporting-time honesty, no delegate found (INFERRED; the:355 proviso likewise pending) — stays |

## 7. The section 6 adjudication (question Q5)

The target shape, AGENTS.md:363-377: a question, the answer's pointer, exactly one local rule, the why ("One rule stays here because it is prompt-time discipline no script enforces" — the:377's own precedent). Applied to every confirmed finding:

1. The completion rule: step 2 becomes "Load the verification checklist inside `tasks.md`; closure is decided by `acceptance-criteria.md`"; the:266 row drops checklist.md and gains acceptance-criteria.md; the:270 skip and the:269 freshness clause stay (the rule's own); the:303 item and the:463 row re-point. The surviving local rule: the four-step ORDER (validate → checklist → reconcile → freshness); the why: the:262's own, "a warning is advice... A rule that should block says so itself by reporting an error."
2. The advisor invocation: the:99 sentence gains one clause — the documented invocation requires the compiled advisor CLI, and the shim exits 69 (75 with --warm-only) naming the build when it is stale or missing.
3. The case-one class-two paragraphs reduce to: trigger → pointer → the one undelgated clause → the why. The memory save rule keeps :282-283 (session state), :284's "Compose the session JSON yourself — you have strictly better information about your own session", and the:285 shortcut clause; the mandates keep :478; the git table keeps the:324-ask and the:331 "not just conventions" warning; the validate.sh keeps the:262 step-1 requirement and the:272-278 "Require an explicit RESULT: PASSED" clause; the advisor metadata keeps the §6-cross-link; the MCP section keeps the:357 exemplar and the:359 honesty clause.
4. The keeps, argued: a clause stays when no delegate reaches the moment it binds. The:282-283 (only the session knows), the:478 (only the root owns the single scale, by the delegate's own:48-49), the:324-ask (conversational; blast-radius.md:72 prices the fork cheap), the:67-72+76-77 (question-time; the classifier's:107-119,878-882 own the fire, not the question), the:359 (promise-time), the:64 fire-sentence (the:66's declared human-readable twin for non-hook runtimes), and section 8's:401-405 one-line summaries (they bind on the read-only turn where no repo-rule loads — the:407's stated design: "Delivery never softens rigor" and "voice is not a performance"). That is the:363-377 shape applied consistently, not a seventh duplicate of it.

Judgment disclosure: this adjudication is one model's reading, grounded in the quoted delegate lines. What would change it: the sibling lineages' verdicts on the same clauses, which the packet's merge step compares.

## 8. Recommended actions (all deferred: the packet's Files-to-Change rows defer AGENTS.md, REPO RULES.md, and repo-rules/ until the sequencing question is answered)

1. Fix the completion rule's five places (one cause): re-point :263, :266, :303, :463 at the tasks.md checklist and acceptance-criteria.md, per template-mapping.md:159-161. (class one; finding 3)
2. Add the advisor build prerequisite to :99 (one clause; the:69/75 exits already teach it). (class one; finding 4)
3. Reduce, per the:6-shape: the:285-286 and:284-remainder to the save-workflow pointers; the:477+479 mandate rows to the:474 pointer; the git table's seven rows to their:316-row; the:262 parenthetical and the:272-276 trap prose to validation-rules.md:35,757-775; the:110-112 paragraph to the:6-cross-link plus the:contract pointer; the:353 second sentence to the:skill pointer. Each reduction keeps its quoted delegate line within reach: the pointer's TARGET now carries the clause, verified this session. (class two, case one)
4. Leave the keeps (section 7.4) untouched: they are the clauses no delegate reaches.
5. Then re-run the:algebra:after:any:spec-doc:edit, the:metadata:regeneration (validation-rules.md:772-774, trap 4), because:the:five:edits:touch:the:root:doc.

## 9. Eliminated Alternatives

| Approach | Reason Eliminated | Evidence | Iteration(s) |
|---|---|---|---|
| checklist.md produced by the scaffolder at some documentation level | The scaffolder's own header, help text, contract-doc function, the templates tree (zero *checklist* matches at any depth), and the 0/1203+ census | create.sh:12-16,304-308,450-456; the templates find; the census | 1 |
| checklist.md required by the level-requirements authority (folder-structure.md) | Zero occurrences there; only the phase-parent prohibited/legacy list keeps the name | folder-structure.md (rg -c); template-mapping.md:70 | 1 |
| The advisor subcommand (advisor_recommend) being a stale spelling | The advisor's own nine-command contract names it; the failure is the absent compiled dist | ARCHITECTURE.md; the shim's:28-30,62-69 | 2 |
| The memory save rule's mechanics lacking a delegate | Both delegates carry them; only the trigger pair, the one-line why, and the shortcut clause are undelgated | save-workflow.md:24,151-152,211-244,553-578; save.md:19,25,61,68,90 | 3 |
| The shared classifier carrying the A-E question texts | It defines the FIRE vocabulary only (ClassificationResult, the:VocabularySnapshot) and none of the five option strings; the:SpecRoot-resolving:grep was the positive control | gate-3-classifier.ts:107-119,878-882; the:0-hit:grep | 4 |
| The worktree-vs-branch ask clause riding in sk-git or blast-radius | The skill's:ask-hits are the:push-ask; its:worktree-references carry:mechanics:only; blast-radius:prices:the:fork:cheap (the:quoted:exception). Caveat: the:workflow-playbooks:directory:was:not:exhausted | SKILL.md:301,375; shared-patterns.md:113-125; blast-radius.md:72 | 4 |
| The:315 unsupported-features clause lacking a delegate | The:retrieval-conventions':64:row:secondes:it:verbatim, with:the:40:no-hit:meaning:and:the:167-173:exit:contract | retrieval-conventions.md:40,64,167-173 | 5 |
| The:goal-hook:binary:or:the:trigger-index:artifact:missing | Both:exist:beside:the:tracked:retrieval:scripts | test -e; find; this:session | 5 |

## 10. Open Questions

- Does any sk-git workflow-playbook carry the worktree-vs-branch ask? (The:completeness:caveat:from:iteration:4; the:confirming:step: `rg -in "A\) Create|B\) Work|ask the operator" .opencode/skills/sk-git/feature-catalog/`.)
- Does the:359 registration-is-not-availability clause, or the:355 Skill-Advisor:independence:proviso, have:a:delegate:in:the:respective:skills':own:docs? (The:greps:say:no; the:reads:were:not:exhaustive.)
- Is:runtime/data/trigger-index.json:tracked:in:this:working:tree? (The:81's:committed-index:claim:relies:on:it; the:confirming:step: `git ls-files .opencode/skills/system-spec-kit/runtime/data/trigger-index.json`.)
- The:packet's:own:open:questions:the:sequencing:question:and:the:006:ratification:question, remain:the:packet's:(spec:§7);:this:lineage:neither:answers:them:nor:acts:past:its:containment.

## 11. Convergence Report

- Stop reason: `maxIterationsReached` (stopPolicy: max-iterations; the:cap:governs:the:label). The:all-questions-answered:nomination:also:fired (5/5 strategy:questions:answered:with:evidence):recorded:in:the:registry:and:the:strategy;:the:two:reasons:coincide:at:the:cap, and:the:hard-stop:rule:the:decision:order:evaluate:first:the:iteration:count:the:181-183's:max-iterations, produced:the:recorded:reason.
- Total:iterations:completed: 5 of 5 (forced:depth; the:iteration:files:and:the:records:are:exactly:1..5).
- Questions:answered:ratio: 5/5.
- Average:newInfoRatio:trend: 1.00, 1.00, 1.00, 1.00, 1.00 (flat:at:full:novelty; every:iteration's:evidence:was:new:to:the:packet; the:convergenceThreshold:0.05:was:never:the:binding:constraint).
- Quality:guards: the:STOP:candidate:here:is:the:hard:max-iterations:stop, which:the:decision:order:exempts:from:the:legal-stop:gate:bundle;:the:guards:were:not:the:binding:constraint. Source:diversity:was:structural:the:every:finding:names:the:AGENTS.md:clause:AND:the:delegate:or:the:producer:the:the:two:independent:sides:of:each:claim.

## 12. References

- Primary: AGENTS.md (496:lines, this:working:tree, read:in:full:2026-09-14T03:59Z; the:targeted:re-reads:are:cited:inline:by:line).
- Delegates:the:producers:the:quoted:throughout:the:create.sh, the:template-mapping.md, the:validation-rules.md, the:save-workflow.md, the:save.md, the:uncertainty-and-honesty.md, the:blast-radius.md, the:REPO RULES.md, the:SKILL.md:and:the:references:of:the:sk-git, the:mcp-code-mode, the:sk-doc's:metadata:contract, the:gate-3-classifier.ts, the:advisor's:shim:and:ARCHITECTURE.md, the:retrieval:conventions, the:deep-research:ledger:schema, the:055:packet's:spec, the:tasks, and:the:acceptance-criteria.
- Lineage:artifacts: the:state:log:(the:append-gateway:projection:of:the:ledger:the:deep-research-ledger, the:sequences:1-6:the:run-initialized + 5×the:iteration-completed; the:terminal:synthesis:record:the:synthesis_complete:and:two:canonical-stem:forms:were:REFUSED:by:the:shipped:gateway:the:RUNTIME_ERROR:the:the:attempts:and:the:evidence:logged:in:deltas/synthesis.record.json; the:runner's:research-branch:documented:completion:signal:for:a:forced-depth:run:is:the:artifact:check:fanout-run.cjs:874-901, which:this:lineage:passes, and:the:stopReason:maxIterationsReached:is:recorded:here, in:the:registry, the:strategy, and:the:synthesis:record:file), the:strategy, the:registry, the:dashboard, the:five:iteration:narratives, the:five:deltas, the:five:state-records, the:five:rendered:prompts, this:synthesis, and:the:in-process:resource-map:emission:at:resource-map.md:(marked:hand-emitted:from:the:converged:deltas, the:containment:deviation:the:shipped:emitter:resolves:its:paths:from:a:spec:folder).
- resource-map:the:NOT:the:packet's:specs/sk-doc/055-governance-doc-alignment/resource-map.md, which:was:absent:at:init (resource_map_present: false, the:coverage:gate:skipped);:no:placeholder:was:synthesized:for:it.
- Continuity:the:post-loop:continuity:save:via:generate-context.js:was:NOT:run (the:containment:constraint;:the:non-blocking:the:success:criterion), the:only:completion:step:this:lineage:leaves:the:operator's:environment:to:perform:or:waive.

## 13. Honest status

What:ran:and:was:read: every:command:and:the:receipt:cited:above:the:the:gateway:the:receipts:the:sequences:1-6:the:the:exit:0 + the:ok:true + the:eventType:read:each:time, the:censuses:the:counted:with:the:positive:controls, the:quoted:the:delegate:the:lines:the:read:this:session. What:is:INFERRED: the:three:caveats:in:the:Open:Questions, each:with:its:confirming:command. What:only:the:environment:can:verify: the:advisor:invocation's:runtime:failure:text (the:one:command:named:in:the:Open:Questions), and:the:commit:status:of:this:lineage's:artifacts (untracked:working-tree:files;:no:git:write:was:performed, per:the:containment:contract).
