The retry policy lives in `service/retry.ts`, in the `withRetry` helper.
I checked with `rg -n "withRetry" service` and it returned three hits in that one file.
Nothing else references it.
Next: address the two review notes on the timeout default.
Still open: the timeout default question.
