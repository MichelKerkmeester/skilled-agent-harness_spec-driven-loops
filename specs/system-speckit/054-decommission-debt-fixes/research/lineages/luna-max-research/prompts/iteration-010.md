# Iteration 10 prompt

Review the active configuration, launcher, CI, ignore, and package-identity surfaces for residue from the retired memory database, memory MCP/tools, spec-memory launcher, zvec lane, system-plugins, and the old system-spec-kit mcp-server identity. Start with the worktree session launcher and runtime path resolution, then compare the successor trigger-index doctor and active CI wording. Do not run commands that write outside the lineage. Every finding must distinguish confirmed path/configuration facts from inferred runtime impact and cite file:line evidence.

