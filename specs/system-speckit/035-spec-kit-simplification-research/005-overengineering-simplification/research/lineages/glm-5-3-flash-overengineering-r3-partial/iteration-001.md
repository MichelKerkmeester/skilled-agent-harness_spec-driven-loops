# Iteration 001 — Angle 1: rule value, registry rows 1–12 (run 1 of 5)

**Focus.** For each of the first 12 registry rows (`runtime/cli/lib/validator-registry.json`): the defect class its rule catches (header comment of the rule script only), whether another row or a template catches the same defect, and whether a test file under `runtime/cli/tests` or `runtime/tests` names the rule id. Finding rule: a rule with an overlapping sibling AND no test.

**Key questions.** KQ-R3-1a: which of rows 1–12 carry a defect class duplicated by another row (or template)? — KQ-R3-1b: which lack a naming test?

**Row 1–12 identity (from the registry, read this iteration):** 1 FILE_EXISTS, 2 PLACEHOLDER_FILLED, 3 COMMENT_HYGIENE_MARKER, 4 SCAFFOLD_NEVER_TOUCHED, 5 STATUS_CROSS_DOC_CONSISTENCY, 6 LEVEL_DECLARED, 7 AC_COVERAGE, 8 AC_CLOSURE, 9 ANCHORS_VALID, 10 FRONTMATTER_MEMORY_BLOCK, 11 MERGE_LEGALITY, 12 SPEC_DOC_SUFFICIENCY. Rows 1–8 = physical `rules/check-*.sh`; row 9 = `native:orchestrator`; rows 10–12 = `ts:spec-doc-structure` (no physical rule script — evidence: registry description + implementing module header + test coverage only).

## Method — tool-call ledger (12/12, hard cap reached)

1. edit — fix self-introduced typo in lineage `deep-research-config.json` (`fanout_lineage_artifact_dir`).
2. read — census `research/confirmed-findings.md` (the mandated single read; all its dispositions are this lane's dedup baseline).
3. read — `runtime/cli/lib/validator-registry.json` (full; 39 rows).
4. bash — JSON-syntax self-check of the three init artifacts (python3 -m json.tool; no node/git/validate.sh per dispatch); `rules/` listing; header comments of rows 1–8's scripts; test-tree occurrence grep (per-file counts; the id→count mapping was lost to a sed and re-taken in call 7); both tests trees' existence confirmed.
5. bash — registry claim-side line numbers; LEVEL_MATCH header (overlap probe); SCAFFOLD header continued; orchestrator.ts location + head; `ts:spec-doc-structure` resolution; per-rule-id test-occurrence counts; vitest-suite mentions; pre-commit hook probe.
6. bash — orchestrator: `spawnSync`/`entries.push`/`check-` occurrences; :925-952 context; the single COMMENT_HYGIENE_MARKER test mention; worktree `.git` pointer.
7. bash — dispatcher :215-252 (resolve/spawn predicates); STATUS_CROSS_DOC TS-side probe (absent); `validate.sh` sourcing probe (0 `rules/check` hits, size 16,265B, mtime Sep 7 06:32).
8. bash — how validate.sh invokes rules (:46-302: registry-print via `node -e`, then the orchestrator — compiled-.js preferred, :275-302); `readValidatorRegistry` callers (:391, :425).
9–12. this file, `deltas/iter-001.jsonl`, the state-JSONL append, the strategy refresh.

Not read (no call remained): orchestrator.ts :385-435 (the dispatch loop's skip/replace logic around :391/:425); `validate.sh` beyond :302 (grep head-truncated at 10 matches); the bodies of any rule script (header-only scope); the `node -e` block at validate.sh:47-61. Recorded as open questions.

## Findings (4 rows; the angle's predicted class "overlap + no test" produced none — see verified-correct 1)

| # | id | Claim side | Actual side | Claimed | Actual | Severity | Recommendation |
|---|----|-----------|-------------|---------|--------|----------|----------------|
| 1 | F3-01 | `runtime/cli/lib/validator-registry.json:48-56` (row 5: severity `error`, no `flags` key, description :56) | `runtime/cli/rules/check-status-cross-doc-consistency.sh:5-10` + `runtime/cli/tests/validation-gate-hardening.vitest.ts:178-188` | "Advisory-by-default check that spec.md and implementation-summary.md status fields classify to the same delivery state." | The script is enforcing-by-default ("Graduated to enforcing only after a real tree-wide census…Set SPECKIT_STATUS_CROSS_DOC_ENFORCE=false to fall back to advisory-only") and the dedicated 3-mode test exercises byDefault/advisory/enforced; the row carries no `flags` key. | P2 | fix — the registry is the rule list of record (per the F2-10 fix) and its one row states the inverted default and hides a live flag; the F2-fixed help printer derives categories from this same registry, so the stale copy is exactly one propagation step from a second F14. |
| 2 | F3-02 | `runtime/cli/lib/validator-registry.json:15-24` (one mechanism: "Rejects unfilled template placeholders…", script `rules/check-placeholders.sh`) | `runtime/rules/…/check-placeholders.sh:8-9,19-21` + `runtime/lib/validation/orchestrator.ts:617,623,630,940` | One registered mechanism rejects unfilled placeholders. | Two synchronized implementations: the registered bash row ("Parity: mirrors the runtime orchestrator validatePlaceholders. Mustache {{…}} is NOT flagged") AND a TS-native twin pushed into every report at :940, whose own header (460→:617 "Canonical spec-doc placeholder markers. Mirrors check-placeholders.sh") and :630 ("itself skipped, matching check-placeholders.sh awk behavior") carry maintenance memoranda — the two files must be edited in lockstep and already drifted into documented Differences (Mustache). | P2 | merge — one implementation owns the class, the other reports by delegation; the census kept F1 (three layers, each carrying what the others cannot), but this rule lives provably in two at once, which is the duplication F1's rationale said the layering avoids. Whether the other three suspected twins (see open question 3) behave the same is it.2's evidence, not claimed here. |
| 3 | F3-03 | `runtime/cli/lib/validator-registry.json:100,112,123,133` (script_path = `native:orchestrator` / `ts:spec-doc-structure`) | `runtime/lib/validation/orchestrator.ts:223-241` (resolver returns `null` for both schemes) + `runtime/cli/core/memory-metadata.ts:1-6` | The row's `script_path` identifies where the rule lives. | For 33 of 39 rows it is a path that exists; for rows 9–14 it is a scheme-string: no file named `*spec-doc-structure*` exists (string search: 2 hits — the memory-metadata module and a test), the implementing module's header says only "Memory classification, session dedup, causal links, and evidence snapshot construction. Extracted from workflow.ts", and the resolver resolves neither scheme — the rows run only because :936-951 hard-pins five of them and its own comment (:946-948) documents that otherwise they "vanish from the report entirely". | P2 | document (a one-line legend for the `native:`/`ts:` schemes and their real targets) or fix (point the rows at the real module paths). Rows 13–14 share the scheme; their family judgment, like the overlap question, belongs to it.2 and is not claimed here. |
| 4 | F3-04 | `runtime/cli/lib/validator-registry.json` as the rule list of record (post-F2-10: "the registry's rows are the rule list") | `runtime/lib/validation/orchestrator.ts:217-222,223-241,425` + `runtime/cli/spec/validate.sh:46` | 39 rows = what a validation runs. | The engine's failure mode is silence: `readValidatorRegistry` returns `[]` when the file is missing (:218) and silently filters non-conforming rows (:219); `resolveRegistryRuleScript` returns `null` on an unresolvable `script_path` (:223-241); the dispatch loop (:425) then simply runs fewer rules, and nothing on the report says so — the :946-948 comment is the mechanism's own admission. The shell side notices only in its help function (:46), which prints "RULES: registry unavailable" and then `return 0`. | P1 | fix — emit a report entry when the registry is missing or N rows fail to resolve; a green report that quietly ran 10 of 39 rule-classes is precisely the green-versus-reality gap this lane's covenant leans on validate to close. (Caveat, stated: :391's consumer was not read and no other upstream guard was traced — call budget; the finding claims only :217-241, :425, :946-948 and validate.sh:46.) |

## Verified as correct (this angle's clean verdicts)

1. Test coverage — 13/13 searched ids are named somewhere under the two tests trees; weakest: COMMENT_HYGIENE_MARKER×1 (`runtime/cli/tests/test-validation-extended.sh:891` — a real harness category banner, not fixture data) and LEVEL_MATCH×2; strongest: ANCHORS_VALID×20. The angle's predicted finding-class (overlapping sibling + no test) therefore bites nowhere in rows 1–12. (Caveat: counts are occurrence counts over code+fixtures in the tests trees; per-file ids were not resolved.)
2. Defect-class fidelity — 8/8 physical-rule headers state a defect class matching their registry description (FILE_EXISTS:16-18↔registry:12; PLACEHOLDER_FILLED:19-21↔:21↔:15-24; SCAFFOLD:44-45↔:37-38; LEVEL:59-60↔:59; AC_CLOSURE:87-89↔:85-86, including the waiver→existing-decision-record language); the single divergence (row 5's default) is F3-01.
3. SCAFFOLD_NEVER_TOUCHED vs PLACEHOLDER_FILLED — distinct classes, not overlapping siblings: scaffold-origin documents additionally gated on the folder's Complete status (`check-scaffold-never-touched.sh:44-45`) vs unconditional literal-marker scan (`check-placeholders.sh:19-21`). The iteration's pre-registered "do the two marker scanners overlap?" question: answered NO at header level.
4. COMMENT_HYGIENE's cross-system boundary is documented in its own header (spec-folder documents at validate time vs the sk-code-quality checker for code comments — `check-comment-hygiene.sh:12-14`): complementary scopes, not duplication.
5. No third never-failing row in 1–12: row 5 enforces by default (3-mode test, vitest:178-188), and the info pair LEVEL_DECLARED/AC_COVERAGE is the census's F6-kept pair (AC_COVERAGE gains its enforce switch from child 010, header `check-ac-coverage.sh:11-13` documents the falsey-typo lesson). F6's count of two stands.
6. ANCHORS_VALID is the most-tested id in the searched trees (20 occurrences, e.g. `validation-engine-coherence.vitest.ts:99` expects it by name).

## New information / novelty

newInfoRatio 0.65 — the census judged these rows' value (F1/F6/F8/F14 kept-or-fixed; F2 fixed the printer), but the header-vs-registry drift on row 5, the proven bash/TS twin-pair for placeholders with its in-code sync duty, the scheme-vs-path resolution gap, and the silent-`[]`/vanish failure mode were all unrecorded; the angle's own predicted finding-class produced a documented NO (13/13 tested).

## Ruled out

- "SCAFFOLD markers subsume placeholder markers (one rule suffices)" — the headers define distinct classes (origin+status-gated vs unconditional literal); evidence: `check-scaffold-never-touched.sh:44-45` vs `check-placeholders.sh:19-21`.
- "COMMENT_HYGIENE duplicates the sk-code code-comment checker" — complementary scopes documented in its own header (`check-comment-hygiene.sh:12-14`).
- "A third never-failing row hides among 1–12" — row 5 enforces by default with a 3-mode test; the only info pair is the census's own F6-kept pair (evidence: :5-10, vitest:178-188, F6).
- "The help printer's F2-fix hides rules again" — the printer derives categories from the registry (:47-61 `node -e` over `VALIDATOR_REGISTRY_JSON`); nothing this iteration saw contradicts the F2-fixed 39-of-39 state.

## Open questions

1. Does `validate.sh` source any `rules/check-*.sh` beyond :302? The :46-302 matches show only the registry-print and the orchestrator invocation (:297/:302); the grep's 10-match head could have swallowed one later `source`. Bears on whether the 8 "Sourced by validate.sh" headers are true or 8 counts of stale self-claim.
2. Which artifact is the "pre-commit hook" cited at `check-comment-hygiene.sh:13-14`? This checkout is a worktree (`.git` = pointer to `/Users/…/Public/.git/worktrees/046-v4-gemini-research`); no hooksPath resolved in-budget.
3. Do the other three suspected bash/TS twin-pairs — FILE_EXISTS↔`validateFileExists` (:939), TEMPLATE_SOURCE↔`validateTemplateSource` (:941, its script = row 30), FRONTMATTER_VALID↔`validateFrontmatterBasics` (:943, its script = row 19) — carry the same documented parity duty? Their scripts are it.2's rows; their TS bodies unread.
4. Do the :425 dispatch loop and the :936-951 hard-pushed block double-run rows 1/15/19/30 (duplicated execution, possible double-entries), or does a skip-list exist at :391/:425 (unread)? The coherence test's exact `['ANCHORS_VALID','FILE_EXISTS']` (:99) proves the pairing is somehow deduped or guarded — mechanism unknown; the `validateFrontmatterBasics` name also suggests a smaller check than the registry's "structure and required semantic values" promise.
5. What consumes the registry `flags` key (does the help printer :47-61 print flags? its `node -e` block unread)? Determines whether F3-01's omission propagates past the row itself.
