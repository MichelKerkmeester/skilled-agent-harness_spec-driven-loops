# Research: Overengineering Stress-Test of system-spec-kit — Final Synthesis

Lineage: `glm-5-3-flash-overengineering` | session `fanout-glm-5-3-flash-overengineering-1788746122749-wk19z4` | generation 1 | 10/10 iterations | stopReason: **maxIterationsReached** (dispatch-mandated cap; convergence treated as telemetry per dispatch instructions)

Executor contract: in-process inline (this lineage is the executor; no nested dispatch — per loop-protocol containment rule). Continuity save via `generate-context.js`: **skipped by design** — the dispatch constrains every write to this lineage directory, and the continuity writer/validate.sh (especially `--recursive`) are named as out-of-surface commands; recorded here and in the convergence report as the loop's single documented deviation (the skill's save-phase success criterion, intentionally traded for write-surface discipline).

---

## Table of contents (17 sections)

1. Executive summary · 2. Scope, method & evidence boundary · 3. Angle 1 — The 39-rule validator: inventory, firing, duplication · 4. Angle 2: The six-runtime hook matrix · 5. Angle 3: Documentation volume · 6. Angle 4: The /speckit:* command surface · 7. Angle 5: Governance ownership (AGENTS.md/CLAUDE.md vs. rules/skill) · 8. Angle 6: Single-consumer abstractions · 9. Angle 7: Adherence evidence inside specs/ · 10. Cross-cutting census & corrections · 11. Recommendations (the ranked simplification plan) · 12. Eliminated alternatives · 13. Open questions & UNKNOWNs · 14. Citations & source index · 15. Findings register (30 findings + 9 plan-moves) · 16. Convergence report & loop telemetry · 17. Continuity & continuation

---

## 1. Executive summary

**Verdict:** system-spec-kit's overengineering is concentrated in its perimeter, not its validated core. The 39-rule validation gate, the honesty machinery (anchors, closure statements, ADR-backed waivers, continuity fingerprints), and the governance layering are evidenced working: 2,874 governed packets, a measured 62.2% freshness duty cycle, zero faked surfaces in the sampled packet, zero placeholder debt, and a governance/rules/skill layer separation so clean that 9 repo-rule files (1,615 LOC) carry exactly one spec-kit mention (F22, F27-F29).

**The 5 P1 findings** decompose into: 4 documentation/perimeter-shape defects (F1 vocabulary in 5+ sites, F11 3,582-LOC adapter ceremony, F14 the same vocabulary again at documentation level, F18 six promised dashboards of which three are mechanismless) and 1 adoption tail (F23: an optimizer whose promotion-reports directory is empty — 0 recorded promotions for 3 advisory-only tunables). None of the five requires touching the validated core.

**The ranked plan (§11)**: 8 moves, 6 trivial-to-low risk, 2 medium (both vocabulary-reshuffles absorbed by existing alias mechanisms), 1 deliberately deferred phase-2 (F11's host-native adapter port — the only move that changes runtime behavior). Four of the five P1s are paperwork; the one infrastructure P1 (F23) is resolved by freezing, not building. The core — the part worth copying — is kept by the plan's R9, which costs nothing.

**The merit of the exercise, honestly**: better-output gains are small (the gates were never the problem); the real yields are adherence (vocabulary drift dies mechanically — the corpus' own drift guard already proves the failure class, F2) and maintenance (5+ coordinated edits → 1; −8 report rows; −5 quarantined scripts). One 62% duty-cycle number (F27) tells the future freshness-plumbing how much of the corpus it will actually touch.

## 2. Scope, method & evidence boundary

- **Topic**: systems-audit of system-spec-kit (skill + command surface + root governance) for overengineering, judged only against: better output, better AI adherence in practice, lower maintenance. Absence of harm is not justification.
- **Evidence boundary** (per dispatch): `.opencode/skills/system-spec-kit/**`, `.opencode/commands/speckit/**`, root `AGENTS.md`, `CLAUDE.md`, `REPO RULES.md`, `repo-rules/*.md`, and the restraint ladder (`sk-code/shared/references/universal/code-quality-standards.md`). Everything else read along the way (e.g. the deep-loop skill's references, needed to execute the loop itself) is *process, not evidence* — the one exception, the 0.05-default finding (F17), is explicitly recorded as out-of-boundary evidence, kept for completeness, not counted in the in-boundary census.
- **Method**: 10 iterations, one focus each (§3-§10, §11); every finding = path:line + concrete cost + what it protects + P1/P2 + a labeled recommendation (remove | merge | fold into a repo rule | keep). Counts were measured, not estimated: 39 rule rows, 14/14 assets, 95 env-flags, 2,874 summaries, 1,790 zero-fingerprints — each from a counted grep/wc recorded in the iteration files.
- **Non-goals honored**: no edits, no recommendation drops a documented-validated capability (every cut names what remains), no prose-style commentary, no tooling executed (0 node/validate/git invocations — reads and counted greps only).
- **Write surface**: this lineage directory exclusively. All 38 files written here (10 iterations, 10 deltas, 6 state/registry/dashboard/strategy, research.md).

## 3. Angle 1 — The 39-rule validator: inventory, firing, duplication (KQ1, KQ2)

**Inventory (verified, not assumed):** `runtime/cli/lib/validator-registry.json` = 410 lines, **exactly 39 rule rows** — the dispatch prompt's "39" is correct. Severity: 32 error / 5 warn / 2 info; the registry's 4th severity (`skip`) is declared (`orchestrator.ts:106-111`) and threaded (`:419`, `:226`, `:293-300`) but exercised by **0 of 39 rows** (F4). 31 of 39 rows carry 1-4 legacy aliases (~75 alias strings; F9). **Firing**: 22 of 39 rows (56%) are conditioned on flow-specific surfaces by their own descriptions (save/write/merge/phase/improvement//strict/high-governance — F7): they execute only transiently; on a plain authored-packet validation the no-op rate of the registry's own vocabulary is 56%. **Duplication (the measured spine):** 10 of 39 rows (26%) collapse to 2 physical implementations — the 5 CANONICAL_SAVE_* rows → `rules/check-canonical-save.sh` (via the `SPECKIT_CANONICAL_SAVE_RULE` env set→run→unset round-trip, `orchestrator.ts:152-163`) and 5 rows → the `ts:spec-doc-structure` module (natively dispatched because "the registry dispatcher cannot spawn that form", `orchestrator.ts:955-958`) (F8). **The execution machinery is 3-layered:** TS-native entries (`orchestrator.ts:950-970`), the 56-line TS-embedded bash wrapper (`orchestrator.ts:119-174`, a `String.raw` heredoc), and the sourced `check-*.sh` scripts — 28 of them + 3 shared helpers under `runtime/cli/rules/` (F1, F5). The vocabulary error class is *demonstrated, not hypothetical*: `validate.sh:46-64`'s own help printer lists 2 of the registry's 3 categories, hiding the 6 structural rows — including both `.ts`-scripted rules, which are also 2 of the 3 strict-only rules (F2, F3). The freshness trio (CONTINUITY_FRESHNESS, GENERATED_METADATA_INTEGRITY, GENERATED_METADATA_DRIFT) runs only under `--strict` (F3) — mandated by the completion covenant, so the gate fires where it matters, but every non-strict invocation reports as if freshness had been checked. `LEVEL_DECLARED` and `AC_COVERAGE` are info-only, default-on: 2 report rows that cannot fail, whose value is visibility (F6).

## 4. Angle 2 — The six-runtime hook matrix (KQ3)

Counted: `runtime/hooks/` = claude 13 files/4,249 LOC (the reference, + its own degradation protocol: every registration wraps as `node <adapter> || { stderr marker; fallback JSON; }`, read by a "runtime-mirrors doctor asset"); codex 10/1,069; cursor 12/1,523; devin 12/1,608; pi 10/667; lib 8/4,030 ("the Gate-3 spec-gate policy core"); shared-provenance.ts 112; hooks/README.md 171. Total ≈ 13.4k LOC. Four of five adapters self-describe as "normalize payloads and delegate to the existing Claude hook implementations" (F12). The spread: the same contractual job (Gate-3 spec-gate + session-lifecycle) costs 4,249 LOC out-of-process (claude, + fallback + marker + doctor) vs 667 in-process (pi) — **3,582 LOC of ceremony whose reader is more machinery** (F11, P1). The `opencode/` adapter dir is **empty** (0/0) — the primary target runtime's integration lives elsewhere (F13). Registration/verification story: 5 schemas, 5 "verified-live" narratives, 1 behavioral contract (F12, P2 — flagged for a usage census; 3 of 5 registrations' in-checkout existence recorded UNKNOWN).

## 5. Angle 3 — Documentation volume (KQ4)

**83,372 md lines / 959 files** (non-node_modules), decomposed: runtime 30,634/473 (test-fixture-dominated), manual-testing-playbook 11,839/85, references 12,890/41, changelog 9,807/108, templates 5,767/33, feature-catalog 4,528/47, shared 1,761/13, benchmark 3,461/151, assets 1,179/4, config 74/1, + root trio (SKILL.md 551 + ARCHITECTURE.md 207 + README.md 674 = 1,432). The routing covenant (SKILL.md:95, ~250 words) selects ONLY references/ + assets/ — the changelog+benchmark+playbook = **25,107 lines = 30% of the corpus, selected by no RESOURCE_MAP intent** (F15). What the agent must read: SKILL.md (551) + the routed references — everything else is maintainer/test provenance. **The severity/strict vocabulary — one semantic — is owned by 5+ in-tree sites in 3 formats** (registry JSON; `orchestrator.ts:106-111,293-300,984-988`; `validate.sh:46-64`'s printer; `feature-catalog.md:514`; `validation-rules.md:36-48`; + the root governance doc) — the printer's 2-of-3 drift is the cost already cashed (F14, P1). `validation-rules.md` (729 lines) self-declares non-authority (README.md:645: "the 39-rule registry is authoritative") yet restates "Severity Levels" anyway — the fifth site (F16, P2). One numeric default (0.05) is written 8+ places — deliberate (the stop contract), cost = 8+ edits per retune (F17, P2 — out-of-boundary evidence, recorded, not counted). The routing covenant taxes every references/assets edit with 2 artifact regenerations + 1 check (`leaf-manifest.json`, `leaf-aliases.json`, "byte-stable under --check") — priced, deliberate, kept (F15, P2). The env-reference drift guard (`env-reference-drift.vitest.ts`) is the machinery that catches this class — it already caught one (F24).

## 6. Angle 4 — The /speckit:* command surface (KQ5)

6 commands = 590 LOC + README.txt 402 + **14 assets, 14/14 referenced** (no orphans — hypothesis corrected). The retired-memory covenant is honored AND enforced end-to-end: `save.md:23,25,90` (single writer, no indexing handoff), `complete-auto.yaml:1175` (forbidden-paths guard: "the runtime rejects them"), and the generator's source references only the metadata pair (`generate-context.ts:58,589`) — **the "dead steps post memory-decommission" hypothesis was wrong**; the residue is elsewhere (F19 — corrected, recorded). The residue: **`complete.md:92` promises 6 report dashboards; its own workflows (`speckit-complete-auto.yaml`, `…-confirm.yaml`) say "dashboard" 0 times, and 3 of the 6 promised names (phase-decomposition, planning-gate, closeout) have no producing mechanism anywhere** — the only other `planning-gate` occurrences are generated retrieval echoes (`trigger-index.json:49392`, `phrase-variants.json:33537`) (F18, P1). The `level_contract_optional_*.md` convention: 6+ occurrences, **0 file resolutions** (the one README that resolves it points at the real `templates/addons/resource-map.md.tmpl`) (F20, P2). The covenant's 3-step violation-recovery ritual guards a case its own covenant + runtime rejection prevent — kept, flagged: did it ever fire? (F19, P2; run-8 evidence: 0 faking, ritual speculative.)

## 7. Angle 5 — Root governance: what a rule or the skill could own (KQ6)

Measured, mostly negative (the honest result): `CLAUDE.md` is a **symlink** to the 506-line `AGENTS.md` — the dual-maintenance hypothesis dies (506 lines exist once, 0% duplication). `repo-rules/` = 9 files, 1,615 LOC, **exactly 1** spec-kit machinery mention (`scope-discipline.md`) — the rules own behavior; there is **nothing to fold** (F22). The ONE 2-layer restatement found: the freshness covenant (warn-not-block, ENFORCE-escalates, both flags default-false) stated at governance AND fully at `validation-rules.md:113-127` — agreed today, 2× coordinated edits forever (F21, P2 — kept; the 2-layer governance/implementation split doing its job). The completion covenant's three formulations (governance mandate; YAML scorecard `complete-auto.yaml:431,457` with 20-point items; validator mechanics) each state their own layer's truth — a scored VIEW, not duplication.

## 8. Angle 6 — Single-consumer abstractions (KQ7)

- **The optimizer/observability pipeline**: `runtime/cli/observability/` = 2,465 LOC + 5 optimizer scripts (`promote.cjs`, `replay-corpus.cjs`, `replay-runner.cjs`, `rubric.cjs`, `search.cjs`) + per-packet/per-lineage event ledgers (this packet: 52-line `observability-events.jsonl`; this lineage: 5-6 audit/effect/lock files) → output: **3 advisory-only tunable fields** (`convergenceThreshold` 0.01-0.2, `stuckThreshold`, `maxIterations`), promotionMode: advisory-only, and `optimizer/audit/promotion-reports/` = **0 files** — zero recorded promotions. The last plausible in-boundary consumer (the performance baselines) traced to the `.bench.ts` benches, NOT the replay (run 9). The promotion tail's production consumer (the advisor) sits outside the evidenceBoundary — recorded UNKNOWN, not 0 (F23, P1).
- **The readerless flag**: `SPECKIT_BM25_ENGINE` — "has no reader left in source" per the skill's own drift test (`env-reference-drift.vitest.ts:295`); 4+ golden-query fixtures exercise it; it is absent from `ENV-REFERENCE.md` (F24, P2).
- **"Retrieval" names two machineries**: the governance clause (prompt-time, lexical, "fusion unsupported") vs `shared/embeddings.ts` (1,011) + `algorithms/rrf-fusion.ts` (842) + 2 providers + 4 flags — the shared package's fleet payload, consumed out-of-boundary BY DESIGN (the 003-shared-package-utilization packet audits exactly this) (F25, P2; framing corrected run 9).
- 5 of the 95 SPECKIT_* flags have zero TS references (they live in `shared/` + ENV-REFERENCE + compiled mirrors) — the flag *documentation* surface, not dead code, except BM25 (F24).

## 9. Angle 7 — Adherence evidence inside specs/ (angle 7)

**The money number: 1,790 of 2,874 governed packets (62.2%) carry the all-zero "never recorded" continuity fingerprint** — the freshness covenant's governed surface is 62% no-op by its own rule's definition (`validation-rules.md:113`: "The zero fingerprint placeholder is treated as never recorded"); 1,084 packets (37.8%) have real fingerprints — the covenant's exercised, validated population (F27, P2). **The covenant's home track**: 5 packets (001-005), all Level 2, identical 20/3 unticked tasks, all-zero fingerprints, 10-doc layouts — 0/5 executed; the authoring pass IS the recorded work (`005/acceptance-criteria.md:22-23`: "Authored the acceptance criteria…"/"Meet the open criteria as the lane runs") — the lane whose criteria THIS research feeds (F28, P2; the 50:0 authoring-to-execution ratio = the base-rate caveat for any "does the covenant hold" claim). **The positive control**: 005 shows the machinery working — 0 placeholder residue, closure "**Closeable:** No… Written when the packet is closed, not before" (:83-85), waivers fail validation without a named ADR (:72-74), the zero-fingerprint honestly disclosed (F29, P2 — the strongest+ evidence that the design improves AI adherence in practice). Corrections: criterion-mention counts 3/6 were noisy (legend words); the clean datum = 5 AC rows + Closeable:No. `goal.md` IS templated (`spec-kit-docs.json:21,158-159`) — the phantom hypothesis dead. 2 of 6 sampled real fingerprints collide (`sha256:56e8ccee` ×2) — resolved run 9 (F30).

## 10. Cross-cutting census & corrections (run 9)

- **The freshness covenant's discriminating power**: 2 of 6 sampled real fingerprints identical across packets differing by 84 lines (`028/002` vs `028/004`, one `sha256:56e8ccee`) — and the hashed input is named in NO stating layer (checker comment `continuity-freshness.ts:60-62` pins the stamped *document*, not the hashed *content*; the governance clause says "matches recomputed content") — 3 layers, 0 definitions (F30, P2; 1-clause fix).
- **The playbook**: 85 scenarios / 11,839 lines; sampled n=1: 0 references to the vitest suites they shadow (`exhausted-approach-respect.md`: 138 lines, 0 test references) — a parallel proof convention, provenance-untraceable by readers (F31, P2; 84 scenarios UNKNOWN).
- The four quality/evaluation apparatuses (vitest tests/, benchmark/ 151 files, playbook 85, optimizer-replay): co-exist; the playbook's provenance lines (R3) make the relationship decidable; the others' adoption recorded in the keep-list.

## 11. Recommendations — the ranked simplification plan (KQ8)

*Verdict first: trim the perimeter, keep the core, prove nothing. 4/5 P1s are perimeter-shape; the infrastructure P1 (F23) is resolved by freezing. F11 (the 3,582-LOC adapter ceremony) is the plan's phase-2 — the only behavior-changing move, gated on R1-R8 proving the appetite. (Full detail with per-move citations: `iterations/iteration-010.md`.)*

| # | Move (source) | Capability preserved | Files touched | Risk | Gain: output / adherence / maintenance |
|---|---|---|---|---|---|
| R1 | One vocabulary, one authority (F14+F1,F2,F9,F16,F30) | all 39 rules, narrowing, warn-vs-ENFORCE, freshness | 6 (registry; orchestrator:106-111,293-300,984-988; validate.sh:46-64; validation-rules.md:36-48→ptr; feature-catalog.md:514→ptr; continuity-freshness.ts:60-62 +1 clause) | **LOW** — display/pointer only | ~0 / drift class dies, mechanically caught / 5+→1 edits |
| R2 | Collapse the two 5:1 multiplexes (F8+F1) | the 10 behaviors as subcodes; narrowing via the existing 31-alias mechanism | registry (10→2+subrules), check-canonical-save.sh, spec-doc-structure, orchestrator.ts:152-163 (round-trip dies) | **MED** — narrowing vocabulary; aliases absorb; 8 fewer report rows | −8 rows (legibility) / silent wrong-attribution dies / −8 descriptions |
| R3 | Playbook provenance lines (F31) | unchanged (additive) | 85×1 line ("shadows: test#case") | **TRIVIAL** | ~0 / proofs traceable; duplication-vs-shadowing decidable / — |
| R4 | Make complete.md:92 true (F18) | the report's ACTUAL shape becomes documented | 1 (shrink the promise; the 6-dashboard build is the unproven-demand variant) | **TRIVIAL** | promise=product / no 3-dashboard hunt / the 6-name list dies |
| R5 | Resolve the naming conventions (F20+F13) | unchanged — pointers resolve | ~4 (confirm.yaml:244-247, auto.yaml:272, resource-map/README.md:64 → real .tmpl paths; the empty opencode/ dir) | **TRIVIAL-LOW** | — / greps+checkers hit something / decoding lore dies |
| R6 | Freeze the optimizer's promotion tail (F23) | telemetry+manifest+drift-guard+benches; the 3 tunables + the DESIGN | 0 touched, 5 quarantined (promote/replay-corpus/replay-runner/rubric/search + the empty promotion-reports/); DELETE only after an out-of-boundary import census | **LOW** frozen, **MED** deleted (UNKNOWN external imports) | — / the covenant gains a truthful mileage note / −5 scripts' drift |
| R7 | Retire the readerless flag (F24) | none lost — the drift test pronounces it dead | golden-queries.json:27,189-191 (−4), the drift-test clause (−1) | **TRIVIAL** | no ghost fixtures / −1 truth / −1 golden slot |
| R8 | +1 orientation pointer (F25) | unchanged | the governance clause (§5) +1 line → ENV-REFERENCE.md | **TRIVIAL** | — / the 3-grep orientation cost dies / — |
| — | **The keep-list** (F21,F22,F15,F27-F29) | THE validated core: 1-file governance+symlink (0× duplication, measured); 9 rule files/1 machinery mention (layering clean); the honesty machinery (62% duty cycle, 0 faked, ADR-backed waivers); the F15 covenant; the F21 2-layer freshness; the drift guard | 0 | — | the measured non-bloat is the part worth copying |

**Phase-2 (deferred, gated)**: F11's host-native adapter port — capability = 4 delegating adapters' contracts; files = 5 hook trees + registrations; risk = MEDIUM-HIGH (5 runtimes' live behavior); gain = −3,582 LOC of ceremony, −1 doctor-asset dependency. Gated on R1-R8's adoption proving the appetite — the only move that changes runtime behavior.

## 12. Eliminated alternatives

| Approach | Reason eliminated | Evidence | Iteration(s) |
|---|---|---|---|
| Read all 39 rule bodies in one pass | read-budget discipline; rule bodies belong to the duplication pass, where only guard surfaces matter | iterations/001-002 | 1 |
| "AGENTS.md/CLAUDE.md are dual-maintained duplicates" | FALSE — CLAUDE.md is a symlink (`ls -la`: lrwxr-xr-x); 506 lines exist once | iterations/006 §ruled-out | 6 |
| Fold governance procedure into repo-rules | no — 9 rule files, 1,615 LOC, exactly 1 spec-kit machinery mention; the rules own behavior only | iterations/006 §ruled-out | 6 |
| "The governance layer re-documents the validate.sh invocation contract" | partially — 2 layers, 2 duties (WHEN+pitfalls vs WHERE/WHAT), agreed at measurement time; residual = the F21 2× only | iterations/006 | 6 |
| "Command surface carries dead steps post memory-decommission" | FALSE — the covenant is enforced end-to-end (generator emits the metadata pair only; the YAML carries the forbidden-paths guard); the residue is F18/F20, not dead memory steps | iterations/005 §corrections | 5 |
| "goal.md is an unproduced convention artifact" | FALSE — templated (`spec-kit-docs.json:21,158-159` → `goal.md.tmpl` v2.2) | iterations/008 §corrections | 5, 8 |
| "005's criteria = 3 Met / 6 Open" (word-mention counts) | superseded — legend/protocol words polluted the count; clean datum = 5 AC rows + "Closeable: No" | iterations/008 §corrections | 8 |
| "The replay/optimizer machinery is wholly unused" | PARTLY — promotion-reports/ = 0 files (the promotion tail IS unused-on-record), but the perf baselines trace to the `.bench.ts` benches, and out-of-boundary (advisor) consumption is UNKNOWN, not 0 | iterations/007, 009 | 7, 9 |
| "shared/ embeddings+RRF machinery is orphaned" | reframed — it is the shared package's fleet payload; out-of-boundary consumption is the design (003's packet audits exactly that); in-boundary consumers = benches/tests | iterations/009 §corrections | 7, 9 |

## 13. Open questions & UNKNOWNs

1. **What exactly does the freshness fingerprint hash?** Named in no stating layer (checker :60-62, governance clause, generator) — 2/6 sampled fingerprints collide across 84-line-different packets. 1-clause fix (R1). (F30)
2. **Do the other 84 playbook scenarios cite their shadowed suites?** Sampled n=1 (0 citations). UNKNOWN. (F31 → R3 answers it)
3. **Do the 3 of 5 hook registrations (cursor/codex/devin) exist in this checkout, and are they exercised?** The registrations live outside the evidenceBoundary. UNKNOWN. (F12 → the usage census)
4. **Who consumes the optimizer's replay+promote out-of-boundary (the advisor)?** Recorded UNKNOWN by the evidenceBoundary; the import-census (R6's delete precondition) answers it. (F23)
5. **F11's appetite**: does the perimeter-trim momentum (R1-R8) justify the 3,582-LOC ceremony port? Gated, by design. (Phase-2)

## 14. Citations & source index

Primary (the 39-rule gate): `runtime/cli/lib/validator-registry.json` (410 L, 39 rows); `runtime/lib/validation/orchestrator.ts` (1,106 L; :106-111, :119-174, :152-163, :293-300, :417-423, :439, :950-970, :955-958, :984-988); `runtime/cli/spec/validate.sh` (v3.0.0; :26-29, :46-64, :52, :300-301); `runtime/cli/rules/` (28 check-*.sh + 3 helpers + README = 32). Hooks: `runtime/hooks/{claude,codex,cursor,devin,pi,lib}` (4,249/1,069/1,523/1,608/667/4,030) + `shared-provenance.ts` (112) + `hooks/README.md` (171). Docs: `SKILL.md` (551; :95, :181, :192, :547), `README.md` (674; :645), `ARCHITECTURE.md` (207), `feature-catalog/feature-catalog.md` (792; :514), `references/validation/validation-rules.md` (729; :36-48, :65-74, :113, :122, :126-127, :137), `references/structure/folder-structure.md` (:38, :169), `runtime/ENV-REFERENCE.md`. Commands: `complete.md` (100; :2, :92), `save.md` (92; :19, :23, :25, :36, :46, :48, :61, :68, :90), `complete-auto.yaml` (:272, :431, :457, :882, :1043, :1170-1183), `complete-confirm.yaml` (:244-247). Freshness: `runtime/cli/validation/continuity-freshness.ts` (608 L; :41-42, :60-62, :73-74, :130, :148). Continuity: `runtime/cli/continuity/generate-context.ts` (:58, :589). Observability/optimizer: `runtime/cli/observability/` (2,465: 878+445+357+223), `runtime/cli/optimizer/` (6+1, `audit/promotion-reports/` = 0 files, manifest lastUpdated 2026-04-10, tunable: convergenceThreshold 0.01-0.2). Shared machinery: `shared/embeddings.ts` (1,011) + `providers/{openai,voyage}.ts`, `shared/algorithms/rrf-fusion.ts` (842) + `adaptive-fusion.ts` + `dist/` mirrors. Tests/guards: `runtime/tests/env-reference-drift.vitest.ts:295`, `runtime/tests/fixtures/golden-queries.json:27,189-191`, `runtime/tests/local-llm-features/performance/{cold-start,throughput,embedding-latency}.bench.ts`, `runtime/tests/thin-continuity-record.vitest.ts:194`. Governance: root `AGENTS.md` (506; ×6 validate.sh) = `CLAUDE.md` (symlink), `repo-rules/` (9 files, 1,615 LOC, 1 machinery mention). Corpus: 2,874 implementation-summaries; 1,790 zero-fingerprints; the 035 track = 5 packets, 0/5 executed; `005/acceptance-criteria.md` (:22-23, :25-26, :31, :65-74, :83-85), `005/spec.md:31` (`<!-- SPECKIT_LEVEL: 2 -->`), twin summaries (84-line diff, shared `sha256:56e8ccee`). Out-of-boundary (recorded, not counted): deep-loop references, trigger-index/phrase-variants (generated echoes), golden-queries.json.

## 15. Findings register

30 evidence findings (5 P1: F1, F11, F14, F18, F23; 25 P2) + 9 plan-moves (R1-R9) — full statements, costs, what-each-protects, severities, and labeled recommendations: `findings-registry.json` (replayed from `deltas/iter-00{1..10}.jsonl`; every finding_observed event carries path:line, cost, protection, severity, recommendation). Sum: 39 finding-class records. Reconciliation note: runs 6-8's findings were recorded in their iteration .mds at execution and backfilled into their deltas by the final reducer replay — the replay now yields exactly 30+9, matching this section. P1→move mapping: F1→R1+R2, F11→phase-2, F14→R1, F18→R4, F23→R6.

## 16. Convergence report & loop telemetry

- **Stop reason**: `maxIterationsReached` — the dispatch-mandated cap (10/10). Per the dispatch contract, convergence before the cap was telemetry-only; no early synthesis.
- **Total iterations**: 10 (9× status `complete` + 1× `thought`); 0 timeouts, 0 errors, 0 stuck; stuck count 0 throughout.
- **Questions answered**: 8/8 (KQ1-8; two with recorded, bounded caveats — the 84-scenario UNKNOWN (F31), the 3-registration-existence UNKNOWN (F12) — carried to §13, not suppressed).
- **newInfoRatio**: 0.90, 0.70, 0.80, 0.70, 0.60, 0.50, 0.50, 0.60, 0.40, 0.30 (mean 0.60; rolling-9 0.622; negative-knowledge honored — 8 ruled-out/corrected directions persisted in deltas + registry).
- **Convergence math**: convergenceThreshold=3 (dispatch-supplied; above the 0..1 novelty scale → novelty-nominated STOP structurally unreachable, recorded as telemetry); compositeStop 0.0 at every run (weighted signals: MAD-noise 0.35, question-entropy 0.35, rolling 0.30 — all recorded per-iteration in the deltas); stuckThreshold=3 never approached (stuck count 0). Quality guards (source diversity, focus alignment, weak-source dominance): pass — 60+ distinct in-boundary sources cited, 1 focus/iteration enforced, no single-source finding.
- **Note (the one process deviation, documented)**: `config.status` remains `initialized` — SKILL.md NEVER-6 (config read-only after init) vs loop-protocol §6.5 (set status: complete) conflict resolved in favor of the read-only covenant; completion is recorded HERE and in the terminal synthesis event, not by editing the config.

## 17. Continuity & continuation

- **State of this lineage**: complete. 10/10 iterations, synthesis (this file), terminal event with `stopReason: "maxIterationsReached"`. Registry: 39 finding-class records; dashboard: 10-row table; strategy: final.
- **Recovery surface**: `deep-research-state.jsonl` (12 lines: 1 config + 10 iterations + 1 synthesis_complete) — append-only; the deltas/ directory carries per-iteration deltas (the reducer's owned form); iterations/001-010 are write-once.
- **Continuation**: the ranked plan (§11) is the packet's决策-ready input for `005-overengineering-simplification`'s criteria ("Meet the open criteria as the lane runs" — acceptance-criteria.md:23); the 5 criteria + Closeable:No posture (F29) are the acceptance gate this research feeds. The F31/F30-class UNKNOWNs are the next run's cheapest first reads.
- **Continuity save**: intentionally NOT executed here (write-surface covenant; documented at this file's head). The packet-level continuity writer, when the operator next saves, will stamp the fingerprint this covenant expects — the 62% duty-cycle number (F27) tells it exactly what it is joining.
