# Iteration 001 — Angle 1: manifest `documents` semantics vs what create.sh writes

- sessionId: fanout-glm-5-3-flash-templates-1788737392077-m8w16s
- Window opened: 2026-09-06T23:52:18Z (init) · iteration executed 2026-09-06T23:52Z–2026-09-07T00:08Z
- Focus: Q1 — for each document, what do the manifest's owner/creationTrigger/absenceBehavior fields say, and does that match what create.sh writes per level and flag?
- Status: complete · newInfoRatio: 1.0
- Novelty justification (1 sentence): first iteration against zero accumulated findings — 3 findings and 2 ruled-out directions, all fully new.
- Executor: this process, inline (detached fan-out; no nested dispatch) · tool calls: 10/12 (8 read-only evidence + narrative + state/delta/projection; research actions 8, within the 3-5 target given the (a) ground-truth inventory, (b) wiring-disproof, (c) consumer-mapping needs — overshoot documented, not silent).
- Quality guards: source diversity 14 distinct files across templates/rules/validator/CLI; no single-weak-source (every finding cites ≥3 records); focus alignment 100% (all evidence serves Q1).

## Focus

Q1 per strategy §3. Role discipline: the manifest (`templates/spec-kit-docs.json`), `runtime/cli/spec/create.sh`, and the validator rules are GROUND TRUTH; prose (README/SKILL/references, and here `templates/EXTENSION-GUIDE.md`, `templates/CONTRACT.md`) is CLAIMS tested against them. Where two ground truths disagree, the disagreement itself is a finding and the enforcement side is the acting truth.

## Actions Taken (all read-only; [SOURCE] = evidence examined)

1. `templates/CONTRACT.md:1-105` — manifest contract: ownership scope (`:51-53`), render flow (`:55-73`), entrypoints (`:77-84`), acceptance-criteria claim "`Provides the closure-gating acceptance criteria document for Levels 2, 3 and 3+`" (`:41`), validation commands (`:87-96`). [SOURCE: templates/CONTRACT.md:41,51-53,77-84]
2. `templates/spec-kit-docs.json:1-60` — `manifestVersion: 1.0.0` (`:3`); `versions` 17×"v2.2" (`:4-22`); `privateTaxonomy` kinds/capabilities/presets (`:23-59`). [SOURCE: templates/spec-kit-docs.json:3,4-22]
3. `templates/spec-kit-docs.json:61-164` — the `documents` section: 17 entries × {template, owner, creationTrigger, absenceBehavior}. Verified pairings: spec/plan/tasks/implementation-summary = author+scaffold+hard-error (`:62-84`); decision-record = author+explicit-option+silent-skip (`:86-90`); phase-parent.spec.md = author+phase-scaffold+hard-error (`:92-96`); resource-map.md = author+explicit-option+**warn** (`:98-102`); context-index.md = author+explicit-option+**warn** (`:104-108`); handover.md = **command**+memory-save+silent-skip (`:110-114`); debug-delegation.md = **agent**+debug-dispatch+silent-skip (`:116-120`); research/research.md = **workflow**+deep-research+silent-skip (`:122-126`); before-after/timeline/roadmap = author+explicit-option+silent-skip (`:128-144`); review/review-report.md = workflow+deep-review+hard-error (`:146-150`); acceptance-criteria.md = author+scaffold+**warn** (`:152-156`); goal.md = author+explicit-option+silent-skip (`:158-162`). Each entry also carries `template` (`:99,105` etc.). [SOURCE: templates/spec-kit-docs.json:61-164]
4. `templates/spec-kit-docs.json:165-545` — levels.1 contract: `requiredCoreDocs` = spec.md, plan.md, tasks.md (`:170-174`); `lifecycleRequiredDocs.afterImplementationStarts` = implementation-summary.md (`:175-179`); `requiredAddonDocs: []` (`:180`); `optionalAddonDocs: []` (`:181`); `lazyAddonDocs` = **8** docs — handover.md, debug-delegation.md, research/research.md, before-after.md, timeline.md, roadmap.md, decision-record.md, goal.md (`:182-191`); `sectionGates.goal.md` exists at L1 (`:521`); NO mention of acceptance-criteria.md / resource-map.md / context-index.md anywhere in the L1 block (`:165-545`, targeted grep: only the two goal.md hits at `:190,521`). [SOURCE: templates/spec-kit-docs.json:170-191,521]
5. `templates/spec-kit-docs.json:546-2467` — key-structure scan of levels 2/3/3+/phase/review/research: `requiredAddonDocs: []` in ALL of 2 (`:560`), 3 (`:1054`), 3+ (`:1608`), phase (`:2170`), review (`:2311`), research (`:2428`) — 7 of 7 counting L1 (`:180`); acceptance-criteria.md inside `optionalAddonDocs` at 2 (`:562`), 3 (`:1056`), 3+ (`:1610`) plus `sectionGates."acceptance-criteria.md"` at 2 (`:998`), 3 (`:1552`), 3+ (`:2121`); NO acceptance-criteria.md mention in the phase (2164-2303) / review (2304-2420) / research (2421-2467) blocks. [SOURCE: templates/spec-kit-docs.json:560,562,998,1054,1056,1552,1608,1610,2121,2170,2311,2428]
6. `runtime/cli/spec/create.sh:26-160,273-326` — `DOC_LEVEL=1` default (`:33`); `--level` 1/2/3/3+ and `--level phase` (`:83-102`); `WITH_LAZY_ADDONS=false` (`:53`); `--with-lazy-addons` sets it true (`:153-155`); help: "`--with-lazy-addons  Add before-after.md, timeline.md, roadmap.md, and decision-record.md`" + "`(off by default; all are valid at every level)`" (`:282-283`). [SOURCE: runtime/cli/spec/create.sh:33,53,83-102,153-155,282-283]
7. `runtime/cli/spec/create.sh:380-470` — `requested_lazy_addon_docs` (`:389-402`): `requested = ['before-after.md','timeline.md','roadmap.md','decision-record.md']` HARDCODED (`:393`); only validates those 4 ⊆ `contract.lazyAddonDocs` (`:394-398`, exit 3). `scaffold_contract_docs` (`:421-437`): required docs (`:423-425`) + `lifecycleRequiredDocs.afterImplementationStarts` (`:427`) + acceptance-criteria.md iff the WHOLE level contract greps as containing the string `"acceptance-criteria.md"` (`:428-433`, comment: "`The closure gate needs this document at every level whose contract lists it...`" `:428-430`) + the 4 lazy docs iff `$WITH_LAZY_ADDONS` (`:434-436`). [SOURCE: runtime/cli/spec/create.sh:389-402,421-437]
8. Wiring proof: `runtime/cli/lib/template-utils.sh` — `copy_template` (`:67-111`) renders EVERY copied template through the inline gate renderer at the target level (`:88-100`) after resolving the file via `_manifest_template_path` (`:88`); `_manifest_template_path` (`:201-226`) maps name→file by NAMING CONVENTION `${template_name}.tmpl` (`:209-213`, phase+spec.md→phase-parent.spec.md.tmpl at `:207-208`) then a role-folder priority scan core→addons→packet-types→root (`:215-224`) — it NEVER reads `documents[]`; `resolve_level_contract` (`:244-260`) prefers the TS resolver `runtime/lib/templates/level-contract-resolver.ts` and falls back to raw JSON of `levels[level]` (`:254-260`); `level_contract_docs_from_json` (`:295-313`) unions `requiredCoreDocs + requiredAddonDocs` ONLY (`:300`). Consumers of the levels-arrays elsewhere: `runtime/lib/validation/orchestrator.ts:473` (`requiredCoreDocs+requiredAddonDocs`), `:507` (`optionalAddonDocs+lazyAddonDocs`); `runtime/lib/validation/spec-doc-structure.ts:217-223` (requiredAddonDocs+lazyAddonDocs into its own `documents` records). Negative evidence: `documents`/`absenceBehavior`/`creationTrigger` have ZERO hits in runtime/lib+runtime/cli except `runtime/cli/doctor.sh:6` (a prose comment); the vocabulary `hard-error|silent-skip` appears nowhere in code; `resource-map|context-index` in runtime/ hits only discovery lists (`runtime/handlers/spec-doc-discovery.ts:143`, `runtime/README.md:154`) and the deep-loop's OWN resource-map YAML (`.opencode/commands/deep/assets/deep-research-confirm.yaml:187,1356-1371`; `deep-review-auto.yaml:161,267-275`) — a different, research-coverage artifact sharing the filename with `templates/addons/resource-map.md.tmpl`; `goal.md` has 0 mentions in SKILL.md (only `references/workflows/goal-set-string-playbook.md:27,29,74,76,91,93` assumes it exists) and 0 in create.sh. [SOURCE: runtime/cli/lib/template-utils.sh:67-111,201-226,244-260,295-313; runtime/lib/validation/orchestrator.ts:473,507; runtime/lib/validation/spec-doc-structure.ts:217-223; runtime/cli/doctor.sh:6; templates/EXTENSION-GUIDE.md:28-39]

## Findings

### f-iter001-001 — P1 — fix (or, minimally, document)

`.opencode/skills/system-spec-kit/templates/spec-kit-docs.json:61-164` — CLAIM: every one of the 17 documents declares implementation semantics: `template` + `owner` + `creationTrigger` (scaffold / phase-scaffold / explicit-option / command→memory-save / agent→debug-dispatch / workflow→deep-research / workflow→deep-review) + `absenceBehavior` (hard-error / warn / silent-skip) — e.g. acceptance-criteria.md "author, scaffold, warn" (`:152-156`), resource-map.md "author, explicit-option, warn" (`:98-102`), research/research.md "workflow, deep-research, silent-skip" (`:122-126`). WHAT THE IMPLEMENTATION DOES: none of it. (a) Doc→file resolution ignores `documents[]` entirely — `_manifest_template_path` builds `${name}.tmpl` by convention (`runtime/cli/lib/template-utils.sh:209-213`) and scans role folders (`:215-224`); (b) the scaffolder's doc set comes from the LEVELS arrays + the flag + the AC-grep (`runtime/cli/spec/create.sh:421-437`, `runtime/cli/lib/template-utils.sh:300`); (c) the validator's required/optional/lazy computations read the SAME levels arrays (`runtime/lib/validation/orchestrator.ts:473,507`; `runtime/lib/validation/spec-doc-structure.ts:217-223`); (d) `hard-error`/`warn`/`silent-skip` exist in runtime code only as a comment (`runtime/cli/doctor.sh:6`); (e) the ONLY place the fields are defined is prose: `templates/EXTENSION-GUIDE.md:28-34` instructs extension authors that `creationTrigger` is "the workflow that creates it" and `absenceBehavior` is one of hard-error/warn/silent-skip — neither is consulted by anything. SEVERITY: **P1 (wrong-or-unused)** — a maintainer editing these 51+17 values changes nothing; the section silently contradicts the enforcement surface (e.g. `absenceBehavior: warn` for resource-map.md/context-index.md/acceptance-criteria.md vs no warn-path; `creationTrigger: scaffold` for acceptance-criteria.md which the scaffolder writes only at L2/3/3+ per the grep at `create.sh:431-433`). RECOMMENDATION: **fix** — wire `creationTrigger`/`absenceBehavior` into `create.sh` + the validator (or drop them); else **document** the section as "descriptive index; behavior = levels.N arrays + naming convention".
[SOURCE: templates/spec-kit-docs.json:61-164; runtime/cli/lib/template-utils.sh:201-226,295-313; runtime/cli/spec/create.sh:421-437,431-433; runtime/lib/validation/orchestrator.ts:473,507; runtime/lib/validation/spec-doc-structure.ts:217-223; templates/EXTENSION-GUIDE.md:28-34; runtime/cli/doctor.sh:6]

### f-iter001-002 — P2 — document (or fix by splitting the field)

`templates/spec-kit-docs.json:182-190` vs `runtime/cli/spec/create.sh:282-283,393` — CLAIM: level-1 `lazyAddonDocs` lists 8 docs (handover.md, debug-delegation.md, research/research.md, before-after.md, timeline.md, roadmap.md, decision-record.md, goal.md) under a field whose --with-lazy-addons consumer scaffolds exactly 4. WHAT THE IMPLEMENTATION DOES: `requested` is hardcoded to the 4 (`create.sh:393`), matching the help text (`:282-283`); the other 4 are owned by other mechanisms (handover=command/memory-save `manifest:110-114`; debug-delegation=agent/debug-dispatch `:116-120`; research/research.md=workflow/deep-research `:122-126`; goal.md=explicit-option `:158-162` — yet goal.md has NO scaffold path: 0 mentions in create.sh, while the L1 contract still defines `sectionGates.goal.md` (`:521`) that can only fire for a goal.md produced outside the scaffolder). TheSubset check (`create.sh:394-398`) validates only the 4. `templates/EXTENSION-GUIDE.md:39` defines the field as "`command-owned or explicit-option files`" — so the 8-list is PROSE-consistent, but no code distinguishes the two ownership halves sharing one field. SEVERITY: **P2 (cosmetic/taxonomic)** — no behavior divergence for the 4; the drift is that the contract's field name promises scaffold-lazy semantics while half its entries are lifecycle-created. RECOMMENDATION: **document** (one line: only the flag-4 are scaffold-written; the lifecycle-4 arrive via their owning workflows) — or **fix** by splitting into flagLazyAddonDocs / lifecycleAddonDocs.
[SOURCE: templates/spec-kit-docs.json:182-190,110-126,158-162,521; runtime/cli/spec/create.sh:282-283,393-398; templates/EXTENSION-GUIDE.md:39]

### f-iter001-003 — P2 — fix

`templates/spec-kit-docs.json:180,560,1054,1608,2170,2311,2428` + `runtime/cli/spec/create.sh:428-433` — CLAIM: CONTRACT.md:41 says addons/acceptance-criteria.md.tmpl "Provides the closure-gating acceptance criteria document for Levels 2, 3 and 3+". WHAT THE IMPLEMENTATION DOES: `requiredAddonDocs` is `[]` in ALL SEVEN level contracts (`:180,560,1054,1608,2170,2311,2428`), so the requirement never comes from the typed field; instead acceptance-criteria.md sits in `optionalAddonDocs` at L2/3/3+ (`:562,1056,1610`) with sectionGates (`:998,1552,2121`), and the scaffolder decides via a substring grep of the ENTIRE level contract (`create.sh:431-433`: `grep -q '"acceptance-criteria\.md"'`) — which hits at L2/3/3+ and misses at L1 (`:165-545` has no such string). Meanwhile the validator still unions the perpetually-empty `requiredAddonDocs` (`runtime/lib/validation/orchestrator.ts:473`; `runtime/cli/lib/template-utils.sh:300`). CONSEQUENCES: (a) the enforcement story ("which levels get AC scaffolded") rests on a fragile whole-JSON substring — any future stray mention of the doc name anywhere inside a level contract (a new sectionGates entry, a note) silently flips the scaffold decision; (b) the typed field that EXISTS for exactly this is dead weight; (c) CONTRACT.md:41's claim is enforced by grep+rule, not by the contract's required-arrays. SEVERITY: **P2 (cosmetic today, structural hazard)**. RECOMMENDATION: **fix** — move acceptance-criteria.md into `requiredAddonDocs` at L2/3/3+ and retire the grep (this also makes `orchestrator.ts:473`'s union non-vacuous); or document the grep as the intended mechanism.
[SOURCE: templates/spec-kit-docs.json:180,560,1054,1608,2170,2311,2428,562,1056,1610,998,1552,2121; runtime/cli/spec/create.sh:428-433; runtime/lib/validation/orchestrator.ts:473; runtime/cli/lib/template-utils.sh:300; templates/CONTRACT.md:41]

## Questions Answered

- **Q1 (angle 1) — ANSWERED**: the manifest's per-document owner/creationTrigger/absenceBehavior semantics are NOT what create.sh implements — they are implemented by NOBODY. The acting truth: (1) scaffold set = `levels.N.requiredCoreDocs` + `lifecycleRequiredDocs.afterImplementationStarts` + (acceptance-criteria.md iff the level contract's JSON contains that substring) + (the 4 hardcoded lazy docs iff `--with-lazy-addons`) [`create.sh:421-437`, `template-utils.sh:300`]; (2) doc→file = naming convention + core→addons→packet-types folder scan [`template-utils.sh:201-226`]; (3) phase/review/research records never carry acceptance-criteria.md (their contracts omit the string; children inherit their OWN level's contract, so AC-ness tracks the child's level, not the parent's). Of the 17 creationTrigger values, the 4 flag-docs (`explicit-option` via `--with-lazy-addons`), the 4 core+summary (scaffold), and phase-parent.spec.md (phase-scaffold) MATCH behavior; handover/debug-delegation/research/review-report (command/agent/workflow) are scaffold-foreign by design; goal.md's "explicit-option" has NO implementing surface in create.sh (carried → CQ1); resource-map.md/context-index.md "explicit-option" likewise lack any creating command in the evidence searched so far (carried → CQ2).

## Questions Remaining

- Q2-Q8 (queued; next: Q2 — README/SKILL core-vs-addon labels vs manifest+create.sh).
- CQ1 (carried): what surface creates goal.md — the promised "explicit-option" mechanism? (0 hits in create.sh; playbook:27-93 ASSUMES goal.md exists; candidate: /speckit:plan or EXTENSION-GUIDE flow → adjudicate in angles 3/6.) [SOURCE: references/workflows/goal-set-string-playbook.md:27,29,74,76,91,93; runtime/cli/spec/create.sh (0 goal.md hits)]
- CQ2 (carried): who produces the promised absenceBehavior=warn for acceptance-criteria.md / resource-map.md / context-index.md — is it the metadata pipeline's discovery (runtime/README.md:154; spec-doc-discovery.ts:143) or generated-metadata-integrity checks? (→ angles 4/5.)

## Ruled Out (do not retry)

- ro-iter001-001: "documents[x].template wires the renderer/copy" — ruled out; `_manifest_template_path` (template-utils.sh:201-226) uses the `${name}.tmpl` convention + role-folder priority scan, never `documents[]`. [SOURCE: runtime/cli/lib/template-utils.sh:201-226]
- ro-iter001-002: "acceptance-criteria.md is a required addon at L2+ (CONTRACT.md:41)" — ruled out at the contract level; `requiredAddonDocs:[]` at `spec-kit-docs.json:560,1054,1608`; the requirement is manufactured by the scaffolder grep (`create.sh:431-433`) and — pending angle 4 — by `check-ac-closure.sh`, not by the contract's required-arrays. [SOURCE: templates/spec-kit-docs.json:560,1054,1608; templates/CONTRACT.md:41; runtime/cli/spec/create.sh:428-433]

## SCOPE VIOLATIONS

None. Writes this iteration: `iterations/iteration-001.md`, `deltas/iter-001.jsonl`, `deltas/event-001.json` (gateway input), the gateway's own ledger+projection writes (`deep-research-audit-ledger/`, `deep-research-effect-ledger/`, `locks-and-fencing-v1/`, `deep-research-state.jsonl`) — all inside the lineage directory; reducer-owned files updated inside the lineage. No packet-level research/ writes, no repo tooling runs (no generate-context.js / validate.sh / git writes).

## Next Focus

Iteration 2 — Angle 2: how README.md (repo root) and SKILL.md label resource-map.md, timeline.md, roadmap.md, goal.md, before-after.md, decision-record.md (core vs addon) vs the manifest (addons/ = 10 tmpls; core/ = 4; packet-types/ = 3) and create.sh:421-437; note EXTENSION-GUIDE.md:28-39 graduates from definition-source to CLAIM-source there.

## Convergence telemetry (advisory only — stopPolicy=max-iterations)

newInfoRatio = 1.0 (3 findings, 2 ruled-out, 0 prior) → NOT qualifying (bar 0.05); consecutiveQualifying 0/3. Loop continues regardless: 9 iterations remain, forced.
