# Iteration 004 — Angle 4: acceptance-criteria.md — which rules read it, what each enforces, and Met-row reality

- sessionId: fanout-glm-5-3-flash-templates-1788737392077-m8w16s
- Window opened: 2026-09-06T23:52:18Z (init) · iteration executed 2026-09-07T01:00Z–01:20Z
- Focus: Q4 — which rules read acceptance-criteria.md (AC_CLOSURE, AC_COVERAGE), what does each enforce, and do Level 2/3 packets under specs/ carry Met rows with evidence or leave it a scaffold? (CQ2 partial: the "warns" claim)
- Status: complete · newInfoRatio: 0.308 (4 new findings / 17 accumulated)
- Novelty justification (1 sentence): all four findings are first-recorded — the README evidence-tracing misattribution, the stale warn-vs-fail gate wording, the advisory/enforce duality, and the live self-referential AC gate — none restate angles 1-3.
- Executor: this process, inline · tool calls: ~20 read-only evidence bash + writes — OVER the 12 budget this iteration; the validate.sh wiring hunt (validate.sh implements no rules itself; the registry JSON was the right first probe) consumed surplus calls. Overshoot documented, not silent; next iterations timebox wiring hunts to one registry probe first.
- Quality guards: source diversity 12 distinct files (two rule scripts, registry JSON, orchestrator, README, folder-structure, spec-kit-docs.json, six packet AC files, check-completion.sh); every finding ≥3 records; focus alignment 100%.

## Focus

Q4 per strategy §3. State read first: state log 4 records, registry 13 findings, dashboard 3/10 iterations.

## Actions Taken

1. `runtime/cli/rules/check-ac-closure.sh` read end-to-end: rule AC_CLOSURE, severity error (`:9-11`); opt-out flag fail-safe (`:28-41`); rollout cutoff default 2026-08-30 via SPECKIT_AC_CLOSURE_CUTOFF (`:43-57`); Created read from spec.md metadata (`:59-76`); completion claim read from the Status CELL of spec.md/implementation-summary.md (`:78-101`); table parsed by HEADER NAME (AC-ID/Status/Waiver columns), malformed rows reported never skipped (`:104-167`); ADRs declared as headings/bold items/table rows in decision-record.md, fenced blocks skipped (`:169-187`); gate inactive below Level 2 (`:219-224`); post-cutoff absent file → RULE_STATUS fail + remediation "Scaffold it from templates/addons/acceptance-criteria.md.tmpl" (`:247-257`); Waived/Superseded must cite ADR-NNN declared in decision-record.md else dangling-fail (`:287-305`); unmet rows fail ONLY when the packet claims completion (`:350-358`), else info "in progress" (`:360-363`); empty table = info no-op (`:323-328`); malformed/duplicate ids = fail (`:315-335`). [SOURCE: runtime/cli/rules/check-ac-closure.sh:9-11,28-57,59-101,104-167,169-187,219-224,247-257,287-305,315-335,350-363]
2. `runtime/cli/rules/check-ac-coverage.sh` read end-to-end: rule AC_COVERAGE — enable flag falsey-only-off (`:13-24`); floor default 0.9 clamped (SPECKIT_AC_COVERAGE_FLOOR) (`:30-48`); lifecycle gate: L2+ AND implementation-summary.md present AND (acceptance-criteria.md OR legacy tasks.md traceability) AND summary status cell ∈ {in-progress…delivered} (`:61-93`); "The merged tasks document is the only home for verification traceability" applies ONLY to packets WITHOUT acceptance-criteria.md (`:95-104`); "acceptance-criteria.md is the canonical home. spec.md is only consulted for packets predating the acceptance-criteria rollout" (`:102-104` area, exact at `:163-164` in _ac_count_total); canonical analyzer parses AC-ID/Verification/Status columns by header name, counts covered = Waived/Superseded OR evidence containing file:line, else malformed (`:195-247`); run_check: canonical=true path takes rows as total from the SAME parse (`:292-298,342-344`); BELOW floor the rule still returns RULE_STATUS pass — message "AC_COVERAGE advisory (under floor)" (`:368-377`); RULE_NAME is never set to anything but pass except via those messages (no fail branch in the whole script). [SOURCE: runtime/cli/rules/check-ac-coverage.sh:13-24,30-48,61-93,95-104,163-164,195-247,292-298,342-344,368-377]
3. Validator registry (`runtime/cli/lib/validator-registry.json`): AC_COVERAGE = `{severity: "info", category: "authored_template", flags: [SPECKIT_AC_COVERAGE, SPECKIT_AC_COVERAGE_ENFORCE, SPECKIT_AC_COVERAGE_FLOOR], description: "Default-on advisory (non-b[locking]...)"}`; AC_CLOSURE = `{severity: "error", category: "authored_template", flags: [SPECKIT_AC_CLOSURE, SPECKIT_AC_CLOSURE_CUTOFF]}`; FILE_EXISTS = check-files.sh's RULE_NAME (`runtime/cli/rules/check-files.sh:24`); "check-completion" appears NOWHERE in the registry (`includes("check-completion") === false`). validate.sh implements no rules itself — it delegates to the orchestrator/registry (`runtime/cli/spec/validate.sh:8-10`). check-completion.sh lives OUTSIDE rules/ at `runtime/cli/spec/check-completion.sh:48` ("Completion Verification Rule: Verify Checklist Completion"). [SOURCE: runtime/cli/lib/validator-registry.json (AC_COVERAGE/AC_CLOSURE entries); runtime/cli/rules/check-files.sh:24; runtime/cli/spec/validate.sh:8-10; runtime/cli/spec/check-completion.sh:48]
4. Reality sample — packet family 035 (all Level 2, Created 2026-09-06 = post-cutoff): all five children HAVE acceptance-criteria.md; 004's table: 4 rows AC-001..004, all Status **Unmet**, Waiver `-`, Verification cells name evidence SOURCES (not yet observed) (`004/acceptance-criteria.md:53-58`); 001/002/003/005: 5 rows each, met:0 unmet:4. Repo-wide census: 144 acceptance-criteria.md files under specs/; Met rows exist in older packets (e.g. mcp-tooling/019-official-obsidian-cli:12, agents/008-orchestrate-external-cli-delegation:11) but spot-checked Met rows cite backticked paths WITHOUT :line → under the canonical analyzer they count as malformed-evidence (advisory detail), not covered. [SOURCE: specs/system-speckit/035-spec-kit-simplification-research/004-template-system-and-acceptance-criteria/acceptance-criteria.md:53-58; 001/002/003/005 acceptance-criteria.md row counts; specs/mcp-tooling/019-official-obsidian-cli/acceptance-criteria.md; specs/agents/008-orchestrate-external-cli-delegation/acceptance-criteria.md]
5. CQ2 partial adjudication: folder-structure.md:125,141 "`acceptance-criteria.md - the closure gate; validation warns when it is absent`" and README:154/165 the same "warns" family — vs check-ac-closure.sh:253-256: post-cutoff absence is RULE_STATUS **fail** (severity error per registry). The "warn" wording is stale against the acting gate. (The only warn left in the AC pipeline is AC_COVERAGE's advisory under-floor message.) [SOURCE: references/structure/folder-structure.md:125,141; README.md:154,165; runtime/cli/rules/check-ac-closure.sh:247-257; runtime/cli/lib/validator-registry.json]

## Findings

### f-iter004-001 — P1 — fix

`README.md:198` vs `runtime/cli/rules/check-ac-coverage.sh:163-164,195-247,292-298` — CLAIM: "`the AC_COVERAGE rule requires each criterion's evidence to trace back into tasks.md`". FACT: in the canonical path (any packet WITH acceptance-criteria.md — mandatory post-cutoff at L2+ per AC_CLOSURE), AC_COVERAGE reads evidence from acceptance-criteria.md's OWN Verification column, parsed by the same analyzer that produces the denominator (`:292-298` "One parse... the two can never disagree"); tasks.md traceability rows are the LEGACY path for packets predating the AC rollout (`:95-104`, `:163-164`). The README describes the dead path as the rule. SEVERITY: **P1** — authors following it write evidence into tasks.md where the canonical gate never looks. RECOMMENDATION: **fix** — "each criterion's evidence lives in acceptance-criteria.md's Verification cell (file:line); tasks.md rows are the pre-rollout fallback".
[SOURCE: README.md:198; runtime/cli/rules/check-ac-coverage.sh:95-104,163-164,195-247,292-298]

### f-iter004-002 — P1 — fix

`README.md:154,165` + `references/structure/folder-structure.md:125,141` vs `runtime/cli/rules/check-ac-closure.sh:247-257` + `runtime/cli/lib/validator-registry.json` (AC_CLOSURE severity error) — CLAIM: "a missing acceptance-criteria.md warns" (README:154), "acceptance-criteria.md # Criteria that gate packet closure (Level 2+)" with "optional, warns if absent" (README:149-151/165), "validation warns when it is absent" (folder-structure:125,141). FACT: for packets created after the 2026-08-30 cutoff at L2/3/3+, absence is a hard FAIL (error severity, remediation "Scaffold it from templates/addons/acceptance-criteria.md.tmpl"); "optional" is true only pre-cutoff (grandfathered advisory). Adjudicates CQ2 for the AC leg: NO warn is produced anywhere for a missing post-cutoff AC file; the promised per-document absenceBehavior=warn (manifest:152-156) remains producerless (f-iter001-001). SEVERITY: **P1** — the docs describe a gate one full severity level weaker than the shipped one. RECOMMENDATION: **fix** both files to "required at L2+ for packets created after 2026-08-30; missing = validation error".
[SOURCE: README.md:149-151,154,165; references/structure/folder-structure.md:125,141; runtime/cli/rules/check-ac-closure.sh:247-257; runtime/cli/lib/validator-registry.json; templates/spec-kit-docs.json:152-156]

### f-iter004-003 — P2 — document

`runtime/cli/rules/check-ac-coverage.sh:368-377` + `runtime/cli/lib/validator-registry.json` (severity info; SPECKIT_AC_COVERAGE_ENFORCE flag) vs `README.md:198` ("requires") — FACT: AC_COVERAGE is advisory by default end-to-end (RULE_STATUS stays pass even under floor; the registry entry is severity info with a separate ENFORCE flag and the description "Default-on advisory (non-blocking)"), while README:198's "requires" and the general "gate" framing imply blocking. The enforce path exists as a flag but is not documented in the audited prose. SEVERITY: **P2** — semantic gap between "advisory with floor" and "requires". RECOMMENDATION: **document** — state the advisory default, the 0.9 floor, and the SPECKIT_AC_COVERAGE_ENFORCE escalation in README's gate paragraph.
[SOURCE: runtime/cli/rules/check-ac-coverage.sh:13-24,368-377; runtime/cli/lib/validator-registry.json; README.md:198]

### f-iter004-004 — P2 — document (self-referential gate observation)

`specs/system-speckit/035-spec-kit-simplification-research/004-template-system-and-acceptance-criteria/acceptance-criteria.md:53-58` vs this lineage's outputs — FACT: packet 004's AC-001 makes THIS lineage the acceptance evidence ("Given the lane launched with stop policy max-iterations, When it exits, Then ten iteration files and ten state events exist | count of files under `research/lineages/glm-5-3-flash-templates/iterations` and of iteration events in the JSONL ledger"), and AC-003 checks the focus-line coverage of these very iteration files. The audited surface and the audit are entangled: when the packet closes, its AC evidence will be read from the artifacts this run produces (ten iteration files; the state log + effect ledger). Also the live Met-row reality for the 035 family: all rows Unmet, in-progress, consistent with AC_CLOSURE's "unmet blocks only on completion claim" design; repo-wide, Met rows exist (8+ packets) and spot-checks show Met rows citing paths WITHOUT :line — which the canonical analyzer scores as malformed evidence (advisory detail), not covered. SEVERITY: **P2** — no defect; a structural observation maintainers need when reading packet 004's closure state (and an evidence-quality note: Met-without-file:line is common in the wild and only ever advisory). RECOMMENDATION: **document** — in template-guide/AC docs, note that Met rows should cite file:line to satisfy AC_COVERAGE's analyzer, and record that packet 004's closure reads this lineage.
[SOURCE: specs/system-speckit/035-spec-kit-simplification-research/004-template-system-and-acceptance-criteria/acceptance-criteria.md:53-58; specs/mcp-tooling/019-official-obsidian-cli/acceptance-criteria.md; specs/agents/008-orchestrate-external-cli-delegation/acceptance-criteria.md; runtime/cli/rules/check-ac-coverage.sh:195-247]

## Questions Answered

- **Q4 (angle 4) — ANSWERED**: acceptance-criteria.md is read by exactly two registry rules: AC_CLOSURE (error; post-cutoff L2+ presence gate + Met/Waived/Superseded tally + ADR-backed waiver check + completion-claim blocking; header-name table parser; malformed rows never skipped) and AC_COVERAGE (info; advisory floor-0.9 evidence-coverage on the file's own Verification column once the implementation lifecycle starts; tasks.md only as pre-rollout fallback; SPECKIT_AC_COVERAGE_ENFORCE to escalate). Level 2/3 packets under specs/: the 035 family (all post-cutoff L2) carries AUTHORED tables, all rows Unmet with source-naming (not yet observed) Verification cells — neither bare scaffolds nor Met-backed; repo-wide, 144 AC files exist and Met rows are common but rarely file:line-cited, which the analyzer treats as malformed-advisory, not covered.
- **CQ2 (AC leg) — ADJUDICATED**: the "warns when absent" claim (README:154/165, folder-structure:125/141) is stale — post-cutoff absence is an error-severity FAIL; no warn path exists for the AC document. Remaining CQ2 residue (resource-map/context-index warn) stays carried to angle 5.

## Questions Remaining

- Q5-Q8 (queued; next: Q5 — checklist.md vs acceptance-criteria.md vs tasks.md verification checklist; which surface the validator registry and the completion exposer read).
- CQ1 (carried): goal.md creation surface (angle 6).
- CQ2 (residue, carried): who — if anyone — warns for missing resource-map.md/context-index.md (angle 5 via metadata/orchestrator scan).
- CQ6 (NEW, carried): where is SPECKIT_AC_COVERAGE_ENFORCE actually consumed (orchestrator severity escalation?), and is it documented anywhere? (angle 5.)

## Ruled Out (do not retry)

- ro-iter004-001: "check-completion.sh is a validator-registry rule" — ruled out: the registry does not mention it; it is the separate Completion Verification Rule at runtime/cli/spec/check-completion.sh:48. [SOURCE: runtime/cli/lib/validator-registry.json; runtime/cli/spec/check-completion.sh:48]
- ro-iter004-002: "AC_COVERAGE can fail a packet" — ruled out: no fail branch exists in the script; under-floor returns pass + advisory message; escalation is only via the registry ENFORCE flag. [SOURCE: runtime/cli/rules/check-ac-coverage.sh:368-377; runtime/cli/lib/validator-registry.json]

## SCOPE VIOLATIONS

None. Writes: `iterations/iteration-004.md`, `deltas/iter-004.jsonl`, `deltas/event-004.json` + gateway ledger/projection writes — all inside the lineage directory. No packet-level writes, no repo tooling.

## Next Focus

Iteration 5 — Angle 5: the three completion-gate surfaces — checklist.md (discovery-canonical, manifest-absent per f-iter003-005), acceptance-criteria.md (angle 4), tasks.md verification checklist (check-completion.sh's input). Determine: which surface the validator registry reads (none of the three has a required-file rule? check FILE_EXISTS scope), which one check-completion.sh + the completion exposer read, and CQ2's residue (any warn for resource-map/context-index). Probe order: registry dump (one call), check-completion.sh head (one call), orchestrator's checklist/COMPLETION handling (one call) — timeboxed.

## Convergence telemetry (advisory only — stopPolicy=max-iterations)

newInfoRatio = 0.308 → NOT qualifying; consecutiveQualifying 0/3. Loop continues: 6 iterations remain, forced.
