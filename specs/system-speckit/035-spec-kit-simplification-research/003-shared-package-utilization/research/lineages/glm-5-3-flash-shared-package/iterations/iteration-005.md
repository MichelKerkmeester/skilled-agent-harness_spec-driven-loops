# Iteration 5: parsing/ + frontmatter/ + predicates/ + unicode-normalization + trigger-extractor — the production backbone and its shim strata

## Focus

The parsing tier (2,739 lines): `parse-frontmatter` (the package's most-imported module), the four memory-quality gates (`memory-sufficiency`, `memory-template-contract`, `secret-scrubber`, `spec-doc-health`) — do they still terminate in the live save flow after the memory-DB retirement? — plus the self-test-only orphans (`quality-extractors`, `boolean-expr`), the versioned-fold contract (`unicode-normalization`, 73 lines) and its isolation doctrine, and `trigger-extractor`'s (703 lines) production termination through the registered shim.

## Findings

| # | path:line | Claimed vs actual | Severity | Recommendation |
|---|-----------|-------------------|----------|----------------|
| F5.1 | `shared/frontmatter/parse-frontmatter.ts` (143) | Claimed: the shared YAML frontmatter parser. Actual: **the package's workhorse — 20+ importer files**, more than any other module: 4 shared-internal dependents (`parsing/memory-sufficiency.ts:5`, `memory-template-contract.ts:5`, `spec-doc-health.ts:14`, `quality-extractors.ts:5`), the retrieval corpus (`retrieval/lib/frontmatter.mjs:18`, `grep-convention.mjs:33`, `rules/check-grep-convention-helper.mjs:16`), the save/quality tier (`core/workflow.ts:63`, `core/quality-gates.ts` chain, `core/frontmatter-editor.ts:9`, `core/find-predecessor-memory.ts:7`, `core/title-builder.ts:9`, `core/validate-memory-quality→:19`, `extractors/spec-folder-extractor.ts:13`, `utils/spec-affinity.ts:14`, `lib/parsing/content-normalizer.ts:11`, `lib/validation/orchestrator.ts:10`, `lib/semantic-summarizer.ts:15`), 3 skill-advisor production imports (`skill-graph/doc-frontmatter.ts:12`, `utils/skill-markdown.ts:12`, + the embedders' frontmatter path), 2 sk-doc imports, and the continuity backfill/migrate CLIs. Twenty specifier forms, production-terminated at every tier. | P2 (verified positive) | Document. |
| F5.2 | `shared/parsing/memory-sufficiency.ts` (414) + `memory-template-contract.ts` (365) + `secret-scrubber.ts` (244) + `spec-doc-health.ts` (372) = 1,395 lines | Claimed (names + module headers): memory-era quality gates. Actual: **live, via exactly one production chain** — `continuity/generate-context.ts` → `core/workflow.ts:59-63` (`evaluateMemorySufficiency`, `validateMemoryTemplateContract`, `evaluateSpecDocHealth`, `scrubSecretsDetailed`) → these four modules;plus the evidence-type readers (`core/memory-metadata.ts:15` `MemoryEvidenceSnapshot`, `core/quality-scorer.ts:21`, `core/quality-gates.ts:10`). `generate-context.ts` is the continuity writer itself (the `/speckit:save` metadata regenerator) — the gates run on the live save path today. The "memory" in every name = the retired-DB era's vocabulary, not retired functionality; the sole direct-save-adjacent test trio (`memory-sufficiency.vitest`, `memory-template-contract.vitest`, `task-enrichment.vitest`) sits in cli/tests. | P2 | Document: these are the *save-quality gates*; the memory-DB reference in their names/headers is residue, not debt. |
| F5.3 | `shared/parsing/quality-extractors.ts` (48) | Claimed: part of the quality-gate parsing tier. Actual: **zero importers anywhere** — not even the barrel (index.ts has no quality-extractors line); its only referencers are its own test plus two *comments* in the embeddings tests praising its "standalone-assertion convention" (`embeddings/registry.test.ts:6`, `profile.test.ts:5-6`). 48 lines + a test, kept alive by its own elegance. | **P1** | Remove: the module and its test (the convention it exemplifies is documented in the comments that cite it — the citation survives). |
| F5.4 | `shared/predicates/boolean-expr.ts` (377) | Claimed (README tier): the shared predicate-evaluation abstraction. Actual: barrel-only (2 re-exports at `index.ts`, F1.3) + its own test; no production or test consumer evaluates it. Notably its would-benatural consumer does NOT use it: `memory-sufficiency.ts` (the actual evaluator of evidence predicates) imports only `parse-frontmatter` — the predicate engine solves a problem nothing currently pipes through it. | **P1** | Remove: module + test + the 2 barrel lines; if a consumer materializes it resurrects from this packet. |
| F5.5 | `shared/unicode-normalization.ts` (73) + the isolation doctrine | Claimed: one canonical fold. Actual: three mechanisms for one 73-line module: (a) direct specifier — 6 importers (hooks: `compact-inject.ts:30`, `hook-state.ts:21`, `shared-provenance.ts:18`; the runtime re-export shim `cli/lib/unicode-normalization.ts:10`; the shared-internal gate-3 (`:17`, the only consumer of `canonicalFold`); the shared-payload fingerprint (`lib/context/shared-payload.ts:13`)); (b) the skill-advisor's **local duplicate** — `mcp-server/lib/shared/unicode-normalization.ts`, header: "Duplicated from system-spec-kit/shared/unicode-normalization.ts **for full skill isolation**. Drift is watched by .github/workflows/isolation-check.yml… Do NOT add new imports from system-spec-kit here. Duplicate locally instead"; (c) the same advisor ALSO direct-imports the shared package 10× (embeddings ×7, rrf-fusion ×2, ipc) — so the isolation doctrine is enforced for a 73-line pure module while the 35 KB stateful embeddings stack (with its @huggingface/transformers dependency and dtype/model surface) crosses the boundary freely. The doctrine's CI (isolation-check.yml) even lives outside all five audited trees. | **P1** | Fix: pick one doctrine — either the advisor duplicates nothing (delete its 73-line copy + the CI audit) or it isolates from the *dependencies* (the embeddings); the current midpoint doubles the file, the CI, and the exception list. |
| F5.6 | `shared/trigger-extractor.ts` (703, "TF-IDF + semantic triggers v11") + `runtime/cli/scripts-registry.json:344-348` | Claimed: the trigger-phrase extraction engine. Actual: **live, at the end of three hops**: the runtime's scripts-registry registers an entry whose own description is "Re-exports from ../../shared/trigger-extractor" (`"path": "runtime/cli/dist/lib/trigger-extractor.js"`) — a registered pass-through shim — consumed by the save-flow extractors (`cli/extractors/session-extractor.ts`, `cli/core/topic-extractor.ts`) alongside the second wrapper (`cli/lib/semantic-signal-extractor.ts:27-28`, which adds the semantic-signals enrichment), plus the golden/regression tests (`semantic-signal-golden.vitest.ts`, `memory-pipeline-regressions.vitest.ts:5`). The shared module's 5 specifier-importers = 2 wrappers + 3 tests. The v11 engine itself = the package's largest single live *compute* module; nothing bypasses the wrappers. | P2 | Merge: register or import the shared module directly and retire the registered pass-through (the registry even documents its own remark); the two consumers keep their enrichment, the third hop dies. |
| F5.7 | `runtime/cli/lib/validate-memory-quality.ts` (1,194) vs `runtime/cli/continuity/validate-memory-quality.ts` (66) | Claimed: n/a (runtime-internal, recorded for the shared-tier verdict it feeds). Actual: the same module name at two paths; the 1,194-line lib/ variant is the real gate (imported by `core/quality-gates.ts:12` as `ValidationDispositionResult`'s home and by `extractors/quality-scorer.ts`), the 66-line continuity/ variant's importer set is comparatively traceless (the -l census cannot separate them without reading). Shared-visibility note: `core/quality-gates.ts:7-10` re-imports the shared `memory-sufficiency` trio *and* the runtime's 1,194-line validator — two validation layers, one `_memory`-era name, 3 files. | P2 | Fix: distinguish the two same-named modules (or merge); a reader auditing utilization should not have to diff 1,194 vs 66 lines to learn which validation actually runs. |

**Ruled out this iteration:** "the parsing tier is memory-DB residue" — the 1,395-line gate stack terminates in the live continuity writer (F5.2); the residue is *naming* and one justification comment, not functionality. Also ruled out: "the embeddings tests consume quality-extractors/boolean-expr" — comment--only references (F5.3).

## Sources Consulted

- Sizes: the 9-file parsing-tier wc (2,739 total); `parse-frontmatter` 20+ importer census (file-level, per-tree); the 4th internal dependent located (`quality-extractors.ts:5`)
- The gates' termination: `generate-context.ts`→`core/workflow.ts:59-63` import block; `workflow.ts` referencer census (8: generate-context, core/index, spec-root-registry, heal-session-ambiguity.sh, 4 tests); `memory-metadata.ts:15`, `quality-scorer.ts:21`, `quality-gates.ts:7-12`
- Orphans: `quality-extractors`/`boolean-expr` full-reference census (self-test + comment-only); `embeddings/registry.test.ts:6`, `profile.test.ts:5-6`
- `unicode-normalization.ts:9,45-61` + its 8-reference census; the advisor's local duplicate (full head: isolation doctrine + `CANONICAL_FOLD_VERSION` + the frozen tables); `isolation-check.yml` reference
- `trigger-extractor` referencer census + `scripts-registry.json:344-348` (the self-described registered shim) + `session-extractor.ts`/`topic-extractor.ts` production imports
- `diff -q` + wc on the two `validate-memory-quality.ts` (1,194 vs 66)

## Assessment

- newInfoRatio: 0.8 — the gates-are-live verdict (F5.2) and the parse-frontpillar census (F5.1) are new; the isolation-doctrine inconsistency (F5.5) and the registered-shim3-hop (F5.6) are new; the orphan sharpenings (F5.3/F5.4) confirm F1.9 with added mechanism.
- Novelty justification: F5.5's three-mechanism doctrine contradiction and F5.6's registry-as-shim are first-recorded; F5.2 converts "memory-era residue?" into "live gates, retired names" — the charter's central question for this tier.
- Confidence: high for F5.1-F5.4, F5.6 (file-level import evidence + the registry's own admission); high for F5.5's doctrine contradiction (the advisor's own header states it); medium for F5.7's importer attribution (the -l census cannot separate same-named modules — flagged).

## Reflection

- Worked: reading the shim headers instead of assuming shimness — the advisor's isolation doctrine (and its CI) was invisible to import-census; the scripts-registry.json self-description settled F5.6's third-hop question.
- Failed: the wc|grep plumbing typo (grep -t) cost one call; the same-named validate-memory-quality pair resists census-level attribution.
- Ruled out: see above.

## Recommended Next Focus

Iteration 6: ipc/ + workspace/ + compact-merger already done + `chunking.ts` + `lib/structure-aware-chunker.ts` + `gate-3-classifier` — no: the remaining UNCOVERED surfaces are `ipc/socket-server.ts` (2 imports, both the advisor shim — the daemon's IPC; who speaks the protocol?), `workspace/repo-root.mjs` (the 9× require, the 4th root-resolver; its 9 callers' invocation contexts — build/sync/CI vs runtime), `chunking.ts` (semanticChunk: imported by embeddings.ts (dead) + hf-local:13 (LIVE!) + the barrel — so it lives through the PROVIDER, not the monolith!) + `lib/structure-aware-chunker.ts` (3 importers: continuity/ast-parser.ts + tests×2 — the markdown-block splitter under the metadata integrity), and the env-var/README-§5 cross-check (the declared Configuration section vs the code's actual env reads — the EMBEDDING_DIM/SPECKIT_CASCADE_PROBE_TIMEOUT_MS/SPECKIT_ROLLOUT_PERCENT/OLLAMA_REQUEST_TIMEOUT_MS question), plus who consumes each of config.ts/paths.ts's exported derivations.
