# Iteration 9: The second-order consolidation — strong-live vs weak-live vs dead, and the memory-era residue ledger

## Focus

Every shared export that *is* production-imported: does its importer's importer terminate in a runner (advisor handler, registered hook, the continuity writer, the plugin, the uncalled CLI)? Plus the charter's memory-era residue question, answered as a *ledger*: each retired-DB convention (`MEMORY_DB_PATH`, `vec_metadata`, `vec_memories_rowids`, the vectors-shard, `runtime/database`, `RESERVED_*`, `SPECKIT_ROLLOUT_PERCENT`, the memory-naming, `.db-updated`) — who references it, is the reference live, and what the residue actually consists of.

## Findings

| # | path:line | Claimed vs actual | Severity | Recommendation |
|---|-----------|-------------------|----------|----------------|
| F9.1 | `shared/config.ts:43-51` + `runtime/lib/graph/access-telemetry.ts:51` + `shared/gate-3-classifier.ts:509` (+ `runtime/core/config.ts:97,103,110`) | Claimed: `DB_UPDATED_FILE` — "Defines database updated file" — a freshness sentinel. Actual: a **phantom sentinel**. Four derivation sites (shared/config:49,51; runtime/core:97 via its own; paths' assertions; the launcher's override consumes the *var*, not the file); exactly **two** verified readers — access-telemetry.ts:51 and gate-3:509 — and both use it as **`path.dirname()` only**: nobody reads the sentinel's content, mtime, or existence. No writer of `.db-updated` was found either (the retired-DB wrote it; the graph store writes `access-telemetry.json` *beside* it, mkdir-ing on demand — access-telemetry.ts:69). Meanwhile `runtime/database/` — the derivation's target — **does not exist** in this checkout (`ls`: no such directory; `runtime/data/` — the *trigger-index* — is the real data dir), and `runtime/core/config.ts`'s three DB-exports have their 4th derivation (cli/config/index.ts, the barrel) with no verified sentinel-read either. Fifty-two lines of shared/config.ts = a ceremonious way to spell one directory, consumed twice as a dirname. | **P1** | Merge: `config.ts` → one exported *directory* (the 3 modules join it); either retire the `.db-updated` costume or give it a writer and a reader; reconcile `runtime/data` (real) vs `runtime/database` (phantom) in one data-topology note. |
| F9.2 | the memory-era residue ledger (the charter's question) | Claimed: the package "predates the memory database's retirement and may retain residue from that era". Actual: **yes — concentrated, not crusty**: the residue = six concentrations, each named: (a) the factory's persisted-ollama precedence + the vectors-shard machinery + `vec_memories_rowids` — inert against the only production store (F2.4); (b) `embeddings.ts` + its barrel + the runtime shim — the retired in-process-inference stack (F2.1), whose *dep* (`@huggingface/transformers`, 0 importers, F7.1) its replacement (the bin model-server) no longer needs shared to declare; (c) `SPECKIT_ROLLOUT_PERCENT` (adaptive-fusion.ts:104) — the migration rollout flag, read only by the dead quartet (double residue); (d) `runtime/database` + `DEFAULT_DB_DIR` + `.db-updated` — the retired-DB's theater (F9.1); (e) the *naming* tier — `memory-sufficiency`, `memory-template-contract`, `MemoryEvidenceSnapshot`, the twin `validate-memory-quality`, `_memory.continuity` — live function under retired-DB vocabulary (F5.2/F5.7); (f) `MEMORY_DB_PATH`/`SPEC_KIT_DB_DIR\|SPECKIT_DB_DIR` — live, repurposed, under documented 2-spelling (F2.3/F6.6). Meanwhile the LIVE core (~60% of the package's 33.5k lines) is current: the advisor's embeddings chain, the parsing/save-gates, the retrieval helpers, gate-3, the compact-merge budget. | **P1** | Remove: the six concentrations are the backlog's hard core (see the ledger rows); the *naming* residue (e) is document-only; (a)-(d) are the deletions; (f) is the env-aliasing fix. |
| F9.3 | the second-order live-chain consolidation (24 production-imported surfaces) | Claimed: the five trees consume the package. Actual: **21-22 strong-live / 2-3 weak-live / 7-8 dead** (of 33): *STRONG (importer terminates in a runner)* — parse-frontmatter (save+retrieval+advisor+hooks), the 4-gate stack (the continuity writer, the framework's own documented memory-save), unicode-normalization (the compact hooks + gate-3 + the shared-payload envelope), trigger-extractor (session/topic extractors → the save flow), the embeddings chain (factory → providers; the advisor's `ensureActiveEmbedder` at advisor-server.ts:295 + `createEmbeddingsProvider` at skill-graph-db.ts:1348), ipc/socket-server (the daemon's transport), gate-3-classifier (the pi plugin + 4 hook adapters + the `SYSTEM_SPEC_GATE_ENFORCE` flag), paths/config (runtime-core + telemetry + gate-3 — the dirname-anchors), rrf-fusion (the advisor's 5-lane scorer), compact-merger + budget-allocator (the PreCompact adapters — 4-runtime registration source-complete: codex/compact-inject, cursor/precompact, the doctor's runtime-mirror inventory, the parity tests), the validation/content-privacy trio, the spec-flow utils (spec-affinity, folder-detector, nested-changelog, content-filter, tree-thinning), sk-doc's frontmatter pair, the advisor's skill-markdown/doc-frontmatter. *WEAK (importer = an uncalled CLI or equally-unproven)* — `scoring/folder-scoring` (rank-memories: isMainModule, 0 invocation references, F3.7); `context-types` (frontmatter-migration: live *code*, whose own invocation = the backfill/migrate CLIs' operational schedule — the weakest strong). *DEAD* — the F3.1 quartet, the F2.1 corpse, the F4.1 contracts, the F5.3/F5.4 orphans, the F6.3 island (8 surfaces, ≈2,300+ lines + 7-9 keeper suites). | **P1** | Document (the ledger); the strong/weak/dead split = the backlog's safety criterion — nothing in the *strong* set is a removal candidate; everything in *dead* already has its evidence. |
| F9.4 | the data-topology (5 homes) | Claimed (README §5 "Where embeddings are stored", :339-341): "shared/ owns no store… the skill advisor… writes them into its own package-local `mcp-server/database/skill-graph.sqlite`". Actual: true *for embeddings* — but the package's surfaces touch **five** persistence homes: `runtime/data/` (the trigger-index — the *live* retrieval), `runtime/database/` (the phantom — 4 derivations, 0 existence, F9.1), the advisor's `mcp-server/database/skill-graph.sqlite` (the vector+graph store), `/tmp/system-hf-embed` (the model-server) and `/tmp/system-skill-advisor` (the daemon-IPC socket) — plus the spec-tree itself as the 6th (the *authored* store). Three derivation systems (F2.9/F6.2/F9.1) govern where these are; the README documents exactly one. | P2 | Document: one data-topology section (5 homes × who owns × who derives) — the "Where embeddings are stored" section is 1/5 of the truth and the only 1/5 that needed no explaining. |
| F9.5 | the compact-merge chain's final hop | Claimed (F3.5): the compact-merge machinery is "strongly live" via the registered PreCompact hooks. Actual: source-side registration-complete — the 4 runtime adapters (claude/compact-inject, codex/compact-inject, cursor/precompact, pi via its own), the doctor's runtime-mirror inventory (`commands/doctor/assets/doctor-runtime-mirrors.yaml`), and the settings-driven-invocation-parity + directive-lifecycle tests. The final hop — the *operator's* installed settings (the agent-side hook config) — lies outside the five audited trees and is the one link this packet has not (and, within its write-surface, cannot) verify. Recorded as: strong-live modulo the operator's settings, with the parity tests as the mechanism. | P2 | Document (the caveat); the packet's claim = "live through the registration seam; the operator's settings = the one unverifiable hop from here." |

**Ruled out this iteration:** "the memory-DB retired *the whole era's machinery*" — the convention-cores survived under new owners (vec_metadata = advisor-written; MEMORY_DB_PATH = launcher-set); what died = the *precedence-branch, the shard layout, the sentinel, the in-process dep, the rollout flag* (F9.2's a-d). Also ruled out: "shared's residue is diffuse" — it concentrates in six loci; the live 60% is current.

## Sources Consulted

- `.db-updated`: the 3-4 derivation sites (config.ts:43-51; runtime/core:97-110; the paths-assertions), the 2 verified readers (access-telemetry.ts:51, gate-3:509 — both dirname), the 0-writer/0-content-read census, `access-telemetry.ts:69` (the on-demand mkdir), the `runtime/database` absence (`ls`), the `runtime/data` reality (the trigger-index)
- The second-order ledger: the 24 production-imported surfaces × their runners (advisor-server.ts:295, skill-graph-db.ts:1348, the 4 compact/precompact adapters, plugins/system-spec-gate.js, the continuity writer, the doctor inventory, the parity tests); the weak set (rank-memories:415, frontmatter-migration's CLIs); the dead set = F2.1+F3.1+F4.1+F5.3+F5.4+F6.3
- The residue concentrations: F2.3/F2.4/F6.4/F6.5/F7.1/F5.2/F5.7/F9.1 cross-referenced; the Voyage-egress guard's spec-reference (factory.ts:88-105, the `014-local-embeddings-setup-a/007-…` citation) — the migration scar-tissue, still on duty
- README:339-341 (the 1-of-5 "Where embeddings are stored") vs the 5-home census (runtime/data, the phantom, the advisor DB, the 2×/tmp)

## Assessment

- newInfoRatio: 0.75 — the consolidation; the new specifics: the phantom-sentinel verdict (4 derivations, 2 dirname-readers, 0 sentinel-reads, 0 existence), the 21-22/2-3/7-8 strong/weak/dead ledger, and the 5-home data-topology.
- Novelty justification: F9.1 collapses the packet's most-derivation finding (F2.9) into its actual function (a dirname); F9.2 turns the charter'shed-fog "may retain residue" into six named concentrations; F9.3's ledger = the packet's first complete strong/weak/dead sentence.
- Confidence: high for F9.1-F9.3 (the censuses were exhaustive within the five trees); high for F9.4 (the 5-home census); F9.5 honestly bifurcates (source-side verified, operator-side outside the audit).

## Reflection

- Worked: chasing the *sentinel* instead of the *path* — the 4-derivation finding (F2.9) promised consolidation, but the 2-reader/0-read/0-write census is what made the 52-line module collapsible; the existence-check on `runtime/database` (one ls) disciplined the whole derivation story.
- Failed: the compact-hook's registration chase stopped one hop short by design (the operator's settings = outside the audited trees) — recorded as the caveat instead of a claim.
- Ruled out: see above.

## Recommended Next Focus

Iteration 10 (final): the consolidation — severity roll-up and P1 dedup (the F-ledger → the L-ledger), the adversarial re-probes of the weakest claims (the F1.5 mechanism explanation; the F9.5 final-hop; the F2.4 vec0-shadow nuance), the ranked remove/merge/move/fix/document backlog (the charter's close), and the synthesis: research.md + the convergence report + the L-registry, with the memory-DB residue flagged exactly where it concentrates.
