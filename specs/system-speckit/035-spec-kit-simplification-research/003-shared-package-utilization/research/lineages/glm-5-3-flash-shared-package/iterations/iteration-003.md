# Iteration 3: ranking/ + scoring/ + algorithms/ + budget-allocator.ts — live retrieval or test floor?

## Focus

Whether `ranking/` (learned-combiner, matrix-math), `scoring/folder-scoring`, `algorithms/` (rrf-fusion, adaptive-fusion, mmr-reranker, index) and `budget-allocator.ts` feed live retrieval or only tests — traced to terminating callers (advisor handler, registered hook, or invoked CLI). Second-order: who consumes the advisor's 5-lane scorer fusion, who registers the compact hook, whether `rank-memories.ts` is invoked at all, and the duplicated `SharedPayload*` definitions.

## Findings

| # | path:line | Claimed vs actual | Severity | Recommendation |
|---|-----------|-------------------|----------|----------------|
| F3.1 | `shared/ranking/learned-combiner.ts` (558) + `shared/ranking/matrix-math.ts` (151) + `shared/algorithms/adaptive-fusion.ts` (468) + `shared/algorithms/mmr-reranker.ts` (157) | Claimed (README.md:64-78): part of the retrieval feature set — "Adaptive Fusion Support: intent-aware weighting primitives a multi-channel ranker can consume"; matrix-math the sharednumeric core; learned-combiner re-exports matrix-math "for backward compatibility" (learned-combiner.ts:17-18). Actual: **1,334 lines with zero production importers**. learned-combiner: barrel-only (`index.ts:274`, itself unused — F1.3) + its own test (`runtime/tests/learned-combiner.vitest.ts`); matrix-math: learned-combiner-internal + `ranking/matrix-math.test.ts` (in-shared); adaptive-fusion: barrel-via-`algorithms/index` + `runtime/tests/adaptive-fusion.vitest.ts` only; mmr-reranker: `runtime/tests/mmr-reranker.vitest.ts` only. Four test lanes guard uncalled code — acceptance fossils, not coverage. | **P1** | Remove: the quartet (1,334 lines + 4 test files) after an acceptance decision, or record them as acceptance-only primitives in the README. |
| F3.2 | `shared/README.md:77-78` vs `skill-advisor/mcp-server/lib/scorer/fusion.ts:5,46` | Claimed: "Adaptive Fusion Support — intent-aware weighting primitives a multi-channel ranker can consume; **the skill advisor's 5-lane scorer is the caller today**". Actual: the advisor's 5-lane scorer imports **only** `fuseResultsMulti` + `RankedList`/`RrfItem` types from `algorithms/rrf-fusion` (fusion.ts:5,46); it contains **zero** references to `FusionWeights`/adaptive weighting, and the 7 intent profiles (`add_feature`, `fix_bug`, `refactor`, `security_audit`, `understand`, `find_spec`, `find_decision`) exist **only** in adaptive-fusion.ts and the shared README — no consumer anywhere defines or reads them. The documented "caller today" calls a different module. | **P1** | Fix: correct the README feature row to name rrf-fusion; if the intent profiles are keepers, name their intended caller — today they are written for a ranker that was never built. |
| F3.3 | `shared/algorithms/rrf-fusion.ts` (842 lines) | Claimed: reciprocal-rank-fusion. Actual: the **only live module of the algorithms/ quartet** — its single production consumer is the advisor's scorer (`handlers/advisor-recommend.ts`, `handlers/advisor-validate.ts`, `lib/scorer/scoring-constants.ts`, `lib/scorer/ablation.ts` → `scorer/fusion.ts:5`), which uses **one function** (`fuseResultsMulti`) and **two types** out of the module's full 842-line surface. It also hosts the "calibrated overlap bonus" (calibratedOverlap/overlapBonus: the only definitions in shared are here), exercised by its dedicated runtime test (`tests/calibrated-overlap-bonus.vitest.ts:12` imports from rrf-fusion); that feature's production reach is the same single advisor import. | P2 | Merge: trim rrf-fusion to the consumed surface or accept it as the fusion home and mark the bonus's reach; today one import justifies 842 lines. |
| F3.4 | `shared/budget-allocator.ts` (102) + `shared/compact-merger.ts:5,150` | Claimed: a shared context-budgeting primitive. Actual: live, but through exactly **one internal caller** — `compact-merger.ts` imports `allocateBudget`+`createDefaultSources` (:5) and calls it at :150 (the compact-hook's token-budget path); its only other consumers are `runtime/tests/budget-allocator.vitest.ts` (the sole user of the `./budget-allocator` named export) and the unused barrel re-export (index.ts:70-76). It does not participate in retrieval. | P2 | Document: it is a compact-merger-internal; consider demoting it from the public export map (pairs with F1.2). |
| F3.5 | `shared/compact-merger.ts:7-39` vs `runtime/lib/context/shared-payload.ts:27-159` | Claimed (index.ts:71-79): the shared package exports `SharedPayloadEnvelope`/`SharedPayloadProvenance`/`SharedPayloadSection`/`PreMergeSelectionMetadata`/`SourceFreshness`/`MergedBrief` as the single source of the compact-merge contract. Actual: the compact-merge machinery is **strongly live** — the PreCompact/compact-inject hooks (`runtime/hooks/claude/compact-inject.ts:20` `mergeCompactBrief`, plus `hooks/codex/compact-inject.ts`, `hooks/cursor/precompact.ts`; registration surfaces in `doctor-runtime-mirrors.yaml` + the directive-lifecycle-bridge test) — but the **envelope type family duplicated**: `shared/compact-merger.ts` defines its own `SharedPayloadSourceKind/Section/Provenance/PreMergeSelectionMetadata/Envelope` (:7-39), while the runtime's `shared-payload.ts` carries a **richer, trust-state-ful superset** (`SharedPayloadKind`/`SharedPayloadTrustState`/`SharedPayloadCertainty`, :27-85+). Nobody imports the shared copies: of the 3 `@spec-kit/shared/compact-merger` specifier-imports, all carry only `mergeCompactBrief`/`MergeInput` (+ the test). Five dead type exports; the evolved contract lives runtime-side (correct per the GAP-A dependency direction — shared cannot import it back). | **P1** | Remove: the 5 superseded type exports from compact-merger (and the barrel rows); the envelope's canonical home is the consumer. |
| F3.6 | `shared/scoring/folder-scoring.ts` (367) + `runtime/lib/utils/index-scope.ts:169` | Claimed: the shared folder-scoring engine. Actual: exactly **one** production specifier-importer — `runtime/cli/continuity/rank-memories.ts:27-28` (value + `FolderMemoryInput` type); the other two of its four specifier-imports are tests (`folder-scoring-overflow`, `unit-folder-scoring-types`). Meanwhile the discovery/SSOT flows (`runtime/handlers/spec-doc-discovery.ts`, `runtime/lib/search/folder-discovery.ts`, `runtime/lib/config/spec-doc-paths.ts` → `index-scope.ts`) implement their **own** folder scope/exclusion logic and merely *cite* the shared module in a comment (`index-scope.ts:169` "shared/scoring/folder-scoring.ts. Excluding it here would override the…") — two folder-scoring implementations, the shared one cherished by tests, the other one actually running. | **P1** | Merge: either point index-scope's exclusionSSOT at shared/scoring/folder-scoring (one implementation), or retire the shared one with rank-memories (F3.7). |
| F3.7 | `runtime/cli/continuity/rank-memories.ts:29,415` | Claimed: part of the continuity machinery. Actual: a standalone CLI (`isMainModule(import.meta.url)` at :415) whose only importers are itself and a test harness (`cli/tests/test-scripts-modules.js`); no reference found in `commands/`, the cli `*.cjs` wrappers, or the workflow docs — it carries 2 of folder-scoring's 4 specifier-imports and is, functionality-wise, the only reason the shared folder-scoring surface has any import at all. | P2 | Fix: either wire it into a documented command/CI step or retire it together with F3.6's decision; today it is a callable orphan. |
| F3.8 | `shared/algorithms/index.ts:3-5` | Claimed: the algorithms barrel (`export * from './rrf-fusion.js'; './adaptive-fusion.js'; './mmr-reranker.js'`). Actual: barrel-over-a-barrel — its only importer is the root barrel (`index.ts:198`), which nobody uses for algorithms (F1.3); the one live algorithms import (advisor → rrf-fusion) bypasses both barrels via the deep specifier. | P2 | Remove: the intermediate barrel once F1.3's barrel decision lands. |

**Ruled out this iteration:** "folder-scoring feeds the live discovery/search scoring" — its discovery-side referencers import `index-scope`, which implements its own logic and only comments the shared module (F3.6); "the skill advisor consumes the shared intent profiles" — zero references (F3.2); "budget-allocator participates in retrieval" — its only production caller is the compact-merge path (F3.4).

## Sources Consulted

- Sizes: `learned-combiner.ts` 558 / `matrix-math.ts` 151 / `adaptive-fusion.ts` 468 / `mmr-reranker.ts` 157 / `rrf-fusion.ts` 842 / `folder-scoring.ts` 367 / `budget-allocator.ts` 102 (= 2,645 lines, 50.4% test-only)
- Importers: learned-combiner/adaptive-fusion/mmr non-test importer search (0 production), `algorithms/rrf-fusion` specifier census (7), budget-allocator full-reference census (definition+internal+barrel+export-map+test), compact-merger specifier census (3)
- Advisor scorer: `scorer/fusion.ts:5,46,532`, `scorer/lanes/explicit.ts:96,182` (string keys, not imports), importer fan-out (advisor-recommend, advisor-validate, scoring-constants, ablation), 5-lane caller fan-out (advisor-status/validate/recommend, render, compat)
- Compact hook: `hooks/claude/compact-inject.ts:15-45`, `hooks/codex/compact-inject.ts`, `hooks/cursor/precompact.ts`, `doctor-runtime-mirrors.yaml`, directive-lifecycle-bridge test; `shared/compact-merger.ts:5,7-39,150,205`; `runtime/lib/context/shared-payload.ts:27-159`
- folder-scoring: specifier census (4 = rank-memories×2 + 2 tests), `index-scope.ts` import+comment lines, its importers (spec-doc-discovery, folder-discovery, spec-doc-paths + 3 tests/allowlist), `rank-memories.ts:16,28-29,415` + commands/cjs invocation search (0)
- intent profiles: `add_feature|security_audit` presence = adaptive-fusion.ts + shared/README.md only; `FusionWeights|fuseResults\b` in skill-advisor = 0

## Assessment

- newInfoRatio: 0.9 — four of five verdicts (test-only quartet, README's false "caller today", duplicated SharedPayload* family, folder-scoring's comment-citation) are new; the compact-hook liveness partially overlaps the census baseline.
- Novelty justification: F3.1/F3.2/F3.5/F3.6 overturn or correct recorded claims (README:77-78, index.ts's export framing, the folder-scoring consumption story); F3.3/F3.4 narrow "live" to "one import".
- Confidence: high for F3.1, F3.3-F3.8 (import/caller/registration evidence); high for F3.2's negative (3 targeted greps, 0 hits) with the usual caveat that only the five trees were searched.

## Reflection

- Worked: terminating every chain in a runner (advisor handler / registered hook / isMainModule CLI with 0 callers) — "imported" and "live" finally separated; the calibrated-overlap-bonus placement surfaced from its test's import.
- Failed: the first non-test-importer grep for learned-combiner/adaptive-fusion/mmr drowned in `sk-doc` baseline-JSON noise — filtered by shared-internal/test exclusions; the index-scope "folder-scoring" hit turned out to be a comment, caught by reading the import block before writing.
- Ruled out: see above.

## Recommended Next Focus

Iteration 4: contracts/ + context-types.ts + types.ts + gate-3-classifier.ts — which symbols actually cross the package boundary (per-importer named-symbol extraction) vs stay internal; `retrieval-trace` (barrel-only, self-test) — who was it built for?; `context-types.ts` (1 importer: frontmatter-migration.ts — CANONICAL_CONTEXT_TYPES/LEGACY_CONTEXT_TYPE_ALIASES; who ELSE consumes the canonical-context story?); `types.ts` (13 KB, 40+ type exports; 2 type-only importers — which of its ~45 symbols are dead?); gate-3-classifier.ts (37 KB; 3 relative-dist bypassers + 1 source-alias test; its own internal imports: DB_UPDATED_FILE + canonicalFold — why does the classifier read the DB sentinel?); and the specialist/advisory yaml+contract files that mention gate-3-classifier (commands/assets) — contractual vs code coupling.
