# Iteration 2: Type duplication — what the cli/core + lib/validation consumer trees declare vs what shared/context-types.ts and shared/types.ts export

## Focus

First declaration-level audit of the two consumer directories the charter names (`runtime/cli/core/*.ts`, `runtime/lib/validation/*.ts`) against `shared/context-types.ts` (50 lines, 6 exports) and `shared/types.ts` (379 lines, 33 exported type declarations). The census (§2) records that rounds 1-2 "searched for barrel-style specifiers and missed relative imports inside the package" — the declaration-vs-export plane was never swept; this angle sweeps it.

## Findings

| # | Claim side | Actual side | Claimed | Actual | Sev | Recommendation |
|---|-----------|-------------|---------|--------|-----|----------------|
| F2.1 | The ×3 doctrine comment — "One shared fence parser for every skill: leading-fence position, closing-fence and CRLF rules stay identical across readers of the same documents" (`sk-doc/.../root-router-contract.cjs:60-61`, `sk-doc/shared/scripts/frontmatter-version.mjs:27-28`, `deep-loop/runtime/scripts/check-contract-drift.cjs:10-11`) + the shared contract `shared/frontmatter/parse-frontmatter.ts:18-38` (`{frontmatter: Record<string,unknown>; body: string; raw: string \| null}`, js-yaml-backed, adopted by 5 same-tree importers: orchestrator.ts:10, workflow.ts:62, frontmatter-editor.ts:9, title-builder.ts:9, find-predecessor-memory.ts:7) | `runtime/lib/validation/spec-doc-structure.ts:95-102` (local, unexported `interface ParsedFrontmatter` = `{rawBlock, error, memoryError, memoryBlock, continuityBlock, fingerprint}`) + its private parse at :270-320 (:278/:288 early-null constructions; :297 `const rawBlock = match[1]` — a fence-regex capture; :298-308 line-loop with `countLeadingSpaces` + `/^[A-Za-z_][A-Za-z0-9_-]*\s*:/` top-level-key heuristics + bespoke error strings); the file's import block is 4×`node:` + 1×`../templates/level-contract-resolver.js` (:5-10) — no shared import | One shared fence parser, identical rules across all readers. | The package's own heaviest spec-doc reader (1370 lines; 5 structural rules incl. FRONTMATTER_MEMORY_BLOCK and POST_SAVE_FINGERPRINT, its failure codes at :117-126) splits fences, walks lines, hand-validates top-level YAML keys, and defines its own 6-field same-name `ParsedFrontmatter` — a second fence-parsing implementation co-located (same `lib/validation/` dir) with the shared one it never imports. The two field sets are disjoint, so the shapes are not substitutes: this is a name collision PLUS a parallel implementation. The fence/CRLF rules the ×3 comment calls singular now live in two places, and this lane knows of no test holding the two implementations equal (the census's parity additions, round-2 R8-01, target jsonc-strip/context-types, not these). INFERRED (comment-only, unverified — no git permitted): the shared type's `raw` was modeled on "the leading-fence regexes" (:31-34) — plausibly this file's, meaning the shared parser was built to replace this very code and this file never adopted it. | P1 | Merge: build the local 6-field scan on the shared parser's output (`raw` → block extraction), or extract the block-scan into shared/parse-frontmatter.ts so both readers import one fence rule; at minimum rename the local type (e.g. `SpecDocFrontmatterScan`) to end the same-name-different-shape collision. |

(1 finding. The five negatives — same-FIELD or same-NAME parallels that did NOT materialize — are below, because a duplication angle's clean地区表 is the deliverable's other half.)

## What this angle verified as correct

1. **No shared-exported type is re-declared anywhere in the two consumer trees.** All 39 shared exported type names were grepped as declaration patterns across both dirs: 24 (`EmbeddingProfile`, `IEmbeddingProvider`, `ProviderInfo`, `ProviderMetadata`, `UsageStats`, `RetryConfig`, `RetryOptions`, `RetryAttemptLogEntry`, `ErrorClassification`, `FolderScore`, `ScoreWeights`, `TriggerConfig`, `TriggerPhrase`, `NgramCount`, `ScoredNgram`, `ExtractionStats`, `ExtractionResult`, `ExtractionBreakdown`, `PriorityPatterns`, `PriorityBuckets`, `TaskType`, `ArchivePattern`, `BatchEmbeddingOptions`, `ModelDimensions`) → 0 hits; the remaining 15 (`CanonicalContextType`, `ContextType`, `FolderScoreOptions`, `TierWeights`, `RankingMode`, `CreateProviderOptions`, `ApiKeyValidationResult`, `EmbeddingCacheStats`, `LazyLoadingStats`, `ProviderResolution`, `ParsedProfileSlug`, `ProfileJson`, `TaskPrefixMap`, `EmbeddingProfileData`, `ProviderResolution`-dup) → 0 hits. (rg without `-a`; TS sources, NUL-composite-key risk rated improbable — noted per the evidence rule's §3.2 caveat.)
2. **The round-1 "duplicated SharedPayload* contracts" class is retired in code**: the twin-grep finds exactly ONE declaration site — `shared/compact-merger.ts:7,9,16,35` (`SharedPayloadSourceKind` unexported + `Section`/`Provenance`/`Envelope` exported; `PreMergeSelectionMetadata` at :25). The ranking-side copies died with 009 exactly as the census recorded (L1/L9). Verified, not assumed.
3. **`WorkflowSavePayloadTextFields` (workflow.ts:196-203) is NOT a SharedPayload* twin**: its 6 fields (`contentSlug, rawCtxFilename, memoryTitle, memoryDescription, sessionData, collectedData`) share nothing with the envelope's `kind/summary/sections/provenance/selection`. The parallel-naming temptation resolves to: different contract, no duplication.
4. **`memory-metadata.ts` is the adoption exemplar**: it imports what shared actually sells (`MemoryEvidenceSnapshot`, memory-metadata.ts:15 via `@spec-kit/shared/parsing/memory-sufficiency`) and declares locally only what shared does not have — its 6 local types (`MemoryClassificationContext:23`, `SessionDedupContext:32`, `CausalLinksContext:39`, the evidence triple `:47,59,66` with BOTH-CASE keys + `_synthetic`/`_provenance`) have zero shared twins (twin-grep: 0 shared hits for all 6 names). No duplication.
5. **`runtime/cli/lib/trigger-phrase-sanitizer.ts` shares nothing it shouldn't**: it declares 4 own types (`TriggerPhraseSanitizeReason:9`, `TriggerPhraseSource:19`, `TriggerPhraseSanitizeResult:22`, `TriggerPhraseSanitizeOptions:28`), takes `phrase: string` (sanitizeTriggerPhrase:129-131), never touches shared/types.ts:284's `TriggerPhrase`, and its only import is `canonicalFold` from its sibling `./unicode-normalization.js` (:7). The shared/extractor-vs-local/sanitizer split is by-CONCERN, not duplication.
6. `DocumentRecord` (:82-85) and `AnchorOccurrence` (:88-92) in spec-doc-structure are neither of them frontmatter shapes (basename/path/content; id/startLine/endLine/body) — they were candidate twins by position, cleared by reading.

## Ruled out this iteration

- *spec-doc-structure's 6-field type being a mere alias of the shared one*: fields disjoint (side-by-side above) — the finding is the second IMPLEMENTATION, not a copyable interface.
- *Hidden shared adoption in spec-doc-structure via its one relative import*: `../templates/level-contract-resolver.js` could theoretically wrap shared parsing — its internals unread (call budget); the :270-320 parse code is IN spec-doc-structure regardless, so the second implementation stands either way.
- *Type duplication in the 12+ further declaring files* (spec-root-*, tree-thinning, orchestrator, config, alignment-validator, post-save-review, topic-extractor, workflow, find-predecessor-memory, spec-writer-freeze): cleared for the 44 grepped shared names; their INTERNAL parallels (e.g. `PacketFile`/`ManifestEntry`/`RootSnapshot`/`PresentPacket` across the spec-root-*.ts family — all modeling "a packet's files" runtime-locally) are NOT shared-vs-consumer duplication and were only recorded, not pursued (no shared side exists for them).

## Open questions

1. Were the "leading-fence regexes" the shared type's :31-34 docs point at literally spec-doc-structure's :297-class regexes? (INFERRED yes; UNVERIFIED — git history read not permitted.)
2. Does `../templates/level-contract-resolver.js` — spec-doc-structure's only local import — wrap any shared frontmatter logic? (unread; affects only the nuance, not the finding)
3. `runtime/cli/lib/unicode-normalization.ts` (the sanitizer's only import) vs `shared/unicode-normalization.ts` (the round-1-verified 6-importer module): sibling copies, shim, or fork? Function-level, outside this angle's type scope; the twin-greps cover types only.
4. Does ANY test assert equivalence between the shared parser and the :270-320 second implementation? None found in this lane's reads; a test-side check (shared/config.test.ts-family, the package's own test glob at shared/package.json:17) was not enumerated for fence-rule assertions.
5. `memory-metadata.ts:25` imports `@spec-kit/runtime/api` — a THIRD package specifier (self-import of the runtime's own workspace name). How the runtime resolves its own specifier, and whether CI provisions it, is angle 4's question (parked).

## Sources consulted

- Declaration census (rg, 11:13:42Z): both shared type modules' export lines; ALL `interface|type|class|enum` declarations in `runtime/cli/core/` + `runtime/lib/validation/` (head-100 of ~70 decls); both dirs' `@spec-kit/shared` import lines (16 hits, 0 for spec-doc-structure)
- Twin-grep (9 consumer-declared names across ALL of shared/**.ts): shared hits only at compact-merger.ts:7-41 + parse-frontmatter.ts:18
- Field-level dumps: shared ParsedFrontmatter (:18-27 of -A9, completed by the :11-40 read); spec-doc-structure :92-101 (-A7, completed by the :70-131 read); the evidence-triple both-sides -A8; SharedPayload -A12; WorkflowSavePayloadTextFields -A12 -B2; the workflow.ts:56-58 memory-sufficiency import (evaluateMemorySufficiency only)
- reads: `spec-doc-structure.ts:70-131` (SpecDocRuleOptions → RULE_FAILURE_CODES + :278-308 parse evidence); `shared/frontmatter/parse-frontmatter.ts:11-40` (the 3-field contract, complete); `memory-metadata.ts:1-46` (header + imports + 3 local types)
- sanitizer: `runtime/cli/lib/trigger-phrase-sanitizer.ts` decl/const/regex census + import lines (1 relative import); TriggerPhraseSanitizeReason's true home: post-save-review.ts:24 imports it from THERE (runtime's own lib), NOT from shared — the 24-name grep's TriggerPhrase:0 + this import together close the question
- 44-name negative-control greps (24 + 15) over both consumer dirs → 0 hits

## Assessment

- newInfoRatio: 0.85 — the declaration plane of these two directories was never swept by rounds 1-2 (the census's own §2 confession); the second-implementation finding (F2.1) and all five negatives are first-recorded. Not 1.0: SharedPayload*'s single-copy status and memory-metadata's adoption behavior were already the recorded expectation (census L9-keep, R8-01-adjacent) — now code-verified rather than new.
- Confidence: high on F2.1 (every cited line read this iteration); medium-high on the "no test holds the two implementations equal" clause (absence-of-test = inherent negative knowledge, reported as such); the INFERRED comment-chain in F2.1 is marked inline.
- Tool calls: 10 of 12 (4 greps incl. the twin+field dumps, 3 reads, 1 tiny correction grep, 3 writes). No scripts, no node, no validate.sh, no git. Writes: this file, delta, state — inside the lineage only.

## Reflection

- Worked: name-first, fields-second, implementation-third — the 9-name twin-grep priced out 8 of 9 candidates cheaply and the one hit got the full read; the ENOENT on `runtime/lib/trigger-phrase-sanitizer.ts` (wrong dir) was resolved by following the actual import path (cli/lib/) instead of assuming.
- Failed: the -A9/-A7 context windows clipped BOTH ParsedFrontmatter bodies on the first pass; caught because the shared side's comment promised more fields than the window showed. Also: bash#3's `TriggerPhrase` grep pointed at the wrong shared file (trigger-extractor.ts) when the type lives in shared/types.ts:284 — recovered from the iteration-1-style decl census instead of trusting the first guess.

## Next

Iteration 3 — exports without consumers and tests: from the package's exported surface (the 6+3+33 typed names plus the 18+9 function/type exports visible at `algorithms/rrf-fusion.ts:805-840`, the ipc trio, parse-frontmatter's 3, compact-merger's 4+1, trigger-extractor's 8, etc. — ≤12 chosen ACROSS the modules), search runtime + cli + sk-doc + deep-loop trees for an importer, and shared/ itself for a test naming the symbol. No importer, or importer-without-test = finding.
