# Iteration 3: /speckit:search vs its contract, presentation asset, unsupported-feature list

## Focus

`search.md` against its own declared sources of truth (`search-presentation.txt`, `retrieval-conventions.md`), plus the charter's flagged contradiction: conventions' "concept lane" over an embedded index that search.md calls unsupported. Doc contradictions and stale database/embedding mentions were also swept.

## Findings

| # | path:line | Claimed vs actual | Severity | Recommendation |
|---|-----------|-------------------|----------|----------------|
| F3.1 | `references/retrieval/retrieval-conventions.md:33,40,42` | Claimed (§1): "Retrieval splits into a keyed lane, a free-text lane **and a concept lane**… Concept search is a ranked retrieval over an embedded index, and it returns a passage that no literal query would have reached… The concept lane earns its place only where the caller cannot spell the target." Actual: no concept lane exists anywhere in the system. `search.md:3` (frontmatter) and `search.md:138` declare exactly two lanes; §1's own table lists only two lanes; the lanes table in §3 of search.md routes "semantic/paraphrase matching" to the **unsupported** notice; presentation §6 lists semantic search as removed-with-database; the header's trigger_phrases include `"concept versus exact retrieval"` which routes this contradiction into its own index. The availability note (§1) even says only "Both index artifacts" are produced by phase 001 — referencing two lanes, not three. | **P1** (self-contradiction in the normative retrieval contract; a caller following §1 could route around the declared unsupported notice) | Fix: delete the concept-lane rows and the §42 paragraph, or rewrite as an explicit "considered and rejected" note; keep the exact-identifier-goes-to-ripgrep guidance which survives independently. |
| F3.2 | `.opencode/commands/speckit/search.md:86-88` (§4 free-text recipe) vs `retrieval-conventions.md:§2.1` | Claimed: "Copy the recipe flags literally from `retrieval-conventions.md`. Each of `--no-config`, the two exclusion globs and the `--` separator closes a specific failure; paraphrasing them reintroduces it." (search.md guardrail, §1). Actual: the router's own inline recipe **omits** `--hidden`, `--glob '!**/.git/**'` and `--glob '!**/scratch/**'` that conventions §2.1 declares mandatory (the `.git` glob exists specifically because `--hidden` would otherwise admit repository internals; `scratch/` exclusion is a named convergence decision, §9). The inline recipe therefore silently diverges from the contract it tells callers to copy literally. | **P1** (a caller pasting the search.md block gets different roots — dotted `.opencode` dirs are only reached because `.opencode` is passed explicitly and is not itself hidden; the divergence is real but scoped: the failure mode `--hidden` guards (dotted subdirs like `.claude` inside the searched roots) — verified empirically: `!**/.git/**` matters the moment `--hidden` is present, and `scratch/` noise is excluded by conventions but not by the router's block) | Fix: make the search.md recipe byte-identical to conventions §2.1, or replace the inline block with a pointer that says "run conventions §2.1 verbatim". |
| F3.3 | `search.md:138` (§6 summary), `search-presentation.txt` §6 vs §1 lane table | Claimed: two lanes; everything else unsupported. Actual: consistent — trigger-index lane, free-text lane, unsupported notice; presentation asset §2/§3/§4/§6/§7 match the router's contract including `STATUS=OK` no-hit, forbidden vocabulary, stderr-on-error. The unsupported list in search.md:113-118 (semantic/fusion/decay/dedup, causal, epistemic, ablation) matches presentation §6's table row-for-row. | P2 (positive) | Document. |
| F3.4 | `search-presentation.txt` §2 field-mapping table | Claimed: `matchClass` slot renders `exact`, `query-containment`, `partial`. Actual: the lookup's MATCH_CLASSES (`lib/normalize.mjs:31-37`) also emit `phrase-containment` and `token-overlap` — the presentation mapping names only 3 of the 5 classes and omits them without saying "verbatim, any class". The rule below it does say "verbatim from the tool", so a careful renderer passes classes through; the enumeration is just incomplete. | P2 | Fix: replace the enumeration with "the tool's matchClass verbatim (one of the five MATCH_CLASSES)" or list all five. |
| F3.5 | `search-presentation.txt` §2, `indexHash` slot | Claimed: renders `indexHash … first 12 characters`. Actual: works only when the caller did not pass `--no-index-hash`; search.md's own §4 trigger-lane invocation omits `--no-index-hash`, so the hash is computed on every router lookup — paying the dominant load cost (`lookup-trigger-index.mjs:58-60` says hashing is the dominant cost) on every invocation for a display-only hash. Minor perf/utilization note. | P2 | Note: acceptable for interactive use; consider `--no-index-hash` in the router's command and rendering "(hash skipped)" — the presentation template already tolerates it. |
| F3.6 | repo-wide grep for stale database/embedding mentions | Claimed (charter): stale mentions may linger. Actual: swept `retrieval-conventions.md`, `search.md`, `memory-system.md`, `trigger-config.md`, `save-workflow.md`, `environment-variables.md`, `embedder-pluggability.md` — every "retired database" mention is a declared-loss row (conventions §1 "What This Does Not Replace"; search.md §4 "The retired continuity database carried stateful capabilities…"). `embedder-pluggability.md:21` explicitly fences spec-folder retrieval OUT of its scope. No stale claim that a live database serves retrieval. | P2 (positive) | Document. |
| F3.7 | `retrieval-conventions.md` frontmatter vs §1 body | Claimed: frontmatter (9 trigger phrases) is the author-controlled input to the Gate 1 index; body §1 declares three lanes. Actual: frontmatter is well-formed and distinctive — the doc's own §8 guidance is followed. The only defect is the body content (F3.1), not the metadata. | P2 (positive) | Document. |
| F3.8 | `search.md:44` §3 lane-selection table | Claimed: "`--triggers`, or the query is a prompt being matched against author-declared `trigger_phrases` → Trigger-index lane". Actual: `--triggers` is listed in the frontmatter argument-hint (`search.md:3`), but §4's trigger-lane invocation never consumes or documents any `--triggers` flag handling — the lane table is the only place it appears; the lookup CLI has no `--triggers` flag. The flag is a pure router-level signal that never binds to anything mechanical. Harmless but undocumented as a no-op passthrough. | P2 | Document `--triggers` as a lane-selector the router consumes and strips, or drop it from the argument hint. |

Ruled out: "presentation asset contradicts router on no-hit semantics" (they agree: exit 1 = STATUS=OK RESULTS=0); "unsupported list drift between search.md and presentation §6" (verified row-for-row match); "stale database/embedding claims in live retrieval docs" (none found; all mentions are declared-loss rows).

## Sources Consulted

- `.opencode/commands/speckit/search.md` (full read)
- `.opencode/commands/speckit/assets/search-presentation.txt` (full read)
- `.opencode/skills/system-spec-kit/references/retrieval/retrieval-conventions.md` (full read)
- Empirical: conventions §4 worked-example recipes re-run at this tree (all exit codes and record counts matched the documented table exactly: 2.1→10 records, 2.2→3 paths documented/9 observed at today's tree, 2.3→9 count lines, 2.4→10 match records, no-hit exit 1, bad root exit 2)

## Assessment

- newInfoRatio: 1.0 — the concept-lane contradiction (F3.1) is the charter's named hypothesis, now confirmed at file:line; recipe divergence (F3.2) is new evidence.
- Novelty justification: F3.1 and F3.2 are contract-level defects, not covered by iterations 1–2; F3.4/F3.5/F3.8 are new presentation-layer gaps.

## Reflection

- Worked: reading presentation asset and conventions side-by-side exposed the 3-vs-5 class enumeration and the recipe drift immediately.
- Failed: the §4 empirical count for 2.2 returned 9 paths vs the documented 3 — the doc's worked example predates today's tree (documented as observed-at-ripgrep-14.1.1 against an older corpus). Not a defect; the doc pins no date, which is itself worth a P2 note on doc maintenance.
- Ruled out: blaming the router wrapper's arg-resolution shell for divergence (it is orthogonal to the recipe flags).

## Recommended Next Focus

Iteration 4: real callers — Gate 1 (AGENTS.md:83, CLAUDE.md:83), `/speckit:resume` yaml, `/doctor speckit-retrieval` yaml, deep-research agent docs, save-workflow — vs the documented contract; do callers pass flags the contract doesn't define or skip required ones?
