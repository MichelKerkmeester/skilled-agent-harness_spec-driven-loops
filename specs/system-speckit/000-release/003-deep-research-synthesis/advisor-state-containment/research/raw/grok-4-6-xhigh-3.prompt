You are deep-research iteration 3 (of 5) for lineage "grok-4-6-xhigh", investigating one issue in this repository. Use Read, Grep and Bash (git) — read only, DO NOT modify any files. Ground every claim in file:line or command output.

ISSUE
The skill-advisor (and other runtime writers) leak nested .opencode / .advisor-state directories INTO specs/ instead of keeping advisor state at the repo root. Live symptom (HEAD): 23 nested .opencode dirs under specs/, plus .advisor-state under specs/hooks/008-pi-caching-like-reasonix/..., specs/mcp-tooling/013-mcp-obsidian/009-community-plugin-support/, and specs/system-deep-loop/z_archive/026-goal-opencode-plugin/... . The packet meant to fix this, specs/system-skill-advisor/017-advisor-audit-and-state-containment, is Draft at 0% completion.

FINDINGS SO FAR (prior iterations of this lineage; build on them, do not repeat):

# grok-4-6-xhigh — iteration 1: Live main Public has 23 nested specs/.opencode dirs; only 3 are advisor-written .advisor-state leaks (gitignored, absent from this worktree).

## Findings
- This research worktree (.sandbox-wt, detached HEAD 7ab46640da) is a git checkout: find specs -type d -name '.opencode' = 17 (16 fixtures + 1 archive); find specs -type d -name '.advisor-state' = 0. Gitignored leaks are not materialized here.
- Live symptom is on the same-SHA main working tree /Users/michelkerkmeester/MEGA/Development/Code_Environment/Public: find specs -type d -name '.opencode' excluding this nested sandbox-wt = 23 (matches the issue count). .opencode/specs -> ../specs (symlink).
- Of those 23, only 3 contain advisor state (each a single untracked skill-graph-generation.json under .opencode/skills/.advisor-state/). git check-ignore -v matches .gitignore:318 /*/**/.opencode/skills/.advisor-state/. They are not in git ls-files.
- Advisor leak 1: specs/hooks/008-pi-caching-like-reasonix/008-implement-fork-improvements/001-correctness-floor/.opencode/skills/.advisor-state/skill-graph-generation.json — generation 0, reason LEGACY_ADVISOR_GENERATION_BUMP, state live, updatedAt 2026-08-08T11:44:11.316Z.
- Advisor leak 2: specs/mcp-tooling/013-mcp-obsidian/009-community-plugin-support/.opencode/skills/.advisor-state/skill-graph-generation.json — same payload shape, updatedAt 2026-08-02T12:14:04.079Z.
- Advisor leak 3: specs/system-deep-loop/z_archive/026-goal-opencode-plugin/review_archive/2026-07-04-documentation-staleness-audit/.opencode/skills/.advisor-state/skill-graph-generation.json — same payload shape, updatedAt 2026-07-04T06:26:22.704Z.
- Writer confirmed: payload fields (reason LEGACY_ADVISOR_GENERATION_BUMP, state live) are hardcoded by writeGenerationAtomic in .opencode/skills/system-skill-advisor/mcp-server/lib/generation.ts:109-117; path is join(workspaceRoot, '.opencode/skills/.advisor-state/skill-graph-generation.json') per lib/freshness/generation.ts:12,29-30. Canonical intended location is repo-root .opencode/skills/.advisor-state/ (README.md:13-21,50; live file generation 13124, reason advisor-server-watcher-reindex, updatedAt 2026-08-15T09:34:16.204Z).
- The other 20 nested .opencode dirs are NOT advisor-state: 16 tracked fixture command trees under specs/system-deep-loop/035-command-surface-benchmark/002-deterministic-fixtures-oracle/fixtures/** (git ls-files = 157 files; contents are commands/*.md yaml; .gitignore:313-317 says leave fixture command-trees alone); 2 gitignored vendored clones (sk-code/z_archive/015-.../external/ponytail-main/.opencode and system-deep-loop/z_archive/024-.../external/loop-cli-main/.opencode; .gitignore:233 external/); 1 gitignored plugin-install spill specs/barter/001-reel-video-flicker-expo/research/.opencode (package.json depends on @opencode-ai/plugin 1.16.2 + 3439 files; ignored by .gitignore:280 specs/barter); 1 tracked historical deep-research dump specs/system-deep-loop/z_archive/008-.../research/.opencode (3 files: iter-002.jsonl x2 + iteration-002.md, added in 606e55cb8a9).
- 017 packet is still a Draft template at 0%: specs/system-skill-advisor/017-advisor-audit-and-state-containment/spec.md:25 completion_pct 0, :50 Status [Draft/In Progress/Review/Complete]; child 001-state-directory-containment/spec.md:26,46 Planned/0%. The historical '23 strays cleaned 2026-06-18' claim is a different completed archive packet (z_archive/013-.../003-advisor-state-spec-folder-leak/spec.md:28 completion_pct 100). Recurrence after that cleanup is confirmed by leak dates 2026-08-02 and 2026-08-08.
- Current findAdvisorWorkspaceRoot fallback (workspace-root.ts:91-94) hoists only when start already contains an .opencode segment; a CWD under specs/<packet> with no .opencode yet returns resolve(start), so mkdir of .opencode/skills/.advisor-state can still land in the packet. 001-state-directory-containment/spec.md:109 still describes a specs-only deny-list at lines 44/49 — that citation is stale vs HEAD.

## Answered
- How many nested .opencode dirs exist under live specs/ at HEAD, and which of them are advisor-written vs fixtures/vendored/other?

## Open
- Which live call sites still pass a specs/ CWD as workspaceRoot after the .opencode hoist (generation.ts vs findAdvisorWorkspaceRoot vs advisor-server.ts:90 process.cwd() fallback)?
# grok-4-6-xhigh iteration 2 — PARSE FAIL

Tracing the advisor workspace-root resolver. Loading the matching skills and memory tools first, then walking the code path.Skill packets loaded. Next I’ll read the deep-research iteration contract and trace the advisor workspace-root resolver.Key resolver files located. Reading them and tracing every live call site that can still pass a `specs/` CWD.Core resolver is in place. Next I’ll map every caller that feeds it a workspace root, especially `process.cwd()` and hooks.Production call sites next: `advisor-server.ts`, `detectRepoRoot`, and hook writers.Production callers next: handlers, schemas, and hook resolvers that still accept a caller CWD.Handlers and tests next — I need the exact fallback that still accepts a `specs/` CWD after the `.opencode` hoist.The generation writer is isolated. Next I’ll follow who feeds it a workspace root and which callers skip the hoist.I have the resolver hole. Next I’ll confirm which production writers still skip it and take a `specs/` CWD as the root.The launcher and OpenCode plugin both look like writers. I’ll read those next, then check remaining CWD fallbacks.The launcher search may have used the wrong pattern. I’ll open the launcher and the plugin `projectDir` path next.Need to see how the plugin chooses `directory` and how recommend handles an explicit `specs/` workspaceRoot.Schema vs resolver mismatch is the next gap. I’ll also check Cursor/Codex hooks and other runtimes that pass CWD.Codex/Cursor hook entrypoints may sit outside the skill tree. I’ll search those next, then confirm generation write on missing file.The Codex/Cursor hooks live under spec-kit. I’ll read their workspace-root handling and how they call the advisor brief.Those hook files are likely re-exports. I’ll read them and locate the shared workspace-root helper.Need `toClaudeShape` and how Codex/Cursor pass `cwd` through. Also check `defaultSkillsRoot` with `maxDepth: 0`.Need exact Cursor/Codex cwd mapping, `runAdvisorSubprocess`, and a live-tree sanity ch

ITERATION 3 FOCUS
Enumerate every runtime writer that can leak state (advisor daemon, launcher, hooks, plugins) and the conditions that trigger a nested write; note any guard that exists and why it is insufficient.

Reply with ONLY a fenced json block:
```json
{"iteration":3,"lineage":"grok-4-6-xhigh","focus_summary":"<one line>","findings":["<grounded finding with file:line or command evidence>", "..."],"answered":["<question this iteration resolved>"],"open_questions":["<still open>"],"confidence":0.0}
```
