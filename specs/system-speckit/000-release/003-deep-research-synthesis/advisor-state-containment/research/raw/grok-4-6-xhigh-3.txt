I'll load the deep-loop and advisor skill packets first, then enumerate every runtime writer that can leak nested advisor state.Skill packets are loaded. Next I’ll pull memory context and the deep-research contract, then enumerate every writer that can nest advisor state.Memory returned a gap; I’ll read the deep-research contract and start enumerating the writer call sites.The resolver and generation writers are loaded. Next I’ll map every production call site that can still write under a `specs/` CWD.Core writers next: server, daemon, handlers, and every `findAdvisorWorkspaceRoot` / `detectRepoRoot` caller.The live leaks match the legacy generation writer. I'll now walk every production caller that can still feed it a `specs/` root.The leaked payload matches the legacy ENOENT writer. Next I’ll map who still calls it and which hooks, launcher, and plugin paths can feed it a `specs/` CWD.`readAdvisorGeneration` writes on ENOENT, so every freshness probe is a writer. I’ll now enumerate hooks, launcher, plugin, and remaining CWD fallbacks.I need the hook/plugin/CLI workspace-root mapping next — those are the likely producers of the legacy generation-0 leaks.The leak payload is generation 0 + `LEGACY_ADVISOR_GENERATION_BUMP`, which is the ENOENT writer. Next I’ll map every brief/hook/plugin caller that can hit that path from a `specs/` CWD.The live leak payload is the ENOENT freshness writer. Next I’ll map every hook/plugin/CLI path that can feed it an un-hoisted `specs/` CWD.I have the ENOENT writer isolated. Next I’ll read the remaining runtime entrypoints that still pass a raw CWD into it.I have the plugin/hook CWD paths. Next I’ll map how Cursor/Codex/CLI actually pass that CWD into the writers, and check remaining lease/DB writers.The generation-0 leak matches the brief freshness probe. I’ll now inventory remaining writers (lease, CLI, validate, DB) and how each runtime feeds them a CWD.The live leaks match the brief freshness probe. I’ll now inventory every other `.advisor-state` writer and how each runtime still feeds it a CWD.The live leak signature is the hook freshness probe, not the daemon publisher. I’ll now inventory every remaining writer and how each runtime still feeds it a CWD.The live leak signature is the brief freshness probe, not the daemon. I’ll now close the remaining CLI, schema-allowlist, and other `.advisor-state` writer paths.The live leak payload is the hook freshness ENOENT writer. I’ll now inventory the remaining CLI, launcher, plugin, and spec-kit writers that can still nest `.opencode`.I have the leak writers. Next I’ll pin launcher/CLI root resolution, the schema allowlist hole, and any other nested-`.opencode` writers.I have the leak signature. Next I’ll verify remaining plugin/guard writers and confirm which ones can still create a nested `.opencode` tree.