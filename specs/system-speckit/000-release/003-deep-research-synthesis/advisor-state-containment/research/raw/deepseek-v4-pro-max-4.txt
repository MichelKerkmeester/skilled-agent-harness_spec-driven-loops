This is a read-only research iteration. Let me trace the containment code and git history.





Key containment commits found. Let me investigate them in detail and also look at the 017 spec.











Critical finding: the generation counter was NOT touched. Let me read the phase 001 spec and verify the current leak paths.













































































```json
{
  "iteration": 4,
  "lineage": "deepseek-v4-pro-max",
  "focus_summary": "Two containment layers shipped post-v3.6.0.0 (repo-root.mjs + hoistAboveOpencodeTree) but the primary leak path — the hook entry → freshness → generation counter chain — is completely unguarded; the 017 spec remains Draft at 0%.",
  "findings": [
    "017 spec is Draft at 0% completion: parent spec.md is Level 1 template with all placeholders; phase 001 spec.md is Level 2 Planned with all tasks unchecked; description.json has placeholder keywords 'broken, missing, inefficient' and completion_pct:0 (spec.md:48-50, description.json:4-8, tasks.md:51-61).",
    "Commit 541d321d7ea (2026-07-27) shipped: (a) repo-root.mjs — shared anchored resolver with sentinel-based walk-up + hoistAboveOpencodeTree fallback (repo-root.mjs:59-70); (b) spec-gate-core.mjs resolveGuardPaths() now calls findRepoRoot() instead of trusting raw cwd (spec-gate-core.mjs:127-130); (c) mk-cli-dispatch-audit.js now calls findRepoRoot() (mk-cli-dispatch-audit.js:55); (d) .gitignore backstop for /*/**/.opencode/skills/.advisor-state/, .spec-gate-state/, and logs/.",
    "Commit cc6ff3c17d8 (2026-07-27) upgraded workspace-root.ts from hoistAboveSpecsTree (specs-only deny-list from v3.6.0.0, changelog line 153) to hoistAboveOpencodeTree (structural boundary: hoist above outermost .opencode segment). Test rewritten from 3 specs-only cases to 7 nested-start cases + doubly-nested self-perpetuation case + blanket check that no returned root contains .opencode (workspace-root.vitest.ts:42-73).",
    "PRIMARY LEAK PATH UNGUARDED: user-prompt-submit.ts:138-142 workspaceRootFor() returns input.cwd ?? process.cwd() — no anchoring. This flows through buildSkillAdvisorBrief (skill-advisor-brief.ts:429) → getAdvisorFreshness (freshness.ts:330-343) → readAdvisorGeneration (generation.ts:184-198) → getAdvisorGenerationPath (generation.ts:179-180) → getSkillGraphGenerationPath (freshness/generation.ts:29-31) which blindly joins '.opencode/skills/.advisor-state/skill-graph-generation.json' to the raw workspaceRoot. Neither findAdvisorWorkspaceRoot nor findRepoRoot is imported in freshness.ts, generation.ts, or skill-advisor-brief.ts (verified via grep: zero matches in all three files).",
    "mkdirSync with recursive:true still auto-creates the full nested tree: freshness/generation.ts:46 writeJsonAtomic and generation.ts:110 writeGenerationAtomic both call mkdirSync(dirname(filePath), { recursive: true }). When workspaceRoot is inside specs/, this creates the full .opencode/skills/.advisor-state/ tree on first ENOENT-triggered write (generation.ts:194-195).",
    "EXISTING LEAKS NOT CLEANED: 41 nested .opencode dirs and 4 .advisor-state dirs remain on disk under specs/ in the main checkout. The 4 advisor-state dirs contain skill-graph-generation.json files (confirmed via find). They are NOT tracked by git (git ls-files returns empty), so the .gitignore backstop works for new leaks but the v3.6.0.0 changelog claim of cleanup (line 153: 'directories that had already leaked were cleaned up') is false for the post-v3.6.0.0 recurrence.",
    "v3.6.0.0 changelog line 153 documents the pre-existing specs-only fix: 'The resolver now refuses to land inside a specs/ subtree... A regression test pins the new behavior, and the directories that had already leaked were cleaned up.' The specs-only guard was the v3.6-era work that regressed because it was a deny-list, not a structural boundary (confirmed by phase 001 spec.md:63-64).",
    "The daemon quarantine/lease/DB paths (writers 2-4 from iteration 3) remain unguarded. However, the daemon resolves its own workspaceRoot correctly from import.meta.dirname (advisor-server.ts:71-91), so these paths are only vulnerable if a daemon is started with cwd inside specs/ — a defense-in-depth gap, not an active leak trigger."
  ],
  "answered": [
    "What actually shipped vs the 017 Draft? Two containment layers shipped outside the spec workflow: (1) repo-root.mjs + spec-gate/dispatch-audit anchoring (541d321d7ea), (2) workspace-root.ts specs-only→structural upgrade (cc6ff3c17d8). The 017 spec itself is entirely unexecuted — Draft at 0%.",
    "Is there partial containment code or is it entirely unbuilt? PARTIAL. Two containment layers exist: Layer A (v3.6-era) — workspace-root.ts with hoistAboveSpecsTree (specs-only, regressed). Layer B (post-v3.6.0.0) — upgraded to hoistAboveOpencodeTree + repo-root.mjs + .gitignore backstop. But the PRIMARY leak path (hook entry → freshness → generation counter) is entirely unguarded — neither findAdvisorWorkspaceRoot nor findRepoRoot is called anywhere in that chain.",
    "What's v3.6-era work vs in-window? v3.6-era (before June 18, 2026): workspace-root.ts with hoistAboveSpecsTree (specs-only deny-list), documented in changelog line 153, claimed cleanup. In-window (July 27, 2026): two commits upgraded to structural boundary + shared resolver. The gap: neither layer covers the hook entry point, which is the actual trigger for the observed leaks."
  ],
  "open_questions": [
    "Why weren't the hook entry point and generation counter chain fixed when the two containment commits explicitly name 'runtime writers' and 'anchor runtime state to the repo root'? The commits fixed spec-gate-core and dispatch-audit but skipped the hook path entirely — was this an intentional scoping decision or an oversight?",
    "The v3.6.0.0 changelog (line 153) claims cleanup was done, but 41 nested .opencode dirs and 4 .advisor-state dirs remain on disk. Was the v3.6-era cleanup real and the current leaks are a post-v3.6.0.0 recurrence, or was the cleanup claim inaccurate from the start?",
    "Should the fix wire findRepoRoot (or findAdvisorWorkspaceRoot) into user-prompt-submit.ts:138-142 workspaceRootFor() before passing workspaceRoot downstream, or should each downstream consumer (freshness.ts, generation.ts) anchor independently? The plan.md says 'land one shared resolver, then convert every writer' — which approach matches the plan's intent?"
  ],
  "confidence": 0.92
}
```
