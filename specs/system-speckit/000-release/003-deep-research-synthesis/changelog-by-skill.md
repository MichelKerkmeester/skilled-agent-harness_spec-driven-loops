# v4.0.0.0 Changelog — by skill

> Per-spec-folder changelog summaries, categorized by skill. 249 folders across the v3.6.0.0 -> v4.0.0.0 window. Generated by DeepSeek V4 Flash.

---

## Spec Kit & Memory

#### system-speckit/000-rust-backend-rewrite-research — Rust rewrite research charter, planned
Authored a phase-parent spec and 20-round deep-research charter investigating whether the ~244K-LOC TypeScript backend should be reimplemented in Rust, with 16 predefined angles across the current native-primitive boundary and the pure-TS compute core. Research-only, no code written and no decision made; purely internal tooling planning. **Breaking:** none.

#### system-speckit/026-graph-and-context-optimization — Release-readiness audits and P0 remediation, closed
Ran ten deep-review audits (workflow correctness, memory data integrity, hook parity, schema governance, validator integrity, docs truth, upgrade safety) and rolled them into one aggregate verdict (P0=8, P1=28, P2=15) with surgical P0 closures. Extended the stress suite from 38 files/94 tests to 56 files/159 tests so all 54 features read `gap_classification=none`, and hardened MCP argument validation and memory health consistency. A 25-iteration security sweep confirmed the repo is clean of the Mini Shai-Hulud worm but flagged host credential exposure (COMPROMISE-CONFIRMED) with a remediation playbook still pending operator action. Purely internal tooling hardening.

#### system-speckit/027-xce-research-based-refinement — Six-track consolidation and release-cleanup pass
Reorganized the packet from 30 loose phases into six themed tracks (release cleanup, research/doctrine, memory store & search, advisor & code-graph, shared infrastructure, verification) using history-preserving moves. Release-cleanup children rewrote the public root README, aligned skill docs, feature catalogs, manual playbooks, commands, agents, and AGENTS.md to shipped state, and a frontmatter investigation proved detailed reference frontmatter has no runtime consumer, enabling a per-skill alignment campaign. Internal governance and documentation tooling; no user-facing behavior change.

#### system-speckit/028-memory-search-intelligence — Release cleanup and daemon playbook remediation
Completed the release-cleanup sweep across 12 code READMEs, skill/repo READMEs, 14 SKILL/reference/asset docs, feature-catalog entries, command docs, agent mirrors, and the 410-scenario manual playbook, all aligned to shipped state. Ran daemon-skills playbook validation (spec-kit 130/130, code-graph 45/45, advisor 57/58) that surfaced 14 real product findings, then remediated eight clusters A–H — including P0 schema drift where production code referenced columns the schema lacks — each verified per cluster before landing on the 028 review branch. Internal tooling hardening; a whole-suite run remains open before the branch merges to main.

#### system-speckit/029-spec-root-resolution-hardening — Spec-root resolution hardening, planned
Phase-parent decomposing the fix for ~20 independent spec-root resolution call sites that disagree across four precedences, unifying them on canonical-first with a resolver registry, canonicalized automatic writers, and retirement of the repo-root `specs` symlink. Research is complete; the five-phase implementation is planned but not started, with each phase gated on strict validation. Purely internal tooling. **Breaking:** alias retirement is intended for this cycle but not yet shipped.

#### system-speckit/031-memory-reindex-embed-performance — Scan write-back fix and daemon hardening
Fixed a P0 data-integrity bug where force reindex/scan could write quality-loop auto-fixes — including destructive content trimming — back into tracked source docs, gating write-back on indexing origin and closing the same gap in the daemon startup scan and file watcher that an independent review found. A follow-on hardening pass added probe collapse, async-ingest origin coverage, a background default for `memory_index_scan`, lease-ID fencing against the lease TOCTOU race, and a canonical model-server socket default, all verified with new regression tests. The packet's original reindex-performance measurement objective has not started, and a daemon restart is still held. Internal tooling; fixes require a daemon restart to go live.

#### system-speckit/032-relocate-specs-folder — Specs root moved to top-level `specs/`
The specs root was flipped from `.opencode/specs/` to a physical top-level `specs/` directory, with `.opencode/specs` kept as a relative symlink. Planning inverted the existing `spec-root-*` migration subsystem's primitives instead of hand-patching literals, and execution surfaced and fixed 6 additional production files with stale old-direction references beyond the 12 originally named call sites. The same cycle removed dead `SPECKIT_CODE_GRAPH_INDEX_*` filter plumbing, audited every README for stale topology (20 findings fixed), and closed a suspected memory-index duplicate-row bug as no-defect while landing a regression test. **Breaking:** canonical spec paths are now `specs/...`; `.opencode/specs` resolves only through the compat symlink.

#### system-speckit/033-spec-template-context-optimization — Template and context optimization
Spec/plan/tasks/implementation-summary templates were consolidated into a shared core with per-level gated addenda (2,931 → 1,314 source lines, byte-identical renders), and `research.md.tmpl` now gates by level (Level 1: 175 lines, was 944). Two advisory plan-adherence validation rules were added — AC-coverage flipped default-on and a new scope-adherence rule with the `MK_SCOPE_*` contract — and `memory-search.ts` gained search-envelope token-budget enforcement with a new 5-test suite. Purely internal tooling: framework users see smaller, level-appropriate templates, but rendered output is unchanged.

#### system-speckit/z_archive/001-fix-command-dispatch — Archived command dispatch vulnerability record
Purely internal archival housekeeping: the folder was rebuilt from current Level 1 templates into a validator-safe historical record of the command dispatch vulnerability fix, replacing stale structure.

#### system-speckit/z_archive/002-agent-routing-doc-update — Archived agent routing doc update record
Purely internal archival housekeeping: the folder was rebuilt from current Level 1 templates into a concise historical record of the agent routing documentation update, dropping obsolete verification scaffolding.

#### system-speckit/z_archive/003-node-modules-relocation — Archived node_modules relocation record
Purely internal archival housekeeping: the incomplete single-file package was replaced with a complete, validator-clean Level 1 doc set preserving the node_modules relocation topic.

#### system-speckit/z_archive/004-script-audit-comprehensive — Archived comprehensive script audit record
Purely internal archival housekeeping: the folder's obsolete higher-level structure was replaced with a compact Level 1 historical record of the comprehensive script audit.

#### system-speckit/z_archive/005-upgrade-speckit-docs — Archive repair: documentation quality upgrade
Rebuilt the archived documentation-quality-upgrade folder as a clean Level 1 record from current templates, preserving the feature summary without the obsolete higher-level shell. Strict validation, markdown reference integrity, and manual review all pass. Purely internal tooling housekeeping.

#### system-speckit/z_archive/006-generate-context-subfolder — Archive repair: generate-context subfolder support
Rebuilt the archived generate-context-subfolder-support folder as a clean Level 1 record from current templates, keeping the original topic explicit. Strict validation, markdown reference integrity, and manual review all pass. Purely internal tooling housekeeping.

#### system-speckit/z_archive/007-upgrade-level-script — Archive repair: spec folder level upgrade script
Rebuilt the archived level-upgrade-script folder as a clean Level 1 record from current templates, keeping the original level-upgrade topic explicit. Strict validation, markdown reference integrity, and manual review all pass. Purely internal tooling housekeeping.

#### system-speckit/z_archive/008-codex-system-wide-audit — Archive repair: system-wide audit remediation
Rebuilt the archived system-wide-remediation folder as a clean Level 1 record from current templates, condensing the extra handover file into a brief archival note. Strict validation, markdown reference integrity, and manual review all pass. Purely internal tooling housekeeping.

#### system-speckit/z_archive/009-full-spec-doc-indexing — Archive repair: full spec document indexing
Rebuilt the archived full-spec-doc-indexing folder as a clean Level 1 record from current templates, condensing the extra handover file into a brief archival note. Strict validation, markdown reference integrity, and manual review all pass. Purely internal tooling housekeeping.

#### system-speckit/z_archive/010-documentation-alignment — Archive repair: documentation alignment for spec 126
Rebuilt the archived documentation-alignment folder as a clean Level 1 record from current templates, keeping the original alignment topic explicit. Strict validation, markdown reference integrity, and manual review all pass. Purely internal tooling housekeeping.

#### 011-upgrade-auto-populate — Upgrade Auto Populate
Archived and normalized to the current Level 1 template set. Docs rewritten for today's validator, passing with zero errors. The full historical narrative was condensed; git history holds the original detail.

#### 012-spec-doc-anchor-tags — Spec Doc Anchor Tags
Archived and normalized to current-template Level 1 docs so the folder validates cleanly today. Stale structure and broken markdown references removed; detailed history condensed into git.

#### 013-memory-overhaul-and-agent-upgrade-release — Memory Overhaul and Agent Upgrade Release
Rolled-up phased release spanning README audits (60+ files), SKILL/reference, command, and agent-config alignment, root README simplification (−105 lines, spec-first narrative), changelog creation, and GitHub release prep for v2.1.0.0 (notes finalized, publication pending on blockers). Most task docs remain template stubs; the delivered substance is the README rewrite and release preparation. **Breaking:** none — release itself was blocked at publication.

#### 014-memory-index-txt-support — Memory Index Txt Support
Archived and normalized to current Level 1 docs, validating with zero errors. Condensed history preserved via git history for anyone needing the original narrative.

#### 015-anchor-enforcement-automation — Anchor Enforcement Automation
Archived and normalized to current-template Level 1 documentation, verified clean. Broken references and stale metadata removed; full original analysis intentionally condensed into the archive record.

#### 016-index-workflows-code — Index Workflows Code
Archived and normalized to current Level 1 docs that pass today's validator. Purely archival housekeeping: folder shape preserved with compatibility stubs, deeper history kept in git history.

#### system-speckit/z_archive/017-command-adherence — Archive normalized to current Level 1 templates
This archived packet's docs were rewritten against the active Level 1 templates so the folder passes today's validator with zero errors instead of remaining in drifted historical form. Extra top-level notes were simplified into short archival context files. Purely internal housekeeping; condensed history is preserved in git.

#### system-speckit/z_archive/018-mcp-issues-after-update — Archive normalized to current Level 1 templates
The MCP-issues-after-update packet was normalized to a validator-compliant Level 1 archive, with stale markdown references and mismatched metadata removed. The folder now documents its topic and cleanup scope cleanly. Purely internal housekeeping; full original narrative remains in git history.

#### system-speckit/z_archive/019-readme-and-summary-with-hvr — Archive normalized to current Level 1 templates
This archive folder was normalized against current Level 1 templates and verified with `validate.sh` until it reported zero errors, preserving a concise reliable record of the README/HVR work. Extra notes were collapsed into archival context files. Purely internal housekeeping; detailed history remains in git.

#### system-speckit/z_archive/020-mcp-working-memory-hybrid-rag — Hybrid RAG archive normalized to Level 1
The phase parent and all seven child phases were rewritten into current-template Level 1 archive records so the whole packet passes validation as a readable archive rather than stale scaffolding. Retained checklist and decision files were converted to compatibility notes instead of full governance documents. Purely internal housekeeping.

#### system-speckit/z_archive/021-spec-kit-phase-system — Phase system shipped as behavioral layer
The phase system moved from proposal into implemented behavior end-to-end: phase detection/scoring, folder scaffolding, phase-aware command routing (via `:with-phases` suffix and `--phase-folder`, not a dedicated command), recursive validation plumbing, and node/reference docs. 28 of 34 tasks are complete; six remain open (three fixture-backed verification tasks plus three superseded command-asset tasks). Verification is not fully closed — the fixture backlog and sign-off gates remain pending, so completion claims stay blocked.

#### system-speckit/z_archive/022-hybrid-rag-fusion — Hybrid RAG fusion delivered across nine sprints
The full hybrid RAG epic shipped: a separate eval database with SHA256 fast-path dedup, graph signals (degree, edge density, co-activation) with hub-domination caps, calibrated scoring with embedding cache and interference/decay handling, query intelligence (RSF fusion at Kendall tau 0.85, confidence truncation, dynamic token budgets), opt-in feedback features, a pipeline refactor (10/10 P0 items, 304+ new tests, clean tsc), indexing/graph work (schema v17→v18, weight-history, consolidation, chunk thinning), long-horizon reporting and ablation, and a productized Memory MCP server with strict input validation and async ingestion. Scale-gated items (R8 summaries, S5 entity linking, INT8 quantization) were explicitly deferred behind documented gates, and Sprint 4 features ship OFF by default until sunset criteria are met. **Breaking:** eval storage moves to a separate `speckit-eval.db` and the schema migration is not backward-compatible for existing index data.

#### system-speckit/z_archive/023-hybrid-rag-fusion-refinement — Full ESM migration plus critical search-pipeline fixes
`@spec-kit/shared`, `@spec-kit/mcp-server`, and `@spec-kit/scripts` were migrated to native ESM (NodeNext emit, `.js` specifiers, `import.meta` globals, scripts interop boundary), closing out with a real 100% test pass rate, a 10-iteration deep-review remediation (security, reliability, perf), and a full compliance audit of 186 spec folders and 2,000+ memory-DB rows. It also fixed two critical bugs that silently returned zero results from every `memory_search`/`memory_context` query (scope enforcement defaulting ON and a minState filter against a nonexistent column), plus reindex false positives, retrieval-quality fixes, and durable adaptive-fusion thresholds. Purely internal framework tooling, but the search fix and ESM import contract directly affect anyone consuming the memory MCP.

#### system-speckit/z_archive/024-compact-code-graph — Hybrid context injection via hooks and tools
Adds a two-step compaction context-injection architecture: PreCompact precomputes critical context and SessionStart injects the cached context, backed by an async Stop-hook for token tracking and auto-save. Includes cross-runtime fallback for hookless runtimes, MCP first-call auto-priming with `session_health`, Gemini CLI hook porting, hook durability/security fixes (injection fencing, SHA-256 session hashing), context-preservation metrics, and 242 automated tests. Purely internal framework tooling — it improves context continuity across Claude Code, OpenCode, Codex, and Gemini.

#### system-speckit/z_archive/025-nested-changelog-per-spec — Nested packet-local changelog generator
Adds a canonical nested-changelog workflow: a single `nested-changelog.ts` generator plus dedicated root/phase templates write chronological packet history into a central `<spec-root>/changelog/` directory, and `/create:changelog`, `/spec_kit:implement`, and `/spec_kit:complete` now align with it. This replaces ad-hoc packet changelog files with a tested, single-source-of-truth template pair. Purely internal framework tooling for spec-kit packet history.

---

## Deep Loop

#### system-deep-loop/030-deep-loop-unification — Unify deep-loop hub and runtime
Merged `deep-loop-workflows` and `deep-loop-runtime` into a single `system-deep-loop` skill, migrating every downstream reference across commands, agents, READMEs, hooks, and advisor routing (~750 files), then re-verified the whole packet under strict validation. The hub also gained skill template conformance (131 asset renames), a 20-iteration deep review that fixed 7 confirmed P1 bugs, a live divergent-mode dogfood, and a rename of the deep router agent to `deep-loop`. **Breaking:** the `deep-loop-workflows` and `deep-loop-runtime` skill identities no longer exist.

#### system-deep-loop/031-smart-routing-benchmark-program — Benchmark routing with circularity meter
Ships a 10-target, 20-run routing benchmark (8 children + both hubs) measuring Mode-A deterministic router replay against Mode-B live model routing, with a circularity meter exposing deterministic-vs-live gaps. All 8 children pass Mode-A on their own non-fabricated gold, the sk-code hub is a drift-guarded union projection of its children, and the meter flagged `code-review` as the most router-overfit target (perfect Mode-A, zero live corroboration). Purely internal benchmarking; CI wiring is the only remaining follow-up.

#### system-deep-loop/032-deep-alignment-mode — New deep-alignment conformance-audit mode
Adds a new `deep-alignment` mode-packet to system-deep-loop that audits artifacts (docs, code, designs, git history) against named parent-skill authorities, reusing the deep-review runtime engine. Shipped the scoping/lane/discovery engine, working adapters for sk-doc, sk-git, sk-design (static + live-render), and sk-code, plus full command/agent/advisor cutover, and remediated the deep-loop runtime from 62 real dogfood findings. The mode-packet is usable; the packet's overall closeout is still in progress.

#### system-deep-loop/033-post-sync-verification-fixes — Fix post-sync test failures and loop robustness
Root-caused and fixed 5 real test failures surfaced while verifying a 3,801-file sync commit, including a genuine input-validation gap in deep-ai-council orchestration and the discovery of that skill's never-run 94-test suite. Two later amendments hardened the deep-review loop: a deterministic stdin-append state helper replacing patch-anchored appends, and a reducer that now self-heals unnumbered or missing-anchor strategy headings instead of hard-stopping. Purely internal tooling.

#### system-deep-loop/034-skill-benchmark-codex-executor — Benchmark models over the codex transport
Adds a runtime-hosted codex dispatch helper and a thin skill-benchmark executor so a GPT-5.6 model can be benchmarked live over codex alongside the existing opencode transport, reusing the identical prompt and parsers. The Tier-1 comparison showed the transports agree within one aggregate point (opencode 86 vs codex 85), and a Tier-2 breadth sweep plus SOL deep-review reconciled 12 adjusted routing recommendations. Purely internal benchmarking tooling.

#### system-deep-loop/035-command-surface-benchmark — Command-surface benchmark across two axes
A new two-axis benchmark for the OpenCode command surface: a deterministic `sk-doc-command` peer adapter auditing structural integrity across the full 36-command corpus, plus a sampled DAB-012–027 behavioral suite scored via a hermetically verified 52-cell scheduler. The `/deep:command-benchmark` launcher, alignment alias, and generated Codex mirror are complete; live model capture is deferred pending operator green-light, so the packet ships partially complete.

#### system-deep-loop/036-deep-loop-innovation — Deep-loop recommendations implementation spine
Implemented all 178 research recommendations from packets 065/001+002 as one cross-mode substrate: a typed append-only event ledger behind a fail-closed transition-authorization gateway, plus sealed reference artifacts, replay fingerprints, receipts/effect recovery, and blinded adjudication. The shared runtime landed additively and dark (fan-out live-tools unblocked and shipped; versioned envelope, authorized ledger, replay fingerprints, and gateway leaves complete), with per-mode authority cutover deferred to later phases. This is purely internal deep-loop framework tooling, not a user-facing feature.

#### system-deep-loop/037-graph-engineering — Graph-engineering deep-loop research
Research-only packet assessing how to evolve deep-loop workflows into graph-engineering-based loops, completed across four repo studies (agent-swarms, graphene-main, GraphARC, graph-engineering-master) plus a NOOA/blog loop-harness study and a cross-study integration capstone. Each ran 20-iteration fan-out deep-research loops with cross-model verification (DeepSeek V4 Pro), and the capstone welds S1-S5 into one graph-based agent-loop design projected over the 036 authority plane. No runtime code was modified; the next evidence class proposed is a mutant-driven shadow vertical-slice packet.

#### system-deep-loop/z_archive/001-sk-recursive-agent-loop — Archived agent-improver program
Archived packet for the `sk-improve-agent` program (renamed from `sk-recursive-agent`): an evaluator-first, approval-gated promotion path with rollback, then a full skill with benchmark harnesses and target profiles, doc/runtime/command/wording alignment phases, and a 5-dimension integration-aware scoring framework replacing keyword-checking. Includes the first self-referential test of the improvement loop against its own `agent-improver.md`. This is archived internal tooling; no user-facing change ships here.

#### system-deep-loop/z_archive/002-sk-agent-improver-test-report-alignment — Archived test-report methodology campaign
Archived five-phase methodology campaign on the `sk-improve-agent` triad: research → implementation + R1 → follow-up research → command-flow stress tests → executable wiring. R1's 0/2/4 score exposed that the discipline lives in the command orchestrator, not the agent body; restructuring by layer produced a final composite PASS 6 / PARTIAL 0 / FAIL 0, with 91 wiring tests passing. Archived internal tooling, but the layer-partition methodology it validated is reusable.

#### system-deep-loop/z_archive/003-deep-loop-command-flow-stress-tests — Deep-loop stress-test scaffold
Scaffolded phase-parent applying the 060/004 command-flow stress methodology to `@deep-research` and `@deep-review`, with per-agent CP-scenario and stress-run sub-phases. All four sub-phases remain placeholders at 0% completion with no authored scenarios or runs. Purely internal tooling scaffold; no user-facing change.

#### system-deep-loop/z_archive/004-sk-deep-agent-improvement — Renamed skill to deep-agent-improvement
Archived rename packet: skill folder `sk-improve-agent` moved to `deep-agent-improvement` via `git mv` (~116 files), joining the `deep-*` family. All 156 advisor routing phrases, skill-graph registry, fusion penalty list, test fixtures, four-runtime command surfaces, root docs, and install guides were migrated with zero active-code residual hits, and the SQLite advisor cache rebuilt (skillCount=18). Symbolic-only rename — no behavior changed; the `@improve-agent` agent and `/deep:start-agent-improvement-loop` command stayed stable per precedent.

#### system-deep-loop/z_archive/005-deep-multi-ai-council-skill — Deep AI Council skill extraction and graph
Shipped the `deep-ai-council` skill as a dedicated package with runtime agent mirrors, a derived SQLite council graph (MCP handlers, strict schemas, bounded queries, convergence signals), and full playbook coverage (DAC-019..DAC-032) plus CI-protected value scenarios proving the graph beats the no-graph baseline (up to 13x fewer calls). Repaired two latent vitest failures and hardened the surface with a one-command test gate, 32-entry feature catalog, replay helper, and v1.1.0.0 changelog. Internal tooling — no breaking user-facing change.

#### system-deep-loop/z_archive/006-deep-skill-evolution — deep-* skill family release cleanup
Ran a consolidated release-cleanup arc across the deep-loop-runtime, deep-review, deep-research, and deep-ai-council skills: doc remediation, strict evergreen-citation sweeps (63 citations across 29 files), a 9-gate legal-stop reconciliation across six deep-review surfaces, and reducer/doc-cluster backlog remediation (LG-0001..0033 fixes with new tests). deep-review shipped v1.10.0.0, deep-ai-council v2.1.1.0 with five new reference/test files. Internal tooling — no breaking change; several child phases still scaffolded or partially complete.

#### system-deep-loop/z_archive/007-deep-agent-improvement-benchmark-mode — Model-benchmark lane for deep-agent-improvement
Two-arc program (19 phases) that added a `model-benchmark` mode and then elevated it to a co-equal Lane B alongside agent-improvement across commands, SKILL.md, references/assets, agents, and scripts — including a dedicated `/deep:start-model-benchmark-loop` command and a fixed latent loop-host arg-forwarding defect. Also fixed deep-loop commands no longer leaving empty archive dirs (lazy, guarded restart branches with regression tests). Internal tooling — Lane A behavior preserved byte-identical.

#### system-deep-loop/z_archive/008-deep-improvement-skill-benchmark-mode — deep-improvement rename plus skill-benchmark lane
Renamed `deep-agent-improvement` to `deep-improvement` (legacy aliases retained) and added Lane C (`skill-benchmark`), a hint-free harness that measures a skill's real-world routing, reference/asset discovery, efficiency, and usefulness with deterministic Mode A scoring plus opt-in D1-inter advisor and D4-R live ablation (54/100 measured n=5). Multiple multi-model deep-research sweeps (20 iterations across 4 models) informed the design; sk-code gained surface-nested routing (aggregate 71→79) and live playbook mode; 358/358 tests green. Internal tooling — breaking only in that the skill/agent id is renamed from `deep-agent-improvement`.

#### system-deep-loop/z_archive/009-deep-loop-parallel-fanout — Deep-loop parallel fan-out
Added an opt-in fan-out layer so `/deep:start-research-loop` and `/deep:start-review-loop` can run N executor lineages concurrently (capped) in isolated sub-packets, with fan-out config schema, a concurrency-capped worker pool, and a status ledger (171/171 tests green). Single-executor stays the default and byte-identical via a parity gate. Partially shipped (parent ~33%): spawn/merge phases pending, and the per-iteration model-schedule design plus the 14-finding remediation are recorded as planned, not implemented.

#### system-deep-loop/z_archive/010-deep-context-gathering — deep-context loop foundational build
Shipped `deep-context` as the fourth deep loop and third deep-loop-runtime consumer: a heterogeneous-parallel, inward codebase-context loop with a `loop_type='context'` coverage-graph extension and cross-executor-agreement convergence producing a reuse-first Context Report. Reached runtime-robustness parity (atomic-state, jsonl-repair, post-dispatch-validate, loop-lock, executor-audit guard), fixed three cross-skill runtime gaps, aligned references to the mature sibling layout, and added runtime agent mirrors, a native-only default executor pool, and un-skippable Phase-0/setup gates across all deep commands. Internal tooling — no breaking change.

#### system-deep-loop/z_archive/011-deep-context-workflow-adoption — Deep-context workflow adoption: PLAN-WORKFLOW LOCK
Shipped a durable, multi-surface governance fix after a judgment failure hand-rolled a manual gather instead of the plan-specified `/deep:start-context-loop`. A new PLAN-WORKFLOW LOCK hard blocker in `AGENTS.md` (symlinked to `CLAUDE.md`) freezes plan-named workflows like scope — verify assumed friction against the contract, flag real blocks, never silently substitute. A new constitutional rule generalizes this to all deep skills (research, review, council, improvement), with anti-pattern and feedback-memory reinforcements.

#### system-deep-loop/z_archive/012-deep-loop-executor-config-dir — Per-executor Claude config-dir routing
Deep-loop fan-out can now route an individual `cli-claude-code` lineage through a caller-provided Claude config directory (e.g. a Fable 5 seat at `~/.claude-account2`) without affecting Codex, OpenCode, native seats, or the parent. Implemented as `CLAUDE_CONFIG_DIR` injected into the spawned env with `~` expansion, permitted only for `cli-claude-code` executors. Purely internal tooling; verified via schema/unit tests and a stubbed `claude` binary.

#### system-deep-loop/z_archive/013-agent-deep-review-optimization — Deep-review improve-agent loop: kept baseline
A 5-iteration improve-agent loop over `@deep-review` via cli-copilot produced `keptBaseline`/`blockedStop`: every candidate scored a perfect 100/100/100/100/100 with zero delta, confirming the substrate-ceiling pattern for already-mature agents. Purely internal tooling; promotion to the operator for review since the structural scorer may miss qualitative gains in the synthesis iteration.

#### system-deep-loop/z_archive/014-agent-deep-research-optimization — Deep-research improve-agent loop: kept baseline
A parallel 5-iteration improve-agent loop over `@deep-research` via cli-copilot also ended `keptBaseline`/`blockedStop` with all candidates at perfect baseline scores and no improvement delta. Same substrate-ceiling conclusion as the deep-review run; promotion deferred to operator review. Purely internal tooling.

#### system-deep-loop/z_archive/015-sk-deep-rename — sk-deep rename, nine phases
Rolled out the repo-wide rename of `sk-deep-review` → `deep-review` and `sk-deep-research` → `deep-research` across nine phases: a 1,514-row active-scope inventory, skill folder renames, `.opencode` internals, `.claude`/`.gemini`/`.codex` mirrors, root docs, advisor rebuild, remediation, and final cleanup. Phase 009 then migrated the skill-graph family schema so `deep-loop` is valid end-to-end (SQLite CHECK constraints, TypeScript enums, mirrors, regenerated graph) and extended deep-review/deep-research disambiguation routing to native results. Mostly internal refactor; the visible change is the skill/agent/command naming family.

#### system-deep-loop/z_archive/016-improve-agent-to-deep-agent-improvement-rename — improve-agent renamed to deep-agent-improvement
Renamed the `@improve-agent` agent to `@deep-agent-improvement` across all four runtime mirrors (`.opencode`, `.claude`, `.gemini`, `.codex`), rotated ~22 internal references in the `deep-agent-improvement` skill, renamed the matching YAML dispatch assets, and added a v1.5.0.0 changelog. Pure semantic rename with zero behavior change, completing the deep-* naming family; the `/deep:start-agent-improvement-loop` slash command was deliberately left unchanged. Verified by strict validation, zero residual grep hits, and advisor recommendation parity.

#### system-deep-loop/z_archive/017-deep-review-three-tier-setup — Deep-review non-interactive setup bypass
`/deep:start-review-loop:auto` gained an explicit three-tier setup-resolution contract: Tier 1 resolves from arguments, pre-bound answers, and defaults; Tier 2 asks one narrow clarification; Tier 3 fails fast with a named missing-inputs error. Autonomous dispatches now bypass the consolidated interactive question block. Verification was dry-run only, as the full loop dispatch was prohibited by the runtime instruction.

#### system-deep-loop/z_archive/018-deep-review-uplift — Deep-review content-hash finding dedup
Phase 3 H-7 added two-tier finding dedup to deep-review synthesis: a primary `content_hash` key (sha256 of file, line range, finding type, normalized description) with a backward-compatible `file:line + title` fallback, plus dimension merging when hashes match. Every `findingDetails[]` entry must now emit `content_hash`. H-9 (bounded evidence interpolation) was explicitly deferred per the council ruling.

#### system-deep-loop/z_archive/019-deep-agent-improvement-uplift — Deep-agent-improvement mutation signature dedup
deep-agent-improvement now computes a sha256 signature for every mutation in `mutation-coverage.json`, and the reducer prefers it over the legacy `dimension::mutationType` key for dedup. An `EXHAUSTED-FROM: iter-NNN` skip-reason format and a `DEEP_AGENT_IMPROVEMENT_SKIP_DEDUP=1` bypass were added, while legacy records without signatures keep working via the fallback key.

#### system-deep-loop/z_archive/020-skill-readme-standardization — Deep-loop skill README standardization
Archived parent grouping the narrative-voice README rewrites for six deep-loop skills (ai-council, deep-context, deep-improvement, deep-loop-runtime, deep-research, deep-review). Each README now opens with a human pitch and at-a-glance table, and stale version lines and drifted counts (e.g. deep-review's version, stuck threshold, state files, quality-gate count) were dropped.

#### system-deep-loop/z_archive/021-multi-ai-council-write-protocol — Multi-AI council write protocol
Archived parent for the phases defining how council deliberations become durable artifacts: output/persistence protocol, a scoped writer library with sha256 audit-trail and rollback, v1.1/v1.2 metadata and deferrals, and five main-agent write-enforcement gates across all four runtime mirrors. One child phase ended blocked on an unwritable Codex mirror.

#### system-deep-loop/z_archive/022-sk-deep-research-evolution — sk-deep-research evolution series archive
Archived parent for the sk-deep-research evolution sequence: the original three-layer creation, refinement research, review-mode split into a dedicated `sk-deep-review` skill, the durable `review/` folder and `research/` path migration, and the runtime-truth foundation with typed stop reasons, legal-stop gates, and resume semantics for both deep loops.

#### system-deep-loop/z_archive/023-deep-loop-workflows — Consolidate five deep-loop skills into one hub
The five deep-loop workflow skills (deep-ai-council, deep-context, deep-improvement, deep-research, deep-review) were merged into one `deep-loop-workflows` hub with five verbatim mode packets, shipped across nine parity-gated phases. The `/deep:*` command routers and agent mirrors in all three runtime trees were repointed to the merged packet, the advisor graph collapsed the five `deep-*` skill IDs into one node with a mode-alias/discriminator layer, and framework docs moved to a two-skill model over the frozen MCP-free `deep-loop-runtime` backend. A deep review returned a conditional pass (0 P0) after ~7 hypotheses were refuted. **Breaking:** the five public `deep-*` skills are gone — routing now targets `deep-loop-workflows` plus a concrete mode.

#### system-deep-loop/z_archive/024-deep-loop-improved — Loop-systems improvements mined from external references
A 51-iteration deep-research run mined vendored `loop-cli-main` and `kasper` references into a ranked 40-item backlog, which was then implemented as a stream of improvements across `deep-loop-runtime`. Purely internal tooling: atomic-state serialize-diff, SHA-256 integrity helpers, a deferred/debounced writer, abortable chunked sleep, lifecycle transition guards, lock-held JSONL merge, loop-lock heartbeat and single-flight hardening, byte-offset log regions, fixed-rate overrun accounting, and a convergence score-delta signal.

#### system-deep-loop/z_archive/025-deep-loop-gpt-reliability — GPT-backed OpenCode deep-loop reliability
Diagnosed why GPT-backed OpenCode fails where Claude succeeds — our contracts are Claude-shaped, and GPT executes the letter — then fixed routing, dispatch, and identity across seven tracks. Route-proof validation now rejects schema-valid wrong-mode artifacts, a registry-resolved deep router and pre-route headers bind leaves before prompt interpretation, the Mode-D self-classification gate was replaced with evidence-based dispatch-context checks, and the `mk-deep-loop-guard` plugin enforces route mismatches at the Task-dispatch boundary. **Breaking:** `ai-council` is now subagent-only and can no longer be invoked as a top-level OpenCode agent.

#### system-deep-loop/z_archive/026-goal-opencode-plugin — /goal OpenCode plugin
A new `/goal` plugin and command mirroring Claude Code's `/goal`: one per-session completion condition is persisted and injected into every turn until met. Built across state store (fail-closed, atomic writes), passive system-context injection, lifecycle tracking with guarded usage accounting, an idle-time completion supervisor, default-off active continuation, deterministic sk-prompt goal enhancement, plus security/correctness fixes and regression-test backfill.

#### system-deep-loop/z_archive/027-deep-loop-behavior-benchmarks — Deep-loop behavioral benchmarks
Each of the five deep-loop sub-skills now carries a durable `behavior_benchmark` package measured by a shared runner (`behavior-bench-run.cjs`, D1-D5 rubric, 11-bucket taxonomy, watchdog and checkpoint extraction). ~120 scored runs across a claude-cli baseline and both GPT-5.5-fast legs found reasoning effort is the load-bearing variable (high eliminates silent stalls; med fakes delegation) and that the Gate-3 documentation halt is the dominant GPT failure across all four command surfaces, confirmed at both efforts.

#### system-deep-loop/z_archive/028-deprecate-deep-context-integrate-capabilities — Deprecate standalone deep-context
Standalone `/deep:context` was deprecated: the command is now a no-write redirect with replacement guidance, while useful codebase-context behavior is preserved inside deep-research and deep-review. Discoverability cleanup removed it from the registry, advisor, active docs, mirrors, and generated indexes, leaving disabled deprecation stubs. **Breaking:** `/deep:context` no longer runs a context loop or writes context artifacts.

#### system-deep-loop/z_archive/029-deep-loop-runtime — Deep-loop runtime hardening sub-phase packet

Phase-parent packet rolling up the deep-loop runtime implementation cycle: the shipped strategy template now carries the seven reducer-owned ANCHOR markers so fresh copies reduce deterministically, fan-out merge gained a content-then-id total order with lag/pending/failed pool gauges, and a resilience cluster (bounded failure taxonomy, transient/fatal retry, orphan-lineage requeue, explicit recover-vs-fresh gate) plus STOP-input corroboration and continuity threading landed. Follow-up hardening fixed orchestrator strict-aware registry parity, a lineage timestamp window, session-id parity tests, and the spec.md section-counts manifest; later phases benchmarked the default-off near-duplicate finding dedup, lag-ceiling, and heartbeat gauges (graduate verdicts, defaults not flipped), fixed a false-firing lag metric and a title-overlap dedup seam, and closed the reliability-weighted convergence cluster as NO-GO with its Level 3 plan kept as design of record. Purely internal framework tooling — no public API or user-facing behavior changes.

---

## External CLI Orchestration

#### cli-external-orchestration/026-cli-external-parent — Parent hub plan for cli-opencode + cli-claude-code
Plans folding the two flat, independently-routable skills into one `cli-external` parent hub with two workflow modes, following the existing sk-doc/sk-prompt hub canon. The 8-phase decomposition covers the full `git mv` relocation, dissolution of both graph identities into one hub `graph-metadata.json`, and the load-bearing rewrite of the executor-delegation scorer to source its alias table from the hub mode-registry instead of a top-level `family === 'cli'` filter. This packet is planning-only (completion 0%, all phases pending) — no content moved and no dispatch behavior changed yet. Internal routing/skill restructure; **Breaking:** once executed, `cli-opencode` and `cli-claude-code` cease to be independently advisor-routable top-level identities.

#### cli-external-orchestration/027-cli-codex-revival — Codex CLI revival, fail-closed
Reverses the earlier Codex deprecation (packet 122) and revives `cli-codex` as a deep-loop executor kind and third `cli-external-orchestration` mode, gated fail-closed on `command -v codex` so an absent binary is never routable or dispatched. Beyond the six core phases (contract pin at 0.144.1, executor support, skill packet, hook adapter layer, agent TOML sync, docs closeout), follow-ups shipped eight Codex guard-hook adapters plus a convergent `~/.codex/hooks.json` installer, and a four-model fast-tier roster (`gpt-5.5` default plus three `gpt-5.6` variants) verified by a 20/20 live model×effort matrix. Purely internal executor-tooling work; some live verification (dist build, hook smoke) is still deferred.

#### cli-external-orchestration/028-cli-hub-rename — Canonical hub directory rename
Renames the external CLI parent hub to the canonical `cli-external-orchestration` directory identity via `git mv` (history preserved), realigns advisor/projection metadata to the hub path, and sweeps live consumers to the new identity while keeping `cli-opencode` as the concrete executor name where semantically correct. Purely internal restructuring with no user-facing behavior change; the closeout phase records targeted rename checks passing while full strict validation stays blocked by a stale compiled dist and unrelated graph prerequisites.

#### cli-external-orchestration/029-cli-devin-revival — Devin CLI revival, fail-closed
Revives `cli-devin` (Cognition's Devin CLI, pinned live at 3000.2.17) as a fifth deep-loop executor kind and another `cli-external-orchestration` mode, gated fail-closed on the `devin` binary. Delivery includes the skill packet, ten hook adapters with lifecycle registration corrected to the documented nested schema (six events live-confirmed), a 20-scenario manual-testing playbook (19 PASS, 1 by-design SKIP), a fail-closed `PermissionRequest` policy adapter, extraction of shared hook-reader boilerplate with a `firstNonBlankString` masking-bug fix backported to Claude's and Codex's `spec-gate-enforce.mjs`, and mirroring of all 13 repo agents into `.devin/agents/`. One honest upstream limitation: Devin 3000.2.17 ignores the PermissionRequest hook's decision under every tested permission mode. Internal executor-tooling work.

#### cli-external-orchestration/030-cli-cursor-creation — Cursor CLI creation, fail-closed
First-time creation of `cli-cursor` (pinned live at 2026.07.23-e383d2b) as a deep-loop executor kind and fourth hub mode, fail-closed on the `cursor-agent` binary with the auth-fail-exits-0 gotcha explicitly documented. Hook adapters were built with the event mapping corrected by live CLI probing — `sessionStart`/`preToolUse`/`postToolUse`/`sessionEnd` fire while `beforeSubmitPrompt`/`stop` never do — and the `{permission:deny}`+exit-2 contract was proven to actually block a tool call. Composer-2.5 got a prompt-models profile plus `check-prompt-quality-card-sync.sh` CI coverage, and a 19-scenario manual-testing playbook was authored. All 14 phases complete and validated. Internal executor-tooling work.

#### cli-external-orchestration/031-cli-pi-creation — Pi CLI integration, fail-closed
Integrates Pi CLI (pi.dev's earendil-works agent, pinned at 0.82.1) as a sixth deep-loop executor kind and sixth hub mode, fail-closed on the `pi` binary with the confirmed unreliable-exit-code gotcha noted. Delivery spans the skill packet, live-confirmed stdio MCP hosting (`sequential_thinking` and `mk-spec-memory` both connected), a type-confirmed 32-event extension surface, and a fail-closed 7-model allowlist built from the operator's own live Pi roster. Bridge phases for skill-discovery, 36-command flattening, and 13-agent translation were design-verified, while the dispatch command-construction body remains an honest stub until Pi's real invocation syntax is confirmed and some phases stay planning-only/Blocked. Internal executor-tooling work.

#### cli-external-orchestration/032-per-mode-provider-model-reference — Per-mode provider/model reference catalogs
Each of the six cli modes now owns a `references/providers-and-models.md` catalog (providers, models, effort tiers, dispatch shapes), registered as advisor-routable leaves in the leaf manifest. The stale smart-routing surface router was expanded from three to all six modes, duplicated model enumerations were trimmed from each mode's cli-reference.md and SKILL.md rosters down to a compact residue plus pointer, and hub docs were reconciled (catalog pointers, version skew 1.1.0.0→1.2.0.0 fixed, phantom scripts directory removed).

#### cli-external-orchestration/033-deepseek-v4-flash-pi-roster — DeepSeek V4 Flash on cli-pi
`deepseek-v4-flash` was added to the enforced cli-pi fan-out roster (`PI_SUPPORTED_MODELS` in executor-config.ts and the synced `PI_ALLOWED_MODELS` mirror), closing the divergence where docs advertised the model but fail-closed dispatch rejected it. The id was live-confirmed on the install first; cli-cursor and cli-devin were intentionally untouched because their providers serve no DeepSeek V4 Flash id.

#### cli-external-orchestration/034-opencode-go-flash-qwen-roster — opencode-go routing for Flash and Qwen Max
`qwen3.8-max` was added to the cli-pi roster, and both it and `deepseek-v4-flash` now route through the OpenCode `opencode-go` gateway in the provider map, with both synced enforcement points updated and docs added to cli-pi and cli-opencode. Roster guard tests moved with the pins and end-to-end dispatches were live-verified; the direct `deepseek` route stays documented for backward compatibility.

#### cli-external-orchestration/035-improved-communication — Portable CLI communication projection layer
A provider-neutral projection layer rewrites terse CLI output into plain English while preserving the canonical output bytes — the portable safety boundary proven across research, contracts, normalized assembly, protected-span fidelity, provider adapters, runtimes, evaluation/observability, and release-hardening phases. The package relocated into the owning `sk-communication` skill and is **off by default** with a private per-machine opt-in, and `sk-communication` is held out of advisor routing through an adjustable denylist. Framework and code are complete and tested (296/296 package tests); the powered blind human non-inferiority study remains the operator-run release gate, so no release is claimed.

#### cli-external-orchestration/041-pi-remote-mobile-agent-like-cc — Pi Remote Mobile Agent program
A nine-phase program defining a private Claude-app-style mobile control plane over Pi CLI, covering contract/threat baseline, test harness, relay protocol and state, auth/tailnet boundary, mobile PWA, approval and remote mutation, push hardening, docs, and release rollout. Purely planning tooling: approved planning packets exist for all nine phases, but no production code, service, or release capability was created.

#### cli-external-orchestration/042-pi-remote-experience-parity — Pi remote experience parity research
A two-lineage deep-research run (cli-codex GPT-5.6, cli-pi deepseek-v4-flash, 20 iterations each) synthesized into a 1079-line research.md: the best-in-class Pi remote experience is a **typed, redacted, replayable event ledger** with a separate authenticated command protocol rather than a terminal mirror. It ranks P0/P1/P2 recommendations for amending the 041 packet and leaves 20 open validation questions before any schema is frozen. Purely research/tooling — no product code shipped.

#### cli-external-orchestration/043-luna-max-deepseek-max-glm-roster — Roster expansion: Luna Max, DeepSeek Max, GLM
GPT-5.6 Luna Max ids join the enforced Cursor and Devin allowlists, DeepSeek V4 Pro/Flash Max join Devin, and GLM 5.3 is documented in the opencode catalog. Every id was list-verified against live CLI output before shipping, and the two hand-synced enforcement points stay byte-identical with cross-checked tests (190/190 green). Purely framework-internal tooling; ids are list-verified, not dispatch-tested.

#### cli-external-orchestration/z_archive/001-cli-gemini-creation — cli-gemini skill created
A documentation-only skill (~3,000 lines across SKILL.md, four references, and prompt templates) enables Claude Code and OpenCode to orchestrate Google's Gemini CLI, with smart routing over 7 intent signals and advisor-boostered discovery (0.95 confidence). No runtime code; pure framework tooling.

#### cli-external-orchestration/z_archive/002-cli-codex-creation — cli-codex created, cli-gemini normalized
Introduced `cli-codex` as a sibling CLI bridge while normalizing `cli-gemini` to a single canonical model reference, then updated advisor and README registration surfaces for consistent discovery. Documentation-only framework tooling.

#### cli-external-orchestration/z_archive/003-cli-claude-code-creation — cli-claude-code skill created
A full skill lets external AIs (Gemini, Codex, Copilot) delegate to Claude Code CLI with reversed orchestration, 9 agent profiles, 3 model tiers, and 10 prompt template categories; deep-reasoning is the default intent and a nesting guard blocks Claude Code delegating to itself. Documentation-only framework tooling; model IDs and agent rosters will drift and need updates.

#### cli-external-orchestration/z_archive/004-cli-copilot-creation — cli-copilot skill created
A skill exposes GitHub Copilot CLI's multi-provider models (7+), cloud delegation, plan mode, and autopilot, completing the 4-CLI ecosystem; it was the first skill implemented via cross-AI (Gemini CLI) delegation. Framework tooling; Copilot flags and model IDs are research-based and may need verification against the live CLI.

#### cli-external-orchestration/z_archive/005-cli-self-invocation-guards — Self-invocation guards strengthened
A documentation-only pass added and unified anti-self-delegation guards across cli-claude-code, cli-gemini, cli-codex, and cli-copilot, pointing each runtime back to its native capabilities instead of a circular delegation loop. Purely internal tooling; guards remain documentation-driven with no runtime detection logic.

#### cli-external-orchestration/z_archive/006-cli-skill-improved-prompting — Two-tier prompt-quality mirror cards
Landed a two-tier prompt-quality architecture: one canonical prompt-quality card in `sk-improve-prompt` plus four guard-safe mirror cards under each CLI skill's `assets/`, each loaded always and gating pre-dispatch prompt checks. High-complexity prompts now escalate to a shared `@improve-prompt` agent instead of inlining the full skill, and `/prompt` gained Inline vs. Agent dispatch modes. A drift-check script hashes the framework-selection table across canonical card and mirrors to keep them in sync.

#### cli-external-orchestration/z_archive/007-cli-codex-tone-of-voice — Codex voice addendum via AGENTS.md
Delivered a user-global Codex voice addendum scoped strictly to Voice, Tone, and Reasoning Visibility, version-controlled at `<repo>/.codex/AGENTS.md` and symlinked from `~/.codex/AGENTS.md` so every Codex CLI session loads it. The originally planned in-skill assets were removed after a three-step pivot (ADR-006/007/008) because orchestrator delegations govern their own voice and the global file supersedes the app snippet. It never overrides the project-level framework, and orchestrators are explicitly barred from injecting voice content into delegations.

#### cli-external-orchestration/z_archive/008-cli-opencode-creation — Fifth cli-opencode sibling skill shipped
Created the fifth CLI sibling skill with the canonical 9-file blueprint, wired it into the advisor graph via JSON + SQLite sibling edges at 0.5, and shipped changelog v1.0.0.0 plus 10 README patches. Self-invocation protection uses a layered env/ancestry/lockfile guard, and discoverability relies on explicit_author intent signals rather than a TOKEN_BOOSTS boost on the noisy "opencode" token.

#### cli-external-orchestration/z_archive/009-cli-testing-playbooks — CLI manual-testing playbooks
Five CLI orchestrator skills now ship matching `manual_testing_playbook/` packages totaling 129 per-feature scenarios across 40 numbered categories, all built via `@write` dispatch and validator-clean, so operators can validate any CLI end-to-end without inventing scenarios. Cross-CLI category invariants let sibling comparison read consistently. Purely internal validation tooling; a 5-iteration deep review surfaced P0/P1 findings that were closed through cross-cutting and per-CLI remediation passes.

#### cli-external-orchestration/z_archive/010-cli-opencode-providers — Provider realignment + auth pre-flight
Removed `github-copilot` as a documented cli-opencode provider, promoted `opencode-go/deepseek-v4-pro --variant high` to the default, and preserved the direct DeepSeek API surface unchanged. Added a one-shot Provider Auth Pre-Flight protocol with a 3-state decision table whose fallback policy ASKs the user before substituting a model — never silently swaps — with auth-error cache invalidation on mid-dispatch `401`s.

#### cli-external-orchestration/z_archive/011-cli-skill-patterns — Sibling CLI skills harmonized
Harmonized the four cli-claude-code/codex/copilot/gemini skills onto the §3 structural surface they lacked versus cli-opencode (post-051): Provider Auth Pre-Flight, Default Invocation block, and an Error Handling table for cli-copilot. Each sibling keeps provider-native flags, agent-routing syntax, and its unique value props, verified by a 33-term do-not-collapse grep gate with all counts at or above baseline.

#### 012-cli-skill-removal-mcp-clickup — Removed mcp-clickup orchestrator skill
The `mcp-clickup` skill was fully decommissioned: skill folder, changelog, advisor scoring entries, skill-graph nodes/edges, README counts, and stale playbook anchors were pruned, and two SQLite databases cleaned. ClickUp-as-a-service references that flow through Code Mode were intentionally preserved since they describe the upstream tool, not the local skill. **Breaking:** the advisor skill set drops from 22 to 21 and ClickUp prompts no longer route to a dedicated skill.

#### 013-cli-copilot-hallucination-caveat — Copilot routing-trace caveat
Two documentation-only caveats landed in `cli-copilot/SKILL.md` and `sk-doc/SKILL.md` flagging cli-copilot's resource-path hallucination behavior on routing-trace tasks, citing stress-matrix accuracy (cli-codex 66.7% vs cli-copilot 11.1%) and recommending cli-codex as the preferred default. The cli-copilot caveat is tightly scoped to avoid a blanket "don't use" misreading. Purely internal tooling — no code or routing behavior changed.

#### 014-cli-copilot-deprecation-due-to-price-hike — cli-copilot total deprecation
cli-copilot was physically deleted repo-wide (303 files, ~10.9k deletions) after the GitHub Copilot price hike made it uneconomical versus cli-codex (gpt-5.5): skill tree, hooks, matrix adapter, test files, advisor scoring, sibling peer references, and the `buildCopilotPromptArg` executor path. The advisor now routes Copilot-shaped prompts to cli-claude-code, and the `copilot` concurrency-override memory is preserved as historical context. **Breaking:** cli-copilot is no longer installable or routable in any runtime.

#### 015-cli-opencode-stdin-fix — cli-opencode stdin-redirect fix
Appended `</dev/null` to all four deep-research/deep-review YAML dispatch workflows and generalized the rule across the cli-opencode skill docs plus its Barter mirror, fixing non-interactive dispatches that hung at 0% CPU with zero output for minutes. A PONG smoke test confirms the fix (16s vs. 12+ min hangs), and a full 10-iteration deep-research loop completed in ~50 min after previously burning ~3 hours on hung attempts. Purely internal tooling.

#### 016-cli-devin-creation — New cli-devin family skill
Shipped `cli-devin`, the fifth `cli-*` family skill, letting any assistant dispatch tasks to Cognition's Devin for Terminal CLI as a peer executor with the shared orchestration contract and a permission-mode risk taxonomy. Its unique local-to-cloud handoff is documented with a 5-check operator-confirmation gate, and all four sibling skills' graph metadata now list it bidirectionally. Framework users gain a peer dispatch path plus the only async cloud-handoff capability in the family.

#### 017-cli-devin-effectiveness-improvements — cli-devin deep-loop effectiveness
cli-devin v1.0.4.0 makes deep-loop iteration recipes mandate `sequential_thinking` via both MCP registration and system instructions, adds agent-config recipes with a deliberately narrow Write scope for defense-in-depth, and hardens the deep-research/review YAML dispatchers with timeouts and tool-rejection detection. The improvements apply only to future deep-loop runs, not in-flight ones. Purely internal tooling.

#### cli-external-orchestration/z_archive/018-cli-devin-prompt-quality — Data-driven RCAF prompt defaults, cross-model
A seven-phase arc replaced cli-devin's anecdote-driven prompt scaffolding with empirically optimized defaults. A bespoke eval loop ran 42 real SWE-1.6 dispatches and found the RCAF framework (Role/Context/Action/Format) beats STAR by 33% (0.5796 vs 0.4357); a 35-dispatch re-run confirmed the ranking, and a 70-dispatch cross-model run on deepseek-v4-pro and kimi-k2.6 validated both bundle-gate-aversion and framework-dominates-anti-hallucination hold across frontier models. RCAF became the cli-devin SWE-1.6 default and medium-density pre-planning was propagated to the sk-prompt master card and four sibling CLI prompt-quality cards.

#### cli-external-orchestration/z_archive/019-cli-opencode-minimax-optimization — MiniMax 2.7 provider, benchmarked frameworks
Added the MiniMax direct-API provider (`minimax-2.7`) to cli-opencode, the shared small-model registry, and the sk-prompt-models sentinel, followed by a 10-iteration efficiency deep-research. A 5-framework benchmark on real MiniMax M2.7 (via the ported eval rig) crowned TIDD-EC with dense pre-planning, which was integrated into the dispatch path. A follow-up switched the default dispatch to the Token Plan provider `minimax-coding-plan` (MiniMax-M3-highspeed default, M2.7-highspeed fallback) while keeping the pay-per-token Direct API selectable.

#### cli-external-orchestration/z_archive/020-cli-opencode-mimo-pro-optimization — MiMo-V2.5-Pro provider, COSTAR default
Wired the Xiaomi Token Plan (Europe) provider `xiaomi-token-plan-ams` and model `mimo-v2.5-pro` into cli-opencode, the shared registry, and the sk-prompt-models sentinel as an explicitly-selectable path. Removed the hard-coded `--agent general` from the deep-skills dispatch paths so MiMo and MiniMax token-plan models run cleanly as cli-opencode executors (fixing the opencode 1.15.13 subagent-fallback warning). A benchmark found COSTAR with lean-to-medium pre-planning the winner (RACE a statistical tie) and, notably, guardrail-heavy TIDD-EC ranked last — MiMo being the opposite of MiniMax.

#### cli-external-orchestration/z_archive/021-cli-gemini-deprecation — Gemini surface removed end-to-end
Eradicated the deprecated Gemini surface in four phases: deleted the checked-in `.gemini/**` runtime and `cli-gemini` skill, cleaned orphaned executor references from the command-layer YAMLs and docs, purged `cli-gemini` executor wiring from skill source/tests/manifests/catalogs/changelogs, and removed Gemini as a host runtime and as a model across the framework. **Breaking:** `cli-gemini` dispatch, the `gemini-cli` runtime detection, and `gemini-flash`/Gemini model references are gone; only the external Gemini-CLI binary state in the user's home is preserved.

#### cli-external-orchestration/z_archive/022-cli-devin-deprecation — cli-devin removed framework-wide
Removed the cli-devin skill and all references across the framework in six phases: re-homed its shared assets to sk-prompt-models, stripped cli-devin from executor config/audit/fanout and model-benchmark dispatch, dropped the swe-1.6 model and Devin hooks/runtime enum, cleaned agent/doc/governance surfaces, audited the historical record, and deleted the `cli-devin` skill directory (~70 files). **Breaking:** `cli-devin` dispatches, swe-1.6 entries, and the Devin runtime/hook surfaces no longer exist; narrative and benchmark history are preserved.

#### cli-external-orchestration/z_archive/023-kimi-k2-7-code-support — Kimi K2.7 Code registered and tuned
Registered Kimi K2.7 Code (`kimi-for-coding/k2p7`) as a first-class small model across cli-opencode, the shared registry, and sk-prompt-models. A prompt-framework bakeoff first saturated into an inconclusive TIE, then a strict adversarial re-run separated the field and promoted COSTAR (with TIDD-EC fallback) while retiring the old RCAF default. Also documented K2.7's broad-scope over-exploration → 600s-timeout → 0-byte failure mode with its mitigation (hard read-cap + 1200s timeout), repaired stale kimi-k2.6 references, and aligned sk-prompt-models filenames to the house underscore convention.

#### cli-external-orchestration/z_archive/024-glm-5-2-support — GLM-5.2 registered and empirically tuned

GLM-5.2 is now a first-class small model across `cli-opencode` and `sk-prompt-models`: dispatchable via `zai-coding-plan/glm-5.2` (or `glm`/`zai` aliases) on the Z.AI GLM Coding Plan subscription, with stale `glm-5.1` references reconciled. Benchmark 008 empirically picked **COSTAR** as primary prompt framework (TIDD-EC fallback, avoid RCAF) over 45 real dispatches, and that verdict replaced the placeholder in the registry, model profile, and quality card. Native vision-to-code is proven via the direct Coding Plan multimodal API (base64 `image_url`), since opencode `--file` is broken for this provider (#20802); dispatch caveats (6–161s latency, ~1/45 transient failure, cost 0) are documented. Internal tooling/skill-surface work; end users gain a new small-model dispatch option.

#### cli-external-orchestration/z_archive/025-cli-codex-deprecation — cli-codex retired from active routing

`cli-codex` was removed from all active OpenCode surfaces: the skill package moved to `z_archive/cli-codex-retired/`, the raw executor kind was dropped from deep-loop runtime, and generic Codex hook/runtime/config references were purged from hooks, commands, advisor, README, and active skill paths (guided by AI Council). Historical specs and changelogs stay intact, and a follow-up amendment restored a minimal `.codex/` + `.agents` symlink bridge for the Codex Desktop app without resurrecting delegation. Verification is extensive (typechecks, matrix/advisor/runtime tests, advisor probe, alignment sweeps), with known limitations including a required OpenCode restart and two hanging script-level council tests. **Breaking:** no more `cli-codex` CLI delegation or Codex hook support — restart required for the retired skill and regenerated graph to load.

---

## Documentation (sk-doc)

#### sk-doc/014-sk-doc-parent — Parent-hub conversion and fleet-wide alignment
The 6-mode sk-doc monolith became a workflow-only parent hub dispatching to self-contained create-* packets, following the two-axis pattern proven by sk-code/sk-design. The rollup covers a 4-iteration deep review of all ten sub-skills, canonical router-template alignment across 18+ router skills, the `doc-quality`→`create-quality-control` rename (~104 references, `/doc:quality` preserved), shared-reference flattening, `/create:*` command coverage (3 added, 4 renamed), template conformance for all 10 routers and 24 agents, asset-subfolder flattening, and a parent-hub logic review (18 findings, 1 P0) with an 8/9-work-unit remediation shipped. **Breaking:** four `/create` commands were renamed to `/create:<packet>` (e.g. `sk-skill`→`skill`, `folder_readme`→`readme`).

#### sk-doc/015-hub-doc-conformance-fixes — Hub-doc conformance review and plan
A bounded deep review of the `cli-external` and `mcp-tooling` hub docs returned a FAIL verdict — 67 distinct P0 findings after dedup — with every reality claim (CLI flags, MCP tools, transport/auth, links) checked against live CLI/MCP truth rather than assumed. The planning packet turned those findings into a collision-free four-work-stream remediation plan (ClickUp, cli-opencode/cli-claude-code, Figma/Chrome DevTools, cross-cutting) with a verify-first protocol and a doc/routing scope boundary. Review and planning only; no hub doc was edited yet — execution awaits fix-agent dispatch.

#### sk-doc/016-create-diff-mode — Local document diff engine
Introduced a portable, local-first AI document diff engine surfaced through a new `create-diff` mode in sk-doc, for comparing before/after documents without git and producing a self-contained local HTML report. Research is complete (TypeScript on Node 22+, semantic/structural comparison with explicit fidelity warnings), and the create-diff mode v1 is delivered as a self-contained embedded engine (ADR-003) with binary/unsupported refusal and a redesigned code-diff report. Later phases — HTML/DOCX/PDF adapters, lifecycle CLI, OCR, quality gates — are planned but not yet implemented.

#### sk-doc/017-fenced-code-structure-parity — Fenced-code template parity fix
Four authoring templates (install guide, parent-skill hub scaffold, skill asset, and the sk-git PR template) were swallowing their own sections because example fences wrapped nested 3-backtick fences. The malformed outer fences were widened to 4 backticks so inner fences stay literal and every real section parses correctly again; fence-aware scans and `validate_document.py` all pass. Purely internal authoring-template fix.

#### sk-doc/018-benchmark-authoring-and-validation-fixes — Benchmark authoring centralization and hardening
create-benchmark became the single home for benchmark-document authoring across all families: skill- and model-benchmark guides/templates authored, a six-family disambiguation table, per-family resource reorganization with 136 consumer references repointed, a Lane A (agent-improvement) guide, and a bidirectional link mesh to the deep-loop lanes. Audit remediation also landed two operator-approved runtime changes — the skill-benchmark D5 structural gate now hard-fails and deep-alignment's alignment budget is capped — followed by registry-gate wiring, a repaired benchmark suite (56/0), and a repaired sk-design command-surface validator. **Breaking:** `run-skill-benchmark.cjs` now exits with code 3 on `BLOCKED-BY-STRUCTURE`/`BLOCKED-BY-REGISTRY` verdicts instead of 0.

#### sk-doc/019-skill-routing-refactor — Fleet-wide routing correctness refactor
A phased program improving routing correctness across the skill fleet. Deep research falsified the suspected sk-doc alias gap (113/113 aliases match), tracing its 20/100 benchmark score to an undefined handoff between two coordinate systems; system-skill-advisor research found confidence dominated by a 0.82 policy floor with three P0 defects; typed-pair research covered sk-design, system-code-graph, sk-prompt, and sk-code. Fix phases delivered a 14-row router audit map, P0 collision fixes (existing-document quality now owned by create-quality-control), P1 trigger scoping with exact sibling handoffs, P2 router standardization with zero source-vs-projection drift, and explicit Smart Routing notes for all ten create-* packets. Mostly internal routing/tooling; system-deep-loop routing research remains in progress.

#### sk-doc/020-hyphen-naming-convention — Repo-wide kebab-case naming migration
Kebab-case is now the sole canonical filesystem-naming form, superseding the old underscore migration, with a written convention doc and a full exemption boundary (`.py`, vendored trees, generated output, tool-mandated names). Shipping this cycle: fail-closed consumer migration, kebab-case enforcement across the create-* generators, a no-new-snake guard that already caught a leaked name, and a reviewable rename/reference-check/reference-rewrite toolchain proven on disposable fixture repos. **Breaking:** in-scope folder, file and script filenames across the repo are renamed to kebab-case and legacy snake_case names are being retired.

#### sk-doc/021-benchmark-naming-and-playbook-results — One dated benchmark convention
All benchmark run folders now follow the single grammar `<YYYY-MM-DD>--<subject>--<variant>`, produced by a Lane C writer that emits seven files into `benchmark/reports/<dated-run>/` and auto-appends a drift-proof index row. The 78 historical run folders were migrated to the grammar and 6,401 path-shaped references repaired against a frozen map; 1,093 references in research logs were intentionally left historical. **Breaking:** benchmark result paths moved under `benchmark/reports/` and renamed to the dated grammar.

#### sk-doc/022-code-readme-coverage — Code README standard and truth gates
A ten-iteration audit found the code-README problem is truthfulness, not coverage. A new `code_folder` validator mode and a manifest-driven auditor make the standard mechanically checkable, truth gates reject broken path references and stale numeric counts, and orientation READMEs document previously undiscoverable authored/generated boundaries. Purely internal tooling.

#### sk-doc/023-feature-catalog-integrity — Widen catalog enforcement
The catalog validator now discovers every present `feature-catalog/` directory, stages the four known backlog packages at WARN, and fails closed on promoted violations instead of exiting 0. Eight new rule surfaces (phantom rows, prose paths, title/description parity, metadata consistency) bring enforcement in line with the standard's eight rules. Purely internal tooling.

#### sk-doc/024-playbook-scenario-coverage — Playbook scenario validator
A node-only operator-contract validator now walks playbook trees without mutation, classifies files by the typed routing-gold signature, derives census counts at runtime, and exits strict-by-default with staged WARN handling. This closes the gap where hand-typed coverage claims had drifted in every hub and several shipped scenarios taught operators to violate a hard repo rule. Purely internal tooling.

#### sk-doc/025-skill-doc-currency — Skill-doc currency repair
Documentation across twelve skill roots describing executors, canon, routing, advisor gates, hook topology and hub surfaces was corrected against HEAD anchors, including durable cli-pi contract notes (print mode exposes only the final message; stdout capped at 20 MB). The work is docs-only apart from one bounded offline smoke expectation; no executor, scorer or user-global installation changed. Purely internal tooling.

#### sk-doc/026-skill-readme-refinement — Skill README template and fleet rewrite
Refined the shared standalone skill README template to v1.9.0.0 (purpose-first pitch, capability sections, HVR enforcement, versioning, 9-item validation checklist) and added a new parent-skill (hub) README template. Wired both into the create-skill workflow and rewrote the whole fleet's READMEs purpose-first on the new standard, including cli-external-orchestration, mcp-code-mode, mcp-tooling, sk-code, sk-design, sk-doc, sk-git, sk-prompt and system-deep-loop, each with a version bump and changelog entry.

#### sk-doc/027-playbook-snippet-template-migration — Playbook snippet template migration
Migrated all 32 manual-testing-playbook routing-gold snippet files to the per-feature scaffold: numbered OVERVIEW/SCENARIO CONTRACT/TEST EXECUTION/SOURCE FILES/SOURCE METADATA sections, prose test execution replacing the 9-column table, and frontmatter trimmed via a blacklist so only proven-dead keys (`category`, `created`, `expected_token_range_*`) were dropped. The snippet template also gained the routing-gold fields it had omitted, and the migration passed the topology gate, package validator and strict validation. Purely internal tooling; the 13 minimal files keep prompt-only execution by design.

#### sk-doc/028-sk-communication-skill — sk-communication skill authored
Added a standalone `sk-communication` skill that routes requests into the communication-projection package while enforcing its invariants: canonical bytes unchanged, privacy-before-ranking, full-projection or safe-native runtime tiers, exact-original failure returns, and content-free telemetry. It passes skill-root metadata, package validation and advisor-routing smoke tests; its one caveat is that the skill and its package ship together on the `skilled/0143` branch.

#### sk-doc/028-sk-create-diagram — sk-create-diagram editorial diagram mode
Forked the diagram-design plugin into `sk-create-diagram`, a new nested sk-doc workflow packet with 27 diagram types, canonical examples, draw.io/Mermaid import and export tooling, and registration as the thirteenth hub mode via `/create:diagram`. A follow-on phase merged ASCII/markdown flowchart capability behind an output-format dial, with `sk-create-flowchart` deprecated in a later phase, and the packet shipped with manual-testing-playbook, feature-catalog, template alignment, benchmark artifacts and strict validation across all 16 phases.

#### sk-doc/029-doc-divider-and-anchor-standard — Divider and anchor documentation standard
Ratified a single bare numbered-H2 documentation standard (no TOC, no nav anchors, `---` dividers between numbered ALL-CAPS H2) after a fleet census found 1,016 of 8,645 files with divider gaps. The validator gained a flag-gated `SKDOC_ENFORCE_STRUCTURE` check, continuity anchors were explicitly exempted, and the fleet normalization and default-on flip were shipped; the one remaining item (T013, flip default-on in a quiet window) is operator-gated. Purely internal documentation tooling.

#### sk-doc/z_archive/001-sk-doc-rename — sk-doc canonical rename and cleanup
Executed the repo-wide canonical `sk-doc` rename: ordered path migration over 17 entries, content migration across 330 files, and removal of stale visual-skill claims from live docs, all closed with remnant-zero checks, symlink validation and strict completion verification. A full forensic artifact trail lives in `scratch/`. Historical spec paths remain discoverable in archived evidence by design. Purely internal tooling; no breaking surface changes.

#### sk-doc/z_archive/002-sk-doc-template-folders — Template storage reorganized into skill/agent groups
Reorganized sk-doc template storage from a single `assets/opencode/` directory into purpose-grouped `assets/skill/` (skill templates) and `assets/agents/` (agent/command templates) directories, then swept path references across 26 active files (sk-doc internals, create commands, install guides, agents). The old `assets/opencode/` folder was deleted and all functional references verified clean. Purely internal tooling: load paths for `/create:*` surfaces now resolve to the grouped layout.

#### sk-doc/z_archive/003-sk-doc-feature-catalog-testing-playbook — Unified catalog/playbook docs contract
Established a single documentation contract for feature catalogs and manual testing playbooks, converting `system-spec-kit` and `mcp-coco-index` playbooks into integrated root-playbook packages and aligning shipped feature-catalog examples to common frontmatter/header conventions. sk-doc gained dedicated template bundles under `assets/documentation/` plus new standards/workflow references regrouped into `references/global/` and `references/specific/`, with downstream create-command and agent consumers updated to match. Purely internal tooling: docs authors get one consistent contract and discoverable templates.

#### sk-doc/z_archive/004-sk-doc-readme-hvr-improvements — Expanded HVR rules and README guidance
Upgraded the sk-doc standards stack with expanded HVR guidance in `references/global/hvr_rules.md`, targeted README template updates, and a new `references/specific/readme_creation.md` workflow guide, with routing and cli-codex follow-up notes recorded in the respective SKILL files. A packet repair pass also replaced stale shorthand references with committed repo paths and restored Level 2 template compliance. Purely internal documentation tooling.

#### sk-doc/z_archive/005-sk-doc-changelog-template — Changelog template centralized in sk-doc
Relocated the canonical changelog/release-notes template from the create-command asset folder into `sk-doc/assets/documentation/` next to all other documentation templates, repointed the `/create:changelog` command surfaces, spec-kit nested-changelog reference, and sk-doc README to the new path. The sk-doc Smart Router gained a `CHANGELOG` intent so the template surfaces on changelog/release-notes requests. The user-facing `/create:changelog` command is unchanged; only the internal slash-command asset surface `/create:assets:changelog_template` was removed.

#### sk-doc/z_archive/006-sk-doc-agent-template-alignment — Agent template aligned to production fleet
Rewrote `.opencode/skills/sk-doc/assets/agents/agent_template.md` (stale 692 lines → 765 lines) to encode current production patterns drawn from the live 10-agent fleet (`code`, `context`, `debug`, `deep-research`, `deep-review`, `improve-agent`, `improve-prompt`, `multi-ai-council`, `orchestrate`, `review`), including BINDING/REFUSE contracts, mcpServers, runtime budgets, and hook-injected context routing. Removed stale retired-agent references so new agents no longer inherit outdated patterns. Note: only 063b landed; packet completion (~70%) depends on remaining 063a promotion/commit scope.

#### sk-doc/z_archive/007-sk-doc-organization — Templates promoted, paths mirrored across runtimes
Reorganized sk-doc assets by promoting the four heavy-traffic templates (`agent_template.md`, `command_template.md`, `feature_catalog/`, `testing_playbook/`) to the `assets/` root and deleting the empty `assets/agents/` subfolder, then propagated the new paths across 24 canonical `.opencode/` files and mirrored them to four runtimes (.claude/.codex via symlink, .gemini and .codex TOML regenerated, agent mirrors byte-identical). Shipped clean after independent Opus verification plus a 7-iteration deep-review, with three surgical doc fixes closing residual stale-path references. Purely internal tooling: template load paths are shorter and uniform across all runtimes.

#### sk-doc/z_archive/008-sk-doc-router-stress-test — sk-doc smart router stress test
Ran a 45-cell stress test of the sk-doc smart router: 15 authored playbook scenarios (SD-001..015 across 5 categories) × 3 CLIs (cli-codex/copilot/opencode), executed via `run-matrix.sh` and synthesized into `matrix.csv` + `review-report.md`. A methodology bug surfaced mid-run — imperative prompts caused real file side-effects — and was fixed by reflective-framing prefixes, with a clean re-run confirming zero side-effects. Verdict was CONDITIONAL→REMEDIATE_AND_SHIP with the headline that cli-codex leads for sk-doc routing (67% intent / 66.7% resource accuracy). Purely internal tooling: router test infrastructure and benchmark evidence, no router code changed.

#### sk-doc/z_archive/009-sk-doc-router-rerun-refined-extraction — Refined extraction surfaces copilot hallucination
Re-ran the 071 extraction with a refined v2 extractor (`extract_metrics_v2.py`), adding negative-segment filtering and basename matching, then read-only-consumed the same stored logs to hold inputs constant. The fairer matcher surfaced a new finding: cli-copilot actively hallucinates resource paths, citing files like `dqi_rubric.md` that do not exist on disk, while codex and opencode stick to real paths. Corrected numbers confirmed 071's qualitative ranking (cli-codex > cli-opencode > cli-copilot). Purely internal tooling; no router code changed.

#### sk-doc/z_archive/010-sk-doc-missing-router-intents-bullet-aware-matrix — Router intent coverage gap closed
Added SD-016 (OPTIMIZATION) and SD-017 (INSTALL_GUIDE) to the playbook, closing the last intent-coverage gap so all 11 RESOURCE_MAP intents are exercised. A bullet-aware v3 extractor re-scored all 17 scenarios × 3 CLIs (51 cells), confirming 072's per-CLI ranking (codex 70.6% > opencode 47.5% > copilot 9.8% resource accuracy) and reproducing the cli-copilot hallucination pattern on both new intents (0% accuracy, 5 false positives each). Re-extracting stored 071 logs avoided a ~30-minute re-run. Purely internal tooling.

#### sk-doc/z_archive/011-sk-doc-skill-readme-and-structure — Agent rename, README asset, coverage
Restructured sk-doc's authoring surface across five phases: relocated creation references to the root `references/`, added a dedicated skill README template wired into SKILL_CREATION, renamed the documentation agent identity from `create` to `markdown` across all runtime mirrors while preserving the `/create:*` command family, added a `06--agent-dispatch/` playbook section exercising `@markdown` across three CLIs, and closed all 16 deep-review findings via a single cli-codex dispatch. The dispatch-coverage scenario (SD-019, cli-codex `@markdown`) was documented as an accepted non-interactive limitation rather than fixed. **Breaking:** any references to the `@create` agent identity must now use `@markdown`.

#### sk-doc/z_archive/012-sk-doc-standards-and-templates — De-numbered catalogs and playbooks
Changed the sk-doc standard so feature catalogs and manual testing playbooks no longer number per-feature snippet filenames — packages now use `NN--category-name/feature-name.md`, with ordering defined by the root listing rather than filename. The two creation references, four templates, two `create:*` command docs, and their YAML assets were aligned; the validator needed no logic change since it keys off the `NN--` category folder. Authored by MiMo as a read-only dispatch (zero cost), applied deterministically, and adversarially reviewed PASS by DeepSeek. Applies to newly generated packages; the ~1,531 existing numbered files are not yet renamed (gated on a later migration phase).

#### sk-doc/z_archive/013-sk-doc-readme — sk-doc README narrative rewrite
Rewrote the sk-doc README in narrative voice, leading with the structure-first quality pipeline (DQI scoring, scripts-versus-AI split) and adding an at-a-glance table plus worked QUICK START examples. Every drifted count and the stale version line were dropped so the README stays evergreen; it now passes its own `validate_document.py` at zero issues, making it exemplary for the doc-standards skill. Purely internal documentation cleanup, with the rewrite drafted by DeepSeek and MiMo as read-only seats and verified by the host.

---

## Design (sk-design)

#### sk-design/001-design-foundations — Reconstruct foundations static-system spec packet
The foundations mode's packet was absent from git and memory, so this adds a Level-2 reconstruction documenting the source-defined static visual system contract: color, type, spacing, layout, hierarchy, grids, responsive adaptation, theming, and tokens. It records the Read/Glob/Grep-only reference-base execution contract, at-most-one procedure-card selection, the role-first token workflow, and the shared sk_code_handoff field ownership, while preserving boundaries against interface, motion, audit, md-generator, and sk-code. This is a draft documentation packet only — it adds no runtime behavior and claims no generated metadata.

#### sk-design/002-design-interface — Reconstruct interface direction and voice packet
This Level-2 reconstruction restores the interface mode's missing packet, capturing its ownership of overall visual direction, voice, information architecture, component direction, composition gates, and interface writing. It records register-first calibration with pinned-axis precedence, anti-default critique, the one-signature rule, conditional routing and resource loading, at-most-one private procedure selection, and the required sk-code build-manifest handoff fields. It remains a best-effort draft and claims no pre-flight, render-fidelity, or runtime verification.

#### sk-design/003-design-motion — Reconstruct motion temporal-layer contract packet
This Level-2 reconstruction restores the motion mode's missing packet, documenting it as the sk-design temporal layer with the restraint gate, motion budget, and named duration/easing tokens mapped to the source timing bands (100–150ms, 200–300ms, 300–500ms, 500–800ms). It covers choreography, interaction feedback, AnimatePresence and presence patterns, performance safeguards, reduced-motion alternatives, and the motion-owned stack-boundary handoff to sk-code. It is a draft documentation packet and claims no measured or executed results.

#### sk-design/004-design-audit — Reconstruct audit QA and critique packet
This Level-2 reconstruction restores the audit mode's missing packet, documenting the evidence-backed, findings-first design QA and critique contract: P0–P3 severity, five-dimension `/20` scoring, accessibility and performance gates, anti-pattern and AI-fingerprint checks, and production-hardening probes. It makes the Read/Glob/Grep-only, no-fix surface explicit and keeps the two deterministic check scripts as downstream-only, with accepted findings handed to sk-code. It is a reconstruction draft that records the contract rather than a target-specific audit verdict.

#### sk-design/005-design-md-generator — Reconstruct md-generator extraction pipeline packet
This Level-2 reconstruction restores the md-generator mode's missing packet, documenting the measured extraction pipeline that crawls a live site via Playwright across five viewports, writes `tokens.json` first, and builds a v3 DESIGN.md with deterministic value sections and a FACTS block. It records stability classification into L1/L2/L3/L4 token layers and the validator's dual value-and-claims score contract (both ≥80, no critical failures) with provenance checks. It is a draft documentation packet that asserts the contract without claiming that an extraction or validation run occurred.

#### sk-design/006-design-mcp-open-design — Reconstruct Open Design transport packet
This Level-2 reconstruction restores the design-mcp-open-design transport mode's missing packet, documenting the `od` CLI and desktop daemon model, WIRE/READ/RUN intent routing, and the mandatory sk-design ground → token-system → critique pairing for any design-bearing work. It records the guarded reads and writes with explicit targets and rollback requirements, deny-by-default classification, freshness and inner-generator binding rules, and the gated multi-turn generation lifecycle from discovery form to `entryFile` and `previewUrl`. It is a draft documentation packet; the transport stays non-mutating for the repository and live `tools/list` verification remains a prerequisite before trusting any tool name or mutability.

#### sk-design/007-design-hub-routing — sk-design hub routing reconstruction
Reconstructs the sk-design hub's routing contract as a spec-only Level-2 draft: one advisor identity, six registry modes, router policy, signal/vocabulary classes, and transform-verb routing, all traced to the intact SKILL.md, mode-registry.json, and hub-router.json. Documentation-only; nothing was implemented and no runtime behavior changed.

#### sk-design/008-design-shared-backbone — sk-design shared backbone reconstruction
Reconstructs the shared backbone — register and context loading, proof gates, dispatch/token contracts, procedure-card schema, shared scripts, benchmark evidence, and the feature catalog — as a spec-only Level-2 draft grounded in the intact shared, benchmark, and feature_catalog trees. Documentation-only; it records source-defined boundaries without adding behavior.

#### sk-design/009-sk-design-claude-parity — Claude parity phased refactor
A twelve-phase refactor moved sk-design toward Claude Design-like behavior while preserving OpenCode-native routing and the single advisor identity: a manager-style hub shell, a private 14-card procedure layer, mode-packet refactors with per-mode proceduresPath, routing-vocabulary optimization, README and feature-catalog completion, manual-testing playbook alignment, and canon verification. A router-mode parity benchmark gate closed with a CONDITIONAL verdict, naming live/browser scenarios as operator follow-up, and the expanded routing-benchmark battery was formally descoped with accepted risk. Not breaking: modes kept authority, routing and the md-generator mutating boundary are unchanged.

#### sk-design/010-sk-design-styles-from-refero — Refero styles library extraction
A resumable Node harness extracted all 1,290 Refero styles (four tabs per style) into the sk-design local token library using the six-file template, after a 50-style pilot returned GO with 0 errors. The complete library — shape-validated 1,290/1,290 and indexed — lands at ~129 MB across 7,744 files, a new local design-evidence corpus for the modes.

#### sk-design/011-sk-design-styles-utilization — Styles library utilization
Deep-research phases decided how the 1,290-style library should be consumed, then the research was built: a retrieval substrate (style-library.mjs, eligibility-first selection, CORPUS_USE_PROOF v1 gate), the design-md-generator v3 schema contract plus bounded STUDY exemplars with a source-leak gate, a shared corpus-context seam enforcing a fixed authority order, interface/audit pilots, foundations/motion wiring, and an Open Design transport grounding-receipt layer. Every phase passed its test suite after adversarial reviews (17/17, 149/149, 162/162, 28/28, 36/36, 43/43, 25/25).

#### sk-design/012-sk-design-program — sk-design program umbrella
Umbrella program over the whole sk-design body of work: research converged on a SQLite+FTS5+vector style database, a shared 9-stage /interface:* creation-command contract, and a "no Rust now" verdict plus an MIT-clean Hallmark reuse analysis. Shipped the persistent style database — first full-corpus 1,290-style generation with shadow parity and ~21x measured speedup, cutover human-gated with the legacy default kept — and restructured the library into an ownership tree (kebab-case rename, byte-parity manifests, database git-ignored). Roadmap phases (JS capabilities, measured native, growth) and the style-DB default flip remain planned; the restructure is internal tooling.

#### sk-design/012-style-database-and-interface-commands — No source documentation
The folder was delivered with no spec docs ("no docs" marker only), so no grounded change summary can be written. Nothing is invented; treat this packet as having no shipped narrative for this cycle.

#### sk-design/013-structure-naming-cleanup — Structure, naming and YAML conformance cleanup
Removed the stray `styles/docs/` folder, renamed six dunder test/fixture directories to kebab-case and repointed every live reference, and conformed the ten `/interface` command workflow YAMLs to the create-command command-standard vocabulary (operating_mode, confidence_framework, gate logic, etc.). Per-mode manual-testing playbooks were deliberately kept once evidence showed per-packet playbooks are the codebase-wide norm. Purely internal tooling/hygiene; the `/interface` commands' runtime behavior is unchanged.

#### sk-design/014-template-conformance — Template conformance audit and fixes
A multi-child program auditing every folder in every sk-design mode against the sk-doc create-skill templates. Shipped work includes honestly de-vendoring design-interface's Apache-2.0 design guidance (re-authored in first-party words, license and citations removed), fixing structural defects in 11 `references/` and 3 `assets/` files, repairing a real `sys.path` bug that broke two checker scripts, correcting a recurring `feature_catalog.md` underscore typo in 4 post-merge files, and resolving open questions (procedures/corpus confirmed already conformant, README frontmatter exempt). Mostly internal hygiene, with a changelog-discipline clarification for sk-doc's nested changelog templates.

#### sk-design/015-deprecate-open-design — Deprecate the Open Design MCP transport
Deprecated the Open Design MCP transport end-to-end: the 45-file mode tree, `.utcp_config.json` server entry, hub/manifests, agent and command references, deep-alignment live-render adapters, and sibling-skill and root-doc citations were all stripped, with a clean residue gate and `validate.sh --strict` (exit 0). Historical records intentionally retain mentions for audit trail (ADR-003). **Breaking:** the Open Design MCP transport is removed — consumers must drop the `open_design` server from `.utcp_config.json` and stop referencing `design-generation-patterns.md` and the live-render adapter.

---

## Code (sk-code)

#### sk-code/017-sk-code-parent — Parent hub: modes + surface packets
The flat sk-code skill became a two-axis parent hub at version 4.0.0.0: workflow modes (implement, quality, debug, verify, review) plus read-only surface packets (webflow, opencode, animation), with the standalone review skill folded in as a mode. 128 files were relocated into mode packets, every broken reference was repointed (including restoring the silently-skipped pre-commit hygiene gate), and a deterministic router benchmark rose from 44 to 71. It also shipped the plugin TUI-overlay fix, the parent-hub canon, and the spec-kit workflow relocation. **Breaking:** routing contract and all skill paths changed; skills and references now live under mode/surface packets instead of flat paths.

#### sk-code/018-rust-standards-for-code-opencode — Rust standards + reference-file hygiene
Rust became a first-class language on the code-opencode surface: standard docs (style_guide, quality_standards, quick_reference, checklist, playbook), `.rs`/Cargo detection, child and parent-union routing, and six registration touchpoints, all verified green with a fail-closed router-replay. In the second workstream, 33 oversized reference/asset docs were split into 104 topic-cohesive parts across code-opencode, code-webflow, and code-quality with the router contract rewired in lockstep — 21/21 guards pass with zero regressions. Largely internal tooling; the user-visible gain is Rust-aware routing and shorter, topic-cohesive reference files.

#### sk-code/019-split-doc-template-alignment — Split-doc template alignment
All 163 mechanically-split reference/asset files across code-opencode, code-webflow, and code-quality were conformed to the create-skill templates: snake_case renames, 5-field frontmatter with 4-part version stamps, OVERVIEW wrappers, and renumbered sections with content preserved verbatim. Every in-hub reference to renamed files was updated. A post-ship 10-iteration deep review drove two P1 remediations — de-duplicating 21 code-webflow Purpose paragraphs from their H1 intros and narrowing a broken-link completion claim — plus four structural fixes from xHigh re-verification. Internal documentation hygiene; no user-facing behavior change.

#### sk-code/020-content-quality-remediation — Content-quality remediation
Fixed 20 code-webflow reference files flagged as out-of-scope follow-ups by 019's review: reconciled a self-contradicting session-cookie SameSite example, hardened a CDN loader with an HTTPS origin allowlist and SRI note before `script.src`, and replaced 19 generic "when implementing or troubleshooting" When-to-Use sections with concrete section-derived scenarios. All 20 files validate clean with zero regressions. Internal documentation; the security examples are now sound teaching material.

#### sk-code/021-code-conformance-alignment — Conformance gate repair + drift sweep
In-progress phase parent: the enforcement mechanism for the sk-code code-opencode standard was itself broken — a dead post-edit hook, a completion gate scoped to one skill tree, and manual-only header shaping — and that blindness let roughly 1,400 authored files drift from the file-opening, comment-hygiene, portability, and containment contracts. The program repairs the gate first, then sweeps the drift in blast-radius order; child 001 sits at 80% with open operator decisions Q1–Q5. Purely internal tooling and not yet shipped to completion.

#### sk-code/022-dist-staleness-rebuild-on-drift — Dist-staleness self-heal rebuild
The dist-staleness guard now self-heals a stale checked-out build at session start via a bounded, fail-open `try_rebuild` (kill-switch defaulted on), ending the recurring cross-session hook error. The orphaned `DIST_PACKAGES` entry that crashed the whole `--all` sweep was removed rather than repaired, and the fix was verified with a stale-then-fresh control. Purely internal tooling.

#### sk-code/023-dist-guard-skip-unprovisioned — Dist guard skips unprovisioned packages
The dist-freshness checker now classifies a package it cannot build (missing `package.json` or `node_modules`) as `unprovisioned` with `stale: false`, so `check-dist-staleness.sh` stays silent instead of warning every session. Fresh and genuinely stale packages are unaffected. Purely internal tooling; operators still own provisioning.

#### sk-code/z_archive/001-sk-code-review-creation — sk-code-review promoted to first-class baseline
sk-code-review was renamed and rebuilt as a stack-agnostic review baseline with a baseline+overlay precedence model. It is wired across `@review` runtime docs, orchestrators, Codex agents, 18 review-dispatch YAML assets, and advisor routing. Known caveat: `sk-doc` validators still reject the consecutive-hyphen name.

#### sk-code/z_archive/002-sk-code-opencode-refinement — KISS/DRY/SOLID review routing linkage
One `sk-code-opencode` policy file and three `sk-code-review` alignment files were updated so review checklists and routing explicitly enforce KISS/DRY/SOLID architecture-lens analysis. The closeout stayed locked to four files; already-compliant harmonization targets were closed as verification-only (no net diff).

#### sk-code/z_archive/003-cli-skill-removal-sk-code-merger-deprecated — Removed deprecated web/full-stack skills
The deprecated `sk-code-full-stack` and `sk-code-web` skills were hard-deleted (128 files), with cross-references pruned across ~50 files and three SQLite indexes, and sk-code's placeholder pointers neutralized. **Breaking:** ~36 markdown files of canonical React/Node/Go/Swift guidance were retired by user decision.

#### sk-code/z_archive/004-sk-code-merger — Merged web + full-stack into sk-code
A new umbrella `sk-code` skill smart-routes by detected stack, carrying Webflow content verbatim while non-web stacks surface placeholders pointing at preserved `sk-code-full-stack` content. Advisor scoring and boost tables were retargeted to the umbrella; legacy skills stay on disk with additive deprecation frontmatter.

#### sk-code/z_archive/005-sk-code-fullstack-branch — Three-stack scaffolding: Next.js + Go stubs
`sk-code` now routes Webflow (live), Next.js, and Go, with 43 project-agnostic stub files (23 Next.js, 20 Go) plus sk-doc-aligned router/universal references. SKILL.md §2 was rewritten to the sk-doc smart-router pattern with honest STUB markers. The stubs are scaffolding, not content.

#### sk-code/z_archive/006-sk-code-opencode-merger — Standalone OpenCode skill merged into sk-code
The standalone `sk-code-opencode` skill was absorbed into `sk-code` as the `OPENCODE` surface, with `WEBFLOW` and `UNKNOWN` retained. Detection moved to a two-axis model (Code Surface from CWD + changed files → Intent Classification → per-surface resource loading) with language sub-detection inside OpenCode, replacing Barter's git-remote project routing. Standards and checklists moved under `references/opencode/` and `assets/opencode/`, Go/NextJS placeholder branches were deleted, and the obsolete skill directory was removed. The full 5-phase implement→debug→verify lifecycle now applies to OpenCode work. **Breaking:** the `sk-code-opencode` skill is gone; all OpenCode system-code work routes through `sk-code` and review goes through `sk-code-review`.

#### sk-code/z_archive/007-sk-code-motion-dev-and-playbook — Motion.dev cross-stack category and playbook refinement
A phase-parent packet elevated motion.dev to a first-class cross-stack peer category (not nested under Webflow) and extended the manual testing playbook with animation-regression, cross-browser, and Core Web Vitals gates. Delivery included motion_dev references/assets, Webflow→motion.dev cross-references, deep-review remediation of findings F-001 through F-014, and a cross-CLI playbook execution harness. Two later phases did not reach green: the cross-CLI Codex smoke test and routing-precision rechecks were blocked because nested Codex could not reach the OpenAI API from the sandbox, and a live advisor probe still read stale SQLite routing data.

#### sk-code/z_archive/008-sk-code-authoring — OpenCode authoring checklists and STACK_FOLDERS contract
The OpenCode surface shipped five 6-section authoring checklists (skill, agent, command, MCP server, spec folder) plus a `spec_folder_write` recipe documenting `validate.sh --strict` failure modes. The machine-readable `STACK_FOLDERS` Python-style dict was restored in SKILL.md §2, closing the F-008-001 regression, and sk-code bumped to v3.2.0.0. This closes 9 of 22 P1 findings from the 077 research; the remaining P1s are addressed by later phases.

#### sk-code/z_archive/009-sk-code-scripts-relocation — Root scripts relocated into asset folders
Purely internal reorganization: the root `sk-code/scripts/` folder was removed, with Webflow minification utilities moved to `assets/webflow/scripts/` and the alignment-drift verifier to `assets/scripts/` (classified as generic OpenCode tooling, not Webflow-specific). All references were updated across a 41-file inventory. Git index staging was unavailable in the sandbox, so moves used filesystem operations rather than `git mv`.

#### sk-code/z_archive/010-sk-code-review-playbook — sk-code-review manual testing playbook
An 18-scenario manual testing playbook for `sk-code-review` gives operators a deterministic gate before declaring review-baseline changes safe, spanning baseline flow, security/correctness minimums, evidence discipline, scope/precedence, stale-context re-review, and cross-CLI orchestration. A DQI pass via `@review` returned two P1 findings that were resolved in-orchestrator: broken agent relative paths (mechanical sed fix) and identical Why-This-Matters boilerplate (18 unique per-scenario rationales).

#### sk-code/z_archive/011-sk-code-review-uplift — PR-state dedup and minimum evidence gates
Added two efficiency gates to `sk-code-review` per AI Council §10.3: M-1 content-hash dedup (`sha256(commit_subject + diff_hash)`, JSONL cache per repo-ref) skips re-review when nothing changed, and M-2 is an opt-in minimum-evidence gate (`SK_CODE_REVIEW_MIN_CHANGED_LINES`) with a skip taxonomy of six never-skipped risk classes (security, config, persistence, deps, sandboxing, public-facing responses). Both emit COMMENTED status and never silently approve or block. Specification-only in this phase — no runtime enforcement code shipped, so downstream review dispatch must implement the contracts.

#### sk-code/z_archive/012-sk-code-animation-mining — Animation surface mining, integrated
Mined the MIT `web-motion-skill` via a gpt-5.5-fast dispatch in an isolated worktree and integrated an additive-only delta into sk-code: a new `references/motion_dev/animation_principles.md` design-principle layer and a novel `principled_reveal.js` snippet, wired through `smart_routing.md`. Non-portable recording scripts and GSAP specifics were correctly dropped, and drift guard plus the full suite stayed green. Purely internal skill tooling; nothing user-facing changed.

#### sk-code/z_archive/013-sk-code-review-readme — sk-code-review README contract corrected
Rewrote the sk-code-review README in a narrative voice that leads with the stack-agnostic, findings-first review baseline and the machine-parsable `Review status:` verdict line. Fixed a materially wrong documented output contract (the old ordering, a phantom `Review Context` heading, and a missing status line) and dropped the stale version, reference count, and a non-existent `assets/` path. Purely internal documentation tooling for the review skill.

#### sk-code/z_archive/014-sk-code-readme — sk-code README surface model corrected
Rewrote the sk-code README in narrative voice, leading with the surface-aware router (WEBFLOW/OPENCODE plus UNKNOWN fallback), the five gated phases, and the Iron Law. Corrected a material error that framed MOTION_DEV as a third surface — it is a cross-stack resource intent loaded after detection, per `description.json` — and stopped conflating the four-axis routing model with the five-phase lifecycle. Purely internal documentation tooling for the code-work skill.

#### sk-code/z_archive/015-sk-code-ponytail-based-refinement — Ponytail transplant research
Delivered a read-only deep-research report (12 iterations, two models) ranking 25 recommendations for transplanting ponytail mechanisms into sk-code/sk-code-review, resolving the design-vs-result gate tension and surfacing 3 latent defects in the Iron Law wording. No skill code was written — implementation is deferred to a follow-up plan packet. Purely internal research tooling.

#### sk-code/z_archive/016-sk-code-rituals — Verification rituals (planned)
Plans the Verification Rituals subsection for `sk-code/SKILL.md`: a mutation-check/claim-falsifier, a verification ladder with named blind spots, and a decision-economy/fail-closed doctrine. Status is PLANNED, not built — all checks are defined but pending implementation. Purely internal skill guidance tooling.

---

## Git (sk-git)

#### sk-git/007-continuous-integration-workflow — Parallel-session git autosync pipeline
Added a trunk-following continuous-integration workflow: a new `git-sync.sh` publish primitive (fetch → fast-forward-or-rebase-abort → non-force push), an IDE-side `git-live-follow.sh` fast-forward follower, and a `worktree-status.sh` dashboard. Each commit from wrapper-launched Claude/Codex/OpenCode sessions now autosyncs to one shared live branch behind a triple gate (env + linked worktree + installed hooks), with Codex SessionStart guards wired to match the other runtimes. Purely internal tooling, but it means the operator's IDE checkout now always reflects every concurrent session's committed work.

#### sk-git/008-research-and-requirements — Parallel-session autosync research
Research phase (no code): reconciled a two-lineage deep-research fan-out through a three-pass verification chain into a frozen architecture decision (ADR-001..006). Locks in a four-plane serialized-publisher design with fast-forward-only publishing, a "primary never behind" invariant with its BLOCKING prerequisites, an AI conflict resolver as a bounded proposer, GitKraken MCP as read-only auxiliary, and a testable acceptance matrix for implementation. Purely internal planning foundation.

#### sk-git/009-skill-scoped-worktree-naming — Owner-first worktree/branch naming
Codified the owner-first grammar (`<skill>/{NNNN}-{slug}` or `skilled/{NNNN}-{slug}`) in sk-git, backed by a locked clone-wide allocator/validator (`worktree-naming.sh`), hardened wrapper/reaper, and a migration-tolerant pre-push gate on new remote branch names. Also executed the first cleanup slice (six merged branches) with the remaining per-branch declutter left operator-gated. **Breaking:** existing `wt/`-style branch names no longer conform and new branches must follow the owner-first grammar, though the pre-push gate is deliberately migration-tolerant (fails open on validator errors, bypassable, never blocks `skilled/v*`).

#### sk-git/010-review-remediation-and-alignment — Worktree-tooling hardening
Closed every confirmed adversarial-review finding RED-first: fixed a shared-path traversal in `worktree-session.sh`, added whole-file PID-grammar markers and exact pair matching to the reaper, and root-caused the allocator stale-lock race (non-mutex recovery marker) to an atomic rename-steal lock proven over 100 runs. All four scripts now align with sk-code's sourceable strict-mode doctrine, and 002's over-claims are reconciled. Purely internal tooling hardening; harnesses green and packet validates with Errors 0.

#### sk-git/011-feature-catalog-and-manual-playbook — Catalog and manual-playbook coverage
Documentation packet: authored an 11-feature catalog across four categories and 19 new manual-testing scenarios (`GIT-023`..`GIT-041`) covering allocator/wrapper/reaper/pre-push valid and invalid paths, then registered both in SKILL.md, the README, and a `v1.3.0.0` changelog. Also repaired pre-existing hyphenated playbook paths that disagreed with on-disk names. Docs only; two workflow features honestly stubbed as "no coverage yet."

#### sk-git/012-readme-enrichment-and-hyphen-naming — Hyphen-case rename and READMEs
Renamed 66 files and 13 directories from snake_case to hyphen-case via `git mv` (history preserved) across references, assets, feature-catalog, and manual-testing-playbook, then repointed every reference including the Smart Router (0 missing) and repaired 109 broken cross-links. Rewrote README.md to the create-readme canon and authored four code READMEs. **Breaking:** the documented resource paths move to hyphen-case, leading the repo-wide 017 migration; the snake_case checker warnings are advisory only.

#### sk-git/012-readme-enrichment-and-hyphen-naming — sk-git/012 README and rename
See above; these six folders cover the full v3.6.0.0 → v4.0.0.0 cycle for sk-git: autosync CI, the research freeze, owner-first naming, hardening, documentation, and the hyphen-case migration.

#### sk-git/013-git-review-remediation — Closed sk-git review findings, hardened workflows
Both open GPT-5.6-sol review items on sk-git were closed by four fresh agents with independent orchestrator re-verification: a new AGENTS.md Git Workspace Safety table (ask-first worktrees, owner-first branch grammar, allocate-never-count), allocator hardening for `mktemp` failure and number exhaustion (harness `PASS=47 FAIL=0`), catalog↔references contract single-sourcing, and two new SKILL.md NEVER refusals. Also fixed a cross-skill sk-doc validator hyphen/underscore doc-type detection. The renumber collision with a concurrent sk-git session is flagged for reconciliation before the next workstream.

#### sk-git/014-gitkraken-mcp-integration — GitKraken MCP integrated end-to-end
Registered the GitKraken MCP server (`@gitkraken/gk mcp`) as a Code Mode manual in `.utcp_config.json`, then documented its full 31-tool surface in a new sk-git reference and wired it into SKILL.md's router and ALWAYS rules. Advisor routing now boosts `gitkraken`/`gitlens launchpad` phrases to sk-git via graph metadata and the explicit lane. Verified live with the real advisor MCP tools across all five phases.

#### sk-git/015-remote-branch-policy — Curated remote push permission policy
Origin is now curated by default: only `main`, `skilled/v*`, and allowlist-file entries push without asking; every other push requires a fresh, explicit go-ahead enforced twice — a MANDATORY sk-git rule for agents plus a `pre-push` hook backstop that also covers human pushes. The hook rewrite passes 21/21 scenarios and three sibling harnesses re-ran clean. **Breaking:** any push to a non-allowlisted remote branch now blocks until explicitly approved; in-flight branches like `origin/skilled/0064-*` will hit the gate on their next push.

#### sk-git/016-git-action-advisory-hook — Advisory hook surfaces git rules at command time
A PreToolUse hook now reads sk-git's hard rules and prints the relevant one the moment a git command runs, closing the gap where rules were written down but never reached anyone. Research drove the design: rules are state discriminators, not verb matches (93% of resets are unstage-only), yielding 17 encoded rules with a measured 0-fires-per-25-ordinary-commands budget and verified control-group firing. The shared parser/evaluator is wired into six runtimes — Claude, Codex, OpenCode, Pi, Cursor, Devin — with READMEs and seven manual-testing playbook features; internal tooling that is user-visible only as a command-time advisory (never a block).

#### sk-git/017-config-filter-transparency — Config filter transparency reference
Adds a reference doc explaining the `maintainer-flags` content filter: which four files it maps, which five keys it rewrites, and why the disk/commit divergence is deliberate, pointing at authoritative sources rather than duplicating them. Purely transparency work — the filter itself was left untouched, and advisory coverage on all four mapped files was verified live.

#### sk-git/018-git-subprocess-env-isolation — Git subprocess env scrubbing hardening
Hardened write-capable git test/tool helpers to scrub repository/config-redirecting `GIT_*` variables from their git subprocesses, closing the vector where a poisoned parent environment silently corrupted the shared worktree `.git/config`. A repo-wide sweep extended the fix to 12 helpers across sk-doc and sk-git, including the mutating `git mv` in `rename_engine_core.py`; the root cause was proven by sandbox reproduction, and read-only git helpers were ruled safe and left unchanged. Internal dev-environment hardening; two pre-existing unrelated suite failures are documented as not regressions.

#### sk-git/019-mass-deletion-guard — Fail-open guard against mass file deletion
Purely internal git-hook tooling: a shared guard now blocks commits (live via pre-commit) and pushes (ready, dormant on this machine) that remove more than `SPECKIT_MASS_DELETION_THRESHOLD` (default 100) tracked files, unless the operator authorizes the one operation with `SPECKIT_ALLOW_MASS_DELETION=1`. It is the durable defense against the 2026-08-13 stale-tree `git add -A` snapshot that erased 902 files and runaway-agent mass deletions. 12 verdict-logic and real-commit integration tests pass.

#### sk-git/z_archive/001-sk-git-github-mcp-integration — Corrected GitHub MCP guidance
Documentation-only correction of the sk-git docs: incorrect GitHub MCP examples were normalized to the verified `github.github_*` form, and the local-git vs. remote-MCP tool choice was clarified for PRs, issues, repo reads, and CI/CD. No runtime behavior changed.

#### sk-git/z_archive/002-sk-git-superset-worktrees — Superset worktree alignment (spec only)
This folder shipped only a specification, never an implementation: it proposed adapting sk-git's worktree workflow to Superset IDE's model — centralized storage at `~/.opencode/worktrees/`, `.opencode/worktree.json` lifecycle hooks with a mandatory user-confirmation security gate, structured init flow, and base-branch fallback. The sk-git skill was not updated; the work is resumable from the spec.

#### sk-git/z_archive/003-sk-git-readme — README confirmed as golden example
No code or docs changed: the phase confirmed the sk-git README is still the canonical nine-section reference exemplar — it validates with zero issues, is HVR-clean, and all nine cited paths resolve. A deliberate no-rewrite to avoid regressing the voice standard the packet measures against.

#### sk-git/z_archive/004-commit-standards-and-retroactive-rewrite — Commit standards locked, rewrite unexecuted
Only phase 002 of the five-phase parent actually shipped: seven ADRs locked the canonical commit-message standard (subject taxonomy, packet-ID prefix, trailer policy, tense, body, special cases, length caps), validated as a deterministic rewrite on 20/20 sampled HEAD commits. The remaining phases — prerequisites, skill update, cli-devin rewrite prompts, and the retroactive 2,795-commit rewrite execution — were scaffolded but never delivered, so the planned history rewrite did not happen.

#### sk-git/z_archive/005-sk-git-reorg-hardening — Large-reorg and staging guidance
Documentation-only hardening of the sk-git skill from the 026 wave-4 reorg's five failure modes: worktree caveats plus gitignored-leftover detection (§8b), a scoped-staging discipline that forbids broad `git add` on dirty trees (§3 Step 7), rename-heavy merge verification with `comm -12` disjointness checks and `merge.renameLimit` (§10), and a new step-ordered `large_reorg_playbook.md` whose step 0 snapshots the gitignored DB dir as the only rollback path.

#### sk-git/z_archive/006-sk-git-worktree-convention-and-advisor-routing — Pre-implementation scaffold, no changes shipped

This packet is a pre-implementation scaffold (completion 0%) for two planned sk-git changes: a numbered feature-worktree convention (`wt/{NNNN}-{name}` branches in `.worktrees/{NNNN}-{name}`) and a skill-advisor routing optimization that adds the new vocabulary to sk-git trigger phrases and reduces overlap with system-spec-kit and sk-code. No behavior changed yet; the implementation summary explicitly marks all edits as pending and two in-flight worktrees are deferred. Purely internal tooling — framework users see no impact from this folder's contents in the v3.6.0.0 → v4.0.0.0 cycle.

---

## Prompt (sk-prompt)

#### sk-prompt/007-sk-prompt-parent — Merge into one parent hub
`sk-prompt` and `sk-prompt-models` merged into a single parent hub exposing two workflow modes, `prompt-improve` and `prompt-models`, with the 2,616-file `sk-prompt-models` move done atomically so no live consumer ever pointed at a dead path. `/prompt:improve` was refactored from a 28KB monolith into a thin create-command router with a dispatch-context gate and split auto/confirm/presentation assets. A router-mode Lane-C benchmark proves routing across both modes at PASS 100/100, and strict parent-hub checks pass clean, making `sk-prompt` the fifth canon-clean hub. **Breaking:** `/prompt` is now `/prompt-improve`, and `sk-prompt-models` content moved to `sk-prompt/prompt-models/`.

#### sk-prompt/z_archive/001-sk-prompt-initial-creation — Initial prompt-improver skill creation
The standalone Prompt Improver AI system (v0.200, ~250KB) was ported into OpenCode as the `sk-improve-prompt` skill: a lean 2,334-word SKILL.md orchestrator plus eight adapted reference files covering DEPTH, 11 frameworks, CLEAR scoring, and text/visual/image/video domains. Advisor routing returns 0.95 confidence on prompt-improvement queries. A later audit flagged that only 2 of the 8 reference files actually existed on disk, since visual/creative modes were subsequently removed.

#### sk-prompt/z_archive/002-sk-improve-prompt-rename — Renamed sk-improve-prompt to sk-prompt
A pure semantic rename of the skill from `sk-improve-prompt` to `sk-prompt` across the whole repo, driven by a 58-file canonical inventory that caught hidden runtime mirrors and edge cases. After the change, a five-prompt advisor probe battery returns `sk-prompt` top-1 for every prompt-improvement intent, strict recursive validation passes, and the final grep shows zero old-name hits in active scope. **Breaking:** skill path and all references changed from `sk-improve-prompt` to `sk-prompt`.

#### sk-prompt/z_archive/003-sk-prompt-testing-playbook-and-agent-rename — Agent rename plus testing playbook
The `@improve-prompt` agent was renamed to `@prompt-improver` across all four runtime mirrors (`.opencode`, `.claude`, `.codex`, `.gemini`) plus 35 active-scope reference files, aligning it with the noun-form agent family. A 28-scenario manual testing playbook for the sk-prompt skill was also authored against sk-doc templates with a root index and validation evidence.

#### sk-prompt/z_archive/004-sk-prompt-small-model-optimization — Small-model optimization suite
A deep-research arc mined the external smallcode-master corpus and landed small-model output-quality deltas across cli-* and sk-* skills: a JSON-Schema permissions matrix with default-deny gate, cli-devin context-budget defaults and opt-in output verification, a shared model registry with Bayesian scoring and quota-pool-aware fallback, and cross-skill budget propagation. The sentinel skill created here became `sk-prompt-models` after two identity refactors, surfacing at 0.95 advisor confidence. All new runtime features are off by default and backward compatible.

#### sk-prompt/z_archive/005-sk-prompt-knowledge-layering — Three-layer prompt-knowledge architecture
Prompt knowledge was layered as sk-prompt (framework tables) → sk-prompt-models (per-model prompt-craft hub with nine conformed profiles) → cli-* executors (lean delegating mirrors). A `recommended_frameworks` field landed for all eight models, the five CLI prompt-quality cards were thinned from ~90-line inline duplicates to pointers, a 3-tier prompt-composition precedence rule was wired into every cli-* SKILL.md, and the C1–C10 seam-drift backlog from deep research was closed with a CI-wired duplication guard so the layering can't silently regress. This is internal tooling; no user-facing behavior changed.

#### sk-prompt/z_archive/006-sk-prompt-models-rename — Rename sk-prompt-small-model to sk-prompt-models
Renamed the skill `sk-prompt-small-model` → `sk-prompt-models` via `git mv` (history preserved) and swept the full ~799-file footprint: the skill's own identity and back-links, 8 referencing skills plus code/config, deep-workflow benchmark YAMLs, agents, CI, README, and 669 spec files (123 historical files correctly left frozen). The advisor index was regenerated so routing now resolves the new name (0.95 confidence), and a broken changelog symlink plus 2 playbook filenames were repaired; remediation also fixed 5 pre-existing skill-graph symmetry failures and re-indexed spec memory. **Breaking:** any reference to `sk-prompt-small-model` must be updated to `sk-prompt-models`.

---

## MCP Tooling

#### mcp-tooling/007-mcp-tooling-parent — mcp tool bridges unified under one hub
Brought `mcp-chrome-devtools`, `mcp-click-up`, and `mcp-figma` under a single `mcp-tooling` parent hub (two workflow bridges plus one figma transport), relocating every tree with `git mv`, unifying the advisor graph identity, and repointing all functional referrers. The hub became the sixth canon-clean parent, gained inventory parity (feature catalogs, install front doors, worked examples), and shipped blind holdouts plus labeled routing corpus for all six modes. The deferred Lane-C benchmark passed (95, route-blind), but an independent deep-review returned release-blocking findings (3 P0, 10 P1, 2 P2) routed to a separate remediation packet. Purely internal framework tooling.

#### mcp-tooling/008-mcp-aside — Aside browser bridge added as fourth mode
Added `mcp-aside-devtools` as the hub's fourth mode, a workflow bridge using the agentic `aside` CLI with an MCP server fallback. Research fan-out, a 41-file packet, hub registration, and green program-wide gates were followed by a doc-truth sweep (registration claims corrected) and a live discovery fixture that confirmed the `aside.aside_repl` callable and corrected the packet's registry-name prediction by one separator. Purely internal framework tooling.

#### mcp-tooling/009-mcp-refero — Refero design transport added as fifth mode
Added `mcp-refero` as the hub's second read-only design-reference transport (fifth mode), researched via deep fan-out, authored as a 29-file transport packet, and registered across every hub surface with transport pairing rules generalized to cover both design transports. Inventory parity and an sk-design de-duplication landed, and live discovery closed the doubled-prefix naming dispute with registry evidence (`refero.refero.refero_<tool>`). Purely internal framework tooling.

#### mcp-tooling/010-mcp-mobbin — Mobbin design research transport added as sixth mode
Added `mcp-mobbin` as the hub's third read-only design-research transport (sixth mode), completing the three-mode expansion program. Registration added the UTCP manual byte-per the researched draft, a 98-line doc-truth sweep flipped the packet to registered-state truth, and live discovery superseded the one-tool baseline by listing three pre-auth tools, resolving the deep-search conflict as a client-settable mode input. Purely internal framework tooling.

#### mcp-tooling/011-routing-remediation — Hub router fixes, route-gold enforced
Remediated the routing findings from the 007 review: the hub replay went from 6/13 to 13/13 intent with fallback-only default-resource semantics (ADR-001), route gold became a hard enforced gate (49/49 packets replay clean, violation blocks with exit 3), the Figma transport's mutation and sk-design pairing metadata became truthful, and six-mode traceability surfaces were regenerated. A Phase 0 regression freeze anchors the fixes; Phase 5 terminal re-review is still pending. Purely internal framework tooling.

#### mcp-tooling/012-template-alignment — MCP packets aligned to canonical templates
Aligned the asIDE, Refero, and Mobbin packet docs to the canonical create-skill/create-readme structures via a three-lane deep-alignment cycle: overviews added to 11 documents, script READMEs rebuilt (DQI ~52 to 76), and Mobbin guidance reconciled against the 2026-07-16 discovery fixture. All three lanes pass with zero P0/P1 findings; the 11 kebab-case filename advisories are an accepted exception under the hyphen-naming pilot. Purely internal framework tooling.

#### mcp-tooling/013-mcp-obsidian — New Obsidian dual CLI+MCP skill mode
Added `mcp-obsidian` as a new mcp-tooling hub mode: headless `notesmd-cli`, the official app-backed CLI, and the cyanheads MCP, plus a feature catalog, three deep community-plugin references (beancount-finance, obsidian-tables, obsidian42-brat), and a manual testing playbook. Live validation passed 13/15 scenarios: `search` (title search) is broken headlessly so the mode must use `search-content`, and the installed Local REST API exposes a different MCP server identity (16 `vault_*` tools) than the documented cyanheads server, so the MCP tool catalog needs reconciliation. A changelog update was deferred under an explicit no-edit scope constraint, now resolved by this entry.

#### mcp-tooling/z_archive/001-mcp-coco-integration — Semantic code search via CocoIndex MCP
Installed CocoIndex Code as a fourth MCP server registered across all six CLI configs, giving every assistant a semantic `search` tool over a local index (6,792 files / 105,965 chunks) with local embeddings — no API key, no code leaves the machine. Hardened the skill with `doctor.sh`/`ensure_ready.sh` readiness helpers, stable strict failure codes 20–25, and a downstream-adoption checklist. Cross-CLI testing proved discovery needs no agent routing but uncovered an upstream daemon concurrency race under `refresh_index=true` — follow-up queries should pass `refresh_index=false`.

#### mcp-tooling/z_archive/002-mcp-testing-playbooks — 78 manual-testing scenarios across MCP skills
Authored 3 new manual testing playbooks (mcp-code-mode, mcp-chrome-devtools, mcp-clickup) and audited the mcp-coco-index playbook, appending 3 gap-filling scenarios — 78 per-feature files across 19 categories, all validator-clean with cross-skill references resolving. Live V7 smoke runs returned 3 PASS + 1 SKIP: no ClickUp CLI is installed (system `cu` is Taylor UUCP), which empirically validates the CU-002 conflict scenario.

#### mcp-tooling/z_archive/003-mcp-figma-transfer — Figma MCP moved to AI_Systems repos
Transferred the mcp-figma skill out of this repo into a new Figma MCP Agent in AI_Systems/Barter (mirroring ClickUp), dual-published a sanitized duplicate to AI_Systems/Public, and removed the local skill — 14 cross-reference files patched and the advisor graph re-indexed. Verification passed across phases (Opus hooks 9/9 and 12/12; 295/296 tests, one pre-existing sk-code `reference-category` failure). **Breaking:** the local `mcp-figma` developer skill is withdrawn; Figma work now routes to the external agent.

#### mcp-tooling/z_archive/004-mcp-click-up-task-management — mcp-click-up skill with cupt + MCP routing
Shipped the `mcp-click-up` skill giving agents a structured, safe ClickUp interface: operation-based routing between the cupt CLI (daily ops) and the official ClickUp MCP (advanced), an embedded pipx/pip install script, and agent invariants (no hardcoded status names, dry-run before batch, `--json` output). validate.sh passes clean and the advisor routes "clickup task management" at 0.95 confidence.

#### mcp-tooling/z_archive/005-nested-mcp-readme-sync — Nested MCP READMEs caught up
Fixed three nested `mcp_server` READMEs that lagged behind their registered tool sets — skill-advisor tools/handlers and code-graph handlers — after a QA audit cross-checked them against source. Purely internal documentation hygiene; no runtime behavior changed.

#### mcp-tooling/z_archive/006-mcp-skill-install-doctor-standardization — Unified mcp install and doctor surface
All five mcp-* skills now share one install-and-diagnose surface: every skill ships an INSTALL_GUIDE.md, a verify-only `install.sh`, and a read-only `doctor.sh`, while the central `/doctor:mcp` command enumerates them all via a new `cli_skill_diagnostics:` class. mcp-figma graduated to 1.0.0.0 (first stable release, live-verified against figma-ds-cli 1.2.0), and mcp-open-design reached parity with an install guide plus bundled-CLI scripts. The three design skills now cross-reference each other in docs and the skill graph. For a framework user this standardizes setup and diagnostics across mcp tooling, though the new doctor scripts were syntax- and fact-checked but not run end-to-end against live apps.

---

## Hooks & Runtime

#### hooks/001-hook-docs-relocation — Hook reference docs relocated to owning trees
Four hook reference documents (`goal-plugin.md`, `injection-contract.md`, `skill-advisor-hook.md`, and its validation doc) moved out of the spec-kit references tree into their owning hook trees, with all 34 live consumers repointed and strict link-aware validation green. Purely internal tooling reorganization — no behavior change.

#### hooks/002-injection-bloat-reduction — Per-prompt injection bloat shadow reductions across runtimes
Twelve phases implemented measurement-first, flag-gated per-prompt injection reductions across every runtime hook adapter: byte-stable delivery receipts, bounded compiled-route presentation, message-identity dedup, full-first/route-only repeat suppression, edge-triggered Gate-3 relay, compact Pi dispatch, and a guardrail activation matrix with per-cell rollback. Everything ships shadow-only with candidate flags off pending the activation gate; alignment sweeps, cheapest-model playbook standardization, and a zero-FAIL playbook remediation pass completed. Purely internal tooling — no runtime output changed.

#### hooks/003-goal-hooks-cross-runtime — Cross-runtime goal hooks for Devin, Cursor, Pi
Ported the OpenCode session-goal system to Devin, Cursor, and Pi via a runtime-neutral goal core and shared active-goal state, with live capability probes fixing honest parity tiers (Cursor injection-only, Pi observe-only verify). Dispatch-shape coverage grew from 2 to 6 CLIs with Codex folded in, OpenCode plugin browsability symlinks were added, and goal docs reconciled — Devin's goal hook was later decommissioned in phase 008. Purely internal tooling.

#### hooks/004-goal-hook-playbooks-and-validation — Goal-hook playbooks and live cross-runtime validation
Named per-runtime goal-hook playbook scenarios for every goal-capable CLI and live-validated the injection using unique canary tokens grepped from raw transcripts: Pi and Devin PASS with the canary quoted verbatim, Cursor is RECORDED-EVIDENCE (its `sessionStart` `agent_message` is non-delivering by contract), and OpenCode's `mk-goal` plus Claude-native `/goal` are documented as not headless-scriptable. Internal tooling with honestly captured negative results rather than claims.

#### hooks/005-pi-input-hook-latency — Pi input hook latency removed via in-process advisor
The Pi `input` hook no longer blocks message sends on a synchronous multi-process spawn chain — the advisor lifecycle now runs in-process, so repeat prompts resolve from the advisor's own 5-minute cache at ~1 ms (was 1.37–1.49 s). A false-"deleted skill" cache-invalidation defect and a build emission defect were fixed along the way; the ~1.3 s cold-prompt tail remains. Purely internal tooling.

#### hooks/006-spec-gate-question-noise — Gate-3 question suppressed on read-only turns
The shared Gate-3 core now withholds the SPEC FOLDER QUESTION on read-only non-answer turns while keeping write-time enforcement intact, re-surfacing it only on answer attempts, and the question text now matches the binding grammar. The Pi adapter strips embedded history and the sibling advisor capsule before classification and keys state on stable session identity. Internal tooling — committed and merged, formal packet close pending review.

#### hooks/007-fable-governor-pi-hook — Pi-only subagent dispatch directive + governor verdict
Researched whether to keep/update/replace the fable governor per-turn hook (ten three-model iterations) and shipped a Pi-only dispatch directive: subagent dispatch now defaults to native pi-subagents unless a `cli-*` mode is explicitly requested. Delivered the governor capsule parity fix, a `tool_call` deny matrix with deep-loop/explicit-mode exemptions, dispatch authorization hardening via a dependency-free command inspector, AGENTS.md Pi-row, and canonical directive-ownership sync in the injection contract. The package-root full-corpus validation remains an explicit exit-1 deferral. **Breaking:** Pi subagent dispatch now defaults to native pi-subagents unless a `cli-*` mode is explicitly named.

#### hooks/008-pi-caching-like-reasonix — Pi prompt-caching split: pi-cache-optimizer + deep-pi
Research recorded a NO-GO on a Reasonix-style caching plugin — the useful scope already ships as `pi-cache-optimizer` — so the packet forked that package with a narrow DeepSeek exclusion guard (vendored in-repo) and adopted `deep-pi`, pinned and integrity-verified, to own DeepSeek-direct models, proving a clean ownership split with real cache-economics evidence (98.4% cache-hit DeepSeek-direct turn). The `deep-pi` fork was hardened (silent-diagnostics fixes, 60/60 tests), vendored, and live-verified, then improved across a correctness floor, persistent observability/cost accounting with cross-process locking, and maintainability/provenance checks. Purely internal Pi tooling.

#### hooks/009-goal-isolation — Cross-runtime session-scoped goal isolation
Goal state is now stored per explicit workspace/runtime/session scope instead of a global singleton, with native Pi bindings (session manager), a scoped Cursor injector, and bounded OpenCode persistence using fixed SHA-256 session keys and lazy migration. Legacy singleton state is diagnostic-only and never injects, requiring explicit migration or archive. Suites pass (91/91 cross-runtime, 128/128 OpenCode), but the post-review repair is uncommitted, so completion is not claimed until an authorized delivery. **Breaking:** legacy global goal state no longer injects and must be migrated or archived explicitly.

#### hooks/010-hook-feature-flags-and-hub-index — Hook feature flags + canonical hub index
The cross-runtime hook layer gained one shared default-on operator contract: a master switch (`MK_HOOKS_DISABLED`) plus canonical flags for all 20 concerns, with legacy aliases preserved, POSIX shell parity for shell and git consumers, and a single canonical README kill-switch index. Operators can persist personal defaults in `hook-flags.env` (live environment values win; missing files fail open). Purely internal tooling.

---

## Skill Advisor

#### system-skill-advisor/000-rust-backend-rewrite — Rust rewrite research for the advisor backend
Phase parent for a two-phase deep-research investigation into whether the ~51K LOC TypeScript `system-skill-advisor` backend (`mcp_server/`) would benefit from a Rust rewrite. Children split the surface by backend half — `001-research/` owns the JS-resident scoring/ranking compute, `002-research/` owns embeddings/vector/serving — each running a pre-planned 16-round pass with cited evidence. Research-only; no Rust is written and no commit to a rewrite happens until evidence lands.

#### system-skill-advisor/017-advisor-audit-and-state-containment — Phase parent for advisor containment audit
Scaffold-only phase parent covering two advisor workstreams: containing the stray nested `.opencode` trees runtime writers leak (40 exist) and auditing the advisor code surface for nine findings. Both children are still `Planned` with template placeholders — purely internal tooling, no behavior shipped yet.

#### system-skill-advisor/018-codex-node-runtime-alignment — Codex advisor startup blocker fixed
Codex now launches the skill advisor under Node 25 ABI 141, matching the installed native SQLite module, so `mk_skill_advisor` initializes instead of aborting the MCP startup cascade. Negative/positive Node ABI controls and live MCP handshakes confirmed the fix, while memory (Node 22) and code mode (Node 24) keep their independent runtime pins. A fresh Codex session is the remaining operator-observable confirmation.

#### system-skill-advisor/z_archive/001-skill-advisor-refinement — Safer routing, faster warm path, permanent gates
Closed the nine-item refinement plan: uncertainty guarding stays on by default even with `--threshold` unless `--confidence-only` is explicit, and command bridges split into their own deprioritized class. Warm-path matching now uses in-process caching with mtime invalidation and frontmatter-only parsing, plus structural batch mode. Permanent regression and benchmark harnesses lock it in — subprocess p95 46.99 ms versus in-process warm 0.61 ms (~25.85x) with 34/34 regression cases passing.

#### system-skill-advisor/z_archive/002-skill-advisor-reindex-and-stress-test — Routing quality program and calibration phases
Roll-up of the completed 065 routing-quality campaign: reindex replay restored the advisor to live freshness (generation 951) with all known-prompt probes routing correctly, then a six-scenario stress campaign scored PASS=1 WARN=1 FAIL=4. Follow-on phases calibrated routing behavior: memory-save negative triggers (plain file-saves no longer route as `memory:save`), a dedicated `create:testing-playbook` route, fixed alias groups for command/skill ids, and ambiguous debug/review routing that boosts review candidates without stealing build requests.

#### system-skill-advisor/z_archive/003-ambiguity-window-confidence-fix — Dual-margin ambiguity detection fixed
Widened the advisor's ambiguity detector from score-only to a dual-margin OR predicate — a candidate is ambiguous when either the score gap or the confidence gap to the top is within 0.05 — resolving the SAD-002 calibration gap from the packet-083 follow-up. Both the ranking-alignment invariant and the feature-catalog's documented confidence window now hold. 37 scorer tests plus 40 handler regression tests pass; live MCP verification is deferred until the production server restarts.

#### system-skill-advisor/z_archive/004-skill-graph — Skill graph infrastructure and metadata quality
Built the graph backbone of the advisor: 21 per-skill `graph-metadata.json` files, a compiler with edge-symmetry validation, and graph-derived routing boosts (enhances/siblings/depends_on/conflict penalties) fused into the advisor. A unified daemon + native TypeScript advisor shipped via MCP with Python/plugin compatibility shims, followed by metadata quality audits, intent-signal and relationship population, and extraction of the advisor into a standalone `system-skill-advisor` package with its own MCP launcher, DB rename, and consumer cutover.

#### system-skill-advisor/z_archive/005-skill-advisor-scoring-engine — Semantic scoring lane and weight tuning
Added an embedding cache to the skill graph, wired a cosine-similarity shadow lane, and promoted it to a live scoring lane at a conservative 0.05 weight with the other four lanes rebalanced to total exactly 1.00. Weight/ablation/damping sweeps over today-correct and harder intent corpora showed semantic weights produced no top-route variance or accuracy lift, so the 0.05 setting was retained and the damping prototype was reverted with the negative empirical finding preserved.

#### system-skill-advisor/z_archive/006-skill-advisor-routing-engine — Intent routing precision
Improved routing accuracy across the whole path: neutralized the cross-encoder length penalty, added a `continuity` fusion profile, migrated 24 tokenizer-broken INTENT boosters to PHRASE boosters (plus 15 new phrase routes, routing "5-dimension agent scoring" correctly at 0.95), and fixed five stale route resources. Shipped a cross-runtime hook surface (Claude/Gemini/Copilot/Codex, 200/200 corpus parity, cache-hit p95 0.016ms), an OpenCode `spec-kit-skill-advisor` plugin, a `/doctor:skill-advisor` setup command, and privacy-preserving affordance evidence.

#### system-skill-advisor/z_archive/007-skill-advisor-production-hardening — Production hardening and safety
Hardened the advisor for production: per-instance plugin state with in-flight bridge dedup and bounded prompt/brief/cache payloads, a static 200-prompt telemetry harness plus live-session wrapper, and eight surgical quality fixes (shared renderer, dispersion guard, correct ambiguity from ranking score). Added native Codex startup/prompt hooks, a Devin UserPromptSubmit hook with plugin rename, a fail-open fallback remediation closing 18/18 review findings, and a freshness audit confirming no P0 stale-context corruption. Mostly internal infrastructure, with user-visible benefits as better routing sanity and prompt-safety guarantees.

#### system-skill-advisor/z_archive/008-skill-advisor-documentation — Documentation alignment and drift fixes
Brought the advisor's docs in line with its code: hook-first invocation is now the primary documented Gate 2 path with the CLI kept as fallback, and fixture-folder READMEs plus TOC anchors were added under sk-doc standards. Fixed a TS5103 build break (one-character tsconfig fix), reconciled the 8-vs-9 tool-count narrative across all package docs, and completed a documentation-quality refactor spanning README rewrite, deferred-item closure (including a 943-instance Oxford-comma sweep), and cross-skill documentation decoupling so each skill describes only itself.

#### system-skill-advisor/z_archive/009-playbook-run-and-remediation — Manual test run and routing fixes
Executed all 46 manual-testing scenarios across native MCP, CLI hooks, python/compat, daemon, and scorer surfaces via CLI-delegated waves, surfacing real defects rather than a clean pass. Remediation recovered corpus accuracy from 50.78% to 74.09% via alias-aware gold matching, fixed P0 scorer routing in both TS and Python scorers (explicit `/X:y` routing, code-mode disambiguation, low-information abstention), recalibrated the bench gates, and made the OpenCode bridge reach its native route. The run is complete with 0 failures across all 50 regression cases.

#### system-skill-advisor/z_archive/010-skill-advisor-embedder-stack — Pluggable embedders, shared spec-memory logic
Skill-advisor gained a pluggable embedder layer (registry of 6 manifests, adapter contract, `vec_<dim>` storage) with the writer cross-wired to the active embedder, plus shared embedder logic and a launcher fix preventing duplicate daemons. Default alignment to mk-spec-memory's Nomic embedder is deferred, so the live pointer behavior changes only for operators who run the swap runbook. Purely internal tooling; operators must rebuild `@spec-kit/scripts` dist before relying on the fresh outputs.

#### system-skill-advisor/z_archive/011-skill-advisor-cli — Additive 9-tool CLI fallback
The `mk_skill_advisor` MCP server gained a manifest-backed, byte-identical 9-tool CLI fallback, with hardening suites (10/10 scorer parity, orphan-reaping, tri-daemon spawn drill) and warm-only hook fallback routed into Claude, Codex, and the OpenCode bridge. **Breaking:** warm-only CLI invocation is mandatory on prompt paths because the one-shot native bridge measured 824.8ms; the Python `skill_advisor.py` facade is kept for backward compatibility.

#### system-skill-advisor/z_archive/012-advisor-doc-trigger-harvest — Doc frontmatter now routes
The advisor now harvests `trigger_phrases` and tier metadata from each skill's `references/`/`assets/` docs into the skill graph, so prompts can match a specific reference file and return a `matchedDocs` pointer. Doc signal rides the existing derived lane with a 0.45 cap, so it assists and annotates routing but cannot hard-route alone. Flag-gated (`SPECKIT_ADVISOR_DOC_TRIGGERS`), default-on in runtime configs; live-daemon adoption needs a fresh session.

#### system-skill-advisor/z_archive/013-advisor-and-codegraph-migrated-items — Launcher recovery, suite, observability
The skill-advisor launcher recovers from dead lease-holder sockets, the vitest suite went from 61 failing to 0, workspace-root fallback no longer leaks `.advisor-state` into spec folders (23 strays cleaned), and routing became debuggable via prompt-safe `why_recommended` attribution plus semantic-lane health. Also shipped: source-provenance guards, a default-off packed BM25F shadow lane, consolidated BFS traversal, and shadow-only feedback calibration. Purely internal tooling.

#### system-skill-advisor/z_archive/014-skill-advisor-frontmatter-alignment — All 15 reference docs conform
All 15 `system-skill-advisor` reference docs now carry the canonical frontmatter contract (trigger phrases, tier, contextType), with the `skill_advisor_hook.md` operator contract gaining its runtime-matrix phrases and five docs correctly demoted from `important` to `normal`. The contract owner now models its own rules, verified by the contract checker at 0 violations. Purely internal documentation maintenance.

#### system-skill-advisor/z_archive/015-skill-advisor-phase-parent — Hard-rule and dispatch hardening
Shipped stall-detection defaults (5-minute lag ceiling, 60s heartbeat) and loud abnormal-exit classification for deep-loop runs, plus a dependency-free `hard_rules` dispatch linter on the cli-opencode/cli-claude-code skills that fails open in preflight. Both acceptance tests passed; B1 hardening and burn-in remain follow-up. Purely internal tooling; operators with long-running deep-loop jobs may see new stall warnings by default.

#### system-skill-advisor/z_archive/016-skill-advisor-tuning — Advisor scorer tuning: RRF, health, penalties
This packet hardened the shared skill-advisor scorer: RRF-fusion now uses the shared deterministic fuse, runtime lane-health distinguishes degraded-empty lanes from matched-nothing lanes in confidence normalization, and an embedding-staleness signal makes the `semantic_shadow` lane fail closed on stale vectors. Default-off seams shipped for conflict re-rank, query-class routing, exact semantic rerank, reliability-weighted learning (Beta posterior), and a metadata-driven executor-delegation resolver that replaced the inline cli-opencode band-aid on both engines. A benchmark graduated the RRF core and conflict-rerank seam (top-1 37/42→38/42, zero regressions) while cutting the self-recommendation guard as redundant with the documented `-0.25` penalty, now regression-locked. Purely internal tooling; live/default flips remain benchmark-gated.

---

## Agents

#### agents/001-terminal-proof-discipline — Terminal-proof discipline integrated across AGENTS.md
The terminal-proof protocol is now permanent operating discipline distributed across AGENTS.md's existing authorities (Four Laws, Verification Standards, Blast-Radius, a new Final-State Verification gate, Execution Behavior, Quick Reference) instead of a standalone lifecycle block. The per-turn directive capsule gains a proof-over-appearance line through the existing governor chain, shipped via a rebuilt dist with all directive-expectation tests green. Framework users get one coherent authority for terminal proofing with no separate protocol to maintain.

#### agents/002-runtime-surface-coverage — Six-runtime enumeration gap audited
A read-only gpt-5.6-sol scout audit mapped every doc and script still naming only three runtimes (Claude, Codex, OpenCode) when the repo actually ships six (`.opencode`, `.claude`, `.codex`, `.cursor`, `.pi`, `.devin`), finding 20+ stale enumeration sites from root AGENTS.md down to per-runtime SYNC manifests. Requirements REQ-001..008 with file:line evidence are captured for implementation. This is a planning/audit deliverable — implementation (T001-T022) has not started, and the packet flags that copilot is removed from the supported set per user decision.

#### agents/003-communication-quality — Communication Quality §8 added to AGENTS.md
A dedicated `## 8. COMMUNICATION QUALITY` section (Writing, Recommendations & Honesty, Turn Framing) was added to the universal AGENTS.md, and the deeper Codex voice spec was reconciled with four additive edits so the two files agree. Only net-new craft principles were lifted from the source — roughly 70% was already covered, more thoroughly, in `.codex/AGENTS.md`. Every runtime now finds its cross-runtime communication rules in one place, and section references were verified safe before renumbering.

#### agents/004-agents-md-bloat-audit — AGENTS.md bloat audit and safe trims
A five-iteration read-only deep-research loop audited the root AGENTS.md and produced a ranked Tier 1/2/3 bloat report with a preserve set protecting every unique normative constraint; convergence was verified (new-info ratio 1.00 → 0.15). The verified-safe subset was then applied ad-hoc outside the packet's read-only scope: all 7 broken constitutional links fixed, 3 duplicate trims (−9 lines), two obsolete Dispatch Rules rows removed, and the §9 pointer-compression + advisor-metadata accuracy correction landed after a three-model cross-review. Not breaking — a pointer-ization floor was reached with no further safe cuts.

