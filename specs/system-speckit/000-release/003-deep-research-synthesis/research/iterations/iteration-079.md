#### Upgrade auto-populate archived and normalized to Level 1
The Upgrade Auto Populate folder was archived and its documentation set rebuilt against the current Level 1 template. Before, the folder had drifted from today's validator: stale structure, broken markdown references, and mismatched metadata; after, it carries a fresh `spec.md`, `plan.md`, `tasks.md`, and `implementation-summary.md` that pass validation with zero errors. Dropping the archive from Level 2/3 to Level 1 removes the heavier enforcement burden while keeping a concise, reliable record of the cleanup scope. Extra top-level notes were condensed into short archival context files, and compatibility stubs were kept only where files already existed. Historical depth is intentionally condensed, so the full original narrative lives in git history. This is an internal, archive-only change with no runtime effect for framework users.

#### Spec-doc anchor tags archived and normalized to Level 1
The Spec Doc Anchor Tags folder was archived and its documentation set rebuilt against the current Level 1 template. Before, the folder had drifted from today's validator with stale structure, broken markdown references, and mismatched metadata; after, it carries a current `spec.md`, `plan.md`, `tasks.md`, and `implementation-summary.md` that pass validation with zero errors. Lowering the archive from Level 2/3 to Level 1 removes the heavier enforcement burden while preserving a concise, reliable record. Extra top-level notes were simplified into short archival context files, and compatibility stubs exist only where files already had them. Historical analysis is intentionally condensed, with the full narrative available via git history. This is an internal, archive-only change with no runtime effect for framework users.

#### Memory overhaul release: docs, READMEs, changelog aligned
This archived phase parent captured the "memory overhaul and agent upgrade" release cycle (spec 130) as a seven-task decomposition: README audit and alignment, SKILL.md and references audit, command config audit, agent config audit, changelog creation, root README update, and GitHub release prep. The README audit swept 60+ README files across `.opencode/`, finding stale source counts (4→5), stale intent counts (5→7), and missing feature references to `upgrade-level.sh`, `check-placeholders.sh`, and the auto-populate workflow. The root README and `.opencode/README.md` were restructured with 11 documented changes — spec kit feature first, memory second, redundant marketing sections removed, punctuation standardized — netting a 105-line simplification. GitHub release preparation for `v2.1.0.0` finalized a release-notes draft covering agent, spec-kit, documentation, and breaking-change sections, and documented publication blockers (clean working tree, release commit, tag, release URL). Several child task summaries remain as unfilled templates, so the full scope of the SKILL.md, command, and agent audits is not documented. This is an internal tooling and documentation-cycle change with no user-visible runtime effect.
