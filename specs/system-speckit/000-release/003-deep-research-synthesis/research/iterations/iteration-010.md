#### Harmonize cli-* skill surfaces with shared patterns
Internal. The four sibling CLI executor skills (claude-code, codex, copilot, gemini) were aligned on the structural surface cli-opencode already had: each gained a Default Invocation block (pinned model plus flags) and a Provider Auth Pre-Flight subsection that detects credentials once before dispatch and routes through an ask-not-substitute three-state decision tree instead of silently picking a fallback. cli-copilot additionally gained an Error Handling table. Every skill kept its native flags and agent-routing syntax, and a do-not-collapse grep gate confirmed all 33 unique per-skill terms survived with counts equal to or above baseline, so the harmonization added consistency without flattening provider-specific value. For framework users, this means whichever CLI they drive, the pre-dispatch auth guidance and invocation defaults now read identically.

#### Remove mcp-clickup skill and prune references
**Breaking:** the mcp-clickup skill is gone. Its skill folder (37 files) and changelog were deleted, and 38 references were pruned across advisor routing tables, skill-graph nodes and edges, README and install-guide listings, manual-testing-playbook anchor rows, a regression fixture, and two SQLite databases (35 rows), dropping the advertised skill count from 22 to 21. ClickUp-as-a-service documentation that flows through Code Mode was deliberately preserved because it describes the upstream tool, not the local orchestrator. For framework users, ClickUp automation still works but now runs directly through Code Mode's `clickup` manual. **Breaking:** new "clickup" prompts no longer receive a high-confidence skill recommendation and must be routed to Code Mode manually.

#### Warn against cli-copilot path hallucination in docs
Internal, doc-only. cli-copilot's SKILL.md "When NOT to Use" section gained a bullet flagging routing-trace tasks where the caller consumes resource paths literally, citing the 11.1% accuracy versus cli-codex's 66.7% on the sk-doc router stress matrix; sk-doc's SKILL.md gained a matching callout that recommends cli-codex as the preferred default for such consumption tasks. Both caveats link to the review report carrying the full evidence and cite the empirical numbers so the guidance is verifiable rather than opinion. The tight "consumed literally" scoping avoids painting cli-copilot as unusable generally, and the caveats live in SKILL.md because that is the surface agents actually read when choosing a CLI. For framework users this reduces the risk of silently misrouting work through a dispatcher that misnames resource paths.
