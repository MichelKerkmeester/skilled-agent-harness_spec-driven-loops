#### Specs root relocated to a top-level directory
The specs root moved out of the skill tree: `specs/` at the repo top level is now the real directory, and `.opencode/specs` is a relative compatibility symlink. The flip came out of dual-executor research that uncovered an existing migration-safety subsystem — but reading it showed its consolidation functions would no-op against the symlinked tree, so a new topology-flip operation was built on five reusable primitives instead. An 11-step runbook flipped 12 planned call sites plus 6 further production files that test-suite inversion exposed, and rebased `.gitignore` for four downstream projects. Memory-index cleanup ran around a daemon/worktree mismatch, resolved via a standalone reindex plus a verified bulk-delete of 10,459 stale-alias rows. The cycle also removed a dead maintainer-mode code-graph flag mechanism, fixed 20 stale-topology README references across 26 files, and landed a regression test for a memory-index dedup scenario. **Breaking:** anything hardcoding `.opencode/specs` (docs, scripts, hooks, downstream projects) must now resolve through `specs/`; an optional `SPEC_KIT_SPECS_DIR` override covers framework-level call sites.

#### Spec templates slimmed and context budget enforced
Authoring a spec now costs far less context: the research template renders 175 lines at Level 1 instead of 944, and the four core templates (spec/plan/tasks/implementation-summary) were consolidated from 2,931 to 1,314 source lines. Every change is proven render-neutral — 25/25 per-level render hashes matched baseline, and Level 3 and above keep byte-identical output. A template-guide read-guard directs agents to the rendered view rather than raw templates, preventing misreading during authoring. Two advisory plan-adherence gates joined the suite: acceptance-coverage checking flipped to default-on (non-blocking) and a new scope-adherence rule, backed by an `MK_SCOPE_*` changed-files contract, validates that edits stay in scope. `memory-search` gained the same search-envelope token-budget enforcement that memory-context already had, covered by a new five-test suite. A 10-iteration deep review returned conditional with eight P1 findings, all remediated or recorded as decisions, and the result shipped as skill `v3.9.0.0`.

#### Archive normalized for the command dispatch fix
Internal housekeeping only: the archived folder for the command-dispatch vulnerability fix was normalized into a clean Level 1 historical record. Core documents were rebuilt from the current templates and the stray README was reduced to a short archival note. The security-focused feature summary is preserved, with no behavior change. This entry exists so the housekeeping is accounted for — there is no user-visible effect.
