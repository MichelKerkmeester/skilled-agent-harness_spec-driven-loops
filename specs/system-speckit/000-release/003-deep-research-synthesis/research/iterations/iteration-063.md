#### Mutation-coverage dedup via sha256 signatures
`deep-agent-improvement` is internal tooling; its mutation-coverage tracking now stamps every proposed mutation with a sha256 signature derived from dimension, mutation type, target section, and a normalized body sample. Before, records were keyed only on `dimension::mutationType`, so near-identical proposals to different target sections could be collapsed or re-proposed; the reducer now prefers the signature and falls back to the old key only for legacy records. `isSignatureSeen()` pre-checks both the tried and exhausted lists before the loop proposes anything, and the `DEEP_AGENT_IMPROVEMENT_SKIP_DEDUP=1` bypass disables only the check while still recording signatures, so later runs keep full history. The skill doc and the replay-consumer playbook gained a signature smoke test with pass/fail criteria.

#### Narrative-voice READMEs for all six deep-loop skills
All six archived deep-loop skills — `deep-ai-council`, `deep-context`, `deep-improvement`, `deep-loop-runtime`, `deep-research`, and `deep-review` — had their READMEs rewritten into a shared narrative skeleton that opens with a human pitch and an at-a-glance table, then explains the problem before the mechanism. Each now leads with its distinctive value (the council's lenses and two-of-three convergence, the by-model parallel sweep, the proposal-first evaluator, the shared runtime the five loops ride, the externalized-state research loop, the severity-weighted review loop) and documents QUICK START plus INTEGRATION boundaries. Stale facts that contradicted the actual trees were dropped, including the deep-review README's six drifted counts (version, thresholds, file counts, quality gates), the stale version lines on deep-research (`1.14.0.0`) and deep-review (`1.11.0.0`), and the outdated feature-catalog and script counts on deep-improvement and deep-loop-runtime. The result is sibling-consistent documentation that no longer states brittle numbers.

#### Multi-AI Council durable write protocol
Council deliberations now persist as durable, auditable artifacts instead of chat-only output. A `persist-artifacts.cjs` helper (later split into a library-backed writer surface) writes config, strategy, state, seats, deliberations, critiques, and reports; an audit trail stamps events with sha256 checksums, and rollback can move a failed round into `failed/` while preserving supersede markers. The shared output-schema reference plus the agent body's §16 Caller Persistence Protocol define the contract, and the main-agent enforcement phase added five gates across all four runtime mirrors: a Step 0 RESOLVE that fails closed without a packet path, ALWAYS/NEVER rules (no chat-only delivery, no unrooted deliberation, persist before completion), and a mandatory nine-item persistence verification checklist with a `DO NOT CLAIM COMPLETION` handler. Mostly internal tooling for the council agent; note that this batch shipped partially blocked — the Codex TOML mirror was unwritable in the sandbox and live sandbox smokes were left for the operator.
