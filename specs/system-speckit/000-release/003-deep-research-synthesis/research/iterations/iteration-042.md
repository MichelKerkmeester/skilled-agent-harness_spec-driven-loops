#### Feature catalog and playbook documentation contract unified
Manual testing playbooks now ship as integrated root packages — a root policy doc plus numbered category folders with per-feature files — instead of root docs with separate sidecar guidance. Feature catalogs were aligned to the same header and frontmatter conventions, so the behavior the playbooks validate now matches what the catalogs describe. `sk-doc` gained dedicated template bundles for both families under `assets/documentation/`, plus new creation references grouped into `references/global/` and `references/specific/`. Downstream create commands and the write agent were repointed at the regrouped reference paths so runtime workflows stay intact. Link-resolution and count audits confirm both playbook packages are complete (`system-spec-kit` 195/195, `mcp-coco-index` 20/20). Internal authoring infrastructure, but it removes the two parallel contracts authors previously had to reconcile.

#### sk-doc standards stack upgraded and packet repaired
Expanded human-verifiable-review (HVR) guidance, targeted README template updates, and a new README creation workflow reference were landed across `sk-doc`'s global references, asset templates, and specific references, with matching routing notes in the sk-doc and cli-codex skills. The delivery then repaired this packet's own docs, replacing stale shorthand file references with committed repo paths and restoring the required template structure. The packet fix is internal and leaves the underlying skill changes untouched. Users of the authoring surface benefit from stronger review guidance and an explicit guide for when and how to write READMEs, not just the scaffold templates.

#### Changelog template consolidated into sk-doc assets
The canonical changelog and release-notes template moved from the create-command asset folder into `sk-doc/assets/documentation/`, so it now sits beside every other documentation template instead of being the one orphaned asset. All five path references in the auto and confirm changelog command YAMLs, the user-facing command doc, and the spec-kit nested-changelog reference were repointed, and the old template and its slash-command surface were removed. The `sk-doc` Smart Router gained a dedicated `CHANGELOG` intent and resource-map entry so the template surfaces on changelog or release-notes requests, and the template was restructured to match the canonical asset skeleton (frontmatter, overview, anchored sections) with content preserved verbatim. `sk-git` needed no changes since it references the command surface, not the template path. **Breaking:** any workflow referencing the old `.opencode/commands/create/assets/changelog_template.md` path or the `/create:assets:changelog_template` surface must resolve to `sk-doc/assets/documentation/changelog_template.md`; frozen historical changelogs intentionally still cite the old path.
