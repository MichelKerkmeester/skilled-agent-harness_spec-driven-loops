#### Archived agent-routing docs normalized to a clean Level 1 record
The agent-routing documentation update folder was rebuilt into a tidy Level 1 historical archive: `spec.md`, `plan.md`, `tasks.md`, and `implementation-summary.md` were recreated from the current templates, replacing the older Level 2 shape whose verification-only structure no longer satisfied current validation rules. Before, the folder carried obsolete validation scaffolding; after, it is a compact, readable record you can trust when inspecting the archived topic. The folder is internal housekeeping, but the repair matters because every future archive is now validator-safe and inspectable without tripping strict-mode checks. It validates with zero errors and preserves the feature summary plus the repair outcome, at the cost of the full historical verification trail.

#### Archived node_modules relocation rebuilt from an incomplete single-file package
The node_modules relocation archive was repaired from a partial single-file state into a proper Level 1 package: `spec.md`, `plan.md`, `tasks.md`, and `implementation-summary.md` were created from current templates because the old shell did not satisfy current file and integrity rules. Before, the topic was hard to inspect and failed validation; after, it is a complete, readable historical record that passes strict folder validation and markdown reference-integrity checks. This is internal tooling, but the change ensures archived infrastructure moves like the dependency relocation stay trustworthy and inspectable. It condenses the migration story into a feature summary and outcome, dropping the full migration trail.

#### Archived comprehensive script audit normalized to Level 1
The comprehensive script audit archive was repaired from a higher-level shell into a clean Level 1 historical record: `spec.md`, `plan.md`, `tasks.md`, and `implementation-summary.md` were rebuilt from current templates, replacing the older archive structure that no longer matched current validation rules. Before, the folder carried obsolete higher-level structure; after, it is concise, readable, and validator-safe with zero errors under strict validation. This is internal housekeeping, but the fix keeps the archived audit history inspectable and future-proof against template and rule drift. The condensed record keeps the audit feature summary and repair outcome while dropping the full historical audit matrix.
