#### Pi remote experience parity research complete
This packet shipped the two-lineage deep-research synthesis that defines the Pi remote experience target: a best-in-class Pi remote is a typed, redacted, replayable event ledger plus a separate authenticated command protocol, not a terminal mirror or chat clone. Both lineages converged on this shape across 20 iterations each, claiming it can exceed the Claude Code + mobile reference on transcript richness, replay determinism, approval integrity, notification privacy, authority legibility, session isolation, and local data control. No code shipped here — the deliverable is `research/research.md` (1079 lines) carrying ranked P0/P1/P2 recommendations for the 041 packet and 20 open validation questions to freeze against the live Pi RPC surface. Note the cli-pi lineage was flagged `failed` by the runtime containment guard during a concurrent-session false positive, but its full output survived on disk and is included. This is research-phase and internal; there is no user-visible behavior change yet.

#### Luna Max, DeepSeek Max, and GLM 5.3 roster additions
Four more models became dispatchable across the external-CLI modes, every id list-verified against live CLI listings before being added. GPT-5.6 Luna Max (`gpt-5.6-luna-max`, `-max-fast`) joined the enforced Cursor allowlist, and `gpt-5-6-luna-max` plus `-max-priority` joined the Devin allowlist — the first GPT-5.6 persona ids in either curated scope, with Devin's Fast tier using the `-priority` suffix rather than `-fast`. DeepSeek V4 Pro Max and Flash Max joined Devin (only the requested max tier; `-low`/`-high` stay out), and `opencode-go/glm-5.3` was added to the opencode-go catalog as documentation only since cli-opencode has no code-enforced allowlist. Stale counts were swept 18→20, including two pre-existing "10 allowed ids" claims, and the deep-loop unit suite ran 190/190 green twice. Grok 4.6 from packet 036 was verified and left unchanged. Ids are list-verified, not dispatch-tested, and no Luna prompt-craft profile was added — documented limitations, not regressions. Not breaking: additive allowlist entries only.

#### cli-gemini skill creation (archived)
This archived packet records the original build of the `cli-gemini` skill that lets Claude Code and OpenCode orchestrate Google's Gemini CLI for cross-AI tasks. It created six skill files (~3000 lines), centered on a SKILL.md whose smart router scores user intent across seven categories and conditionally loads only the relevant reference and template resources, plus a conductor/executor model where Claude Code plans and Gemini executes. Skill advisor entries were added with synonym maps and intent boosters, reaching 0.95 routing confidence on "use gemini" queries. The packet sits in `z_archive` and predates this release cycle, so this is internal history rather than a shipped v4.0 change; it carries no user-visible effect in this cycle.
