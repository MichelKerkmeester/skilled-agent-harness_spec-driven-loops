#### Command-surface benchmark for command effectiveness and adherence
The deep-alignment mode now hosts a two-axis benchmark that measures the OpenCode command surface without averaging its two results. A deterministic peer adapter audits every command's structural integrity across the full corpus — 36 canonical command sources and their 36 generated mirrors (37 after the launcher ships) — while a sampled behavioral suite, DAB-012 through DAB-027, actually invokes commands and scores adherence. Behind it, `create-benchmark` gained a fifth authored `conformance_benchmark` family, a reproducible 13-tree fixture corpus with an independent CLI oracle and frozen expectations, a production `sk-doc-command` adapter covering all four command topologies, and an opt-in schema v2 for the shared behavior-benchmark runner that adds direct-dispatch, postcondition, and boundary evidence while preserving every v1 result byte-for-byte. A bounded, machine-reconciled 52-cell scheduler and manifest gate the operator-approved live model matrix. The new `/deep:command-benchmark` launcher binds both engines to one spec folder and run ID, keeping instrumentation orthogonal to subject results — a valid subject failure stays `STATUS=OK INSTRUMENT=VALID`. This is internal benchmarking tooling with no user-visible result changes; existing commands, the v1 runner contract, and the default `sk-doc` path are unchanged.

#### Dark evidence-ledger spine with live-search fan-out
The deep-loop runtime now ships the first slices of an evidence-ledger architecture built from 178 research recommendations: an append-only, typed, versioned event ledger behind a default-deny transition-authorization gateway, sealed reference artifacts, versioned replay fingerprints, and certified receipts with crash-safe recovery of interrupted external effects. Everything lands additive and dark — legacy writers, persistence, and authority stay authoritative and unchanged, so no behavior shifts until later phases cut over mode by mode. One piece is already user-visible: the fan-out scheduler now resolves live-tool policy per executor, requests proven web search, rejects unproven tool/policy combinations before dispatch, fingerprints invocations without secrets, and compiles deterministic multi-model manifests while preserving the legacy 256-lineage ceiling. The program is anchored by an immutable 178-row recommendation ledger, an accepted six-primitive spine architecture, a frozen transition/versioning/rollback policy, and a pinned baseline census (22 event surfaces, 46 persisted-state surfaces, a 53-scenario corpus). The grounding research catalogued 290 repos and 305 insights across the loop-engineering landscape. Internal substrate work: the only behavioral delta is the reject-before-dispatch fan-out guard; no legacy path is imported or changed.

#### Graph-engineering research toward graph-based deep loops
This is a research-only packet assessing how to evolve deep-loop workflows into graph-engineering-based loops, grounded in a reference corpus spanning GraphARC, graph-engineering-master, the NVIDIA NOOA paper, and the agent-swarms and graphene projects. Five repo studies plus a cross-study integration capstone each ran fan-out deep-research loops with a high/xhigh gpt-5.6-sol pass (20 iterations, or 10 rounds for the capstone) and independent DeepSeek V4 Pro verification, converging on one coherent design: a graph decides eligible work and ordering, a bounded loop executes each unit, typed evidence stacks, and only the 036 authority plane — currently running dark — may authorize protected mutations. The output is a versioned graph-IR design layered as a projection over that spine with shadow parity, plus six additive loop/harness deltas and an agreed next evidence class of a mutant-driven shadow vertical-slice. GraphARC's lesson is recorded: admission is a precondition, not authorization. Purely internal research; no runtime code was modified, so there is no user-visible change.
