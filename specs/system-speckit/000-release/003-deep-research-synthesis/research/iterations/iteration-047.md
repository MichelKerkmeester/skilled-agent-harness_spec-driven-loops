#### Feature catalog and manual-playbook coverage shipped
sk-git now ships a complete feature catalog alongside its manual-testing playbook. The catalog adds 11 per-feature files across four categories (worktree naming, session lifecycle, workflow playbooks, remote-platform integration), and the playbook grows from 22 to 41 scenarios with 19 new owner-first worktree-tooling cases (GIT-023..GIT-041) covering allocator, wrapper, reaper, and pre-push valid and invalid paths. The catalog and playbook are registered in SKILL.md and README with a v1.3.0.0 changelog, bumping SKILL.md to 1.3.0.0 and README to 1.1.0.28. A pre-existing defect where hyphenated paths in docs pointed at on-disk underscore_case names was corrected. Two workflow features honestly carry "no coverage yet" stub rows rather than fabricated test anchors. For framework users the payoff is discoverability: each capability is documented and manually verifiable end to end.

#### README enrichment and hyphen-case file migration
sk-git's docs were enriched and its file tree migrated to the repo's preferred hyphen-case naming, with history preserved via `git mv` across 66 files and 13 directories. Every reference was repointed to the new paths: the SKILL.md Smart Router sweep reports 0 missing resources, and a resolve-guided pass repaired 109 broken relative cross-links across 22 files. The root README was rewritten to the create-readme canon, adding a HOW IT WORKS section covering the owner-first grammar, the allocator lock, the ask-first rule, launch-wrapper autosync, the reaper contract, and safety refusals. Four code READMEs were authored for `scripts/`, `scripts/tests/`, `.github/workflows/`, and `.github/hooks/scripts/`. `package_skill.py --check` passes with 12 advisory-only snake_case findings that clear once the repo-wide sk-doc/017 migration flips the checker. Not a breaking change for users: files keep their history and every internal link still resolves.

#### Git review remediation round two closes
Six review findings from the previous sk-git review round were fixed and independently re-verified by four fresh agents, each on a disjoint file domain. Changes include a new Git Workspace Safety table in AGENTS.md (ask-first worktree choice, allocate-never-count, no direct branch creation), allocator hardening that fails cleanly on mktemp failure and at number exhaustion (>= 9999 returns empty with a non-zero exit, matching `allocate`), CI/CD routing single-sourced to the `gh` CLI across four spots (two non-existent GitHub-MCP tool names removed), two new SKILL.md NEVER refusals (#9 `--no-verify`, #10 amend-published), and the cross-skill sk-doc validator now recognizing hyphen and underscore doc types. The allocator harness runs PASS=47 FAIL=0 and `validate.sh --strict` reports 0 errors. For framework users the fix list tightens safety guarantees around worktree creation and strengthens the docs' self-consistency. A concurrent sk-git spec renumber was observed in the working tree and flagged for reconciliation before further work.
