#### sk-code-review promoted to first-class review baseline
The single-hyphen `legacy-single-hyphen-review` skill was hard-renamed to `sk-code-review` and rebuilt as a stack-agnostic review baseline with weighted intent scoring, ambiguity handling, and a baseline+overlay precedence matrix. Review now loads the universal baseline first, detects the stack, then layers one stack-specific overlay, so universal security and correctness minimums are enforced alongside per-stack standards. `@review` runtime docs across all agent profiles (OpenCode, ChatGPT, Gemini, Claude, Codex) were rewritten to the explicit four-step loading order, all 18 review-dispatch YAML assets gained a `standards_contract`, and advisor routing now tops `sk-code-review` for review prompts while preserving git behavior. **Breaking:** the legacy skill was hard-renamed with no compatibility alias, so any existing pointer to the old path must be updated.

#### OpenCode and review policies aligned on KISS/DRY/SOLID
A tightly scoped refinement updated the `sk-code-opencode` policy file and three `sk-code-review` alignment files to give review routing an explicit architecture lens. The review `SKILL.md` keeps its findings-first behavior while exposing routing to architecture-level analysis, and the correctness/performance and SOLID checklists now carry the KISS, DRY, and SRP/OCP/LSP/ISP/DIP prompts the routing logic actually uses. Harmonization targets already in compliance were closed as verification-only outcomes (no net diff, EVT-001/001b/001c PASS), so the change stays inside the four evidence-confirmed files. Spec validation exits with one advisory `SECTION_COUNTS` warning, which is documented as non-blocking.

#### Retire sk-code-full-stack and sk-code-web skills
Both deprecated skills were hard-deleted from disk — 128 files including sk-code-full-stack's internal changelog — and every cross-reference was pruned across advisor data, skill graphs, READMEs, install guides, agent definitions, and three SQLite databases (~2000 rows). The ten placeholder pointers under `sk-code/{references,assets}/<stack>/` were neutralized to `canonical_source: null` stubs pointing at git history, webflow scripts were retargeted from `sk-code-web/scripts/` to `sk-code/scripts/`, and skill counts dropped 22→20 (advisor) and 17→15 (install-guide). **Breaking:** React, Node.js, Go, Swift, and React Native guidance (~36 markdown files) is retired by explicit user decision; restore content from git history before 2026-04-30 or author fresh content.
