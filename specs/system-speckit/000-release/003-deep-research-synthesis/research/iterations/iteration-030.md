#### Add verification rituals to the sk-code skill
The verification guidance inside the sk-code skill now carries three new rituals. After a suite goes green, you are told to break the production code and confirm the test actually fails, and to distinguish a genuine failing assertion (true-RED) from a suite that never compiled or ran (compile-RED) — so a green run is only treated as evidence after it has caught something. A verification ladder now runs unit, in-memory, on-server, and live, with each rung's blind spot named up front, so you no longer claim "works" from a rung that never exercised the failing path. Bare TODOs are replaced with named seams that carry a closing condition, dead controls are forbidden, and structural invariants are preferred over disciplinary reminders. The rituals are advisory text confined to the verification section; the smart router is byte-untouched, and promoting the doctrine to always-on rules is left as a gated follow-up.

#### Document the design-foundations static visual system mode
A new spec packet reconstructs the design-foundations mode's contract, which previously shipped with no tracked documentation at all. It records the mode's ownership of the static visual system — color, typography, spacing, layout, hierarchy, grids, responsive adaptation, theming, and token naming — plus its read-only execution contract (reference-based file reads and searches only, no workspace mutation, no script execution), the at-most-one procedure-card selection rule, and the implementation handoff envelope it passes to the code skill. The packet is explicitly a best-effort reconstruction: every claim traces to the shipped source, and none of it adds or changes runtime behavior. This entry is internal documentation; there is no user-visible effect.

#### Document the design-interface direction and voice mode
A new spec packet reconstructs the design-interface mode's contract, which previously shipped with no tracked documentation at all. It records the mode's ownership of overall direction: visual voice, information architecture, component direction, interface-level composition, motion posture, critique, and interface writing — including register-first calibration (Brand-vs-Product posture and the internal VARIANCE/MOTION/DENSITY dials), subject and audience grounding, one justified aesthetic risk, the anti-AI-default critique, and the mechanical quality floor of responsive behavior, visible focus, reduced motion, touch targets, and contrast. It also captures the conditional resource loading, the at-most-one procedure-card selection with its no-procedure fallback, and the handoff manifest required of the code skill. The packet is explicitly a best-effort reconstruction and neither adds nor changes runtime behavior. This entry is internal documentation; there is no user-visible effect.
