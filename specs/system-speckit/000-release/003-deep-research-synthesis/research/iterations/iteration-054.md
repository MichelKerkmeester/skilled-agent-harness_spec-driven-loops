#### Rename sk-prompt-small-model to sk-prompt-models across repo
The prompt-craft skill previously called `sk-prompt-small-model` (about 799 files) is now `sk-prompt-models`. The rename used `git mv` so the folder's history is preserved, and swept 826 files / 4,596 occurrences of the old name across the skill's own identity files, 8 referencing skills, specs (669 files), commands, scripts, agents, READMEs, the pre-commit hook, CI workflows, and the cross-runtime mirrors under `.claude/` and `.codex/`. 123 spec and history files that also mention the even older `sk-small-model` name were frozen and left untouched, a broken changelog symlink was repaired, and the advisor index was regenerated so routing resolves the new name (0.95 confidence) with zero live residual. Along the way, 5 pre-existing graph-symmetry validation failures were fixed and spec-memory was re-indexed for both live packets. **Breaking:** any prompt, command, or config referencing `sk-prompt-small-model` should now use `sk-prompt-models`.

#### Unify deep-loop skills into one system-deep-loop hub
Deep-loop orchestration was two separate skills — `deep-loop-workflows` (the invokable hub) and `deep-loop-runtime` (its backend) — and is now one skill, `system-deep-loop`, with the runtime nested inside as `runtime/` and the version bumped to 2.0.0.0. The structural merge touched ~1,483 files, then a dependency-ordered migration rewrote the remaining `deep-loop-workflows`/`deep-loop-runtime` references across ~750 more files (commands, agents, READMEs, hooks, advisor routing, `.claude`/`.codex` mirrors, GitHub CI), leaving only 14 deliberately preserved mentions. The router agent was also renamed from `deep` to `deep-loop` across all three runtime mirrors so it no longer collides with the `/deep:*` command namespace. A template-conformance pass renamed 131 hyphen-case asset files in deep-improvement to snake_case, and a 20-iteration deep review found and fixed 7 P1 bugs, ending PASS with 15 P2 advisories. Live dogfood runs of research and review with divergent convergence mode completed 10/10 iterations each, confirming the pivot mechanism correctly stays disabled at `maxIterationsReached`. **Breaking:** anything referencing `deep-loop-workflows`, `deep-loop-runtime`, or the `@deep` router agent must be updated to `system-deep-loop` and `@deep-loop`.

#### Smart-routing benchmark program: 20-run matrix and circularity meter
This folder is internal tooling: the smart-routing benchmark program, which measures how well each skill's router actually routes. It delivered a complete 20-run matrix — 8 skill children plus 2 hubs (sk-code and system-deep-loop), each scored in Mode-A (deterministic router replay) and Mode-B (live gpt-5.5-fast) — with all 8 children passing Mode-A on their own gold. A drift guard enforces that the sk-code parent router is exactly the union of its children plus a parent tier, and the working two-layer hub run stayed byte-identical across that projection rewrite. The headline result is the circularity meter: it exposes router overfitting, most sharply for code-review, which scored a perfect 100 in Mode-A but 0 live with zero resource recall. The original Mode-B numbers were later shown to be a measurement artifact and corrected upward by follow-up children. As pure internal tooling this has no user-visible effect; the only remaining follow-up is wiring the Mode-A configs into CI.
