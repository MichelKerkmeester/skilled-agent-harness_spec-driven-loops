#### Motion.dev raised to first-class cross-stack reference category
Motion.dev is no longer treated as Webflow-only: sk-code now ships a peer `motion_dev/` reference and asset category alongside the Webflow surface, covering install, `animate()`, scroll/in-view, gestures, springs, layout animation, and performance. The manual testing playbook gains animation regression and release-readiness coverage — pinned CDN smoke tests, version/export validation, reduced-motion behavior, visual baselines, browser compatibility, Core Web Vitals, and compositor checks. Webflow docs cross-reference the new Motion material while Webflow-specific CDN and `window.Motion` guidance stays untouched, and the skill's router docs and metadata were refreshed so the category is discoverable. A deep-review pass then remediated the flagged findings, including a documented dynamic-import pattern for Motion+. The cross-CLI routing harness and routing-precision fixes (doc-only markdown edits routed as sk-doc, non-Webflow Motion prompts kept out of Webflow promotion) also shipped, but their Codex rechecks could not complete because nested Codex couldn't reach the OpenAI API from the sandbox, so those phases remain unverified.

#### OpenCode authoring checklists, recipe, and restored contract
sk-code's OpenCode side now ships first-class authoring assets matching the scope its SKILL.md has always declared. Five new checklists — skills, agents, commands, MCP servers, and spec folders — follow one canonical six-section structure and each points to concrete prior examples in the codebase, and a new spec-folder-write recipe documents the strict-validation failure modes maintainers hit. The machine-readable `STACK_FOLDERS` surface contract was restored in SKILL.md and a stale relative link fixed, closing nine of the 22 P1 findings from the 077 research. sk-code bumps to v3.2.0.0 with a compact changelog entry. Mostly internal tooling: the user-visible effect is consistent authoring guidance and runnable reference points for future skill, agent, command, MCP, and spec-folder work.

#### Root scripts relocated into surface asset folders
The root `scripts/` folder inside sk-code is gone: the three Webflow minification utilities moved under `assets/webflow/scripts/`, while the cross-language alignment verifier and its tests moved to generic `assets/scripts/` because they check OpenCode alignment rather than Webflow behavior. All 41 referencing docs, resource maps, and historical spec files were updated so stale old-path strings are gone repo-wide, and the moved scripts re-passed their syntax and test checks. Purely internal tooling — no user-facing behavior changes. The Git index couldn't be staged from the sandbox, so the moves are filesystem-level; reviewing the diff with rename detection enabled will show them as renames.
