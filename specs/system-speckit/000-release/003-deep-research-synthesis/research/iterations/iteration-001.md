#### Terminal-proof discipline distributed across framework gates
Agents now must back every load-bearing claim with terminal evidence, not just assert it. The standalone Terminal Discipline block in AGENTS.md was removed and its mechanics distributed across the Four Laws, verification standards, blast-radius management, execution behavior, and a new Final-State Verification hard gate that blocks completion until the authoritative check passes. Each turn, the per-turn capsule now carries a one-line proof-over-appearance directive injected through the existing governor chain — defined in the renderer and mirrored for the OpenCode fallback, so every runtime reminds the model to prove claims before declaring success. The AGENTS.md diff landed as 51 insertions and 2 deletions, the compiled dist and directive-expectation tests were rebuilt, and strict validation passes clean. For framework users the effect is behavioral: "it works" must now be backed by rerunning the authoritative gate, not by phrasing.

#### Runtime surface audit maps the six-runtime gap
This packet is internal planning — it ships the audit, not the fix. A read-only scout pass enumerated every place the repo still names only Claude, Codex, and OpenCode even though it ships six runtimes (also Cursor, Pi, Devin), finding 20+ stale enumeration sites from the root AGENTS.md contract down to per-runtime SYNC.md manifests. Findings are codified as requirements with file:line evidence, and the 3-phase plan (tasks T001-T022) is approved for the next session; implementation has not started, so no runtime behavior changes yet. Copilot is being dropped from the supported set per the operator's deprecation decision, and Devin MCP registration stays absent because no checked-in config exists to support that support claim.

#### Communication quality section unified across runtimes
The root AGENTS.md gains a dedicated Communication Quality section so every runtime finds the cross-runtime voice rules in one place; the former §8 and §9 become §9 and §10. Drafted from practitioner commentary, only genuinely net-new craft principles were lifted — one idea per sentence, atomic paragraphs, progressive jargon disclosure, lead-with-the-recommendation-but-earn-it, and the Ask→Do framing for ambiguous requests — since the deeper Codex voice spec already covered the rest. That spec was then reconciled with four small additive edits so the two files stay in agreement rather than drift, deliberately importing one-idea/SVO/atomic but not "keep it short," which would contradict Codex's existing medium-length rhythm. For framework users this means consistent communication rules across every runtime and no more conflicting guidance; the change is additive, with nothing removed.
