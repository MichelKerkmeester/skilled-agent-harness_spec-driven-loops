#### Agent template aligned with current production patterns
The sk-doc agent template was rebuilt to match what the fleet's production agents actually do. The stale 692-line template grew to 765 lines carrying current patterns drawn from the live context, deep-review, and multi-AI-council agents, so anything you scaffold from it now inherits today's working design instead of retired ones. The old standalone related-resources section, a retired documentation-agent reference, and a standalone intro-paragraph section were removed. The production examples table now lists the current ten-agent fleet, and the template documents a 600-line cap for production agents while staying under its own 900-line ceiling. BINDING and REFUSE remain canonical, grep-checkable contracts so newly scaffolded agents stay consistent with the fleet. Internal change; existing agents are unaffected.

#### sk-doc asset templates promoted, references updated across runtimes
The four most-used sk-doc templates moved one folder shallower. `agent_template.md` and `command_template.md` left `assets/agents/`, and the `feature_catalog/` and `testing_playbook/` package folders left `assets/documentation/`, landing directly under `assets/` with the now-empty `agents/` folder deleted. All in-repo references were updated across four runtimes, with Claude and Codex command mirrors following automatically as symlinks and the Gemini/Codex agent files byte-copied and TOML-checked. Verification ran twice: a strict validation pass, then an independent reviewer that re-ran all eight deterministic checks in a fresh context; a follow-up review produced three more doc fixes (a broken cross-link, a stale folder tree, and deprecated examples). This is internal framework tooling with no user-visible behavior change. **Breaking:** any external reference to the old paths — `assets/agents/*` and `assets/documentation/{feature_catalog,testing_playbook}/` — must migrate to the new `assets/` root locations.

#### Smart router stress test ships playbook and matrix
sk-doc now ships a manual testing playbook plus the data from a full router stress test. Fifteen scenarios across five categories (intent detection, resource loading, unknown-input fallback, cross-CLI dispatch, token cost) were authored with machine-readable expectations and then executed against three CLI executors — codex, copilot, opencode — for a 45-cell matrix. The run surfaced a methodology bug: imperative prompts caused real side effects (seven stray skeleton folders) until scenarios were reframed as read-only routing traces, after which the clean run produced zero side effects. The published report ranks the codex CLI best for routing tasks — 67% intent accuracy, 66.7% resource accuracy, ~38k tokens, 100% reliability — versus copilot (40% / 5.6%) and opencode (53% / 43.3%, 87% reliability) — and carries a ship-with-remediation verdict plus five follow-up findings. This is mostly internal testing tooling, but the playbook is a ready-made way to exercise the router yourself and the report is concrete guidance for picking a CLI.
