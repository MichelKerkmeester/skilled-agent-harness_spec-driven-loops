#### sk-code-review README rewritten with corrected output contract

The sk-code-review README was rewritten from a mechanical reference into a narrative pitch that leads with the skill's distinctive value: a stack-agnostic, findings-first review baseline that always enforces security and correctness minimums and classifies every finding as P0/P1/P2 with file:line evidence. The old README documented the output contract wrong — it ordered Findings before the summary, invented a "Review Context" heading that does not exist, and omitted the mandatory `Review status:` line that automation depends on. The rewrite matches the real contract (Summary, Findings, Removal/Iteration Plan, Next Steps, ending with the exact status line) and adds a QUICK START covering the `@review` dispatch. The version line, a reference count (9 versus the real 10) and a nonexistent `assets/` path were dropped. The result is a single authorable page that gate parsers and humans read consistently.

#### sk-code README corrected to surface-aware router and five phases

The sk-code README was rewritten to lead with the surface-aware router, the five gated phases and the Iron Law instead of burying them in mechanics. The key correction: MOTION_DEV was presented as a third surface and valid surface-override value, but the skill only defines two surfaces (WEBFLOW, OPENCODE) plus an UNKNOWN fallback — MOTION_DEV is a cross-stack resource intent loaded after surface detection, confirmed against `description.json`. The rewrite also stops conflating the four-axis routing model with the five-phase lifecycle (Research, Implementation, Code Quality Gate, Debugging, Verification), so readers no longer expect a surface or a phase count that does not exist. It frames sk-code as the one skill end users customize for their own stack, and drops the version line, line count and intent count. The page now reads as one 198-line narrative with the router, phases and verification commands front and center.

#### Ponytail-based refinement research for sk-code

A 12-iteration, two-model deep-research investigation mapped which ponytail mechanisms are worth transplanting into sk-code and sk-code-review, delivered as a prioritized, file-mapped recommendation report. It resolved the core tension: ponytail gates design upfront (a 6-rung restraint ladder, no loop) at Phase 1, while sk-code's Iron Law verifies results at Phase 3 — complementary rather than conflicting, provided the no-loop stance is not imported literally. The 25 ranked recommendations split into 8 adopt-now (notably the Design Restraint Ladder in the always-loaded universal doc, native review rows, and a canary-locked `Review status:` triplet), 9 adopt-later (including canonicalizing the Iron Law and promoting a mirror-sync check to a repo-wide gate) and 8 do-not-adopt (including an Iron-Law-bypass verification slider). The sweep also found and verified 3 latent defects: Iron Law wording drifted in three ways, a mirror-sync script is mis-scoped to promotion-time, and a stack-folder binding is unenforced. This was research only — no skill code changed, and the recommendations await a separate planning and implementation packet, with the restraint-ladder integration still to be proven before adoption.
