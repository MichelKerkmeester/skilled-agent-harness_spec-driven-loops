#### 18-scenario manual testing playbook for sk-code-review
sk-code-review now ships an 18-scenario manual testing playbook, giving operators a deterministic gate before declaring a review-baseline change safe to ship. Before, the review doctrine had no reproducible verification; now each scenario pairs an exact human-AI prompt and command sequence with pass/fail criteria tied to real sk-code-review reference files, so two operators following the same prompt produce the same verdict. Six categories anchor it — baseline review flow, security and correctness minimums, severity and evidence discipline, scope and precedence, re-review and stale context, and cross-CLI orchestration. The cross-CLI category is what makes it portable: scenarios cover native @review, cli-codex delegation, and cli-opencode plus cli-gemini handback reconciliation, all preserving the same severity schema and file:line evidence rule so multi-AI handoffs don't degrade into incompatible buckets. Delivered via a single cli-codex (gpt-5.5 high) dispatch, it passed `validate.sh --strict`, the document validator, and an 18/18 structural sweep; a dogfood DQI review raised two P1s that were fixed, including rewriting identical boilerplate rationales into 18 per-scenario failure-mode explanations.

#### PR-state dedup and min-evidence gate for sk-code-review
sk-code-review gains two opt-in efficiency gates that stop it from burning a full review on work that doesn't need one. M-1 PR-state content-hash dedup hashes the commit subject plus the diff content; when a review returns the same signature, the skill reports `COMMENTED (no changes since last review)` instead of rerunning the pass, with results cached in a JSONL file capped at 100 entries per repository. M-2 is a minimum-evidence gate driven by the `SK_CODE_REVIEW_MIN_CHANGED_LINES` environment variable (default 0, so it does nothing until set); below the threshold it skips, but six risk classes are never skipped — security/auth, config files, persistence, dependency manifests, sandboxing/exec, and public-facing responses. Both gates only emit a COMMENTED status and never silently approve or block, so nothing ships without a human noticing. Note this release is specification-only: the gates are documented contracts for downstream review dispatch to implement, with no runtime enforcement code shipped yet.

#### Mined web-motion-skill into sk-code's animation surface
sk-code's animation surface gained the design-principle layer it was missing, mined from the open-source (MIT) web-motion-skill project. The analysis and drafting ran in an isolated `wt/0006` worktree via a gpt-5.5-fast dispatch, which correctly dropped the source's recording scripts and GSAP-specifics as non-portable and re-framed the portable principle layer for Motion.dev; only the human-reviewed, additive parts were integrated to main. That adds `references/motion_dev/animation_principles.md` (timing vocabulary, easing-as-perceived-physics, stagger direction, anticipation and overshoot ranges, arc and depth, visual-review cues) and a composed `assets/motion_dev/snippets/principled_reveal.js` with a reduced-motion fallback, plus cross-links in `smart_routing.md`, the performance-and-pitfalls guide, the snippets README, and the Webflow animation workflows. Everything is additive-only and MIT-attributed — the six existing Motion.dev references were enumerated to the dispatch so nothing was duplicated — and the drift guard plus full 349-test suite pass, so routing behavior is unchanged.
