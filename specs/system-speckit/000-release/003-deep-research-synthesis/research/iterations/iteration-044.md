#### Refined extraction exposes CLI resource-path hallucination
The sk-doc router test matrix was re-run with a refined extractor that filters paragraphs leading with negative markers and matches resource filenames by basename in addition to full paths. The fairer matching moved measured accuracy from 5.6% to 11.1% for cli-copilot and from 43.3% to 47.2% for cli-opencode, while cli-codex was unchanged. The rerun surfaced a new finding: cli-copilot does not just over-list resources, it hallucinates file names that do not exist in the framework, such as `dqi_rubric.md` and `dqi_checklist.md`, where cli-codex and cli-opencode cite only real paths. The qualitative ranking from the prior run holds: cli-codex leads, cli-opencode follows, cli-copilot trails. The extraction output ships as `matrix_v2.csv` and `review-report-v2.md` in the packet. This is internal test tooling, but it sharpens the router's standing guidance to prefer cli-codex for documentation routing.

#### Router matrix covers all intents with bullet-aware extraction
Two new scenarios, SD-016 (OPTIMIZATION) and SD-017 (INSTALL_GUIDE), close the last gap in the sk-doc router test matrix, so all eleven router intents are now exercised. They ship as `004-optimization.md` and `005-install-guide.md` in the manual testing playbook, with the playbook index updated. A third-generation extractor parses markdown bullets in three passes, and re-extracting the stored logs across 17 scenarios and 3 CLIs (51 cells) confirms the established ranking: cli-codex at 70.6% resource accuracy, cli-opencode at 47.5%, cli-copilot at 9.8%. All six new cells picked the right intent, but cli-copilot scored 0% accuracy on both new intents with five false-positive resource references each, reproducing the hallucination pattern. The full matrix ships as `matrix_v3.csv` with findings in `review-report-v3.md`. The bullet-aware method is documented for reuse by future test packets.

#### Skill README template, reference moves, markdown agent rename
Five phases shipped under this parent. The six sk-doc creation guides were relocated from `references/specific/` up to the root `references/` folder, with every consumer updated to the new paths. A dedicated skill README template (`assets/skill/skill_readme_template.md`) was added and wired into the SKILL_CREATION workflow, which raised its token-cost baseline from three to four loaded resources. The documentation authoring agent was renamed from `create` to `markdown` across the `.opencode`, `.claude`, `.gemini`, and `.codex` mirrors while every `/create:*` command stayed unchanged, and the manual testing playbook gained three agent-dispatch scenarios (SD-018/019/020) that exercise `@markdown` across cli-claude-code, cli-codex, and cli-opencode. A deep-review's seven P1 and nine P2 findings were closed in a single 129-second cli-codex dispatch, with the one structural gap recorded as an accepted limitation. **Breaking:** anything referencing the `create` agent or the old `references/specific/` paths must migrate to `markdown` and the root `references/` paths.
