#### De-numbered snippet filenames in catalog and playbook standards
The standards governing feature catalogs and manual testing playbooks no longer number the per-feature snippet files. Previously every snippet carried a global `NNN-` filename prefix that ran across every category, forcing a wholesale renumber on each insert. Now authors create `NN--category-name/feature-name.md`: a numbered category folder holding un-numbered feature files, with snippet order defined by the root catalog or playbook listing rather than the filename. The two creation references, both root and both snippet templates, the two `create:*` command docs and their four YAML assets were aligned to the new shape. The validator needed no logic change because it already keys per-feature detection off the `NN--` category-directory pattern; only a stale code comment was updated. Existing packages are untouched — the ~1,531 already-numbered snippet files keep their names, with migration deferred to a later phase gated on a new tool — so the user-visible win is that inserting a feature into a catalog or playbook is now a drop-in file with no cascading renumber.

#### README rewritten in narrative voice, drifted counts dropped
The sk-doc README was rewritten in a narrative voice and now leads with the skill's core value: a structure-first quality pipeline where a deterministic script extracts and scores a document before the AI judges it, so every doc of a given type ships in the same shape and meets the same bar. The rewrite made the README its own showcase by dropping every drifted count and the version line — a version that disagreed with SKILL.md and the newest changelog, six scripts where eight exist, thirteen references where there are fourteen, seven flowchart patterns where there are six files, and three skill templates where there are five. It describes the families instead, so the doc stays evergreen rather than aging into inaccuracy. The new HOW IT WORKS covers the DQI bands (40 structure, 30 content, 30 style, across four bands) and the scripts-versus-AI split, and QUICK START shows real `extract_structure.py` and `validate_document.py` runs with expected exit codes. At 181 lines it passes sk-doc's own validator with zero issues — fitting, since the skill owns the README standard and now meets it itself.

#### Continuous integration workflow for sk-git
sk-git gained a trunk-following continuous-integration workflow so the operator's IDE checkout always reflects work committed by concurrent AI sessions. Each session keeps its isolated worktree and databases, but a new publish primitive (`git-sync.sh`) syncs every commit to one shared live branch through a safe sequence: fetch, fast-forward or rebase-and-abort-on-conflict, then a non-force push. An IDE-side follower (`git-live-follow.sh`) fast-forward-follows that branch only when the checkout is clean, and a dashboard (`worktree-status.sh`) reports each worktree's branch, ahead/behind and dirty state against the live branch. Publishing fires automatically from the post-commit hook, gated so only environment-flagged, linked-worktree wrapper sessions autosync and manual commits never do, with every failure mode non-destructive. The publish path is identical for Claude, Codex and OpenCode; wiring it revealed OpenCode already ran both SessionStart guards via its plugin, so only Codex needed them added. A sandbox battery against a fake local remote verified the primitive across all paths (9 passed, 0 failed), and the never-force-push contract means concurrent sessions cannot clobber each other's published commits.
