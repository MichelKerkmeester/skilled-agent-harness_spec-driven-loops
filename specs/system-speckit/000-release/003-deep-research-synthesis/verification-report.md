# v4.0.0.0 Changelog — Verification Report

> 41 features × 2 independent cli-devin verifiers (deepseek-v4-flash-max + glm-5-2-max), each investigating the real repo. Summary: 17 OK · 13 minor · 4 review · 7 flagged.

## Per-feature verdicts

| ID | Track | Feature | flash | glm-max | Status |
|---|---|---|---|---|---|
| 001 | system-speckit | Specs Move to the Top Level | CONFIRMED | CONFIRMED | ✅ OK |
| 002 | system-speckit | Reindexing Stops Rewriting Your Edits | CONFIRMED | (none) | ✅ OK · disagree |
| 003 | system-speckit | Smaller Templates, Same Output | CONFIRMED | CONFIRMED | ✅ OK |
| 004 | system-deep-loop | One Skill, Hub and Backend Together | MINOR_ISSUES | WRONG | 🔴 FLAG · disagree |
| 005 | system-deep-loop | A Conformance-Audit Mode | OVERSTATED | OVERSTATED | 🟠 review |
| 006 | cli-external-orchestration | One Hub for the External CLIs | CONFIRMED | CONFIRMED | ✅ OK |
| 007 | cli-external-orchestration | Executors That Only Run When Installed | CONFIRMED | CONFIRMED | ✅ OK |
| 008 | cli-external-orchestration | Surfaces Retired | OVERSTATED | CONFIRMED | 🟠 review · disagree |
| 009 | sk-doc | One Hub, Many Packets | MINOR_ISSUES | MINOR_ISSUES | 🟡 minor |
| 010 | sk-doc | Three New Ways to Author | MINOR_ISSUES | MINOR_ISSUES | 🟡 minor |
| 011 | sk-doc | Kebab-Case Is Now the One Name | (none) | CONFIRMED | ✅ OK · disagree |
| 012 | sk-doc | Benchmarks, One Way | CONFIRMED | CONFIRMED | ✅ OK |
| 013 | sk-design | A Style Library You Own | MINOR_ISSUES | CONFIRMED | 🟡 minor · disagree |
| 014 | sk-design | Interface Creation Commands | MINOR_ISSUES | CONFIRMED | 🟡 minor · disagree |
| 015 | sk-design | A Claude-Style Hub | MINOR_ISSUES | CONFIRMED | 🟡 minor · disagree |
| 016 | sk-design | Open Design Transport Removed | MINOR_ISSUES | MINOR_ISSUES | 🟡 minor |
| 017 | sk-code | One Hub, Two Axes | OVERSTATED | WRONG | 🔴 FLAG · disagree |
| 018 | sk-code | Review, First Class | MINOR_ISSUES | MINOR_ISSUES | 🟡 minor |
| 019 | sk-code | Rust Joins the Code | CONFIRMED | CONFIRMED | ✅ OK |
| 020 | sk-code | Deprecated Skills Retired | (none) | WRONG | 🔴 FLAG · disagree |
| 021 | sk-git | Every Commit Reaches Your IDE | MINOR_ISSUES | MINOR_ISSUES | 🟡 minor |
| 022 | sk-git | Remote Push Is Curated By Default | CONFIRMED | CONFIRMED | ✅ OK |
| 023 | sk-git | A Fail-Open Guard Against Mass Deletion | CONFIRMED | MINOR_ISSUES | 🟡 minor · disagree |
| 024 | sk-git | Owner-First Branch Names | CONFIRMED | CONFIRMED | ✅ OK |
| 025 | sk-prompt | One Hub for Prompt Craft | MINOR_ISSUES | (none) | 🟡 minor · disagree |
| 026 | sk-prompt | A Profile for Every Model | OVERSTATED | WRONG | 🔴 FLAG · disagree |
| 027 | sk-prompt | The Names Settle | MINOR_ISSUES | OVERSTATED | 🟠 review · disagree |
| 028 | mcp-tooling | One Hub for the MCP Bridges | CONFIRMED | CONFIRMED | ✅ OK |
| 029 | mcp-tooling | Three Transports and a Browser Bridge | CONFIRMED | CONFIRMED | ✅ OK |
| 030 | mcp-tooling | Your Vault at the Terminal | CONFIRMED | MINOR_ISSUES | 🟡 minor · disagree |
| 031 | mcp-tooling | Figma Leaves the Tree | WRONG | WRONG | 🔴 FLAG |
| 032 | hooks | Goals in Every Tool | WRONG | WRONG | 🔴 FLAG |
| 033 | hooks | Pi Chooses Its Own Path | CONFIRMED | MINOR_ISSUES | 🟡 minor · disagree |
| 034 | hooks | Snappier Pi | CONFIRMED | CONFIRMED | ✅ OK |
| 035 | hooks | One Switch for the Hooks | CONFIRMED | CONFIRMED | ✅ OK |
| 036 | system-skill-advisor | A Standalone Advisor with a Skill Graph | OVERSTATED | CONFIRMED | 🟠 review · disagree |
| 037 | system-skill-advisor | A Command-Line Front Door | WRONG | MINOR_ISSUES | 🔴 FLAG · disagree |
| 038 | system-skill-advisor | A Faster, Safer Warm Path | MINOR_ISSUES | MINOR_ISSUES | 🟡 minor |
| 039 | system-skill-advisor | Codex Starts the Advisor | CONFIRMED | CONFIRMED | ✅ OK |
| 040 | agents | Terminal Proof Is Now the Law | CONFIRMED | CONFIRMED | ✅ OK |
| 041 | agents | Communication Quality §8 | CONFIRMED | CONFIRMED | ✅ OK |

## Items needing attention (issues, overstatements, disagreements)

### 002 — Reindexing Stops Rewriting Your Edits  ·  _Spec Kit & Memory_
  - (verdicts differ; no explicit issue text)

### 004 — One Skill, Hub and Backend Together  ·  _The Deep Loops, Unified and Extended_
  - **flash** (MINOR_ISSUES, conf 0.85): 1) The mode enumeration is wrong: 'context' is not a deep-loop mode (it is a separate one-shot retrieval agent); the fifth family is 'alignment', which was added post-merge, and pre-merge users had only four modes (research, review, council, improvement) — so 'five modes you already use... behave exactly as before' is doubly off. 2) 'the deep router agent is now simply named deep-loop' is stale: the router was renamed deep->deep-loop (fb329577970) but was deprecated and deleted as dead in 85170406f87 (routing moved into /deep:* command bodies) before the unification merge closed; no deep-loop.md agent exists in .opencode/agents or .claude/agents at HEAD.
    - _suggested fix:_ Change the mode list to 'research, review, council, alignment, improvement' (seven modes across five families) and note alignment is a post-merge addition; reword the router sentence to state the deep router agent was renamed deep->deep-loop during unification but was subsequently retired as dead with routing moved into the /deep:* command bodies, so no router agent exists today.
  - **glm** (WRONG, conf 0.92): (1) The enumerated five modes are wrong: 'context' is not a deep-loop mode (no workflowMode, no packet, no agent at HEAD; @context is a separate one-shot retrieval skill) and 'alignment' is omitted — the actual five are research, review, ai-council, improvement, alignment. (2) 'the deep router agent is now simply named deep-loop' is contradicted at HEAD: phase 009 renamed deep→deep-loop (fb329577970) but commit 85170406f87 subsequently deleted both deep-loop.md agent mirrors as a dead router superseded by compiled-command-contracts; no deep-loop agent file exists in .claude/agents/ or .opencode/agents/ at HEAD. (3) 'council' is imprecise — the user-facing mode/command/agent is 'ai-council' (council is only the runtimeLoopType).
    - _suggested fix:_ The five-mode hub and its backend runtime merged into one skill, system-deep-loop. The two prior packages — the workflow hub and the backend runtime — are now a single skill, with every downstream reference (commands, agents, READMEs, hooks, advisor routing) repointed at the new home. The five mode families — research, review, ai-council, improvement, and alignment — behave exactly as before. The old deep-loop-workflows and deep-loop-runtime skill identities no longer exist, so anything you script that still names one of them now points at a signpost that isn't there. (Note: the primary deep-loop router agent that phase 009 had renamed to deep-loop was later deprecated and removed in packet 036 — compiled-command-contracts now resolve /deep:* routing directly — so do not present 'deep-loop' as a live router agent.)

### 005 — A Conformance-Audit Mode  ·  _The Deep Loops, Unified and Extended_
  - **flash** (OVERSTATED, conf 0.92): The claim lists 'sk-design (static and live-render)' among working adapters, but the live-render adapter was deliberately removed within the release window (commit 80dce88a7db retires the Open Design transport; phase 010's own implementation-summary also disclosed check() never rendered anything, consuming caller-supplied render evidence instead). Everything else — new mode, named-authority conformance auditing, deep-review runtime reuse, v1 adapters for sk-doc/sk-git/sk-code/static sk-design, usability, and in-progress closeout — is accurate.
    - _suggested fix:_ Change 'sk-design (static and live-render)' to 'sk-design (static; the live-render adapter was retired with the Open Design transport)' or simply 'sk-design (static)'.
  - **glm** (OVERSTATED, conf 0.92): The claim 'Working adapters already cover sk-doc, sk-git, sk-design (static and live-render), and sk-code' is factually wrong about live-render: the sk-design live-render adapter was REMOVED in a BREAKING CHANGE commit (80dce88a7db, 'feat(sk-design)!: retire Open Design transport') during the release window. Current shipped adapters are sk-doc, sk-doc-command, sk-git, sk-design (STATIC ONLY), and sk-code — five variants, no live-render. Secondary minor overstatement: 'reusing the deep-review runtime engine' is only partially accurate — deep-alignment ships its own `check-convergence.cjs` backend (mode-registry.json declares `backendKind: alignment-convergence`) because, per that script's own header, `runtime/scripts/convergence.cjs`'s loopType enum does not accept 'alignment'; it reuses the shared `reduce-state.cjs`/`reduce-alignment-state.cjs` reducers but not the deep-review convergence engine directly.
    - _suggested fix:_ A new `deep-alignment` mode joins the loop family. Hand it artifacts — docs, code, designs, git history — and it audits them against the named parent-skill authority they're supposed to follow, reusing shared deep-review runtime reducers (reduce-state.cjs, reduce-alignment-state.cjs) behind a dedicated alignment-convergence backend so it stays consistent with the loops you already know. Working adapters cover sk-doc, sk-git, sk-design (static), and sk-code (the sk-design live-render adapter was retired with the Open Design transport during this release). The mode is registered and its scripts are independently runnable today; a full live end-to-end run remains the open acceptance step, and the packet's overall closeout is still in progress.

### 008 — Surfaces Retired  ·  _Orchestrating Other AIs_
  - **flash** (OVERSTATED, conf 0.9): (1) Timing misattributed: both bridge removals are pre-v3.6.0.0, so 'Surfaces Retired' as a v4.0.0.0 change is misdated. (2) 'removed end-to-end ... runtime detection' is false: system-spec-kit runtime-detection.ts still returns 'copilot-cli' and its feature-catalog docs (runtime-detection.md, cross-runtime-fallback.md, session-start-priming.md) still list Copilot as a supported runtime. (3) Model references remain: .opencode/agents/orchestrate.md 'Copilot profile' (updated 2026-08-13, in-window), prompt-improver.md and sk-prompt-improve SKILL.md still accept target_cli 'copilot', plus install-guide options. (4) 'Copilot-shaped prompts now route to Claude Code' is unsupported as a routing change — no copilot alias exists in hub-router/mode-registry; the only evidence is the 014 summary's emergent advisor fallback ('Top recommendation is cli-claude-code (next closest peer)').
    - _suggested fix:_ 'The Gemini and Copilot bridges were retired in the v3.6 cycle (Copilot price hike, May 2026; Gemini surface cleanup, June 2026); v4 carried only residual reference cleanup (ARCHITECTURE.md, hook bridge). cli-gemini and cli-copilot are gone from the skill tree, advisor scoring, and hub routing — copilot-shaped dispatch queries now land on the closest remaining executor (cli-claude-code). Note: spec-kit runtime detection still recognizes copilot-cli and a few agent/install-guide references survive. New in v4, off by default: the sk-communication projection layer rewrites terse CLI output to plain English while keeping canonical bytes intact, excluded from advisor routing until the blind human study gate passes.'

### 009 — One Hub, Many Packets  ·  _Documentation as a System_
  - **flash** (MINOR_ISSUES, conf 0.93): (1) '@create is now @markdown' is false as a release change: the agent was already named markdown at v3.6.0.0 (rename happened 2026-05-10, ~5 weeks before the tag); the in-window markdown-agent-sync phase only synced path references. (2) '/doc:quality kept as an alias' is imprecise: it is the packet's primary bound command (mode-registry.json 'command': '/doc:quality'), created within this release (8f3607a5167, 2026-07-06) and preserved through the packet rename (phase 012), but its command file was deleted on 2026-07-07 (80cc87de1f2) and at HEAD only the registry binding/doc references remain, so 'kept' overstates its current availability. Minor: 'the same two-axis shape' mirrors the spec's own phrasing, though sk-doc (and sk-design) are workflow-only, one-axis hubs.
    - _suggested fix:_ Replace the agent sentence with 'the markdown agent's references were synced to the dissected packet paths (the @create→@markdown rename itself predates v3.6.0.0)', and reword the quality item to 'the doc-quality packet is now create-quality-control, with /doc:quality preserved as its bound command'.
  - **glm** (MINOR_ISSUES, conf 0.85): (1) WRONG: 'the documentation agent you call @create is now @markdown' — @markdown already existed at v3.6.0.0 and no @create agent ever existed in this lineage; no rename commit exists in v3.6.0.0..HEAD. (2) OVERSTATED: 'the same two-axis shape sk-code and sk-design already use' — sk-doc's own SKILL.md states three times there is 'no surface axis' / 'workflow-only' / 'NEVER add a surface axis', so sk-doc is single-axis, unlike sk-code which is genuinely two-axis (workflow+surface).
    - _suggested fix:_ The six-mode sk-doc monolith became a workflow-only parent hub that routes to self-contained create-* packets, adopting the parent-hub-plus-mode-registry pattern sk-code and sk-design already use (sk-doc itself is single-axis: workflow-only, with no surface axis). Four /create commands were renamed to match their packet (sk-skill → skill, folder_readme → readme, sk-skill-parent → skill-parent, testing-playbook → manual-testing-playbook), the doc-quality packet is now create-quality-control with /doc:quality kept as an alias, and the /create:* command family itself is untouched. (Drop the '@create is now @markdown' sentence: @markdown predates this release and no @create agent existed in this lineage.)

### 010 — Three New Ways to Author  ·  _Documentation as a System_
  - **flash** (MINOR_ISSUES, conf 0.95): The only inaccuracy: the claim says sk-create-diagram 'imports and exports draw.io and Mermaid', but export is to PNG/SVG only — draw.io/Mermaid import (import-drawio.md, import-mermaid.md, drawio_extract.py, mermaid_extract.py) is real, while export.md and the SKILL.md state 'exports generated diagrams to PNG / SVG' with no draw.io/Mermaid export path.
    - _suggested fix:_ Change to: '...imports draw.io and Mermaid sources and exports diagrams to PNG/SVG, and is reachable through the new /create:diagram command.'
  - **glm** (MINOR_ISSUES, conf 0.92): The claim says sk-create-diagram 'imports and exports draw.io and Mermaid', but the actual skill only IMPORTS draw.io and Mermaid (redrawing them into editorial diagrams); EXPORT is to PNG/SVG only (see references/import-export/export.md titled 'Export to PNG / SVG'). There is no draw.io or Mermaid export path.
    - _suggested fix:_ Replace 'imports and exports draw.io and Mermaid' with 'imports draw.io and Mermaid sources and exports generated diagrams to PNG/SVG'.

### 011 — Kebab-Case Is Now the One Name  ·  _Documentation as a System_
  - **glm** (CONFIRMED, conf 0.9): Minor nuances (not material): (1) the guard is CI-gated only — 014-001 lists pre-commit wiring as a documented follow-up ('Gate host: CI only'); (2) a handful of leading-underscore names remain (_utils.sh, _common.sh, _args.cjs, tests/_support), but these were explicitly carved out as the separate phase-012 follow-on, whose own impl-summary says 'Nothing yet — this phase is the roadmap'; (3) the 020 packet's own timeline.md and v4-reconciliation-inventory.md state the migration is 'unexecuted'/'Planned and unexecuted', which is stale relative to the squashed commit cc77a1e550a and the actual kebab-cased repo tree.

### 013 — A Style Library You Own  ·  _The Design Surface_
  - **flash** (MINOR_ISSUES, conf 0.87): The '~21x faster' figure (p95 1150->53 ms) is explicitly labeled indicative in the impl summary ('perf trace is my 10-query representative set, not the operator's real workload — the ~21x is indicative; the operator confirms materiality on their trace'), yet the changelog states it as settled; vector lookup exists in schema/code but depends on queue-populated embedding profiles, so it may be provisioned rather than live.
    - _suggested fix:_ Consider '...runs about 21x faster than the old path on a representative 10-query trace (operator confirmation of the real workload is still pending)' to match the documented caveat.

### 014 — Interface Creation Commands  ·  _The Design Surface_
  - **flash** (MINOR_ISSUES, conf 0.92): (1) 'a shared nine-stage contract under /interface:*' is loose: the contract physically lives at .opencode/skills/sk-design/shared/creation-contract.md and is referenced (not stored) by the /interface:* commands. (2) The present-tense framing is stale at HEAD: commits b217d74b819/8b54f7522d1/c1981d2b913 retired the audit and foundations commands and merged motion into interface, so only two /interface:* commands and four workflow YAMLs remain (sk-design README: 'the two canonical /interface:* creation commands') — the 'ten command workflow files' describe an intermediate state, and later same-window commits did change the surface (mode renames to sk- prefix, /design:* namespace retired), which a standalone 'runtime behavior is unchanged' can mislead on.
    - _suggested fix:_ A shared nine-stage creation contract (Route Proof, Context Manifest, Progressive Brief, Grounding, Mode Plan, Creative/Diagnostic Work, Critique/Revision, Proof, Deliver/Handoff) in sk-design's shared references now scripts the /interface:* creation commands, making a run reproducible and gateable from the terminal. In the same pass, the ten per-command workflow files were conformed to the create-command command-standard vocabulary; the workflows' execution fields were preserved value-identical. (Note: the /interface:* surface has since narrowed to the two canonical commands, design and design-reference.)
  - **glm** (CONFIRMED, conf 0.82): Minor wording imprecisions, not factual errors: (1) 'under /interface:*' is loose — the contract file lives at .opencode/skills/sk-design/shared/creation-contract.md and is referenced/expanded by the /interface:* commands via @-include, not located under them; (2) 'In the same pass' conflates two distinct packets — the nine-stage contract shipped in 012-sk-design-program/003-interface-commands/001 (2026-07-19) while the ten-YAML conformance shipped in 013-structure-naming-cleanup (2026-07-23); (3) material omission — 6 of the 10 conformed YAMLs were subsequently retired in the same release window when the audit, foundations, and motion commands were folded/retired (commits b217d74b819, 8b54f7522d1, c1981d2b913), so at HEAD only /interface:design and /interface:design-reference remain (4 YAMLs). The claim's own assertions (nine-stage, ten files, standard vocabulary, runtime unchanged) all hold, but a reader would infer a stable ten-file end state that no longer exists.

### 015 — A Claude-Style Hub  ·  _The Design Surface_
  - **flash** (MINOR_ISSUES, conf 0.9): Card count is stale: phase 003 created 14 cards (6+3+1+2+1+1), but commits b217d74b819/8b54f7522d1/0536eed47e4 (2026-07-27/29, in-window) retired the audit+foundations modes; the current-state feature catalog and on-disk tree show 12 cards (interface 10 incl. 3 folded foundations + 1 motion, md-generator 1, shared 1) — the 2 audit cards were deleted.
    - _suggested fix:_ Change 'fourteen procedure cards' to 'fourteen procedure cards (twelve shipping today after the audit-mode retirement folded the audit vocabulary into the interface mode)' or simply 'a private layer of procedure cards (twelve today, from fourteen at creation)'.

### 016 — Open Design Transport Removed  ·  _The Design Surface_
  - **flash** (MINOR_ISSUES, conf 0.9): 'Hub references... all gone' is marginally overstated: 3 stale 'nested transport packet' phrases survive in live hub docs (.opencode/skills/sk-design/SKILL.md:15 'two design modes plus a nested transport packet'; README.md:107 and :120), referencing the removed transport's structure and contradicting the now two-mode registry, though none name Open Design or misroute users. 'Figma and the terminal remain your design transports' also omits mcp-refero/mcp-mobbin, which remain as design transports in mcp-tooling — a summary choice, not an inaccuracy.
    - _suggested fix:_ Tighten to: 'its mode tree, server entry, agent and command links, and live-render adapters are all gone, and hub routing references were stripped' (or fix the residual 'nested transport packet' lines in SKILL.md/README.md); optionally phrase survivors as 'Figma (mcp-figma) and the terminal (the two sk-design modes) remain your design transports'.
  - **glm** (MINOR_ISSUES, conf 0.9): The closing sentence 'Figma and the terminal remain your design transports' is factually wrong. Only Figma (mcp-figma) remains as a design transport per the authoritative sk-design hub docs; 'the terminal' was the Open Design transport's interface modality (od CLI 'from the terminal') and was the thing removed in this release, not something that remains. The rest of the claim (mode tree, server entry, hub refs, agent/command links, live-render adapters, design-generation-patterns.md, .utcp_config.json drop) is accurate and well-evidenced.
    - _suggested fix:_ Replace 'Figma and the terminal remain your design transports' with 'Figma remains your design transport' — the Open Design transport was the terminal-driven one (the od CLI run 'from the terminal'), and it is what this release removed.

### 017 — One Hub, Two Axes  ·  _One Code Skill_
  - **flash** (OVERSTATED, conf 0.9): (1) The mode/surface lists are stale for the shipped release: implement/debug/verify were dissolved into shared/references/workflow_*.md and animation folded into code-webflow before v4.0.0.0-beta.1, so the release routes to quality/review modes and webflow/opencode surfaces only (sk- prefixed); 'It now routes…' misstates the release state. (2) 'This is the one place in the release with a hard breaking change' is wrong: the release window includes breaking commits for the specs/ root flip, sk-design Open Design transport retirement, and goal-plugin session/Devin-command changes; the restructure itself is breaking (old flat paths moved), but not uniquely so.
    - _suggested fix:_ Reword to reflect the shipped state, e.g.: 'The flat skill became a parent hub routing on two axes — workflow modes (quality, review) and read-only surface packets (webflow, opencode) — after implement/debug/verify doctrine was consolidated into shared references and animation folded into webflow. All 128 relocated files were repointed, including a pre-commit hygiene gate that had been silently skipped, and the deterministic router benchmark rose from 44 to 71. This restructure is a hard breaking change — the routing contract and every skill path moved under the mode and surface packets — but not the only one in the release.'
  - **glm** (WRONG, conf 0.9): The claim lists 5 workflow modes (implement, quality, debug, verify, review) and 3 surface packets (webflow, opencode, animation) — this is the intermediate post-013 state that phase 022 explicitly superseded within the same release: implement/debug/verify are NOT workflow modes (dissolved into shared/references/workflow_*.md doctrine), and animation is NOT a surface packet (folded into sk-code-webflow as a non-skill overlay). A reader would expect code-implement/code-debug/code-verify/code-animation packets that do not exist. Also 'All 128 relocated files were repointed' conflates phase 004's 128 git-mv relocations with phase 007's 77 repointed external-reference files; the pre-commit gate was among the 77, not the 128.
    - _suggested fix:_ The flat skill became a parent hub routing on two axes: two workflow modes that act (sk-code-quality, sk-code-review) and two read-only surface evidence packets (sk-code-webflow, sk-code-opencode), each surface carrying the shared implement→debug→verify workflow doctrine plus its stack knowledge (with the Motion.dev animation overlay folded into webflow). The 128 relocated flat files and 77 external references were repointed — including a pre-commit hygiene gate that had been silently skipping — and a deterministic router benchmark rose from 44 to 71. This is the one place in the release with a hard breaking change: the routing contract and every skill path moved under the mode and surface packets, so anything still pointing at the old flat paths must be updated.

### 018 — Review, First Class  ·  _One Code Skill_
  - **flash** (MINOR_ISSUES, conf 0.9): (1) 'Renamed and rebuilt as a stack-agnostic review baseline' attributes pre-window work (Feb-Apr 2026, per archived packet 001 and commit 2361f1d893b) to this release; the in-window work was only the hub fold (doctrine moved, not rebuilt) plus the code-review->sk-code-review prefix rename, and the claim's 'renamed...then folded' order inverts the actual sequence (fold Jul 4, rename Jul 28). (2) The surviving caveat is wrong: the sk-doc validators no longer reject 'sk-code-review' — the hyphen rejection was the Feb 2026 known limitation against the then-double-dash name, and since v3.6.0.0 the validators require hyphen-case names and only reject consecutive hyphens.
    - _suggested fix:_ Drop or reword the caveat (e.g. 'the earlier validator mismatch with hyphenated names was resolved; validators now require hyphen-case names'), and scope the entry to the in-window work: 'the standalone review skill was folded into the sk-code hub as a non-mutating code-review mode (later renamed sk-code-review) on the existing baseline-plus-overlay model, and wired across agents, orchestrators, Codex configs, command dispatch and the advisor.'
  - **glm** (MINOR_ISSUES, conf 0.88): The final caveat ('the doc validators still reject the skill's hyphenated name') is factually wrong: all three doc validators now accept sk-code-review (empirically verified passing). The historical FAIL ('cannot contain consecutive hyphens') is in archived z_archive/001 and predates the fold-in. Secondary nit: 'was renamed' is misleading — the standalone sk-code-review was folded in as code-review (phase 005, 2026-07-04) then renamed back to sk-code-review (commit dad347226d3, 2026-07-28, 'rename mode packets to sk- prefix'), so the net name is unchanged, not a single rename.
    - _suggested fix:_ Drop the caveat sentence entirely (the doc validators no longer reject the hyphenated name — quick_validate/validate_document/package_skill all pass on sk-code-review). Reword the rename: 'sk-code-review was rebuilt as a stack-agnostic review baseline and folded into the hub as a mode (briefly named code-review, then restored to sk-code-review under the sk- prefix rename).'

### 020 — Deprecated Skills Retired  ·  _One Code Skill_
  - **glm** (WRONG, conf 0.82): (1) Release-window misattribution: the retirement shipped ~7 weeks BEFORE v3.6.0.0, so it is not a v4.0.0.0 (v3.6.0.0..HEAD) change at all — `git log v3.6.0.0..HEAD --grep='hard-remove'` returns nothing and the old skill dirs are gone at v3.6.0.0. (2) Stack list is incomplete: the claim names "React, Node, Go and Swift" but omits React Native, which contributed 7 of the 36 reference .md files (and 16 total files) that left the tree. The "roughly three dozen" count itself is accurate (36 reference .md files), and the cross-ref pruning / placeholder neutralization are real (43 files edited, 6 _placeholder.md files rewritten in 970a85039ff).
    - _suggested fix:_ Drop the entry from the v4.0.0.0 changelog (the retirement predates v3.6.0.0 and is not in this release window), or reword to attribute it correctly and include the missing stack: "sk-code-web and sk-code-full-stack were retired in a prior release (before v3.6.0.0): cross-references pruned across the tree and the umbrella's placeholder pointers neutralized. By decision ~36 canonical reference pages across React, React Native, Node, Go and Swift left the tree and are no longer available as fallback."

### 021 — Every Commit Reaches Your IDE  ·  _Safer Git_
  - **flash** (MINOR_ISSUES, conf 0.95): The third of the 'three checks' is mislabeled: it is the non-fatal (`|| true`) guard, not 'installed hooks'. Hook installation is a precondition for autosync to fire at all (spec.md risk row, implementation-summary limitation 2), but it is not one of the three gate conditions.
    - _suggested fix:_ ...behind three checks (an environment flag, a linked worktree, and a non-fatal guard), firing only when the git hooks are installed, and a fast-forward follower...
  - **glm** (MINOR_ISSUES, conf 0.82): The claim's 'three checks (an environment flag, a linked worktree, and installed hooks)' mismatches the spec's authoritative triple gate (SPECKIT_AUTOSYNC=1 + SPECKIT_LIVE_BRANCH + linked-worktree): it collapses the two env vars into one and substitutes 'installed hooks' — a real deployment precondition but not a runtime gate the autosync evaluates — as the third check. Every other assertion (trunk-following, autosync each commit, wrapper-launched, Claude/Codex/OpenCode, one shared live branch, fast-forward follower keeping IDE current, background-session-lands-work-is-already-there) is directly supported.
    - _suggested fix:_ '...behind a tight triple gate (an autosync env flag, its paired live-branch env var, and a linked-worktree check), firing only where the git hooks are installed...' Or, kept punchy: '...behind a tight gate (an environment flag and a linked-worktree check) that fires only from installed hooks in a wrapper-launched session...'.

### 023 — A Fail-Open Guard Against Mass Deletion  ·  _Safer Git_
  - **glm** (MINOR_ISSUES, conf 0.82): The motivating-incident file count is stated as 902 (in both the changelog and the backing spec.md/commit message), but raw `git show --name-status` on the cited commit e3a66403df2 counts 903 deleted tracked files — an off-by-one. All feature mechanics (threshold 100, explicit-flag bypass, live-for-commits/ready-for-pushes, fail-open) are exactly supported by the code.

### 025 — One Hub for Prompt Craft  ·  _Prompt Engineering_
  - **flash** (MINOR_ISSUES, conf 0.9): The breaking-change name is outdated: `/prompt` was indeed renamed `/prompt-improve` (5afd2f6522f), but later in the same window 43d234ad29b replaced it with `/prompt:improve` (commands/prompt/improve.md), deleting prompt-improve.md; at HEAD the registry, command-metadata, and v1.1.0.0 changelog all name `/prompt:improve`, which explicitly flags `/prompt-improve` as the stale name. Also minor anachronism: the 28KB monolith was `/prompt` then `/prompt-improve`, not `/prompt:improve` (that name is the new router); and the folded packet folder is `sk-prompt/sk-prompt-models/` on disk (standalone skill was `sk-prompt-small-model` at v3.6.0.0, renamed within the window).
    - _suggested fix:_ Replace the last sentence with: 'The breaking change is the command name: `/prompt` became `/prompt-improve`, then `/prompt:improve` — the current canonical name (the old 28KB monolith was the `/prompt`/`/prompt-improve` command). Everything that lived in the standalone sk-prompt-models skill moved under `sk-prompt/sk-prompt-models/`.'

### 026 — A Profile for Every Model  ·  _Prompt Engineering_
  - **flash** (OVERSTATED, conf 0.9): Wrong/stale counts: 'nine conformed profiles, one per small model' (6 profile files for 8 registered models at HEAD; historically 9 = 8 profiles + _index.md); 'recommended_frameworks field on all eight models' (7 of 8; haiku has no field); 'eleven prompt frameworks' (7 frameworks at HEAD; the 11-framework library incl. VIBE/VIBE-MP/FRAME/MOTION predates or was cut within this release); 'five CLI skills' (six carry the 3-tier precedence rule at HEAD). Confirmed: ~90-line duplicated quality cards (89-96) replaced by one 3-tier rule per skill, and the three-layer hub layout with single-place profiles.
    - _suggested fix:_ The per-model prompt-craft hub now holds six conformed profiles plus an index (the 8-model registry's recommended_frameworks covers seven entries), routing can suggest which of the seven prompt frameworks fits the model you're calling, and the six CLI skills each carry a single three-tier precedence rule instead of ~90-line duplicated quality cards, layered as framework tables in sk-prompt, per-model profiles in the prompt-models mode, and lean delegating mirrors in the cli-* executors.
  - **glm** (WRONG, conf 0.92): Three of four headline numbers are materially wrong: 'nine conformed profiles' is actually 6 profile files; 'eleven prompt frameworks' is actually 7 (RCAF/COSTAR/RACE/CIDI/TIDD-EC/CRISPE/CRAFT); 'five CLI skills' is actually 6 at HEAD (all carry the rule). 'all eight models' with recommended_frameworks is overstated — only 7 of 8 have it (haiku lacks the field). The qualitative claims (three layers, single source of truth, ~90-line cards replaced) are correct.
    - _suggested fix:_ The per-model prompt-craft knowledge is now a real hub with six conformed profiles, one per active small model, and a `recommended_frameworks` field on seven of the eight model entries so routing can suggest which of the seven prompt frameworks fits the model you're calling. The six CLI skills each carry a single three-tier precedence rule for composing prompts instead of around 90 lines of duplicated quality cards. The knowledge sits in three layers — framework tables in `sk-prompt`, per-model profiles in the `prompt-models` mode, and lean delegating mirrors in the `cli-*` executors — so a model's profile lives in exactly one place.

### 027 — The Names Settle  ·  _Prompt Engineering_
  - **flash** (MINOR_ISSUES, conf 0.92): 'the advisor already resolves the new name at full confidence' is overstated: both the archived implementation summary and a live advisor run show 0.95, not full (1.0) confidence; the old name sk-prompt-small-model also still resolves to the same target at 0.95. Minor framing note: the models rename chain began earlier (packet 114/008, sk-small-model→sk-prompt-small-model) but the final rename itself landed in this window, so 'started in earlier releases' applies cleanly only to the skill/agent renames.
    - _suggested fix:_ Change 'the advisor already resolves the new name at full confidence' to 'the advisor already resolves the new name at high confidence (0.95)'.
  - **glm** (OVERSTATED, conf 0.86): (1) 'closed out here' misattributes two pre-v3.6.0.0 renames (sk-improve-prompt→sk-prompt, @improve-prompt→@prompt-improver, both 2026-05-06) to v4.0.0.0. (2) The dominant v4.0.0.0 change — the 007-sk-prompt-parent fold-in dissolving the standalone sk-prompt-models skill into the sk-prompt parent hub — is not mentioned. (3) The remediation 'update sk-prompt-small-model → sk-prompt-models' is stale: sk-prompt-models no longer exists as a top-level skill post-fold-in (test -d → gone; hub graph identity is sk-prompt), so the correct target is sk-prompt (the hub); also 0 active-scope sk-prompt-small-model refs remain. (4) 'advisor resolves the new name at full confidence' is misleading: the advisor resolves the hub identity sk-prompt, not sk-prompt-models as a standalone, and the recorded probe confidence is 0.9262, not 1.0.
    - _suggested fix:_ Reframe around the actual v4.0.0.0 work: the sk-prompt-small-model→sk-prompt-models rename (2026-06-28) was the final standalone rename, and the 007-sk-prompt-parent program then folded the renamed sk-prompt-models skill into the sk-prompt parent hub as the prompt-models workflow packet (completed 2026-07-09), dissolving its standalone advisor identity. The earlier sk-improve-prompt→sk-prompt and @improve-prompt→@prompt-improver renames shipped in a prior release (May 2026, pre-v3.6.0.0) and are not v4.0.0.0 changes. Any lingering sk-prompt-small-model reference should now point to sk-prompt (the hub) — not sk-prompt-models, which no longer exists as a top-level skill; the advisor routes prompt-engineering queries to the single sk-prompt hub identity.

### 030 — Your Vault at the Terminal  ·  _MCP Tooling_
  - **glm** (MINOR_ISSUES, conf 0.82): The claim 'title search is broken headlessly so the mode uses content search instead' misstates the fallback: SKILL.md lines 157-158 and handover line 81 show name/title lookups are steered to `notesmd-cli list` + filter, while `search-content` is the body-content search surface (a different operation), not the substitute for title search.
    - _suggested fix:_ '…title search is broken headlessly so the mode steers name lookups to `notesmd-cli list` + filter (with `search-content` reserved for body text) instead…'

### 031 — Figma Leaves the Tree  ·  _MCP Tooling_
  - **flash** (WRONG, conf 0.92): 'The local mcp-figma developer skill is withdrawn' is false — the skill is present, registered, routed, and actively maintained through the release window. 'Figma work now routes to a separate Figma agent outside this repo' is false for this repo — the hub still routes to mcp-figma/SKILL.md; the external Barter Figma agent exists only per the archived cross-repo transfer spec, never as this repo's routing target, so no repointing happened or is required. The heading 'Figma Leaves the Tree' appears nowhere in the repo.
    - _suggested fix:_ Drop or invert the entry: the mcp-figma skill is NOT withdrawn in v4.0.0.0; it remains the hub's registered Figma transport. Optionally note that a Barter Figma agent was built in a separate repo (per archived spec z_archive/003-mcp-figma-transfer) but that its skill-removal commits never landed in this repo's history, so existing local-skill scripts/habits keep working unchanged.
  - **glm** (WRONG, conf 0.97): Every load-bearing assertion is false for this repo. (1) 'The local mcp-figma developer skill is withdrawn' — false: it was relocated (folded into the mcp-tooling parent hub as a transport mode), not withdrawn; the skill is live and user-invocable. (2) 'Figma work now routes to a separate Figma agent that lives outside this repo' — false: routing stays local via mcp-tooling/mode-registry.json (workflowMode mcp-figma → .opencode/skills/mcp-tooling/mcp-figma/); no external Figma agent exists in this repo's routing. (3) 'repoint them at the external agent' — false/misleading: the only path change is .opencode/skills/mcp-figma/ → .opencode/skills/mcp-tooling/mcp-figma/ (a same-repo relocate); no external agent to repoint to. The author likely confused the z_archived tri-repo withdrawal spec (different repos, 2026-05-05) with this repo's v4 fold-in work, which is the opposite action.
    - _suggested fix:_ Replace the entry with: 'mcp-figma was folded into the new mcp-tooling parent hub as a transport mode and now lives at .opencode/skills/mcp-tooling/mcp-figma/ (previously the flat path .opencode/skills/mcp-figma/). Repoint any scripts or habits that referenced the old flat path to the nested hub location. Routing is unchanged and stays local — Figma work still routes to the in-repo mcp-figma transport packet, not an external agent.'

### 032 — Goals in Every Tool  ·  _Hooks, Goals and the Runtime_
  - **flash** (WRONG, conf 0.9): 1) Claim says the goal system 'ports to Devin, Cursor and Pi', but Devin goal support was deliberately decommissioned in this same release window (commit cac19bbfa5e, 'BREAKING CHANGE: the Devin CLI no longer exposes ... a passive session-goal hook'; 009 impl-summary: 'zero Devin goal registrations') — Devin is not a port target in the released state. 2) 'Pi observes without verifying' is contradicted by the shipped adapter: Pi runs the heuristic verifier at turn_end and nudges via sendMessage; it only cannot force continuation (void-returning events). 3) 'a common active-goal store' reflects the pre-isolation model (one shared active-goal file per 003), superseded by per-scope hashed state files that the claim itself describes. Confirmed: Cursor injection-only, per-workspace/runtime/session storage, and the breaking legacy-singleton diagnostic-only/migrate-or-archive behavior.
    - _suggested fix:_ 'Your session goal now means the same thing in every tool you open. The goal system that started in OpenCode ports to Cursor and Pi through a shared core and a per-workspace/runtime/session store (Devin's goal hook was decommissioned in this release). Live capability probes recorded what each tool can actually do: Cursor is injection-only, and Pi verifies heuristically at turn end but cannot force continuation — it can only nudge. Goals no longer live as one global singleton; they are stored per workspace, runtime and session, so two projects cannot see each other's goals. **Breaking:** a goal saved under the old global scheme no longer injects; it sits in a diagnostic-only state until you migrate or archive it explicitly.'
  - **glm** (WRONG, conf 0.9): 1) 'ports to Devin, Cursor and Pi' is false at HEAD: 009 deliberately decommissioned Devin goal hooks (zero registrations, no adapter files) and REQ-010/SC-005 require removing stale Devin support claims — presenting a Devin goal port as a v4 feature reverses the actual release outcome. 2) 'Pi observes without verifying' is false: Pi has a shipped, live-verified turn-end verify (observe-only verifier + nudge); the real ceiling is no forced auto-continue, not no verify. 3) 'carries the same weight wherever you work' overstates the parity the capability matrix deliberately breaks (Cursor injection-only, Pi verify-only/no-continue, Devin gone). The confirmed parts are: shared core, honest live capability probes, Cursor injection-only, per-workspace/runtime/session storage, and the Breaking legacy-singleton diagnostic-only quarantine.
    - _suggested fix:_ The OpenCode goal system ports to Cursor and Pi via a shared core (Devin adapters were prototyped this cycle then deliberately decommissioned, and stale Devin claims removed). Live capability probes fixed each runtime's honest tier: Cursor is injection-only; Pi verifies at turn/session end but cannot force continuation. Goals are now stored per workspace, runtime, and session, so concurrent sessions cannot see each other's goals. Breaking: a goal saved under the old global singleton no longer injects; it sits in diagnostic-only quarantine until you migrate or archive it explicitly.

### 033 — Pi Chooses Its Own Path  ·  _Hooks, Goals and the Runtime_
  - **glm** (MINOR_ISSUES, conf 0.82): The phrase 'instead of quietly routing out to another model' overstates the prior behavior as a firm default; the spec says the prior state was ad-hoc/undefined ('pi may default to ad-hoc dispatch or cli-* skills'), not a guaranteed route-out. The 'Breaking: this is a changed default' wording is also slightly loose — it is a NEW default plus active enforcement (Phase 004 deny) that breaks previously-working ad-hoc cli-* spawn, rather than a change to an established prior default.
    - _suggested fix:_ When you hand Pi a subtask, it now hands it to its own subagents by default. Pi subagent dispatch defaults to native pi-subagents unless you explicitly name a `cli-*` mode, so a general request uses Pi's workers instead of ad-hoc dispatch to a cli-* executor. **Breaking:** cli-* spawn shapes (e.g. `cursor-agent -p`, `devin -p`, `opencode run`) that previously worked ad-hoc are now actively denied unless the current turn's user text names that `cli-*` mode — name the mode when you want a specific executor.

### 036 — A Standalone Advisor with a Skill Graph  ·  _The Skill Advisor_
  - **flash** (OVERSTATED, conf 0.9): The entry presents pre-v3.6.0.0 work as new in v4.0.0.0 ('now routes', 'grew', 'In the same pass'): none of the described work is inside the v3.6.0.0..HEAD window, whose actual advisor work was different (018 Codex Node runtime alignment, 017 audit/state-containment scaffold, 000 Rust-rewrite research, command-bridge generation, retiring code-graph/Open Design routing). The '21 per-skill metadata files' figure is also stale vs HEAD (12 files / 11 compiled skills).
    - _suggested fix:_ Replace with the window's actual advisor work, e.g.: 'The advisor graph was re-tuned: the compiled skill graph now spans 11 skills, routing to the retired code-graph and Open Design skills was removed, command routing moved to generated bridges, and the Codex launcher was pinned to the Node ABI required by its native SQLite module.' If the skill-graph history must be mentioned, note it shipped in v3.6.0.0 and use the current count.
  - **glm** (CONFIRMED, conf 0.92): Minor nuances not material: (1) conflicts_with edge exists in schema + a conflict-penalty function exists, but no conflicts were populated at ship time (all conflicts_with arrays empty) — claim implies conflicts are recorded data, true only at schema level; (2) 'every consumer cut over' is accurate at call-site level, but spec_kit_memory retained a temporary deprecated stdio proxy forwarding to the standalone server during a migration window, which the claim omits.

### 037 — A Command-Line Front Door  ·  _The Skill Advisor_
  - **flash** (WRONG, conf 0.93): The entire entry describes work released in v3.6.0.0, not the v3.6.0.0..HEAD window: 'You can now drive the advisor from the command line', 'gained', 'is now mandatory', and the 824.8ms measurement all predate the tag (the backing packet is archived under z_archive/011). The 'one breaking change' flag is also unsupported: v3.6.0.0 framed the CLI as additive with the primary MCP path intact, and the warm-only mandate applies only to the CLI fallback in prompt-submit hooks (fail-open at 0.8ms, warm 120-198ms), so nothing user-facing broke. In-window advisor CLI work (e.g. generated command bridges dffe5a06c03, trusted-prompt-time tests, bridge fallback canonicalization 9d6d8cec44c) is not what the entry describes.
    - _suggested fix:_ Move this entry out of the v4 changelog (it shipped in v3.6.0.0, already announced as 'Scriptable Command-Line Front Doors') and drop the 'breaking change' flag; if the section needs in-window content, describe the actual window work: command routing cut over to generated bridges, trusted-prompt-time CLI tests, and canonicalized bridge/hook fallback directives.
  - **glm** (MINOR_ISSUES, conf 0.74): (1) Release-window misattribution: the CLI fallback was fully shipped and present at the v3.6.0.0 baseline, so 'You can now drive the advisor from the command line' attributes an already-released capability to v4.0.0.0; the only v3.6.0.0..HEAD commits touching these files are kebab-case/plugin/checkpoint refactors, not the feature. (2) 'Breaking change' is overstated: warm-only is a design constraint on an additive CLI fallback; the 824.8ms one-shot native bridge was never viable on the prompt path ('kills per-prompt hook use'), so no previously-working behavior is broken.
    - _suggested fix:_ Drop the 'You can now' framing or move the entry to the release that actually shipped it (pre-v3.6.0.0). If retained as context, reword: 'The mk_skill_advisor CLI fallback (shipped previously) exposes a manifest-backed, byte-identical 9-tool surface hardened by scorer-parity, orphan-reaping, and a tri-daemon spawn drill, with warm-only invocation on prompt paths (the one-shot native bridge measured 824.8ms); the Python skill_advisor.py facade remains for backward compatibility.' Replace 'One breaking change to know' with a non-breaking design note, since warm-only constrains the additive CLI rather than breaking prior behavior.

### 038 — A Faster, Safer Warm Path  ·  _The Skill Advisor_
  - **flash** (MINOR_ISSUES, conf 0.93): The 'roughly a 25x gap' is a misattribution: 25.8538x is the structural batch mode's throughput multiplier, not the cold-vs-warm latency ratio, which is 46.9855/0.6081 ≈ 77x. All other assertions (0.61ms/46.99ms, caching, frontmatter-only parsing, batch mode, harnesses, uncertainty-on-by-default with confidenceOnly opt-out) are supported. Minor: the CLI exposes thresholds via tool options/--json, not a literal --threshold flag.
    - _suggested fix:_ Change to: 'so a warm answer lands in 0.61ms against 46.99ms for the cold subprocess path — roughly a 77x gap — while structural batch mode multiplies one-shot throughput ~25x, all pinned by permanent regression and benchmark harnesses.'
  - **glm** (MINOR_ISSUES, conf 0.9): All named features and the 0.61ms/46.99ms numbers are supported (rounded from 0.6081/46.9855). However, the claim frames the two latencies as a "roughly a 25x gap", but 46.99/0.61 ≈ 77x, not 25x. The 25x is the benchmark report's throughput_multiplier (batch amortization), a different metric than the latency ratio between the two stated numbers — so the attached ratio understates the actual latency gap ~3x.
    - _suggested fix:_ Replace "roughly a 25x gap" with a metric-accurate phrase, e.g.: "a warm answer lands in 0.61ms against 46.99ms for the cold subprocess path (a ~77x latency gap), with batch mode lifting throughput ~25.85x — now pinned by permanent regression and benchmark harnesses."
