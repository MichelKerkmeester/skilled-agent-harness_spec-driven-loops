# v4.0.0.0 — Release Notes

> 2849 commits since `v3.6.0.0`. Generated by the cheap-model release-notes pipeline.

v4.0.0.0 collapses Spec-Kit's sprawling surface into a smaller set of deliberate entry points: three skills (`sk-code`, `sk-prompt`, `system-deep-loop`) now cover code, prompt, and deep-loop work, while every CLI, MCP, and git tool shares one hook lifecycle, one spec gate, and one router. Context now survives compaction and restarts automatically across every runtime, memory search returns relevant results through hybrid vector + BM25 + graph fusion, and six fail-closed CLI executors carry live-verified provider and model catalogs. The gains land through focused renames, removals, and new default push and dispatch policies, so most upgrade cost is mechanical naming and routing work.

## Highlights

- **One skill per domain.** `sk-code` (all code modes with stack auto-detection), `sk-prompt` (improvement + small-model dispatch), and `system-deep-loop` (research, review, improvement, council) replace a fragmented set of per-surface and per-workflow skills.
- **Six fail-closed CLI executors.** Dispatch Codex, Claude Code, OpenCode, Cursor, Devin, and Pi through one framework, each with a per-mode provider/model catalog and roster verified against the live CLI before shipping.
- **Context survives compaction and restarts automatically.** Precomputed before compaction and auto-injected into new sessions, including hookless runtimes like OpenCode, with a `session_health` traffic-light check.
- **Memory search returns relevant results.** Hybrid retrieval fuses vector, BM25, and graph signals, fixing the "always zero results" bug and adding caching and dedup.
- **Semantic code search cuts token usage 10–50x.** A CocoIndex-backed `search` tool answers by intent ("find the auth middleware") across all six CLI configs, fully local and private.
- **Live-site `DESIGN.md` generation.** Pull a v3 style reference from any reachable site with every value tracing verbatim to `tokens.json` and a built-in validator gating fidelity.
- **Skill advisor is ~25x faster and more accurate.** In-process caching plus blended semantic/keyword/graph routing, with ambiguity flags for near-ties and prompt-safe `why_recommended` attribution.
- **safer git defaults.** Remote-push permission policy, owner-first worktree naming, mass-deletion and risky-command guards, and hardened refusals back a faster daily workflow.

## Breaking changes

- **Removed CLI executors:** `cli-gemini` and `cli-copilot` are gone — route through `cli-codex` or `cli-claude-code`. `cli-opencode` now defaults to the `opencode-go/deepseek-v4-pro` gateway, and Kimi K2.7 Code replaces the retired K2.6 registration.
- **Pi defaults changed:** subagent dispatch now defaults to native `pi-subagents`, and previously stored global goals no longer load automatically — migrate or archive them to the new per-session scoping.
- **Figma MCP work moved** to a new Figma MCP Agent, replacing the old `mcp-figma` skill.
- **`sk-code` merged:** `sk-code-full-stack`, `sk-code-web`, and the standalone `sk-code-opencode` skills were removed into the single `sk-code` skill; React/Node/Go/Swift/React Native guidance now returns a neutralized placeholder.
- **Open Design transport removed:** the `od` CLI, stdio MCP, and its 45-file skill tree are gone; design routes through Figma and Code Mode only.
- **`sk-doc` renames:** `/create:skill`, `/create:skill-parent`, `/create:readme`, and `/create:manual-testing-playbook` were renamed; `/create:flowchart` folded into `/create:diagram`; generated output now enforces kebab-case; benchmark runs migrated to the dated `<YYYY-MM-DD>--<subject>--<variant>` grammar; the `visual-skill` alias was removed; and the authoring agent is now `@markdown` (was `create`).
- **`sk-prompt` renames:** `sk-improve-prompt` → `sk-prompt`, `sk-prompt-small-model` → `sk-prompt-models`, `@improve-prompt` → `@prompt-improver`.
- **Deep-loop renames:** `system-deep-loop` replaces the `deep-loop-workflows` hub; the router agent is `@deep-loop` (was `deep`); `multi-ai-council` → `deep-ai-council`; `deep-agent-improvement` → `deep-improvement`; `/speckit:deep-review` replaces `/speckit:deep-research:review`; `/deep:context` is deprecated.
- **`sk-git` push policy:** pushes to any non-allowlisted remote branch are now blocked unless explicitly confirmed (`SPECKIT_ALLOW_REMOTE_PUSH=1` or a one-time go-ahead).

## Upgrade notes

- Reference the new names when invoking skills, agents, and commands (`sk-prompt`, `@prompt-improver`, `@deep-loop`, `@markdown`); the old aliases are gone from every runtime mirror.
- Re-route removed CLI executors to `cli-codex` or `cli-claude-code`, and update any Pi subagent dispatch or previously stored global goals to the new per-session scoping.
- Spec folders still resolve at the old path via a compatibility symlink, and existing benchmark run folders were already migrated — but pushes now require allowlisting or `SPECKIT_ALLOW_REMOTE_PUSH=1` for one-off pushes to non-allowlisted branches.

---

## What changed

### Agents
- Communication-quality rules now live in one shared home in `AGENTS.md`, so every runtime stays consistent instead of drifting.
- New guidance covers clear plain-language writing, honest recommendations, and an Ask-then-Do framing for complex requests.
- The deep Codex voice spec was reconciled with the root rules to keep the two in sync.
- The rules tune delivery without sacrificing rigor, avoiding an over-constrained or timid AI voice.

### External CLI Orchestration

- **Six fail-closed CLI executors.** Dispatch coding, review, and deep-loop work through Codex, Claude Code, OpenCode, Cursor, Devin, and Pi — every mode fails closed before dispatch when its binary is missing, and all six share the framework's full hook lifecycle, scope-lock, and spec gates.
- **One model catalog per mode.** Each CLI mode now carries a dedicated `providers-and-models.md` catalog (canonical provider/model/effort-tier lists in one place), and the hub's smart router covers all six modes instead of the previous three.
- **Expanded, live-verified model rosters.** New dispatch options include GPT-5.6 Luna/Terra/Sol (with per-model reasoning-effort ceilings), GPT-5.6 Luna Max, DeepSeek V4 Flash/Pro Max/Flash Max, Qwen 3.8 Max, GLM 5.3, Kimi K2.7 Code, MiniMax, MiMo-V2.5-Pro, and GLM-5.2 — every id verified against the live CLI before shipping.
- **Cross-runtime agent and hook consistency.** Codex agents are generated from your canonical OpenCode agent set, Devin uses all 13 repo agents as subagents, and Cursor sessions enforce spec gates through native `hooks.json` adapters that are live-verified to actually block denied tool calls.
- **Opt-in plain-English output projection.** Enable `sk-communication` per machine to rewrite terse CLI output into readable prose while preserving the exact original underneath, with an automatic original-only fallback and a compatibility doctor.
- **Reduced dispatch hangs.** Deep-research/deep-review scripted dispatch now appends `</dev/null>`, cutting a 12+ minute stall to ~16 seconds and a full 10-iteration loop from ~3 hours to ~50 minutes.

**Breaking:** The `cli-gemini` and `cli-copilot` executors are removed — route those workflows through `cli-codex` or `cli-claude-code` instead. `cli-opencode` now defaults to the opencode-go/deepseek-v4-pro gateway (the `github-copilot` provider is no longer documented), and Kimi K2.7 Code replaces the retired K2.6 registration.

### Hooks & Runtime

- **Goals now follow you across runtimes.** Set an active goal once and it reaches Cursor, Pi, and Devin sessions from a shared goal-state file, with a new `goal manage` command (set/show/history/clear/complete/pause/resume/doctor) mirroring the OpenCode `/goal` router.
- **Goals are scoped per workspace, runtime, and session**, so parallel sessions no longer overwrite each other's active goal; Pi gets a native `/goal-pi` command bound to its own session identity.
- **Pi message sends got ~1000x faster on repeat prompts** — they resolve from the advisor's in-process cache in ~1–2 ms instead of ~1.4 s, and the 5-minute cache now invalidates correctly.
- **Pi prompt-cache ownership is now clean and observable**, with a hardened deep-pi extension for DeepSeek-direct models, persistent machine-readable cost stats, and per-session diagnostics surfaced instead of silently swallowed.
- **The SPEC FOLDER QUESTION no longer nags on read-only turns**, and every gate option now tells you exactly to reply with the folder path (or skip with E).
- **Hook bloat reduction ships opt-in and shadow-verified** — route summaries cap at three targets with a `+K more` digest, and repeat injections dedupe on session/message identity without changing your prompts byte-for-byte until you flip the flag.
- **One flag (or 20 individual toggles) now governs the whole hook layer**, with personal defaults persisted in `.opencode/hooks/hook-flags.env` and a single canonical index at `.opencode/hooks/README.md`.
- **Goal-hook playbooks now cover every goal-capable runtime**, with live verification proving the goal appears verbatim in Pi and Devin model output (Cursor stays model-invisible by contract; OpenCode's native goal tracking remains TUI-only).

**Breaking:** Pi subagent dispatch now defaults to native `pi-subagents` and denies dispatch-shaped calls lacking explicit `cli-*` authorization, and previously stored global goals no longer load automatically — you must migrate or archive them to the new per-session scoping.

### MCP Tooling
- Design reference research gained two read-only CLI transports — **mcp-refero** (styles, screens, flows) and **mcp-mobbin** (screens, flows, sections) — both pairing with sk-design for judgment; Mobbin needs only a browser OAuth handshake, while Refero requires a paid plan.
- A new **mcp-aside-devtools** skill drives the Aside AI browser via the aside CLI (agent tasks or deterministic REPL) or the Aside MCP through Code Mode, with a doctor, installer, and 15-scenario playbook.
- A new **mcp-obsidian** mode manages vaults and notes from AI sessions, combining the headless notesmd-cli, the app-backed obsidian CLI, and MCP at the file layer, with feature routing and plugin reference guidance.
- The new **mcp-click-up** skill gives agents a structured, safe ClickUp interface — cupt CLI for daily ops, the official MCP for advanced features, a one-command installer, and safety invariants like dry-run before batch changes.
- All six MCP bridges now route through a unified **mcp-tooling hub**, and the router sends every tested prompt to the correct skill (13/13 hub-wide, 49/49 across modes), with benchmarks now blocking wrong-route verdicts instead of silently passing.
- Semantic code search — a CocoIndex-backed `search` tool working across all six CLI configs — lets you ask by intent ("find the auth middleware"), runs fully local and private, and typically cuts token usage 10-50x versus Grep-Read-Filter.
- Every MCP skill now ships consistent install and diagnostic tooling under a single `/doctor:mcp` command, plus manual-testing playbooks (75 scenarios across 19 categories) and READMEs that list the full tool set.
**Breaking:** Figma MCP work moved to a new **Figma MCP Agent** (published in AI_Systems/Barter and a sanitized copy in AI_Systems/Public), replacing the old mcp-figma skill; existing figma-developer-mcp tooling still works here.

### Code (sk-code)

- One `sk-code` skill now routes every code request to the right mode — implement, quality, debug, verify, or review — auto-detecting your stack (Webflow, OpenCode, Rust, React/Next.js, Go, Swift, React Native) and bundling that surface's standards with the workflow.
- Code review became a first-class mode: a stack-agnostic baseline (universal security and correctness minimums) runs on every review, then a detected stack overlay (opencode, web, or full-stack) adds specific rules, with KISS/DRY/SOLID and SRP/OCP/LSP/ISP/DIP checks routed explicitly into the checklist.
- Rust is now a first-class sk-code language, auto-detected via `.rs` plus Cargo files and backed by a full style guide, quality standards, and quality gate enforcing deterministic builds, documented `unsafe`, and stable napi-rs/WASM interop — multi-language tasks load both standards in one pass.
- Review efficiency improved: a state hash skips unchanged PRs, skipped or deduped reviews still report COMMENTED rather than silently passing, and an opt-in `SK_CODE_REVIEW_MIN_CHANGED_LINES` skips trivial diffs while sensitive work (security, config, persistence, dependencies, sandboxing) is always reviewed.
- Verification is harder to fake: a mutation-check ritual requires breaking the code to confirm a test truly fails, and a verification ladder (unit → in-memory → on-server → live) names each rung's blind spot up front.
- The stale-dist warning no longer paints over terminal chat input and now skips unprovisioned packages in bare worktrees, reporting only genuine staleness through system context with an operator log.
- Webflow reference docs are safer and clearer: session-cookie and CDN-loader examples are secure to copy (allowlisted HTTPS origin plus Subresource Integrity), and every "When to use" section states a concrete scenario from the page's own content.

**Breaking:** The deprecated `sk-code-full-stack`, `sk-code-web`, and standalone `sk-code-opencode` skills were removed and merged into the single `sk-code` skill. React/Node.js/Go/Swift/React Native guidance now returns a neutralized placeholder pointing to git history, while Webflow's live guidance and the webflow deployment workflow remain fully working.

### Design (sk-design)

- Generate a v3 `DESIGN.md` style reference from any reachable live site, with every color, spacing, font, shadow, and radius value tracing verbatim to `tokens.json` so designs stay truthful to the source rather than invented.
- A built-in validator gates the result on value fidelity and claims quality, and dark-mode, responsive, motion, icon, and accessibility sections appear only when real evidence is detected — with honest absence otherwise.
- A local, offline library of all 1,290 design styles from styles.refero.design (129 MB / 7,744 files, shape-validated 1,290/1,290) lets you pull real-world references without leaving the skill.
- You can query that library with a retrieval CLI, and the design modes (interface, audit, foundations, motion) now ground their work in it with a fixed authority order — your brief first, then mode judgment, target evidence, and corpus reference — while `design-md-generator` validates output against one versioned v3 schema.
- Design handoffs to Open Design carried a metadata-only grounding receipt with proposed-vs-returned reconciliation, keeping style provenance verifiable without exposing raw payloads.
- The sk-design hub now makes intake, visible planning, and proof expectations explicit before work starts, with fourteen private procedure cards giving each of the five modes a written trigger, output contract, and proof gate.
- Design-mode routing vocabulary was sharpened and synchronized across the hub, registry, and router, and the modes gained complete feature catalogs plus 23 new manual-testing scenarios.
- **Breaking:** The Open Design transport (the `od` CLI and stdio MCP) and its 45-file skill tree are fully removed; design workflows now route exclusively through the two remaining transports, Figma and Code Mode.

### Documentation (sk-doc)

- The `/create:*` command family now covers every documentation artifact, adding `/create:command`, `/create:benchmark`, `/create:diagram` (27 HTML/SVG diagram types plus ASCII/markdown flowcharts), and `/create:diff` for self-contained local before/after document reports.
- Four commands were renamed to the `/create:<packet>` convention — `/create:skill`, `/create:skill-parent`, `/create:readme`, and `/create:manual-testing-playbook` — and `/create:flowchart` was folded into `/create:diagram`.
- All ten create-* skills now share one consistent routing contract, so documentation requests land on the right tool (existing-document audit goes to create-quality-control, README/flowchart/diagram stay with their dedicated packets) and bare trigger words like "benchmark" no longer hijack a skill.
- Documentation skills resolve through the single canonical `sk-doc` alias everywhere, and the authoring agent is now `@markdown` across every runtime mirror.
- Generated skills, feature catalogs, and playbooks now ship with kebab-case directories and filenames, backed by a repo-wide guard that fails on new snake_case names (Python, vendored trees, lockfiles, and generated output stay exempt).
- Benchmark runs now use one dated naming convention (`<YYYY-MM-DD>--<subject>--<variant>`), write results into `<skill>/benchmark/reports/<dated-run>/`, and auto-append to a drift-proof index; all 78 existing run folders were migrated.
- Two new opt-in truth gates mechanically enforce the code-folder README standard, catching broken path references and stale inventory counts, and a fleet auditor discovers durable code folders and tracks results in a persisted manifest.
- Skill READMEs were rewritten purpose-first (one-line pitch, problem-first overview, banned em-dash/Oxford-comma style enforcement), and the new `sk-communication` skill turns terse agent output into plain English without touching canonical bytes.
- Generated markdown now follows one ratified structure standard (numbered ALL-CAPS H2 headings separated by `---` dividers, READMEs free of TOC/nav anchors), opt-in via `SKDOC_ENFORCE_STRUCTURE=1`, while spec-kit continuity anchors stay required and untouched.

**Breaking:** `/create:skill`, `/create:skill-parent`, `/create:readme`, and `/create:manual-testing-playbook` were renamed; `/create:flowchart` was removed in favor of `/create:diagram`; all new generated output now uses kebab-case filenames enforced repo-wide; benchmark runs migrated to the dated `<YYYY-MM-DD>--<subject>--<variant>` grammar; the `visual-skill` alias was removed in favor of `sk-doc`; and the authoring agent was renamed from `create` to `@markdown`.

### Git (sk-git)
- **Remote push permission policy:** only `main`, `skilled/v*` release branches, and your allowlisted patterns push to origin without asking; every other push needs a fresh confirmation, backstopped by a pre-push hook and a new `validate-remote-allowlist <branch>` command (local branch/worktree creation stays unrestricted).
- **Live-branch autosync:** every commit from a wrapper-launched AI session auto-publishes to a shared live branch that your primary IDE checkout fast-forwards to — gated to those sessions only, never force-pushing, and behaving identically across Claude, Codex, and OpenCode.
- **Owner-first worktree naming:** new worktrees get consistent `<skill>/{NNNN}-{slug}` names with the number auto-allocated by a locked, clone-wide counter (collision-safe under concurrency, failing cleanly at ≥9999), and branches are created only via `git worktree add`.
- **Safer session and reaper tooling:** worktree sessions reject `..` and absolute paths so they can't escape the workspace, and the reaper verifies both daemon PID and the exact `work/<runtime>/<slug>` directory before signalling — only auto-deleting clean, merged, inactive sessions.
- **Risky-command advisory hook:** a state-gated advisory (17 rules) fires the moment you type dangerous git like hard resets on a dirty tree, forced cleans, unmerged `branch -D`, or stash clears — across six runtimes, never blocking, and suppressible.
- **Mass-deletion guard:** commits deleting over ~100 tracked files are blocked by default, tunable via `SPECKIT_MASS_DELETION_THRESHOLD`, authorizable per-operation with `SPECKIT_ALLOW_MASS_DELETION=1`, audited, and fail-open so a missing hook never locks you out.
- **Hardened git refusals:** sk-git now refuses `git commit --no-verify` and amending published commits, on top of existing force-push and secrets protections.
- **GitKraken MCP integration:** the GitKraken server (31 tools) is registered in Code Mode with a safety guide, and GitKraken-shaped requests route automatically to sk-git.
- **Documentation and test coverage:** sk-git ships a feature catalog of all 11 capabilities, a 19-scenario manual-testing playbook (GIT-023–GIT-041), per-directory READMEs, and corrected `github.github_*` and `gh`-only CI/CD references.

**Breaking:** the new remote push permission policy changes default behavior — pushes to any non-allowlisted remote branch are now blocked unless explicitly confirmed (with `SPECKIT_ALLOW_REMOTE_PUSH=1` or a one-time go-ahead).

### Prompt (sk-prompt)
- The `sk-prompt` skill is now the single home for prompt work, merging prompt-improvement and small-model dispatch into one hub with a router benchmark that scores 100/100 across both modes.
- Run `/prompt-improve` for the full DEPTH-thinking, CLEAR-scored prompt-enhancement engine — it replaces the old `/prompt` command (the `/prompt` command and prompt-improver agent still work; only names changed).
- Get automatic framework selection across 11 prompt frameworks plus CLEAR quality scoring, with six intent categories (text, UI, image, video, framework, format) routing your request to the right guidance.
- Get per-model prompt-craft profiles for all eight active small models (DeepSeek, GLM-5.2, and more), each naming preferred frameworks, fallbacks, avoid lists, and evidence, so dispatch stays inside each model's context window.
- All five CLI executor skills now share one three-tier prompt-composition precedence rule, with inline framework tables thinned into mirrors of a single canonical card to end cross-executor drift.
- Opt-in extras ship disabled by default: output verification, quota-aware fail-fast model fallback, and Bayesian scoring — enable them when you need them.
- A 28-scenario manual testing playbook for the sk-prompt skill ships for validating prompt-improvement workflows.

**Breaking:** Several names changed framework-wide: the skill is now `sk-prompt` (was `sk-improve-prompt`), the small-model skill is now `sk-prompt-models` (was `sk-prompt-small-model`), and the agent is now `@prompt-improver` (was `@improve-prompt`). The old names are gone from all runtime mirrors — use the new names when invoking or referencing them.

### Deep Loop
- All deep-loop workflows — research, review, improvement, and AI council — now live in a single unified `system-deep-loop` skill, invoked through a router agent renamed to `@deep-loop` (formerly `deep`) so it no longer collides with `/deep:*` commands or `deep-*` agents.
- Research and review loops gained a divergent convergence mode (verified in 10/10-iteration runs), while a 20-iteration self-review of the unified skill caught and fixed 7 runtime bugs.
- New `/deep:alignment` mode audits your docs, code, designs, or git history against a chosen parent skill's own standards and templates, verify-first, with a per-authority known-deviation list to suppress recurring findings.
- Deep-review and AI council are more robust: strategy files with missing headings now self-heal, iteration state appends deterministically instead of crashing, and council output records per-seat execution provenance while rejecting reserved executor collisions up front.
- Benchmarking broadened: `--executor codex` measures models live over the codex transport (measurement-equivalent to opencode, 86 vs 85), `/deep:command-benchmark` audits the command corpus on structural + behavioral axes, and `create-benchmark` now authors a `conformance_benchmark` family.
- Deep-loop runs gained an append-only, tamper-evident event ledger with versioned replay fingerprints and certified phase receipts, plus live-search fan-out with per-executor tool-policy enforcement.
- AI council now persists a durable, sha256-audited write protocol and a SQLite-backed decision graph (`council_graph_*` MCP tools) that replays provenance up to 13x faster than re-reading sources.
- Improvement and model/skill benchmarking converge on an evaluator-first loop with five-dimension holistic scoring (replacing ~15-20% keyword checks), sha256 mutation dedup, and cross-executor config-dir routing.
- Durability is standard across loops — named stop reasons, legal-stop gates, resume cursors, append-only journals, and duplicate finding/mutation dedup — with `/goal` adding persistent per-session goals and strict verifier auto-completion.
- **Breaking:** the unified `system-deep-loop` skill replaces the old `deep-loop-workflows` hub + `deep-loop-runtime` backend; the router agent is now `@deep-loop`; `multi-ai-council` → `deep-ai-council`, `deep-agent-improvement` → `deep-improvement`, and `@improve-agent` → `@deep-agent-improvement`; `/speckit:deep-review` is now a standalone command replacing `/speckit:deep-research:review`; standalone `/deep:context` is deprecated (its capabilities folded into `/deep:research` and `/deep:review`); and the ai-council agent is now subagent-only.

### Skill Advisor

- Routing is markedly more accurate: recommendations now blend semantic similarity, keyword, and graph signals through a single metadata-driven resolver shared by both scoring engines, so intent-described prompts land on the right skill and misroutes (e.g. agent-scoring queries) are fixed.
- Near-tie matches are now flagged as ambiguous instead of collapsed into one confident pick, while clearly superior recommendations are still selected decisively.
- Routing defaults are safer: `--threshold` no longer silently disables uncertainty guarding (opt into confidence-only behavior explicitly), and command bridges are deprioritized for natural-language prompts unless you use an explicit slash command.
- The warm path is roughly 25x faster thanks to in-process caching and frontmatter-only parsing, and batch mode (`--batch-file`/`--batch-stdin`) cuts subprocess overhead.
- The advisor runs as a standalone MCP server with a native TypeScript scorer and a reindexed, validated skill graph, plus a 9-command CLI whose schemas are byte-identical to the MCP tools.
- Proactive skill suggestions now surface through prompt hooks across Claude, Gemini, Copilot, Codex, and Devin runtimes, failing open quickly (with a warm-only CLI fallback) when the advisor is unavailable.
- The advisor is more resilient: it detects and reports stalled deep-loop runs, recovers from dead sockets, shares one bridge call for concurrent identical prompts, and fails closed with a stale-vector warning rather than serving untrusted matches.
- You can now see *why* a skill was recommended via `includeAttribution`'s prompt-safe `why_recommended` breakdown, and check vector-lane health on demand through `advisor_status`.
- The embedder is pluggable: pick from six options (including jina-embeddings-v3, nomic-embed-text-v1.5, and bge-m3) and swap the active one with a single operator call backed by a documented runbook.
- A new `/doctor:skill-advisor` command analyzes your skills, optimizes scoring tables, and re-indexes the graph, while skill CLI dispatches are checked against hard rules before running.

### Spec Kit & Memory

- **Spec folders moved** to a top-level `specs/` directory (from `.opencode/specs/`), with the old path kept as a compatibility symlink so existing references keep working.
- **Context now survives compaction and restarts automatically** — it's precomputed before compaction, auto-injected into new sessions, and auto-primes across startup, resume, clear, and compact events (including hookless runtimes like OpenCode), plus a new `session_health` tool reports traffic-light session status.
- **Memory search actually returns relevant results** — hybrid retrieval fuses vector, BM25, and graph signals with confidence-based truncation, fixes the "always zero results" bug, and adds caching and dedup for faster repeat lookups.
- **Safer memory tooling** — deleting a single record now requires confirmation, and full scans/reindexes no longer rewrite your tracked source `.md` documents; `memory_index_scan` also runs in the background by default.
- **Split any spec into phases** using the `:with-phases` suffix and `--phase-folder` argument on existing plan/complete commands, with phases recommended, scaffolded, and validated automatically.
- **Current and consistent docs** — the rewritten root README documents the full workflow, and skill/command/env docs now match shipped behavior; the spec tree is organized into six themed tracks instead of a flat list.
- **Leaner templates and tighter checks** — Level 1 research templates scaffold ~175 lines instead of 944, the four core templates were consolidated into one shared core, and a new advisory scope-adherence check catches out-of-scope edits before review.
- **Nested changelog generation** for packet roots and child phases via `/create:changelog`, written to the parent's centralized `changelog/` directory.
- **Daemon and MCP hardening** — MCP tools reject unapproved arguments before running, the daemon gained startup reliability and ownership-safety fixes, and memory health reports actual index consistency.

---

## Appendix: Internal & developer changes

#### agents
- **Terminal-proof discipline woven into agent framework** — Each agent turn now opens with a proof-over-appearance directive through the existing governor chain, so responses favor demonstrated verification over confident claims.
- **Runtime surface audit maps six-runner gaps** — You now have an evidence-backed map of every doc and script still enumerating three runtimes (Claude, Codex, OpenCode) instead of the six actually shipped, with 20+ sites identified.
- **AGENTS.md bloat audit and cleanup** — A five-iteration read-only audit of the root AGENTS.md produced a ranked bloat report that separated safe cuts from a protected preserve set of unique rules.

#### cli-external-orchestration
- **cli-opencode and cli-claude-code consolidated under cli-external hub** — Consolidated the cli-opencode and cli-claude-code skills into one cli-external parent hub with two workflow modes, aligning them with the canonical hub structure already used by sk-code, sk-doc, sk-prompt, and system-deep-loop.
- **Canonical cli-external-orchestration hub identity** — `cli-external-orchestration` is now the single canonical hub directory for external CLI dispatch, with full git history preserved through the rename.
- **Pi Remote Mobile Agent: nine-phase plan approved** — Approved a nine-phase implementation program for a private, Claude-app-style mobile control plane over the Pi CLI, from contract and threat baseline through release verification and rollout.
- **Pi remote experience research landed** — Completed a two-lineage deep-research run converging on a typed, redacted, replayable event ledger with a separate authenticated command protocol as the target Pi remote-experience shape.

#### hooks
- **Hook reference docs relocated to owning trees** — Four hook reference docs now live in the trees that own them, so each doc sits next to the code and config it describes.

#### mcp-tooling
- **MCP packet docs aligned with canonical templates** — The asIDE DevTools, Refero, and Mobbin packet documentation now follows the canonical create-skill and create-readme structures, so navigation through assets, references, and script READMEs is predictable.

#### sk-code
- **Unified template alignment for sk-code split docs** — All 163 split reference/asset docs across code-opencode, code-webflow, and code-quality now conform to one standard template—snake_case filenames, uniform frontmatter, and a consistent OVERVIEW structure—making the skill surface predictable and validation-clean.
- **Conformance gate repair and drift sweep groundwork** — Authored files are now actually checked against the sk-code standard, because the post-edit hook that was silently dead now runs.
- **Stale dist builds now self-heal at session start** — Session-start staleness checks actually detect stale builds again now that the broken watch entry that crashed the sweep was removed.
- **sk-code scripts relocated into asset folders** — Webflow minification utilities now live beside Webflow assets under assets/webflow/scripts/, keeping surface-specific build tooling co-located with what it processes.
- **sk-code-review now has an 18-scenario manual testing playbook** — sk-code-review now ships an 18-scenario manual testing playbook, so operators can deterministically verify review behavior before declaring a baseline change safe to ship.
- **Ponytail refinement research for sk-code** — A 12-iteration, two-model deep-research pass produced a prioritized, file-mapped recommendation report for transplanting ponytail's design-gating and review mechanisms into sk-code and sk-code-review, resolving the design-vs-result verification tension as complementary.

#### sk-design
- **Documented design-foundations mode contract** — Captured the already-shipped design-foundations mode's static visual-system contract (axes, activation triggers, read-only execution, procedure-card rule, and sk-code handoff) as a tracked spec-folder packet, so it is now inspectable in git and memory.
- **Interface mode spec packet reconstructed** — The sk-design interface mode now has a tracked spec-folder packet that documents its direction, voice, information architecture, composition, routing, and handoff contract, so the mode's behavior is inspectable without reading source code.
- **Reconstructed motion-mode spec packet** — The shipped sk-design motion mode now has a tracked, source-faithful spec packet, so its restraint gate, timing/easing bands, routing, and reduced-motion handoff are inspectable without reading the skill source.
- **sk-design audit mode spec reconstruction** — Documented the already-shipped sk-design audit mode in a Level-2 spec packet, making its findings-first P0-P3 severity, five-dimension /20 scoring, accessibility coverage, and Read/Glob/Grep-only surface inspectable and verifiable.
- **sk-design hub routing contract documented** — The sk-design hub routing contract is now inspectable as a Level-2 spec packet instead of living only in the three intact hub source files.
- **Shared backbone now has a tracked spec** — The sk-design shared backbone is now documented in a tracked spec packet for the first time, recording the register, context-loading, proof-gate, dispatch, token, procedure, and verification contracts its modes already consume.
- **Style database engine built, ~21x faster** — The sk-design style engine gained an indexed SQLite+FTS5+vector database built over all 1,290 styles, delivering roughly 21x faster retrieval than the flat-file path when enabled.
- **Style database packet: no docs** — This packet carries no source documentation, so no user-facing behavior can be described or claimed.
- **sk-design Structure & Naming Cleanup** — Removed a stray `styles/docs` README and misplaced playbook whose content the hub playbook already covers, leaving playbook guidance in one canonical place.
- **sk-design template conformance and cleanup** — sk-design skill docs are now consistent with the create-skill templates, with structural defects fixed across design-interface (references, assets, feature catalog, playbook) and design-motion.

#### sk-doc
- **Hub-doc conformance review and remediation plan** — The cli-external and mcp-tooling hub docs were audited line-by-line against live CLI/MCP truth and the sk-doc templates, so no reality claim was taken on faith.
- **Feature-catalog integrity validation and enforcement** — Feature catalogs are now automatically validated for integrity across every catalog under `.opencode/skills/`, so drift can no longer mislead reading agents.
- **Playbook package validator catches drifted operator scenarios** — Manual-testing playbook packages can now be validated mechanically against the sk-doc operator-scenario contract, replacing hand-typed coverage claims that had drifted in every hub counted.
- **sk-doc template storage regrouped by component type** — Skill, agent, and command templates now live in purpose-grouped folders (assets/skill/ and assets/agents/), making them easier to find and maintain.
- **sk-doc agent template aligned to production patterns** — Agents scaffolded from the sk-doc template now mirror current production patterns from the live 10-agent fleet instead of stale examples.
- **Flattened template paths, references updated** — sk-doc's most-used templates (agent and command templates, plus the feature-catalog and testing-playbook packages) now load from shorter, top-level paths under assets/.
- **sk-doc smart router stress test** — The sk-doc smart router now has a 45-cell stress-test matrix (15 scenarios across 3 CLIs) plus a manual testing playbook, so routing behavior is verified before release.
- **Refined extractor exposes cli-copilot path hallucination** — We now measure each CLI's resource-citation accuracy fairly (basename + full-path matching), and cli-copilot scores just 11.1% because it invents file names like dqi_rubric.md that don't exist in sk-doc.
- **Router intent coverage extended and measured** — The sk-doc router's test coverage now spans all 11 RESOURCE_MAP intents, closing the OPTIMIZATION and INSTALL_GUIDE gap left by earlier runs.

#### sk-git
- **Autosync architecture research frozen** — Closed out the parallel-session git autosync research phase with a frozen architecture decision: a four-plane serialized publisher that never lets the primary branch fall behind and never force-pushes, with the AI conflict resolver bounded as a proposer-only and GitKraken limited to read-only observability.
- **Maintainer-flags filter now documented** — A new reference explains the maintainer-flags content filter, mapping the four config files it rewrites and the five keys it changes.
- **Git subprocess env isolation hardening** — sk-git and sk-doc tooling now scrub repository/config-redirecting GIT_* variables (GIT_DIR, GIT_WORK_TREE, GIT_CONFIG, and friends) from every git subprocess, so a poisoned parent environment can no longer corrupt the real repository's .git/config.
- **Superset worktree alignment: spec only, never shipped** — No user-facing change: this sk-git worktree-alignment spec was written but never implemented, so the skill was not updated.
- **sk-git README confirmed current and valid** — Confirmed the sk-git README stays authoritative as the framework's canonical voice reference, so docs keep one consistent tone across skills.
- **Canonical commit-message standards locked for repo** — The framework now has a locked, canonical commit-message standard covering subject taxonomy, packet-ID handling, trailer policy, body policy, special cases, and length caps (50-soft / 72-hard).
- **Planned worktree convention and advisor routing** — sk-git will standardize feature worktrees on `wt/{NNNN}-{name}` branches with matching `.worktrees/{NNNN}-{name}` directories, numbered as the highest existing index plus one.

#### system-deep-loop
- **Smart-routing benchmark: 20-run verification matrix** — Every skill router is now verified by a full 20-run benchmark matrix — 8 children plus both hubs, each scored in deterministic router-replay and live-model modes — so routing regressions are caught before they reach users.
- **Graph-Engineering Research Charts Next Deep-Loop Architecture** — Research defined a single coherent design for evolving system-deep-loop into a graph-based agent-loop engine, in which a graph decides eligible work and order, a bounded loop executes each unit, typed evidence stacks, and only the 036 authority plane may authorize protected mutations.
- **Deep-loop command-flow stress-test scaffolding** — Internal QA scaffolding was prepared to stress-test the deep-research and deep-review command flows, extending the methodology already proven on the agent-improvement loop.
- **Rename sk-improve-agent to deep-agent-improvement** — The agent-improvement skill is renamed from `sk-improve-agent` to `deep-agent-improvement`, aligning it with the `deep-review` and `deep-research` naming family.
- **Parallel fan-out groundwork** — Deep-loop gained internal building blocks for opt-in parallel multi-executor fan-out: validated fan-out config parsing plus a concurrency-capped worker pool with a JSONL status ledger, with the single-executor path left byte-identical.
- **Deep-review optimization keeps baseline unchanged** — The @deep-review agent was run through a 5-iteration improvement loop via cli-copilot and kept as-is, since every candidate scored at the 100/100/100/100/100 ceiling with zero delta.
- **Deep-research skill audit keeps baseline** — Ran a 5-iteration improvement loop on the @deep-research skill covering discipline hardening, concision, edge cases, integration, and best-of synthesis, with every candidate scoring 100 across all five quality axes.
- **Deep-loop skills renamed to deep-review and deep-research** — The deep-loop skills are now consistently named `deep-review` and `deep-research` across every surface — folders, OpenCode internals, runtime mirrors, root docs, and configs — instead of the old `sk-deep-*` IDs.
- **Deep-loop runtime hardening and convergence signals** — State writes are now cheaper and safer: unchanged content is skipped via serialize-diff, JSONL merges run under lock with dedupe, and a deferred writer coalesces redundant saves.
- **Behavioral benchmarks for all five deep-loop skills** — Every deep-loop skill — deep-review, deep-research, deep-context, deep-ai-council, and deep-improvement — now ships a behavior benchmark package that scores executors against realistic, often vague user prompts.

#### system-skill-advisor
- **Rust rewrite of skill advisor researched** — Mapped whether the skill-advisor backend's ~51K LOC TypeScript deserves a Rust rewrite, so any engineering commitment is grounded in cited evidence instead of assumptions.
- **Contain stray state directories, audit advisor** — Stops runtime writers from planting nested .opencode state trees and cleans up the 40 already leaked into the workspace.
- **Codex skill advisor Node ABI startup fix** — Codex sessions now finish MCP startup instead of aborting, because the skill advisor launches on the Node runtime (ABI 141) its native SQLite module requires.
- **Skill doc triggers route advisor matches** — The skill advisor now harvests title, description, trigger phrases, and tier from each skill's references/assets docs (84 of ~355 today) into the skill graph when SPECKIT_ADVISOR_DOC_TRIGGERS=true, so doc-level detail that used to be dead metadata can influence routing.
- **Advisor docs aligned to canonical contract** — Skill routing is now more reliable because all 15 system-skill-advisor reference docs carry the canonical frontmatter contract (importance tier, context type, trigger phrases).

#### system-speckit
- **Rust-rewrite feasibility research** — Opened a 20-round deep-research pass to decide whether, where, and how the TypeScript backend (~244K LOC) would benefit from a Rust rewrite, grounded in code-referenced evidence rather than assumptions.
- **Documentation cleanup aligned to shipped state** — Repository and skill documentation now matches the shipped codebase: READMEs, skill docs, agent definitions, command docs, and feature catalogs were swept for stale references, wrong counts, and broken paths, with every corrected path verified to resolve.
- **Spec Root Resolution Hardening** — Spec-root resolution now consistently uses a single canonical-first contract everywhere, eliminating the conflicting behaviors previously spread across ~20 call sites and reducing the risk of divergent or duplicate spec data.
- **Archived command-dispatch fix as clean record** — The command-dispatch vulnerability fix is now a clean Level 1 historical archive you can inspect without stale structure or broken references.
- **Agent routing doc archive normalized** — The archived Agent Routing Documentation Update is now a clean, validator-safe Level 1 historical record you can inspect without obsolete verification scaffolding.
- **Archived node-modules relocation packet normalized** — The node_modules relocation archive is now a readable, validator-safe Level 1 record instead of an incomplete single-file package.
- **Archived script-audit records kept readable** — You can now inspect the archived comprehensive script audit as a clean, validator-safe Level 1 historical record.
- **Archived documentation-upgrade folder repaired to current standards** — The archived documentation-quality-upgrade topic was rebuilt to the current Level 1 structure, so its feature summary and outcome remain inspectable without obsolete higher-level scaffolding.
- **Archived generate-context subfolder record** — The generate-context subfolder support archive was normalized into a clean, validator-safe Level 1 historical record that preserves the feature summary and repair outcome.
- **Spec-folder level upgrade script archive cleaned** — The archived spec-folder level upgrade script topic now reads as a clean Level 1 historical record, so you can inspect it without obsolete higher-level structure.
- **Audit archive normalized to Level 1** — The system-wide remediation folder now reads as a concise, validator-safe historical record instead of carrying obsolete higher-level structure.
- **Archived full spec-doc indexing record** — Archived the full spec-document indexing work as a clean, Level 1 historical record rebuilt from current templates.
- **Archived documentation-alignment folder normalized to current templates** — Archived the documentation-alignment topic as a compact Level 1 historical record that is validator-safe and easy to inspect.
- **Archive docs normalized for validator compatibility** — The archived Upgrade Auto Populate folder now has a current Level 1 documentation set that passes today's validator with zero errors.
- **Archived spec docs normalized to pass validation** — You can now open archived spec folders and inspect them without tripping over stale structure, broken markdown references, or mismatched metadata.
- **Spec-Kit README cleanup and release readiness** — Finding what you need in the repo docs is faster now that the root README is restructured spec-first, with redundant marketing sections removed and navigation simplified (net -105 lines).
- **Archive normalized to current template** — Archived Memory Index Txt Support docs were rebuilt to the current Level 1 template so the archive reads cleanly instead of exposing stale structure and broken references.
- **Archived packet normalized to current docs** — Archived work now opens with a clean, validator-compatible Level 1 doc set instead of stale structure and broken markdown references.
- **Archived Index Workflows spec normalized** — Archived the Index Workflows Code spec as a Level 1 packet that passes today's validator, so the folder can be inspected without tripping over stale structure or broken references.
- **Command Adherence archive normalized** — The Command Adherence packet's archived docs were rewritten to pass today's validator, so the archive reads cleanly instead of showing drifted structure or broken references.
- **Archive normalized for current validator** — Archived MCP Issues After Update docs were normalized to the current Level 1 templates so the folder now passes today's validator with zero errors.
- **Archived docs normalized to current templates** — Archived documentation now follows current Level 1 templates, so the folder validates cleanly without carrying Level 2/3 enforcement burdens.
- **Archive normalization for the working-memory hybrid-RAG packet** — The completed MCP working-memory hybrid-RAG feature phases are now archived as concise, self-contained Level 1 archive records.

