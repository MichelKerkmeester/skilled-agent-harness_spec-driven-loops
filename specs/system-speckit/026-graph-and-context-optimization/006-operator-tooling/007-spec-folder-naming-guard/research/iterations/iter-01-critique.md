# Iteration 01 — Adversarial Critique + Quantification (gpt-5.5-fast xhigh)

_Read-only deep-research pass. Source: live tree scan + repo file evidence._

I’ll keep this read-only and base the critique on the prior research, live tree scans, and command outputs rather than assumptions.
The routed deep-research workflow normally externalizes state, but I’m not invoking that write-oriented loop because the user instruction is explicitly read-only. I’ll use its evidence/citation discipline only.
The prior packet itself claims “created mainly through `create.sh`,” but it also records raw `mkdir`/`Write` as a bypass. I’m going to quantify both the filesystem defects and the observable creation-call evidence.
I’ll treat the live tree scan as the source of truth for counts, then separately distinguish hard violations from heuristic-only candidates so the false-positive risk is visible.
The live `.opencode/specs` scan no longer contains `028-026-program-research`; existing docs now cite it as missing/stale in later research. I’ll keep the count tied to the current live tree and call out that the motivating defect is historical rather than currently present.
I have not found live 028/029 folders in either `.opencode/specs/system-spec-kit` or `specs/system-spec-kit`; I’ll avoid treating them as current-count defects. Next I’m checking whether repository logs/docs preserve independent creation-path evidence.
The repository has strong normative routing to `create.sh`, but the observable logs I found are weak for proving dominance: they show direct `mkdir`/`write` activity and no matching `create.sh` tool-call logs in the searched artifact logs. That makes “dominant path” unproven, not established.
The current code confirms the existing `create.sh` gate is syntactic only and that phase creation is flag-driven; the validator documentation similarly only covers syntax. I’m checking the executable validator rule and hook registration next.
The hook-system reference in this tree describes prompt-time parity, not a uniform pre-write deny surface; I’ll cite the matrix plus the live registration files to separate hook aspiration from current enforcement.
Codex has a checked-in `hooks.json`, but it registers prompt/session/compact only, not `PreToolUse`; the pre-tool handler exists as code but is not live in that file. That weakens the “only Codex has one today” premise for this checkout’s actual configuration.
The only current track-root `*-cleanup` candidate is `111-base-files-renumbering-name-cleanup`; I’m reading it before deciding whether to count it as a real violation or just a heuristic hit.
One count command hit `ENOBUFS` because matching `write` log lines are huge serialized tool payloads. I’ll rerun using `rg --count-matches` so only compact counts are emitted.
The final count is: 752 current `NNN-*` directories under `.opencode/specs` to depth 4; 0 syntax violations; 0 embedded sibling-number matches; 1 current generic/mislocated track-root cleanup candidate. I’m verifying the “historical 028 is now missing” citation before finalizing.
1. **P0 design-flaw - `create.sh = guarantee` is overstated.** Evidence: `create.sh` is normative in docs (`.opencode/skills/system-spec-kit/SKILL.md:441`, `.opencode/commands/speckit/plan.md:491`, `.opencode/commands/speckit/assets/speckit_plan_auto.yaml:52`), but current `create.sh` validates syntax only (`create.sh:681-687`) and routes location by mode flags (`create.sh:827-843`, `create.sh:978-981`). Count evidence from searched logs/docs: `COMMAND_CREATE_SH_COMMAND_LINES=4`, `SPEC_LOG_CREATE_SH_TOOLCALLS=0`, `SPEC_LOG_MKDIR_SPEC_TOOLCALLS=5`, `SPEC_LOG_WRITE_SPEC_PATH_TOOLCALLS=176`. Recommendation: call this a **canonical-path gate**, not a system guarantee; make `validate.sh` semantic naming the durable invariant.

2. **P1 - Current live defect count is low but non-zero.** Evidence: live scan command `find .opencode/specs -maxdepth 4 -type d -name '[0-9][0-9][0-9]-*' -print` returned `TOTAL_DIRS=752`, `INVALID_STRICT_SYNTAX=0`, `EMBEDDED_SIBLING_MATCHES=0`, `GENERIC_TRACK_ROOT=1`, `UNION_VIOLATION_CANDIDATES=1`. The one current high-confidence candidate is `.opencode/specs/skilled-agent-orchestration/111-base-files-renumbering-name-cleanup`; its spec says it is “026 cleanup remediation” (`spec.md:2-3`) and targets `.opencode/specs/system-spec-kit/026-graph-and-context-optimization/...` (`spec.md:56`, `spec.md:86-93`). Recommendation: count current violations as **1**, plus historical `028-026-program-research`; handle relocation/remediation separately.

3. **P2 - Embedded-number heuristic has false-positive risk if broad, but strict sibling matching looks safe.** Evidence: scan found `EMBEDDED_3DIGIT_TOKEN_TOTAL=2` and `EMBEDDED_SIBLING_MATCHES=0`: `003-skill-advisor-render-103-alignment` is a child under packet 103 and intentionally references 103 (`spec.md:2-3`, `spec.md:54-56`); `009-p2-032-cleanup` references a P2 finding ID, not a sibling packet (`spec.md:2-3`, `spec.md:55`). Recommendation: hard-block only when a **direct track-root** folder’s slug body starts with a hyphen-delimited existing sibling number, e.g. `028-026-*`; warn or ignore child-folder and non-leading tokens.

4. **P1 - Hook layer is not worth making central.** Evidence: hook matrix shows Gemini has no checked-in project hook and Codex is config-gated (`hook_system.md:103-107`, `hook_system.md:117`). Claude live config has `PostToolUse` only for Write/Edit, not `PreToolUse` (`.claude/settings.local.json:86-97`). Checked-in Codex hooks register prompt/session/compact only, not `PreToolUse` (`.codex/hooks.json:27-37`). The Codex pre-tool handler exists, but currently returns `{}` for non-Bash tools (`pre-tool-use.ts:215-217`) and its default policy is a Bash destructive-command denylist (`pre-tool-use.ts:63-82`). Recommendation: defer per-runtime pre-write hooks or keep them best-effort; invest first in shared classification + `create.sh` canonical gate + `validate.sh` catch-later.

**Verdict:** the prior PARTIAL design weakens: shared classifier plus `validate.sh` holds, but `create.sh` should be downgraded from “hard guarantee” to “canonical-path gate,” and hooks should be optional complexity, not a core layer.
