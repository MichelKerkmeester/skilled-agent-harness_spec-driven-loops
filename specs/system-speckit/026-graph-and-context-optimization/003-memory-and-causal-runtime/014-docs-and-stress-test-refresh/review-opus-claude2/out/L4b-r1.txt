Independent Opus 4.8 cross-check complete. I read all three in-scope files end-to-end and traced the replay, recycle, drain, and handshake paths against source. Findings (one line each):

[P1] UTF-8 multibyte frame corruption in string-based splitter (regression vs raw pipe) | .opencode/bin/lib/launcher-session-proxy.cjs:72-82 | evidence: `buffer += chunk.toString('utf8')` decodes each socket chunk independently, so a CJK/emoji char split across chunk boundaries → U+FFFD on both sides; newline framing survives (0x0A never mid-sequence) so content corrupts silently and is forwarded as valid JSON; hits clientSplitter (memory_save content) and backendSplitter (:627, memory_search results); old `bridgeStdioToSocket` used byte-exact `input.pipe/socket.pipe` (launcher-ipc-bridge.cjs:97-98); same flaw in internalHandshake :269 and probeDaemon :175, while probeModelServer :267-271 correctly uses Buffer.concat | fix: decode via `string_decoder.StringDecoder` so partial sequences are held until complete

[P2] Double `initialize` to fresh backend when initial handshake never completed | .opencode/bin/lib/launcher-session-proxy.cjs:607-637 | evidence: if backend dies after receiving but before answering initialize, it stays in pendingRequests; attachFreshSocket replays it via internalHandshake (:609) AND replaySnapshot re-sends it (classifyFrame marks initialize replayable :54-57/:117) → new backend gets two initializes with same id; internalHandshake silently consumes the first response so the replay is load-bearing for client delivery | fix: skip getCachedInitializeId() in replaySnapshot and forward the handshake response + delete the id from pending

[P2] recycleDaemonInPlace clears lease while launcher stays alive → competing-launch window | .opencode/bin/mk-spec-memory-launcher.cjs:733 | evidence: recycle SIGTERMs child then clearLeaseFile() unlinks the lease, but replacement is spawned later by child-exit scheduleRelaunch→setTimeout(launchServer, backoffMs) (:855-859); during backoff isLeaseHeld() is false so a concurrent (multi-client) launcher passes the strict-single-writer gate (main:954-958) and can spawn a competing context-server racing the same UDS | fix: keep the lease on in-place recycle and let relaunch rewrite childPid

[P2] Permanent reattach give-up leaves state='REATTACHING'; loop-guard can relaunch | .opencode/bin/lib/launcher-session-proxy.cjs:669,680-686 | evidence: failPendingAndEnd never mutates state; termination depends on stop() flipping stopped=true inside pumpOutputQueue after the queue drains; under client-stdout backpressure (outputDrainWaiting) stop() is deferred, so ensureReattachRunning's finally sees `state==='REATTACHING' && !stopped` and restarts a fresh 40-attempt loop | fix: set state='CLOSED' at give-up

[P2] memory_save replayed at-least-once on recycle with acknowledged-incomplete idempotency | .opencode/bin/lib/launcher-session-proxy.cjs:33,125 | evidence: REPLAYABLE_TOOL_NAMES includes memory_save; in-code comment concedes "primary-row dedup; secondary-index idempotency remains a known follow-up"; a backend that commits then dies before responding gets memory_save replayed on the new backend, duplicating enrichment/secondary (schema v30) index rows | fix: add request-id dedup at the handler, or drop memory_save from the replayable set until the secondary-index gap is closed

Clean areas (examined, not findings): detachSocket drain-flag clearing (:387) correctly fixes the swap-bound hang and the new drain handler is double-guarded; keepalive id-reservation + reserved-prefix rejection (:544-560) closes the id-collision swallow; -32002 protocol-drift fail-closed (:611-622) is correct with acceptable fail-open-when-unknown; keepalive timer-at-enqueue under backpressure only fires on a genuinely silent (stuck) backend, so recycle is appropriate.

VERDICT: CONDITIONAL (P0=0 P1=1 P2=4)
