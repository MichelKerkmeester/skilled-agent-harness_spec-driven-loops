## Focus

This iteration audited arc 007, `ollama-and-bge-promotion`, under the 016 embedder-testing umbrella. The focus was whether the Ollama adapter, BGE promotion/rebaseline, indexer-surface investigation, and newer text-embedder survey docs still match current code and commit history.

## Actions Taken

1. Read the arc parent `spec.md`, `description.json`, and `graph-metadata.json`; confirmed the parent is a light phase-parent with no parent-level `plan.md`, `tasks.md`, or `implementation-summary.md`.
2. Read child phase docs for `001-indexer-surface-investigation`, `002-cocoindex-ollama-adapter`, `003-bge-code-v1-confirmation-and-promote`, and `004-newer-text-embedders-survey`, including implementation summaries and graph metadata where present.
3. Spot-checked current CocoIndex source files referenced by the docs: `cocoindex_code/embedders/registered_embedders.py`, `cocoindex_code/core/shared.py`, `cocoindex_code/config/config.py`, `cocoindex_code/config/settings.py`, and `tests/test_ollama_routing.py`.
4. Ran grep probes for `requires_ollama_daemon`, `ollama/nomic-embed-text`, `OLLAMA_API_BASE`, `LiteLLMEmbedder`, `_DEFAULT_MODEL`, `bge-code-v1`, `jina-embeddings-v2-base-code`, `MEASURE=0`, `ccc index`, `Operation not permitted`, `implementation-summary`, and benchmark/evidence terms.
5. Ran `find` probes for arc files and `evidence/` outputs; `003-bge-code-v1-confirmation-and-promote` contains only `spec.md`, `pre-confirmation-margin-analysis.md`, `description.json`, and `graph-metadata.json`, with no evidence CSV/JSONL or implementation summary.
6. Compared path history with `git log --oneline -- ...` and targeted `git show --stat --name-only` on `b9993c0f14` (007/003 rescope), `36e85b9d49` (007/004 HOLD verdict), and `8364bdd5b7` (nomic CodeRankEmbed production promotion).
7. Checked current git status for research outputs and arc/source paths to avoid attributing unrelated uncommitted benchmark/coverage changes to this iteration.

## Findings

| Finding ID | Category | File:line + grep evidence | Recommended action |
|---|---|---|---|
| f-iter008-001 | DEAD | Parent arc docs still describe the pre-rescope BGE path: parent status is `Planned` at `.opencode/specs/system-spec-kit/026-graph-and-context-optimization/013-embedder-testing-and-architecture/007-ollama-and-bge-promotion/spec.md:23`, says bge-code-v1's `11/18` needs confirming at `spec.md:35`, requires 3-run evidence at `spec.md:70`, and still says success is BGE promotion or hold at jina-code at `spec.md:97`. Current child scope says the old framing was invalidated at `003-bge-code-v1-confirmation-and-promote/spec.md:23` and a single corrected 4-candidate rebaseline replaced the 3-run replay at `003-bge-code-v1-confirmation-and-promote/spec.md:57-59`; current production code defaults to Nomic at `.opencode/skills/mcp-coco-index/mcp_server/cocoindex_code/config/config.py:21` and `.opencode/skills/mcp-coco-index/mcp_server/cocoindex_code/embedders/registered_embedders.py:255`. | Refresh the parent `spec.md`, `description.json`, and `graph-metadata.json` to say 003 was rescoped/superseded and that the production default is Nomic CodeRankEmbed, not a pending BGE-vs-jina decision. |
| f-iter008-002 | UNSHIPPED | 002 is marked complete at `002-cocoindex-ollama-adapter/spec.md:42`, requires a passing smoke test at `002-cocoindex-ollama-adapter/spec.md:88`, and claims the operator can run `ccc reset --force && ccc index && ccc search` with `ollama/nomic-embed-text` at `002-cocoindex-ollama-adapter/spec.md:97`. Its own verification table records the full isolated `ccc index`/search smoke as `FAIL` with `Operation not permitted` at `002-cocoindex-ollama-adapter/implementation-summary.md:143`, and the limitation says a clean end-to-end smoke still needs rerun before broad rollout at `implementation-summary.md:152`. | Downgrade the e2e success claim or add a follow-up validation record from an unsandboxed machine. Until then, treat Ollama support as unit/direct-LiteLLM verified, not end-to-end shipped. |
| f-iter008-003 | DEAD | 002's implementation handoff names old flat files at `002-cocoindex-ollama-adapter/implementation-summary.md:72-73` and `implementation-summary.md:97-99` (`cocoindex_code/registered_embedders.py`, `cocoindex_code/shared.py`). Current source lives at `.opencode/skills/mcp-coco-index/mcp_server/cocoindex_code/embedders/registered_embedders.py:135` and `.opencode/skills/mcp-coco-index/mcp_server/cocoindex_code/core/shared.py:144`; direct reads of the old paths returned `No such file or directory`, and `rg --files ... | rg '(registered_embedders|shared|config\.py|settings\.py)$'` only surfaced the reorganized config/settings paths plus the embedders/core files. | Update the 002 handoff/path references after the feature-grouped CocoIndex refactor so future recovery agents land on live source files. |
| f-iter008-004 | MISSED | 003 defines required outputs: 4-row CSV + 72-row JSONL at `003-bge-code-v1-confirmation-and-promote/spec.md:84`, ranking table in `implementation-summary.md` at `spec.md:85`, explicit PROMOTE/HOLD decision at `spec.md:87`, and strict validation at `spec.md:89`. The folder contains only `spec.md`, `pre-confirmation-margin-analysis.md`, `description.json`, and `graph-metadata.json` per the `find .../003-bge-code-v1-confirmation-and-promote -maxdepth 2 -type f` probe; no `implementation-summary.md` or `evidence/` exists. Meanwhile commit history shows `8364bdd5b7 feat(016/004): promote nomic-ai/CodeRankEmbed as production embedder default`, and current README says the default was ratified in packet 018 at `.opencode/skills/mcp-coco-index/README.md:69`. | Either close 003 as superseded by the later 018/016/004 promotion evidence, or backfill the promised implementation summary with links to the actual benchmark folder and decision commit. |
| f-iter008-005 | BUGGED | 003's own scope says to run the corrected 4-candidate pipeline once, not 3-run replays, at `003-bge-code-v1-confirmation-and-promote/spec.md:57-59` and explicitly makes 3-run replays out of scope at `spec.md:70-72`. The same spec later says a PROMOTE outcome promotes "a curated subset of this 3-run evidence" at `spec.md:112-114`. | Fix the internal contract drift in 003's skill-local promotion section: it should reference the corrected single-run rebaseline or the later 018 evidence, not nonexistent 3-run evidence. |
| f-iter008-006 | BUGGED | 001 produced the requested mapping table in `001-indexer-surface-investigation/research.md:15-30` and sub-phase implications at `research.md:94-106`, but metadata still says draft/planned: `research.md:4` has `status: "draft"`, `001-indexer-surface-investigation/spec.md:22` says `Status | Planned`, and `001-indexer-surface-investigation/graph-metadata.json:42` says `"status": "planned"`. | Mark 001 complete or explicitly leave it draft with missing-validation rationale. Current metadata hides completed research from status-driven recovery. |
| f-iter008-007 | DEAD | Parent R4 still says 004 ships a text fixture, 4-candidate benchmark CSV/JSONL, and hit-rate evidence at parent `spec.md:71`; parent `description.json:4` also says "benchmark mk-spec-memory text embedders (jina-code vs bge-m3 / bge-large-en-v1.5)". The 004 child was rescoped to research-only and excludes benchmarks at `004-newer-text-embedders-survey/spec.md:78-80`, closes with `MEASURE=0` at `004-newer-text-embedders-survey/research.md:65-71`, and states no Phase-2 benchmark is authorized at `research.md:112-114`. | Replace the parent 004 requirement with the actual survey contract: candidate triage plus HOLD/no-bench when MEASURE=0. Remove stale bge-m3/jina-code benchmark phrasing from `description.json`. |
