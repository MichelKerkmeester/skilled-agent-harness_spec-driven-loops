## Focus

Iteration 4 audited arc `003-skill-advisor-stack`, covering the phase parent plus children `001-pluggable-architecture`, `002-jina-swap-and-reindex`, `003-install-guide-docs`, and `004-skill-graph-db-writer-cross-wire`.

## Actions Taken

1. Skimmed the phase-parent `spec.md` and `graph-metadata.json`; confirmed root `plan.md`, `tasks.md`, and `implementation-summary.md` are intentionally absent because the target is a phase parent.
2. Read child implementation summaries for `001`, `002`, `003`, and `004`, plus `002/evidence/swap-runbook.md`.
3. Spot-checked source implementation in `mcp_server/lib/embedders/schema.ts`, `mcp_server/lib/embedders/registry.ts`, `mcp_server/lib/skill-graph/skill-graph-db.ts`, `mcp_server/lib/scorer/lanes/semantic-shadow.ts`, `INSTALL_GUIDE.md`, and `README.md`.
4. Ran exact grep probes for `setActiveEmbedder`, `active_embedder_name`, `DEFAULT_ACTIVE_EMBEDDER`, `DEFAULT_EMBEDDER_NAME`, `jina-embeddings-v3`, `embeddinggemma-300m`, and skill-advisor env vars.
5. Compared arc/source history with `git log -- .opencode/specs/.../003-skill-advisor-stack` and `git log -- .opencode/skills/system-skill-advisor/...`; relevant commits include `ed5eb0e567` (001 pluggable layer), `e5daebb03f` (003 docs), `c0ec765f4b` (004 writer cross-wire), `ab0c7de71b` (004 review advisories), and `fbf4b7b2b5` (stack changelog).
6. Attempted CocoIndex semantic search for the implementation surface; the MCP call was cancelled/unavailable, so this pass used `rg` + direct source reads.

## Findings

| Finding ID | Category | File:line + grep evidence | Recommended action |
|---|---|---|---|
| f-iter004-001 | MISSED | Arc success still requires the actual Jina activation: `.opencode/specs/system-spec-kit/026-graph-and-context-optimization/013-embedder-testing-and-architecture/003-skill-advisor-stack/spec.md:58` says "Default flip from gemma -> jina-embeddings-v3" and `spec.md:91` requires `vec_metadata.active_embedder_name` to report `jina-embeddings-v3`; source still falls back to gemma at `.opencode/skills/system-skill-advisor/mcp_server/lib/embedders/schema.ts:18-21`, and the stack changelog confirms the default remains `embeddinggemma-300m` at `CHANGELOG.md:197`. | Either execute the `002` swap runbook now that `004` shipped, or retitle this arc as infrastructure-only and create a concrete follow-on for the production pointer flip, reindex, daemon restart, and smoke tests. |
| f-iter004-002 | BUGGED | The code has two competing "default" concepts: `.opencode/skills/system-skill-advisor/mcp_server/lib/embedders/registry.ts:10` exports `DEFAULT_EMBEDDER_NAME = 'jina-embeddings-v3'`, while `.opencode/skills/system-skill-advisor/mcp_server/lib/embedders/schema.ts:18-21` exports `DEFAULT_ACTIVE_EMBEDDER` as `embeddinggemma-300m`; docs then have to explain this split at `001-pluggable-architecture/implementation-summary.md:43-44`. | Rename the registry constant to something like `PREFERRED_EMBEDDER_NAME` or `PARITY_TARGET_EMBEDDER_NAME`, leaving `DEFAULT_ACTIVE_EMBEDDER` as the only runtime default. Add a small test that asserts the intended relationship. |
| f-iter004-003 | DEAD | Skill-advisor user docs still describe the pre-004 half-wired state after the writer cross-wire shipped: `.opencode/skills/system-skill-advisor/INSTALL_GUIDE.md:337-339` says the write path still uses the legacy factory and pointer flip is deferred to `010/004`; `INSTALL_GUIDE.md:379` repeats "Until packet 010/004 closes the writer cross-wiring"; `.opencode/skills/system-skill-advisor/README.md:224` says the active swap is deferred to `010/004`. Source now dispatches the writer through the adapter branch at `.opencode/skills/system-skill-advisor/mcp_server/lib/skill-graph/skill-graph-db.ts:812-817`, and the 004 review says PASS-with-advisories at `004-skill-graph-db-writer-cross-wire/review/review-report.md:27`. | Update INSTALL_GUIDE and README to distinguish "004 shipped" from "operator has not yet executed the Jina production swap"; keep the warning about not flipping without reindex/smoke, but remove the false writer-gap claim. |
| f-iter004-004 | DEAD | The 002 swap runbook is stale in two ways: it claims `MANIFESTS` has "8 vetted embedders" at `002-jina-swap-and-reindex/evidence/swap-runbook.md:20`, but the registry has six entries at `.opencode/skills/system-skill-advisor/mcp_server/lib/embedders/registry.ts:13-63`; it also says `010/004` is "REQUIRED, not yet shipped" at `swap-runbook.md:55-58`, while 004 summary marks the writer cross-wire implemented at `004-skill-graph-db-writer-cross-wire/implementation-summary.md:37-40`. | Refresh the runbook before any operator follows it: six-manifest table, `010/004 shipped` prerequisite status, current rebuild command, and updated smoke baseline. |
| f-iter004-005 | BUGGED | The `002` spec still advertises an env-var swap path: `002-jina-swap-and-reindex/spec.md:52` says "set env var or call setActiveEmbedder"; source selection reads only `vec_metadata` through `.opencode/skills/system-skill-advisor/mcp_server/lib/embedders/schema.ts:84-94` and writes only explicit function arguments at `schema.ts:97-121`. The exact env grep found skill-advisor disable/DB envs but no embedder-selection env in `.opencode/skills/system-skill-advisor/mcp_server/lib`, `handlers`, or `scripts`. | Patch `002/spec.md` to remove the env-var promise, or implement and document a real env bridge. Current docs should name `setActiveEmbedder(db, name, dim)` as the only supported swap surface. |
| f-iter004-006 | DEAD | `004` metadata was not reconciled after its review completed: `004-skill-graph-db-writer-cross-wire/implementation-summary.md:63-66` and `implementation-summary.md:110-111` still list post-impl deep-review and the 002 summary update as pending; the review report exists with "5 of 5" iterations at `review/review-report.md:22`, PASS-with-advisories at `review/review-report.md:27`, and says 010/004 has no gate-blocking defects at `review/review-report.md:52`. The `002` summary also already says 004 shipped/unblocked at `002-jina-swap-and-reindex/implementation-summary.md:16-18` and `:40`. | Reconcile `004/implementation-summary.md` and continuity frontmatter so resume agents do not redispatch a completed review or re-update an already-updated 002 summary. |
| f-iter004-007 | MISSED | A prior review finding remains open in source: `001-pluggable-architecture/review/review-report.md:99` flags `setActiveEmbedder` creating `vec_<dim>` outside the pointer transaction, and `review-report.md:164` recommends wrapping both operations; current source still calls `ensureVecTableForDim(db, dim)` before opening the transaction at `.opencode/skills/system-skill-advisor/mcp_server/lib/embedders/schema.ts:103-120`. | Close or explicitly waive the P2. If closing, move `ensureVecTableForDim` inside the transaction body or document why orphan `vec_<dim>` tables are acceptable after a failed pointer write. |
