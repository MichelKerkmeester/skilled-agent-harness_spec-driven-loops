# Iteration 008 — Local-LLM Legacy Hunt

## Focus
This iteration scanned the traceability surfaces most likely to keep stale defaults alive after packet 014: user-facing README and INSTALL_GUIDE files, skill references, MCP config notes, and CocoIndex model guidance. I focused on wording that presents Voyage, MiniLM, OpenAI, Nomic, or the generic `context-index.sqlite` as a default, recommended setup, or canonical operator path, while skipping historical packet 014 context, this review packet, regression fixtures already covered by earlier iterations, and optional-provider support that does not assert a default.

## Findings

| ID | Severity | Dimension | File:Line | Evidence (quote) | Disposition | Recommendation |
|----|----------|-----------|-----------|------------------|-------------|----------------|
| L-008-001 | P1 | traceability | .mcp.json:22 | "\"_NOTE_5_GET_VOYAGE_KEY\": \"Get Voyage key: https://dash.voyageai.com/api-keys (recommended, 8% better than OpenAI)\"," | confirmed-residue | Remove the Voyage recommendation and quality claim from the root MCP config note; keep Voyage only as optional explicit-provider setup. |
| L-008-002 | P1 | traceability | .opencode/skills/system-spec-kit/README.md:146 | "\| Embedding API \| None (HuggingFace local) \| Voyage AI recommended for best quality \|" | confirmed-residue | Update the requirements table to state hf-local EmbeddingGemma is the default and cloud providers are optional. |
| L-008-003 | P1 | traceability | .opencode/skills/system-spec-kit/README.md:699 | "\| `VOYAGE_API_KEY`     \| Recommended \| Voyage AI embeddings (1024d, best retrieval quality) \|" | confirmed-residue | Replace the recommended Voyage env row with canonical hf-local defaults and move Voyage into an optional cloud-provider section. |
| L-008-004 | P1 | traceability | .opencode/skills/system-spec-kit/README.md:720 | "\"VOYAGE_API_KEY\": \"your-key-here\"" | confirmed-residue | Change the generic MCP server configuration example so it works without a Voyage key and reflects `EMBEDDINGS_PROVIDER=auto` falling through to hf-local. |
| L-008-005 | P1 | traceability | .opencode/skills/system-spec-kit/shared/README.md:198 | "│       ├── voyage.ts           # Voyage AI (recommended)" | confirmed-residue | Rename the provider-tree annotation so `voyage.ts` is optional/fallback, not recommended. |
| L-008-006 | P1 | traceability | .opencode/skills/mcp-coco-index/references/settings_reference.md:47 | "### Example: Voyage Code 3 (recommended)" | confirmed-residue | Lead the settings reference with the canonical sentence-transformers `google/embeddinggemma-300m` default; mark Voyage Code 3 as optional LiteLLM setup. |
| L-008-007 | P1 | traceability | .opencode/skills/mcp-coco-index/references/settings_reference.md:127 | "\| `sentence-transformers/all-MiniLM-L6-v2` \| sentence-transformers \| None \| 384 \| Local, offline, current default \|" | confirmed-residue | Replace the MiniLM current-default row with `google/embeddinggemma-300m`, bf16, 768 dimensions. |
| L-008-008 | P1 | traceability | .opencode/skills/mcp-coco-index/README.md:160 | "The local model (`all-MiniLM-L6-v2`) requires no API key and works offline, making it the right default for most projects." | confirmed-residue | Update the CocoIndex README narrative to make `google/embeddinggemma-300m` the local default and remove MiniLM-as-default guidance. |
| L-008-009 | P1 | traceability | .opencode/skills/mcp-coco-index/README.md:276 | "model: all-MiniLM-L6-v2              # or voyage/voyage-code-3" | confirmed-residue | Refresh the global settings YAML sample to use `google/embeddinggemma-300m` and keep Voyage as an explicit alternate. |
| L-008-010 | P1 | traceability | .opencode/skills/mcp-coco-index/INSTALL_GUIDE.md:620 | "\| **Recommended** \| Voyage Code 3 via LiteLLM (API key required, best code search quality)      \|" | confirmed-residue | Remove the Voyage recommendation from the install-guide summary and set EmbeddingGemma as the default/recommended local setup. |
| L-008-011 | P1 | traceability | .opencode/skills/mcp-coco-index/INSTALL_GUIDE.md:843 | "- **Default Embedding**: [all-MiniLM-L6-v2](https://huggingface.co/sentence-transformers/all-MiniLM-L6-v2)" | confirmed-residue | Replace the external-resource default link with `google/embeddinggemma-300m` or remove the stale default label. |
| L-008-012 | P1 | traceability | .opencode/skills/system-spec-kit/mcp_server/INSTALL_GUIDE.md:113 | "\| `.opencode/skills/system-spec-kit/mcp_server/database/context-index.sqlite` \| Default repo-local memory database used by the checked-in configs \|" | confirmed-residue | Update the runtime-path table to describe profile-derived sqlite filenames, with hf-local EmbeddingGemma q8 768d as the default profile. |
| L-008-013 | P1 | traceability | .opencode/skills/system-spec-kit/mcp_server/INSTALL_GUIDE.md:116 | "local fallback stays on `context-index.sqlite`, while Voyage and OpenAI profiles get their own profile-specific filenames in the same directory." | confirmed-residue | Remove the claim that the local fallback keeps the legacy generic filename; document the filename-keyed hf-local default. |
| L-008-014 | P1 | traceability | .opencode/install_guides/README.md:1344 | "sqlite3 .opencode/skills/system-spec-kit/mcp_server/dist/database/context-index.sqlite \"SELECT COUNT(*) FROM memory_index;\"" | confirmed-residue | Change troubleshooting commands to resolve or show the active profile-derived Memory MCP sqlite file instead of the deleted generic database. |

## Iteration summary
- Files scanned: 4123
- New findings: 14 (P0=0, P1=14, P2=0)
- Out-of-scope/historical noted but NOT flagged: 8
- Notes: Traceability residue is now mostly duplicate-shaped around the same stale defaults, but still present in distinct user-facing surfaces. I treated changelog/release-note mentions, optional provider support without default language, Qwen model references unrelated to embeddings, package-lock dependency rows, and the 014/015 packet areas as non-findings. No P0 traceability finding surfaced in this pass.
