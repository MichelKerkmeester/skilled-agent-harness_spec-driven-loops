# RS-007 — spec-memory ensure (sidecar present)

- Section: §8
- Verdict: SKIPPED
- Started: 2026-05-20T20:18:35Z
- Completed: 2026-05-20T20:18:40Z

## Commands run

None (SKIPPED)

## Observed signals

- Expected: exactly one sidecar PID
- Actual: Not executed

## Rationale

SKIPPED — Requires launching mk-spec-memory MCP via the configured runtime. This scenario requires the full MCP server infrastructure which is not bootable in this shell-based dispatch. The ensure helper's `/health` probe path and EADDRINUSE handling can only be verified in the context of actual MCP server startup.

## Surrogate

No surrogate executed. The ensure helper logic can be verified by inspecting the source at `.opencode/skills/system-spec-kit/mcp_server/bin/lib/ensure-rerank-sidecar.cjs` which implements the health probe and port-bind atomicity checks.
