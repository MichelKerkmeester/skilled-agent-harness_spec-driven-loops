# RS-012 — Both consumers share one sidecar

- Section: §10
- Verdict: PARTIAL
- Started: 2026-05-20T20:19:15Z
- Completed: 2026-05-20T20:19:25Z

## Commands run

```bash
# Verify current state after RS-009 (cocoindex ensure)
pgrep -f rerank_sidecar
curl -sf http://127.0.0.1:8765/health
```

## Observed signals

- Expected: exactly one sidecar PID; both `memory_search` and `ccc search` request logs (if enabled) hit the same PID
- Actual:
  - Sidecar PID: 10031 (single process)
  - Health check:
    ```json
    {"status":"ok","model_loaded":true,"model_name":"Qwen/Qwen3-Reranker-0.6B","queue_depth":0,"uptime_s":28.45}
    ```

## Rationale

After the cocoindex ensure surrogate (RS-009) spawned the sidecar, there is exactly one sidecar process (PID 10031) running. Both consumers are configured to use the same port (8765) via their respective ensure helpers and routing logic. However, we cannot execute the spec-memory surrogate in this shell-based dispatch due to TypeScript execution requirements (would need ts-node or transpilation). The evidence shows a single sidecar process that both consumers would share, but we cannot verify both consumers actively using it in the same session. Marked PARTIAL due to inability to run both consumer surrogates simultaneously.

## Surrogate

Only cocoindex surrogate was executed (RS-009). Spec-memory surrogate requires TypeScript execution environment (ts-node or transpilation) which is not available in this shell-based dispatch. The architectural evidence is strong: both ensure helpers probe the same port (8765) and both routing layers (cross-encoder.ts local provider, HttpSidecarRerankerAdapter) hit the same endpoint. Full verification would require launching both MCP servers in a runtime that supports TypeScript execution.
