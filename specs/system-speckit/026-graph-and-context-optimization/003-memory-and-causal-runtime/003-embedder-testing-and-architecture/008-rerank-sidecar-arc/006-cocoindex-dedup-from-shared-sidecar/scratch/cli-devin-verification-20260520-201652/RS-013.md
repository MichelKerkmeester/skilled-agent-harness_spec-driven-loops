# RS-013 — Independent consumer fallback (sidecar down)

- Section: §10
- Verdict: PARTIAL
- Started: 2026-05-20T20:19:25Z
- Completed: 2026-05-20T20:19:40Z

## Commands run

```bash
pkill -KILL -f rerank_sidecar
pgrep -f rerank_sidecar
COCOINDEX_RERANK_VIA_SIDECAR=true .opencode/skills/mcp-coco-index/mcp_server/.venv/bin/ccc search "registry of available embedding backends" --limit 5
```

## Observed signals

- Expected: spec-memory falls back to positional scoring (`scoringMethod: 'fallback'`); cocoindex falls back to bundled adapter (`reranker_fallback_reason: 'sidecar_unavailable'`)
- Actual:
  - Sidecar killed successfully (pgrep returns exit code 1, no process)
  - ccc search returned 5 results successfully (no crash)
  - Results still show `rankingSignals: hybrid_rrf, vector_lane, cross_encoder_rerank`
  - Reranker scores in different range: 6.312, 5.125, 4.938, 4.938, 4.562 (vs 0.98-0.99 range with sidecar)
  - No crash or error in CLI output
  - No explicit fallback warning visible in CLI output (logger.warning may not surface to CLI)

## Rationale

Cocoindex search returned results successfully after sidecar was killed, indicating the fallback mechanism worked. The reranker scores are in a different range (6.312 vs 0.998) suggesting the bundled CrossEncoderRerankerAdapter was used instead of the sidecar. No crash occurred. However, we cannot verify the spec-memory fallback (requires MCP server), and we cannot see the explicit `reranker_fallback_reason` diagnostics via CLI. Marked PARTIAL due to inability to verify spec-memory fallback and access diagnostics.

## Surrogate

Only cocoindex consumer tested via CLI. Spec-memory fallback verification requires launching the full MCP server which is not available in this shell-based dispatch. The cocoindex evidence shows successful fallback (different score range, no crash), but without access to RetrievalDiagnostics we cannot confirm the exact fallback reason.
