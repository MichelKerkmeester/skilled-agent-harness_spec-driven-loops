# RS-009 — cocoindex ensure (sidecar absent)

- Section: §9
- Verdict: PASS
- Started: 2026-05-20T20:18:45Z
- Completed: 2026-05-20T20:18:55Z

## Commands run

```bash
# Python surrogate script that:
# 1. Kills existing sidecar
# 2. Calls _ensure_rerank_sidecar_for_mcp() from cocoindex_code/cli.py
# 3. Verifies sidecar started with pgrep
# 4. Checks /health endpoint

/opt/homebrew/bin/python3 /tmp/rs009_surrogate.py
```

## Observed signals

- Expected: sidecar PID appears within 10s; `/health` returns `status:ok`
- Actual:
  - Ensure function stderr:
    ```
    [ensure-rerank-sidecar] sidecar spawned PID=10031 listening on :8765
    [cocoindex mcp] rerank sidecar: {"spawned": true, "port": 8765, "ownerPid": 10031}
    ```
  - Sidecar PID count: 1
  - Health check:
    ```json
    {"status":"ok","model_loaded":false,"model_name":"Qwen/Qwen3-Reranker-0.6B","queue_depth":0,"uptime_s":2.51}
    ```

## Rationale

The `_ensure_rerank_sidecar_for_mcp` function successfully spawned the sidecar when it was absent. Sidecar PID 10031 appeared within 10s (actually within ~3s). Health endpoint returned status:ok. The ensure helper works correctly via Python surrogate without requiring the full MCP server launch.

## Surrogate

Python surrogate invoked `_ensure_rerank_sidecar_for_mcp()` directly from cocoindex_code/cli.py, bypassing the full MCP server startup. This validates the ensure helper logic in isolation. Note: The surrogate set `SPECKIT_CROSS_ENCODER=true` to satisfy the ensure function's enablement check; in production, cocoindex uses `COCOINDEX_RERANK_VIA_SIDECAR` for routing control while the ensure helper may need adjustment for cross-skill env var awareness.
