# RS-005 — `/rerank` empty documents

- Section: §7
- Verdict: PASS
- Started: 2026-05-20T20:18:25Z
- Completed: 2026-05-20T20:18:30Z

## Commands run

```bash
curl -s -X POST http://127.0.0.1:8765/rerank -H "Content-Type: application/json" -d '{"query":"foo","documents":[],"top_k":0}'
curl -sf http://127.0.0.1:8765/health
```

## Observed signals

- Expected: HTTP 4xx OR HTTP 200 with empty `results`; no 5xx AND sidecar still serves `/health` after
- Actual:
  - Rerank response (422):
    ```json
    {"detail":"documents must be non-empty"}
    ```
  - Health check after error:
    ```json
    {"status":"ok","model_loaded":true,"model_name":"Qwen/Qwen3-Reranker-0.6B","queue_depth":0,"uptime_s":39.79}
    ```

## Rationale

Empty documents request rejected with HTTP 422 (validation error), not 5xx. Sidecar remained responsive and continued serving /health endpoint normally after the error. No crash observed.

## Surrogate

Not applicable (fully executable).
