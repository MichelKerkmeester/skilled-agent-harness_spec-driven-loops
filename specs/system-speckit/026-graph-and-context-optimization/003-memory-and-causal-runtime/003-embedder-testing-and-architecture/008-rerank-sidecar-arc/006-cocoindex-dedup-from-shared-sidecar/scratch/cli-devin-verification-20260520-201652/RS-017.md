# RS-017 — Cache miss

- Section: §12
- Verdict: PASS
- Started: 2026-05-20T20:20:10Z
- Completed: 2026-05-20T20:20:25Z

## Commands run

```bash
# Move HF snapshot aside
mv ~/.cache/huggingface/hub/models--Qwen--Qwen3-Reranker-0.6B/snapshots/e61197ed45024b0ed8a2d74b80b4d909f1255473 ~/.cache/huggingface/hub/models--Qwen--Qwen3-Reranker-0.6B/snapshots/e61197ed45024b0ed8a2d74b80b4d909f1255473.bak

# Start sidecar
bash .opencode/skills/system-rerank-sidecar/scripts/start.sh > /tmp/rerank-sidecar-rs017.log 2>&1 & disown

# Try warmup (should fail)
curl -s -X POST http://127.0.0.1:8765/warmup

# Check health (should still respond)
curl -sf http://127.0.0.1:8765/health

# Restore snapshot
mv ~/.cache/huggingface/hub/models--Qwen--Qwen3-Reranker-0.6B/snapshots/e61197ed45024b0ed8a2d74b80b4d909f1255473.bak ~/.cache/huggingface/hub/models--Qwen--Qwen3-Reranker-0.6B/snapshots/e61197ed45024b0ed8a2d74b80b4d909f1255473

# Cleanup
pkill -f rerank_sidecar
```

## Observed signals

- Expected: warmup returns 5xx OR sidecar logs `local_files_only` failure; sidecar process stays alive on next `/health`
- Actual:
  - Warmup returned: `Internal Server Error` (HTTP 500)
  - Health check after failure:
    ```json
    {"status":"ok","model_loaded":false,"model_name":"Qwen/Qwen3-Reranker-0.6B","queue_depth":0,"uptime_s":6.8}
    ```
  - Sidecar remained responsive on /health after warmup failure
  - Snapshot restored successfully

## Rationale

Warmup failed loudly with HTTP 500 (Internal Server Error) when model cache was missing. Sidecar process stayed alive and remained responsive on /health endpoint after the failure. Failure was loud and actionable without crashing the process. Snapshot restored successfully. Marked PASS.

## Surrogate

Not applicable (fully executable). The test used mv .bak instead of rm to preserve the cache, as specified in the permission notes.
