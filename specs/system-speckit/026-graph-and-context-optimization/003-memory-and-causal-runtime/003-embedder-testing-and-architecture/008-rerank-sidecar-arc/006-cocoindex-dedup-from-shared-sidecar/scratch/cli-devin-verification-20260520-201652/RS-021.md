# RS-021 — Loopback bind verification

- Section: §13
- Verdict: PASS
- Started: 2026-05-20T20:20:55Z
- Completed: 2026-05-20T20:21:00Z

## Commands run

```bash
# Start sidecar
bash .opencode/skills/system-rerank-sidecar/scripts/start.sh > /tmp/rerank-sidecar-rs021.log 2>&1 & disown

# Check bind address
sleep 3 && lsof -iTCP:8765 -sTCP:LISTEN
```

## Observed signals

- Expected: LISTEN entry shows `127.0.0.1:8765` (or `localhost:8765`), NOT `0.0.0.0:8765` or `*:8765`
- Actual:
  ```
  COMMAND   PID              USER   FD   TYPE             DEVICE SIZE/OFF NODE NAME
  Python  14129 michelkerkmeester   10u  IPv4 0xe40d2b1e0eed3b0d      0t0  TCP localhost:ultraseek-http (LISTEN)
  ```

## Rationale

lsof shows the sidecar listening on `localhost:ultraseek-http` (port 8765), which corresponds to 127.0.0.1:8765. The binding is NOT on `0.0.0.0` or `*` (all interfaces). Loopback bind is correctly enforced. Marked PASS.

## Surrogate

Not applicable (fully executable). The loopback bind can be verified by inspecting the start.sh script which should contain `uvicorn --host 127.0.0.1`.
