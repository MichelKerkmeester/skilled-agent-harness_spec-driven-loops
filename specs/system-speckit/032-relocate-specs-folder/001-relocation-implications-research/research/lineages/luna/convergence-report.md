# Convergence Report

- Stop reason: `maxIterationsReached`
- Total iterations: 5
- Questions answered: 5 / 5
- Remaining key questions: 0
- Configured convergence threshold: `0.05`
- New-information ratios: `1.00, 0.85, 0.75, 0.65, 0.45`
- Inline convergence: no earlier legal stop; the terminal five-iteration cap fired after the final question was answered.
- Coverage graph: unavailable on runs 1–5 because `better-sqlite3` was compiled for Node ABI 127 while the active Node requires ABI 141.
- Executor route: requested `cli-codex model=gpt-5.6-luna`; nested dispatch was rejected by the `cli-codex` recursion guard, so the bounded direct-mode fallback was used and recorded in state.
- Reducer result: 5 iterations, 0 open questions, 27 projected key findings, resource map emitted, 0 corruption warnings.
- Scope result: all workflow artifacts were written under this detached lineage; parent packet synthesis and continuity writeback were intentionally deferred.

The run reached the hard iteration cap rather than graph-assisted convergence. The research conclusion is therefore evidence-complete for the five configured questions, but not a claim that the unavailable coverage graph independently confirmed convergence.
