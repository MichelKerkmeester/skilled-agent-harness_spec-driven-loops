# Iteration 002 — live consumer and route census

## Focus

Audit every live instruction and executable route outside the retired server tree that can grant, call, configure, or route to `system-spec-memory`. Concentrate on canonical and mirrored agents, `/memory:*`, `/speckit:*`, `/deep:*`, `/doctor:*`, hooks, plugins, launchers, and runtime configuration. Keep `system-skill-advisor` and its shared `hf-embed` transport as explicit retain/exception candidates.

## Required evidence

- Verify canonical agent and command paths with cited line ranges, then map runtime mirrors.
- Distinguish tool grants and call instructions from narrative mentions and historical packet records.
- Confirm the exact phase owner and replacement for each live consumer family.
- Identify any shared process, socket, or flag whose deletion would break a retained subsystem.
- Reuse the line-level machine inventory at `inventory.external.json`; do not broaden the repository scan with `--no-ignore`.

## Boundaries

Read anywhere in the repository. Write only the iteration narrative and delta under this lineage. Exclude `z_archive`; treat `.opencode/skills/system-spec-kit/mcp-server` as one aggregate entry; do not modify the parent or child spec packet, run a validator, invoke memory MCP, or stage files.

## Completion test

Produce a cited iteration narrative, a valid first-line delta record, and one append-gateway event input. Continue to iteration 003 regardless of an interim convergence signal because `stopPolicy=max-iterations`.
