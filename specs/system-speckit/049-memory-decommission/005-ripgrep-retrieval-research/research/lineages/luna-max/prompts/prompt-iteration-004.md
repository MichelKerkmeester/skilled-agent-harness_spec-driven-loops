# Iteration 4 prompt: parity, frozen probes, and measurable gates

Read the current lineage state, strategy, and iterations 1-3 before researching. Close the remaining parity question with repository evidence. Inspect the existing trigger/search fixtures and tests, phase 001 tasks and acceptance criteria, and the relevant ripgrep flag contracts already identified. Produce amendment-ready recommendations for phases 001 and 004 covering:

1. A hermetic parity harness that compares the legacy `exactTriggerSearch` SQL lane, the generated trigger index, and the ripgrep convention wrapper without confusing lexical parity with semantic paraphrase recall.
2. A frozen prompt set with positive, negative, scope, malformed, Unicode, archive, anchor, duplicate, and truncation cases.
3. Observable metrics and a latency protocol for the 200ms cold-lookup budget, deterministic generation, diagnostics, no leakage, and exit-status handling.
4. Failure modes and ranked edits to the phase 001/004 specs, plans, tasks, and acceptance criteria, each with file/line citations.

Do not modify repository files outside this lineage. Write the iteration markdown, one canonical delta JSONL record, and one legacy-shaped gateway event. Convergence is telemetry only; iteration 5 must still run.
