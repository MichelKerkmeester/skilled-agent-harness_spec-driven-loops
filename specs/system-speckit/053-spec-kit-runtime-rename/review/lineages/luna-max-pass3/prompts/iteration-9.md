Review iteration 9

Run a correctness adversarial replay over package manifests, lockfile workspace entries, TypeScript references, public exports, freshness traversal, and the moved runtime symlink. Reconcile every earlier candidate with current source evidence and record any unresolved verification limitations without executing repository tooling.

The max-iterations policy still requires the final tenth pass after this one, even if telemetry is stable.
