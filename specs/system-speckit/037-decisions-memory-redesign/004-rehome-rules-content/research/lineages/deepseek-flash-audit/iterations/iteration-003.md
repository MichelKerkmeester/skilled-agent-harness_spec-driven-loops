# Iteration 3: TESTS sweep — constitutional/learned tests and their post-deprecation assertions

## Focus
Every named constitutional/learned test; state what each MUST assert AFTER deprecation.

## Findings

### F3.1 gate-d-regression-constitutional-memory.vitest.ts (28 matches, 268 lines)
Four tests:
1. `:120-139` — `findConstitutionalFiles` discovery skips README placeholders (temp-dir fixture).
2. `:141-151` — loads `automated-writers-never-overwrite-manual.md` as constitutional advisory; asserts `importanceTier: constitutional`, `contextType: decision`, content phrase.
3. `:153-163` — loads `entity-cooccurrence-is-not-causal.md`; same assertion shape.
4. `:165-267` — search with `includeConstitutional: true` keeps constitutional row at top, drops archived/legacy; asserts `canonicalSource: 'constitutional'`, `stage1.constitutionalInjected: 1`, `countsBySourceKind.constitutional: 1`, source contract `gate-d-reader-ready-v1`. [SOURCE: file:gate-d-regression-constitutional-memory.vitest.ts:120,141,153,165,227,253,261]
- AFTER deprecation MUST assert: discovery (test 1) either removed with `findConstitutionalFiles` or asserts it finds nothing; tests 2-3 deleted or re-pointed at the rehomed root-doc content (no `importanceTier: constitutional` frontmatter anywhere); test 4 deleted or inverted: `includeConstitutional: true` produces NO constitutional rows, `constitutionalInjected: 0`, no constitutional source-kind counts, and no `canonicalSource: 'constitutional'` possible.

### F3.2 constitutional-filtering.vitest.ts (79 lines)
- `:37-60` — `getConstitutionalMemories` forwards includeArchived=true to store; `:62-78` — defaults includeArchived=false. Tests the public wrapper at `vector-index-queries.ts:516`. [SOURCE: file:constitutional-filtering.vitest.ts:37,62]
- AFTER deprecation MUST assert: wrapper deleted with the constitutional query path (or returns empty always if wrapper kept for migration); test deleted in the same change as the code.

### F3.3 token-budget-constitutional-sync.vitest.ts (34 matches, 300 lines)
- `:99-140+` — T206 suite: envelope token-budget truncation keeps `constitutionalCount` and `summary` ("Found N memories (M constitutional)") in sync; constitutional tail result is PINNED during squeeze (drop priority ordinary → additive backfill → constitutional). Drives real `enforceEnvelopeResultBudget` from context-server.ts. [SOURCE: file:token-budget-constitutional-sync.vitest.ts:99,103,133,138]
- AFTER deprecation MUST assert: no constitutional pin tier exists; truncation drops by ordinary priority only; `constitutionalCount` field removed from envelope (or always 0); summary form drops "(M constitutional)". Test rewritten around the new envelope shape or deleted with the pin code.

### F3.4 scoring-gaps.vitest.ts (10 matches)
- `:201-213` — `getConstitutionalFilter()` SQL shape assertions; `:236-254` — `shouldAlwaysSurface` truth table (constitutional=true, normal/critical/deprecated/nonexistent=false). [SOURCE: file:scoring-gaps.vitest.ts:201,236]
- AFTER deprecation MUST assert: `getConstitutionalFilter` and `shouldAlwaysSurface` removed from importance-tiers exports; test sections deleted with them (scoring-gaps keeps its other gap tests).

### F3.5 memory-crud-update-constitutional-guard.vitest.ts (32 matches)
- `:153-201` — memory_update tier guard: non-constitutional path requesting constitutional → downgraded to important + `tier_downgrade_non_constitutional_path` audit; row inside `/constitutional/` keeps tier without audit.
- `:203-229` — rejects tier change away from constitutional on constitutional rows (`E_CONSTITUTIONAL_SELF_EDIT`); `:320-339` — same for protection-removing updates; `:341+` — safe updates with `expectedHash`. [SOURCE: file:memory-crud-update-constitutional-guard.vitest.ts:153,180,203,320,341]
- AFTER deprecation MUST assert: `importanceTier: 'constitutional'` is rejected/normalized for ALL paths (no constitutional tier exists); `E_CONSTITUTIONAL_SELF_EDIT` and `tier_downgrade_non_constitutional_path` governance audits removed; remaining tests (source_kind protection) unchanged.

### F3.6 retrieval-directives.vitest.ts (26 matches)
- T-A4-01..05 (56 its): extraction of `retrieval_directive` metadata (surfaceCondition "Always surface when:", priorityCondition "Prioritize when:") from imperative rule content; T14 `:179-182` — source defaults to "constitutional memory" when title undefined. [SOURCE: file:retrieval-directives.vitest.ts:57,179]
- AFTER deprecation MUST assert: module deleted with `enrichWithRetrievalDirectives` consumers (memory-surface.ts:346-348); T14's "constitutional memory" fallback source removed.

### F3.7 stage1 tests passing includeConstitutional:true
- `stage1-expansion.vitest.ts:524` — config `includeConstitutional: true`; `stage1-embedder-degrade.vitest.ts:150` — "Constitutional rows must still surface even though the vector lane was..." (degraded-embedder fallback injection); `stage1-llm-reformulation-trace.vitest.ts:91`; also `spec-folder-prefilter.vitest.ts:358`, `retrieval-level.vitest.ts:251` (always-surface constitutional row injected in global retrieval). [SOURCE: file:stage1-expansion.vitest.ts:524, file:stage1-embedder-degrade.vitest.ts:150, file:retrieval-level.vitest.ts:251]
- AFTER deprecation MUST assert: stage1 never injects constitutional candidates regardless of flag; degraded-embedder test asserts plain lexical search without constitutional guarantee; retrieval-level 'global' test drops the constitutional injection assertion.

### F3.8 Other constitutional test tail (per-file counts; must-assert summary)
- High-count: `memory-save-index-scope.vitest.ts:25` (index-scope constitutional handling), `full-spec-doc-indexing.vitest.ts:15`, `checkpoint-restore-invariant-enforcement.vitest.ts:16` + `checkpoint-restore-readme-poisoning.vitest.ts:6` (checkpoint constitutional-path governance → assert no constitutional restore path), `handler-memory-index.vitest.ts:6`, `handler-memory-index-scan-jobs.vitest.ts:6`, `handler-memory-index-cooldown.vitest.ts:10`, `handler-memory-index-async-scan.vitest.ts:3`, `memory-index-scoped-scan-gating.vitest.ts:7` (indexer scan include_constitutional — assert default false or discovery removed), `orphan-sweep-time-budget-and-refresh.vitest.ts:13` (orphan sweep constitutional handling), `hooks-ux-feedback.vitest.ts:12` + `memory-search-ux-hooks.vitest.ts:10` + `tiered-injection-turnNumber.vitest.ts:10` + `dual-scope-hooks.vitest.ts:9` + `hook-precompact.vitest.ts:1` + `compact-merger.vitest.ts:2` (surface-hook constitutional injection — assert no constitutional surface section), `context-server.vitest.ts:19` (T000g compaction-hook source asserts autoSurfaceAtCompaction — assert surface without constitutional), `cold-start.vitest.ts:3`, `unit-tier-classifier-types.vitest.ts:10` + `tier-classifier.vitest.ts:10` + `importance-tiers.vitest.ts:10` (tier type surfaces — assert constitutional removed from union), `decay.vitest.ts:11` + `hybrid-decay-policy.vitest.ts:5` + `fsrs-hybrid-decay.vitest.ts:5` + `attention-decay.vitest.ts:7` + `fsrs-scheduler.vitest.ts:6` (decay paths for constitutional — assert tier gone), `active-row-predicate.vitest.ts:8` + `active-row-read-policy.vitest.ts:4` (constitutional lane — assert lane removed), `gate-d-regression-memory-tiers.vitest.ts:4`, `gate-d-regression-intent-routing.vitest.ts:6` (includeConstitutional:true at :339 — assert flag removed), `gate-d-regression-4-stage-search-pipeline.vitest.ts:6`, `eval-metrics.vitest.ts:18`, `feature-eval-scoring-calibration.vitest.ts:5` (eval constitutional metrics — assert removed), `learned-feedback.vitest.ts:2+3` (SPECKIT_LEARN_FROM_SELECTION_START fixtures; learned-trigger 30-day TTL — assert learned surface unchanged (system C) or removed with C), `feedback-retention-reducer.vitest.ts:7` + `memory-retention-feedback-learning.vitest.ts:4` + `feedback-safety-posture.vitest.ts:3` (feedback learning incl. constitutional-adjacent paths), `vector-index-store-remediation.vitest.ts:15` + `vector-index-store.vitest.ts:10` + `vector-index-queries.vitest.ts:1` + `vector-index-impl.vitest.ts:2` + `vector-index-schema-*.vitest.ts` (store/schema constitutional handling — assert tier removed from schema/validation), `search-results-format.vitest.ts:3` (envelope summary "(M constitutional)" — assert dropped), `tool-input-schema.vitest.ts:2` (includeConstitutional schema props — assert removed), `memory-save-trigger-guards.vitest.ts:2`, `save-response-metadata-advisory.vitest.ts:2`, `append-exempt-serializer.vitest.ts:10` + `cleanup-script-audit-emission.vitest.ts:10` (audit emissions for constitutional paths), `spec-memory-cli-fallback-hooks.vitest.ts:2`, `trigger-config-extended.vitest.ts:8`, `trigger-promotion.vitest.ts:2`, `retention-forgetting-on-path.vitest.ts:2`, `pe-gating-provenance.vitest.ts:2`, `mutation-hooks.vitest.ts:2` + `mutation-hooks-statediff.vitest.ts:1` + `hooks-mutation-wiring.vitest.ts:1` (constitutionalCacheCleared fields — assert removed), `search-archival.vitest.ts:3`, `search-fallback-tiered.vitest.ts:2`, `pipeline-v2.vitest.ts:3`, `budget-allocator.vitest.ts:4`, `chunking-orchestrator.vitest.ts:4`, `degree-computation.vitest.ts:8`, `rrf-degree-channel.vitest.ts:3`, `five-factor-scoring.vitest.ts:5`, `composite-scoring.vitest.ts:6`, `unit-composite-scoring-types.vitest.ts:1`, `score-normalization.vitest.ts:1`, `scoring.vitest.ts:11`, `scoring-observability.vitest.ts:1`, `folder-scoring.vitest.ts:2`, `memory-types.vitest.ts:2`, `index-scope.vitest.ts:4`, `incremental-index-v2.vitest.ts:1`, `index-dedup-projection-evicted.vitest.ts:1`, `bm25-index.vitest.ts:1`, `hybrid-search.vitest.ts:1`, `memory-lineage-state.vitest.ts:4`, `memory-lineage-backfill.vitest.ts:1`, `create-record-lineage-regressions.vitest.ts:1`, `vector-index-schema-migration-refinements.vitest.ts:3`, `validation-metadata.vitest.ts:4`, `memory-parser-extended.vitest.ts:4`, `secret-scrubber.vitest.ts:1`, `safety.vitest.ts:1`, `short-critical-quality-gate.vitest.ts:2`, `orchestrator-error-cascade.vitest.ts:2`, `cross-feature-integration-eval.vitest.ts:1`, `modularization.vitest.ts:1`, `memory-search-transient-miss-e2e.vitest.ts:2`, `memory-search-response-path-hygiene.vitest.ts:2`, `memory-search-eval-channels.vitest.ts:2`, `memory-search-channel-telemetry.vitest.ts:2`, `channel-telemetry-*.vitest.ts`, `handler-memory-search-live-envelope.vitest.ts:2`, `handler-memory-index-needs-rebuild.vitest.ts:1`, `provenance-envelope.vitest.ts:2`, `retrieval-rescue.vitest.ts:1`, `memory-retention-sweep.vitest.ts:6`, `graph-signals.vitest.ts:1`, `feature-eval-graph-signals.vitest.ts:2`, `access-tracker-extended.vitest.ts:3`, `trigger-matcher.vitest.ts:1`, `adaptive-ranking.vitest.ts:1`, `stage2-fusion.vitest.ts:1`, `stage4-filter.vitest.ts` etc. — common pattern: fixtures/assertions referencing constitutional tier rows. [SOURCE: per-file grep counts, Iter 3]
- AFTER deprecation, the tail class MUST assert: no test fixture creates `importance_tier = 'constitutional'`; no test asserts constitutional injection/surface/pinning/boost; tier-union tests drop the value; schema/validation tests reject the tier.

### F3.9 memory-learn-command-docs.vitest.ts (scripts/tests)
- `:16-23` — `it.fails.skip` asserts learn.md is "# /memory:learn — Constitutional Memory Manager" and lacks contradictory wording; `:26-58` — `it.skip` asserts 6 docs (commands/README.txt, memory/README.txt, search.md, speckit/complete.md, root README.md, .opencode/README.md) mention `/memory:learn` + no legacy wording + combined text matches `/Constitutional memory manager|constitutional memories/i`. [SOURCE: file:scripts/tests/memory-learn-command-docs.vitest.ts:16,26,57]
- AFTER deprecation MUST assert: either the command is retired (test deleted) or the docs assertion flips to "no constitutional memory management claims" — the current `:57` positive regex on constitutional wording must be inverted or removed.

## Sources Consulted
- mcp-server/tests/*.vitest.ts (per-file grep counts + targeted reads), scripts/tests/memory-learn-command-docs.vitest.ts

## Assessment
- newInfoRatio: 0.9 — test map complete for named suites; tail classification is aggregate (counts) not per-test detail.
- Novelty justification: third disjoint surface (tests); some tail files only count-verified.
- Confidence: high for named suites; medium for aggregate tail (fixture-level assertions inferred from counts — flagged as inferred in F3.8).

## Reflection
- Worked: file-count ranking then targeted reads for the 9 named suites.
- Ruled out: reading all 90+ test files individually — aggregate classification sufficient for the inventory; named suites carry the load-bearing assertions.

## Recommended Next Focus
Iter 4: READMEs + SKILL.md + hooks + skill-advisor render.ts + feature-catalog + manual-testing playbooks + injection-contract.md.
