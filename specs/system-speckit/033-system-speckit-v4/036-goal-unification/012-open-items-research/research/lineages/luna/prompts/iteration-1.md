Resolved route: mode=research target_agent=deep-research

# Inline iteration 1 — adversarial citation audit

Read the lineage state and strategy first. Treat the first report at
`specs/system-speckit/033-system-speckit-v4/036-goal-unification/012-open-items-research/research/research.md`
as a hypothesis. Open its cited files. Audit all five rings.

Record every material claim that does not hold, every cost or blast-radius estimate that omits a
live dependency, and every alternative the report dismissed without enough evidence. Preserve
claims that survive, and classify each disagreement as `underspecified` or `thin evidence`; do not
average answers. Cite `file:line` for every current-behavior claim. Write the narrative to
`iterations/iteration-001.md`, the structured delta to `deltas/iter-001.jsonl`, and the canonical
route-proof state record for this iteration.

The next iteration must make one recommendation for each of: prose line width, unowned surfaces,
the root README goal section, the word `goal` naming four things, and which facts earn a machine
check. Convergence before iteration 2 is telemetry only.
