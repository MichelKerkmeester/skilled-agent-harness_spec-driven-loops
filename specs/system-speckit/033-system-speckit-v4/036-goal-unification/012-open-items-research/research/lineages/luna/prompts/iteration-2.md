Resolved route: mode=research target_agent=deep-research

# Inline iteration 2 — five direct recommendations

Read the latest state and strategy and the completed iteration-001 narrative before researching.
For each of the five open items, give one recommendation, say exactly where it differs from the
first report, cite the evidence that separates the answers, and state what would have to be true
for the first report's answer to win. Do not average disagreements. Label the disagreement
`underspecified` when the desired contract is not named and `thin evidence` when the repository
does not show the needed incident, query, or measurement.

The five items are: prose line width; unowned surfaces; the root README goal section; the word
`goal` naming four things; and which facts earn a machine check. Cite `file:line` for every claim
about current behavior. Write `iterations/iteration-002.md`, `deltas/iter-002.jsonl`, and the
canonical route-proof state record. Convergence remains telemetry; the configured cap is reached
after this iteration.
