---
iteration: 6
mode: review
focus: D2 Security — provider factory, registry and custom-model dimension boundaries
---

Read the current shared embedding registry, factory, startup profile, auto-selection path and HF-local client tests. Check provider/model identity, dimension metadata, fallback precedence and persisted-profile consistency. Revalidate carried findings from current sources. Do not run tests, validators, generators, git writes or nested dispatch; write only under the bound lineage directory.

BANNED OPERATIONS

- Do not modify target, implementation, tests, runtime files, git state or any path outside the bound lineage directory.
- Do not run a nested CLI, agent, task dispatch, validator or generator.

ALLOWED WRITE PATHS

- The bound lineage directory only: prompts/, iterations/, deltas/, state JSONL, registry, strategy, dashboard, logs and final report.
