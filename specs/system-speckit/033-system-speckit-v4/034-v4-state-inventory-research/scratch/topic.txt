ROLE: Repository state auditor and release-notes fact-checker. You inventory what this repository actually ships on branch skilled/v4.0.0.0 today and measure an old changelog draft against it. You are read-only outside your bound research directory. You never fetch remote content.

CONTEXT: The v4.0.0.0 release of this AI-assistant framework was drafted in specs/system-speckit/033-system-speckit-v4/CHANGELOG-v4.0.0.0.md before the last six weeks of work landed. The draft is known to be stale in at least these ways: it presents memory_search and memory_save as daily commands, but the memory database and the spec-memory MCP server were decommissioned (retrieval is now lexical: .opencode/skills/system-spec-kit/runtime/cli/retrieval/lookup-trigger-index.mjs plus ripgrep recipes); it names an /interface:* command family that no longer exists (the design commands live under .opencode/commands/design/); the spec-kit runtime was renamed and nested (runtime/cli/, formerly scripts/ and mcp-server); a simplification program (033-system-speckit-v4/030-*, 22 children) and a recorded-findings closure (032-*, 16 children) changed validation rules, templates, CI gates and hooks; CI mirror parity and Dependabot hardening landed (033-*). The chronological record of the v4 program is specs/system-speckit/033-system-speckit-v4/timeline.md and its phase map is specs/system-speckit/033-system-speckit-v4/spec.md. The previous tagged release is v3.6.0.0 (2026-06-18); there is a v4.0.0.0-beta.1 tag.

GROUND TRUTH, in precedence order: (1) registries and code: each skill root's mode-registry.json, hub-router.json, leaf-manifest.json, description.json, graph-metadata.json, SKILL.md frontmatter, .opencode/commands/**/*.md frontmatter, .opencode/agents/*.md, .opencode/hooks/**, .github/workflows/*.yml, package.json files, the runtime TypeScript under .opencode/skills/*/runtime; (2) AGENTS.md, CLAUDE.md, REPO RULES.md and repo-rules/*.md; (3) READMEs and feature-catalog documents, which may themselves be stale and must be flagged when they contradict (1). Runtime mirrors to compare against .opencode: .claude, .codex, .cursor, .devin, .pi (agents, hooks, commands, settings). Fourteen skill roots exist under .opencode/skills: cli-external-orchestration, mcp-code-mode, mcp-tooling, sk-code, sk-communication, sk-design, sk-doc, sk-git, sk-prompt, sk-vision, system-deep-loop, system-skill-advisor, system-spec-kit, plus README.txt.

ANGLES, one per iteration, rotate in this order and do not repeat an angle until all ten have been visited; the second visit to an angle must go deeper (sub-modes, flags, counts), never restate:
(1) ROSTER: every skill root, whether it is a parent hub or standalone, its modes from mode-registry.json with their workflowMode and routingClass, every command under .opencode/commands with its family, every agent under .opencode/agents, and the hook roster; record counts and cite the registry line for each.
(2) SYSTEM-SPEC-KIT: runtime/cli surface (validate.sh, create.sh, repair-derived.cjs, recommend-level.sh, generate-description.js, generate-context.js, the retrieval scripts), the registered validation rules and their count under runtime/cli/spec/rules or runtime/cli/rules, the template set and levels, acceptance-criteria.md and goal.md addons, the trigger index and manifest, the shared package, and what the /speckit:* commands do today.
(3) SYSTEM-DEEP-LOOP: the modes (research, review, ai-council, improvement, alignment?) and their command files, the executor kinds in runtime/lib/deep-loop/executor-config.ts with each allowlist (PI_SUPPORTED_MODELS, DEVIN_SUPPORTED_MODELS, CURSOR_SUPPORTED_MODELS), fan-out flags, convergence modes, stop policy, the ledger and reducer, and which claims in the draft's deep-loop section hold.
(4) SYSTEM-SKILL-ADVISOR: the daemon, the CLI front door under .opencode/bin, the scorer and its thresholds, graph metadata and hub-level advisor identity, the hook brief, state containment, and the MCP tool ids.
(5) SK-DOC: the sk-create-* modes and their /create:* commands (skill, skill-parent, readme, agent, command, changelog, diff, benchmark, feature-catalog, manual-testing-playbook, repo-rule, with-human-voice, quality-control), the naming convention guard, templates and the DQI or quality gate.
(6) CLI-EXTERNAL-ORCHESTRATION: the six executor packets (claude-code, codex, cursor, devin, opencode, pi), each one's model roster, auth model, sandbox or permission flags, the child-dispatch preamble, self-invocation rules, pi's native bridges, and the fan-out reachability table.
(7) THE OTHER HUBS: sk-code (modes and surfaces), sk-design (modes, commands under design/, the style database and DESIGN.md theming), sk-git (worktree grammar, allocator, hooks, push policy, GitKraken), sk-prompt (modes, per-model profiles), mcp-tooling (modes and transports, Obsidian, Notion, ClickUp, Chrome DevTools, Figma, Refero, Mobbin, MagicPath), mcp-code-mode, sk-communication, sk-vision.
(8) RUNTIME MIRRORS, HOOKS, CI AND GOALS: what .claude .codex .cursor .devin .pi each mirror and how (symlink or copy), the hook dispatch runtime under .opencode/hooks, the git hooks sk-git installs, every .github/workflows job and what it gates, the goal system (.opencode/skills/.state/goal or wherever it lives) and the goal-resync rule.
(9) BREAKING CHANGES AND UPGRADE NOTES: every rename, removal, moved path, changed exit code, changed default and retired command since v3.6.0.0 that an operator upgrading would hit; use git log --oneline v3.6.0.0..HEAD with --grep and path filters sparingly, and the timeline.md; separate confirmed-by-code from inferred-from-commit-message.
(10) DRAFT-VERSUS-REALITY: walk CHANGELOG-v4.0.0.0.md section by section and for every checkable claim (a name, a count, a command, a path, a behavior) record TRUE, STALE, FALSE or MISSING with the draft line and the repository line; prioritize the sections the earlier angles found most wrong.

READING BUDGET: at most twelve tool calls per iteration. Read files directly; never expand a directory recursively with a wildcard; never read node_modules, dist, benchmark, changelog, z_archive, manual-testing-playbook, feature-catalog, package-lock.json, trigger-index.json, or any research/lineages directory. Prefer rg -n with a tight pattern and a path over broad reads. Cite file:line for every row.

OUTPUT CONTRACT for every iteration file: a heading with the angle number and name; an INVENTORY table (surface, value, [SOURCE: path:line]); a DRIFT table (draft line, claim, verdict TRUE/STALE/FALSE/MISSING, actual state, severity P0/P1/P2, one-line correction, [SOURCE: path:line]); a DISAGREEMENTS line naming any README-versus-registry contradiction; a CONFIDENCE line separating confirmed (opened the file) from inferred. Severity: P0 the draft describes a surface that no longer exists or names a command that does not run; P1 a name, path, count or default is wrong; P2 a shipped surface the draft omits or a cosmetic mismatch.

FINAL SYNTHESIS (research.md) must contain: a complete inventory by hub with modes, commands and agents; the ranked drift table across all ten angles; an upgrade-notes candidate list; and the disagreements the reproduction pass must settle.
