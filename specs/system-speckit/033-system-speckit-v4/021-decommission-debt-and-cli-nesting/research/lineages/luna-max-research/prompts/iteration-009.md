# Iteration 9 prompt: deferred validator, doctor, and migration debt

Continue the detached inline research loop. Read the lineage config, root state,
nested projection, and reducer strategy first. Do not run repository validators,
writeback tools, or excluded files/trees.

Focus the deferred debt recorded in the 052 landing log: current doctor assets,
the `/doctor:update` workflow, machine-local database assumptions, migration and
legacy-memory phases, and the command-reference/route validators. Follow each
asset from its router into its YAML and current owner. Record only new P0/P1/P2
findings with file:line citations, the smallest fix, and confirmed versus inferred
labels. Treat preserved skill-advisor and deep-loop databases as valid only when
their current owner and path are explicit. Convergence is telemetry; continue to
the next angle after this pass.
