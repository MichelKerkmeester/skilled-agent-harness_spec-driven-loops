# Iteration 14 prompt

Audit system-spec-kit documentation and runtime README contracts for references to code, handlers, commands, or test paths that no longer exist after the memory/database decommission and runtime rename. Focus on `.opencode/skills/system-spec-kit/**`; do not duplicate the already-recorded DB environment, install recovery, Pi mirror, or trigger-index coverage findings. Verify named paths by source inventory without reading excluded directories or writing outside the lineage.

