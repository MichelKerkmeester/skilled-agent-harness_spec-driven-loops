# Iteration 4 prompt

Resolved route: mode=research target_agent=deep-research

Focus the dependency/importer balance across shared, scripts, and runtime. Read only package manifests and bounded source directories. Identify declared packages with no current non-test importer, value importers without a package owner, and dependencies retained only to support the retired SQLite/vector lane. Treat Pi host peers and preserved advisor IPC as separate owners unless evidence says otherwise.
