# Iteration 6: ipc/ + workspace/ + chunking + lib/ + the environment-variable ledger — who reads what, and the 2-spelling bug family

## Focus

The remaining unbrooded surfaces — `ipc/socket-server.ts` (the daemon's IPC, its three parties), `workspace/repo-root.mjs` (the 9× require, its diverged copy, what it actually keys on), `chunking.ts` (whose lone live import turns out to be a provider), `lib/structure-aware-chunker.ts` + `continuity/ast-parser.ts` (the second dead island) — and the charter's explicit demand: every environment variable and path `config.ts`/`paths.ts`/embeddings resolve, and who reads each, cross-checked against the README §5's declared Configuration table.

## Findings

| # | path:line | Claimed vs actual | Severity | Recommendation |
|---|-----------|-------------------|----------|----------------|
| F6.1 | `shared/ipc/socket-server.ts:142,217` | Claimed: the shared IPC server for the advisor daemon. Actual: **live-strong, three parties** — the advisor's shim imports it twice (`mcp-server/lib/ipc/socket-server.ts:16,22` → `advisor-server.ts`, LIVE); the socket *location* is negotiated by the launcher side (`bin/system-skill-advisor-launcher.cjs` + `lib/launcher-ipc-bridge.cjs`, the `SPECKIT_IPC_SOCKET_DIR` readers, 21+ files including `bin/hf-model-server.cjs`, the smoke tests, `worktree-session.sh`, the projection layer); and `bin/skill-advisor.cjs:25-26` independently carries the defaults (`/tmp/system-skill-advisor` + `daemon-ipc.sock`). The environment contract, however, is duplicated: `SPECKIT_MAX_SECONDARY_CLIENTS` is *read* at socket-server.ts:142 and merely *allowlisted* at launcher.cjs:152 (`CHILD_ENV_ALLOWLIST`) — two places to keep in sync, no enforcement: a newly read-but-unallowlisted var silently never reaches the daemon. | P2 | Document the launcher→shared env-allowlist as a contract (and consider deriving the allowlist from the reader's list, not a second hand-written table). |
| F6.2 | `shared/workspace/repo-root.mjs` (70) + `runtime/hooks/lib/workspace/repo-root.mjs` (a copy, already diverged) | Claimed (module header:1-15): "Every writer that persists state, in any package, must anchor to the repository root" — the .opencode-**file-sentinel** + the *outermost-`.opencode`-segment hoist* design (NOT git-based — no rev-parse/.git reference). Actual: the 4th root-resolver in the package's five-resolver story (F2.9/F4.3), consumed via the export map's only non-redundant named entry — 9× `require('@spec-kit/shared/workspace/repo-root.mjs')` — all of them **build/sync/CI tooling** (sync-prompts/sync-agents ×2, sync-runtime-mirrors, generate-command-routers, dist-freshness, replay-corpus, extract-from-evidence), zero runtime request paths. And the very same workspace tree *also* carries `runtime/hooks/lib/workspace/repo-root.mjs` — a **copy of the module that has already diverged** (`diff -q`: differs) — inside the package (`runtime`) that sits next to `shared/` and depends on it (`file:../shared`). Two hook-standalone strategies coexist: copy (repo-root) vs relative-`shared/dist` import (spec-gate), one directory apart. | **P1** | Merge: the diverged hooks copy is the eldest residue — reconcile it to the shared file (the sentinel design rationale, header:3-22, argues for exactly one authority) or formally split the "hook-standalone" tier; today the two repo-roots disagree and nothing tests their drift. |
| F6.3 | `shared/lib/structure-aware-chunker.ts` (222) + `runtime/cli/continuity/ast-parser.ts` | Claimed (README tier `lib/`): the markdown-structure-aware chunker. Actual: the **second dead island** — the chunker's sole production-grade importer is `continuity/ast-parser.ts:8` (`chunkMarkdown`, `splitIntoBlocks`), and `ast-parser.ts` itself has no `isMainModule`/`process.argv` (not a CLI), is absent from `scripts-registry.json` and `cli/core/index.ts`, and is imported by exactly **one test** (`cli/tests/test-ast-parser.js`). The pair (chunker + parser + 2-3 test files, ≈340+ lines) terminates in nothing; the metadata-integrity machinery that plausibly motivated it derives its headings elsewhere. Same pattern as F3.1's quartet and F4.1's contracts: complete, tested, unwired. | **P1** | Remove: both modules + their tests, after the acceptance decision; or wire the parser into the continuity/quality flow it was extracted for — today it runs for no one. |
| F6.4 | `shared/chunking.ts` (143) | Claimed: the section-aware semantic chunker. Actual: live via **one import** — the hf-local *provider* (`embeddings/providers/hf-local.ts:13` imports `semanticChunk` + `MAX_TEXT_LENGTH`); its other three exports (`RESERVED_OVERVIEW:23`, `RESERVED_OUTCOME:25`, `MIN_SECTION_LENGTH:27`) are consumed only by the production-dead monolith (`embeddings.ts:20`, F2.1) and a test that imports it by **relative source path** (`runtime/tests/chunking-semantic.vitest.ts:10` `'../../shared/chunking'` — a third convention, neither the specifier nor the vitest alias). The compaction-reservation constants are the retired-DB compaction's vocabulary, surviving inside a dead import. | P2 | Merge: after F2.1, hf-local's `semanticChunk` call is the module's only reason to exist — either inline it into the provider (one fewer shared surface) or explicitly re-scope the module header to "provider-internal"; the 3 reservation constants follow the monolith's fate. |
| F6.5 | `shared/**` env reads (clean census: 24 distinct variables) vs `shared/README.md:319-328` (§5 Configuration) | Claimed: the Configuration table — 7 variables (`EMBEDDINGS_PROVIDER`, `OLLAMA_EMBEDDINGS_MODEL`, `HF_EMBEDDINGS_MODEL`, `HF_EMBEDDINGS_DTYPE`, `VOYAGE_API_KEY`, `OPENAI_API_KEY`, `OPENAI_EMBEDDINGS_MODEL`). Actual: shared reads **24** — the declared 7 plus 17 undocumented: `VOYAGE_EMBEDDINGS_MODEL` (voyage.ts:130), `VOYAGE_BASE_URL` (voyage.ts:25-26, auto-select.ts:148), `VOYAGE_API_URL` (profile.ts:252), `OPENAI_BASE_URL` (factory + openai.ts:104), `OLLAMA_BASE_URL` (ollama.ts:81, profile.ts:252, factory), `OLLAMA_REQUEST_TIMEOUT_MS` (adapters/ollama.ts:110), `HF_EMBED_AUTH_TOKEN` (hf-local.ts:493), `HF_EMBED_SERVER_READY_TIMEOUT_MS` (hf-local.ts:649), `SPECKIT_HF_MODEL_SERVER_LOADING_MAX_MS` (hf-local.ts:655), `SPECKIT_HF_READY_LATCH_TTL_MS` (hf-local.ts:672), `SPECKIT_CASCADE_PROBE_TIMEOUT_MS` (auto-select.ts:117), `SPECKIT_ROLLOUT_PERCENT` (adaptive-fusion.ts:104), `MEMORY_DB_PATH` (paths.ts:164 + factory + hf-local.ts:346 — three readers, and the launcher *sets* it: launcher.cjs:286-303), `SPEC_KIT_DB_DIR`/`SPECKIT_DB_DIR` (config.ts:10, profile.ts:274, factory ×2), `VITEST`/`NODE_ENV`/`SPECKIT_TEST` (paths.ts:68-70), `SPECKIT_IPC_SOCKET_DIR` (socket-server.ts:217 + hf-local), `SPECKIT_MAX_SECONDARY_CLIENTS` (socket-server.ts:142). Reads by liveness tier: the adapter/ollama/auto-select/factory/paths/socket cluster = **live** (F2.3/F5.5/F6.1); `SPECKIT_ROLLOUT_PERCENT` = the *dead* quartet's rollout flag (double residue); `VOYAGE_API_URL` = a parallel-spelling seam (F6.6). | **P1** | Fix: the §5 table declares 7 of 24; 5 of the undocumented are operator-facing contract (MEMORY_DB_PATH via the launcher, the HF_*/SPECKIT_HF_* model-server trio, the DB_DIR spellings). Rewrite §5 as the *categorized* ledger (provider/DB/IPC/test) or point it at the code census — a 29%-complete configuration section is how the 2-spelling bugs stayed invisible. |
| F6.6 | the 2-spelling bug-family (4 instances) | Claimed: n/a (a structural finding). Actual: the package resolves the *same* thing under two names in four places: (a) `SPEC_KIT_DB_DIR \|\| SPECKIT_DB_DIR` — config.ts:10, factory (fingerprint + candidates), profile.ts:274; (b) the Voyage base URL — `VOYAGE_API_URL` (profile.ts:252, the *profile/storagederivation*) vs `VOYAGE_BASE_URL` (voyage.ts:25-26, the *startup validation* — its own comment:19 "honor VOYAGE_BASE_URL for startup validation, not just runtime" — the history of the split is IN the comment); (c) `SYSTEM_SKILL_ADVISOR_MEMORY_DB_PATH`→`MEMORY_DB_PATH` (launcher:286-303 — deliberate, documented); (d) `SYSTEM_SKILL_ADVISOR_CLI_DEV_ALLOW_STALE \|\| SPECKIT_SKILL_ADVISOR_CLI_DEV_ALLOW_STALE` (bin/skill-advisor.cjs:27-28). Each pair = two resolutions of one deployment fact; (b) means an operator setting only `VOYAGE_API_URL` gets *profiled* against the override but *validated* against the default endpoint. | **P1** | Fix: one env-aliasing note in §5 (or one resolution point per resource: the factory already funnels Voyages through `resolveVoyageBaseUrl()` — route the profile through it and (b) collapses). |

**Ruled out this iteration:** "chunking is consumed by the compaction path" — its only live caller is the hf-local provider; the RESERVED_* compaction constants ride the dead monolith (F6.4). Also ruled out: "ast-parser is a CLI" (no isMainModule/argv, no registry row) and "repo-root.mjs is git-aware" (it keys on the .opencode file-sentinel + the outermost-segment hoist — the header design rationale, 1-22).

## Sources Consulted

- `ipc/socket-server.ts:142,217`; the `SPECKIT_IPC_SOCKET_DIR` 21-reader -l (supersets the allowlist); `bin/skill-advisor.cjs:24-26`; `launcher.cjs:152` (CHILD_ENV_ALLOWLIST) and :286-303
- `workspace/repo-root.mjs:1-32` (the design-rationale header + the marker), the 9-require census (F1.4), `diff -q` vs `runtime/hooks/lib/workspace/repo-root.mjs` (differs), the callers' build/sync/CI identity
- `lib/structure-aware-chunker.ts` (wc: 222); `continuity/ast-parser.ts` (no isMainModule/argv; 1 importer: `cli/tests/test-ast-parser.js`; absent from scripts-registry + core/index)
- `chunking.ts:21-27,71`; its 4 referencers; `hf-local.ts:13`; `runtime/tests/chunking-semantic.vitest.ts:8-12` (relative-source)
- THE ENV CENSUS: `rg -o` with source-filters (24 distinct), the 3 tail vars located (`auto-select.ts:117`, `adapters/ollama.ts:107-110`, `adaptive-fusion.ts:104`); `profile.ts:248-258` (VOYAGE_API_URL); `voyage.ts:19-26` (VOYAGE_BASE_URL + its own history comment); the README §5 declared-7 table; the `SPEC_KIT_DB_DIR|SPECKIT_DB_DIR` four-read confirmation; the `VOYAGE_BASE_URL|VOYAGE_API_URL` repo-wide census

## Assessment

- newInfoRatio: 0.85 — the env-var ledger (24-vs-7) and its 2-spelling bug-family, the diverged repo-root copy, and the second dead island are all new; the ipc-liveness confirms the census.
- Novelty justification: F6.5/F6.6 produce the charter's "every environment variable and who reads each" deliverable with named readers/setters; F6.2's already-diverged same-workspace copy and F6.3's test-only chunker/parser pair are first-recorded.
- Confidence: high for F6.1-F6.5 (direct reads, diffs, and censuses); high for F6.6 (the code + its own comments); the SPECKIT_MAX_SECONDARY_CLIENTS *setter* question resolved (allowlist-pass-through, not a setter).

## Reflection

- Worked: re-running the env census with source-only filters — the first pass silently included `dist/` (a 1,200-line compiled vanished-module line-number); reading `repo-root.mjs`'s rationale header corrected the "git-aware" assumption before it reached the ledger.
- Failed: one rg rooted at the wrong cwd (`.opencode` from inside `.opencode/skills/system-spec-kit`); the allowlist/reader duplication needed two passes to stop calling the launcher a "setter".
- Ruled out: see above.

## Recommended Next Focus

Iteration 7: the export-map/index/dead-export audit + the dist-freshness verification — the promised F1.7 follow-through: recompute the dist-freshness source hash the way `runtime/cli/lib/dist-freshness.cjs` does (read its scheme) and compare against the committed marker; ALSO the barrel's dead-export rows (already 3 of 24 families used) vs the export map's `./*` wildcard; the `test:task-enrichment` script that just echoes ("No task-enrichment tests in @spec-kit/shared" — package.json:20 — a script that runs nothing); the three dependencies' (js-yaml, @modelcontextprotocol/sdk, @huggingface/transformers) actual importers inside shared (js-yaml = parse-frontmatter? the MCP-SDK = socket-server; transformers = hf-local dynamic import?); and the tsconfig/`build`-entry reality (what does `tsc --build` actually include?).
