# Iteration 7: The dist-freshness verdict (executed, not assumed) + the dependency trio + the build's shipped tests

## Focus

F1.7's promised verification — the dist-freshness question settled by *executing the enforcement machinery's own read-only functions* — plus the three runtime dependencies' in-shared importers, the undeclared test-runner dependency, the no-op `test:task-enrichment` script, and the build's shipped-test-outputs question.

## Findings

| # | path:line | Claimed vs actual | Severity | Recommendation |
|---|-----------|-------------------|----------|----------------|
| F7.1 | `shared/package.json:26-28` | Claimed: three runtime dependencies power the package (`@huggingface/transformers ^3.8.1`, `@modelcontextprotocol/sdk ^1.24.3`, `js-yaml ^4.3.0`). Actual: **the heaviest dependency has zero importers** — no static or dynamic import of `@huggingface/transformers` exists anywhere in shared sources (the only "hits" are comments: auto-select.ts:489 — "pure-Node @huggingface/transformers **HTTP model server**" — the *server* side, which lives in `.opencode/bin/hf-model-server.cjs` and does the heavy lifting; shared = its *client* via the unix-socket, F2.5). The other two: `js-yaml` = exactly 1 importer (`frontmatter/parse-frontmatter.ts:12`, the workhorse — LIVE), `@modelcontextprotocol/sdk` = 1 (`ipc/socket-server.ts:14` `StdioServerTransport` — LIVE, whose own comment :41 documents the doctrine "every consuming package satisfies it with its own @modelcontextprotocol/sdk copy — the three…", i.e. the deliberate duplicate-dependency pattern this package's ghost dep exemplifies). Also: shared declares **no devDependencies at all**, yet its `test` script needs `tsx` (`node --import tsx`) — declared only by the *sibling* package (`system-deep-loop/runtime/package.json`: `tsx 4.22.4`); the shared's test lane = transitive hope, not a declaration. | **P1** | Fix: drop `@huggingface/transformers` from shared's deps (the model-server owns it) or mark it tier-reserved with a comment naming the real importer; declare `tsx` (or run the suite through a declared runner); the copy-proprietorship comment at socket-server.ts:41 deserves its §5 entry. |
| F7.2 | `shared/dist/.dist-freshness-…-84cc09eabb25.json` vs `shared/**` — **executed verdict** | Claimed (F1.7, deliberately unresolved): the marker (sourceHash `a01c3f33…`, 12:13:43Z) vs the sources (16:29:35Z) — "freshness unresolved, not stale". Actual: **resolved, by the machinery's own functions** — `collectSourceFiles(pkg, root, 'default')` → 60 watched files, 0 missing; `hashSourceFiles(root, files)` → **`ec541613187c46d6de01a0aef0679d4905d8aaf4f29dfb30150127639e708b8f`** ≠ the marker's `a01c3f337ff18567…`. The committed dist+marker = **stale relative to the current sources** (the 16:29:35Z event = a *content-changing, .ts-only* rewrite — the .md/.json/.cjs files kept their 11:00-11:28 mtimes, so this was not a checkout touch). Combined with F1.6: **the dual-surface divergence is now proven, not hypothetical** — the production importers (runtime/cli via its installed symlink → the *worktree's* shared/dist) execute the 12:13Z artifact while the test lane (the vitest source alias) exercises the 16:29Z sources: the tests are newer than production. | **P1** | Fix: rebuild + re-record the marker **in a provisioned checkout** (the machinery cannot do it here — see F7.3) and re-land the artifact, or make the 16:29-style sources-only sync regenerate the marker as part of the same step; today nothing can even see the gap. |
| F7.3 | `runtime/cli/lib/dist-freshness.cjs` (972 lines) + its invocation surface | Claimed: the freshness guard. Actual: a **multi-package policy engine** (DIST_PACKAGES: the shared package + `system-spec-kit/runtime` + the advisor-CLI's entry files; per-package sourceCandidates, `excludedSegments: ['tests','test-fixtures','fixtures']`, `entrySourceCandidates` like `is-phase-parent`, per-entry distItems + rebuildCommands), enforced at 8+ sites (`bin/skill-advisor.cjs:24`, `runtime/scripts/finalize-dist.mjs`, `hooks/pi/session-start-advisories.ts`, `cli/spec/validate.sh`, `sk-code/sk-code-quality/scripts/check-dist-staleness.sh`, 3 test lanes) — and its verdict **for the flagship package in this checkout is "unprovisioned"**: `checkPackageFreshness('system-spec-kit/shared')` → `status: "unprovisioned"`, `stale: false`, "missing package.json or node_modules" — the shared root has no `node_modules` of its own (it is consumed *through other packages' symlinked installs*, never installed as a root), so the watcher cannot assess or rebuild it. The launcher special-cases nothing (0 hits for "unprovisioned" in `bin/skill-advisor.cjs`) — the flag that distinguishes "verified fresh" from "could not check" reaches the gate and is silently folded into *pass*. Of the engine's three observation modes (fresh / stale / unprovisioned), the primary post reports the third, and the only consumer that reads it treats it as the first. | **P1** | Fix: provision the shared root (one `npm install` — its 3 deps are listed) so the 972-line engine can watch what it was built to watch, or teach the gate to *say* "unverified" instead of passing; a 69%-of-nothing guard is worse than none, because it *looks* enforced. |
| F7.4 | `shared/tsconfig.json` (`"include": ["**/*.ts"]`, no test exclusion) + `shared/dist/` (64 entries) | Claimed: `build` → the published surface. Actual: the build compiles its **tests** — no `*.test.ts` exclusion — so of the 64 committed dist entries, ~44 (≈69%) are the 11 test files' 4-fold outputs (`js` + `d.ts` + 2×`map`, e.g. `dist/config.test.{js,d.ts,js.map,d.ts.map}`), importable via the `'./*' → './dist/*'` wildcard and imported by **nobody** (the tests themselves run from TS via `tsx` — the compiled copies are pure dead weight in the served artifact; the remainder of dist = ~33 implementation outputs + their maps/d.ts + the marker + tsbuildinfo). | P2 | Fix: exclude `**/*.test.ts` from the build (the test lane never needed the compilation); ~44 of 64 committed artifact files disappear. |
| F7.5 | `shared/package.json:20` (`"test:task-enrichment": "echo 'No task-enrichment tests in @spec-kit/shared'"`) | Claimed: the uniform test-interface's shared-package stub. Actual: **zero invocation references** across `.opencode` (skills, plugins, commands, bin) — nobody calls the uniform name; the uniform interface it stubs for is invoked by no aggregate lane either (the workspace root's own `package.json` mentions neither `task-enrichment`, `tsx`, `workspaces`, nor `shared`). Within the charter's five-tree scope, the shared package's 11-suite test lane therefore has **no invocation reference at all** — its only door (`npm test` in the package) is one nobody walks through. (Caveat kept honest: `.github/workflows/*` — including the isolation check that guards the F5.5 doctrine — sits *outside* the audited trees and is the one unexcluded candidate.) | P2 | Fix: wire the shared test lane into whatever aggregate actually runs (or the documented validation command), and then the no-op stub earns its existence; as committed, both the lane and its stub are Ceremony. |

**Ruled out this iteration:** "the 16:29:35Z population was a worktree-checkout artifact" — a checkout touches all tracked files; the .md/.json/.cjs kept 11:00-11:28 mtimes, so the event was .ts-sources-only *and* content-changing (the executed hash mismatch, F7.2). Also ruled out: "the launcher treats unprovisioned specially" (0 hits — it folds; the finding stands *by* the absence).

## Sources Consulted

- `runtime/cli/lib/dist-freshness.cjs` (972 lines: SOURCE_EXTENSIONS:15, the DIST_PACKAGES spec:26-141, collectSourceFiles:217, the hash core:266-273, hashFile:277, checkPackageFreshness:629+, module.exports:955) — executed read-only: `collectSourceFiles` + `hashSourceFiles` (60 files, 0 missing, `ec541613…`) and `checkPackageFreshness('system-spec-kit/shared')` (status: unprovisioned, stale: false)
- `shared/package.json:20,26-28` (the 3 deps, the 0 devDeps, the no-op script); the import census: `js-yaml` (1: parse-frontmatter.ts:12), `@modelcontextprotocol/sdk` (1: socket-server.ts:14 + its :41 doctrine comment), `@huggingface/transformers` (0 — comments only: auto-select.ts:489, registry.ts:81, types.ts:16-18)
- `shared/tsconfig.json` (include: `**/*.ts`, no test exclusion) + the dist census (64 entries; the config.test 4-fold sample); `system-deep-loop/runtime/package.json` (tsx 4.22.4 — the tsx's only declaration)
- Invocation censuses: `test:task-enrichment` (0 in .opencode), the workspace-root `package.json` (no workspaces/tsx/task-enrichment/shared), "unprovisioned" in `bin/skill-advisor.cjs` (0 — the silent fold)

## Assessment

- newInfoRatio: 0.9 — the headline (F7.2's executed staleness proof + F7.3's unprovisioned-pass-through) resolves the packet's oldest open question with machine evidence; the dependency-trio verdict (F7.1) and the shipped-tests accounting (F7.4) are new.
- Novelty justification: F7.2/F7.3 upgrade F1.7 from "unresolved, conditional" to "proven stale + the guard structurally blind at its primary post"; F7.1's 0-importer heaviest dep and F7.4's 69%-test-artifact accounting are first-recorded.
- Confidence: high for F7.2 (the machinery's own exported functions, executed; the inputs deterministic); high for F7.1/F7.4/F7.5 (censuses); high for F7.3's unprovisioned verdict (the engine's own output) with the noted .github caveat.

## Reflection

- Worked: executing the enforcement machinery's *exported pure functions* instead of hand-replicating a 972-line policy — `checkPackageFreshness`'s own output (unprovisioned) and `hashSourceFiles`'s own verdict (ec541613 ≠ a01c3f33) are the ground truth by construction; both call sites stayed read-only.
- Failed: two signature stumble-throughs before the (pkg, root, entry) → {files} → hashSourceFiles(root, files) chain was right; the sed +1p multi-line BSD quirk cost a retry.
- Ruled out: see above.

## Recommended Next Focus

Iteration 8: the test-coverage census vs the coverage floor — per module: which test suites cover it (shared-own: 11; consumer-side: runtime/tests 16 + cli/tests 3 + skill-advisor 6 + deep-loop 3), which modules have NO test at all (the F7.5 lane + gate-3-classifier? compact-merger? budget-allocator? — the F1.9/F3.1 islands' tests = their ONLY exercise), which suites are fossils of retired surfaces (the memory-DB, the monolith's behavioral tests), and which tests are the ONLY keepers of their modules (the F7.5 ceremony); plus the 11-vs-module coverage matrix against the "happy path + one edge per public surface" floor.
