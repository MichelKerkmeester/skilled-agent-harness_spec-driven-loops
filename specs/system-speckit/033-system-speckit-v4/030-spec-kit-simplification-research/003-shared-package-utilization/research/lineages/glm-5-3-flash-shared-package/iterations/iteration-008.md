# Iteration 8: The test-coverage census — 33 surfaces × 12 shared-own + consumer-side suites vs the coverage floor

## Focus

Per-module test coverage against the floor (happy path + one edge per public surface): which of the 33 implementation modules have shared-own suites, which have consumer-side suites, which have **none**; which suites are the only keepers of their (already-condemned) subjects; which suites are fossils of retired surfaces; and which cross-file contracts (the 2-spelling equivalences, the diverged copy, the allowlist, the duplicated constant) have **zero** tests watching them.

## Findings

| # | path:line | Claimed vs actual | Severity | Recommendation |
|---|-----------|-------------------|----------|----------------|
| F8.1 | the 33-module coverage matrix | Claimed (README §2 + the 11-suite "test" script, package.json:18): the package is tested. Actual: 12 shared-own suites (config, embeddings/{auto-select,profile,registry}, frontmatter/parse-frontmatter, parsing/{quality-extractors,secret-scrubber,spec-doc-health}, predicates/boolean-expr, ranking/matrix-math, ipc/socket-server, contracts/retrieval-trace) + ~20 consumer-side suites (runtime/tests ×16, cli/tests vitest ×3+, skill-advisor/mcp-server/tests ×6, deep-loop ×3). **Eight of 33 modules have no suite anywhere in the audited surface**: `utils/retry` (0 — F1.9's barrel-only orphan), `utils/jsonc-strip` (2 production importers — `core/config.ts:11`, `lib/content-filter.ts:11` — 0 tests), `utils/token-estimate` (1 importer, `core/tree-thinning.ts:79` — 0 tests), `workspace/repo-root.mjs` (**9 production require()s — the most-called untested module in the package** — plus its already-diverged copy, also untested, F6.2), and the three provider implementations `embeddings/providers/{voyage,openai,ollama}.ts` (protocol-bearing, 0 vitest suites — their only exercise is the unwired `.cjs` lane's `test-embeddings-factory.cjs` (F7.5) plus incidental advisor-integration). The pure-type modules (types.ts, embeddings/types, the dead compact-merger type family) are structural, floor-n/a. | **P1** | Fix: the cheapest high-value gap = the 9-caller `repo-root.mjs` (one behavioral suite: the sentinel, the outermost-hoist, the nested-`.opencode` leak its own header,1-22, describes) and the 3 providers' happy+error path; `jsonc-strip`/`token-estimate` = one edge each; `retry` follows its module's verdict. |
| F8.2 | the 7 keeper-suites | Claimed: the tests protect behavior. Actual: seven suites are the **only exercise their subject ever gets** — `parsing/quality-extractors.test.ts` (F5.3: 0 importers), `predicates/boolean-expr.test.ts` (F5.4: barrel-only), `ranking/matrix-math.test.ts` (F3.1: internal-only), `contracts/retrieval-trace.test.ts` (F4.1: 0 consumers), `cli/tests/test-ast-parser.js` (F6.3: the island's 1 importer), and the monolith trio `cli/tests/test-embeddings-behavioral.js` + `test-embeddings-factory.cjs` + `test-scripts-modules.js` (F2.1: the dead monolith's — themselves unwired, F7.5). These are not tests *of* the package; they are the package's acceptance fossils — green checks guarding uncalled code. | **P1** | Fix: when the acceptance decision lands (F3.1/F4.1/F5.3/F5.4/F6.3), the seven suites retire with their subjects — or the survivors are re-anchored to the *live* surfaces they clone. |
| F8.3 | the fossil suites: `runtime/tests/factory-auto-resolution.vitest.ts` + the monolith trio | Claimed: the factory's test coverage. Actual: `factory-auto-resolution.vitest.ts` exercises exactly the machinery F2.4 proved inert in production — the `runtime/database/*.sqlite` scan, the `vec_metadata`/`active_embedder_*` read, the persisted-pointer precedence, the vectors/-shard fallback — against a production path that the launcher's `MEMORY_DB_PATH` (launcher.cjs:286-303) always bypasses. It is the retired memory-DB machinery's acceptance suite, still green against a branch nothing reaches. Combined with the monolith trio (which tests the production-dead embeddings.ts *and* runs unwired), the fossils cluster = 4 suites guarding 2 dead codepaths. | **P1** | Fix: re-anchor the auto-resolution suite to the *live* resolution order (explicit→network-probe→hf-local) or mark it archived-acceptance in the README; the monolith trio dies with F2.1. |
| F8.4 | `shared/config.test.ts:31-40` + `runtime/tests/production-db-isolation.vitest.ts` | Claimed: two suites, two guards. Actual: **one invariant, asserted twice through two implementations** — "the database dir = `<skill-root>/runtime/database`, not under shared": config.test.ts *spawns a child* (deleting `SPEC_KIT_DB_DIR`/`SPECKIT_DB_DIR`/`MEMORY_DB_PATH` from its env — note it *clears* both spellings, proving neither the equivalence nor the precedence) and asserts the shared config.ts's `DB_UPDATED_FILE` lands at `<SKILL_ROOT>/runtime/database`; the production-db-isolation suite asserts the *same* invariant through `shared/paths.ts`'s derivation (the `ProductionDatabaseResolutionError`'s own F2.9-#2 machinery). The duplication the derivations (F2.9) exhibit is faithfully mirrored by their tests: 2 derivation paths, 2 suites, 1 invariant, 0 equivalence checks. | P2 | Merge: when F2.9 collapses the derivations, these two suites collapse into one; today they are the duplication's shadow — and, uniquely, its only guards. |
| F8.5 | the untested cross-file contracts (4) | Claimed: implicit — the conventions are "kept in sync". Actual: the package's *coupling* lives in four conventions with zero tests: (a) the 2-spelling equivalences (`SPEC_KIT_DB_DIR ≡ SPECKIT_DB_DIR` — config.ts:10 + profile.ts:274 + factory ×2; config.test.ts only *deletes* both, it never proves they agree); (b) the diverged-forever repo-root copy (F6.2 — `diff` differs, no drift test); (c) the launcher's `CHILD_ENV_ALLOWLIST` vs the readers (F6.1 — socket-server.ts:142 reads `SPECKIT_MAX_SECONDARY_CLIENTS` that the allowlist must remember, launcher.cjs:152); (d) the byte-identical hf-socket constant (F2.5 — hf-local.ts:40-42 "MUST stay byte-identical" to model-server-supervision.cjs, by comment). Every one of these is exactly the failure mode theCharter's "shared assumes layout it should not know" predicts, and none is mechanically watched. | **P1** | Fix: one parity-suite (4 assertions: the spellings, the two repo-roots' diff-identity *or* their documented divergence, the allowlist ⊇ the readers, the socket-constant equality) — the highest-coverage-per-line tests available to this package. |

**Ruled out this iteration:** "the memory-era gate tests are fossils" (F5.2 — they are the *live* gates' suites); "the 3 cloud/ollama providers are integration-tested" — within the audited surface their only direct exercise is the unwired `.cjs` (the advisor's suites exercise the *factory/adapter* seams, not the provider request paths); "config.test.ts guards the 2-spelling" — it deletes both names, it never compares them.

## Sources Consulted

- The 12 shared-own suites (the file tree) + the consumer-side suites (runtime/tests 16, cli/tests, skill-advisor/tests 6+, deep-loop 3) — per-module attribution of every suite to its subject(s)
- The 0-suite census: `retry`, `jsonc-strip`, `token-estimate`, `repo-root` (+ the diverged copy), `voyage/openai/ollama` (rg ×5; the Provider-class grep = test-embeddings-factory.cjs only)
- The keeper analysis: F5.3/F5.4/F3.1/F4.1/F6.3/F2.1 cross-referenced to their suites' import graphs
- `runtime/tests/factory-auto-resolution.vitest.ts` (the vec_metadata/scan subject) vs `launcher.cjs:286-303` (the always-set override) — the inert-branch
- `shared/config.test.ts:1-40` (full: the spawnSync, the 3-env deletion, the two assertions) vs `runtime/tests/production-db-isolation.vitest.ts` (the paths-side twin)
- The 4 conventions: config.ts:10 / profile.ts:274 / factory; the repo-root diff (F6.2); launcher.cjs:152 vs socket-server.ts:142; hf-local.ts:40-42 vs model-server-supervision.cjs

## Assessment

- newInfoRatio: 0.8 — the matrix, the 0-suite set (8 of 33, incl. the 9-caller 0-test repo-root), the keeper/fossil split, and the 4 unwatched cross-file contracts are new; the derivation-duplication's test-shadow (F8.4) extends F2.9.
- Novelty justification: F8.1's "the most-called module in the package has no tests" and F8.5's "every coupling = an unwatched convention" are first-recorded; F8.3 names the memory-DB acceptance fossils as a *set* for the first time.
- Confidence: high for F8.1-F8.4 (suite-to-subject attribution + the 0-hit censuses); high for F8.5 (absence-of-test = verified by the 4 greps); the .github caveat (F7.5) carries over to the invocation question.

## Reflection

- Worked: attributing every suite to its subject *before* counting — the keeper/fossil split fell out of the F3-F6 verdicts; the config.test read (it clears, not compares, the spellings) upgraded F6.6's evidence from "no equivalence test" (assumed) to "the closest test deletes both" (observed).
- Failed: the first Provider-coverage grep matched its own subject files; the repo-root "test" -l caught vitest configs and playbooks — filtered by test-file-ness.
- Ruled out: see above.

## Recommended Next Focus

Iteration 9: the second-order/live-chain consolidation + the memory-era residue sweep — every shared-export that *is* production-imported: does its importer's importer terminate in a runner (advisor handler, registered hook, the continuity writer, the plugins)? The residue checklist: `MEMORY_DB_PATH`/`SPEC_KIT_DB_DIR|SPECKIT_DB_DIR`/`vec_metadata`/`vec_memories_rowids`/`vectors/context-vectors__*`/`runtime/database`/`RESERVED_*`/`SPECKIT_ROLLOUT_PERCENT`/`MEMORY_SUFFICIENCY`-naming/`access-telemetry.json` (the retired-DB's path conventions) — each: who references it, is the reference live, what breaks if the convention were renamed today; plus the F1.6/F7.2 divergence's blast-radius (which production behaviors execute 12:13Z semantics while their tests assert 16:29Z).
