# Iteration 1: Package map + consumer census — export map vs actual usage, resolution wiring, first-pass alive/orphan classification

## Focus

`package.json` export map, `index.ts` barrel, and the complete first-order consumer census: every `@spec-kit/shared/...` import in the five consumer trees (system-spec-kit/runtime, system-skill-advisor, system-deep-loop, sk-doc, .opencode/bin), the resolution mechanism each uses, and a first-pass classification of all 33 implementation surfaces as externally-imported / internal-only / orphan. Memory-DB-era wiring noted; module-internal verdicts deferred to iterations 2-9.

## Findings

| # | path:line | Claimed vs actual | Severity | Recommendation |
|---|-----------|-------------------|----------|----------------|
| F1.1 | census, five trees (whole) | Claimed (README.md:22-23, 95): consumers = "CLI scripts (runtime/cli/)", "the spec-kit engine (runtime/)", "the skill advisor — the embedding stack's only consumer today". Actual: **78 distinct files issue 132 package-specifier import statements** (52 production, 26 tests). Production importers: runtime/cli 31, skill-advisor/mcp-server 10, sk-doc 2, runtime/core 1, runtime/hooks 3, runtime/lib 5; system-deep-loop = **test-only** (3 vitest files, zero production imports); `.opencode/bin` = **zero imports of any kind** — its only couplings are a relative require of the *runtime's* freshness checker (`bin/skill-advisor.cjs:24`) and a comment (`system-skill-advisor-launcher.cjs:288`). The charter's "five consumer trees" = 4 importing trees + 1 indirect. README consumer list (3 trees) omits deep-loop and sk-doc. | P2 | Document: correct the README consumer list; treat .opencode/bin as indirect-only and system-deep-loop as test-only. |
| F1.2 | `shared/package.json:6-13` (exports) | Claimed: 7 entry points (`.`/main, `./workspace/repo-root.mjs`, `./compact-merger`, `./budget-allocator`, `./embeddings`, `'./*.js'`, `'./*'`). Actual: the bare root `'@spec-kit/shared'` (`.` + `main`) has **zero importers**; the three named subpath entries `./compact-merger`, `./budget-allocator`, `./embeddings` are **redundant** — each resolves to the identical `dist/<name>.js` that the `./*` star already serves (their 5 importing files would be unaffected if only `'./*'` remained). The only non-redundant named entry is `./workspace/repo-root.mjs` (source `.mjs`, no dist twin), used 9× via `require()` — the **only** require()-style specifier observed in any consumer. | P2 | Fix: reduce the export map to `.` + `./workspace/repo-root.mjs` + the wildcard; delete `main` (exports wins, it shadows nothing today). |
| F1.3 | `shared/index.ts:5` vs usage | Claimed (index.ts:5-6): "Central export surface for all shared modules. Re-exports types, utilities, embeddings, scoring, and chunking." Actual: the barrel is imported by exactly **2 files** (`runtime/cli/lib/semantic-summarizer.ts:16` — one *type*; `runtime/tests/embedding-weighting.vitest.ts:3` — `buildWeightedDocumentText` + `WeightedDocumentSections`), i.e. **3 symbols of ~24 re-exported module families**. Nothing imports `chunking`, `budget-allocator`, `compact-merger`, `retry`, `boolean-expr`, `retrieval-trace`, `folder-scoring`, `learned-combiner`, `algorithms/*` *through the barrel*; those modules' real consumers all use deep paths. Meanwhile README.md:97 mandates "Consumers should import via `@spec-kit/shared/*` path alias" — the deep specifier — not this barrel. | **P1** (11.8 KB of ceremonial surface; every new shared export adds dead weight here) | Merge: shrink the barrel to the 2-3 symbols that actually cross it, or delete it and keep `types` + deep paths. |
| F1.4 | `runtime/hooks/lib/spec-gate/spec-gate-core.mjs:54`, `runtime/hooks/cursor/spec-gate-prebind.mjs:25`, skill-advisor `mcp-server/scripts/routing-accuracy/gate3-corpus-runner.mjs:7`, deep-loop `runtime/lib/deep-loop/artifact-root.cjs:17-19`, `bin/skill-advisor.cjs:24`, `runtime/vitest.config.ts:31-33` | Claimed (README.md:97-98): `@spec-kit/shared/*` alias import convention, stability guaranteed by the package boundary. Actual: production consumers reach the package through **four mechanisms**: (a) package specifier via the `'./*'→dist` wildcard (majority, 123 statements); (b) relative **dist** bypass — `'../../../../shared/dist/gate-3-classifier.js'` (spec-gate), `'../../../shared/dist/…'` (prebind), `'../../../../system-spec-kit/shared/dist/…'` (gate3-corpus-runner, cross-skill!); (c) relative **source** require — `artifact-root.cjs` requires `shared/review-research-paths.cjs` via `path.join(__dirname, …)`, a 16.7 KB CJS file with **no export-map entry at all**; (d) test-time **source** alias — vitest maps `'@spec-kit/shared'` to `../shared` (TS source), so tests exercise source while production runs `dist`. The export map is advisory, not an enforcement boundary; gate-3-classifier.ts (37.2 KB, the largest module) is imported by package specifier **zero** times. | **P1** | Fix: either widen the export map to the real seams (add `./review-research-paths.cjs`; import gate-3 via specifier) or document the four sanctioned mechanisms; today the map predicts nothing. |
| F1.5 | resolution wiring: `runtime/package.json` (`file:../shared`), `runtime/cli/package.json` (`file:../../shared`), skill-advisor `mcp-server/package.json` (`file:../../system-spec-kit/shared`), `system-deep-loop/package.json` + `runtime/package.json`, `sk-doc/package.json` (all `file:../system-spec-kit/shared`) | Claimed: five consumer trees each declare the dependency. Actual: installed `node_modules/@spec-kit/shared` symlinks exist in exactly **2 of 5** trees in this worktree (runtime/cli → 4×`../system-spec-kit/shared`; skill-advisor/mcp-server → 3×`../`). Empirically: `require.resolve` from `sk-doc/shared/scripts` → **MODULE_NOT_FOUND**; from deep-loop scripts → resolves to `/Users/…/MEGA/Development/Code_Environment/Public/.opencode/skills/system-spec-kit/shared/dist/…` — i.e. through the **parent checkout's** node_modules, outside this worktree. sk-doc's 2 importing scripts and deep-loop's 3 importing test files therefore depend on where Node's ancestor walk lands, not on their own declared dependency. | **P1** | Fix: install (or symlink) the dependency where it is declared, or document that resolution is delegated to the launcher/CI environment; today the wiring story differs per tree. |
| F1.6 | `runtime/vitest.config.ts:31-33` | Claimed: tests validate the package. Actual: the alias binds `'@spec-kit/shared'` to `../shared` — the **TypeScript source** — while every production import resolves `dist/`. Combined with F1.7, the test lane and the served artifact can diverge silently; nothing in the test config references `dist/`. | P2 | Document the dual surface, or make one smoke test import through the real specifier+dist. |
| F1.7 | `shared/dist/.dist-freshness-…-84cc09eabb25.json` vs `shared/**.ts` | Claimed: the marker+checker machinery guarantees dist tracks source (`finalize-dist.mjs` records; `bin/skill-advisor.cjs:24` re-checks at advisor launch with exit-69 stale protocol and two `*_DEV_ALLOW_STALE` spellings; 3 further test lanes). Actual, as committed: marker written 12:13:43Z (`origin:"checker"`, sourceHash `a01c3f33…`, distHash `71dd75b2…`) while **every** `shared/**.ts` carries mtime 16:29:35Z and `dist/gate-3-classifier.js` 11:29:31Z. Nothing has re-verified the marker against the current sources in this worktree. (Hash recompute deferred to iteration 7 — this ledger records it unresolved, not stale.) | **P1** (conditional) | Fix: recompute the source hash in iteration 7 and either refresh the marker or confirm the 16:29 event was content-neutral; if neither, the advisor's own launch gate is guarding a fossil. |
| F1.8 | `shared/README.md:64-67,124-131,267-268` | Claimed: "Key Statistics: Top-Level TS Modules 14 … code graph contracts"; "Verify Installation: Expected source files: … code-graph-contracts.ts"; "`runtime/lib/providers/embeddings.ts` → explicit named re-exports from `@spec-kit/shared/embeddings`". Actual: no `code-graph-contracts.ts` exists — the contracts directory holds `retrieval-trace.ts` (the 14th "module" is phantom, so the module count is 13); the documented `runtime/lib/providers/embeddings.ts` shim was **not** among the 78 importing files and its existence is not yet confirmed (checked in iteration 2). The README's own verification checklist, run as written, reports a missing file. | P2 | Document: fix the expected-file list, the count, and either restore or stop advertising `runtime/lib/providers/embeddings.ts`. |
| F1.9 | first-pass classification, 33 surfaces | Externally imported (≥1 production import): frontmatter/parse-frontmatter (20+), unicode-normalization (6), utils/path-security (5), trigger-extractor (5), parsing/memory-sufficiency (5), embeddings/factory (7), scoring/folder-scoring (4), algorithms/rrf-fusion (7), lib/structure-aware-chunker (3), compact-merger (1 prod+2 tests), utils/jsonc-strip (2), parsing/spec-doc-health (2), ipc/socket-server (2, both = the skill-advisor shim), embeddings/registry (1+test), embeddings/auto-select (1+test), embeddings/types (2, shim+test), embeddings/adapter (1, shim), embeddings/adapters/ollama (1, shim), embeddings/providers/hf-local (1, test), embeddings barrel (1, re-export shim), context-types (1), config (1), paths (1), types (2, both type-only), parsing/secret-scrubber (1), parsing/memory-template-contract (1). No package-specifier importer anywhere: **contracts/retrieval-trace** (self-test + barrel), **predicates/boolean-expr** (self-test + barrel), **parsing/quality-extractors** (self-test only — not even in the barrel), **utils/retry** (397 lines, barrel-only, **no test**), **ranking/matrix-math** (internal-only via learned-combiner + self-test), **chunking** (barrel-only; one unresolved internal edge, see F1.10), **embeddings/profile** (internal: factory + barrel). Second-order liveness (does the importer itself run?) = iterations 2-9; these verdicts are first-order only. | **P1** (the clustering: ≥4 of 33 surfaces likely unconsumed in production) | Remove/merge decisions wait for second-order confirmation; this row fixes the census baseline. |
| F1.10 | `shared/chunking.ts` importers | Claimed (README.md:65): top-level module. Actual: one barrel import (`index.ts:20`) + one unresolved internal edge (`from './chunking.js'` at an unnamed file, line 156) — attribution pending; if that edge is the barrel's own multiline import, `chunking.ts` has **zero** non-barrel importers despite owning `PriorityPatterns/PriorityBuckets` (types imported by nothing else). | P2 (pending) | Resolve the edge attribution in iteration 2 before any verdict. |

**Ruled out this iteration:** none yet — establishing iteration (only positive identifications; no approach retired). Note: mirror trees (`.pi/skills`, `.claude/agents/…`) excluded by charter (the five consumer trees only); no reading of `dist/`-internal deltas.

## Sources Consulted

- `shared/package.json`, `shared/index.ts` (imports 1-80 + import-block lines 50-300), `shared/README.md` (head + §1/§1B/§2/§3/§5 anchors)
- Full specifier census: 132 import statements / 78 files across the five trees (rg, dedup, per-file)
- require() census: 9× `'@spec-kit/shared/workspace/repo-root.mjs'`; relative-bypass reads of spec-gate-core.mjs:54, spec-gate-prebind.mjs:25, gate3-corpus-runner.mjs:7, artifact-root.cjs:17-19, bin/skill-advisor.cjs:24
- Wiring: 5× consumer package.json dependency blocks; node_modules/@spec-kit symlink survey; empirical `node -e require.resolve` ×2 (read-only)
- `runtime/vitest.config.ts` (alias), `runtime/scripts/finalize-dist.mjs` (head), `shared/dist/.dist-freshness-*.json`, mtimes of `shared/**.ts` vs `dist/gate-3-classifier.js`

## Assessment

- newInfoRatio: 1.0 — first pass over this angle; the census, the four resolution mechanisms, the 2-of-5 wiring, and the barrel's 3-symbol reality were all previously unrecorded in this packet.
- Novelty justification: every row is first-recorded; F1.3/F1.4/F1.5 overturn the README's own consumption story rather than merely extending it.
- Confidence: high for F1.1-F1.6, F1.8-F1.9 (direct census evidence); medium for F1.5's mechanism explanation (the MEGA-path observation is verified; the exact ancestor-walk mechanics are inferred); F1.7 deliberately unresolved.

## Reflection

- Worked: rooting every census grep at the five tree paths (the `.opencode`-rooted variant silently misses symlinked subtrees); running the two `require.resolve` probes converted the wiring question from assumption to observation.
- Failed: the internal import-edge attribution initially printed line numbers without filenames — recovered by re-running per-file; the worktree/parent-checkout topology cost several probes without full explanation.
- Ruled out: n/a (establishment iteration).

## Recommended Next Focus

Iteration 2: the embeddings subsystem — `embeddings.ts` (1,202-line monolith) vs `embeddings/*` (factory, registry, profile, auto-select, adapter+adapters/ollama, 4 providers, types); the skill-advisor shim layer (`mcp-server/lib/embedders/*`), the launcher's MEMORY_DB_PATH→factory coupling, `bin/hf-model-server.cjs` (does it import shared at all?), the `vec_metadata`/`active_embedder_*` convention (who writes it today?), the unresolved F1.10 chunking edge, and which of `generateEmbedding`-family functions have any live caller.
