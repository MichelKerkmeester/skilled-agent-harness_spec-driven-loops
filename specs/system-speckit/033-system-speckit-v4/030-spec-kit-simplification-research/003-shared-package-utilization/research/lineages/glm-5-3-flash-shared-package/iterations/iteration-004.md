# Iteration 4: contracts/ + context-types.ts + types.ts + gate-3-classifier.ts — what crosses the boundary vs what stays internal

## Focus

Which `contracts/` + `context-types.ts` + `types.ts` symbols actually cross the package boundary (named-symbol extraction per importer) versus stay internal or dead; `gate-3-classifier.ts`'s true consumer set (the hook/plugin invokers vs its 27-export surface) and its runtime-layout assumptions; the duplicated `DegradedModeContract`/`SharedPayload*`-style contract divergence; whether the "single source of truth" header claims survive their own call graphs.

## Findings

| # | path:line | Claimed vs actual | Severity | Recommendation |
|---|-----------|-------------------|----------|----------------|
| F4.1 | `shared/contracts/retrieval-trace.ts:14-54+` (whole, 6 exported types) | Claimed (module header): "Typed contracts for the retrieval pipeline that enforce end-to-end type safety with trace stages and degraded-mode handling. C136-08: Foundation for observability and pipeline instrumentation." Actual: **zero consumers anywhere** — all six symbols (`RetrievalStage`, `TraceEntry`, `RetrievalTrace`, `EnvelopeMetadata`, `ContextEnvelope`, `DegradedModeContract`) are referenced only by the barrel's re-export (`index.ts:205-212`), whose own comment (index.ts:208-211) documents the second duplication: `DegradedModeContract` is "already exported by ./algorithms/adaptive-fusion" while "the retrieval-trace variant adds degradedStages" — two definitions of the same contract, one variant-bearing, neither imported. The observability/pipeline instrumentation it was the foundation for (C136-08) is the same unwired pipeline as F3.1's quartet (candidate→filter→fusion→rerank→stages: `RetrievalStage:14-22` is literally that quartet's API). | **P1** | Remove: the module + its test + the barrel's 8-line re-export, together with the F3.1 decision; if the pipeline is ever built, the contract resurrects from this packet. |
| F4.2 | `shared/context-types.ts:5-6,11` (whole, 50 lines) | Claimed: "Single source of truth for context type definitions, canonical values, and legacy alias mappings. Used across shared/, runtime/, and runtime/cli/." Actual: one specifier-importer — `runtime/cli/lib/frontmatter-migration.ts` (which consumes `CANONICAL_CONTEXT_TYPES` + `LEGACY_CONTEXT_TYPE_ALIASES`), itself live (7 referencers incl. the retrieval corpus's own `retrieval/lib/frontmatter.mjs` and the continuity backfill/migrate CLIs). But 2 of the 4 value-exports (`resolveCanonicalContextType`, `isLegacyContextType`) and the `CanonicalContextType` type have **zero callers** in the five trees; the module's stated reason to exist — "legacy aliases still in the DB" (:11) — points at the retired memory database, while today's consumers are the save-time frontmatter migration/backfill paths. No other module redefines the 4 canonical values (verified: `'planning'` appears in exactly one runtime cli/lib file — the importer). | P2 | Fix: trim to the 2 consumed value-exports (+ the 2 types, if kept); update the header to name the current owner (frontmatter migration), retiring the DB reference. |
| F4.3 | `shared/gate-3-classifier.ts` (908 lines, 27 exports) | Claimed: the charter's machine contract — "owns the exact vocabulary and is authoritative for runtimes that call it". Actual: **the most-live module in the package** — invoked by the pi plugin (`.opencode/plugins/system-spec-gate.js`, which also reads `SYSTEM_SPEC_GATE_ENFORCE`, matching the root-doc's exemption) and the four runtime hook adapters (`hooks/pi/spec-gate-classify.ts` + `spec-gate-enforce.ts`, `hooks/codex/spec-gate-classify.mjs`, `hooks/cursor/spec-gate-prebind.mjs`, `hooks/claude` via `lib/spec-gate/spec-gate-core.mjs:54`), plus the advisor's `lib/policy-plan.ts` — and yet its **consumed surface is 3-4 of 27 exports** (`classifyPrompt`, `validateSpecFolderBinding`, + `normalizePrompt` by one test). The workspace-root machinery inside it — `SPEC_ROOTS` (:125-137), `findWorkspaceRoot`/`getSpecRoots`/`collectSpecFolderCandidates` (:348-418) — has **zero importers**: the advisor uses its own `findAdvisorWorkspaceRoot`/`findWorkspaceRoot` (mcp-server/lib/…), and the runtime's stated SSOT (`cli/core/spec-root-registry.ts:92-94`) merely *cites* these symbols by file:line in a provenance table without importing them. The package's 5th root-resolver (after config.ts, paths.ts, factory.ts, workspace/repo-root.mjs) is uncited except by documentation. | **P1** | Merge: make `spec-root-registry.ts` import the machinery it cites (turning the provenance table into a dependency), or extract the root machinery out of the classifier; either way one root-resolver story for five. |
| F4.4 | `shared/gate-3-classifier.ts:507-512` vs `runtime/lib/graph/access-telemetry.ts:51` | Claimed: shared is layout-neutral (the enforced GAP A: shared/ must not import runtime/ — enforced by `cli/evals/check-architecture-boundaries.ts:7-13`). Actual: the classifier **re-derives the runtime's telemetry-store location and filename by convention** — `readJsonRecord(path.join(path.dirname(DB_UPDATED_FILE), 'access-telemetry.json'))` (:509, the literal store name) — while the store's owner derives the same path independently (`access-telemetry.ts:51` `path.join(path.dirname(DB_UPDATED_FILE), STORE_FILENAME)`). Two derivations of one path, connected only by a duplicated string; a renamed or relocated store silently desyncs the Gate-3 last-active-child lookup, and the classifier's correctness now assumes the *runtime's* graph store exists and is fresh. (This via `DB_UPDATED_FILE`, the second consumer of the shared/config derivation — see F2.9's five-derivation ledger.) | P2 | Fix: export the store path from one derivation (or inject it into the classification options — `ClassificationOptions` at :75 exists for exactly this) instead of assuming the file's name and location. |
| F4.5 | `shared/types.ts` (38 type-only exports) | Claimed (README.md:65-66, index.ts:8-56): the shared type foundation. Actual: the boundary-crossing subset is ~10 of 38 — `ExtractionResult`+`ScoredNgram` (the two type-only importers, `semantic-signal-extractor.ts:28` + `cli/lib/trigger-extractor.ts:16`) and the provider/factory contract family (`IEmbeddingProvider`, `ProviderResolution`, `CreateProviderOptions`, `ApiKeyValidationResult`, `ProviderInfo`, `ProviderMetadata`, `EmbeddingProfile*`…). The remainder is shared-internal plumbing or dies with its already-condemned owner: the retry quartet (`RetryConfig`/`ErrorClassification`/`RetryOptions`/`RetryAttemptLogEntry`:157-183) — the contract of `utils/retry.ts`, which nothing imports (F1.9); the monolith-adjacent stats/`UsageStats`/`EmbeddingCacheStats`/`LazyLoadingStats`/`BatchEmbeddingOptions`/`ModelDimensions` (:80-150); the folder-scoring quintet + `RankingMode`/`TierWeights` (:198-244); `PriorityPatterns`/`PriorityBuckets` (chunking's, :251-257); `TaskPrefixMap`/`TaskType` (:379-387, the dead task-prefix machinery's vocabulary). Also inverted for a contracts module: `types.ts:56` imports `HfLocalDtype` *from a provider implementation* (`embeddings/providers/hf-local.js`) — the types module depends on one provider's internals. | P2 | Fix: no deletion beyond the owners' (F2.1/F3.1); when those die, retire their type rows; meanwhile move `HfLocalDtype` into embeddings/types.ts so the type layer stops importing provider internals. |
| F4.6 | `shared/contracts/` (directory) + `contracts/README.md` | Claimed: the package's "code graph contracts" directory (per the README's own stats — which call the file `code-graph-contracts.ts`, F1.8). Actual: one 60-line type-only module + its own test = the entire directory; no `index.ts`, no second contract, not even in the barrel's file set it claims to anchor. The directory exists to host one dead module (F4.1). | P2 | Merge: delete the directory with F4.1, or — if contracts/ is to survive as a concept — it needs a second, live inhabitant; today it is a directory-per-60-lines. |

**Ruled out this iteration:** "context-type semantics are duplicated in the runtime" — the 4 canonical values appear in exactly one runtime file (the importer); "the skill advisor consumes the shared gate-3 root machinery" — its `findWorkspaceRoot` hits are the advisor's own helpers; "the compiled contract yamls import the classifier" — they carry the vocabulary as *data* (the `CommandContract` the classifier validates), not imports.

## Sources Consulted

- `shared/contracts/retrieval-trace.ts:1-54+` (export surface), `shared/index.ts:205-212` (the duplication comment), symbol census ×6 (`RetrievalStage/TraceEntry/RetrievalTrace/EnvelopeMetadata/ContextEnvelope/DegradedModeContract` — barrel-only)
- `shared/context-types.ts:1-50` (full), `CANONICAL_CONTEXT_TYPES/LEGACY_CONTEXT_TYPE_ALIASES/CanonicalContextType/resolveCanonicalContextType/isLegacyContextType` caller censuses; `frontmatter-migration.ts` referencer census (7)
- `shared/gate-3-classifier.ts` (wc: 908/27 exports; import:16-17; store read:507-512; SPEC_ROOTS:125-137; binding types:49-93), invoker census (plugins/system-spec-gate.js + tests, policy-plan.ts, hooks/{pi,codex,cursor,claude}, adversarial-unicode test, gate3-corpus-runner), `SYSTEM_SPEC_GATE_ENFORCE` readers (plugin + projection + worktree-session.sh), `runtime/cli/core/spec-root-registry.ts:90-94` (provenance-table citation), the advisor's own workspace-root helper (skill-graph-db.ts:27)
- `shared/types.ts:13-387` (38 surfaces), its 2 importers' named-symbol extraction, the four dead-owner type families cross-referenced to F1.9/F2.1/F3.1/F3.6
- Enforcement context: `cli/evals/check-architecture-boundaries.ts:7-13` (GAP A/B), `cli/package.json` + `tests/architecture-boundary-enforcement.vitest.ts`

## Assessment

- newInfoRatio: 0.85 — the contracts/context-types/gate-3 verdicts are new; the provider-contract half of types.ts was anticipated by the census (shared-internal plumbing).
- Novelty justification: F4.1 (zero consumers, duplicated contract) and F4.3 (the most-live module whose cited machinery nobody imports) are first-recorded; F4.4's dual derivation of the telemetry-store path is a new charter "layout knowledge" hit; F4.2's DB reference = a direct memory-era residue confirmation.
- Confidence: high for F4.1-F4.4 (negative censuses were pattern-exhaustive over the five trees + shared-internal); high for F4.5's boundary count; F4.6 is bookkeeping.

## Reflection

- Worked: named-symbol extraction per importer (not just module-level) — it turned "imported" into "3 of 27 exports" for the package's most-live file; the barrel's own duplication comment (index.ts:208-211) did half of F4.1's work.
- Failed: the first symbol census missed `ContextEnvelope` (searched 4 of 6 symbols) — caught and re-run before writing; the advisor's `findWorkspaceRoot` hits initially looked like shared-machinery consumption until the advisor-local helper surfaced.
- Ruled out: see above.

## Recommended Next Focus

Iteration 5: parsing/ + frontmatter/ + predicates/ + unicode-normalization.ts + trigger-extractor.ts — the parsing一半 of the package: `parse-frontmatter` (20+ importers — the workhorse; its internal 3+1 dependents in parsing/*), `memory-sufficiency`/`memory-template-contract` (the memory-era quality-gate pair; who calls workflow.ts's validation TODAY), `quality-extractors` (self-test-only orphan), `secret-scrubber`/`spec-doc-health` (1 production importer each: workflow.ts — LIVE via the save flow?), `boolean-expr` (barrel-only; who evaluates predicates at all?), `unicode-normalization` (6 importers — the hooks' CANONICAL_FOLD_VERSION + the gate-3 canonicalFold + the shared-payload fingerprint — the versioned-fold CONTRACT: who bumps it?), `trigger-extractor` (5 importers: semantic-signal-extractor + cli/lib/trigger-extractor (a 2nd shim?) + golden/regression tests — TF-IDF v11: LIVE in which command?), and the memory-era residue census for the parsing tier.
