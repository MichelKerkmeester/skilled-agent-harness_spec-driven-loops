# Iteration 002 — Angle 2: rule value, registry rows 13–39, judged by family (run 2 of 5)

**Focus.** Registry rows 13–39 (27 rows) by the same method — header comments of the rule scripts only, ≤12 reads — grouped by family (graph-metadata, canonical-save, acceptance/metadata) and the family judged once.

**Key questions.** KQ-R3-2a: per family, do the guards divide one defect class redundantly or disjointly, and who tests them? — KQ-R3-2b: which family executions disagree with their written registrations?

**Row 13–39 identity:** 13 CROSS_ANCHOR_CONTAMINATION, 14 POST_SAVE_FINGERPRINT (both `ts:spec-doc-structure` — no physical script, covered by it.1 F3-03), 15 TOC_POLICY, 16 AI_PROTOCOLS, 17 COMPLEXITY_MATCH, 18 FOLDER_NAMING, 19 FRONTMATTER_VALID, 20 GREP_CONVENTION, 21 LEVEL_MATCH, 22 GRAPH_METADATA_PRESENT, 23 GRAPH_METADATA_CHILD_DRIFT, 24 GRAPH_METADATA_CHILD_IDENTITY, 25 GRAPH_METADATA_SHAPE, 26 METADATA_DISK_PATH_CONSISTENCY, 27 DESCRIPTION_SHAPE, 28 NORMALIZER_LINT, 29 SPEC_DOC_INTEGRITY, 30 TEMPLATE_SOURCE, 31–35 CANONICAL_SAVE_{ROOT_SPEC_REQUIRED, SOURCE_DOCS_REQUIRED, LINEAGE_REQUIRED, PACKET_IDENTITY_NORMALIZED, DESCRIPTION_GRAPH_FRESHNESS}, 36 CONTINUITY_FRESHNESS, 37 GENERATED_METADATA_INTEGRITY, 38 GENERATED_METADATA_DRIFT, 39 IMPROVEMENT_ARTIFACTS. Physical implementations read (headers only): 17 bash scripts + 3 `validation/*.ts` modules + LEVEL_MATCH's remainder + check-links.sh (the F2-11 standalone, read for the overlap probe). Rows 13–14: header evidence = their registry descriptions + the orchestrator's :946-948 pushes.

## Families, as the headers actually draw them

- **Graph-metadata family (rows 22–27, six guards):** two generated JSON files (graph-metadata.json, description.json) + the continuity ids, guarded by four bash scripts (check-graph-metadata 53L, -child-drift 168L, -child-identity 120L, -shape 125L, + check-metadata-disk-consistency 101L + check-description-shape 103L) and, since the orchestrator's native block, by :953-955. The 23/24 pair = complementary halves of ONE invariant (the 23's own header says a listed-entry-with-no-folder is "deliberately NOT reported, to avoid flagging drift no refresh would reconcile" — exactly what the 24 reports); 25/26/27 = shape/path/type guards; 37 (strict) = the schema superset.
- **Canonical-save family (rows 31–35):** ONE 53-line script + one helper .cjs, five registered error-rows, all five ids tested (4/3/4/2/2) — the census's F8-kept design, holding; its grandfathering windows carry dates 129 days past (F3-09).
- **Acceptance/metadata trio (36–38) + 39:** two of the three rows are executed IN-PROCESS by the orchestrator (:954-955) — their registered `validation/*.ts` bridges are spawned by nobody (F3-06); the third (CONTINUITY_FRESHNESS) is the ONLY row executed from compiled output, and that output is stale (F3-10). Row 39 = a conditional warn with no test.

## Findings (6 rows; ≤12)

| # | id | Claim side | Actual side | Claimed | Actual | Severity | Recommendation |
|---|----|-----------|-------------|---------|--------|----------|----------------|
| 1 | F3-05 | The registry rows and script headers as written (16× `rules/check-*.sh:4` "Sourced by validate.sh"; registry:176-195 rows 16–17; registry:216-225 row 22) | `runtime/cli/spec/validate.sh:46-302` + `runtime/lib/validation/orchestrator.ts:351,398-404,953,963-973` + `runtime/cli/rules/check-ai-protocols.sh:9,200,208` + `runtime/cli/rules/check-complexity.sh:9,123-194` | Each row/header describes how and what its rule does. | Four written claims trail the executed architecture: (a) 16+ bash headers say "Sourced by validate.sh" while validate.sh (:46-302) only prints help from the registry and invokes the ORCHESTRATOR (:297/:302), which *spawns* the scripts (:351) — the accurate modern wording exists one directory over (`check-links.sh:6-7`: "A standalone scan…has no validator-registry row, so validate.sh never sources it"); (b) row 16: the header's own `# Severity: warn` (:9) vs the registry's `error` (:176-185) vs the script's emitted statuses (`RULE_STATUS="fail"` :200, `"warn"` :208); (c) row 17: header warn (:9) vs registry error (:186-195) vs observed assignments pass/info (:123-194, tail unread); (d) row 22's executed implementation is a one-line existsSync on graph-metadata.json ONLY (:953, "Graph metadata checked") where the registry row promises BOTH "generated description and graph metadata" — the description.json half is nobody's in the :939-955 block. Nothing catches the drift because the verdict is entry-STATUS counts (:963-966) whose own comment says the severity tiers are "decorative" (:967-973): no behavioral test can ever catch documentation drift. | P2 | fix — regenerate the registry rows and the 16 script headers against the :398-404 architecture (rows 16/17: one agreed severity; row 22's description: what :953 actually checks); the three graph-metadata-era scripts' narrative headers are the pattern to copy. It.1's F3-01 (row 5) is the same defect class at a third row — one root, cited not re-claimed. |
| 2 | F3-06 | The registry's own spawn contract (rows 1,2,19,22,30,37,38 → physical scripts; "the registry's rows are the rule list" per the F2-10 fix) | `runtime/lib/validation/orchestrator.ts:938-959` (unconditional TS pushes, then `nativeRuleIds = new Set(entries.map((item) => item.rule))` and `entries.push(...selected, ...runRegistryShellRules(folder, level, nativeRuleIds, opts))`) + `:398-404` (`return !nativeRuleIds.has(rule.rule_id)`) | Seven registered rows' implementations are what their runs execute. | The native block answers those seven ids unconditionally (:939-955), the dispatcher then skips any row in that set (:404), so seven physical implementations — check-files.sh (76L), check-placeholders.sh, check-frontmatter.sh (180L), check-graph-metadata.sh (53L), check-template-source.sh (105L), validation/generated-metadata-integrity.ts (105L), validation/generated-metadata-drift.ts (108L), ≈700 lines — are never spawned in production. The two bridges have no other caller in the source tree (their own :34-40 usage banners are the only references); every one still carries the "Sourced by validate.sh" claim; their tests exercise the scripts directly or via ids (FILE_EXISTS×15, PLACEHOLDER×6, FRONTMATTER_VALID×4, TEMPLATE_SOURCE×70, GRAPH_METADATA_PRESENT×2, GENERATED_METADATA_INTEGRITY×3, + the drift-gate flag×7) — tested, maintained, unwired. And the in-process replacements can be narrower than the registrations they silence: FRONTMATTER_VALID-TS calls itself "continuity basics" (:901-902) vs the dormant 180-line "structure and required semantic values" rule; GRAPH_METADATA_PRESENT-TS is one existsSync vs the 53-line bash. | P1 | merge — retire the seven implementations (or consciously re-designate them as the TS side's fallback) and let the registry rows say which implementation runs; the F3-02 parity duty (placeholders) becomes maintenance of dead code the day nobody argues otherwise. |
| 3 | F3-07 | `runtime/cli/lib/validator-registry.json:255-265` (row 24: severity error, "Flags graph-metadata.json children_ids entries whose leading path identity is not the packet's own current identity (stale rename residue the refresh writer now prunes)") | `runtime/cli/rules/check-graph-metadata-child-identity.sh:5-9` vs `runtime/cli/rules/check-graph-metadata-child-drift.sh:6-9` + test-coverage counts | The children_ids freshness field is guarded, and this guard's absence of a test is incidental. | Rows 23 and 24 are two scripts (168L + 120L = 288 lines) over ONE field, deliberately split into complementary directions: 23 reports on-disk children the list is missing and, by its own header, does NOT report "a listed entry with no matching folder"; 24 reports exactly those stale-identity leftovers ("residue from an earlier name or location"). 23 is tested (3 occurrences); 24 has ZERO id-mentions across both tests trees — the angle's finding rule (overlapping sibling AND no test) fires. | P2 | merge — one children_ids consistency guard reporting both directions (missing-children + stale-identity), one test; or, if the warn/error asymmetry is deliberate, the test gap on the error-severity half still stands. |
| 4 | F3-08 | `runtime/cli/lib/validator-registry.json:288-298` (row 27, warn, "required name/description/level keys present and well-typed") | `runtime/cli/validation/generated-metadata-integrity.ts:6-11` ("through the shared schemas plus the path-prefix and status-enum invariants") + the executed in-process twin `orchestrator.ts:954` + zero test-tree mentions of DESCRIPTION_SHAPE | Row 27 is the default mode's description.json shape check. | The schema check that subsumes it (row 37: required keys + well-typedness is exactly what a schema asserts) is strict-only, and row 27 itself has no test; the default (non-strict) run's description-shape coverage therefore hangs entirely on the one untested guard. The angle's rule fires (overlapping sibling AND no test), but the mirror-image caveat is part of the finding: deleting 27 because 37 covers it removes 37's default-mode shadow, since 37 only runs under strict. | P2 | merge — fold the required-keys check into the shared-schema path as a non-strict facet, or keep 27 and name a test; either resolves the untested-single-guard state. |
| 5 | F3-09 | `runtime/cli/rules/check-canonical-save.sh:6-8` ("Temporary rollout allowlist expires at 2026-05-01T00:00:00Z; save_lineage enforcement becomes hard…on/after 2026-05-01T00:00:00Z") + the grandfather modes of rows 37/38 ("keeps violations non-blocking until the scoped migration graduates", `generated-metadata-integrity.ts:9-11`, `-drift.ts:8-9`) | Today is 2026-09-07 (lineage clock); the date is 129 days past; the test-side fossils: GENERATED_METADATA_GRANDFATHER_ENV×12, GENERATED_METADATA_GRANDFATHER×2, GENERATED_METADATA_Z_EXCLUSION×2, CANONICAL_SAVE_CUTOFF×2, CANONICAL_SAVE_STALE_MS×3, CANONICAL_SAVE_HEARTBEAT_MS×3 | A rollout window guards the graduation. | The windows expired 2026-05-01, four months before the census that judged these rows and 129 days before this lane; the grandfather/allowlist conditionals they describe are necessarily past their own dates, yet the dated scaffolding (the canonical-save windows, the integrity/drift "until graduation" modes, and at least six test-side constant families) is still the registered, tested, documented behavior. The census recorded OTHER grandfatherings (F23 optimizer adoption, F27/F2-15 attestation figures, F2-08's presence-rule cutoff); the validator-side 2026-05-01 windows appear in no census row. | P2 | fix — the graduation dates have passed, so the "until" conditions are dead in both directions: collapse each to its post-cutoff behavior and retire the expired-window test cases, or document the now-permanent state. |
| 6 | F3-10 | `runtime/cli/lib/validator-registry.json:367-375` (row 36: script_path `validation/continuity-freshness.ts`, strict_only, error) | `runtime/lib/validation/orchestrator.ts:78` (`VALIDATOR_DIST_VALIDATION_ROOT = …/runtime/cli/dist/validation`) + `:233-241` (the row resolves to the COMPILED `continuity-freshness.js`) + `:304` (spawnSync node) + `runtime/cli/dist/validation/continuity-freshness.js` (18,628B, built 09:09) vs `runtime/cli/validation/continuity-freshness.ts` (mtime 10:34) + `validate.sh:281` (`--entry validation-orchestrator`) | The freshness rule validates continuity freshness. | It is the only registered row executed from compiled output through the spawned-node path (rows 37/38 are answered in-process at :954-955), and its compiled artifact is 85 minutes older than its source — while the only freshness guard validate.sh installs is scoped `--entry validation-orchestrator`, i.e. it attests the OTHER dist tree (runtime/dist, marker 08:48), not the cli/dist the :233-241 resolution actually reads. The freshness machinery's own strict-only row is the one没有一个guard attests. (Both dist roots were listed at 12:07Z; the freshness-marker inventory — five runtime stamps plus the cli stamp — shows the machinery works where it looks.) | P2 | fix — have the :281 guard attest every compiled artifact the dispatcher resolves, or execute the row via the tsx loader the way the orchestrator's own fallback (:301-302) already does. |

## Family verdicts (the angle's unit of judgment)

- **graph-metadata (22–27):** five bash guards + one native existsSync + one strict schema, over two generated files and the continuity ids. The 23/24 pair is a deliberately bidirectional split of one field — good design, but only one direction has a test (F3-07); 27 is the untested, warn-only, default-mode residual of 37's strict schema (F3-08); 22's executed half is a one-liner that checks one of the two promised files (F3-05e). 25/26 are tested (3× each) and their headers divide scope cleanly (shape+pointers vs path/identity) — no finding.
- **canonical-save (31–35):** one 53-line script + helper, five registered error-rows, five-of-five ids tested — the F8-kept attribution design, vindicated; the only new value in the family is the expired 2026-05-01 windows (F3-09).
- **acceptance/metadata trio (36–38) + 39:** 37/38 are executed in-process (:954-955) while their registered bridge files are callerless (F3-06); 36 is the lone dist-spawned row and its artifact freshness is guarded by nobody (F3-10); 39 is a conditional warn, untested, no overlap — its zero-coverage state is recorded, not a finding under this angle's rule.

## Verified as correct (this angle's clean verdicts)

1. The dispatcher's narrowing contract: an unrecognized SPECKIT_RULES name is FATAL, not ignored — "a narrowed run that silently matches nothing reports a clean pass for a packet nobody checked, which is the one failure a gate must never have" (orchestrator.ts:406-412). The it.1-F3-04 lesson, learned and implemented in exactly one corner.
2. The SPECKIT_RULES × native-skip interaction is sound: the :956 set is built before the :957 selection filter, so a narrowed run still runs the TS implementation of any id it names and never double-runs one (the :957-filter selects the TS entry; :404's skip removes only the bash duplicate). Read, not inferred: :956-959.
3. The freshness guard's failure semantics: stale (rc 69) → exit 3; the guard itself failing → ALSO exit 3, with the stated reason "A freshness check that cannot run proves nothing about the build it guards" (validate.sh:283-298). Both halves fail closed.
4. Rows 16/17/18/20/21/28/30/13/14: header ↔ registry agreement on severity AND description (the only disagreements are F3-05's rows); GREP_CONVENTION's "Classification lives in the node helper because bash cannot parse YAML honestly" (:13-14) is the honest-divide pattern.
5. The canonical-save five: all five registered ids are named by tests (4/3/4/2/2) — the F8-kept design is not just attributed but exercised.
6. AI_PROTOCOLS is covered via its documented alias (test-validation-extended.sh:925 "Individual Rule: AI_PROTOCOL (check-ai-protocols.sh)"); the alias→id selection channel (orchestrator.ts:426-428) is therefore exercised end-to-end.
7. The LEVEL_MATCH helper (`utils/template-structure.js`, 31,681B) is hand-written ESM source ("'use strict'; import fs…"), not a stale build product — the F3-04-adjacent suspicion dies for this file.
8. The continuity-freshness module declares its rule id as a type+literal ('CONTINUITY_FRESHNESS' at :31, :214, :230, :245 of continuity-freshness.ts) — the row/report attribution chain is not lost on the spawned path.

## New information / novelty

newInfoRatio 0.7 — round two censused the completion chain and the catalog, not the dispatcher: the seven-implementation dormant set with its ~107 test references (F3-06), the executed-vs-registered documentation drift and itsDecorative-tier explanation (F3-05), the 23/24 and 27/37 pairings against their test coverage (F3-07/F3-08), the expired 2026-05-01 validator epoch (F3-09), and the unguarded freshness of the freshness row itself (F3-10) were all unrecorded.

## Ruled out

- "check-links (the F2-11 standalone) duplicates SPEC_DOC_INTEGRITY" — its own header limits it to wikilinks across skill markdown (:6-7); row 29's class is inline-markdown references; different classes, so 29's zero test-mentions stays analysis, not a finding.
- "The seven dormant scripts are untested residue" — the .sh harness exercises them by name (category banners at test-validation-extended.sh:891, :925); they are unwired, not rotten (folded into F3-06's cost column).
- "ROW 22's existsSync check also covers description.json" — the :953 call names only graph-metadata.json; no other :938-955 push references description.json existence; recorded as F3-05(e) rather than a separate row.
- "The duplicated GREP_CONVENTION class" — its node-helper rationale is documented in its own header (:13-14, "bash cannot parse YAML honestly"); one classification, one home; no duplication.

## Open questions

1. Does `validate.sh` source any `rules/check-*.sh` beyond :302? (Carried from it.1; three greps now agree the :46-302 mechanism is registry-print + orchestrator invocation, and `check-links.sh:6-7` attests no sourcing — the residue is the head-truncation caveat only.close it.1-OQ-1 as 95%-settled; the 5% = unread lines.)
2. Does COMPLEXITY_MATCH's body tail (:195-207) ever emit warn/fail? The five assignments observed (:123-194) are pass/info only; the head-5 grep truncated the remainder — which of its two written severities is behaviorally true is undecided (F3-05c's caveat).
3. What changed in `validation/continuity-freshness.ts` at 10:34Z (the same minute touched `utils/template-structure.js`) — source-newer-than-compiled is proven by mtimes; whether the 09:09 build already contains the 10:34 semantics is undecidable without the freshness machinery this lane may not run.
4. Whether the help printer (:47-61 `node -e`) prints severities or flags — it.1-OQ-5, still unread; bears on F3-05's propagation surface.
5. Whether `validateGeneratedMetadataIntegrity/Drift` (:954-955) import the same `@spec-kit/runtime/api` checks the bridges wrap (the bridges' :10-11 import) — if yes, the dormant bridges are pure protocol shims; if no, there are THREE implementations of the same checks. Definitions unread (beyond the call-sites).
