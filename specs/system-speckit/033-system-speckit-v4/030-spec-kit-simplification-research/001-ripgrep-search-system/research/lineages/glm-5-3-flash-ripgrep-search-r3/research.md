# Research Synthesis: Ripgrep Search System — Round-Three Angle Audit

**Lineage:** `glm-5-3-flash-ripgrep-search-r3` · **Session:** `fanout-glm-5-3-flash-ripgrep-search-r3-1788773746570-6hn9ct` · **Loop:** research · **Executor:** cli-pi (glm-5.3-flash), inline (no nested dispatch)
**Spec folder:** `specs/system-speckit/035-spec-kit-simplification-research/001-ripgrep-search-system`
**Stop reason:** `maxIterationsReached` (5/5 — the charter mandates exactly 5, one fixed angle each, no early convergence; convergenceThreshold 3 = telemetry only; per-iteration newInfoRatio: 0.8, 0.9, 0.7, 0.7, 0.8)
**Angle order (dispatch, exactly):** 1 Gate 1 instruction parity across runtimes · 2 the doctor retrieval target · 3 the memory-residue sweep and its allowlist · 4 the two boundary tables · 5 trigger-phrase authoring guidance versus the judge
**Non-goals honored:** no edits to any repo file; no database/vector redesign; no prose-style review; no census re-reporting without new evidence. All operations were read-only or wrote only inside this lineage directory. The 013/006 remediation packets resolved (iteration 3) to phase-children of 035: `specs/system-speckit/035-spec-kit-simplification-research/{006-retrieval-drift-remediation,013-trigger-phrase-quality-enforcement}` — the dispatch's track-rooted short forms were elisions.

---

## 1. Verdict

Round three read the five angles the first two rounds left thin, and the system held: **no new P0, four P1, fourteen P2**. The strongest defects sit where the youngest layer — 013's phrase judge and 006/013's doctor hardening — meets its readers: three of the doctor's nine declared staleness signals have no implementing activity while its outputs slots pretend otherwise (R3-2.2/2.3/2.4), the diagnostic's own probe invocation is a fifth undocumented recipe shape that omits the very flags its convention marks mandatory (R3-2.1), and the generation-side judge call silently divides the folder-token verdict from the validator's, against the judge module's own "so every enforcer keeps the same verdict" purpose clause (R3-5.1). The guidance layer (conventions section 8) answers the judge's seven classes with concept-words and no numbers, and misnames one producer. Everything the earlier rounds remediated held under receipt: the 9-word vocabulary is verbatim, the committed four-way manifestHash pair is internally consistent, the phraseQuality pollution signal is populated and read, 8/8 sampled allowlist exemptions still apply, and both boundary tables resolve — including the grandchild 002. The judge's verdict vocabulary is now the most precisely-quoted prose in the retrieval system; what remains is telling the humans the numbers.

## 2. The Ledger

18 rows: 0×P0, 4×P1, 14×P2. Full Claimed/Actual/Evidence for every row lives in `deltas/iter-001.jsonl…iter-005.jsonl`; the narratives in `iterations/iteration-001.md…iteration-005.md`.

### P1 rows (expanded)

| ID | Path:line | Claimed vs actual | Recommendation |
|----|-----------|-------------------|----------------|
| **R3-2.1 — The doctor's retrieval probe is a fifth undocumented recipe shape** | `.opencode/commands/doctor/assets/doctor-speckit-retrieval.yaml:157-158` + `_routes.yaml:42` | Claimed: "run the path-only recipe from retrieval-conventions.md section 2.2; the broken-roots re-run proves the wrapper." Actual: the invocation lacks `--hidden` and `!**/scratch/**` vs conventions:92-96; its `.git` glob is vacuous without `--hidden`; and no yaml or route line invokes `rg-wrapper.mjs` (conventions:315, README:89) although :158 claims the probe proves the wrapper — a fifth undocumented recipe shape (README:22: the only sanctioned hand-composed one is 2.4). Without `--hidden`, dotted-directory misses read as clean no-matches (conventions:76) — the exact failure class the probe exists to catch; 006 fixed the same defect class only in search.md. | fix — make the probe's invocation the section 2.2 recipe verbatim (or route it through the wrapper), or document the fifth shape. |
| **R3-2.2 — The fresh-manifest staleness signal is promise-only** | `doctor-speckit-retrieval.yaml:118-119` vs `:155-156,188` | Claimed: "manifest_hash_mismatch: the index's recorded manifestHash does not match a FRESH manifest over the same included paths (severity medium)." Actual: the only manifestHash comparison is :155's four-way committed-vs-committed (a different signal); the only .mjs invocation is :156's OPTIONAL measure-cold-lookup; no activity computes or reads a fresh manifest; outputs slot :188 cannot be populated as declared. | document — L6's 006 fix added the committed-pair check; the fresh-manifest clause remains promise-only (census-kinship disclosed, not a re-report). |
| **R3-2.3 — The ambient-config signal has no flagless second run** | `doctor-speckit-retrieval.yaml:129-131` vs `_routes.yaml:41-42` | Claimed: "ripgrep_ambient_config: a recipe run WITHOUT --no-config returns a different result set than the same recipe WITH it (severity HIGH)." Actual: the workflow runs the recipe ONCE with `--no-config` plus one broken-roots re-run (:158); no flagless second run and no result-set comparison exists in the yaml or the route; the signal exists to enforce conventions:222-233 (section 7) and its owner never executes it. | fix — add the flagless second invocation and the result-set comparison, or downgrade the signal's declared severity. |
| **R3-5.1 — The judge's seventh class never reaches the generation diagnostics** | `phrase-judge.mjs:3-7,96-109` + 013 `implementation-summary.md:55,57,61` vs `generate-trigger-index.mjs:244-246` + `grep-convention.mjs:719` | Claimed: the judge module's purpose clause — its re-exporting enforcers "keep the same verdict" — and 013's "One judge, three readers" (:55) with "phrases and owning documents **per class**" (:61). Actual: `generate:244` judges every unique key WITHOUT the folder context, so `judge:100`'s `context.folderTokens ?? []` is always empty at generation and folder-token phrases fall through to the `single-token` class (judge:104-109); the class string `folder-token-fallback` exists ONLY in `phrase-judge.mjs:99` and its two tests (`grep-convention.vitest.ts:465`, `grep-convention-rule.vitest.ts:135` — rule row `'900-folder-token-fallback' → 'generic-trigger' → 'warn'`), while the validator's per-document call passes the context (`grep-convention.mjs:719`). One phrase, two labels: `single-token` in the doctor's pollution signal, `folder-token-fallback` in the validator's warnings. | fix — the smallest repair is one sentence at the call site (or in 013's continuity) accounting for the divergence; the full fix (per-occurrence judging with folderTokens) collides with the unique-key design 013:61 actually shipped. |

### P2 rows (compact)

| ID | One-line shape | Rec |
|----|----------------|-----|
| R3-1.1 | `.codex/AGENTS.md:9-12` — four fenced sh instructions hard-code the absolute workstation `context.sh` path (4236 bytes; resolves on this machine only) | document |
| R3-1.2 | `AGENTS.md:83` — the entire retrieval instruction surface is the single root AGENTS.md (CLAUDE.md = its verified symlink); `.pi/AGENTS.md` and `.devin/AGENTS.md` are missing; zero lookup mentions in `REPO RULES.md`, `.codex/AGENTS.md`, `.cursor/rules/*`, `.pi/PLUGINS.md`, `.pi/SYNC.md`, `.devin/SYNC.md` | document |
| R3-2.4 | `yaml:135-137` — anchor_marker_drift (low) declared with outputs slot :192, but no phase-0/1 activity reads anchors; conventions:113+ (section 2.4) defines the pair grammar and mandates orphan reporting — the doctor is that diagnostician and never scans | fix (fold into the 5-sample packet reads, or document as telemetry-only) |
| R3-2.5 | `yaml:38` — lookup_exit_class "0 or 1 on the frozen prompt set", but no activity reads `fixtures/prompt-set.json`; the operational probe :154 is bound by `_routes.yaml:41` to ONE inline literal (`resume work session context`); the frozen five having no runtime reader (README:78) means their staleness changes nothing the doctor reports | document |
| R3-2.6 | `yaml:179` — "any indexed file newer than the index" over 13,722 indexed paths (11,871 specs/ + 1,851 .opencode/ occurrences), but no step extracts the index's paths[]; the nearest improvisation (`find -newer`) is a SUPERSET because conventions:269+ (section 9) keeps research/lineages, dist and tests/fixtures out of the index while the recipe roots carry them (receipt: 0 lineage paths indexed) | document |
| R3-2.7 | `yaml:155,174,252` — the four-way compare is REAL and passing (all four committed artifacts carry the equal manifestHash `b6953bc6…e9e0e7` — the 013 closeout = one consistent run), but no step defines what an absent/unreadable FIXTURE yields (STATUS=MISSING covers only the missing index) | document (a missing fixture is its own signal) |
| R3-3.1 | residue-allowlist: the retired engine (`mcp-server/`) is GONE from the skill tree while `runtime/ENV-REFERENCE.md` survived (4 sweep-term hits) — "lives in the engine tree; excluded with it" is half-stale; and `barter/` does not exist; the exemptions themselves still APPLY (receipt-verified) | fix (two one-line reason refreshes) |
| R3-3.2 | census N1 third clause + the 013 packet — 013's 8 documents mention the memory-residue sweep ZERO times (spec:78's "owner" = the 826-documents content decision; impl:53/goal:105's "sweep" = the repo-wide frontmatter sweep, a different mechanism); the 013-touched README:87 still bills the sweep as "a one-shot acceptance check" | fix (one ownership sentence in the README's section-4 sweep row, or amend the census's N1 row) |
| R3-4.1 | `conventions:33` — "the two share no mechanism" vs the document's own section 5 (the lanes SHARE normalization and ranking through `lib/normalize.mjs` + `lib/rg-lane.mjs`; the README's scoring-parity boundary row says so) — what they share none of is a RETRIEVAL mechanism | fix (one word) |
| R3-4.2 | `conventions:64-65` — the loss table's rows 5-6 double-book decay/access/dedup; row 6's cell repeats row 5's tail verbatim and exists only to carry two unique prose assets; the feature-catalog:36 loss sentence = the EXACT row-5 list, union-once — the cleaner form already exists in this repo; the 013-remediation touched the file and left the pair standing | merge |
| R3-5.2 | `conventions:252-253` — the Warn-On bullets carry no numbers: the 10-token prose flip (judge:41-46,85-92), the 37-word stop list (judge:27-37), the 120-char oversize variant (normalize:25; judge:41-46) | fix |
| R3-5.3 | `conventions:254` — "Body-derived fallbacks" vs the judge's BOTH-classes-are-the-editor's receipts (judge:17-22,50-62; conventions:257's own prose); neither class string appears anywhere in the conventions | fix (rename, or name the classes) |
| R3-5.4 | `spec-kit-docs.json` (the dispatched first nominee) = 0 trigger-phrase mentions; the field's in-repo examples = the four core `.tmpl` + `EXTENSION-GUIDE.md:4-8`, all judge-clean; no guidance prose; the only guidance = conventions section 8:241-261, referenced by nothing | document (one pointer line) |
| R3-5.5 | `sk-doc/.../examples.md:95,141` — sound prescription (all 10 of its own example phrases judge-clean), zero mention of the judge/section 8/any class; the bare-symbol case the prescription licenses is exactly `single-token`, 825 documents' disease (013:122) | document (one pointer line) |

## 3. What round three verified as correct

- **it1:** the lookup's interface truth — usage (:15), `parseArgs` (`--json`, bare `--` → literal prompt, value-flags, unknown-`--`→exit 2, no-prompt→exit 2), `loadIndex` = plain `readFileSync` of the 3,856,761-byte committed index, zero daemon/child-process imports: the instruction's "reads the committed index and needs no daemon" holds at source; the `CLAUDE.md → AGENTS.md` symlink (census §3's reason, re-verified, not re-reported); the nodeterm `context.sh` target exists (4236 bytes).
- **it2:** the committed four-way pair internally consistent (4× `b6953bc6…e9e0e7` — the check's inputs are real, V10's fix holds); the `phraseQuality` bucket populated (single-token 825, numeric-only 121, generic-workflow-word 171, prose-sentence 18, stop-word-only 12, editor-fallback 4 — the corpus_pollution signal has real data); `artifact.mjs:164` enforces `TRIGGER_INDEX_SCHEMA_VERSION` (the schema-mismatch signal's premise); `/doctor:update`'s ownership VERIFIED (`assets/doctor-update.yaml:369` invokes the generator; all 14 target/presentation assets exist); the `research/lineages` tree NOT indexed (0 lineage paths — the self-pollution hypothesis dies).
- **it3:** 8/8 sampled allowlist exemptions apply (existence + true residue recounted: 4/8/1/4 sweep-term hits in the four sampled "zero-hit" files — the 4-pattern probe misses only under the FULL term set, which the sweep's 41 tool-names + 8 literal-terms carry); the retired engine absent from the skill tree.
- **it4:** the relocation/loss coverage resolves everywhere — the bare `002` = `033/017/002-memory-consumer-rewire` (a grandchild; the three-002 ambiguity self-corrects via the :60 disambiguator one row above); the feature-catalog's three-part block (:28-36) = deliberate summary-here/full-there; its loss sentence = the verbatim §1-row-5 list.
- **it5:** the 9-word vocabulary verbatim (judge:11-14 ≡ conventions:251); the editor-fallback pair (judge:17-22 ≡ :257); `:252`'s "Stopword-only phrases" warning exists (the spaced spelling hid it from a hyphen-grep); :255+`:261` cover both judge count-classes and carry the landed L2-remediation clause; 26/26 inspected phrases across the three guidance surfaces survive the judge (2+ tokens under `normalize.mjs:62-69`'s non-ascii-alnum-to-space); the counting is class-dynamic (`generate:244-246`); the verdict reason strings carry the convention's own prose (judge:108 ≈ :261) — the judge→author closed loop 013:64 promised, at the verdict level.

## 4. Questions & open items

- **Answered (6, as recorded in the state):** q-gate1-parity (it1) · q-doctor-target (it2) · q-allowlist (it3) · q-boundary-tables (it4) · q-guidance-vs-judge (it5) · q-phrasequality-classes (it5).
- **Carried (5, each closed by its iteration's findings rather than by a verified-correct):** q-instruction-surfaces (it1 — the surfaces were enumerated; the gaps ARE the it1 rows) · q-check-integrity (it2 — 3 of 9 signals unimplemented = the it2 rows) · q-residue-sweep (it3 — the undocumented owner = R3-3.2) · q-cross-doc-consistency (it4 — the two precision defects = the it4 rows) · q-sk-doc-guidance (it5 — no warning reaches the author = R3-5.5).
- **Newly logged (3, it5's open questions, unanswered by design — recorded, not guessed):** (1) where the 120-character `oversized` variant (judge:41-46; `MAX_PHRASE_LENGTH`, normalize:25) actually lives, given the judge returns only the seven classes; (2) whether the committed phraseQuality ledger ships the `'ok'` bucket alongside the six negatives, and at what granularity the doctor's pollution read (it2's `yaml:141` receipt) sums; (3) the folder-token blast radius — how many of the 314 single-token keys (825 owning documents) would re-bucket under a context-ful judge.
- **Negative knowledge (14 approached-and-ruled-out, all evidenced in the deltas):** census-N7's Gate-5-not-Gate-1 design, the AGENTS/CLAUDE symlink, the four-way compare's self-reference, the phraseQuality read's self-reference, the 006/013 doctor fixes, the 8 specs-glob allowlist rows' redundancy, the fixtures' historical classification, the .pi/git foreign-tree cost, the feature-catalog's one-row relocation, the bare-002 ambiguity, the hardcoded-bucket-list theory, the EXTENSION-GUIDE single-token theory, the 5-of-7 Warn-On note, the templates'-phrases-violate-the-judge theory.

## 5. Convergence report (summary)

- **Stop reason:** `maxIterationsReached` (charter: exactly 5, one angle each, no early convergence; threshold 3 = telemetry only).
- **Iterations completed:** 5/5; all `status: complete`; zero timeouts/errors.
- **Question coverage:** 6 answered + 5 carried (closed-by-finding, noted above) + 3 newly logged (§4).
- **newInfoRatio trend:** 0.8 → 0.9 → 0.7 → 0.7 → 0.8 — not monotonic, and honestly so: every angle was virgin, so novelty did not decay the way a re-confirmation round's would.
- **Severity totals:** 0×P0, 4×P1, 14×P2 across 18 rows.

## 6. Method & evidence notes

- Every row carries `path:line` on the claim side and on the actual side; one-sentence claimed-vs-actual; severity; recommendation. Self-reported success (013's closeout, the README's ownership claims) was treated as unverified throughout; the code receipts (the context-less call, the dynamic counting, the validator's context) outranked the prose.
- Read-only: no repo file edited; no node/validate.sh/git run (documented deviation — the append gateway unavailable in this inline lineage; state/deltas/event records written directly, schemas mirroring the prior rounds' gateway output; the reducer-owned `findings-registry.json` and `deep-research-dashboard.md` intentionally left to the fanout runner).
- Tool budgets: each iteration's ledger is recorded in its own iteration file; all within the 12-call cap (it5: 10 of 12 — 6 evidence, 4 record writes; one evidence call lost at the compaction boundary and re-run). Counts were recounted, not reused, whenever a later pass leaned on them (the 13,722-path receipt in it2; the 26-phrase judge-clean sweep in it5).
- The census (`research/confirmed-findings.md`) was read once, in iteration 1, as ordered; the prior syntheses were consulted only where an angle sent this executor there.

## References

- Iteration narratives: `iterations/iteration-001.md` … `iteration-005.md` (this directory)
- Per-iteration structured deltas: `deltas/iter-001.jsonl` … `iter-005.jsonl`; state log: `deep-research-state.jsonl`; event log: `state-events.jsonl`
- This synthesis: `synthesis-record.json`, `convergence-report.json`
- Ground truth: `.opencode/skills/system-spec-kit/runtime/cli/retrieval/**` (`lib/phrase-judge.mjs`, `lib/normalize.mjs`, `lib/grep-convention.mjs`, `generate-trigger-index.mjs`, `sweep-memory-residue.mjs`, `fixtures/residue-allowlist.json`, `lookup-trigger-index.mjs`), `.opencode/commands/doctor/**` (`assets/doctor-speckit-retrieval.yaml`, `_routes.yaml`, `assets/doctor-update.yaml`), `.opencode/skills/system-spec-kit/references/retrieval/retrieval-conventions.md`, `.opencode/skills/system-spec-kit/feature-catalog/feature-catalog.md`, `.opencode/skills/system-spec-kit/templates/**`, `.opencode/skills/sk-doc/sk-create-feature-catalog/references/**`, the 013 packet, `AGENTS.md`/`CLAUDE.md`, and the round-1/2 syntheses + census under `research/`.
