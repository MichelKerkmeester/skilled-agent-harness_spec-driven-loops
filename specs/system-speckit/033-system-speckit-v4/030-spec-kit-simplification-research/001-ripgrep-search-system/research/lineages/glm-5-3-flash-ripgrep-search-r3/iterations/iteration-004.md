# Iteration 4 — the two boundary tables

- **Lineage:** glm-5-3-flash-ripgrep-search-r3 · **Angle 4 of 5** · TS receipts: 11:42:00Z / 11:45:57Z / 11:47:27Z / 11:48:23Z
- **Angle:** feature-catalog's boundary table vs retrieval-conventions §1, row by row — every loss and every relocation in BOTH, same destination — then the retrieval README's limits section against the same two.
- **Method:** 5 evidence calls (the feature-catalog locate-grep, the conventions claim-side anchors, the 033-children roster, the 002-destination resolution, the 028 cross-check) + 4 record writes = 9. The conventions §1 and the retrieval README were read in full in iterations 2-3 (mtimes receipt-013-fresh, receipt [I3]) — this iteration's evidence = the two remaining surfaces, disclosed here.

## The two accounts, side by side

| # | conventions §1 (claim) | feature-catalog :28-36 (the other account) | Same? | Same destination? |
|---|------------------------|--------------------------------------------|-------|-------------------|
| R | memory_search → §2.1 (structured JSONL) | :31 — ONE row, all three tools → "the ripgrep recipes in ...conventions.md §2, ranked caller-side per §5" | resolution of the same target | YES (§2 ⊇ §2.1/2.2/2.4; §5 = the ranker pair) |
| R | memory_quick_search → §2.2 (path-only) | (same row) | | YES |
| R | memory_context → §2.4 + the §5 caller-side ranker | (same row) | | YES |
| L | :60 — resume/context assembly → "Owned by phase 002-memory-consumer-rewire" | :36 — NOT listed; delegated ("§1 carries the full boundary table") | by delegation | YES (§1 = the full table; the 017/002 packet exists — receipt [B]) |
| L | :61 — continuity frontmatter writing → "Owned by phase 002" | :36 — NOT listed; delegated | by delegation | YES (the bare "002" = the :60-1-rows-above elision; receipt [B]: the packet = 033/017/002-memory-consumer-rewire) |
| L | :62-:63 — causal graph & drift; resource maps | :36 — NOT listed; delegated | by delegation | YES (§1's own devices: "named unsupported capability" / "a static generated path catalog") |
| L | :64 — semantic paraphrase, vector, BM25 fusion, decay, access tracking, session dedup → "Deliberate lexical-only loss" | :36 — THE EXACT :64 CAPABILITY LIST, verbatim, "a declared loss, not a relocated capability" | YES, verbatim | YES |
| L | :65 — decay, access tracking, session dedup → "Deliberate loss with no owner" | :36 — subsumed (the :64-list covers the trio) | SEE **R3-4.2** | — |

## Findings (2)

| ID | Claim (path:line) | Actual (path:line + receipt) | Severity | Recommendation |
|----|-------------------|------------------------------|----------|----------------|
| R3-4.1 | `retrieval-conventions.md:33` — "Retrieval splits into a keyed lane and a free-text lane, and the two **share no mechanism**." | The claim is contradicted by the document's OWN §5 and by the README's limits: conventions §5, ranking bullet 2 — "Normalized match class, in the order **lib/normalize.mjs** ranks them... The lookup emits these labels verbatim" (the shared rank primitive, inside this same document); README §5 Boundaries, row 2 — "**lookup-trigger-index.mjs and rg-wrapper.mjs share normalization and ranking through lib/normalize.mjs and lib/rg-lane.mjs** rather than each re-deriving it" (the explicit sharing, in the limits section this angle checks). The two lanes DO share a mechanism — the scoring/normalization one; what they share none of is a RETRIEVAL mechanism (index scan vs grep). :33 states the truth without its qualifier, one section before the document's own §5 proves the broader reading wrong | P2 | fix — one word: "the two share no *retrieval* mechanism" (the precision-is-the-product document; its §2 opening already demands "copy the flags rather than paraphrasing them") |
| R3-4.2 | `retrieval-conventions.md:64-:65` — the What-This-Does-NOT-Replace table's rows 5 and 6: :64 "Semantic paraphrase, vector and BM25 fusion, decay, access tracking and session dedup → Deliberate lexical-only loss..."; :65 "Decay, access tracking and session dedup → Deliberate loss with no owner..." | The canonical loss table double-books three capability terms: :65's list is a SUBSET of :64's (decay, access tracking, session dedup appear in BOTH rows), :65's cell repeats :64's cell tail VERBATIM ("Callers must behave explicitly on a no-hit rather than degrading to a guess"), and :65 exists only to carry two unique prose assets — "with no owner" and the stateful-store rationale ("All three were properties of a stateful store that observed its own reads. Every lane here is stateless"). Feature-catalog :36 proves the cleaner form already exists in this repo: ONE row, the exact :64 capability list, no duplication. Two rows = two places for the next editor to desync; the 013-remediation touched this file (receipt [I3]) and left the pair standing (no detriment claimed — 013's scope was phrase quality) | P2 | merge — one row: the :64 list + the no-owner/stateful-store rationale folded into its boundary cell; the no-hit-behavior clause said once |

## What the angle verified as correct (receipts, no rows)

1. **The relocation-destination chain resolves** — feature-catalog :31's coarse "§2, ranked caller-side per §5" points at the conventions' §2.1/§2.2/§2.4+§5 precision, and both ends exist (`## 5. RANKING CONTRACT` = the :194 anchor; §2 = the recipes section, read in it2). Coarse-here/precise-there = the intended pointer chain, and the pointer RESOLVES.
2. **The loss accounting = summary-here/full-there, exactly as designed** — feature-catalog :36 quotes the :64 capability list verbatim and delegates: "§1 carries the full boundary table." One pointer = one thing to keep in sync = drift-resistant. The delegation is honest: §1's :60-:65 = six rows, all reachable.
3. **The index-authority triangle is closed across four surfaces** — README §5 Boundaries row 1 ("runtime/data/trigger-index.json is generated only by generate-trigger-index.mjs. No script here hand-edits it") + conventions :44 Availability note ("The index and its manifest are produced together by generate-trigger-index.mjs") + the doctor-yaml's invariant (the doctor "never regenerates... regeneration is owned by the /doctor:update orchestrator") + the Ownership receipt from iteration 2 (doctor-update.yaml:369 = the generator invocation). One writer, four consistent descriptions.
4. **"No daemon, no MCP" = four-surface consistent** — README §5 Boundaries row 3 ("plain Node process reading files or shelling out to rg directly" — which ANTICIPATES the residue sweep's child-process spawn, the one subprocess import in the tree); AGENTS.md:83 ("needs no daemon"); the doctor-yaml's lookup_policy ("There is no socket to probe and no daemon to start. Exit 0 = ...1 = clean no-hit...2 or higher = an execution error"); the lookup .mjs's synchronous-no-extra-modules header. No surface contradicts.
5. **The README's "Current state" bullets = conventions-consistent, fact for fact** — the generator's three roots ("specs/, .opencode/skills/ and .opencode/install-guides/") = §9's root-coverage YES rows EXACTLY; the lookup's 3-char floor + 8-token cap = conventions §8's last paragraph, the same fact twice (the census's N9/N10 fix = cross-document consistent); "the context-and-anchor recipe of §2.4 is composed by hand" = §2.4 exists, and the scratch-glob note delegates to §9, which carries the row.
6. **The §1's :67 bounding sentence** — "Nothing in this document may be read as a claim that the two index lanes plus grep cover the rows above" — fences the document's own claims: the loss table's honesty disclaimer, exactly where a reader would over-credit the lanes.
7. **The "002" destination references resolve** — receipt [B]: `specs/system-speckit/033-system-speckit-v4/017-memory-database-decommission/002-memory-consumer-rewire` EXISTS. Row :60 uses the full name; row :61's bare "phase 002" = the one-row-above elision, self-disambiguating. (Recorded, not filed: 028/002-speckit-memory is a second "002 + memory" packet — the ambiguity is real but the :60 disambiguator sits one row above, which is the self-correcting design.)

## Census cross-check (read once, iteration 1)

- The census's round-1 "What This Does Not Replace" territory was NOT censused row-by-row (no L/V/N row covers the :64/:65 pair or the :33 qualifier) — both findings are new. The census's §9-adjacent rows (N7's repo-rules exclusion, V9's tests/fixtures row) were re-verified in passing while reading §9 — consistent, not re-reported.
- The catalog's boundary block (:28-:36) was NOT touched by either round: the 1-line residue hit (it3's receipt) = :31, and :36's delegation = the reason-row's OTHER clause ("names the tools it maps") — both census-records hold; this pass adds the shape, not a correction.

## What the angle did NOT read (call cap)

The feature-catalog's 792 lines beyond :28-:36 (the other boundary-adjacent prose: :630's retired flag family, :666's manifest subsystem removal, :768/:772's advisor-survival clauses — noted in the locate-grep, not audited: the angle's scope = THE boundary table, which :28-:36 is); the conventions' §3-§6 (the it2 disclosure carries: skimmed for context, none audited); the 017/002 packet's own docs (the 002 destination's CONTENT — the reference resolves; whether the 002 delivered its ownership = the packet's own completion-verification, outside this angle).

## Open questions

1. Was the feature-catalog's one-row/three-tools relocation shape (":31") a deliberate granularity choice or the pre-§2.1-2.4 un-split ancestor? (The conventions' three-destination precision = the SHAPE; the catalog's = the COMPRESSion — if deliberate, it is the right summary; the delegation sentence (:36) suggests deliberate. Not evidence-settled.)
2. Does any OTHER catalog section claim boundary authority over the retrieval losses (the :630/:666/:768/:772 hits = the other retired-adjacent prose)? The :28-36 block = the only TABLE; the prose = unaudited within this angle's scope.

## Process notes

- This iteration = the lane's leanest (9 calls): iterations 2-3's shared-read strategy (the conventions + the README, read once, cited three times) paid its dividend here — the entire row-by-row comparison needed only the feature-catalog's 9-line block plus anchors.
- The 017/002 resolution: THREE "002+memory" packets exist (033/002 = daemon-reaper-orphan-classification, receipt [A]; 028/002 = speckit-memory, receipt [A]; 017/002 = memory-consumer-rewire, receipt [B]) — the bare-"002" reference class is therefore genuinely two-ways-ambiguous in the abstract, and the conventions survive it only via the :60 disambiguator. Recorded; not filed (the one-row-above design).

## What Worked / What Failed / Next Focus

- **Worked:** the summary-here/full-there design test (the :36 delegation made the feature-catalog's side a 9-line read); the verbatim-quote identification (:36 = :64 word-for-word) — the strongest single receipt of the iteration.
- **Failed:** nothing; 9/12 calls, 3 unused.
- **Next Focus (Iteration 5):** trigger-phrase authoring guidance versus the judge — `lib/phrase-judge.mjs` (112 lines), then the guidance: the trigger-phrase template in `templates/spec-kit-docs.json` (2,429 lines — the judge-the-EXAMPLES question), retrieval-conventions §8 (IN CONTEXT: the Include/Warn-On lists + "Declare phrases of two or more tokens" + the judge-paragraph + the 3-char/8-token prompt-limits), and sk-doc's trigger-phrase rule (one file, found by name). Any guidance that tells an author to write a phrase the judge now classifies as banned, or that omits a class the judge enforces, is a finding. Seeds: the judge's class set per the census's N4-fix (generic, fallback, stop-word, prose, folder-token + the 013-added single-token + numeric-only) vs the §8 Warn-On list's FIVE bullets (generic-workflow-words, stopword-only, prose sentences, body-derived fallbacks, single-token+numeric-only — NO folder-token bullet!) vs the committed diagnostics' SIX-class phraseQuality bucket (editor-fallback, generic-workflow-word, numeric-only, prose-sentence, single-token, stop-word-only — ALSO no folder-token!); the §8 Include-list's "paired with a second token" example (`normalizeTriggerText helper` — the JUDGE'S-OWN-DEMO!).
