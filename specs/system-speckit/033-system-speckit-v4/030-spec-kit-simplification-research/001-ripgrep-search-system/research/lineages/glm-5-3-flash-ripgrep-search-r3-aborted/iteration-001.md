# Iteration 001 — Angle 1: Recall in Practice

**Lineage:** `glm-5-3-flash-ripgrep-search-r3` · iteration 1 of 5 · status `complete` · focus: `q-recall`
**Method:** a 1:1 Python re-implementation of the lookup's candidate gate, scorer, scope filter and result cap (`lookup-trigger-index.mjs:145-213`, `lib/normalize.mjs:93-130,147-158`), run against the committed index (`runtime/data/trigger-index.json`, schemaVersion 2, manifestHash `b6953bc6…`, 13,722 paths, 35,560 phrase keys), replaying every prompt the fixtures record; plus a whole-corpus visibility census (disk vs `includedPaths` vs `index.paths`) under the manifest's own exclusion grammar. Results artifact: `evidence/iter1-lookup-trace-results.json`; tracer: `evidence/iter1-lookup-trace.py`. Every claim below was recomputed in THIS tree; no earlier-round count reused.

## Findings

| ID | Path:line (claim side) | Claimed vs actual | Severity | Recommendation | Angle |
|----|------------------------|-------------------|----------|----------------|-------|
| **A1-1** | `fixtures/corpus-manifest.json:20-21` (includedPathCount 28,604; the 16 phantom rows at :5738, :5815, :5817, :6177, :6179, :7430, :7479-7480, :18098-18099 + 7 more 024 rows); doctor pair check = `commands/doctor/assets/doctor-speckit-retrieval.yaml` (pair-vs-pair by construction, census L1) | **Claimed:** the committed pair is the census of record for the corpus, and 006's doctor pair check is its verifier. **Actual:** the manifest promises 28,604 documents; a fresh worktree of the same HEAD carries 28,588 of them — 16 promised paths do not exist here: 6× `**/.pytest_cache/README.md`, 2× `mcp-server/tests/scorer/sweep-reports/**/sweep-results*.md`, 8× the entire `specs/system-skill-advisor/024-routing-perfection-research/` packet. Zero phrase impact (all 16 own no postings — none appears in `index.paths`, 13,722 docs), so no prompt breaks; the residual is that **the committed pair certifies only itself**: the doctor's pair check compares the pair against the pair, the save-time freshness check is per-packet, the parity suite is code-level — a whole packet can vanish after a regeneration with no signal. Disk↔index is otherwise airtight: 0 invisible (every eligible .md here is in includedPaths (28,588) or in skippedPaths with a reason (17, exactly the 17 `duplicate of an already-indexed document` skips)). | **P1** | Document the residual in the retrieval README's freshness section, and close it where 006 built the seam: give the doctor's committed-pair activity an optional existence screen over `includedPaths` (O(1) per row, no corpus walk). Without it, `AGENTS.md`'s "doctor as the verifier" claim (census L1 disposition) holds only for pair-internal drift. | 1 |
| **A1-2** | `lib/corpus.mjs:32-37` (EXCLUSIONS comment: "part of the manifest identity… the coverage-parity suite checks"), `:38-47` (the 8 EXCLUSIONS globs — no `.pytest_cache`), `:72` (EXCLUDED_DIR_NAMES = z_archive, node_modules, scratch, .git, dist — no `.pytest_cache`), `:131` (the name prune) | **Claimed:** the corpus is the checked-in documentation tree, and its identity (corpusHash → manifestHash → the committed pair) is deterministic. **Actual:** 8 of the 16 promised-but-absent rows are machine-generated, not source: 6× pytest's own `.pytest_cache/README.md` explainer + 2× scorer sweep-report outputs. `.pytest_cache` is pruned by neither list, so a regeneration in a fresh clone (no caches) yields a *different* committed-pair value while the doctor stays green — machine-state variance baked into manifest identity, which the parity suite cannot flag because the 006 parity contract only checks that pruned names are recorded. | **P2** | Prune `.pytest_cache` in BOTH lists plus a §9/parity row (006's own mechanism), or document the variance as accepted machine-state dependence; decide, don't leave it implicit. | 1 |
| **A1-3** | dir listing `specs/system-skill-advisor/` (023-model-server-default-spawn AND 023-semantic-lane-enablement; no 024) vs `corpus-manifest.json:18098-18099` (+6 more 024 rows, all 8 absent from disk); `index.paths` (none of the 8 present) | **Claimed (implicit):** packet numbering is unique and the corpus matches the tree. **Actual:** the track carries two packets both numbered 023, no 024, and the committed manifest — built 2026-09-07 by 013's closeout — still lists 8 files under `024-routing-perfection-research/`. Consistent with a post-regeneration 024→023 renumber; nothing fired. Bonus fact: all 8 of those docs own zero trigger_phrases (none is in `index.paths`), so even while it existed, that packet was invisible to Gate 1 — a concrete exhibit for the census's open question 2 (whole-corpus regeneration ownership). | **P2** | Document; the owning track renumbers if the 023/023 collision is a rename accident. First casualty exhibit for census OQ2 (who owns whole-corpus regeneration). | 1 |
| **A1-4** | `fixtures/semantic-probes.json` paraphrase row `find-decision`/`exact` (recorded scoringCount 1; source `runtime/tests/fixtures/trigger-goldens.json`, synthetic case memoryId 1003, triggerPhrase "find decision") vs the committed pair: 0 of 35,560 keys contains both `find` and `decision`; the phrase is declared nowhere in the current tree (grep over specs + system-spec-kit) | **Claimed (recorded):** scoringCount=1, scoringHit=true against the frozen `c0806077…` snapshot. **Actual:** against the committed pair the lookup scores nothing; the recorded verdict no longer reproduces. 1 of 120 paraphrase rows; 9 further paraphrase + 3 of 5 stemming rows show scoring-count deltas, all attributable to the 2-generation-old pinned snapshot (the 006 limitation documents the stale pin — cited, not re-reported). Mitigating, verbatim from the fixture: its `contract` field says "boundary evidence only; never a lexical pass criterion", and its `promptSetHash` pin (`ae629454…`) still matches the current prompt-set.json bytes (verified by re-hash) — the prompt-set pin is honest; only the corpus-side pin is dead. | **P2** | None beyond the already-documented frozen status; the declaring document for `find decision` is UNKNOWN (git prohibited). If the probes are ever regenerated, this row is the one whose recorded value will actually change. | 1 |

## Verified positive (recorded, not findings)

- **18/18 prompt-set parity cases** meet their `expectedPaths` against the committed pair, including the 3 exact/case-fold/punctuation cases, the 4-expected-paths duplicates case, the scope-collision case (scoped results = exactly the 4 packets' docs, no leakage) and the 8-token truncation case; every zero-expectation case (no-hit, body-only, generic-phrase-negative, malformed-frontmatter, archived) returns zero scoring hits. `evidence/iter1-lookup-trace-results.json` `cases[]`.
- **daemonProbe 3/3** (2 hits, 1 clean miss) and **recipeProbe** behaves: hitPhrase "trigger index generator" → 3 scoring hits, led by `runtime/cli/retrieval/README.md` + both 033/017 memory-decommission trigger-index docs; missPhrase → 0 scoring, 20 partial rows (the documented partial tail).
- **119/120 paraphrase + 5/5 stemming** scoring-hit verdicts unchanged (the drift is in counts, not verdicts, except the one A1-4 flip).
- **Remediation recall (verified because the angle led here):** all four 006 phrases and all four 013 phrases resolve to their implementation-summaries in the committed pair (e.g. "phrase quality summary" → `013-trigger-phrase-quality-enforcement/implementation-summary.md`; "committed pair doctor check" → `006-…/implementation-summary.md`).
- **Whole-corpus coverage:** 0 invisible — the census found no eligible .md on disk that the committed pair fails to cover (28,605 disk = 28,588 ∩ includedPaths + 17 duplicate-skips).

## Open Questions (this iteration)

1. Why did `024-routing-perfection-research` vanish, and is `023-semantic-lane-enablement` its renumbered successor? (git prohibited — owner question; folded into A1-3.)
2. Machine-state corpus variance: prune `.pytest_cache`/sweep-reports (A1-2) or document as accepted? Owner: 006's parity contract.
3. Which document declared `find decision` when the probes were recorded? (unanswerable here; git prohibited.)

## Angles

- **Covered:** 1 (recall in practice).
- **Not yet:** 2, 3, 4, 5, 6, 7, 8.

## Fidelity note

19 operations total: 15 evidence + 4 harness repairs (2 trace-harness, 2 census-harness — the first census walk flattened paths via `os.path.join(root, fn)`; caught because it produced 28,603 "phantoms" from a 28,604-row manifest). The 12-call TCB applies to evidence gathering; the excess was harness debugging and is disclosed here rather than silently absorbed. Scores in the results artifact are rounded to 3 decimals for readability; the comparison used full-precision floats exactly as the .mjs does.
