# Iteration 5: Doc contradictions sweep + footprint quantification (AGENTS/CLAUDE/REPO RULES)

## Focus

Complete the doc-contradiction angle (concept lane already confirmed in iter 3; remaining sweep items: availability note, "two exclusion globs" wording, latency claim, mirror-sync claims) and quantify retrieval's footprint in AGENTS.md/CLAUDE.md/REPO RULES.md to evaluate whether a dedicated `repo-rules/retrieval.md` could reduce root-doc footprint to a pointer.

## Findings

| # | path:line | Claimed vs actual | Severity | Recommendation |
|---|-----------|-------------------|----------|----------------|
| F5.1 | `retrieval-conventions.md:44` (Availability note) | Claimed: "**Both** index artifacts are produced by phase `001-trigger-index-replacement`." Actual: §1 declares three lanes and one concept lane over "an embedded index"; §10 links one research lineage and two scripts (lookup, rg-wrapper). No second index artifact is named anywhere in the doc — the keyed index is the only one. "Both index artifacts" is residue of the pre-decommission design (keyed + concept/embedded index) that F3.1 already caught in the body. Second corroborating stale fragment. | **P1** (same root cause as F3.1; the availability note hard-codes a two-index world) | Fix together with F3.1: one editing pass over §1 removing concept-lane rows, the "Both index artifacts" phrasing, and re-pointing the availability note at the single keyed index. |
| F5.2 | `search.md:27` | Claimed: "Each of `--no-config`, **the two exclusion globs** and the `--` separator closes a specific failure." Actual: conventions §2.1 carries FOUR exclusion globs (`!**/z_archive/**`, `!**/node_modules/**`, `!**/.git/**`, `!**/scratch/**`). "Two" matches neither the normative recipe nor search.md's own inline block (which has two globs — the source of the count, and of F3.2's drift). The count is self-consistent only with the drifted inline recipe. | P2 (evidence that F3.2's inline recipe is the reference the router author actually used) | Fix with F3.2: align recipes, update the count to four (or stop counting and point at §2). |
| F5.3 | `references/memory/trigger-config.md:39` | Claimed: "`lookup-trigger-index.mjs` — Fast phrase matching over the committed index — **<50ms response**". Actual: measured cold lookups today at ~67–85 ms wall (two probes); the repo's own latency fixture (`fixtures/latency-report.json`) records median 61–76 ms with max 91 ms per prompt against a 200 ms budget. The <50 ms target in the doc is not met by the fixture evidence nor by today's probe; the fixture's own budget is 200 ms. | P2 | Fix: change the doc's target to the fixture's 200 ms budget (or annotate "<50 ms aspirational; budget 200 ms; observed median ~65–76 ms"). |
| F5.4 | `fixtures/latency-report.json` (`indexPath`, `manifestHash` fields) | Claimed: fixture documents the index it measured. Actual: `indexPath` points at a different worktree (`/Users/michelkerkmeester/worktrees/public/017-memory-decommission/...`), a different index size (3,814,726 B vs committed 3,813,675 B), and a third manifestHash (`c0806077…`). A fourth corpus snapshot inside retrieval's own fixtures. | P2 | Fix: regenerate latency-report at this tree when the index is regenerated (same command as F1.1's fix run), or make its path repo-relative. |
| F5.5 | `lib/rg-lane.mjs` + `rg-wrapper.mjs` + conventions §1 header ("replacing `memory_search`, `memory_context` and `memory_quick_search`") | Claimed (conventions §9): recipes "mirrored in code by `lib/rg-lane.mjs` and `rg-wrapper.mjs`". Actual: both files exist in `runtime/cli/retrieval/`; rg-wrapper is the §2 front door. Mirror claim consistent; no drift found between wrapper flags and §2 recipes on spot-check of the two exclusion sets. | P2 (positive) | Document. |
| F5.6 | `AGENTS.md` retrieval footprint (grep counts) | Claimed (charter hypothesis): root-doc footprint could shrink to a pointer. Actual footprint: **5 retrieval-related lines** out of 507 (lines 61, 83, 330, 463, 477) — the Gate 1 step (1 line + invocation), the §5 mandatory-tools row, the quick-reference row, and the maintenance entry. CLAUDE.md is byte-identical (md5-verified), so any edit doubles. `REPO RULES.md` (91 lines) carries **zero** retrieval mentions; its trigger table routes 9 posture rules, none retrieval-related. | See F5.7 | — |
| F5.7 | footprint verdict | A dedicated `repo-rules/retrieval.md` would hold ~15–25 lines: the Gate 1 invocation + exit-code semantics, the lexical-only boundary sentence, the regenerate-after-phrase-change rule, and pointers to conventions + search.md. Benefit: AGENTS.md drops 4 of its 5 mentions (keeping the Gate 1 step as a pointer), and the mirror problem (AGENTS/CLAUDE byte-identity) stops compounding. Cost: Gate 5 currently fires on first WRITE only — retrieval needs to fire on every prompt (Gate 1), so a repo-rules file would NOT be loaded at prompt time unless AGENTS.md keeps at least the Gate 1 invocation inline. **Verdict: a repo-rules retrieval file fits the §4 mandate (§4 "Out: skill routing, workflow selection…" — retrieval is workflow mechanics, which the router says stays with AGENTS.md §2 and the skills) — the natural home is NOT repo-rules but the existing `retrieval-conventions.md` + a one-line Gate 1 pointer. Root-doc footprint is already small (5 lines); the real duplication is AGENTS↔CLAUDE byte-mirroring, not root-doc length.** | P2 (design note) | Recommend: keep Gate 1 inline in AGENTS/CLAUDE (it must load per-prompt, and Gate 5 won't), move nothing to repo-rules; instead make line 330's row and 477's maintenance entry the only other mentions by collapsing 463 into 330's pointer. Net saving: ~2 lines; low value. Record as considered-and-rejected with the Gate 5 load-timing rationale. |
| F5.8 | `retrieval-conventions.md:§9` root-coverage table vs `lib/corpus.mjs:14` | Claimed: `specs`, `.opencode/skills`, `.opencode/install-guides` shared; rest of `.opencode` ripgrep-only; mirrors excluded both lanes. Actual: table matches `CORPUS_ROOTS` exactly; exclusion-coverage table matches `EXCLUDED_DIR_NAMES` + `FIXTURE_DIR_PATTERN` (outside specs/) + `research/lineages` scoping. One residual asymmetry: corpus walker also prunes `dist` (`corpus.mjs:23` includes `dist` in EXCLUDED_DIR_NAMES) which the §9 table does not list. | P2 | Fix: add a `dist` row to §9's exclusion table (reason: build output, never authored docs). |
| F5.9 | `.opencode/specs` symlink alias handling (`lib/corpus.mjs:96-110, 186-196`) vs §9 | Claimed (§9 mirrors row): mirrors are symlinks onto `.opencode` docs, walking them would add duplicate-skip noise. Actual: walker refuses symlinked directories unconditionally and folds the `.opencode/specs` alias path onto `specs/`; duplicate routes are settled deterministically (document location wins over link; ties by lower path). The alias exists precisely because some trees expose `specs` under `.opencode`; the walker keeps the index free of it. Consistent, but §9 does not name the `.opencode/specs` alias handling — a caller reproducing the lane manually would double-count. | P2 | Document the alias fold in §9 (one row). |

Ruled out: "CLAUDE.md diverges from AGENTS.md" (byte-identical); "a third stale concept-index fragment elsewhere in live docs" (grep across `references/retrieval`, `references/memory`, commands, feature-catalog found only the two known fragments — conventions:33/40/42 and :44).

## Sources Consulted

- `AGENTS.md` (footprint greps, lines 61–83, 325–335, 455–480), `CLAUDE.md` (md5 vs AGENTS.md), `REPO RULES.md` (full read, iter 4 retained), `repo-rules/*.md` (grep sweep, iter 4 retained)
- `retrieval-conventions.md` §1 availability note, §2 glob census, §9 tables
- `search.md:27`
- `references/memory/trigger-config.md:39`
- `fixtures/latency-report.json` (parsed), fresh cold-lookup timing probes ×2 (read-only)
- `lib/rg-lane.mjs`, `rg-wrapper.mjs` (existence + flag spot-check)

## Assessment

- newInfoRatio: 0.7 — F5.1 (second stale fragment) and F5.3/F5.4 (latency-claim vs fixture conflict, third/fourth snapshot hashes) are new; footprint rows (F5.6/F5.7) quantify a hypothesis the charter named but did not measure.
- Novelty justification: the latency-target contradiction and the four-snapshot manifestHash inventory are first recorded here; the footprint verdict is a derived design finding with a stated rationale.

## Reflection

- Worked: parsing the latency fixture exposed both the stale path and the third hash — fixtures are the best fossil record of corpus drift.
- Failed: nothing material; the footprint verdict required rejecting the charter's implied repo-rules direction on load-timing grounds, which is a documented deviation with rationale, not a miss.
- Ruled out: repo-rules placement for retrieval mechanics (Gate 5 fires on first write, not per prompt — see F5.7).

## Recommended Next Focus

Iteration 6: full caller census update + wiring re-verification after iter 4's claims — re-grep for any second-order consumers (YAML workflows, scripts, tests) that reference `retrieval-conventions.md` recipes; verify the rg-wrapper's rank implements §5's tuple; check `measure-cold-lookup.mjs` and `sweep-memory-residue.mjs` utilization vs documented purpose.
