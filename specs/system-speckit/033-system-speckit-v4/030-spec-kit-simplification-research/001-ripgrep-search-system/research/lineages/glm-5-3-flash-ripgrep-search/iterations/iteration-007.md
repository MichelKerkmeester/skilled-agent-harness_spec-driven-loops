# Iteration 7: Removal/merge candidates — retrieval/ file inventory, README accuracy, test-lane wiring

## Focus

Full inventory of `runtime/cli/retrieval/` vs documented capability; the README's own validation contract executed as written; retrofit-convention.mjs utilization; fixtures size/benefit; save-time freshness nuance for the F4.3/F1.1 staleness story.

## Findings

| # | path:line | Claimed vs actual | Severity | Recommendation |
|---|-----------|-------------------|----------|----------------|
| F7.1 | `retrieval/README.md` §7 (validation commands) | Claimed: run `npx --prefix scripts vitest run --config runtime/vitest.config.ts scripts/tests/trigger-index.vitest.ts …` from the skill root; determinism check via `node scripts/retrieval/generate-trigger-index.mjs`. Actual: **both command shapes are broken at this tree.** `scripts/` does not exist under the skill root (it is `runtime/cli/`); the listed test paths don't resolve ("No test files found"); and even the corrected path (`runtime/cli/tests/trigger-index.vitest.ts`) is **excluded** by `runtime/vitest.config.ts` (its `exclude:` names `runtime/cli/**` verbatim). The working invocation is the skill-root project config: `npx vitest run --config vitest.config.ts --project cli tests/trigger-index.vitest.ts` → 48/48 pass. The determinism check's script path is equally stale (`scripts/retrieval/…` vs `runtime/cli/retrieval/…`). The README documents a validation gate that cannot run as written. | **P2** (borderline P1: it is the surface's own documented quality gate, actively misdirecting; the tests exist and pass — only the doc is wrong) | Fix: rewrite §7 with the working invocation and correct paths; or point at the workspace test script (`npm run test --workspace=@spec-kit/cli`). |
| F7.2 | `runtime/cli/core/workflow.ts:344-…,1721-1738` + `tests/workflow-trigger-index-freshness.vitest.ts` | Claimed (F4.3 working hypothesis): no automated freshness instrument exists. Actual: refined — a **save-time per-packet freshness check exists**: `checkTriggerIndexFreshness` compares the saved packet's `spec.md` phrases against the committed index and logs STALE with a regenerate hint (never blocks; NFR-R01/R02 degrade-to-`unavailable`). It is scoped to one packet's spec.md at save time; it cannot catch corpus-wide staleness (today's F1.1: fixtures + corpus regenerated, no save of any affected packet since). F4.3 stands for the doctor; F1.1 stands; the ledger gains a third partial instrument. | P2 (correction/refinement of F4.3 — recorded as its own row so the ledger does not overstate the gap) | Fold into F1.1's fix: the save check stays per-packet; add the doctor manifestHash comparison for whole-corpus sync. |
| F7.3 | `retrieval/README.md` §6 (entrypoints) | Claimed: "`rg-wrapper.mjs` and `sweep-memory-residue.mjs` default their search root to the current working directory… without `--root`/`--search-root` fails to find `specs/`". Actual: accurate for rg-wrapper (verified F6.4: `--root` = cwd); README documents the distinction F6.4 says is under-documented — but only in §6 prose, not at the wrapper's own usage header (`rg-wrapper.mjs:25-28` shows `[--root <dir>]` with no cwd explanation). README right, code header terse. | P2 | Fix at code header (carry README's sentence up), not README. |
| F7.4 | `retrofit-convention.mjs:1-60` | Claimed (README §1): "applies the greppable-corpus convention… pipeline". Actual: 1,000+ line five-stage pipeline (enumerate/dry-run/process/rescan/verify-preimage) with frozen manifests, fail-closed classification, preimage-checked atomic writes. Sole external reference: `references/structure/grep-convention.md`. Utilization: one-shot retrofit tooling — its 48 KB in `retrieval/` is the largest single file in the directory, serving a completed one-time migration (the grep convention is now enforced by GREP_CONVENTION validator + generator instead). | P1 (displacement candidate: a completed-migration tool occupying the retrieval hot directory; test suite `retrofit-convention-pipeline.vitest.ts` keeps it alive) | Recommend: relocate to an archive/maintenance area (e.g. `z_archive/tools/` or a `maintenance/` sibling) with its test, or delete if the migration is accepted-complete and the sweep (`sweep-memory-residue.mjs`) shows zero residue. Non-goal this packet: do not delete — record as the top removal candidate. |
| F7.5 | fixtures size census | Actual: corpus-manifest.json 3.9 MB + generation-diagnostics.json 4.4 MB + phrase-variants.json 2.9 MB + semantic-probes.json 67 KB + others ≈ 11.2 MB committed under `retrieval/fixtures/`, plus grep-convention fixture docs (8 malformed-shape docs — well-formed test corpus, small). diagnostics carries every non-OK row for 28k documents; variants is operator-trace-only (F1.9). Benefit is traceability; cost is a 3.8 MB index + 11 MB fixtures every clone carries. | P2 | Recommend: keep manifest+diagnostics (needed by doctor/parity), consider regenerating variants on demand or dropping from default publication. |
| F7.6 | test-lane inventory (`runtime/cli/tests/`) | Actual: 14 retrieval-adjacent suites exist: trigger-index (48 tests, verified passing), rg-wrapper-recipes, retrieval-coverage-parity, retrieval-repo-root, grep-convention(+rule), sweep-memory-residue, retrofit-convention-pipeline, workflow-trigger-index-freshness, trigger-phrase-{filter,sanitizer,no-prose-bigrams,manual-preservation}. Coverage is broad and real — retrieval is the most-tested subsystem in the runtime. Strengthens F4.8: index is consulted by tests as a first-class consumer. | P2 (positive) | Document. |
| F7.7 | `retrieval/README.md` §2 architecture diagram | Claimed: ASCII box diagram of the six scripts. Actual: accurate against the tree (six scripts + lib + fixtures); trigger_phrases sources named (`specs/`, `.opencode/`, `.opencode/install-guides/`). Consistent. Note: the diagram lives right above a §7 whose commands are stale (F7.1) — the doc is half-current, which is how F7.1 survived review. | P2 | Fix with F7.1 pass. |
| F7.8 | removal/merge shortlist (derived) | Ranked candidates, no documented capability lost: (1) `retrofit-convention.mjs` → relocate/archive (F7.4); (2) phrase-variants.json → on-demand (F7.5/F1.9); (3) `sweep-memory-residue.mjs` → document as one-shot or fold its allowlist check into doctor (F6.6); (4) conventions §1 concept-lane rows → delete (F3.1/F5.1); (5) search.md inline recipe → pointer to §2.1 (F3.2); (6) `EXCLUSIONS` prose-glob list → derive from operational sets (F1.4). Each is a doc/tooling simplification; none touches the two-lane retrieval capability itself. | P2 (ledger consolidation) | Carry into synthesis ledger verbatim. |

Ruled out: "README's working-directory claim wrong for lookup" (lookup takes absolute default index path — cwd-independent, README doesn't claim otherwise); "vitest cli project missing" (skill-root project config verified working, 48/48).

## Sources Consulted

- `retrieval/README.md` (full read), `runtime/cli/tests/` listing, `runtime/vitest.config.ts` + skill-root `vitest.config.ts` (include/exclude), `runtime/cli/package.json` scripts
- Empirical: README's §7 commands as written (failed: "No test files found"); corrected invocation with `--project cli` (passed: 48/48, 362 ms)
- `runtime/cli/core/workflow.ts:280-360, 1700-1740` (save-time freshness check)
- `retrofit-convention.mjs` (header + exports), `references/structure/grep-convention.md` (sole external ref)
- fixtures size census (`ls -l`); `fixtures/grep-convention/` listing; `fixtures/residue-allowlist.json` head

## Assessment

- newInfoRatio: 0.8 — F7.1 (broken documented validation gate, executed-to-fail evidence) and F7.2 (save-time freshness check exists; refines the staleness story) are new; F7.4/F7.5 quantify the removal shortlist.
- Novelty justification: the README gate failure is reproducible evidence, not a reading; the workflow.ts freshness check was missed in iteration 4's census and is corrected here with file:line.

## Reflection

- Worked: executing the README's commands verbatim (instead of assuming doc accuracy) turned a suspicion into a reproducible F7.1; running the corrected command proved the tests themselves are healthy.
- Failed: iteration 4's hook census missed workflow.ts's save-time check because it searched for hook wiring, not library imports — corrected here in-row (F7.2) rather than letting F4.3 overstate.
- Ruled out: see above.

## Recommended Next Focus

Iteration 8: cross-cutting simplicity roll-up — consolidate all P1s; verify AGENTS.md:477's maintenance row vs the actual regenerate triggers (who regenerates after skill-doc phrase changes?); check `install-guides` trigger_phrases quality; sample-verify the committed index against 3 known live docs' current frontmatter to quantify staleness impact (how many live phrases the served index is missing).
