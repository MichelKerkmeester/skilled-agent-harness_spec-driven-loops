---
title: "Tasks: Close every open goal decision"
description: "The ordered work and its evidence, from the shared split through the live Devin probe."
trigger_phrases:
  - "goal open decisions tasks"
  - "goal closure tasks"
importance_tier: "normal"
contextType: "implementation"
---
<!-- SPECKIT_TEMPLATE_SOURCE: tasks-core | v2.2 -->
# Tasks: Close every open goal decision

---

<!-- ANCHOR:notation -->
## 1. NOTATION

`[x]` done with evidence in the same row. `[ ]` not done.
<!-- /ANCHOR:notation -->

---

<!-- ANCHOR:completion -->
## 2. COMPLETION

| # | Task | State | Evidence |
|---|------|-------|----------|
| 1 | Put the four open decisions to the operator | [x] | Answered: criteria as their own field, refuse non-UTF-8, hash check for the two session-free runtimes, build both infrastructure items |
| 2 | Add the objective split and whole-item criteria selection to the shared module | [x] | Three unit tests in `goal-slice.test.cjs` |
| 3 | Render criteria as their own field in both renderers and both block shapes | [x] | Three tests in `goal-core.test.cjs`; parity test extended |
| 4 | Name only the packet on the objective line so criteria appear once | [x] | Asserted in the criteria render test |
| 5 | Refuse a non-UTF-8 log append with a named error | [x] | Test asserts the error code and byte-identical file |
| 6 | Accept link notation and enforce real-path containment in the binding-row rule | [x] | Three tests in the spec-doc validator suite |
| 7 | Give Claude Code and Codex a hash-based resend signal and state the nesting rules | [x] | Shared block in the three lifecycle workflows |
| 8 | Pin the three workflow goal blocks against drift | [x] | Contract test, negative-controlled |
| 9 | Add the hooks corpus root and record it in the coverage table | [x] | Retrieval suites pass; a lookup for binding a goal returns the contract first |
| 10 | Document what a text set does to a binding | [x] | README sentence plus a test |
| 11 | Settle the Devin host merge rule | [x] | Live run reported both hook contexts in one turn |
| 12 | Write the two missing phase-level roll-ups | [x] | Review report and hardening research roll-up |
<!-- /ANCHOR:completion -->

---

<!-- ANCHOR:cross-refs -->
## 3. CROSS REFERENCES

Ranked list: `../008-hardening-research/research/research.md`. Review advisories:
`../007-retirement-docs-and-verification/review/review-report.md`.
<!-- /ANCHOR:cross-refs -->

---

<!-- ANCHOR:protocol -->
## 4. PROTOCOL

Shared module first, then the renderers, then the validator, then assets and retrieval, then the
documents. The compiled orchestrator is rebuilt after any validator change before the packet gate runs.
<!-- /ANCHOR:protocol -->

---

<!-- ANCHOR:pre-impl -->
## 5. PRE-IMPLEMENTATION

| Check | Result |
|-------|--------|
| No adapter parses the injected block's fields | Confirmed by search across the Pi, Cursor and Devin adapters |
| No prompt-submit producer denies on Devin | Confirmed: all three emit context only and exit zero |
| Level recommendation | Level 1, no phase |
<!-- /ANCHOR:pre-impl -->

---

<!-- ANCHOR:code-quality -->
## 6. CODE QUALITY

Comments carry the durable reason and no artifact labels. Both renderer helpers are documented at the
seam rather than inline.
<!-- /ANCHOR:code-quality -->

---

<!-- ANCHOR:testing -->
## 7. TESTING

Eleven new tests. Two negative controls run and observed: a renamed criteria label fails the parity
test, and an altered workflow file fails the drift check.
<!-- /ANCHOR:testing -->

---

<!-- ANCHOR:fix-completeness -->
## 8. FIX COMPLETENESS

Every open decision and advisory from the two prior phases is closed. Nothing is carried forward.
<!-- /ANCHOR:fix-completeness -->

---

<!-- ANCHOR:security -->
## 9. SECURITY

The binding-row rule now refuses a target that resolves outside the packet, matching the containment
the runtime already enforces, so a symlink cannot make a packet validate while the runtime refuses it.
<!-- /ANCHOR:security -->

---

<!-- ANCHOR:docs -->
## 10. DOCUMENTATION

The hook README, the retrieval coverage table, the Devin goal-hook playbook, the two roll-ups and the
packet's before/after record.
<!-- /ANCHOR:docs -->

---

<!-- ANCHOR:file-org -->
## 11. FILE ORGANIZATION

No new modules. The shared helpers live beside the frontmatter boundary they belong with.
<!-- /ANCHOR:file-org -->

---

<!-- ANCHOR:summary -->
## 12. SUMMARY

Twelve tasks, all closed with evidence in the same row.
<!-- /ANCHOR:summary -->
