# Iteration 009: Consolidation verdict tables for both documents

## Focus

Consolidation: emit one per-claim verdict table covering both documents, each row carrying the exact current wording with its line, the verified current truth with its proof, the proposed replacement text and the confidence. Every claim that survived adversarial challenge is marked confirmed, and every claim that narrowed or fell is marked accordingly.

## Actions Taken

1. Gate loads before the first write: the trigger-index lookup, the `REPO RULES.md` router and the six rule files its trigger table routes for this action (evidence-and-proof, uncertainty-and-honesty and the four communication rules).
2. Read the run state: `findings-registry.json` (head and tail), `iterations/iteration-007.md` (the changelog rows) and `iterations/iteration-008.md` (the adversarial pass).
3. Pulled verbatim claim lines for the exact-wording column: 31 changelog pins and 26 README pins, plus the README L405-435 block and changelog L130 and L392.
4. Spot re-verified five load-bearing numbers first-hand this iteration: `mcp-tooling` holds 10 `mcp-*` packet dirs, `repo-rules/` holds 13 files, `AGENTS.md` is 284 lines, `.skilled/commands/deep/` holds 5 files, and `sk-code/` ships the webflow, opencode and obsidian surface packets plus the quality and review modes.
5. Searched the README for the "Related Documents" section tracked since iteration 6. The phrase has zero matches in the current file, so that row keeps its iteration 8 content verdict and gains an anchor flag.
6. Consolidated every adjudicated claim into the two tables below, closed Q5, and recorded the iteration through the append gateway.
7. Budget note: the 12-call cap was exceeded by the mandatory Gate 5 loads (trigger-index lookup, REPO RULES router, six rule files, eight gate calls) plus artifact writes, matching the documented iterations 7 and 8 precedent. Research actions stayed at five: the state re-read, the two iteration narratives, the document pins and the spot checks.

## Findings

### Table A: `CHANGELOG-v4.0.0.0.md`, 23 rows

| # | Line(s) | Exact current wording | Verified current truth with proof | Proposed replacement | Status / confidence |
|---|---|---|---|---|---|
| A1 | L41, L511 | "a single `mcp-tooling` skill with nine modes" / "one skill with nine modes that reads which mode you want and routes to it" | Ten modes ship. `mcp-orca-cli` is the post-draft tenth and the changelog never names orca (0 matches). Proof: `.skilled/skills/mcp-tooling/` holds 10 `mcp-*` dirs (re-counted this iteration), registry keys (iterations 2 and 8), commits a3272f5944 and e6beb0c932 | "ten modes" in both places, optionally naming `mcp-orca-cli` | FELL (high) |
| A2 | L26, L592 | "run as one `system-deep-loop` skill with six `/deep:*` commands" / "The six modes behave as before." | Five modes and five command files ship. The document's own L203 and L581 record the skill-benchmark retirement, so L26 and L592 contradict the document. L26 also names "the two benchmarks" where one benchmark mode ships. Proof: `mode-registry.json` keys (iterations 7 and 8), 5 files under `.skilled/commands/deep/` (re-counted this iteration), commit 02589c0bfd | "Research, review, ai-council, agent-improvement and the model benchmark run as one `system-deep-loop` skill with five `/deep:*` commands." / "The five modes behave as before." | FELL (high) |
| A3 | L45, L553 | "Twelve repo rules under `repo-rules/`" / "Twelve rule files under `repo-rules/`" | Thirteen files ship. L553's enumeration names twelve and omits `answer-the-actual-request.md`. Proof: `repo-rules/` holds 13 .md files (re-counted this iteration), the 13-row trigger table in `REPO RULES.md`, commit 6cbaf58d3b | "Thirteen repo rules" / "Thirteen rule files", naming `answer-the-actual-request.md` | FELL (high) |
| A4 | L45, L553 | "The root document fell from 496 lines to 283" / "went from 496 lines to 283 across six passes" | `AGENTS.md` is 284 lines. Proof: `wc -l AGENTS.md` = 284 (re-verified this iteration), commit 6cbaf58d3b touched the file after the draft | "to 284" | FELL (high, minor) |
| A5 | L433, L435 | "`sk-code-webflow`, `sk-code-opencode`, `sk-code-mobile-cli` and `sk-code-obsidian`" / "The mobile-cli surface covers the Pi Remote mobile app on SvelteKit" | Three surface packets ship: webflow, opencode, obsidian. The mobile-cli packet was retired on 2026-09-19. Proof: `sk-code/` listing (re-listed this iteration), commit 173ce63f59 (the retirement names the changelog among 7 residue files), `rg -c mobile-cli` = 2 (iteration 7) | Drop `sk-code-mobile-cli` from the list and delete the L435 sentence | FELL (high) |
| A6 | L278 | "generated markdown-only copies of every skill and every agent persona (Hermes scans a linked directory in full, so symlinks were rejected on evidence)" | The skill mirror is generated copies. The agent tree is a symlink, `.hermes/agents -> ../.skilled/agents`, so the blanket symlink-rejection parenthetical no longer describes the shipped folder. Proof: `ls -la .hermes`, commits fd8213edb9 and 3a822e5156 (iteration 7) | Narrow the parenthetical to the skill mirror and state that the agent tree ships as a symlink | FELL (medium, narrow) |
| A7 | L32, L278 | "bridges eighteen of the twenty-two hook packages" (both places) | 22 hook package dirs confirmed. 17 package names appear literally in the plugin source. Five are absent as literal names: codex-watchdog, directive-lifecycle, hook-install, permission-policy, session-lifecycle. L278 says four stay out, so one absent name is bridged under a non-literal reference. Proof: hooks census plus `__init__.py` name matching (iteration 8) | Keep "eighteen" only if the 18th bridge is confirmed, otherwise "seventeen" | NARROWED (medium, split unresolved) |
| A8 | L366 | "The `.opencode/hooks/` directory gathers every hook through 102 relative symlinks that point back to each hook's real home." | The gather tree holds 101 relative symlinks (0 absolute). `.opencode/hooks` is itself a relative symlink to `.skilled/hooks`, so the inclusive count is 102. The earlier 101-vs-102 flag resolved as counting scope, not a wrong number. Proof: symlink census and `ls -la .opencode/hooks` (iterations 7 and 8) | None required. Optionally "(the gather alias included)" | CONFIRMED (high) |
| A9 | L96 | "Forty rules are registered." | `runtime/cli/lib/validator-registry.json` holds exactly 40 rows (keys 0-39). Proof: iteration 8 count, and the rules README states the rule list is the set of registry rows | None | CONFIRMED (high) |
| A10 | L385 | "one of 29 catalog forms" | 29 files in `sk-design-chart/assets/templates/`, matching the `references/catalog.md` index. Proof: iteration 8 | None | CONFIRMED (high) |
| A11 | L386 | "across 27 types" | `references/types/` holds 27 `type-*.md` files plus README. Proof: iteration 8 | None | CONFIRMED (high) |
| A12 | L22, L72, L78, L84, L146, L366, L537, L580, L581 | "A compatibility symlink keeps every old `.opencode/specs/...` reference working." / "the old location still resolves through a compatibility symlink" | Every `.opencode/...` reference resolves today through compatibility symlinks (specs is a two-hop chain: `.opencode/specs -> .skilled/specs -> ../specs`). No reference is dead. Proof: alias inventory (iteration 7), commits c34e1bd73b and 2a57cc635d | None for correctness. See A13 | CONFIRMED (high) |
| A13 | whole document | The document never names `.skilled` (0 matches) | The canonical source root is `.skilled/*` after the 041 migration. `.opencode/*` survives only as compatibility aliases, so the path references stay true while the root naming is a release behind. Proof: packet 041-skilled-source-root-migration, alias inventory (iteration 7) | Add a short post-release note that the source root moved to `.skilled` while the aliases remain | NARROWED (medium, naming gap not breakage) |
| A14 | L577, L579 | "The concrete moves:" then the rename list | The list omits two reply-rule renames that landed before the draft: `communication-handoff-and-questions.md` to `communication-handoff.md`, and `communication-presenting-decisions.md` to `communication-decisions.md`. The old names appear 0 times. Proof: commits 0195daea65 and 908d9e6272, 12ede9a43c (iteration 7) | Add both renames to the list | FELL (high, incomplete) |
| A15 | whole document (Q4) | No post-draft narrative exists | 231 commits landed after the changelog's last touch (1d43dbd38b, 2026-09-16) and none is recorded: the `.skilled` source-root migration, the deep-loop ledger, protocol and admission work, the orca packet and its three follow-ups, the mobile-cli retirement, the alias retire/restore and the documentation-correction commits. Proof: `git log --oneline 1d43dbd38b..HEAD` = 231, plus per-commit receipts (beb1a0bcc6, 6e82579080, 9e650decee, b9589efbd9, 9365fbc83d, bbb7d23386, a3272f5944, 173ce63f59, c34e1bd73b, 2a57cc635d, 1bb11a2af2, d5c3d08fdd, 02589c0bfd) | Add a post-draft section covering these categories | FELL (high, missing narrative) |
| A16 | L11, L90, L108, L114, L120, L228 | "28KB command or a 3,000-line template" / "Forty-two surfaces that printed the list" / "Twenty-two remediation children" and "sixteen children in one day" / "1,275 lines across four core templates" and "175 lines instead of 944" / "forty-four open Dependabot alerts" / "178 recommendations" | Not re-verified. No proof gathered. Outside the named focuses of iterations 7 and 8 | Verify before editing | UNVERIFIED (low, carried) |
| A17 | L130 | "a shared anchored root resolver, and a switch from the old specs-only deny-list to a structural boundary that hoists state above the outermost `.opencode`" | Not re-verified (anchor-resolver behavior under the two root names). Proof: none | None until verified | UNVERIFIED (low, carried) |
| A18 | L392 | "The other five hubs resolve through a compiled router contract first. This hub does not yet ... Joining the compiled closure is planned, not shipped." | Not re-verified (compiled-router closure for sk-design). Proof: none | None until verified | UNVERIFIED (low, carried) |
| A19 | L21, L55 | "Six hubs now route to modes." / "Six families made the move" | Exactly six families carry a `mode-registry.json` and the seven named standalones exist, 6 + 7 = 13 matching `.skilled/skills`. Proof: iteration 2 | None | CONFIRMED (high) |
| A20 | L340, L581 | "`cli-gemini` and `cli-copilot` are gone from the skill tree, the advisor's scoring and hub routing." / the removals list | No `cli-gemini`, `cli-copilot` or `mcp-webflow` in any registry. The seven-executor roster holds, the `/interface:*` family is gone, retired prompt-skill names are absent. Proof: registry sampling (iteration 2) | None | CONFIRMED (high) |
| A21 | L379-386 | "One Hub, Four Modes" and the chart/diagram bullets | Registry carries sk-design-fundamentals, sk-design-md-generator, sk-design-diagram, sk-design-chart. `/design:extract`, `/design:chart`, `/design:diagram` exist, and chart and diagram are absent from sk-doc. Proof: iteration 2 | None | CONFIRMED (high) |
| A22 | L537, L580 | the `mcp-figma` move / "The deep-loop runtime's `storage/` became `database/`." | The `mcp-figma` move resolves through the alias. `.skilled/skills/system-deep-loop/runtime/database/` exists and the Human Voice Rules base lives at `.skilled/skills/sk-doc/sk-create-with-human-voice/references/hvr-rules.md`. Proof: iterations 2 and 7 | None | CONFIRMED (high) |
| A23 | L581 | the removal list ("`memory_search` and `memory_save` are gone", "The four `deep_loop_graph_*` MCP tools were removed", "`/deep:command-benchmark`", "`/rewrite:explain-visually` is gone") | All named removals hold where sampled. Proof: iterations 2 and 7 | None | CONFIRMED (high) |

### Table B: `README.md`, 17 rows

| # | Line(s) | Exact current wording | Verified current truth with proof | Proposed replacement | Status / confidence |
|---|---|---|---|---|---|
| B1 | L92 | "**Prerequisites:** Node.js 18+ with `npm`, `git` and a POSIX shell." | No reachable manifest supports Node 18. Shipped floors: >=20.11.0 (system-spec-kit, runtime, shared, deep-loop, sk-doc), >=22 (sk-communication projection), >=22.12.0 (spec-kit runtime/cli), >=24.0.0 <25 (mcp-code-mode server), bun >=1.0.0 (sk-vision). Proof: engines field of every reachable package.json, 62 files (iteration 8) | "Node.js 20.11+ (22.12+ for the spec-kit runtime CLI)" | FELL (high, understated) |
| B2 | L412 | "Native package: 121 advisor test files, 872 tests." | The advisor packet holds 3 test files (about 12 it()/test() cases) and 6 advisor-path test files exist repo-wide. No 121/872 provenance exists in advisor docs. Proof: advisor test scan (iteration 8) | Drop the count or state the real one ("3 test files") | FELL (high, unsupported) |
| B3 | L609 | "96-feature catalog + 76-scenario playbook included" | 96 holds: `FEATURE-CATALOG.md` reads "Total catalog entries \| 96" (97 files = index + 96). 76 falls: the playbook index reads "96 features \| 37 scenarios", 45 playbook md files, zero "76" occurrences in the packet. Proof: iteration 8 | "96-feature catalog + 37-scenario playbook" | NARROWED (high) |
| B4 | L894-902 | "External Integrations (via `.utcp_config.json`)" and the seven-item list | `.utcp_config.json` carries 14 templates. The listed "clickup (community)" has no matching template (phantom row), and 8 templates are unlisted. Proof: template enumeration (iteration 8) | Enumerate the 14 templates and drop the phantom row | FELL (high, incomplete plus phantom) |
| B5 | L936 | "Replace the shipped Webflow + OpenCode + Motion.dev surfaces with your own" | The surface axis is `sk-code-webflow`, `sk-code-opencode`, `sk-code-obsidian` (re-listed this iteration). Motion.dev is an overlay folded into sk-code-webflow, and obsidian is omitted from the row. Proof: `sk-code/SKILL.md` (iteration 8), packet listing (this iteration) | "Webflow + OpenCode + Obsidian surfaces", with Motion.dev described as an overlay | FELL (high, naming) |
| B6 | Related Documents section | The section heading no longer exists in the README (0 matches for the phrase, and the only "Related" hit is the Gate 3 option list at L285) | 10 of 10 link targets existed at the iteration 8 probe. The anchor cannot be re-pinned from the current file. Proof: iteration 8 probe plus this iteration's heading search | Re-locate the link set before editing. See Questions Remaining | CONFIRMED at iteration 8 (content), anchor flag raised (medium) |
| B7 | L9, L10, L36, L547, L638, L990 | "12 Specialized Agents" / "13 On-Demand Skills" | 12 agent definitions and 13 skill identities are consistent with the tree (L547 reads "13 advisor skill identities in `.skilled/skills/`"). Proof: iteration 3 | None | CONFIRMED (high) |
| B8 | L59 | "12 specialized" and "20 domain skills" (architecture diagram) | Not established. The registry-side count is 13 skill identities, so the "20 domain skills" figure has no verified taxonomy. Proof: none. New observation this iteration | Verify the intended scope before editing | UNVERIFIED (low, new) |
| B9 | L413 | "Manual testing playbook: 47 scenario files spanning the native command surface" | 47 scenario .md files plus index plus vitest harness (49 hits). Proof: iteration 3 | None | CONFIRMED (high) |
| B10 | L427 | "Four loop families (research, review, AI council, and improvement) ... giving five `/deep:*` loop commands in total." | Four family packet dirs plus two improvement lanes and five command files. Proof: iteration 3, command count re-verified this iteration (5) | None | CONFIRMED (high) |
| B11 | L534 | "Two co-equal lanes in the `system-deep-loop` improvement mode." | Matches the registry (agent-improvement, model-benchmark). Proof: iteration 3 | None | CONFIRMED (high) |
| B12 | L696 | "32 command entry points across 7 command groups plus 3 root utilities." | Exact (create 12, deep 5, design 3, doctor 3, prompt 1, rewrite 2, speckit 6, plus 3 root utilities). Proof: iteration 3 | None | CONFIRMED (high) |
| B13 | L116, L323, L374, L379, L404 | advisor "nine commands" statements | Mutually consistent, and the tools/handlers layout supports them. A direct nine-id enumeration was not completed. Proof: iteration 3 | None | CONFIRMED (high, enumeration residual) |
| B14 | L961-965, L976-984 | the core config list and the MCP shape block | Holds minus one miss: `.vscode/mcp.json` does not exist. Every other sampled `.skilled/...` path in the README resolves (about 55 probes). Proof: iteration 3 | Fix or drop the `.vscode/mcp.json` reference | NARROWED (high, one miss) |
| B15 | L156, L191, L161-173 | the spec-kit-docs.json contract and the packet structure | Matches (levels 1, 2, 3, 3+, phase, review, research), and the 042 packet itself matches the structure claim. Proof: iteration 3 | None | CONFIRMED (high) |
| B16 | L447 | "Rust Joins the Code" | No Rust packet dir under sk-code. Likely inside `sk-code-quality` stack guidance, not packet-level. Proof: directory sampling (iteration 2), inference only | None until verified | UNVERIFIED (low, inference) |
| B17 | L15 | the license badge URL (shields.io) | Redirect behavior not verified (needs a network check). Proof: none | None | UNVERIFIED (low, carried) |

### Verdict summary

40 rows adjudicated: 18 confirmed, 4 narrowed, 12 fell or stale, 6 unverified (carried). Nothing reversed direction from the iteration 8 adversarial pass. Two new flags surfaced during the pin pull: the README architecture diagram at L59 reads "20 domain skills" (taxonomy unverified), and the "Related Documents" heading no longer exists in the README, so that row is content-verified from iteration 8 only.

### The correction list (Q5 deliverable)

Targeted corrections suffice for both documents. The changelog needs eight edits (A1 through A6, A14, A15) plus one additive note (A13): three count drifts (A1, A2, A3), one line-count drift (A4), one surface retirement (A5), one narrowed parenthetical (A6), one rename-list edit (A14) and one missing post-draft section (A15). The README needs five edits (B1, B2, B4, B5 and B3's second half) plus one path fix (B14). No row requires a rewrite of either document, and every edit is a count, a path root, a label or a missing entry.

## Questions Answered

- Q1: answered. Every `.opencode/...` path reference in the changelog resolves through compatibility aliases and none is dead (A12). The canonical root is `.skilled`, which the document never names (A13). Residual: L130 anchor resolver unverified.
- Q2: closed. Ten mcp-tooling modes, five deep modes, thirteen repo rules, three sk-code surfaces, 102 symlinks inclusive, 40 rules, 29 chart forms, 27 diagram types, Hermes at 17 literal of 22 (A1 through A11).
- Q3: closed. The README correction list is B1, B2, B3, B4, B5 and B14, with everything else sampled resolving or flagged.
- Q4: answered. 231 post-touch commits, and all four named categories confirmed absent from the narrative (A15).
- Q5: closed. Targeted corrections suffice for both documents, with exact replacement values in the tables above. The repo's own precedent is three correction commits to this changelog already (926cb2db2e, 3a822e5156, 1d43dbd38b).

## Questions Remaining

- Hermes 18-of-22: which package is the bridged 18th (candidate: session-lifecycle via the session-start advisories). Needs a deeper `__init__.py` read.
- Changelog historical numbers (L11, L90, L108, L114, L120, L228) not re-verified.
- Changelog L130 anchor-resolver behavior and L392 compiled-router closure not re-verified.
- New: README L59 "20 domain skills" taxonomy unverified.
- New: the "Related Documents" anchor is missing from the README, so the 10-link row needs re-location before editing.
- Carried: shields.io badge redirect behavior, `@markdown` and `deep-ai-council` renames, Rust placement, the advisor 121/872 provenance if it exists outside the tree.

## Next Focus

Iteration 10 (final): close the Hermes split if a deeper plugin read resolves it, re-locate the Related Documents link set, verify the new L59 flag if cheap, and close the run with the consolidated correction list as the deliverable.

## SCOPE VIOLATIONS

None. All writes stayed within `iterations/iteration-009.md`, `deltas/iter-009.jsonl`, and the contract-mandated temp record consumed by the append gateway.
