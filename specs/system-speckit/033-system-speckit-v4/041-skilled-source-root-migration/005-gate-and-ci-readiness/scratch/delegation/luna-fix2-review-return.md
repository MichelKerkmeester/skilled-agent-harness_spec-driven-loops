## Verdict

Not ready: four P1 fail-open paths and one P2 false positive remain in `.github/scripts/check-gate-inputs.sh:106`, `.github/scripts/check-gate-inputs.sh:116`, `.github/scripts/check-gate-inputs.sh:161` and `.github/scripts/check-gate-inputs.sh:253`.

## Findings

| ID | Severity | File:line | Scenario (the inputs and the wrong outcome) | Suggested fix |
|---|---|---|---|---|
| P1-001 | P1 must fix | `.github/scripts/check-gate-inputs.sh:116`, `.github/scripts/check-gate-inputs.sh:122`, `.github/scripts/check-gate-inputs.sh:148` | A hook command such as `git diff --cached --name-only \| grep -E '^(\.)opencode/agents/'` is a valid one-root regex. The parser sees only `(\.)`, misses the composed `opencode` root, and the line contains no contiguous `.opencode` for `parser-miss`, so it passes. | Parse composed regexes or fail closed on any unrecognized regex containing a root name. Add this exact pattern as a fixture. |
| P1-002 | P1 must fix | `.github/scripts/check-gate-inputs.sh:248`, `.github/scripts/check-gate-inputs.sh:253`, `.github/scripts/check-gate-inputs.sh:106` | Inline `paths: ['.opencode/skills/**', '.skilled/skills/**', '!.opencode/skills/private/**']` records the positive pair but ignores the negated one-root filter. Per-line accounting then marks the whole line read, so the missing negated twin passes. | Normalize and twin-match an optional leading `!`; add an inline negated-filter fixture. |
| P1-003 | P1 must fix | `.github/scripts/check-gate-inputs.sh:161`, `.github/scripts/check-gate-inputs.sh:166`, `.github/scripts/check-gate-inputs.sh:194` | `git diff --cached --name-only -- "$REPO_ROOT/.skilled/skills/missing/SKILL.md"` is classified as a `note` through the `.skilled` link, while `roots()` does not record the path because the root follows `/`. The missing one-root pathspec therefore passes. | Make variable-path classification context-aware, recording root paths used in git segments for twin checks and existence checks. |
| P1-004 | P1 must fix | `.github/scripts/check-gate-inputs.sh:106`, `.github/scripts/check-gate-inputs.sh:181`, `.github/scripts/check-gate-inputs.sh:186` | `echo "See .opencode/docs/not-a-gate.md" && source .opencode/missing.sh` emits a message for the first segment but does not parse the `source` command. Because `unread_lines` tracks only the line number, the missing input is treated as read and passes. | Track consumed root occurrences, not only consumed line numbers. Add a same-line message plus unparsed-command fixture. |
| P2-001 | P2 should fix | `.github/scripts/check-gate-inputs.sh:107`, `.github/scripts/check-gate-inputs.sh:116`, `.github/scripts/check-gate-inputs.sh:122` | A valid trailing comment such as `true # documented regex: grep -E '^\.(opencode)/agents/'` is stripped by the normal lexer but scanned raw by `REGEX_AWK`, causing a false `filter-twins` failure. A trailing comment containing `.opencode/docs` likewise triggers `parser-miss`. | Apply comment stripping consistently to regex scanning and unread-line accounting. |
Codex exit 0, 2026-09-17T06:38:36Z to 2026-09-17T06:53:31Z, --model gpt-5.6-luna, reasoning xhigh, service tier fast, --sandbox read-only.
