## Verdict

The committed change does what phase 005 needs: the kept-basename skip (`.opencode/skills/sk-doc/shared/scripts/check_no_new_snake_case.py:291-292`) rescues exactly the four tracked names the later move would otherwise flag (`005-gate-and-ci-readiness/spec.md:164`), the three new unittest cases cover the positive rule and both negatives, and the packet's own AC-012 records the fail-first discipline with a 7-of-7 suite run (`005-gate-and-ci-readiness/acceptance-criteria.md:73`). Of the four hunted shapes, none is mishandled: under `-z` git never quotes paths and the similarity score rides the status token (`R100`/`R073`), so the untouched `startswith(("R","C"))` test (`check_no_new_snake_case.py:170`) is score-agnostic and the two-path assumption plus truncated-record bound (`:171-172`) predate this diff; no directory component loses its check because the skip fires only on full-path equality with the destination (`:291`), pinned by the third case's `assertNotIn` (`test_no_new_snake_case_guard.py:141`); and no new test is vacuous — the second case passes against the old code by design ("still") and still discriminates a skip lacking the basename comparison, which the AC's recorded observation itself acknowledges. The 1→2 return-shape change (`:142-144`) breaks no consumer: the three modules importing from the guard (`check_authored_name_kebab.py:23`, `reference_checker_core.py:19`, `rename_engine_core.py:27`) reference none of its changed functions, and both added comments carry durable WHY only, with no ids or spec paths. What survives review are four defects: the copy half of the requirement holds only when the copy's source itself changed since the ref, and no test exercises a `C` record at all (P1); kept-ness ignores whether the source exists at the ref (P2); the phase's own REQ-011 handoff omits the guard's two `.opencode` literals it deliberately left (P2); and phase 007's rehearsal expectation of 4 offenders predates this change (P2). At the migration's 17,767×17,767 path matrix (007-source-root-move/plan.md:256) the inexact similarity pass is limit-skipped, so the skip's effectiveness there rests on exact identical-blob pairing — which a pure move produces, with the T043 rehearsal as the empirical backstop. Evidence basis: reads only — the diff, both changed files, the three importers, the workflow (`.github/workflows/naming-standard-guard.yml:45-46`) and the packet's requirement texts; nothing was executed, and the 7-of-7 run is the packet's recorded observation, not mine.

## Findings

| ID | Severity | File:line | Scenario | Suggested fix |
|----|----------|-----------|----------|---------------|
| F-1 | P1 | `.opencode/skills/sk-doc/shared/scripts/check_no_new_snake_case.py:157` (with `:170-179`, `:183-187`) | `--find-copies` without `--find-copies-harder` considers only sources that themselves changed, so copying an unchanged tracked grandfathered file — `cp` + `git add` (diff yields `A`), or unstaged (arrives via the untracked branch, `:186-187`) — never produces a `C` record, `kept_names` stays empty for it, and the basename is reported. REQ-012's promise, "reports no offender for a rename or copy that keeps its basename" (`005-gate-and-ci-readiness/spec.md:164`), therefore fails in the most common copy case. No test exercises a `C` record at all: the fixture's `move` (`test_no_new_snake_case_guard.py:54-56`) uses `git mv`, which only ever yields `R`, and AC-012's three scenarios (`005-gate-and-ci-readiness/acceptance-criteria.md:73`) are all moves. | Add `--find-copies-harder` so a verbatim copy pairs as an exact `C100` and skips (the extra cost is bounded — the inexact similarity stage is limit-skipped at 17,767×17,767 anyway), and add one `cp`-based case; or amend REQ-012 to "a copy whose source changed since the ref" so the requirement matches the shipped behavior. |
| F-2 | P2 | `.opencode/skills/sk-doc/shared/scripts/check_no_new_snake_case.py:173-178` (rationale at `:147-148`, `:289-290`) | kept-ness is basename equality only, never existence at the ref: a copy whose source was itself added since the ref (base without `pkg/new_thing.yaml`; worktree adds it and copies it → `A` + `C100` with equal basenames) skips its final component although that basename is genuinely new, contradicting the docstring's "moved an existing name rather than introducing one". The paired Add record still flags the basename, so the exit stays 1, but the FAIL report names `pkg/new_thing.yaml` and never `pkg2/new_thing.yaml`. | Decide kept-ness where `base_paths` already exists (`:280`): have `_changed_destinations` also return the record's source (or the (source, destination) pairs — the new second set's only consumer is the unpack at `:284`, so the blast radius is that one line) and skip the final component only when the source is present at the ref. |
| F-3 | P2 | `.opencode/skills/sk-doc/shared/scripts/check_no_new_snake_case.py:199`, `:304` vs `005-gate-and-ci-readiness/acceptance-criteria.md:74` | This phase modified the guard (`005-gate-and-ci-readiness/implementation-summary.md:94`) but left its own `.opencode` root literals in place — `_under_specs` at `:199` and `_current_completed_spec_roots` at `:304`. REQ-011 (`005-gate-and-ci-readiness/spec.md:163`) requires the handoff to name "each script the gates call whose own root literal this phase leaves in place, with its line", yet the recorded AC-011 list (8 items, none of them the guard) omits it, so 006 may read the gate-side authority as having no residual literal there. 002's reference map does carry both lines as mechanical (`002-per-runtime-reference-map/research/lineages/swe2/working/map-c--skill-sk-doc--shared.md:14`), so the exposure is ownership ambiguity, not silence. | Add the guard's `:199`/`:304` to the REQ-011 handoff surfaces (spec, implementation-summary, the AC-011 observed list), or state there explicitly that 002's reference map owns them. |
| F-4 | P2 | `007-source-root-move/tasks.md:101` (with `007-source-root-move/spec.md:174`) | T043's rehearsal "expecting 4 names" was computed under the pre-change guard: after this change, a move that leaves the four untouched reports 0, while a move that also edits any of them reports it anyway (a below-100 pairing needs the inexact stage, which is limit-skipped at 17,767×17,767), so T043 as written either fails spuriously or passes for the wrong reason. 007's risk note, "the CI naming guard reports four grandfathered snake_case names as new … routes them to 005" (`007-source-root-move/spec.md:174`), likewise assumes the four still fail. | Before 007 executes, re-derive T043's expectation from the landed guard (0 for a byte-identical move) and have the rehearsal assert the skip — that the four no longer appear — rather than the debts. |

Tool calls: read /private/tmp/claude-501/-Users-michelkerkmeester-MEGA-Development-Code-Environment-Public/6d11af6f-653e-4807-aca8-1c09c81640c1/scratchpad/p005/naming-guard.diff; find; ls .opencode/skills/sk-doc/shared/scripts; ls .skilled/skills/sk-doc; read .opencode/skills/sk-doc/shared/scripts/check_no_new_snake_case.py; read .opencode/skills/sk-doc/scripts/tests/test_no_new_snake_case_guard.py; read .opencode/skills/sk-doc/shared/scripts/naming_root_resolver.py; find .opencode; find .opencode; grep specs/system-speckit/033-system-speckit-v4/041-skilled-source-root-migration; grep .; read .github/workflows/naming-standard-guard.yml; read specs/system-speckit/033-system-speckit-v4/041-skilled-source-root-migration/005-gate-and-ci-readiness/spec.md; grep .opencode/skills/sk-doc/shared/scripts; grep specs/system-speckit/033-system-speckit-v4/041-skilled-source-root-migration/005-gate-and-ci-readiness/acceptance-criteria.md

Pi exit 0, 2026-09-16T23:19:43Z to 2026-09-16T23:39:19Z, --mode json, tools read,grep,find,ls.
