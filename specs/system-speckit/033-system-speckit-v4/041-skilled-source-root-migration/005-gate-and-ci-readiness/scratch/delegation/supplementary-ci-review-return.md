All evidence is in. Every extraction form the real hooks/workflows use is captured (I traced the three grep-regex lines, the four bash arrays, the `git -- pathspec` clauses, and the `${REPO_ROOT}/.opencode/...` assignments through the AWK programs line by line), every `-e`-checked target exists at the paths a fresh CI checkout has, all 36+ twin pairs are present, and the drill's expected wording matches the hooks verbatim. The weaknesses I found are coverage gaps, not live failures — the committed check passes its own contract.

## Verdict

The contract is sound and the committed state satisfies it. I traced the check's three extraction programs against every real consumer: all ten `$REPO_ROOT/.opencode/...`-style references in the five hooks are in captured forms and their targets exist (`pre-commit:57,104,228,257,296-298,508`, `pre-push:48,65,252,297`, `post-commit:28`, `post-merge:26`, `post-rewrite:27`, `prepare-commit-msg:55`, legacy `hooks/git/pre-commit:15,20-21,60`); the only three root-regex lines all name both roots inside one group so the group rule scores them `group-ok`, and no scanned file trips the literal-one rule; every one of the 36+ path-filter pairs (including the 18+18 in `routing-registry-drift.yml:26-61,68-103`) and every quoted pathspec pair (`pre-commit:148,163,165,306-309,355-362`, `pre-push:333-344`) has its twin verbatim in the same file; dependabot's quoted-`/.opencode/` extraction matches the real `directories:` shape; every bare/inline/continuation reference in the 21 workflows (e.g. `routing-registry-drift.yml:139,164,170,183-185,196`, `runtime-no-spec-import.yml:39,44`, `strict-pass-freshness-report.yml:56-57`) either resolves or falls into the documented dynamic (`node_modules`) or bare (`npm --prefix .opencode`) buckets. The fail-closed steps are correctly gated: `advisory-checks.yml:27,41` really does keep `continue-on-error: true`, so the new step-level `exit 1` matches its comment, and the other four guards exist at exactly the paths the workflows probe. The drill's twelve message expectations (`BLOCKED [gate:...]`/`WARNING [gate:...]` wordings) match the hooks verbatim, and the fixture's line-number expectations (test 3 line 4, test 4 line 12, test 5 line 5) all hold. Portability: the new scripts use only POSIX-awk, process substitution, `compgen -G` and quoted-parameter patterns — nothing I found breaks on bash 3.2.57 orBWK awk, and the comment-hygiene directive is honored throughout the new code (no artifact ids, no spec paths in comments). What remains are five coverage gaps — trigger-shape, resolution, and drill-strictness gaps that would let a *future* regression reach main unseen, none of which is live today.

## Findings

| ID | Severity | File:line | Scenario | Suggested fix |
|----|----------|-----------|----------|---------------|
| F1 | P2 | `.github/scripts/check-gate-inputs.sh:151-159,162` | The workflow parser only recognizes `paths:`/`paths-ignore:` as block lists whose `-` entries sit at deeper indent than the key (`check-gate-inputs.sh:152`: `ind > pind`; trigger at `:159` requires end-of-line). A workflow written `paths: ['.opencode/skills/**']` (inline) or with entries at the *same* indent (legal YAML) skips the filter branch entirely; its tokens are then picked up by the run loop (`:162`) and `-e`-resolve as directories (`:182-192`), so `mentions>0, items>0` and the parser-miss backstop also stays silent. Wrong outcome: a later one-root edit to such a trigger reports `RESULT: PASSED` while the `.skilled` leg never fires. No workflow uses that shape today (all 13 `paths:` occurrences are block form). | In the inpaths branch, also treat a same-line/inline-array value and a non-deeper `-` entry as filter material, or require ≥1 emitted `filter` item per `paths:` key (the current parser-miss guard only fires when *no* item at all is yielded). |
| F2 | P2 | `.github/scripts/check-gate-inputs.sh:130-133,178-181` | Filters and hook pathspecs are only string-twin-checked (`grep -qF` of the swapped root); neither side is ever existence-resolved. Scenario after the move (the layout this check exists to approve): `.skilled/agents/` is deleted — `pre-commit:156,159` (mirror-source pathspecs) and the workflow filters (`spec-kit-check.yml:14-15`) still string-match their twins, so the check passes while every trigger points at a tree that no longer exists and the mirror/agent gates silently stop firing. The drill never exercises this: every `break_input` case targets a resolved `repo`/`run` input, never a pathspec or filter. | After the twin `grep` succeeds, `-e`-resolve (or `compgen -G` for the `*`-bearing ones, reusing the resolver at `:186-192`) at least one concrete representative per twin pair. |
| F3 | P2 | `.github/scripts/tests/broken-move-drill.sh:296-298` | The foreign control computes `new_lines="$(grep -vxF -f foreign-pre.log foreign-cur.log)"` and passes when it is empty. With an *empty* `pre.log`, grep with an empty pattern file selects nothing on both GNU and BSD, so `new_lines` is always empty: the "prints nothing the earlier hook did not" half of the control is vacuous, and only `rc_cur == 0` is actually being tested. Whether any FIRST_RULE^ hook is silent in the foreign repo depends on the historic blobs the drill extracts at runtime (`:49-57`) — if one is, the current hook could print arbitrary new output (e.g. a newly added WARNING) there and every control would still pass. | Before trusting the comparison, assert the earlier hook actually produced output (e.g. `[[ -s "$WORK/foreign-pre.log" ]] || fail "control produced no baseline"`) — or compare both directions so an empty baseline is itself a reported failure. |
| F4 | P2 | `.github/scripts/tests/broken-move-drill.sh:66-68,88-91,109-110` | The move is staged via `git add -A .opencode .skilled` (`:66`) while `.opencode` is already a symlink, so the tracked `.opencode/...` entries still resolve and are unchanged — the move commit is additions-only (~2N+1 paths; the `-M` at `:68` detects no renames) and the mass-deletion count over `MOVE_LINE` is zero. Consequently the section-1 expectation "pre-push lets the move itself through" (`:109-110`) holds even if the mass-deletion gate were removed entirely, and the comment's claim that a diff across the move "sees as every .opencode file deleted" (`:88-91`) is not what that expectation exercises. (The dedicated mass-deletion case at `:185-186` is unaffected — it fires from `_MASS_DEL_MISSING=1` regardless of the range.) | Pin the shape at `:68`: assert the deleted-count from `git diff --name-only --diff-filter=D HEAD~1 HEAD | wc -l` equals what the comment promises, so the drill states which behavior it tested and a future rearrangement of the staging cannot silently weaken the range. |
| F5 | P2 | `.github/workflows/gate-inputs.yml:3-5` | `push: branches: [main, 'skilled/**']` means a direct push to any other branch runs nothing; only the `pull_request:` leg (`:5`) is truly unconditional. Scenario: the move lands as direct pushes to a work branch — no independent check runs until the PR, while the phase's stated intent is "CI runs on every push, with no path filter" (`.github/workflows/README.md:50` says "yes, no path filter", which is true but quietly branch-scoped). | Drop the `branches` filter (the job is two text scans — cheap), or add the branch scoping to the README's trigger-table row so the documented claim matches the exercised behavior. |

Tool calls: read /private/tmp/claude-501/-Users-michelkerkmeester-MEGA-Development-Code-Environment-Public/6d11af6f-653e-4807-aca8-1c09c81640c1/scratchpad/p005/ci-contract.diff; ls .github/scripts; read /private/tmp/claude-501/-Users-michelkerkmeester-MEGA-Development-Code-Environment-Public/6d11af6f-653e-4807-aca8-1c09c81640c1/scratchpad/p005/ci-contract.diff; grep .github/workflows; grep .github/workflows; find; ls .opencode/scripts/git-hooks; ls .github/workflows; grep .opencode/scripts/git-hooks; read .opencode/scripts/git-hooks/pre-commit; read .opencode/scripts/git-hooks/pre-push; grep .github/workflows/routing-registry-drift.yml; read .opencode/scripts/git-hooks/prepare-commit-msg; read .opencode/scripts/git-hooks/post-commit; grep .github/workflows/routing-registry-drift.yml; read .opencode/scripts/git-hooks/post-merge; read .opencode/scripts/git-hooks/post-rewrite; read .opencode/scripts/git-hooks/commit-msg; read .opencode/hooks/git/pre-commit; read .github/workflows/advisory-checks.yml; grep .opencode/skills/system-spec-kit/runtime/cli; grep .opencode; grep .opencode/skills/system-spec-kit; ls .opencode/bin/tests/fixtures/no-spec-import; ls .opencode/skills/sk-doc/sk-create-skill/scripts; ls .opencode/hooks/dispatch/lib; ls .opencode/hooks/shared; ls .opencode/skills/sk-doc/sk-create-manual-testing-playbook; ls .opencode/skills/cli-external-orchestration

Pi exit 0, 2026-09-16T22:34:14Z to 2026-09-16T23:15:24Z, --mode json, tools read,grep,find,ls.
