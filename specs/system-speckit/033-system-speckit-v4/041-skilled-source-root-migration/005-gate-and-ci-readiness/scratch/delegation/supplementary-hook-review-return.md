## Verdict

The diff does what its four rules say, and two close reads could not break it. Rule A holds: every `.opencode/`-naming git input in the changed hooks now has its `.skilled/` twin — the agent filter (`.opencode/scripts/git-hooks/pre-commit:109`, `.opencode/hooks/git/pre-commit:65`), the mirror-parity outputs, sources and globs (`pre-commit:148,156-161,163,165`), the card-sync and mutation-class trigger regexes (`pre-commit:230,259`), the route pre-filter and per-hub pathspecs (`pre-commit:308-309,359-362`), the pre-push skill-range and routing-bytes pathspecs (`.opencode/scripts/git-hooks/pre-push:149,339-344`) — and I found no un-twinned `.opencode/` pathspec left in any of the six hooks plus the SessionStart check (the other hooks name no git pathspec at all). Rule B's seven+four outcomes all name their path and a pre-existing bypass, the four cannot-block hooks keep their exit status, and Rule D adds no variable. Both re-mint gates survive the linked source root in a trace, including the `cd -P`/physical-add mechanics, and every discriminating harness case I chased fails against the pre-change hook, so the cases test the change. Two things survive review as gaps, both P2: the agent-mirror gate's `.skilled/` half stops at the hook, where the unchanged checker's own path filter drops it, and the new SessionStart harness's silence expectations quietly depend on the host's system gitconfig. I ran nothing (read-only dispatch): the verdicts are line-traces, the 175-case and 4-of-4 PASS numbers are the author's (`implementation-summary.md:144-145`), the harness runs are the one thing only the operator can reproduce, nothing I reviewed was edited, the phase's commits are committed but none pushed (`implementation-summary.md:117`).

## Findings

| ID | Severity | File:line | Scenario | Suggested fix |
|----|----------|-----------|----------|---------------|
| F1 | P2 | `.opencode/scripts/git-hooks/pre-commit:109` and `.opencode/hooks/git/pre-commit:65` → `.opencode/skills/system-deep-loop/deep-improvement/scripts/check-agent-mirror-sync.cjs:32` | After the move, the tracked canonical spelling of an agent becomes `.skilled/agents/<name>.md`. An edit synced to `.skilled/agents/` but not `.claude/agents/` stages only the `.skilled/` path; the widened greps now deliver it, but the checker's `AGENT_PATH_RE` admits `.opencode|claude` only, so `agentNameFromPath` (`:40-42`) returns null, the name list empties, and the checker exits 0 with "no agent files to check" (`:66-68`) — the commit-time mirror gate silently checks nothing for exactly the path every tracked agent edit will then carry. Only CI's renamed filter catches it afterwards (`implementation-summary.md:66`); the handoff is documented (`implementation-summary.md:183-184`), but the new harness cases stub the checker to log its argv (`pre-commit.test.sh:887-900`, `:1049-1063`), so they prove the handoff, not the recognition, and would still pass if the checker never learned `.skilled/`. | Add `skilled` to the alternation at `check-agent-mirror-sync.cjs:32` before the tree moves (the dedup at `:64` keeps a synced `.skilled`+`.claude` pair to one check), and add one harness case whose stub prints the names it derived, so the checker's own recognition — not just the argument handoff — is what fails first when it breaks. |
| F2 | P2 | `.opencode/bin/tests/check-git-hooks.test.sh:19,34` vs `.opencode/bin/check-git-hooks.sh:59` | The new harness pins only the global layer (`GIT_CONFIG_GLOBAL=/dev/null`, `:19`) and, unlike the older harnesses, never sets `core.hooksPath` inside its fixture (compare `pre-commit.test.sh:43`, `pre-push.test.sh:34`). The check derives its comparison target from `git -C "$REPO_ROOT" rev-parse --git-path hooks` (`check-git-hooks.sh:59`), which also consults the system config, so on a host whose `/etc/gitconfig` sets a global `core.hooksPath`, case 1 compares the fixture's symlink against the wrong hooks directory, prints the "Invalid git hook symlink(s)" warning, and fails its silence expectation — a latent, machine-dependent pass that happens to hold here. | In `setup_repo` (`check-git-hooks.test.sh:25`), pin `git -C "$TMP" config core.hooksPath "$TMP/.git/hooks"` (all four cases stay correct under it), or export `GIT_CONFIG_NOSYSTEM=1` beside the `/dev/null` pin. |

## Decisions

1. **Sound** — `pre-commit:63-66` tests `SPECKIT_SKIP_COMMENT_HYGIENE` where the replaced `-d .opencode/skills/sk-code` condition ignored it, so the documented bypass now actually bypasses, and a bypassed gate with no checker left legitimately runs nothing; the collateral (sk-code-dir → sentinel) only changes checkouts the rules no longer call toolchain.
2. **Sound** — the `cd -P` root (`pre-commit:327`) feeding the activation root, the authored-copy strip (`:341`) and the physical `git add` (`:435`) matches the observed "beyond a symbolic link" failure, and test 31 (`pre-commit.test.sh:986-993`) proves the 2-manifest count through a real link where the old `.opencode`-path staging fails.
3. **Sound** — the OR-sentinel entries (`pre-commit:310`, `:509`) stop blocking repositories that never had the route modules or the re-derive tool, while inside the toolchain a missing module still blocks naming it (`pre-commit:315-324`, `:562-566`); tests 33-35 cover both directions.
4. **Sound** — both reversals (`pre-push:124-129`, `:168-180`) fail closed only where the sentinel marks a broken install, keep their existing approval variables, leave the broken-but-present source fail-open (`pre-push:84-90`) and the release/live exemptions intact (`pre-push:156-158`, `:168-172`; the release case passes with no approval, `pre-push.test.sh:1176-1178`); the priced-in consequence is that an allowlisted update (`main`) now wants `SPECKIT_ALLOW_REMOTE_PUSH=1` when only the naming script is gone, because the built-in allowlist lives in the missing file.
5. **Sound** — `post-commit:46,59-60`: the default-enabled publish (the documented "absent resolver means enabled", `post-commit:45`) now warns that the kill switch is dead and proceeds — the right polarity for a side that never blocks — and the missing-publisher warning (`:69-70`) says what did not happen.

Tool calls: read /private/tmp/claude-501/-Users-michelkerkmeester-MEGA-Development-Code-Environment-Public/6d11af6f-653e-4807-aca8-1c09c81640c1/scratchpad/p005/hook-rules.diff; ls specs/system-speckit/033-system-speckit-v4/041-skilled-source-root-migration/005-gate-and-ci-readiness; read /private/tmp/claude-501/-Users-michelkerkmeester-MEGA-Development-Code-Environment-Public/6d11af6f-653e-4807-aca8-1c09c81640c1/scratchpad/p005/hook-rules.diff; read .opencode/bin/check-git-hooks.sh; read .opencode/scripts/git-hooks/pre-push; read .opencode/scripts/git-hooks/pre-commit; read .opencode/scripts/git-hooks/post-commit; read .opencode/scripts/git-hooks/tests/pre-commit.test.sh; read .opencode/scripts/git-hooks/tests/pre-push.test.sh; read .opencode/hooks/git/pre-commit; grep .opencode/skills/system-deep-loop/deep-improvement/scripts/check-agent-mirror-sync.cjs; read .opencode/skills/system-deep-loop/deep-improvement/scripts/check-agent-mirror-sync.cjs; grep .opencode/scripts/git-hooks/lib/autostash-orphan-guard.sh; read .opencode/scripts/git-hooks/lib/autostash-orphan-guard.sh; grep .opencode/scripts/git-hooks/prepare-commit-msg; grep .opencode/scripts/git-hooks/tests/prepare-commit-msg.test.sh; read specs/system-speckit/033-system-speckit-v4/041-skilled-source-root-migration/005-gate-and-ci-readiness/implementation-summary.md

Pi exit 0, 2026-09-16T22:04:49Z to 2026-09-16T22:31:13Z, --mode json, tools read,grep,find,ls.
