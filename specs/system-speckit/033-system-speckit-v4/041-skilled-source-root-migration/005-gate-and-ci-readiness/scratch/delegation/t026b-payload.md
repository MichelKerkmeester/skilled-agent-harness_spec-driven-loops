# Edits for unit t026b

Each edit names a file, an OLD block and a NEW block. The block is the text between its two fence lines, without the fence lines themselves.

## Edit 1

File: `.opencode/scripts/git-hooks/tests/README.md`

OLD:

~~~~text
must anchor a leftover `autostash` stash entry. |
~~~~

NEW:

~~~~text
must anchor a leftover `autostash` stash entry. Where the toolchain ships, each of the three hooks warns when the guard library is missing, and an autosync commit warns about a missing kill switch or publisher. Under a linked source root the guard still anchors and logs under `.skilled/logs`. |
~~~~

## Edit 2

File: `.opencode/scripts/git-hooks/tests/README.md`

OLD:

~~~~text
must re-derive rather than block. |
~~~~

NEW:

~~~~text
must re-derive rather than block. Source-root cases stage `.skilled/` paths through each filter, re-mint a hub through a linked source root, block on each missing gate script where the toolchain ships and keep a repository without the toolchain committable. The legacy hygiene helper gets the same cases. |
~~~~

## Edit 3

File: `.opencode/scripts/git-hooks/tests/README.md`

OLD:

~~~~text
Exercises the owner-first branch naming gate, migration tolerance, release branches and the explicit bypass. |
~~~~

NEW:

~~~~text
Exercises the owner-first branch naming gate, migration tolerance, release branches and the explicit bypass. A range under `.skilled/skills` reaches the skill-root metadata checker, and routing drift under `.skilled/` blocks. Where the toolchain ships, a missing permission script, deletion library or route guard blocks while release branches and approvals still pass. |
~~~~

## Edit 4

File: `.opencode/scripts/git-hooks/tests/README.md`

OLD:

~~~~text
and a real commit through both hooks keeps the stamped id. |
~~~~

NEW:

~~~~text
and a real commit through both hooks keeps the stamped id. Where the toolchain ships, a missing allocator warns once and leaves the message unstamped, and an allocator under a linked source root still stamps. |
~~~~
