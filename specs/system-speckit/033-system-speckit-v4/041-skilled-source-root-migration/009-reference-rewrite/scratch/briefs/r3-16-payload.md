Columns: path, line, column, hash, class and detail from the rule, text of the line.

~~~~tsv
.skilled/skills/system-spec-kit/runtime/hooks/lib/workspace/README.md	16	187	487cc4faa4f6c861	R3:directory	`lib/workspace/` holds one file: a repository-root resolver that any runtime writer can call before it persists state. Deriving a write root from `process.cwd()` instead plants a nested `.opencode/` tree wherever the process happened to run
.skilled/skills/system-spec-kit/runtime/hooks/lib/workspace/README.md	21	100	dfb6b5a8c717dbdb	R3:segment	- If the walk exhausts without finding the sentinel, it falls back to hoisting above the outermost `.opencode` segment in the starting path, so a candidate that is already inside an `.opencode` tree can never resolve to a subtree of itself.
.skilled/skills/system-spec-kit/runtime/hooks/lib/workspace/README.md	21	183	dfb6b5a8c717dbdb	R3:segment	- If the walk exhausts without finding the sentinel, it falls back to hoisting above the outermost `.opencode` segment in the starting path, so a candidate that is already inside an `.opencode` tree can never resolve to a subtree of itself.
.skilled/skills/system-spec-kit/runtime/hooks/lib/workspace/README.md	22	154	c5ead765b6a3ac82	R3:entry:...	- The sentinel is a real authored file, not a bare directory, because a directory sentinel is self-perpetuating: once a buggy caller creates `<wrong-dir>/.opencode/...`, every later walk-up from that subtree finds it and returns the wrong r
.skilled/skills/system-spec-kit/runtime/hooks/lib/workspace/README.md	39	92	3565aadda97ecc77	R3:segment	| `hoistAboveOpencodeTree(dir)` | Function | Return the directory containing the outermost `.opencode` segment in `dir`, or `null` when `dir` is not inside an `.opencode` tree. Used as `findRepoRoot`'s fallback. |
.skilled/skills/system-spec-kit/runtime/hooks/lib/workspace/README.md	39	160	3565aadda97ecc77	R3:segment	| `hoistAboveOpencodeTree(dir)` | Function | Return the directory containing the outermost `.opencode` segment in `dir`, or `null` when `dir` is not inside an `.opencode` tree. Used as `findRepoRoot`'s fallback. |
.skilled/skills/system-spec-kit/runtime/hooks/lib/workspace/README.md	71	26	139bc7a50e9c7fca	R3:bare	│ directory          │  │ .opencode segment, or return  │
.skilled/skills/system-spec-kit/runtime/lib/utils/README.md	24	199	b90711d6be4f89c5	R3:directory	- `index-scope.ts` is the single source of truth for what is in scope for memory indexing, generated-metadata derivation, and code-graph scanning, and for the code-graph inclusion policy that gates `.opencode/` skills, agents, commands, spe
.claude/SYNC.md	3	39	2c257e59a4a303f1	R3:bare	description: "How .claude derives from .opencode: which surfaces are symlinks, which is a guarded fork, which are hand-authored, and how to detect drift."
.claude/SYNC.md	8	3	f1075b1218772250	R3:directory	> `.opencode/` is the source of truth for shared content. `.claude/` is mostly a set of whole-directory or generated per-file symlinks onto it — with one deliberate content fork, `agents/`, held in step by a pre-commit gate.
.claude/SYNC.md	34	59	a4c9e6696fb903f6	R3:directory	| `hooks/*` (18 symlinks) | per-file symlinks | scattered `.opencode/**` | Yes — guarded by the mirror generator |
.claude/SYNC.md	41	90	0b8d1eb5f94d0785	R3:entry:...	`hooks/` is a **discovery mirror only**. Every command string in `settings.json` targets `.opencode/...` directly, so the symlinks exist for humans and tooling to find adapters, not for execution. See `hooks/README.md`.
.claude/SYNC.md	58	49	334bf10696902e5f	R3:bare	# 1. Verify the agent fork is still aligned with .opencode (this is the pre-commit gate)
.claude/hooks/README.md	3	147	b256784f137cfa3f	R3:bare	description: "Discovery-only symlinks for the repository's Claude hook scripts; runtime wiring remains in .claude/settings.json and points to real .opencode paths."
.claude/hooks/README.md	11	67	819d43026259c281	R3:directory	`.claude/hooks/` contains 21 symlinks to hook scripts owned under `.opencode/`. Most target their owning skill under `.opencode/skills/`; the six fully-portable guard cores (`dispatch-preflight-lint.mjs`, `dispatch-audit-posttooluse.mjs`, `
.codex/SYNC.md	3	38	1c373715bcbdf989	R3:bare	description: "How .codex derives from .opencode: the two generators that own agents and prompts, the outbound hooks installer, and how to detect drift."
.codex/SYNC.md	16	152	0637b10bdb0777a1	R3:directory	First, it cannot consume the shared trees directly: it needs TOML agents and a flat prompt namespace. So instead of symlinks, two generators transform `.opencode/` into Codex's dialect. Both own their output directory completely — they dele
.codex/SYNC.md	30	59	bd11db7023ebbeac	R3:directory	| `hooks/*` (16 symlinks) | per-file symlinks | scattered `.opencode/**` | Yes — mirror generator |
.codex/SYNC.md	117	285	2dc48533856bce17	R3:directory	**Orphaned entries.** Ownership is keyed on the adapter path, so renaming a hook script leaves the old entry unrecognised in `~/.codex/hooks.json` — where the preserve-third-party rule would keep it forever, invoking a file that no longer e
.codex/SYNC.md	117	368	2dc48533856bce17	R3:directory	**Orphaned entries.** Ownership is keyed on the adapter path, so renaming a hook script leaves the old entry unrecognised in `~/.codex/hooks.json` — where the preserve-third-party rule would keep it forever, invoking a file that no longer e
.codex/hooks/README.md	3	142	756c649f25caddc7	R3:bare	description: "Discovery-only symlinks for the repository's Codex hook scripts; runtime wiring remains in .codex/hooks.json and points to real .opencode paths."
.codex/hooks/README.md	11	66	ce1a818e15f48140	R3:directory	`.codex/hooks/` contains 18 symlinks to hook scripts owned under `.opencode/`. `mcp-route-guard.cjs`, `dispatch-preflight-lint.mjs`, `dispatch-audit-posttooluse.mjs`, and `post-edit-quality.cjs` target `.opencode/hooks/` (the fully-portable
.cursor/SYNC.md	3	39	bb6838fe23b3eb5d	R3:bare	description: "How .cursor derives from .opencode and .claude: symlinked agents and commands, the hand-authored rules file Devin also reads, and how to detect drift."
.cursor/SYNC.md	33	40	c6a97bb57bb95588	R3:directory	| `hooks/*` (15) | symlink | scattered `.opencode/**` | discovery mirror only |
.cursor/hooks/README.md	3	144	42024816a62d90d3	R3:bare	description: "Discovery-only symlinks for the repository's Cursor hook scripts; runtime wiring remains in .cursor/hooks.json and points to real .opencode paths."
.devin/SYNC.md	3	38	57e0b0d751517090	R3:bare	description: "How .devin derives from .opencode and .claude: the nested agent symlink shape, the strict-YAML constraint, inherited rules, and how to detect drift. Devin carries no mirrored command surface."
.devin/SYNC.md	30	35	a386cb5a80281f87	R3:directory	| `hooks/*` | symlink | scattered `.opencode/**` | discovery mirror only |
.devin/SYNC.md	100	9	a06a02d656c3817e	R3:directory	- Every `.opencode/**` script `hooks.v1.json` invokes has a matching symlink in `hooks/`.
.devin/hooks/README.md	3	138	810e0df6f882df78	R3:bare	description: "Discovery-only symlinks for Devin-specific hook adapters; runtime wiring remains in .devin/hooks.v1.json and points to real .opencode paths."
.devin/hooks/README.md	11	77	1396ef3c2fb5d73e	R3:directory	`.devin/hooks/` contains 19 relative symlinks to Devin adapters owned under `.opencode/`. `mcp-route-guard.cjs`, `dispatch-preflight-lint.mjs`, `dispatch-audit-posttooluse.mjs`, `post-edit-quality.cjs`, and `task-dispatch-guard.cjs` target 
.hermes/SYNC.md	3	39	496580415f00f153	R3:bare	description: "How .hermes derives from .opencode: curated per-skill symlinks, generated prompt templates, one project plugin bridging the shared guard cores, and the operator steps Hermes keeps user-level."
.hermes/SYNC.md	26	88	a96c7fd81c606cf0	R3:directory	| `plugins/repo-guards/` | **hand-authored** project plugin | shared guard cores under `.opencode/**` | Behavioral drift only; `hermes plugins validate` checks the manifest |
.hermes/SYNC.md	52	37	ef6a43041f5463ea	R3:directory	- Guard-core behavior changes under `.opencode/**` → review `plugins/repo-guards/__init__.py` by hand; it shells out to the cores, so a renamed core path is the drift to watch.
.hermes/plugins/repo-guards/__init__.py	7	59	59f6236ea0db7444	R3:directory	of them: each hook shells out to the existing core under ``.opencode/`` with the same JSON payload
.pi/SYNC.md	3	35	b5ab05cf0a05bb29	R3:bare	description: "How .pi derives from .opencode: two generators for agents and prompts, hand-authored extensions bridging the shared guard cores, and how to detect drift."
.pi/SYNC.md	16	25	c5de2781b17dd59d	R3:runtime-view	Canonical for agents is `.opencode/agents/` — the same upstream Codex uses, *not* the `.claude/agents/` fork that Cursor and Devin symlink. The generator maps OpenCode's `permission:` block onto Pi's built-in tool names, so a `.pi/agents/*.
.pi/SYNC.md	29	91	4bb84ed654dce07e	R3:directory	| `extensions/*.ts` + `lib/` | **hand-authored** guard bridges | shared guard cores under `.opencode/**` | Behavioral drift only; no checker |
.pi/SYNC.md	45	37	34affc0a7ddef33b	R3:directory	- Guard-core behavior changes under `.opencode/**` → review `extensions/*.ts` by hand; nothing regenerates them.
.pi/extensions/README.md	75	25	90ab8be4e5ab5f9f	R3:directory	Paths without a leading `.opencode/` are relative to `.opencode/skills/`. The four `.opencode/hooks/` cores are the fully-portable guard cores relocated out of their owning skill; see [`../../.opencode/hooks/README.md`](../../.opencode/hook
.pi/extensions/README.md	98	85	501b5e4a2e37736c	R3:directory	| Ownership | Guard decisions belong to the shared `.mjs`/`.cjs` core modules under `.opencode/`. Session-lifecycle decisions belong to the Claude hook dist files under `system-spec-kit/runtime/dist/hooks/claude/`. These files own only even
~~~~
