# Review ledger: links and generated state (2026-09-17)

| Review | Finding | Severity claimed | Disposition | Evidence |
|--------|---------|------------------|-------------|----------|
| hooks | H-001 `.skilled/` hook commands break consumer checkouts that expose only `.opencode` | P0 | Answered by design, handoff to 010 | ADR-003 keeps `.opencode` only for opencode, root discovery, the consumer contract, the spec link, dual-root alternates and frozen records (`004-migration-design/decision-record.md:298`). Phase 010 creates an untracked `.skilled` link beside `.opencode` in every consumer root on this machine and installs the Codex hooks under `.skilled/` (`010-machine-and-consumer-cutover/spec.md:114`, `:133`, `:158`). Claude, Cursor and Devin hooks are project-level and never run in consumer projects, whose documented layout holds only `.opencode`, `.opencode-local`, `opencode.json` and `AGENTS.md` (`PUBLIC-RELEASE.md:21-28`). Nothing from this phase reaches a consumer or a home config, so the ordering hazard is 010's: consumer `.skilled` links before the global Codex install |
| hooks | H-002 the mirror generator finds no hook path if run before the registrations are regenerated | P2 | No change | The runbook runs `sync-hook-registrations.cjs` before `sync-runtime-mirrors.cjs`, and the mirror check after the write requires 168 mirrors |
| hooks | H-003 the Gate 1 pointer intro conditions Gate 1 on the source tree path | P1 | No change, pre-existing | `git show 1464a85667:.skilled/skills/system-spec-kit/runtime/cli/runtime-mirrors/sync-gate1-pointers.cjs` line 31 already conditioned the intro on `.opencode/skills/system-spec-kit`. The edit only adds the `.skilled` spelling |
| runtime copies | F-001 the Hermes agent header names `.hermes/agents/<file>` while the generator reads `.skilled/agents` | P1 | No change, pre-existing | The header text is independent of `AGENTS_DIR` at the start commit, and `.hermes/agents` is a link that resolves to the same file |
| runtime copies | F-002 `validate-command-references.cjs` expects `.opencode/commands` in the Codex prompt headers | P1 | Fixed in unit u23 | The inventory, the mirror-name mapping and the orphan pointer now accept either root, preferring `.skilled/commands` |
| contracts and routing | SRM-001 a retained publication spelled `.opencode` fails textual comparisons after `RUNTIME_ROOT` moves to `.skilled` | P1 | Not reachable here, handoff to 010 | No rollback sibling, lock or publication state exists under `.skilled/bin/lib` in the worktree or under the main checkout's `.opencode/bin/lib`. Phase 010 confirms none exists before the main checkout takes the move |
| derived and retrieval | F-001 `init-skill-graph.sh` cuts `pwd -P` at `/.opencode/`, so the repo root is wrong since the move | P1 | Routed to 009 | A break caused by the move commit, not by this phase's edits, in a script outside this phase's scope. The same class holds `permissions-gate.ts:243`, `sweep-memory-residue.mjs:307` and `alignment-validator.ts:382` |
| derived and retrieval | F-002 dist-freshness roots under `.skilled` miss in a consumer checkout | P1 | Answered by design, handoff to 010 | As H-001 |
| derived and retrieval | F-003 corpus and ripgrep roots under `.skilled` narrow or error in a consumer checkout | P1 | Answered by design, handoff to 010 | As H-001 |
| links | none | - | - | Safe as planned |
| command references | CMD-001 the validator inventories `goal-opencode.md` and `vision.md`, which the prompt generator excludes, and reports them missing | P1 | No change, pre-existing | At `1464a85667` the inventory already walked every command document without the generator's exclusion. The edit only chooses the root, so phase 009 takes the exclusion with the validator's text patterns |
