## Verdict

Not safe to run as edited. The graph initializer fails after the source move, while `.opencode`-only consumer checkouts can make dist freshness fail and retrieval silently narrow its corpus or error (`init-skill-graph.sh:15-18,57`; `dist-freshness.cjs:154-155,629-636`; `corpus.mjs:161-168`; `rg-wrapper.mjs:191-198`).

## Findings

| ID | Severity (P0 blocks the write run, P1 must fix before the write run, P2 should fix) | File:line | Scenario (the inputs and the wrong outcome) | Suggested fix |
|---|---|---|---|---|
| F-001 | P1 | `.skilled/skills/system-skill-advisor/runtime/scripts/init-skill-graph.sh:15-18,57` | Run `bash .opencode/skills/system-skill-advisor/runtime/scripts/init-skill-graph.sh` from the repository. `pwd -P` yields a `.skilled` path, so the `/.opencode/*` pattern does not match; `REPO_ROOT` remains the scripts directory and the compiler call targets a nonexistent nested `.opencode` path. | Derive the repository root with a resolver that handles both `/.skilled/` and `/.opencode/`, or use the shared source-root resolver. |
| F-002 | P1 | `.skilled/skills/system-spec-kit/runtime/cli/lib/dist-freshness.cjs:28-30,154-155,629-636`; `.skilled/skills/sk-code/sk-code-quality/scripts/check-dist-staleness.sh:127-143` | In a consumer checkout containing only `.opencode` and no `.skilled`, run `check --package system-spec-kit/runtime --workspace-root /consumer`. It resolves `/consumer/.skilled/...`, reports the package root missing, and an attempted stale-dist rebuild executes a `.skilled`-relative command that cannot resolve from `/consumer`. | Resolve package roots and rebuild commands through `.skilled` with `.opencode` fallback, using the resolved root rather than a hard-coded directory. |
| F-003 | P1 | `.skilled/skills/system-spec-kit/runtime/cli/retrieval/lib/corpus.mjs:31,161-168`; `.skilled/skills/system-spec-kit/runtime/cli/retrieval/generate-trigger-index.mjs:27,115-124,492-498`; `.skilled/skills/system-spec-kit/runtime/cli/retrieval/lib/rg-lane.mjs:61,152-174`; `.skilled/skills/system-spec-kit/runtime/cli/retrieval/rg-wrapper.mjs:67,191-198` | With `/consumer/.opencode` available but `/consumer/.skilled` absent, invoke the generator with `--repo-root /consumer`: the three `.skilled` corpus roots are skipped and a narrowed index is published. Run the ripgrep wrapper from `/consumer`: it passes the missing `.skilled` root to ripgrep and returns an error instead of finding files through `.opencode`. | Select `.skilled` when present and `.opencode` otherwise for corpus and ripgrep roots; add an alias-only consumer fixture and fail closed if required roots remain unavailable. |
Codex exit 0, 2026-09-17T14:16:12Z to 2026-09-17T14:29:00Z, --model gpt-5.6-luna, reasoning xhigh, service tier fast, --sandbox read-only.
