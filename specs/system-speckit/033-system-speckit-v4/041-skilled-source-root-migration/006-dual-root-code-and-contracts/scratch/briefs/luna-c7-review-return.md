## Verdict

Not ready: the resolver changes cover ordinary layouts, but capped-root handling, legacy aliasing, and several callers still produce incorrect roots or paths in supported scenarios.

## Findings

| ID | Severity (P0 blocks the phase, P1 must fix, P2 should fix) | File:line | Scenario (the inputs and the wrong outcome) | Suggested fix |
|---|---|---|---|---|
| F1 | P1 | `.opencode/skills/system-skill-advisor/runtime/lib/utils/workspace-root.ts:33-36`; `.opencode/skills/system-spec-kit/shared/workspace/repo-root.mjs:45-48` | With sentinel `.opencode/specs/marker`, only `.skilled/specs/marker` exists, and start is under `.skilled/skills`, both resolvers accept the forbidden `.skilled/specs` spelling and return the repository root. | Do not dual-spell `.opencode/specs`; preserve the legacy alias literally. |
| F2 | P1 | `.opencode/skills/system-skill-advisor/runtime/lib/utils/workspace-root.ts:122`; `.opencode/skills/system-spec-kit/shared/workspace/repo-root.mjs:112`; `runtime/lib/daemon/watcher.ts:132` | Repository `/tmp/.skilled/outer/repo` has its sentinel under `repo/.opencode`; `findAdvisorWorkspaceRoot(repo, {maxDepth: 0})` returns `/tmp`, so the watcher uses `/tmp/.opencode/skills` instead of the repository’s skills directory. | Ensure a capped walk honors a sentinel at the supplied start root, or avoid passing an explicit repository root through the capped resolver. |
| F3 | P1 | `.opencode/skills/system-skill-advisor/runtime/lib/daemon/watcher.ts:128-132` | In a `.skilled`-only checkout with `repo/.skilled/skills/alpha`, `defaultSkillsRoot(repo)` returns `repo/.opencode/skills`; discovery finds no skills and the watcher monitors the wrong tree. | Derive the skills path from the real source-root spelling. |
| F4 | P1 | `.opencode/skills/system-skill-advisor/runtime/handlers/advisor-validate.ts:218-220` | In a `.skilled`-only checkout, the resolver finds `repo`, but the caller checks only `repo/.opencode/...`; it then returns `process.cwd()` instead of `repo` (and analogous benchmark callers fall back or throw). | Validate both sentinel spellings or use the resolver result without a single-spelling post-check. |
| F5 | P2 | `.opencode/skills/system-skill-advisor/runtime/tests/schemas/advisor-tool-schemas.vitest.ts:203-220` | The `deepStart` case passes on the pre-change implementation because the old `.opencode`-only hoist already returns `nestedRepo`; it does not prove the new capped ancestor fallback. | Add a fixture where the old outermost hoist differs, such as an inner `.skilled` tree beneath an outer `.opencode`. |
| F6 | P2 | `.opencode/skills/system-skill-advisor/runtime/lib/utils/workspace-root.ts:86-91`; `runtime/schemas/advisor-tool-schemas.ts:29-33` | The workspace-root documentation says fallback returns `resolve(start)`, although a start under `.opencode` now hoists; the schema comment names a bare `.opencode/skill` sentinel while the code checks `skills/system-spec-kit/SKILL.md`. | Update both comments to describe the current fallback and sentinel precisely. |
Codex exit 0, 2026-09-17T10:22:53Z to 2026-09-17T10:32:43Z, --model gpt-5.6-luna, reasoning xhigh, service tier fast, --sandbox read-only.
