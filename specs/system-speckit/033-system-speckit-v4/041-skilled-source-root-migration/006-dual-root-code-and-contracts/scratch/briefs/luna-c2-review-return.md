## Verdict

Request changes. The new layout coverage contains false-green assertions at `.opencode/skills/system-speckit/runtime/cli/tests/test-folder-detector-functional.js:589-600` and does not exercise migration manifest or migration behavior at `.opencode/skills/system-speckit/runtime/cli/tests/spec-root-validation-matrix.vitest.ts:212-235`.

## Findings

| ID | Severity (P0 blocks the phase, P1 must fix, P2 should fix) | File:line | Scenario (the inputs and the wrong outcome) | Suggested fix |
|---|---|---|---|---|
| F1 | P1 | `.opencode/skills/system-speckit/runtime/cli/tests/test-folder-detector-functional.js:589-600` | For `layout='skilled-only'` or `'whole-link'`, use R3 and pass `.skilled/specs/${fixture.relativePacketId}`. Any thrown error, including an unrelated detector failure, is converted into `pass`, so the row is green without producing the expected canonical result. | Accept only the documented unsupported error, or rethrow unexpected errors. |
| F2 | P1 | `.opencode/skills/system-speckit/runtime/cli/tests/test-folder-detector-functional.js:579-584`; `.opencode/skills/system-speckit/runtime/cli/tests/test-folder-detector-functional.js:944-949` | For R3 in `skilled-only`, the absolute path `workspace/.skilled/specs/${fixture.relativePacketId}` passes whenever containment resolves to canonical, but it also passes if production incorrectly adds `.skilled/specs` as an approved root. The boolean assertion cannot distinguish those outcomes. | Assert the canonicalized returned path, or add a real `.skilled/specs` decoy and verify it is rejected as a root. |
| F3 | P1 | `.opencode/skills/system-speckit/runtime/cli/tests/spec-root-validation-matrix.vitest.ts:212-235`; `.opencode/skills/system-speckit/runtime/cli/tests/spec-root-migration-manifest.vitest.ts:52-168`; `.opencode/skills/system-speckit/runtime/cli/tests/spec-root-migration.vitest.ts:64-194` | Running R1, R3, or R7 under `skilled-only` or `whole-link` never calls `buildMigrationManifest` or migration functions. A regression that enumerates `.skilled/specs` or moves the wrong alias therefore leaves all new layout rows green. | Parameterize manifest and migration tests with the three layouts and assert the expected root classification and migration result. |
| F4 | P1 | `.opencode/skills/system-speckit/runtime/cli/tests/spec-root-validation-matrix.vitest.ts:219-220`; `.opencode/skills/system-speckit/runtime/cli/core/spec-root-fixtures.ts:149-151`; `.opencode/skills/system-speckit/runtime/cli/tests/test-folder-detector-functional.js:565-567` | In `skilled-only` with R3, `detectSpecFolder('.skilled/specs/${fixture.relativePacketId}')` can return the canonical path through its fallback search, so the comment claiming the missing `.opencode` path hides the legacy entry from “every resolver” is inaccurate. The added comments also embed source/spec paths. | Remove path-specific comment labels and state the narrower durable rule, limited to canonical and legacy root enumeration. |
| F5 | P2 | `.opencode/skills/system-speckit/runtime/cli/core/spec-root-fixtures.ts:158-165,243-245` | Calling `materializeRootFixture(R3, 'whole-link')` where symlink creation is denied throws during setup before the cleanup handler is installed, leaving the `spec-root-fixture-*` temporary directory behind. | Put all temporary setup inside a cleanup-protected `try/finally`, including symlink creation. |
Codex exit 0, 2026-09-17T12:06:46Z to 2026-09-17T12:19:16Z, --model gpt-5.6-luna, reasoning xhigh, service tier fast, --sandbox read-only.
