# Iteration 6: Documentation surface

## Focus

Classify path mentions in the repository's instruction, installation, runtime-sync and
release documents as load-bearing contracts versus prose or historical records.

## Findings

### Surface: 8, documentation surface

- **Finding:** Root `AGENTS.md` uses `.opencode` in mandatory gate commands, the skill-advisor front door, completion validation, active agent directories, command/skill discovery and CLI dispatch instructions. These are executable operating instructions, not descriptive prose. **Classification:** `blocker`. **Consequence for the cutover:** The gate and routing instructions must point at the consumer path that remains valid for the intended runtime, or they will direct future sessions to missing commands and stale source roots. This is the minimum load-bearing documentation class.
  - [SOURCE: `AGENTS.md:45-65`]
  - [SOURCE: `AGENTS.md:79-91`]
  - [SOURCE: `AGENTS.md:172-174`]
  - [SOURCE: `AGENTS.md:267-282`]

- **Finding:** `README.md` and `CONTRIBUTING.md` contain copy-paste installation, build, retrieval and validation commands that execute `.opencode` paths, and the README describes `.opencode/skills` as the place where users add skills. **Classification:** `mechanical`. **Consequence for the cutover:** These command examples and links must be rewritten or deliberately retained as compatibility-facing `.opencode` paths; leaving them stale makes a fresh user follow commands that no longer reach the real source tree.
  - [SOURCE: `README.md:94-116`]
  - [SOURCE: `README.md:129-131`]
  - [SOURCE: `README.md:238-256`]
  - [SOURCE: `README.md:311-315`]
  - [SOURCE: `CONTRIBUTING.md:31-48`]

- **Finding:** `PUBLIC-RELEASE.md` defines `.opencode/` as the public framework source, says consuming projects symlink their `.opencode/` to the public checkout, and documents component locations under `.opencode/skills`, `.opencode/commands`, `.opencode/scripts` and `.opencode/agents`. It also records the constraint that Git cannot track content behind a symlink. **Classification:** `manual`. **Consequence for the cutover:** This is an external packaging contract, not merely stale wording. The document must distinguish the new real source root from the still-public `.opencode` consumer surface, and its symlink/Git constraints must be reconciled with the chosen compatibility topology.
  - [SOURCE: `PUBLIC-RELEASE.md:1-3`]
  - [SOURCE: `PUBLIC-RELEASE.md:10-36`]
  - [SOURCE: `PUBLIC-RELEASE.md:53-66`]

- **Finding:** Runtime sync documents describe which positions are symlinks, which are generated copies, which are real forks, and which commands check or regenerate them. Claude's sync document names `.opencode/commands`, `.opencode/skills`, the mirror generator and mirror checks as operational contracts. **Classification:** `mechanical`. **Consequence for the cutover:** Sync manifests and their command examples must be updated with the generator's new source root while preserving each runtime-native destination; changing only the prose leaves the checks and generated topology inconsistent.
  - [SOURCE: `.claude/SYNC.md:24-41`]
  - [SOURCE: `.claude/SYNC.md:59-65`]
  - [SOURCE: `.claude/SYNC.md:102-104`]

- **Finding:** Generated Codex and Pi prompt files embed the original `.opencode/commands/<path>` source location in a “generated by” header and body, while the generator implementation owns those values. **Classification:** `regenerate`. **Consequence for the cutover:** These markdown files are not independent prose edits; they must be regenerated after the source-path renderer changes, or the mirror checks will report stale generated content and users will be sent to the old canonical path.
  - [SOURCE: `.codex/prompts/create-agent.md:1-6`]
  - [SOURCE: `.pi/prompts/create-changelog.md:1-6`]
  - [SOURCE: `.opencode/skills/system-spec-kit/runtime/cli/codex/sync-prompts.cjs:82-99`]

- **Finding:** Historical changelogs intentionally retain old directory names as point-in-time records; the deep-loop migration changelog explicitly distinguishes current-state documentation from historical references. **Classification:** `manual`. **Consequence for the cutover:** Historical records should not be bulk-rewritten merely to make counts zero. The cutover design must separate instructions and live links from historical evidence, otherwise it destroys chronology and makes the documentation audit less trustworthy.
  - [SOURCE: `.opencode/skills/system-deep-loop/runtime/changelog/v1.3.0.0.md:21-27`]
  - [SOURCE: `.opencode/skills/system-deep-loop/runtime/changelog/v1.3.0.0.md:43-56`]

## Sources Consulted

- Root policy and workflow instructions.
- README, contributor and public-release contracts.
- Runtime sync manifests, generated prompt files and historical changelog guidance.

## Assessment

- `newInfoRatio`: 0.88
- Novelty justification: The documentation surface divides into executable instructions, external packaging contracts, generated markdown and historical records; these require different treatments.
- Confidence: high for cited load-bearing documents; the full prose-versus-instruction classification of every markdown file remains UNKNOWN without a document-by-document review.

## Reflection

- Worked: focusing on commands, links, generators and runtime contracts rather than counting mentions alone.
- Ruled out: bulk rewriting all markdown references as a safe migration strategy.
- Failed: no defensible exact count of load-bearing versus prose mentions can be derived from the supplied aggregate alone.

## Recommended Next Focus

Inspect outside-repository runtime configuration, installed hooks and home-level references that a repository move cannot update.
