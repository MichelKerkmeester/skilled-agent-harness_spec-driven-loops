## runtime:claude — 17 files

| file | `.opencode` lines | what the reference does | origin | needed change | class |
|---|---|---|---|---|---|
| `.claude/SYNC.md` | :3,8,18,26,27,28,29,30 (+13 more) | documents the runtime's derivation contract | authored | rewrite: the manifest describes `.opencode`-as-source; post-move it must describe `.skilled` as source and `.opencode` as consumer | manual |
| `.claude/agents/README.txt` | :8,11 | documents the fork and its `.opencode/agents` sibling + crosswalk | authored | mechanical rewrite of the two refs | mechanical |
| `.claude/agents/ai-council.md` | :21,111,114,408,630,650,683,704 (+10 more) | forked agent body; `.opencode` refs are doc/command paths inside the body | authored fork — kept aligned to canonical agents by the pre-commit mirror gate | mechanical rewrite of body refs; the gate's upstream becomes `.skilled/agents` | mechanical |
| `.claude/agents/code.md` | :28,51,58,368,436,500,510 | forked agent body; `.opencode` refs are doc/command paths inside the body | authored fork — kept aligned to canonical agents by the pre-commit mirror gate | mechanical rewrite of body refs; the gate's upstream becomes `.skilled/agents` | mechanical |
| `.claude/agents/context.md` | :57,61,153,176,415,416,417 | forked agent body; `.opencode` refs are doc/command paths inside the body | authored fork — kept aligned to canonical agents by the pre-commit mirror gate | mechanical rewrite of body refs; the gate's upstream becomes `.skilled/agents` | mechanical |
| `.claude/agents/debug.md` | :342,645,646 | forked agent body; `.opencode` refs are doc/command paths inside the body | authored fork — kept aligned to canonical agents by the pre-commit mirror gate | mechanical rewrite of body refs; the gate's upstream becomes `.skilled/agents` | mechanical |
| `.claude/agents/deep-improvement.md` | :70,230,231,232,233,234 | forked agent body; `.opencode` refs are doc/command paths inside the body | authored fork — kept aligned to canonical agents by the pre-commit mirror gate | mechanical rewrite of body refs; the gate's upstream becomes `.skilled/agents` | mechanical |
| `.claude/agents/deep-research.md` | :95,265,338,562,563,564,565 | forked agent body; `.opencode` refs are doc/command paths inside the body | authored fork — kept aligned to canonical agents by the pre-commit mirror gate | mechanical rewrite of body refs; the gate's upstream becomes `.skilled/agents` | mechanical |
| `.claude/agents/deep-review.md` | :175,228,265,278,279,280,297,360 (+5 more) | forked agent body; `.opencode` refs are doc/command paths inside the body | authored fork — kept aligned to canonical agents by the pre-commit mirror gate | mechanical rewrite of body refs; the gate's upstream becomes `.skilled/agents` | mechanical |
| `.claude/agents/design.md` | :80,90,106,117,119,121,222,223 (+5 more) | forked agent body; `.opencode` refs are doc/command paths inside the body | authored fork — kept aligned to canonical agents by the pre-commit mirror gate | mechanical rewrite of body refs; the gate's upstream becomes `.skilled/agents` | mechanical |
| `.claude/agents/markdown.md` | :124,179,180,181,182,183,184,185 (+15 more) | forked agent body; `.opencode` refs are doc/command paths inside the body | authored fork — kept aligned to canonical agents by the pre-commit mirror gate | mechanical rewrite of body refs; the gate's upstream becomes `.skilled/agents` | mechanical |
| `.claude/agents/orchestrate.md` | :21,155,177,198,397,467,650,817 (+1 more) | forked agent body; `.opencode` refs are doc/command paths inside the body | authored fork — kept aligned to canonical agents by the pre-commit mirror gate | mechanical rewrite of body refs; the gate's upstream becomes `.skilled/agents` | mechanical |
| `.claude/agents/prompt-improver.md` | :86,87,107,165,370,371,372,373 | forked agent body; `.opencode` refs are doc/command paths inside the body | authored fork — kept aligned to canonical agents by the pre-commit mirror gate | mechanical rewrite of body refs; the gate's upstream becomes `.skilled/agents` | mechanical |
| `.claude/agents/review.md` | :84,455,456 | forked agent body; `.opencode` refs are doc/command paths inside the body | authored fork — kept aligned to canonical agents by the pre-commit mirror gate | mechanical rewrite of body refs; the gate's upstream becomes `.skilled/agents` | mechanical |
| `.claude/hooks/README.md` | :3,11,46,67,68 | documents the mirror surface | authored | mechanical rewrite of prose refs | mechanical |
| `.claude/mcp.json` | :6 | registers the code_mode MCP server — `args`/`command` execs `.opencode/bin/mcp-code-mode-launcher.cjs` | authored | point the launcher arg at `.skilled/bin/mcp-code-mode-launcher.cjs` (or compat) | mechanical |
| `.claude/settings.json` | :23,28,33,43,53,63,73,85 (+14 more) | executes hooks — every `.opencode` ref sits in the generated `hooks` key command strings | authored file, generated `hooks` key — `sync-hook-registrations.cjs` from `hook-registry.json` `script` fields | regenerate the hooks key after the registry's script paths change (or keep `.opencode` via compat) | regenerate |
