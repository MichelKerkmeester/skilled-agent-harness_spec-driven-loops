### Map A section: opencode-internal-changelog (49 links)

| link | raw target | resolves into .opencode | required raw target — `.opencode` compat link survives | required raw target — no `.opencode` compat link | origin | class |
|---|---|---|---|---|---|---|
| `.opencode/changelog/cli-external-orchestration/cli-claude-code` | `../../skills/cli-external-orchestration/cli-claude-code/changelog` | yes | `unchanged (resolves through .opencode compatibility link)` | `unchanged (link and target travel together into .skilled/)` | hand-made index symlink (no generator found; changelog tooling only links/checks markdown) | mechanical |
| `.opencode/changelog/cli-external-orchestration/cli-codex` | `../../skills/cli-external-orchestration/cli-codex/changelog` | yes | `unchanged (resolves through .opencode compatibility link)` | `unchanged (link and target travel together into .skilled/)` | hand-made index symlink (no generator found; changelog tooling only links/checks markdown) | mechanical |
| `.opencode/changelog/cli-external-orchestration/cli-opencode` | `../../skills/cli-external-orchestration/cli-opencode/changelog` | yes | `unchanged (resolves through .opencode compatibility link)` | `unchanged (link and target travel together into .skilled/)` | hand-made index symlink (no generator found; changelog tooling only links/checks markdown) | mechanical |
| `.opencode/changelog/cli-external-orchestration/parent` | `../../skills/cli-external-orchestration/changelog` | yes | `unchanged (resolves through .opencode compatibility link)` | `unchanged (link and target travel together into .skilled/)` | hand-made index symlink (no generator found; changelog tooling only links/checks markdown) | mechanical |
| `.opencode/changelog/mcp-code-mode` | `../skills/mcp-code-mode/changelog` | yes | `unchanged (resolves through .opencode compatibility link)` | `unchanged (link and target travel together into .skilled/)` | hand-made index symlink (no generator found; changelog tooling only links/checks markdown) | mechanical |
| `.opencode/changelog/mcp-tooling/mcp-aside-devtools` | `../../skills/mcp-tooling/mcp-aside-devtools/changelog` | yes | `unchanged (resolves through .opencode compatibility link)` | `unchanged (link and target travel together into .skilled/)` | hand-made index symlink (no generator found; changelog tooling only links/checks markdown) | mechanical |
| `.opencode/changelog/mcp-tooling/mcp-chrome-devtools` | `../../skills/mcp-tooling/mcp-chrome-devtools/changelog` | yes | `unchanged (resolves through .opencode compatibility link)` | `unchanged (link and target travel together into .skilled/)` | hand-made index symlink (no generator found; changelog tooling only links/checks markdown) | mechanical |
| `.opencode/changelog/mcp-tooling/mcp-click-up` | `../../skills/mcp-tooling/mcp-click-up/changelog` | yes | `unchanged (resolves through .opencode compatibility link)` | `unchanged (link and target travel together into .skilled/)` | hand-made index symlink (no generator found; changelog tooling only links/checks markdown) | mechanical |
| `.opencode/changelog/mcp-tooling/mcp-figma` | `../../skills/mcp-tooling/mcp-figma/changelog` | yes | `unchanged (resolves through .opencode compatibility link)` | `unchanged (link and target travel together into .skilled/)` | hand-made index symlink (no generator found; changelog tooling only links/checks markdown) | mechanical |
| `.opencode/changelog/mcp-tooling/mcp-magicpath` | `../../skills/mcp-tooling/mcp-magicpath/changelog` | yes | `unchanged (resolves through .opencode compatibility link)` | `unchanged (link and target travel together into .skilled/)` | hand-made index symlink (no generator found; changelog tooling only links/checks markdown) | mechanical |
| `.opencode/changelog/mcp-tooling/mcp-mobbin` | `../../skills/mcp-tooling/mcp-mobbin/changelog` | yes | `unchanged (resolves through .opencode compatibility link)` | `unchanged (link and target travel together into .skilled/)` | hand-made index symlink (no generator found; changelog tooling only links/checks markdown) | mechanical |
| `.opencode/changelog/mcp-tooling/mcp-notion` | `../../skills/mcp-tooling/mcp-notion/changelog` | yes | `unchanged (resolves through .opencode compatibility link)` | `unchanged (link and target travel together into .skilled/)` | hand-made index symlink (no generator found; changelog tooling only links/checks markdown) | mechanical |
| `.opencode/changelog/mcp-tooling/mcp-obsidian` | `../../skills/mcp-tooling/mcp-obsidian/changelog` | yes | `unchanged (resolves through .opencode compatibility link)` | `unchanged (link and target travel together into .skilled/)` | hand-made index symlink (no generator found; changelog tooling only links/checks markdown) | mechanical |
| `.opencode/changelog/mcp-tooling/mcp-refero` | `../../skills/mcp-tooling/mcp-refero/changelog` | yes | `unchanged (resolves through .opencode compatibility link)` | `unchanged (link and target travel together into .skilled/)` | hand-made index symlink (no generator found; changelog tooling only links/checks markdown) | mechanical |
| `.opencode/changelog/mcp-tooling/parent` | `../../skills/mcp-tooling/changelog` | yes | `unchanged (resolves through .opencode compatibility link)` | `unchanged (link and target travel together into .skilled/)` | hand-made index symlink (no generator found; changelog tooling only links/checks markdown) | mechanical |
| `.opencode/changelog/sk-code/code-mobile-cli` | `../../skills/sk-code/sk-code-mobile-cli/changelog` | yes | `unchanged (resolves through .opencode compatibility link)` | `unchanged (link and target travel together into .skilled/)` | hand-made index symlink (no generator found; changelog tooling only links/checks markdown) | mechanical |
| `.opencode/changelog/sk-code/code-obsidian` | `../../skills/sk-code/sk-code-obsidian/changelog` | yes | `unchanged (resolves through .opencode compatibility link)` | `unchanged (link and target travel together into .skilled/)` | hand-made index symlink (no generator found; changelog tooling only links/checks markdown) | mechanical |
| `.opencode/changelog/sk-code/code-opencode` | `../../skills/sk-code/sk-code-opencode/changelog` | yes | `unchanged (resolves through .opencode compatibility link)` | `unchanged (link and target travel together into .skilled/)` | hand-made index symlink (no generator found; changelog tooling only links/checks markdown) | mechanical |
| `.opencode/changelog/sk-code/code-quality` | `../../skills/sk-code/sk-code-quality/changelog` | yes | `unchanged (resolves through .opencode compatibility link)` | `unchanged (link and target travel together into .skilled/)` | hand-made index symlink (no generator found; changelog tooling only links/checks markdown) | mechanical |
| `.opencode/changelog/sk-code/code-review` | `../../skills/sk-code/sk-code-review/changelog` | yes | `unchanged (resolves through .opencode compatibility link)` | `unchanged (link and target travel together into .skilled/)` | hand-made index symlink (no generator found; changelog tooling only links/checks markdown) | mechanical |
| `.opencode/changelog/sk-code/code-webflow` | `../../skills/sk-code/sk-code-webflow/changelog` | yes | `unchanged (resolves through .opencode compatibility link)` | `unchanged (link and target travel together into .skilled/)` | hand-made index symlink (no generator found; changelog tooling only links/checks markdown) | mechanical |
| `.opencode/changelog/sk-code/parent` | `../../skills/sk-code/changelog` | yes | `unchanged (resolves through .opencode compatibility link)` | `unchanged (link and target travel together into .skilled/)` | hand-made index symlink (no generator found; changelog tooling only links/checks markdown) | mechanical |
| `.opencode/changelog/sk-communication` | `../skills/sk-communication/changelog` | yes | `unchanged (resolves through .opencode compatibility link)` | `unchanged (link and target travel together into .skilled/)` | hand-made index symlink (no generator found; changelog tooling only links/checks markdown) | mechanical |
| `.opencode/changelog/sk-design` | `../skills/sk-design/changelog` | yes | `unchanged (resolves through .opencode compatibility link)` | `unchanged (link and target travel together into .skilled/)` | hand-made index symlink (no generator found; changelog tooling only links/checks markdown) | mechanical |
| `.opencode/changelog/sk-design-md-generator` | `../skills/sk-design-md-generator/changelog` | yes | `unchanged (resolves through .opencode compatibility link)` | `already dangling today: unchanged (link and target travel together into .skilled/)` | hand-made index symlink (no generator found; changelog tooling only links/checks markdown) | mechanical |
| `.opencode/changelog/sk-doc/create-agent` | `../../skills/sk-doc/sk-create-agent/changelog` | yes | `unchanged (resolves through .opencode compatibility link)` | `unchanged (link and target travel together into .skilled/)` | hand-made index symlink (no generator found; changelog tooling only links/checks markdown) | mechanical |
| `.opencode/changelog/sk-doc/create-benchmark` | `../../skills/sk-doc/sk-create-benchmark/changelog` | yes | `unchanged (resolves through .opencode compatibility link)` | `unchanged (link and target travel together into .skilled/)` | hand-made index symlink (no generator found; changelog tooling only links/checks markdown) | mechanical |
| `.opencode/changelog/sk-doc/create-changelog` | `../../skills/sk-doc/sk-create-changelog/changelog` | yes | `unchanged (resolves through .opencode compatibility link)` | `unchanged (link and target travel together into .skilled/)` | hand-made index symlink (no generator found; changelog tooling only links/checks markdown) | mechanical |
| `.opencode/changelog/sk-doc/create-command` | `../../skills/sk-doc/sk-create-command/changelog` | yes | `unchanged (resolves through .opencode compatibility link)` | `unchanged (link and target travel together into .skilled/)` | hand-made index symlink (no generator found; changelog tooling only links/checks markdown) | mechanical |
| `.opencode/changelog/sk-doc/create-diagram` | `../../skills/sk-doc/sk-create-diagram/changelog` | yes | `unchanged (resolves through .opencode compatibility link)` | `already dangling today: unchanged (link and target travel together into .skilled/)` | hand-made index symlink (no generator found; changelog tooling only links/checks markdown) | mechanical |
| `.opencode/changelog/sk-doc/create-diff` | `../../skills/sk-doc/sk-create-diff/changelog` | yes | `unchanged (resolves through .opencode compatibility link)` | `unchanged (link and target travel together into .skilled/)` | hand-made index symlink (no generator found; changelog tooling only links/checks markdown) | mechanical |
| `.opencode/changelog/sk-doc/create-feature-catalog` | `../../skills/sk-doc/sk-create-feature-catalog/changelog` | yes | `unchanged (resolves through .opencode compatibility link)` | `unchanged (link and target travel together into .skilled/)` | hand-made index symlink (no generator found; changelog tooling only links/checks markdown) | mechanical |
| `.opencode/changelog/sk-doc/create-manual-testing-playbook` | `../../skills/sk-doc/sk-create-manual-testing-playbook/changelog` | yes | `unchanged (resolves through .opencode compatibility link)` | `unchanged (link and target travel together into .skilled/)` | hand-made index symlink (no generator found; changelog tooling only links/checks markdown) | mechanical |
| `.opencode/changelog/sk-doc/create-quality-control` | `../../skills/sk-doc/sk-create-quality-control/changelog` | yes | `unchanged (resolves through .opencode compatibility link)` | `unchanged (link and target travel together into .skilled/)` | hand-made index symlink (no generator found; changelog tooling only links/checks markdown) | mechanical |
| `.opencode/changelog/sk-doc/create-readme` | `../../skills/sk-doc/sk-create-readme/changelog` | yes | `unchanged (resolves through .opencode compatibility link)` | `unchanged (link and target travel together into .skilled/)` | hand-made index symlink (no generator found; changelog tooling only links/checks markdown) | mechanical |
| `.opencode/changelog/sk-doc/create-repo-rule` | `../../skills/sk-doc/sk-create-repo-rule/changelog` | yes | `unchanged (resolves through .opencode compatibility link)` | `unchanged (link and target travel together into .skilled/)` | hand-made index symlink (no generator found; changelog tooling only links/checks markdown) | mechanical |
| `.opencode/changelog/sk-doc/create-skill` | `../../skills/sk-doc/sk-create-skill/changelog` | yes | `unchanged (resolves through .opencode compatibility link)` | `unchanged (link and target travel together into .skilled/)` | hand-made index symlink (no generator found; changelog tooling only links/checks markdown) | mechanical |
| `.opencode/changelog/sk-doc/parent` | `../../skills/sk-doc/changelog` | yes | `unchanged (resolves through .opencode compatibility link)` | `unchanged (link and target travel together into .skilled/)` | hand-made index symlink (no generator found; changelog tooling only links/checks markdown) | mechanical |
| `.opencode/changelog/sk-git` | `../skills/sk-git/changelog` | yes | `unchanged (resolves through .opencode compatibility link)` | `unchanged (link and target travel together into .skilled/)` | hand-made index symlink (no generator found; changelog tooling only links/checks markdown) | mechanical |
| `.opencode/changelog/sk-prompt` | `../skills/sk-prompt/changelog` | yes | `unchanged (resolves through .opencode compatibility link)` | `unchanged (link and target travel together into .skilled/)` | hand-made index symlink (no generator found; changelog tooling only links/checks markdown) | mechanical |
| `.opencode/changelog/sk-vision` | `../skills/sk-vision/changelog` | yes | `unchanged (resolves through .opencode compatibility link)` | `unchanged (link and target travel together into .skilled/)` | hand-made index symlink (no generator found; changelog tooling only links/checks markdown) | mechanical |
| `.opencode/changelog/system-deep-loop/deep-ai-council` | `../../skills/system-deep-loop/deep-ai-council/changelog` | yes | `unchanged (resolves through .opencode compatibility link)` | `unchanged (link and target travel together into .skilled/)` | hand-made index symlink (no generator found; changelog tooling only links/checks markdown) | mechanical |
| `.opencode/changelog/system-deep-loop/deep-improvement` | `../../skills/system-deep-loop/deep-improvement/changelog` | yes | `unchanged (resolves through .opencode compatibility link)` | `unchanged (link and target travel together into .skilled/)` | hand-made index symlink (no generator found; changelog tooling only links/checks markdown) | mechanical |
| `.opencode/changelog/system-deep-loop/deep-research` | `../../skills/system-deep-loop/deep-research/changelog` | yes | `unchanged (resolves through .opencode compatibility link)` | `unchanged (link and target travel together into .skilled/)` | hand-made index symlink (no generator found; changelog tooling only links/checks markdown) | mechanical |
| `.opencode/changelog/system-deep-loop/deep-review` | `../../skills/system-deep-loop/deep-review/changelog` | yes | `unchanged (resolves through .opencode compatibility link)` | `unchanged (link and target travel together into .skilled/)` | hand-made index symlink (no generator found; changelog tooling only links/checks markdown) | mechanical |
| `.opencode/changelog/system-deep-loop/parent` | `../../skills/system-deep-loop/changelog` | yes | `unchanged (resolves through .opencode compatibility link)` | `unchanged (link and target travel together into .skilled/)` | hand-made index symlink (no generator found; changelog tooling only links/checks markdown) | mechanical |
| `.opencode/changelog/system-deep-loop/runtime` | `../../skills/system-deep-loop/runtime/changelog` | yes | `unchanged (resolves through .opencode compatibility link)` | `unchanged (link and target travel together into .skilled/)` | hand-made index symlink (no generator found; changelog tooling only links/checks markdown) | mechanical |
| `.opencode/changelog/system-skill-advisor` | `../skills/system-skill-advisor/changelog` | yes | `unchanged (resolves through .opencode compatibility link)` | `unchanged (link and target travel together into .skilled/)` | hand-made index symlink (no generator found; changelog tooling only links/checks markdown) | mechanical |
| `.opencode/changelog/system-spec-kit` | `../skills/system-spec-kit/changelog` | yes | `unchanged (resolves through .opencode compatibility link)` | `unchanged (link and target travel together into .skilled/)` | hand-made index symlink (no generator found; changelog tooling only links/checks markdown) | mechanical |
