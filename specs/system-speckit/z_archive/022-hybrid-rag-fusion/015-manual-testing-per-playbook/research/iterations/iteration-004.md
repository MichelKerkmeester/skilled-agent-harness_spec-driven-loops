This review will require reading the catalog, playbook, and spec artifacts, then writing a markdown report. Before I touch files, I need the spec-folder choice for this work and the target report location in one go:

1. Spec folder for this review:
- `A. Existing` — use an existing spec folder you want this review attached to
- `B. New` — create a new spec folder for this review
- `C. Update related` — attach it to the related workflow under `specs/system-spec-kit/022-hybrid-rag-fusion/015-manual-testing-per-playbook/`
- `D. Skip` — only if you want to bypass spec-folder docs for this task
- `E. Phase folder` — write inside a phase folder such as `.../010-graph-signal-activation/` or `.../011-scoring-and-calibration/`

2. Report file target:
- `1. One combined report` in a single markdown file
- `2. Two separate reports` split by category 10 and 11

If you want, you can answer in one line like: `C + 1`, or give the exact folder/file path.