---
title: "Deep Review Iteration 7 Prompt"
loop_type: review
iteration: 7
---

# Iteration 7 review prompt

Review the shared system-spec-kit engine, canonical context payload, folder discovery, retrieval generator, templates, examples, and cross-reference surfaces. Read only the bounded scope list and directly imported modules as needed. Compare the preserved advisor-local payload duplicate with the canonical context contract, then trace the Level 2 checklist and acceptance-document references through templates and workflow guidance. Recheck deterministic corpus/index behavior and database path boundary evidence. Treat any producer-enum drift, missing artifact reference, or template contradiction as a separate hypothesis only when it changes an observable contract; do not duplicate F001-F008.

Required evidence:

- Use only the bounded paths in `scratch/review-scope.txt` as review targets.
- Cite exact source paths and line numbers for every finding.
- Distinguish confirmed source behavior from inferred interoperability or decommission impact.
- Check P0, P1, and P2 severity; prefer a refinement or ruled-out entry when the existing F004 evidence-link finding already covers the same defect.
- Do not run tests, validators, generators, graph writes, continuity writers, or repository writes. Keep all output under the bound lineage directory.
- Convergence is telemetry only; continue the max-iterations loop.

Review verdict must be the final line of the iteration narrative and must be `PASS`, `CONDITIONAL`, or `FAIL`.
