Writing the lineage packet now: initialized state, six review iterations up to the configured max, reducer-style registry/dashboard, resource map, and final synthesis report under the requested `gpt-2` artifact root only.Verifying the lineage artifacts parse and each iteration ends with the required verdict line before returning the completion sentinel.FANOUT_LINEAGE_COMPLETE:gpt-2