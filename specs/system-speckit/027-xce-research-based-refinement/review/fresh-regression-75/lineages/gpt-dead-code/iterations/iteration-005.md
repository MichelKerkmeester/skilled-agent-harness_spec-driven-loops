# Seat gpt-dead-code — iteration 5 (global #58)

- Model: `claude-opus-4-8` · Executor: claude · Angle: gpt-dead-code
- Verdict: **CONDITIONAL** · Findings: P0=0 P1=0 P2=4

## Analysis

I have enough verified evidence. Let me consolidate the analysis.

## Analysis — Seat `gpt-dead-code` (dead exports in 027 new lib modules)

**Method:** Enumerated every exported symbol in the 36 newly-added (`--diff-filter=A`) lib modules across the three daemons (`system-spec-kit`/mk-spec-memory, `system-skill-advisor`/mk-skill-advisor, `system-code-graph`/mk-code-index), then traced every reference repo-wide with `rg -nw`, separating production call sites from test/stress-only references and self-references.

**Most new exports are genuinely wired** — e.g. `promoteMetadataEdges`→`memory-index.ts`, `sweepCausalEdges`→4 storage callers, `traverseGraphBfs`→`query.ts`, `buildVectorDegradationSignal`→3 handlers, the idempotency/provenance helpers→`memory-save.ts`/`memory-crud-update.ts`, advisor `aliases`/`weights-config`/`feedback-calibration`→`advisor-validate.ts`/`fusion.ts`. The graph-traversal helpers exposed via `__testables`/port classes are an intentional convention.

**One important false-positive avoided:** `session-trace-causal-reducer.ts` (329 LOC) has zero production callers, but its module header states *"No live feedback logging path imports or invokes this module"*, it is flag-gated (`SPECKIT_SESSION_TRACE_CAUSAL_INFERENCE` defaults OFF), and specs mark it deferred/shadow-only. Per seat rules, intentional shadow code is **not** a defect — excluded.

**Confirmed dead exports** (public top-level exports, *not* in a `__testables` convention block, with no production consumer):

1. **`notifyStatediffSubscribers`** (`statediff.ts:230`) — `rg` finds exactly **one** occurrence repo-wide (its own definition). No production caller, *no test*. Pure dead code even within the (otherwise test-covered) deferred statediff engine.
2. **`resolveEdgeTierBasement`** + re-export `EDGE_TIER_STATE_LIMITS` (`edge-tier-basement.ts:53,87`) — the module's only exports; consumed solely by `feedback-retention-reducer.vitest.ts`. The wired retention reducer (`feedback-retention-reducer.ts`) and sweep (`memory-retention-sweep.ts`) never import it, so the "edge-floor" decision the impl-summary describes never executes in production.
3. **`codeHash`** (`canonical-fingerprint.ts:68`) — sibling `canonicalFingerprint` is wired into `incremental-index.ts`, but `codeHash(` is invoked only in two test files; production supplies `input.codeHash` by other means.
4. **`flattenCompositeTarget`** (`statediff.ts:169`) — test-only; not called by the module's own `planStatediff` nor any production path.

These are maintainability/hygiene (P2) — no correctness or security impact, no data loss. Reporting fewer/high-confidence per seat guidance.

```json
{"seat":"gpt-dead-code","model":"openai/gpt-5.5-fast","angle":"Exported helpers/functions in 027 new code with zero callers (dead exports).","verdict":"CONDITIONAL","summary":"4 dead public exports in new spec-memory lib modules (statediff, edge-tier-basement, canonical-fingerprint). Hygiene only; no correctness/security impact. session-trace reducer excluded as intentional shadow.","files_reviewed":[".opencode/skills/system-spec-kit/mcp_server/lib/storage/statediff.ts",".opencode/skills/system-spec-kit/mcp_server/lib/feedback/edge-tier-basement.ts",".opencode/skills/system-spec-kit/mcp_server/lib/feedback/feedback-retention-reducer.ts",".opencode/skills/system-spec-kit/mcp_server/lib/feedback/session-trace-causal-reducer.ts",".opencode/skills/system-spec-kit/mcp_server/lib/feedback/shadow-replay-corpus.ts",".opencode/skills/system-spec-kit/mcp_server/lib/feedback/replay-seed-vocab.ts",".opencode/skills/system-spec-kit/mcp_server/lib/storage/canonical-fingerprint.ts",".opencode/skills/system-spec-kit/mcp_server/lib/storage/memo.ts",".opencode/skills/system-spec-kit/mcp_server/lib/storage/idempotency-receipts.ts",".opencode/skills/system-spec-kit/mcp_server/lib/storage/write-provenance.ts",".opencode/skills/system-spec-kit/mcp_server/lib/storage/near-duplicate.ts",".opencode/skills/system-spec-kit/mcp_server/lib/causal/sweep.ts",".opencode/skills/system-spec-kit/mcp_server/lib/causal/frontmatter-promoter.ts",".opencode/skills/system-spec-kit/mcp_server/lib/observability/retrieval-observability.ts",".opencode/skills/system-spec-kit/mcp_server/lib/graph/bfs-traversal.ts",".opencode/skills/system-spec-kit/mcp_server/lib/triggers/semantic-trigger-matcher.ts",".opencode/skills/system-spec-kit/mcp_server/lib/ops/job-store.ts",".opencode/skills/system-spec-kit/mcp_server/lib/ops/sqlite-busy-retry.ts",".opencode/skills/system-spec-kit/mcp_server/lib/search/db-instance-lock.ts",".opencode/skills/system-skill-advisor/mcp_server/lib/scorer/aliases.ts",".opencode/skills/system-skill-advisor/mcp_server/lib/scorer/weights-config.ts",".opencode/skills/system-skill-advisor/mcp_server/lib/scorer/feedback-calibration.ts",".opencode/skills/system-skill-advisor/mcp_server/lib/scorer/lanes/bm25.ts",".opencode/skills/system-skill-advisor/mcp_server/lib/skill-graph/bfs-traversal.ts",".opencode/skills/system-skill-advisor/mcp_server/lib/skill-graph/doc-frontmatter.ts",".opencode/skills/system-code-graph/mcp_server/lib/graph/bfs-traversal.ts"],"findings":[{"severity":"P2","dimension":"maintainability","title":"Dead export: notifyStatediffSubscribers has zero callers anywhere","file":".opencode/skills/system-spec-kit/mcp_server/lib/storage/statediff.ts:230","evidence":"`rg -nw notifyStatediffSubscribers` over the whole repo returns exactly one hit — the export at statediff.ts:230. No production caller and not even a test references it, unlike sibling exports planStatediff/applyStatediffActions which at least have statediff.vitest.ts coverage.","why":"A public exported async function with no caller and no test is unreachable code that adds maintenance surface and falsely implies a subscriber-notification path exists in the statediff reconciliation layer.","recommendation":"Delete notifyStatediffSubscribers, or wire it into applyStatediffActions if subscriber notification is actually intended; if kept as staged foundation, add a test and a comment noting it is not yet wired."},{"severity":"P2","dimension":"maintainability","title":"Dead module: resolveEdgeTierBasement (edge-tier-basement.ts) only consumed by a test","file":".opencode/skills/system-spec-kit/mcp_server/lib/feedback/edge-tier-basement.ts:53","evidence":"resolveEdgeTierBasement and the re-export EDGE_TIER_STATE_LIMITS are the module's only exports; the sole importer is feedback-retention-reducer.vitest.ts:8. The wired reducer feedback-retention-reducer.ts and memory-retention-sweep.ts (which DO import evaluateFeedbackRetention/recordFeedbackRetentionAudit) never import edge-tier-basement.","why":"The edge-floor retention decision the impl-summary (004-retention-reducer/implementation-summary.md:42,65) presents as built never runs against any production retention candidate — the helper is exercised only by unit tests, so the described causal-edge durability protection is not actually applied.","recommendation":"Wire resolveEdgeTierBasement into the retention sweep's edge path, or remove the module and the corresponding impl-summary claim if edge flooring is deferred."},{"severity":"P2","dimension":"maintainability","title":"Dead export: codeHash() helper has no production call site","file":".opencode/skills/system-spec-kit/mcp_server/lib/storage/canonical-fingerprint.ts:68","evidence":"`rg -n 'codeHash\\('` shows the function invoked only in tests (canonical-fingerprint.vitest.ts:21-22, incremental-index-foundation.vitest.ts:30,76,91). Production incremental-index.ts imports canonicalFingerprint but not codeHash, and supplies input.codeHash from elsewhere (incremental-index.ts:745 reads input.codeHash, never calls the helper).","why":"An exported hashing helper intended for memo/code-version invalidation that no production path uses is dead code; it can drift from how production actually computes the codeHash value, creating a false sense the helper is the source of truth.","recommendation":"Either route production codeHash computation through this helper for consistency, or delete it and keep the tests on canonicalFingerprint."},{"severity":"P2","dimension":"maintainability","title":"Dead export: flattenCompositeTarget is test-only and unused by the statediff planner","file":".opencode/skills/system-spec-kit/mcp_server/lib/storage/statediff.ts:169","evidence":"`rg -nw flattenCompositeTarget` returns the definition (statediff.ts:169) and a single test reference (statediff.vitest.ts:56). It is not called by planStatediff (statediff.ts:132-168) nor any production handler that uses createStatediffAction.","why":"A public helper exported but never consumed by its own module's planner or production widens the API surface with no purpose and suggests an integration that does not exist.","recommendation":"Inline or remove flattenCompositeTarget, or have planStatediff use it if composite-target flattening is meant to be part of the plan path."}]}
```
