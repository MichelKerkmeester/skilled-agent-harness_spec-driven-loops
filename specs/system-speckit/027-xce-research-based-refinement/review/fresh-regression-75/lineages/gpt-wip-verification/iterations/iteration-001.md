# Seat gpt-wip-verification — iteration 1 (global #67)

- Model: `claude-opus-4-8` · Executor: claude · Angle: gpt-wip-verification
- Verdict: **CONDITIONAL** · Findings: P0=0 P1=0 P2=2

## Analysis

My review is complete. Here is my analysis.

## Review: gpt-wip-verification — finding-remediation completeness (traceability)

**Slice:** `005-verification-and-remediation/001-finding-remediation` — the epic's finding ledger (`backlog/p1-backlog.json` 132 entries, `backlog/p2-backlog.json` 119 entries, `remediation-plan.md` census).

**Method:** I treated the registry as source-of-truth (per `remediation-plan.md:14`), enumerated every disposition, reconciled the published census against it, and spot-verified "fixed" entries against live code.

### What held up
- **No entry is silently dropped at the registry level.** All 132 P1 entries carry a `disposition`; all 119 P2 entries carry a `p2_decision`. Counts sum exactly (P1 51 fixed / 24 refuted / 57 downgraded = 132; P2 56 FIX / 60 WAIVE / 3 ALREADY_FIXED = 119).
- **The 18 `fixed_*` entries with stale `v2_fix_sketch` are NOT silently-closed.** Their `v2_proof` reads "still broken," but I verified the underlying fixes actually landed later: `/spec_kit:` namespace has 0 live refs in `manual_testing_playbook.md` and no `commands/spec_kit/` refs survive in `feature_catalog/`; `isProtectedExistingRow` (`memory-crud-update.ts:85,88,95`) now checks `is_pinned` and `critical` tier; `bm25-baseline.ts:267` now passes configurable `k` into `computeNDCG`. The dispositions are honest in outcome; only the leftover sketch metadata is stale.

### What prior passes missed (real findings)
1. **Census doesn't reconcile with its own source-of-truth.** `remediation-plan.md:32` claims "Downgraded to P2 and triaged there | 63 (of which fixed/waived per P2 rules)". The backlog actually holds **57** downgraded entries (`downgraded_p2` 10 + `downgraded_p2_v2` 47), and the P1 column of the census sums to **138 against a stated 132 total**. The "of which fixed/waived per P2 rules" claim is false for 8 entries.
2. **8 downgraded P1 entries were never triaged in P2.** 57 downgraded vs only 49 carrying a `p2_decision`. The 8 untriaged (lines 240, 275, 684, 1433, 1452, 1715, 1786, 1803) stop at "downgraded" with no FIX/WAIVE. Entry **1715** ("Comparison helper ignores the configured NDCG cutoff") is the sharpest — `downgraded_p2` with **neither `v2_proof` nor `p2_decision`** (zero closure rationale). It's the closest thing to a silently-closed finding, though I confirmed the underlying code was independently fixed, so it's a bookkeeping gap, not a live defect.

Both are low blast-radius (all 8 entries are default-off / opt-in / test-infra / doc-nit, and per-entry dispositions exist), so no P0/P1 live defect. But in a packet whose deliverable *is* an honest finding ledger, a headline census that overcounts and overclaims triage completeness is a genuine traceability defect.

```json
{"seat":"gpt-wip-verification","model":"openai/gpt-5.5-fast","angle":"005 finding-remediation completeness: every tracked finding has a real remediation or an honest open status (no silently-closed).","verdict":"CONDITIONAL","summary":"Registry is honest per-entry, but the closure census overcounts downgrades (63 vs actual 57; P1 column sums to 138≠132) and falsely claims all downgrades were fixed/waived — 8 downgraded P1s carry no p2_decision.","files_reviewed":[".opencode/specs/system-spec-kit/027-xce-research-based-refinement/005-verification-and-remediation/001-finding-remediation/remediation-plan.md",".opencode/specs/system-spec-kit/027-xce-research-based-refinement/005-verification-and-remediation/001-finding-remediation/backlog/p1-backlog.json",".opencode/specs/system-spec-kit/027-xce-research-based-refinement/005-verification-and-remediation/001-finding-remediation/backlog/p2-backlog.json",".opencode/skills/system-spec-kit/manual_testing_playbook/manual_testing_playbook.md",".opencode/skills/system-spec-kit/mcp_server/handlers/memory-crud-update.ts",".opencode/skills/system-spec-kit/mcp_server/lib/eval/bm25-baseline.ts"],"findings":[{"severity":"P2","dimension":"traceability","title":"Closure census does not reconcile with source-of-truth backlog and falsely claims all downgrades were triaged","file":".opencode/specs/system-spec-kit/027-xce-research-based-refinement/005-verification-and-remediation/001-finding-remediation/remediation-plan.md:32","evidence":"Census says 'Downgraded to P2 and triaged there | 63 (of which fixed/waived per P2 rules)'. The cited backlog actually has 57 downgraded entries (downgraded_p2=10 + downgraded_p2_v2=47, confirmed by grep+uniq), and only 49 of those 57 carry a p2_decision. The P1 census column sums to 51+24+63=138 against the stated 132 total; correct downgraded count 57 makes 51+24+57=132.","why":"In a verification-and-remediation packet whose deliverable is an honest finding ledger, the headline closure summary miscounts the downgrade bucket by 6, fails to sum to its own total, and asserts fixed/waived triage for 8 entries that were never triaged — misleading any operator/reviewer auditing closure completeness.","recommendation":"Correct the downgraded count to 57, fix the P1 column to sum to 132, and either triage the 8 untriaged downgrades with a p2_decision or soften the 'of which fixed/waived per P2 rules' claim to reflect that downgraded-but-deferred entries exist."},{"severity":"P2","dimension":"traceability","title":"Downgraded P1 finding closed with zero verification or disposition rationale (closest to silently-closed)","file":".opencode/specs/system-spec-kit/027-xce-research-based-refinement/005-verification-and-remediation/001-finding-remediation/backlog/p1-backlog.json:1715","evidence":"Entry 'Comparison helper ignores the configured NDCG cutoff' (lines 1700-1715) carries disposition 'downgraded_p2' with no v2_proof and no p2_decision/p2_reason — unlike the other 56 downgrades. 7 of the 8 untriaged downgrades (240,275,684,1433,1452,1786,1803) at least record a v2_proof rationale; this one records nothing. The underlying code defect was independently resolved (bm25-baseline.ts:224,267 now thread configurable k into computeNDCG), but the registry gives no indication of verification or fix.","why":"A tracked P1 finding was de-prioritized out of the P1 fix/refute accounting with no recorded verification, fix, or waive rationale — the registry alone cannot tell whether it was resolved, deferred, or abandoned, violating the 'honest open status' / 'disposition is source of truth, nothing fixed unverified' contract (remediation-plan.md:14).","recommendation":"Add a v2_proof (citing bm25-baseline.ts:267 as resolved) and a p2_decision/p2_reason to entry idx for the NDCG-cutoff finding, and audit the other 7 untriaged downgrades for the same closure-rationale gap."}]}
```
