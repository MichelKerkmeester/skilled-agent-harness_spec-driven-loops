# Seat gpt-wip-verification — iteration 3 (global #69)

- Model: `claude-opus-4-8` · Executor: claude · Angle: gpt-wip-verification
- Verdict: **CONDITIONAL** · Findings: P0=0 P1=1 P2=1

## Analysis

I have enough verified signal. Here's my review.

## Analysis — Seat `gpt-wip-verification` (angle: 005 residual-design-units status accuracy)

**Scope reviewed:** `005-verification-and-remediation/004-residual-design-units/` (spec.md, tasks.md, implementation-summary.md, description.json, graph-metadata.json) + its direct dependencies: parent `005-verification-and-remediation/spec.md` phase map and the claimed-shipped code (`lib/feedback/shadow-replay-corpus.ts`, `replay-seed-vocab.ts`, `lib/architecture/layer-definitions.ts`).

**Code claims — verified TRUE (no findings):**
- Unit B: `shadow-replay-corpus.ts` + `replay-seed-vocab.ts` exist; `assertCorpusPrivacy` guard present; `INTENT_CENTROID_SEEDS` exported. Matches tasks T005/T006 + impl-summary.
- tri-109: `memory_index_scan_status` and `memory_index_scan_cancel` are registered in `layer-definitions.ts:115` alongside `memory_index_scan` — confirms the 37→39 tool-surface claim.
- tri-138: `dynamic-token-budget.ts` + `layer-definitions.ts` carry the `1500`/`includeFullReport`/`exclusionAudit` budget changes.
- Unit C: launcher file `.opencode/bin/mk-code-index-launcher.cjs` exists and the docs honestly mark it document-only (not mutated). Consistent.

The substantive "shipped" code is real. The defects are in **status/metadata traceability**, which is exactly this seat's lens.

**Finding 1 (P1, traceability) — parent phase-map misrepresents this child as "Planned"; T012 falsely checked.**
`005-verification-and-remediation/spec.md:111` shows `| 004 | 004-residual-design-units/ | … | Planned |`. The child claims `Status: Resolved` (spec.md:53) with `completion_pct: 90` and committed code. Meanwhile `tasks.md:82` marks **T012 `[x]`** asserting "parent phase-map row 30 status reflected." That verification task's claim is contradicted by the actual parent file — the row still says **Planned** (not-started), not Complete/In-Progress. The parent map is the canonical aggregate-progress surface ("Parent spec tracks aggregate progress via this map," spec.md:116) and is what `/speckit:resume` reads to list child statuses, so an operator/resume is told a shipped unit is unstarted. A checked-off verification box whose claim is false is a real traceability defect.

**Finding 2 (P2, traceability) — spec.md continuity frontmatter never reconciled past scaffold.**
`spec.md` frontmatter still carries scaffold-time values: `completion_pct: 0` (line 28), `recent_action: "Populated spec/plan/tasks/impl-summary…"`, `next_safe_action: "design Unit A vector reconcile first"` (line 18), while the same file's METADATA table says `Status: Resolved` and `implementation-summary.md`/`tasks.md` frontmatter say `completion_pct: 90`. The project's completion rule explicitly requires reconciling `_memory.continuity` fields and `spec.md` status. Operational impact is low (the resume ladder reads continuity only from `implementation-summary.md` per ADR-004), so P2 — but a direct reader of spec.md's machine block would conclude the packet is 0% done and the next step is to start Unit A, which already shipped. (Secondary: spec §3 still quantifies Unit A as "5 success rows missing" / "~314-row reconcile" while the implementation actually found and reset 91 rows — spec scope never reconciled to the implemented reality.)

No P0s. The epic's code is implemented as claimed; the gaps are unreconciled status surfaces.

```json
{"seat":"gpt-wip-verification","model":"openai/gpt-5.5-fast","angle":"005 residual-design-units status accuracy: claimed status vs actual implementation.","verdict":"CONDITIONAL","summary":"Shipped code matches claims (Unit B, tri-109, tri-138 verified). Two status-traceability gaps: parent phase-map still 'Planned' for a Resolved child (T012 falsely checked); spec.md continuity frontmatter never reconciled past scaffold.","files_reviewed":[".opencode/specs/system-spec-kit/027-xce-research-based-refinement/005-verification-and-remediation/004-residual-design-units/spec.md",".opencode/specs/system-spec-kit/027-xce-research-based-refinement/005-verification-and-remediation/004-residual-design-units/tasks.md",".opencode/specs/system-spec-kit/027-xce-research-based-refinement/005-verification-and-remediation/004-residual-design-units/implementation-summary.md",".opencode/specs/system-spec-kit/027-xce-research-based-refinement/005-verification-and-remediation/004-residual-design-units/graph-metadata.json",".opencode/specs/system-spec-kit/027-xce-research-based-refinement/005-verification-and-remediation/spec.md",".opencode/skills/system-spec-kit/mcp_server/lib/architecture/layer-definitions.ts",".opencode/skills/system-spec-kit/mcp_server/lib/feedback/shadow-replay-corpus.ts",".opencode/skills/system-spec-kit/mcp_server/lib/feedback/replay-seed-vocab.ts"],"findings":[{"severity":"P1","dimension":"traceability","title":"Parent phase-map shows shipped child as 'Planned'; T012 falsely marked complete","file":".opencode/specs/system-spec-kit/027-xce-research-based-refinement/005-verification-and-remediation/spec.md:111","evidence":"Parent phase-map row reads '| 004 | 004-residual-design-units/ | … | Planned |', but the child spec.md:53 declares Status 'Resolved' (completion_pct 90, code committed). tasks.md:82 marks T012 [x] claiming 'parent phase-map row 30 status reflected' — that claim is false against the actual parent file.","why":"The parent map is the canonical aggregate-progress surface (spec.md:116) that /speckit:resume reads to list child statuses; reporting a shipped unit as not-started misleads resume/operator. A checked verification task whose claim contradicts the file is a traceability defect.","recommendation":"Update parent 005 spec.md row 004 status from 'Planned' to 'Complete' (or 'In Progress' if Unit C follow-on is tracked open), then either keep T012 [x] truthfully or note the residual follow-on."},{"severity":"P2","dimension":"traceability","title":"Child spec.md continuity frontmatter never reconciled past scaffold","file":".opencode/specs/system-spec-kit/027-xce-research-based-refinement/005-verification-and-remediation/004-residual-design-units/spec.md:28","evidence":"spec.md frontmatter still has completion_pct: 0 (line 28), recent_action 'Populated spec/plan/tasks/impl-summary', next_safe_action 'design Unit A vector reconcile first' (line 18), while the same file's METADATA says Status 'Resolved' and impl-summary/tasks frontmatter say completion_pct 90. spec §3 also still quantifies Unit A as ~314 rows / 5 missing-success while tasks T004 + impl-summary record 91 rows actually reset.","why":"The completion rule requires reconciling _memory.continuity and spec.md status; a direct reader of spec.md's machine block would conclude the packet is 0% done with Unit A unstarted. Low operational impact only because the resume ladder reads continuity from implementation-summary.md (ADR-004).","recommendation":"Reconcile spec.md frontmatter (completion_pct, recent_action, next_safe_action) to the resolved state and update the §3 Unit A row counts to the implemented 91-row reality, or add a one-line 'implemented numbers differ' note."}]}
```
