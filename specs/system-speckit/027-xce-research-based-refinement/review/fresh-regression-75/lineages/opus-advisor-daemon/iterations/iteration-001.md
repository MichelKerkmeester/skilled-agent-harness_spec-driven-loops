# Seat opus-advisor-daemon — iteration 1 (global #12)

- Model: `claude-opus-4-8` · Executor: claude · Angle: opus-advisor-daemon
- Verdict: **PASS** · Findings: P0=0 P1=0 P2=1

## Analysis

## Analysis

**Scope:** Live lexical lane (`lexical.ts::scoreLexicalLane`, the only one wired into fusion at `fusion.ts:193`) and the packed BM25F shadow lane (`bm25.ts`). The BM25 lane is confirmed **shadow-only / default-OFF**: its only non-test reference is via `scoreLexicalShadowLanes` (lexical.ts:102-111), which is itself called nowhere in the live path, and the regression test asserts `flagOnLive === flagOffLive` (bm25-lexical-shadow.vitest.ts:76). Per review rules, shadow/observe-only code is not a defect source, so I held its known simplifications (global-length-normalized BM25F instead of per-field normalization; near-instant tf saturation because field weights ≥ K1) as design choices, not bugs. The git history confirms these files were last touched by packet 145/003, not epic 027.

**BM25 math verification (shadow):** idf uses the always-positive `log(1 + (N-df+0.5)/(df+0.5))` form (bm25.ts:337) — no negative-idf trap. Posting packing in `finalize()` reindexes all parallel typed arrays through the same `order[].index` (bm25.ts:220-233) — consistent. Tie-break is deterministic: `score desc, then skillId.localeCompare` (bm25.ts:282). Field-weight ordering (name 4.0 > keywords 3.0 > intentSignals 2.4 > domains 2.2 > derivedTriggers 1.8 > description 1.0) matches the tests' assertions. `resolveFieldWeights` correctly preserves an explicit `0` override (nullish coalescing). No correctness defect found in the BM25 lane.

**Live lexical lane — one real scoring-math issue:** `scoreLexicalLane` passes the *synonym-expanded* token set as `promptTokens` (lexical.ts:63 → :68), and `scoreTokenOverlap` normalizes hits by `max(3, promptTokens.length)` (text.ts:91). Because the denominator grows with the number of synonyms injected, a query whose tokens have rich synonym sets is systematically scored *lower per hit*, even for the skill the prompt literally names. The expansion is meant to raise recall (numerator) for related skills, but it simultaneously dilutes the normalizer for the originating skill. This is the one citable scoring-math correctness concern in default-on code; impact is bounded (feeds fusion, not a crash), so P2.

No P0/P1. The assigned slice is sound; the BM25 lane is correct and properly isolated.

```json
{"seat":"opus-advisor-daemon","model":"claude-opus-4-8","angle":"Advisor scorer lexical/packed-BM25 lane correctness: scoring math, field weights, tie-breaks.","verdict":"PASS","summary":"BM25 shadow lane math/weights/tie-breaks correct and confirmed default-OFF (not in live path). One minor live-lane scoring dilution: synonym expansion inflates the overlap denominator.","files_reviewed":[".opencode/skills/system-skill-advisor/mcp_server/lib/scorer/lanes/lexical.ts",".opencode/skills/system-skill-advisor/mcp_server/lib/scorer/lanes/bm25.ts",".opencode/skills/system-skill-advisor/mcp_server/lib/scorer/text.ts",".opencode/skills/system-skill-advisor/mcp_server/lib/scorer/fusion.ts",".opencode/skills/system-skill-advisor/mcp_server/tests/scorer/bm25-lexical-shadow.vitest.ts"],"findings":[{"severity":"P2","dimension":"correctness","title":"Synonym expansion dilutes lexical overlap denominator","file":".opencode/skills/system-skill-advisor/mcp_server/lib/scorer/text.ts:91","evidence":"scoreTokenOverlap normalizes by max(3, promptTokens.length); scoreLexicalLane (lexical.ts:63,68) feeds it expandedTokens() (original tokens + SYNONYMS). Each injected synonym enlarges promptTokens.length, lowering the per-hit normalized score even when the synonym matches nothing.","why":"A prompt that literally names a skill but whose tokens carry many synonyms (e.g. save/context/search each add 3) gets a larger denominator, so the correctly-matching skill is scored lower than it would be without expansion — the opposite of the expansion's recall intent. Can push a true match below downstream thresholds in the live fusion lane.","recommendation":"Normalize by the count of the ORIGINAL (pre-synonym) prompt tokens: pass tokenize(prompt) length as the denominator basis while still using expandedTokens for numerator matching, or have scoreTokenOverlap take a separate denominatorCount argument derived from the un-expanded query."}]}
```
