# Seat gpt-wip-doctrine — iteration 3 (global #65)

- Model: `claude-opus-4-8` · Executor: claude · Angle: gpt-wip-doctrine
- Verdict: **CONDITIONAL** · Findings: P0=0 P1=1 P2=1

## Analysis

I have enough evidence. Let me write the review.

## Review: Seat `gpt-wip-doctrine` — 027/001 Scoped Pre-Execution Gates (docs vs code)

**Scope reviewed:** the packet `001-research-and-doctrine/002-gem-team-adoption/002-scoped-preexec-and-handoff-gates` (spec/plan/tasks/checklist/implementation-summary) against the actual implemented gate logic in `.opencode/agents/{orchestrate,debug,code}.md`, `.opencode/skills/sk-code/SKILL.md`, and the shared contract `agent-io-contract.md`.

### What matches (faithfully implemented)
- **Gate A (typed debug→implement handoff)** — fully traceable. `debug.md:128-134,484-491` emits `root_cause`/`target_files`/`fix_recommendations`/`confidence` and frames it as a downscale of Gem's `debugger_diagnosis` (REQ-002, REQ-007 ✓). `orchestrate.md:261,267-279` preserves it on dispatch (REQ-003 ✓). `code.md:68-81` receiver-validates and returns `BLOCKED`/`LOW_CONFIDENCE` on missing fields (REQ-004 ✓).
- **Gate C (pre-mortem)** — `orchestrate.md:222,263` gates the pre-mortem field on `complexity in {medium,high}` (REQ-006 ✓).
- **Predicate centralization** — all three predicates live only in `orchestrate.md:218,259-263`; the contract explicitly states leaf agents must not carry copies (`agent-io-contract.md:115`). This honors REQ-001 and the NFR "predicates live in one place."

### Defect found — Gate B is not where the spec says it is

REQ-005 is a **P0 blocker** whose acceptance criteria names a specific artifact: *"`sk-code/SKILL.md` requires a contract / boundary test / executable acceptance check before production edits for `change_class ∈ {api,schema,integration}`."* SC-002 and the edge-case table assert the same sk-code behavior.

The implemented reality:
- `sk-code/SKILL.md` carries **none** of this — grep for `boundary | contract-first | change_class | api/schema | pre-execution` returns no gate (the only hits are unrelated Motion/CONFIG lines).
- `code.md` also carries no `change_class`/boundary gate.
- Gate B exists **only** in `orchestrate.md:262` and the shared contract `agent-io-contract.md:130`, and the contract deliberately forbids pushing it into leaf agents (`agent-io-contract.md:115`).

Consequence: the boundary contract-first gate fires **only on the `@orchestrate` dispatch path**, not on direct `sk-code`/`@code` code work — which, per root `CLAUDE.md` ("Code work routes through the `sk-code` skill"), is the dominant code path the spec targeted. The gate's effective coverage is materially narrower than the spec describes.

This is disclosed honestly as a "scope exception" (spec.md:138; impl-summary "Read/flagged"; tasks T009; checklist CHK-024). But the disclosure was handled by **downgrading a P0 to a P1 checklist item** (CHK-024 is `[P1]`) and still marking the packet 15/15 P0 / `completion_pct: 100` / Status "Implemented". A P0 whose named acceptance artifact is absent should have been routed through an amendment/deferral, not reported as complete. P1-class requirements (REQ-008/009) legitimately allow "user-approved deferral"; P0 (REQ-005) does not, per the spec's own protocol.

### Secondary defect — spec is internally self-contradictory (never reconciled)

REQ-001 + NFR Maintainability (spec.md:209: predicates in "one place" `@orchestrate`, "cannot drift silently across agents") directly conflicts with REQ-005's literal requirement that `sk-code/SKILL.md` carry the gate. The contract (`agent-io-contract.md:115`) codifies the centralized design, making REQ-005 literally unsatisfiable as written. The spec docs never reconcile this; REQ-005 should have been amended to "orchestrator/contract carries the predicate" rather than left as a literally-unmet P0 marked complete.

Everything else (Gates A/C, downscale framing, legacy-warn-not-fail, no governance edits, strict-validate green) traces cleanly. Strict validation passing is expected — it does not cross-check REQ↔file mapping, so it cannot catch this class of gap.

```json
{"seat":"gpt-wip-doctrine","model":"openai/gpt-5.5-fast","angle":"001 scoped pre-execution gates: do the gates described match the implemented gate logic?","verdict":"CONDITIONAL","summary":"Gates A/C trace cleanly. Gate B (P0 REQ-005) is implemented only in @orchestrate, not in sk-code as the spec names; coverage is narrower than specified yet P0 is marked complete.","files_reviewed":[".opencode/specs/system-spec-kit/027-xce-research-based-refinement/001-research-and-doctrine/002-gem-team-adoption/002-scoped-preexec-and-handoff-gates/spec.md",".opencode/specs/system-spec-kit/027-xce-research-based-refinement/001-research-and-doctrine/002-gem-team-adoption/002-scoped-preexec-and-handoff-gates/plan.md",".opencode/specs/system-spec-kit/027-xce-research-based-refinement/001-research-and-doctrine/002-gem-team-adoption/002-scoped-preexec-and-handoff-gates/tasks.md",".opencode/specs/system-spec-kit/027-xce-research-based-refinement/001-research-and-doctrine/002-gem-team-adoption/002-scoped-preexec-and-handoff-gates/checklist.md",".opencode/specs/system-spec-kit/027-xce-research-based-refinement/001-research-and-doctrine/002-gem-team-adoption/002-scoped-preexec-and-handoff-gates/implementation-summary.md",".opencode/agents/orchestrate.md",".opencode/agents/debug.md",".opencode/agents/code.md",".opencode/skills/sk-code/SKILL.md",".opencode/skills/system-spec-kit/references/workflows/agent-io-contract.md"],"findings":[{"severity":"P1","dimension":"traceability","title":"Gate B (P0 REQ-005) absent from sk-code/SKILL.md; orchestrator-only, yet P0 marked complete","file":".opencode/skills/sk-code/SKILL.md:1","evidence":"grep for boundary|contract-first|change_class|api/schema|pre-execution in sk-code/SKILL.md returns no gate; the only Gate B logic is orchestrate.md:262 and agent-io-contract.md:130, and agent-io-contract.md:115 forbids leaf agents from carrying the predicate. code.md has no change_class gate either. REQ-005 (spec.md:153) is P0 and names sk-code/SKILL.md as the surface; SC-002 (spec.md:172) and the edge-case table (spec.md:227) assert sk-code-path behavior. Packet reports 15/15 P0, completion_pct:100, Status 'Implemented' (checklist.md:130, impl-summary.md:45); the gap is mapped to CHK-024 marked [P1] (checklist.md:77).","why":"A P0 requirement's named acceptance artifact does not exist. Because the contract deliberately keeps Gate B out of leaf agents, the gate never fires on direct sk-code/@code code work (the dominant code path per root CLAUDE.md) and only fires on the narrower @orchestrate dispatch path — so the api/schema/integration coverage the spec promises is materially narrower than implemented. Marking this P0 complete (rather than amended/deferred) overstates closure; the spec's own protocol allows 'user-approved deferral' for P1 only, not P0.","recommendation":"Either (a) amend REQ-005/SC-002 to state the predicate is orchestrator/contract-owned and explicitly accept that direct sk-code invocations are not gated, reclassifying the sk-code edit as a tracked deferral; or (b) add the boundary contract-first reminder to sk-code/SKILL.md Phase 0 so the gate covers direct code work. Do not leave a P0 whose named artifact is absent marked 15/15 complete."},{"severity":"P2","dimension":"traceability","title":"spec.md internally contradicts itself on where Gate B lives; contradiction never reconciled","file":".opencode/specs/system-spec-kit/027-xce-research-based-refinement/001-research-and-doctrine/002-gem-team-adoption/002-scoped-preexec-and-handoff-gates/spec.md:209","evidence":"NFR Maintainability (spec.md:209) and REQ-001 require predicates to 'live in one place (@orchestrate) so their scope cannot drift silently across agents', which the shared contract codifies (agent-io-contract.md:115: 'should not spread heuristic copies across leaf agents'). REQ-005 (spec.md:153) simultaneously requires sk-code/SKILL.md to carry the gate. Both are listed as satisfied P0s.","why":"The two requirements cannot both be literally true under the chosen centralized design, making REQ-005 unsatisfiable as written. The packet docs resolve it silently in code but never amend the spec, leaving a traceability artifact where a P0 acceptance criterion contradicts the implemented architecture.","recommendation":"Reconcile the spec: keep the centralized-predicate design and rewrite REQ-005/SC-002 to reference the orchestrator/contract surface (or an explicit sk-code Phase-0 reminder), removing the literal 'sk-code/SKILL.md requires...' wording that conflicts with REQ-001/NFR."}]}
```
