User specified the deep-review workflow and an explicit artifact root. I’ll load the workflow contract first, bind `artifact_dir` directly to the provided lineage path, then keep all review outputs inside that directory.The workflow contract requires the command-owned state machine artifacts (`deep-review-config.json`, JSONL state, strategy, dashboard, iterations, report). I’ll read the protocol/state references and the current lineage directory before initializing or resuming anything.The lineage artifact directory is effectively fresh, so this will initialize a new lineage state using the provided `session_id` and direct artifact root. Next I’m resolving the spec-folder scope from its packet docs and resource map before writing the initial state.This target is a phase parent with many child phases, so the review scope needs to be anchored by the parent spec, metadata, and resource map rather than assuming a single flat implementation packet.I found an evidence-backed parent/child drift pattern before writing state: `011-command-presentation-workflow-separation` exists and has metadata, but the parent `spec.md`, `description.json`, and `resource-map.md` do not consistently include it. I’ll initialize the state now and carry that through the iteration records with exact citations.I’m writing the lineage packet now: immutable config, append-only JSONL state, reducer-style registry/dashboard, five iteration narratives with claim-adjudication packets for P1 findings, and final synthesis outputs.State initialization and reducer-style summaries are in place. I’m adding the per-iteration narratives and synthesis report next, with each iteration ending in the exact required verdict line.The lineage packet now has config, JSONL, registry, strategy, dashboard, five iteration files, deltas, audit log, resource map, and synthesis report. I’m running a focused validation of JSON parsing and the mandatory iteration verdict-line contract.The lineage-local schema checks passed. I’m also running the target spec strict validator as the workflow’s release-readiness validation gate; if it reports existing packet issues, I’ll record that only inside this lineage report.Strict recursive validation for the target spec folder passed with 0 errors and 0 warnings. I’m recording that evidence in the lineage report, then I’ll finish with the required completion token.FANOUT_LINEAGE_COMPLETE:gpt-3