SKILL ROUTING: User directed -> deep-review. Spec folder and artifact directory are explicitly provided; I’ll load the review workflow contract, bind the artifact root to the override, then keep all writes inside the lineage directory.The memory trigger rejected the provided fan-out session id as not server-managed, so I’m retrying without that MCP session field while preserving the user-provided session id in workflow artifacts.The workflow contract confirms this is command-owned fan-out; I’m treating this invocation as the lineage execution phase, not spawning nested agents or external loops. I’ll create lineage-local state, iteration, dashboard, resource-map, and report artifacts only under `review/lineages/gpt-1`.I found two evidence-backed release-blocking review issues while reading the implementation: the production async warmup never finalizes the last packed batch, and the memory budget fixture’s 69.2 MB body text is made entirely of stop words, so it does not exercise body postings memory. I’ll record these as P0 findings in the lineage artifacts.Initializing lineage-local review state now. Existing `gpt-1` contains only `.executor-state/`, so the review artifacts can be added without overwriting prior review output.Writing the lineage artifacts with six iterations: four coverage passes plus two stabilization/replay passes, stopping at `maxIterations` because active P0 findings block convergence.Verifying the lineage output now: JSON/JSONL parsing, required iteration verdict lines, and the packet strict validator.FANOUT_LINEAGE_COMPLETE:gpt-1