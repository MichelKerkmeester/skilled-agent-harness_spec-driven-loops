SKILL ROUTING: User directed → deep-review. I’ll load the requested workflow contract first, then run only the lineage-local review outputs under the provided artifact directory.The workflow contract forbids direct nested deep-review dispatch and direct `opencode run` self-invocation from inside OpenCode. I’m treating this invocation as the already-spawned lineage worker and will write the required lineage state/artifacts directly under the supplied override directory.Phase init inputs are clear: this is a Level 1 shipped spec for the packed BM25 fallback, with four implementation/test files named by the summary. I’m reviewing those files read-only, then will synthesize iteration artifacts into the lineage directory.The code review is now on the implementation path. One notable risk area is incremental packed-index updates after `finalizePackedPostings()`, so I’m checking that path and the FTS5 comparison semantics before writing findings.Phase main loop produced two active findings: one P1 around async warmup never compacting the last batch, and one P1 around fallback scoped retrieval limiting before DB-side filtering. I’m now writing the lineage packet with six iterations, convergence state, dashboard, resource map, and final report under the requested `gpt-3` directory.Core JSON state and registry are written. I’m adding the human-readable strategy, iteration narratives, dashboard, and synthesis report now.Synthesis artifacts are written. I’m doing a lightweight artifact integrity check now: JSON parsing, JSONL parsing, expected file presence, and final verdict-line validation for all six iteration files.FANOUT_LINEAGE_COMPLETE:gpt-3