SKILL ROUTING: User directed → deep-review. Using the provided existing spec folder and restricting writes to the lineage artifact directory.Deep-review workflow is the named workflow and cannot be hand-rolled; reading its contract now, then I’ll run only the required phases with the artifact root bound to the provided override.The skill contract requires canonical state files plus per-iteration markdown and JSONL deltas, with target files read-only. I’ll inspect the workflow references and existing lineage directory before writing the init artifacts.Initialization needs target scope discovery before state files are created. I’ll read the target spec packet and current lineage directory, then write the review packet only under the lineage directory.Memory context rejected the supplied blank/session identifier, so I’ll continue with direct packet evidence and omit memory session binding if retrieval is needed again.The target packet is complete and names the implementation surfaces. I’m reviewing the correctness dimension for the shipped confidence rebalance and calibration wiring, then I’ll synthesize with `maxIterations=1`.I found one actionable correctness issue: the shipped starter labeled-set JSON is an object with a `pairs` array, but `loadLabeledSet()` only accepts a top-level array, so the bundled starter set cannot be loaded by the shipped loader as-is.Writing the deep-review state packet now: config, JSONL state, findings registry, strategy, iteration narrative, dashboard, synthesis report, and executor prompt/log/delta artifacts, all under the lineage directory.State packet is written. I’m doing a read-only artifact validation now: JSON/JSONL parse checks and the required iteration final-line contract.Artifact validation passed. I’ll run the packet strict validator read-only before the final completion signal.Strict spec validation passed with 0 errors and 0 warnings. I’m updating the lineage report/dashboard/state to reflect that evidence before finalizing.FANOUT_LINEAGE_COMPLETE:gpt55-p017c004-2