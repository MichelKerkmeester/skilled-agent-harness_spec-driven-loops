Act as a general code-implementation agent (seat BF08). Spec folder: /Users/michelkerkmeester/MEGA/Development/Code_Environment/Public/.opencode/specs/system-spec-kit/027-xce-research-based-refinement/005-verification-and-remediation/007-release-alignment-review/004-code-remediation (PRE-APPROVED at Gate 3 — make the edits below directly; do NOT re-ask Gate 3 and do NOT ask the user anything).

FIRST load .opencode/skills/sk-code/SKILL.md, get its OpenCode surface tag, and follow that surface's standards. Also read .opencode/skills/system-spec-kit/constitutional/comment-hygiene.md.

You fix sk-code misalignments. SURGICAL, behavior-NEUTRAL edits only — comment / type / strict-mode / header edits ONLY. Do not change runtime behavior.

ALLOWED WRITE PATHS (edit ONLY these files):
- .opencode/skills/system-code-graph/mcp_server/lib/apply-orchestrator.ts
- .opencode/skills/system-code-graph/mcp_server/lib/code-graph-db.ts
- .opencode/skills/system-code-graph/mcp_server/lib/phase-runner.ts
- .opencode/skills/system-code-graph/mcp_server/lib/query-result-adapter.ts
- .opencode/skills/system-code-graph/mcp_server/lib/recovery-procedures.ts
BANNED: no delete, no rename, no new files, no edits outside the listed files, NO behavior change, no --no-verify.

FIX RULES by theme:
- Comment-hygiene (ephemeral ids in comments: `FIX RC3-B`, `P4-12`, `packets 006-009`, `SC-002`, ADR-/REQ-/CHK-/task-/finding ids, or a concrete `.opencode/specs/NNN-...` path): DROP the perishable label, KEEP the durable WHY. Never delete an explanatory comment outright — only strip the ephemeral identifier.
- Shell script missing strict mode: add `set -euo pipefail` immediately after the shebang/header.
- `any` / `any[]` in a public/exported type: replace with the real type inferred from usage; it MUST still type-check.
- Missing or non-boxed module/file header: add or normalize to the sk-code OpenCode header convention (cosmetic; in scope).
- catch parameter not typed `unknown`, or exported symbol missing TSDoc: fix only if trivial and behavior-neutral.

DO-NOT-FIX (binding — confirmed false positives; leave untouched):
- `// Feature catalog: <name>` comments are ALLOWED (NOT in comment-hygiene's forbidden list). Do NOT remove them.
- TSDoc `@example` values like `specFolder: 'specs/001-demo'` — illustrative, not real pointers. Leave.
- Any file under a `dist/` directory — build output, regenerated by the build. Do NOT edit.
- Any cited finding that does not reproduce against the live file — mark refuted.

FINDINGS TO FIX (confirm-then-fix each; P0/P1/P2 all in scope):
- [P1] .opencode/skills/system-code-graph/mcp_server/lib/apply-orchestrator.ts:725 — Exported function lacks explicit return type | sk-code TypeScript checklist P1: all exported functions have explicit return type annotations. | fix: Annotate the return type, e.g. the return type of buildReadinessBlock.
- [P2] .opencode/skills/system-code-graph/mcp_server/lib/code-graph-db.ts:1138 — Possible ephemeral artifact id in comment | Comment Hygiene: comments must not cite perishable ticket/feature/task identifiers; if `CG-003` is not a durable code enum, this violates the hard comment rule. | fix: Replace `CG-003` with the durable transaction-safety rationale or document why it is a stable code value.
- [P2] .opencode/skills/system-code-graph/mcp_server/lib/code-graph-db.ts:7 — Import groups are out of prescribed order | sk-code code organization: imports order is Node built-ins, third-party packages, local modules, then type-only imports. | fix: Put node: imports before better-sqlite3, then local imports, then type-only imports.
- [P1] .opencode/skills/system-code-graph/mcp_server/lib/phase-runner.ts:228 — Non-null assertions lack preceding justification | sk-code TypeScript checklist P1: non-null assertions have a justification comment on the preceding line. | fix: Replace with explicit guards or add concise preceding WHY comments.
- [P1] .opencode/skills/system-code-graph/mcp_server/lib/query-result-adapter.ts:6 — Exported public interfaces lack TSDoc | sk-code TypeScript checklist P1: exported functions, classes, and interfaces have TSDoc comments. | fix: Add TSDoc to exported interfaces/types or keep non-public contracts unexported.
- [P1] .opencode/skills/system-code-graph/mcp_server/lib/query-result-adapter.ts:107 — Catch variables are not explicitly typed unknown | sk-code TypeScript checklist P1: catch blocks use unknown type and narrow with instanceof. | fix: Use catch (error: unknown) or catch (cause: unknown) and keep the existing narrowing.
- [P2] .opencode/skills/system-code-graph/mcp_server/lib/recovery-procedures.ts:4 — Possible ephemeral recovery-procedure IDs in code comment | Uncertain applicability: comment hygiene forbids perishable task, requirement, checklist, packet, phase, ADR, ticket, or finding IDs in code comments. | fix: If CG-RP-* is spec-local, replace it with durable behavior names; if stable API terminology, document that outside the comment.

After editing, re-read each edited line. Your final message MUST be exactly one fenced json block:
```json
{"seat":"BF08","fixed":[{"file":"path","theme":"","after":""}],"refuted":[{"file":"path","why":""}]}
```
