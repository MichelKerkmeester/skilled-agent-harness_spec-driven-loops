Act as a general code-implementation agent (seat BF15). Spec folder: /Users/michelkerkmeester/MEGA/Development/Code_Environment/Public/.opencode/specs/system-spec-kit/027-xce-research-based-refinement/005-verification-and-remediation/007-release-alignment-review/004-code-remediation (PRE-APPROVED at Gate 3 — make the edits below directly; do NOT re-ask Gate 3 and do NOT ask the user anything).

FIRST load .opencode/skills/sk-code/SKILL.md, get its OpenCode surface tag, and follow that surface's standards. Also read .opencode/skills/system-spec-kit/constitutional/comment-hygiene.md.

You fix sk-code misalignments. SURGICAL, behavior-NEUTRAL edits only — comment / type / strict-mode / header edits ONLY. Do not change runtime behavior.

ALLOWED WRITE PATHS (edit ONLY these files):
- .opencode/skills/system-spec-kit/mcp_server/lib/search/pipeline/stage2-fusion.ts
- .opencode/skills/system-spec-kit/mcp_server/lib/search/pipeline/stage4-filter.ts
- .opencode/skills/system-spec-kit/mcp_server/lib/search/rerank/retrieval-rescue.ts
- .opencode/skills/system-spec-kit/mcp_server/lib/search/vector-index-mutations.ts
- .opencode/skills/system-spec-kit/mcp_server/lib/search/vector-index-queries.ts
BANNED: no delete, no rename, no new files, no edits outside the listed files, NO behavior change, no --no-verify.

FIX RULES by theme:
- Comment-hygiene (ephemeral ids in comments: `FIX RC3-B`, `P4-12`, `packets 006-009`, `SC-002`, ADR-/REQ-/CHK-/task-/finding ids, or a concrete `.opencode/specs/NNN-...` path): DROP the perishable label, KEEP the durable WHY. Never delete an explanatory comment outright — only strip the ephemeral identifier.
- Shell script missing strict mode: add `set -euo pipefail` immediately after the shebang/header.
- `any` / `any[]` in a public/exported type: replace with the real type inferred from usage; it MUST still type-check.
- Missing or non-boxed module/file header: add or normalize to the sk-code OpenCode header convention (cosmetic; in scope).
- catch parameter not typed `unknown`, or exported symbol missing TSDoc: fix only if trivial and behavior-neutral.

DO-NOT-FIX (binding — confirmed false positives; leave untouched):
- `// Feature catalog: <name>` comments are ALLOWED (NOT in comment-hygiene's forbidden list). Do NOT remove them.
- TSDoc `@example` values like `specFolder: 'specs/001-demo'` — illustrative, not real pointers. Leave.
- Any file under a `dist/` directory — build output, regenerated by the build. Do NOT edit.
- Any cited finding that does not reproduce against the live file — mark refuted.

FINDINGS TO FIX (confirm-then-fix each; P0/P1/P2 all in scope):
- [P0] .opencode/skills/system-spec-kit/mcp_server/lib/search/pipeline/stage2-fusion.ts:301 — Ephemeral fix id in score-clamping comment | Comment hygiene hard block: no finding/task ids in comments; durable WHY only. | fix: Drop `F2.03 fix` and retain the downstream-score-safety rationale.
- [P1] .opencode/skills/system-spec-kit/mcp_server/lib/search/pipeline/stage4-filter.ts:47 — Numbered sections are out of order | sk-code code_organization.md:99-106 and TypeScript style_guide.md:133-147 require numbered sections to start at 1 for the first divider present and increment sequentially. | fix: Renumber section dividers sequentially and keep one divider format per file.
- [P1] .opencode/skills/system-spec-kit/mcp_server/lib/search/rerank/retrieval-rescue.ts:86 — Exported functions lack TSDoc | sk-code typescript_checklist.md:207-216 requires exported functions, classes, and interfaces to have TSDoc comments. | fix: Add concise TSDoc above both exported functions describing behavior and return value.
- [P0] .opencode/skills/system-spec-kit/mcp_server/lib/search/vector-index-mutations.ts:282 — TSDoc examples embed numbered spec paths | system-spec-kit constitutional/comment-hygiene.md forbids spec paths and packet numbers in code comments. | fix: Use neutral placeholders or variables instead of specs/001-demo in comments.
- [P2] .opencode/skills/system-spec-kit/mcp_server/lib/search/vector-index-mutations.ts:288 — Snake_case exported TypeScript functions | sk-code references/opencode/typescript/style_guide.md says TypeScript function names use camelCase; legacy alias exception may apply, so severity is P2. | fix: If legacy API requires snake_case, expose camelCase primary functions with snake_case aliases.
- [P0] .opencode/skills/system-spec-kit/mcp_server/lib/search/vector-index-queries.ts:960 — Ephemeral severity/id label in comment | Comment hygiene hard block: comments must not embed perishable finding/task labels; keep durable WHY only. | fix: Replace `HIGH-004 FIX` with the durable reason for concurrent reads.
- [P0] .opencode/skills/system-spec-kit/mcp_server/lib/search/vector-index-queries.ts:355 — TSDoc examples embed numbered spec paths | system-spec-kit constitutional/comment-hygiene.md forbids spec paths and packet numbers in code comments. | fix: Replace numbered sample spec paths with non-artifact placeholders.

After editing, re-read each edited line. Your final message MUST be exactly one fenced json block:
```json
{"seat":"BF15","fixed":[{"file":"path","theme":"","after":""}],"refuted":[{"file":"path","why":""}]}
```
