Act as a general code-implementation agent (seat BF11). Spec folder: /Users/michelkerkmeester/MEGA/Development/Code_Environment/Public/.opencode/specs/system-spec-kit/027-xce-research-based-refinement/005-verification-and-remediation/007-release-alignment-review/004-code-remediation (PRE-APPROVED at Gate 3 — make the edits below directly; do NOT re-ask Gate 3 and do NOT ask the user anything).

FIRST load .opencode/skills/sk-code/SKILL.md, get its OpenCode surface tag, and follow that surface's standards. Also read .opencode/skills/system-spec-kit/constitutional/comment-hygiene.md.

You fix sk-code misalignments. SURGICAL, behavior-NEUTRAL edits only — comment / type / strict-mode / header edits ONLY. Do not change runtime behavior.

ALLOWED WRITE PATHS (edit ONLY these files):
- .opencode/skills/system-skill-advisor/mcp_server/scripts/skill_advisor.py
- .opencode/skills/system-skill-advisor/mcp_server/scripts/verify-zombie-soak.sh
- .opencode/skills/system-spec-kit/mcp_server/context-server.ts
- .opencode/skills/system-spec-kit/mcp_server/handlers/causal-graph.ts
- .opencode/skills/system-spec-kit/mcp_server/handlers/memory-context.ts
BANNED: no delete, no rename, no new files, no edits outside the listed files, NO behavior change, no --no-verify.

FIX RULES by theme:
- Comment-hygiene (ephemeral ids in comments: `FIX RC3-B`, `P4-12`, `packets 006-009`, `SC-002`, ADR-/REQ-/CHK-/task-/finding ids, or a concrete `.opencode/specs/NNN-...` path): DROP the perishable label, KEEP the durable WHY. Never delete an explanatory comment outright — only strip the ephemeral identifier.
- Shell script missing strict mode: add `set -euo pipefail` immediately after the shebang/header.
- `any` / `any[]` in a public/exported type: replace with the real type inferred from usage; it MUST still type-check.
- Missing or non-boxed module/file header: add or normalize to the sk-code OpenCode header convention (cosmetic; in scope).
- catch parameter not typed `unknown`, or exported symbol missing TSDoc: fix only if trivial and behavior-neutral.

DO-NOT-FIX (binding — confirmed false positives; leave untouched):
- `// Feature catalog: <name>` comments are ALLOWED (NOT in comment-hygiene's forbidden list). Do NOT remove them.
- TSDoc `@example` values like `specFolder: 'specs/001-demo'` — illustrative, not real pointers. Leave.
- Any file under a `dist/` directory — build output, regenerated by the build. Do NOT edit.
- Any cited finding that does not reproduce against the live file — mark refuted.

FINDINGS TO FIX (confirm-then-fix each; P0/P1/P2 all in scope):
- [P0] .opencode/skills/system-skill-advisor/mcp_server/scripts/skill_advisor.py:2301 — Research artifact pointer in code comment | Comment hygiene hard block: no ephemeral artifact pointers in code comments; keep durable WHY only. | fix: Remove the `130 research.md` and `closes 130` references; inline the durable routing rationale.
- [P0] .opencode/skills/system-skill-advisor/mcp_server/scripts/verify-zombie-soak.sh:6 — Packet-number comment violates comment hygiene | Comment hygiene hard block: no packet/phase numbers or perishable spec identifiers in code comments. | fix: Replace the packet and scenario labels with the durable launcher-invariant reason for the soak check.
- [P0] .opencode/skills/system-spec-kit/mcp_server/context-server.ts:142 — Ephemeral IDs in comments | sk-code comment hygiene: no packet/phase/task/checklist/requirement/ticket ids in comments; keep durable WHY only. | fix: Remove P/KL/SEC numeric labels and keep the durable behavior/rationale plus stable CWE where useful.
- [P0] .opencode/skills/system-spec-kit/mcp_server/context-server.ts:1037 — Broken section numbering | TypeScript checklist P0: numbered ALL-CAPS section headers must be preserved and sequential. | fix: Number TOOL DEFINITIONS and TOOL DISPATCH, or collapse them and renumber subsequent sections.
- [P2] .opencode/skills/system-spec-kit/mcp_server/context-server.ts:9 — Type-only imports not separated | TypeScript checklist P2: imports used only in type positions use separated `import type`; quick reference puts type-only imports last. | fix: Move type-only imports into a final import group.
- [P2] .opencode/skills/system-spec-kit/mcp_server/context-server.ts:1050 — Unjustified any at MCP boundary | TypeScript quality: prefer unknown over any on public surfaces; permitted any interop needs a justification comment. | fix: Use a local MCP response type or add a precise SDK interop justification.
- [P2] .opencode/skills/system-spec-kit/mcp_server/handlers/causal-graph.ts:6 — Section numbering starts at zero | OpenCode code_organization.md §2 requires the first numbered code section to start at 1 and increment sequentially. | fix: Renumber DEPENDENCIES to section 1 and update following section numbers.
- [P1] .opencode/skills/system-spec-kit/mcp_server/handlers/memory-context.ts:1210 — Non-null assertion without justification | TypeScript checklist P1: non-null assertions must have a preceding justification comment or be replaced with explicit narrowing. | fix: Avoid detectedIntent! by guarding or defaulting before indexing INTENT_TO_MODE.

After editing, re-read each edited line. Your final message MUST be exactly one fenced json block:
```json
{"seat":"BF11","fixed":[{"file":"path","theme":"","after":""}],"refuted":[{"file":"path","why":""}]}
```
