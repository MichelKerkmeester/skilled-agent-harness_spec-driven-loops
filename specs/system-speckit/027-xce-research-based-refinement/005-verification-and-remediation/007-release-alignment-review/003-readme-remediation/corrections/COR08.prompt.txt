Act as a markdown documentation-fixer (correction seat COR08). Spec folder: /Users/michelkerkmeester/MEGA/Development/Code_Environment/Public/.opencode/specs/system-spec-kit/027-xce-research-based-refinement/005-verification-and-remediation/007-release-alignment-review/003-readme-remediation (PRE-APPROVED at Gate 3 — make the edits directly; do NOT re-ask Gate 3 or ask anything).

An accuracy audit found that a PRIOR automated fix wrote WRONG or imprecise values in the file(s) below. Apply each audit-verified correction SURGICALLY: replace the wrong text with the actual truth. The audit already verified the truth against the cited evidence — confirm at the evidence path, then correct.

ALLOWED WRITE PATHS (edit ONLY these):
- .opencode/skills/system-spec-kit/README.md
BANNED: no delete, no rename, no new files, no edits outside the listed files, no --no-verify.

CORRECTIONS (for each: find the wrong text, replace with the actual truth; verify at evidence first):
1. WRONG (now in file): # Run all 37 validation rules
   ACTUAL TRUTH: The validator registry currently defines 38 rules total. Default validation selects 36 non-strict rules; strict mode adds EVIDENCE_MARKER_LINT and conditionally CONTINUITY_FRESHNESS when SPECKIT_COMPLETION_FRESHNESS is enabled, so 'all 37' is not the complete current rule count.
   EVIDENCE: .opencode/skills/system-spec-kit/scripts/lib/validator-registry.json:1-313; .opencode/skills/system-spec-kit/scripts/spec/validate.sh:390-396,828-835
2. WRONG (now in file): `validate.sh` | Run 37 validation rules. Use `--recursive` for phase folders, `--verbose` for details
   ACTUAL TRUTH: validate.sh is backed by a 38-rule registry. It runs 36 default non-strict rules, plus strict-only rules according to strict/env gates.
   EVIDENCE: .opencode/skills/system-spec-kit/scripts/lib/validator-registry.json:1-313; .opencode/skills/system-spec-kit/scripts/spec/validate.sh:390-396,828-835
3. WRONG (now in file): **Result**: A pass/warn/error report across 37 rules with actionable fix instructions.
   ACTUAL TRUTH: The result is not universally across 37 rules: the registry has 38 rules, default validation excludes strict-only rules, and strict validation conditionally includes CONTINUITY_FRESHNESS.
   EVIDENCE: .opencode/skills/system-spec-kit/scripts/lib/validator-registry.json:1-313; .opencode/skills/system-spec-kit/scripts/spec/validate.sh:390-396,828-835
4. WRONG (now in file): [`references/validation/validation_rules.md`](./references/validation/validation_rules.md) | All 37 validation rules with fixes
   ACTUAL TRUTH: The authoritative registry has 38 validation rules; validation_rules.md itself is a partial reference and points to the registry as authoritative.
   EVIDENCE: .opencode/skills/system-spec-kit/scripts/lib/validator-registry.json:1-313; .opencode/skills/system-spec-kit/references/validation/validation_rules.md:70

Important nuance to honor: spec-memory CLI front door = 37 tools; MCP server = 39. Validation registry = 38 rules (36 default non-strict + strict-only). Do not re-introduce the wrong value.

After editing, re-read each corrected line. Final message MUST be one fenced ```json block:
```json
{"seat":"COR08","corrected":[{"file":"","to":""}],"skipped":[{"file":"","why":""}]}
```
