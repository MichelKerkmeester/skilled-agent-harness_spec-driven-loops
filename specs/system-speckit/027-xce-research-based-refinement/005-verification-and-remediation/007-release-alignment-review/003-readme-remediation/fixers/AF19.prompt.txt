Act as a markdown documentation-fixer (seat AF19). Spec folder: /Users/michelkerkmeester/MEGA/Development/Code_Environment/Public/.opencode/specs/system-spec-kit/027-xce-research-based-refinement/005-verification-and-remediation/007-release-alignment-review/003-readme-remediation (PRE-APPROVED at Gate 3 — make the edits below directly with the Edit/Write tools; do NOT re-ask Gate 3 and do NOT ask the user anything).

You fix STALE content in README files so they match current repo reality. SURGICAL edits only — fix the cited claims, keep everything else intact. Find the correct current value by reading the cited file PLUS its SKILL.md / source / config before editing.

ALLOWED WRITE PATHS (edit ONLY these files):
- .opencode/skills/system-spec-kit/references/workflows/agent-io-contract.md
- .opencode/skills/system-spec-kit/references/workflows/quick_reference.md
- .opencode/skills/system-spec-kit/scripts/README.md
- .opencode/skills/system-spec-kit/scripts/memory/README.md
- .opencode/skills/system-spec-kit/scripts/setup/README.md
BANNED: no delete, no rename, no new files, no edits outside the listed files, no --no-verify.

DO-NOT-FIX (binding — confirmed false positives; leave untouched):
- The CLI front-door "37 tools" figure: `spec-memory.cjs` CLI is genuinely 37; only the MCP-server/daemon surface is 39. Fix a "37" ONLY when the sentence is about the MCP server/daemon, NOT the CLI front door.
- Any `// Feature catalog:` comments and TSDoc `@example` values.
- Any cited claim that does NOT actually reproduce against the live file — mark it refuted; never invent a fix.

FINDINGS TO FIX (confirm-then-fix each; P0/P1/P2 all in scope, including cosmetic/style):
- [P1] .opencode/skills/system-spec-kit/references/workflows/agent-io-contract.md:37 — Agent roster omits current deep-loop agents | stale: Dispatch header agent enum lists @context, @code, @review, @debug, @markdown, @ai-council, and @deep-research. -> correct: Current OpenCode agents also include deep-context, deep-review, deep-improvement, prompt-improver, and orchestrate.
- [P1] .opencode/skills/system-spec-kit/references/workflows/quick_reference.md:103 — SpecKit command namespace is stale | stale: Command map uses /spec_kit:resume, /spec_kit:plan, /spec_kit:implement, and /spec_kit:complete. -> correct: Current command files are under .opencode/commands/speckit/ and current gates reference /speckit:resume.
- [P1] .opencode/skills/system-spec-kit/references/workflows/quick_reference.md:107 — Deep command map omits current modes | stale: SpecKit workflow command map lists only /deep:research and /deep:review. -> correct: The deep-loop registry includes context, research, review, ai-council, and agent-improvement modes with commands /deep:context, /deep:research, /deep:review, /deep:ai-council, and /deep:agent-improvement.
- [P1] .opencode/skills/system-spec-kit/references/workflows/quick_reference.md:166 — Continuity edit rule conflicts with current gate | stale: Do not use Write/Edit tools to author continuity surfaces directly. -> correct: AGENTS.md explicitly allows quick direct edits to _memory.continuity in implementation-summary.md without generate-context.js.
- [P2] .opencode/skills/system-spec-kit/scripts/README.md:58 — root topology omits active script zones | stale: The root package topology lists the script folder structure but omits current README-bearing zones such as config/ and validation/. -> correct: config/ is the current config barrel boundary and validation/ contains active validators used by validation flow, pre-commit and write-time comment hygiene.
- [P1] .opencode/skills/system-spec-kit/scripts/memory/README.md:181 — generate-context flow overstates indexing | stale: The canonical save flow ends with "context artifacts and index are updated" and the outputs table says memory index updates and embedding rows are written through the CLI flow. -> correct: Step 11.5 does not always index. When the mk-spec-memory daemon is alive, standalone indexing is skipped to avoid a second SQLite writer and the caller is told to finish via MCP memory_index_scan.
- [P2] .opencode/skills/system-spec-kit/scripts/setup/README.md:30 — setup topology omits installer helper surface | stale: The setup/ package topology only lists prerequisite, native-module and Node-version helper scripts. -> correct: setup/ also ships install.sh plus _utils.sh. install.sh is a Spec Kit Memory MCP installer that installs dependencies, builds the TypeScript workspace and runs native-module health checks.

After editing, re-read each edited line to verify. Your final message MUST be exactly one fenced json block:
```json
{"seat":"AF19","fixed":[{"file":"path","claim":"what was stale","after":"what it now says"}],"refuted":[{"file":"path","why":"why skipped"}]}
```
