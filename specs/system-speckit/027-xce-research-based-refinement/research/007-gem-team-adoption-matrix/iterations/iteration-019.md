# Iteration 019: RQ-C completeness critic

**Focus:** RQ-C completeness critic  
**Executor:** cli-opencode `openai/gpt-5.5-fast` --variant high (read-only). **Status:** complete. **newInfoRatio:** 0.44.  
**Raw output:** prompts/iteration-019.out  ·  **Prompt:** prompts/iteration-019.prompt

### COMPLETENESS REVIEW
#### 1. Coverage gaps
- Specialized leaf-agent playbooks were under-carried. The 13 angles covered broad orchestration/quality/security, but not the concrete value of `gem-code-simplifier`, `gem-browser-tester`, `gem-designer`, and `gem-devops` as narrowly scoped operating modes. Gem explicitly lists browser testing, code simplification, design, and DevOps agents (`specs/system-spec-kit/027-xce-research-based-refinement/external/gem-team-main/README.md:202-217`; `specs/system-spec-kit/027-xce-research-based-refinement/external/gem-team-main/AGENTS.md:33-50`). The useful slice is not “add more agents”; it is mode-specific checklists/presets: behavior-preserving simplification (`gem-code-simplifier.agent.md:46-63`), E2E evidence capture with console/network/a11y signals (`gem-browser-tester.agent.md:46-61`), and deployment approval/health-check contracts (`gem-devops.agent.md:46-61`).
- PRD.yaml as a living operational spec was examined as “knowledge layers,” but not deeply as a write-back control surface. Gem defines `docs/PRD.yaml` as the product requirements spec driving planning, implementation, and verification (`README.md:119-125`), updates decisions back into PRD during learning persistence (`README.md:165-171`), and has planner fields for acceptance criteria plus `prd_update_recommended` (`gem-planner.agent.md:82-86`, `gem-planner.agent.md:106-107`). Spec-kit already has richer spec folders, so the candidate is not PRD adoption; it is an explicit “requirements drift/write-back recommendation” field for subagent/planner outputs.
- Planner self-scoring and reviewer-focus were under-weighted. Gem planner computes `quality_score`, `reviewer_focus`, wave counts, dependencies, and risk (`gem-planner.agent.md:108-110`, `gem-planner.agent.md:169-182`), and the CHANGELOG calls out adding `quality_score`, `reviewer_focus`, and codebase validation to planning/critique (`CHANGELOG.md:65-74`). This could be a low-cost way to route review attention without adding new gates.
- Some CHANGELOG signals were found but not promoted into the surviving candidate list. Iteration 012 identified focus-area research findings lookup, plan-template cache, shared-component pre-save checks, failure-log reinjection, and accessibility snapshots as concrete signals (`iteration-012.md:10-15`, `iteration-012.md:17-21`). The strongest missed-for-subpacket item is probably focus-scoped research lookup before broad memory expansion, because it is narrower than the context-envelope candidate and more actionable (`CHANGELOG.md:236-237`; `iteration-012.md:18-19`).

#### 2. Strongest counter-argument
The strongest argument against adopting anything is that the adversarial pass already proved most value is overlap dressed as novelty. The original strategy set a high bar because spec-kit is already mature across memory, code graph, skill advisor, deep-loop, gates, and agent systems (`deep-research-strategy.md:131-136`), and iterations 014-098 downgraded every top claim to narrow adapter slices rather than new mechanisms (`iteration-014.md:17-20`; `iteration-015.md:17-20`; `iteration-016.md:16-17`; `iteration-017.md:17-20`; `iteration-018.md:17-20`). The counter-argument is therefore maintenance cost and schema/gate creep: Gem Team is a compact YAML/JSON-first framework, while spec-kit already relies on richer spec-folder governance, evidence-heavy markdown contracts, and Logic-Sync escalation.

#### 3. Weakest candidates (drop or downgrade)
- Drop “OWASP standing scan” as a sub-packet; downgrade to naming/checklist discoverability. Iteration 018 found broad existing security coverage and narrowed the gap to OWASP labeling or a command alias, not missing security review (`iteration-018.md:10`, `iteration-018.md:14`, `iteration-018.md:18`).
- Downgrade memory taxonomy + promotion thresholds to tag-level refinement only. Iteration 015 found existing `contextType`, `importanceTier`, `memoryType`, causal links, quality fields, and search filters; the exact `gotcha` / `failure_mode` labels are missing, but the remaining value is “ADAPT-XS” and should not change canonical memory types (`iteration-015.md:11`, `iteration-015.md:15`, `iteration-015.md:20`).
- Drop full distribution/APM adoption. The only plausible slice is drift-control or a generator for existing runtime dirs; APM manifest, marketplace packaging, release automation, and all-harness install are premature for an in-repo system (`iteration-013.md:10-13`, `iteration-013.md:20-23`; `apm.yml:1-20`).

#### 4. Under-weighted risks on the strongest candidates
- Typed agent I/O and dispatch schemas risk flattening evidence-rich agent outputs into brittle protocol compliance. Iteration 018 says existing agents already have structured contracts, just not one uniform schema (`iteration-018.md:15`), and iteration 011 explicitly rules out JSON-only replacement because review/context outputs need rich markdown evidence and recommendations (`iteration-011.md:23-27`). This should be an adapter/header, not a rewrite.
- Diagnose-then-fix handoff gating risks false confidence and workflow latency if applied outside true debug handoffs. The surviving value is only a machine-checkable handoff schema for `root_cause`, `target_files`, and `fix_recommendations` (`iteration-014.md:15`, `iteration-014.md:20`), while spec-kit already has a strong @debug method and @code surgical-fix expectations (`iteration-014.md:14-15`).
- Contract-first ordering and pre-mortem risk becoming universal ceremony. Iteration 014 narrowed contract-first to boundary/API/schema work and rejected universal test-first as a hard blocker (`iteration-014.md:18`); iteration 016 narrowed resilience to a mandatory medium/high pre-mortem field, not a wholesale failure-taxonomy overhaul (`iteration-016.md:16-17`).

### NET RECOMMENDATION
Worth 027 sub-packets: a compact typed dispatch/output adapter that preserves markdown bodies; a narrow debugger-diagnosis handoff gate; and a medium/high pre-mortem or planner reviewer-focus refinement, possibly combined with boundary contract-first checks. Drop full OWASP, memory-taxonomy, and APM/distribution packets; keep them as aliases, tags, or future drift-control notes. Auto-skills should remain proposal-only and low priority.

### METRICS
newInfoRatio: 0.44
novelty: The main new information is that specialized leaf-agent playbooks, PRD write-back semantics, and planner reviewer-focus scoring were under-carried even after the adversarial narrowing.
status: complete
focus: RQ-C completeness critic
