# Iteration 015: RQ-V2 adversarial — multi-agent context (context-package + memory taxonomy)

**Focus:** RQ-V2 adversarial — multi-agent context (context-package + memory taxonomy)  
**Executor:** cli-opencode `openai/gpt-5.5-fast` --variant high (read-only). **Status:** complete. **newInfoRatio:** 0.38.  
**Raw output:** prompts/iteration-015.out  ·  **Prompt:** prompts/iteration-015.prompt

### VERDICTS
| Claim | Original verdict | Adversarial result (upheld/downgraded/refuted) | Revised verdict | Strongest counter-evidence (file:line) |
|---|---|---|---|---|
| 1 — Context Package dispatch snapshot + typed read-directives | ADAPT, lead candidate | downgraded | ADAPT-S: only add a typed dispatch header/read-directives if needed; do not replace existing Context Package/session payload machinery | `.opencode/agents/context.md:230`, `.opencode/skills/system-spec-kit/mcp_server/handlers/session-resume.ts:665`, `.opencode/skills/system-spec-kit/mcp_server/lib/context/shared-payload.ts:247` |
| 2 — Memory taxonomy: gotcha/failure_mode as first-class retrieval labels | ADAPT | downgraded | ADAPT-XS: label/tag-level refinement only; existing taxonomy covers most retrieval value, but these exact labels are not first-class | `.opencode/skills/system-spec-kit/mcp_server/lib/parsing/memory-parser.ts:61`, `.opencode/skills/system-spec-kit/mcp_server/lib/config/memory-types.ts:34`, `.opencode/skills/system-spec-kit/shared/context-types.ts:9` |

### EVIDENCE
- [V-015-01] Claim 1 context-package snapshot: The gap is materially smaller than claimed. `@context` is explicitly the exploration gateway and synthesizes a structured Context Package after combining continuity, memory, graph, and codebase findings (`.opencode/agents/context.md:50-59`). Its required output is already a consolidated package with Memory Context, Codebase Findings, Pattern Analysis, Nested Dispatch Status, Gaps & Unknowns, and Recommendation (`.opencode/agents/context.md:230-284`). `memory_context` is also a real L1 orchestration layer with auto/quick/deep/focused/resume modes (`.opencode/skills/system-spec-kit/mcp_server/handlers/memory-context.ts:914-952`) and routes deep/focused/resume strategies into consolidated MCP responses (`.opencode/skills/system-spec-kit/mcp_server/handlers/memory-context.ts:992-1098`). Even stronger: `session_resume` builds a single `payloadContract` with `memory-resume`, `code-graph-status`, and `structural-context` sections plus provenance (`.opencode/skills/system-spec-kit/mcp_server/handlers/session-resume.ts:665-709`) and an OpenCode transport plan (`.opencode/skills/system-spec-kit/mcp_server/handlers/session-resume.ts:710-719`). What is not present is the Gem-specific persisted per-plan `context_envelope.json`, guaranteed “hand this exact snapshot to every subagent” dispatch invariant, progressive per-wave enrichment, or typed `safe_to_assume` / `verify_before_use` / `do_not_re_read` fields. The shared payload schema has `key/title/content/source/certainty/structuralTrust` sections and provenance, not read-directive fields (`.opencode/skills/system-spec-kit/mcp_server/lib/context/shared-payload.ts:247-299`).
- [V-015-02] Claim 2 memory taxonomy: The exact `gotcha` / `failure_mode` labels are not first-class memory retrieval types, but spec-kit already has several overlapping taxonomies. Parsed memory rows carry `contextType`, `importanceTier`, `memoryType`, `memoryTypeSource`, `memoryTypeConfidence`, causal links, `documentType`, and quality fields (`.opencode/skills/system-spec-kit/mcp_server/lib/parsing/memory-parser.ts:61-82`). `memory_type` is a cognitive/decay taxonomy with `working`, `episodic`, `prospective`, `implicit`, `declarative`, `procedural`, `semantic`, `autobiographical`, and `meta-cognitive` (`.opencode/skills/system-spec-kit/mcp_server/lib/config/memory-types.ts:34-43`), backed by schema/index migration (`.opencode/skills/system-spec-kit/mcp_server/lib/search/vector-index-schema.ts:621-625`). `context_type` is deliberately narrow: `implementation`, `research`, `planning`, `general`, with legacy `decision`/`discovery` aliases (`.opencode/skills/system-spec-kit/shared/context-types.ts:9-13`), and DB constraints enforce only those canonical context values plus the importance-tier enum (`.opencode/skills/system-spec-kit/mcp_server/lib/search/vector-index-schema.ts:2609-2614`). Public search filtering exposes `tier` and `contextType`, not a first-class `gotcha` or `failure_mode` filter (`.opencode/skills/system-spec-kit/mcp_server/schemas/tool-input-schemas.ts:154-167`; `.opencode/skills/system-spec-kit/mcp_server/lib/search/vector-index-queries.ts:283-300`). Scoped grep across `handlers`, `lib`, and `schemas` found `gotcha` absent and `failure_mode` only as incidental error/test wording, not a memory taxonomy.

### REVISED NET-NEW VALUE
Claim 1: Genuine value is not “create a context package” because spec-kit already has one. The sub-packet-worthy slice is a small typed dispatch preamble: stable snapshot id, file shortlist, and read-directive buckets layered onto existing Context Package / `payloadContract`.

Claim 2: Genuine value is a lightweight reusable-error label surface, not a new memory system. Best fit is probably tags/learned triggers or a narrow retrieval facet for `gotcha` and `failure_mode`; making them canonical `context_type` or `memory_type` values would fight existing schemas.

### METRICS
newInfoRatio: 0.38
novelty: The main new information is that `session_resume` already has a shared payload envelope and OpenCode transport plan, further shrinking Claim 1 beyond the earlier @context-only counterargument.
status: complete
focus: RQ-V2 adversarial — multi-agent context (context-package + memory taxonomy)
