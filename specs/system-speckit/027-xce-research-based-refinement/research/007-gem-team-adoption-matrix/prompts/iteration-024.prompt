## Context
You are MiMo-V2.5-Pro giving an INDEPENDENT cross-model second opinion on a completed adoption study. SUBJECT A = "Gem Team" at `specs/system-spec-kit/027-xce-research-based-refinement/external/gem-team-main/`. SUBJECT B = this repo's "spec-kit" (`.opencode/agents/**`, `.opencode/skills/**`). A prior phase-007 study run by a DIFFERENT model (gpt-5.5-fast) — 13 analysis angles + 5 adversarial verifiers + 1 completeness critic — concluded **"validate-don't-transform": the spec-kit is mature; every candidate was downgraded; 3 sub-packets survived**. Iteration 024, READ-ONLY. Spec folder pre-approved — skip Gate-3; change nothing. Your value is disagreement where warranted: a fresh model catching over- or under-claims.

## Objective
Independently judge the phase-007 synthesis: does the "validate-don't-transform" verdict hold, and is EACH surviving sub-packet (P1 typed-agent-io-adapter; P2 scoped-preexec-and-handoff-gates; P3 planner-review-focus-and-drift-hint) correctly scoped and genuinely net-new — not already covered by the spec-kit, and not over-trimmed?

## Style
precise, no preamble

## Tone
neutral, independent, willing to disagree

## Audience
automated pipeline — parsed by a script. Preamble, prose wrapping, or writing to a file BREAKS parsing.

## Response
Return your whole analysis as your FINAL TEXT MESSAGE. Do NOT write/create/edit any file. Output EXACTLY this, nothing before "### CROSS-MODEL ASSESSMENT":
### CROSS-MODEL ASSESSMENT
| Item | gpt-5.5 conclusion | MiMo verdict (agree / disagree / refine) | Evidence (file:line) |
(rows: Overall verdict; P1; P2; P3)
### MISSED OR OVER-CLAIMED
(anything the gpt-5.5 run got wrong, missed, or over/under-scoped — with file:line)
### METRICS
newInfoRatio: <0.00-1.00>
novelty: <one sentence>
status: complete
focus: RQ-MV cross-model validation

---
Pre-plan (lean):
1. Read `specs/system-spec-kit/027-xce-research-based-refinement/research/007-gem-team-adoption-matrix/research.md` (§1, §3, §6, §7) + `sub-packet-proposals.md` (P1/P2/P3 scope + risk).
2. Spot-check 2-3 load-bearing claims against the real spec-kit files (e.g. `.opencode/agents/code.md`, `context.md`, `debug.md`) and `external/gem-team-main/README.md`/`AGENTS.md`.
3. For the overall verdict + each sub-packet: agree / disagree / refine, with file:line. Flag anything missed or over-claimed.
