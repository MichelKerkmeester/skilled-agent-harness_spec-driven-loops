# Research Phase 001: XCE Adoption Matrix

Initial XCE feature triage for the 027 packet.

## Contents

| Path | Purpose |
|---|---|
| `iterations/` | Iteration narratives `001-010`. |
| `deltas/` | JSONL deltas `001-010`. |
| `prompts/` | pt01 prompt inputs. |
| `sub-packet-proposals.md` | Initial sub-packet proposal set from the adoption matrix. |

## Scope

This phase classifies XCE ideas into adopt, adapt, defer, and skip categories, then proposes candidate follow-up packets. Later phases narrow or supersede parts of this initial framing.
