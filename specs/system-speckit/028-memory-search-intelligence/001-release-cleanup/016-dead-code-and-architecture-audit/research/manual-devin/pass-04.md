Now I have enough evidence. Let me compile the report.

---

# Dead Code & Architecture Audit — Runtime Mirrors & Root Config

**Scope:** `.claude/`, `.codex/`, `.cursor/`, `.devin/` mirror directories + repo-root config and dotfiles.
**Spec folder:** `.opencode/specs/system-speckit/028-memory-search-intelligence/001-release-cleanup/016-dead-code-and-architecture-audit`
**Mode:** Read-only. No files were modified.

---

### F1 — CAT-2: `.claude/agents/` is a stale physical mirror of canonical `.opencode/agents/`

**Path:** `.claude/agents/` (real directory, not a symlink)

**Evidence:** Every `.claude/agents/*.md` file has fewer lines than its `.opencode/agents/*.md` canonical counterpart — all 13 agents are behind:

| Agent | `.claude/agents/` | `.opencode/agents/` | Delta |
|---|---:|---:|---:|
| ai-council | 782 | 800 | -18 |
| code | 530 | 545 | -15 |
| context | 448 | 469 | -21 |
| debug | 637 | 652 | -15 |
| deep-alignment | 542 | 559 | -17 |
| deep-improvement | 238 | 253 | -15 |
| deep-research | 573 | 590 | -17 |
| deep-review | 554 | 571 | -17 |
| design | 124 | 138 | -14 |
| markdown | 308 | 323 | -15 |
| orchestrate | 803 | 814 | -11 |
| prompt-improver | 356 | 371 | -15 |
| review | 483 | 499 | -16 |

The other `.claude/` subdirectories (`skills`, `specs`, `commands`, `changelog`) are symlinks to `.opencode/`; `agents` is the only physical-copy directory and has drifted. The `.codex/agents/*.toml` files are auto-generated by `.opencode/skills/system-spec-kit/scripts/codex/sync-agents.cjs`; no equivalent generator exists for `.claude/agents/`.

**Verify:**
```bash
wc -l .claude/agents/*.md .opencode/agents/*.md
```

**Blast radius:** Low. Claude Code runtime loads stale agent definitions; other runtimes (OpenCode, Codex) load the canonical content. Behavioral divergence between Claude and other runtimes is silent.

---

### F2 — CAT-5: CI mirror-sync checker misses 12 of 13 drifted agents due to token-set comparison

**Path:** `.opencode/skills/system-deep-loop/deep-improvement/scripts/lib/mirror-sync-verify.cjs`

**Evidence:** `mirror-sync-verify.cjs:71-74` tokenizes the agent body into a `Set` of lowercase 3+ char tokens, then `compareBodyTokens()` at `:83-96` compares sets for missing/unexpected tokens. Set-based comparison cannot detect content additions where all new tokens already appear elsewhere in the file. Running the checker with `--all` against the 13 drifted agents from F1 catches only 1:

```
$ node .opencode/skills/system-deep-loop/deep-improvement/scripts/check-agent-mirror-sync.cjs --all
DRIFT  orchestrate [claude] missing=["mirror"] unexpected=[]
BLOCKED: 1 agent(s) have out-of-sync runtime mirrors: orchestrate
```

The other 12 agents pass despite 11-21 line differences because the added content's tokens are all already present in the stale copy.

**Verify:**
```bash
node .opencode/skills/system-deep-loop/deep-improvement/scripts/check-agent-mirror-sync.cjs --all
```

**Blast radius:** Medium. The `agent-mirror-sync.yml` CI gate is the sole automated guard for agent mirror drift. 92% of drift escapes it. A simpler shape: compare normalized body text with exact string equality (after frontmatter strip + runtime-specific text normalization), not token-set equality.

---

### F3 — CAT-5: `agent-mirror-sync.yml` detects `.codex/agents/` changes but checker never compares them

**Path:** `.github/workflows/agent-mirror-sync.yml:29` + `.opencode/skills/system-deep-loop/deep-improvement/scripts/lib/mirror-sync-verify.cjs:17-20`

**Evidence:** The CI workflow's file-detection grep includes `.codex/agents/`:
```
git diff --name-only --diff-filter=ACMD "$BASE" "$HEAD" | grep -E '^\.(opencode|claude|codex)/agents/'
```
But `mirror-sync-verify.cjs:17-20` `RUNTIME_MIRRORS` array only has `opencode` and `claude` entries — no `codex`. When a `.codex/agents/code.toml` path is passed to the checker, `agentNameFromPath()` extracts `code`, then `verifyMirrorSync()` compares only `.opencode/agents/code.md` vs `.claude/agents/code.md`. The `.codex/agents/code.toml` file is never read or compared.

**Verify:**
```bash
grep -n 'RUNTIME_MIRRORS' .opencode/skills/system-deep-loop/deep-improvement/scripts/lib/mirror-sync-verify.cjs
grep -n 'codex.*agents' .github/workflows/agent-mirror-sync.yml
```

**Blast radius:** Medium. `.codex/agents/*.toml` drift is invisible to CI. The workflow claims to guard all three runtimes but only guards two.

---

### F4 — CAT-1: Root `package.json` `"dev"` script references non-existent `src/3_staging/server.js`

**Path:** `package.json:4`

**Evidence:** `package.json` defines `"dev": "node src/3_staging/server.js"` but the `src/` directory does not exist at the repo root:
```
$ ls src/3_staging/server.js
ls: src/3_staging/server.js: No such file or directory
$ ls src/
ls: src/: No such file or directory
```

**Verify:**
```bash
cat package.json | grep -A1 '"dev"'
ls src/3_staging/server.js
```

**Blast radius:** Low. `npm run dev` fails. The real package.json is `.opencode/package.json` (different deps: `@opencode-ai/plugin`, `vitest`, `vite`, `esbuild`).

---

### F5 — CAT-1: Root `package.json` dependencies (`chokidar`, `cors`, `express`) have zero imports in the codebase

**Path:** `package.json:7-9`

**Evidence:** `package.json` declares `chokidar`, `cors`, and `express` as dependencies. None are imported anywhere:
```
$ grep -r "require.*chokidar\|from ['\"]chokidar" --include="*.js" --include="*.cjs" --include="*.mjs" --include="*.ts" .
(no matches)
$ grep -r "require.*express\|from ['\"]express['\"]" --include="*.js" --include="*.cjs" --include="*.mjs" --include="*.ts" .
(no matches)
$ grep -r "require.*cors\|from ['\"]cors['\"]" --include="*.js" --include="*.cjs" --include="*.mjs" --include="*.ts" .
(no matches)
```
The root `node_modules/` (80 entries, gitignored) installs these unused deps. The `.opencode/package.json` has completely different dependencies.

**Verify:**
```bash
grep -rn "require.*chokidar\|from ['\"]chokidar" --include="*.{js,cjs,mjs,ts}" .
grep -rn "require.*express\|from ['\"]express['\"]" --include="*.{js,cjs,mjs,ts}" .
```

**Blast radius:** Low. Unused dependencies inflate install time and audit surface. The root `package.json` + `package-lock.json` (32 KB) appear vestigial from a prior project structure.

---

### F6 — CAT-1: `karabiner.json` shortcut #5 references non-existent agent `write.md`

**Path:** `karabiner.json` (shortcut key `Cmd+Ctrl+5`)

**Evidence:** The Karabiner shortcut pastes `"Create or update documentation with [runtime_agent_path]/write.md subagent(s)"`. No `write.md` agent exists in `.opencode/agents/` or `.claude/agents/`. The 13 actual agents are: ai-council, code, context, debug, deep-alignment, deep-improvement, deep-research, deep-review, design, markdown, orchestrate, prompt-improver, review. The documentation agent is `markdown.md`, not `write.md`.

**Verify:**
```bash
grep -o 'write\.md' karabiner.json
ls .opencode/agents/write.md .claude/agents/write.md 2>&1
ls .opencode/agents/markdown.md
```

**Blast radius:** Low. The shortcut pastes a dead reference into the clipboard; the AI runtime would fail to resolve `[runtime_agent_path]/write.md`.

---

### F7 — CAT-4: `karabiner.json` is a personal macOS keyboard config misplaced in a public repo

**Path:** `karabiner.json`

**Evidence:** The file is a Karabiner-Elements complex-modifications profile with machine-specific keyboard shortcuts for AI workflows. It is not referenced by any framework config, CI workflow, or build process. It is not in `.gitignore`. It contains personal workflow shortcuts (clipboard pastes for sequential thinking, orchestrate dispatch, review dispatch).

**Verify:**
```bash
grep -rn 'karabiner' .github/ .opencode/ .claude/ .codex/ .cursor/ .devin/ opencode.json .gitignore 2>/dev/null | grep -v Binary
```

**Blast radius:** Low. No functional impact on the framework. Personal config in a public repo leaks workflow details and adds noise.

---

### F8 — CAT-3: `.rename-engine-disposable` is scratch residue not in `.gitignore`

**Path:** `.rename-engine-disposable`

**Evidence:** 42-byte file containing `"semantic-rename-engine disposable fixture"`. Not in `.gitignore`. Not referenced by any config, script, or workflow. Appears to be a disposable marker file from a rename-engine test or operation.

**Verify:**
```bash
cat .rename-engine-disposable
grep -n 'rename-engine-disposable' .gitignore
grep -rn 'rename-engine-disposable' .github/ .opencode/bin/ .opencode/scripts/ 2>/dev/null
```

**Blast radius:** Low. Stray file with no purpose.

---

### F9 — CAT-5: `.codex/config.toml` MCP config drifted from `opencode.json`/`.claude/mcp.json` — missing 2 servers, different Node version

**Path:** `.codex/config.toml`

**Evidence:** `.codex/config.toml` defines 3 MCP servers (mk-spec-memory, mk_skill_advisor, mk_code_index). `opencode.json` and `.claude/mcp.json` define 5 (those 3 plus `sequential_thinking` and `code_mode`). The Codex config is missing `sequential_thinking` and `code_mode`.

Additionally, `.codex/config.toml` hardcodes `/Users/michelkerkmeester/.nvm/versions/node/v22.23.1/bin/node` for all servers, while `opencode.json`/`.claude/mcp.json` use PATH-resolved `"node"` for most servers and `/Users/michelkerkmeester/.nvm/versions/node/v24.9.0/bin/node` for `code_mode`. Current `node --version` is `v22.23.1`; the `v24.9.0` path referenced by `code_mode` is UNKNOWN (cannot verify existence from this sandbox).

**Verify:**
```bash
grep -c 'mcp_servers' .codex/config.toml
grep -c '"mcpServers"' opencode.json
grep 'sequential_thinking\|code_mode' .codex/config.toml
grep 'node/v' .codex/config.toml opencode.json .claude/mcp.json
node --version
```

**Blast radius:** Medium. Codex runtime lacks `sequential_thinking` and `code_mode` MCP servers. Machine-specific absolute Node paths break portability and diverge between runtimes.

---

### F10 — CAT-5: `.claude/mcp.json` duplicates `opencode.json` MCP config — two sources of truth

**Path:** `.claude/mcp.json` + `opencode.json`

**Evidence:** Both files define the same 5 MCP servers (sequential_thinking, mk-spec-memory, mk_skill_advisor, mk_code_index, code_mode) with identical env vars and args, but in different formats (Claude format vs OpenCode format). `.mcp.json` at repo root is a symlink to `.claude/mcp.json`, and `.cursor/mcp.json` is a symlink to `.mcp.json`. The env var blocks (including all `_NOTE_*` comments) are duplicated verbatim. Any change to one must be manually replicated to the other.

**Verify:**
```bash
grep -c 'mcpServers\|"mcp"' .claude/mcp.json opencode.json
grep '_NOTE_DB' .claude/mcp.json opencode.json
```

**Blast radius:** Medium. Config drift between the two copies is silent — no CI guard checks MCP config parity. A simpler shape: single source in `opencode.json`, with `.claude/mcp.json` generated from it by a format-converting sync script (analogous to `sync-agents.cjs` for `.codex/agents/*.toml`).

---

### F11 — CAT-1: `.github/hooks/scripts/` reference non-existent `dist/hooks/copilot/` directory

**Path:** `.github/hooks/scripts/session-start.sh:11` + `.github/hooks/scripts/user-prompt-submitted.sh:11`

**Evidence:** Both scripts check for `.opencode/skills/system-spec-kit/mcp-server/dist/hooks/copilot/session-prime.js` and `user-prompt-submit.js` respectively. The `copilot/` directory does not exist under `dist/hooks/`:
```
$ ls .opencode/skills/system-spec-kit/mcp-server/dist/hooks/
claude  codex  cursor  devin  (no copilot)
```
Both scripts have a fallback path (lines 12-17 in `session-start.sh`, lines 12-19 in `user-prompt-submitted.sh`) that runs when the file is missing, so they don't crash — but the primary code path is dead.

**Verify:**
```bash
ls .opencode/skills/system-spec-kit/mcp-server/dist/hooks/copilot/ 2>&1
grep -n 'copilot' .github/hooks/scripts/session-start.sh
grep -n 'copilot' .github/hooks/scripts/user-prompt-submitted.sh
```

**Blast radius:** Low. GitHub Copilot integration falls back to a stub. The `copilot/` hooks were never built or were removed.

---

### F12 — CAT-1: `routing-registry-drift.yml` trigger path references non-existent spec directory

**Path:** `.github/workflows/routing-registry-drift.yml:18` and `:33`

**Evidence:** The workflow's `paths` trigger includes `.opencode/specs/sk-doc/019-skill-routing-refactor/015-router-unification-program/**`. This directory does not exist. The `019-skill-routing-refactor/` spec tree has `015-cutover-hardening`, not `015-router-unification-program`:
```
$ ls .opencode/specs/sk-doc/019-skill-routing-refactor/
... 015-cutover-hardening ... (no 015-router-unification-program)
```

**Verify:**
```bash
grep '015-router-unification-program' .github/workflows/routing-registry-drift.yml
ls .opencode/specs/sk-doc/019-skill-routing-refactor/015-router-unification-program/ 2>&1
ls .opencode/specs/sk-doc/019-skill-routing-refactor/ | grep 015
```

**Blast radius:** Low. The stale trigger path never fires; the workflow still runs on other valid triggers.

---

### F13 — CAT-3: `.gitignore` has stale entries for paths that no longer exist + duplicate entries

**Path:** `.gitignore`

**Evidence:** Multiple stale entries reference paths that don't exist on disk:

| Line(s) | Entry | Exists? |
|---|---|---|
| 25 | `.env.pre-pat-removal.bak` | No |
| 255 | `/ai_systems` | No |
| 256 | `/barter` | No |
| 268 | `.opencode/barter` | No |
| 271-272 | `.opencode/specs/barter` (duplicated) | Yes (but listed twice) |
| 276-277 | `.claude/specs/barter` (duplicated) | Yes (but listed twice) |
| 279-282 | `.codex/specs/barter` (duplicated) | No (and listed twice) |
| 284-287 | `.agents/specs/*` (4 entries) | No (`.agents/` directory doesn't exist) |
| 292 | `AGENTS_Barter.md` | No |

**Verify:**
```bash
ls /ai_systems /barter .opencode/barter .agents AGENTS_Barter.md .env.pre-pat-removal.bak 2>&1
grep -n 'barter' .gitignore
grep -n '.agents/' .gitignore
```

**Blast radius:** Low. Dead `.gitignore` entries don't cause functional issues but add maintenance noise and obscure active entries.

---

### F14 — CAT-1: `.env.example` is silently ignored by `.gitignore` `.env.*` pattern with no negation

**Path:** `.gitignore:22` + `.env.example`

**Evidence:** `.gitignore:22` has pattern `.env.*` which matches `.env.example`. There is no `!.env.example` negation anywhere in the file. The file exists on disk but is silently ignored by git. `.env.example` is conventionally a tracked template file that documents required environment variables for new users.

**Verify:**
```bash
grep -n '\.env\.' .gitignore
grep -n 'env\.example' .gitignore
ls .env.example
```

**Blast radius:** Low-Medium. New users cloning the repo won't receive `.env.example` through git. If it was force-added before the ignore rule, it may still be tracked — UNKNOWN (git commands blocked in this sandbox). A simpler shape: add `!.env.example` negation after line 22.
