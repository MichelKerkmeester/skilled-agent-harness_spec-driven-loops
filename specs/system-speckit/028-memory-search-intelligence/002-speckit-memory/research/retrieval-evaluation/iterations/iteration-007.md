---
title: "Iteration 7 (A7): reindex-as-consolidation + the 019 maintenance-grace TTL"
trigger_phrases: []
---
# Iteration 7 (A7): reindex-as-consolidation + the 019 maintenance-grace TTL

> Model: **Opus 4.8 via claude2** (acct#2, read-only, 1200s + read-cap). Orchestrator-written. newInfoRatio **0.7**. **Reindex IS the right host for the cosine half of reorg; the 019 TTL is a relationship, not a constant.**

## Candidates
1. **A7-1-reorg-pass-inside-reindex → NET-NEW (H/M).** Add a cosine-reconsolidation sweep as a 4th `runIndexScan` phase over `successfullyIndexedFiles`, reusing the scan's primitives. 018's responsive scan **already supplies every primitive** Initiative B's cadence-reorg needs: cancellation between units (`memory-index.ts:976-980`), a 200-row `setImmediate` macrotask-yield + cancel-check (`:1136-1148`), embedding-drain ordering (`:995,1063`), a ref-counted maintenance marker refreshed at phase boundaries (`:1440-1484`). The per-doc reorg unit is already implemented synchronously on-save: `reconsolidate()` = embed→K-NN→`determineAction` (≥0.88 MERGE / [0.75,0.88) supersede-edge / <0.75 store) (`reconsolidation.ts:202`). **`synthesis/06:117` calls cadence-reorg "a shape we don't have" — unaware 018 concurrently shipped exactly that shape.**
2. **A7-2-split-initiative-b-by-doctrine → EXTENDS 027 doctrine (H/S).** Host only the LLM-free embedding-driven reconsolidation (merge/supersede) inside reindex behind a shadow flag; keep `LT-bg-sleeptime-agent` + `LT-llm-transcript-chunking` (transcript→archival) as a separate governed packet. Why: reorg MUTATES (content merge + `supersedes` edges) → breaks the scan's re-run-is-safe idempotency (`memory-index.ts:962-966` mtime-only-on-success, never merges/deletes content rows); the LLM half has no loop/cost governor. 027: Initiative B is shadow-gate-class ("mutates archival off-turn").
3. **A7-3-content-hash-12-partial-transfer → EXTENDS (L/S) — resolves a reconciliation needs-verify.** `synthesis/06`'s #12 `CG-content-hash-reprocessing-trigger` (flagged "verify vs reindex — may be NO-TRANSFER"): the scan re-derives embeddings/edges on changed content (`force/updated/reinforced` re-embed `memory-index.ts:1039-1059`) but does NOT do the merge/supersede reorg (a separate gated path needing a `pre-reconsolidation` checkpoint the scan never creates, `reconsolidation-bridge.ts:514-527`). Verdict: **#12 is half-covered.**
4. **A7-4-marker-ttl-to-lease-window → NET-NEW (M/S) — the 019 TTL gap.** Replace hardcoded `MAINTENANCE_MARKER_TTL_MS=180_000` (justified only against an observed ~79s phase, `maintenance-marker.ts:23-25`) with `ownerLease.ttlMs × K, K>2`. Three timescales must order: lease TTL=60s, heartbeat=30s, **stale-reclaim=2×TTL=120s** (`launcher.cjs:420,457,526`); marker TTL=180s. Both timers are `.unref()` setInterval and starve under a blocking synchronous phase, so a busy daemon loses lease-heartbeat AND marker-refresh together — the marker is the only thing stopping `shouldAdoptDespiteProbe` from reaping a live mid-scan daemon. The **180s>120s ordering (60s margin) is the real invariant but is implicit**.
5. **A7-5-phase-yield-invariant → EXTENDS 018 (M/S).** Codify: every synchronous phase longer than TTL/2 must call `maintenance.refresh()`. 018's 200-row yield + phase-boundary refresh keep every non-yielding span under the refresh window; a wedged daemon cannot refresh → marker lapses exactly TTL-after-last-refresh → **zombie reap-latency ceiling = one TTL**. Residual risk: a future synchronous phase added without a yield/refresh hook exceeds 180s → false reap.

## Key finding
**Part 1 — reindex is the right host, but only for the cosine half.** Every primitive Initiative B needs is already in 018's scan; the LLM-free `reconsolidate()` + `LT-turn-cadence-trigger` graft as a shadow-gated 4th phase, while content-merge mutation + the LLM-transcript reorg stay shadow-gated / separate packet (doctrine split). A separate background agent would rebuild the loop/cancellation/marker the scan already owns. **Part 2 — the TTL is a relationship:** `2×lease_ttl (120s) < TTL_marker < acceptable-zombie-latency`. Too short → lease heartbeat + marker lapse together during a long phase → premature reap of a live daemon; too long → a daemon wedged *inside* a phase pins the single-writer slot for up to one TTL → zombie pileup. 180s is correct (120s floor + 60s margin) but the principled form is `ownerLease.ttlMs × K` + the no-phase-exceeds-TTL/2 invariant that caps the zombie ceiling.

## Next focus
A7-1 is a correctness/cadence candidate gated by the doctrine split (A7-2); A7-4 + A7-5 close the 019 TTL gap as a stated invariant. Adversarial-verify A7-1's "reconsolidate is LLM-free + grafts cleanly" (does determineAction call an LLM anywhere?) before treating the host claim as load-bearing.
