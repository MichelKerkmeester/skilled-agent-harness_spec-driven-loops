# Iteration 10 (verify-correctness): adversarial verify of the 4 sharpest mechanisms

> Model: **Opus 4.8 via claude2** (acct#2, read-only, refute-by-default). Orchestrator-written. newInfoRatio **0.6** (net-deflationary — corrected 2 of the campaign's sharpest claims). **1 REFUTE, 1 REFINE, 2 CONFIRM.**

## Verdicts
1. **A3-S5-DEMOTION → REFINE.** Mechanism REAL (a row lacking `.similarity` falls to `resolveEffectiveScore`→`rrfScore` ~0.01-0.05, `types.ts:70-77`, sinking below ~0.5 cosine hits in the head) — but **corrected scope: HEAD-ONLY permutation**, `headCount=min(10,len)`, length+membership invariant (`hybrid-search.ts:2433,2444`) → it can **never evict** a hit, only lower its rank within top-10; gated default-on + skipped in evaluationMode; **frequency bounded** by vector-lane coverage (only lexical-exclusive vector-miss docs that still reached the fused head — uncommon rare-token/exact-match hits, not pervasive). The code comment "a strong lexical hit is not unfairly zeroed out" (`:2426`) understates: not zeroed, but pinned ~0.03 ⇒ below all vector hits.
2. **A8-INTERSECTION-BLINDNESS → REFUTE (most-overstated).** The biconditional is false. **TRUE half:** the per-result `deltas[]` + improved/degraded/unchanged counts ARE intersection-only (loop pushes only when `shadowRankMap.get(id)!==undefined`, `rank-metrics.ts:246-247`). **FALSE half (the named one):** `ndcgDelta` is NOT from the intersection — it is `computeNDCG(condenseJudgedRanking(shadowRanked)) − computeNDCG(condenseJudgedRanking(liveRanked))` (`:268-274`); `condenseJudgedRanking` keeps ALL judged items in the full shadow list (`:66-70`) and `computeNDCG` discounts by each item's own rank (`:170`). So a candidate that ADDS a *judged* result the live set lacked **does move** `shadowNdcg`→`ndcgDelta` (and `mrrDelta`). Blind to recall additions only for the `deltas`/counts, NOT for ndcgDelta. The only zero-contribution case is an *unlabeled* added item — a **qrels-coverage** property, not "intersection blindness."
3. **A7-1-RECONSOLIDATE-LLM-FREE → CONFIRM.** `determineAction(similarity)` is pure threshold (`reconsolidation.ts:202`); grep for LLM/anthropic/openai/claude/gpt/fetch/chat/completion = EMPTY; similarity via a `findSimilar` K-NN callback, embedding via an OPTIONAL injected callback — no model called inside the module. **Safe to graft into the reindex scan without injecting an LLM.**
4. **A2-CALIBRATION-PHANTOM → CONFIRM.** (a) `fitCalibration` has ZERO non-test callers; (b) no `ECE`/`Brier`/`reliabilityDiagram`/`calibrationError` symbol anywhere under `lib/` — no calibration-quality metric exists; (c) isotonic apply inert by default (`maybeCalibrate` returns raw unless `isConfidenceCalibrationEnabled()` env-flag AND a model file loads, `confidence-scoring.ts:188-191`). Minor framing fix: it's one env-flag + model-file-presence (not two boolean env flags), but functionally double-gated + inert without a model — "phantom" holds.

## Net effect (corrections that flow into synthesis)
- **A8-3 (new-channel-blindness) must be rescoped:** the existing promotion gate is NOT structurally blind to recall additions — `ndcgDelta` *is* sensitive to judged additions. The real, narrower gaps are (i) the per-result deltas/counts are intersection-only, and (ii) *unlabeled* added items contribute zero (a qrels-coverage problem, addressable by the C9-2 label backfill). The A8 CLASS-C recall-delta panel is still useful (explicit recall@k over the union is cleaner than relying on ndcgDelta's judged-coverage), but its *justification* changes from "the gate is blind" to "the gate's recall sensitivity is implicit + coverage-limited."
- **A3-S5 (demotion) is real but bounded** (head-only, rank-not-eviction, rare) → its A/B is still worth running, but the expected effect size is small; the demotion-count guard (A8 CLASS-R) remains the right instrument.
- A7-1 + A2 confirmed → A7's reindex-graft + A2's ECE-first ordering stand.

## Next focus
Feed these corrections into the GO/no-go synthesis (iter-12): downgrade A8-3's "structural blindness" framing; keep S5's A/B but flag small effect size; A7-1 + A2 confirmed GO. The campaign's net-deflation is healthy (the sharpest claims survived scrutiny only in corrected form).
