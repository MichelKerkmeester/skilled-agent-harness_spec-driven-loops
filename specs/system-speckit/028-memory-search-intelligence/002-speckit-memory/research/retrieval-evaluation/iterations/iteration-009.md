# Iteration 9 (A1-deepen): the three "missing accuracy lanes" are ONE harness extension

> Model: **Opus 4.8 via claude2** (acct#2, read-only, 1200s + read-cap). Orchestrator-written. newInfoRatio **0.6** (consolidation, not new discovery). **The A1/A2/A5 lanes share one root blind-spot and one build spine — they are one extension with a forced linear build order.**

## Candidates
1. **C9-1-shared-single-pass-emit → EXTENDS eval_run_ablation host (H/M).** The root blind-spot: `runAblation`'s per-query baseline loop captures ONLY `EvalResult[]` (`{memoryId,score,rank}`, `ablation-framework.ts:594-606`) — never the verdict/confidence/tier the pipeline already computed. Add a parallel per-query capture (verdict + per-result resolved confidence + each row's `importance_tier`/`created_at`) in the existing baseline pass. Capture mechanism already exists: `captureScoreSnapshot` (`pipeline/types.ts:403`) + `resolveAbsoluteRelevance` (`:89`, the gate's confidence number); `PipelineRow.importance_tier`/`created_at` (`:19,26`).
2. **C9-2-enrichGroundTruth-tagging → EXTENDS (H/S).** One DB-join pass deriving 3 label views from graded relevance(0-3): (a) **citability** per query = `max(relevance)≥2 → expect cite` (with `hard_negative` forcing expect-non-citable); (b) **binary** per (query,result) = `relevance≥threshold` → `CalibrationSample.relevant`; (c) **tier-tag** = JOIN `memory_index` for `importance_tier`+`created_at`. **`GroundTruthEntry` ALREADY has optional `tier`/`createdAt`** (`eval-metrics.ts:42-45` — plumbing half-built); reuses `inspectGroundTruthAlignment`'s `SELECT FROM memory_index` (`ablation-framework.ts:247`). **Closes A5's "no tier-label on golden relevances" gap automatically via DB-join, not manual annotation.**
3. **C9-3-three-corpus-metrics → NET-NEW (gate confusion + ECE/Brier) / EXTENDS (cold) (H/M).** Three metrics in `eval-metrics.ts`, **pooled at the aggregation layer** (a confusion matrix / reliability diagram is corpus-level, so they attach at `buildAggregatedMetrics`, NOT per-query `computeQueryMetrics`): (1) gate-verdict confusion (TP/FP/TN/FN + precision/recall/F1); (2) ECE+Brier+reliability bins over `{rawValue, binary label}`; (3) cold-appearance-rate + cold-precision over tier-tagged relevances. Cold metric EXTENDS `computeColdStartDetectionRate` (`eval-metrics.ts:475`). All read C9-1's snapshot. Zero verdict-accuracy AND zero ECE exist in the 12-metric `computeAllMetrics` (`:586`).
4. **C9-4-assertEmbeddingCoverage → NET-NEW / EXTENDS assertGroundTruthAlignment (H/S).** Extend the alignment guard (today checks parent/chunk/missing only, `ablation-framework.ts:314-352`) with an embedding-coverage check over golden `memoryId`s that THROWS-with-remediation when coverage < threshold (i.e. the deferred reindex hasn't run). Without it: ECE mis-reads (un-embedded rows fall to lexical-fallback confidence), cold-vector surfacing is structurally impossible, gate-accuracy reads the wrong scale. Drop-in at `ablation-framework.ts:580-586`.

## Key finding
**The three "missing accuracy lanes" are ONE harness extension, not three** — same root blind-spot (`runAblation` captures only `EvalResult[]`), same build spine. The build order is forced + linear: **(0) `assertEmbeddingCoverage` precondition** (cheap, reuses the alignment query — gate-zero, every downstream number untrustworthy without it) → **(1) shared single-pass diagnostic emit** (the enabling substrate; reuses `captureScoreSnapshot`+`resolveAbsoluteRelevance`) → **(2) one `enrichGroundTruth` tagging step** (DB-join, all 3 label views at once) → **(3) three corpus-level metrics** at the aggregation layer. The crux payoff: **C9-3's ECE metric is precisely the live-evidence gate that lets isotonic `fitCalibration` graduate from its phantom proxy seed** — and C9-1's emit + C9-2's binary label *is* the labeled-traffic join the calibration module's own header (`confidence-calibration.ts:34-39`) names as the one missing piece. One harness extension simultaneously: (a) measures gate accuracy (A1), (b) produces the calibration evaluator AND the real fitting samples 027 doctrine demands before isotonic defaults-on (A2), (c) verifies cold-tier reachability once reindex clears (A5). **Three lanes, one host, one spine — and A8's promotion gate consumes their metrics.**

## Next focus
This + A8 (iter-8) are the implementation spine for the whole intelligence-class roadmap: C9 builds the metrics, A8 gates on them. Remaining: adversarial-verify the sharpest mechanisms (A3-S5 demotion, A8 intersection-blindness, A7-1 reconsolidate-is-LLM-free) + the GO/no-go synthesis (the doctrine-classed tiering).
