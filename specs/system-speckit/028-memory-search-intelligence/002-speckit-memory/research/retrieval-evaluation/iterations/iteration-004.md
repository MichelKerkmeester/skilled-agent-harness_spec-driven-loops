# Iteration 4 (A2): close 017/S4's flag-off isotonic calibration

> Model: **Opus 4.8 via claude2** (acct#2, read-only). Orchestrator-written. newInfoRatio **0.7**. **The machinery is fully built but uncalled; the "proxy seed" §13 says shipped is a phantom; and 027's promote-on-evidence doctrine is currently *unexecutable* for this flag (no calibration-error metric exists).**

## Infra state (verified)
`confidence-calibration.ts` ships a complete isotonic PAV `fitCalibration` (:145), `applyCalibration` (:196), model loader (:104), labeled-set loader (:73). Apply hook = `maybeCalibrate()` (`confidence-scoring.ts:187-192`), double-gated by `SPECKIT_CONFIDENCE_CALIBRATION` (opt-in, default-OFF) **AND** `SPECKIT_CONFIDENCE_CALIBRATION_MODEL` (model-JSON path). **`fitCalibration` has ZERO callers outside its vitest.** No proxy-seed JSON, no generator, no fit pipeline exist — the "proxy seed" is **conceptual only** (correction to §13).

## Candidates
1. **A2-C1-FIT-INPUT-JOIN → NET-NEW/EXTENDS (H/S).** Harvest the label→`CalibrationSample` pairs — the module's own named blocker. The fit's x-axis (`CalibrationSample.rawValue`) is the `rebalancedValue` at `confidence-scoring.ts:318` fed to `maybeCalibrate` (:323); nothing captures it (`confidence-calibration.ts:36-39` flags this as "the labeled-traffic follow-up"). Host: the wave-1 `eval_run_ablation` already loops the 110-query golden set per-query → instrument `computeResultConfidence` to emit `(query, memoryId, rebalancedValue)` + pair with the label. Math built; only harvest-glue missing.
2. **A2-C2-GRADE-BINARIZE → NET-NEW (M/S).** Every label surface is graded 0-3 (`GroundTruthRelevance`, `LlmJudgeLabel`, `ManualLabel`) but `CalibrationSample.relevant` is binary 0|1 and `loadLabeledSet` *rejects* non-binary (`confidence-calibration.ts:91`). Design: binarize grade≥2→1 (aligns with the judge's overlap≥0.25→grade-2 band). 110 queries × ~5-10 results = 550-1100 pairs — well above the ~50-100 floor.
3. **A2-C3-CAPTURE-RAWVALUE-AT-CITE → EXTENDS recordUserSelection (M/M).** `recordUserSelection` stores `selected_rank` but NOT the per-result confidence → traffic labels can't supply the x-axis without re-running search; and it's wired only at `checkpoints.ts:831`, not the cite path (wave-1 A1 gap). Add `raw_confidence REAL` populated from `confidence.value` at render + wire to the cite path → live `(rawValue, relevant)` with no search replay.
4. **A2-C4-CALIBRATION-ERROR-METRIC → NET-NEW (H/S) — the validation crux.** `eval-metrics.ts` has 12 *ranking* metrics but **ZERO calibration metric** — ECE/Brier/reliability are absent everywhere (grep-clean). Calibration accuracy is orthogonal to ranking, so the fit *cannot be validated* with what exists. Add ECE = Σ|acc(bin)−conf(bin)|·(n_bin/N) on a held-out split + reliability bins. **Sibling of A1's gate-verdict-accuracy lane — both are accuracy lanes the harness lacks.**
5. **A2-C5-SHADOW-3WAY → EXTENDS ablation (M/S).** identity (model OFF) vs proxy-seed fit vs real-traffic fit, scored on held-out ECE; model is path-swappable at runtime (`confidence-scoring.ts:170-179`). Forces *materializing* the phantom proxy-seed as the shadow baseline the traffic-fit must beat.

## Key finding
The label→fit→promote path is blocked by **two missing joins + one missing metric**, all EXTENDS into the 110-query harness. Ordering: ship **ECE metric (C4) + golden-set harvest+binarize (C1+C2) first** → a validatable real-fit from the *existing* labels with **no traffic dependency**; then C3 (cite-path raw-confidence) + C5 (3-way shadow) upgrade to live traffic — the specific bar §13 sets for flipping default-on. The "proxy seed" is a phantom that must itself be built to serve as the shadow baseline.

## Next focus
A2-C4 (ECE) + A1's gate-accuracy lane are the same class — "accuracy lanes the harness lacks." Fold into one A8 measurement-methodology candidate. Verify `fitCalibration`'s zero-callers + the no-ECE grep before treating confirmed.
