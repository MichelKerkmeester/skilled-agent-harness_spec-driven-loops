# Iteration 30 (Round M): Broaden — the two unmined 027 doctrine tracks

## Focus
Do 028's process/deep-loop findings overlap 027's `001-research-and-doctrine` (peck) + `005-verification-and-remediation`? Read-only broaden.

## Findings (newInfoRatio 0.55) — a notable cross-packet discovery
**TRACK-001 (peck verification-discipline): REVERSE-TRANSFER.** 027's peck packet ALREADY encodes verify-first / anti-finding-softening / don't-trust-the-stored-value — the exact doctrine 028's adversarial-verify + finding-is-a-hypothesis rounds rediscovered:
- "an active P0 forces FAIL (VERDICT_LOCK), never CONDITIONAL/partial" + reviewer read-budget (`006-peck-verification-discipline/spec.md:183,109`) = 028's finding-is-a-hypothesis.
- **Structural twin of 028's newInfoRatio gap:** peck noted "`session_dedup.fingerprint` exists but is never recomputed against content … a green checklist `[x]` survives later in-scope edits" (`:75`) = the SAME computed-but-never-fed-back dormant-value bug as 028's newInfoRatio-computed-but-never-ingested (`004-deep-loop/research.md:311`).

**TRACK-005 (verification-and-remediation): reverse-transfer on doctrine, no-transfer on machinery.** `003-deep-research-remediation` runs an explicit Fable verify-first gate (re-verify each finding vs current code, drop refuted, `spec.md:3,55,118`) = 028's adversarial-verify. But the deep-loop convergence/newInfoRatio MACHINERY never touched it — `002-tri-system-deep-research` ran a fixed 50-angle sweep, no convergence gating (`:47,57`). LEVERAGE M.

## Most-likely-wrong
That peck's fingerprint-never-recomputed and 028's newInfoRatio-never-ingested are "the same transferable anti-pattern" vs two coincidental dormant-value bugs — the structural-twin framing could be an interpretive overlay.

## Next Focus
The methodological convergence (027 peck ≈ 028 adversarial-verify) + the shared dormant-value anti-pattern are a distinct ledger section. Round N capstone Q11 + ledger.
