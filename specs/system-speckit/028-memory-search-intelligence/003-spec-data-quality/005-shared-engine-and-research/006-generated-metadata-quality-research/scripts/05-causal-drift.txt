You are a senior research analyst proposing concrete improvements to the spec-kit GENERATED JSON quality. READ-ONLY, do not edit any file. The repo root is the CWD. Read the actual source (find the .ts behind any .d.ts, grep widely). Context: the spec-kit system generates two metadata JSON files per spec folder, description.json and graph-metadata.json. A working session uncovered real data-quality problems with these GENERATED jsons: (1) the generators backfill-graph-metadata.js and generate-description.js IGNORE the folder argument and regenerate the WHOLE specs tree, touching 100+ unrelated files per run; (2) backfill-graph-metadata.js CRASHES when its walk reaches z_future with 'not under a supported specs root', and it even created a stray graph-metadata file in an unrelated project (anobel.com); (3) the generators are non-deterministic, bumping last_save_at on files whose real content did not change; (4) the derived status field carried free-text and em-dashes instead of a closed enum; (5) causal_summary drifts from the doc after the doc is HVR-edited; (6) backfill sometimes sets parent_id to null wrongly; (7) specFolder paths are inconsistently formatted, some with the .opencode/specs/ prefix and some stripped; (8) the global descriptions.json regenerate pulls in other sessions' folders so it cannot be committed scoped. The goal is to improve the QUALITY and SAFETY of these generated jsons and the generators that produce them.

YOUR ANGLE: causal_summary and description drift from the doc.
Start from: .opencode/skills/system-spec-kit/scripts/spec-folder/generate-description.ts; .opencode/skills/system-spec-kit/mcp_server the causal_summary derivation.
INVESTIGATE: The JSON causal_summary and description drift from the doc after the doc is HVR-edited. Propose how to keep them in sync, a drift check or a deterministic re-derive that runs only when the doc changed.

Return 2 to 4 concrete proposals. Each: a title, the problem with file:line evidence you actually read, the proposed change, the expected benefit, an effort estimate (S/M/L), a risk, and a priority (P0/P1/P2). Ground every claim in code you read. If an angle is already solved, say so and lower the priority.