SKILL ROUTING: advisor 0.93 sk-code (review mode, loaded) and 0.93 sk-doc (create-repo-rule, loaded). Read-only turn, so no spec-folder question.

**Nothing is wrong with it today.** The function handles all twelve real descriptions correctly, the parsed values match every router summary, and the checker passes 9 of 9 (exit 0). It has three latent gaps, it fails loud on every one of them, and the single-line convention it assumes is written down in its own module.

**P2, check-repo-rules.cjs:92-94: wrapped descriptions come back truncated.** The regex matches a single line and the function returns at the first hit, so a folded value yields just the indicator character. I probed the real function with a folded description: it returned `">"` where a conforming YAML reader gets the full sentence. A plain value continued onto a second line loses that line by the same mechanism. The damage surfaces later as a check 9 failure, "summary differs from its description" (:385-386), which points at the summary rather than at the parse.

**P2, :92-94: trailing comments survive.** `description: short # note` parses as `short # note` (probe C) while YAML ends the value at the unquoted hash. Same failure path through check 9.

**P2, :94: escapes are kept, not decoded.** The quote strip removes exactly one outer pair. Doubled apostrophes come back as `a ''b'' c` (probe D) and `\"` survives as a literal backslash sequence (probe E). None of this fires on this corpus: the only inner quote in the twelve descriptions is an apostrophe inside double quotes, at delegation-and-orchestration.md:3, which the current strip handles correctly.

Every gap fails loud, never as a silent wrong pass. I ran each probe result through the same comparison check 9 uses (:372, :385-386) and each diverges, and check 2 guarantees every rule has an index row (:240-243), so every description really does get compared. The identical strip idiom sits at :83 for trigger phrases, so any future fix here must land there too or the two parsers drift. My recommendation is to change nothing: nothing fails today, the one-line rule is documented at :366-369, and the cheapest discipline for the next author is the one the corpus already follows, one properly closed, double-quoted line.

What ran: the checker itself (`RESULT: PASSED (9/9 checks)`, exit 0), a frontmatter sweep of all twelve rule files plus an rg pass confirming every description sits on line 3, closed and single-line, five direct probes of the function via its own source loaded into a Function wrapper (no files written, nothing to roll back), and the skill-advisor. The check 9 consequence above is derived from the probes plus the cited compare, nothing in this report is inferred, and nothing here needs your verification. State: read-only, nothing edited, nothing committed, your working tree untouched.

Nothing is blocked on you. If you want the three gaps hardened, say the word and I will route it as a change.

Review status: APPROVED
