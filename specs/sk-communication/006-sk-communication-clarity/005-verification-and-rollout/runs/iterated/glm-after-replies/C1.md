The push-race retry policy lives in `.opencode/bin/git-sync.sh`. The attempt loop starts at line 182, the retry bound and jittered backoff sit at lines 227-229 and 272-274, and the default of 3 attempts comes from `RETRIES="${SPECKIT_SYNC_RETRIES:-3}"` at line 48, documented in `.env.example:420`. Its requirement is REQ-005 in `specs/sk-git/020-live-sync-harden-default-on/spec.md` (retries sleep a short randomized delay instead of looping instantly).

If you meant a different retry, there are three others in the repo: the advisor's retryable exit-75 handling in `.opencode/skills/system-skill-advisor/hooks/lib/skill-advisor-cli-fallback.ts`, the lease-probe retries in `.opencode/bin/lib/launcher-ipc-bridge.cjs`, and the three-attempts debugging bound in `AGENTS.md` §127.

Nothing is blocked on you. If it was a different retry you had in mind, say which and I will point at it.
