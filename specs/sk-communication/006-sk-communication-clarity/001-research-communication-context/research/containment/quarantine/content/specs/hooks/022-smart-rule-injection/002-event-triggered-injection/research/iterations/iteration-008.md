{"timestamp":"2026-09-12T14:46:21.225Z","runtime":"claude","status":"fail_open","freshness":"unavailable","durationMs":137,"cacheHit":false,"errorCode":"NONZERO_EXIT","errorDetails":"CLI_EXIT_64"}
# ITERATION 8 FINDINGS

*This pass read first-hand: `injection-contract.md` (full), all three hook-concern READMEs, the completion sentinel contract, `.claude/settings.json` (full hooks object), `.opencode/scripts/git-hooks/pre-commit` (full), the post-edit router README + Claude adapter source, `git-preflight-advisory.mjs` + `git-rule-checks.mjs` + the sk-git hooks README + sk-git `SKILL.md` frontmatter, `mcp-route-guard.js` + the plugin index, `fable-subagent-guard.mjs`, `opencode.json`, the GIT-042 playbook scenario, and `specs/sk-git/016-git-action-advisory-hook/` (spec + 001 implementation summary + research verdicts). What it adds: **the fleet's missing event-keyed injection family — the sk-git preflight advisory, live on all six runtimes, composing model-visible content at the command event — which no iteration and no line of `injection-contract.md` records**; **the fleet's own measured admission doctrine for event-keyed advisories**, from a ten-iteration sibling research program; the correction of iteration 7's two-prong admission theory to a per-lane theory; four new candidates; and the plain answer on Idea Two's timing.*

---

## 1. EVENT SURFACE

The round's tables are incomplete in one structurally important way: the fleet's *maturest* event-keyed injection family was never in them.

**The miss: the sk-git preflight advisory family.** Event = PreToolUse on `Bash`/`exec`/`Shell` (Claude/Codex/Devin/Cursor direct; OpenCode `tool.execute.before`; Pi `tool_call`→`tool_result`) — **6 of 6 runtimes, model-visible** (`.opencode/skills/sk-git/scripts/hooks/README.md:76-81`; `.opencode/plugins/README.md:35`; `.pi/extensions/README.md:26`). It **composes content at the event**: it parses `hard_rules:` frontmatter out of `sk-git/SKILL.md` (17 rules, `SKILL.md:7-75`) and evaluates each check against live repository state *before the command runs* (`.opencode/skills/sk-git/scripts/hooks/git-preflight-advisory.mjs:112-119`). Its own header states the design rationale in this round's exact vocabulary: *"The git rules were already written down and still did not reach anyone, because they were surfaced by prompt routing while the damage happens at command time. This hook closes that gap"* (`:8-11`). Delivery is `additionalContext` JSON (`:136-138`), not stdout. It is capped at 3 findings (`:45`, `:122-133`), has three suppression tiers (`:64-75`), and never blocks (`:13-15`).

**It is absent from `injection-contract.md`.** A case-sensitive grep for `git-preflight` in the contract returns nothing; only `dispatch-preflight-lint` appears (`injection-contract.md:67`). The contract's claim to "catalog every injection point by content" (`:18`) therefore has a family-sized hole — iteration 7 found the `mkHookDrift` gap; this one is larger. It is also absent from all seven prior iterations of this round (only match for "preflight" is iteration-6's dispatch-preflight citation, `iteration-006.md:70`).

**The admission doctrine ships in code, and it is measured.** `git-rule-checks.mjs:11-19`: *"gate on state, never on the verb. Roughly one in seven operations in this repository is a `reset`, but the overwhelming majority merely unstage … the same rule keyed to the commit actually moving fires about a hundredth as often and stays worth reading … a check that cannot find a discriminator does not belong here. All checks fail open. Uncertainty means silence, because a false positive spends the one thing this system cannot rebuild once lost, which is the reader's attention."* The originating research program measured the budget: **~1 advisory per 100 git mutations, per rule** (`specs/sk-git/016-git-action-advisory-hook/001-advisory-research/implementation-summary.md:28`); 93% of resets unstage-only (`:57-60`); **three of five memory-proposed rules did not survive measurement** (`:96`). The packet's phase rules: research before encoding, advise never block, **no invented rules** (`spec.md:129-131`; blocking explicitly out of scope, `:97`).

**Other first-hand deltas.**
- **A third backward read at a tool event**: `fable-subagent-guard.mjs` (Claude only, PreToolUse `Task|Agent`) reads the session transcript's last *main-loop* model line, sidechain-excluded (`:39-64`), and denies Fable-inheriting dispatches (`:94-114`). Backward reads are production in three places now — goal verify, sentinel, this guard.
- **Claude's registered event set, first-hand** (`.claude/settings.json:16-203`): `PreToolUse` (Bash / Task / Task|Agent / `mcp__claude_ai_.*` / Write|Edit), `UserPromptSubmit`, `SessionStart`, `Stop` (async×2, `:146`, `:152`), `SessionEnd` (`:157-167`), `PostToolUse` (Write|Edit / Bash), `PreCompact`. No `Notification` registration here. `SYSTEM_SPEC_GATE_ENFORCE: "0"` is set in-repo (`:7`) — the write-gate deny path is off on this runtime today.
- **Read-only sessions**: unchanged from iteration 6 — session start, prompt submit, turn-end family, compaction fire; session end is `[NONE]` (`injection-contract.md:220-224`); tool events are shape-keyed. The git-preflight is **read-only-quiet by state**: `git status`/`log`/`diff` evaluate to no violation and produce nothing; it speaks only when a git-shaped command would quietly do something other than it appears. **Unused sets**: unchanged since iteration 5 (Cursor's unwired list, Claude `PermissionRequest`, Pi's ~25/33) — inherited, not re-read.

---

## 2. IDEA ONE — completion presentation

**REFUSE — all three tests fail, and the new material gives the sharpest one yet: the admitted event-injection family fires *before* the damaging action; a completion-presentation injection cannot, by construction.**

- **Does the completion event reach the rules? No.** The presentation carriers are the resident digests with load commands (`AGENTS.md:405`, `:407`, `:409`; close-out verbatim `:501`) plus each run's own completion contract (`presenting-decisions.md:129-132`). No injection anywhere in the fleet carries presentation content (iteration 6's finding, `prior-findings.md:111`), and the completion event's only model-visible output remains Pi's evidence advisory (`completion/README.md:68`). *(The operator's premise "rules that bind only on write turns" is already corrected: `AGENTS.md:405` binds on "every substantive reply"; `handoff-and-questions.md:35` on "end a turn, of any kind.")*
- **Is the guidance worth the channel? No — under the fleet's own doctrine.** The shipped family's first criterion is *the event precedes the damage while the action window is open* (`git-preflight-advisory.mjs:5-9`; `016/spec.md:54-56`). At completion time the reply exists and the human has read it — the family's total silence in that position is the model. Its payload discipline is a state discriminator against live repo state (`git-rule-checks.mjs:11-16`); "the tail of the message looks claim-ish" (`completion/README.md:31`) has no state to discriminate, fires 477 times / 63 days with >95% one low-specificity class (iteration 6's measurement, `iteration-006.md:19`), and the content would be disposition-shaped — the exact class the prompt lane retired (`injection-contract.md:54`).
- Standing legs, one line each: measured frequency; action window (iteration 5); restatement; channel upgrade is the twice-refused consent class. Nothing here reopens.

---

## 3. IDEA TWO — structured question triggering

**FATAL — plainly. Detection is solvable; the remedy is not "hard", it is self-contradictory: the predicate and the remedy sit on opposite sides of the turn boundary on every runtime.**

- Before the reply exists (prompt time, tool time), "should have asked" is not evaluable — the sentinel's own trigger is a property of finished text (`completion/README.md:31`). After it exists (turn end), every surface that *has* the reply can only rewrite or append — authorship violations already refused on two runtimes (NC-C; `iteration-004.md` §3), and the operator has already answered the turn. The one family of surfaces that could make the model ask again — turn-reopening continuations — is Claude's (doubly off: advisory-only by design, `completion/README.md:64`; `async: true` here `.claude/settings.json:146`, `:152`) plus OpenCode's goal continuation (requires an active un-met goal and its prompt tells the model *not* to ask; `opencode-goal.js:2473` per iteration 3). No runtime crosses the boundary.
- Cross-check against the newly found family: the admitted composed content **judges the pending command, not a completed reply**, and never speaks in the model's voice (`git-preflight-advisory.mjs:128-138`). A missed ask has no author outside the model and no state discriminator (§ the doctrine's "a check that cannot find a discriminator does not belong", `git-rule-checks.mjs:13-14`). The three-condition bar (`handoff-and-questions.md:101-110`) is prose-default on the compliant side, so any form detector fires on compliant turns.
- **Answer: fatal — on authorship and disjointness, not on detection difficulty.** The only surviving form of this class is the one already shipped (fixed-enum, pre-composition, Gate 3 — `injection-contract.md:72-84`), and it is a different mechanism with a different admission.

---

## 4. NEW CANDIDATES

The corrected bar first, because iteration 7's version was wrong. **Admission is per-lane, not one two-prong test** (`iteration-007.md:90` generalized the prompt lane's rule). The **prompt lane** demands enforcement (014 `spec:77`; `injection-contract.md:54`). The **event lane** — the shipped family's own doctrine — demands: **E1** the event precedes the damage with the window open; **E2** a machine-derived payload from state the event makes readable (not a restatement); **E3** a state discriminator under a measured volume budget (~1/100 per rule); **E4** advisory-only, fail-open, capped, suppressible. The family is openly *not* gate-backed per check — *"the existing pre-commit, commit-msg and pre-push hooks own enforcement; this packet advises"* (`016/spec.md:97`) — which is why iteration 7's "gate-backed content" prong was a prompt-lane rule miscast as universal.

**H-1. Encode the "no direct branch creation" prohibition into the shipped git-preflight family.** *(Never proposed.)*
- *Event/runtimes:* the family above, 6/6.
- *Content/trace:* the prohibition is existing sk-git prose (`SKILL.md:507`, "never `git branch`, `git checkout` plus `-b`, or `git switch` plus `-c`"; `AGENTS.md:331`) but is **not one of the 17 encoded rules** (`SKILL.md:7-75`; `git-rule-checks.mjs:153-429`) — so today it rides prompt routing only, which is exactly the gap `016/spec.md:54-56` exists to close.
- *Silence:* matches only the three banned shapes; the sanctioned form (`git worktree add -b`) stays silent. *Wrong-fire:* one capped, suppressible advisory line.
- *Deciding test — the doctrine's own discriminator rule:* the broad form is verb-keyed and "does not belong here" (`git-rule-checks.mjs:13-14`); the narrow form (a hand-picked name inside the allocator's numbered namespace, colliding silently with future allocations) has a state read and the damage is real and silent. But frequency is **unmeasured**, and 016's own limitation says it cannot be derived from reflogs (`001/implementation-summary.md:123-124`). The program's step order bars encoding first (`spec.md:129-131`).
- *Verdict:* **SURVIVOR SIDE IN PRINCIPLE, DEFER — measurement before encoding**, per the fleet's own 3-of-5 refuted lesson (`001/implementation-summary.md:96`).

**H-2. Model-visibility parity for `mcp-route-guard` on OpenCode.** *(Never proposed.)*
- *Event:* `tool.execute.before` before a native external MCP call (`.opencode/plugins/mcp-route-guard.js:71-83`). The same finding is model-visible on the other five runtimes (`injection-contract.md:169-175`); OpenCode is log-only (`mcp-route-guard.js:4-10`).
- *The only stated reason is console corruption* (`:40-43`), which the transform drain does not commit — two sibling plugins drain findings into model context exactly this way (`.opencode/plugins/README.md:33`, `:35`). But "log-only" is written as the adapter's purpose (`:8`), so this is a **consent question, not a pure transport gap** (unlike NC-A, whose registration already carries `additionalContext` on its drift path, `.claude/settings.json:175`).
- *Domain check:* the plugin is dormant until a native external server is registered; `opencode.json:10-20` registers only the code-mode launcher — **zero live fires today**.
- *Verdict:* **DEFER — empty domain, then a parity decision owned by the mcp-route-guard concern.** Deciding test: dormant-domain + consent.

**H-3. Destructive-tier check for `git worktree remove --force` on a dirty worktree.** *(Never proposed; recorded as the obvious idea the fleet's own rules bar.)*
- *Shape:* mirrors the retained destructive tier — rare operation plus explicit destructive token plus positive state (dirty target), exactly the `reset-hard-discards-changes` form (`git-rule-checks.mjs:336-342`); the 17 checks contain nothing for it.
- *Deciding test — the program's own phase rule:* "Every encoded rule must trace to existing sk-git prose or an observed incident. No invented rules" (`016/spec.md:131`). Prose covers reap *order* and the reaper's proof requirements (`SKILL.md:372`; `feature-catalog/session-lifecycle/worktree-reaper.md:81`), never forced removal of a dirty tree; no recorded incident exists. Encoding it would be rule invention.
- *Verdict:* **REFUSE — trace rule.** (If the operator wants it, the route is prose → incident → rule, in that order.)

**H-4. Recorded, bad by construction: inject "say where you are going" at `afterAgentThought` (Cursor, unwired) or Pi `agent_start`.** *(Never proposed; the obvious-bad finding.)*
- Fires per thought / per agent start — the retired every-turn shape in an event costume; fails E3 by volume and E2 by residency (`AGENTS.md:407`; `presenting-decisions.md:100-107`); E1 has no damage it precedes. **REFUSE — frequency + restatement.**

**Prior candidates under the corrected bar** (one line each, testing earlier ones): C2 read-ledger — E1 passes, E3 fails (unsound discriminator: unobservable `cat`/offset reads, `iteration-006.md:63`); G-1 generated anchors — E3 fails (the finding state *is* the compliant intermediate, `iteration-007.md:48-55`); G-3 stuck-loop — E2 fails (payload resident, `AGENTS.md:192`) and the repetition count grades a symptom; C4/G-2/G-4/NC-B/NC-D unchanged. Every refusal remains consistent; none is reopened.

---

## 5. VERDICT

1. **Idea One — REFUSE.** Deciding test: **event position under the fleet's own first criterion** (the admitted family fires before the damage; completion content cannot), plus measured frequency and restatement. New this pass: a live control showing what an admissible event payload looks like — and it is not a disposition.
2. **Idea Two — REFUSE, fatal.** Deciding test: **predicate–remedy disjointness across the turn boundary + authorship**; no shipped event-family member judges a completed reply or composes model-voice text.
3. **The round's structural correction (not a candidate):** admission is per-lane. Iteration 7's gate-backed-content prong is the prompt lane's rule; the event lane admits by E1–E4, as the shipped six-runtime family demonstrates (`016/spec.md:97`; `git-rule-checks.mjs:11-19`). **NC-A remains the round's one admitted object** and is now doubly supported (prompt-lane gate: `pre-commit:40-84`; event-lane doctrine: findings are final-state artifact properties — `post-edit-quality/README.md:44-53`), with the live delivery probe still the standing adoption gate.
4. **H-1 branch-creation rule — DEFER.** Deciding test: **measure-before-encode**, the program's own 3-of-5-refuted step order.
5. **H-2 mcp-route-guard parity — DEFER.** Deciding test: **dormant domain + consent** (log-only is written intent; the stated reason covers console only).
6. **H-3 worktree remove --force — REFUSE.** Deciding test: **encode only from prose or incident** (`016/spec.md:131`).
7. **H-4 agent-start intended-path — REFUSE.** Deciding test: **E3 volume + E2 residency**.
8. **Fleet notes to reconcile (not candidates):** (a) `injection-contract.md` omits the git-preflight family and its §18 completeness claim needs amending — this round's §1 tables inherited the gap; (b) the completion sentinel's dominant-class misfires (iteration 6, log `:468`, `:476`) are, under the newly found doctrine, an E3 violation — fix the discriminator before any payload ever rides that channel; (c) the sk-git hooks README's Claude row says "no matcher" (`.opencode/skills/sk-git/scripts/hooks/README.md:76`) while the registration carries `"matcher": "Bash"` (`.claude/settings.json:19`) — verify and reconcile.
