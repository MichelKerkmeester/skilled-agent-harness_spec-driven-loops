{"timestamp":"2026-09-12T14:40:07.557Z","runtime":"claude","status":"fail_open","freshness":"unavailable","durationMs":69,"cacheHit":false,"errorCode":"NONZERO_EXIT","errorDetails":"CLI_EXIT_64"}
# Iteration 7

## 1. DIAGNOSIS

**The reframe holds for four incidents, and needs a correction for #3.** Incidents #1, #2, #4, #5 each satisfied every existing standard as written: a command ran, its output and exit status were read (`AGENTS.md:244`), a receipt was cited per row 1 (`AGENTS.md:243`), and §2's command-evidence clause (`evidence-and-proof.md:62-63`) was met. The one-sentence summary: the corpus checks that evidence was *had* — read, run, cited — and never that it was *able to disagree*. Incident #3 is not "shallow": every line range was verified at full depth. It is level-mismatched — a reading existed at parts-level, none at the claim's level. "Read too shallowly" is the wrong axis for it; "read at the wrong level" is the right one.

**Per incident, the nearest text and why it could not fire:**
- **#1 / #4** — the one clause about a fruitless search (`evidence-and-proof.md:81-84`) asks whether the file was read; its remedy is `-a`, byte-encoding. The failures were query-form against target-form (prefixed vs bare key; line-oriented vs wrapped text). The resident mirror is positive-only: `AGENTS.md:345`.
- **#2** — a prefix read satisfies `AGENTS.md:244`, and the "affirmative marker" clause (`evidence-and-proof.md:69-70`) was satisfied *by the misleading first line itself*.
- **#3** — `evidence-and-proof.md:52` owns DERIVED and `:122-123` is scoped to "computed answer"; an inherited total presents as a known fact with a prior analysis as receipt, which row 1 accepts.
- **#5** — the contradiction machinery (`uncertainty-and-honesty.md:91-99`; `AGENTS.md:97`) invites the report and its format accepts "Fact A, with its source"; `:35` fires on unverified numbers and the digits were verified — their role was not.

**The gap, stated as one predicate — new this iteration.** The corpus already carries the *concept* of confirmation, addressed to a second party: row 3, "A sub-agent's 'COMPLETE' … until **something you ran confirms them**" (`AGENTS.md:245`). What no text defines is what makes a run *confirming*. Row 3's "confirms" is undefined; row 4's "ground it" is undefined; §2's "read" is satisfied by a prefix. Every incident is a **first-party** finding, where even row 1's receipt bar — "carries its evidence" — has no discriminating-power criterion behind it. The standards fired *as addressed*; the missing predicate is what makes a reading support its claim.

**Why #3 differs, and whether one rule covers both.** #1/#2/#4/#5 are instrument defects: a reading was taken and could not have produced the falsifying result (wrong key form, first line, non-distinguishing digit match). #3 is a composition defect: the readings were sound and complete at their own level; no reading existed at the claim's level. One *rule* can cover both only as a **two-arm criterion** — a check demonstrated able to disagree, *or* a derivation at the claim's own level — because the repairs differ (re-run/read the target vs re-derive the total) while the classification question is the same ("is this reading able to support this claim?"). One *mechanism* cannot: a single check-demonstration clause has no valid instantiation for arithmetic, so it handles #3 only by labeling it inferred and never names the positive repair. That defect is real in I6's text and is repaired below.

## 2. WHERE IT BELONGS

- **New rule file: refused.** Test 1 (`decision-tests.md:38-41`): this binds on read-only turns; Gate 5 fires on the first write (`AGENTS.md:122`); the designed outcome is "Nothing fires → `AGENTS.md` alone governs" (`REPO RULES.md:18`). Stronger, from the corpus's own prior record: injection and "Gate-5-to-read-only, universal loading" were both tested and refused, and the recorded live remedy for an uncovered clause of this class is a text promotion into the resident layer — "REFUSE the injection. LEAVE AT GATE 5 … CHANGE THE RULE INSTEAD" (`specs/hooks/022-smart-rule-injection/001-deep-research/research/iterations/iteration-010.md:55`), with its own candidate routed "beside 'Finding = hypothesis'" (`:59`).
- **New AGENTS.md row: refused.** I5's restraint ground (`decision-tests.md:121`) and I6's ownership ground stand. New supporting test: row 3 (`AGENTS.md:245`) already establishes the register, so the amendment adds a *criterion*, not a doctrine; a sixth row would split "confirmed" across two criteria and leave the first-party/second-party asymmetry standing.
- **Halt Conditions list (`AGENTS.md:52-55`): tested and refused.** All four members are execution-state triggers with a stop-and-report remedy (missing file, string not found, merge conflict, unclear boundary). Claim-time classification's remedy is a label or one more observation, not a halt.
- **Admitted: row 1 amendment (`AGENTS.md:243`) + the `evidence-and-proof.md:51` mirror** — same footprint as I6, with three repairs below. The mirror is coherence: `evidence-and-proof.md:30` subordinates the file and §1 (`:49-53`) owns the tier definitions.
- **Coordination flag:** round 1's live recommendation promotes the delegation self-lens (`repo-rules/delegation-and-orchestration.md:45-47`, "no single model's verdict, the delegate's or your own, closes a question") into this same table (`iteration-010.md:59`). Two promotions into one table should land deliberately; this one is the narrower and incident-tested one.

## 3. THE PROPOSAL

**Edit 1 — `AGENTS.md:243` (full replacement row):**
> | **Confirmed vs inferred** | A load-bearing claim carries its evidence (file:line, command, artifact). It is confirmed only by a check that has shown it can disagree — it has produced, on a case of the same fragile shape (the wrapped text for a wrapped one, the key form for a key) whose answer is already in front of you, read or run here rather than remembered, the result that would have contradicted the claim — or by a derivation at the claim's own level, steps shown. A one-way reading (a pattern that never hit, a probe's first line, a total nobody re-derived, a match that never distinguished roles) is inferred: say so, and what would confirm it. |

**Edit 2 — `evidence-and-proof.md:51` (OBSERVED row):**
> | **OBSERVED** | You ran it and read the output and exit status, and the check has shown it can disagree — produced the contradicting result on a case of the same fragile shape whose answer was already in front of you | Cite the receipt: the command, the `file:line`, the artifact path |

**The three deltas over I6 (`iteration-006.md:28-32`), each tested this iteration:**
1. **Control provenance.** I6's "a case whose answer was already known" lets the control's premise be remembered. A remembered control inherits the same wrong model that produced the failure (§11: reason from data, not memory, `evidence-and-proof.md:179-193`); #5's "known weight/label pair" was exactly a recalled role. Fix: the answer must be *in front of you — read or run here*.
2. **Control relevance.** I6's "the same check" can be read at tool level: a successful, unrelated grep earlier in the session satisfies it while the instrument stays blind (#1). Fix: the control must exercise the claim's fragile shape.
3. **The aggregate arm.** I6's single check-clause cannot instantiate for a total; strictly read, even a freshly re-derived total has no "check demonstrated on a known case" and would stay textually unconfirmable. Fix: name the derivation path as the second confirmation arm.

**Applied to the record:** #1 — control on a mode key whose registration is visible in the registry just read; the prefixed grep fails the control too, exposing the form — caught and repairable. #2 — "a probe's first line" is named one-way; confirmation is a demonstration on a known-broken case or reading the span — caught. #3 — derivation arm; caught with the positive repair, not only a label. #4 — the fragment pattern must be demonstrated on text of the same fragile shape (a known wrapped instance, answer read here); where none exists, inferred — caught. #5 — "a match that never distinguished roles" is named; confirmation is role comparison on numbers read in their own context — caught, and the weakest instantiation of the five; said so.

## 4. WHAT IT COSTS

- **Wrong fires:** direct positives collapse in one beat ("file exists" against a listing; "tests passed" against a runner with counts) — same class as I6, no extra commands. Real friction is absence, aggregate, and classification claims — one control (or label) each.
- **Delta friction of the three repairs:** (i) the provenance clause occasionally forces finding a visible control where a remembered one would have passed wrongly; when none exists the output is INFERRED plus a confirm-step, which is the honest answer. (ii) The fragility clause occasionally demands a second, better control. (iii) The derivation arm *removes* an I6 false-negative on repaired totals rather than adding cost.
- **Frequency:** per instrument per turn, plus per one-way claim; base rate is five false confident claims in one session.
- **Edit mechanics:** row change escalates to the operator (`agents-md-integration.md:122-127`); the count sentence stays "five" (`AGENTS.md:239`); CI's path filter covers the mirror file but not AGENTS.md row semantics (`repo-rules-corpus.yml:5-8`).
- **Propagation — re-verified first-hand this iteration:** `:243`/`:245` are line-identical in all three files (`Public/AGENTS.md:243-245`, `/Users/michelkerkmeester/MEGA/Development/Mobile CLI/AGENTS.md:243-245`, `…/Obsidian Plugin/AGENTS.md:243-245`, same content digests). The mechanism is contested by earlier reads (a symlink claim vs I5's UNKNOWN, `prior-findings.md:114`, overturned by `iteration-006.md:50`), so the landing step edits-or-confirms all three places rather than trusting either.
- **Gaming:** a rigged control is now bounded by the receipt clause — the control's answer must be visible in the transcript, inspectable; blanket INFERRED stays priced by "say what would confirm it" (`evidence-and-proof.md:53`).
- **Ceiling:** text only. Enforcement routes are closed by record (`iteration-010.md:55-57`); this buys honest classification and, on #1/#4, one recovering command.

## 5. VERDICT

**Adopt I6's direction — row 1 amendment plus `evidence-and-proof.md:51` mirror — with the three repairs above.** I6's criterion survives its tests; its text did not, in two places (blind-control provenance/relevance; the aggregate arm absent), and both defects are construction-level and fixable inside the same two-line footprint. The new grounding is that the corpus already names the confirmation act for second-party findings (`AGENTS.md:245`) and leaves "confirms" undefined; this amendment supplies the criterion and lets first-party claims use it — no new doctrine, no new row, no new file. Refuse the rule file, the sixth row, the Halt Conditions entry, and the communication/injection surfaces, each tested and cited above. Open question left for later iterations: whether the repaired text still fails some incident the record has not yet shown — this iteration found none in the five.
