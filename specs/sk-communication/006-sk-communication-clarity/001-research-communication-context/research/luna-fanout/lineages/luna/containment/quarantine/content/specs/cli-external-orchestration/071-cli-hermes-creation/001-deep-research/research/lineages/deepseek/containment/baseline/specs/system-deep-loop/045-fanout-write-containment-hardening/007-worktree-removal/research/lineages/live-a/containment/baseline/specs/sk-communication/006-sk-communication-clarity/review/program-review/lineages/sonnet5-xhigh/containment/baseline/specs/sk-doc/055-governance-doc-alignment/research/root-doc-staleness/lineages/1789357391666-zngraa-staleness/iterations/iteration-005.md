# Iteration 5: The five named candidates, second half, the broadened class-one residue, and the section 6 adjudication

focusTrack: redundant-detail

## Focus

The advisor metadata placement paragraph (AGENTS.md:110-112) and the MCP routing section (351-359) under the three-way test; the broadened class-one residue (the retrieval's unsupported-features claims at :315, the goal-hook binary, the committed trigger index at :81); then the question the first four iterations built toward: does the section 6 question-to-answer routing shape govern the rewrite of every confirmed case-one finding, and where does it not apply? (Strategy questions Q4's second half and Q5.)

## Actions Taken

1. Read the skill-root metadata contract's overview, its class table, its required/forbidden placement table, its second-source rationale, and its not-the-spec-folder-schema guard.
2. Read the tool-naming convention's overview and discovery clause, and the mcp-code-mode skill's allowed-tools and discovery step.
3. Ran the class-one residue probes: the retrieval conventions' unsupported-features row, the goal hook's binary, and the trigger-index artifacts plus their committed status.
4. Adjudicated Q5 across all five iterations' confirmed findings, from the section 6 and section 8 texts (in this session's context since the initial read) plus the accumulated delegate quotes.

## Findings

- **F-005-1 (OBSERVED — the advisor metadata placement paragraph, AGENTS.md:110-112: case one, with one root-only clause).** The delegate carries the paragraph's content in its own words. The cross-guard: "Not the spec-folder schema. `description.json` and `graph-metadata.json` also exist under `.opencode/specs/` under a completely separate continuity schema. The two are never the same file and never interchangeable. This contract governs `.opencode/skills/<root>/` only, and the gate never scans the spec tree" [SOURCE: .opencode/skills/sk-doc/sk-create-skill/references/shared/skill-root-metadata-contract.md:32]. The classes: "| **H** — packet hub | ... | Declares **both** `mode-registry.json` and `hub-router.json` |" and "| **S** — standalone routed-resource skill | ... | Declares **neither** |" [SOURCE: :40-41]. The placement table: "`description.json` | required | **forbidden** | authored", "`mode-registry.json` | required | **forbidden**", "`hub-router.json` | required | **forbidden**", "`leaf-manifest.config.json` | **forbidden** | required" [SOURCE: :65-68], with the rationale: "Forbidden is not stylistic. Each forbidden file would create a second source of truth for something the class already declares elsewhere: a hub carrying a standalone manifest config would hand the generator two competing inputs, and a standalone carrying registry, router, or description would claim a hub identity its packet layout cannot back" [SOURCE: :73], reinforced at :77 ("Adding one to a standalone root would produce prose nothing consults, so the contract forbids it rather than leaving it as a harmless option"). The fleet audit the root's:110 promises is the contract's own: its description line, "the two-class contract, the discriminator that decides a root's class, and the fleet gate that enforces it" [SOURCE: :5]. The second paragraph (the:112, the hub's two-stage routing and the never-report-a-mode-as-ruted clause) is the skill-hub-routing rule's content plus its pointer: the rule's own index line, "registered is not routed, and a gate run without its hub argument checks something else" [SOURCE: repo-rules/REPO RULES.md:68, the row routing to repo-rules/skill-hub-routing.md], and the class-table pointer, ".opencode/skills/sk-doc/sk-create-skill/references/parent-skill/parent-skills-nested-packets.md" (AGENTS.md:112). What the root uniquely contributes: the cross-reference INTO the root's own §6 ("These filenames also name spec-folder continuity metadata (§6), under a separate schema") — a delegate cannot reference the root's sections, so that link, and only that link, stays. Case one; the reduction keeps the §6-cross-link plus two pointers.
- **F-005-2 (OBSERVED — the MCP routing section, AGENTS.md:351-359: mixed; one clause reducible, the exemplar clause and the honesty clause stay).** The naming convention owns the prefixing: its subtitle, "Master the critical naming pattern to avoid the #1 most common Code Mode error" [SOURCE: .opencode/skills/mcp-code-mode/references/naming-convention.md:1-6], and its doubt-clause, "When in doubt: Use `search_tools()` or `list_tools()` to discover the correct tool name" [SOURCE: :578], which is also the skill's discovery step ("1. Discover tools with `search_tools()` or `list_tools()`" [SOURCE: .opencode/skills/mcp-code-mode/SKILL.md:258] with the allowed-tools list at :4). Against that: the:357 ("**Enumerate at runtime, never from a written list.** `search_tools()`, `list_tools()` and `tool_info()` are the discovery surface. Naming is transport-dependent — `mcp-code-mode/references/naming-convention.md` owns the prefixing rules and the `cli`-manual exception") is already the target shape, a trigger, a delegation, and a why, and is the in-document exemplar. The:353's calling mechanics duplicate the skill's own contract: "Native MCP servers are registered per runtime (...) and called directly. Code Mode manuals are registered in `.utcp_config.json` and called through `call_tool_chain()`" — the manuals' call path is the skill's allowed-tools line, `mcp__code_mode__call_tool_chain` [SOURCE: SKILL.md:4], and the roster-discipline ("Read the config for the current roster; a list written here goes stale between commits") is the:353's own why. The:355 (the Skill Advisor outside both systems) names the shim's independence this lineage itself relies on (iteration 2's:28-30,62-69); the:359 ("**Registration is not availability.** ... Never promise that a manual named in the config is live") is reporting-time honesty with no delegate found (thegreps over the naming convention and the skill returned no registration-availability clause; INFERRED, what would confirm: reading the skill's remaining references). Verdict: the:353's second sentence reduces to the skill pointer; the:357 exemplar and the:359 honesty clause stay; the:355 proviso stays pending its delegate check.
- **F-005-3 (OBSERVED — the broadened class-one residue: all three probes cleared, one verbatim).** The retrieval's unsupported-features clause, AGENTS.md:315 ("Semantic paraphrase, vector and BM25 fusion, decay, access tracking and session dedup are unsupported, and a miss is a clean no-hit"), is the retrieval conventions' OWN table row: "| Semantic paraphrase, vector and BM25 fusion, decay, access tracking and session dedup | Deliberate lexical-only loss with no owner. Decay, access tracking and dedup were properties of a stateful store that observed its own reads, and every lane here is stateless; callers must behave explicitly on a no-hit rather than degrading to a guess" [SOURCE: .opencode/skills/system-spec-kit/references/retrieval/retrieval-conventions.md:64], with the no-hit discipline at :40 ("a phrase that no document spells literally is a clean no-hit, never a nearest guess") and the exit contract at :167-173 (a no-hit is exit 0/1's own class, "Never report it as no-hit" for 2+). Case one: the feature list, the no-hit meaning, and the caller discipline are the reference's; the root's:315 row keeps its purpose (what the tool row is for: "Research, context recovery, saves") and its:316-recipes pointer. The goal hook: `.opencode/hooks/goal/bin/goal.cjs` EXISTS [SOURCE: test -e, this session], so the GOAL POSTURE RULE's:294 pointer ("Mechanics (binding, the strip, the budget, the log) are `system-spec-kit`'s and the goal hook's") holds, and the rule's:290-293 binding clauses are the case-two/prompt-time keeps. The trigger index: `runtime/data/trigger-index.json` EXISTS alongside the two committed retrieval scripts [SOURCE: find, this session; the:81 lookup and the:466 generator both precede], and the:466 contract ("commit the regenerated index and manifest together") states the committed-ness the:81's "reads the committed index" relies on; whether THIS working tree's data file is itself tracked was not finally verified (the:git ls-files glob returned the scripts, not the data file) — INFERRED, what would confirm: `git ls-files .opencode/skills/system-spec-kit/runtime/data/trigger-index.json`. Cleared with that caveat.
- **F-005-4 (DERIVED from F-001..F-005-3 — the section 6 adjudication, question Q5).** The:363-377 target shape is: a question, the answer's pointer, one local rule, the why. Applied to every confirmed finding:
  - *The completion rule (class one, the:263-270 group and:303,463).* The rewrite the shape produces: step 2 becomes "Load the verification checklist inside `tasks.md`; closure is decided by `acceptance-criteria.md`" (the delegates' own lines: template-mapping.md:159 "the verification checklist lives in tasks.md" and the:161 AC_CLOSURE; the:055 packet's spec.md:124-125); the:266 evidence-row list drops `checklist.md` and gains `acceptance-criteria.md`; the:270 skip and the:269 freshness clause stay (level-determinism and the freshness mechanism are the rule's own); the:303 Self-Check item and the:463 row re-point to tasks.md's checklist. The one local rule that survives: the four-step ORDER (validate → checklist → reconcile → freshness), whose why is the:262's own: a warning is advice, the gate decides. Five places, one cause, one rewrite.
  - *The advisor invocation (class one, behavior).* The:99 sentence stays true only of a built workspace; the shape's fix is one clause: the documented invocation requires the compiled advisor CLI, and the shim exits 69 (75 with --warm-only) naming the build when it is stale or missing [SOURCE: .opencode/bin/skill-advisor.cjs:28-30,62-69].
  - *The case-one class-two findings, each collapses to trigger → pointer → one clause → why.* The memory save rule: keep:282-283 (session state: which folder, whether Gate 3 was answered), keep:284's "Compose the session JSON yourself — you have strictly better information about your own session", point:284's second half and:285-286 at save-workflow.md (its:24,151-152,211-244,553-578) and the:285 shortcut clause stays as the one undelgated mechanic. The mandates: keep:478 (the delegate's:48-49 back-pointer), reduce:477+479 to the:474 pointer. The git table: keep the eight trigger-rows as pointers, keep the:324-ask clause (case three) and the:331 "not just conventions" warning (the why), reduce the seven rows' mechanic detail to the skill (whose:79,301,307 and conventional-commit-workflows.md:32,34 carry them). The validate.sh: keep the:262 step-1 requirement (run it, strictly) and:272-278's "Require an explicit RESULT: PASSED" clause, reduce the:262 exit-semantics parenthetical and the:274-276 trap prose to validation-rules.md:35,757-775 (the reference's:758-760 says the copy goes stale — the root's copy is the demonstration). The advisor-metadata paragraph: keep the §6-cross-link, point:110-112 at the contract:32,40-41,65-73 and the nested-packets reference. The MCP section: keep:357 (the exemplar), reduce:353's second sentence to the skill:4 pointer, keep:355 pending its delegate check, keep:359.
  - *The keeps, argued not asserted.* A clause stays when no delegate reaches the moment it binds: the:282-283 (only the session knows), the:478 (only the root owns the single scale, by the delegate's own:48-49), the:324-ask (conversational, priced-exception per blast-radius.md:72), the:67-72 and:76-77 (question-time vocabulary; the classifier's:107-119,878-882 own the fire, not the question), the:359 (promise-time), the:64-fire-sentence (the:66's declared human-readable twin for non-hook runtimes), and section 8's:401-405 one-line summaries (they bind on the read-only turn where no repo-rule loads, the:407's stated design: "Delivery never softens rigor" and "voice is not a performance"). That is the:363-377 shape applied consistently: pointer by default, one clause where nothing else reaches, the why always.
  - *Judgment disclosure:* this adjudication is one model's reading, grounded in the quoted delegate lines; what would change it: the sibling lineages' verdicts on the same clauses, which the packet's merge step will compare.

## Questions Answered

- Q4 completed: the advisor metadata placement = case one (the:110-112 reduction keeps the §6-cross-link); the MCP routing = mixed (the:353's second sentence reducible; the:357 exemplar and the:359 honesty clause stay; the:355 proviso pending its delegate check).
- Q5 answered: the section 6 shape governs every confirmed case-one rewrite; the keep-cases are exactly the clauses no delegate reaches, enumerated above; section 8's summaries are the design's deliberate degradation path, not duplication.

## Questions Remaining

None. All five strategy questions answered; the synthesis follows this iteration.

## Sources Consulted

- AGENTS.md:81, 99, 110-112, 262-278, 282-286, 315-316, 322-331, 351-359, 363-377, 399-407, 463, 466, 475-479
- .opencode/skills/sk-doc/sk-create-skill/references/shared/skill-root-metadata-contract.md:1-32,40-41,47,65-73,77,85
- .opencode/skills/mcp-code-mode/references/naming-convention.md:1-6,578; .opencode/skills/mcp-code-mode/SKILL.md:4,69,103,258
- .opencode/skills/system-spec-kit/references/retrieval/retrieval-conventions.md:40,61-64,167-173
- .opencode/hooks/goal/bin/goal.cjs (existence); .opencode/skills/system-spec-kit/runtime/data/trigger-index.json (existence); the two retrieval .mjs scripts (tracked)
- repo-rules/REPO RULES.md:68 (the skill-hub-routing row); repo-rules/blast-radius.md:72; the Sections 3-6+8-10 texts of this document's first-read evidence
- This lineage's ledger receipts, sequences 1-5

## Assessment

- newInfoRatio: 1.0
- Novelty justification: the second-half verdicts, the:64-verbatim seconding of the:315 features, the goal-hook and trigger-index existence checks, and the five-question adjudication are all first recordings; the adjudication synthesizes prior findings but its keep-case list and its judgment disclosure are new.
- Confidence: the quoted delegate lines OBSERVED. INFERRED, each with its confirming step: the:359-has-no-delegate (read the skill's remaining references); the:355's independence-clause delegate (the system-skill-advisor's own docs); the:81's committed-ness for the data file (the targeted git ls-files).

## Reflection

What worked: ending on the adjudication rather than another sweep: the four prior iterations had accumulated exactly the evidence the:363-377 test consumes, and the keep-cases fell out of the delegates' own words rather than judgment.
What failed: the:git ls-files glob did not reach the data file, so the:81's "committed" survives as INFERRED with the confirming command named; and the:359/:355 delegate-absence calls rest on greps, not reads, and are marked as such.
Ruled out (see deltas/iter-005.jsonl): that the:315 feature-list lacks a delegate (the:64 row secondes it verbatim), and that the goal-hook binary or the trigger index is missing.

## SCOPE VIOLATIONS

None. Every write this iteration stayed inside the lineage directory.

## Recommended Next Focus

Synthesis. Compile research.md at the lineage root (the 17-section contract: findings deduplicated by theme, citations carried, the mandatory Eliminated Alternatives between the Recommendations and the Open Questions, the convergence report), set the config's status to complete, and record the terminal synthesis_complete event through the gateway with stopReason "maxIterationsReached".
