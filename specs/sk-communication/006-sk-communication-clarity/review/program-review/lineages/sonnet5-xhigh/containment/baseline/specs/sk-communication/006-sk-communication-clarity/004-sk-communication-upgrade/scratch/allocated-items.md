The eight authorised engine items of this phase, with their owning surfaces.

1. One instruction declaration: src/config/copy-editing-instruction.ts
2. The instruction resolves to the wording standard's reply base: src/config/copy-editing-instruction.ts, the skill file it reads, the docs ship copy the sealed pack forced
3. Both profiles read provider-default thinking: src/config/local-provider.ts, src/runtime/external-cli-projection.ts
4. The five pass markers moved inside the guard whose comparisons they describe: src/fidelity/validator.ts
5. The claim-omission veto inside the same guard: src/fidelity/semantics.ts, src/fidelity/validator.ts
6. The changeKind field on the accepted record, no-op when unchanged: src/contracts/projection.ts, src/fidelity/types.ts, src/fidelity/validator.ts
7. The test floor: test/config/copy-editing-instruction.test.ts, test/providers/helpers.ts
8. The package gate: npm run check, before and after, recorded under scratch/
