{
  "resourceContractVersion": 1,
  "skill": "cli-jev",
  "version": "0.1.0.0",
  "description": "Declarative packet registry: workflowMode, packetKind, backendKind, toolSurface, and advisorRouting for cli-jev. The advisor routes the hub identity; the hub resolves its one transport packet.",
  "discriminator": {
    "workflowMode": "Public hub/mode key. Stable identity used by the hub router and this registry. Carried by every mode.",
    "packetKind": "Axis discriminator: workflow for process/lifecycle packets, surface for read-only evidence-base packets, transport for packets that bridge to an external tool's CLI/MCP surface.",
    "backendKind": "Which backend runs the mode. Surface packets use evidence-base. Workflow packets use the hub's declared backend kinds."
  },
  "advisorRoutingContract": {
    "routingClass": "How the advisor finds the packet. lexical = weighted-regex scoring and projection maps. alias-fold = projection-only alias. metadata = resolved by hub membership with no advisor map entry. command-bridge = routed by command. Surface packets and transports use metadata.",
    "legacyAdvisorId": "Legacy skill id used by lexical and alias-fold projection maps only.",
    "advisorDefaultMode": "Marks the default mode a multi-mode legacy id folds to. Never inferred from array order.",
    "legacyAliases": "Advisor system-id aliases for lexical or alias-fold routes; drift guards compare these order-insensitively.",
    "driftGuard": "Path to the registry-vs-advisor drift-guard test. Required (here or in the advisor-projection extension) when any mode uses routingClass lexical or alias-fold.",
    "packetSkillName": "The packet SKILL.md name. Folder equals packetSkillName unless grandfatheredFolderMismatch is true.",
    "grandfatheredFolderMismatch": "Required on every mode; false unless a legacy folder/name mismatch is intentionally preserved.",
    "toolSurface": "Required on every mode; declares allowed tools, forbidden tools, mutating posture, and any bash allowlist."
  },
  "modes": [
    {
      "workflowMode": "cli-usage",
      "packetKind": "transport",
      "backendKind": "cli-dispatch",
      "toolSurface": {
        "allowed": ["Read", "Bash", "Grep", "Glob"],
        "forbidden": ["Write", "Edit", "Task"],
        "mutatesWorkspace": false,
        "bashAllowlist": []
      },
      "packet": "cli-usage",
      "packetSkillName": "cli-usage",
      "grandfatheredFolderMismatch": false,
      "command": null,
      "aliases": [
        "jev cli",
        "jev judgment",
        "typesafe jev",
        "type-safe jev",
        "cli-jev",
        "cli jev",
        "jev probability judgment",
        "typed judgment cli"
      ],
      "advisorRouting": {
        "routingClass": "metadata",
        "packetSkillName": "cli-usage"
      }
    }
  ],
  "extensions": {
    "transport-axis": {
      "declaredField": "transports",
      "description": "The transport axis: modes that bridge an external tool's CLI/MCP surface, return a typed verdict, and never perform the hub's own judgment. A transport declares mutatesWorkspace:false and forbids Write/Edit/Task; it selects, while a workflow acts. The advisor stays blind to the axis — every transport is routingClass 'metadata' and the hub resolves it, so no advisor map entry exists per transport.",
      "transports": ["cli-usage"],
      "enforcedBy": "parent-hub-check rule 3h (routingClass metadata, mutatesWorkspace false, forbidden Write/Edit/Task, membership in this array) and rule 5i (tieBreak lists workflow modes before transports)"
    }
  }
}
