diff --git a/.skilled/skills/cli-external-orchestration/mode-registry.json b/.skilled/skills/cli-external-orchestration/mode-registry.json
index 95fc01b224a..4309235ae88 100644
--- a/.skilled/skills/cli-external-orchestration/mode-registry.json
+++ b/.skilled/skills/cli-external-orchestration/mode-registry.json
@@ -1,11 +1,11 @@
 {
   "skill": "cli-external-orchestration",
-  "version": "1.6.0.0",
+  "version": "1.7.0.0",
   "resourceContractVersion": 1,
   "description": "Declarative mode registry: the single source of truth for the discriminator (workflowMode/packetKind/backendKind) PLUS the advisorRouting projection for cli-external-orchestration. The hub reads this file to resolve a mode; the advisor routes any external-CLI-dispatch query to the single identity cli-external-orchestration and the hub picks the mode. All seven modes route by hub membership (routingClass 'metadata'), so there is no advisor map entry per mode and no merged-identity projection — the advisor does not read this file at runtime. This registry is also the executor-delegation scorer's source of truth for which CLI executors exist (system-skill-advisor/runtime/lib/scorer/executor-delegation.ts), keyed by packetSkillName — and every mode here is a workflow executor.",
   "discriminator": {
     "workflowMode": "Public hub/mode key. Stable identity used by the hub router and this registry. Carried by every mode.",
-    "packetKind": "The axis discriminator: 'workflow' for cli-opencode, cli-claude-code, cli-codex, cli-cursor, cli-devin, cli-pi, and cli-hermes — each classifies intent, chooses/confirms a provider, and conducts a dispatched CLI session whose writes land in this repo's workspace. One extension (transport-axis) is declared, currently holding no transports; no surface-axis, because no mode in this hub is read-only evidence — all seven orchestrate, and each mode's declared writes land in this repo's workspace. cli-cursor's native worktree/cloud-worker surfaces are opt-in escape hatches, not its default dispatch shape.",
+    "packetKind": "The axis discriminator: 'workflow' for cli-opencode, cli-claude-code, cli-codex, cli-cursor, cli-devin, cli-pi, and cli-hermes — each classifies intent, chooses/confirms a provider, and conducts a dispatched CLI session whose writes land in this repo's workspace. No extension is declared: the transport-axis left with the cli-jev transport, which is now the cli-usage mode of its own hub at .skilled/skills/cli-jev/, and no surface-axis exists because no mode in this hub is read-only evidence — all seven orchestrate, and each mode's declared writes land in this repo's workspace. cli-cursor's native worktree/cloud-worker surfaces are opt-in escape hatches, not its default dispatch shape.",
     "backendKind": "Which backend runs the mode: cli-dispatch for all seven external CLI modes."
   },
   "advisorRoutingContract": {
@@ -241,13 +241,5 @@
         "routingClass": "metadata"
       }
     }
-  ],
-  "extensions": {
-    "transport-axis": {
-      "declaredField": "transports",
-      "description": "The transport axis: modes that bridge an external tool's CLI/MCP surface, return a typed verdict, and never perform the hub's own judgment. A transport declares mutatesWorkspace:false and forbids Write/Edit/Task; it selects, while a workflow acts. The advisor stays blind to the axis — every transport is routingClass 'metadata' and the hub resolves it, so no advisor map entry exists per transport.",
-      "transports": [],
-      "enforcedBy": "parent-hub-check rule 3h (routingClass metadata, mutatesWorkspace false, forbidden Write/Edit/Task, membership in this array) and rule 5i (tieBreak lists workflow modes before transports)"
-    }
-  }
+  ]
 }
