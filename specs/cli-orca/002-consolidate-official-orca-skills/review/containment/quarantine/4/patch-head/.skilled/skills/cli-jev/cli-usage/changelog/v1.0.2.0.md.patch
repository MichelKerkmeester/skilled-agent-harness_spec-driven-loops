diff --git a/.skilled/skills/cli-jev/cli-usage/changelog/v1.0.2.0.md b/.skilled/skills/cli-jev/cli-usage/changelog/v1.0.2.0.md
index 80761b868d8..6cc21de6e22 100644
--- a/.skilled/skills/cli-jev/cli-usage/changelog/v1.0.2.0.md
+++ b/.skilled/skills/cli-jev/cli-usage/changelog/v1.0.2.0.md
@@ -17,6 +17,8 @@
 - The judgment contract: the eight hard rules and their implemented checks, the four references, the
   question-shaping card, the `JEV-` scenario ids and the recorded run verdicts are the same evidence
   at a new path.
-- The dispatch shape's display name. The audit still reports a `jev` command as a `cli-jev` dispatch
-  because `cli-jev` remains the hub id and a mode alias; only the packet path the guards read
-  (`cli-jev/cli-usage/SKILL.md`) moved with the folder.
+- The dispatch shape registered in the dispatch audit. Its display name stays `cli-jev`, because
+  `cli-jev` remains the hub id and a mode alias, while the packet path the guards read moved to
+  `cli-jev/cli-usage/SKILL.md` in the decoupling step that removed the retired mode from the old hub.
+  The eight checks are enforced again at preflight: `jev run @request.json --value` is refused with
+  the packet's own `[jev-value-not-with-run]` rule text.
