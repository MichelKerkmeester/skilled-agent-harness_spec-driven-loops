DEEP-REVIEW
Resolved route: mode=review; target_agent=@deep-review; execution=single_review_iteration; state_source=externalized_files; do_not_switch_mode=true

# Iteration 3 Prompt

STATE SUMMARY: Iteration 3 of 4; dimension=traceability; prior findings P0=0 P1=0 P2=1; stopPolicy=max-iterations.

Execute both hard traceability protocols against current packet and implementation evidence. Run strict packet validation, inspect every checked-row evidence class, reconcile completion wording with sibling packet 020, and emit typed adjudication for every new P0/P1. Target files are read-only; write only inside `confirm-b`.
