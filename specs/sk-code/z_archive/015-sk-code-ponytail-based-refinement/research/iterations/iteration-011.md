# Iteration 11 — Round 2 (adversarial cross-verify) — seat round2-opus (claude-opus-4-8)

- Verifies: gpt-lane + synthesis recommendations (refute-first)
- Status: complete | exit=0 | duration=283s
- Net: all gpt-lane/synthesis recs SURVIVE; 0 refuted as already-present (grep-confirmed absent). One correction: ladder is highest-VALUE but highest-RISK in ADOPT-NOW (not 'lowest-risk'); safe first wave = additive doc rows.

## Seat output

I have read all cited files and run the refute-first checks. Producing the verdict to stdout per the orchestrator's output rule (read-only sub-agent task; no plan file, no ExitPlanMode).

---

# Round-2 Adversarial Cross-Verify — GPT-lane + Synthesis

**Stance:** refute-first. Every claim defaulted to REFUTED unless the on-disk files confirmed it. I read the real targets — `code_quality_checklist.md`, `removal_plan.md`, `code-task-scorer.cjs`, `correctness-gate.cjs`, `sweep-benchmark.cjs`, plus the three iteration files and `spec.md` — and grepped to disprove pre-existence.

## CLAIM 1 — iter-008 stdlib/native/shrink rows + removal_plan `Replacement` field → **CONFIRMED**

- **stdlib / native gap is real.** `code_quality_checklist.md` §6 Maintainability (L103-118) lists repeated-logic, magic-literals, nesting, hidden-coupling, function-length, Demeter, least-astonishment, TODO/FIXME, hardcoded-env — but **no** "hand-rolled stdlib" or "native-duplication" row. `rg -ni "stdlib|standard.library|hand-rolled|native|replacement"` over both files returned **zero hits**. Pre-existence refuted → the rows are genuinely additive.
- **Non-overlap holds.** There is *conceptual* overlap with §7 KISS ("abstractions without need", L127) and §6, but no row *names* stdlib/native replacement. The proposed §6 rows target a distinct, concrete smell. Additive, not duplicative.
- **shrink is honestly a *partial* gap.** §6 (nesting, >20-line functions, L111) and §7 DRY partially cover "same behavior, fewer lines," but no explicit "shorter built-in expression" row exists. iter-008 F2 labels it "Partial gap" itself — no overstatement.
- **shrink → ADOPT-LATER is correct.** It is the most subjective of the three (style-churn / nit-noise risk); iter-008 F2 flags "could become style churn," iter-010 rank 9 defers it. Sound. The two objective rows (stdlib/native) stay ADOPT-NOW; severity correctly anchored P2 per §7 L141.
- **`Replacement` field gap is real.** `removal_plan.md` §2 table (L41-48) = Location/Rationale/Evidence/Impact/Deletion Steps/Verification — **no** Replacement field. Additive, low-risk actionability improvement. iter-008 F3 concedes "Deletion Steps implies this but does not require it" — accurate.

## CLAIM 2 — iter-006 fold `code_loc` + over-eng markers into Lane B sweep *behind the correctness gate*, NOT a clone → **CONFIRMED**

- **Sweep does NOT already emit these.** `rg "code_loc|overengineering|simplicity"` across `sweep-benchmark.cjs`, `sweep-reporter.cjs`, `code-task-scorer.cjs`, `sweep-stats.cjs`, `score-model-variant.cjs` → **zero hits**. Pre-existence refuted → additive.
- **Row-vector evidence matches the iteration.** `sweep-benchmark.cjs:444-471` carries `correctness_pass_rate`, `format_adherent`, `output_words/chars`, `latency_ms`, and **null** `tokens_in/out`/`cost_usd` (L468-470) — exactly as iter-006 claims (output_words/chars present; code_loc absent; token/cost null). Line cites are slightly loose (`scoreCodeTask` is L320-365, not "300-360") but the mechanism is accurate.
- **"Behind correctness gate" is achievable with the *existing* `correctness-gate.cjs`.** `applyGate` (L124) already partitions groups into `eligible` (correctness_mean ≥ threshold) vs `ineligible`, and ranks **only the eligible**. A new `code_loc_median` reported "after correctness eligibility" is a pure reporter-layer addition — the eligible/ineligible split makes gating trivial, and `code_loc` could also drop straight into the `byFormatThenEfficiency` tiebreaker (L75-84). No gate redesign needed. CONFIRMED.
- **Correctly bucketed ADOPT-LATER** (iter-010 rank 11, "good eval add-on; not core skill behavior") — deliberately **not** in ADOPT-NOW. Appropriate: heavy/noisy benchmark runs, eval-only surface. Bucket stands.

## CLAIM 3 — iter-010 synthesis ADOPT-NOW bucket + best/overrated picks → **DOWNGRADED** (one framing overstatement; bucketing itself sound)

- **ADOPT-NOW set (8 items) matches table ranks 1-8 exactly.** Every item I could check on disk holds: stdlib/native rows (real gap), `Replacement` field (real gap), Review-status canary (target is real — `sk-code-review/SKILL.md:350-358` `Review status: APPROVED|REQUESTED_CHANGES|COMMENTED`, a genuine automation-parsed triplet), and the ladder's three target paths all exist (`sk-code/SKILL.md`, `references/phase_detection.md`, `references/universal/code_quality_standards.md`). SC-002 traceability intact.
- **DO-NOT-ADOPT bucket is sound — nothing unfairly rejected.** lite slider = Iron-Law violation; PromptFoo clone = duplicates the already-gated sweep; standalone ponytail-review = violates the one-baseline rule; `net -N`/LOC severity = numeric-gate anti-pattern that contradicts §7's P2-default + non-numeric gating; `ceiling:` in ALLOWED_PATTERNS = same-line forbidden-ID bypass; new YAGNI severity class = confuses fix-scope with action-direction. All defensible.
- **"Most overrated = verification intensity slider" → CONFIRMED.** It is the flashiest ponytail transplant but the least compatible: `lite` lowers Phase-3 evidence (Iron-Law breach), `full/ultra` merely rename existing conditional escalation. Correctly demoted.
- **DOWNGRADE — the "best pick" superlative is overstated.** The synthesis calls the Phase-1 Design Restraint Ladder **both** "highest-value **and** lowest-risk." Plausibly highest-value, but it is the **highest-risk item in the ADOPT-NOW set**: it mutates sk-code's actual Phase-1 workflow and touches surface-router precedence + the Iron Law (the synthesis rates it "Low-**medium**"), whereas #3/#8 are pure additive doc rows rated "Low." "Lowest-risk overall" should read "acceptably low-risk." The ADOPT-NOW *bucketing* is fine; only the dual-superlative framing is wrong.
- **Soft spot, not a reassignment:** #6 (ceiling comments as P2-downgrade evidence) is the weakest ADOPT-NOW — it introduces a review *behavior* (downgrade-on-comment), not just a checklist row — but it is bounded by P2-only + the explicit never-suppress-security/correctness floor (iter-010 conflict matrix), so it stays ADOPT-NOW.
- **Unverified dependency (flagged, not blocking):** the ladder's *clean* integration into Phase 1 without breaking `OPENCODE>WEBFLOW>UNKNOWN` precedence or the Iron Law is *asserted* by synthesis; I did not exercise it. Acceptable for a research/recommendation packet (SC-003 = no code changed), but the follow-up `/speckit:plan` packet must prove it before implementing.

## Net

The GPT-lane + synthesis recommendations **survive refute-first cross-verification.** Every claimed gap is real on disk, every target is grep-traceable, bucketing is internally consistent, and negative-knowledge correctly rejects the Iron-Law-violating and numeric-gate items. The single substantive correction: the synthesis over-labels the Design Restraint Ladder as *lowest-risk* — it is the highest-risk ADOPT-NOW item. Safe first implementation wave = the additive doc rows (stdlib/native, `Replacement` field, Review-status canary); treat the ladder as highest-value-but-verify-integration.

```json
{"seat":"round2-opus","verifies":"gpt-lane + synthesis","verdicts":[{"claim":"iter-008 stdlib/native/shrink rows + removal_plan Replacement field","verdict":"CONFIRMED","evidence":"code_quality_checklist.md:103-118 (no stdlib/native row), :122-142 (no shorter-built-in row), removal_plan.md:41-48 (no Replacement field); rg stdlib|native|replacement => 0 hits","reason":"All four gaps real on disk and additive; rows non-overlapping with the explicitly-named §6/§7 rows; shrink honestly a partial gap.","adjusted_bucket":"ADOPT-NOW"},{"claim":"iter-008 shrink row bucketed ADOPT-LATER (style-churn)","verdict":"CONFIRMED","evidence":"iteration-008.md:15,34 (Partial gap + Medium style-churn risk); iteration-010.md:26 rank 9","reason":"shrink is the most subjective of the three; deferral is correct while stdlib/native stay ADOPT-NOW at P2.","adjusted_bucket":"ADOPT-LATER"},{"claim":"iter-006 fold code_loc + over-eng markers into Lane B sweep behind correctness gate, not a clone","verdict":"CONFIRMED","evidence":"rg code_loc|overengineering|simplicity over sweep-benchmark.cjs/sweep-reporter.cjs/code-task-scorer.cjs => 0 hits; sweep-benchmark.cjs:444-471 row vector (output_words present, code_loc absent, tokens/cost null at 468-470)","reason":"Markers genuinely absent today (additive); gating is straightforward — correctness-gate.cjs:124 already ranks only eligible groups, so an after-eligibility metric is a reporter-layer add, no gate redesign.","adjusted_bucket":"ADOPT-LATER"},{"claim":"iter-006 'behind correctness gate' achievable with existing correctness-gate.cjs","verdict":"CONFIRMED","evidence":"correctness-gate.cjs:124-172 applyGate eligible/ineligible split; :75-84 byFormatThenEfficiency tiebreaker","reason":"Eligible-only ranking + an existing efficiency tiebreaker chain make code_loc trivially gateable downstream of the row vector.","adjusted_bucket":"ADOPT-LATER"},{"claim":"iter-010 ADOPT-NOW bucket {ladder, ladder-in-universal-doc, stdlib/native rows, Review-status canary, neutral ceiling comments, ceiling-as-P2-downgrade, needed-ness KISS prompt, Replacement field}","verdict":"CONFIRMED","evidence":"iteration-010.md:18-25 ranks 1-8; sk-code-review/SKILL.md:350-358 Review status triplet; sk-code/SKILL.md + references/phase_detection.md + references/universal/code_quality_standards.md all exist","reason":"All eight are real, grep-traceable, severity-neutral additions; DO-NOT-ADOPT rejections (lite/clone/standalone/numeric-gate) are all sound. #6 is the softest (adds behavior) but bounded by the never-suppress-security floor.","adjusted_bucket":"ADOPT-NOW"},{"claim":"iter-010 best pick = Phase-1 Design Restraint Ladder (highest-value AND lowest-risk)","verdict":"DOWNGRADED","evidence":"iteration-010.md:58 best='Low-medium risk' vs :20,25 stdlib/native+Replacement rated Low; targets sk-code SKILL.md/phase_detection/universal","reason":"Plausibly highest-value but it is the HIGHEST-risk ADOPT-NOW item (mutates Phase-1 workflow + router precedence + Iron Law); 'lowest-risk overall' is overstated — additive doc rows are strictly lower-risk. Bucket stays ADOPT-NOW.","adjusted_bucket":"ADOPT-NOW"},{"claim":"iter-010 most-overrated = verification intensity slider","verdict":"CONFIRMED","evidence":"iteration-010.md:36,49,60; lite lowers Phase-3 evidence (Iron-Law breach), full/ultra rename existing conditional escalation","reason":"Correctly identified as the flashiest-but-least-compatible transplant; DO-NOT-ADOPT is right.","adjusted_bucket":"DO-NOT-ADOPT"}],"false_positives_found":["None of the GPT-lane/synthesis recommendations are refuted as already-present or duplicative — all claimed gaps confirmed absent on disk.","Only non-fatal issue: synthesis labels the ladder simultaneously highest-value AND lowest-risk; it is the highest-risk item in its own ADOPT-NOW bucket (framing overstatement, not a mis-bucket).","Ladder's clean integration into sk-code Phase-1 (no break to OPENCODE>WEBFLOW>UNKNOWN precedence / Iron Law) is asserted but not exercised here — must be verified in the follow-up plan packet."],"net_assessment":"GPT-lane + synthesis recommendations survive refute-first verification. Every claimed gap (stdlib/native/shrink rows, Replacement field, code_loc/over-eng markers) is genuinely absent on disk and additive; targets are grep-traceable; buckets are sound and internally consistent; negative-knowledge correctly rejects Iron-Law-violating and numeric-gate items. Single correction: demote the synthesis's 'lowest-risk' framing of the Design Restraint Ladder to 'acceptably low-risk' — it is the highest-risk ADOPT-NOW item. Recommend implementing the additive doc rows (stdlib/native, Replacement field, Review-status canary) as the safe first wave and treating the ladder as highest-value-but-verify-integration."}
```
