# Doctor MCP debug fixture

Frozen sentinel for a diagnostic-only `debug` route. The scenario omits `--fix`, so the selected workflow may inspect health but must not repair or rewrite configuration.

No fixture write is permitted.
