# Command-surface fixture specification

Author a minimal conformance benchmark with benchmark id `fixture-command-surface` for the nested demo mode. The package is an input contract only and must not invoke an adapter or alignment run.
