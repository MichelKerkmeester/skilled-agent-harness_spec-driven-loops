# Design-audit workflow fixture

Frozen input for a read-only design-quality audit of a small interface. The source contains deliberate contrast, focus, and responsive weaknesses so the audit can return concrete findings without changing the files.

No fixture write is permitted.
