# Doctor MCP install fixture

Frozen sentinel for the positional `install` route. The cell stops at the workflow-owned installation approval gate, before any install command can run.

No fixture write is permitted.
