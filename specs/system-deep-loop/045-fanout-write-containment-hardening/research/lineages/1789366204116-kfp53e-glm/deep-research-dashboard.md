# Deep-Research Dashboard

**Session.** fanout-glm-1789366204116-kfp53e | generation 1 | lineageMode new | loopType research
**Policy.** convergenceMode=off (telemetry only) | stopPolicy=max-iterations | convergenceThreshold=0.05 | stuckThreshold=3
**Auto-generated by the lineage reducer (inline).** Never manually edited; rebuilt from the state log, deltas, registry, and strategy.

## Gauges

| Gauge | Value |
|---|---|
| Iterations completed | 3 / 3 (CAP REACHED — stop reason maxIterationsReached) |
| newInfoRatio trend | 1.0 → 0.85 → 0.75 |
| Findings (registry) | 19 (P1×11, P2×5, P3×3) |
| Ruled out (registry) | 5 |
| Questions answered | 5 / 5 (q1 structural-only; its weights carried) |
| Convergence (telemetry) | off — the loop ran to the cap by policy |
| Last event | iteration 3 complete, VERIFY_EXIT=0 ×3, ledger seq 4, ledger-backed |

## Iterations

| # | Focus | newInfoRatio | Status | Findings | Verify |
|---|---|---|---|---|---|
| 1 | cost decomposition: what the 22.2 s and the 1.6 GB actually are | 1.0 | complete | 8 | OK (route-proof + delta, ledger-backed) |
| 2 | challenge the mechanism rulings: cone granularity, shared state, clonefile gaps | 0.85 | complete | 6 | OK (route-proof + delta, ledger-backed) |
| 3 | relocation pins, the consolidated unmeasured ledger, the reordered adoption | 0.75 | complete | 5 | OK (route-proof + delta, ledger-backed) |

## Open Questions (carried to synthesis)

q1 leg weights; the 101M dependency-bootstrap provenance; the skip-primary's one-time cost; the clonefile's foreign-stat/ignored-tree; the 29-worktree production interference; mid-run relocation; the unpinned version floor; the 14-site guard audit. (Registry `openQuestions`, 10 items.)

## Next Focus

None — the cap is reached; synthesis follows.
