1|The sweep's liveness proof: what marker proves a worktree is still owned, who writes it and when, how it survives an interrupted run, and whether the existing loop-lock lease can simply be projected into the worktree namespace instead of inventing a second mechanism
2|Copy-back write semantics: ordering, atomicity and ownership claim when the lineage directory is published back into the main checkout, and which of the four possible concurrent writers (the parent's salvage pass, a second fan-out using the same label, the operator, a stale previous copy) it may overwrite
3|Whether the existing lease machinery (TTL, heartbeat, nonce, fenced reclaim) can cover the two genuinely shared surfaces (the worktree prefix and the copy-back target) by widening its scope, or whether those surfaces need a different mechanism, and what breaks either way
4|The verification gap: what a concurrent negative control must assert so a destructive sweep cannot pass, and which OTHER current acceptance criteria would wrongly certify a hazard as correct
5|Sweep versus resume ordering: what happens when an interrupted run's worktrees are reclaimed while the ledger is about to requeue that lane, plus the retained-worktree case after a copy-back failure, and how the two mechanisms should be sequenced
