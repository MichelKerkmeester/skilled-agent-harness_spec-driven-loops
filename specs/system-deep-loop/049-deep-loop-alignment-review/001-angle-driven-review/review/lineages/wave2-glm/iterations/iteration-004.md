# Dimension / Focus

**Angle 19 — The containment promise chain and the severity scale** (spec.md:106). Dimensions: **correctness (primary)** — the promise/act/truth gap of the verdict machinery, the packet's release-readiness' root; **traceability (secondary)** — the promise-site↔contract mapping and the 4-vs-3-tier question.

The angle demands two end-to-end traces and two rulings: (A) the containment promise — "comments promise fail-closed, inline code advises, the runner preserves" — trace it and say which level is authoritative; (B) the severity scale — the packet promises four tiers, the machinery speaks three — trace both, say which tier-set is authoritative. This iteration traces BOTH to their termini and finds the SAME disease angle 17 found in the fleet governance (F010's 23:3): **two unreconciled authorities, convention-heldtrue, enforcement only at the parent**.

Method note: this lane's own artifacts are evidence — three dispatched iterations (the runner-spawned, native-pi, receiptless-at-the-command-level) + the measured gate-outputs (3× exit 0 via 2b not-enforced) + the invocation-metadata (the only receipt-shaped artifact this lane produced) + the 3× self-reported CONDITIONAL verdicts (reducer-verifiable-in-advance: P0 0 ∧ P1 > 0 → CONDITIONAL, :1847-1852).

# Files Reviewed

1. `.opencode/commands/deep/assets/deep-review-auto.yaml` (:1429-1558 the cli-codex dispatch-branch: "Fail closed: a requested codex executor must dispatch codex or abort" :1446; the receipts/recursion-guard/env-allowlist :1554-1555; "Fails closed when the codex binary is absent (exit 1) — parity with the fan-out cli-codex lineage guard in fanout-run.cjs; codex routing never degrades to native" :1556; the stdin-`-`/`-c model_reasoning_effort=`/`--sandbox workspace-write`/`-c approval_policy=never` mechanics :1557-1558; the structural write-containment note :1515; the pivot failure contract :823 "A native dispatch failure binds status=rejected... then proceeds"; the fanout/single-executor branch :198-217; the stop-reason ceilings :626; :1436-equivalent the runner-side "NEVER spawn a nested CLI" — 17 codex-mentions, ALL the codex-branch's own)
2. `.opencode/commands/deep/assets/deep-research-auto.yaml` (17 codex-mentions — the parallel cli-codex branch: the SAME receipt/guard/allowlist contract, by-count)
3. `.opencode/commands/deep/assets/deep-review-confirm.yaml` + `deep-research-confirm.yaml` + `.opencode/commands/deep/review.md` + `research.md` (ZERO codex-mentions — the confirm variants and the command layer carry NO dispatch-receipt machinery at all)
4. `.opencode/skills/system-deep-loop/deep-review/assets/prompt-pack-iteration.md.tmpl` (:69 the verdict-mapping — "PASS if no P0 or P1...; CONDITIONAL if any P1 (no P0); FAIL if any P0. P2-only findings → PASS. An active P0 forces `Review verdict: FAIL` — never relabel it as conditional, partial, mixed, or advisory, and truncated/partial output is not a valid substitute for the final line. Downstream automation (synthesis phase, CI gate parser) parses this final line via exact string match — do not vary the format"; :148 — "`--event-json` must name the SINGLE-record file... Exit `0` = the record is durable...; exit `2` = refused → STOP and name the failed check. Never fall back to a direct write.")
5. `.opencode/skills/system-deep-loop/runtime/scripts/verify-iteration.cjs` (:178-183 the verdict check — the verdictPattern, EXACTLY ONE such line, the FINAL line: the FORMAT, not the truth; :269-291 the 2b fail-loudly; :286-292 the 2c "advisory only — a warning, never a hard failure"; :200-201+289-290 the not-enforced mid-migration)
6. `.opencode/skills/system-deep-loop/runtime/scripts/reduce-state.cjs` (:1847-1852 the verdict ternary — computed from the FINDINGS' severity, not from any lane's self-report; :1896 "- Provisional Verdict: ${verdict}"; :2050 the CONDITIONAL-until-search-debts caveat (the v2-world); the SEVERITY_KEYS/WEIGHTS constants (read at phase_init))
7. `.opencode/skills/system-deep-loop/runtime/scripts/fanout-merge.cjs` (:12 "strongest-restriction: any lineage P0 → merged FAIL"; :23 `SEVERITY_RANK = { P0: 3, P1: 2, P2: 1 }`; :751-757 the REVIEW MERGE + "Deduplication is by findingId; cross-lineage P0 wins if any lineage reports it"; :781 the status-only-filter/empty-PASS guard; :823 `activeP0 = severity === 'P0' && (disposition ?? status) === 'active'`)
8. `.opencode/skills/system-deep-loop/runtime/scripts/fanout-run.cjs` (:473 `'cli-codex': 'SPECKIT_CODEX_STATE_DIR'`; :2067-2094 the codex availability+command assembly; :2327 the adapter-flags note; :2491-2501 the model-family fronts; :1436 the prompt-side "NEVER spawn a nested CLI, agent, or subprocess..."; the ZERO verify-iteration references — recorded at iterations 1-2, re-confirmed)
9. `.opencode/skills/system-deep-loop/deep-review/SKILL.md` (:3/:10/:14 "P0/P1/P2"; :108 the REVIEW_ITERATION keywords "P0","P1","P2"; :323-325 the severity table — the repo's 3-row severity MEANING; :331-333 the verdict effects; :55 the fanout-merge strongest-restriction: "any lineage active P0 → merged FAIL")
10. The FOUR promise-sites: `goal.md:47` ("hand every confirmed finding of any severity, P0 through P3, to a phase of the parent"), `goal.md:57` (D3: "Every finding cites file and line, is rated P0 to P3, and is verified by the orchestrator..."), `spec.md:83` ("...rates each finding P0 to P3 **with the repo's severity meaning**. Angles 11 to 20 are rewritten after wave one lands, so a wave-two lane reads this file fresh."), `spec.md:138` ("| REQ-003 | Every finding, P0 to P3, is verified against the tree and bound to a parent phase or recorded as refuted with the reason |")
11. The 9+6 non-codex residue-loci: `.opencode/skills/cli-external-orchestration/cli-hermes/SKILL.md:345`, `cli-pi/SKILL.md:134` (the SIX PROVIDERS: openai-codex, opencode-go, cline-pass, llmgateway/DevPass, minimax, xiaomi — "Two of them... are not Pi builtins and exist only because `.pi/models.json` declares them") + `:303`, `ROUTER.md:24` + `:54` + `:92` (the CODEX keyword-weights) + `:109-110` + `:145`, `cli-cursor/SKILL.md:84` ("unlike Codex's ~/.codex/state/<id>/lock") + `:263` ("Each `.cursor/agents/<name>.md` is a **symlink** to the canonical `.claude/agents/<name>.md... One source of truth, so a mirror can never drift from the agent it mirrors") + `:416`, `cli-devin/SKILL.md:294` (the SAME symlink/canonical/never-drift sentence) + `:361` (the PID-scoped kill discipline) + `:464`
12. The wave1-glm precedent: its report's F006 (:66, "spec.md is the 4-tier outlier... spec.md:83, :135 vs sk-code review-core.md:32-34, :95 + the state schema... the spec's P3 is orphaned. Fix: amend the spec's wording (the parent's, at the REQ-003 binding)") + the W7 clauses (:32, :42, :91)
13. This lane's own artifacts (the insider evidence): 3× state-rows of self-reported `CONDITIONAL` (each reducer-verifiable: P0 0 ∧ P1 > 0), 3× gate-outputs `{"ok":true,"detail":"iteration N complete: narrative + route-proof + delta"}` exit 0, the runner-written `invocation-metadata.json` (receipt-shaped, lane-level), the runner-written audit/effect-ledger intent+completion frames (the runner-level receipts)

# Scorecard

- Iteration 4 of 5. Focus: angle 19. Dimensions: correctness + traceability. No gating failures.
- Findings: 2 new (P0 0, P1 1, P2 1) → cumulative 15 active (P0 0, P1 4, P2 11); 0 resolved; 0 refined.
- `newFindingsRatio` (count-based, precedent): (2 + 0.5×0) / (13 + 2 + 0) = 0.13.
- Convergence telemetry (mode: off — signals TELEMETRY ONLY; stop = iteration 5 of 5, `stopReason: maxIterationsReached`): rollingAvg (last 2) = (0.31 + 0.13)/2 = 0.22 ≥ 0.08 → the rolling signal votes NO-STOP; the 4-point MAD window (1.00, 0.44, 0.31, 0.13): mean 0.47, MAD ≈ 0.27 (noisy); the novelty-DECAY (1.00 → 0.44 → 0.31 → 0.13) = the expected saturation shape — the corroboration that the lane's 5th iteration is the deliberate close, NOT a licence to stop early (the covenant: stop = iteration 5, `stopReason: maxIterationsReached`); coverage 4/4 = 1.0 maintained (correctness: THIS angle's primary; the others: 16-18); minStabilizationPasses (elevated to 2): iterations 4-5 = the recorded consecutive pass — this is pass 1 of the recorded pair; semanticNovelty: the two-authority verdict-stack + the receipts-privilege = new mechanism level; compositeStopScore: not emitted (mode off).
- Duration: 2026-09-15T13:38:00Z → 13:52:00Z (840,000 ms).

# Findings by Severity

## P0

No P0 findings this iteration.

## P1

### F014 — The containment promise is a seven-stage relay held by convention: comments promise fail-closed, the inline code advises, the verifier checks only the verdict's FORMAT, the lane self-reports, the reducer labels its own compute PROVISIONAL, the runner preserves, and the ONE hard consequence lives at the PARENT's merge — while the receipt/recursion/allowlist machinery is codex-branch-ONLY and the pack's "downstream automation" parses the SELF-REPORTED line, giving the verdict TWO unreconciled authorities (correctness, cross-consumer)

**Evidence — the relay, stage by stage.**
1. PROMISE (the comments): review-auto:1446 "Fail closed: a requested codex executor must dispatch codex or abort"; :1556 "Fails closed when the codex binary is absent (exit 1) — parity with the fan-out cli-codex lineage guard in fanout-run.cjs; codex routing never degrades to native"; the pack:148 "exit 2 = refused → STOP and name the failed check. Never fall back to a direct write"; the pack:69 "never relabel it as conditional, partial, mixed, or advisory... Downstream automation (synthesis phase, CI gate parser) parses this final line via exact string match — do not vary the format"; the verifier's 2b comment "Fail the iteration loudly".
2. ADVISE (the inline code): the 2c receipt check — "advisory only — a warning, never a hard failure" (verify-iteration.cjs:286-292); the ledger-backing's not-enforced (checkLedgerBacking :200-201, "it only turns into a real finding once..."); the (028) staging ("failure is non-fatal" — the confirm:1620 et al); the pivot failure contract (review-auto:823 — a native dispatch failure "binds status=rejected... then proceeds").
3. FORMAT-ONLY (the verifier): the verdict check = the verdictPattern, exactly ONE such line, the FINAL line (:178-183) — the SHAPE of "Review verdict: PASS|CONDITIONAL|FAIL", never its truth. A lane reporting PASS over three active P1s passes.
4. SELF-REPORT (the lane): the verdict's only carrier in-iteration = the narrative's final line — this lane's 3× self-issued CONDITIONAL (each, coincidentally, reducer-derivable: P0 0 ∧ P1 > 0).
5. RECOMPUTE (the reducer, PROVISIONAL): :1847-1852 — the verdict computed from the FINDINGS' severity (the 3-tier ternary), :1896 "- Provisional Verdict: ${verdict}" — the truth arrives at synthesis, explicitly labeled PROVISIONAL, i.e. the machinery itself acknowledges it is not the record of until then; :2050 the search-debt caveat (v2-world).
6. RUNNER-PRESERVES: zero verify-iteration references; the acceptance = records+disk+cap+stopReason (:976-1028); :1436 the prompt-side "NEVER spawn a nested CLI..." (a promise AGAIN, at the runner).
7. THE ONE HARD (the PARENT): fanout-merge.cjs :12/:751-757/:823 — the strongest-restriction, "Any lineage with an active P0 finding causes the merged result to be FAIL", SEVERITY_RANK {P0:3, P1:2, P2:1}, the active-P0 filter, the empty-PASS-over-active-P0s guard (:781) — the ONLY enforced consequence of ANY verdict, in the PARENT's step.

**The receipts-privilege.** The INTENT/COMPLETION dispatch receipts, the recursion guard (CODEX_SESSION_ID), and the env allowlist (CODEX_/OPENAI_/AZURE_OPENAI_) are implemented in the AUTO YAMLs' cli-codex branch ONLY (:1554-1555, "Routed through runAuditedExecutorCommand so codex produces INTENT + COMPLETION dispatch receipts"), running 17+17 codex-mentions across the two AUTO variants — while BOTH confirm variants, BOTH command .mds, and the SKILL carry 0/0/0/0/1. The runner-level receipts are UNIVERSAL (the runner's audit/effect-ledger intent+completion+hash-frames — this lane's own dispatched-and-ledgered record), but the COMMAND-level receipts are codex-branch-only: a native-pi dispatch (this lane) produced NO command-level receipt — its invocation-metadata.json is receipt-SHAPED (lane-level, runner-authored), not receipt-ENVIED. The residue hunt settles the spec's "copy-pasted codex residue in three non-codex branches" as COMPARATIVE PROSE, not copied mechanism: all 9+6 non-codex hub-mentions (cli-hermes:345, cli-pi:134/:303, ROUTER:24/:54/:92/:109-110/:145, cli-cursor:84/:263/:416, cli-devin:294/:361/:464) are sibling-roster, provider-roster, keyword-weight, or genuine contrast sentences — each cites a real codex relationship; NONE copies the receipt machinery. The residue that IS: the PROMISE-text (the runner's :1436, the (:1446-1558) notes) circulating without its non-codex ENFORCER.

**The two verdict authorities.** The pack:69's downstream automation "parses this final line via exact string match" — the SELF-REPORTED line; the reducer:1847 COMPUTES the verdict from the findings and :1896 prints it as PROVISIONAL; the merge:751 enforces the COMPUTED. If the downstream parser and the reducer are one component, the pack's clause is merely stale; if they are two (the pack says "synthesis phase, CI gate parser"), the packet's release-readiness root datum has two voices — the same 2-authority disease as F010's 23:3. Either way the pack's "do not vary the format" promise guards the REPORTED fact while nothing in-loop guarantees it equals the COMPUTED one; the divergence, when it eventually happens, is recorded-not-repaired (the state-log row keeps the false self-report; the registry carries the reducer's).

**Affected surfaces:** the pack's verdict/gateway clauses · verify-iteration.cjs 2b/2c/verdictPattern · reduce-state.cjs :1847-1852/:1896 · the runner's acceptance+prompt · fanout-merge.cjs :751-823 · the AUTO YAMLs' codex-branch receipt machinery · the 9+6 comparative loci · this lane's invocation-metadata and 3 self-reported verdicts.

**Claim-adjudication packet (required, P1).**
- **claim**: the verdict's truth is enforced NOWHERE in-loop — the promise/act relay reaches the truth only at the parent's merge, the in-loop checks are format- and advisory-grade, the command-level receipts are codex-branch-only, and the pack's own downstream-automation clause sets a SECOND verdict authority (the self-reported line) against the reducer's computed one.
- **evidenceRefs**: the seven relay stages (each cited); the 17+17-vs-0+0+0+0+1 receipt-coverage census; the 9+6 comparative-loci adjudication; this lane's 3× measured gate-outputs and 3× self-reported-then-reducer-derivable CONDITIONALs.
- **counterevidenceSought**: searched for the pack:69's "CI gate parser" — NOT LOCATED (Dead End 1: the clause names it; no repo component answers to it this iteration); searched the reducer for a state-log-vs-registry repair of a divergent self-report — the :1896 "Provisional" label acknowledges the gap, no repair located; searched the runner for a verdict-truth check — none (the acceptance never compares the self-report to the findings); searched the 9+6 hub-loci for copied receipt/guard/allowlist MECHANISM — all comparative.
- **alternativeExplanation**: the design may be deliberate provisional-then-merged (the :1896 label + the merge's rank = the truth-at-the-parent BY DESIGN, the pack:69's format-discipline = the parser-stability of a REPORTED-PROSE whose truth the merge owns) — this reading survives all evidence and reduces the defect to the 2-authority AMBIGUITY plus the receipts-privilege; it does not survive as "the in-loop gates enforce the promise" — they demonstrably advise.
- **finalSeverity**: P1. **confidence**: 0.8.
- **downgradeTrigger**: if the located downstream parser IS the reducer's own compute (one component, two names) AND the receipts-privilege is documented as codex-mandated (a recorded decision, not an omission), this drops to P2 documentation-staleness.

**Recommendation.** Close the relay: have the verifier's verdict check ALSO compare the self-reported verdict to the record's own findingsCount/summary (a 5-line consistency check at the FORMAT-check's collar); either locate-and-cite the pack:69's downstream parser or reword it to the reducer; extend runAuditedExecutorCommand's receipts to the non-codex branches or strike :1556's "parity" claim; note the (:1896)-PROVISIONAL→(:751)-enforced handoff in ONE place, by name.

## P2

### F015 — The severity scale: four promise-sites — now including a REQUIREMENT — against an all-3-tier machinery, where the spec's own wording cites the 3-tier meaning; the P3→P2 collapse remains undocumented convention; the authoritative tier-set is 3; wave1's F006 is STILL-TRUE post-rewrite, its citation stale by +3 lines, its recorded fix unapplied, and the very rewrite that should have amended the wording instead HARDENED it into a requirement (traceability, premise-delta)

**Evidence.**
1. THE FOUR PROMISE-SITES: goal.md:47 ("hand every confirmed finding of any severity, P0 through P3, to a phase of the parent"); goal.md:57 (D3: "is rated P0 to P3"); spec.md:83 ("rates each finding P0 to P3 **with the repo's severity meaning**"); spec.md:138 (REQ-003: "Every finding, P0 to P3, is verified against the tree and bound to a parent phase or recorded as refuted with the reason") — the wave-two rewrite (14:31) KEPT the 4-tier wording at :83 AND ADDED the requirement at :138.
2. THE ALL-3-TIER MACHINERY: the SKILL's severity table :323-325 (three rows — "the repo's severity meaning" the spec:83 ITSELF invokes: the 4-symbol/3-meaning drift is INTERNAL to the promise's own sentence); the SKILL's keywords :108 (P0/P1/P2); the pack:69's verdict-mapping (P2-only → PASS — no P3 case); review-core.md 1.5.0.11 (wave1: :32-34, :95); the state schema's 3-key findingsSummary/findingsNew validation; the reducer's SEVERITY_KEYS [P0,P1,P2] + SEVERITY_WEIGHTS {10/5/1}; the 9-gate set's severity-WEIGHTED signals; the .codex failure_type (p0|p1|p2|low_confidence); the merge's SEVERITY_RANK {P0:3,P1:2,P2:1}. EVERY computational, validating, gating, merging, and verdict-mapping surface: THREE.
3. THE COLLAPSE, UNDOCUMENTED: the packet's lanes rate P0/P1/P2 (wave1: "the lane recorded P0/P1/P2 per the governing contracts; the spec's P3 is orphaned"; this lane: the same, plus the recorded convention "any packet-P3 rating collapses to record-P2 with the four-tier intent stated in the iteration narrative" — an in-lane convention, in NO governing document). NO contract states the collapse; NO machinery carries a P3.
4. THE AUTHORITATIVE RULING (the angle's question): the 3-TIER EXECUTABLE CONTRACTS. The reducer's ternary, the validator's 3-key, the weights, the rank, the pack's mapping, the merge's filter — the operative definitions of P0/P1/P2 and the WHOLE effect-vocabulary (PASS/CONDITIONAL/FAIL/hasAdvisories) — are 3-tier; the 4th symbol exists only in the promise-text, where it is already HALF-RETRACTED by the spec:83's own "with the repo's severity meaning". The binding layer (REQ-003's "bound to a parent phase") is OUTSIDE this packet's write surface — the P3 is representable THERE (the parent's records), orphaned HERE (the lane/loop/merge records): the precise defect is the packet-INTERNAL records are 3-tier while the packet-BOUNDARY obligation is 4-tier-worded.
5. WAVE1-F006, STILL-TRUE AND HARDENED: the wave1 citation "spec.md:83, :135" — the 14:31 wave-two rewrite SHIFTED :135→:138 (+3 lines: the verified requirement-table), KEPT the wording, ADDED the REQ-003 row, and did NOT apply wave1's recorded fix ("amend the spec's wording"): the 4-tier promise survived its own amendment-window and gained binding force. Wave1's finding stands; its line-cite is stale by +3; its scope ("the 4-tier outlier... the P3 is orphaned") now undercounts by two promise-sites (goal:47, :57) and one severity-CLASS (a requirement).

**Affected surfaces:** goal.md:47, :57 · spec.md:83, :138 (REQ-003) · the SKILL :323-325/:331-333 · the pack:69 · the state schema's 3-key validation · the reducer's SEVERITY_KEYS/WEIGHTS · the merge's SEVERITY_RANK · the .codex failure_type · wave1-F006 (the SC-001 wording).

**Recommendation.** EITHER amend the four promise-sites to "P0 to P2 (the repo's severity meaning)" — a 4-word fix ×4, the cheapest convergence — OR (the wave1- alternative, now cheaper to do RIGHT) downgrade the SPEC-INTERNAL wording and UPGRADE ONE contract: add P3 to SEVERITY_WEIGHTS (weight 0.5), the state schema's validation, the reducer's keys, and the pack's verdict-mapping, and record the collapse rule ("a packet-P3 = a machinery-P2 with the intent noted") in state-jsonl.md where the validation lives. Do it before the parent's REQ-003 verification pass, so the requirement's wording and the verifying machinery agree.

# Traceability Checks

- **spec_code — PASS** (gate class: hard). Required: BOTH end-to-end traces + BOTH authority rulings. Executed: the promise-chain traced to 7 stages (the promise-comments → the advises → the format-check → the self-report → the provisional-recompute → the runner-preserve → the merge's hard consequence), each cited; the receipts-privilege measured (17+17-vs-0+0+0+0+1 + the lane's own receiptless-command-level record); the residue adjudicated comparative (9+6 loci, each citing a real relationship); the severity chain traced to the 4 promise-sites + the 11 machinery surfaces + the undocumented collapse + BOTH rulings (3-tier authoritative; the relay's truth = the merge). Boundary: the pack:69's "CI gate parser" NOT LOCATED (Dead End 1); the (1896)-PROVISIONAL→(751)-enforced handoff = acknowledged-not-repaired (the :1896 label IS the acknowledgment).
- **checklist_evidence — notApplicable** (gate class: hard). Unchanged: no checklist.md; the AC-001 scaffold; completion_pct 0; the predicate inactive. Assessed at synthesis.

Summary: required 2, executed 1, pass 1, partial 0, fail 0, blocked 0, notApplicable 1, gatingFailures 0.

# Assessment

**Verdict rationale.** CONDITIONAL: 0 active P0, 4 active P1 (F001, F007, F010, F014) — the lane proceeds to its iteration-5 stop; convergence telemetry (rolling 0.22, MAD-noisy, coverage 1.0) votes no-stop; mode off.

**Continuity — the adjudications:**
1. **The angle's two rulings, stated plainly.** (A) The containment promise: NO in-loop level is authoritative for TRUTH — the comments promise, the code advises, the verifier formats, the lane reports, the reducer computes-PROVISIONAL, the runner preserves; the ONLY enforcement = the PARENT's merge (:751, the strongest-restriction). The authoritative level = the reducer's compute, finalized by the merge. (B) The severity scale: the 3-TIER machinery is authoritative; the 4-tier = the promise-text's internal 4-symbol/3-meaning drift, nowRequirement-hardened.
2. **Wave1-F006 — STILL-TRUE, HARDENED, STALE-CITED** (F015): the +3 shift (:135→:138), the wording kept, the REQ added, the fix unapplied; the scope widened (4 sites; a requirement).
3. **The receipts-privilege — measured** (F014): codex-branch-only command-level receipts; the runner-level = universal; this lane = the receiptless-command-level precedent, RUNNING.
4. **The 3-lane latent-clean record**: wave1-glm (CONDITIONAL: P0 0/P1 2 ✓), wave1-deepseek (FAIL: P0 1 ✓), this lane (CONDITIONAL×3: P0 0/P1 >0 ✓) — the convention holds 5/5 published verdicts; the divergence = LATENT (the F014 one-typo scenario), not manifest.
5. **The +2 canonicality votes** (cli-cursor:263, cli-devin:294 — "symlink to the CANONICAL .claude/agents/<name>.md... One source of truth, so a mirror can never drift"): F010's vote 23:3 → **25:3** (27 assertions total, with SYNC.md's .opencode-voice; the "never drift" = true-by-mechanism for the 2 symlinked trees only — the OTHER FOUR can drift, nobody compares: the promise's SCOPE = the symlink half of the fleet).
6. **The .pi models note** (cli-pi:134): SIX providers, "Two of them... are not Pi builtins and exist only because `.pi/models.json` declares them" — the pi-harness's OWN model-attraction layer (below the .pi/agents layer) — telemetry for the F013-adjacent picture; NOT adjudicated (the .pi/models.json unread).

**Premise deltas this iteration owes the parent (SC-001):** "the promise diverges at three levels" → it diverges at SEVEN stages (or: the three NAMED levelsplus four the angle's wording didn't count); "copy-pasted codex residue in three non-codex branches" → the 9+6 = COMPARATIVE prose, the residue = the receipts-privilege (the promise-text without the non-codex enforcer); "every governing contract is three-tier" → confirmed and EXTENDED (11 surfaces, now including the merge's rank and the .codex failure_type); wave1-F006's 2 promise-sites → 4, one of them a REQUIREMENT.

# Ruled Out

1. **The 9+6 non-codex hub-mentions as copied mechanism (the spec's "residue" reading)** — ruled out: each of the 15 loci cites a REAL codex relationship (the sibling-roster lists, the provider-roster, the keyword-weights, the ~/.codex/state/lock contrast, the symlink/canonical sentences, the PID-kill discipline's family list). Evidence: the 15-loci sed.
2. **The runner as a verdict-enforcer** — ruled out: its acceptance = records+disk+cap+stopReason; the verdict's TRUTH appears nowhere in it; :1436 = another PROMISE (the 7th stage). Evidence: the acceptance contract + the 0-verifier census (iterations 1-2, re-confirmed).
3. **"The P3 is representable nowhere"** — ruled out: REQ-003's binding layer (the parent's records) = OUTSIDE this packet's contract-set; the P3 = representable THERE, orphaned HERE. Evidence: the :138 wording + the 11-surface 3-tier census.
4. **This lane's verdicts as possibly reducer-divergent** — ruled out: 3× CONDITIONAL, each with P0 0 ∧ P1 > 0 in its own record — the pack:69 mapping and the reducer:1847 ternary agree on every one. Evidence: the 3 state-rows + the 2 implementations.

# Dead Ends

1. **The pack:69's "CI gate parser"** — NAMED, NOT LOCATED: no repo component answered to "CI gate parser" in this iteration's reads (the pack's synthesis/CI = the referenced-not-found pair); the 2-authority question (F014) hinges on whether it IS the reducer. Angle-20 or the parent's.
2. **`.claude/SYNC.md`** — still unread (the F010 downgradeTrigger; the likeliest 25:3→1-voice reconciler). Carried: angle-20.
3. **The state-log-vs-registry divergence REPAIR** — the :1896 PROVISIONAL label ACKNOWLEDGES the (self-report|compute) gap; the repair-mechanism for a recorded-but-false self-report row = UNLOCATED (the reducer prints; nobody rewrites the log — append-only by covenant).
4. **The .pi/models.json provider layer** (cli-pi:134's SIX providers, 2 non-builtin) — the harness-level model-attraction beneath .pi/agents; unread this iteration (the F013-adjacent telemetry).
5. **The (:1896)-PROVISIONAL→(:751)-enforced handoff** — acknowledged-in-one-place remains TODO: the recommendation's "by name" record does not exist yet (nobody documents the provisional→enforced-merge handoff).

# SCOPE VIOLATIONS

None. Writes: `prompts/iteration-004.md`, `iterations/iteration-004.md`, `deltas/iter-004.jsonl`, `logs/iter-004-events.jsonl`, +2 appended state-log rows (append-only, the sanctioned lane-direct writer); the verifier run read-only. No reviewed artifact modified.

# Recommended Next Focus / Next Dimension

**Angle 20 (spec.md:107) — What wave one surfaced that angles 11 to 20 do not cover**, dimensions: maintainability (primary) + security (secondary). Declared work: read BOTH wave-one reports + registries, list every finding no angle 11-20 re-examines, examine those, and add what the whole-system read makes visible only now. THIS LANE'S PREPARED INVENTORY (from the phase_init orphan-analysis + these four iterations): the orphaned-finding set (wave1-glm F005, F014, F015, F016 + wave1-deepseek F005, F006, +F010-borderline) = the still-true re-verification; the whole-system finds ALREADY accumulating: (a) the 2-authority disease as the PACKET-WIDE pattern (F010's 23:3→25:3 canonicality, F014's pack:69-vs-reducer:1847, the receipts-privilege), (b) the angle-coverage gap class (the executor-parity/allowlist/scoring-vocabulary findings with no wave-two angle), (c) the wave1-F006 +3-stale-cite + the unapplied fix, (d) the .claude/SYNC.md + the CI-gate-parser = the two unread reconcilers. Also the recorded convention: the lane's 5th iteration = the deliberate close; the covenant's stabilization pair (4-5) completes here; the convergence telemetry's decay (1.00→0.44→0.31→0.13→?) = the honest corroboration.

Review verdict: CONDITIONAL
