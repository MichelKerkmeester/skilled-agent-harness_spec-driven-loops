# Dimension / Focus

**Angle 18 — Agent mirror dialects** (spec.md:105). Dimensions: **traceability (primary)**, maintainability (secondary). The rationale: a declaration that no mechanism consumes is a translation loss regardless of how faithfully it is written; this iteration maps every agent's declarations across the mirror fleet and names, per loss, the mechanism that would have had to consume it. Maintainability-secondary: the fleet's own maintenance burden (72+ declaration files in 6 dialects, 4 generators/symlinks, 2+ checkers) IS the maintainability surface — the angle's subject doubles as its dimension.

The declared deliverable: the 12-agent × 6-tree declaration matrix (TABLE 1) + the named translation losses (TABLE 2 + F010-F013). Prior coverage: wave1-glm's F009-F013 (five findings, spot-check depth, four trees: .opencode/.claude/.codex/.pi); this iteration extends the matrix to the 5th and 6th trees (.cursor, .devin — in NO wave-one finding) and completes the 12-agent × 6-Tree census. The 12 agents: ai-council, code, context, debug, deep-improvement, deep-research, deep-review, design, markdown, orchestrate, prompt-improver, review.

Census mechanics: heads/permission-blocks/diff -q/wc -c across the trees; the .codex column read at full depth (12 TOMLs; the review.toml end-to-end; the 12-file census output ran 6,396 lines — the .codex mirrors are FULL-body `developer_instructions`, not stubs).

# Files Reviewed

1. `.opencode/agents/` — 12 .md + README.txt (13 entries); full permission-block completions for ai-council, code, orchestrate (the 12-key + 10-key vocabularies); the temperature distribution (9×0.1, 3×0.2); the mode keys (10×subagent, 1×all, 1×primary)
2. `.claude/agents/` — 12 .md + README.txt (1,608B, no temperature); 12 fronts (name/description/tools TitleCase CSV; NO temperature/mode/permission keys); the 11×-".claude-canonical" + 1×-".opencode-canonical" (deep-review) Path-Convention split; the prose deny-half ("never runs shell commands", "NEVER performs nested delegation", "never edits files")
3. `.cursor/agents/` — 12 .md, NO README; 12 fronts (identical schema to .claude); diff -q ×3 (review, ai-council, deep-improvement): BYTE-IDENTICAL to .claude; the same 11+1 provenance split
4. `.codex/agents/` — 12 .toml read at FULL depth (~6,400 lines total): each = `# Agent:` + `# Converted from: .opencode/agents/X.md` + name/description + `sandbox_mode = "read-only"` (12/12) + `model = "gpt-5.5"` (12/12) + `model_reasoning_effort = "high"` (12/12) + `developer_instructions = '''<the full .opencode body, INCLUDING the Hook-Injected Advisor Context block and a .opencode-canonical Path Convention (sampled: review.toml)>'''`
5. `.pi/agents/` — 12 .md: GENERATED dialect (YAML-list tools: read/write/edit/bash/grep/find/ls — no glob; QUOTED name/description; NO permission/temperature keys); the `# Unmapped OpenCode permission keys: ...` comments in 11/12 (the exception: deep-review.md, mechanized — see F011); mtimes: ai-council.md Sep 5 09:58, dir Sep 15 06:49 — ALL PRE-DATE wave1's 10:15Z read
6. `.devin/agents/` — 12 DIRECTORIES × 1 symlink each (`AGENT.md -> ../../../.claude/agents/X.md`); ZERO own declaration bytes (review/ sampled: the resolution + the=target 25,511B)
7. `.pi/SYNC.md` — the maintenance contract: the GENERATOR declaration ("`.pi/agents/` and `.pi/prompts/` are **generated** trees... compiler output, never hand-edit them; edit the canonical source and re-run the generator"); the canonicality clause ("Canonical for agents is `.opencode/agents/` — the same upstream Codex uses, *not* the `.claude/agents/` fork that Cursor and Devin symlink"); the generator (`sync-agents-pi.cjs` + its `--check`; the recorded live drift mode: "a retired command's stale prompt"); the unmapped-key mapping note ("The generator maps OpenCode's `permission:` block onto Pi's built-in tool names, so a `.pi/agents/*.md` never byte-matches either parent tree"); the extensions-as-native-code doctrine (no hooks.json dialect)
8. `.opencode/commands/doctor/scripts/agent-roster-mirror-check.cjs` (head-55) — LINKED = cursor/devin ("symlink mirrors must resolve to the canonical file, not merely exist — a real file here would be a silent fork"); AUTHORED = opencode/codex/pi ("a different frontmatter dialect is expected, so only presence is checked, never content equality"); `CANONICAL_DIR = .claude/agents` + the comment "The Claude tree is canonical: it holds the full agent bodies that the Cursor and Devin mirrors symlink back to"
9. `.opencode/skills/cli-external-orchestration/cli-pi/SKILL.md:207` — the persona-injection contract: "Core Pi has no native persona surface on `pi -p`, so INLINE the persona block into the payload — the child cannot resolve agent paths by reference. A persona-less leaf runs as a generic assistant, dropping its tool-scope, verification gates, and output contract. ... Compose every dispatch as `{resolved agent persona + task prompt}` ... (AGENTS.md §9; never hardcode a runtime)"
10. The pi harness's own docs: 35 files inventoried (`compaction.md` … `windows.md`); 5 mention "agents" (json, development, skills, settings, security — none is an agents-parsing contract); the pi README: ZERO `.pi/agents` references
11. The governance reference census (the mirrors' 12 reference sites): 4×SYNC.md (.pi/.cursor/.claude/.devin), `.opencode/scripts/git-hooks/pre-commit`, `.opencode/commands/doctor/assets/doctor-runtime-mirrors.yaml`, the roster-check.cjs, `.opencode/skills/sk-doc/sk-create-frontmatter/assets/frontmatter-templates.md`, the 3 cli-devin docs — the mirrors are GOVERNED, not orphaned
12. The wave1-glm precedent: its report's F009-F013 findings (:69-73), the W4 clauses (:29, :40, :88, :107), the skill_agent deferral (:164)
13. The marker/greps: `sandbox_mode` 12/12, `model = ` 12/12, `temperature` = exactly 2 of 36 non-.opencode files (.claude/ai-council.md + .pi/ai-council.md — BOTH PROSE, not front keys), the wc -c size row (review.md: 25,726/.opencode, 25,511/.claude, 25,511/.cursor, 25,659/.codex, 25,599/.pi, 25,511/.devin→.claude)

# Scorecard

- Iteration 3 of 5. Focus: angle 18. Dimensions: traceability + maintainability. No gating failures.
- Findings: 4 new (P0 0, P1 1, P2 3) → cumulative 13 active (P0 0, P1 3, P2 10); 0 resolved; 0 refined.
- `newFindingsRatio` (count-based, precedent): (4 + 0.5×0) / (9 + 4 + 0) = 0.31.
- **Dimension coverage 4/4 = 1.0**: correctness (angles 16-18), traceability (16-18), security (17), maintainability (THIS angle, primary — the 72-file six-dialect maintenance topology). Every configured dimension now has at least one evidence-bearing pass.
- Convergence telemetry (mode: off — signals TELEMETRY ONLY; stop = iteration 5 of 5, `stopReason: maxIterationsReached`): rollingAvg (last 2) = (0.44 + 0.31)/2 = 0.375 ≥ 0.08 → the rolling signal votes NO-STOP; the 3-point MAD window now exists (1.00, 0.44, 0.31 → deviations 0.42/0.14/0.27, MAD ≈ 0.28 — noisy, no vote); coverage signal 4/4 = 1.0 (newly complete, stabilization just satisfied — the signal's stability clause needs the recorded consecutive passes, formally iterations 4-5 per the covenant); semanticNovelty: high (the 5th/6th trees + the SYNC/checker governance pair = entirely unread mechanism); compositeStopScore: not emitted (mode off). minStabilizationPasses (elevated to 2): stabilization evidence accumulating; the covenant's formal count = iterations 4-5.
- Duration: 2026-09-15T13:18:00Z → 13:38:00Z (1,200,000 ms).

# Findings by Severity

## P0

No P0 findings this iteration.

## P1

### F010 — The mirror fleet is a two-tiara topology with no bridge: .codex+.pi derive from .opencode while .cursor+.devin mirror .claude, no mechanism ever compares the two tiaras, they already differ by 215 bytes on review.md, and the fleet's own governance voices disagree 23:3 on WHICH tree is canonical (traceability, class-of-bug)

**Evidence.**
1. The two derivation chains, documented: `.pi/SYNC.md` — "Canonical for agents is `.opencode/agents/` — the same upstream Codex uses, *not* the `.claude/agents/` fork that Cursor and Devin symlink" (so: .opencode → .codex (converted) and .opencode → .pi (generated); .claude → .cursor + .devin (symlinked)); the doctor's `agent-roster-mirror-check.cjs` — `CANONICAL_DIR = .claude/agents`, "The Claude tree is canonical: it holds the full agent bodies that the Cursor and Devin mirrors symlink back to", LINKED = cursor/devin, AUTHORED = opencode/codex/pi.
2. The comparative enforcement: the checker verifies symlink RESOLUTION for cursor/devin ("must resolve to the canonical file, not merely exist — a real file here would be a silent fork") but for the three independently-authored/generated trees "only presence is checked, never content equality" — the checker's own words. The .pi tree's REAL content guard is a SECOND, unreconciled mechanism (`sync-agents-pi.cjs --check`, per SYNC.md), and NOTHING anywhere compares .opencode against .claude.
3. The measured divergence: `.opencode/agents/review.md` = 25,726B vs `.claude/agents/review.md` = 25,511B — the two tiaras' SAME-NAMED agent ALREADY differs by 215B, ungoverned by any mechanism. Meanwhile `.claude ≡ .cursor` BYTE-IDENTICAL (diff -q ×3: review, ai-council, deep-improvement — all silent) and .devin = .claude by symlink: the fork-side of the fleet is perfectly faithful; the .opencode-side faithfully serves ITS derivatives (.pi/review.md = 25,599B, .codex/review.toml = 25,659B — each within 130B of its tiara). The 215B sits precisely AT the uncompared seam.
4. The canonicality vote: 26 "canonical" assertions across the governance corpus — 23× ".claude-canonical" (11 .claude Path-Convention notes + 11 .cursor notes (byte-identical copies) + the checker's CANONICAL_DIR) vs 3× ".opencode-canonical" (the .claude/.cursor deep-review.md notes + .pi/SYNC.md; the .codex conversion notes point at .opencode too). TWO answers, 23:3, no recorded reconciliation — and the .pi/SYNC.md EXPLICITLY calls .claude "the fork", which the 22 copied notes (each declaring ITS OWN tree "the canonical runtime path") flatly contradict.

**Consequence.** Any edit landed on one tiara silently skips the other: the .opencode→.codex/.pi derivatives and the .claude→.cursor/.devin mirrors can drift apart indefinitely, and the difference is invisible to every published checker (the 215B proves it already happened at least once). The roster-check catches MISSING agents (its purpose: "nothing... notices when a newly-added agent reaches only some of them") but not DIVERGED ones.

**Affected surfaces:** the 72+ declaration files · `.pi/SYNC.md` · `agent-roster-mirror-check.cjs` · `sync-agents-pi.cjs --check` · the pre-commit hook · the doctor's mirror-asset.

**Claim-adjudication packet (required, P1).**
- **claim**: the six-tree fleet's canonicality is asserted by two unreconciled authorities (SYNC.md: .opencode; the roster-check: .claude), content equality is enforced ONLY where symlink resolution implies it, the .opencode↔.claude seam is compared by NO mechanism, and that seam is already diverged (215B, review.md).
- **evidenceRefs**: the eight evidence blocks above; the 3× diff -q; the six-entry wc -c size row; the 26-assertion, 23:3 vote.
- **counterevidenceSought**: searched for any .opencode↔.claude comparison mechanism (the 12 reference sites: SYNC×4, pre-commit, doctor yaml, roster-check, sk-doc template, 3× cli-devin — none compares the tiaras); searched the roster-check for a content-MAJORITY mode (its own comment denies it: "only presence... never content equality"); searched for a THIRD canonicality voice (the 26-assertion census: only two answers).
- **alternativeExplanation**: the 215B may be an INTENTIONAL dialect adjustment (the .claude fork legitimately lacks the permission-Map keys the .opencode fronts carry — the byte difference ~ the permission/tools schema difference), making the divergence BY-DESIGN and only the 23:3 voice-disagreement the defect; OR the SYNC.md's ".claude/agents/ fork" phrase may be STALE relative to the checker. Either reading leaves the seam uncompared; only the severity of the voice-disagreement moves.
- **finalSeverity**: P1. **confidence**: 0.8.
- **downgradeTrigger**: if `.claude/SYNC.md` (unread: Dead End 2) reconciles the two voices (e.g. "the .claude tree is the runtime-canonical FORK; .opencode is the generation-UPSTREAM, and the 215B is the sanctioned transformation delta") AND some mechanism or documented invariant bounds the .opencode↔.claude divergence, this drops to P2 documentation-ambiguity.

**Recommendation.** Name ONE canonical (amend the loser: the checker's comment, or SYNC.md's clause, or the 22 copied notes); add the missing bridge check (a .opencode↔.claude comparison, even weighting-1 equality of the derived/edited halves' semantic anchors) to the roster-check; record the sanctioned transformation deltas (the permission-Map↔tools-CSV, the .codex TOML wrapper, the .pi unmapped-comment register) so equality-with-exceptions is checkable.

## P2

### F011 — The permission deny-half translates FIVE ways, not three: enforced keys, prose (byte-identical twins), .pi's structured # Unmapped comments + omission, .codex's sandbox_mode mechanism, and .devin's by-reference — and the .pi # Unmapped mechanism is now PROVEN, correcting wave1's F010/F011 on the precise halves (correctness, matrix/evidence)

**Evidence.** The five treatments of "what this agent may NOT do": (1) `.opencode`: the `permission:` Map — runtime-ENFORCED — with a PER-AGENT vocabulary (12 keys: ai-council, code; 10: orchestrate — no chrome_devtools/external_directory; 8-9: context, prompt-improver, review — where review notably keeps bash:allow while denying write/edit); (2) `.claude` ≡ `.cursor` (byte-identical): PROSE ("never runs shell commands", "NEVER performs nested delegation", "Read-only... never edits files"); (3) `.pi`: the `# Unmapped OpenCode permission keys: ...` YAML comments (11/12) + implied-by-omission for denied keys; (4) `.codex`: `sandbox_mode = "read-only"` (12/12) — a MECHANISM, not prose; (5) `.devin`: by-reference (each `AGENT.md → .claude/agents/X.md` symlink). The spec's premise ("the permission deny half translates three ways") and wave1-F010's identical "three ways" BOTH undercount; and the .pi half-verdict needs its precise split: wave1-F010's ".pi silent" is TRUE for the DENIED half (denied keys vanish — no comment, no key) but the ALLOWED-BUT-UNMAPPABLE half is comment-RECORDED. The mechanism, proven 5/5: `# Unmapped` = {k ∈ .opencode permission : value = allow ∧ k ∉ pi's toolset} — ai-council: "webfetch, task, external_directory" (ALL THREE :allow in .opencode ✓); code: "patch" (:allow ✓); context: "external_directory" (:allow ✓); orchestrate: "task" ONLY (its webfetch:deny correctly absent ✓); deep-review: NO comment (its allows ⊆ pi's tools; its denies — webfetch, chrome_devtools — vanish ✓). BOTH exceptions mechanized. And the provenance recount wave1-F011 needed: the .pi # Unmapped comments + the .codex `# Converted from:` headers + .pi/SYNC.md = recorded provenance mechanisms the "one of four trees" verdict missed; the mtimes (ai-council.md Sep 5, dir Sep 15 06:49) PRE-DATE wave1's 10:15Z read, so the comments were there to miss — genuinely under-read, not a post-review addition. (Also: .cursor has NO README — wave1's "README in 2/4" = .opencode+.claude, confirmed.)

**Affected surfaces:** the 72+ files' permission/role layers · the .opencode 8-12-key sparse vocabulary · the .pi generator's unmapped-key register · the .codex sandbox mechanism · the .devin 12 symlinks.

**Recommendation.** Adopt ONE documented crosswalk (the .pi # Unmapped mechanism is 80% of one already — generalize it: every tree records, in ITS OWN dialect, which .opencode keys it maps, which it drops, and why); retire the prose-only deny-half (or generate it from the Map); add the .codex sandbox_mode ↔ .opencode deny-Map correspondence to the crosswalk; note the per-agent key-vocabulary variance (12/10/8-9) in whichever reference claims a uniform permission vocabulary.

### F012 — The .pi consumption contract resolves wave1's body-order question: .pi/agents = generated compiler output, the pi harness parses NOTHING, the dispatch INLINES the persona, and the 25-50KB persona rides EVERY pi dispatch (traceability, cross-consumer)

**Evidence.** Three walls close the question. (1) GENERATION: `.pi/SYNC.md` — the `.pi/agents/` trees are "generated... compiler output, never hand-edit them"; the generator `sync-agents-pi.cjs` transforms the canonical `.opencode/agents/` into "Pi's dialect" ("a .pi/agents/*.md never byte-matches either parent tree"); the recorded drift mode: "a retired command's stale prompt". (2) NO PARSER: the pi harness's own docs — 35 files, 5 mentioning "agents" (json, development, skills, settings, security — none an agents-parsing contract), the pi README: ZERO `.pi/agents` references; the dispatch contract's own words: "Core Pi has no native persona surface on `pi -p`". (3) CONSUMPTION-BY-INLINE: cli-pi SKILL:207 — "INLINE the persona block into the payload — the child cannot resolve agent paths by reference"; "Compose every dispatch as `{resolved agent persona + task prompt}`"; "A persona-less leaf runs as a generic assistant, dropping its tool-scope, verification gates, and output contract"; the persona source: "the ACTIVE runtime's agent directory (AGENTS.md §9; never hardcode a runtime)". THEREFORE: (a) wave1's .pi body-order question — the .pi bodies' section ORDER is unconstrained by any parser (nothing reads sections); the file = the wholesale-inlined persona, whose ONLY order-semantics is prompt-attention (the persona precedes the task); the actual order = the .opencode serialization, mechanically inherited — RESOLVED: no order contract exists to violate; (b) the prompt-bloat consequence is STRUCTURAL: the .pi/ai-council.md = 50,476B and .pi/review.md = 25,599B — inlined WHOLE per dispatch, ahead of the task prompt (noted, unmeasured — no dispatch-log evidence: telemetry); (c) the .pi↔.opencode size deltas (review: 25,599 vs 25,726 = 127B) = the sanctioned transformation delta, trackable by the generator's --check.

**Affected surfaces:** `.pi/agents/*.md` (the 12 generated bodies) · `sync-agents-pi.cjs` · the cli-pi dispatch contract (SKILL:207) · the AGENTS.md §9 persona-resolution rule · every pi dispatch of the 12 personas.

**Recommendation.** Record the resolution where wave1's question lives (the wave1 report's Deferred/Dead-End note): ".pi/agents = generated, unconsumed-by-parser, inlined-by-dispatch"; have the cli-pi persona-INLINE step cite the generator (so a hand-edited .pi file advertises itself as stale); measure one real dispatch's persona+task byte budget before treating the 50KB-inline as free.

### F013 — The sampling translation loss is GRADED, not binary, and ai-council alone crosses every grade: the .claude fork dropped the mechanism key but kept the prose sentence — "demoted, not lost" — while the .codex trio (model + reasoning-effort + sandbox) remains the fleet's only other mechanism-bearing column (correctness, premise-delta)

**Evidence.** The 6-tree sampling ladder, measured: (1) `.opencode` — the MECHANISM: `temperature:` fronts, 9×0.1 + 3×0.2 (debug, deep-improvement, design) — plus ai-council's body-PROSE (the sentence .claude inherited); (2) `.codex` — ITS OWN mechanism trio, 12/12: `model = "gpt-5.5"`, `model_reasoning_effort = "high"`, `sandbox_mode = "read-only"` — the effort knob is the .codex-native sampling; (3) `.pi` — fronts: NO temperature (the generator's dialect: name/description/tools/comment only), but 1/12 BODY-PROSE: ai-council (the .opencode prose, generator-inherited); (4) `.claude` — fronts: NO temperature; 1/12 PROSE: ai-council ("the .claude fork dropped the key but kept the sentence"); (5) `.cursor` — ≡ .claude BYTE-IDENTICAL (diff -q): the same 1/12 prose; (6) `.devin` — = .claude BY-REFERENCE. So the wave1-F009 premise ("sampling configuration is .opencode-only; three mirrors substitute either nothing or a different knob") extends precisely: the "different knob" = .codex's effort/sandbox trio (12/12, mechanism-grade); the .pi fronts ALSO substitute nothing (the generator DROPS the temperature key — a premise nuance: .pi's omission is GENERATOR-AUTHORED, not fork-authorial); and the graded residue: exactly ONE agent (ai-council) retains its sampling as PROSE in three trees + .codex's inherited instructions — the only agent whose sampling survives its own translation, as speech. Wave1-F013's model-attraction premise ("three trees silent, the fourth pins gpt-5.5") extends: the 5th/6th trees (cursor ≡ .claude; devin → .claude) are silent by inheritance — 5 trees mechanism-silent, 1 pinned 12/12. And the ROLE key (.opencode `mode:`: 10×subagent + 1×all + 1×primary) translates NOWHERE as a key — it survives as DESCRIPTION-prose ("LEAF, dispatched only by @orchestrate") in .claude/.cursor and vanishes into the .codex/.pi bodies: the sixth translation loss, recorded here for the crosswalk (F011's recommendation).

**Affected surfaces:** the 12×6 temperature/model/mode declaration cells · the .pi generator's key-drop list · the .claude fork's ai-council prose · wave1-F009/F013's premise wording at the SC-001 merge.

**Recommendation.** In the F011 crosswalk, add the sampling row: mechanism (which tree, which key) / prose-residue (which sentence, which trees) / dropped (the .pi generator's key-drop); decide whether the .codex effort-trio is the CROSS-RUNTIME sampling authority (then .opencode's temperature needs a documented mapping to it) or a .codex-local override (then the fleet's sampling story is "mechanism ×2, prose ×1, silence ×9" and the F011 table gains its grading column.

# Traceability Checks

- **spec_code — PASS** (gate class: hard). Required: "map every agent's declarations across the four runtimes and name each translation loss" + the five declared premise-claims. Executed: the 12×6 presence+mechanism matrix (TABLE 1 — 72/72 presence, the 5-6-tree mechanism/prose/silence lattice); the five named premise-claims each adjudicated against the FULL fleet (sampling: F013's graded ladder; deny-half: F011's five treatments vs "three ways"; tool lexicon: the four written dialects + the .pi register + the .devin by-reference; model-attraction: F013's 5-silent/1-pinned; leaf contract: the mode-key translation + wave1-F012's insider-confirmation below); the 5th/6th trees (cursor/devin) covered for the FIRST time in the packet. Boundary: locus+sampled (12 fronts/tree, 3× diff, 1 full .codex body + 11 counted), the .claude/SYNC.md unread (Dead End 2), the 215B divergence's hunk-composition unresolved.
- **checklist_evidence — notApplicable** (gate class: hard). Unchanged: no checklist.md; the AC-001 scaffold + completion_pct 0, the exemption predicate inactive. Assessed at synthesis.

Summary: required 2, executed 1, pass 1, partial 0, fail 0, blocked 0, notApplicable 1, gatingFailures 0.

# Assessment

**Verdict rationale.** CONDITIONAL: 0 active P0, 3 active P1 (F001, F007, F010) — the lane continues to its iteration-5 stop; convergence telemetry (rolling 0.375, coverage 1.0) votes no-stop and is mode-off anyway.

**Continuity — the wave1-F009/F010/F011/F012/F013 adjudication + the .pi question:**
1. **The .pi body-order question — RESOLVED** (F012): no parser; generated order; prompt-attention semantics; inlined wholesale.
2. **F009 (sampling) — CONFIRMED + GRADED** (F013): the .opencode mechanism, the .codex rival-mechanism, the .pi generator-drop, the ai-council prose residue, the 11/12 silence.
3. **F010 (deny-half) — CORRECTED-PRECISELY** (F011): five treatments, not three; the .pi = silent-on-DENIED + comment-on-ALLOWED-UNMAPPABLE (the mechanism proven 5/5); the comments predate wave1's read (mtimes) — genuinely missed.
4. **F011 (provenance 1-of-4) — UNDERCOUNT** (F011): the .pi # Unmapped register + the .codex Converted-from + the SYNC/doctor canonicality voices; the fleet is governed (12 reference sites), the governance is the finding.
5. **F012 (budget: 4 statements, 0 enforcers) — STILL-TRUE, INSIDER-CONFIRMED**: this lane's two mechanical-gate runs returned `detail: "…narrative + route-proof + delta"` (NO budgetProfile/edgeCases check), and the state schema's required-16 (state-jsonl.md, read at phase_init) carry no budget key either — the 4 statements (agent file:167, the :520 self-certification, the dispatch pack, the state schema) vs 0 enforcers, now attested FROM the enforcement side's own output. Wave1's finding stands unchanged; no new ID (the lane's F012 = the .pi consumption, the recorded lane-scoped namespace applies at the SC-001 merge).
6. **F013 (model-attraction) — CONFIRMED + EXTENDED** (F013): 5 mechanism-silent trees (the 5th/6th by inheritance), 1 pinned 12/12.

**Premise deltas this iteration owes the parent (SC-001):** "the permission deny half translates three ways" → FIVE; "map... across the four runtimes" → the fleet is SIX trees (cursor/devin in no wave-one finding); "sampling is OpenCode-only" → mechanism-true, prose-graded (ai-council); "model attraction... unowned in three" → five trees mechanism-silent.

# Ruled Out

1. **The mirrors as ungoverned dead weight** — ruled out: 12 reference sites (4×SYNC.md, the pre-commit hook, the doctor's mirror-asset + roster-check, the sk-doc template, the 3 cli-devin docs) + two generation/verification mechanisms (sync-agents-pi.cjs --check; the roster-check's LINKED-resolution). Evidence: the reference census + the checker head.
2. **The .cursor tree as .claude-PLUS (the hook-note as a .cursor-only layer)** — ruled out: diff -q ×3 = byte-identical; the hook-note appears in .claude too (my 14-vs-12-line head asymmetry created the illusion; the .codex bodies carry it as well — it is fleet-wideBOILERPLATE, not a .cursor dialect feature). Evidence: the 3× diffs + the .codex review.toml's block.
3. **The .pi # Unmapped comments as post-wave1 additions (the "wave1 couldn't have seen them" out)** — ruled out: ai-council.md mtime Sep 5 09:58, the .pi/agents dir Sep 15 06:49 — both PRE-DATE wave1's 10:15Z-11:30Z window. The miss was under-reading, not timing. Evidence: the mtimes.
4. **The .claude/README.txt as the temperature-documentation home (the F009 "documented-known" rescue)** — ruled out: .claude/agents/README.txt (1,608B) mentions NO temperature; the 2-tree temperature census = exactly 2 hits, both the ai-council PROSE. Evidence: the rg.

# Dead Ends

1. **The 215B .opencode↔.claude divergence's composition** — presence + size + qualitative diff only (diff -q, no -u); WHICH hunks (the permission-Map↔tools-CSV schema, the convention lines, or deeper prose) is unresolved; a 15-line diff -u | head settles it. Angle-20 telemetry.
2. **`.claude/SYNC.md`** — EXISTS (the 4×SYNC.md set), NOT read. It is the likeliest home of the tiara reconciliation (F010's downgradeTrigger); the 23:3 vote may be 0:26 inside it. Next-iteration or angle-20.
3. **The .codex convention-line target, 11 of 12** — review.toml sampled = ".opencode-canonical"; the other 11 counted (1 "canonical runtime path reference" each) but their TARGET path unread.
4. **Whether `sync-agents-pi.cjs --check` actually runs** in the pre-commit hook (the hook references the mirrors; its precise check-list unread) — the .pi content-guard's enforcement TIMING (commit-time vs doctor-time vs never) is unadjudicated.
5. **The 50KB-inline prompt-bloat** — structural (F012) but unmeasured: no dispatch-log evidence of the persona+task byte budgets; telemetry.

# SCOPE VIOLATIONS

None. Writes: `prompts/iteration-003.md`, `iterations/iteration-003.md`, `deltas/iter-003.jsonl`, `logs/iter-003-events.jsonl`, +2 appended state-log rows (append-only, the sanctioned lane-direct writer); the verifier run read-only. No reviewed artifact modified; the .codex full-read (6,396-line census output) hit the harness's 50KB display cap and spilled to a /var/folders/.../pi-bash-*.log tempfile — outside the lineage but harness-created, not lineage-authored.

# Recommended Next Focus / Next Dimension

**Angle 19 (spec.md:106) — the containment promise chain and the severity scale**, dimensions: correctness (primary) + traceability. Declared work: trace BOTH chains end to end — (a) the containment promise: which level is authoritative where "the promise diverges at three levels, comments promise fail-closed, inline code advises, the runner preserves, with copy-pasted codex residue in three non-codex branches"; (b) the severity scale: this packet's spec promises FOUR tiers where every governing contract (review-core.md 1.5.0.11's P0/P1/P2; the state-jsonl validation; the convergence weights; the reducer's SEVERITY_KEYS) is THREE-tier — supply the documented P3→P2 collapse rule and name the authoritative level. Hand-offs: this lane's iterations 1-2 recorded the four-tier-intent note (any packet-P3 rating collapses to record-P2, the discrepancy reportable); the waiver question (whether the runtime's 9-gate set ever gates on P3) is angle-19's to adjudicate; wave1's F006 (severity-vocabulary alignment, W7) = the precedent finding to extend.

Review verdict: CONDITIONAL
