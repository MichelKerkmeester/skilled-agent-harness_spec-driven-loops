# Deep-Review Report — lane wave2-glm (angles 16-20)

Loop: deep-review · Mode: review · Session: `fanout-wave2-glm-1789475514883-p58bmd` · Model: glm-5.3-flash (reasoningEffort max) on cli-pi · Executed: 2026-09-15, 5 iterations, 12:50-14:06Z (final iteration 13:52-14:06Z) · Synthesis: 2026-09-15T14:08:00Z · Stop: maxIterationsReached (5/5, `stopPolicy: max-iterations`, `convergenceMode: off`)

---

## 1. Executive Summary

- **Overall verdict: CONDITIONAL** — 5 active P1 (F001, F007, F010, F014, F016), no active P0.
- **hasAdvisories: true** — 13 active P2 advisories.
- **Active counts: P0 0 · P1 5 · P2 13** (18 active, 0 resolved; 0 corruption warnings; dimension coverage 4/4, closed at it5; the five narrative verdict lines all CONDITIONAL and every one derivable from its own record's severity counts — the two authorities agree 5/5, see F016).
- **Review scope**: the second wave's second lane — angles 16-20 of the target packet `specs/system-deep-loop/049-deep-loop-alignment-review/001-angle-driven-review`, one angle per iteration: (16) the confirm-vs-auto variant-drift census, both variant pairs, step by step, every divergence named with its consequence; (17) the ledger-stem producers and the cutover cliff, every stem against its producers and every producer against its stems, both modes, both directions; (18) the agent-mirror declaration dialects, extended to the FULL six-tree × 12-agent × 5-attribute matrix; (19) the containment promise chain end-to-end and the severity scale, with the ruling on which level is authoritative; (20) what wave one surfaced that no wave-two angle covers — the seven-orphan second-look plus the whole-system read. Every finding cites file:line; the lane's finding IDs are LINEAGE-SCOPED (namespace by sessionId at the SC-001 merge — the twin lane `wave2-deepseek` (angles 11-15) runs concurrently and the IDs collide until namespaced; the wave-one lanes' F001-F0nn likewise).

## 2. Planning Trigger

`/speckit:plan` **IS required** — 5 active P1 and 13 P2 advisories demand remediation phases under the parent. Planning packet:

```json
{
  "triggered": true,
  "verdict": "CONDITIONAL",
  "hasAdvisories": true,
  "activeFindings": 18,
  "remediationWorkstreams": [
    "WS-A (P1) the two-authority convergence: F016 — ONE workstream, FOUR rows (the 25:3 canonicality, the pack:69-vs-reducer:1847 verdict, the 4-vs-11-surface severity, the receipts-privilege), the uniform 3-step repair (name ONE authority, record the sanctioned-delta, the bridge-check); first row = read .claude/SYNC.md, last = locate the CI-parser",
    "WS-B (P1) the containment relay closing: F014 — the verifier's 5-line verdict-consistency check at the FORMAT collar, the pack:69 downstream clause located-or-reworded, the receipts parity (extend runAuditedExecutorCommand to the non-codex branches or strike :1556), the PROVISIONAL-to-ENFORCED handoff documented in ONE place by name",
    "WS-C (P1) the line-1 config quartet: F001 — resolve the never-bound {AUTO_SESSION_ID} token (declared twice, bound nowhere, the literal-fallback guard dead-on-arrival) and the research-confirm 9-key/0-lineage literal; reconciles with wave1's F001 (their confirm-config repair) at the SC-001",
    "WS-D (P1) the ledger authority: F007 (with F008, F006) — document the emergent flip (gateway watermark + frames pair = the WHEN), consolidate-or-spelt the triple-split enforcement, bound the registration-bounded projection ceiling before any cutover",
    "WS-E (P2) the mirror-fleet governance: F010-F013 — the two-tiara reconciliation (SYNC.md read, the 215B seam via diff -u), the five-treatment deny-half crosswalk, the mode-key sixth loss, the sampling-graded-loss decision, the model-attraction ownership",
    "WS-F (P2) the severity vocabulary: F015 — the 4-word fix x4 (the promise-sites to P0-P2) OR the P3 machinery (SEVERITY_WEIGHTS 0.5, the schema, the pack verdict-mapping) + the collapse rule recorded in state-jsonl.md; BEFORE the parent's REQ-003 verification pass; continues wave1's F006/W7",
    "WS-G (P2) the orphan carry and the trust graduation: F017 + F018 — the 7 second-look verdicts into the merge ADDITIVELY, the REQ-003 pass = the 7 rows foremost, the attestation-grade-per-executor table (the merge inputs it), the 3 cheap tests (allowlist validation, the parity fixture, the self-report consistency)",
    "WS-H (P2) the variant-drift residuals: F002-F005 — the 18 omitted steps restored-or-annotated, the gateway wiring matrix honestly annotated (the packs' mandate vs the 12/2/2/1 wiring), the resource-map provenance chain (the doubly-stale :454 pointer incl.), the unstaged invariant qualified"
  ],
  "specSeed": "under specs/system-deep-loop/049-deep-loop-alignment-review/: a remediation phase binding these 18 findings per REQ-003 (each verified against the tree — all 18 carry file:line — bound to a parent phase or recorded refuted-with-reason). Wave-two REQ-002 duty: DISCHARGED (the angles 16-20 were rewritten from the wave-one findings before this lane launched — the recorded provenance: the 14:31 spec rewrite, which hardened wave1-F006 into REQ-003 while leaving its fix unapplied, see F015). The wave-three duty, if any, launches WITH its seeds: the 6+1 gap-classes of F017 (provenance-pointer drift; approval-compression claims; undated allowlists; unmechanized parities; registry-minus-vocabulary sets; hermes lane-unique refs; + the 0.1.* version-skew) — the PROVEN mechanism: this lane's angles 16-20 = all born of wave1's deferrals",
  "planSeed": [
    "1. F016: open the two-authority convergence workstream (4 rows, the 3-step shape); first action = the .claude/SYNC.md read (the cheapest possible reconciler); last = the CI-parser locate (the verdict-stack's linchpin)",
    "2. F014: the verifier consistency check + the pack:69 clause + the receipts parity decision + the ONE-PLACE PROVISIONAL->enforced handoff note",
    "3. F001: the {AUTO_SESSION_ID} binding decision (bind it in-YAML or document the external-shell substitution) + the research-confirm line-1 literal (add lineage/mode/stopPolicy/executor or annotate the thin-interactive-tier contract)",
    "4. F007/F008/F006: the cutover readiness — the migration emitter, the projection registration decision (the 56-of-61 authorless stems), the state reference docs' vocabulary prose, the flip documented WHERE it happens (shadow-projection-store.ts)",
    "5. F010-F013: the fleet governance — ONE canonicality decision (the two-tiara vote 25:3 -> ONE), the crosswalk, the mode-key/mode-prose decision, the sampling decision, the 215B bridge (diff -u) and the sanctioned-delta ledger",
    "6. F015: the severity amendment (4 words x4 OR the P3 machinery) BEFORE the REQ-003 pass so the requirement's wording and the verifying machinery agree",
    "7. F017/F018: the SC-001 mechanics — the 7 second-look verdicts merged ADDITIVELY, the attribution convention recorded (this lane = the corroborating second-look where the concurrent sister scooped), the trust-graduation table + the 3 cheap tests mechanizing the REQ-003's own duty"
  ],
  "findingClasses": ["line-1-config-record-shape-quartet", "confirm-variant-step-omissions", "gateway-mandate-wiring-matrix", "fanout-resource-map-posture-asymmetry", "git-policy-variant-conditional-resolved", "registry-authorlessness-both-modes", "ledger-authority-emergent-triple-split", "projection-ceiling-arms-bounded", "carried-question-closure-triptych", "two-tiara-topology-no-bridge", "deny-half-five-treatments-unmapped-mechanism-proven", "pi-generated-unparsed-inlined-persona-order-resolved", "sampling-graded-loss-ai-council-prose-residue", "seven-stage-containment-relay-two-verdict-authorities", "severity-four-promise-sites-vs-three-tier-machinery", "two-authority-whole-system-governance-disease", "orphan-coverage-gap-design-intake", "attestation-grades-undocumented-merge-trust-equal"],
  "affectedSurfacesSeed": [
    ".opencode/commands/deep/assets/ (all four deep-command variant YAMLs) + .opencode/commands/deep/ (both command markdowns)",
    ".opencode/skills/system-deep-loop/runtime/ (append-mode-event.cjs, reduce-state.cjs, verify-iteration.cjs, fanout-run.cjs, fanout-merge.cjs, the ledger schemas, legacy-projections/, deep-loop/executor-config.ts) + the state reference docs (deep-review/references/state/)",
    "the six-tree agent mirror fleet (.opencode/.claude/.codex/.cursor/.pi/.devin/agents) + its governance (the four SYNC.md files, the doctor's agent-roster-mirror-check.cjs, the sync-agents-pi.cjs generator, .pi/models.json)",
    "the prompt pack template (deep-review/assets/prompt-pack-iteration.md.tmpl) + the deep-review SKILL.md severity/verdict tables",
    "specs/system-deep-loop/049-deep-loop-alignment-review/001-angle-driven-review/ (spec.md:83/:103-107/:138, goal.md:47/:57)"
  ],
  "fixCompletenessRequired": false
}
```

## 3. Active Finding Registry

Unified register of all 18 active findings. The three this-iteration findings carry the full 12-field treatment; the 15 prior findings are carried in compact recapitulation form — their authoritative 11-field records (evidence, recommendation, disposition, findingClass, scopeProof, affectedSurfaceHints) live verbatim in the iteration records (state-log lines 2, 4, 6, 8, 10) and their deltas; nothing here infers beyond them.

Note on the reducer-owned registry: `deep-review-findings-registry.json` lists **34 open entries** — the 18 F-ids PLUS 16 reducer-synthesized `SUMMARY-P1/P2-*` aggregation mirrors (its own strategy anchors record the same 0/9/25 view). The SAME reducer binary (mtime 2026-09-05) rendered the precedent lane's 20-registry clean, so the difference is input-shape behavior, recorded as deferral #7 — not corruption (exit 0, 0 corruption warnings, 0 field warnings). The 18 F-ids below are the authoritative register.

- **F016 (P1, two-authority-whole-system-governance-disease)** — *The two-authority pattern is the packet's whole-system governance disease: FOUR measured instances, ZERO reconciled, ONE enforced consequence, and the supervision model = trust + post-hoc-merge, uniformly.* [SOURCE: this lane's own F010 (the 25:3 canonicality vote, the 215B uncompared seam), F014 (the pack:69 self-report vs the reducer:1847-1852/:1896 PROVISIONAL compute, the runner preserves, the merge:751-823 = the ONE hard consequence), F015 (the 4 promise-sites vs the 11-surface 3-tier machinery), and the receipts-privilege (17+17 codex-branch-only vs the native lanes' runner-envelope)]. Dimension: maintainability. Evidence: in every governed domain the packet ships (the agent fleet, the review verdict, the severity vocabulary, the dispatch receipts): TWO authorities pronounce, ZERO mechanism reconciles, the divergence = latent (held by reviewer discipline: 5/5 published verdicts convention-clean across THREE lanes, 34-days-of-allowlist, 2x-manual-parity, the 215B-frozen seam), and the ONE enforced consequence (the parent's strongest-restriction) adjudicates the last link only. Impact: the packet's correctness = its reviewers' honesty + the parent's single P0-filter, nothing between; every future divergence is detected post-hoc by humans, or never. Fix: the uniform 3-step repair, 4 rows, ONE workstream (see WS-A). Disposition: finding (active); the full 7-field adjudication packet (the 5th-single-authority search = the state-log's 1-mechanical-JSON-parse exception; the intentional-REDUNDANCY alternative, compatible, the cost unstated; the downgradeTrigger: any-1-of-4 reconciled anywhere → P2) — iterations/iteration-005.md. scopeProof: the 4 instances = this lane's own findings re-cited with their loci; the 34d + 215B = the proof both ways; the INSIDER: this lane = the 4th instance RUNNING (receiptless 3x, computed-derivable 5x). affectedSurfaceHints: the fleet's canonicality; the verdict stack; the severity vocabulary; the receipts-privilege; the SC-001 merge; the parent's NEW workstream.

- **F017 (P2, orphan-coverage-gap-design-intake)** — *Seven wave-one findings have NO wave-two angle by design; 7/7 STILL-TRUE on second look; the REQ-003's bind-or-refute = their only remaining disposition mechanism; the merge = the last chance; the NEXT wave's angle-design inherits the gap-classes as seeds.* [SOURCE: the seven TODAY-loci — w1-glm F005 (the :454 pointer, doubly-stale: the target now renders the verify-cross-references bullet), F014 (the 3→2 approval compression, REFINED: documented-AT-BIRTH in the :551-553 JSDoc, confidence 0.65→0.85), F015 (the dated-allowlist rot, ACCRUED: 30→34 days MEASURED, protocol still absent), F016 (parity, RECOUNTED: the manual count 1→2 — this lane's 72/72+diff census = the second, fixture still absent), w1-ds F005/F010 (verbatim: graph-metadata:113/:124 + the 5-term Keywords block; leaf-manifest:68, ROUTER at ZERO) and F006 (the :58 membership coda READ: STILL-TRUE, the 2 aliases unscored)]. Dimension: maintainability. Evidence: the 10 wave-two angles = the chosen centers of mass; the 7 orphans = the BETWEEN (a provenance comment, a permission compression, an allowlist rot, a parity fixture, 2 hub vocabularies, 1 hermes-leaf reference = NO angle's center) — this iteration = the FIRST examination any of them received; the disposition machine = the REQ-003 (spec:138) bind-or-REFUTE, and the SC-001 merge = the last chance. Impact: unexamined-until-5/5 findings enter the merge as active-with-a-second-look; the parent's CHOICE = bind or refute, no third path; the 6+1 gap-classes = the NEXT wave's angle-design input (the proven mechanism: this lane's 16-20 = all born of wave1's deferrals). Fix: at the SC-001 — (a) merge the 7 verdicts ADDITIVELY as second-look evidence, (b) the REQ-003 pass = the 7 rows FOREMOST, (c) the next wave's spec = the 6+1 seed-classes. Disposition: finding (active); the sister-parallel uncertainty (the 3 w1-ds orphans = adjacent to the concurrent sister's 11/13/15) recorded: the attribution convention = this lane's = the corroborating second look. scopeProof: 7/7 read at their TODAY-loci; the NO-ANGLE-COVERS = the 5x5 adjudication; the FIRST-EXAMINATION = the registries' own untouched-active dispositions. affectedSurfaceHints: the 7 orphan-registry entries; the SC-001; the REQ-003 pass; the next wave's spec; the sister's angles 11-15 (coordinate-only).

- **F018 (P2, attestation-grades-undocumented-merge-trust-equal)** — *The attestation asymmetry: the merge trusts BOTH dispatch-grades equally while the trust-model is executor-DEPENDENT and undocumented; the self-attestation stack = THREE unaudited layers, and the 34-day-allowlist-rot = its proof-the-pattern-bites.* [SOURCE: the AUTO YAMLs' codex-branch receipt machinery (:1554-1558) = the DOUBLE-attested grade (receipts + recursion-guard + env-allowlist + the --sandbox OS-confinement forwarded, executor-config.ts:136) vs the native-pi SINGLE grade (the child's rows + the runner's LANE-LEVEL envelope: the invocation-metadata + the audit/effect-ledger frames — this lane = the 3x-receiptless-AT-COMMAND-LEVEL RUNNING precedent) vs the wave-one lanes' grades = UNDOCUMENTED; fanout-merge.cjs:751-823 = the strongest-restriction on the FINDINGS' severity, the attestation grade UNWEIGHTED]. Dimension: security. Evidence: the self-attestation stack the merge trusts = THREE unaudited layers — (a) the child's own finding rows (the :1847-recompute = the ARITHMETIC over the CHILD'S CLAIMS, the claims unaudited), (b) the undated validation (the :312 self-annotation + the :307-331 dated comments = the last live check 2026-08-12 = 34 days, the accrual MEASURED), (c) the twice-manual parity (the fixture = the absent). Impact: the non-repudiation = graded (the codex = un-repudiable at the command level; the pi = repudiable); the rot = the same disease, accrued on schedule, unwatched; the 4-lane wave's output = trusted by the F016 pattern. Fix: (a) the TRUST-GRADUATION — document the attestation grade PER EXECUTOR, one table, the merge inputs it (the weight = the tier, not the ignore); (b) the 3 CHEAP TESTS = the unaudit-half: the allowlist validation, the 1 parity fixture, the state-row findings-summary consistency check — the SAME SHAPE as the REQ-003's verification = the parent's already-owed duty, MECHANIZED. Disposition: finding (active). scopeProof: the grades = the 2 implementations' diff + the :136-137 credit-map; the 3 layers = this lane's own artifacts (the insider); the 4-lane identities = the negative census. affectedSurfaceHints: the AUTO codex-branches; the runner's lane-level ledgers; the invocation-metadata; the merge's trust algorithm; the allowlist + its annotation; the it4-F016 fixture absence; the 4-lane executor identities.

- F001 (P1, line-1-config-record-shape-quartet) — *The four variant line-1 config literals are four different record shapes; only review-auto's can agree with its config from line 1.* [.opencode/commands/deep/assets/: review-auto:466-467 = the 16-key full-block; research-auto:368 = 12 keys; research-confirm:369 = 9 keys, no lineage/mode/stopPolicy/executor; review-confirm = the precedent's 412/:422 divergence]. Dimension: correctness. Impact: the "config and state_log agree from line 1" invariant = review-auto's declaration, mechanically unbacked in the other three; all four detached lanes' actual line-1 records carry topic+executor that NO workflow literal emits. Fix: WS-C. Disposition: active. (The recorded downgradeTrigger RESOLVED at iteration 2: the {AUTO_SESSION_ID} token = declared twice, bound NOWHERE — F009; the finding STANDS at P1.)
- F002 (P2, confirm-variant-step-omissions) — *research-confirm omits 18 steps of its own auto (55 of 73), functional ones included, while both research variants render the same prompt pack.* [detect_resource_map, init_complete, marker_scan, compose_save_payload, the spec-doc validations, the telemetry-heartbeat trio, ...+18]. Dimension: correctness. Impact: functional gaps (state summary, marker scan, init-complete, the resource-map cluster) — the research-pair analogue of the review pair's omissions. Fix: restore-or-annotate. Disposition: active. (Workstream: WS-H.)
- F003 (P2, gateway-mandate-wiring-matrix) — *Gateway mandate vs wiring: all four packs mandate gateway-only state writes, no variant wires them all; the research loop's salvation is the research-only legacy branch.* [the wiring: review-auto 12, review-confirm 2, research-auto 2, research-confirm 1; the upcast = research-gated, append-mode-event.cjs:398-408, the bare-review-throw :445]. Dimension: correctness. Impact: the confirm variants' in-loop records = off-contract; the review-mode's = mechanically IMPOSSIBLE through the gateway (the bare-iteration throw) — the recorded five-clause direct-writer justification. Fix: WS-H (the wiring honesty) + WS-D (the cutover). Disposition: active.
- F004 (P2, fanout-resource-map-posture-asymmetry) — *The in-lineage fanout resource-map emission exists only in the research variant's contract.* [research-confirm:1342-1360 (the --fanout-resource-map-only + the in-place-read guarantee); the review pair = the FULL reducer (review-auto:2176 with the artifact arg, review-confirm:1546 without)]. Dimension: correctness. Impact: a review-side fanout lane either rewrites its own registry/strategy/dashboard or skips emission; the precedent lane's omission = its recorded deviation. Fix: the review variant's compact emission or the documented posture. Disposition: active. (Workstream: WS-H.)
- F005 (P2, git-policy-variant-conditional-resolved) — *The unstaged invariant is review-auto-only; the other three variants stage the artifact dir by a recorded contract fix — closes the precedent's 0.6-confidence git-policy question.* [review-auto:2453 the sole unstaged step; the other three stage it, failure non-fatal, restorable]. Dimension: correctness. Impact: the variants' end-states differ by recorded design, not drift; the (028) note = the staging trio's contract. Fix: qualify the invariant's scope. Disposition: active. (Workstream: WS-H.)
- F006 (P2, registry-authorlessness-both-modes) — *The 61-spelling registered ledger vocabulary is 92% authorless: 27 of 31 review-canonical stems AND 28-or-29 of 29 research spellings have no mechanical producer, the reference prose never names the vocabulary, and the unregistered-variant framing needs its clarification.* [the 8-surface producer census: 5 spoken (claim_adjudication 2, migration 2, recovery_baseline 1, iteration_error 1, run_now_restored 1); the dotted-stem vocabulary = TypeScript-only; the 4 state reference docs = ZERO dotted-stem mentions]. Dimension: correctness. Impact: the operative dialect never reaches the ledger; the registered vocabulary = enforced nowhere. Fix: WS-D. Disposition: active. (The it2 refinement superseding wave1-F020's 31-denominator: the claim_adjudication:452 = the LEGACY-ALIAS key, 0.8 confidence, the reviewed-packet premise's "28 of 31" = the review side only.)
- F007 (P1, ledger-authority-emergent-triple-split) — *The ledger-authority flip is emergent, its write side is the gateway's shadow projection store, and enforcement is triple-split: receipt-gated not-enforced, a 1/0/1/0 invocation matrix, and a runner acceptance that never calls the verifier — the containment story and the acceptance story disagree.* [shadow-projection-store.ts = the WRITE side (wave1's dead end, located); the flip = watermark+frames, no ceremony, the frames = the lineage-PARENT's shared namespace; the 1/0/1/0 verifier invocation; the runner = zero verifier references; the measured: this lane's legacy-direct records gated TODAY, ok:true exit 0, the 2b not-enforced]. Dimension: correctness. Impact: a lane can pass the acceptance contract while failing the default-ON gate the YAML's own step would run; the WHEN = bounded, the WHO = split, the WHAT-IF = the projection ceiling (F008). Fix: WS-D. Disposition: active; confidence 0.8; the full claim-adjudication packet (counterevidence: the not-enforced path MEASURED, not assumed; the alternative: the deliberate-soft-launch reading, compatible; the downgradeTrigger: the enforcement-consolidation-or-justification prose) — iterations/iteration-002.md.
- F008 (P2, projection-ceiling-arms-bounded) — *The sanctioned future's state-log richness has a registration-bounded ceiling: the projection consumes exactly the registered stems (whose spoken half is 5 of 61), EMIT_ITERATION_ROWS is currently true, and the anticipated suppression is a one-constant flip.* [deep-review-projections-contract.ts:283-294, :450-464, :288; the arms = buildEvidenceRecord/buildAdjudicationRecord/finding_lineage_recorded; the rich review iteration record = NEITHER spoken NOR arm-accepted]. Dimension: correctness. Impact: at cutover, the projection = the registration-bounded view — the attribution collapse wave1-F020 documented = the DESIGNED consequence of THIS constant, reversible. Fix: the registration decision BEFORE the flip (which of the 56 authorless stems earn producers, or the suppression flipped consciously). Disposition: active. (Workstream: WS-D.)
- F009 (P2, carried-question-closure-triptych) — *Three carried questions closed: the {AUTO_SESSION_ID} token is declared twice and bound nowhere (research-auto's literal-fallback guard is dead-on-arrival for token-populated configs); research-auto:2474's second git-add is reference-only appendix; F001's downgradeTrigger resolves WITHOUT demotion.* [research-confirm:358 the lineage.sessionId populate vs :369 the 0-lineage record; research-auto:332/:477; the appendix note "intentionally excluded from workflow.steps"]. Dimension: correctness. Impact: three open threads of wave one + this lane's iteration-1 = closed with the evidence; the F001 = STANDS. Fix: none (the closures = the finding-record's duty). Disposition: active (as the closure record; the substantive defects = F001's).
- F010 (P1, two-tiara-topology-no-bridge) — *The mirror fleet is a two-tiara topology with no bridge: .codex+.pi derive from .opencode while .cursor+.devin mirror .claude, no mechanism ever compares the two tiaras, they already differ by 215 bytes on review.md, and the fleet's own governance votes 25:3.* [.pi/SYNC.md = the .opencode-canonical; the doctor's agent-roster-mirror-check.cjs = CANONICAL_DIR .claude, the symlink-resolution checks for cursor/devin, the PRESENCE-ONLY for opencode/codex/pi; the 11+11 copied Path-Convention notes + 2 deep-review exceptions + the .codex conversions = the 25:3]. Dimension: traceability. Impact: the uncompared seam = the divergence'sProof-it-happens; a new agent reaching only some trees = silently missing. Fix: WS-E (the reconciliation = F016's first repair). Disposition: active; confidence 0.8; the downgradeTrigger: an unread .claude/SYNC.md reconciliation or a documented divergence-invariant → P2. (packet: iterations/iteration-003.md)
- F011 (P2, deny-half-five-treatments-unmapped-mechanism-proven) — *The permission deny-half translates FIVE ways, not three: enforced keys, prose (byte-identical twins), the .pi # Unmapped comments + omission, the .codex sandbox_mode mechanism, and .devin by-reference — the .pi # Unmapped mechanism PROVEN, correcting wave1-F010's three-treatment census.* [the proven 5/5: the comment lists exactly the ALLOWED keys with no Pi equivalent, the DENIED vanish by omission, deep-review the sole 12th with no comment (its allowed keys = all Pi-mappable); + the .opencode mode-key = the SIXTH translation loss]. Dimension: correctness. Impact: the deny-half's enforcement = per-runtime, five treatments, no crosswalk; wave1's "1 of 4 trees" provenance = an undercount (the .pi register + the .codex headers + the SYNC.md). Fix: the crosswalk (WS-E). Disposition: active.
- F012 (P2, pi-generated-unparsed-inlined-persona-order-resolved) — *The .pi consumption contract resolves wave1's body-order question: .pi/agents = generated compiler output, the pi harness parses NOTHING, the dispatch INLINES the persona, and the 25-50KB persona rides every pi dispatch.* [sync-agents-pi.cjs = the generator, --check = the drift guard; the pi docs = 5-mentions-no-contract; cli-pi/SKILL.md:207 = the inlining mandate; the 127B delta = the sanctioned transformation]. Dimension: security. Impact: the body order = inherited, not contracted; the inline = the 50KB-ai-council prompt-bloat, structural, unmeasured-telemetry. Fix: record the inheritance + the telemetry decision (WS-E). Disposition: active.
- F013 (P2, sampling-graded-loss-ai-council-prose-residue) — *The sampling translation loss is GRADED, not binary, and ai-council alone crosses every grade: the .claude fork dropped the mechanism key but kept the prose sentence — demoted, not lost — while the .codex trio (model + reasoning-effort + sandbox) remains the only mechanism, and 11 of 12 agents are silent.* [the mechanism: .opencode 9x0.1/3x0.2; the .codex 12/12 trio; the .pi = generator-omitted; the ai-council 2-file prose inheritance; the .devin = the .claude reference]. Dimension: correctness. Impact: the sampling semantics = mechanism x2, prose x1, silence 11-of-12 — extending wave1-F013 to the five-mechanism-silent-trees-vs-one-pinned shape. Fix: WS-E. Disposition: active.
- F014 (P1, seven-stage-containment-relay-two-verdict-authorities) — *The containment promise is a seven-stage relay held by convention: comments promise fail-closed, the inline code advises, the verifier checks only the verdict's FORMAT, the lane self-reports, the reducer labels its own compute PROVISIONAL, the runner preserves, and the ONE hard consequence lives at the PARENT'S merge.* [review-auto:1446/:1556; the pack:69/:148; verify-iteration.cjs:178-183/:286-292; the state rows; reduce-state.cjs:1847-1852/:1896/:2050; the runner's zero; fanout-merge.cjs:751-823]. Dimension: correctness. Impact: a lane reporting PASS over three active P1s passes the FORMAT gate; the receipts-privilege = codex-branch-only; the verdict = TWO unreconciled authorities (the 5/5 convention-clean = the LATENT-clean record). Fix: WS-B. Disposition: active; confidence 0.8; the downgradeTrigger: the located downstream parser = the reducer's own compute AND the receipts-privilege documented-as-codex-mandated → P2. (packet: iterations/iteration-004.md)
- F015 (P2, severity-four-promise-sites-vs-three-tier-machinery) — *The severity scale: four promise-sites — now including a REQUIREMENT — against an all-3-tier machinery, the spec's own wording cites the 3-tier meaning, the P3-to-P2 collapse remains undocumented convention, the authoritative tier-set is 3, and wave1-F006 is STILL-TRUE post-rewrite, its citation stale by +3 lines and its recorded fix unapplied.* [goal.md:47/:57, spec.md:83/:138; the 11 surfaces: SKILL:323-325/:331-333/:108, the pack:69, review-core 1.5.0.11, the 3-key schema, SEVERITY_KEYS/WEIGHTS, the 9 gates, the .codex failure_type, the merge SEVERITY_RANK]. Dimension: traceability. Impact: the 4-symbol/3-meaning drift = internal to spec:83's own sentence; the REQ-003's verification pass = verifies 4-tier wording WITH 3-tier machinery; the binding layer (the parent's records) = outside this packet. Fix: WS-F, BEFORE the REQ-003 pass. Disposition: active. (The RULING: the 3-tier executable contracts = authoritative; the P3 = the promise-text's orphan.)

## 4. Remediation Workstreams

Eight, severity-ordered (the full one-line versions in §2's packet):

- **WS-A (P1) the two-authority convergence** — F016's 4 rows, the uniform 3-step shape; first = the .claude/SYNC.md read, last = the CI-parser locate; the identity: the supervision = the design-choICE, the DOCUMENT-IT.
- **WS-B (P1) the containment relay** — F014: the 5-line consistency check, the pack:69 clause, the receipts parity, the ONE-PLACE handoff.
- **WS-C (P1) the line-1 config quartet** — F001: the {AUTO_SESSION_ID} binding, the research-confirm literal; reconciles with wave1's F001.
- **WS-D (P1) the ledger authority** — F007/F008/F006: the emergent flip documented, the enforcement consolidated-or-spelt, the registration decision, the reference-prose gap; the WRITE side = shadow-projection-store.ts (wave1's dead end, now located).
- **WS-E (P2) the mirror-fleet governance** — F010-F013: the ONE-canonicality decision, the 5-treatment crosswalk, the mode-key loss, the sampling decision, the 215B bridge.
- **WS-F (P2) the severity vocabulary** — F015: the 4-word fix x4 OR the P3 machinery + the recorded collapse rule; BEFORE the REQ-003 pass.
- **WS-G (P2) the orphan carry + the trust graduation** — F017/F018: the additive merge, the 7-rows-foremost pass, the attestation-grade table, the 3 cheap tests = the REQ-003's duty mechanized.
- **WS-H (P2) the variant-drift residuals** — F002-F005: the 18 steps, the wiring honesty, the provenance chain, the invariant scope.

## 5. Spec Seed

- **F015 (the severity)**: EITHER amend the four promise-sites (goal.md:47, goal.md:57-D3, spec.md:83, spec.md:138-REQ-003) to "P0 to P2 (the repo's severity meaning)" — a 4-word fix x4 — OR add the P3 machinery (SEVERITY_WEIGHTS 0.5, the state-schema validation, the reducer keys, the pack verdict-mapping) and record the collapse rule in state-jsonl.md. Either way: BEFORE the parent's REQ-003 verification pass, so the requirement's wording and the verifying machinery agree.
- **spec.md:83's internal 4-symbol/3-meaning drift**: the sentence promises P0-P3 "with the repo's severity meaning" while the repo's meaning IS the 3-tier table — the wording referendum rides WS-F.
- **F017 (the NEXT wave's spec)**: the 6+1 gap-classes = the angle-DESIGN input, the proven mechanism (this lane's 16-20 = all born of wave1's deferrals); the SPEC-SEED = the angle rows written FROM the gaps, not from the centers of mass.
- **F006 (the vocabulary prose)**: the four state reference docs under deep-review/references/state/ carry ZERO dotted-stem mentions — the prose contract never names the registered vocabulary; the seed = the vocabulary appendix or the explicit pointer.
- **F016 (the governance model)**: the seed = the supervision-model statement (trust + post-hoc-merge) recorded ONCE, in the packet's OR the skill's governance prose, with the 4 instances' sanctioned-deltas.

## 6. Plan Seed

1. **F016/WS-A**: the convergence workstream (4 rows, the 3-step shape); the SYNC.md read = the first action; the CI-parser = the last.
2. **F014/WS-B**: the verifier's 5-line check (the FORMAT collar); the pack:69 clause located-or-reworded; the receipts parity decision; the ONE-PLACE handoff note.
3. **F001/WS-C**: the token binding (in-YAML or the documented external substitution); the research-confirm literal (the 4-missing-block decision).
4. **F007/F008/F006/WS-D**: the migration emitter; the registration decision (the 56-of-61); the reference-prose; the flip documented where it happens.
5. **F010-F013/WS-E**: the ONE-canonicality decision; the crosswalk; the 215B diff -u (the 15-line answer, 3 iterations carried); the sanctioned-delta ledger.
6. **F015/WS-F**: the severity amendment BEFORE the REQ-003 pass.
7. **F017/F018/WS-G**: the SC-001 mechanics — the additive carry, the attribution convention, the trust-graduation table, the 3 cheap tests.

## 7. Traceability Status

Core protocols first, then overlays.

- **spec_code (core, hard gate)**: 5 records — 5/5 PASS (it 1: the four-variant line-1 + the step/phase + the wiring + the resource-map + the git censuses, spec.md:103; it 2: the 61-stem, 2-mode, 2-direction producer census, spec.md:104; it 3: the 12x6x5 declaration matrix, spec.md:105; it 4: the 7-stage chain traced to its termini + the 4-site/11-surface severity + BOTH rulings, spec.md:106; it 5: both reports + both registries read, the 7/7 examined, the 3 whole-system additions, spec.md:107). Every cited locus carries file:line; the boundaries are the recorded dead-ends (§8), not gaps.
- **checklist_evidence (core, hard gate)**: notApplicable x5 — the target's acceptance-criteria.md is an unfilled scaffold (no checklist.md; the predicate inactive: implementation-summary.md = the template author, completion_pct 0); the final assessment = the synthesis, recorded HERE: no per-iteration checklist rows existed to check; the parent-REQ rows = the REQ-003's verification pass, the parent's.
- **AC_COVERAGE: exempt** — lifecycle-early target: the scaffold's single AC-001 row (Status Unmet, no verification evidence) has no real rows to cover; floor 0/0.
- *Resource Map Coverage Gate: skipped by contract — `resource_map_present: false` + `resource_map.emit: false` in the lane config; the :2177-EXACT reducer invocation (WITH --emit-resource-map, faithful to the AUTO variant's directive where the precedent lane omitted the flag) recorded `resourceMapSkipped: true, reason: config.resource_map.emit=false` — the recorded expectation, delivered; no resource-map.md, no coverage-gate inputs.*
- **Overlay protocols** (feature_catalog_code, playbook_capability): not separately assessed — the lane's crossReference carries them, the iterations' evidence touched their loci incidentally (the feature-catalog = the mode-registry/hub-router vocabulary work of it5; the playbook = the cli-hub dispatch prose of it4), but no dedicated overlay pass ran; deferred-with-loci, the same disposition the precedent lanes recorded.

## Dimension Expansion Map

Breadth only — this section cannot alter the Executive Summary verdict, hasAdvisories, the finding registry, or any self-check results.

- **correctness** — primary: it1 (F001-F005), it2 (F006-F009), it4 (F014); secondary: it5 (F016-F018's evidence-loci). 11 findings.
- **traceability** — primary: it3 (F010-F012), it4 (F015); secondary: it1, it5. 5 findings.
- **security** — primary: it2 (F007-F008); it5 (F018). 3 findings.
- **maintainability** — primary: it5 (F016-F017); it3 (F013's graded-loss reading). 3 findings.
- Coverage 4/4 = 1.0; the it5 = the maintainability dimension's primary pass, the coverage CLOSING iteration (recorded in it3's telemetry as the covenant).

## 8. Deferred Items

1. **The pack:69 "CI gate parser"** — UNLOCATED through it4+it5 (the verdict-stack's linchpin, F016's last repair, WS-B's second clause). The handoff: locate-and-cite or reword.
2. **`.claude/SYNC.md`** — UNREAD through it3+it5 (the likeliest home of the tiara reconciliation = F010's downgradeTrigger = F016's FIRST repair). The handoff: read it FIRST (the cheapest possible reconciler).
3. **The 215B tiara-seam composition** — carried THREE iterations (diff -q silent, diff -u never run; the 15-line answer). The handoff: one command.
4. **The mcp-policy.md provenance** (w1-ds-F010's root cause) — the WHEN/WHY-ADDED = the git history, out of the it5 read budget; the STILL-TRUE = undisputed.
5. **The it2 dead-ends** (handed to it5,Angle-20 ran out before them): the research-side projection contracts + the schemas' prepareEvent join + the shadow-projection-store's write behavior (the reader contract cited, the writer = the F007-located file, unread) + WHICH of checkLedgerBacking's two frames-roots produced the found ledger.
6. **The confirm-variant execution archaeology** (wave1's deferral): whether any historical confirm-lane run exercised the mandated gateway path — untouched by angles 16-20 (the angle-16 census = the declarations, not the executions).
7. **The registry's 16 SUMMARY-* entries** — the reducer's aggregation mirrors (§3's note): the SAME binary rendered wave1's registry clean, so the input-shape trigger = UNPINNED; the reducer's SUMMARY-emitter branch = unread. Exit 0, 0 corruption; the 18 F-ids = the authoritative register.
8. **The overlays** (feature_catalog_code, playbook_capability): deferred-with-loci (§7), the precedent lanes' recorded disposition.

## 9. Search Ledger

The lane's greps/censuses, by proof-family (every finding's evidence = these, plus the reads):

- **The producer censuses** (it2, the 61-stem): the 8-surface rg/grep matrix (the 4 variant YAMLs, the runner, the gateway, both reducer copies, the verifier) x both registries x both directions; the corrective recount (the 61 vs the 32; the legacy-alias reading at 0.8).
- **The variant census** (it1): the step/phase-key counts (71/60/73/55), the line-1 literal key counts (16/12/9/8), the wiring counts (12/2/2/1), the resource-map flag/arg provenance.
- **The mirror matrix** (it3): the 6-tree x 12-agent presence (72/72), the 3x diff -q, the byte histogram (25,726/25,511/25,511/25,659/25,599/25,511), the temperature/marker counts (the 2-hit prose-residue, the 12/12 sandbox, the 11/12 unmapped-comments), the 25:3 vote census.
- **The containment chain** (it4): the 17+17-vs-0+0+0+0+1 codex-mention census; the 15 comparative-loci adjudication; the promise-site count (4) vs the machinery surfaces (11).
- **The orphan loci** (it5): the 7 TODAY-loci greps (the :454 pointer, the :547-570/:551-553/:136-137/:307-331/:312 executor-config, the :3208 call, the 4+1 fixture search, the :113/:124+SKILL:10 vocabulary, the :147/:41/:58 registry-roster, the :68+ROUTER hermes) + the :58-membership coda read.
- **The reducer run** (the synthesis): the :2177-EXACT invocation, exit 0, `resourceMapSkipped: true, reason: config.resource_map.emit=false`, 0 corruption warnings, 0 search debt, cleanSearchProof.
- No blocked searches; no unconcluded greps; every counted claim = a command with its output read.

## 10. Audit Appendix

Machine-owned markers preserved: this report references the reducer-owned registry (`deep-review-findings-registry.json` — 34 entries = the 18 F-ids + the 16 SUMMARY-mirrors, §3's note), the dashboard and the strategy anchors (the 5 ANCHOR-pair sections the :2177-EXACT reduction filled: the COMPLETED-DIMENSIONS 4/4, the RUNNING-FINDINGS 0/9/25) without mutating them; the state log remains append-only (12 rows at synthesis: 1 config, 5 iterations, 5 adjudications, 1 synthesis event); all artifacts deliberately UNSTAGED (the review-auto git semantics — this lane's F005, the recorded end-state; the merger restores at the parent's discretion).

The verdict chain, recorded 5/5: every narrative's final line = `Review verdict: CONDITIONAL`, each derivable from its own record (P0 0, P1 > 0) — the pack:69 mapping and the reducer:1847 ternary AGREE on all five; the divergence = latent, the pattern = F016's.

*Lane wave2-glm complete: 5/5 iterations (angles 16-20), verdict CONDITIONAL, hasAdvisories, 18 active findings (0/5/13), 0 resolved, 0 corrupted, 0 gating failures, 5/5 mechanical gates exit 0, dimension coverage 4/4. Continuity: deferred to the parent orchestrator (the strategy's recorded covenant). End of report.*
