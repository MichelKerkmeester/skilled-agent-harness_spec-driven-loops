# Egress proxy for notebook kernels — default-deny, allowlist only.
#
# Kernels have NO direct internet route (they sit on an `internal` Docker
# network / behind a deny-all NetworkPolicy on K8s); their only way out is this
# proxy, which permits ONLY the domains in ./allowed_domains. Keep that file in
# sync with notebook_runtime_settings.egress_allowlist.
http_port 3128

acl allowed_domains dstdomain "/etc/squid/allowed_domains"
acl SSL_ports port 443
acl Safe_ports port 80
acl Safe_ports port 443
acl CONNECT method CONNECT

# Only CONNECT to 443 (HTTPS), and only to allowlisted domains.
http_access deny CONNECT !SSL_ports
http_access deny !Safe_ports
http_access allow allowed_domains
http_access deny all

# Don't leak client details upstream.
forwarded_for delete
via off
httpd_suppress_version_string on

# Keep it lean; no on-disk cache.
cache deny all
