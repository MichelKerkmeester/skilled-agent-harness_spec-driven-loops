# Study 6 · cross-study-integration — research folder (CAPSTONE)

> **The capstone.** Study 6 of the [037 graph-engineering program](../../context-index.md) — not a new repo study. It welds studies S1–S5 into ONE coherent graph-based agent-loop design. 10-round integration run (gpt-5.6-sol **xhigh**/fast, `stopReason: maxIterationsReached`).

**Takeaway:** Five studies describe one system — a graph decides eligible work and order, a bounded loop does each unit, typed evidence stacks, and only 036 (currently dark) may authorize a protected mutation. The capstone delivers a **cross-cutting spine**, **eight proposed unified artifacts** (P1 authority contract · P2 promotion-evidence bundle · P3 memory/knowledge/belief layering · P4 036 capability inventory · P5 graph/subgraph/LEAF boundary · P6 typed gate state machine · P7 rollout/rollback DAG · P8 measurement + arbitration), **six resolved tensions**, **one integrated architecture**, and **one real next step** (a mutant-driven shadow vertical slice). The eight artifacts are proposed, unimplemented schemas — not shipped code.

**Verdict:** DeepSeek V4 Pro **PASS-WITH-FIXES**. It confirmed the cross-links are real integrations (not concatenations) and that 036-dark is carried honestly end-to-end; its fixes (artifacts relabeled proposed/unimplemented, the two convergence systems reconciled, loose citations constrained, R-owner + issuer-security gap added) were all applied.

## Read these (in order)
1. [`findings-plain-language.md`](findings-plain-language.md) — the whole integrated design in plain terms.
2. [`research.md`](research.md) — the full integrated architecture (spine + P1–P8 + tensions).
3. [`verification-deepseek-v4-pro.md`](verification-deepseek-v4-pro.md) — the independent second-model verdict + applied fixes.
4. [`resource-map.md`](resource-map.md) — the sources this study drew on.

## Provenance (machine-generated — evidence, not reading material)
`deep-research-state.jsonl` (per-round + route-proof records), `deep-research-config.json`, `deep-research-dashboard.md`, `findings-registry.json`, `observability-events.jsonl`, `orchestration-*.json/log`, `fanout-attribution.md`, and `lineages/cross-integration-sol-xhigh/` (raw fan-out: `iterations/`, `prompts/`, `deltas/`, `artifacts/`, logs).

← Back to the [program index](../../context-index.md) · [handover](../../handover.md)
