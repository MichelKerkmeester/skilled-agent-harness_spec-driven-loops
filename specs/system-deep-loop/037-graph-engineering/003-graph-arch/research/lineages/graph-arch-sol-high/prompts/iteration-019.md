DEEP-RESEARCH
Resolved route: mode=research; target_agent=@deep-research; execution=single_iteration; state_source=externalized_files; do_not_switch_mode=true

# Deep-Research Iteration 19 of 20

## Focus

Concrete schema and protocol decision matrix. Consolidate the evidence into implementation-ready but non-implementing decisions for the named contracts: `GraphAdmissionProofV1`, `SealedCompiledGraphV1`, `OrganizationGraphPolicyV1` plus compiled form, `GraphApprovalGateV1`, compile/admission `TransitionRefusalV1` variants, `GraphExecutionEventV1` plus replay-to-OTel projection, hierarchical budget reservation/debit/receipt records, and governance promotion evidence. For each: owner, producer, verifier, immutable/fresh fields, canonical encoding/digest, lifecycle, references, failure/refusal codes, runtime integration, 036 ordering, compatibility, mutants, and when-not-to-use.

Resolve naming conflicts or overlaps exposed in iterations 1–18. Identify which schemas are new payload families versus compositions of existing runtime primitives; avoid creating duplicate ledgers or authorities. Every finding must cite iteration/source evidence and classify against a named prior decision.

## Outputs and constraints

Read `.opencode/agents/deep-research.md` completely and lineage state first. One LEAF iteration, 3–5 batched actions, no subagents. Write only `iterations/iteration-019.md`, append one state record, and `deltas/iter-019.jsonl`; canonical record first. Every finding cited. Narrative must include master decision matrix, per-contract field/lifecycle decisions, integration dependency graph/table, eliminated alternatives, compatibility/migration, mutant coverage, and non-applicability. Route proof iteration/run 19 with executor `cli-codex/gpt-5.6-sol/high/fast`. Continue to iteration 20 regardless of convergence.
