DEEP-RESEARCH
Resolved route: mode=research; target_agent=@deep-research; execution=single_iteration; state_source=externalized_files; do_not_switch_mode=true

# Deep-Research Iteration 13 of 20

## Focus

Cross-cutting runtime mapping — shadow parity. Turn the decisions from iterations 3–12 into an exact shadow comparison contract using `.opencode/skills/system-deep-loop/runtime/lib/shadow-parity/`. Define required frozen inputs, observation classes, causal-prefix comparisons, earliest divergence ownership, normalization exclusions, authorityMutation/effect suppression invariants, replay/audit/budget/approval comparisons, and promotion evidence. Determine which GraphARC records can be legacy observations and which cannot serve as truth.

Read actual shadow-parity types/adapters/tests plus GraphARC trace/replay/policy/session/budget sources and 036 promotion specs. Build on studies 1–2; every finding classifies a named prior decision. Extend the mutant corpus with comparison-specific faults and explicit when-not-to-use boundaries.

## Outputs and constraints

Read `.opencode/agents/deep-research.md` completely and lineage state first. One LEAF iteration, 3–5 actions, no subagents. Write only `iterations/iteration-013.md`, append one state record, and `deltas/iter-013.jsonl`. Every finding cited. Narrative must include comparison schema, observation/frozen-input matrix, normalization rules, earliest-owner algorithm, promotion bundle, mutants, legacy compatibility, and non-applicability. Route proof iteration/run 13; executor `cli-codex/gpt-5.6-sol/high/fast`. Continue to max iterations.
