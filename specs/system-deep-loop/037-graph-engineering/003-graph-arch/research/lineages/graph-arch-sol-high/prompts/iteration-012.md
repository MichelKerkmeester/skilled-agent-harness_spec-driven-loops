DEEP-RESEARCH
Resolved route: mode=research; target_agent=@deep-research; execution=single_iteration; state_source=externalized_files; do_not_switch_mode=true

# Deep-Research Iteration 12 of 20

## Focus

Cross-cutting runtime mapping — mode registry and policy compilation. Determine how `OrganizationGraphPolicyV1`, compiled graph policy, admission manifests, gate kinds, executor capabilities, and graph transition/resource classes should relate to `.opencode/skills/system-deep-loop/mode-registry.json` and its consumers. Preserve the distinction between descriptive capability registration, governance policy, admission proof, and 036 authorization. Identify fields that belong in registry metadata versus policy artifacts versus runtime evidence; reject policy-by-renaming or mode aliases as authority.

Read the real registry and its loaders/tests plus relevant 036 and GraphARC policy/admission sources. Build on iterations 1–11; every finding classifies against a named prior decision. Include migration/versioning, tenant/role scope, deny→ask→allow compilation, audit provenance, mutants, and when-not-to-use boundaries.

## Outputs and constraints

Read `.opencode/agents/deep-research.md` completely and lineage state first. One LEAF iteration, 3–5 actions, no subagents. Write only `iterations/iteration-012.md`, one state append, and `deltas/iter-012.jsonl`. Every finding needs source/inference marker. Narrative must include ownership matrix, proposed registry integration, compilation flow, trust boundaries, alias/rename mutants, compatibility and non-applicability. Route proof iteration/run 12; executor `cli-codex/gpt-5.6-sol/high/fast`. Continue to max iterations.
