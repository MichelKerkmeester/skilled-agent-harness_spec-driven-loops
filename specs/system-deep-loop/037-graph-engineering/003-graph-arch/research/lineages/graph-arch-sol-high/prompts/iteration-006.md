DEEP-RESEARCH
Resolved route: mode=research; target_agent=@deep-research; execution=single_iteration; state_source=externalized_files; do_not_switch_mode=true

# Deep-Research Iteration 6 of 20

## Focus

R4 — One durable human-gate contract. Compare and unify GraphARC's three approval styles: planner proposal-fingerprint handshake/files, policy role-routed ASK handlers, and durable session holds/decisions. Reconcile them with graphene-main's dependency-vector gate and the 036 effect/fencing packages. Decide one `GraphApprovalGateV1` open → decide → revalidate → append → resume/timeout/cancel protocol with no checkpoint-file or direct-callback bypass.

Trace `planner/approval_file.py`, `planner/loop.py`, `policy/approvals.py`, `session/approval.py`, `session/runtime.py`, `session/store.py`, approval/session/planner tests, and the eval-gate blog. Map to authorized-ledger and locks/fencing. Build on iterations 1–5; classify every finding against a specific prior decision and state when not to use the gate.

## Outputs and constraints

Read `.opencode/agents/deep-research.md` completely and lineage state first. Execute one LEAF iteration, 3–5 research actions, no subagents. Write only `iterations/iteration-006.md`, append one canonical record, and write `deltas/iter-006.jsonl`. Every finding needs `[SOURCE: file:line]` or `[INFERENCE: ...]`. Narrative must include unified schema, state machine, dependency/freshness revalidation, idempotency, timeout/cancel semantics, bypass mutants, runtime mapping, and when-not-to-use. Route proof iteration/run 6; executor `cli-codex/gpt-5.6-sol/high/fast`. Continue despite convergence telemetry.
