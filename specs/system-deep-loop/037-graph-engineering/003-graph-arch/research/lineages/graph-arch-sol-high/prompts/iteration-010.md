DEEP-RESEARCH
Resolved route: mode=research; target_agent=@deep-research; execution=single_iteration; state_source=externalized_files; do_not_switch_mode=true

# Deep-Research Iteration 10 of 20

## Focus

R8 — Governance mutant corpus plus staged promotion. Convert GraphARC's staged examples, gate tests, and negative tests into a promotion-blocking mutant matrix that proves no admission bypass, policy rename laundering, argument escalation, stale approval, materialization/registry TOCTOU, partial-denial execution, trace/audit disagreement, budget reservation/reset bypass, or unauthorized resume/effect. Define earliest owning gate, expected refusal/evidence, authority/effect invariants, promotion stage, and required shadow observations.

Examine `examples/stage0_dag.py` through `stage6_memory.py`, all `tests/test_stage*_gate.py`, admission/policy/planner/replay/budget tests, `Graph Engineering Roadmap.md`, system-deep-loop `runtime/lib/shadow-parity/`, authorized-ledger, locks/fencing, hierarchical-budgets, and 036 promotion evidence. Build on iterations 1–9 and extend—not repeat—the studies 1–2 parity corpus. Classify each finding against a named prior decision. Include when-not-to-use boundaries.

## Outputs and constraints

Read `.opencode/agents/deep-research.md` completely and lineage state first. One LEAF iteration, 3–5 actions, no subagents. Write only `iterations/iteration-010.md`, append one record, and `deltas/iter-010.jsonl`. Every finding needs a source/inference marker. Narrative must contain mutant matrix, stage/promotion gates, earliest-owner rules, evidence bundle, coverage gaps, runtime mapping, and non-applicability. Route proof iteration/run 10; executor `cli-codex/gpt-5.6-sol/high/fast`. Continue to max iterations.
