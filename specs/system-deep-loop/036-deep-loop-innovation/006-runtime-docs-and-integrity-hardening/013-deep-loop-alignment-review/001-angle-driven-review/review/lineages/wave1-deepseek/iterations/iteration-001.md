---
title: "Iteration 1: Angle 1 — Routing artifact parity, system-deep-loop hub"
trigger_phrases: []
---
# Iteration 1: Angle 1 — Routing artifact parity, system-deep-loop hub

## Focus

Angle 1. The seven routing artifacts at the `system-deep-loop` hub root — `SKILL.md` (mode table), `mode-registry.json`, `hub-router.json`, `ROUTER.md`, `leaf-manifest.json`, `graph-metadata.json`, `description.json` — read against each other for agreement on mode names, counts, routing classes, versions, and leaf sets. Every disagreement is a finding.

Dimension: traceability (primary), correctness (secondary).
Method: direct read of all seven artifacts plus the two consumers that bind them (`.opencode/bin/lib/compiled-routing/009-parent-hub-rollout/002-system-deep-loop/lib/registry-compiler.cjs`, `.opencode/skills/sk-doc/sk-create-skill/scripts/generate-leaf-manifest.cjs`), and exact-path/exact-key cross-checks by script. No fixes.

## Files Reviewed

- `.opencode/skills/system-deep-loop/SKILL.md:1-174`
- `.opencode/skills/system-deep-loop/mode-registry.json:1-153`
- `.opencode/skills/system-deep-loop/hub-router.json:1-65`
- `.opencode/skills/system-deep-loop/ROUTER.md:1-119`
- `.opencode/skills/system-deep-loop/leaf-manifest.json:1-214`
- `.opencode/skills/system-deep-loop/graph-metadata.json:1-174`
- `.opencode/skills/system-deep-loop/description.json:1-37`
- `.opencode/skills/system-deep-loop/README.md:59-117`
- `.opencode/skills/sk-code/{SKILL.md,mode-registry.json,hub-router.json,description.json,ROUTER.md}` (version-parity control)
- `.opencode/skills/cli-external-orchestration/{SKILL.md,mode-registry.json,hub-router.json,description.json,ROUTER.md}` (version-parity control)
- `.opencode/bin/lib/compiled-routing/004-compiler-n1-shadow/compiler/compiler.cjs:182-187`
- `.opencode/bin/lib/compiled-routing/009-parent-hub-rollout/002-system-deep-loop/lib/registry-compiler.cjs:319-322`
- `.opencode/skills/sk-doc/sk-create-skill/scripts/generate-leaf-manifest.cjs:161-181`

## Scorecard

- Dimensions covered: traceability, correctness
- Files reviewed: 15 artifacts (7 hub routing artifacts, 1 hub README, 5 sibling-hub control artifacts, 2 consumers)
- New findings: P0=0 P1=4 P2=2
- Refined findings: P0=0 P1=0 P2=0
- New findings ratio: 1.00

## Cross-Artifact Agreement Matrix

| Property | SKILL.md | mode-registry.json | hub-router.json | ROUTER.md | leaf-manifest.json | graph-metadata.json | description.json | Agree? |
|---|---|---|---|---|---|---|---|---|
| mode roster | research, review, ai-council, agent-improvement, model-benchmark (`:22-27`, `:58`) | 5 modes (`:30-152`) | 5 routerSignals (`:18-44`) | 5 intent families (`:38-50`) | 5 mode entries (`:2-212`) | 5 intent families (`:49-84`) | "five modes" (`:3`) | **yes** |
| improvement lane count | "2 lanes" `:27`, `:58` **vs** "3 lanes" `:161` | "two improvement lanes" `:16` | n/a | "two improvement lanes" `:46-47` | 2 entries, identical sets | n/a | "two improvement lanes" `:3` | **NO** |
| registered-mode count | "six registered" `:131` | `modes.length === 5` | 5 signals | 5 families | 5 entries | 5 families | "five modes" | **NO** |
| version field | 3.0.0.0 `:3` | 2.0.0.1 `:3` | 2.0.1.1 `:3` | 1.0.1.0 `:11` | absent | absent | 2.2.3.0 `:4` | **NO** |
| always-loaded preamble | omits the question | — | `["ROUTER.md","mode-registry.json"]` `:13` | `DEFAULT_RESOURCE = []`, "No always-loaded preamble" `:67-69` | — | — | — | **NO** |
| model-benchmark leaf set | "keep the leaf sets dual-read" `:120` | — | 2 resources `:42` | 2 leaves (`:100-103`) | 61 leaves (`:96-162`) | — | — | **NO** |
| routing classes (registry aliases) | — | 5 alias arrays (`:48,72,96,121,147`) | 5 vocabulary classes | 5 intent signals | — | — | — | **NO (model-benchmark)** |
| `allowed-tools` = union of `toolSurface.allowed` | `Read,Write,Edit,Bash,Grep,Glob,Task,WebFetch` `:5` | union identical | — | — | — | — | — | **yes** |
| every `RESOURCE_MAP` path on disk and in `leaf-manifest` | — | — | — | 14/14 verified | 14/14 verified | — | — | **yes** |
| `hub-identity` absent from every mode class | — | — | verified clean | stated intent `:52-53` | — | — | — | **yes** |

## Findings

### P0, Blocker

- None.

### P1, Required

- **F001**: The seven routing artifacts carry six mutually disagreeing version values and two artifacts carry no version at all, while both sibling hubs keep the two pairs the hub diverges on in exact parity. Values read this iteration: `SKILL.md:3` = `3.0.0.0`; `mode-registry.json:3` = `2.0.0.1`; `hub-router.json:3` = `2.0.1.1`; `ROUTER.md:11` = `1.0.1.0`; `description.json:4` = `2.2.3.0`; `leaf-manifest.json` has no `version` key (top-level keys are exactly `modes`, `resourceContractVersion`); `graph-metadata.json` has no `version` key (only `schema_version: 2`). The drift is not uniform across the fleet, so it cannot be dismissed as "different files version independently": `sk-code` and `cli-external-orchestration` both hold `SKILL.md` == `description.json` (4.2.2.0 == 4.2.2.0; 1.5.0.0 == 1.5.0.0) and `sk-code` also holds `mode-registry.json` == `hub-router.json` (4.1.0.1 == 4.1.0.1). `system-deep-loop` is the outlier on both pairs (`SKILL.md` 3.0.0.0 ≠ `description.json` 2.2.3.0; `mode-registry.json` 2.0.0.1 ≠ `hub-router.json` 2.0.1.1). A reader or drift gate cannot tell whether these artifacts are the same generation. [SOURCE: .opencode/skills/system-deep-loop/SKILL.md:3] [SOURCE: .opencode/skills/system-deep-loop/mode-registry.json:3] [SOURCE: .opencode/skills/system-deep-loop/hub-router.json:3] [SOURCE: .opencode/skills/system-deep-loop/ROUTER.md:11] [SOURCE: .opencode/skills/system-deep-loop/description.json:4] [SOURCE: .opencode/skills/sk-code/SKILL.md:3] [SOURCE: .opencode/skills/sk-code/description.json:4]
- **F002**: The hub's own `SKILL.md` states two different improvement-lane counts and a registered-mode count that no other artifact supports. `:27` says "**improvement** (2 lanes)"; `:58` says "the two improvement lanes `agent-improvement` and `model-benchmark`"; `:161` says "`improvement` (3 lanes) — evaluator-first agent/model/skill improvement." Three lanes is not a live roster: `mode-registry.json` registers five modes with no `skill-improvement` entry, `deep-improvement/scripts/shared/loop-host.cjs:40` validates `new Set(['agent-improvement','model-benchmark'])`, `README.md:62` says "the three improvement lanes" too, and `mode-registry.json:16` says "the two improvement lanes (agent/model)". Separately, `:131` says "A new mode is needed beyond the six registered", while `mode-registry.json` holds exactly five modes, `hub-router.json:18-44` holds five `routerSignals`, `leaf-manifest.json:2-212` holds five mode entries, and `description.json:3` says "five modes". A reader told to extend "the six registered" is pointed at a roster that does not exist. [SOURCE: .opencode/skills/system-deep-loop/SKILL.md:161] [SOURCE: .opencode/skills/system-deep-loop/SKILL.md:131] [SOURCE: .opencode/skills/system-deep-loop/mode-registry.json:30-152] [SOURCE: .opencode/skills/system-deep-loop/deep-improvement/scripts/shared/loop-host.cjs:40]
- **F003**: `hub-router.json` and `ROUTER.md` contradict each other on whether the hub has an always-loaded default resource, and the shape `hub-router.json` uses is not a value the compiled-routing authored contract accepts. `hub-router.json:13` sets `"defaultResource": ["ROUTER.md", "mode-registry.json"]` (a two-element array). `ROUTER.md:67-69` states the opposite policy in its machine-readable block: "No always-loaded preamble: deep-loop routing loads only the selected mode's leaves so the hub default route stays minimal (disambiguation on no match)." followed by `DEFAULT_RESOURCE = []`. The compiled-routing authored-source validator requires this field to be a scalar string when non-null — `if (normalized.defaultResource !== null) { assertNonEmptyString(normalized.defaultResource, 'defaultResource'); }` — so an array is outside the authored contract for the field the same artifact names. Scope of impact stated honestly: this hub's own compiler branch does not read the field (`registry-compiler.cjs` for `002-system-deep-loop` references `routerPolicy.ambiguityDelta`, `routerPolicy.defaultMode`, and `routerPolicy.tieBreak` and contains zero references to `defaultResource`), so no live route fails today; the defect is the unresolved contradiction between the two artifacts an operator reads to learn the hub's no-match behaviour. [SOURCE: .opencode/skills/system-deep-loop/hub-router.json:13] [SOURCE: .opencode/skills/system-deep-loop/ROUTER.md:67-69] [SOURCE: .opencode/bin/lib/compiled-routing/004-compiler-n1-shadow/compiler/compiler.cjs:184-186] [SOURCE: .opencode/bin/lib/compiled-routing/009-parent-hub-rollout/002-system-deep-loop/lib/registry-compiler.cjs:319-322]
- **F004**: `leaf-manifest.json` gives the two improvement lanes byte-identical leaf sets, so they are not independently addressable, and `ROUTER.md` disagrees with that set by a factor of thirty. Both manifest entries carry 61 leaves and the arrays compare equal (`JSON.stringify(agentImprovement) === JSON.stringify(modelBenchmark)` → `true`); the shared set contains all 15 `references/agent-improvement/**` files and all 39 `references/model-benchmark/**` + `assets/model-benchmark/**` files. The generator's own contract for this case is the opposite: `generate-leaf-manifest.cjs:161-163` says the manifest emits "One mode entry per declared mode (not per physical packet directory), so an N-to-1 alias fan-out (two modes sharing one packet folder) keeps distinct, independently addressable leaf sets." Meanwhile `ROUTER.md:100-103` scopes `MODEL_BENCHMARK` to exactly two leaves (`deep-improvement/references/model-benchmark/evaluator-contract.md`, `deep-improvement/references/model-benchmark/lane-b-mechanics.md`), and `hub-router.json:39-43` gives model-benchmark two resources against a shared `deep-improvement/SKILL.md`. `SKILL.md:120` requires the `ROUTER.md` `RESOURCE_MAP` and `leaf-manifest.json` leaf sets to be kept in sync, so 2-vs-61 is a live sync failure on the artifact pair that rule names. [SOURCE: .opencode/skills/system-deep-loop/leaf-manifest.json:96-162] [SOURCE: .opencode/skills/system-deep-loop/ROUTER.md:100-103] [SOURCE: .opencode/skills/sk-doc/sk-create-skill/scripts/generate-leaf-manifest.cjs:161-163] [SOURCE: .opencode/skills/system-deep-loop/SKILL.md:120]

### P2, Suggestion

- **F005**: Two hub artifacts whose stated job is to describe the live roster still carry retired-mode vocabulary. `SKILL.md:8`'s keyword comment still lists `conformance`, `standard-authority`, `conformance-review`, `read-only-default`, `context-gathering`, and `reuse-catalog` — none of which names a mode in `mode-registry.json`, and the hub's own `:131` calls the roster six-and-no-more. `graph-metadata.json` — the ONE advisor identity file for the whole skill — carries `standard-authority` in `derived.key_topics` at `:124` and `skill benchmark` in `derived.trigger_phrases` at `:113`, against a live roster whose second improvement lane is `model-benchmark`. These are derived discovery terms, not prose, so they can still surface the hub for a request naming a family the hub no longer routes. [SOURCE: .opencode/skills/system-deep-loop/SKILL.md:8] [SOURCE: .opencode/skills/system-deep-loop/graph-metadata.json:124] [SOURCE: .opencode/skills/system-deep-loop/graph-metadata.json:113]
- **F006**: `mode-registry.json` declares two `model-benchmark` aliases that the hub's scoring vocabulary cannot see, so a request phrased with them scores zero. Registry `:147` lists `"prompt framework benchmark"` and `"benchmark-harness"` in the model-benchmark `aliases` array. `hub-router.json:58-60` scopes the `model-benchmark-aliases` class to `["/deep:model-benchmark", "model benchmark", "benchmark a model"]` — neither registry term is present. `SKILL.md:8` separately carries `benchmark-harness` as a hub keyword, confirming the term is considered live somewhere while being unroutable in the scoring layer that resolves the mode. [SOURCE: .opencode/skills/system-deep-loop/mode-registry.json:147] [SOURCE: .opencode/skills/system-deep-loop/hub-router.json:58-60] [SOURCE: .opencode/skills/system-deep-loop/SKILL.md:8]

## Claim Adjudication

```json
{"findingId":"F001","claim":"The seven system-deep-loop hub routing artifacts carry six disagreeing version values, with two carrying no version key, while both sibling hubs keep the SKILL.md/description.json pair in exact parity.","evidenceRefs":[".opencode/skills/system-deep-loop/SKILL.md:3",".opencode/skills/system-deep-loop/mode-registry.json:3",".opencode/skills/system-deep-loop/hub-router.json:3",".opencode/skills/system-deep-loop/ROUTER.md:11",".opencode/skills/system-deep-loop/description.json:4",".opencode/skills/system-deep-loop/leaf-manifest.json:1-214",".opencode/skills/sk-code/SKILL.md:3",".opencode/skills/cli-external-orchestration/description.json:4"],"counterevidenceSought":"Read every hub-root JSON directly with node rather than trusting grep, checked whether leaf-manifest and graph-metadata carry any version-like key at all (they carry resourceContractVersion and schema_version, which are contract discriminators, not artifact versions), and read the same five artifacts in sk-code and cli-external-orchestration to test whether independent per-file versioning is the fleet convention.","alternativeExplanation":"Perhaps only SKILL.md and description.json are meant to track each other and the rest intentionally version independently, in which case nothing here is wrong. That reading is rejected because system-deep-loop is the outlier on precisely that pair (3.0.0.0 vs 2.2.3.0) while both siblings match on it, so the divergence is a hub-local defect rather than a fleet rule.","finalSeverity":"P1","confidence":0.82,"downgradeTrigger":"Downgrade to P2 if a documented convention is found stating that hub-root artifact versions are intentionally independent and that SKILL.md/description.json parity is not required.","transitions":[{"iteration":1,"from":null,"to":"P1","reason":"Initial discovery; the sibling-hub control makes this a hub-local defect rather than a fleet convention"}]}
```

```json
{"findingId":"F002","claim":"SKILL.md states two different improvement-lane counts and a registered-mode count of six while every other artifact in the hub registers five modes and two improvement lanes.","evidenceRefs":[".opencode/skills/system-deep-loop/SKILL.md:27",".opencode/skills/system-deep-loop/SKILL.md:58",".opencode/skills/system-deep-loop/SKILL.md:131",".opencode/skills/system-deep-loop/SKILL.md:161",".opencode/skills/system-deep-loop/mode-registry.json:30-152",".opencode/skills/system-deep-loop/description.json:3",".opencode/skills/system-deep-loop/deep-improvement/scripts/shared/loop-host.cjs:40"],"counterevidenceSought":"Checked whether a third improvement lane exists that the registry simply fails to register: grepped loop-host.cjs for the validated mode set, grepped the deep-improvement packet for a skill-improvement lane, and counted registry modes, hub-router routerSignals, leaf-manifest mode entries and description.json prose independently. Found no third lane anywhere.","alternativeExplanation":"'Three improvement lanes' could be a forward-looking statement about an intended skill-improvement lane, and 'the six registered' could count the hub itself or a retired mode. Both readings are rejected: SKILL.md:131 frames the count as an existing roster a developer extends, and no artifact anywhere registers a sixth mode or a third lane.","finalSeverity":"P1","confidence":0.95,"downgradeTrigger":"Downgrade to P2 if a registered skill-improvement lane is added and SKILL.md:131's count is corrected to the real roster size.","transitions":[{"iteration":1,"from":null,"to":"P1","reason":"Initial discovery; internal contradiction inside one document plus agreement of five independent artifacts against it"}]}
```

```json
{"findingId":"F003","claim":"hub-router.json declares a two-element array defaultResource of hub-root files while ROUTER.md states in its machine-readable block that no always-loaded preamble exists, and the compiled-routing authored contract accepts only a scalar string for that field.","evidenceRefs":[".opencode/skills/system-deep-loop/hub-router.json:13",".opencode/skills/system-deep-loop/ROUTER.md:67-69",".opencode/bin/lib/compiled-routing/004-compiler-n1-shadow/compiler/compiler.cjs:184-186",".opencode/bin/lib/compiled-routing/009-parent-hub-rollout/002-system-deep-loop/lib/registry-compiler.cjs:319-322"],"counterevidenceSought":"Tested whether the hub's compiled compiler actually consumes the field before calling this a live break, by grepping the system-deep-loop registry-compiler for defaultResource (zero hits) and reading the three routerPolicy fields it does consume. Also checked whether the array is a legitimate multi-value form anywhere in the compiled contract.","alternativeExplanation":"The field may be inert legacy metadata for this hub, since its compiler branch never reads it — that is why the impact is bounded to the artifact contradiction rather than a broken route, and it is stated that way in the finding. The array shape still cannot be a valid authored value on any hub whose compiler does read the field.","finalSeverity":"P1","confidence":0.85,"downgradeTrigger":"Downgrade to P2 if hub-router.json's defaultResource is removed or reduced to null as unused metadata, leaving ROUTER.md's empty preamble as the single stated policy.","transitions":[{"iteration":1,"from":null,"to":"P1","reason":"Initial discovery; two artifacts state opposite no-match policies"}]}
```

```json
{"findingId":"F004","claim":"leaf-manifest.json assigns both improvement lanes byte-identical 61-leaf sets so they are not independently addressable, while ROUTER.md scopes the model-benchmark lane to two leaves against a rule requiring the two artifacts to stay in sync.","evidenceRefs":[".opencode/skills/system-deep-loop/leaf-manifest.json:96-162",".opencode/skills/system-deep-loop/ROUTER.md:100-103",".opencode/skills/system-deep-loop/hub-router.json:39-43",".opencode/skills/sk-doc/sk-create-skill/scripts/generate-leaf-manifest.cjs:161-163",".opencode/skills/system-deep-loop/SKILL.md:120"],"counterevidenceSought":"Compared the two arrays programmatically for byte equality rather than by eye, counted the lane-specific members in each (15 agent-only, 39 benchmark-only), and read the generator's docstring for its stated N-to-1 alias-fan-out contract. Also checked whether the typed-pair uniqueness rule still holds despite identical sets.","alternativeExplanation":"Sharing a packet folder could be read as licensing a shared leaf list, and the (workflowMode, leafResourceId) pair stays unique because workflowMode differs, so dual-read typing itself is not broken. That reading is rejected as an excuse for the manifest shape because the generator's own contract explicitly promises distinct independently addressable sets for exactly this fan-out case, and it does not explain the 2-vs-61 disagreement with ROUTER.md.","finalSeverity":"P1","confidence":0.8,"downgradeTrigger":"Downgrade to P2 if ROUTER.md is amended to declare the full shared 61-leaf set for model-benchmark, making the two artifacts agree even though the lanes remain non-distinct.","transitions":[{"iteration":1,"from":null,"to":"P1","reason":"Initial discovery; generator contract, ROUTER.md scope and SKILL.md sync rule all disagree with the manifest"}]}
```

```json
{"findingId":"F005","claim":"SKILL.md's keyword comment and graph-metadata.json's derived discovery terms still carry retired families that no registry mode names.","evidenceRefs":[".opencode/skills/system-deep-loop/SKILL.md:8",".opencode/skills/system-deep-loop/graph-metadata.json:124",".opencode/skills/system-deep-loop/graph-metadata.json:113",".opencode/skills/system-deep-loop/mode-registry.json:30-152"],"counterevidenceSought":"Checked whether any retired term maps to a live mode under a renamed key, by listing every registry workflowMode and alias and testing each residue term against them. None matched, and grepping deep-improvement/*.json found no lane registry reintroducing them.","alternativeExplanation":"The Keywords comment is a free-text hint block rather than a contract, and graph-metadata derived fields are machine-generated from source docs, so the residues may simply reflect older source text waiting on a regeneration. Accepted as the likely cause, which is why this is filed as a documentation/discovery gap rather than a routing defect.","finalSeverity":"P2","confidence":0.9,"downgradeTrigger":"Resolve when both files are regenerated and the retired terms disappear; keep as P2 while they remain discoverable.","transitions":[{"iteration":1,"from":null,"to":"P2","reason":"Initial discovery; residue is real but advisory discovery text, not a control path"}]}
```

```json
{"findingId":"F006","claim":"mode-registry.json declares two model-benchmark aliases that the hub-router scoring vocabulary omits, so prompts using them score no mode.","evidenceRefs":[".opencode/skills/system-deep-loop/mode-registry.json:147",".opencode/skills/system-deep-loop/hub-router.json:58-60",".opencode/skills/system-deep-loop/SKILL.md:8"],"counterevidenceSought":"Listed registry-only and hub-only terms per mode by set difference rather than sampling, then checked whether the two missing terms are handled by any adjacent class such as hub-identity or launcherVocabularyClasses. They are not, and hub-identity is deliberately reserved for defer evidence.","alternativeExplanation":"A bare 'benchmark harness' phrase may be intentionally left unscored so the hub defers instead of guessing a lane. Rejected as the reason here because the same term is registered as an alias of a specific mode and appears in the hub keyword block, which is inconsistent with deliberate deferral.","finalSeverity":"P2","confidence":0.78,"downgradeTrigger":"Downgrade to resolved if a decision record states that benchmark-harness and prompt framework benchmark are intentionally left unscored so the hub defers.","transitions":[{"iteration":1,"from":null,"to":"P2","reason":"Initial discovery; real recall gap with the lowest consequence of the six"}]}
```

## Traceability Checks

| Protocol | Status | Gate | Evidence | Notes |
|----------|--------|------|----------|-------|
| spec_code | partial | hard | `specs/system-deep-loop/049-deep-loop-alignment-review/001-angle-driven-review/spec.md:86` | The phase spec's angle 1 names exactly seven artifacts and the properties they must agree on; this iteration covered all seven and all five properties. Partial rather than pass because four properties disagree and the spec offers no resolution authority for a disagreement. |
| checklist_evidence | notApplicable | hard | — | The phase spec carries no checklist rows for individual review iterations; the doc's own §4 poses the two REQ rows against the lane run as a whole, not against one iteration. Assessed at synthesis. |

## Assessment

- New findings ratio: 1.00
- Dimensions addressed: traceability, correctness
- Novelty justification: six independent failure classes in the seven-artifact parity set — version-generation ambiguity, an internal count contradiction, two artifacts stating opposite no-match policies, a leaf-set scope mismatch against a stated sync rule, retired discovery vocabulary, and an alias-to-vocabulary recall gap. Each rests on a different artifact pair and a different consumer, so none is a restatement of another.

## Ruled Out

- **`allowed-tools` ≠ union of mode tool surfaces**: ruled out. The union over all five `mode-registry.json` `toolSurface.allowed` arrays is exactly `Read, Write, Edit, Bash, Grep, Glob, Task, WebFetch`, which is exactly the `SKILL.md:5` frontmatter grant — the hub's own §2 invariant holds. [SOURCE: .opencode/skills/system-deep-loop/SKILL.md:5] [SOURCE: .opencode/skills/system-deep-loop/mode-registry.json:37-42]
- **A `RESOURCE_MAP` path pointing at a missing or unregistered leaf**: ruled out for all 14 entries. Every `ROUTER.md` `RESOURCE_MAP` path exists on disk and every one is registered in `leaf-manifest.json` under its own workflow mode. [SOURCE: .opencode/skills/system-deep-loop/ROUTER.md:79-104]
- **A `hub-identity` leakage that would let a bare hub name route to a mode**: ruled out. The four `hub-identity` keywords appear in no mode's class, which is exactly the behaviour `hub-router.json:16` requires.
- **`mode-registry.json` `packet` paths pointing at missing packets**: ruled out. All four distinct packets (`deep-research`, `deep-review`, `deep-ai-council`, `deep-improvement`) resolve to a present `SKILL.md`.
- **The mode roster itself disagreeing across artifacts**: ruled out. All six roster-bearing artifacts name the same five modes. The counts that disagree are the *prose* counts in `SKILL.md`, filed as F002.

## Dead Ends

- **Treating `leaf-manifest.json` and `graph-metadata.json` as "missing version" findings on their own**: both carry a version-like discriminator (`resourceContractVersion: 1`, `schema_version: 2`) whose meaning is contractual rather than chronological. Counting that as an absent artifact version would make F001 unactionable, so the claim is scoped to the six real version values and the two genuine absences.
- **Reading the F003 array shape as proof of a live route failure**: the hub's own compiled branch never reads `defaultResource`, so the array does not break a route on this hub. Pursuing that framing would have produced a false P0.

## Recommended Next Focus

Angle 2 — the same seven-artifact parity question for `sk-code` and `cli-external-orchestration`. F001's sibling-hub control already read two artifacts from each; angle 2 must complete the set, and it must test the phase spec's specific claim that `cli-external-orchestration` registers seven modes including `cli-hermes` while its `ROUTER.md` and `SKILL.md` say six.

Review verdict: CONDITIONAL
