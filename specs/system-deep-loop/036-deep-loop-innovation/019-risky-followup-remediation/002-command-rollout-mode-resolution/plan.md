---
title: "Implementation Plan: Command Rollout-Mode Resolution"
description: "Phased plan to decide the intended deep/* rollout mode, make tests and config agree, and recompile the contracts without an unintended behavior change."
importance_tier: "high"
contextType: "general"
_memory:
  continuity:
    packet_pointer: "system-deep-loop/036-deep-loop-innovation/019-risky-followup-remediation/002-command-rollout-mode-resolution"
    last_updated_at: "2026-08-26T11:05:01.338Z"
    last_updated_by: "claude"
    recent_action: "Authored the rollout-mode phased plan"
    next_safe_action: "Phase 1: determine the intended default mode"
---
# Implementation Plan: Command Rollout-Mode Resolution

<!-- SPECKIT_LEVEL: 2 -->
<!-- SPECKIT_TEMPLATE_SOURCE: plan-core + level2-verify | v2.2 -->

---

<!-- ANCHOR:summary -->
## 1. SUMMARY

### Technical Context

| Aspect | Value |
|--------|-------|
| **Symptom** | `check-contract-drift` + `render-command-contract` fail: `STALE_SOURCE_DIGEST` for deep/review, deep/research, deep/ai-council |
| **Trap** | Recompiling clears staleness but flips deep/review rollout mode `fix`→`fallback` |
| **Renderer** | `scripts/render-command-contract.cjs` exposes `resolveMode` + `COMMANDS` |
| **Verification** | `render-command-contract` + `check-contract-drift` + whole suite |

### Overview
The failing tests are two entangled things: stale compiled contracts (a mechanical recompile) and a rollout-mode default that the recompile changes. The plan decides the intended mode first — from the rollout config, the renderer's `resolveMode` logic, and git history — then makes the config/compiler and the `resolveMode` test agree on that decision, and only then recompiles all deep/* contracts so the staleness clears with the mode intentionally set.

<!-- /ANCHOR:summary -->
---

<!-- ANCHOR:quality-gates -->
## 2. QUALITY GATES

### Definition of Ready
- [ ] `resolveMode` traced; what sets `fix` vs `fallback` understood
- [ ] The intended default decided with a cited source of truth

### Definition of Done
- [ ] `check-contract-drift` + `render-command-contract` pass
- [ ] `resolveMode` and the compiled contracts agree
- [ ] No unintended runtime behavior change; whole-suite delta clean

<!-- /ANCHOR:quality-gates -->
---

<!-- ANCHOR:architecture -->
## 3. ARCHITECTURE

### Pattern
Decide-then-regenerate. The rollout mode is a deliberate config, not an accident of compilation. The correct default is fixed at its source; the compiler and the test are made to reflect it; the compiled artifacts are regenerated last so the digest is fresh and the behavior is the intended one.

### Key Components
- **`resolveMode`** — the renderer's default-mode resolver (in `render-command-contract.cjs`).
- **Rollout config / injection mode** — whatever supplies the default (env, manifest, or a constant).
- **Compiled contracts** — `deep-*.contract.md`, regenerated by `compile-command-contracts.cjs`.

<!-- /ANCHOR:architecture -->
---

<!-- ANCHOR:phases -->
## 4. IMPLEMENTATION PHASES

### Phase 1: Investigate & decide
- [ ] Read `resolveMode` + the `COMMANDS` map in `render-command-contract.cjs`; find what selects `fix` vs `fallback`
- [ ] Check git history / the rollout config for the intended default of the deep commands
- [ ] Confirm whether the stale source docs are the current intended content (not a half-applied edit)
- [ ] **Decide** the default: `fix` (compiled contract body) or `fallback` (legacy body), with the cited source of truth

### Phase 2: Implement
- [ ] If `fix` is intended: correct the compiler/config so a recompile keeps `fix`
- [ ] If `fallback` is intended: update the `resolveMode` expectation in the test
- [ ] Recompile all deep/* contracts (`compile-command-contracts.cjs --command … --write`)
- [ ] Confirm the compiled body matches the decided mode

### Phase 3: Verify
- [ ] `check-contract-drift` passes (no `STALE_SOURCE_DIGEST`)
- [ ] `render-command-contract` passes; `resolveMode('deep/review')` = decided mode
- [ ] `legacy-projections` still passes (compiled-contract census unaffected)
- [ ] Whole runtime suite vs the 017 baseline: no new failures

<!-- /ANCHOR:phases -->
---

<!-- ANCHOR:testing -->
## 5. TESTING STRATEGY

| Test Type | Scope | Tools |
|-----------|-------|-------|
| Targeted | `render-command-contract`, `check-contract-drift`, `legacy-projections` | vitest |
| Behavior | The rendered body matches the decided mode | render CLI diff |
| Regression | Whole runtime suite vs baseline | vitest run |

<!-- /ANCHOR:testing -->
---

<!-- ANCHOR:dependencies -->
## 6. DEPENDENCIES

| Dependency | Type | Status | Impact if Blocked |
|------------|------|--------|-------------------|
| `compile-command-contracts.cjs` | Internal | Green | No recompile |
| The rollout config / source of truth | Internal | Unknown | No decision |
| The 017 whole-suite baseline | Internal | Green | Regression yardstick |

<!-- /ANCHOR:dependencies -->
---

<!-- ANCHOR:rollback -->
## 7. ROLLBACK PLAN

- **Trigger**: The recompile changes behavior in an unintended way discovered late.
- **Procedure**: `git checkout -- commands/deep/assets/compiled/deep-*.contract.md` and revert any config/test edit; the change is confined to the compiled artifacts + one config or test.

<!-- /ANCHOR:rollback -->
---

<!-- ANCHOR:phase-deps -->
## L2: PHASE DEPENDENCIES

```
Phase 1 (Decide mode) ──> Phase 2 (Config/test + recompile) ──> Phase 3 (Verify)
```

| Phase | Depends On | Blocks |
|-------|------------|--------|
| Investigate & decide | None | Implement |
| Implement | The decision | Verify |
| Verify | Implement | None |

<!-- /ANCHOR:phase-deps -->
---

<!-- ANCHOR:effort -->
## L2: EFFORT ESTIMATION

| Phase | Complexity | Estimated Effort |
|-------|------------|------------------|
| Investigate & decide | Medium | 1-2 hours |
| Implement (config/test + recompile) | Low-Medium | 1 hour |
| Verify (suite) | Low | 30 min (+ suite runtime) |

<!-- /ANCHOR:effort -->
---

<!-- ANCHOR:enhanced-rollback -->
## L2: ENHANCED ROLLBACK

### Pre-deployment Checklist
- [ ] The pre-recompile contracts snapshotted (git)
- [ ] The decided mode recorded

### Rollback Procedure
1. `git checkout -- commands/deep/assets/compiled/deep-*.contract.md`.
2. Revert the config or test edit.
3. Re-run the three targeted tests to confirm the prior state.

### Data Reversal
- **Has data migrations?** No — compiled artifacts + one config/test.

<!-- /ANCHOR:enhanced-rollback -->
