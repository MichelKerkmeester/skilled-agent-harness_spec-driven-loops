# Deep Review — Iteration 003

- **Wave**: A (breadth)
- **Dimension**: Spec-alignment / traceability (all three commits)
- **Executor**: cli-pi glm-5.3-flash (read-only)
- **Status**: complete
- **Conductor verification**: P0 independently CONFIRMED (see below)

## Summary

Commit 3 and most of commits 1/2 verify clean against their specs, but commit 1 leaves a **REQ-001 contradiction on disk** — a confirmed P0.

## Findings

- **[P0] [SOURCE: .codex/prompts/deep-alignment.md:1] (CONFIRMED by conductor)** Four git-tracked runtime prompt mirrors for the removed commands survived the removal, violating REQ-001 ("no surface dispatches/documents deep-alignment or command-benchmark") and contradicting implementation-summary.md's recorded "0 active files."
  - Orphans (all exist + git-tracked, all generated from now-deleted command files): `.codex/prompts/deep-alignment.md`, `.codex/prompts/deep-command-benchmark.md`, `.pi/prompts/deep-alignment.md`, `.pi/prompts/deep-command-benchmark.md`. Each carries `<!-- Generated by sync-prompts…cjs from .opencode/commands/deep/{alignment,command-benchmark}.md -->` and instructs the agent to read/follow those files — both deleted by `8849444aa6`.
  - **Root cause**: the removal deleted the `.codex/agents/` + `.pi/agents/` mirrors but never re-ran the prompt-sync generators, which unlink orphaned outputs.
  - **Conductor check**: `git ls-files` confirms all 4 tracked; `.opencode/commands/deep/{alignment,command-benchmark}.md` confirmed deleted; `sync-prompts.cjs` + `sync-prompts-pi.cjs` present.
  - **Fix**: run both sync generators; re-run the REQ-001 grep until it returns 0 active files.
- **[P2] [SOURCE: specs/system-deep-loop/036-deep-loop-innovation/024-executor-kind-routing/checklist.md:74]** The 024 packet's completion record still cites the deleted `deep-alignment-auto.yaml` as verified evidence (also `024/spec.md:89`), with no supersession note for commit 1's cascade.
  - **Fix**: add a supersession note (or amend the evidence rows) in the 024 packet pointing at the 025 cascade.

**Verified clean**: commit 3 gate markers gone from commands/deep + prompt/; 022 render-authorization + test reverted; commit 2 branches present in both surviving YAMLs with correct per-YAML executor field + dispatchId; commit 1 in-scope surgery holds (mode-registry clean, conformance stripped while mcp_promotion kept, 6 agents match, behavior-benchmark + create/* Phase-0 gates preserved as Out-of-Scope requires).

## Delta

| New P0 | 1 | New P1 | 0 | New P2 | 1 |

Review verdict: FAIL
