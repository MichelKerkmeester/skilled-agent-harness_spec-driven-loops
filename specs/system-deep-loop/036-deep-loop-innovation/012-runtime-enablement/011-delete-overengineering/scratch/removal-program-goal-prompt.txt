Finish the over-engineering removal program under specs/system-deep-loop/036-deep-loop-innovation/012-runtime-enablement/011-delete-overengineering (phase parent). Worktree .worktrees/022-012-runtime-enablement-build. Do NOT push. You ORCHESTRATE; GLM-5.2-High via cli-devin is the implementer/remover; you VERIFY (read every diff against the wave's tasks.md, run the gates — devin CANNOT run vitest, so you run them).

STATE: 011 is a phase parent (lean trio; the prior completed-deletion docs are in scratch/completed-deletion-mission/). The removal set is proven — a 10-iteration deep-research audit (gemini-flash + ox-alpha converged on the same 8 findings) plus a repo-wide zero-caller re-proof; full evidence, per-finding import-graph proof, and KEEP list in research/research.md. Five dependency-ordered waves are scaffolded:
- 001-leaf-removals — F5 shadow-adapters, F6 recovery manifest, F8 dead AUTHORITY_FLIP_COMMON constants (~460 LOC; lowest risk).
- 002-legacy-compat-converters — F1: 7 per-mode legacy-compatibility.ts (~4,306). KEEP deep-research-ledger-schema/legacy-compatibility.ts (LIVE at scripts/append-mode-event.cjs:233,453).
- 003-mode-contracts-value-layer — F2: conformance/strict-gate-validator/compatibility-policy/substrate-ports (~2,988). KEEP mode-contract-types.ts (import type by all 8 reducers); make index.ts types-only.
- 004-rollout-flip-tooling — F3 enable-modes.cjs + lib/fleet-enablement/, F4 flip-authority.cjs (~3,026). verify-authority.cjs already covers the read-only need flip served.
- 005-authority-registry-cas-reduction — F7: remove authority-registry CAS mutators (prepareCutover/compareAndSwap/compareAndSwapRollback/compareAndSwapFinalize), KEEP the read path (read/readDefault/isValidAuthorityRecord/selectAuthorityRoute/AUTHORITY_FLIP_MODE_ORDER) (~500). HIGH-ADJACENCY to the fail-closed gateway; run LAST, only after 004 removes its sole callers.

DISCIPLINE (in force): per wave — re-grep zero callers → sever barrels/imports FIRST → delete → tsc (no new TS2307; ≤57-error baseline) + verify-authority.cjs (8 modes new_authoritative_final, allOnLedger true) + runtime vitest suite (failing set unchanged BY NAME vs a freshly captured baseline) → one commit <100 files (NEVER override the mass-deletion guard). The live ledger loop is out of scope and must survive every wave: append gateway, authorized-ledger, event envelopes, legacy-projections, replay-fingerprint, the 8 reducers, sealed artifacts, receipts, and the authority-registry READ path. Comment hygiene HARD BLOCK: no spec paths / packet-phase / ADR-REQ-CHK-task ids in CODE comments — durable WHY only. Do NOT touch or commit *.sqlite or *.jsonl. Every green is guilty until a perturbation turns it red — re-measure the baseline immediately before each wave. Finding = hypothesis: re-prove zero callers at the moment of deletion, not from the doc.

EXECUTOR (authoritative): GLM 5.2 High via cli-devin (model uid glm-5-2). Dispatch the wave's removal directly: devin -p "<precise manifest from the wave's tasks.md: files to delete, barrels/tests to sever, the KEEP list, STOP-on-surviving-caller>" --model glm-5-2 --permission-mode dangerous --respect-workspace-trust false (run in the worktree; devin has no reasoning-effort flag — High is baked in the uid; drop --sandbox, it rejects writes in print mode). Brief exactly; the remover edits/deletes, you read the diff against tasks.md and run all gates before committing. STOP the remover on any surviving caller or scope violation.

REMAINING: execute waves in order 001 → 002 → 003 → 004 → 005; after each green wave, flip its parent PHASE MAP row to Complete and set graph-metadata.json derived.last_active_child_id. When all five land green, run validate.sh --strict recursively on 011 to Errors:0, reconcile the parent continuity, then stop for the operator ff-merge gate to v4/main (no push without an explicit in-the-moment go-ahead).
