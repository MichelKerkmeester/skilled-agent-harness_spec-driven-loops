1|Resolution mechanics: exactly how Node resolves an import through a symlinked node_modules directory, what --preserve-symlinks and --preserve-symlinks-main change, and which of those resolution paths the four workspace self-links actually break. Prove the breakage with a minimal reproduction rather than reasoning about it
2|Deriving the self-link set instead of hardcoding four: how to detect reliably which entries in a dependency tree point back at repository source rather than at vendored packages, across the layouts npm, pnpm and yarn produce, and whether package.json workspace declarations are a sounder source than filesystem inspection
3|Recreating the self-links inside a worktree: whether the recreated links should be relative or absolute, what each choice breaks (moving the worktree, nested worktrees, the containment path resolution that calls fs.realpath), and what happens when a linked tree is read-only or on a different filesystem
4|The rule as written versus the invariant it protects: what sk-git's provisioning rule currently forbids that is actually safe, the narrowest correct wording, and every other place in this repository that depends on the current broad wording, including the launch wrapper which already symlinks and the large-reorg playbook
5|Proving isolation held: how a test can demonstrate that a lane resolved a workspace module from its own worktree rather than the main checkout, what the observable difference is, and whether the existing worktree test harness can carry that assertion
