# Independent Verification Report: Four Fixes Review

Overall verdict: All four core claims are CONFIRMED by code inspection, test suites, and live commands, but one critical regression was introduced in the build validation suite (compiled command contract drift).

| Claim | Verdict | Evidence |
|---|---|---|
| **1. Route proof survives a re-projection (main fix)**<br>• State-log record with route proof passes without warning<br>• State-log lacking route proof but delta carrying it passes with warning naming the delta<br>• Neither carrying route proof fails with `route_proof_missing` (exit 1)<br>• Missing state-log record fails with `state_record_missing` (existence must not come from delta)<br>• File-order change does not weaken any other check (delta checks, ledger backing, gateway receipts) | **CONFIRMED** | • Code in `.skilled/skills/system-deep-loop/runtime/scripts/verify-iteration.cjs` (lines 257–360): `stateRouteReason = checkRouteProof(iterationRecord, ...)` and `deltaRouteReason = checkRouteProof(deltaRecords.findLast(...))` evaluate both sources. Step 2 checks state-log iteration record existence first (`REASONS.STATE_RECORD_MISSING` at line 277 before checking deltas). Step 3 checks delta JSONL and requires `type === 'iteration'` (`DELTA_FILE_MISSING` at line 287). Step 4 checks route proof: if both fail, returns `stateRouteReason` (`route_proof_missing` or `route_proof_mismatch`); if only state fails, returns `ok: true` and pushes warning `route proof for iteration N came from deltas/iter-NNN.jsonl; the <stateLogName> projection carries no leaf dispatch fields` (line 305). Step 2b (ledger backing) and Step 2c (gateway receipts) remain fully active.<br>• Command: `cd .skilled/skills/system-deep-loop/runtime && npx vitest run tests/unit/verify-iteration.vitest.ts` exits 0 with `20 passed (20)`. Lines 52, 81, 89, 99 cover all required states.<br>• **Evidence strength judgment**: Reading the delta is **stronger evidence** for *route proof* (leaf identity and execution path) because `deltas/iter-NNN.jsonl` is written directly by the leaf agent and retains the original dispatch fields before projection upcasting drops them. However, for *iteration existence and persistence*, reading the delta is **weaker evidence** than the projection because a leaf could write a local delta without gateway admission or ledger backing. |
| **2. Prompt pack asks for identity required by gateway**<br>• Prompt pack requires `runId`, `sessionId`, and `lineageId`<br>• Those three satisfy gateway rule in `legacy-compatibility.ts`<br>• Template still renders (exactly 16 substitution tokens)<br>• Delta-file example is consistent with required record | **CONFIRMED** | • In `.skilled/skills/system-deep-loop/deep-research/assets/prompt-pack-iteration.md.tmpl`: Line 60 requires: `The record MUST also carry stable identity: runId, sessionId and lineageId, all set to the run's session id, which you read from {state_paths_config} (the lineage.sessionId field). A record without them is refused with stable-identity-missing...` Line 63 defines the schema with `"runId":"<run session id>","sessionId":"<run session id>","lineageId":"<run session id>"`.<br>• In `runtime/lib/deep-research-ledger-schema/legacy-compatibility.ts`: `stableTargetIdentity` (lines 208–223) calls `hasIterationIdentity` (line 157), which calls `hasStableIdentity` (line 152): `isNonEmptyString(record.runId ?? record.sessionId) && isNonEmptyString(record.lineageId ?? record.parentSessionId ?? record.sessionId)`. The three fields provide non-empty strings, preventing the line 294 `stable-identity-missing` refusal.<br>• Template substitution tokens: Exactly 16 unique tokens match `extractTemplateTokens()`. Unit test `tests/unit/prompt-pack.vitest.ts` passes (`11 passed`).<br>• Delta example consistency: Line 83 carries `"type":"iteration","iteration":3,"runId":"<run session id>","sessionId":"<run session id>","lineageId":"<run session id>",...`, identical to the required schema. |
| **3. Per-iteration tool-call budget**<br>• `maxToolCallsPerIteration` is 24 in `deep-research-config.json`<br>• Workflow carries `tool_call_budget: { target: 12, max: 24 }`<br>• Identify other locations still pinning old 12 | **CONFIRMED** | • Confirmed in `.skilled/skills/system-deep-loop/deep-research/assets/deep-research-config.json` line 12: `"maxToolCallsPerIteration": 24`.<br>• Confirmed in `.skilled/commands/deep/assets/deep-research-auto.yaml` line 98: `tool_call_budget: { target: 12, max: 24 }`.<br>• Locations still pinning the old 12:<br>  1. `.skilled/commands/deep/assets/deep-research-confirm.yaml` line 114: `tool_call_budget: { target: 8, max: 12 }` (interactive workflow companion not updated).<br>  2. `.skilled/skills/system-deep-loop/deep-research/references/protocol/loop-protocol.md` line 299: `tracked against config.maxToolCallsPerIteration (default: 12)`.<br>  3. `.skilled/skills/system-deep-loop/deep-review/assets/deep-review-config.json` line 18: `"maxToolCallsPerIteration": 12` (and deep-review workflows `deep-review-auto.yaml` line 89 / `deep-review-confirm.yaml` line 88 pin `soft_max: 12`).<br>  4. Active run configs in specs, e.g. `specs/system-speckit/033-system-speckit-v4/042-v4-doc-freshness/research/deep-research-config.json` line 16 pins `12`. |
| **4. Two documentation defects**<br>• `cli-devin/SKILL.md` curated model-family list covers all IDs in allowlist (`DEVIN_SUPPORTED_MODELS` in `executor-config.ts` and `DEVIN_ALLOWED_MODELS` in `fanout-run.cjs`)<br>• `specs/.../041-skilled-source-root-migration/spec.md` continuity block removes "Plan phases 003 to 011", updates `completion_pct` from 90 to 100, and is supported by `goal.md` | **CONFIRMED** | • `DEVIN_SUPPORTED_MODELS` in `executor-config.ts` (lines 431–454) and `DEVIN_ALLOWED_MODELS` in `fanout-run.cjs` (lines 2426–2449) agree set-for-set (21 IDs each). All 21 IDs are named in `.skilled/skills/cli-external-orchestration/cli-devin/SKILL.md` line 252 (DeepSeek: 3, Gemini: 1, GLM-5.2: 6, GLM-5.3: 2, GPT-5.6 Luna Max: 2, SWE-2: 3, SWE-1.7: 3, plus base `swe`).<br>• In `specs/system-speckit/033-system-speckit-v4/041-skilled-source-root-migration/spec.md` lines 15–28: `next_safe_action` was replaced with `"None outstanding; other-machine cutover is the operator's item"`, `completion_pct` was updated to `100`, `recent_action` reads `"Migration closed; compatibility root settled as per-entry symlinks"`. All statements are supported by `goal.md` Sections 3 and 4, which record all 11 phases completed and validated. |

## Other findings

1. **Broken compiled command contract drift (Failing Test & Script)**:
   The compiled command contract `.skilled/commands/deep/assets/compiled/deep-research.contract.md` has drifted and was not recompiled after editing source files (`deep-research-auto.yaml`, `deep-research-config.json`, `prompt-pack-iteration.md.tmpl`, and `deep-research.md`).
   - Running `node .skilled/skills/system-deep-loop/runtime/scripts/check-contract-drift.cjs` fails with exit code 2:
     `[CONTRACT DRIFT] command=deep/research class=STALE_SOURCE_DIGEST reason=source digest mismatch for .skilled/commands/deep/assets/deep-research-auto.yaml recordedSha256=de26b18848bab61509a19d30959aebabddc5efaecd4de6d29fc3aa611ffe1675 liveSha256=700c280ec2a0d12abf9ec75f584875b37d629660e55e5d95998c6445efb0cc78`
     (and 3 other source files).
   - Running `cd .skilled/skills/system-deep-loop/runtime && npx vitest run tests/unit/check-contract-drift.vitest.ts` fails (`1 failed | 9 passed`).
   - Fix requires running: `node .skilled/skills/system-deep-loop/runtime/scripts/compile-command-contracts.cjs --command deep/research --write`.
2. **Asymmetric tool budget between autonomous and interactive deep-research**:
   `.skilled/commands/deep/assets/deep-research-auto.yaml` has `tool_call_budget: { target: 12, max: 24 }`, but `.skilled/commands/deep/assets/deep-research-confirm.yaml` (line 114) was left at `{ target: 8, max: 12 }`. Interactive users running `:confirm` will encounter the old 12-call ceiling that motivated this fix.
3. **Spec hierarchy drift in parent packet 033**:
   In `specs/system-speckit/033-system-speckit-v4/spec.md` (lines 330–339), phase 41 `041-skilled-source-root-migration` is still described as `in progress` with description `research phase done, cutover design next`, despite packet 041 having finished all 11 phases with `completion_pct: 100`. Furthermore, phase 42 retains raw placeholders (`[Phase 42 scope]`, `[Criteria TBD]`, `[Verification TBD]`).
4. **Internal status inconsistency in 041 spec.md**:
   In `specs/system-speckit/033-system-speckit-v4/041-skilled-source-root-migration/spec.md`, the frontmatter reports `completion_pct: 100`, but line 47 of the metadata table still displays `| **Status** | Draft |`.
5. **AC_COVERAGE advisory warnings in 050 spec**:
   In `specs/system-deep-loop/050-spec-protocol-ledger-events/`, running `validate.sh --strict` reports `AC_COVERAGE advisory (under floor): 0/5 ACs have evidence; floor 5/5. Cite file:line in the Verification cell, or retire the criterion through a decision record` due to citation format in `acceptance-criteria.md`.

## Residual risk

- **CI / Git hook failure**: Any commit or pre-push check executing `check-contract-drift.cjs` will fail until the compiled contract for `deep/research` is regenerated.
- **Interactive research loop failures**: Runs started with `:confirm` or without `:auto` use `deep-research-confirm.yaml` where the tool call limit remains 12, risking truncation or failed iterations when mandatory repo-rules load.
- **Deep-review prompt parity**: While `deep-research` now explicitly instructs the leaf on providing `runId`, `sessionId`, and `lineageId`, `deep-review`'s prompt pack template does not yet mandate `runId` or `lineageId` in its schema documentation (though its examples carry `sessionId`).

Verification complete: 4 confirmed, 0 wrong, 0 partial, 5 other findings.
