Spec folder and detached artifact boundary are already provided, so I’ll keep all writes confined to the lineage directory and first load the required deep-research workflow contract.Memory trigger lookup rejected the detached lineage session id because it is not a server-managed memory session; I’ll retry without binding that MCP session while preserving the user-provided session id for lineage artifacts.SKILL ROUTING: User directed → `deep-research`; executor is `cli-opencode`, so I’ll also read the CLI executor contract before composing any iteration dispatch.The deep-research contract requires command-owned state rather than an ad hoc loop, but the user’s boundary forbids writes outside the lineage directory. I’m checking the command/workflow files to identify the phase entrypoint that can honor the provided artifact override without running `resolveArtifactRoot`.I found the registered command doc and the fan-out playbook. Next I’m reading the loop/state contracts and the packet prompt section the request points to.The command doc confirms this prompt is already a fan-out lineage worker invocation: I should not spawn another `opencode run`; I need to perform the lineage’s own phase outputs inside `lineages/gpt-critical`. I’ll read the existing packet synthesis and prior lineage syntheses before iteration 1, as required by §9.4.Prior inputs are loaded. I’m now checking the live routing and validator sources that the critical review turns on, especially the Phase 0 gates, `orchestrate`/`deep` dispatch contracts, council route-proof fields, plugin hook surface, and validator comparator.The key source pattern is already visible: current `deep.md` is deterministic, while `orchestrate.md` is broad and NDP-constrained; the council route proof is a registry/YAML/runtime contradiction. I’ll gather the remaining exact citations, then write the lineage packet.The live evidence supports several sharper corrections than the prior GPT lineage: Phase 0 is a real Mode-D firing point, ai-council route proof is a false-pass risk, and the plugin can mutate tool args but fail-closed semantics remain unproven. I’m going to create the lineage state, 10 iterations, registry/dashboard, and synthesis under `gpt-critical` only.Phase init state is fresh: only `.executor-state/` exists in the lineage directory. I’m writing canonical deep-research artifacts now, preserving append-only-style JSONL ordering and one write-once iteration file per iteration.Core state files are in place. I’m adding the 10 write-once iteration narratives and final synthesis now.All lineage artifacts are written under the allowed directory. I’m running a local structural check on JSON/JSONL and expected file counts before returning the completion marker.FANOUT_LINEAGE_COMPLETE:gpt-critical