# Research Brief A3 — LEAF dispatch protocol vs GPT role absorption

READ-ONLY RESEARCH TASK. No file writes are requested. Do not ask about spec
folders or documentation scope. Do not ask any questions; produce the analysis.

## Measured problem (packet 033)

In the dispatch modes (review, research), the gpt-5.5-fast MEDIUM executor
"absorbs the LEAF role": instead of dispatching the required sub-agent, it does
the specialist work inline and — worst case — writes route-proof records as if
it had dispatched (cells RVB-007-med and RSB-007-med: full artifact set, zero
task events, fabricated route-proof JSONL). It also absorbed-by-timeout on
RSB-005-med ("dispatch, do not research it yourself" — it researched itself for
15 minutes). HIGH effort eliminates this in dispatch modes. Claude never
absorbs. We need the dispatch protocol to be absorption-resistant at ANY effort.

## Your task

Read (repo-root relative):
1. `.opencode/commands/deep/assets/deep_review_auto.yaml` — find the step that
   instructs dispatching the LEAF agent (deep-review) and the route-proof
   requirements.
2. `.opencode/skills/deep-loop-workflows/deep-review/assets/prompt_pack_iteration.md.tmpl`
   — the per-iteration prompt handed to the LEAF.
3. `.opencode/skills/deep-loop-workflows/deep-research/SKILL.md` — sections on
   LEAF dispatch/iteration protocol (skim for the dispatch instruction shape).
4. `.opencode/skills/deep-loop-runtime/` — list files; read whichever file
   defines route-proof / state JSONL expectations (e.g. state or guard modules)
   enough to cite how dispatch is evidenced.

Diagnose: what in the protocol's wording lets a literal executor believe doing
the work itself satisfies the contract? Is dispatch expressed as an
imperative step with a verifiable artifact BEFORE work may proceed, or as
descriptive prose? Why can the executor write route-proof records without
dispatching — is the proof generated by the host (forgeable) rather than by the
dispatch mechanism (unforgeable)? Compare with how the fabrication was caught
mechanically in the benchmark (task events vs route-proof divergence).

## Output contract (strict)

Markdown, no preamble, no questions. Sections:

### FINDINGS
Numbered, each citing `file:line` evidence. Mechanism-level.

### PROPOSALS
Numbered. Each: **Tag** (simplify|optimize|re-approach), **Change** (file +
concrete edit, e.g. hard-gate the loop on a dispatch receipt the host cannot
forge, move route-proof writing into the dispatch tool path, add an explicit
"if you produced findings without a dispatch event, abort" check), **Expected
effect** (cells RVB-007, RSB-005, RSB-007 pass at medium effort), **Effort**
(S|M|L), **Risk**.

3-6 findings, 3-5 proposals. Stay on dispatch/absorption only.
