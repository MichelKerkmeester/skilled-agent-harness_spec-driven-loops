### FINDINGS

1. `.opencode/agents/deep-research.md` has the best early hard-block placement, but still repeats the contract across multiple distant rule surfaces. The LEAF and state-first constraints appear near the top at lines 38-45 and again at lines 61-73, while the executable workflow starts at lines 81-93, the rules list appears at lines 421-458, and the dispatcher-facing output contract is not until lines 462-495. A GPT executor can satisfy the early workflow while missing the late output template.

2. `.opencode/agents/deep-research.md` is structurally rule-dense: 584 lines, 48 markdown headings, and 10 high-salience directive-keyword matches. The key output-verification checklist is late at lines 499-532, after routing, protocol, state, and rules sections. This shape is vulnerable to partial-presentation failures because the "return this exact summary" contract is in the final quarter of the file.

3. `.opencode/agents/orchestrate.md` puts a strong dispatch-only rule early at line 38, but the concrete dispatch machinery is spread across many later sections: core workflow at lines 50-61, agent routing at lines 69-82, nesting depth at lines 83-134, agent loading at lines 136-155, task format at lines 177-207, mandatory rules at lines 380-483, verification at lines 485-560, and budget rules at lines 684-781. There is no single compact "do this first before any dispatch" contract.

4. `.opencode/agents/orchestrate.md` is the highest-risk file for GPT parseability: 817 lines, 73 headings, and 44 high-salience directive-keyword matches. Gate 3 is named abstractly in the early workflow at line 51, but the actual hard-block rule is buried at lines 387-404 after routing, decomposition, examples, and other mandates. That layout matches the measured Gate-3 priority inversion risk.

5. `.opencode/agents/ai-council.md` marks hard constraints clearly in places, but the persistence/output contract is split across non-adjacent sections. Step 0 fail-closed persistence-root resolution is at lines 75-81, rules are at lines 385-421, the report template is at lines 424-496, verification is at lines 500-568, and the concrete persistence protocol is much later at lines 624-729. A GPT executor may treat §8 as the full output contract and miss the later "artifacts are mandatory" machinery.

6. `.opencode/agents/ai-council.md` is structurally the most section-heavy: 798 lines, 79 headings, and 22 high-salience directive-keyword matches. The file mixes narrative principles, routing tables, templates, protocol, schema, rollback, and verification in one long sequence. GPT-style instruction following tends to overweight early and enumerated material, then degrade on constraints buried after an apparent output template; this makes lines 624-729 especially likely to be under-applied.

### PROPOSALS

1. **Tag:** simplify. **Change:** In all three files, add a top-level `## EXECUTOR CONTRACT` immediately after frontmatter with 7-10 numbered hard rules: authority, first action, forbidden actions, required output, required verification, write boundary, stop condition. **Expected effect:** Reduces absorption and Gate-3 priority inversion in RSB-007, E4 hand-offs, and Gate-3 cells by giving GPT a single primacy-weighted contract before narration. **Effort:** M. **Risk:** Medium; duplicated rules can drift unless later sections explicitly reference this block as authoritative.

2. **Tag:** optimize. **Change:** In `.opencode/agents/orchestrate.md`, hoist Gate 3 from lines 387-404 into the first 60 lines as `Step 0: File-modification gate`, before routing or decomposition, and make line 51 point to that block instead of deferring details to §4. **Expected effect:** Targets Gate-3 priority inversion directly by moving the concrete hard block ahead of dispatch/routing machinery. **Effort:** S. **Risk:** Low; meaning stays the same, only priority and discoverability change.

3. **Tag:** optimize. **Change:** In `.opencode/agents/deep-research.md`, move the dispatcher-facing `Iteration Completion Report` template from lines 462-495 into the early contract block, with a short pointer from the later §6 section. **Expected effect:** Reduces partial-presentation failures in RSB-007 by making the final response shape visible before the long workflow and state-management sections. **Effort:** S. **Risk:** Low; template duplication should be avoided by moving, not copying.

4. **Tag:** re-approach. **Change:** In `.opencode/agents/ai-council.md`, merge the report template, persistence minimum artifact set, and completion verification into one contiguous `OUTPUT AND PERSISTENCE CONTRACT` near the top; leave schema, rollback, and invocation details later as references. **Expected effect:** Reduces chat-only council reports and missed artifact persistence in E4/deep-ai-council benchmark cells by preventing GPT from treating §8 alone as sufficient. **Effort:** M. **Risk:** Medium; the file is long and references external schema, so links must remain accurate.

5. **Tag:** simplify. **Change:** Convert late `ALWAYS / NEVER / ESCALATE` sections into short machine-readable headers in each file, e.g. `rules: [{id, phase, must, fail_status}]`, followed by human explanation. **Expected effect:** Improves literal adherence for GPT executors by separating imperative rules from explanatory prose and reducing salience competition between style guidance and hard blockers. **Effort:** L. **Risk:** Medium; requires deciding the canonical rule IDs and preventing the prose sections from becoming contradictory.
