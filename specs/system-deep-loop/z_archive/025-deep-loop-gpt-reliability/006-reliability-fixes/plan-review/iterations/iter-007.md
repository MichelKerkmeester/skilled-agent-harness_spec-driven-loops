</think>
</think>
</think>
</think>
</think>
</think>
## GAPS

**1. `deep-improvement`'s two sibling lanes (`model-benchmark`, `skill-benchmark`) are unbenchmarked and unphased.** The 033 5×3 grid measured only IMB (agent-improvement); `005-scorecard.md:17` lists "deep-improvement (IMB, 5)" while `mode-registry.json:101-136` and `SKILL.md:26` enumerate three lanes, each with its own `.opencode/commands/deep/assets/deep_{model-benchmark,skill-benchmark}_{auto,confirm}.yaml` and `presentation.txt`. Phases 003 (render), 004 (receipts), 005 (progress), 007 (agent contracts), 009 (pacing) all name "review/improvement" but never enumerate the two sister lanes. `deep_model-benchmark_auto.yaml` is 200+ lines of autonomous workflow with the same Gate-3 halt surface, the same prompt-pack absorption surface, and the same budget-edge class as `deep_review_auto.yaml`. **Severity: blocker.** **Amendment:** add a phase (or scope-amend 003/004/005/007/009) that explicitly lists the two unbenchmarked lanes and either re-runs the same fix package against them or formally records the deferral with a target date.

**2. The `:confirm` path is uniformly out of scope across all ten phases.** 033 ran only `:auto` (FX-001/002/003/004 are bare-auto per `033-deep-loop-behavior-benchmarks/005-scorecard.md` and the synthesis §1 cell ids `RVB-008/RSB-008/ACB-004/IMB-004/IMB-005` all name autonomous halts). Yet `:confirm` paths exist for every deep command (`deep_ai-council_confirm.yaml`, `deep_context_confirm.yaml`, `deep_research_confirm.yaml`, `deep_review_confirm.yaml`, `deep_agent-improvement_confirm.yaml`, `deep_model-benchmark_confirm.yaml`) and Phase 002's Gate-3 bridge text leaves them untouched — the bridge's "interactive-scoped ask-first" extension means :confirm must still ask the spec-folder question, but no phase names what the consolidated setup prompt must render on :confirm, which F-008 explicitly scoped as "constraint finding — the fix must land in the presentation layer or command doc". **Severity: blocker.** **Amendment:** amend Phase 003 scope to name the five `_confirm.yaml` paths and the `render.no_suffix` / `render.confirm` blocks in each presentation asset, not just the bare-auto halt.

**3. Eleven of fourteen agent files are unaddressed by Phase 007.** F-019 names "all 3 sampled" — `orchestrate.md` (817 lines), `deep-research.md` (584), `ai-council.md` (798). `.opencode/agents/` actually contains 14: `code.md` (545), `context.md` (469), `debug.md` (652), `deep-context.md` (392), `deep-improvement.md` (253), `deep-review.md` (564), `deep.md` (104), `design.md` (121), `markdown.md` (317), `prompt-improver.md` (371), `review.md` (499) are unsampled and named nowhere in `035/007-agent-executor-contracts/spec.md`. Phase 007 REQ-001 says "in each agent file" without enumeration; F-019 explicitly disclaims "all 3 sampled" — the 034 sampling budget left 79% of the surface unaudited for the same defect class. **Severity: blocker.** **Amendment:** amend Phase 007 REQ-001 to enumerate the full 14-file list and add a sub-finding scan of the 11 unsampled files for Gate-3 hard-block position, completion-template position, and dual-audience wording; insert the retrofits the scan uncovers.

**4. Non-deep-loop command surfaces sharing the auto/confirm/Gate-3 machinery are entirely out of scope.** `.opencode/commands/` contains ~35 commands across `doctor/` (3 files), `speckit/` (4), `memory/` (4), `create/` (7), `design/` (5), plus root-level `agent_router.md`, `prompt.md`, `goal_opencode.md`. Many share the same `:auto`/`:confirm` contract shape: `create_parent_skill_auto.yaml`, `create_changelog_auto.yaml`, `create_sk_skill_auto.yaml`, `create_folder_readme_auto.yaml`, `create_testing_playbook_auto.yaml` each ship an autonomous YAML + presentation asset + confirm YAML; `doctor/update.md` is a router; `speckit/{plan,resume,implement,complete}.md` and `memory/{save,search,manage,learn}.md` run through the same Gate-3 / Gate-2 / Gate-1 / Gate-4 pipeline. Phase 002's Gate-3 bridge, Phase 003's render contract, Phase 006's routing offer, and Phase 010's injection slimming apply globally via AGENTS.md but never name these surfaces — meaning the fixes will land in code paths the 033-validated cells do not exercise. **Severity: blocker.** **Amendment:** add a Phase 011 (or scope-amend 002/003/006/010) that names the non-deep-loop surfaces and either extends the fix packages to them or records a written boundary in `035/spec.md` §3 explaining why they are excluded.

**5. "GPT-safe" was validated only against `gpt-5.5-fast` — five other opencode executors are unmeasured.** The 033 legs were `claude-cli`, `gpt-fast-med`, `gpt-fast-high` (all `gpt-5.5-fast` variants per `005-scorecard.md:9`). The synthesis thesis ("our systems are Claude-shaped") is asserted from this single non-Claude model. The repo's small-model dispatch surface per AGENTS.md §8 (`sk-prompt-models`) covers DeepSeek-v4-pro, Kimi-k2.7-code, MiniMax-M3, MiMo-V2.5-Pro, GLM-5.2 via `cli-opencode`; none appear in the synthesis verification map (§6). Phase 010 REQ-001 (root-policy dedupe) is described as "broad salience benefit, no single guaranteed benchmark flip" — its claim of model-generality is unverified. **Severity: major.** **Amendment:** add a single executor leg (e.g., DeepSeek-v4-pro or Kimi-k2.7-code) to one phase's acceptance harness — the cheapest is Phase 010, which already disclaims a guaranteed flip — to verify that "executor-safe" matches "GPT-safe".

**6. MCP / hook / plugin injection paths other than the root policy are not addressed by Phase 010.** F-029 names "session-prime 2000-token budget; continuity brief 2400 chars; advisor brief 2048" as the bounded hooks, with the root policy as the dominant load. Phase 010 REQ-002 scopes only "session-prime + plugins" deferral and root-policy dedupe. The remaining injection surface is unaddressed: `mk-spec-memory_session_bootstrap` / `session_resume` payloads, `mk_code_graph_status` structural-context blocks, `mk_skill_advisor_advisor_recommend` brief, `mk-spec-memory_memory_match_triggers` HOT/WARM tier injection, `code_mode_call_tool_chain` tool-chain preambles, and the `mcp-code-mode` / `mcp-chrome-devtools` / `mcp-figma` / `mcp-open-design` / `mcp-click-up` transport briefs (per AGENTS.md §5). All enter the executor's prompt under autonomous command runs and compete for salience with the command contract, but none are slimmed. **Severity: major.** **Amendment:** amend Phase 010 scope to enumerate and either slim or defer each of these injection paths for autonomous command runs.

**7. F-038 (deterministic setup loader) is the strongest single structural fix in the synthesis but is dropped from the phase map.** `synthesis.md:35` ranks "Deterministic setup loader" as the "Best long-term shape; L effort, integration risk" and F-038 names it "Strongest long-term structural fix for setup/gate/rendering classes". `035/spec.md` Phase Documentation Map closes 41 of 44 findings; F-038 does not appear in any phase's "Closes" column (phase 008 closes F-035/F-036/F-037/F-009 only — compiled contract, not setup loader). REQ-004 in the parent spec says "Every actionable 034 finding (41 of 44) is closed by exactly one phase; the 3 non-actionable findings are documented" — F-038 is actionable and not closed, so the success-criteria count is wrong by one. **Severity: major.** **Amendment:** either add a Phase 011 that closes F-038 (L effort, lower priority) or formally defer F-038 in `spec.md` §4/§5 with an explicit "documented, not phased" entry matching the F-003/F-008/F-044 treatment so the 41-of-44 claim holds.
