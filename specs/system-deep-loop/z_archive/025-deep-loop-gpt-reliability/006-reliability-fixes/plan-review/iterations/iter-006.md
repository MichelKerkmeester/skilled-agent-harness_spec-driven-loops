</think>
</think>
</think>
</think>
</think>
</think>
## GAPS

1. **Multi-cause cell primary/secondary-cause attribution is required by every child spec but specified nowhere.**
   - Evidence: every phase spec repeats "record primary/secondary cause for any multi-cause cell" (035/002-gate3-precedence/spec.md:87, 035/005-progress-records/spec.md:87, 035/001-benchmark-harness-hardening/spec.md:86, etc.); synthesis §5.2 names it; findings-registry F-044 lists it as a verification-process item; gap-registry.md:GAP-03 already flagged the ACB-004 double-claim. Yet no file defines the field name, JSON schema, scorer branch, or aggregation rule. ACB-004 fails via Gate-3 halt at med AND stall at high (scorecard.md:23, scorecard.md:25), and phases 002 + 005 both claim that cell. Without attribution, either phase can claim the full flip; the rerun protocol adds a string the system has no way to read.
   - Severity: **blocker**.
   - Amendment: amend 035/001-benchmark-harness-hardening/spec.md REQ to define a `primary_cause` / `secondary_cause` field in the result JSON (schema v2), with allowed values derived from the 033 framework's bucket set; add a "multi-cause cells" list (`ACB-004`, `ACB-005`, `CXB-004`) the runner may not collapse into a single bucket; require phase 002 and phase 005 acceptance rows to cite the per-effort verdict each will own (e.g., 002 → "med halt → autonomous execute"; 005 → "high stall → liveness complete").

2. **A single post-fix run is the entire acceptance criterion for every cell flip, despite measured GPT non-determinism.**
   - Evidence: 033 framework.md:212–220 mandates single-sample default + manual 3-sample reruns for contested cells; decision-record.md D-006 records the policy. Scorecard.md:43–44 documents nondeterministic fail-close-vs-dispatch at gpt-fast-high between sibling cells (RSB-001 vs RSB-007; IMB-004 halt vs IMB-005 pass). The 035 plan ("re-run the affected cells … after the change") is a single re-run per cell per effort; no phase's success criteria specify N samples, stability criteria, or which cells are "contested." A Phase 002 single re-run that flips ACB-004 may be a stable win, or a coin-flip — the plan cannot distinguish.
   - Severity: **major**.
   - Amendment: amend 035/001-benchmark-harness-hardening/spec.md REQ-003 to include a per-phase N-sample rule: ≥3 stable verdicts (≥2/3 in expected bucket) for any cell that is being claimed as flipped, or `≥3` for any cell flagged `contested` by the 033 framework (CXB-004, ACB-004, ACB-005 per scorecard.md:25, scorecard.md:52, scorecard.md §4 item 3). Add a `provenance: n-sample` field to result JSON; require a flip-failure footnote when a flip is one-sample.

3. **Phase 001 rewrites the very scenario prompts Phase 006 uses as its acceptance cells.**
   - Evidence: 035/001-benchmark-harness-hardening/spec.md REQ-002 mandates rewriting ACB-003, IMB-003, RSB-004 path-free (closing F-025, findings-registry.md:33). 035/006-routing-offer/spec.md:121 names the SAME three cells (ACB-003, IMB-003, RSB-004) as its acceptance cells. Phase 001 invalidates the historical baselines those three cells are compared against; phase 006's "flip" can only be measured against the post-001 prompts, but the plan documents no baseline-restoration rule for these cells.
   - Severity: **major**.
   - Amendment: amend 035/001-benchmark-harness-hardening/spec.md REQ-002 + REQ-003 to require that the new path-free versions of ACB-003/IMB-003/RSB-004 be captured into `baselines/` with a `prompt-version: post-f-025` marker BEFORE any phase 006 acceptance re-run; require phase 006's success criterion to cite both the pre-fix baseline AND the post-001 prompt-version baseline.

4. **Phases 007 (agent-contracts) and 010 (injection-slimming) lack dedicated acceptance cells and the "indirect measurement" is undefined.**
   - Evidence: 035/007-agent-executor-contracts/spec.md:87 explicitly states "Cross-class (Gate-3 inversion, presentation, absorption); measured indirectly." 035/010-injection-slimming/spec.md:85 states "Prompt salience (indirect; supports the Gate-3 + presentation cells)." Neither spec defines: (a) what counts as the "indirect" measurement, (b) what cell-flip counts as proof, or (c) what regression on a non-named cell would falsify acceptance. The remaining mechanism ("re-run affected cells; no regression") is a no-fix-no-regression observation, not a positive flip.
   - Severity: **blocker** for these two phases as currently written.
   - Amendment: amend 035/007-agent-executor-contracts/spec.md REQ-001 to add a Top-of-file EXECUTOR CONTRACT block **size-cap or token-count assertion** as a measurable artifact (e.g., `grep -c `^## EXECUTOR CONTRACT` <file>` returns count = 1 in the first 60 lines after frontmatter; or a token-budget test asserting hard rules land in the first K tokens). Amend 035/010-injection-slimming/spec.md REQ-001 to require a token-injection assertion (measure root-policy token count before/after at runtime; assert ≤ 1 canonical copy). Either makes acceptance falsifiable without inventing new 033 cells.

5. **The 033 harness's own gaps (F-014 rewrite-detection, F-025 path-token leak) were retroactively discovered; the plan does not require re-scoring the full 32-cell × 3-leg matrix before using historical scores as the parent comparison.**
   - Evidence: findings-registry.md F-014 (line 22) and F-025 (line 33) were diagnosed mid-program, AFTER phases 002–004 had been scored (033/001-framework-and-harness/decision-record.md:60 records the scorecard moment). The 035 plan accepts cell flips only on "affected cells" (per phase spec); the remaining 23 cells × 3 legs (~69 results) carry forward scores scored with the broken runner. Specifically: rewrite-heavy GPT cells (RVB/RSB where GPT rewrote existing fixture artifacts) and the three vague-ask cells are candidates for misclassification under the broken runner. A cell the 035 plan treats as a stable "pass baseline" may actually be a previously-misclassified `role_absorption` that the harness under-detected.
   - Severity: **major**.
   - Amendment: amend 035/001-benchmark-harness-hardening/spec.md REQ-003 to require a one-shot re-run of all 32 cells × 3 legs (~96 runs) with the corrected runner BEFORE any phase 002+ cell-flip re-run; publish delta-classification report (`previously-pass-now-X`). Cells whose bucket changes invalidate every later phase's "baseline leg green" claim for that cell.

6. **The 120-second no-progress watchdog cannot differentiate "fix resolved a halt" from "fix induced a stall."**
   - Evidence: 033/framework.md:199 — watchdog window 120000 ms; framework.md:193 — hard budget 15 min (25 min for council/improvement). Phase 002 targets Gate-3 halts (RVB-008/RSB-008/ACB-004-med/IMB-004/IMB-005 — fires in ~1 s as `phase0_block`). Phase 005 claims ACB-004-high, ACB-005, CXB-004 stall-flips (requires distinguishing stall before/after). Scorecard.md:25 documents that "high is not stall-free" in council/context — it silently stalls on demanding cells, which is the same bucket phase 005 claims to fix. No plan rule requires a per-mode stall-rate comparison vs the scorecard's pre-fix stall histogram (scorecard.md §1: council gpt-fast-high 2/5 stalls; context 1/6 stall).
   - Severity: **major**.
   - Amendment: amend 035/001-benchmark-harness-hardening/spec.md REQ-003 to require a pre-fix/post-fix per-mode `stuck_no_progress` rate record (count of stalls per mode per leg); phase 005's success criterion must include "structured-mode stall rate per leg decreased by ≥Y" rather than a single cell-flip.

7. **The Claude baseline host confound (D-007) carries forward unchallenged; per-phase D5 latency claims are not required.**
   - Evidence: 033/001-framework-and-harness/decision-record.md:62–64 records the resolution: `claude` CLI is a different host binary than every other executor; host overhead is folded into every D5 ratio. framework.md:149–154 explicitly states "any report, table, or chart that cites a D5 ratio must restate this confound inline." Yet every 035 phase spec uses the rubric phrase "baseline leg green" (035/001/spec.md:96, 035/002/spec.md:97, 035/003/spec.md:97, …). "Green" reads as "did not regress on bucket state," but Phase 005 / 009 latency claims (IMB-001-high natural completion, pacing) implicitly need D5 evidence that cannot be cleanly attributed to model behavior vs host load.
   - Severity: **major**.
   - Amendment: amend every 035 phase spec's Risks table to require a "D5 baseline confound restatement" line on any phase that names a latency-sensitive cell (phases 005, 009, 010); define an acceptance-side latency-rule that compares the GPT leg's `tTerminal` against ITSELF pre/post (paired t-style), not against the Claude baseline.

8. **Cell coverage is bounded by design but not flagged; real-user inputs outside the 32 scenarios are invisible.**
   - Evidence: 033/spec.md:74–77 documents the 32 scenarios chosen by authors, weighted toward vague/concise, on bare/vague entry surfaces. The 035 plan treats the suite as the closing acceptance gate. Findings-registry F-024 (line 32) notes the trigger inventory "lacks natural phrasings ('best way to handle', 'make the agent better', 'research how')." Any user prompt that doesn't match the chosen 32 has no acceptance evidence — by design, but the 035 parent spec frames success as "GPT reliability fixed" rather than "the 32 measured scenarios behave reliably."
   - Severity: **minor** (by design) but currently un-flagged.
   - Amendment: amend 035/spec.md Success Criteria §5:1 to append "(scoped to the 32-scenario behavior-benchmark suite; no evidence for prompts outside that set)" wherever it claims reliability improvements; add a `coverage_outside_32: unmeasured` row to the Risks table.
