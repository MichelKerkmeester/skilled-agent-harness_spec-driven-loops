# Focus

Do other skills' own `SKILL.md`, `references/`, and `assets/` mention `mk-goal.js`, `/goal`, or `mk_goal`, and are those mentions still accurate post-remediation?

# Actions Taken

1. Read the active deep-research strategy, state log, and deep-research quick reference before searching. The active next focus is the related-skill documentation sweep, with carried-forward questions on README files, `ENV_REFERENCE.md`, `usage_limited`, and goal-state cleanup claims. [SOURCE: .opencode/specs/deep-loops/032-goal-opencode-plugin/research/deep-research-strategy.md:63-71] [SOURCE: .opencode/specs/deep-loops/032-goal-opencode-plugin/research/deep-research-strategy.md:131-144]
2. Ran a goal-specific grep over `.opencode/skills`, `.claude/skills`, `.agents/skills`, and repo `README.md` files for `mk-goal.js`, `mk_goal`, `goal_opencode`, `/goal`, `usage_limited`, `goal-state`, `store_health`, and `MK_GOAL`.
3. Confirmed the live command filename by globbing command markdown: `.opencode/commands/*goal*.md` returned only `.opencode/commands/goal_opencode.md`; `.opencode/commands/goal.md` returned no files. [SOURCE: .opencode/commands/goal_opencode.md]
4. Cross-checked candidate docs against the live plugin source for the new env vars, cleanup/archive functions, `store_health`, and `mutation` output. The source defines `MK_GOAL_STATE_ARCHIVE_RETENTION_DAYS`, `MK_GOAL_STATE_ACTIVE_RETENTION_DAYS`, and `MK_GOAL_STATE_SWEEP_INTERVAL_MS`; it also defines `pruneArchive`, `archiveGoalStateFile`, `sweepOrphanedActiveStates`, `recordProviderUsageLimit`, and status output with `store_health` plus `mutation=`. [SOURCE: .opencode/plugins/mk-goal.js:40-42] [SOURCE: .opencode/plugins/mk-goal.js:825] [SOURCE: .opencode/plugins/mk-goal.js:847] [SOURCE: .opencode/plugins/mk-goal.js:874] [SOURCE: .opencode/plugins/mk-goal.js:1354] [SOURCE: .opencode/plugins/mk-goal.js:1602-1646]
5. Ran a final negative-check grep for markdown that still says `usage_limited` is unimplemented/dead or that goal-state never cleans up.

# Findings

- **[F-ITER004-001, P2] `system-skill-advisor/README.md` still says live OpenCode-run tool invocation is under investigation.** The README states that the separate `/goal` plugin exposes `mk_goal` / `mk_goal_status`, but that live OpenCode-run tool invocation is still under investigation. [SOURCE: .opencode/skills/system-skill-advisor/README.md:85] That conflicts with the same skill's current feature catalog, which says a real `opencode serve` run listed `mk_goal` and `mk_goal_status`, and a live model turn called `mk_goal` and persisted state. [SOURCE: .opencode/skills/system-skill-advisor/feature_catalog/hooks-and-plugin/goal-opencode-plugin.md:41] This is a current README drift, not an archived historical note.
- **[F-ITER004-002, P2] `ENV_REFERENCE.md` is still incomplete for the three cleanup/archive env vars.** The goal-plugin env section lists `MK_GOAL_PLUGIN_DISABLED`, `MK_GOAL_AUTONOMY`, `MK_GOAL_DEBUG`, and the max-char caps, then ends with operator guidance. [SOURCE: .opencode/skills/system-spec-kit/mcp_server/ENV_REFERENCE.md:648-660] It does not list `MK_GOAL_STATE_ARCHIVE_RETENTION_DAYS`, `MK_GOAL_STATE_ACTIVE_RETENTION_DAYS`, or `MK_GOAL_STATE_SWEEP_INTERVAL_MS`, even though the live plugin defines those env names. [SOURCE: .opencode/plugins/mk-goal.js:40-42]
- **[F-ITER004-003, P3] Plugin-level README coverage still omits the new cleanup/status-output surface.** `.opencode/plugins/README.md` describes `mk-goal.js` as owning per-session state, active-goal injection, tools, usage accounting, continuation, default export, and test surface. [SOURCE: .opencode/plugins/README.md:46-50] The root README points readers to `.opencode/plugins/README.md` for the plugin contract. [SOURCE: README.md:1230-1233] That plugin README does not mention the new archive/sweep env vars, `store_health`, or `mutation=` output. This is weaker than F-ITER004-001 because it may be intended as a summary, but the root README labels it as the contract entry point.
- **[OBS-ITER004-001] The command filename remains `goal_opencode.md`, not `goal.md`.** The live command glob found only `.opencode/commands/goal_opencode.md` and no `.opencode/commands/goal.md`. This keeps iteration 2 and 3's filename conclusion valid.
- **[OBS-ITER004-002] Several related-skill mentions are accurate as boundary notes.** `system-spec-kit/mcp_server/plugin_bridges/README.md` correctly says the goal plugin is not a daemon bridge and points to `references/hooks/goal_plugin.md`. [SOURCE: .opencode/skills/system-spec-kit/mcp_server/plugin_bridges/README.md:36] `system-spec-kit/ARCHITECTURE.md` correctly says standalone local plugins such as `mk-goal.js` own their state and hooks directly. [SOURCE: .opencode/skills/system-spec-kit/ARCHITECTURE.md:160]
- **[OBS-ITER004-003] The final negative-check grep found old `usage_limited` and cleanup claims outside the related-skill README focus.** Current phase specs still contain the pre-remediation problem statements: phase 013 says `usage_limited` had zero production writers, and phase 014 says goal-state files were never removed by plugin lifecycle handling. [SOURCE: .opencode/specs/deep-loops/032-goal-opencode-plugin/013-design-fidelity-and-polish/spec.md:76] [SOURCE: .opencode/specs/deep-loops/032-goal-opencode-plugin/014-goal-state-cleanup-and-archive/spec.md:74] The grep also found archived research records with the same old claims. [SOURCE: .opencode/specs/deep-loops/032-goal-opencode-plugin/research_archive/2026-07-01-plugin-implementation-audit/iterations/iteration-006.md:16] Classification: these are not related-skill SKILL/reference/README docs, and some may be intentionally historical, but they should be revisited in a later iteration if the audit scope includes completed phase specs and archives.
- **[RULED OUT] Generic `goal` wording in unrelated skills is not `/goal` plugin documentation.** For example, `sk-prompt/SKILL.md` matched only ordinary prompt-goal wording, not the OpenCode plugin or `mk_goal`. [SOURCE: .opencode/skills/sk-prompt/SKILL.md:287]

# Questions Answered

- `.opencode/commands/goal_opencode.md` is still the only live goal command filename observed in `.opencode/commands/`; `.opencode/commands/goal.md` is absent.
- Other skill docs do mention the goal plugin. The notable stale live-current mention found this iteration is `system-skill-advisor/README.md:85`, which still says live OpenCode-run tool invocation is under investigation.
- `ENV_REFERENCE.md` remains incomplete for the three new cleanup/archive env vars.
- The skill/README sweep did not find a related-skill doc still saying `usage_limited` is dead or goal-state never cleans up. A broader repo grep did find those old claims in completed phase specs and archived research; those need separate classification because they may be historical context rather than live operator documentation.

# Questions Remaining

- Do completed phase specs and research archives need remediation notes or are historical problem statements allowed to remain unchanged after phases 013-014 landed?
- Should `.opencode/plugins/README.md` be treated as a full plugin contract page, or only a high-level index that can defer cleanup/env/status details to `system-spec-kit/references/hooks/goal_plugin.md`?
- Do `feature_catalog` and `manual_testing_playbook` entries outside the already-updated list contain similar stale live-verification wording?

# Next Focus

Rotate to repo-level and packet-local README/spec archive classification: determine whether old `goal.md`, `usage_limited dead`, and `goal-state never cleaned up` statements in completed phase specs, review archives, changelog indexes, and root/plugin READMEs are historical context or current-staleness findings that should be remediated.
