# Research SKIPPED — Project Manager plugin deprecated

**Status:** intentionally skipped (not researched).
**Date:** 2026-08-22

## Why this leg was skipped

The Project Manager plugin (`StepanKropachev/obsidian-pm`) was **deprecated and uninstalled from the vault** on 2026-08-22, consolidating its role onto **Notion Bases** (heavy feature overlap: task frontmatter, dependencies, Table/Board/Timeline views) plus **Meta Bind + JS Engine** for the live task-timer buttons.

This deep-research leg was scoped to optimize `references/plugins/project-manager/` — documentation that is being **removed** in the follow-up skill-doc consolidation phase (`008-notion-bases-consolidation`). Researching docs slated for deletion is wasted executor time, so the driver skips this child and proceeds to the still-valuable legs (`004-dataview`, `005-notion-bases`).

## To re-enable

Delete this file; the driver's resume-skip (`[[ -s research/research.md ]]`) will then run the 4-iteration cycle on the next launch.

## Superseding work

- Vault change: PM uninstalled, Notion Bases retained as the task/calendar foundation.
- Doc change: `references/plugins/project-manager/` removal tracked under phase `008-notion-bases-consolidation`.
