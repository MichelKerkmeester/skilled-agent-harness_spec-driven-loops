# Iteration 3 Dispatch Prompt

Read canonical state first. Establish the `health-viz` common grammar, date variables, time slicing, appearance and click overrides, validation failures, and the empty-folder mock-data edge case.
