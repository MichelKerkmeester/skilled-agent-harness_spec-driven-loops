# Iteration 001 prompt record

Recover source-backed facts only: plugin identity, entry point, every persisted setting and default, command IDs/titles, registered file/query processors, process invocation arguments, and vault/file boundaries. Record uncertainty rather than filling gaps.
