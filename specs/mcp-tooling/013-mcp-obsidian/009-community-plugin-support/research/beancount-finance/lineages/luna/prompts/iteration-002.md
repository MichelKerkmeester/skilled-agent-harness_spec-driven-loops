# Iteration 2 prompt pack

Focus: establish the Beancount v3-compatible ledger data model independently of plugin UI code.

Verify: chronological directive semantics; `open`, `close`, transactions/postings, `balance`, `pad`, `note`, `commodity`, `price`, `include`, options/query directives; multi-currency balancing; cost and price annotations; lot matching and booking.

Required outputs: valid file-layer examples, invariants, and failure modes an AI must check before and after edits.
