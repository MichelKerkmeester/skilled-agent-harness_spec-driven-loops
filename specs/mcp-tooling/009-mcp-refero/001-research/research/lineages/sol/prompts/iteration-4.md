# Iteration 4 prompt

Resolved route: mode=research target_agent=deep-research

Inspect the existing `mcp-tooling`, `mcp-code-mode`, `mcp-figma`, and `sk-design` contracts. Derive the exact architecture for a new `mcp-refero` transport packet: registry identity, backend, tool permissions, Code Mode namespace and discovery contract, read-only argument guardrails, `sk-design` judgment ordering, and non-credentialed verification fixtures. Do not implement the packet or modify anything outside the bound lineage artifact directory.
