TASK

Research how the repo-rule system of this repository should extend across the wider system, and which parts of AGENTS.md no longer earn being fully written out in an always-loaded document. Produce one evidence-cited research report. This run is read-only.

Four questions, each answered separately:
1. Which new rules do the skills and other system surfaces actually justify? A refusal is a valid and expected answer.
2. Which existing rules need changing, and why?
3. Where is cross-skill or cross-system integration missing or broken?
4. Which parts of AGENTS.md can be cut, compressed, or relocated to an owner? Removing content that does not earn a full writeup in an always-loaded document is an allowed outcome, not a failure.

CONTEXT (verified against the repository; re-verify any line number before citing it)

- The rule set lives in repo-rules/, currently 11 files. A root REPO RULES.md routes to them: a trigger table in section 2, an index in section 3, and a scope boundary in section 4. Rule files, trigger rows and index rows must stay equal in count, so any proposal that adds or removes a rule must state all three edits.
- AGENTS.md is the always-loaded framework, 501 lines at the time of writing, and CLAUDE.md is a symlink to it. Re-count before quoting a number.
- The constraint that decides most proposals: Gate 5 fires on the FIRST WRITE of a session and never on a read-only turn. The trigger sentence is at AGENTS.md:122. A rule file is therefore silent on any turn that only reads. Anything that must bind while reading cannot move into a rule file. Three separate proposals have already been decided by this.
- Whether a rule may exist at all is decided by four tests in .opencode/skills/sk-doc/sk-create-repo-rule/references/decision-tests.md: always-loaded, scope boundary, four-part refusal, restraint. They refuse most proposals, and section 5 of that file routes every refusal to where the content belongs instead. Run every proposal, including every relocation, through all four.
- Structural contract: .opencode/skills/sk-doc/sk-create-repo-rule/references/rule-anatomy.md. Length bands cap at 250 lines, over which a rule is split or cut. The no-duplicate-trigger-phrase constraint is in the sibling file creation-standards.md, near line 74. Verify both before citing.
- Two blocks inside AGENTS.md look duplicated and are not. The Restraint Signals table is the only copy, and the overengineering rule defers to it rather than repeating it: see the sentence at repo-rules/prevent-overengineering.md:102. The Confidence Thresholds bands are the only copy, and the honesty rule points at them without carrying them: see repo-rules/uncertainty-and-honesty.md:48. Do not propose removing either on a duplication argument.
- Already done, not to be re-proposed: a close-out rule and a presenting-decisions rule were added, the communication rule was split, validation mechanics were moved out of AGENTS.md into the spec-kit validation reference, and the expanded proof standards were relocated to the evidence rule.

INSTRUCTIONS

Read before asserting. Starting set: AGENTS.md, REPO RULES.md, all 11 files under repo-rules/, and the references under .opencode/skills/sk-doc/sk-create-repo-rule/. Widen from there into .opencode/skills/ and whatever other system surfaces the four questions reach.
For question 1, survey the surfaces that exist, report what each would need bound, then run every candidate through the four decision tests before proposing it.
For question 2, read each existing rule file against the surfaces it claims to govern. A change needs an observed failure, not an improvement in the abstract.
For question 3, trace the named integration points: AGENTS.md references into repo-rules/, rule files referencing one another, and the skills or commands that depend on either. Report the ones that do not resolve or that contradict each other.
For question 4, evaluate each AGENTS.md section against the always-loaded test: must it still bind on a turn where no trigger fires. Name the owner for any relocation, and state whether the receiving surface loads at the moment the content must bind.

DO

- Carry a file:line citation for every claim, and make sure each one resolves at the current state of the repository.
- Report a refusal with the deciding test named. A well-evidenced refusal is worth more than an invented rule.
- Quote the deciding text when it decides, instead of paraphrasing it.
- State plainly where the evidence is insufficient to answer a question.

DO NOT

- Do not draft rule text, rewrite AGENTS.md, or produce ready-to-paste content.
- Do not edit, create, move, or delete any file outside your own lineage directory under specs/agents/010-repo-rule-system-integration/research/lineages/.
- Do not manufacture proposals to look productive. Returning zero new rules is an acceptable result when the evidence supports it.
- Do not cite a file that was not opened, and do not carry a line number over from this brief without re-checking it. Two line numbers supplied to an earlier draft of this brief turned out to be wrong, so treat every number here as a hint and not as a fact.
- Do not propose a rule whose content would have to bind on read-only turns.

OUTPUT FORMAT

One section per question, in order, headed Q1 through Q4, each carrying its own findings and citations.
Then RANKED RECOMMENDATIONS: one ordered list across all four questions, each item naming the change, the evidence, the files affected, and the decision tests it passes.
Then REFUSALS: every proposal considered and declined, each naming the test it failed and where the content belongs instead.

ITERATION 1 OF 10.
This is the first pass. Establish the ground truth by reading before asserting.

Return only this iteration's findings, in the output format above.
