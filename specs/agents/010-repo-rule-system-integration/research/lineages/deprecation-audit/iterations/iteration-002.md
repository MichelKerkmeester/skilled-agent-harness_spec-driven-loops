{"timestamp":"2026-09-11T18:55:49.837Z","runtime":"claude","status":"ok","freshness":"live","durationMs":1976,"cacheHit":false,"skillLabel":"sk-doc"}
# Adversarial audit — skill-benchmark retirement

**VERDICT: The retirement is materially incomplete. It is not gone from every surface, and several of the residue classes are executable, enforced by passing tests, or actively generated into new files.** The hub's own registry surfaces are genuinely clean; the claim fails on graph metadata, the retained `sk-create-benchmark` packet, ~12 live operator documents, the sk-doc scaffolder, two test fixtures, and multiple tests that pin the lane. Every claim below carries a file:line. This run was read-only (tools: read/grep/find/ls only — no shell), so nothing was executed; load-path conclusions are from source, and the two sk-doc fixture tests were not run but their pass conditions are statically determined (see §6).

---

## 1. Reachability — what can still resolve the lane

**Clean** (verified this session): `mode-registry.json` (five modes), `hub-router.json`, `leaf-manifest.json`, `.opencode/commands/deep/` (five files, zero matches for the lane), the `.claude`/`.cursor`/`.codex`/`.pi` mirrors (zero matches across all four trees), the agent trees, the advisor projections (`aliases.ts` DEEP_MODE_BY_CANONICAL has 4 entries), the compiled command manifest, and `.opencode/commands/deep/assets`. The earlier "prefixed spelling" trap is covered this run: bare `skill-benchmark`, `skill benchmark`, `skill_benchmark`, `deep-skill-benchmark`, `--mode=skill-benchmark`, and script names were all searched across every file type.

**Still reachable:**

- `/create:benchmark`'s startup prompt still offers the lane: `.opencode/commands/create/assets/create-benchmark-presentation.txt:86` — `C) skill_benchmark`, with `:90-91` "Selecting B-E stops with a branch-not-registered message". Selecting C reaches the live command and then dead-ends.
- The sk-doc parent hub still advertises the family: `.opencode/skills/sk-doc/SKILL.md:31` — "Author MCP-promotion, behavior, **skill-benchmark**, and model-benchmark packages".
- The deep command contract still aliases the deleted command: `.opencode/skills/sk-doc/sk-create-command/assets/command-contract.json:276` lists `"/deep:skill-benchmark"` among `invocation_aliases`.
- The **hub's own graph metadata still triggers on the lane**: `.opencode/skills/system-deep-loop/graph-metadata.json:113` — `"skill benchmark"` in `derived.trigger_phrases`; the same file's `lastUpdated` is `2026-07-14`, predating the 2026-09-11 retirement (`description.json:37` also `2026-07-14`). A repo-wide, glob-scoped grep (`**/graph-metadata.json` under `.opencode`) returns exactly one match — this one. **The "removed from graph metadata" claim is false as stated.**
- **A live parity test enforces keeping the family**: `.opencode/skills/sk-doc/scripts/tests/test_create_benchmark_family_registry.py:24-33` extracts the literal `FAMILIES` from `sk-create-benchmark/SKILL.md` and asserts at `:67-90` that every family has `references/<kebab>/` + `assets/<kebab>/` and appears in the family table + README. `FAMILIES` still contains `skill_benchmark` (`sk-create-benchmark/SKILL.md:111-112`), and the dirs exist (`assets/skill-benchmark/`, `references/skill-benchmark/`). The test passes only because the lane's authoring packet is fully intact — deleting that subtree now breaks this test.
- **Two plugin tests pin the mode as a valid deep-improvement multiplex**: `.opencode/plugins/tests/system-deep-loop-guard.test.cjs:37,111,125` and `.opencode/plugins/tests/claude-task-dispatch-guard.test.cjs:49,168,185` fabricate registries with `{workflowMode: 'skill-benchmark', agent: 'deep-improvement'}` and assert warning text matching `agent-improvement|model-benchmark|skill-benchmark`.
- **Four runtime unit tests assert its identifiers**: `runtime/tests/unit/deep-improvement-common-ledger-schema.vitest.ts:912`, `agent-improvement-ledger-schema.vitest.ts:1042,1070`, `blinded-adjudication.vitest.ts:40,1120,1126`, `write-set-conflict-graph.vitest.ts:729` (hard-orders `'007-skill-benchmark'`).

## 2. Dangling references — surviving files pointing at deleted paths

- **Scaffolder emits the deleted runner into every new skill**: `.opencode/skills/sk-doc/sk-create-skill/scripts/init_skill.py:126-132` generates a `benchmark/README.md` whose §3 RUNNING A BENCHMARK block is `node .../deep-improvement/scripts/skill-benchmark/run-skill-benchmark.cjs`. **The copy at `.opencode/skills/sk-doc/scripts/init_skill.py:130` is identical.** `empty_reports_index` (both copies, ~:138-142) still documents a "matching the harness writer byte for byte" two-writer contract whose second writer is deleted.
- **Dead re-run instructions in six+ benchmark READMEs** (all previously confirmed clean by no means — they still teach the lane):
  - `sk-doc/benchmark/README.md:43-52` (`loop-host.cjs --mode=skill-benchmark`), `:60` (deep-improvement "owns the Lane C harness"), `:62-63` link the deleted `scoring-contract.md` and `commands/deep/skill-benchmark.md`.
  - `cli-external-orchestration/benchmark/README.md:47` (same block); `cli-claude-code/benchmark/README.md:42` (same block — an additional hub beyond prior findings).
  - `sk-code/benchmark/README.md:53,63` (`--mode=skill-benchmark`) and `:145` (link to deleted command).
  - `mcp-tooling/benchmark/README.md:44-47` (invokes deleted `run-skill-benchmark.cjs`), `:13`.
  - `sk-vision/benchmark/README.md:53,82` (deleted runner path), `:60` contains the literal non-executable line `node the retired scenario-persistence wrapper \`; `sk-vision/benchmark/reports/README.md:20` says rows are appended by "the benchmark harness (the retired scenario-persistence wrapper / `run-skill-benchmark.cjs`)".
  - `sk-communication/benchmark/README.md:37`, `sk-design-fundamentals/benchmark/README.md:37` (deleted runner), plus `sk-prompt/benchmark/README.md:20,57-58,68,70,71,79` (Lane C harness, deleted `build-report.cjs`, deleted scoring-contract, deleted `/deep:skill-benchmark` link).
- **`node the retired scenario-persistence wrapper` placeholder** (a redacted command that no longer runs) survives in ~10 live files: `sk-create-manual-testing-playbook/SKILL.md:299`, its `manual-testing-playbook/manual-testing-playbook.md:237,276` and `operator-contract/persist-scenario-result.md:41,56,61,100`; `system-spec-kit/manual-testing-playbook/ux-hooks/directive-lifecycle-dedup.md:105,157` + `system-spec-kit/feature-catalog/ux-hooks/directive-lifecycle-dedup.md:80`; `sk-communication/manual-testing-playbook/manual-testing-playbook.md:95`; `sk-code/sk-code-mobile-cli/manual-testing-playbook/manual-testing-playbook.md:184`; `sk-code/sk-code-obsidian/manual-testing-playbook/manual-testing-playbook.md:151`; `sk-design/sk-design-diagram/manual-testing-playbook/capture-review/capture-review.md:113`. The packet's limitation said these docs "still reference" the wrapper — they now carry a placeholder that satisfies the marker check while remaining unrunnable.
- **sk-create-benchmark teaches Lane C end-to-end**: `SKILL.md:111-112` (FAMILIES), `:120` (`SKILL_BENCHMARK` intent signal), `:146-148` (RESOURCE_MAP), `§10` heading at `:484`, `:646-650` ("`/deep:skill-benchmark` … run their lanes"), `:668` links the deleted `deep-improvement/references/skill-benchmark/scoring-contract.md` **and** the deleted `build-report.cjs`. Same dead links inside its own reference assets: `references/skill-benchmark/serving-snapshot-schema.md:211-213`, `assets/skill-benchmark/skill-benchmark-readme-template.md:179-182,189` (incl. `{{PATH_TO_SKILL_BENCHMARK_COMMAND}} -> .opencode/commands/deep/skill-benchmark.md`), `references/skill-benchmark/skill-benchmark-storage-guide.md:150` ("verify against `run-skill-benchmark.cjs`"). Its operator scenarios instruct running the deleted tooling: `manual-testing-playbook/evidence-and-boundaries/archive-compiled-routing-safely.md:43` runs `render-serving-snapshot.cjs` (which throws at load — §3).
- **router-replay named as a live consumer in five unrelated files**: hub `SKILL.md:47,49,51` ("the deterministic router-replay and benchmarks parse", "the router-replay contract resolves the hub's mode…"), hub `ROUTER.md:57,61` (heading "MACHINE-READABLE ROUTER (replay / benchmark source)" — "the deterministic router-replay parses"), `sk-doc/sk-create-skill/references/parent-skill/parent-hub-router-schema.md:30` (lists "`/deep:skill-benchmark` router-replay" as consumer #1 of `projectHubRouter`), `sk-code/sk-code-mobile-cli/SKILL.md:89`, `mcp-tooling/mcp-figma/SKILL.md:139-141` ("the skill-benchmark router-replay reads"). The tool is deleted; these design contracts point at nothing.
- **sk-doc frozen test fixtures name deleted dirs/READMEs**: `scripts/tests/code-folder/durable-directory-manifest.json:522,542-543` (`assets/skill-benchmark`, `scripts/skill-benchmark`, `…/tests`) and `:598-600` (`runtime/lib/skill-benchmark-ledger-schema`, `-reducers`, `-sealed-artifacts`); `baseline-readme-verdicts.json:7117,7127,7347,7357,7927,7937,7947` (seven deleted README paths). See §6 for why these now break their consumers.
- **Counts/indexes (brief item 4)**:
  - Hub `SKILL.md:120` "the **three** improvement lanes stay distinct"; `:131` "beyond the **six** registered"; `:161` "improvement (**3 lanes**)".
  - Hub `README.md:62` "explicit `null` only for the **three** improvement lanes".
  - `deep-review/README.md:176` "across **three lanes**: agent improvement, model benchmark and skill benchmark".
  - Root `README.md:422` "**three co-equal lanes** … giving **six** `/deep:*` loop commands in total"; `:941` "including agent improvement and **model/skill benchmarking**".
  - Sk-doc benchmark READMEs repeat "Lane C … D1-D5" framing (citations above).
  - No changelog entry records the retirement (`system-deep-loop/changelog/` ends at `v3.0.0.0.md`; that file's own `:26` records the pre-retirement six-mode registry).
  - `sk-doc/sk-create-benchmark/references/skill-benchmark/skill-benchmark-storage-guide.md`/`serving-snapshot-schema.md` are live files whose cross-links are dead (above).
  - (`.opencode/skills/README.txt:31` claims "Eleven top-level skill identities" vs 13 folders — checked: pre-existing catalog drift, **not** lane-related and not caused by this retirement.)

## 3. Broken dependents — load-path analysis (run-readiness)

- `sk-create-benchmark/scripts/render-serving-snapshot.cjs:47-50` — **top-level** `require` of `.opencode/skills/system-deep-loop/deep-improvement/scripts/skill-benchmark/compiled-routing-parity.cjs` (deleted). The script throws `MODULE_NOT_FOUND` at load; any operator scenario that calls it (e.g. `archive-compiled-routing-safely.md:43`) cannot complete.
- `sk-create-benchmark/scripts/archive-compiled-routing.cjs:34-41` — **top-level** requires of deleted `build-report.cjs` and `compiled-routing-parity.cjs`; `_args.cjs` additionally at `:222-225`. Throws at load.
- `sk-create-skill/scripts/validate-compiled-routing-scenarios.cjs:387-390` — the deleted `_args.cjs` require sits inside `if (require.main === module)`, so **library use is fine** (its test `tests/validate-compiled-routing-scenarios.test.cjs:19` requires it and passes), but **CLI invocation crashes**. This matches the packet's wording ("failing only on the path that reaches its require").
- `init_skill.py` (both copies) writes a dead command and a one-sided writer contract into every newly scaffolded skill (see §2).
- **Two packet limitations are STALE (damage has been fixed since the packet was written):** `sk-code/sk-code-opencode/scripts/run-all-drift-guards.sh:51-54` now records the third guard as missing with an explanatory comment (no deleted vitest path; script prints "all 2 guards PASSED", `:61-62`); and `sk-doc/scripts/tests/test-root-name-consumer-matrix.cjs` now requires only surviving modules (all six resolve), contrary to the packet's "throws at require time" claim.

## 4. Over-removal check

No over-removal found. Preserved report corpora are present across every expected owner: `cli-external-orchestration` (incl. all cli-* subhubs, ~150 json reports), `mcp-tooling`, `sk-code` (+ four surface skills), `sk-design/sk-design-diagram` (incl. 2026-09-11 runs), `sk-doc`, `sk-git`, `sk-prompt`, `sk-vision`, `system-deep-loop` + `deep-improvement/benchmark/reports`, `system-spec-kit`, and `.pi/extensions` (`pi-cache-optimizer`, `pi-fast-mode-w-subagent-support`). A `find` for `**/skill-benchmark-report.json` under `.opencode` returned a full corpus (~250 files) with no emptied owner tree. The `2026-09-11--manual-testing-playbook--capture-review-3` run in sk-design still embeds the deleted wrapper path as recorded evidence — expected for immutable run folders. **Not verifiable read-only:** the "554 files byte-identical" and "196 deleted / 57 edited" numbers need git history, which this toolset cannot reach; presence-level checks pass, byte-level claims are unverified.

## 5. Generated artifacts

- Advisor projections, alias maps, and the compiled command manifest are clean and consistent with the five-mode registry (spot checks this session; the routing-registry drift guard exists in `system-skill-advisor/mcp-server/tests/`).
- `leaf-manifest.json` and `description.json` agree with the two-lane reality (both readable as clean; `description.json:3` names "two improvement lanes … (agent-improvement, model-benchmark)").
- **`graph-metadata.json` was not regenerated** — trigger phrase + `2026-07-14` `lastUpdated` (§1). This directly contradicts the claim that graph-metadata registration was removed.

## 6. Tests that keep the lane alive or now fail

- **Blocked from passing without regeneration** (both under `.opencode/skills/sk-doc/scripts/tests/`):
  - `test_readme_manifest.py:44-50` compares the frozen directory set to a freshly derived one (`audit_readmes.py:265-295` derives purely from the current tree) → the six deleted entries in the frozen manifest guarantee a set mismatch. Caveat: that fixture **already carried a documented 284-entry drift** before the retirement (`specs/sk-doc/028-sk-create-diagram/015-flowchart-deprecation/implementation-summary.md:110`), so this test was already failing; the retirement added to an acknowledged debt without the prune that the prior deletion packet performed for its own 3 entries (`…/015/spec.md:94`).
  - `test_readme_verdict_parity.py:78-81` re-runs the validator over **every** baseline row → the seven deleted README rows in `baseline-readme-verdicts.json` cannot match. That baseline was documented as `diff_entries=0` over 1,026 tracked READMEs at its regeneration (`specs/system-speckit/033-system-speckit-v4/032-recorded-findings-closure/016-cross-session-operator-items/goal.md:94`), i.e. before the retirement — so these mismatches are newly introduced by the retirement and unaccounted for in its known limitations. Neither test is wired into CI (`.github/workflows` runs only `test_no_new_snake_case_guard.py` and `test_naming_root_resolver.py` from that tree, `naming-standard-guard.yml:51-52`), so the failures are latent, run manually as those packets document.
- Runtime unit tests and plugin tests pin the lane's constants/mode (§1).
- `test_create_benchmark_family_registry.py` — passes by enforcing the lane's family (§1).

## 7. The retirement packet's own accuracy

Accurate known limitations: the two broken sk-create-benchmark scripts, the deleted playbook-persistence wrapper, sk-create-benchmark teaching Lane C, runtime shared-type unions, the constant-asserting tests, spec-kit retrieval corpora indexing the vocabulary (I re-confirmed `trigger-index.json` hits are live sk-create-benchmark asset paths + spec history, not deleted script paths), and other-skills' benchmark READMEs (understated — they still teach re-run, not merely "describe stored reports"). Stale: the sk-code drift-guard item and the test-root-name item (§3). **Absent from the packet entirely:** deep-review README, root README, mcp-figma, parent-hub-router-schema, command-contract.json, init_skill.py (×2), the sk-doc fixture JSONs, plugin tests, the `/create:benchmark` option C, the sk-doc parent hub row, and the graph-metadata trigger phrase.

## Method & limits

Read-only: no command was executed. "Broken at load" conclusions come from source (top-level vs guarded requires). The two Python fixture tests were not run; their failure is statically implied but not observed. No git access, so deletion counts and byte-identity claims are outside what this run could check. All findings above carry file:line so they can be re-verified with a shell in the fixing run. My recommendation: treat the residue list as the work item — the highest-value order is graph-metadata regeneration, the sk-doc fixture regeneration, the retained-packet decision (either restore a runner or redact the packet's Lane C teaching), and the scaffold/README sweep.
