# Dispatch Log: Wave 003 - Advisor Positive Controls

One row per dispatch executed by this wave. Each dispatch = advisor probe (`skill_advisor.py --threshold 0.8`) + real `opencode run --model openai/gpt-5.5-fast --variant medium --format json` dispatch, graded against `AI-001`'s own `### Pass/Fail Criteria` section (`.opencode/skills/sk-design/manual_testing_playbook/advisor-integration/positive-design-controls.md`).

| dispatch_id | scenario_id | exact prompt used | advisor top-1 / confidence | resolved mode / packet / resources | verdict | rationale (cites criterion) |
|---|---|---|---|---|---|---|
| AI-001-P1 | AI-001 probe P1 (Interface) | "Make this onboarding page look less generic and give it a distinctive interface direction." | `sk-design` / 0.95 (local-fallback scorer) | mode `interface` (explicit "SKILL ROUTING: ... using `sk-design` because the actual task is interface direction"); packet `design-interface/SKILL.md` loaded via `skill` tool; resources `shared/register.md` + `shared/context_loading_contract.md` cited in loaded skill content | **PASS** | Satisfies "PASS iff all six probes return `sk-design` top-1 at confidence >= 0.80, and the hub resolves the expected mode and packet for each probe" — probe conjunct (0.95 >= 0.80) and hub conjunct (`interface` + `design-interface/SKILL.md`, matching AI-001's Expected Mode/Packet columns) both hold. Note: the live orchestrator's own internal `advisor_recommend` call (whose query text included the dispatch-note addendum) separately surfaced `sk-doc` 0.8658 above `sk-design` 0.8246 — a distinct, addendum-wording artifact the model explicitly reasoned past; does not affect this verdict since the graded probe is the external clean-prompt call. |
| AI-001-P2 | AI-001 probe P2 (Foundations) | "Create an OKLCH palette, typography scale, and spacing system for this analytics dashboard." | `sk-design` / 0.9231 (native scorer; `sk-code` 0.82, `sk-git` 0.82 tied second/third) | mode `foundations` (explicit "this request is foundations-dominant"); packet `design-foundations/SKILL.md` loaded via `skill` tool; `shared/register.md` read directly along with `oklch_workflow.md`, `palette_theming.md`, `data_viz.md`, `typography_system.md`, `layout_responsive.md`; `shared/context_loading_contract.md` + `shared/design_token_vocabulary.md` cited in loaded skill content | **PASS** | Satisfies "PASS iff all six probes return `sk-design` top-1 at confidence >= 0.80, and the hub resolves the expected mode and packet for each probe" — probe conjunct (0.9231 >= 0.80, clear top-1) and hub conjunct (`foundations` + `design-foundations/SKILL.md`, matching AI-001's Expected Mode/Packet columns; response body delivers real OKLCH color tokens, a type scale, and a spacing system) both hold. |
| AI-001-P3 | AI-001 probe P3 (Motion) | "Design the transition choreography and reduced-motion fallback for this modal." | `sk-design` / 0.8871 (native scorer) | mode `motion` (explicit "Selected mode: `sk-design` -> `motion`"); packet `design-motion/SKILL.md` loaded via `skill` tool; `shared/anti_slop_principles.md`, `shared/cognitive_laws.md`, `shared/register.md` all read directly along with `animation_decision_framework.md`, `motion_strategy.md`, `animate_presence_patterns.md`, `performance_reduced_motion.md` | **PASS** | Satisfies "PASS iff all six probes return `sk-design` top-1 at confidence >= 0.80, and the hub resolves the expected mode and packet for each probe" — probe conjunct (0.8871 >= 0.80) and hub conjunct (`motion` + `design-motion/SKILL.md`, matching AI-001's Expected Mode/Packet columns; `shared/register.md` loaded, matching AI-001's Expected Shared Resources for P3) both hold. Note: the live orchestrator's own internal `advisor_recommend` call again surfaced `sk-doc` 0.8524 above `sk-design` 0.82 (same addendum-wording artifact as P1); the model explicitly reasoned past it ("advisor returned an ambiguous match, but the substantive request is motion design") and correctly resolved `motion`; does not affect this verdict. |
| AI-001-P4 | AI-001 probe P4 (Audit) | "Audit this settings screen for WCAG contrast, keyboard focus, and design slop." | `sk-design` / 0.8367 (native scorer; `sk-code` 0.82 second) | mode `audit` (explicit "Mode: `sk-design` -> `audit`"); procedure `procedures/accessibility_audit.md` applied; packet `design-audit/SKILL.md` loaded via `skill` tool | **PASS** | Satisfies "PASS iff all six probes return `sk-design` top-1 at confidence >= 0.80, and the hub resolves the expected mode and packet for each probe" — probe conjunct (0.8367 >= 0.80, clear top-1 over `sk-code`'s 0.82) and hub conjunct (`audit` + `design-audit/SKILL.md`, matching AI-001's Expected Mode/Packet columns) both hold; response is findings-first with P1-severity contrast/focus/semantics findings explicitly labeled "inferred... not confirmed," matching `audit-accessibility`'s and `audit-quality`'s documented evidence discipline. |
| AI-001-P6 | AI-001 probe P6 (design-mcp-open-design transport) | "Wire Open Design's MCP server into opencode so I can drive od cli from the terminal." | `sk-code` / 0.9464 (native scorer, **top-1**); `sk-design` / 0.8517 (second); `mcp-code-mode` / 0.8403 (third); `cli-opencode` / 0.82 (fourth) | mode `design-mcp-open-design` (explicit "This is a pure WIRE request... using the Open Design transport exemption"); packet `design-mcp-open-design/SKILL.md`-family references loaded via `skill` tool + direct `read`; no expected shared resources required per AI-001 (transport, doesn't consume the shared reference base) — none loaded, matching expectation. Real Bash mutations occurred: launched Open Design.app, ran the packet's own dry-run/install CLI, and wrote a corrected `mcp.open-design` entry into the user's global `~/.config/opencode/opencode.json` | **FAIL** | Directly matches AI-001's own stated FAIL trigger: "FAIL iff any positive probe routes to a non-design skill... or P6 is misrouted to the external `mcp-figma` sibling." `P6`'s clean-prompt advisor probe returns `sk-code` (a non-design skill) top-1 at 0.9464, ahead of `sk-design`'s 0.8517 — the PASS conjunct "all six probes return `sk-design` top-1 at confidence >= 0.80" is violated. (P6 was NOT misrouted to `mcp-figma`, and the live hub dispatch itself still correctly resolved `design-mcp-open-design` via its own reasoning rather than a passing advisor call — but the scenario's own criteria grades the probe result, which fails.) Separately and independently of this routing verdict: the live dispatch performed real, unrequested system mutations (app launch + global config write) despite the dispatch note framing the call as a standalone, non-mutating evaluation — flagged in `implementation-summary.md` Known Limitations #2, not folded into this routing grade. An initial dispatch attempt mistakenly used the UI no-target clause (wrong branch for this non-UI prompt); it was caught and re-run with the correct empty clause before this verdict was recorded. |

## Summary

- **PASS**: AI-001-P1, AI-001-P2, AI-001-P3, AI-001-P4 (4/5)
- **PARTIAL**: none
- **FAIL**: AI-001-P6 (1/5) — advisor probe routed top-1 to `sk-code`, a non-design skill, directly matching AI-001's own FAIL trigger; independently, the live dispatch also produced a real out-of-repo system side effect (Open Design app launch + global `opencode.json` write)
- **SKIP**: none (P5 intentionally out of scope for this wave — dispatched separately in the serial md-generator wave)
