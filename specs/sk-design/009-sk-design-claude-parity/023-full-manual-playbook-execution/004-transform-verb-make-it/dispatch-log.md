# Dispatch Log: Phase 004 - Transform Verb make it Wave

One row per real `cli-opencode` dispatch run by this wave. Model: `openai/gpt-5.5-fast --variant medium --format json`. Advisor probe uses the clean scenario prompt with no addendum; the real dispatch appends the standalone-evaluation addendum (empty no-target-clause for all 5 — none of these prompts name a hypothetical local UI target).

| dispatch_id | scenario_id | exact prompt | advisor probe top-1 / confidence | real-dispatch resolved mode / packet / resources | verdict | rationale (cites specific criterion) |
|---|---|---|---|---|---|---|
| TV-001-V1 | TV-001 | `Make it bolder without changing the product copy.` | no match >= 0.8 (`[]`) | Internal `mk_skill_advisor_advisor_recommend` call ranked `sk-design` **3rd** (score 0.13, confidence 0.82) behind `sk-doc` (0.87) and `sk-code` (0.84), `"ambiguous": true`. Loaded generic `sk-design` hub file-listing only (never `design-interface/SKILL.md` or its resources), then loaded `sk-code` and used `apply_patch` to edit this repo's own `README.md` (hero markup), confirmed by `git diff -- README.md`. | **FAIL** | `make-it-interface.md` PASS requires "load `design-interface/SKILL.md`" — never happened. Instead of interface guidance, the dispatch treated this live repo as the target and made a real, uncommitted edit; reverted via `git restore -- README.md`. |
| TV-001-V2 | TV-001 | `Make it quieter while keeping the same layout.` | `sk-design` 0.82, uncertainty 0.18, passes threshold | Text: `Mode plan: interface with foundations constraints.` Read `mode-registry.json`, `design-interface/SKILL.md`, `design-foundations/SKILL.md`, `design-mcp-open-design/SKILL.md`, `shared/register.md`, `shared/context_loading_contract.md`, `design_principles.md`, `brief_to_dials.md`, `oklch_workflow.md`; cited `interface_preflight_card.md`. Tried `open-design_get_artifact`/`open-design_list_projects`; daemon unreachable, asked for a target rather than fabricating one. No file mutation. | **PASS** | `make-it-interface.md`: "PASS iff all variants resolve interface, load `design-interface/SKILL.md`, and avoid audit framing" — all three met; interface stated as primary driver, foundations as a supporting constraint. |
| TV-001-V3 | TV-001 | `Make it distill the visual hierarchy down to fewer competing elements.` | `sk-design` 0.8368, uncertainty 0.12, passes threshold | Text: `Selected design route: foundations with a light interface pass, because the request is specifically about simplifying visual hierarchy...`. Loaded `mode-registry.json`, `design-foundations`, `design-interface`, `design-mcp-open-design` (in that order). Daemon unreachable; no file mutation. | **FAIL** | `make-it-interface.md`: "FAIL iff any variant resolves audit, foundations, motion, or md-generator" — this variant explicitly stated `foundations` as the primary resolved route (`interface` demoted to "a light pass"). Root cause: `design-interface/SKILL.md`'s own routing prose says "If hierarchy... is the main ask, route to foundations," which contradicts the scenario's cited `commandProjectionParity` mapping of `distill` to `interface` — a real packet-internal routing-rule tension, out of scope to fix here. |
| TV-001-V4 | TV-001 | `Make it delight users with one memorable interface detail.` | no match >= 0.8 (`[]`) | Text: `Route: sk-design interface + foundations, ... because the request asks for an interface change.` Loaded `sk-design`, `design-interface` (full `SKILL.md` content confirmed), `design-foundations`; cited `design_principles.md`, `brief_to_dials.md`, `interface_preflight_card.md`. Tried Open Design artifact/project lookup; daemon unreachable, asked for a target. No file mutation. | **PASS** | `make-it-interface.md` PASS bar met: interface stated as primary ("the request asks for an interface change"), `design-interface/SKILL.md` loaded with resources cited, no audit framing, applied (not asked-should-happen) but safely blocked on a missing target. Advisor-probe miss (empty `[]`) did not predict the real dispatch's correct routing. |
| TV-002-V1 | TV-002 | `Should it be bolder, or is the current hierarchy already strong enough?` | `sk-design` 0.82, uncertainty 0.22, passes threshold | Text: `Route: sk-design audit mode for a narrow hierarchy critique.` Loaded `sk-design`, `design-audit`; cited all 5 expected resources (`corpus_map.md`, `audit_contract.md`, `critique_hardening.md`, `transform_remediation.md`, `audit_report_template.md`). Answered with conditional critique guidance ("Default recommendation: don't make it globally bolder yet... Use bolder treatment only if..."), not a blind application. No file mutation. | **PASS** | `should-it-be-audit.md`: "PASS iff all variants resolve audit, load `design-audit/SKILL.md`, and answer as critique or evaluation" — all three met. |

## Wave rollup

- **TV-001 (all 4 variants)**: 2/4 PASS (V2, V4), 2/4 FAIL (V1, V3). Since `make-it-interface.md`'s own Pass/Fail Criteria is scoped per-scenario as "PASS iff **all** variants resolve interface," `TV-001` as a whole does **not** meet its scenario-level PASS bar — 2 of 4 constituent prompts diverge from the expected `interface` resolution, for two distinct root causes (V1: no-target repo-editing escalation via `sk-code`; V3: `foundations`/`interface` routing-rule tension baked into `design-interface/SKILL.md`'s own prose).
- **TV-002 (this wave's V1 only)**: 1/1 PASS. `V2`-`V4` are out of scope for this wave (dispatched by `005-transform-verb-should-it-be`); no scenario-level rollup is claimed here.
- **Repo hygiene**: 1 unintended real-repo edit occurred (`TV-001-V1` -> `README.md`), caught by post-dispatch `git status --porcelain` and reverted via `git restore` before this folder's docs were written. `git status --porcelain -- README.md` is clean as of this log's writing.
