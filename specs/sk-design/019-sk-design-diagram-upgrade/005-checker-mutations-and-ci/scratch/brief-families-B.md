GATE 3 IS PRE-RESOLVED. DO NOT ASK THE DOCUMENTATION-SCOPE QUESTION. You are a non-interactive dispatched worker with AI_SESSION_CHILD=1 and SYSTEM_SPEC_GATE_ENFORCE=0 set; the spec folder is specs/sk-design/019-sk-design-diagram-upgrade/005-checker-mutations-and-ci. Nobody is at a prompt; do not print options or stop to confirm. You are done only when the named files exist and the verification has been run and its output read. Never write a task id, finding id or spec path inside code or comments; comments say why, briefly.
PERSONA: careful Node.js author writing small pure modules against a fixed harness.

THE HARNESS (read it first, read-only): .opencode/skills/sk-design/sk-design-diagram/scripts/check-diagram-corpus.cjs. It loads every .opencode/skills/sk-design/sk-design-diagram/scripts/families/*.cjs; a module exports { name, scope: 'file' | 'corpus', run(ctx) }. For scope 'file', ctx carries: file, label, kind ('template' | 'example' | 'extra'), src, clean (html comments stripped), regions ({styles, scripts, attrs, markup}), flattenTags(markup), stripCssComments, tally(name, n), record(name, 'error', label, message), palette (the parsed token source), gates ({channel, luminance, contrast, round2}), fs, path, root. Every assertion calls tally once; every failure calls record with a message that says what and WHY, in one sentence, like the two existing modules .opencode/skills/sk-design/sk-design-diagram/scripts/families/metadata.cjs and catalog-bidirectional.cjs (read both for the shape). Verify with: node .opencode/skills/sk-design/sk-design-diagram/scripts/check-diagram-corpus.cjs — it must print RESULT: PASSED. If a family fails on the current corpus, do NOT weaken it silently and do NOT edit any asset: print the failing file and reason in your summary; the conductor decides whether the corpus or the assertion is wrong. Keep each module under 80 lines. Print the checker's summary block and a three-line report at the end.

WRITE SCOPE: four new files under .opencode/skills/sk-design/sk-design-diagram/scripts/families/: grid-4px.cjs, orthogonal-connectors.cjs, node-budget.cjs, derivation-gates.cjs. Nothing else. Read .opencode/skills/sk-design/sk-design-diagram/assets/color/diagram-palette.json (skins, roles, gates, departures) and .opencode/skills/sk-design/sk-design-diagram/references/foundations/derivation-record.md § GATES before writing derivation-gates.

grid-4px (scope file, kinds template and example): every x, y, width, height, cx, cy, rx, ry attribute on rect, circle, ellipse, line (x1,y1,x2,y2), and g transform="translate(a,b)" in regions.markup must be an integer divisible by 4. Exemptions, which are the signed rule: any attribute on a <text> or <tspan> element (font sizes and text baselines are exempt); stroke-width and opacity; the dot-pattern <pattern> element and its children; rx on a rect (a radius may be 6). Report each offending element once as "element attr=value". Note: text baselines are the flowchart's known offsets and must not be reported.

orthogonal-connectors (scope file): every <line> in the connector layer must be axis-aligned: x1==x2 or y1==y2. Exempt: lines inside an element with aria-hidden="true" (icon glyphs); lines inside a <g> whose class or id contains "spoke" or "axis" or "radar"; lines whose parent <g> has data-diagram-decoration="true". Also exempt any file whose basename is example-radar.html for its spokes. For <path> elements with class or marker attributes, check that the d attribute uses only M, L, H, V, A, Q and Z commands with L segments that are axis-aligned (allow the A arc commands for the rounded corners); a diagonal L segment is an error naming the path's id or its first 40 characters of d.

node-budget (scope file, kinds template and example): count elements carrying data-diagram-node in regions.markup and elements carrying data-diagram-arrow. A file with more than 9 tagged nodes or more than 12 tagged arrows is an error stating the counts and the budget. A file with zero tagged nodes is NOT an error today: tally the assertion and record nothing, because the corpus predates the tag; the budget binds what is tagged. Do not count raw <rect> elements.

derivation-gates (scope file, kind template only): find the block between /* DIAGRAM_PALETTE:BEGIN skin=<name> */ and /* DIAGRAM_PALETTE:END */ inside regions.styles (exactly one, else error). Parse every --color-<role>: <value>; line. For every role whose value is a six-digit hex, compute contrast through ctx.gates against the skin's ground (palette.grounds[skin], i.e. the paper role's value in the same block). Text roles ink, muted, soft must reach palette.gates.textOnPaper; every other hex role must reach palette.gates.markOnPaper; accent must reach palette.gates.accentAgainstInk against ink. Skip roles listed in palette.gates.ungated, and skip the ground itself and paper-2 / page (a ground against itself is meaningless). A ratio under its gate is an error naming role, ratio at two decimals and gate — UNLESS palette.departures has an entry with the same skin and role whose measured value matches the ratio at two decimals; then tally and record nothing. Also assert that the block's value for every role equals palette.skins[skin].roles[role].value (the template and the source must agree byte for byte), naming any drift.

VERIFY: node .opencode/skills/sk-design/sk-design-diagram/scripts/check-diagram-corpus.cjs — print the summary block. Then a negative control: copy template-full.html to /tmp/broken2/t.html, change --color-muted to #dddddd and one rect's x to 13, and run with --extra /tmp/broken2; show that derivation-gates reports muted under its gate and grid-4px reports the x. (The --extra file has kind 'extra'; make the two families run for kind 'extra' as well.)
