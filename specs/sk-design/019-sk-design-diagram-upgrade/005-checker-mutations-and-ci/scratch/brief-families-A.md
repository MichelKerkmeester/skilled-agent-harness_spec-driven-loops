GATE 3 IS PRE-RESOLVED. DO NOT ASK THE DOCUMENTATION-SCOPE QUESTION. You are a non-interactive dispatched worker with AI_SESSION_CHILD=1 and SYSTEM_SPEC_GATE_ENFORCE=0 set; the spec folder is specs/sk-design/019-sk-design-diagram-upgrade/005-checker-mutations-and-ci. Nobody is at a prompt; do not print options or stop to confirm. You are done only when the named files exist and the verification has been run and its output read. Never write a task id, finding id or spec path inside code or comments; comments say why, briefly.
PERSONA: careful Node.js author writing small pure modules against a fixed harness.

THE HARNESS (read it first, read-only): .opencode/skills/sk-design/sk-design-diagram/scripts/check-diagram-corpus.cjs. It loads every .opencode/skills/sk-design/sk-design-diagram/scripts/families/*.cjs; a module exports { name, scope: 'file' | 'corpus', run(ctx) }. For scope 'file', ctx carries: file, label, kind ('template' | 'example' | 'extra'), src, clean (html comments stripped), regions ({styles, scripts, attrs, markup}), flattenTags(markup), stripCssComments, tally(name, n), record(name, 'error', label, message), palette (the parsed token source), gates ({channel, luminance, contrast, round2}), fs, path, root. Every assertion calls tally once; every failure calls record with a message that says what and WHY, in one sentence, like the two existing modules .opencode/skills/sk-design/sk-design-diagram/scripts/families/metadata.cjs and catalog-bidirectional.cjs (read both for the shape). Verify with: node .opencode/skills/sk-design/sk-design-diagram/scripts/check-diagram-corpus.cjs — it must print RESULT: PASSED. If a family fails on the current corpus, do NOT weaken it silently and do NOT edit any asset: print the failing file and reason in your summary; the conductor decides whether the corpus or the assertion is wrong. Keep each module under 80 lines. Print the checker's summary block and a three-line report at the end.

WRITE SCOPE: four new files under .opencode/skills/sk-design/sk-design-diagram/scripts/families/: accessible-svg.cjs, unique-ids.cjs, no-external.cjs, marker-vocabulary.cjs. Nothing else.

accessible-svg (scope file): over flattenTags(regions.markup), the first <svg ...> that carries role="img" must carry aria-labelledby naming ids that both exist in the file; its first child element must be <title> (allow whitespace only between them); that <title> and the named <desc> must be non-empty; and the two ids must not be the bare words title or desc. A file with no <svg role="img"> at all is an error for kind template or example, and skipped for a specimen sheet (skip files whose path ends with assets/icons.html; note the harness does not pass icons.html today). Nested <svg> elements after the first are ignored.

unique-ids (scope file): every id="…" in regions.markup must be unique within the file; report each duplicated id once with its count. Ids inside HTML comments are already gone.

no-external (scope file): the file may reference exactly one remote host, fonts.googleapis.com (and its font files host fonts.gstatic.com), in <link rel="stylesheet" href="https://fonts.googleapis.com/…"> and @import or url() to those hosts. Any other http(s):// or protocol-relative // reference in a src, href, url(), @import, or <script src> is an error naming the reference. Read markup and styles; do not read scripts for this (a URL in a string is not a fetch).

marker-vocabulary (scope file): every <marker id="X"> defined in the file must be referenced by at least one marker-start, marker-mid or marker-end attribute or CSS property (url(#X)); and every url(#X) marker reference must resolve to a defined <marker id="X">. Defining what you do not draw, and drawing what you did not define, are both errors naming the id.

VERIFY: node .opencode/skills/sk-design/sk-design-diagram/scripts/check-diagram-corpus.cjs — print the summary block. Also run the four modules' assertions against a deliberately broken copy: copy one example to /tmp/broken/example-x.html, remove its <title>, duplicate an id, add <script src="https://cdn.example.com/a.js"></script>, and reference url(#nope); run node .opencode/skills/sk-design/sk-design-diagram/scripts/check-diagram-corpus.cjs --extra /tmp/broken and show that each of the four families reports exactly the defect you planted.
