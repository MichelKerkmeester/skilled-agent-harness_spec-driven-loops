GATE 3 IS PRE-RESOLVED. DO NOT ASK THE DOCUMENTATION-SCOPE QUESTION. You are a non-interactive dispatched worker with AI_SESSION_CHILD=1 and SYSTEM_SPEC_GATE_ENFORCE=0 set; the spec folder is specs/sk-design/019-sk-design-diagram-upgrade/003-applicator-and-sentinels. Nobody is at a prompt; do not print options or stop to confirm. You are done only when the file exists on disk and the verification below has been run and its output read.
PERSONA: you are a careful Node.js author. Write one CommonJS script, no dependencies beyond node's fs and path, no comment that names a task id, finding id or spec path. Comments say why, briefly.

WRITE SCOPE: exactly one new file — .opencode/skills/sk-design/sk-design-diagram/scripts/apply-diagram-tokens.cjs. Read these first, read-only: .opencode/skills/sk-design/sk-design-diagram/assets/color/diagram-palette.json (the token source), .opencode/skills/sk-design/sk-design-diagram/scripts/color-gates.cjs (require it by relative path './color-gates.cjs'; it exports channel, luminance, contrast, round2), and the four files under .opencode/skills/sk-design/sk-design-diagram/assets/templates/ (each carries one block between the comment lines /* DIAGRAM_PALETTE:BEGIN skin=<name> */ and /* DIAGRAM_PALETTE:END */ wrapping lines like   --color-paper:   #f5f5f5;   /* white-smoke */ ). For the CLI shape only, you may read /Users/michelkerkmeester/MEGA/Development/Code_Environment/Public/.opencode/skills/sk-design/sk-design-chart/scripts/apply-design-md.cjs — never require or copy from it.

WHAT THE SCRIPT DOES
1. CLI: node apply-diagram-tokens.cjs [--default | --source <json>] --out <dir> [--forms a,b] [--skin light|dark|terminal]. --out is required and must not be inside assets/templates. --default reads the token source named above; --source reads another JSON of the same shape. --forms limits which template basenames (without .html) are processed; default all four. --skin overrides the skin named in a file's BEGIN marker for that run.
2. For each template: find the block between the BEGIN and END markers (exactly one; refuse a file with zero or two). For every line inside it matching --color-<role>: <value>; keep the line's leading whitespace, the role, the exact spacing before the value, and any trailing /* comment */ byte for byte; replace only the value with the skin's role value from the token source. A role the file declares that the skin lacks is an error naming the role. Lines outside the block are copied untouched, so --font-* declarations never change.
3. Gates, computed per file with the ported module, against that skin's ground (the role named by grounds[skin], i.e. paper): for every role in the file's block that is a six-digit hex — not an rgba(...) value — compute contrast against the ground. Roles listed in gates.ungated are skipped. Text roles (ink, muted, soft) must reach textOnPaper 4.5; every other hex role must reach markOnPaper 3.0; accent must also reach accentAgainstInk 1.5 against the skin's ink. A failing ratio is refused with a message naming the role, the ratio at two decimals, the gate, and the value that would just clear it (search lightness toward the direction that helps, in steps of 1/255 on each channel), UNLESS a departures entry in the token source matches the skin and role and its measured value equals the computed ratio at two decimals; then print a one-line DEPARTURE note and continue.
4. Output: write the themed copy to <out>/<basename>.html. Print one line per file: WROTE <path>, then RESULT: PASSED. Any refusal prints RESULT: FAILED and exits 1 without writing partial output for that file.
5. Keep it under 220 lines. module.exports the main function and the block-substitution function so tests can call them.

VERIFY (run these, print the output):
  mkdir -p /tmp/diagram-default && node .opencode/skills/sk-design/sk-design-diagram/scripts/apply-diagram-tokens.cjs --default --out /tmp/diagram-default && diff -rq /tmp/diagram-default .opencode/skills/sk-design/sk-design-diagram/assets/templates && echo IDENTICAL
The diff must print nothing and IDENTICAL must appear. If it does not, fix the script until it does; do not touch the templates or the JSON. Then print a three-line summary: file written, line count, and the DEPARTURE lines the run printed.
