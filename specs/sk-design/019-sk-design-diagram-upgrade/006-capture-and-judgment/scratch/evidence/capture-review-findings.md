# Capture review findings, 39 captures, two fresh readers (Claude Sonnet 5)

36 of 39 pass every applicable read. Failures:
- example-dp-integration.png — focal balance: the accent is on three boxes and nearly every edge; the colour is doing duty as both a focus signal and a flow-type signal.
- example-venn.png — focal balance: no accent anywhere; the answer wedge is solid navy, which reads as a warning state rather than the answer.
- templates/template-full.png — focal balance: the demo drawing spreads the accent across five elements.
Capture discipline: the three sequence-oauth variants are cropped at the 900px viewport, so their legends are cut off; a tall diagram needs a full-page capture.
Non-blocking taste notes: it-state's CSV/EXPORT/XLSX bus is tangled; org-chart carries an unused legend entry; radar's blue series is buried under fills; loop-terminal's chrome adds weight.
