GATE 3 IS PRE-RESOLVED. DO NOT ASK THE DOCUMENTATION-SCOPE QUESTION. You are a non-interactive dispatched worker with AI_SESSION_CHILD=1 and SYSTEM_SPEC_GATE_ENFORCE=0 set; the spec folder is specs/sk-design/019-sk-design-diagram-upgrade/006-capture-and-judgment. Nobody is at a prompt; do not print options or stop to confirm. You are done only when the named files are on disk and the verification has been run and its output read. Never write a task id, finding id or spec path inside the documents; write the durable reason instead.
PERSONA: careful technical writer extending an existing testing playbook in its own voice and structure.

WRITE SCOPE: exactly two files — edit .opencode/skills/sk-design/sk-design-diagram/manual-testing-playbook/manual-testing-playbook.md, and create .opencode/skills/sk-design/sk-design-diagram/manual-testing-playbook/capture-review/capture-review.md. Nothing else. Read first: the playbook root in full (353 lines; note the MANUAL_PLAYBOOK_RESULT_PERSISTENCE_CONTRACT marker near line 25, the scenario sections 7-9 with their DIA-/IMP-/CMD- entries, and the two trailing sections 10 and 11); one scenario leaf as the shape to mirror, .opencode/skills/sk-design/sk-design-diagram/manual-testing-playbook/diagram-generation/editorial-style-and-connectors.md; and the judged-boundary comment at the top of .opencode/skills/sk-design/sk-design-diagram/scripts/check-diagram-corpus.cjs (the items the checker states it does not hold — quote them exactly, do not paraphrase).

CHANGES TO THE PLAYBOOK ROOT
1. Insert a new section "## 10. CAPTURE REVIEW (`CAP-001`)" after section 9 and renumber the two trailing sections to 11 and 12 (update any in-file references to their numbers). The section carries one entry "### CAP-001 | Capture review of the rendered corpus" in the same shape as the DIA- entries (objective, real user request, expected signals, pass/fail, evidence, leaf link to capture-review/capture-review.md).
2. In the persistence-contract note under the marker, add one sentence naming the capture-review scenario as persisted through the same runner, and one rule: every SKIP verdict carries a reason in the reason column, and a SKIP with an empty reason is a failed run.
3. Add "## 13. GRADUATION LOG" at the end: one paragraph stating that a judged read which becomes computable moves INTO the corpus checker as a named family and is logged here with its date, family and evidence, and that nothing travels the other way — a family never returns to the eye; then an empty table with columns Item | Graduated | Family | Evidence | Removed from.
4. Add CAP-001 to the cross-reference index in what is now section 12.

THE LEAF capture-review/capture-review.md, mirroring the DIA-002 leaf's frontmatter (title "CAP-001 -- Capture review of the rendered corpus", description one sentence, version 1.1.0.0) and sections. Its contract:
- The six judged reads, each a yes/no question a reader answers looking at a capture: (1) connector overlap — do any two connectors share a stroke or cross without a hop; (2) attach fan — do connectors entering the same box edge sit at least 12px apart; (3) visible gap — does every arrow label sit 6-10px clear of its line with an opaque mask; (4) behind-box — does any connector pass behind a box that is not its source or target without being dashed; (5) focal balance — is the accent on one or two elements that deserve it; (6) type fit — is this the right diagram type for the question the title asks. Two non-blocking taste notes: the remove test (what could be deleted) and density (does it need a guide to read).
- The skin-pinned third capture: alongside the stock capture, the applicator re-themes the file from a source that moves the accent, the copy is rendered, and the reader confirms the two pictures differ by eye in paper, ink or accent; a pair that reads the same means the skin did not reach the paint.
- The settled double-capture: each file is captured twice with a settle interval and the two are compared; a difference means the drawing was still moving (an animation or a late font), and the second capture is the one reviewed.
- The label-mask measurement for font substitution: the page is rendered once with web fonts and once with remote fonts disabled; each arrow-label mask rect is cropped from both with a margin; ink pixels found outside the mask's own bounds in the no-fonts render, where the with-fonts render had none, are overflow; fail threshold: any mask with overflow. Name example-high-level.html's arrow-label masks as the reference case.
- The graduation threshold for connector geometry: overlap and the attach fan may leave the judged column only when a 2D pass over parsed path geometry computes segment intersections and per-edge attach spacing for every connector in every corpus file with zero false positives on the current corpus.
- Persistence: every outcome is written through run-manual-playbook-scenario.cjs into benchmark/reports/<dated-run-label>/; the report markdown is renderer-owned and never hand-authored; every SKIP carries its reason.

VERIFY (run and print): grep -c "MANUAL_PLAYBOOK_RESULT_PERSISTENCE_CONTRACT" .opencode/skills/sk-design/sk-design-diagram/manual-testing-playbook/manual-testing-playbook.md (expect 1); grep -n "^## " .opencode/skills/sk-design/sk-design-diagram/manual-testing-playbook/manual-testing-playbook.md (expect 13 numbered sections in order); grep -c "CAP-001" .opencode/skills/sk-design/sk-design-diagram/manual-testing-playbook/manual-testing-playbook.md (expect at least 3); test -f .opencode/skills/sk-design/sk-design-diagram/manual-testing-playbook/capture-review/capture-review.md. Then a three-line summary.
