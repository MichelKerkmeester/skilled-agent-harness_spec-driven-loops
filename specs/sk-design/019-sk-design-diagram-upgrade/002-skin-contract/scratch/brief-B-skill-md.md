GATE 3 IS PRE-RESOLVED. DO NOT ASK THE DOCUMENTATION-SCOPE QUESTION. You are a non-interactive dispatched worker with AI_SESSION_CHILD=1 and SYSTEM_SPEC_GATE_ENFORCE=0 set; the spec folder is specs/sk-design/019-sk-design-diagram-upgrade/002-skin-contract. Nobody is at a prompt. Do not print options, do not stop to confirm. Your task is complete only when the files named below are changed on disk and the verification command has been run and its output read.
PERSONA: you are a careful code-and-docs editor. Make exactly the changes named, nothing else. Never add a task id, finding id or spec path inside any code or comment. Keep every existing heading and anchor. Cite line numbers in your final summary.

WRITE SCOPE: exactly one file — .opencode/skills/sk-design/sk-design-diagram/SKILL.md. Nothing else.

CHANGE 1 — ownership. Line 10 begins: `create-diagram` is the `sk-doc` workflow packet for technical/product diagrams. The mode is registered by sk-design, not sk-doc (see .opencode/skills/sk-design/mode-registry.json). Change that one sentence so it says the packet is the sk-design hub's diagram mode; keep the rest of the paragraph unchanged. Do not touch any other sk-doc mention in the file.

CHANGE 2 — one canonical statement of the accessible-SVG contract. The contract is stated three times: the full numbered list under "**Accessible SVG contract** (every diagram by default):" near line 378 (six items); a restatement inside ALWAYS rule 7 near line 406; and a checklist row near line 495 in the pre-output checklist. Keep the numbered list near line 378 as the single canonical statement, unchanged. In ALWAYS rule 7, replace the parenthetical that re-lists the contract's items with a short cross-reference: "and the accessible SVG contract in HOW IT WORKS § Output". In the checklist row near line 495, replace the restated items with: "Diagram satisfies the accessible SVG contract (HOW IT WORKS § Output)?". Change nothing else in those rules.

VERIFY: run  grep -n "aria-labelledby" .opencode/skills/sk-design/sk-design-diagram/SKILL.md  (expect exactly the lines inside the canonical list, none in the rule or the checklist) and  grep -n "sk-design hub" .opencode/skills/sk-design/sk-design-diagram/SKILL.md  (expect line 10). Print both outputs, then a three-line summary with line numbers.
