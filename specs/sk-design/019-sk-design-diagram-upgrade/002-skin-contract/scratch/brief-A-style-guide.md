GATE 3 IS PRE-RESOLVED. DO NOT ASK THE DOCUMENTATION-SCOPE QUESTION. You are a non-interactive dispatched worker with AI_SESSION_CHILD=1 and SYSTEM_SPEC_GATE_ENFORCE=0 set; the spec folder is specs/sk-design/019-sk-design-diagram-upgrade/002-skin-contract. Nobody is at a prompt. Do not print options, do not stop to confirm. Your task is complete only when the files named below are changed on disk and the verification command has been run and its output read.
PERSONA: you are a careful code-and-docs editor. Make exactly the changes named, nothing else. Never add a task id, finding id or spec path inside any code or comment. Keep every existing heading and anchor. Cite line numbers in your final summary.

WRITE SCOPE: exactly two files — .opencode/skills/sk-design/sk-design-diagram/references/foundations/style-guide.md (edit) and .opencode/skills/sk-design/sk-design-diagram/references/foundations/derivation-record.md (create). Nothing else.

CHANGE 1 — document the fallback chains that already ship. In style-guide.md section "## 2. TYPOGRAPHY", the table's Family column names bare families. The four templates already declare fallback chains at .opencode/skills/sk-design/sk-design-diagram/assets/templates/template.html lines 15-17: --font-sans: 'Geist', system-ui, sans-serif; --font-serif: 'Instrument Serif', serif; --font-mono: 'Geist Mono', ui-monospace, monospace. Add one short paragraph directly under the "### Font stack" code block that states these three chains verbatim, says every template root and every inline SVG font-family already carries them, and that a file opened with no network therefore renders in the fallback rather than in a browser default. Do not add any fallback code anywhere; this is documentation of what exists.

CHANGE 2 — retire the warm inversion rule. style-guide.md has a section "#### Inversion rule (light → dark)" stating that rgba(28,25,23, X) becomes rgba(250,247,242, X). Zero of the 34 examples use those warm values; 32 of 34 use the cool rgba(45,49,66, X). Replace that section's body with two sentences: the dark values are hand-picked, not derived by a formula (the accent moves from #eb6c36 to #f08a59 by hue, saturation and lightness together, so no single rule reproduces it), and the record of every value and its origin lives in derivation-record.md (link it as [derivation-record.md](derivation-record.md)).

CHANGE 3 — create derivation-record.md next to style-guide.md, with frontmatter (title "Diagram Palette Derivation Record", description one sentence, trigger_phrases three short phrases, importance_tier "important", contextType "reference", version 1.0.0.0). Body sections, in this order, with numbered H2 headings like the sibling file:
 1. OVERVIEW — what the record is: every colour role in one of three lists (light, dark, terminal), each value in one of four kinds: primary (a value chosen by hand, stated verbatim), derived (computed from a primary by a stated rule, e.g. ink at an alpha), fixed (a value that never re-themes), untokenized (a file that carries its own fixed skin and is exempt from the record — today only assets/examples/example-sequence-oauth-dark.html). Say the applicator of a later phase reads this file and the corpus checker re-derives every derived value from it.
 2. LIGHT — a table Role | Value | Kind | Rule or source, filled from style-guide.md's role table (paper #f5f5f5, paper-2 #ececec, ink #2d3142, muted #4f5d75, soft #7a8399, rule rgba(45,49,66,0.12) derived = ink at 0.12, rule-solid #bfc0c0, accent #eb6c36, accent-tint rgba(235,108,54,0.08) derived = accent at 0.08, link #2e5aa8), plus two rows the guide lacks: backend-fill #ffffff primary ("the backend/API/step node fill; 40 occurrences in 13 files; never a paper substitute") and high-level-chevron #3d4460 primary, type-scoped to type-high-level only.
 3. DARK — the same table from the guide's dark column (paper #2d3142, paper-2 #393e53, ink #f5f5f5, muted #bfc0c0, soft #8e98ac, rule rgba(245,245,245,0.12) derived = ink at 0.12, rule-solid rgba(191,192,192,0.25) derived = muted at 0.25, accent #f08a59 primary with the note "hand-picked; not a lightness shift of the light accent", accent-tint rgba(240,138,89,0.10) derived, link #6a95d8).
 4. TERMINAL — the guide's terminal table (terminal-page #0a0a0a, terminal-paper #141414, terminal-bar #1b1b1b, terminal-border #2b2b2b, terminal-ink #f5f5f5, terminal-muted #9a9a9a, terminal-soft #5c5c5c, terminal-accent #ff5a36, terminal-accent-tint rgba(255,90,54,0.12) derived = terminal-accent at 0.12).
 5. GATES — a table Gate | Threshold | Applies to: text-on-paper 4.5:1 (ink, muted, and any text role against its ground); mark-on-paper 3.0:1 (any stroke or fill that carries meaning); accent-against-ink 1.5:1; hairlines ungated (rule, rule-solid — decoration, reviewed by eye). Then a "Recorded departures" table with one row: accent #eb6c36 on #f5f5f5 measures 2.863:1 against the 3.0:1 mark gate; kept as the brand accent by decision; never re-derived. And one row for soft #7a8399 at 3.48:1: may not carry sublabel or eyebrow text.
 6. PINS — a table File | sha256 listing the four template files under .opencode/skills/sk-design/sk-design-diagram/assets/templates/ with their real sha256 (compute each with: shasum -a 256 <file>). State that these four files are the stock skin's source of values and that a pin that stops matching means a template changed without this record being updated.
 7. TOLERANCES — one short paragraph: derived alphas are exact; contrast ratios are compared at two decimals.
Keep it under 160 lines. Use plain prose, no marketing.

VERIFY: run  grep -c "derivation-record.md" .opencode/skills/sk-design/sk-design-diagram/references/foundations/style-guide.md  (expect at least 1) and  shasum -a 256 .opencode/skills/sk-design/sk-design-diagram/assets/templates/*.html  and confirm the PINS table matches. Print the two commands' output, then a three-line summary of what changed with line numbers.
