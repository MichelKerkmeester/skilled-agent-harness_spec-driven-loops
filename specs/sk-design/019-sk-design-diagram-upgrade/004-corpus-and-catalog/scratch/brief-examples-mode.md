GATE 3 IS PRE-RESOLVED. DO NOT ASK THE DOCUMENTATION-SCOPE QUESTION. You are a non-interactive dispatched worker with AI_SESSION_CHILD=1 and SYSTEM_SPEC_GATE_ENFORCE=0 set; the spec folder is specs/sk-design/019-sk-design-diagram-upgrade/004-corpus-and-catalog. Nobody is at a prompt; do not print options or stop to confirm. You are done only when the named files are on disk and the verification has been run and its output read. Never write a task id, finding id or spec path inside code or comments.
PERSONA: careful Node.js author extending an existing script without changing its template behaviour.

WRITE SCOPE: exactly one file — .opencode/skills/sk-design/sk-design-diagram/scripts/apply-diagram-tokens.cjs (edit). Read first: that script; .opencode/skills/sk-design/sk-design-diagram/assets/color/diagram-palette.json (note the new "examples" object: skinByFile, defaultSkin, untokenized; and the light skin now carries backend-fill, high-level-chevron, series-1..5); and two examples, .opencode/skills/sk-design/sk-design-diagram/assets/examples/example-architecture.html and .opencode/skills/sk-design/sk-design-diagram/assets/examples/example-loop-terminal.html (they carry NO sentinel block; their colours are hex literals in SVG attributes like fill="#4f5d75" and inside <style> blocks).

CHANGE — add an examples mode. Flag --examples processes every .html under .opencode/skills/sk-design/sk-design-diagram/assets/examples/ (still writing copies to --out; --forms filters by basename as before). For each example: skip it, printing SKIP <name> untokenized, if its basename is in examples.untokenized; otherwise its skin is examples.skinByFile[basename] or examples.defaultSkin. Build the map from each of that skin's role values (lower-cased hex) to the role name. Then rewrite the file by replacing every six-digit hex literal (case-insensitive, whole token: a # followed by exactly six hex digits) with the target skin's value for the role it maps to, preserving everything else byte for byte. A literal that maps to no role of that skin is a refusal naming the file and the value; do not write that file. In --default mode every literal maps to its own value, so the copy must be byte-identical to the source. Run the same gate computation as for templates over the roles the file actually uses. Keep the template path untouched: --default --out over the templates must still be byte-identical.

VERIFY (run and print):
  rm -rf /tmp/dx && node .opencode/skills/sk-design/sk-design-diagram/scripts/apply-diagram-tokens.cjs --default --examples --out /tmp/dx | tail -3 && diff -rq /tmp/dx .opencode/skills/sk-design/sk-design-diagram/assets/examples && echo EXAMPLES-IDENTICAL
  rm -rf /tmp/dt && node .opencode/skills/sk-design/sk-design-diagram/scripts/apply-diagram-tokens.cjs --default --out /tmp/dt | tail -1 && diff -rq /tmp/dt .opencode/skills/sk-design/sk-design-diagram/assets/templates && echo TEMPLATES-IDENTICAL
Both IDENTICAL lines must print (the untokenized file is skipped, so exclude it from the first diff with --exclude=example-sequence-oauth-dark.html). Fix the script until they do; never touch the JSON or the examples. Then print a three-line summary with the new line count.
