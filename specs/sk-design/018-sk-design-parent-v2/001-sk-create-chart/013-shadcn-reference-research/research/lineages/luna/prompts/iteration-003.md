# Iteration 3 Prompt

Compare shadcn's tooltip and pointer contract with the standalone corpus. Inspect `indicator`, `hideLabel`, `hideIndicator`, `labelFormatter`, `formatter`, `nameKey`, and `labelKey`; count accessibility-layer coverage; compare the role/table floor, inert versus interactive registers, pointer reach, and keyboard handlers. Attempt an actual keyboard walk if a browser is available, and explicitly report if it is not. Do not infer runtime behavior from source alone.
