# Deep research iteration 5 prompt

Focus: missing micro-forms and checker assertions.

Read the current question-first catalog, all 26 current template identities and
the checker contract before comparing references. Inspect the local Tremor
tracker, bar-list and spark captures, LayerChart captures 01 through 04, the
Vercel analytics sparkline and the Apple ring or health captures. Decide which
missing forms answer a distinct reader question rather than duplicating an
existing `progress-single`, `unit-ring`, `bar-rows` or `daily-line` form. Then
sort the conclusions from angles 1 through 4 into rules the checker can
enforce, with the exact existing assertion names and a concrete failure that
each rule would catch, versus judgements that must stay per-template.

Do not propose code, dependencies, framework changes, remote fetches or a
palette change. The curated local library and repository files are the only
evidence surface. Keep the distinction between an existing checker assertion
and a proposed new assertion explicit.

Required output sections:

## Iteration 5 — Missing micro-forms and checker assertions
### What was read
### What was seen or measured
### Findings
### Recommendations
### What this iteration could not settle

Every finding must carry a local source citation. Record ruled-out directions,
newInfoRatio and a one-sentence novelty justification.
