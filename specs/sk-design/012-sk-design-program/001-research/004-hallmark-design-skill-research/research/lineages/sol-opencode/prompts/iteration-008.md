DEEP-RESEARCH
Resolved route: mode=research; target_agent=@deep-research; execution=single_iteration; state_source=externalized_files; do_not_switch_mode=true

# Deep-Research Iteration 8

Read state and prior findings first. Execute one LEAF iteration only.

## Focus
Evaluate Hallmark ROADMAP proposals for `hallmark variant`, brand-first flow -> generated design.md, theme-aware motion tokens, structural cookbook, codebase study, multi-page coherence, explainability/negative capability, and emotion-first prompting against actual shipped sk-design modes and `/interface:*` commands. Determine which are already shipped, which deserve surgical strengthening in existing commands/references, which are genuinely new, and which should be skipped. Reconcile with prior conclusions: no separate redesign/variant command, curated themes cannot become presets, measured extraction remains canonical, and semantic motion modifiers belong in existing assets. Produce roadmap rows with exact target paths and phased dependencies.

## Writes
Only `iterations/iteration-008.md`, one append-safe run-8 state record, and `deltas/iter-008.jsonl` inside the detached lineage. Use direct write-once files and byte-preserving append; do not use combined/context-dependent patches. No other writes or sub-agents. Max 12 calls.

Include Roadmap Matrix, Candidate Matrix, Ruled Out, Dead Ends, exact citations, value/effort/licensing, and next focus. Canonical route proof exact with target `deep-research`; delta first equals state; verify unique run 8.
