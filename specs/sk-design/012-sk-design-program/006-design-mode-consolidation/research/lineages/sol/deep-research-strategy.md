# Deep Research Strategy — sk-design Refinement

## 1. OVERVIEW

Investigate the current consolidated sk-design hub for concrete simplifications, removable ceremony, doctrine drift, and retirement-created capability gaps. Recommendations must solve a current evidenced problem, state cost, and use the smallest change. New abstractions, schemas, ceremony, and speculative future-proofing are out of scope.

## 2. TOPIC

Refinement opportunities after retiring `/interface:audit`, `/interface:foundations`, and `commandSubworkflows`, with four registered modes, three public commands, foundations folded into interface, seven binary mechanical preflight checks, and a storage-neutral styles facade over 7,812 files.

<!-- ANCHOR:key-questions -->
## 3. KEY QUESTIONS (remaining)
- [x] Which current hub artifacts still carry duplicated or retired-surface ceremony that imposes a concrete maintenance or routing cost?
- [x] Where does the current hub drift from sk-doc create-skill parent-hub doctrine, and which drift is worth fixing now?
- [x] Did folding foundations and audit into interface create a present capability or proof gap that the seven binary preflight checks do not cover?
- [x] Does the storage-neutral styles facade or 7,812-file package create a current operational burden that can be reduced without a new abstraction?
- [x] Which evidence-backed refinements rank highest by value-to-cost, and which plausible proposals are explicitly not worth doing?

<!-- /ANCHOR:key-questions -->

## 4. NON-GOALS

- Do not implement any refinement.
- Do not invent a new mode, command, registry field, schema, procedure layer, or compatibility alias.
- Do not propose speculative future-proofing.
- Do not revisit retired commands merely to preserve transition compatibility.
- Do not modify files outside this lineage packet.

## 5. STOP CONDITIONS

- Complete exactly five iterations because `stopPolicy=max-iterations`.
- Each iteration must use a distinct focus and preserve negative knowledge.
- Synthesis must rank recommendations by value-to-cost, state implementation cost, and list proposals not worth doing.

<!-- ANCHOR:answered-questions -->
## 6. ANSWERED QUESTIONS
- Which current hub artifacts still carry duplicated or retired-surface ceremony that imposes a concrete maintenance or routing cost?
- Where does the current hub drift from sk-doc create-skill parent-hub doctrine, and which drift is worth fixing now?
- Did folding foundations and audit into interface create a present capability or proof gap that the seven binary preflight checks do not cover?
- Does the storage-neutral styles facade or 7,812-file package create a current operational burden that can be reduced without a new abstraction?
- Which evidence-backed refinements rank highest by value-to-cost, and which plausible proposals are explicitly not worth doing?

<!-- /ANCHOR:answered-questions -->

<!-- MACHINE-OWNED: START -->
<!-- ANCHOR:what-worked -->
## 7. WHAT WORKED
- separating active files from history prevented obsolete benchmark/changelog evidence from inflating the cleanup list. (iteration 1)
- pairing file statistics with actual engine commands separated storage scale from operational failure. (iteration 2)
- comparing command claims with the exact proof surfaces separated a real measurement gap from a request to restore deleted commands. (iteration 3)
- comparing each duplicated literal to its declared authority exposed one surgical deletion without inventing a consolidation layer. (iteration 4)
- requiring a reproduced present problem for every action kept the final list deletion-heavy and bounded. (iteration 5)

<!-- /ANCHOR:what-worked -->

<!-- ANCHOR:what-failed -->
## 8. WHAT FAILED
- an unrestricted retired-term search crossed the 7,812-file styles corpus and historical benchmark payloads, producing noise. (iteration 1)
- reading the root README beyond its first table rows added no signal because it mechanically repeats the broken pattern. (iteration 2)
- a broad keyword search included auditability prose and historical material, so the query was narrowed to live loadable resources and explicit owner labels. (iteration 3)
- repository-wide consumer search crossed historical research logs, so only current command and skill sources were admitted as evidence. (iteration 4)
- validators alone could not detect semantic overclaim or stale prose; exact contract comparisons remained necessary. (iteration 5)

<!-- /ANCHOR:what-failed -->

<!-- ANCHOR:exhausted-approaches -->
## 9. EXHAUSTED APPROACHES (do not retry)
### **Add severity, confidence, or evidence fields to the mechanical card:** the public command already requires an Evidence Ledger. -- BLOCKED (iteration 3, 1 attempts)
- What was tried: **Add severity, confidence, or evidence fields to the mechanical card:** the public command already requires an Evidence Ledger.
- Why blocked: Repeated iteration evidence ruled this direction out.
- Do NOT retry: **Add severity, confidence, or evidence fields to the mechanical card:** the public command already requires an Evidence Ledger.

### **Delete all presentation assets:** confirm mode has a current bundled-intake display responsibility. -- BLOCKED (iteration 4, 1 attempts)
- What was tried: **Delete all presentation assets:** confirm mode has a current bundled-intake display responsibility.
- Why blocked: Repeated iteration evidence ruled this direction out.
- Do NOT retry: **Delete all presentation assets:** confirm mode has a current bundled-intake display responsibility.

### **Enable the persistent backend by default:** the fallback works and no performance target was supplied. -- BLOCKED (iteration 2, 1 attempts)
- What was tried: **Enable the persistent backend by default:** the fallback works and no performance target was supplied.
- Why blocked: Repeated iteration evidence ruled this direction out.
- Do NOT retry: **Enable the persistent backend by default:** the fallback works and no performance target was supplied.

### **Further `commandSubworkflows` cleanup:** no active implementation remains. -- BLOCKED (iteration 4, 1 attempts)
- What was tried: **Further `commandSubworkflows` cleanup:** no active implementation remains.
- Why blocked: Repeated iteration evidence ruled this direction out.
- Do NOT retry: **Further `commandSubworkflows` cleanup:** no active implementation remains.

### **Grant Bash/browser tools to interface:** existing `sk-code` and browser integration own measured runtime proof. -- BLOCKED (iteration 3, 1 attempts)
- What was tried: **Grant Bash/browser tools to interface:** existing `sk-code` and browser integration own measured runtime proof.
- Why blocked: Repeated iteration evidence ruled this direction out.
- Do NOT retry: **Grant Bash/browser tools to interface:** existing `sk-code` and browser integration own measured runtime proof.

### **Merge auto and confirm YAMLs:** requires conditional workflow semantics or a new shared template. -- BLOCKED (iteration 4, 1 attempts)
- What was tried: **Merge auto and confirm YAMLs:** requires conditional workflow semantics or a new shared template.
- Why blocked: Repeated iteration evidence ruled this direction out.
- Do NOT retry: **Merge auto and confirm YAMLs:** requires conditional workflow semantics or a new shared template.

### **Migrate to database-only storage:** no measured failure justifies removing the flat authority. -- BLOCKED (iteration 2, 1 attempts)
- What was tried: **Migrate to database-only storage:** no measured failure justifies removing the flat authority.
- Why blocked: Repeated iteration evidence ruled this direction out.
- Do NOT retry: **Migrate to database-only storage:** no measured failure justifies removing the flat authority.

### **Not worth doing: add checklist evidence/severity schema or grant interface runtime tools.** The existing Evidence Ledger and `sk-code` boundary cover those jobs. -- BLOCKED (iteration 5, 1 attempts)
- What was tried: **Not worth doing: add checklist evidence/severity schema or grant interface runtime tools.** The existing Evidence Ledger and `sk-code` boundary cover those jobs.
- Why blocked: Repeated iteration evidence ruled this direction out.
- Do NOT retry: **Not worth doing: add checklist evidence/severity schema or grant interface runtime tools.** The existing Evidence Ledger and `sk-code` boundary cover those jobs.

### **Not worth doing: merge auto/confirm YAMLs or delete presentation assets.** That creates conditional workflow abstraction and removes real confirm-mode behavior. -- BLOCKED (iteration 5, 1 attempts)
- What was tried: **Not worth doing: merge auto/confirm YAMLs or delete presentation assets.** That creates conditional workflow abstraction and removes real confirm-mode behavior.
- Why blocked: Repeated iteration evidence ruled this direction out.
- Do NOT retry: **Not worth doing: merge auto/confirm YAMLs or delete presentation assets.** That creates conditional workflow abstraction and removes real confirm-mode behavior.

### **Not worth doing: more `commandSubworkflows` cleanup or historical-term scrubbing.** Active machinery is absent; history is provenance. -- BLOCKED (iteration 5, 1 attempts)
- What was tried: **Not worth doing: more `commandSubworkflows` cleanup or historical-term scrubbing.** Active machinery is absent; history is provenance.
- Why blocked: Repeated iteration evidence ruled this direction out.
- Do NOT retry: **Not worth doing: more `commandSubworkflows` cleanup or historical-term scrubbing.** Active machinery is absent; history is provenance.

### **Not worth doing: redesign the registry/hub, delete procedure folders, or remove Open Design transport.** Current topology validates and those surfaces have distinct present roles. -- BLOCKED (iteration 5, 1 attempts)
- What was tried: **Not worth doing: redesign the registry/hub, delete procedure folders, or remove Open Design transport.** Current topology validates and those surfaces have distinct present roles.
- Why blocked: Repeated iteration evidence ruled this direction out.
- Do NOT retry: **Not worth doing: redesign the registry/hub, delete procedure folders, or remove Open Design transport.** Current topology validates and those surfaces have distinct present roles.

### **Not worth doing: remove the styles facade, migrate to database-only storage, prune/remote-fetch the corpus, or enable persistent storage by default.** No measured storage failure exists. -- BLOCKED (iteration 5, 1 attempts)
- What was tried: **Not worth doing: remove the styles facade, migrate to database-only storage, prune/remote-fetch the corpus, or enable persistent storage by default.** No measured storage failure exists.
- Why blocked: Repeated iteration evidence ruled this direction out.
- Do NOT retry: **Not worth doing: remove the styles facade, migrate to database-only storage, prune/remote-fetch the corpus, or enable persistent storage by default.** No measured storage failure exists.

### **Not worth doing: restore audit or foundations commands/modes/subworkflows.** Capabilities are reachable from interface. -- BLOCKED (iteration 5, 1 attempts)
- What was tried: **Not worth doing: restore audit or foundations commands/modes/subworkflows.** Capabilities are reachable from interface.
- Why blocked: Repeated iteration evidence ruled this direction out.
- Do NOT retry: **Not worth doing: restore audit or foundations commands/modes/subworkflows.** Capabilities are reachable from interface.

### **Prune or remotely fetch the corpus:** would add selection policy, availability, and migration ceremony. -- BLOCKED (iteration 2, 1 attempts)
- What was tried: **Prune or remotely fetch the corpus:** would add selection policy, availability, and migration ceremony.
- Why blocked: Repeated iteration evidence ruled this direction out.
- Do NOT retry: **Prune or remotely fetch the corpus:** would add selection policy, availability, and migration ceremony.

### **Rebuild the hub or redesign the registry schema:** validation passes and the core one-hub/one-registry topology conforms. -- BLOCKED (iteration 1, 1 attempts)
- What was tried: **Rebuild the hub or redesign the registry schema:** validation passes and the core one-hub/one-registry topology conforms.
- Why blocked: Repeated iteration evidence ruled this direction out.
- Do NOT retry: **Rebuild the hub or redesign the registry schema:** validation passes and the core one-hub/one-registry topology conforms.

### **Rebuild the hub to remove all repetition:** package structure validates and repetition alone is not a runtime defect. -- BLOCKED (iteration 4, 1 attempts)
- What was tried: **Rebuild the hub to remove all repetition:** package structure validates and repetition alone is not a runtime defect.
- Why blocked: Repeated iteration evidence ruled this direction out.
- Do NOT retry: **Rebuild the hub to remove all repetition:** package structure validates and repetition alone is not a runtime defect.

### **Remove or reclassify the Open Design transport axis:** it represents current distinct routing semantics and would require more change than it saves. -- BLOCKED (iteration 1, 1 attempts)
- What was tried: **Remove or reclassify the Open Design transport axis:** it represents current distinct routing semantics and would require more change than it saves.
- Why blocked: Repeated iteration evidence ruled this direction out.
- Do NOT retry: **Remove or reclassify the Open Design transport axis:** it represents current distinct routing semantics and would require more change than it saves.

### **Remove procedure folders:** each workflow packet has concrete, separately triggered procedures, which sk-doc explicitly permits. -- BLOCKED (iteration 1, 1 attempts)
- What was tried: **Remove procedure folders:** each workflow packet has concrete, separately triggered procedures, which sk-doc explicitly permits.
- Why blocked: Repeated iteration evidence ruled this direction out.
- Do NOT retry: **Remove procedure folders:** each workflow packet has concrete, separately triggered procedures, which sk-doc explicitly permits.

### **Remove the storage-neutral facade:** it is the smallest existing isolation seam and has live consumers. -- BLOCKED (iteration 2, 1 attempts)
- What was tried: **Remove the storage-neutral facade:** it is the smallest existing isolation seam and has live consumers.
- Why blocked: Repeated iteration evidence ruled this direction out.
- Do NOT retry: **Remove the storage-neutral facade:** it is the smallest existing isolation seam and has live consumers.

### **Restore `/interface:foundations` or an internal foundations subworkflow:** all current static-system resources are reachable from interface. -- BLOCKED (iteration 3, 1 attempts)
- What was tried: **Restore `/interface:foundations` or an internal foundations subworkflow:** all current static-system resources are reachable from interface.
- Why blocked: Repeated iteration evidence ruled this direction out.
- Do NOT retry: **Restore `/interface:foundations` or an internal foundations subworkflow:** all current static-system resources are reachable from interface.

### **Restore an audit mode:** current review capability exists; the defects are overclaimed proof and stale ownership labels. -- BLOCKED (iteration 3, 1 attempts)
- What was tried: **Restore an audit mode:** current review capability exists; the defects are overclaimed proof and stale ownership labels.
- Why blocked: Repeated iteration evidence ruled this direction out.
- Do NOT retry: **Restore an audit mode:** current review capability exists; the defects are overclaimed proof and stale ownership labels.

### `commandSubworkflows` produced no active-source hit and should not be reintroduced as a cleanup target. -- BLOCKED (iteration 1, 1 attempts)
- What was tried: `commandSubworkflows` produced no active-source hit and should not be reintroduced as a cleanup target.
- Why blocked: Repeated iteration evidence ruled this direction out.
- Do NOT retry: `commandSubworkflows` produced no active-source hit and should not be reintroduced as a cleanup target.

### Broad retired-term cleanup would conflate real topology labels with ordinary auditability language. -- BLOCKED (iteration 5, 1 attempts)
- What was tried: Broad retired-term cleanup would conflate real topology labels with ordinary auditability language.
- Why blocked: Repeated iteration evidence ruled this direction out.
- Do NOT retry: Broad retired-term cleanup would conflate real topology labels with ordinary auditability language.

### Counting card boxes does not establish coverage quality; comparing claimed proof to the card and quality-floor boundaries does. -- BLOCKED (iteration 3, 1 attempts)
- What was tried: Counting card boxes does not establish coverage quality; comparing claimed proof to the card and quality-floor boundaries does.
- Why blocked: Repeated iteration evidence ruled this direction out.
- Do NOT retry: Counting card boxes does not establish coverage quality; comparing claimed proof to the card and quality-floor boundaries does.

### Counting files proves scale, not harm; it cannot justify a migration by itself. -- BLOCKED (iteration 2, 1 attempts)
- What was tried: Counting files proves scale, not harm; it cannot justify a migration by itself.
- Why blocked: Repeated iteration evidence ruled this direction out.
- Do NOT retry: Counting files proves scale, not harm; it cannot justify a migration by itself.

### Historical changelog and benchmark hits for retired modes were not treated as live drift; they are provenance, not runtime authority. -- BLOCKED (iteration 1, 1 attempts)
- What was tried: Historical changelog and benchmark hits for retired modes were not treated as live drift; they are provenance, not runtime authority.
- Why blocked: Repeated iteration evidence ruled this direction out.
- Do NOT retry: Historical changelog and benchmark hits for retired modes were not treated as live drift; they are provenance, not runtime authority.

### Measuring lines and bytes identified review surface, not value; the recommendation emerged only after finding a contradictory duplicated lane enum. -- BLOCKED (iteration 4, 1 attempts)
- What was tried: Measuring lines and bytes identified review surface, not value; the recommendation emerged only after finding a contradictory duplicated lane enum.
- Why blocked: Repeated iteration evidence ruled this direction out.
- Do NOT retry: Measuring lines and bytes identified review surface, not value; the recommendation emerged only after finding a contradictory duplicated lane enum.

### Re-running route/package/storage checks found no hidden structural failure; further validation would repeat established evidence. -- BLOCKED (iteration 5, 1 attempts)
- What was tried: Re-running route/package/storage checks found no hidden structural failure; further validation would repeat established evidence.
- Why blocked: Repeated iteration evidence ruled this direction out.
- Do NOT retry: Re-running route/package/storage checks found no hidden structural failure; further validation would repeat established evidence.

### Searching all `.opencode` consumers pulled historical specs and logs; those do not prove a current runtime dependency. -- BLOCKED (iteration 4, 1 attempts)
- What was tried: Searching all `.opencode` consumers pulled historical specs and logs; those do not prove a current runtime dependency.
- Why blocked: Repeated iteration evidence ruled this direction out.
- Do NOT retry: Searching all `.opencode` consumers pulled historical specs and logs; those do not prove a current runtime dependency.

### The user-provided “7,812 files” count is accurate, so no further inventory reconciliation is needed. -- BLOCKED (iteration 2, 1 attempts)
- What was tried: The user-provided “7,812 files” count is accurate, so no further inventory reconciliation is needed.
- Why blocked: Repeated iteration evidence ruled this direction out.
- Do NOT retry: The user-provided “7,812 files” count is accurate, so no further inventory reconciliation is needed.

### Treating every occurrence of “audit” as stale would wrongly flag ordinary auditability language and historical provenance. -- BLOCKED (iteration 3, 1 attempts)
- What was tried: Treating every occurrence of “audit” as stale would wrongly flag ordinary auditability language and historical provenance.
- Why blocked: Repeated iteration evidence ruled this direction out.
- Do NOT retry: Treating every occurrence of “audit” as stale would wrongly flag ordinary auditability language and historical provenance.

<!-- /ANCHOR:exhausted-approaches -->

<!-- ANCHOR:ruled-out-directions -->
## 10. RULED OUT DIRECTIONS
- **Rebuild the hub or redesign the registry schema:** validation passes and the core one-hub/one-registry topology conforms. (iteration 1)
- **Remove or reclassify the Open Design transport axis:** it represents current distinct routing semantics and would require more change than it saves. (iteration 1)
- **Remove procedure folders:** each workflow packet has concrete, separately triggered procedures, which sk-doc explicitly permits. (iteration 1)
- `commandSubworkflows` produced no active-source hit and should not be reintroduced as a cleanup target. (iteration 1)
- Historical changelog and benchmark hits for retired modes were not treated as live drift; they are provenance, not runtime authority. (iteration 1)
- **Enable the persistent backend by default:** the fallback works and no performance target was supplied. (iteration 2)
- **Migrate to database-only storage:** no measured failure justifies removing the flat authority. (iteration 2)
- **Prune or remotely fetch the corpus:** would add selection policy, availability, and migration ceremony. (iteration 2)
- **Remove the storage-neutral facade:** it is the smallest existing isolation seam and has live consumers. (iteration 2)
- Counting files proves scale, not harm; it cannot justify a migration by itself. (iteration 2)
- The user-provided “7,812 files” count is accurate, so no further inventory reconciliation is needed. (iteration 2)
- **Add severity, confidence, or evidence fields to the mechanical card:** the public command already requires an Evidence Ledger. (iteration 3)
- **Grant Bash/browser tools to interface:** existing `sk-code` and browser integration own measured runtime proof. (iteration 3)
- **Restore `/interface:foundations` or an internal foundations subworkflow:** all current static-system resources are reachable from interface. (iteration 3)
- **Restore an audit mode:** current review capability exists; the defects are overclaimed proof and stale ownership labels. (iteration 3)
- Counting card boxes does not establish coverage quality; comparing claimed proof to the card and quality-floor boundaries does. (iteration 3)
- Treating every occurrence of “audit” as stale would wrongly flag ordinary auditability language and historical provenance. (iteration 3)
- **Delete all presentation assets:** confirm mode has a current bundled-intake display responsibility. (iteration 4)
- **Further `commandSubworkflows` cleanup:** no active implementation remains. (iteration 4)
- **Merge auto and confirm YAMLs:** requires conditional workflow semantics or a new shared template. (iteration 4)
- **Rebuild the hub to remove all repetition:** package structure validates and repetition alone is not a runtime defect. (iteration 4)
- Measuring lines and bytes identified review surface, not value; the recommendation emerged only after finding a contradictory duplicated lane enum. (iteration 4)
- Searching all `.opencode` consumers pulled historical specs and logs; those do not prove a current runtime dependency. (iteration 4)
- **Not worth doing: add checklist evidence/severity schema or grant interface runtime tools.** The existing Evidence Ledger and `sk-code` boundary cover those jobs. (iteration 5)
- **Not worth doing: merge auto/confirm YAMLs or delete presentation assets.** That creates conditional workflow abstraction and removes real confirm-mode behavior. (iteration 5)
- **Not worth doing: more `commandSubworkflows` cleanup or historical-term scrubbing.** Active machinery is absent; history is provenance. (iteration 5)
- **Not worth doing: redesign the registry/hub, delete procedure folders, or remove Open Design transport.** Current topology validates and those surfaces have distinct present roles. (iteration 5)
- **Not worth doing: remove the styles facade, migrate to database-only storage, prune/remote-fetch the corpus, or enable persistent storage by default.** No measured storage failure exists. (iteration 5)
- **Not worth doing: restore audit or foundations commands/modes/subworkflows.** Capabilities are reachable from interface. (iteration 5)
- Broad retired-term cleanup would conflate real topology labels with ordinary auditability language. (iteration 5)
- Re-running route/package/storage checks found no hidden structural failure; further validation would repeat established evidence. (iteration 5)

<!-- /ANCHOR:ruled-out-directions -->

<!-- ANCHOR:divergence-frontier -->
## 10A. SATURATED DIRECTIONS AND DIVERGENCE FRONTIER
- Completed pivots: 0
- Failed pivots: 0
- Audited overrides: 0
- Saturated: none yet
- Pivot lineage: none yet
- Remaining frontier: none recorded

<!-- /ANCHOR:divergence-frontier -->

<!-- ANCHOR:carried-forward-open-questions -->
## 11A. CARRIED-FORWARD OPEN QUESTIONS
- Whether the folded interface preflight retains audit/foundations capability at adequate depth. (iteration 1)
- Final value-to-cost ranking across all evidence. (iteration 1)
- Whether the styles facade creates a current operational burden. (iteration 1)
- Whether folded audit/foundations coverage has a present proof gap. (iteration 2)
- Final recommendation ranking and rejection list. (iteration 2)
- Final value-to-cost ranking and explicit rejection list. (iteration 3)
- Which current duplicated workflow or presentation assets impose a concrete maintenance cost rather than merely having similar shapes? (iteration 3)
- Which evidence-backed refinements rank highest by value-to-cost, and which plausible proposals are explicitly not worth doing? (iteration 4)
- None within the research topic. (iteration 5)

<!-- /ANCHOR:carried-forward-open-questions -->

<!-- ANCHOR:next-focus -->
## 11. NEXT FOCUS
[All tracked questions are resolved]

<!-- /ANCHOR:next-focus -->

<!-- MACHINE-OWNED: END -->

## 12. KNOWN CONTEXT

- Source pointers: `.opencode/skills/sk-design/SKILL.md`, `mode-registry.json`, `hub-router.json`, three `/interface:*` commands, `design-interface/`, and `styles/`.
- Doctrine pointer: `.opencode/skills/sk-doc/create-skill/SKILL.md`.
- Runtime context: the code graph is absent; direct file and exact-text evidence is authoritative for this lineage.
- Resource map: `resource-map.md` not present; skipping coverage gate.

## 13. RESEARCH BOUNDARIES

- Max iterations: 5
- Convergence threshold: 0.05
- Stop policy: max-iterations; convergence is telemetry only before iteration 5
- Per-iteration budget: 12 tool calls, 10 minutes
- Progressive synthesis: true
- Current generation: 1
- Started: 2026-07-27T05:57:06Z
