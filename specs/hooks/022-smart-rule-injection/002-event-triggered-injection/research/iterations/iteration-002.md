{"timestamp":"2026-09-12T14:08:43.210Z","runtime":"claude","status":"ok","freshness":"live","durationMs":963,"cacheHit":false,"skillLabel":"system-deep-loop"}
All verification done. Findings for iteration 2, in the requested shape.

---

## 1. EVENT SURFACE
*Verified against the wiring files (`.claude/settings.json`, `.cursor/hooks.json`, `.devin/hooks.v1.json`, `.codex/hooks.json`, `.pi/extensions/`, `.opencode/plugins/`), not against the injection contract's roster — two of those disagree.*

| Event class | Fires on | Wired here | Read-only reach | Model channel today |
|---|---|---|---|---|
| Session start | all 6 | `.claude/settings.json:96-137`, `.cursor/hooks.json:4-40`, `.devin/hooks.v1.json:2-38`, `.codex/hooks.json:3-42`, Pi `session_start`, OpenCode `session.created` | Always | `[SYS]` continuity; drift fallbacks (below) |
| Prompt | all 6 | advisor + Gate-3 everywhere; Cursor's `beforeSubmitPrompt` registered but **dormant** (`injection-contract.md:103`) | Always | `[SYS]`/`[MSG]`; **absent on Cursor** |
| Tool pre/post | all 6 | guards registered per runtime | Only when a matching read command runs (Bash/Shell/exec matchers) | `[BLOCK]`/`[SYS]`; route guard `[LOG]` on OpenCode (`injection-contract.md:175`) |
| Turn/completion end | Claude `Stop` (`:138`), Codex `Stop` (`.codex/hooks.json:122`), Devin `Stop` (`.devin/hooks.v1.json:152`), Pi `turn_end`, OpenCode `session.idle` | sentinel registered on all five; goal verify on Pi/OpenCode | Yes — a research wrap-up can trip the tail-400 claim regex (`completion/README.md:31`) | Four run `[LOG]`/stderr-only; **Pi model-visible** (`completion-evidence.ts:72-78`); Cursor: none — see correction 1 |
| Compaction | Claude/Codex `PreCompact`, Devin `PostCompaction` (`:169`), Pi `session_compact` + **`session_compact_failed`** (pi docs `extensions.md:333-334`) | yes (Cursor `preCompact` wired, delivery unconfirmed `session-lifecycle/README.md:62`) | Long sessions only | `[NONE]` at fire, cached; recovery `[SYS]` next start |
| Session end | Claude `SessionEnd` (`:157`), Cursor `sessionEnd` (`.cursor/hooks.json:41`), Devin `SessionEnd` (`:181`), Codex Stop-as-end (`:136-143`), Pi `session_shutdown`, OpenCode `session.deleted` | cleanup only | Always | `[NONE]` |

**Exposed-but-unwired slots (the survey's payoff):** Claude `SubagentStop`, `Notification`. Cursor documents 17+ agent events (`.opencode/skills/cli-external-orchestration/cli-cursor/references/hook-contract.md:71`) of which `stop`, `afterAgentResponse`, `afterAgentThought`, `subagentStart`, `subagentStop`, `postToolUseFailure`, `afterMCPExecution`, `beforeShellExecution`, `afterShellExecution`, `beforeReadFile`, `afterFileEdit` are unwired — five explicitly "Not wired … No current repository guard consumes these events" (`.opencode/skills/system-spec-kit/runtime/hooks/cursor/README.md:38`). Pi: `agent_start`, `agent_end`, **`agent_settled`**, `turn_start`, `message_start/update/end`, `context`, `session_before_compact`, `session_compact_failed`, `tool_execution_*`, `ui_prompt_start/end`, `before_agent_start` (pi docs `extensions.md:281-348`, `:452-484`, `:567-599`, `:651-685`). OpenCode `session.status`, `session.resumed` (`.opencode/plugins/README.md:126`) — bookkeeping only.

**Corrections to the received ground truth, each tested first-hand:**

1. **The Cursor sentinel is not dark — it is dead.** The adapter exists (`.opencode/hooks/completion/cursor/completion-evidence-response.mjs`) but is registered nowhere: no `afterAgentResponse` key in `.cursor/hooks.json`; a repo-wide grep finds only the README rows and the module itself; the runtime's own README lists `afterAgentResponse` under "**Not wired**" (`runtime/hooks/cursor/README.md:38`). So the consent-math count is: detectors run on 4 of 6 (Claude, Codex, Devin, OpenCode — all log-channel), model-visible on 1 (Pi), absent on 1 (Cursor). `completion/README.md:67` overstates its Cursor row. Cursor also has no turn-end event at all in this repo — `sessionEnd` substitutes for the never-firing `stop` (`session-lifecycle/README.md:57`).
2. **Claude's Stop-side hooks are fire-and-forget by config.** Both `session-stop.js` and `completion-evidence-stop.cjs` are registered `"async": true` (`.claude/settings.json:146`, `:152`). Any Stop-side injection proposal must additionally flip async→sync — a second, separate config change on top of the policy change the prior round priced.
3. **One member of the "stderr-only" session-start advisory family already reaches the model.** `install-codex-hooks.mjs --check` exits 1 on drift (`.opencode/bin/install-codex-hooks.mjs:383-384`), so the wrapper's `||` branch injects `[codex-hooks] drift detected` as `additionalContext` on Claude (`.claude/settings.json:132-133`) and `agent_message` on Cursor (`.cursor/hooks.json:30-34`). The other three checks (worktree, git-hooks, dist-staleness) remain stderr-only (`injection-contract.md:252-256`); Pi's notify is a headless no-op (`:256`).
4. **Pi's extension surface is wider than its README table** — `.pi/extensions/` discovers `completion-evidence.ts` and `task-dispatch-guard.ts` (ls), both omitted from the table at `.pi/extensions/README.md:18-28` and its §3A deferral line (`:88`) is stale.
5. **`agent_settled` is the readership boundary, defined at source:** it fires when Pi "will not continue running automatically"; `agent_end` may still be followed by retry/auto-compact/follow-ups (pi docs `extensions.md:569`). Every event before it may have a next model turn; at it, no reader exists. Pi's turn-end events are void — "cannot force continuation" (`goal-context.ts:169-173`).
6. Read-only sessions touch every class above except compaction-when-context-is-full. The only model-delivered payloads on that path today: session-start continuity (all six), Pi's sentinel advisory on unsupported claims (`completion-evidence.ts:72-78`), and goal nudges on Pi/OpenCode (`.opencode/plugins/README.md:31`).

## 2. IDEA ONE — completion presentation
**Verdict: REFUSE — confirmed, and the new tests make it stronger.**

- **The trigger does not exist as an event.** "A goal was met" is emitted by nothing on any runtime. It is a 0.72-confidence heuristic verdict (`goal/README.md:46`) computed only at Pi `turn_end` (`goal-context.ts:229-231`), and it deliberately produces *silence* when met (`:233`). "Work completed" is the sentinel's tail-400-char claim regex (`completion/README.md:31`) — a heuristic, not an event, and its one model-visible instance exists to flag *unsupported* claims, not to carry presentation guidance.
- **The payload is resident and the moment is contract-owned.** `AGENTS.md:405-409` carries the communication/presenting/handoff digests with load commands; `:501` carries the close-out obligation verbatim. The rule defers at its own moment: "Where the run has its own contract for this, that contract wins" (`presenting-decisions.md:129-132`), and the deep modes' completion contracts own their reports (`AGENTS.md:464-465`).
- **Delivery is structurally late**, and on Claude verifiably fire-and-forget (correction 2). Upgrading any of the four log-channel sentinels is the same consent cost the prior round refused for this detector (001, iteration 6 C2; refusal restated in `iteration-010.md`).
- Deciding test: trigger absence. Secondary: restatement. Nothing new reopens it.

## 3. IDEA TWO — structured question triggering
**Verdict: REFUSE. Plainly: detection is solvable; the remedy is fatal.**

- **Detection solvable** — the reply exists at turn end and is readable on every runtime that fires one (`completion/README.md:64-69`).
- **Remedy fatal, now bounded from the outside.** Pi exposes the strongest timing surfaces that exist in any runtime: `message_end` handlers can *replace* the finalized assistant message (pi docs `extensions.md:621`), and `ui_prompt_start/end` fire around blocking user prompts (`:583-599`). Neither helps: a role-preserving replacement cannot make the model *ask*, and `ui_prompt_*` observes extension-authored prompts, not model prose. Claude's block-continuation exists in the API but is doubly disabled for these hooks — advisory-only by design (`completion/README.md:64`) and `async: true` in config (`.claude/settings.json:146`, `:152`). No runtime currently contains a surface that can reopen a finished turn for a better question.
- **The judgment is the second killer.** "Should have asked" requires the three-condition bar (`handoff-and-questions.md:101-106`: alternatives nameable; the answer changes what you do next; not resolvable from code/default) — none computable from reply text — and prose is the rule's default (`:108-110`), so a form detector fires on compliant turns.
- Deciding test: remedy availability at fire time.

## 4. NEW CANDIDATES
Each held to the same bar: event, runtimes, silence, wrong-fire cost, portability, bar side.

**NC1 — Subagent-completion nudge.** Event: Claude `SubagentStop`, Cursor `subagentStop` (both documented — `hook-contract.md:71` — both unwired; `cursor/README.md:38`). Runtimes: 2 of 6 at best (Devin/Pi expose no equivalent post-subagent event; Pi's `task-dispatch-guard` is dispatch-side only). Silence fails on contact: subagent dispatch is routine in exactly the sessions that matter — Claude wires two Task guards (`settings.json:39`, `:49`) and the deep-loop guard exists to police repeat handoffs (`.opencode/plugins/README.md:38`). Payload would be "Finding = hypothesis" — resident verbatim (`AGENTS.md:245`). **REFUSE — restatement + not rare.** The bad-obvious exhibit the survey owed the round.

**NC2 — Claude `Notification`.** Fires when the runtime wants the *operator* (approval needed / idle). No model turn follows; by the next turn the operator has already acted, so there is no action for an injection to change. Portability: 1 of 6. **REFUSE — wrong addressee by construction.** Second bad-obvious exhibit.

**NC3 — OpenCode route-guard channel parity.** Event: a native `mcp_*` call matching a covered manual. Today the OpenCode plugin writes "a bounded, rotated workspace log" only (`.opencode/plugins/README.md:30`; `injection-contract.md:175` — "[LOG] … genuinely invisible to the OpenCode model"), while the identical shared core is `[SYS]` on the other five (`:175`), and OpenCode already has a working model-visible transform for this exact class (`system-dist-freshness-guard.js` via `experimental.chat.system.transform`, `:106-112`; `plugins/README.md:39`). Payload is genuinely non-resident ("you should have routed through the manual") and the event is discrete. But it fails the survivor test on the gate leg: nothing enforces Code Mode routing — which is exactly why the prompt round retired routing directives (`injection-contract.md:54`; 001 iteration 2, refusal B). **REFUSE — gate leg.** Honest reading: OpenCode's dark channel is not the gap; the gate-less payload is. It is nonetheless the only new candidate whose text already ships model-visible on five runtimes.

**NC4 — Session-start machine-state drift (check-git-hooks / dist-staleness / worktree-guard).** Event: session start (all runtimes that wire the sweep). What is uniquely here: **dynamic machine state** — the one class resident promotion provably cannot cover — rare silence (drift only), and a precedent *inside* the family: codex-hooks drift is already model-visible on Claude/Cursor via the failure branch (correction 3). The test that kills it: **does the payload change any model decision in the session?** No. All three checks are operator-remediation (reinstall hooks, rebuild dist, fix worktree state); the human already receives them interactively, and the model's actions — including pushes, which the gate itself governs — do not depend on hook presence. **REFUSE — no model action**, noting it is the strongest new shape on the residency axis, and the first candidate across both rounds that passes residency without being promotable.

**NC5 — Pi `session_compact_failed` / `session_before_compact`** (exposed, unwired; pi docs `extensions.md:333-334`, `:452-484`). If compaction fails, either the session continues with context intact — nothing was lost, so no recovery payload applies — or it aborts, and no reader exists. **REFUSE — nothing lost / no reader.**

Compressed one-liners for the rest of the survey (same bar, decided on sight): Cursor `afterAgentThought` — per-thought frequency, silence impossible; `beforeReadFile`/`afterFileEdit`/`beforeShellExecution`/`afterShellExecution`/`postToolUseFailure`/`afterMCPExecution` — born-complete or per-file/per-call noise (the N2 family: the failure output is the message, `injection-contract.md:150-151`); Pi `message_start/update`, `context`, `tool_execution_*`, `agent_start`, `turn_start` — machinery or N2-family, and `context` is a per-LLM-call transform with no admissible payload; OpenCode `session.status`/`session.resumed` — bookkeeping.

## 5. VERDICT
Ranked, each with the test that decided it:

1. **Idea One — REFUSE.** Deciding test: the trigger is not an event (no goal-met emission anywhere; `goal/README.md:46`, `goal-context.ts:229-233`), compounded by resident payload (`AGENTS.md:405-409`, `:501`) and fire-and-forget delivery (`settings.json:146`, `:152`).
2. **Idea Two — REFUSE.** Deciding test: remedy availability — detection solvable; no surface can reopen a finished turn (bound by pi docs `extensions.md:621`/`:583-599` and `settings.json:146`/`:152`), and the substance bar is not computable from text (`handoff-and-questions.md:101-110`).
3. **NC4 drift — REFUSE.** Deciding test: no model action — fails despite passing rarity and residency; its precedent is operator-directed.
4. **NC3 route-guard parity — REFUSE.** Deciding test: gate leg — nothing enforces the ruthe; the OpenCode `[LOG]` setting is a decision, not a gap (`injection-contract.md:175`).
5. **NC1 SubagentStop — REFUSE.** Deciding test: restatement (`AGENTS.md:245`) plus silence failure in fan-out sessions.
6. **NC2, NC5, one-liners — REFUSE.** Deciding tests: wrong addressee / no reader / born-complete respectively.

**Iteration-1 claims tested this pass:** both key ground-truth claims survive — Pi's sentinel injection (`completion-evidence.ts:72-78`) and the verifier's deliberate silence on `met` (`goal-context.ts:233`) verified first-hand; the corpus second-loader lock stands (`check-repo-rules.cjs:5-9`). What changed: Cursor's detector is dead, not dark (correction 1); Claude's Stop channel is async (correction 2); the drift precedent exists (correction 3); `agent_settled` supplies the exact readership boundary (correction 5).

**Open threads for iteration 3** (not converged): whether any detector output can carry a gate leg — Pi's sentinel is the closest shipped form (specific, evidence-derived, enforcement-adjacent via `validate.sh`); whether Cursor's deliberate event deferrals (`cursor/README.md:38`) should be re-tested against current Cursor CLI delivery, since its sentinel adapter is otherwise complete machinery with no consumer; and whether the drift class can ever pass the model-action test if remediation is ever made model-executable.
