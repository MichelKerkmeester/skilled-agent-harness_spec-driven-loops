You are researching one question for this repository. Read-only: do not edit anything.

QUESTION
What should the system inject at an EVENT, rather than at a prompt? An event is something
that just happened: work completed, a goal was met, a turn ended, a session ended, a gate
passed or failed.

WHY THIS IS OPEN, AND WHAT IS ALREADY SETTLED
A prior ten-iteration round asked whether to inject rule content keyed on the USER'S
PROMPT VOCABULARY. It refused eighteen candidates and the refusal stands. Do not
re-propose any prompt-vocabulary trigger; that question is closed.

This round asks a different question the prior round never tested. An event trigger has a
silence condition for free: the event is rare and discrete, so the hook is quiet by
default. That is exactly what every refused candidate lacked.

GROUND TRUTH — verify, then build on it, do not rediscover it
Read `.opencode/hooks/injection-contract.md` section 4 in full. Three facts matter:
- The Completion Evidence Sentinel already DETECTS when a completion claim looks
  unsupported by evidence, and injects nothing. Channel `[LOG]`. Advisory-only was an
  explicit v1 decision, not an oversight.
- Session Stop runs and injects nothing. Channel `[NONE]`.
- The goal hook already reads BACKWARD COMPOSITION at turn end, flattening the ending
  message plus tool results into evidence. That mechanism is proven and running.
So there are live detectors with their output channel switched off. Whether to switch any
on is a policy change, and one prior candidate was refused specifically for upgrading an
advisory log to an injection without consent. Treat that as a real cost, not a formality.

THREE THINGS TO COVER, IN THIS ORDER
1. THE OPERATOR'S FIRST IDEA: when work completes or a goal is met, inject guidance on how
   to present final findings. The repo has rules for this that bind only on write turns.
   Does the completion event reach them, and is the guidance worth the channel?
2. THE OPERATOR'S SECOND IDEA: smartly trigger the structured question tool. Note the
   timing problem before you evaluate it: deciding a turn SHOULD have asked requires
   judging a reply that does not exist at prompt time, and at turn end that turn is over.
   Say plainly whether that is fatal or solvable.
3. CANDIDATES THE OPERATOR DID NOT NAME. This is a required section, not optional. Survey
   every event the runtimes actually expose and ask what each one is uniquely positioned
   to carry. Propose at least three the operator has not thought of, and hold them to the
   same bar. An obvious idea nobody proposed because it is bad is also a finding.

THE BAR — apply it, do not re-derive it
`injection-contract.md` records that two prompt-time directives were RETIRED for
"restating a disposition every turn", and that the survivor stays "because it is not a
disposition — it names a specific prohibition a pre-commit gate enforces, and the gate is
what makes it load-bearing." Every candidate says which side it falls on.

REQUIREMENTS FOR EVERY CANDIDATE
- The EVENT it keys on, and which runtimes actually fire it. Runtimes differ; a candidate
  that works on one runtime only is a partial answer, and say so.
- The SILENCE condition. Being event-keyed is not automatically enough: say how often the
  event really fires in a working session.
- What it costs when it fires wrongly.
- PORTABILITY: the rule corpus is shared by symlink across three repos whose skills are
  independent directories. Hub names are stable; paths inside hubs are not.

READ THESE
- .opencode/hooks/injection-contract.md, all of it
- .opencode/hooks/completion/, .opencode/hooks/session-lifecycle/, .opencode/hooks/goal/
- AGENTS.md, the Gate 5 block and the close-out requirements in section 10
- repo-rules/handoff-and-questions.md and repo-rules/presenting-decisions.md, the two rules
  the operator's ideas are reaching for
- specs/hooks/022-smart-rule-injection/001-deep-research/research/ for what is already refused

ANSWER IN THIS SHAPE
1. EVENT SURFACE — which events exist, which fire on read-only sessions, which are unused.
2. IDEA ONE — completion presentation. Verdict plus evidence.
3. IDEA TWO — question triggering. Verdict plus evidence.
4. NEW CANDIDATES — at least three, held to the same bar.
5. VERDICT — ranked, each with the test that decided it.

Every claim carries a file:line that resolves. A well-evidenced refusal beats an invented
hook. Nothing converges early: later iterations test earlier ones, not restate them.

ITERATION 4 OF 10.
Earlier iterations of this lineage already found the following. Do NOT repeat them.
Go deeper, test what they assumed, or cover a surface they missed.

### Iteration 1
## 1. EVENT SURFACE
## 2. IDEA ONE: completion presentation
## 3. IDEA TWO: structured question triggering
## 4. NEW CANDIDATES
## 5. VERDICT

### Iteration 2
## 1. EVENT SURFACE
## 2. IDEA ONE — completion presentation
- **The trigger does not exist as an event.** "A goal was met" is emitted by nothing on any runtime. It is a 0.72-confidence heuristic verdict (`goal/README.md:46`) computed only at Pi `turn_end` (`goal-context.ts:229-231`), and it deliberately produces *silence* when met (`:233`). "Work completed" is the sentinel's tail-400-char claim regex (`completion/README.md:31`) — a heuristic, not an event, and its one model-visible instance exists to flag *unsupported* claims, not to carry presentation guidance.
- **The payload is resident and the moment is contract-owned.** `AGENTS.md:405-409` carries the communication/presenting/handoff digests with load commands; `:501` carries the close-out obligation verbatim. The rule defers at its own moment: "Where the run has its own contract for this, that contract wins" (`presenting-decisions.md:129-132`), and the deep modes' completion contracts own their reports (`AGENTS.md:464-465`).
- **Delivery is structurally late**, and on Claude verifiably fire-and-forget (correction 2). Upgrading any of the four log-channel sentinels is the same consent cost the prior round refused for this detector (001, iteration 6 C2; refusal restated in `iteration-010.md`).
- Deciding test: trigger absence. Secondary: restatement. Nothing new reopens it.
## 3. IDEA TWO — structured question triggering
- **Detection solvable** — the reply exists at turn end and is readable on every runtime that fires one (`completion/README.md:64-69`).
- **Remedy fatal, now bounded from the outside.** Pi exposes the strongest timing surfaces that exist in any runtime: `message_end` handlers can *replace* the finalized assistant message (pi docs `extensions.md:621`), and `ui_prompt_start/end` fire around blocking user prompts (`:583-599`). Neither helps: a role-preserving replacement cannot make the model *ask*, and `ui_prompt_*` observes extension-authored prompts, not model prose. Claude's block-continuation exists in the API but is doubly disabled for these hooks — advisory-only by design (`completion/README.md:64`) and `async: true` in config (`.claude/settings.json:146`, `:152`). No runtime currently contains a surface that can reopen a finished turn for a better question.
- **The judgment is the second killer.** "Should have asked" requires the three-condition bar (`handoff-and-questions.md:101-106`: alternatives nameable; the answer changes what you do next; not resolvable from code/default) — none computable from reply text — and prose is the rule's default (`:108-110`), so a form detector fires on compliant turns.
- Deciding test: remedy availability at fire time.
## 4. NEW CANDIDATES
## 5. VERDICT

### Iteration 3
## 1. EVENT SURFACE
## 2. IDEA ONE — completion presentation
## 3. IDEA TWO — structured question triggering
- 5 of 6 runtimes contain no turn-reopening surface at all.
- The 1 of 6 that does (OpenCode's goal continuation, §1) is doubly unusable: it requires an *active, un-met goal* (`opencode-goal.js:2621-2675`) and its authored prompt tells the model **not to ask** (`:2473`). The fleet's single reopen channel exists to *suppress* the behavior this idea wants to increase.
- Pi's `message_end` replacement (the only other modifier surface, pi docs `extensions.md:621` per iteration 2) has an operator-side workaround iteration 2 did not test: append the ask to the finalized message so the human sees a question the model never asked. Mechanically available. Refused this pass on **authorship integrity** — the replacement attributes speech to the model, and the operator would answer a question composed by a heuristic — plus it still cannot compute "should have asked." Recorded as candidate NC-C below.
## 4. NEW CANDIDATES
- *Event:* `PostToolUse`/`tool_result` on Write|Edit, all six runtimes (`post-edit-quality/README.md:26`).
- *Runtimes:* 6 fire, **3 deliver** — Cursor `agent_message` (`:69`), Pi tool-result content (`:70`), OpenCode transform drain (`:71`); **3 stdout-only** — Claude (`:66`), Codex (`:67`), Devin (`:68`), where the repo's own words are "not model context in normal use." (Correction to `injection-contract.md:204`, which names only Claude/Devin; Codex shares the shape.)
- *Silence:* findings only. The router returns at most one checker per edit (`:16`) and every no-finding path is silence (`:128-129`); the comment-hygiene checker fires only on actual violations.
- *Wrong-fire cost:* low — specific file:line findings, redacted (`:56`), line-level escape (`hygiene-ok`), warn-only, kill-switch `SK_CODE_POST_EDIT_QUALITY_DISABLED` (`:115`).
- *Portability:* the payload is produced by the shared runtime-neutral core (`lib/post-edit-router.cjs`, `:99`); the change is 3 adapter transports in skill-owned code, adding no new cross-repo path references. Nothing here reads the rule corpus.
- *Bar side — survivor side, uniquely in this round.* The comment-hygiene finding restates the exact prohibition the admitted directive carries (`injection-contract.md:52-54`), enforced by the pre-commit gate; the event delivers it at the edit — *before* the gate's late block — on the runtimes that already chose this delivery.
- *Consent status:* this is a **transport repair, not a channel activation**: the identical payload is already model-visible on 3 of 6, and Claude's own registration returns `hookSpecificOutput.additionalContext` on its drift path (`.claude/settings.json:175`), so the envelope is in use in the same registration. It is not the class the prior round refused (a log upgraded without consent); nothing new is admitted to the model that half the fleet does not already receive.
- *Required verification before adoption:* a live probe (`claude --debug`) that PostToolUse additionalContext is delivered. **Self-kill condition:** if Claude/Codex stdout turns out to reach the model after all, the candidate evaporates and there is nothing to build.
- *Event:* session start (all 6); payload would be the completion-sentinel's accumulated advisories from prior sessions. New shape: nobody proposed surfacing the *backlog* rather than the current turn's finding.
- *Silence:* advisories exist. Rare by construction.
- *Wrong-fire cost:* past-tense claims with no live remedy; dedup is keyed per packet+claim (`completion/README.md:43`), so replay re-serves what the operator already read; session start is the one channel the architecture keeps to continuity, with its advisory family deliberately stderr-only (`injection-contract.md:252-256`).
- *Bar side — retired.* No gate, no remedy for a claim already made — and the existing mechanism on `session.created` is a *sweep* that prunes this state (`completion/README.md:52`), not a reader. The direction of the shipped design is the opposite of this candidate.
- *Event:* Pi `message_end` — 1 of 6 (partial answer, stated as such).
- *What it was uniquely positioned to do:* append the structured-choice question the model failed to ask onto the finalized, operator-visible message — the only surface in any runtime that can put text into the human's view after the turn ended.
- *Silence:* the same uncomputable predicate as Idea Two — none.
- *Wrong-fire cost:* the mechanism manufactures the failure it pretends to fix: a question attributed to the model, answered by the operator, composed by a heuristic. A wrong fire changes what the operator does next on fabricated authority — the highest wrong-fire cost of any candidate in either round.
- *Bar side — retired*, now on a second, stronger leg: even with a perfect detector it cannot be admitted, because it delivers content the model never produced. The honest conclusion: the only surface that can *fake* the ask is exactly the surface that must not.
- *"Can any detector output carry a gate leg?"* Tested: the sentinel's evidence condition is checkable — `check-completion.sh` prints `RESULT: BLOCKED` for missing evidence markers (`check-completion.sh:468-470`, detection at `:323-325`) — but that string is a report, not an enforcement; the authoritative gate for completion claims is `validate.sh`, a command the model runs (`AGENTS.md:264-266`). The sentinel cannot inherit a gate leg it does not have. Pi's sentinel channel remains a deficiency notice with a resident remedy. Thread closed, refusal stands.


Be selective about what you open; a partial answer with resolving citations beats an exhaustive read that never finishes.
Return only this iteration's findings, in the output format above.
