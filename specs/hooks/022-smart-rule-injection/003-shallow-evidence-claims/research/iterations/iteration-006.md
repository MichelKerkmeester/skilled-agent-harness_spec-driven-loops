{"timestamp":"2026-09-12T14:35:03.696Z","runtime":"claude","status":"fail_open","freshness":"unavailable","durationMs":173,"cacheHit":false,"errorCode":"NONZERO_EXIT","errorDetails":"CLI_EXIT_64"}
## 1. DIAGNOSIS

**The reframe holds, and its sharpest test came from this lineage itself.** I5 closed with "propagation is UNKNOWN from this seat … the parent tree holds only `Public/`" (`iteration-005.md:43`). That is a negative search at the wrong scope: the siblings are not under `Code_Environment/` — they are under `Development/`, one level higher. `ls /Users/michelkerkmeester/MEGA/Development` resolves `Mobile CLI/` and `Obsidian Plugin/`; both `AGENTS.md` files read **line-identical to this checkout at `:239-249`** (same content digests: `:239` `cbf6d892`, `:243` `c70ab436` in all three), and `synthesis.md:13` records both as absolute symlinks into this checkout. So I3/I4's propagation claim was true, I5's `ls` could not have said otherwise, and I5's honest-UNKNOWN is the *same instrument-scope defect*, expressed as unnecessary doubt instead of false confidence. The reframe is not just confirmed; it reproduces in the meta-layer — shallow instruments produce both failure directions.

**Why the existing standards did not fire — per incident.** All five satisfied row 1's receipt test (`AGENTS.md:243`: "carries its evidence (file:line, command, artifact)"). The decisive gap is narrower than I4/I5's "untyped question": the corpus's falsifier vocabulary exists, but every instance is **action-scoped**:

- **#1 / #4** — `evidence-and-proof.md:81-84` is the one clause about a fruitless search, and its remedy is `-a`, byte-encoding. It asks whether the file was read, never whether the pattern could match the target's authored form (prefixed vs bare key; line-oriented vs wrapped text). `AGENTS.md:345` "widen the pattern" is the positive mirror only.
- **#2** — `AGENTS.md:244` governs *whether* output was read, not how much; the first line "loads ok" satisfied it, fetched from a read that could never show the error line. The nearest completeness clause, `evidence-and-proof.md:69-70` ("require the affirmative marker"), was satisfied *by the misleading first line itself*.
- **#3** — `evidence-and-proof.md:52` owns the missing tier (DERIVED: "Show the observation and the step from it"); the total was re-presented as a known fact, and the resident row has no middle tier. `evidence-and-proof.md:122` triggers on "computed answer", not on an inherited one.
- **#5** — `uncertainty-and-honesty.md:91-99` halts on two facts that conflict, with a report format accepting "Fact A, with its source"; nothing requires the two sides to be the same *kind*, so a digit match satisfied halt and format. `:35` fires on unverified numbers, and the digits were verified — their role was not.

**The scope finding.** The corpus's negative-control/falsifier doctrine is change-scoped and tool-scoped, never claim-scoped: `evidence-and-proof.md:92-97` ("Before the fix, reproduce the exact failing symptom"), `:103-108` (baselines for "no regressions"), `AGENTS.md:247` (row 5). "Watch it fail first" is a trigger phrase *inside that fix-scoped section* (`:9`). The claim-side of watching a check fail — proving the check can produce the falsifying result — is absent everywhere, and row 4's lone falsifier phrase, "what would change it" (`AGENTS.md:246`), is disclosure-shaped: an option ("or say it is judgment"), with a relabel remedy, not a criterion on confirmation.

**#3 versus the rest, pressed further.** I5 established the receipts there were valid at their own level (`iteration-005.md:15`). Under this iteration's test the two kinds unify without a new standard: the question "has this check ever produced the contradicting result on a known case?" is level-generic. For #1/#2/#4/#5 a check ran and its discriminating power was never demonstrated; for #3 no check exists at the total's level at all — the honest answer is "never", which routes straight to the existing DERIVED duty (`evidence-and-proof.md:52`) and derivation rule (`:122-123`). One rule covers both because the *absence* of a demonstration, not the claim's provenance, is the trigger.

## 2. WHERE IT BELONGS

- **New rule file: refused — Test 1** (`decision-tests.md:32-33`, `:38-41` → `:131`). The failures occur while reading and reporting; `REPO RULES.md:18` states the designed outcome: "Nothing fires → `AGENTS.md` alone governs."
- **New AGENTS.md row: refused.** I5's restraint ground (`decision-tests.md:121`) stands, and there is a stronger one: row 1 *owns* the confirmed/inferred boundary. A sixth row would give "confirmed" two competing criteria — receipt-presence (`AGENTS.md:243`) versus discriminating power — while a confirmed claim under one is inferred under the other. The fix belongs inside row 1, not beside it.
- **Section in communication.md / per-turn injection: refused** (I4-I5, `communication.md:183-186`; enforcement-gated surfaces, `iteration-005.md:20`). Not re-argued.
- **Admitted: row 1 amendment (`AGENTS.md:243`) + tier mirror at `evidence-and-proof.md:51`.** The mirror is coherence, not scope creep: `evidence-and-proof.md:30` subordinates the file to `AGENTS.md`, and §1 owns the tier definitions (`:49-53`) — leaving §1's OBSERVED loose while row 1 tightens would make §1 wrong. Count sentence stays "five" (`AGENTS.md:239`); no maintenance.

## 3. THE PROPOSAL

I5's territory (row 1, falsifier framing, tier mirror) with its two clauses replaced by the missing empirical half. I5's Edit 1 asks "could that evidence have come out otherwise about the claim" and enumerates three defect shapes (`iteration-005.md:28`; its own summary calls it "a classification … with its three shapes enumerated", `:34`). I5's Edit 2 predicate — "the claim's falsifier sits inside what you read" (`:32`) — read literally can never hold for a claim that turns out true (had the falsifier appeared, the claim would be false); the only coherent reading is the counterfactual *would have sat inside*. Both wordings are answered from the agent's model of its instrument — and that model is precisely what was wrong in all five incidents (wrong form-space #1/#4, wrong read-span #2, wrong provenance #3, wrong discriminating power #5). A counterfactual test answered from the broken model cannot repair it (I4's own mechanism, `iteration-004.md:13`, turned on I5). The demonstration must be observational:

> **Edit 1 — `AGENTS.md:243`, row 1:**
> | **Confirmed vs inferred** | A load-bearing claim carries its evidence (file:line, command, artifact) and is confirmed only once the same check has produced, on a case whose answer was already known, the result that would have contradicted the claim. A one-way reading (a pattern that has never hit, a total nobody has re-derived) has not been shown able to disagree: say inferred, and what would confirm it. |

> **Edit 2 — `repo-rules/evidence-and-proof.md:51`, OBSERVED row:**
> | **OBSERVED** | You ran it and read the output and exit status, and the check has produced, on a case whose answer was already known, the result that would have contradicted the claim | Cite the receipt: the command, the `file:line`, the artifact path |

Application to the record:

- **#1** — calibrate the pattern on a mode key with a known registration (bare form). A grep that has never hit a known instance cannot support "not registered." The control either exposes the form bug — and usually *finds the mode* — or proves the pattern blind. **Caught, and repairable, not merely re-labeled.**
- **#2** — the single-line pattern has never retrieved a known multi-line require (caught); the probe read has never shown its own failure line, so "loads ok" rides an undemonstrated read (caught; confirm by reading the span).
- **#3** — the range audit has never produced a total at any level; no known-case demonstration exists, so the claim is not OBSERVED and routes to the DERIVED derivation duty (`evidence-and-proof.md:52`, `:122-123`). **Caught.**
- **#4** — a fragment search calibrated on known-present text retrieves the wrapped fix; the un-calibrated line-pattern cannot. **Caught, and repairable.**
- **#5** — the similarity match must produce its rejection on a known weight/label pair before "second scale" stands; it cannot, so inference, confirm by comparing roles. **Caught.**

The delta over I4/I5: the test is a fact about the transcript plus one control run — its execution *produces* the missing instrument knowledge instead of presupposing it — and it can turn a wrong claim into the right one (#1, #4) where I5's output was only a label.

## 4. WHAT IT COSTS

- **Fires wrongly on:** direct positives whose claim *is* the reading ("file exists" against a listing; the contradicting result, an omission, is a familiar outcome of the same check). One beat, usually zero extra commands. Real friction lands on interpreted claims — absence, breakage, totality, sameness — the incident population itself.
- **Frequency — the profile improves.** The unit is the *instrument per turn*, not the claim: one calibration of a grep pattern covers every later claim behind it. A dense research turn runs a handful of distinct checks, so a handful of controls, not I4/I5's tens of per-claim self-questions. Against five retractions in one session, the trade is favorable.
- **Gaming:** a rigged control (a demonstration that cannot fail). Bounded because the control's output sits in the transcript, inspectable; blanket INFERRED remains available only at the price `evidence-and-proof.md:53` already sets ("say what would confirm it").
- **Edit mechanics:** an AGENTS.md row change escalates, it is not mechanical (`agents-md-integration.md:122-127`). CI does not cover it (`repo-rules-corpus.yml:5-8` = `REPO RULES.md`, `repo-rules/**`, the checker only — confirmed first-hand). Count sentence unchanged.
- **Propagation — resolved this iteration, first-hand:** siblings at `Development/Mobile CLI/AGENTS.md` and `Development/Obsidian Plugin/AGENTS.md`, `:239-249` identical to this file (same digests); `synthesis.md:13` records both as absolute symlinks into this checkout, so one edit lands in three places; a post-edit read of `:243` in both siblings is the cheap check. I5's UNKNOWN is withdrawn — the enumeration it prescribed is what found this.
- **Residual:** unenforceable, skippable, like every text rule. It changes the criterion an honest agent applies; it does not make a dishonest one run the control.

## 5. VERDICT

**Adopt.** Row 1 amendment plus the `evidence-and-proof.md:51` mirror, with the demonstration clause replacing I5's modal and taxonomy wording. Refuse the new row, the new rule file, the communication.md placement, and per-turn injection — each already tested and routed elsewhere in prior iterations or above. The gains over the lineage's current best: the criterion is observational, so it does not inherit the wrong-instrument model that caused all five incidents; it can repair absence/breakage claims rather than only re-label them; its cost is per instrument, not per claim; and the landing path is now verified in all three repositories instead of UNKNOWN. The honest ceiling does not move: text buys honest classification and, on absence claims, one recovering command — never enforcement.
