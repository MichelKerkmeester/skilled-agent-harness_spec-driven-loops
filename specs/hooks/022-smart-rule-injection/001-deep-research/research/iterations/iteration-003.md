{"timestamp":"2026-09-12T12:48:47.294Z","runtime":"claude","status":"ok","freshness":"live","durationMs":1201,"cacheHit":false,"skillLabel":"sk-code"}
## ITERATION 3 FINDINGS

### 1. INVENTORY — read-only bindings, with the lineage's method corrected

Iteration 1 collected "Fires when" lines and called them read-only bindings; that conflates two clocks. The useful test is **violation-clock**: a rule binds on a read-only turn only if its *failure* can occur in a turn that mutates no file. Trigger-clock (a trigger that starts from a read-only event) does not count. On the violation-clock test:

**Reply-shaped bindings (failure = a sentence in the answer) — the 8 from iteration 1, each now checked against its resident carrier:**

- `communication.md:37` → resident at `AGENTS.md:404` (pointer plus "Load it before answering"), with the always-on minimum kept resident by policy: `REPO RULES.md:80-83` — "§8 keeps the two clauses that must bind even when nothing loads."
- `evidence-and-proof.md:34-38` → `AGENTS.md:239`: "These four bind unconditionally, including on a read-only turn where Gate 5 never fires and no rule file loads" (+ table `:241-245`).
- `uncertainty-and-honesty.md:33-37` → `AGENTS.md:482` ("Never fabricate") + §2 confidence table.
- `presenting-decisions.md:35-39` → `AGENTS.md:499`. `handoff-and-questions.md:35-39` → `AGENTS.md:500`.
- `root-cause-and-debugging.md:34-37` → §3 debugging block, via its expansion pointer `AGENTS.md:175`.
- `skill-hub-routing.md:38-39` → `AGENTS.md:114` (two-stage paragraph incl. "never report a mode as routed because a registry entry exists").
- `delegation-and-orchestration.md:41` → **the one thin spot.** A grep of AGENTS.md for "own reading alone / one model / verdict closes" returns only `:239`; §9 carries a bare posture pointer. The self-directed clause (`delegation-and-orchestration.md:47` — "no single model's verdict, the delegate's or your own, closes a question") has only a weak resident analogue: `AGENTS.md:245` covers *other-produced* findings, not your own closing verdict.

**The three files iteration 1 never opened — none has a read-only violation:**

- `blast-radius.md:33-37` = delete/overwrite/truncate/migrate/deploy/publish/send/install/"call that leaves this machine" — mutations all. The read-adjacent stakes read (`:46-48`) fires at the start of non-trivial *work* and is resident at `AGENTS.md:163`.
- `scope-discipline.md:35` — "You notice a defect…" is a read-only *event*; the failure (fixing it) is a write. Notice-and-name is the compliant path, not a failure mode.
- `prevent-overengineering.md:39` — trigger explicitly includes "thinking" 'flexible', but the failure (adding) is a write, and the loop is resident at `AGENTS.md:181` (pre-write pass).

**Finding:** no corpus rule has a read-only violation with a downstream detector. Every read-only binding is reply-shaped, and the reply layer is the one this repo deliberately keeps gate-free: the completion sentinel sees unsupported completion claims and is [LOG]-only "by explicit design" (`.opencode/hooks/injection-contract.md:240-244`), as are the session-start advisories (`:252-256`) and OpenCode's mcp-route-guard (`:175`). Not an omission — a policy of not injecting nagging about the sentence layer.

### 2. CANDIDATES

**A. Inverse injection — fire on the turns Gate 5 misses** (the naive reading of the question). Trigger steelman: the Gate-3 classifier (`.opencode/hooks/injection-contract.md:82-83`; machine contract `system-spec-kit/shared/gate-3-classifier.ts`, per AGENTS.md §2 Gate 3) scores the prompt *no mutation* → Gate 5 will stay silent → inject the read-only-relevant rules. **Silence: none exists** — the class it fires for is "most turns," and its content is resident by explicit design (`AGENTS.md:239`, `REPO RULES.md:80-83`). **Wrong-cost:** maximal — pays tokens on the majority turn-class to re-assert what §4/§8/§10 already carry. **Bar: dead-center retired side** — "restated a disposition every turn" (`.opencode/hooks/injection-contract.md:54`), and its own trigger fires exactly where the content is already guaranteed load-bearing.

**B. Check-weakening directive riding the Gate-3 classifier** (iteration 2 tested this candidate but not on the classifier surface). Trigger: classifier says mutation + prompt contains failing-check vocabulary. Silence: classifier below threshold. Wrong-cost: every debug turn — highest-frequency write turn — for content Gate 5 loads on that same turn (weakening is a write). **Bar: fails the gate leg, and the classifier cannot repair that.** The pre-commit chain is now enumerated (`.opencode/hooks/git/README.md:18`): comment hygiene, mass-deletion, doc-model-refs, mirror-parity, prompt-card-sync, MCP mutation-class, compiled-routing re-mint, tool-ownership — none detects a loosened assertion or skipped test; mass-deletion's ceiling catches bulk file deletion, not `it.skip`. Result: **a missing gate leg cannot be repaired by a better trigger.** Leave at Gate 5. Forward marker: if a test-weakening gate ever lands, `root-cause-and-debugging.md:109-114` becomes directive-shaped by construction.

**C. Bulk-deletion directive — the closest structural sibling of the survivor** (mid-turn composition, late gate at commit). Trigger steelman: "delete/remove/clean up" in the prompt. **Silence steelman fails observability:** the deletion set does not exist at prompt time — either it fires on every removal turn, or it needs mid-turn state a prompt hook doesn't have. Self-detection at composition also fails: bulk-ness is a count against a threshold, not a shape the composer can see while composing one deletion — unlike the survivor, whose violation ("am I typing a spec path into this comment?") is self-detectable at composition. And when the operator asks for the delete, the instruction self-authorizes, the same logic as `AGENTS.md:332` for pushes; the mass-deletion gate retains the runaway case. **Refuse.**

**D. trigger_phrases pointer injection** (new surface: every corpus file carries machine-readable frontmatter — `scope-discipline.md:4-22`, `prevent-overengineering.md:4-27`, `delegation-and-orchestration.md:5-24` — and the corpus is the symlink-shared layer; the phrases feed the Gate-1 trigger index per AGENTS.md §10). A hook matching phrases injects "load `repo-rules/X.md`". **Silence: no phrase hit — observable, unlike A/B/C.** Wrong-cost: vocabulary ≠ imminence — "best practice" (`prevent-overengineering.md:12`) in a discussion turn triggers a rule that binds "before the first write" (`prevent-overengineering.md:32`); it re-implements Gate 1's lookup with worse judgment. **Bar: no gate leg; and the load it names is what Gate 5 already mandates when the action arrives.** Refuse — noting this is nonetheless the only *portable* trigger design (§4).

### 3. SURFACE

Mechanically yes, productively no. §2 of the contract is the home (`.opencode/hooks/injection-contract.md:46-140`); canonical renderer `render.ts` (`:65`) with channels per runtime (`:66`). But every existing "smart" silence is **content absence or session state, never rule relevance**: sk-vision "a path that does not resolve produces nothing, deliberately" (`:100`); spec-gate "once per session until answered" (`:82`); goal brief renders nothing with no goal (`:106-140`); and the survivor's directive silences by **cadence**, not relevance — full delivery on first message and after lifecycle boundaries, suppressed within the epoch (`.opencode/hooks/injection-contract.md:64`; `.opencode/hooks/directive-lifecycle/README.md:18`, "keeps the… directive block from being re-injected on every turn… re-arms full delivery only on a lifecycle boundary"). That is repetition-suppression; it does not satisfy the operator's "smart" requirement by mode. The only prompt-time relevance signal in the fleet is the Gate-3 classifier, and it fires exactly on turns where Gate 5 already does its work. Cost asymmetry matters: a corpus rule is one symlinked markdown edit; a new directive is a change in each repo's advisor skill (independent directories), its lifecycle state, and a kill-switch row (`.opencode/hooks/README.md:38-62`).

### 4. PORTABILITY

Satisfiable only for pointer-form, corpus- or workspace-keyed triggers: discovery keyed to `REPO RULES.md` presence via install-anchored walk (`.opencode/skills/system-skill-advisor/hooks/skill-advisor-hook.md:36`, `:53`); payload as `repo-rules/<file>.md` — a convention owned by the corpus itself (`REPO RULES.md:36-50`), stable wherever it is symlinked; never rule content or hub-internal paths (the `skill-hub-routing.md:86` "commands are in the reference above" pattern). Candidate D alone survives this test; A/B/C have no functioning trigger to port. One free constraint: a hook ships per repo — the markdown corpus cannot carry one — so any injection degrades to nothing where the local hook is absent (fail-open, `skill-advisor-hook.md:25`); acceptable only if a corpus-side pointer compensates. Portability is satisfiable and, for now, unused.

### 5. VERDICT

**Refuse.** The answer to the question as asked is *none* — no repo rule is better served by prompt-time injection than by Gate 5. The reasons are structural, not incidental: (1) every read-only binding is reply-shaped, and the reply layer is covered in the resident texts by explicit design (`AGENTS.md:239` says so in terms; `REPO RULES.md:80-83` documents the mechanism) — injecting them recreates the retired directive class by definition; (2) the bar's gate leg is unsatisfiable inside the corpus — the enumerated gate chain (`.opencode/hooks/git/README.md:18` plus pre-push) enforces mechanical invariants (comment shape, manifests, deletion counts), and no posture rule has a downstream detector; (3) the survivor's shape is *single, self-detectable-at-composition prohibition with a later catching gate* — no corpus rule is that shape, which is why exactly one directive exists and why nothing joins it. Where resident coverage is thinnest — `delegation-and-orchestration.md:41`, the only read-only trigger with no verbatim AGENTS.md duplicate — the remedy that fits this repo is a clause or a gate, not a hook, and even that is weak (its substance is largely `AGENTS.md:245` applied to self). If a genuine read-only gap ever appears, the attested remedies in this repo are the resident-promotion pattern (`AGENTS.md:239`'s explicit "binds even when nothing loads") or a tool-time surface (`.opencode/hooks/injection-contract.md:148-204`), not prompt-time injection.

**Later-iteration tests applied to earlier ones:** iteration 1's inventory method is corrected (trigger-clock vs violation-clock; its three omitted files now classified). Iteration 2's "resident layer" holding is verified item-by-item, with one finding it missed (`delegation:41`). Iteration 2's check-weakening refusal is re-tested against the classifier trigger surface and the enumerated gate chain — the refusal holds, and generalizes: **a missing gate leg cannot be repaired by a better trigger.**

Nothing converged; nothing left for the operator.
