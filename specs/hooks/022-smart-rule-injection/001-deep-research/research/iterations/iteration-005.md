{"timestamp":"2026-09-12T12:58:11.490Z","runtime":"claude","status":"fail_open","freshness":"unavailable","durationMs":89,"cacheHit":false,"errorCode":"NONZERO_EXIT","errorDetails":"CLI_EXIT_64"}
## ITERATION 5 FINDINGS

### 1. INVENTORY — the read-only set is unchanged; what iteration 5 adds is its structural classification and the gate-side proof

All 11 rule files were re-read this pass, trigger lines verified directly rather than inherited: `communication.md:35-38`, `evidence-and-proof.md:32-38`, `uncertainty-and-honesty.md:31-37`, `presenting-decisions.md:33-39`, `handoff-and-questions.md:33-39`, `delegation-and-orchestration.md:35-41`, `root-cause-and-debugging.md:32-37`, `blast-radius.md:31-37`, `scope-discipline.md:33-39`, `prevent-overengineering.md:35-40`, `skill-hub-routing.md:33-39`.

The read-only-binding subset holds: `communication.md:37`, `evidence-and-proof.md:34-38`, `uncertainty-and-honesty.md:33-37`, `presenting-decisions.md:35-39`, `handoff-and-questions.md:35-39` ("About to end a turn, of any kind"), `delegation-and-orchestration.md:41`, `root-cause-and-debugging.md:34` (a failing check is usually reached by read-only investigation), `skill-hub-routing.md:38-39`. The remaining three (`blast-radius.md:33-37`, `scope-discipline.md:35-38`, `prevent-overengineering.md:37-40`) are act-shaped events whose prohibited *response* is a write; iterations 1/3 settled that, and the classification survives — but it is worth naming why: the event can occur read-only, the violation cannot.

**New (5.1) — every read-only bind constrains an agent-side act, not a prompt-side fact.** Each firing condition in the set is about what the model is *about to write or say* (`communication.md:37`, `presenting-decisions.md:35`, `handoff-and-questions.md:35`, `delegation-and-orchestration.md:41`). The failure signal exists only after the prompt event: the contract's own model is "fire once per user turn, **before the model call**" (`injection-contract.md:48`). This is the property the earlier iterations kept circling without naming; §2 shows it decides every candidate.

**New (5.2) — the corpus states this problem itself, for its broadest rule.** `communication.md:40-42`: "a rule about how replies read has to load whenever a reply is being written, or it silently stops applying to the short answers that need it most." The corpus noticed the reply-time binding problem and its chosen remedy was **not** a hook — delivery moved down into the corpus with the always-on minimum retained resident (`REPO RULES.md:80-83`), and `AGENTS.md:239` promoted four standards with "bind unconditionally, including on a read-only turn where Gate 5 never fires." Both directions of promotion have precedent; the hook fleet has none for this shape.

**New (5.3) — the uncovered set, verified by grep rather than inheritance.** A vocabulary grep of `AGENTS.md` for the delegation clause's phrase family (`judgment question`, `one model`, `single model`, `lens`, `verdict closes`, `own reading`, `second opinion`) returns **no matches**. The only delegation content resident is two bare pointers, `AGENTS.md:418` and `AGENTS.md:488`. Nearest residents: the "Finding = hypothesis" row covering other-produced claims (`AGENTS.md:239-245`) and "≥80% Proceed with citable source" (`AGENTS.md:94`). So the zero-carrier read-only obligation is exactly one: `delegation-and-orchestration.md:41` + `:166-174` §6, plus its self-check row `:227`. Iteration 3 called this thin; iteration 5 confirms it is *empty* resident, and it is the only one.

**New (5.4) — the gate chain adjudicates no corpus obligation; this is the class-level proof the bar's second leg fails for all candidates.** Pre-commit's seven blocking sub-gates are comment hygiene, agent-mirror sync, mirror parity, prompt-card sync, MCP mutation-class, compiled-routing re-mint, spec derived-metadata re-mint (`.opencode/scripts/git-hooks/README.md:24`, `:102`); pre-push runs mass-deletion ceiling + remote-branch allowlist (`.opencode/scripts/git-hooks/README.md:107`; `.opencode/hooks/git/README.md:24-29`). The subjects are comments, mirrors, cards, mutation classes, re-mints, deletion volume, branch names — none is a corpus rule. The single adjacency, mass-deletion ↔ `blast-radius.md:33`'s delete clause, is push-time, bypassable (`SPECKIT_ALLOW_MASS_DELETION=1`, `.opencode/scripts/git-hooks/README.md:107`), and has no prompt-side signature for "about to delete a lot" anyway. So no corpus candidate can pass the bar's "a gate enforces it" clause — the failure is structural, not per-candidate.

### 2. CANDIDATES — two constructions never tested, one re-test, each with its silence condition and bar side

Derived test, applied below. From `injection-contract.md:54`, the bar's surviving justification is: the directive is "not a disposition — it names a specific prohibition a pre-commit gate enforces, and the gate is what makes it load-bearing." Two clauses: **(a) specific prohibition, not a disposition; (b) gate-enforced.** Passing both is still necessary but not sufficient for injection, because of the operator's silence constraint — which iteration 2's push refusal is now the test case for, not a reopened question: push passes (a)+ the gate leg (`.opencode/scripts/git-hooks/README.md:107`) and fails only because the failure turn (an unprompted push) has no prompt-time signature. Iteration 5 formalizes this as the **third condition: the firing predicate must correlate with failure risk**, and shows below that for corpus rules conditions (b) fails *and* the third is unconstructible — independently.

**C1 — the Gate-3 rider (new).** Append "load `REPO RULES.md` before writing" to the existing classifier-gated question.
- **Trigger:** the prompt scores as probable file mutation (`injection-contract.md:82`), once per session until answered.
- **Silence:** read-only prompts (the classifier says no); answered session. Precise, observable — and wrong-shaped.
- **Cost when wrong:** fires on every session's first write-intent turn carrying text that restates the *resident* Gate 5 block (`AGENTS.md:121-129`), including its own sentence "that load is a Read, not a Gate Action, so on a file-modification request it queues behind Gate 3" (`AGENTS.md:126`).
- **Bar side — retired.** Restatement of resident text (`AGENTS.md:121-129`), and the load it asks for already fires at exactly that moment. It covers zero of the gap: the read path is where it does not fire, by construction. Its one merit is negative: it proves the fleet's only prompt-classifier points at the write path, the opposite pole of the uncovered surface.

**C2 — topic-class rule pointer, once per session (new; the strongest construction the mechanism can carry).**
- **Trigger:** prompt vocabulary matches a corpus rule class (e.g., judgment/recommendation vocabulary → `presenting-decisions.md` pointer).
- **Silence:** first matching prompt per session, via directive-lifecycle dedup (`injection-contract.md:64`); no vocabulary match, never. This satisfies the operator's letter — one line, once.
- **Cost when wrong:** two-part, and the second part is new. (i) Topic selectivity ≠ failure selectivity: it fires on the compliant majority and cannot distinguish the turn where the model will bury the verdict from the turn where it will not — the failure is agent-side (5.1). (ii) **Cadence dedup is anti-correlated with relevance.** Once-per-session delivery is *guaranteed* to be absent on the second and later offending turns, and present on the first turn of the class, when no reply obligation is yet in play. The only silence the protocol offers optimizes spam, not timeliness — iterations 2/4 listed "cadence" as a silence class without noting it can never be relevance.
- **Bar side — (a) passes on read-only sessions, (b) fails.** On a pure read-only session nothing carries the corpus (`AGENTS.md:122`), so a pointer would add, not restate. But no gate enforces any corpus reply obligation (5.4), so the directive would spend context without a backstop — which is the exact reason `:54` gives for the hygiene survivor's load-bearingness.
- **Additional collapse:** the admissible payload set is the uncovered set, which is one clause (5.3). Any *narrow* version of C2 converges on C4; any *broad* version (cover the corpus's vocabulary) fires on nearly every substantive prompt and becomes the constant delivery the retired shape was (`:54`). Both ends fail.

**C4 — direct hook on the delegation self-lens clause (re-test).**
- **Trigger:** `delegation-and-orchestration.md:41`, "about to answer a judgment question … from your own reading alone."
- **Silence: not constructible, and now provably so.** The failure is the choice to *solo-answer*; that choice is made after the prompt event every predicate can see (`injection-contract.md:48`). The clause's own conditional ("from your own reading alone") is not observable at injection time; the only observable is the topic class ("is this the right design"), which fires on the whole judgment-discussion population, compliant and failing alike. The fleet's predicate inventory — score threshold (`:52`), mutation classifier (`:82`), entity resolution (`:100`), session binding (`:106-140`), delivery cadence (`:64`) — reads prompt-side or session-side evidence only. No predicate reads the model's future composition. This supersedes iteration 4's "not constructible" with the reason: it is not that no predicate was found, it is that the failure dimension is invisible to the predicate class.
- **Cost when wrong:** re-asserts an epistemic stance on every design turn; and the rule file is level 3 (`REPO RULES.md:24-32`), so an injected copy could not carry tier-1 force anyway.
- **Bar side — retired.** (b) fails; disposition-shaped reminder.
- **Remedy (tested, not repeated):** iteration 4 said "change the rule." Iteration 5 names the target and the portability proof. Candidate homes: the four-standard table (`AGENTS.md:237-245`), whose "Finding = hypothesis" row already covers *other-produced* claims — the self-lens clause is its mirror and belongs beside it — or §7 escalation (`AGENTS.md:445` region, "Confidence stays <80%…"). Both promotion precedents exist: down into the corpus (`REPO RULES.md:80-83`) and up with "bind unconditionally" (`AGENTS.md:239`). The clause is repo-generic, so promotion into shared `AGENTS.md` is the maximally portable form — zero paths, identical across all three repositories. Direction is genuinely the operator's call, because it decides *where* the clause lives for all three repos.

### 3. SURFACE — the contract can transport text; it cannot transport a smart silence for agent-side failures, and the repo has already chosen the surfaces for reply-shaped checks

Transport exists: a constant directive would ride the advisor brief's pipeline unchanged — canonical text in `render.ts` (`injection-contract.md:65`), delivered on six runtimes (`:66`), deduped by directive-lifecycle (`:64`). But a *constant* corpus directive restates dispositions on its face — retired (`:54`). A *conditional* directive has no precedent: the hygiene text is fixed and appended "regardless of the recommendation" (`:52`), the OpenCode mirror is a hand-duplicated copy of the same text (`:65`), and Pi's adapter is "a forwarder, not an owner" that "injects no Pi-only directive of its own" (`:67`). Repo-conditional evaluation would have to be implemented identically in the canonical renderer and the mirror — a new cross-runtime parity burden — and the one existing prompt classifier lives in a different concern with its own six adapters (`:83-84`), whose state is question-and-answer, not delivery (`:82`).

More decisive: the fleet's surface choice maps cleanly to where the violation is detectable, and the corpus's obligations are detectable nowhere.
- Action-detectable → **[BLOCK]** at tool time, where "the denial reason is the content the model receives" (`injection-contract.md:150-154`) — structural silence: fires only on the violating call. This is the perfect-silence surface, and it exists; it simply has no corpus subject (5.4).
- Stop-detectable reply claims → **[LOG]** advisory, kept invisible "by explicit design: 'Advisory only for the entire v1 rollout, never `{decision:"block"}`'" (`:240-244`). This is the repo's one experiment aimed at a corpus-shaped failure — evidence-and-proof's completion claims — and its deliberate setting is non-blocking and non-injected.
- Entity-resolvable prompt content → **[SYS]** prompt-time (`:100`), whose cost rationale is the fleet's best silence statement: a path that does not resolve "produces nothing, deliberately, so a filename mentioned in passing never spins a local GPU" (`:100`).
- Session-bound state → goal injections (`:106-140`), spec-gate's once-per-session-until-answered (`:82`).

So: no new mechanism is needed to *carry* an injection — one is needed to *justify firing it*, and every existing justification (score, mutation intent, resolved entity, session state, cadence) is blind to a failure that happens inside the model after delivery. The question's demand — "name the observable signal" for silence — has no satisfying answer for agent-side obligations, and the repo's own placement of reply-shape checking at Stop, invisible, is the precedent that this is by design (`:242`).

### 4. PORTABILITY — pointer form survives the layout test; the surviving remedy does not need to

- **Discovery is proven.** The advisor precedent resolves workspace "install-anchored, not CWD-relative, so the hook stays correct off-root" (`skill-advisor-hook.md:36`, `:53`, `:98`), and Gate 5's own resolution key is "any repository whose root holds a `REPO RULES.md`" (`AGENTS.md:122`). A pointer-form candidate can find the router in all three repos by the same walk.
- **Names are stable by construction.** The corpus is shared by symlink; the router routes "per-rule documents under `repo-rules/`" (`REPO RULES.md:4`), and the trigger table (`REPO RULES.md:40-50`) is the central routing index. A pointer naming `repo-rules/<file>.md` breaks nothing — those names are the corpus's own stable vocabulary.
- **Content form cannot port.** Inlining rule text duplicates the single source into a hub-owned renderer, across "independent directories" — the same divergence risk the contract already carries for the OpenCode mirror (`injection-contract.md:65`).
- **Command form cannot port.** Commands live inside hubs whose internal paths are not stable, and the corpus deliberately keeps mechanics out: the scope statement excludes "the *mechanics*" and routes them to the skills (`REPO RULES.md:85-89`), and `skill-hub-routing.md` itself points rather than names.
- **Mechanism tax, newly documented.** A new injection concern is not a file: a core must qualify as Node-builtins-only with no skill-content dependency (`.opencode/hooks/README.md:80-87`, `:171-172`), wiring is per-repo config pointing "directly at the real file" (`:173`), and the concern needs a kill-switch row (`:36`) plus a coverage-matrix row (`:229`). An fs-reading pointer hook would pass the dependency test — so portability is satisfiable for pointer form, and is **not** the deciding leg (confirming iteration 2's conclusion with the doctrine that governs it).
- **The remedy that survives is the most portable option.** Promotion into `AGENTS.md` has zero path dependencies and is identical across repositories; both promotion directions have precedent (`REPO RULES.md:80-83`; `AGENTS.md:239`).

### 5. VERDICT — refuse the injection; leave the corpus at Gate 5; change the one uncovered rule

- **INJECT: nothing.** C1 retired side (restates `AGENTS.md:121-129`; covers the write path where Gate 5 already fires). C2 retired side (content leg passes on read-only sessions, gate leg fails; and cadence dedup is provably anti-correlated with relevance). C4 retired side (gate leg fails; failure dimension invisible to the predicate class). The incumbent hygiene directive remains the bar's demonstration case — it passes because a checker adjudicates the violation and a gate blocks it (`injection-contract.md:54`; `.opencode/scripts/git-hooks/README.md:24`), exactly the property no corpus candidate has (5.4).
- **LEAVE AT GATE 5:** the corpus stays a write-time layer, and that is now defensible rather than accidental: its read-only obligations are carried residently (5.2–5.3), its broadest rule's own text says the load happens "whenever a reply is being written" (`communication.md:40-42`), and the one thing a prompt-time hook could add — a pointer on a topic class — cannot be silent on the axis that matters.
- **CHANGE THE RULE:** promote a compact form of `delegation-and-orchestration.md:166-174` §6 into the resident layer — beside the "Finding = hypothesis" row (`AGENTS.md:237-245`) or into §7 — mirroring how delivery (`REPO RULES.md:80-83`) and the four standards (`AGENTS.md:239`) were promoted. This is the only obligation in the read-only set with no carrier, and promotion is the corpus-native, fully portable remedy. Operator decision on direction (shared `AGENTS.md` vs. repo-local retention), because it decides the clause's home for all three repos.
- **The direct answer to the silence question:** for repo-rule obligations, no smart silence exists to build. Injections in this fleet stay silent where a machine can adjudicate the failure before it happens — a gate blocks it (`:54`), a checker resolves the entity (`:100`), a session boundary bounds it (`:64`, `:82`) — and where nothing can, the repo has already placed the check at a different surface and left it invisible (`:242`). "Smart" here is not a hook property; it is a property of adjudicability, and the corpus is precisely the part of the rulebase that no machine adjudicates.
