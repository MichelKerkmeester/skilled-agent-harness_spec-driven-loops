{"timestamp":"2026-09-12T13:05:22.144Z","runtime":"claude","status":"fail_open","freshness":"unavailable","durationMs":80,"cacheHit":false,"errorCode":"NONZERO_EXIT","errorDetails":"CLI_EXIT_64"}
## 1. INVENTORY — the read-side set re-verified by direct grep, plus two additions, one negative, and the carrier-form map the fix needs

The set iterations 1–6 recorded holds; all eleven trigger blocks in `repo-rules/` were re-read this pass. New entries:

- `presenting-decisions.md:37` — "About to start a multi-step stretch of work the reader will not see inside." A long read-only research or review pass — this session's shape — binds it. Carrier: `AGENTS.md:406` (content-form: "the intended path stated before a long stretch of work").
- `blast-radius.md:37` — "Any call that leaves this machine." The corpus's one action class that need not be a *file* write; Gate 5's definition is file-write-scoped by its Gate-3 framing (`AGENTS.md:122`), so the mapping is at best indirect. It is already double-carried: resident (`AGENTS.md:163`, "Name the rollback, stop for yes") plus the mechanical pre-push gate.
- **Negative:** `grep "read-only|read only" repo-rules/` returns zero. No rule file names the read-only turn; the read-side obligation set exists only in trigger phrasing. The corpus never states the division this question is about — only the resident layer and `retrieval-conventions.md:283` do.
- The corpus's own load doctrine sharpens the gap: `REPO RULES.md:12-13` — "Match on the action you are about to take"; "Load before the action. A rule read afterwards is a post-mortem." Six router rows (42 evidence, 43 delegation, 46 uncertainty, 47 communication, 48 presenting, 49 handoff) plus row 50's reporting half describe acts that are not file writes; for those rows, the router's own rule 2 is unsatisfiable through Gate 5.

**Carrier-form map (new).** Read-side obligations are carried in the resident layer in four forms: (i) full content — `AGENTS.md:239-245` ("These four bind unconditionally, including on a read-only turn where Gate 5 never fires and no rule file loads"); (ii) pointer + load command — `:404` ("Load it before answering" — communication, whose file states why at `communication.md:40-42`: a reply-reader "has to load whenever a reply is being written, or it silently stops applying to the short answers that need it most"); (iii) pointer + content summary — `:406`, `:408`; (iv) bare expansion pointer — `:418`, `:488` (both verified by grep). The delegation self-lens clause (`delegation-and-orchestration.md:47`) sits in class (iv) with no content and no load command; a hard grep of AGENTS.md for "own reading|single model" returns only `:239`'s unrelated sentence.

**New evidence on why phrase-keyed detection would miss it:** the rule's own `trigger_phrases` (`delegation-and-orchestration.md:4-24`) enumerate delegation machinery ("sub-agent", "fan-out lineage", "deep loop", "two delegates disagree") — the vocabulary of the *compliant* action — while the human-readable router row (`REPO RULES.md:43`) is what names the failure ("answer a judgment question from your own reading alone"). The machine-readable phrase list and the failure case are disjoint by construction.

**Live demonstration:** this turn binds the clause (a judgment question answered from own reading) and no rule file is loaded. What carried the grounding requirement was the operator's briefing ("every claim carries a file:line"), an operator-side workaround of exactly this gap.

## 2. CANDIDATES — three new constructions, one retest, and a policy test of the directive slot

**(C1) Retrieval-lane registration of repo rules — retest of iteration 4, with the parent record now read.** Trigger: phrase hit in the Gate 1 lookup (`AGENTS.md:61` makes that lookup a Gate Action that runs on read-only turns). Silence: no hit — a genuine predicate; the frontmatter exists. Cost: "surface a rule as a context candidate" where no action is at hand — the policy's own words — plus the committed index's cold-parse cost on every lookup (`retrieval-conventions.md:281`). Bar side: **retired side; the parent round already ruled "Defend, do not amend" (`rules-round2/iterations/iteration-010.md:83`), and the coverage table is parity-enforced** (`retrieval-conventions.md:270`). Iteration 4's refusal holds. New content: a *rationale correction* — the entry's stated reason ("loaded at Gate 5 through the trigger table", `:283`) under-describes the corpus, since six rows cannot load that way on read-only turns; the defensible reason is the parent's ("rule files are obligations, not context", `iteration-010.md:83`) plus the resident-layer division. That is a doc sentence, not a hook.

**(C2) Advisor-graph rule entries — new.** Trigger: advisor score ≥ 0.8 (`skill-advisor-hook.md:21`, `:37`) — the fleet's only working relevance threshold, and Gate 2 fires on read-only turns (the parent round said exactly this: `rules-round2/prior-findings.md:132`, "a repo rule would be write-gated (`AGENTS.md:122`) — strictly narrower than the mechanism it duplicates"). Silence: below threshold. Cost: a second, non-authoritative router for the same vocabulary, drifting against the real one. Bar side: **retired, three legs** — (a) the corpus's scope statement puts the advisor's domain Out ("skill routing, workflow selection", `REPO RULES.md:85-89`): the corpus is conduct, the advisor routes expertise; (b) advisor identity files are skill-root/hub-only artifacts (`AGENTS.md:112`), so rules cannot be graph nodes without a fake hub; (c) threshold silence is still *topic* silence — it fires on the compliant judgment-discussion population and misses the solo-answer turn, per C1's demonstrated mismatch.

**(C3) Tool-time consent guard for destructive/egress calls — new, the strongest surviving construction.** Trigger: a bash call whose shape matches destroy/egress patterns. **Silence: the command does not match — per-call, observable, real.** Cost when wrong: fires on compliant calls; the same `rm -rf` or `curl POST` is correct when the operator asked for it, and "was there an intervening yes" is a heuristic over the transcript, not a property of the call. Bar side: **fails the gate leg and repeats signal≠failure.** The tool-time advisory precedents fire where the failure *is* the payload — dispatch-preflight's `opencode run` without a stdin redirect is visible in the command text (`injection-contract.md:156-167`, sample `:162`); mcp-route-guard fires on the wrong-shaped call (`:169-175`). Blast-radius's failure is the missing confirmation, which is not in the payload. The mechanically checkable subset already has an enforcer (pre-push). Refuse.

**(C4) Session-start corpus pointer — new channel.** Trigger: session start in a repo holding `REPO RULES.md` (`injection-contract.md:212-218`). Silence: only repo-absence. Cost: fires every session, restating the resident Gate 5 block (`AGENTS.md:121-129`), which already names REPO RULES.md and the trigger-table procedure. Bar side: **retired** — constant restatement, no gate.

**Directive-slot policy test — tests iteration 6's C1 without repeating it.** The bloat-reduction packet's scope forecloses the conditional form by policy: "Unconditional directive removal or no-match/failure silence (breaks the fail-open guardrail contract)" is explicitly Out (`specs/hooks/002-injection-bloat-reduction/spec.md:75`), the program's aim is "full policy once per session/lifecycle epoch, route-only deltas on repeats" (`:62`), and the retention test is verbatim in the retirement packet: "It names one prohibition, and a pre-commit gate rejects a commit that violates it. That is a rule with an enforcer, not a disposition, and it stays" (`specs/hooks/014-retire-governor-and-proof-directives/spec.md:71-73`). Consequence: the slot admits only content that is unconditionally in force *and* enforcer-backed; no corpus rule is either. The failure-based silence that would make a rule's injection smart is *prohibited by policy* in that slot, and fail-open ("any error also stays full", `injection-contract.md:64`) would defeat it anyway.

## 3. SURFACE — the contract carries text; each prompt-time surface fails a different requirement

The four prompt-time surfaces are the advisor brief (`injection-contract.md:50-68`), spec-gate (`:70-84`), sk-vision (`:86-104`), and goal (`:106-140`). Mapped against a corpus obligation: the classifier points at the write path by construction (`:82`; iteration 5's negative proof); entity resolution resolves content the model *cannot* otherwise see (`:100`, the OCR block) — a rule file is content the model can read, so resolution adds nothing; session binding is operator-created state, not corpus advice; the advisor's threshold routes expertise, not conduct. No new surface is needed, and none fits.

Corrections to iteration 6's surface claim, from the checker itself: the corpus is now a gated artifact in its own right — `.github/workflows/repo-rules-corpus.yml:21-36` runs `check-repo-rules.cjs` fail-closed on PRs touching the router or corpus (this is the parent round's P5 item 1, recommended at `iteration-010.md:77`, since executed). The checker's module comment states the corpus has exactly one consumer class: "nothing else reads it… no hook or workflow touches the router" (`check-repo-rules.cjs:5-9`). Iteration 6's "second loader the corpus gate cannot see" is directionally right but mechanistically wrong: the checker polices files↔router↔phrases *agreement* (`:24-35`, `:166-195`), not name stability, so a consistent rename passes CI — a name-hardcoding hook rots silently, and a re-deriving one becomes the first machine consumer whose cost the policy already priced (`retrieval-conventions.md:281`).

## 4. PORTABILITY — discovery is doubly proven; names are the part that is not frozen

The `(REPO RULES.md + repo-rules/)` discovery key now has a second production precedent beside the advisor's install-anchored walk (`skill-advisor-hook.md:36`): `check-repo-rules.cjs:42-52` (`findRepoRoot` walks up for both, "so the command works from anywhere in the tree"). Discovery over a differently laid-out repo is proven twice over.

Correction of iteration 5's "Names are stable by construction": the corpus *permits* consistent renames — the checker fails only on disagreement, so router+file moving together passes. A hook must therefore either re-derive names from the router at every fire (a prompt-time parse, priced at `retrieval-conventions.md:281`) or bake names and rot on renames CI blesses. Pointer-form survives only as the former, and that cost belongs in the record. The surviving remedy needs none of this: a resident clause is path-free and byte-identical across all three repositories.

## 5. VERDICT

**Refuse the injection — all lanes, all constructions tested across seven iterations.** No repo rule is better served by a prompt-time hook than by the mechanisms that already exist; the question's dichotomy is false for the read-side set. Gate 5 cannot serve those obligations (`AGENTS.md:122`: "Read-only turns never fire it"), the resident layer already serves every one except `delegation-and-orchestration.md:47`, and the single prompt-time slot that could carry a directive is reserved for gate-backed §1-class prohibitions (`014/spec.md:71-73`) with failure-silence banned (`002/spec.md:75`). The two wishes that motivated this packet are likewise served by *wider* mechanisms — a write-gated rule or hook would be, in the parent round's words, "a third, narrower mechanism… competing with two wider ones (agent ALWAYS; Gate 2/artifact trigger)" (`rules-round2/iterations/iteration-010.md:48`), and the repo's investment went instead into corpus integrity (`repo-rules-corpus.yml:21-36`).

**Change the rule instead — one clause, operator's call.** The delegation self-lens clause is the sole uncovered read-side obligation; the remedy inherits an exact template: §8's *load-command* form (`AGENTS.md:404`), the mechanism the corpus documents for its broadest-trigger rule (`REPO RULES.md:80-83`). Candidate homes: give the `:418`/`:488` pointers a load command, or add the mirror row beside "Finding = hypothesis" in the standards table (`:241-245`) — self-lens is that row's inward reflection. It modifies shared `AGENTS.md`, so portability is trivial.

**Recorded corrections to earlier iterations:** the packet premise ("both refused because a rule file cannot reach a read-only turn", `specs/hooks/022-smart-rule-injection/spec.md:39-40`) is narrower than the parent record — reach was one deciding test among several (scope boundary for P2, `iteration-010.md:24`; routing ownership plus reach for P3, `:40`); the retrieval entry's rationale under-describes the corpus (§2 C1); iteration 6's checker mechanism is corrected in §3. The hard constraint is answered: for every refused candidate the silence condition is stated in §2, and the finding is that rule content admits only four silences — vocabulary/entity absence (misses the failure), delivery cadence (anti-correlated, and policy-banned in that slot), non-matching tool shape (real but decoupled from the omission), and session presence (restates resident text). No fifth kind exists in the fleet, and the two mechanisms that do bind read-side are standing carriers, not hooks.
