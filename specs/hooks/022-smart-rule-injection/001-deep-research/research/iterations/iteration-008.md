{"timestamp":"2026-09-12T13:11:50.366Z","runtime":"claude","status":"fail_open","freshness":"unavailable","durationMs":92,"cacheHit":false,"errorCode":"NONZERO_EXIT","errorDetails":"CLI_EXIT_64"}
## ITERATION 8 FINDINGS

*New this pass: the corpus's own admission procedure (`sk-create-repo-rule` decision tests — never cited in this lineage), the directive slot's purpose statement verbatim, firsthand verification of iteration 7's load-bearing citations, and the design-space closure. Where I verified an earlier iteration's claim, I say so; one minor citation correction is recorded.*

---

### 1. INVENTORY — the read-side set, re-verified, plus the policy that classifies it

**The trigger set (my own grep this pass; all resolve):** `delegation-and-orchestration.md:41` (judgment question answered "from your own reading alone"), `:37-40`; `communication.md:37` (every substantive reply); `handoff-and-questions.md:35-39`; `presenting-decisions.md:35-39` (incl. `:37`, a multi-step read-only stretch — this session's shape); `evidence-and-proof.md:34-37`; `uncertainty-and-honesty.md:35` (naming an unverified path/flag/version); `skill-hub-routing.md:38` (reporting routing/claiming integration); `root-cause-and-debugging.md` fires-when block (diagnosis is normally read-only). **Negative re-verified firsthand:** `grep "read-only|read only" repo-rules/` → zero matches. No rule file names the read-only turn; the read-side set exists only in trigger phrasing.

**Carriers re-verified by direct read this pass:** `AGENTS.md:239` ("These four bind unconditionally, including on a read-only turn where Gate 5 never fires and no rule file loads") + table `:241-246`; §8's load command `:404`; §7 mandates `:482-484`; and the two `delegation-and-orchestration.md` pointers `AGENTS.md:418` and `:488` — both confirmed *bare* (expansion pointer, no content, no load command). Delegation's self-lens clause read in full: statement at `:45-47` ("no single model's verdict, the delegate's or your own, closes a question"), inward expansion §6 at `:166-174`, self-check `:227`. **It remains the sole uncovered read-side obligation.** The parent round counted the same two carriers independently (`agents/010-repo-rule-system-integration/.../rules-round2/iterations/iteration-010.md:83` — "the two mechanisms that *do* bind across read-only turns — the four unconditional verification standards (`AGENTS.md:239-246`) and §8's load instruction (`:404-410`)").

**The new finding: the corpus has an admission procedure and it is decisive here** (`sk-create-repo-rule/references/decision-tests.md`, missed by iterations 1–7 whose citation base was `REPO RULES.md` and the retirement packets). Test 1: *"A rule file loads on a trigger. Content that must bind when no trigger has fired cannot live in one"* (`:32-33`); *"Ask: on a turn where nothing fires, must this still hold?"* (`:38`); *"**Yes** → it belongs in `AGENTS.md`. Refuse the rule"* (`:40`). Its §5 destination table (`:129-135`) admits exactly four homes — AGENTS.md as a compressed row (`:131`), a §2/skill surface, a section in an existing rule, or nowhere. **Injection is not a destination the procedure has.** The near-miss paragraph states the read-side problem as policy: communication "survives only because its trigger was widened to *every substantive reply* — and §8 still keeps the two clauses that must bind unconditionally. **A total move needs a total trigger, or the content goes quiet**" (`:48-51`). The read-side set is precisely Test-1 content.

**The wiring model (`agents-md-integration.md`):** "The router's trigger table is the only thing that **loads** one; the pointer from the governed `AGENTS.md` section is the only thing that makes it **findable at the moment of need**" (`:25-27`); three wiring points, no fourth (`:33-41`). A rule-injecting hook would be an unmodelled fourth wiring point, not a hook detail.

### 2. CANDIDATES — the three constructions still formally open, tested against the new policy sources

Iterations 1–7 already refused the predicate constructions (push policy, routing vocabulary, debugging, retrieval index, Gate-3 classifier, vocabulary-keyed directives, advisor constants, completion back-loop, blast tool-time, session-start pointer, advisor-graph entries). I do not repeat them; their silence conditions are on record. What remains is the *class-level* test and the design space's other pole:

**N1 — "Any corpus rule in the prompt-time directive slot" (class test).**
- **Trigger:** whichever rule-vocabulary predicate a builder picks; the slot fires on every prompt and the predicate only decides payload.
- **Silence:** inherits the lifecycle — full constant on the first proven message and after lifecycle boundaries, route-only otherwise, "any error also stays full" (`injection-contract.md:64`). Positional, never relevance; no rule-relevance predicate exists in the fleet (iterations 4–6). "Fires when relevant" cannot be written for corpus content.
- **Cost when it fires wrongly — now measured:** the pre-reduction constant payload was ~763 bytes / ~190 tokens, ~94.7% of the per-turn advisor payload, with a 10-turn session carrying ~9,600 bytes of repeated policy text (`specs/hooks/002-injection-bloat-reduction/spec.md:59`); the shipped program is "full policy once per session/lifecycle epoch, route-only deltas on repeats," targeting an ~82% cut (`:62`). A new constant partially reverses a measured, shipped reduction. Wrongly-fires case: every read-only turn of every session.
- **Bar side — retired, and now purpose-barred.** The slot's admission rule is verbatim: *"Every prompt carries the directive that has an enforcement mechanism, and nothing else"* (`specs/hooks/014-retire-governor-and-proof-directives/spec.md:77`; the survival test at `:71-73`). No corpus rule has an enforcer: the gate inventory is nine mechanical gates (`git/README.md:18` — comment hygiene, mirror-sync, mass-deletion, doc-model-refs, mirror-parity, prompt-card-sync, MCP mutation-class, compiled-routing re-mint, tool-ownership) and the one claim-shape checker is `[LOG]`-only "by explicit design" (`injection-contract.md:240-244`). Both bar prongs fail by construction of the corpus's own precedence: rule files are level 3 (`REPO RULES.md:24-32`).

**N2 — "Delegation §6 load-command on judgment turns" (the last standing construction).**
- **Trigger:** judgment-question vocabulary (`delegation-and-orchestration.md:41`).
- **Silence:** "no judgment vocabulary in the prompt" — observable, but topic-absence, not failure-absence: it fires on the compliant judgment-discussion population and cannot see the solo-answer choice, which is made after the prompt event every predicate can read (iterations 5–6's proof, cited not restated; the clause's own conditional "from your own reading alone" has no prompt-time signature). The fleet's one backward predicate observes only a finished turn (`injection-contract.md:106-140`).
- **Cost when it fires wrongly:** re-asserts an epistemic stance on every design turn; carries level-3 content that cannot match the rule's force; and it would make the hook a second machine consumer of a corpus whose design statement is "nothing else reads it… no hook or workflow touches the router" (`check-repo-rules.cjs:5-9`).
- **Bar side — retired, and now procedure-routed.** It is Test-1 content ("must bind on a turn where nothing fires" — the read-only judgment turn), so the procedure's destination is `AGENTS.md` as a compressed row (`decision-tests.md:38-40`, `:131`). The candidate is answered before the bar is reached.

**N3 — "Extend Gate 5 to read-only turns" (the design space's other pole).**
- **Trigger:** every prompt in a repo holding `REPO RULES.md`.
- **Silence:** none that is not cadence — constant delivery.
- **Cost when it fires wrongly:** surfaces a rule "as a context candidate" where no action is at hand — the failure pre-ruled in the retrieval-lane decision (`retrieval-conventions.md:283`, verified verbatim: "indexing them would surface a rule as a context candidate"); the parent's reading: "rule files are obligations, not context" (`iteration-010.md:83`).
- **Bar side — retired** (the constant shape). Its role is closure: trigger-loading misses read-side by construction (`AGENTS.md:122`); universal loading spams; and the middle — selective prompt injection — is closed by N1 (no admissible purpose) and N2 (no failure-observable predicate). **The design space is exhausted, not merely unmapped.**

### 3. SURFACE — carryable mechanically; three policy locks, one design lock

The contract can transport text without a new mechanism class: canonical renderer and adapters (`injection-contract.md:65-66`), per-runtime channels (`:66`). Three locks close it for rule content. (i) The slot's purpose (`014:77`) admits only enforcement-backed prohibitions — a positive rule, stronger than iteration 7's negative reading of the retention test. (ii) "No-match/failure silence" in this lane is *policy-prohibited* — verbatim Out: "Unconditional directive removal or no-match/failure silence (breaks the fail-open guardrail contract)" (`002:75`) — and the question's demanded silence is exactly that. (iii) Within the one lane, the failure directions are documented and opposite: a scoring/graph failure resolves to `{}`, "the prompt proceeds untouched" (`skill-advisor-hook.md:25`, `:134`), while a dedup-state failure "stays full" (`injection-contract.md:64`) — any state-based smart silence fails toward the spam the operator forbids. Design lock: the corpus is single-consumer by written design (`check-repo-rules.cjs:5-9`), policed by one checker now scheduled by CI (`.github/workflows/repo-rules-corpus.yml:5-8`, `:24-36`); a hook would be the second machine consumer, outside the checker's walk. *(Minor correction to the literal comment: the workflow now triggers on `REPO RULES.md`/`repo-rules/**`; "no workflow touches the router" should read "no runtime consumer" — the consumption model is unchanged.)* Adding a directive is a multi-file code change at the canonical owner plus both fallback emitters (`014:86-87`, `:100-105`), not configuration.

### 4. PORTABILITY — discovery proven twice; names unfrozen; the hook would re-implement, the resident remedy is path-free

Discovery over a different layout is proven twice: install-anchored walk (`skill-advisor-hook.md:36`, "not CWD-relative, so the hook stays correct off-root") and the checker's own `findRepoRoot` (`check-repo-rules.cjs:42-52`, "works from anywhere in the tree") — my own read, both verified. Names are the unstable half: the checker fails only on *disagreement* among files↔rows↔phrases (`:24-35`), so a CI-blessed consistent rename passes — a name-baking hook rots silently; a re-deriving hook parses the router per fire (iteration 7's correction, which I confirm). New this pass: the checker lives at a **hub-internal path** (`.opencode/skills/sk-doc/sk-create-repo-rule/scripts/`); hub names are stable, paths inside hubs are not (`spec.md:52-56`) — so a portable hook cannot invoke or reuse the checker and must re-implement the ten-line walk. And the federation rule, verbatim: a shared rule is "edited, versioned and retired in one place" (`agents-md-integration.md:141-144`) — a hook carrying rule content becomes a second place to change, per sibling, with none of the four sibling mechanics accounting for it (`:146-172`). The resident remedy needs zero paths and is byte-identical across all three repositories.

### 5. VERDICT

**REFUSE the injection — every construction, now over-determined by six independent closures:** the slot's purpose admits only enforcement-backed content (`014:77`); no corpus rule is enforcement-backed (nine mechanical gates, `git/README.md:18`; `[LOG]`-only claim checker, `injection-contract.md:240-244`); Test 1 routes read-side-binding content to the resident layer (`decision-tests.md:38-40`); no failure-aligned prompt-time predicate exists (iterations 4–6, N2); failure-keyed silence is policy-prohibited and fail-open works against it (`002:75`, `injection-contract.md:64`); and the corpus is single-consumer by design (`check-repo-rules.cjs:5-9`).

**LEAVE AT GATE 5** for the action-triggered corpus — unchanged; the router's own doctrine holds where a write exists to match (`REPO RULES.md:12-13`).

**CHANGE THE RULE INSTEAD — one clause, form now prescribed by procedure.** Delegation §6 (`:166-174`) is the sole uncovered read-side obligation; Test 1 sends it to `AGENTS.md` "as a compressed row" (`decision-tests.md:131`) — the refinement over iteration 7's two-homes suggestion: the procedure names the form. Fitting homes: beside "Finding = hypothesis" in the standards table (its inward mirror, `AGENTS.md:245`) or promoted in §9's pointer region (`:418`/`:488`, verified bare today). Operator decision — integration §6 draws the boundary: "Adding or removing a pointer is mechanical. Everything else escalates" (`agents-md-integration.md:120-127`).

**Recorded compliance with the packet's acceptance rule** (`spec.md:69`): the refusal is recorded with the tests that produced it — Test 1 (always-loaded) refuses the class; the directive-lane purpose statement refuses the carrier; Test 2 (scope/routing) was the parent's decider for the two motivating proposals (`iteration-010.md:24`, `:40`), with the read-only constraint as the additional, not sole, reason (`:26`). **Corrections:** iteration 2's paraphrase citing `git/README.md:18` as "mechanical invariants only" slightly miscites the line (it is the hook-inventory sentence); the substance is confirmed by the nine named gates in that file (`:18`, `:28-29`). All other iteration-7 citations I tested this pass verified as written.
