# Pi Remote Deep-Review Synthesis

## Executive Verdict

The build has no verified P0 defect. After source verification and semantic deduplication, the merged registry's 15 P1 rows reduce to **five real P1 blockers**: missing transcript projection, incomplete device-revocation authority drain, accept-edits grants that survive invalidation, an incomplete authoritative release gate, and stale canonical packet state. The implementation is a coherent, tested monitoring-first scaffold, not the mobile agent-control product promised by the parent outcome: live command transport, per-session children, durable enrollment, and live protected mutation are explicit current limits rather than newly discovered defects (`.pi/pi-remote/README.md:3-12`; `.pi/pi-remote/docs/security.md:51-71`; `.pi/pi-remote/docs/architecture.md:37-70`). Release remains **CONDITIONAL** until the five P1s are fixed or the governing product scope is formally narrowed, and the operator-only rollout evidence remains pending (`.pi/pi-remote/docs/release-verification.md:71-85`).

## Verification Summary

- Registry IDs reviewed: **39**.
- Canonical claims after semantic deduplication: **36**. Three merges: `F010` + `P1-003` and `F011` + `P2-022` are duplicate cross-lineage claims; `P2-009` + `P2-015` are a same-lineage near-duplicate.
- Status by canonical claim: **17 CONFIRMED, 1 PLAUSIBLE, 18 REJECTED** (= 36; matches the ranked table and `synthesis.json`).
- Canonical severity: **5 P1, 13 P2, 18 rejected/no active severity**.
- Cross-model corroboration: **2 canonical claims / 4 registry IDs** (`F010` + `P1-003`; `F011` + `P2-022`). The reducer's per-ID `_lineages` metadata misses these semantic duplicates.

## Ranked Findings

Validated severity is shown below. A dash means the hypothesis is rejected and creates no remediation item.

| IDs | Severity | Status | Corroboration | Source | Root cause | Concrete fix |
|---|---|---|---|---|---|---|
| F007 | P1 | CONFIRMED | single | `.pi/pi-remote/apps/pi-remote-relay/src/approval/approval-service.ts:292-317` | `requestFromGrant()` neither rechecks current policy nor invalidates active grants when policy, epoch, service, or restart state changes; lifecycle drains only leases/in-flight work at `:351-354,423-443`. | Add transactional grant invalidation for policy disable/family change, epoch invalidation, service close, restart, and device revocation; recheck `policy.isAllowed(action.tool)` before decrementing a grant; add negative lifecycle tests. |
| F006 | P1 | CONFIRMED | single | `.pi/pi-remote/apps/pi-remote-relay/src/http/server.ts:340-344` | Device revocation calls `AuthService` and push unsubscribe only; it never asks `ApprovalService` to revoke authority, contrary to the revocation contract (`004-auth-and-tailnet-boundary/spec.md:109`). | Add a principal/device-to-approval revocation API that drains matching leases, grants, and in-flight work before returning 204; test revoked-device authority end to end. |
| F003 | P1 | CONFIRMED | single | `.pi/pi-remote/apps/pi-remote-relay/src/index.ts:119-144` | Pi events are persisted as `pi.*`, while transcript storage and the PWA accept only `transcript.block` (`relay-store.ts:297-316`; `state.ts:269-276`); no producer exists. | Add a typed Pi-event-to-transcript-block projector before publish, preserving stable block IDs/revisions and redaction; test text, thinking, plan, tool, diff, result, usage, and unknown events. |
| F014 | P1 | CONFIRMED | single | `.pi/pi-remote/scripts/release-verify.mjs:30-43` | The authoritative runner executes typecheck, Vitest, builds, rollback, and thresholds, but no browser suite; the app defines no lint/format scripts (`package.json:11-18`) despite the phase gate requiring them (`009-release-verification-and-rollout/spec.md:103-109`). | Add real browser coverage and repository-approved lint/format scripts, then make `release:verify` invoke them and record their versions/output. |
| F010 + P1-003 | P1 | CONFIRMED | both | `specs/cli-external-orchestration/001-pi-remote-mobile-agent-like-cc/spec.md:12-25,45-52`; `001-contract-and-threat-baseline/implementation-summary.md:37-50` | Canonical continuity and all phase summaries still say Draft/not started although implementation, tests, docs, and release evidence exist (`.pi/pi-remote/package.json:11-18`; release evidence `:19-107`). | Reconcile parent and all nine child statuses, completion claims, checklists, summaries, evidence links, and implemented-vs-accepted-limit boundaries; do not mark operator-only gates complete. |
| P2-019 | P2 | CONFIRMED | single | `research/deep-research-config.json:11`; `research/orchestration-summary.json:3-5` | Both research pointers use `041=` instead of the real `041-` folder name. | Correct the two pointers and regenerate only the research reduction artifacts through the owning workflow. |
| P2-018 | P2 | CONFIRMED | single | `research/deep-research-dashboard.md:3-6`; `research/lineages/cli-pi-gpt-56-sol/deep-research-dashboard.md:3-23`; `research/lineages/cli-pi-deepseek-v4-flash/deep-research-dashboard.md:3-23` | The top-level reducer remained at 0/20 although multiple lineage syntheses completed. | Repair the path first, rerun the research reducer, and verify parent dashboard/registry totals against lineage terminal states. |
| P2-020 | P2 | CONFIRMED | single | `spec.md:95-107` | Aggregate planned paths were never reconciled to the actual `.pi/pi-remote/{tests,deploy,docs}` layout. | Update the aggregate and affected child file maps to the live app-local paths, explicitly recording the workspace relocation. |
| P2-023 | P2 | CONFIRMED | single | `008-documentation-and-runbooks/spec.md:78-86`; `.pi/pi-remote/docs/architecture.md:20-35` | Planned document filenames no longer match the split live runbooks, and no dedicated protocol document carries the API/schema compatibility contract. | Reconcile the planned deliverable map with the eight live docs and add a protocol reference, or designate and verify an existing document as the canonical protocol surface. |
| P2-013 | P2 | CONFIRMED | single | `009-release-verification-and-rollout/tasks.md:63-71`; `009-release-verification-and-rollout/plan.md:157-162` | The task compresses rollback into a misleading order, while the detailed plan and code correctly disable/drain before restore (`rollback-drill.ts:90-127`). | Rewrite the task to the authoritative order: disable mutation, remove ingress, revoke/drain, stop, restore/down-migrate, preserve sessions, smoke test. No code fix. |
| P2-021 | P2 | CONFIRMED | single | `.pi/pi-remote/apps/pi-remote-relay/src/policy/mutation-policy.ts:9-42` | The docstring says families are independently enabled, but `enableFamily()` clears the prior family and status exposes one family. | Change the durable description to "exactly one enabled family" and add/assert replacement semantics in the policy test. |
| F015 | P2 | CONFIRMED | single | `.pi/pi-remote/apps/pi-remote-relay/src/auth/policy.ts:1-32` | State-changing approval, accept-edits, push, logout, and revoke operations are named and authorized under `ReadOnlyAction`, obscuring audit/policy semantics. | Rename the action taxonomy to neutral authenticated actions or split read/query from state-changing actions without weakening default deny. |
| P2-001 | P2 | CONFIRMED | single | `001-contract-and-threat-baseline/spec.md:171-180` | Six phase totals say 81 although their component rows sum to 71. | Recalculate and correct all six affected totals, then add a packet arithmetic check. |
| P2-011 | P2 | CONFIRMED | single | `001-contract-and-threat-baseline/plan.md:257-273`; `001-contract-and-threat-baseline/decision-record.md:136-155,203-227` | The plan's ADR section mirrors only the baseline ADR and omits the accepted PWA-stack and supported-baseline decisions. | Link or summarize ADR-002 and ADR-003 in the plan's decision section. |
| P2-016 | P2 | CONFIRMED | single | `001-contract-and-threat-baseline/implementation-summary.md:10-23` | The continuity pointer omits the checklist, decision record, and implementation summary itself across all phases. | Regenerate phase continuity metadata with all canonical resume files after status reconciliation. |
| P2-009 + P2-015 | P2 | CONFIRMED | single | `006-approval-and-remote-mutation/spec.md:104-113` | The spec defines default-off/family gating but not the operational trigger, authentication boundary, or in-flight/restart semantics. Code implements startup-only changes and in-flight abort (`security.md:55-65`), so this is spec closure, not a new runtime bug. | Amend the requirement to state startup-local control, stop/restart procedure, lease/grant drain, in-flight abort, and restart behavior. |
| P1-001 | P2 (downgraded) | CONFIRMED | single | `003-relay-protocol-and-state/spec.md:82-110`; `003-relay-protocol-and-state/plan.md:144-152` | Phase 003 requires an authenticated command channel while excluding auth and listing phase 004 only as a successor, making its phase-local closure unsatisfiable. Downgraded because phase 004 owns the implemented application-auth boundary (`004-auth-and-tailnet-boundary/spec.md:103-112`). | Make phase 004 an explicit provider/dependency for command-channel authentication, or narrow phase 003 to the typed/idempotent command contract and defer authentication acceptance to phase 004. |
| P2-012 | P2 | PLAUSIBLE | single | `008-documentation-and-runbooks/spec.md:102-111`; `.pi/pi-remote/docs/incident-playbooks.md:46-62` | The phase requirement does not explicitly name compromise response; the live lost-device playbook does prescribe restart/re-enrollment but not a general credential-compromise trigger or rotation workflow. | Add a concise suspected-compromise playbook linking detection to ingress removal, device/session revocation, secret/key rotation, evidence preservation, and re-enrollment. |
| F001 | - | REJECTED | single | `.pi/pi-remote/apps/pi-remote-relay/tests/auth.test.ts:78-99`; `.pi/pi-remote/README.md:3-12` | No command route exists, but the deployed app intentionally rejects prompt mutation and is documented monitoring-first; this is an accepted product gap, not a hidden implementation defect. | No new bug work. Track live command transport only if the parent product scope remains unchanged. |
| F002 | - | REJECTED | single | `.pi/pi-remote/apps/pi-remote-relay/src/index.ts:20-48`; `.pi/pi-remote/docs/architecture.md:37-38` | The fixed host/workspace/session and one child are real but explicitly documented as the current single-host design. | No new bug work. Treat multi-session child ownership as planned scope reconciliation. |
| F004 | - | REJECTED | single | `.pi/pi-remote/apps/pi-remote-relay/src/rpc/supervisor.ts:164-173`; `.pi/pi-remote/docs/rollback.md:81-85` | ENOENT fixture fallback and health omission are real and explicitly documented with an operator live-event check; the review relisted an accepted limit. | No new bug work. Keep fixture labeling and operator smoke-test evidence. |
| F005 | - | REJECTED | single | `.pi/pi-remote/apps/pi-remote-relay/src/auth/auth-service.ts:63-98`; `.pi/pi-remote/docs/security.md:27-39` | Authentication state is memory-only by deliberate, prominently documented design; restart re-enrollment is the accepted recovery model. | No new bug work unless durable enrollment becomes a new requirement. |
| F008 | - | REJECTED | single | `.pi/pi-remote/apps/pi-remote-web/src/cache.ts:13-16,32-83`; `.pi/pi-remote/docs/rollback.md:64-69` | Cache survives logout/revocation, but the product explicitly retains a bounded stale read-only cache as non-authority. This conflicts with stale phase wording, not the documented runtime contract. | No runtime fix under the accepted model; reconcile the phase requirement to retained evidence or explicitly change the privacy policy first. |
| F009 | - | REJECTED | single | `.pi/pi-remote/apps/pi-remote-relay/src/http/server.ts:168-176,464-479`; `.pi/pi-remote/docs/architecture.md:37-60` | Selectors are not principal-scoped, but the current product has one fixed host/workspace/session and no multi-tenancy; opaque arbitrary IDs yield unknown/empty state, not cross-tenant data. | No current bug work. Add principal/workspace ACLs before multi-session or multi-user expansion. |
| F011 + P2-022 | - | REJECTED | both | `.pi/pi-remote/scripts/release-verify.mjs:61-92`; `.pi/pi-remote/docs/release-verification.md:3-17,43-69` | Top-level PASS means runnable machine gates passed; stage readiness is separately printed, recorded, and enforced by `--require-ready`. The contract explicitly says missing operator evidence keeps stages NOT-READY. | No logic fix. An optional field rename (`machineStatus`) could reduce ambiguity but is not required remediation. |
| P1-002 | - (downgraded then rejected) | REJECTED | single | `.pi/pi-remote/apps/pi-remote-web/src/auth.ts:12-21,91-101,181-205`; `.pi/pi-remote/docs/security.md:27-39` | The implementation pins a non-extractable IndexedDB device key, clears it on device revocation, and deliberately retains it on logout; the review conflated the HttpOnly session cookie with browser-managed credentials. | No code fix. Add explicit ownership wording to phase 004/005 only during packet reconciliation. |
| P2-002 | - | REJECTED | single | `001-contract-and-threat-baseline/spec.md:86-93`; `002-automated-test-harness/spec.md:86-94` | Both phases name the same planned directory, but phase 001 owns definitions while phase 002 owns executable tests; no conflicting files or live duplicate producer was shown. | No remediation unless exact file ownership later collides. |
| P2-003 | - | REJECTED | single | `001-contract-and-threat-baseline/spec.md:110-118`; `002-automated-test-harness/spec.md:111-119` | Reused cross-cutting IDs are phase-qualified by folder, and the review found zero ambiguous bare citations. | No remediation. Use folder-qualified citations. |
| P2-004 | - | REJECTED | single | `004-auth-and-tailnet-boundary/spec.md:103-112`; `.pi/pi-remote/deploy/setup-tailscale-serve.sh:16-44` | The requirement allows two concrete mechanisms; implementation unambiguously pins the ephemeral secret branch. | No security fix. Record the selected branch during packet reconciliation. |
| P2-005 | - | REJECTED | single | `008-documentation-and-runbooks/spec.md:136-173` | `L2:` headings are deliberate Level-2 template structure, not a defect. | No remediation. |
| P2-006 | - | REJECTED | single | `008-documentation-and-runbooks/spec.md:28-29`; `008-documentation-and-runbooks/checklist.md:28-42,103-109` | Phase 008 is Level 2, so no ADR or Level-3 CHK-100 block is required; the checklist explicitly says "decision where required." | No remediation. |
| P2-007 | - | REJECTED | single | `008-documentation-and-runbooks/tasks.md:60-70`; `008-documentation-and-runbooks/plan.md:105-124` | "Implementation" versus "Core Implementation" is harmless display wording with no behavioral or ownership ambiguity. | No remediation. |
| P2-008 | - | REJECTED | single | `005-mobile-pwa-and-reconciliation/spec.md:107-117`; `007-push-and-platform-hardening/spec.md:103-108` | Phase 005 needs three attention surfaces, while phase 007 intentionally owns and defines the bounded class vocabulary. | No remediation; an explicit forward link is optional. |
| P2-010 | - | REJECTED | single | `006-approval-and-remote-mutation/spec.md:104-109,151-160` | The canary list already covers logs, test artifacts, and all implemented durable/remote surfaces; no telemetry or diagnostics exporter exists to omit. | No speculative work. Add those rows only when such exporters are introduced. |
| P2-014 | - | REJECTED | single | `009-release-verification-and-rollout/spec.md:125-129`; `.pi/pi-remote/release/rollout.json:3-45` | The spec states the invariant and the authoritative config enumerates every stage/evidence subset. Duplicating the config in prose would add drift. | No remediation. Keep the config as source of truth. |
| P2-017 | - | REJECTED | single | `description.json:21-25`; `review/orchestration-summary.json:1-20` | The memory names say the deep review completed; both lineages did complete 20 iterations. `CONDITIONAL` is a verdict, not incomplete execution. | No remediation. |

## Buckets

### A. Real Code Bugs To Fix Now

1. `F007`: grant invalidation and policy recheck (`approval-service.ts:292-317,351-354,423-443`).
2. `F006`: device-revocation authority drain (`http/server.ts:340-344`; contract `004/spec.md:109`).
3. `F003`: typed transcript projection (`index.ts:119-144`; consumers `relay-store.ts:297-316`, `state.ts:269-276`).
4. `F014`: authoritative gate coverage (`release-verify.mjs:30-43`; required matrix `009/spec.md:103-109`).
5. `F015`: authorization taxonomy cleanup (`auth/policy.ts:1-32`).
6. `P2-021`: mutation-policy description correction (`mutation-policy.ts:9-42`).

### B. Spec And Traceability Reconciliation

- `F010` + `P1-003`: canonical lifecycle/status reconciliation (`spec.md:12-25,45-52`; phase summaries such as `001/implementation-summary.md:37-50`).
- `P1-001`: phase-003/004 command-auth dependency closure (`003/spec.md:82-110`; `003/plan.md:144-152`).
- `P2-001`, `P2-011`, `P2-016`: arithmetic, ADR mirror, and continuity pointers (`001/spec.md:171-180`; `001/plan.md:257-273`; `001/implementation-summary.md:10-23`).
- `P2-009` + `P2-015`: kill-switch operational semantics (`006/spec.md:104-113`).
- `P2-012`: compromise-response wording (`008/spec.md:102-111`; current lost-device playbook `incident-playbooks.md:46-62`).
- `P2-013`: rollback task ordering (`009/tasks.md:63-71`; authoritative plan `009/plan.md:157-162`).
- `P2-018`, `P2-019`: research path and reducer state (`research/deep-research-config.json:11`; `research/deep-research-dashboard.md:3-6`).
- `P2-020`, `P2-023`: live path and documentation-deliverable map (`spec.md:95-107`; `008/spec.md:78-86`).

### C. Known-Accepted Gaps, Not New Work

- Monitoring-only deployment: live prompt/steer/follow-up/abort and the live approval-extension path are intentionally unwired (`README.md:3-12`; `security.md:51-65`). Covers rejected `F001` and the live-mutation aspect around `P2-009/P2-015`.
- One fixed host/workspace/session and one child are the current architecture (`architecture.md:37-38,58-60`). Covers rejected `F002` and `F009` under the present single-tenant boundary.
- Enrollment, sessions, challenges, tickets, and revocation state are memory-only by design (`security.md:27-39`; `operations.md:55-74`). Covers rejected `F005`.
- Enrollment is JSON transfer; no QR graphic or live camera scanner is promised (`platform-support.md:13-20`). This was named in the dispatch as an accepted limit and is not a review finding.
- Approval endpoints are authenticated state-changing endpoints despite the server's historical `ReadOnly` naming (`security.md:41-53`; `auth/policy.ts:1-32`). The naming issue remains `F015`, but endpoint existence is not a release defect.
- A bounded stale read-only transcript cache survives logout/restart as non-authority (`rollback.md:64-69`; `platform-support.md:1-5`). Covers rejected `F008`.
- Machine verification PASS and rollout readiness are separate contracts; stage promotion requires `--require-ready` (`release-verification.md:3-17,43-69`). Covers rejected `F011/P2-022`.

### D. Operator-Only Evidence

- Live Pi executable, real event flow, and fixture-vs-live state: target-host smoke test (`rollback.md:81-85`).
- Real Tailscale Serve identity, direct-backend rejection, and Funnel absence: target tailnet (`release-verification.md:71-83`).
- `sandbox-exec` escape denial and abort termination: exact macOS deployment OS and disposable workspace (`security.md:67-71`).
- iOS/Android install, background push, Focus/Do Not Disturb, service-worker restart, and physical mobile behavior (`platform-support.md:22-24`; `incident-playbooks.md:99-103`).
- Live Pi extension mutation and contained protected runner: operator integration boundary (`release-verification.md:81-85`; `incident-playbooks.md:42-44`).

## Recommended Remediation Order

Use the existing three-gate sequence after **every** step: `npm run typecheck`, `npm test` (must remain **63 passed**), and `npm run build -w @pi-remote/web`. The stored baseline is 63/63 tests and a successful web build (`release/evidence/release-verify-v1-2026-08-13T04-43-23-769Z.json:35-64`). For release-runner changes, also run `npm run release:verify` and inspect the emitted stage statuses; this supplements rather than replaces the three gates.

1. **Fix F007 grant invalidation first.** It is the smallest high-impact authority bug and establishes the common drain primitive. Add focused policy-disable, epoch, restart, close, and grant-policy-recheck tests, then run the three gates.
2. **Fix F006 device-revocation drain.** Reuse the authority-drain primitive to revoke leases, grants, and in-flight work before returning success. Add endpoint-level revocation tests, then run the three gates.
3. **Fix F003 transcript projection.** Implement one typed/redacted producer and its projector tests before touching broader session architecture. Run the three gates and confirm the PWA build still renders every typed-block variant.
4. **Fix F014 release coverage.** Add browser, lint, and format gates, then update the runner. Run the three gates, then `npm run release:verify`; confirm all new machine gates are present and rollout stages remain independently NOT-READY without operator evidence.
5. **Fix F015 and P2-021 naming/taxonomy.** Rename without changing authorization outcomes; add taxonomy/policy assertions and run the three gates.
6. **Reconcile packet truth and dependency closure.** Resolve `F010/P1-003`, `P1-001`, `P2-001`, `P2-011`, `P2-016`, `P2-020`, and `P2-023`, preserving accepted limitations and pending operator gates. Run the three gates because docs describe those commands/contracts, then run recursive strict packet validation through the owning spec workflow.
7. **Repair research provenance.** Fix `P2-019`, rerun the reducer for `P2-018`, and verify top-level counts against each lineage dashboard. Run the three gates to ensure no product drift, then validate the packet again.
8. **Close the remaining P2 wording rows.** Address `P2-009/P2-015`, `P2-012`, and `P2-013`; run the three gates and packet validation.
9. **Collect operator-only evidence last.** Run live Pi, real Tailscale Serve, sandbox escape/abort, physical iOS/Android, accessibility, performance, and live extension-mutation checks. Supply verified evidence to `release:verify`, then require each intended stage explicitly with `check-rollout --require-ready` (`release-verification.md:53-69`).

Review status: REQUESTED_CHANGES
