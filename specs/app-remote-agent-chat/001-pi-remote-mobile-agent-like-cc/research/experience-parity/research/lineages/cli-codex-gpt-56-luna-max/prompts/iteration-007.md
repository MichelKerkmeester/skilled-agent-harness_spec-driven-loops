# Iteration 007 — Content-free attention push

Resolve the apparent contradiction between useful needs_input/finished/error notifications and a strict no-decision-content push policy. Define the durable attention event, generic Web Push payload, authenticated pull, deep link, deduplication, and stale behavior.
