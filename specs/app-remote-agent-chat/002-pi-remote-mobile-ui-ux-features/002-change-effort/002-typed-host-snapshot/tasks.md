# Tasks — Typed host snapshot, reconciliation, and redacted outcomes

- [ ] Add `RuntimeSnapshotDto` containing the authoritative `RuntimeStateDto` and `RuntimeModelCatalogDto` for one session.
- [ ] Add bounded runtime issue-code constants/types for `unsupported`, `host-unavailable`, `foreground-required`, `rate-limited`, `delivery-unknown`, `invalid-response`, and local `offline` mapping.
- [ ] Make runtime control failure variants carry an issue code rather than a browser-visible arbitrary reason string.
- [ ] Keep accepted and stale responses bound to host state snapshots.
- [ ] Add exact-key guards and public exports for the snapshot and issue codes.
- [ ] Reject unknown issue codes, extra keys, unbounded strings, invalid revisions, invalid levels, and snapshots whose session IDs do not match.
- [ ] Keep the runtime projection allowlisted and bounded in `redaction.ts`.
- [ ] Ensure unknown thinking IDs remain internal values without arbitrary labels or reasons in a user-facing field.
- [ ] Add a read-only snapshot hydrate that reads state, levels, and models together, preserves Pi’s advertised order/subset, and deduplicates concurrent hydrate calls.
- [ ] Map host rejection, unsupported capability, and ambiguous transport failures to typed issue codes without returning raw Pi/RPC error text.
- [ ] Preserve revision checks, control-ID idempotency, lifecycle invalidation, and the existing settled mutation lane.
- [ ] Add `POST /api/runtime/reconcile` as a read-only `runtime:read` operation.
- [ ] Ensure reconcile consumes no mutation ticket and forwards no intent.
- [ ] Return the typed snapshot on success and a fixed machine error code on failure.
- [ ] Keep `/api/runtime/state`, `/api/runtime/models`, and `/api/runtime/control` compatible during rollout.
- [ ] Return a bounded `Retry-After` header for runtime rate limiting without exposing host response bodies.
- [ ] Add snapshot fetching and transport-error normalization to `apps/pi-remote-web/src/relay.ts`.
- [ ] Prove valid three-, five-, and seven-level snapshots, issue-code outcomes, unknown IDs, and malformed/extra-key payloads.
- [ ] Prove atomic read hydration, no mutation/ticket consumption for reconcile, exact foreground enforcement, issue-code mapping, raw-reason absence, and no Plan-mode side effect.
