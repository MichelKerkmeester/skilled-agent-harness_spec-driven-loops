# Tasks — Complete runtime state machine and mutation boundary

- [ ] Add `apps/pi-remote-web/src/runtime-issues.ts` with the bounded local issue union and local copy allowlist.
- [ ] Include local copy for Offline, foreground ownership, rate limiting, host unavailable, and delivery-unknown outcomes.
- [ ] Ensure formatters accept only local catalog labels and never host reason strings for visible or assistive copy.
- [ ] Route runtime hydration through `/api/runtime/reconcile`.
- [ ] Validate `RuntimeSnapshotDto` before it enters React state.
- [ ] Map offline, 403 foreground, 429 rate-limit, 503 host-unavailable, invalid payload, and abort/timeout conditions to bounded issue metadata.
- [ ] Parse only a bounded `Retry-After` delay.
- [ ] Request a fresh ticket immediately before each runtime mutation.
- [ ] Generate a unique control ID per attempt and do not cache or persist either value.
- [ ] Expand the reducer for `checking`, `ready-adjustable`, `ready-off-only`, `ready-empty`, `streaming`, `pending`, `accepted`, `stale`, `unsupported`, `offline`, `foreground-required`, `rate-limited`, `host-unavailable`, `delivery-unknown`, and `inconsistent-state`.
- [ ] Keep confirmed runtime state separate from pending operation/requested level and issue state.
- [ ] Add a synchronous in-flight ref checked before dispatch.
- [ ] Apply a cross-browser 10-second mutation deadline.
- [ ] Classify the deadline as delivery-unknown; clear no ticket, retry nothing, and block mutation until read-only hydrate confirms state.
- [ ] Reconcile once after stale/unsupported outcomes with zero automatic mutation retries.
- [ ] Automatically hydrate after connectivity returns and require a new deliberate selection before mutating again.
- [ ] Trigger read-only refresh on sheet-open callback, visible `document.visibilitychange`, online recovery, and sync-connection transition to live.
- [ ] Deduplicate concurrent refreshes.
- [ ] Lock model, effort, and mode mutation controls while a runtime mutation is pending.
- [ ] Keep Build/Plan state unchanged during effort transitions.
- [ ] Pass the session connection/live transition through `App.tsx` without coupling the hook to transcript state.
- [ ] Mount one document-level polite atomic status region for runtime confirmations and failures.
- [ ] Cover every reducer outcome, same-tick locking, exact one-request behavior, 10-second timeout, retry-after reconciliation, refresh triggers, raw-copy leakage, and Plan-mode isolation.
- [ ] Update `RuntimeStrip.test.tsx` to prove controls disable on all non-ready authority states and never display raw `error` text.
