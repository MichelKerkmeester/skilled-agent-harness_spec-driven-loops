# Tasks — Progressive highlighting, accessibility, and release hardening

- [ ] Add `useHighlightedCode.ts` and `highlight.worker.ts` or an equivalent worker module with the fixed allowlist for Bash, JavaScript, TypeScript, JSX/TSX, JSON, HTML, CSS, Markdown, Python, Go, Rust, YAML, SQL, diff, ANSI, and plaintext; retain plaintext before and after highlighting.
- [ ] Enforce the 20,000-character/1,000-line cutoff before worker dispatch, send only redacted source and bounded request/revision metadata, ignore stale responses, and clear memory-only highlight output on revision, viewer, or session disposal.
- [ ] Keep `CodeCard.tsx` and the F6 code renderer semantically selectable with ordinary text nodes, safe language labels, no `dangerouslySetInnerHTML`, and plaintext for unsupported/unknown languages.
- [ ] Harden `SafeMarkdown.tsx` and its corpus against raw HTML, unsafe URL schemes, data URLs, media, forms, iframes, scripts, malformed nesting, ANSI bytes, and bidi controls while preserving verbatim Copy.
- [ ] Extend `CommandOutputCard.tsx`, `F6ViewerAdapter.tsx`, and the viewer provider integration for revision reconciliation, stale cache, connection loss, terminal-without-result, source removal, running-tail follow, Jump to latest, and no replay refocus/reopen.
- [ ] Update `style.css` and viewer styles for light/dark shell wells, syntax-token contrast, two-color focus rings, logical properties, RTL prose, 200% text, 320px/landscape reflow, safe areas, contained overscroll, and reduced-motion transitions of at most 100ms.
- [ ] Add cleanup instrumentation for workers, pending requests, timers, selection-preserving revisions, listeners, focus restoration, history entries, scroll locks, DOM, and cached source copies across repeated large-block open/close cycles.
- [ ] Extend web tests with large-block, worker, stale-response, reduced-motion, contrast, bidi, RTL, 200% text, overflow, focus/scroll, virtualized-row, cache, and repeated open/close cases.
- [ ] Add security-negative coverage to relay negative-control/redaction tests and protocol guard tests for every field reaching the worker or clipboard.
- [ ] Extend recorded relay integration and projector tests with streaming/replay, more-redacted higher revisions, connection loss, truncation, and no-secret propagation while retaining `transcript:read`/`sync:read` authority.
- [ ] Extend `scripts/rich-content-cdp.mjs` with `rich-release` fixtures and true-390px light/dark captures for inline, F6, running, failure/truncation, stale, malformed, 200% text, and reduced-motion states, plus overflow/hit-box/focus/scroll/live-edge assertions.
- [ ] Run the installed-PWA physical-device checklist on the oldest supported iPhone in portrait and landscape for Safari/standalone, VoiceOver, Voice Control, native selection, external keyboard, Back/edge-back, suspension/bfcache, relay loss, RTL, 200% text, reduced motion, streaming, large code, and repeated viewer open/close.
