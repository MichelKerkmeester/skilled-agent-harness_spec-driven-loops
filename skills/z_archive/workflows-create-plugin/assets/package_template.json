{
  "name": "opencode-your-plugin-name",
  "version": "1.0.0",
  "description": "OpenCode plugin - describe what it does",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts",
    "dev": "tsup src/index.ts --format esm --dts --watch",
    "clean": "rm -rf dist",
    "prepublishOnly": "npm run clean && npm run build",
    "test": "node -e \"import('./dist/index.js').then(async (m) => { const hooks = await m.default({ directory: process.cwd(), project: { name: 'test' }, worktree: process.cwd(), client: {}, $: () => {} }); console.log('Hooks:', Object.keys(hooks)); console.log('Type:', typeof hooks); console.log('Is function?:', typeof hooks === 'function'); })\""
  },
  "keywords": ["opencode", "plugin"],
  "license": "MIT",
  "optionalDependencies": {
    "@opencode-ai/plugin": "^1.0.174"
  },
  "devDependencies": {
    "tsup": "^8.0.0",
    "typescript": "^5.7.0"
  }
}
