# Branches origin may receive without an explicit per-push approval.
#
# One glob per line. `main` is built into the hook and needs no entry here.
# Everything absent from this file is refused, and no entry here permits
# CREATING a remote branch that does not already exist — that always needs the
# operator, whatever the environment says.
#
# Keep this list short. A branch listed here can be published by any session
# without anyone being asked.

skilled/v4.0.0.0
