The sk-git README now gives the skill a purpose-first identity through the refined standalone skill README template. This documentation-only release touched only the README text, version field and changelog folder.

## What's New at a Glance

- **The README now follows the standalone skill template as the skill's front door.** Its one-line blockquote pitch states the outcome before any tool name, followed by an AT A GLANCE table and a problem-first OVERVIEW. The template section order continues through RELATED DOCUMENTS, with FEATURES and STRUCTURE folded into it, the REQUIREMENTS table moved to QUICK START and the capability table becoming the Git Workspace Safety Layer inside OVERVIEW.
- **Human Voice Rules now hold across the README body.** The em dash in the preflight advisory row and every other HVR violation were removed. The body has no em dashes, semicolons or Oxford commas.
- **The version field now matches its changelog entry.** The frontmatter version moved from `1.4.0.0` to `1.4.1.0`, closing the gap after the changelog folder topped out at `v1.3.2.0.md`.
- **The runtime instruction surface stayed untouched.** No `SKILL.md` content changed. The smart router and rule set also stayed untouched. Script, allocator, validator and hook behavior did not change, nor did branch grammar, commit rule, workspace rule or command surface.

## Upgrade

No migration required.
