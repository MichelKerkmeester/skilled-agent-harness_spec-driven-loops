---
title: "mcp-obsidian changelog v0.11.0.0"
description: "README conformance release from the skill-readme refinement packet: Human Voice Rule fixes in the plugin capability table and version alignment with the skill release track."
trigger_phrases:
  - "mcp-obsidian v0.11.0.0"
  - "obsidian readme conformance"
importance_tier: "normal"
contextType: "general"
version: 0.11.0.0
---
<!-- Reconstructed: the original of this entry was deleted by a concurrent deep-loop containment sweep and was never committed. Content restored from the session record. -->
The README conformance pass from the skill-readme refinement packet fixes the README's voice violations and aligns its version field with the changelog head. This release adds no new CLI, MCP or plugin surface, leaves skill behavior unchanged from `v0.10.0.0` and is the README itself.

## What's New at a Glance

- **The `README.md` version changes from `0.4.0.0` to `0.11.0.0`.** The field never moved during the `v0.5.0.0` through `v0.10.0.0` releases while the README text did. This release catches the field up to the head of the release track so the frontmatter and the changelog agree.
- **The Plugin Knowledge Layer table removes two prose semicolons.** The Git and Outliner rows use separate sentences.
- **The `AT A GLANCE` Works on row changes its punctuation.** It no longer has a comma before a two-item alternative.
- **The `SKILL.md` version stays at `0.10.0.0`.** No skill file, reference set, feature-catalog card or playbook scenario moved in this release. No vault, plugin or runtime data was touched.
- **Earlier changelog entries stay byte-identical.** The released entries span `v0.1.0.0` through `v0.10.0.0`.

## Upgrade

No migration required.
