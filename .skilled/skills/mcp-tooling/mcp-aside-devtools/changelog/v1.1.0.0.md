---
title: "mcp-aside-devtools changelog v1.1.0.0"
description: "README purpose-first rewrite from the skill-readme refinement packet: one-line pitch, problem-first OVERVIEW, lane capability layer and Human Voice Rule cleanup."
trigger_phrases:
  - "mcp-aside-devtools v1.1.0.0"
  - "aside devtools readme rewrite"
importance_tier: "normal"
contextType: "general"
version: 1.1.0.0
---

The `README.md` rewrite from the skill-readme refinement packet replaces the older tabular reference-card style with a purpose-first rewrite on the refined template. Every verified fact from the prior text survives the rewrite.

## What's New at a Glance

- **A one-line pitch leads with the delivered outcome.** It appears as a blockquote under the H1 before any tool name. The frontmatter description mirrors it.
- **The OVERVIEW starts with the reader's problem.** It puts the reader's situation and wrong-lane failure mode before any feature list.
- **A four-row table names each lane's capability.** Its rows cover agent tasks, deterministic REPL steps, MCP composition and DevTools parity.
- **The README body fixes its punctuation failure.** The previous body failed the Human Voice Rules punctuation gate. The rewrite removes em dashes and semicolons. Oxford-comma patterns are gone from every list.
- **The README version changes to 1.1.0.0.** It changes from 1.0.0.0 to align with this changelog head. `SKILL.md` stays at 1.0.0.0. No skill file, reference set, feature-catalog card or playbook scenario moved in this release.
- **The released v1.0.0.0 changelog entry stays byte-identical.** No vault, plugin or runtime data was touched.

## Upgrade

No migration required.
