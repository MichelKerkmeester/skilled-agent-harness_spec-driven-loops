This update makes the skill more useful for everyday browser work without extra steps. It prioritizes a lightweight route, fills gaps in browser checks and corrects support references.

## What's New at a Glance

- **The lightweight command-line path is the default for everyday browser work.** Simple browser tasks do not need the overhead of the fuller integration path, making routine work quicker and more predictable. The fuller path is kept as a fallback when needed.
- **The skill documents more browser checks.** It covers screenshot capture, network monitoring, console access, cookie handling and better terminal composability. These additions fill gaps in capturing visuals, inspecting traffic and checking browser state, so more common tasks can stay in one workflow instead of branching out to extra tools.
- **A few support references are corrected.** The release fixes wrong documentation links and section pointers so people can follow the guidance without hitting dead ends.

## Upgrade

No migration required.
