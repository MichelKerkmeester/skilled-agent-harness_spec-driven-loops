---
title: "mcp-orca-cli v0.1.1.0"
description: "Documentation conformance release for the Orca CLI workflow packet."
version: 0.1.1.0
---

# mcp-orca-cli v0.1.1.0

**Released:** 2026-09-19

This release closes the documentation gaps that 0.1.0.0 shipped with. Routing, mutation gates and safety semantics are unchanged. The packet's validators now report 0 issues across every scoped document.

## What changed

- Installation guide: the guide now carries the packet's frontmatter block, an Overview, a Prerequisites section, an Installation section and the numbered-section separators. The install-guide validator count drops from 7 issues to 0.
- Runtime SKILL.md: the ten missing section separators, the numbering gap that skipped section four, the stray "Workflow lanes" heading and the remaining serial-comma clauses and em dashes. The skill validator count drops from 11 issues to 0.
- Playbook voice: clause periods replace the 85 contract semicolons across the ten scenarios. The root playbook's flagged serial-comma clauses were removed.
- Released entry: the 0.1.0.0 note's serial commas were normalized. Its facts are unchanged.
- Versions: SKILL.md, README.md, INSTALL-GUIDE.md, the root playbook and the ten scenario files carry 0.1.1.0. The four references keep 0.1.0.0 because their content is unchanged.

## Known limitations

Unchanged from 0.1.0.0. Command flags remain version-matched to the installed guide. The CLI-only backend declaration stands until a live Orca MCP callable is verified. Authenticated, mutation, publishing and browser lanes still require their operator authorization.
