This documentation-only release keeps the prompt-quality-card drift guard accurate after its canonical card moved to a different skill. The guard's logic never depended on the card's path, so only its documented canonical-location comment needed to follow the card.

> Spec folder: `.opencode/specs/skilled-agent-orchestration/130-prompt-knowledge-layering/013-card-relocation-and-guard/` (Level 1)

## What's New at a Glance

- **The guard comment follows the canonical card.** `cli_prompt_quality_card.md` moved from `sk-prompt/assets/` to `sk-prompt-models/assets/`, and `check-prompt-quality-card-sync.sh` now points to `sk-prompt-models/assets/cli-prompt-quality-card.md`. Its logic checks five `cli-*` card filenames, table absence and `SKILL.md` pointers rather than the canonical card's path, while `CHECK 1-4` semantics, the pre-commit orchestrator wiring and the CI workflow are unchanged.

## Upgrade

No migration required.
