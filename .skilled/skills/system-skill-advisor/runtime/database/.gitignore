# Runtime skill-graph state. The tracked fallback lives at ../scripts/skill-graph.json.
*.sqlite
*.sqlite-*
*.sqlite.*
*.db
*.db-*
skill-graph.json
.system-skill-advisor-launcher.json
.system-skill-advisor-launcher.lockdir/
.skill-advisor-owner.json

# /doctor:update orchestrator state. It lives here rather than beside the spec-kit
# mcp-server because this directory is the one /doctor:update already mutates and
# keeps under version-control hygiene.
.doctor-update.*

# Runtime-quarantined SQLite copies (auto-recovery output). Never commit.
*.corrupt
*.corrupt-shm
*.corrupt-wal
