---
title: "create-manual-testing-playbook v1.0.1.2"
description: "README conformance release from the skill-readme refinement packet: purpose-first rewrite against the refined template, version alignment with the release track and Human Voice Rule fixes."
trigger_phrases:
  - "create-manual-testing-playbook v1.0.1.2"
  - "playbook readme conformance"
importance_tier: "normal"
contextType: "general"
version: 1.0.1.2
---

This release turns the manual-testing playbook README's reference-card deck into a purpose-first narrative on the refined template and corrects its release-track version, so its frontmatter and changelog agree again. Skill behavior does not change. The authoring workflow is unchanged from v1.0.1.1.

## What's New at a Glance

- **The README adopts the new section order.** It opens with a one-line pitch blockquote, an `AT A GLANCE` table and a problem-first `OVERVIEW`. The nine-field Scenario Contract appears as a capability layer after What It Does and before `QUICK START`, `HOW IT WORKS`, `TROUBLESHOOTING`, `FAQ`, `VERIFICATION` and `RELATED DOCUMENTS`, while the narrative keeps every fact from the old card deck.
- **`README.md` now carries version `1.0.1.2`.** It moves from `1.0.0.0` to a fresh release above the track head, aligning its frontmatter with the changelog after the field lagged the release track through `v1.0.1.1`.
- **The `README.md` prose passes the Human Voice Rules check.** It has zero em dashes, zero semicolons and zero Oxford-comma pattern hits, down from 8 in the pre-rewrite body.
- **Runtime instructions and release history stay in place.** `SKILL.md` stays at `1.0.1.1` and no runtime instruction, reference file or command asset moved in this release. The released changelog entries `v1.0.0.0` through `v1.0.1.1` stay byte-identical, and no vault, plugin or runtime data was touched.

## Upgrade

No migration required.
