---
title: "create-agent changelog v1.0.1.2"
description: "README conformance release from the skill-readme refinement packet: purpose-first rewrite against the refined template, version alignment with the release track and Human Voice Rule fixes."
trigger_phrases:
  - "create-agent v1.0.1.2"
  - "agent readme conformance"
importance_tier: "normal"
contextType: "general"
version: 1.0.1.2
---

# create-agent v1.0.1.2

The README conformance pass from the skill-readme refinement packet. The purpose-first skeleton the rename refactor introduced holds, so the pass verifies every gate, fixes the Human Voice Rule violations and lands the version field on a fresh entry above the track head.

## 1. NEW

- The Agent File Capability Layer in OVERVIEW: a table that names what the skill operates at file level for each runtime surface, the OpenCode `permission:` schema and the Claude Code `tools:` allow-list, plus the update pass.
- No skill behavior change. The authoring workflow itself is unchanged from v1.0.1.1.

---

## 2. CHANGED

- `README.md` version `1.0.0.0` to `1.0.1.2`. The field lagged the release track through the v1.0.1.1 release. This release lands it on a fresh entry above the track head.
- `README.md` body. The Human Voice Rules prose gate is clean: zero em dashes, zero semicolons and zero Oxford commas, with the banned-word list also clean.
- `README.md` frontmatter description. The Oxford comma between the permissions item and the validation item is removed.

---

## 3. NOT CHANGED

- `SKILL.md` stays at `1.0.1.1`. No runtime instruction, reference file or command asset moved in this release.
- The released changelog entries `v1.0.0.0` through `v1.0.1.1` stay byte-identical.
- No vault, plugin or runtime data was touched.
