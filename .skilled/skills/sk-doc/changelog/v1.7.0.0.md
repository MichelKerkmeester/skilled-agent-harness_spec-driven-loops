Supported skill files can pass sk-doc's checks without false warnings now that its validator recognizes valid script and template formats.

> Spec folder: `.opencode/specs/skilled-agent-orchestration/116-deep-skill-evolution/008-deep-skill-doc-evolution` (Level 2)

## What's New at a Glance

- **CommonJS and ESM scripts are valid.** The script-extension allowlist in `package_skill.py` adds `.cjs` and `.mjs` alongside the existing `.py`/`.sh`/`.bash`/`.js` extensions. A `README.md` in `scripts/` is allowed as documentation.
- **Multi-extension templates pass the asset name check.** The check evaluates the base name before any extension, so `prompt_pack_round.md.tmpl` passes the snake_case check.

## Upgrade

No migration required.
