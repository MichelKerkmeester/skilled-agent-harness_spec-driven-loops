---
title: "create-diff v1.0.0.0 - Preview Registration"
version: 1.0.0.0
---
# v1.0.0.0 - Preview Registration

The initial `create-diff` release registers a nested `sk-doc` workflow mode and orchestration wrapper for the engine pending in packet 136 phases 002–005.

## What's New at a Glance

- **The hub registers `create-diff` for routing.** The registration is in `mode-registry.json` and `hub-router.json`.
- **`SKILL.md` defines the preview contract.** The mode is preview-gated and uses conservative aliases. The release also includes `README.md`.
- **Packet-local metadata is absent.** The mode has no packet-local `graph-metadata.json`.

## Upgrade

No migration required.
