---
title: "Carried spans fixture"
description: "Carries a quoted user sentence, a quoted error string, a fenced sample and an inline span, each holding text the pass must not touch."
---

# Carried Spans Fixture

The reviewer asked us to delve into the numbers; the reply was robust.

The user wrote: "It's important to delve into the tapestry of options."

The run fails with `cannot leverage the robust parser` when the standard moves.

```bash
echo "We leverage the platform; the results were pivotal."
```

Nothing inside the quotation, the error string or the fenced sample belongs to the pass.
