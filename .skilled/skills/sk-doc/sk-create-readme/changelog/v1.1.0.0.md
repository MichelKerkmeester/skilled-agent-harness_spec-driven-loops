---
title: "create-readme changelog v1.1.0.0"
description: "README conformance release from the skill-readme refinement packet: purpose-first rewrite on the refined template with a version bump and a changelog entry."
trigger_phrases:
  - "create-readme v1.1.0.0"
  - "create readme changelog"
importance_tier: "normal"
contextType: "general"
version: 1.1.0.0
---

The README conformance pass from the skill-readme refinement packet puts the document's purpose before its details. Confirmed commands, paths and scripts from v1.0.0.0 are preserved.

## What's New at a Glance

- **The `README.md` names its three output shapes.** Its `The Three Output Shapes` capability table covers the general README, code-folder README and install guide by what each operates on at the file level.
- **The `README.md` follows the refined `skill-readme-template.md` structure.** It puts a one-line pitch up front and an AT A GLANCE table as the first section, and uses a problem-first OVERVIEW and numbered ALL-CAPS H2 sections with dividers. The pitch, AT A GLANCE table, quick start steps, lifecycle prose, troubleshooting rows, FAQ answers, verification checks and related documents table carry over from v1.0.0.0.
- **The `README.md` version field changes to `1.1.0.0`.** It changes from `1.0.0.0`, while `SKILL.md` stays at `1.0.0.0`.
- **The validator examples use `.opencode/skills/sk-doc/scripts/validate_document.py`.** The validator invocation and audit `--validator` argument use this path instead of `shared/scripts/`. The previous `shared/scripts/` path is a byte-identical copy, so every shown command still runs.
- **The released `v1.0.0.0` changelog entry stays byte-identical.** No skill file, reference set, asset or script moved in this release, and no vault, plugin or runtime data was touched.

## Upgrade

No migration required.
