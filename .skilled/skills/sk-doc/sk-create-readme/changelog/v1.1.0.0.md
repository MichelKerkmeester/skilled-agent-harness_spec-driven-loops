---
title: "create-readme changelog v1.1.0.0"
description: "README conformance release from the skill-readme refinement packet: purpose-first rewrite on the refined template with a version bump and a changelog entry."
trigger_phrases:
  - "create-readme v1.1.0.0"
  - "create readme changelog"
importance_tier: "normal"
contextType: "general"
version: 1.1.0.0
---

# create-readme v1.1.0.0

The README conformance pass from the skill-readme refinement packet. The skill README was rewritten purpose-first on the refined skill-readme template: a one-line pitch up front, a problem-first OVERVIEW, an AT A GLANCE table as the first section, a capability table for the three output shapes and numbered ALL-CAPS H2 sections with dividers. Every confirmed command, path and script from the previous version survived the rewrite. The HVR grep gate returns zero hits.

## 1. NEW

- `README.md` capability section `The Three Output Shapes` naming the general README, the code-folder README and the install guide with what each one operates at the file level.

---

## 2. CHANGED

- `README.md` rewritten purpose-first per `skill-readme-template.md`. The pitch, the AT A GLANCE table, the quick start steps, the lifecycle prose, the troubleshooting rows, the FAQ answers, the verification checks and the related documents table all carry over from v1.0.0.0.
- `README.md` version field bumped from `1.0.0.0` to `1.1.0.0`.
- `README.md` validator invocation and the audit `--validator` argument now use `.opencode/skills/sk-doc/scripts/validate_document.py`. The previous `shared/scripts/` path is a byte-identical copy, so every shown command still runs.

---

## 3. NOT CHANGED

- `SKILL.md` stays at `1.0.0.0`. No skill file, reference set, asset or script moved in this release.
- The released changelog entry `v1.0.0.0` stays byte-identical.
- No vault, plugin or runtime data was touched.
