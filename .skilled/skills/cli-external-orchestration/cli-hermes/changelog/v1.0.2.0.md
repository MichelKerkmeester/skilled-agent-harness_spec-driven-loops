---
title: "cli-hermes v1.0.2.0"
version: 1.0.2.0
---

Hermes's LLM Gateway roster moves its Luna and Sol entries to GPT-6. `gpt-6-luna` and `gpt-6-sol` replace the 5.6 ids in the closed roster, its fan-out mirror and the tests that keep the two equal, and the roster stays at seven ids.

> Spec folder: `specs/cli-external-orchestration/077-gpt-6-luna-sol-cutover` (Level 2)

## What's New at a Glance

- **GPT-6 Luna and Sol on `llmgateway`** -- both ids are bare gateway literals the gateway's `/v1/models` catalog listed on 2026-09-23, dispatched with `--provider llmgateway` like the rest of the roster.
- **Standing stated plainly** -- the other five ids were probed live; `gpt-6-luna` passed its first live probe on 2026-09-23, `gpt-6-sol` is catalog-listed and still waits for one, and the roster table says which is which.

## Upgrade

No migration required. The `~/.hermes/config.yaml` provider block is unchanged.
