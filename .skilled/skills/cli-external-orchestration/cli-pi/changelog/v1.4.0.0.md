# cli-pi v1.4.0.0

## Changed

- Rewrote the skill README purpose-first on the refined skill README template. The README now opens with a one-line pitch and a problem-first OVERVIEW, carries the three output contracts as a capability table, documents the conductor model, the conservative self-invocation guard and the prompt-quality delegation. It closes with troubleshooting, FAQ, verification and related documents. The frontmatter version moved from 1.2.0.0 to 1.4.0.0, re-aligning the README with the changelog head.
- Added the Output Contract Layer capability table. Print, JSON and RPC each state what the skill returns and when the contract earns its place, matching the pilot's Plugin Knowledge Layer pattern.
- Added the manual-testing playbook to VERIFICATION and RELATED DOCUMENTS so the reader can find the scenario suite behind the checks.

## Not Changed

- Dispatch behavior. The probe (`command -v pi`), the self-invocation guard (ancestry plus `.pi` heuristic), the three headless surfaces and the shared deep-loop delegation are preserved from the prior README.
- The reference set. All seven `references/*.md` files, both `assets/*.md` files and the pinned contract pointer keep their paths and purposes.
- No `SKILL.md` content, template asset, other skill README, vault file or runtime data moved.
