The cli-pi v1.4.0.0 release rewrites the skill README purpose-first on the refined template and adds details about its outputs and verification material.

## What's New at a Glance

- **The skill README now follows the refined template.** It opens with a one-line pitch and problem-first OVERVIEW, documents the conductor model, conservative self-invocation guard and prompt-quality delegation, then closes with troubleshooting, FAQ, verification and related documents.
- **The README frontmatter version moves from `1.2.0.0` to `1.4.0.0`.** This re-aligns the README with the changelog head.
- **An Output Contract Layer capability table covers Print, JSON and RPC.** Each entry states what the skill returns and when its contract earns its place, matching the pilot's Plugin Knowledge Layer pattern.
- **VERIFICATION and RELATED DOCUMENTS now include the manual-testing playbook.** This lets readers find the scenario suite behind the checks.
- **Dispatch behavior remains unchanged.** The `command -v pi` probe, self-invocation guard with its ancestry and `.pi` heuristic, three headless surfaces and shared deep-loop delegation are preserved from the prior README.
- **All seven `references/*.md` files, both `assets/*.md` files and the pinned contract pointer keep their paths and purposes.** No `SKILL.md` content, template asset, other skill README, vault file or runtime data moved.

## Upgrade

No migration required.
