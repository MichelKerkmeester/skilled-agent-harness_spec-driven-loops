---
title: "cli-pi v1.5.11.0"
version: 1.5.11.0
---

The rule that sends dispatches to the shared runtime now says which dispatches it covers.

> Spec folder: `specs/system-speckit/033-system-speckit-v4/065-cli-hermes-pi-dispatch-rules` (Level 1)

## What's New at a Glance

- **The shared runtime covers research and review lineages only.** The core principle, Execution Ownership, the Dispatch Lifecycle, ALWAYS rule 2 and the success criteria sent every dispatch to the shared deep-loop runtime, whose runner accepts only the `research` and `review` loop types. A single build or doc dispatch now uses the child dispatch envelope in `references/providers-and-models.md` §5.

## Upgrade

No migration required.
