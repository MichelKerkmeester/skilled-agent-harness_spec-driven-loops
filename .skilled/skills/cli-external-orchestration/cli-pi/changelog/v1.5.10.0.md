---
title: "cli-pi v1.5.10.0"
version: 1.5.10.0
---

The live smoke now runs in the operator's agent directory and tests the default model with read-only tools. It can confirm that the configured model route is reachable.

> Spec folder: `specs/cli-external-orchestration/077-gpt-6-luna-sol-cutover` (Level 2)

## What's New at a Glance

- **PI-017 uses the operator's configured credentials.** The smoke runs `pi -p --offline --model llmgateway/deepseek-v4.1-flash --tools read,grep,find,ls --no-extensions --no-skills --no-prompt-templates` on the operator's agent directory. The former step used an empty `PI_CODING_AGENT_DIR`, which had no credentials and could not reach a model. The 2026-09-23 run exited 0 in 17 seconds and listed only `read`, `grep`, `find` and `ls`.

## Upgrade

No migration required.
