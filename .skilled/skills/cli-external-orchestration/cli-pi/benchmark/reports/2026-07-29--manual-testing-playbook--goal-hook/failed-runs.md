# Failed Runs

_Derived after the fact from this run's stored record, not written at run time._

> cli-pi · live · claude · gpt (offline via openai-codex-responses) · offline

No scenario recorded a FAIL verdict across 4 checked behavior(s): 3 PASS, 1 SKIP (`PI-021-RESTORE` -- session_start restore was not observed in a single-shot capture; recorded as SKIP with an explicit re-run condition, not inferred as a failure).
