The `cli-devin` model list now includes Devin's new Max-tier options, expanding coverage from four model families to five. Devin's live roster exposes per-tier DeepSeek V4 models and a full GPT-5.6 Luna persona.

> Spec folder: `specs/cli-external-orchestration/043-roster-update-luna-deepseek-glm-gemini/`

## What's New at a Glance

- **Four Max-tier IDs join the model list.** The DeepSeek additions are `deepseek-v4-flash-max` and `deepseek-v4-pro-max`. The first GPT-5.6 Luna Max IDs are `gpt-5-6-luna-max` and `gpt-5-6-luna-max-priority`.
- **Luna Max uses `-priority` for Fast.** `gpt-5-6-luna-max-priority` is the Fast variant.
- **The DeepSeek additions stop at Max.** The request covered "max thinking levels", so Devin's exposed `-low` and `-high` tiers remain excluded.
- **The other model entries stay unchanged.** GLM-5.2, Grok (4.5 and 4.6), SWE-1.7 and the existing `deepseek-v4` / `deepseek-v4-pro` slugs remain in the list.
- **All four IDs were list-verified, not dispatch-tested.** On 2026-08-14, `devin models list` printed these names verbatim: "DeepSeek V4 Flash Max", "DeepSeek V4 Pro Max", "GPT-5.6 Luna Max Thinking" and "GPT-5.6 Luna Max Thinking Fast". Per operator decision, no live `devin -p --model …` dispatch was run.
- **The `cli-devin` docs match the new roster.** Documentation now reports five model families, up from four. Every model roster enumeration includes all four IDs. `references/providers-and-models.md` carries the fullest account, including the `-priority`-is-Fast note and dated verification.

## Upgrade

No migration required.
