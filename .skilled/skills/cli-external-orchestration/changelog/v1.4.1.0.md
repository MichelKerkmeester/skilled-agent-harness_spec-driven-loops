Gemini 3.7 Flash High is now within the enforced model scope of `cli-cursor` and `cli-devin`, so both modes accept it.

> Spec folder: `specs/cli-external-orchestration/043-roster-update-luna-deepseek-glm-gemini/004-gemini-3-7-flash-high/` (Level 2)

## What's New at a Glance

- **`cli-cursor` adds `gemini-3.7-flash-high`.** It is the first Gemini ID in the curated allowlist, which grows from 20 to 21 IDs. Cursor displays it as "Gemini 3.7 Flash" with the High tier in the ID suffix. See `cli-cursor/changelog/v1.4.1.0.md`.
- **`cli-devin` adds `gemini-3-7-flash-high`.** It is the first Gemini UID in the curated scope, which grows from five families to six. See `cli-devin/changelog/v1.4.1.0.md`.
- **Both new IDs were confirmed in live CLI listings.** They were also dispatch-tested end to end on 2026-08-15.
- **The cli-devin routing description now names Gemini alongside the existing families.** `shared/references/smart-routing.md` updates that description. Touched mode docs change the `cli-cursor` roster count from "20-id" to "21-id" and the `cli-devin` count from "five families" to "six." Every touched instance of "Claude / Gemini / Kimi ids are out of scope" changes to "Claude / other Gemini / Kimi ids are out of scope," keeping Claude, Kimi and other Gemini IDs out of scope.
- **Routing behavior, pre-existing roster entries and the enforcement mechanism are unchanged.** The hub still reads `hub-router.json` and `mode-registry.json` with the same `tieBreak` order and `cli-opencode` as the default mode. DeepSeek, GLM-5.2, GPT-5.6 Luna Max, Grok 4.5/4.6, SWE-1.7 and the opencode-go GLM 5.3 catalog row are unchanged. The new IDs were added to the existing enforced allowlists.

## Upgrade

No migration required.
