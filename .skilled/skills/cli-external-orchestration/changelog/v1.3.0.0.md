`cli-external-orchestration` v1.3.0.0 rewrites the skill README on the refined standalone template, giving readers its purpose before the routing details.

## What's New at a Glance

- **The mode roster table carries all six mode pointers.** The README opens with a one-line pitch and a problem-first OVERVIEW, while HOW IT WORKS keeps the routing facts. The frontmatter version changed from `1.2.0.0` to `1.3.0.0`.
- **Hub routing behavior stays unchanged.** The hub still reads `hub-router.json` and `mode-registry.json` with the same `tieBreak` order and `cli-opencode` as the default mode. The six mode packets and their dispatch contracts stay unchanged. No `SKILL.md` content moved.

## Upgrade

No migration required.
