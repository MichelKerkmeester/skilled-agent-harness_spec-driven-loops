# cli-external-orchestration v1.3.0.0

## Changed

- Rewrote the skill README purpose-first on the refined standalone template. The README now opens with a one-line pitch and a problem-first OVERVIEW, carries the six mode pointers in a mode roster table and keeps the routing facts in a HOW IT WORKS section. The frontmatter version moved from 1.2.0.0 to 1.3.0.0.

## Not Changed

- Routing behavior. The hub still reads `hub-router.json` and `mode-registry.json` with the same `tieBreak` order and `cli-opencode` as the default mode.
- The six mode packets and their dispatch contracts. No `SKILL.md` content moved.
