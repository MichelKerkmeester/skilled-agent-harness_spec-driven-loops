# cli-opencode v1.3.8.0: Benchmark-Evidence Citations Point to the Hub

This documentation-only release repoints the MiniMax and MiMo benchmark-evidence citations in `assets/prompt-templates.md` so they remain accurate and navigable. OpenCode dispatch behavior is unchanged.

> Spec folder: `.opencode/specs/skilled-agent-orchestration/131-registry-benchmark-consolidation/`

## What's New at a Glance

- **MiniMax and MiMo citations point to the benchmark hub.** The `assets/prompt-templates.md` entries for MiniMax (120/003) and MiMo (126/004) now point to the `sk-prompt-models/benchmarks/` hub paths. Their origin sub-phases now hold only a `BENCHMARK-RELOCATED.md` pointer, so the citations resolve to the relocated run-data.

## Upgrade

No migration required.
