This mechanical reference update keeps `sk-prompt` a forkable generic engine and leaves dispatch behavior unchanged.

> Spec folder: `.opencode/specs/skilled-agent-orchestration/130-prompt-knowledge-layering/013-card-relocation-and-guard/` (Level 1)

## What's New at a Glance

- **The canonical card reference now points to the `sk-prompt-models` hub.** `cli_prompt_quality_card.md` moved there from `sk-prompt`, so this skill's links continue to resolve.

## Upgrade

No migration required.
