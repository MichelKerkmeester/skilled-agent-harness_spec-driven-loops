---
title: "cli-opencode v1.4.9.0"
version: 1.4.9.0
---

The `openai` provider grid moves to GPT-6. `openai/gpt-6-sol` and `openai/gpt-6-luna`, with their `-fast` variants, replace the GPT-5.6 slugs across the provider reference, the CLI reference and the variant playbook.

> Spec folder: `specs/cli-external-orchestration/077-gpt-6-luna-sol-cutover` (Level 2)

## What's New at a Glance

- **The GPT-6 slug grid matches the catalog.** The base and `-fast` slugs for both personas match what `opencode models openai` listed on 2026-09-23.
- **The Pro column is honest.** That listing showed no `-pro` slug for either persona, so the grid now tells you to confirm a Pro slug is live before dispatching it.

## Upgrade

No migration required. The default route stays on `opencode-go`.
