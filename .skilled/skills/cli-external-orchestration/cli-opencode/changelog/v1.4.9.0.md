---
title: "cli-opencode v1.4.9.0"
version: 1.4.9.0
---

The `openai` provider grid moves to GPT-6. `openai/gpt-6-sol` and `openai/gpt-6-luna`, with their `-fast` variants, replace the GPT-5.6 slugs across the provider reference, the CLI reference and the variant playbook.

> Spec folder: `specs/cli-external-orchestration/077-gpt-6-luna-sol-cutover` (Level 2)

---

## What Changed

#### Provider Roster

- **GPT-6 slug grid** -- the base and `-fast` slugs for both personas match what `opencode models openai` listed on 2026-09-23.
- **An honest Pro column** -- that listing showed no `-pro` slug for either persona, so the grid now tells you to confirm a Pro slug is live before dispatching it.

## Files Changed

| File | What changed |
| ---- | ------------ |
| `SKILL.md`, `README.md` | GPT-6 persona grid in the provider summary |
| `references/providers-and-models.md` | GPT-6 slug grid and the Pro column caveat |
| `references/cli-reference.md` | GPT-6 slugs in the model options |
| `references/destructive-scope-violations.md` | GPT-6 ids in the guidance |
| `manual-testing-playbook/multi-provider/variant-levels-comparison.md` | Variant sweep runs against `openai/gpt-6-sol` |

## Upgrade

No migration required. The default route stays on `opencode-go`.
