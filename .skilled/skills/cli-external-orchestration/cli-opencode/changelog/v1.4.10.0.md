---
title: "cli-opencode v1.4.10.0"
version: 1.4.10.0
---

The `openai` references stop offering slugs OpenCode does not serve. v1.4.9.0 kept `-pro` slugs in the GPT-6 grid and suggested `openai/gpt-6-sol-pro` as a fallback, but `opencode models openai` lists no `-pro` slug, so a dispatch following that advice would fail.

> Spec folder: `specs/cli-external-orchestration/077-gpt-6-luna-sol-cutover` (Level 2)

## What's New at a Glance

- **The GPT-6 grid has no Pro tier.** It shows two personas at two speed tiers, four slugs, and notes that the catalog's `openai/gpt-6-astra` and `openai/gpt-6-astra-fast` are not on the roster.
- **The fallback names a slug that exists.** With no default model configured, the suggested flagship is `openai/gpt-6-sol --variant high`, and the `--model` examples list only served slugs.

## Upgrade

No migration required.
