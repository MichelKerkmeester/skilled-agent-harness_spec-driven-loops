# cli-opencode v1.3.9.0: Tier-3 Guidance, Model Links and Discovery Triggers

This refinement closes prompt-guidance and discovery drift found by the 130 deep research. It leaves dispatch behavior unchanged.

> Spec folder: `.opencode/specs/skilled-agent-orchestration/130-prompt-knowledge-layering/009-refine-hub-cli-connections/`

## What's New at a Glance

- **Tier-3 guidance points to the canonical prompt card.** `SKILL.md` now points its deep-path escalation bullet to `sk-prompt/assets/cli-prompt-quality-card.md` under its Tier 3 deep-path guidance instead of repeating the trigger list that had drifted from canonical.
- **The prompt card links to each model profile.** `assets/prompt-quality-card.md` replaces its directory-only pointer with per-model profile links for `deepseek`, `kimi`, `qwen` and `glm`. Its introduction now names the `opencode-go` cluster it dispatches.
- **Discovery recognizes the model names.** `graph-metadata.json` adds `deepseek`, `kimi`, `qwen` and `glm` to `trigger_phrases` and `key_topics`, and refreshes `last_updated_at`. These models are dispatched here, and `qwen3.6` is now reachable by name.

## Upgrade

No migration required.
