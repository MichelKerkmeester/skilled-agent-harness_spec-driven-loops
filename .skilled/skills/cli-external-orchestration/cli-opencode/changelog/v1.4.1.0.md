# cli-opencode v1.4.1.0: purpose-first README rewrite on the refined skill README template

This documentation-only release rewrites `README.md` as a purpose-first guide for an external session that cannot load the project runtime. It follows the refined skill README template and the `mcp-obsidian` pilot shape, replacing an aspect table and feature-list sections that had no human entry point. It reconciles the README's version and provider information with the release record and documented catalog without changing runtime, router or dispatch behavior.

> Spec folder: `.opencode/specs/sk-doc/026-skill-readme-refinement/005-mode-child-readme-revisit/005-cli-opencode/` (Level 2)

## What's New at a Glance

- **The README opens with a one-line pitch in a blockquote and an AT A GLANCE table.** Its problem-first OVERVIEW names the external session's inability to load the project runtime before the feature list. The prose is HVR-clean.
- **The Dispatch Surface makes the skill's capabilities visible.** It covers the full project runtime, parallel detached sessions, cross-AI handback, the provider catalog and agent delegation. The README retains the default invocation shape, `</dev/null` rule, top-level `--agent` caveats, three-layer self-invocation guard and `--share` confirmation gate.
- **The README's provider prose now matches the unchanged v1.4.0.0 catalog, and its FAQ model-pick answer was corrected to use the curated roster.** The catalog documents `deepseek` as the default alongside `minimax`, `xiaomi` and `openai`. The README removes `kimi-for-coding`, the Token-Plan providers and `zai-coding-plan` while keeping `references/providers-and-models.md` as the single source for model ids and `--variant` effort.
- **The README's `version:` field now reads `1.4.1.0`.** It replaces `1.3.0.29` and places the README above the providers-and-models release. That release had already moved `SKILL.md` and the changelog to `v1.4.0.0`, although its claimed README bump had not landed.
- **The RELATED DOCUMENTS table now includes two references.** Readers can use the table to open the references the body points to.

## Upgrade

No migration required.
