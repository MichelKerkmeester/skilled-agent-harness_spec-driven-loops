This documentation-only change adds GLM 5.3 to the catalog because the live `opencode-go` gateway offers it, while the previous catalog entry for that gateway listed only DeepSeek V4 Flash and Qwen 3.8 Max. Because `cli-opencode` has no code-enforced model allowlist (a code-based list of permitted models), its free-form `--model provider/model-id` option already let users send requests to this model. The ID appeared in `opencode models opencode-go` on 2026-08-14 and was confirmed in that list only, not by sending a request (operator decision).

> Spec folder: `specs/cli-external-orchestration/043-roster-update-luna-deepseek-glm-gemini/`

## What's New at a Glance

- **`references/providers-and-models.md` §2 now includes `opencode-go/glm-5.3`.** The row identifies Z.AI GLM 5.3 via the Go gateway and changes the family line to name DeepSeek, GLM and Qwen instead of only DeepSeek and Qwen. The gateway also fronts `glm-5.1` and `glm-5.2`, which remain outside the curated catalog scope.

## Upgrade

No migration required.
