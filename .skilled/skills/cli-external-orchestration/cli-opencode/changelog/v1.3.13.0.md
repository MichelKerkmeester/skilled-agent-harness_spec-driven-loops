This release completes cli-opencode's deferred documentation work, updating its reference and asset documentation without changing dispatch behavior.

> Spec folder: `.opencode/specs/skilled-agent-orchestration/130-prompt-knowledge-layering/015-deferred-cleanup-entangled-playbook-assets/` (Level 1)

## What's New at a Glance

- **The reference now includes MiniMax-M3 and the held-back dividers.** The deferred `cli_reference.md` edit now lands in `references/cli-reference.md`, which lists `minimax-coding-plan/MiniMax-M3` as a confirmed-live dispatchable id. The row records 20 clean benchmark dispatches and acceptance of `--variant high`. Phase 014 had held back the content edit. The section dividers applied in phase 014 land with it.
- **`assets/prompt-templates.md` no longer has an extra leading divider.** The `---` before section 1 was removed because the sk-doc asset template omits it. This brings the document in line with sibling asset cards.

## Upgrade

No migration required.
