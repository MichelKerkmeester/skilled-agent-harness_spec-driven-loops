This rule-only documentation update makes the Codex workflow easier to follow and trust by preventing circular delegation.

## What's New at a Glance

- **The main guide tells Codex to use its built-in features instead of calling itself through the `cli-codex` skill.** This prevents circular delegation through the skill.

## Upgrade

No migration required. This is a rule-only documentation update.
