This release makes a mechanical reference update, leaving dispatch behavior unchanged.

> Spec folder: `.opencode/specs/skilled-agent-orchestration/130-prompt-knowledge-layering/013-card-relocation-and-guard/` (Level 1)

## What's New at a Glance

- **The canonical `cli_prompt_quality_card.md` moved from `sk-prompt` to the `sk-prompt-models` hub.** This skill's references now point to its new path, so every link keeps resolving and `sk-prompt` stays a forkable generic engine.

## Upgrade

No migration required.
