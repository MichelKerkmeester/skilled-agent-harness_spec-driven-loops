Codex requests now route through `cli-codex`, a nested workflow packet under `cli-external-orchestration`. The revived contract preserves its dispatch safeguards and delegates orchestrated execution to the existing deep-loop runtime.

> Spec folder: `.opencode/specs/skilled-agent-orchestration/134-cli-codex-revival/003-cli-codex-skill-packet/` (Level 1)

## Why This Release

The `cli-codex` skill contract had been retired.

## What's New at a Glance

- **The retired contract returns within the hub.** `cli-codex` is now a nested mode.
- **Codex dispatch keeps its safeguards.** Availability remains fail-closed, and Codex self-invocation remains prohibited.
- **Orchestrated execution reuses the existing path.** It continues through the deep-loop runtime.
- **The hub adds workflow mode `cli-codex`.** Codex requests use it through `cli-external-orchestration`.

---

## Codex Dispatch

#### Contract Has a Nested Home

The retired skill contract moved into `cli-external-orchestration/cli-codex/` as a nested mode. Its contract follows nested-packet metadata rules.

&nbsp;

#### Dispatch Checks Codex First

Before dispatch, `command -v codex` must succeed. Codex self-invocation remains prohibited.

&nbsp;

#### Execution Keeps One Path

Orchestrated execution delegates to the existing deep-loop `cli-codex` adapter. It does not add a second process path.

---

## Hub Integration

#### Mode and Paths Align

The hub registry and lexical router now include workflow mode `cli-codex`. Operator and reference paths now resolve from the renamed hub topology.

---

## Upgrade Notes

- **Breaking:** Route Codex requests through `cli-external-orchestration` using workflow mode `cli-codex`.
