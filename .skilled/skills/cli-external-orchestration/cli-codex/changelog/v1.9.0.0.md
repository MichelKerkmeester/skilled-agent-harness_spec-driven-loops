The cli-codex README still carried the older tabular reference-card style. Its version field had drifted to 1.5.0.0 while the changelog folder already held entries through v1.8.0.0. This release rewrites the README purpose-first on the refined skill README template: a one-line pitch right after the title, a problem-first OVERVIEW that states the reader's situation before any feature list, an AT A GLANCE table up front, a dedicated dispatch capabilities section and the same narrative voice the mcp-obsidian pilot established. The rewrite keeps every capability, trap, boundary and integration fact of the prior document, including the two silent `codex exec` traps, the three-layer self-invocation guard, the agent roster and the per-model effort ceilings.

The README now passes the README validator with zero issues and the Human Voice Rules greps with zero em dashes, semicolons, Oxford commas or banned words in the body. The version field moves to 1.9.0.0, matching the changelog head.

## Files Changed

| File | Change | Why |
| ---- | ------ | --- |
| `README.md` | Purpose-first rewrite per the refined skill README template with a one-line pitch, problem-first OVERVIEW, dispatch capabilities section and HVR-clean body, `version:` → 1.9.0.0 | Brings the mode README to the fleet standard set by the mcp-obsidian pilot and restores the version field to the changelog head |
| `changelog/v1.9.0.0.md` | Added, this entry | Records the README release per the per-skill changelog convention |

Source: packet .opencode/specs/sk-doc/026-skill-readme-refinement/005-mode-child-readme-revisit/002-cli-codex/
