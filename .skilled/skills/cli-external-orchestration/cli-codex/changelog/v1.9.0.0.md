v1.9.0.0 rewrites the `cli-codex` README purpose-first, replacing its older tabular reference-card style with a structure organized around the reader's situation.

> Spec folder: `.opencode/specs/sk-doc/026-skill-readme-refinement/005-mode-child-readme-revisit/002-cli-codex/` (Level 2)

## What's New at a Glance

- **A one-line pitch follows the title.** The problem-first OVERVIEW states the reader's situation before any feature list.
- **The AT A GLANCE table sits up front.** The rewrite adds a dedicated dispatch capabilities section.
- **The rewrite follows the refined skill README template.** It keeps the narrative voice established by the mcp-obsidian pilot.
- **The prior Codex details remain.** The rewrite keeps every capability, trap, boundary and integration fact, including the two silent `codex exec` traps, the three-layer self-invocation guard, the agent roster and per-model effort ceilings.
- **The README version catches up with the changelog head.** Its version field was `1.5.0.0` and now reads `1.9.0.0`. The changelog folder already held entries through `v1.8.0.0`.

## Upgrade

No migration required.
