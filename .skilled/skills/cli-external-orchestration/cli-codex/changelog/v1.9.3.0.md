---
title: "cli-codex v1.9.3.0"
version: 1.9.3.0
---

The model roster and profile guidance now point to the files the skill and checks use. This keeps CX-002 and the playbook index aligned with the skill's supported models.

> Spec folder: `specs/cli-external-orchestration/077-gpt-6-luna-sol-cutover` (Level 2)

## What's New at a Glance

- **CX-002 and the playbook index use the provider roster.** Both read `references/providers-and-models.md` §2. `references/cli-reference.md` has no `Supported Models` table and links to that roster.
- **Codex profiles use one config file per profile.** The providers reference now describes each profile as its own `$CODEX_HOME/<name>.config.toml` file, selected with `-p <name>`, matching `SKILL.md`. It drops the old `[profiles.<name>]` block example from `.codex/config.toml`. CX-002 checks `~/.codex/luna-impl.config.toml` and `~/.codex/sol-verify.config.toml`.

## Upgrade

No migration required.
