This maintenance release clarifies where `cli-codex` prompt guidance comes from. Shared card content now follows the canonical `sk-prompt` card while local notes remain. The precedence rule and the `SKILL.md` cross-link make that delegation path self-documenting, and the manual testing playbook's citations now match the canonical card's current structure.

> Spec folder: `specs/skilled-agent-orchestration/130-cli-codex-prompt-card-thinning/`

## What's New at a Glance

- **The prompt card delegates shared guidance.** `assets/prompt-quality-card.md` keeps cli-codex-specific notes and points readers to the sk-prompt master card for canonical content.
- **`SKILL.md` now states the precedence order.** The sk-prompt master card comes first, the cli-codex card second and inline guidance third. `SKILL.md` also adds a cross-link to the sk-prompt hub's canonical card.
- **The playbook citations follow current headings.** `references/manual-testing-playbook.md` points readers to headings in the canonical sk-prompt card, matching its current structure.

## Upgrade

No migration required.
