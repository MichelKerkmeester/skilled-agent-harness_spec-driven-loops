This release brings cli-codex's prompt-quality card into line with the `sk-doc` asset template used across the other CLI skills, so its structure matches theirs. Dispatch behavior is unchanged.

> Spec folder: `.opencode/specs/skilled-agent-orchestration/130-prompt-knowledge-layering/012-cli-doc-alignment/` (Level 1)

## What's New at a Glance

- **The prompt-quality card now follows the `sk-doc` asset template.** `assets/prompt-quality-card.md` adds `## 1. OVERVIEW` for Purpose and Usage. Its sections are recased and renumbered in ALL-CAPS, with `## N. RELATED RESOURCES` last. The 7-framework table and CLEAR check stay in the canonical card without being inlined. The canonical-card pointer is unchanged.

## Upgrade

No migration required.
