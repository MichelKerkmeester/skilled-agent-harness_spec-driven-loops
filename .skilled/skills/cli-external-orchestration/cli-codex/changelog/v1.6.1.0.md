This release makes `SKILL.md` more concise by moving repeated reference material into pointers to `references/cli-reference.md`, condensing it from 4630 to 3976 words. Models and contracts remain unchanged.

## What's New at a Glance

- **`SKILL.md` points to the always-loaded CLI reference.** The flag glossary, unique-capabilities table, essential-command examples and troubleshooting table now live in `references/cli-reference.md`, which is ALWAYS loaded on every invocation. `SKILL.md` retains only three dispatch-critical gotchas and a pointer: the read-only sandbox default silently does nothing, `service_tier="fast"` must be explicit and there is no `--reasoning-effort` flag.
- **Repeated guidance appears once.** The duplicate install/auth block, repeated per-model ceiling clause and duplicated sandbox and fast-mode warnings were consolidated into one authoritative statement each. Section 8 points to Section 5's reference index instead of restating it.
- **Existing rules and routing references are preserved verbatim.** The 16 ALWAYS rules, 5 NEVER rules, 4 ESCALATE conditions and Memory Handback protocol remain as written. The four-model GPT-5.5 / GPT-5.6 roster, per-model reasoning-effort ceilings and user-override resolution table are also preserved verbatim. So are the `INTENT_SIGNALS` / `RESOURCE_MAP` / `LOADING_LEVELS` router dictionaries, self-invocation guard and auth pre-flight decision tree.

## Upgrade

No migration required. Dispatch behavior, routing and every rule are unchanged. The full flag, sandbox, session and troubleshooting reference now lives solely in `references/cli-reference.md`, which is ALWAYS loaded on every invocation.
