The missing dividers let numbered H2 headings (level-two section headings) run together in several cli-claude-code docs. Adding them makes the docs scan cleanly and brings them in line with the sk-doc reference and asset templates and the rest of the skill set. Dispatch behavior is unchanged.

> Spec folder: `.opencode/specs/skilled-agent-orchestration/130-prompt-knowledge-layering/014-doc-divider-sweep-and-sk-prompt-cleanup/` (Level 1)

## What's New at a Glance

- **Every numbered H2 after the first now has its divider.** The change adds a blank-padded standalone `---` before each such heading in `references/cli-reference.md`, `references/integration-patterns.md`, `assets/prompt-quality-card.md` and `assets/prompt-templates.md`. The edits add only blank and `---` lines.

## Upgrade

No migration required.
