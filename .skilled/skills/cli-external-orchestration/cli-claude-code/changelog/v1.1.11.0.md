The canonical CLI prompt quality card moved from `sk-prompt` into the `sk-prompt-models` hub, so this release repoints the skill's references. This mechanical update keeps links resolving without changing dispatch behavior.

> Spec folder: `.opencode/specs/skilled-agent-orchestration/130-prompt-knowledge-layering/013-card-relocation-and-guard/` (Level 1)

## What's New at a Glance

- **The canonical `cli_prompt_quality_card.md` moved from `sk-prompt` to the `sk-prompt-models` hub.** The links resolve and `sk-prompt` stays a forkable generic engine.

## Upgrade

No migration required.
