This release aligns the `cli-claude-code` prompt-quality card with the sk-doc asset template used across the other CLI skills. Dispatch behavior is unchanged.

> Spec folder: `.opencode/specs/skilled-agent-orchestration/130-prompt-knowledge-layering/012-cli-doc-alignment/` (Level 1)

## What's New at a Glance

- **The prompt-quality card gains the template's opening and closing sections.** `assets/prompt-quality-card.md` opens with `## 1. OVERVIEW` for Purpose and Usage and ends with `## N. RELATED RESOURCES` after recasing and renumbering Shared Layer, Model Overrides, Delegation / Precedence and Failure Patterns as ALL-CAPS sections. The framework and CLEAR tables stay in the canonical card rather than being inlined, while the pointer to that card remains unchanged.

## Upgrade

No migration required.
