Non-interactive Claude Code delegation can pause when the delegated agent lacks project context. This update helps avoid that stop point.

## What's New at a Glance

- **When an active spec folder exists, the calling AI must include it in the Claude Code handoff.** This tells the delegated agent which approved spec folder applies.

## Upgrade

No migration required. This is a rule-only change.
