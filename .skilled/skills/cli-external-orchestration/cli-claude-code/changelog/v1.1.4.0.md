This release shortens `cli-claude-code`'s guide while keeping the `cli-*` family consistent. Sibling `cli-*` skills received the same treatment in coordinated commits.

> Spec folder: `N/A`
> Scope: cross-cutting cli-* family hygiene, no spec packet.

## What's New at a Glance

- **Shared guidance moved to two new references.** The helper bodies and seven-step Memory Handback Protocol were byte-for-byte identical across all five `cli-*` sibling skills. They now live in `shared_smart_router.md` and `memory_handback.md` under `system-spec-kit/references/cli/`.
- **Claude Code's routing data stays in `SKILL.md`.** `INTENT_SIGNALS`, `RESOURCE_MAP`, `LOADING_LEVELS` and `UNKNOWN_FALLBACK_CHECKLIST` remain inline beside a six-line stub naming the helper-call sequence and a one-line cross-reference.
- **The compression is limited to wrapper boilerplate.** The verbose `ALWAYS`, `NEVER` and `ESCALATE` prose was compressed, while Section 8 collapsed to a five-line stub pointing back to Section 5.
- **The self-invocation guard remains intact.** `SKILL.md` keeps the top-of-file `CRITICAL` admonition and Section 1's first bullet, "You ARE Claude Code already." Its three-layer `detect_self_invocation` check remains visible, using the environment, process ancestry and state lockfile before it calls `refuse(...)`.
- **The external interface stays unchanged.** The frontmatter, `Keywords` HTML comment and advisor-consumed metadata remain the same, and no script changes are required. Agents that read the body of `SKILL.md` see compressed but equivalent guidance, with full helper-body or Memory Handback caveat detail available on demand through the new shared references.
- **The root playbook now uses lowercase.** It was renamed from `MANUAL_TESTING_PLAYBOOK.md` to `manual_testing_playbook.md`, and all scenario references were updated.

## Upgrade

No migration required.
