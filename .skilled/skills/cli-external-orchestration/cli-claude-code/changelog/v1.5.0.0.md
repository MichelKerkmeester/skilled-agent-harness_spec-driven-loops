This release brings the Claude Code skill README into line with the refined skill README template. No `SKILL.md` content, template asset, other skill README, vault file or runtime data moved.

## What's New at a Glance

- **The README now follows a purpose-first structure.** It opens with a one-line pitch and problem-first OVERVIEW, explains the dispatch lifecycle, self-invocation guard, auth pre-flight and Memory Handback, then closes with troubleshooting, FAQ, verification and related documents.
- **The agent roster now uses a capability table.** The table carries all 13 `.claude/agents/*.md` definitions and their purposes.
- **The sibling table now matches the hub README roster.** The previous version listed `cli-opencode` twice. The rewrite names all six `cli-X` siblings once with their providers.
- **The README version now matches the changelog.** Its frontmatter version changed from `1.1.0.20` to `1.5.0.0`.
- **Established dispatch behavior remains unchanged.** The default `claude -p` envelope uses `claude-sonnet-4-6` with `--output-format text`, while deep reasoning uses `claude-opus-4-6 --effort high`. The self-invocation guard and auth pre-flight remain as before. The Memory Handback procedure also remains in place.

## Upgrade

No migration required.
