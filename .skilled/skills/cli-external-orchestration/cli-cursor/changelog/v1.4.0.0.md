Cursor's live listing showed a GPT-5.6 Luna persona alongside Sol and Terra. `cli-cursor` uses that listing to add only the two Max-tier Luna IDs to its curated roster, making GPT-5.6 its first persona IDs in that scope.

> Spec folder: `specs/cli-external-orchestration/043-roster-update-luna-deepseek-glm-gemini/`

## What's New at a Glance

- **The curated roster adds two GPT-5.6 Luna Max IDs.** `gpt-5.6-luna-max` and `gpt-5.6-luna-max-fast` take the list from 18 to 20 and are the first GPT-5.6 persona IDs in the curated Cursor scope. Only Max is in scope: Sol, Terra and the other Luna tiers remain out, while Composer, GLM 5.2, Grok 4.5 and Grok 4.6 stay unchanged.
- **Cursor's listing confirms the new names, not dispatch testing.** On 2026-08-14, `cursor-agent --list-models` printed "GPT-5.6 Luna 1M Max" and "GPT-5.6 Luna Max Fast," and each ID was copied from the live listing, not inferred. By operator decision, the addition was list-verified only and not dispatch-tested, unlike the Grok 4.6 addition.
- **The `cli-cursor` docs now match the 20-ID roster.** Docs with allowlist counts move from 18 to 20, those listing curated families add GPT-5.6 Luna Max and two stale "10" counts become "20" after packet 036 raised the roster to 18. The shared routing guide at `.opencode/skills/cli-external-orchestration/shared/references/smart-routing.md` adds GPT-5.6 Luna Max to the Devin roster mention, a related change recorded in `cli-devin/changelog/v1.4.0.0.md`.

## Upgrade

No migration required.
