This release normalizes mcp-code-mode's documentation headings to improve readability and consistency.

## What's New at a Glance

- **Decorative symbols are removed from headings, which now align with current validation rules.** The heading style is normalized across the package, making the docs cleaner to scan and less likely to fail validation checks.

## Upgrade

No migration required. Pull the latest version when convenient.
