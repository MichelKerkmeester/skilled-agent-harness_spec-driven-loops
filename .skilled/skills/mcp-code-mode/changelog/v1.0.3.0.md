This small release standardizes the maintenance-script headers, giving the package a more polished look and making it easier to scan.

## What's New at a Glance

- **`update.sh` and `validate_config.py` now use matching headers.** The shell and Python scripts look and read like the rest of the codebase.

## Upgrade

No migration required.
