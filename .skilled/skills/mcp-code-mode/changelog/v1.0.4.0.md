This release brings the maintenance scripts in line with the project shell and Python standards, improving safety, readability and error handling.

## What's New at a Glance

- **`.opencode/skills/mcp-code-mode/scripts/update.sh` now uses stricter defaults.** It handles variables more clearly and provides better error output, so routine updates are less likely to fail in confusing ways.
- **`.opencode/skills/mcp-code-mode/scripts/validate_config.py` has a clearer structure.** Its documentation is clearer and its typing is stronger. It uses narrower exception handling, making the validation path easier to maintain and trust.

## Upgrade

No migration required.
