The authenticated `official` surface is now observed rather than source-read.

## What's New at a Glance

- **The operator stored an `official` credential in the machine's credential store, and the two previously SKIP rows now carry recorded PASSes.** `auth status` reports `ok`/`stored`/store path at exit 0, `auth test` reports `valid` with the model id, and one judgment per type returned the documented shapes (`noul` a probability, `choice` a submitted key, `score` a zero-based position). The run lives in `benchmark/reports/2026-09-20-authenticated-verification/`.
- **The truth surfaces were reconciled with the run.** These are the playbook's provider boundary and scenario index, the two scenario files' recorded results, the provider reference's confirmed-versus-unconfirmed section, the catalog's unconfirmed section and the `SKILL.md` availability bullet.
- **One new source-read distinction is recorded.** Once a credential file exists, an absent provider is reported as `stored <provider> API key is empty` rather than `<provider> API key is not stored`. Both exit 3 with empty stdout.
- **The key lives only in the machine's credential store.** No packet file or captured stream carries it, and the no-key scenarios reproduce by pointing `XDG_CONFIG_HOME` at an empty directory.
- **The `vercel`, `openrouter` and `custom` response bodies remain unconfirmed.** So do their pinned-default-model currency and every quota or latency figure.

## Upgrade

No migration required.
