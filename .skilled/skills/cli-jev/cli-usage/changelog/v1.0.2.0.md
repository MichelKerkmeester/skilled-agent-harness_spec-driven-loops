Jev transport now lives under its own `cli-jev` hub as `cli-usage`, while its judgment contract and dispatch shape remain in place.

## What's New at a Glance

- **Jev transport is now `cli-usage`, the first mode in its `cli-jev` hub.** The packet moved to `.skilled/skills/cli-jev/cli-usage/`. The `v1.0.0.0` and `v1.0.1.0` entries are preserved as written from when the mode lived under the old family name.
- **The packet and hub have distinct names.** The packet's `SKILL.md` frontmatter declares `name: cli-usage`. Hub-level docs, including this packet's `SKILL.md`, `README.md`, feature catalog, benchmark index and playbook, now name `cli-jev` rather than `cli-external-orchestration`.
- **The `transport-axis` extension moved with the packet.** `cli-usage` is the only transported mode the new hub declares. The new hub's registry carries the axis.
- **The judgment contract is unchanged.** Its eight hard rules and their implemented checks, four references, question-shaping card, `JEV-` scenario ids and recorded run verdicts remain the same evidence at the new path.
- **The dispatch shape registered in the dispatch audit is unchanged.** The display name stays `cli-jev` because it remains the hub id and a mode alias. Decoupling removed the retired mode from the old hub and moved the guards' packet path to `cli-jev/cli-usage/SKILL.md`. At preflight, the eight checks are enforced again: `jev run @request.json --value` is refused with the packet's `[jev-value-not-with-run]` rule text.

## Upgrade

No migration required.
