# v3.8.0.0, The README Rebuilt as Purpose-First Narrative

The system-spec-kit README moved from a tabular reference card to the purpose-first narrative standard the repo adopted for standalone skill READMEs. It now opens with the outcome the skill delivers and keeps every shipped fact in a structure a person can scan.

## What's New at a Glance

- **The layout is modeled on the mcp-obsidian pilot and the refined skill README template.** The README leads with a one-line pitch and a problem-first OVERVIEW that states the reader's situation before any feature list. AT A GLANCE opens the document. Numbered ALL-CAPS sections carry the detail and capability tables state what the skill operates at the file and data level.
- **The validation story was corrected to the current registry state.** It now states 45 rules with four strict-only rules gated behind `--strict`, replacing the stale 36-of-38 counts.
- **The lifecycle diagram now reflects current Gate 3.** It shows the current options with Option E for skip and the phase-parent resume pointer behavior.
- **Every shipped behavior claim from the previous README survived the rewrite.** That covers the four documentation levels, the phase parent lean trio, the packet-local changelog workflow, the five retrieval channels, the FSRS decay tiers, the save arbitration outcomes, the hardening defaults, the launcher recycle error codes and the full script inventory.
- **The README frontmatter version moves from `3.6.0.99` to `3.8.0.0`.** That is the next version after the latest changelog entry `v3.7.1.0`. The README version now reads the current release state of the skill documentation.

## Upgrade

No migration required. The README is documentation only. The `3.6.0.99` to `3.8.0.0` jump closes the gap between the README version field and the changelog history, which already reached `v3.7.1.0`.
