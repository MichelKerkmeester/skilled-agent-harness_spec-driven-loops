A 291-scenario manual testing playbook run against the memory system found 12 failures, and four of them had fixable root causes. This release restores memory-system startup checks, corrects stale evaluation data and clears code-quality warnings from the ESM migration. The other eight failures were knowledge gaps in the database, including missing environment-variable documentation, and limitations in the test setup, not code bugs.

> Spec folder: `.opencode/specs/system-spec-kit/023-hybrid-rag-fusion-refinement/006-review-remediation/` (Level 2)

## What's New at a Glance

- **Startup tests explicitly run startup after import.** This restores checks for dimensions, dynamic instruction generation and per-connection instruction refresh after the import safety guard stopped imports from starting the MCP server.
- **The evaluation framework's 290-entry ground-truth file was remapped.** After months of database changes, 294 IDs no longer matched any record. The existing mapping script updated all 290 entries to the correct current records, with zero left unmapped.
- **Ten code quality warnings were cleared across seven files.** Five unused imports and variables were removed, two variables that never changed became constants and two MCP SDK (the server framework) type annotations that must be loosely typed were marked with explicit exceptions.
- **The unused CommonJS import is removed.** The leftover `createRequire` import from the old module system is gone.

## Upgrade

No migration required.
