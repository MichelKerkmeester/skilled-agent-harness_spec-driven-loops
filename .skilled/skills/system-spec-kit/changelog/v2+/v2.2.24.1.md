This release is a documentation-only patch for the system spec kit. It refreshes the written guidance and keeps the published information aligned with the current project so the workflow is easier to follow and easier to trust.

> Spec folder: `.opencode/specs/system-spec-kit/020-mcp-working-memory-hybrid-rag/007-documentation-alignment/`

## What's New at a Glance

- **Metric tables refreshed.** Updated counts in the skill root `README.md` and `mcp-server/README.md` so published numbers match current repository state.
- **Config default corrected in docs.** `SPECKIT_INDEX_SPEC_DOCS` is now documented as default true in the MCP README.
- **Reusable node docs added.** Created the `when-to-use` and `gate-3-integration` node docs for concise gate and workflow guidance.
- **Skill patch bump.** `SKILL.md` is updated from 2.2.24.0 to 2.2.24.1.
- **No runtime delta.** The patch introduces no MCP tool, handler or storage behavior changes.

## Upgrade

No migration required. Pull latest to get corrected documentation metadata.
