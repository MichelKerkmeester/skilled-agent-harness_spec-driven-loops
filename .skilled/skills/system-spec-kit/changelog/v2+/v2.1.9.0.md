This release is MCP cleanup and alignment. It resolves all 36 findings from the spec 101 misalignment audit, removes 5 obsolete commands and achieves code-documentation alignment.

## Why This Release

The release focuses on making the instructions and release notes easier for non-developers to follow. Its goal is a workflow that is easier to follow and easier to trust.

## What's New at a Glance

- **The spec 101 misalignment audit is resolved.** All 36 findings are closed, reducing confusing failures.
- **6 command files were updated.** They now use `memory_context` integration and standardized quality gates.
- **2 pre-existing test failures are fixed.** The fixes make the system more dependable.
- **Code-documentation alignment is achieved.** `ADR-008` documents the alignment decisions. The release notes and instructions are easier to read and trust.
- **5 obsolete commands were removed.** They are `Code`, `index` and `memory/{checkpoint,database,search}.md`. The project becomes smaller and easier to maintain.

## Upgrade

No migration required.
