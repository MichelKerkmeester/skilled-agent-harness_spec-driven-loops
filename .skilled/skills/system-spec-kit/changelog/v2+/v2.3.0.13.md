Sprint 140 clears error-class validation debt and restores spec structure for the `140-hybrid-rag-fusion-refinement` Level 3+ folder. It restores required root artifacts, normalizes template-source metadata across root and phase docs and removes non-research TOC violations, making the instructions and release notes easier for non-developers to follow.

> Spec folder: `.opencode/specs/system-spec-kit/022-hybrid-rag-fusion-refinement/` (Level 3+)

## Why This Release

The goal is a workflow that is easier to follow and trust.

## What's New at a Glance

- **Required root artifacts were restored.** They include a decision record and an implementation summary.
- **Template-source metadata was normalized.** The work covered root and phase docs.
- **Non-research root TOC sections were removed.** This satisfies TOC policy.
- **Obsolete audit scratch artifacts were removed.** They were under `Opencode/skill/system-spec-kit/mcp-server/scratch/`.

---

## Spec Structure

#### Required Records and Clear Metadata

The root folder lacked required artifacts, so the sprint added a decision record and an implementation summary. The root spec anchor mismatch for metadata was fixed, and missing `# SPECKIT_TEMPLATE_SOURCE` frontmatter markers were added across affected root and phase files. TOC sections were removed from non-research root docs to satisfy TOC policy. Validation for Sprint-140 moved from FAILED (Errors: 10) to PASSED WITH WARNINGS (Errors: 0, warnings remain advisory).

---

## Release Maintenance

#### Audit Scratch Cleanup

Obsolete audit scratch artifacts were removed from `Opencode/skill/system-spec-kit/mcp-server/scratch/`. The release also included tracked runtime eval-db sidecar updates to `Opencode/skill/system-spec-kit/mcp-server/database/speckit-eval.db-shm` and `Opencode/skill/system-spec-kit/mcp-server/database/speckit-eval.db-wal`.

## Upgrade Notes

No upgrade needed. Existing spec content remains valid and no schema migration is required. This release improves validation conformance and documentation governance.
