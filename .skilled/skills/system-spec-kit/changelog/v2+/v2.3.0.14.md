This release brings a bulk index hard-clean and `z_archive` indexing parity. Spec docs under `z_archive` now join memory index discovery, and forced indexing is hardened to avoid prediction-error and reconsolidation collapse during rebuilds. A clean full re-index completed with alias cleanup and a database sync back to the main paths.

> Spec folder: `.opencode/specs/system-spec-kit/022-hybrid-rag-fusion-refinement/010-comprehensive-remediation/`

## What's New at a Glance

- **`z_archive` is out of the spec-doc exclusion.** Memory index discovery now finds spec docs stored under `z_archive`.
- **Memory file classification allows spec documents under `z_archive`.** That enables the validation path for them.
- **All `z_archive` memory files are indexed.** Most `z_archive` spec docs are indexed too. The residual exclusions are limited to `scratch/` and hidden backup folders.
- **`force: true` indexing skips prediction-error candidate selection and reconsolidation-on-save.** This prevents duplicate suppression and merge behavior in bulk rebuild scenarios.
- **Long-running forced scans stop looping.** Repeated upsert/collapse loops for identical file paths are prevented.
- **The memory database got a full clean and bulk re-index.** The rebuilt profile database is synced to the main database paths used by the MCP runtime.
- **Residual identical alias artifacts are reconciled.** The database update marker is refreshed for live runtime visibility. Memory_health reports a healthy system with alias divergence resolved.

## Upgrade

No schema migration required. Existing indexing flows continue to work, and forced reindexes are now safer for bulk rebuild operations. The `scratch/` and hidden-directory exclusions from `z_archive` discovery are intentional.
