This release makes automatic spec-session selection more dependable for resume and handover workflows. It prioritizes active spec folders, resolves equivalent paths consistently and handles low-confidence selections with confirmation or a safe fallback.

> Spec folder: `.opencode/specs/system-spec-kit/022-hybrid-rag-fusion/005-auto-detected-session-bug/`

## Why This Release

The release focuses on fixing important problems and explaining the results in clearer terms. The goal is a workflow that is easier to follow and trust.

## What's New at a Glance

- **Archived targets are avoided deterministically.** Detection prioritizes active, non-archived spec folders over stale archive or fixture candidates, reducing confusing failures.
- **Path aliases work across both roots.** `specs/` and `.opencode/specs/` equivalents resolve consistently.
- **Ambiguous selections get safe handling.** Low-confidence detection paths require confirmation or a safe fallback.
- **Resume and handover guidance follows selection behavior.** The `/spec_kit:resume` and `/spec_kit:handover` guidance matches the detector's behavior.
- **The detector implementation was active.** No unnecessary rewrite was needed.

## Upgrade

No migration required. Existing command flows continue working with higher selection reliability, and explicit path input still overrides auto-detection when provided.
