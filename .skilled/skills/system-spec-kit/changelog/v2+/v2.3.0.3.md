This behavioral fix makes memory indexing deterministic across aliased spec roots and standardizes importance-tier resolution in parsing and scoring. It makes the workflow easier to follow and trust.

> Spec folder: `.opencode/specs/system-spec-kit/022-hybrid-rag-fusion/003-index-tier-anomalies/`

## What's New at a Glance

- **Aliased spec roots no longer cause duplicate indexing.** Parser discovery canonicalizes and deduplicates logical paths before indexing, preventing overlap between `specs/` and `.opencode/specs/`.
- **Merged file sets are unique before batch processing.** This reduces confusing failures and makes memory indexing more dependable.
- **Tier precedence is now deterministic.** Parser and scoring paths resolve importance tiers in this order: precedence metadata, inline marker, then default. This keeps tier assignment consistent.
- **Scan diagnostics clarify deduplication.** Scan outputs explain unique-versus-skipped behavior, making deduplication clearer.
- **No schema migration is included.** Historical row cleanup is outside this spec's scope.

## Upgrade

No migration required. Restart MCP services and run a fresh index scan to confirm deduplicated scan counters and stable tier mapping in diagnostics.
