This release is a code audit remediation. It works on two fronts: coding standard violations and the `dist/` directory. The goal is a workflow that is easier to follow and easier to trust.

## What's New at a Glance

- **All P0/P1 coding standard violations are remediated.** The fixes span 12 task categories.
- **The `dist/` directory is fully rebuilt.** It now matches the TypeScript source.

## Upgrade

No migration required.
