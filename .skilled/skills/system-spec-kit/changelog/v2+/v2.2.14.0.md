Nested spec folder paths such as `003-parent/121-child` failed when `path.isAbsolute()` did not handle relative subfolder paths. `generate context` now fixes the P0 regression.

> Spec folder: `specs/system-spec-kit/z_archive/006-generate-context-subfolder/`

## What's New at a Glance

- **Subfolder path resolution now uses a reusable utility.** It handles both absolute and relative inputs consistently.
- **A subfolder resolution test was added.** It verifies nested path resolution.

## Upgrade

No migration required.
