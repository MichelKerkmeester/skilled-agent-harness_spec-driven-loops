This release corrects spec-folder paperwork to match completed work and actual validator behavior, making the workflow easier to follow and trust.

> Spec folder: `.opencode/specs/system-spec-kit/022-hybrid-rag-fusion/014-hydra-db-based-features` (Level 3)

## What's New at a Glance

- **Decision-record statuses now match completion.** In 014, three records changed from Proposed to Accepted after the work was complete.
- **The 014 checklist separates verification from sign-offs.** It identifies completed verification separately from Product Owner approval and Security review, which are deferred until production handoff, and explains why they do not block completion.
- **The Level 2 `015-skill-alignment` checklist corrects its validation claim.** The validation script exits with errors because the validator flags relative paths such as `../../../../skill/...` as broken. The checklist explains that these paths resolve correctly from deep inside the spec-folder tree and why the errors are acceptable.
- **The implementation summary documents relative-path resolution.** It explains that the `015-skill-alignment` folder's depth inside `.opencode/specs/...` lets `../../../../skill/` correctly reach `.opencode/skills/`, a dependency that had been assumed.

## Upgrade

No migration required. These documentation-only changes make no code changes and have no runtime impact.
