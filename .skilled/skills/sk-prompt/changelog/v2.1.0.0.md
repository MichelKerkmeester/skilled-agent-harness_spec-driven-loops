`sk-prompt` is now a cleanly forkable, pure prompt-framework engine after its one non-generic asset, the CLI-dispatch-specific prompt quality card, moved out. It has no small-model or CLI-dispatch ties.

> Spec folder: `.opencode/specs/skilled-agent-orchestration/130-prompt-knowledge-layering/013-card-relocation-and-guard/` (Level 1)

## What's New at a Glance

- **`cli_prompt_quality_card.md` moved to `sk-prompt-models/assets/`.** The engine keeps its seven framework definitions plus CLEAR scoring in `references/patterns-evaluation.md` and DEPTH thinking in `references/depth-framework.md`. Its `assets/` folder contains the format guides and framework registry.
- **`graph-metadata.json` no longer points to the moved card.** The card was removed from `enhance_when`, `key_files`, `entities` and `source_docs`, so no dangling references into the moved card remain.

## Upgrade

No migration required.
