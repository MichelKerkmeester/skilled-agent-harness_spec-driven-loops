---
title: "sk-prompt-improve v2.3.1.0 - README rewrite"
version: 2.3.1.0
---

# v2.3.1.0 - README rewrite

The sk-prompt-improve README now matches the pilot README family. This replaces a reference-card layout that led with tables before explaining why.

## What's New at a Glance

- **The README follows the refined README template.** It uses a one-line pitch, a problem-first OVERVIEW, numbered ALL-CAPS sections with dividers, narrative sections that name outcomes before tooling and a framework capability layer.
- **The README preserves its existing content.** It covers the seven frameworks, DEPTH phases, CLEAR rubric, operating modes, agent escalation path, quick start commands, troubleshooting rows, FAQ answers and related document links.
- **The verification path is corrected.** It now uses `sk-prompt-improve` instead of the legacy `prompt:improve` folder name.
- **The release adds `changelog/v2.3.1.0.md`.** The README version field changes from `2.3.0.21` to `2.3.1.0`, matching this entry.
- **The runtime contract still owns routing and workflow rules.** `SKILL.md` content and frontmatter, templates, scripts, references, assets and the manual testing playbook are unchanged. The README documents current state only.

## Upgrade

No migration required. The release is documentation-only and does not change behavior.
