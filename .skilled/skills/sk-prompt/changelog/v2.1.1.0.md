This release finishes the phase-013 card relocation by removing stale `cli_prompt_quality_card.md` references from `sk-prompt/SKILL.md`. `sk-prompt` now documents only what it owns.

> Spec folder: `.opencode/specs/skilled-agent-orchestration/130-prompt-knowledge-layering/014-doc-divider-sweep-and-sk-prompt-cleanup/` (Level 1)

## What's New at a Glance

- **Four stale card references are gone.** The card moved to the model-craft hub in 2.1.0.0. `SKILL.md` still named `cli_prompt_quality_card.md` in four places: a §1 activation-trigger sentence, a Core-References bullet with a broken `./assets/` link, the `## 8. FAST-PATH ASSET` section describing an asset that no longer lives here and an entry in the §10 resource-discovery list.
- **The §1 sentence now describes the deep-path escalation relationship.** It does so without the moved filename. The broken Core-References bullet, the FAST-PATH ASSET section and the resource-list entry were removed.
- **Two section headings were renumbered.** `§9 INTEGRATION POINTS` and `§10 REFERENCES` became `§8` and `§9`. The router pseudocode was already correct and was left untouched.

## Upgrade

No migration required.
