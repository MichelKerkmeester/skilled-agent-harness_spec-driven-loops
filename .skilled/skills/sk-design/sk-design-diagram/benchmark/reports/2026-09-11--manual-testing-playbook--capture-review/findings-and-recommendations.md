# Findings And Recommendations

> sk-design-diagram · doc · capture-review

1 failing scenario(s) grouped into 1 recorded pattern(s).

## 1. Six judged reads over all 39 captures by two fresh readers: 36 pass; focal balance fails on dp-integration (accent on three boxes and most edges), venn (no accent at all) and template-full (accent on five elements). Skin-pinned third capture differs from the stock by eye (architecture, accent moved). Settled double-capture matches (swimlane). Label-mask measurement at 1:1 with remote fonts disabled: 1 of 14 arrow-label masks overflows (swimlane HANDOFF), 0 of 12 type-tag boxes. Three sequence captures are cropped at the 900px viewport. Verdict FAIL: the corpus has three focal-balance defects and one label overflow to repair, and the capture step needs full-page height.

Affects 1 scenario(s): CAP-001.

---

Grouping reflects only the reasons this run recorded. Scenarios whose reason was not captured are grouped together and need a re-run before they can be diagnosed.
