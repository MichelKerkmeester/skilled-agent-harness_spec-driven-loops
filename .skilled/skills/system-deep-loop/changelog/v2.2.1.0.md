---
title: "system-deep-loop changelog v2.2.1.0"
description: "Additive model-roster expansion in the executor allowlists: cli-cursor and cli-devin each gain one Gemini 3.7 Flash High id, kept in sync with the fanout-run.cjs mirrors and their vitest fixtures."
trigger_phrases:
  - "system-deep-loop v2.2.1.0"
  - "executor allowlist gemini 3.7 flash high"
importance_tier: "normal"
contextType: "general"
version: 2.2.1.0
---

# system-deep-loop v2.2.1.0

## Changed

- Expanded two executor allowlists in `runtime/lib/deep-loop/executor-config.ts` with live-verified, additive model ids (phase 004 of packet 043; every id confirmed present verbatim in the live `cursor-agent --list-models` / `devin models list` output on 2026-08-15):
  - `CURSOR_SUPPORTED_MODELS` 20 → 21: adds `gemini-3.7-flash-high` (the first Gemini id in the curated Cursor scope; Cursor's display name is "Gemini 3.7 Flash" — the High tier lives in the id suffix).
  - `DEVIN_SUPPORTED_MODELS` 23 → 24: adds `gemini-3-7-flash-high` (the first Gemini uid in the curated Devin scope, sixth curated family).
- Kept the hand-duplicated mirrors `CURSOR_ALLOWED_MODELS` / `DEVIN_ALLOWED_MODELS` in `runtime/scripts/fanout-run.cjs` byte-identical to the source arrays, and updated the vitest fixtures (`executor-config.vitest.ts`, `fanout-run.vitest.ts`). The `ALLOWED ≡ SUPPORTED` cross-check and `combo-matrix` derivation tests confirm parity.
- Updated the rationale doc comments to record the 2026-08-15 list-verification and the **dispatch-test**: both ids returned a live model response via probe dispatch (exit 0) on 2026-08-15 — unlike the 2026-08-14 additions, which were list-verified only.

## Not Changed

- The allowlist enforcement mechanism, sort order, and fail-closed rejection of off-list ids. Sibling Gemini tiers (`gemini-3.7-flash-low`/`-medium` on cursor, `gemini-3-7-flash-minimal`/`-low`/`-medium` on devin) remain rejected and are covered by new negative fixtures.
- DeepSeek, GLM-5.2, GPT-5.6 Luna Max, Grok 4.5 / 4.6 entries, SWE-1.7, the Pi roster, and every other executor config surface.
- Verification: the touched unit suites pass — `vitest run executor-config.vitest.ts fanout-run.vitest.ts combo-matrix.vitest.ts` → 3 files, 190 tests, all green.

Source: `specs/cli-external-orchestration/043-roster-update-luna-deepseek-glm-gemini/004-gemini-3-7-flash-high/`
