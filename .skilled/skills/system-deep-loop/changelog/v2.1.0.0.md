---
title: "system-deep-loop changelog v2.1.0.0"
description: "Purpose-first README rewrite for the deep-loop hub: one-line pitch, problem-first overview, mode family table and a verification close, with the version bumped to 2.1.0.0."
trigger_phrases:
  - "system-deep-loop v2.1.0.0"
  - "deep loop readme rewrite"
importance_tier: "normal"
contextType: "general"
version: 2.1.0.0
---

The hub README moves into the purpose-first narrative voice the fleet now uses. It helps a new reader understand what deep-loop workflows deliver before the routing machinery. No skill behavior or `SKILL.md` content changed, leaving the runtime instruction surface untouched.

## What's New at a Glance

- **A one-line pitch leads the README.** A blockquote after the H1 states the delivered outcome before any tool name, following the refined skill README template and the `mcp-obsidian` exemplar.
- **The overview puts the five-identity problem first.** "Why This Skill Exists" opens with the five-identity disambiguation problem before any feature list.
- **A Mode Family table covers every workflow mode.** Each row says what its mode runs, modeled on the pilot's Plugin Knowledge Layer pattern.
- **An FAQ answers four new-reader questions.** They cover the single-identity design, the `runtime/` boundary, artifact locations and mode routing.
- **A verification close groups the README's checks.** It brings together the README validator, hub canon check, advisor drift-guard and manual testing playbook.
- **The README adopts a numbered ALL-CAPS section model.** AT A GLANCE comes first. OVERVIEW is required. This replaces the old reference-card style.
- **The README frontmatter version advances.** It changes from 2.0.0.0 to 2.1.0.0.
- **The rewrite preserves the README's durable facts.** Invoke routes, mode names, the three-tier discriminator, layout, artifact locations and add-a-mode surfaces all survive the rewrite.
- **No mode packet, registry, router, manifest or vault file changed.** The hub changelog history and the frozen `runtime/` changelog stay in place.

## Upgrade

No migration required.
