Self-test, bug fixes, reducer improvements, and skill advisor routing sync for `deep-improvement`. Phase 009 ran the improvement loop on the agent-improver's own definition and found a real bug (stale command path). Phase 010 fixed that bug plus 4 reducer issues and promoted the best candidate improvements. Phase 011 updated the skill advisor routing and README documentation. The goal is to make the workflow easier to follow and easier to trust.

> Spec folder: `.opencode/specs/skilled-agent-orchestration/041-sk-recursive-agent-loop` (Phases 009-011)

---

## What Changed

This release touched several parts of the workflow. The notes below explain the main changes in plain language.

- **Self-test completed** (Phase 009): Ran `/deep:agent-improvement` targeting `agent-improver.md` itself. 3 iterations, baseline 99→100, all 5 dimensions converged at 100. First real end-to-end validation of the skill on its own agent.
- **Stale command path fixed** (Phase 010): Canonical agent-improver.md had `/deep:agent-improvement-improver` pointing to a nonexistent file. Fixed to `/deep:agent-improvement` → `.opencode/commands/deep/agent-improvement.md`. Synced to all 3 mirrors.
- **Reducer family fix** (Phase 010): `inferFamily()` no longer hardcodes `session-handover` for every profile. Agent-improver now shows its own name in the dashboard.
- **Configurable plateau window** (Phase 010): Plateau detector reads `stopRules.plateauWindow` from config (default 3). Short runs can now use a window of 2.
- **Accepted counting fix** (Phase 010): Dashboard now counts `candidate-acceptable` and `candidate-better` recommendations as accepted (previously always showed 0).
- **Candidate improvements promoted** (Phase 010): Halt condition for missing inputs, merged checklists, checkbox self-validation, 4th anti-pattern, scan report provenance note, summary box label update.
- **Skill advisor routing** (Phase 011): Added 7 INTENT_BOOSTERS, 13 PHRASE_INTENT_BOOSTERS, 8 COMMAND_BRIDGES for 5D scoring, integration scanning, dynamic profiling, and all `/deep:` + `/create:` slash commands.
- **README updates** (Phase 011): Skill README bumped to v1.0.0.0. Root README added Context-Prime and Agent-Improver to Agent Network, rewrote Improve Agent command description. 5 command README.txt files fixed.

---

## Files Changed

| File | Action | Description |
| --- | --- | --- |
| `.opencode/agents/agent-improver.md` | Modified | Fixed command path, promoted candidate improvements (halt condition, merged checklists, 4th anti-pattern) |
| `.claude/agents/agent-improver.md` | Modified | Mirror sync |
| `.agents/agents/agent-improver.md` | Modified | Mirror sync |
| `.codex/agents/agent-improver.toml` | Modified | Mirror sync, wording fix |
| `.opencode/skills/deep-loop-workflows/deep-improvement/scripts/reduce-state.cjs` | Modified | Family derivation, configurable plateau window, accepted counting |
| `.opencode/skills/deep-loop-workflows/deep-improvement/assets/improvement-config.json` | Modified | Added `plateauWindow` field |
| `.opencode/skills/deep-loop-workflows/deep-improvement/assets/improvement-config-reference.md` | Modified | Documented `plateauWindow` |
| `.opencode/skills/scripts/skill_advisor.py` | Modified | Phase 008+ routing entries + COMMAND_BRIDGES |
| `.opencode/skills/README.md` | Modified | Version bump to 1.0.0.0 |
| `README.md` | Modified | Added Context-Prime and Agent-Improver agents, rewrote Improve commands |
| `.opencode/commands/README.txt` | Modified | Added improve group, fixed command counts |
| `.opencode/commands/speckit/README.txt` | Modified | Added deep-review command |
| `.opencode/commands/create/README.txt` | Modified | Removed prompt (moved to improve), added changelog |

---

## Upgrade

No migration required. The `plateauWindow` config field defaults to 3 (same as the previous hardcoded value). Existing runtime configs without the field continue working identically.
