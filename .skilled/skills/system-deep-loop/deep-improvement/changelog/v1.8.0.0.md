deep-improvement is easier to navigate because its reference guidance is organized by topic.

> Spec folder: `.opencode/specs/skilled-agent-orchestration/116-deep-skill-evolution/008-deep-skill-doc-evolution` (Level 2)

## What's New at a Glance

- **References now sit in four topic folders.** The fifteen reference files moved from one flat directory into `scoring/`, `promotion-gates/`, `integration/` and `workflow/`. The smart router discovers references recursively, so routing is unchanged.
- **The documentation now points to the subfolder paths.** The README structure tree and every inbound link were updated, including `SKILL.md` REFERENCES paths, `RESOURCE_MAP` intent entries and the command reference.

## Upgrade

No migration required.
