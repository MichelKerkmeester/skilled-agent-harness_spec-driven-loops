deep-research's reference library is now easier for operators and the smart router to navigate.

> Spec folder: `.opencode/specs/skilled-agent-orchestration/116-deep-skill-evolution/008-deep-skill-doc-evolution` (Level 2)

## What's New at a Glance

- **The reference files are grouped by topic.** The thirteen files that sat in one flat directory now live under `convergence/`, `state/`, `protocol/` and `guides/`. The smart router discovers references recursively, so routing behavior is unchanged.
- **Documentation paths follow the new structure.** The README structure tree and every inbound link now resolve to the subfoldered paths. This includes the `SKILL.md` REFERENCES paths, README structure diagram, command and agent-mirror references and the cross-skill deep-review pointer.

## Upgrade

No migration required.
