# v1.13.0.0, Reference and Asset Alignment

The move from `1.12.0.0` to `1.13.0.0` makes `deep-research`'s existing shared deep-skill reference and asset alignment explicit.

> Spec folder: `.opencode/specs/skilled-agent-orchestration/116-deep-skill-evolution/000-release-cleanup/001-deep-loop-runtime/003-reference-asset-alignment/` (Level 3)

## What's New at a Glance

- **The target resource family was already in place.** The previous reference split had already given `deep-research` the quick reference, loop protocol, convergence split, state split, prompt-pack template, runtime capability matrix, config, strategy and dashboard assets.
- **The existing sk-doc-shaped reference family remains unchanged in behavior.** It continues to route through `references/quick-reference.md`, `references/loop-protocol.md`, `references/convergence*.md` and `references/state*.md`. The prior phase's split references already matched the target family, so no new `deep-research` reference files were required.
- **README and SKILL identify `deep-research` as the iterative investigation member.** They place it in the shared deep-skill resource model. The README statistics and related-resource language were updated, keeping operator-facing metadata in sync with `deep-ai-council` and `deep-review`.
- **Navigation distinguishes the three skills' signals.** It separates research novelty signals from review severity signals and council agreement signals.
- **The config template now uses `research_archive`.** The archive root was corrected to match the current README/archive convention.
- **The existing runtime assets remain canonical.** No new runtime assets were required.

## Upgrade

No migration required. No runtime YAML, reducer, script, command or agent behavior changed.
