# v2.1.0.0, Release Cleanup

This release updates the deep-ai-council documentation to the current sk-doc templates and HVR standards. It adds a root feature catalog and closes the audit findings with surgical alignment fixes without changing Smart Router (§3) or introducing backward-incompatible changes.

> Spec folder: `.opencode/specs/skilled-agent-orchestration/116-deep-skill-evolution/000-release-cleanup/004-deep-ai-council`

## Why This Release

The root feature catalog was missing. Audit findings called for surgical alignment fixes.

## What's New at a Glance

- **`feature-catalog/feature-catalog.md` is the new root inventory.** It contains 32 per-feature summaries across 9 categories.
- **The README now follows `skill_readme_template.md`.** Its rewrite uses a marketing-leaning, HVR-clean voice.
- **All 32 catalog-source backlinks now point to `feature_catalog.md`.** They appear with the per-feature summaries.
- **`SKILL.md` coverage now says 32 scenarios across 9 categories.** This corrects its playbook-coverage count.
- **`SKILL.md` now declares version `2.1.0.0`.** It replaces `2.0.0.0` in the frontmatter.
- **Two guidance corrections reached `SKILL.md` and the scoring rubric.** They address a stale phase reference and a top-score anchor.
- **The README's runtime mirrors and changelog references were corrected.** Agent mirrors identify actual runtime files, and the changelog list reflects the current head.
- **The `ai-council.*` rename was deferred.** The agent files keep their current name.

---

## Documentation

#### The Catalog Fills the Gap

The summaries cover 9 categories and include links and scenario IDs. The `Canonical catalog source` backlinks previously pointed to the playbook.

&nbsp;

#### The README Follows the Template

The rewrite was anchored at about 70% of the root README intensity. Its structure follows `system-spec-kit/README.md`.

&nbsp;

#### Coverage, Version and Guidance Are Updated

The earlier `SKILL.md` §7 count was 18 scenarios across 7 categories. The `SKILL.md` frontmatter changed from `2.0.0.0` to `2.1.0.0`. The stale "Phase 001 spec folder" reference was removed from §7 as a partial correction. The Integration-dimension top-score anchor in `references/scoring-rubric.md` was reworded to "no friction" for HVR compliance.

&nbsp;

#### README References Match

Agent mirrors now point to the actual `ai-council.*` files across three runtimes. The changelog list reflects the current head.

---

## Compatibility and Deferred Work

#### Existing Agent Names Are Preserved

The agent files keep the `ai-council.*` name instead of changing to `deep-ai-council.*`. The operator declined the rename to avoid breaking live `--agent ai-council` callers. Broader `SKILL.md` §7 extraction-era checklist reframing was left as a P2 follow-on.

## Upgrade Notes

No upgrade needed.
