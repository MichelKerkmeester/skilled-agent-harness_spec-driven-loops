Deep-review documentation now gives readers one consistent account of the legal-stop gate model, with every surface naming the same nine `Gate`-suffix gates. The authoritative producer is `step_emit_blocked_stop` in both `deep_review_{auto,confirm}.yaml` workflows, which emits the 9-gate `gateResults` bundle. This is a documentation-only release, and the YAML workflows were read-only sources of truth.

> Spec folder: `.opencode/specs/skilled-agent-orchestration/116-deep-skill-evolution/000-release-cleanup/003-deep-review/001-gate-model-reconciliation/` (Level 2)

## Why This Release

It closes the gate-model drift cluster LG-0013, LG-0016, LG-0031 and LG-0032 surfaced by the `003-deep-review` phase-5 deep-research loop.

## What's New at a Glance

- **`convergence.md` now documents the full emitted gate set.** The documentation had drifted to describe 6 or 7 gates depending on the surface, omitting `candidateCoverageGate` and `graphlessFallbackGate`, and one table also omitted `claimAdjudicationGate`. The gates, in emission order, are `convergenceGate`, `dimensionCoverageGate`, `p0ResolutionGate`, `evidenceDensityGate`, `hotspotSaturationGate`, `claimAdjudicationGate`, `fixCompletenessReplayGate`, `candidateCoverageGate` and `graphlessFallbackGate`. The §Section-1 `blocked_stop` example includes the two v2-rollout gates with their YAML field shapes (`searchDebt`/`missing` and `mode`/`missing`/`unavailabilityReason`). The prose key listing in `convergence.md` now names all nine gates and notes that the two v2-rollout gates pass trivially when the search path is inactive.
- **`convergence.md` names the YAML producer as authoritative.** The §6 LEGAL-STOP GATE BUNDLE note replaces its known-drift warning with a reconciled mapping. Its table, `buildReviewLegalStop` pseudocode and recovery-strategy table each add the two missing rows, while `scripts/reduce-state.cjs` is gate-name-agnostic and was confirmed unchanged.
- **`loop_protocol.md` now describes all nine gates.** §Step-2 corrects its former seven-gate description and identifies `candidateCoverageGate` and `graphlessFallbackGate` as v2-rollout gates.
- **`state_format.md` updates its event example.** The `blocked_stop` example grows from five gates to the full nine-gate `gateResults` bundle and points to `convergence.md` §Section-1 as the authoritative shape.
- **`feature_catalog` now enumerates its quality gates.** `04--severity-system/quality-gates.md` §2 CURRENT REALITY has a table of all nine gate names with one-line check descriptions and explains how the three contract-level binary gates relate to the nine named legal-stop bundle gates.
- **`DRV-018` connects its checks to the gate bundle.** The scenario validates the three contract-level binary gates for evidence, scope and coverage. Its WHY THIS MATTERS section maps those checks to the nine named event-shape gates, so the three-gate scenario is not in tension with the model, and the stale `convergence.md` Section 10.4 anchor now points to §6 LEGAL-STOP GATE BUNDLE.

## Upgrade

No migration required.
